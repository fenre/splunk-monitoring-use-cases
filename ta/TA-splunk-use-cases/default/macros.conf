# ---------------------------------------------------------------------
# GENERATED by scripts/build_ta.py — DO NOT EDIT BY HAND.
# Source of truth: use-cases/*.md + INDEX.md (Quick Start), catalog.json.
# Re-run `python3 scripts/build_ta.py` after updating content.
# ---------------------------------------------------------------------


[uc_index_os]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 1.

[uc_index_virt]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 2.

[uc_index_k8s]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 3.

[uc_index_cloud]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 4.

[uc_index_net]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 5.

[uc_index_storage]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 6.

[uc_index_db]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 7.

[uc_index_app]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 8.

[uc_index_idm]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 9.

[uc_index_sec]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 10.

[uc_index_email]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 11.

[uc_index_devops]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 12.

[uc_index_obs]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 13.

[uc_index_ot]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 14.

[uc_index_dc]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 15.

[uc_index_itsm]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 16.

[uc_index_zerotrust]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 17.

[uc_index_fabric]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 18.

[uc_index_hci]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 19.

[uc_index_capacity]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 20.

[uc_index_vertical]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 21.

[uc_index_compliance]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 22.

[uc_index_ba]
definition = index=*
iseval = 0
# Operator override: replace `index=*` with the specific
# index(es) for category 23.

[uc_index_any]
definition = index=*
iseval = 0
# Catch-all — falls back to main indexes

[uc_fields_cim]
definition = `comment("CIM-normalised fields enforced in searches")`
iseval = 0
# Placeholder for CIM normalisation decisions
