# -----------------------------------------------------------------
# GENERATED by scripts/generate_splunk_app.py — DO NOT EDIT BY HAND.
# Source of truth: use-cases/cat-*/uc-*.json + data/regulations.json.
# Re-run `python3 scripts/generate_splunk_app.py` after content edits.
# -----------------------------------------------------------------

[UC-22.10.1 — HIPAA Risk Analysis Evidence — Asset & ePHI System Inventory]
description = HIPAA Risk Analysis Evidence — Asset & ePHI System Inventory | UC 22.10.1 | regulation=hipaa-security | criticality=critical | clauses=§164.308(a)(1)
search = (index=assets sourcetype IN ("cmdb:server","es:assets")) OR (index=vulnerability sourcetype="tenable:vuln") \
| eval ephi_system=if(match(lower(_raw),"epic|cerner|clarity|caboodle|emr|ehr|phi|hipaa"),1,0) \
| stats latest(_time) as last_seen values(sourcetype) as sources dc(host) as hosts by system_name, ip \
| lookup ephi_system_register.csv ip OUTPUT contains_ephi, data_owner, baa_id \
| where contains_ephi="true" OR ephi_system=1 \
| sort - last_seen
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(1)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.1
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.10 — Business Associate Contracts — BAA Coverage for Connected Systems]
description = Business Associate Contracts — BAA Coverage for Connected Systems | UC 22.10.10 | regulation=hipaa-security | criticality=high | clauses=§164.308(a)(1)(ii)(B)
search = index=epic sourcetype="epic:interface" earliest=-7d \
| stats latest(_time) as last_seen values(interface_name) as ifaces by remote_host \
| lookup baa_register.csv hostname as remote_host OUTPUT vendor, baa_status, baa_expiry \
| where isnull(baa_status) OR baa_status!="Active" \
| eval days_since=round((now()-last_seen)/86400,1) \
| sort - last_seen
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(1)(ii)(B)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.10
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.11 — Business Associate Agreements — Expiry & Auto-Renewal Monitoring]
description = Business Associate Agreements — Expiry & Auto-Renewal Monitoring | UC 22.10.11 | regulation=hipaa-security | criticality=high | clauses=§164.308(b)(1)
search = | inputlookup baa_master.csv \
| eval days_to_end=round((strptime(baa_end,"%Y-%m-%d")-now())/86400,0) \
| eval alert_tier=case(days_to_end<0,"EXPIRED",days_to_end<=30,"CRITICAL",days_to_end<=90,"WARN",1=1,"OK") \
| where alert_tier!="OK" \
| table vendor_id, vendor_name, baa_end, auto_renew, alert_tier, data_categories \
| sort days_to_end
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(b)(1)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.11
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.12 — Sanction Policy — Privileged Abuse on EHR Audit Logs]
description = Sanction Policy — Privileged Abuse on EHR Audit Logs | UC 22.10.12 | regulation=hipaa-security | criticality=critical | clauses=§164.308(a)(1)(ii)(C)
search = index=epic sourcetype="epic:audit" AccessType="PatientChart" earliest=-30d \
| lookup employee_dependents.csv employee_id as USER_ID OUTPUT dependent_patient_hash \
| eval pat_hash=sha256(PAT_ID) \
| where pat_hash=dependent_patient_hash \
| stats count by USER_ID, PAT_ID \
| where count>=1
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(1)(ii)(C)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.12
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.13 — Unique User Identification — Shared or Generic EHR Accounts]
description = Unique User Identification — Shared or Generic EHR Accounts | UC 22.10.13 | regulation=hipaa-security | criticality=critical | clauses=§164.312(a)(2)(i)
search = (index=epic sourcetype="epic:audit") OR (index=citrix sourcetype="citrix:session") \
| eval account=upper(coalesce(USER_ID, UserName)) \
| lookup forbidden_shared_accounts.csv account OUTPUT policy \
| where isnotnull(policy) \
| stats dc(PAT_ID) as patients dc(client_ip) as src_ips count as events by account, _time span=1h \
| where events>10 \
| sort - events
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(a)(2)(i)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.13
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.14 — Emergency Access Procedure — Downtime / Break-Glass Account Usage]
description = Emergency Access Procedure — Downtime / Break-Glass Account Usage | UC 22.10.14 | regulation=hipaa-security | criticality=critical | clauses=§164.312(a)(2)(ii)
search = (index=windows EventCode="4622" OR EventCode="4624") Account_Name="svc_epic_emergency*" earliest=-30d \
| append [ \
    search index=epic sourcetype="epic:audit" AccessContext="*" earliest=-30d \
    | rex field=AccessContext "(?<ctx>Downtime|Emergency)" \
    | where ctx IN ("Downtime","Emergency") ] \
| eval principal=coalesce(Account_Name, USER_ID) \
| stats earliest(_time) as first latest(_time) as last dc(host) as systems by principal, ctx \
| eval duration_h=round((last-first)/3600,2) \
| sort - last
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(a)(2)(ii)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.14
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.15 — Automatic Logoff — Stale Clinical Workstation Sessions]
description = Automatic Logoff — Stale Clinical Workstation Sessions | UC 22.10.15 | regulation=hipaa-security | criticality=high | clauses=§164.312(a)(2)(iii)
search = index=citrix sourcetype="citrix:session" earliest=-7d \
| eval start=strptime(SessionStart,"%Y-%m-%d %H:%M:%S") \
| eval end=if(isnotnull(SessionEnd), strptime(SessionEnd,"%Y-%m-%d %H:%M:%S"), now()) \
| eval session_hours=round((end-start)/3600,2) \
| where session_hours>12 AND match(Application,"Hyperspace|Epic") \
| stats max(session_hours) as max_hrs by UserName, client_ip, PublishedApplication \
| sort - max_hrs
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(a)(2)(iii)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.15
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.16 — Encryption of ePHI at Rest — TDE / BitLocker Status for Clinical Datastores]
description = Encryption of ePHI at Rest — TDE / BitLocker Status for Clinical Datastores | UC 22.10.16 | regulation=hipaa-security | criticality=critical | clauses=§164.312(a)(2)(iv)
search = index=database sourcetype="sqlserver:tde_status" earliest=-1d \
| eval encrypted=if(match(lower(encryption_state),"encrypted|on"),1,0) \
| lookup ephi_sql_instances.csv instance_name OUTPUT contains_phi, owner_team \
| where contains_phi="true" AND encrypted=0 \
| stats latest(_time) as last_check by instance_name, database_name, owner_team
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(a)(2)(iv)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.16
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.17 — Audit Controls — High-Volume ePHI Read Baseline & Anomaly]
description = Audit Controls — High-Volume ePHI Read Baseline & Anomaly | UC 22.10.17 | regulation=hipaa-security | criticality=critical | clauses=§164.312(b)
search = index=epic sourcetype="epic:audit" AccessType="PatientChart" earliest=-90d \
| bin _time span=1d \
| stats dc(PAT_ID) as daily_patients by USER_ID, _time \
| eventstats median(daily_patients) as med by USER_ID \
| eval z=(daily_patients-med)/sqrt(med+0.001) \
| where z>4 AND daily_patients>30 \
| sort - z
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(b)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.17
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.18 — Integrity — Unexpected UPDATE/DELETE on PHI Tables (Clarity/Caboodle)]
description = Integrity — Unexpected UPDATE/DELETE on PHI Tables (Clarity/Caboodle) | UC 22.10.18 | regulation=hipaa-security | criticality=critical | clauses=§164.312(c)(1)
search = index=mssql sourcetype="mssql:audit" earliest=-24h action_class="DML" \
| where match(lower(object_name),"pat|patient|phi|epic") AND match(lower(statement),"update|delete") \
| eval is_bulk=if(match(statement,"(?i)where\s+1=1|truncate|delete\s+from"),1,0) \
| where is_bulk=1 OR (match(lower(application_name),"microsoft sql server management studio") AND _time < relative_time(now(),"@d+18h")) \
| stats count by session_server_principal_name, object_name, application_name \
| sort - count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(c)(1)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.18
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.19 — Entity Authentication — Smart Card / Certificate Logon Failures]
description = Entity Authentication — Smart Card / Certificate Logon Failures | UC 22.10.19 | regulation=hipaa-security | criticality=high | clauses=§164.312(d)
search = index=windows sourcetype="WinEventLog:Security" EventCode="4625" earliest=-24h \
| search Logon_Process="Kerberos" OR Authentication_Package="Negotiate" \
| eval fail_reason=Status, Sub_Status \
| stats count values(WorkstationName) as workstations by TargetUserName, fail_reason, src_ip \
| where count>=5 \
| sort - count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(d)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.19
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.2 — HIPAA Risk Management — Control Deficiency Tracking]
description = HIPAA Risk Management — Control Deficiency Tracking | UC 22.10.2 | regulation=hipaa-security | criticality=critical | clauses=§164.308(a)(1)
search = index=grc sourcetype="grc:hipaa_control" earliest=-90d framework="HIPAA_Security_Rule" \
| eval overdue=if(status!="Closed" AND now()>strptime(due_date,"%Y-%m-%d"),1,0) \
| stats count as open_controls sum(overdue) as overdue_controls by owner, control_family \
| sort - overdue_controls
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(1)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.2
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.20 — Transmission Security — TLS 1.0/1.1 Deprecation for EHR Integrations]
description = Transmission Security — TLS 1.0/1.1 Deprecation for EHR Integrations | UC 22.10.20 | regulation=hipaa-security | criticality=high | clauses=§164.312(e)(1)
search = index=network (sourcetype="pan:decrypted" OR sourcetype="stream:tls") earliest=-7d \
| eval tls=coalesce(ssl_version, tls_version) \
| where tls IN ("TLSv1","TLSv1.0","TLSv1.1","SSLv3") \
| lookup ephi_integration_subnets.csv dest_ip OUTPUT integration_name, contains_ephi \
| where contains_ephi="true" \
| stats count by dest_ip, integration_name, tls, app \
| sort - count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(e)(1)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.20
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.21 — Access Control — Role-Based Violations (Coder Accessing Medication Admin)]
description = Access Control — Role-Based Violations (Coder Accessing Medication Admin) | UC 22.10.21 | regulation=hipaa-security | criticality=critical | clauses=§164.308(a)(4), §164.312(a)(1)
search = index=epic sourcetype="epic:audit" earliest=-24h \
| lookup epic_user_roles.csv USER_ID OUTPUT hr_role, allowed_activities mvsep="|" \
| eval allowed=if(isnull(allowed_activities), "*", allowed_activities) \
| where isnotnull(hr_role) AND NOT match(Activity, allowed) \
| stats count values(Activity) as bad_activities by USER_ID, hr_role \
| sort - count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(4),§164.312(a)(1)
action.uc_compliance.param.versions = HIPAA Security Rule@2013-final,HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.21
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.22 — Remote ePHI Access — MFA Gap for VPN + O365 Clinical Mail]
description = Remote ePHI Access — MFA Gap for VPN + O365 Clinical Mail | UC 22.10.22 | regulation=hipaa-security | criticality=critical | clauses=§164.308(a)(1), §164.312(e)(1)
search = index=o365 sourcetype="ms:o365:management" Workload="AzureActiveDirectory" Operation="UserLoggedIn" earliest=-24h \
| eval mfa=if(match(ExtendedProperties,"MFA\s*completed|multi-factor"),1,0) \
| lookup hipaa_ephi_users.csv user OUTPUT in_ephi_group \
| where in_ephi_group="true" AND mfa=0 \
| stats count by user, ClientIP, ApplicationId \
| sort - count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(1),§164.312(e)(1)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.22
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.23 — FHIR / SMART on FHIR App Access to ePHI Scopes]
description = FHIR / SMART on FHIR App Access to ePHI Scopes | UC 22.10.23 | regulation=hipaa-security | criticality=high | clauses=§164.312(d)
search = index=interop sourcetype="epic:fhir_audit" earliest=-24h \
| eval scopes=split(scope," ") \
| mvexpand scopes \
| lookup fhir_app_register.csv client_id OUTPUT approved_scopes_mv \
| eval approved=if(isnotnull(mvfind(approved_scopes_mv,scopes)),1,0) \
| where approved=0 AND status_code IN ("200","201") \
| stats count by client_id, scopes, user_id, patient_id \
| sort - count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(d)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.23
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.24 — Medical Device Integration — Unapproved HL7 Feeds to EMPI]
description = Medical Device Integration — Unapproved HL7 Feeds to EMPI | UC 22.10.24 | regulation=hipaa-security | criticality=high | clauses=§164.312(a)(1)
search = index=hl7 sourcetype="hl7:receive" earliest=-7d \
| rex field=_raw "^MSH\\|[^|]*\\|[^|]*\\|(?<sending_app>[^|]*)\\|(?<sending_facility>[^|]*)" \
| stats earliest(_time) as first_seen latest(_time) as last_seen dc(PID) as patients by sending_ip, sending_facility, sending_app \
| lookup approved_hl7_senders.csv sending_ip sending_facility OUTPUT approved \
| where isnull(approved) OR approved="false" \
| sort - patients
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(a)(1)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.24
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.25 — Endpoint Controls — ePHI Clipboard/Print from VDI]
description = Endpoint Controls — ePHI Clipboard/Print from VDI | UC 22.10.25 | regulation=hipaa-security | criticality=high | clauses=§164.312(a)(1)
search = index=citrix sourcetype="citrix:session" earliest=-7d ClientDrive="On" OR Clipboard="ClientToServer" \
| lookup hipaa_epic_users.csv UserName OUTPUT is_clinical_user \
| where is_clinical_user="true" \
| stats count by UserName, client_ip, PublishedApplication, ClientDrive, Clipboard \
| sort - count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(a)(1)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.25
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.26 — Transmission Security — Unencrypted SMTP with PHI Patterns]
description = Transmission Security — Unencrypted SMTP with PHI Patterns | UC 22.10.26 | regulation=hipaa-security | criticality=critical | clauses=§164.312(e)(1)
search = index=mail sourcetype="proofpoint:mta" earliest=-24h \
| eval tls_used=if(match(lower(smtp_tls_status),"none|no|plaintext"),0,1) \
| regex subject="(?i)\b(MRN|medical record|DOB|date of birth)\b" \
| where tls_used=0 \
| stats count values(rcpt) as recipients by sender, subject, client_ip \
| sort - count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(e)(1)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.26
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.27 — Integrity — Caboodle / Clarity ETL Job Failures & Partial Loads]
description = Integrity — Caboodle / Clarity ETL Job Failures & Partial Loads | UC 22.10.27 | regulation=hipaa-security | criticality=high | clauses=§164.312(c)(1)
search = index=etl sourcetype="sqlserver:agent_job" earliest=-48h (job_name="*CABOODLE*" OR job_name="*CLARITY*") \
| eval ok=if(match(lower(run_status),"succeeded|success"),1,0) \
| stats latest(run_status) as last_status latest(_time) as last_run sum(ok) as ok_runs count as runs by job_name, server_name \
| eval health=if(ok_runs=runs,"OK","DEGRADED") \
| where health!="OK" \
| sort last_run
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(c)(1)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.27
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.28 — Workstation Security — Unattended Unlocked Sessions in Clinical Pods]
description = Workstation Security — Unattended Unlocked Sessions in Clinical Pods | UC 22.10.28 | regulation=hipaa-security | criticality=high | clauses=§164.310(b)
search = index=windows sourcetype="WinEventLog:Security" EventCode IN ("4800","4801") earliest=-7d \
| eval action=if(EventCode="4800","lock","unlock") \
| transaction WorkstationName startswith=eval(action="unlock") endswith=eval(action="lock") maxspan=8h \
| eval idle_during=if(duration>3600 AND eventcount<3,1,0) \
| where idle_during=1 \
| table WorkstationName, duration, user, _time
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.310(b)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.28
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.29 — Device & Media Controls — USB Mass Storage on ePHI Workstations]
description = Device & Media Controls — USB Mass Storage on ePHI Workstations | UC 22.10.29 | regulation=hipaa-security | criticality=critical | clauses=§164.310(d)(1)
search = index=windows sourcetype="WinEventLog:Microsoft-Windows-Kernel-PnP/Configuration" EventCode="400" earliest=-7d \
| where match(ClassName,"USBSTOR|Disk&Ven_") \
| lookup clinical_workstations.csv ComputerName OUTPUT contains_ephi_workstation \
| where contains_ephi_workstation="true" \
| stats count by ComputerName, ClassName, DeviceID \
| sort - count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.310(d)(1)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.29
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.3 — Information System Activity Review — Cross-Source ePHI Access Summary]
description = Information System Activity Review — Cross-Source ePHI Access Summary | UC 22.10.3 | regulation=hipaa-security | criticality=critical | clauses=§164.308(a)(1)(ii)(D)
search = (index=epic sourcetype="epic:audit" AccessType="PatientChart") OR (index=cerner sourcetype="cerner:audit" Action="PatientChartView") OR (index=citrix sourcetype="citrix:session" Application="*Hyperspace*") OR (index=vpn sourcetype="paloalto:globalprotect" status="success") \
| eval user=coalesce(USER_ID, PractitionerId, user, src_user) \
| eval PAT_ID=coalesce(PAT_ID, PatientId) \
| bin _time span=1d \
| stats dc(PAT_ID) as patients_touched dc(host) as systems by _time, user \
| sort - patients_touched
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(1)(ii)(D)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.3
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.30 — Media Controls — Large PHI Print Jobs to Non-Secure Printers]
description = Media Controls — Large PHI Print Jobs to Non-Secure Printers | UC 22.10.30 | regulation=hipaa-security | criticality=high | clauses=§164.310(d)(2)
search = index=windows sourcetype="WinEventLog:Microsoft-Windows-PrintService/Operational" EventCode="307" earliest=-7d \
| eval pages=tonumber(Pages) \
| lookup secure_print_queues.csv PrinterName OUTPUT secure_queue \
| where (isnull(secure_queue) OR secure_queue="false") AND pages>=50 \
| stats sum(pages) as total_pages count as jobs by UserName, PrinterName \
| sort - total_pages
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.310(d)(2)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.30
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.31 — Facility vs Logical Access — Badge-In Without VPN/SSO for Remote Roles]
description = Facility vs Logical Access — Badge-In Without VPN/SSO for Remote Roles | UC 22.10.31 | regulation=hipaa-security | criticality=high | clauses=§164.310(a)(1)
search = index=physical sourcetype="genetec:access" result="Granted" earliest=-1d \
| lookup badge_to_user.csv badge_id OUTPUT samaccountname, work_mode \
| where work_mode="OnsiteOnly" \
| eval day=strftime(_time,"%Y-%m-%d") \
| join type=left samaccountname, day [ \
    search index=vpn sourcetype="paloalto:globalprotect" earliest=-1d \
    | eval day=strftime(_time,"%Y-%m-%d") \
    | stats earliest(_time) as vpn_first by src_user, day \
    | rename src_user as samaccountname ] \
| where isnull(vpn_first) \
| stats count by samaccountname, reader, day
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.310(a)(1)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.31
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.32 — Workstation Use — After-Hours Login from Non-Clinical IP Space]
description = Workstation Use — After-Hours Login from Non-Clinical IP Space | UC 22.10.32 | regulation=hipaa-security | criticality=high | clauses=§164.310(b), §164.310(c)
search = index=citrix sourcetype="citrix:session" earliest=-7d \
| eval hour=strftime(_time,"%H") \
| where (hour<6 OR hour>22) \
| iplocation client_ip \
| lookup clinical_role_users.csv UserName OUTPUT role \
| where role IN ("RN_INPATIENT","MD_INPATIENT") AND NOT match(Org,"*Health System*") \
| stats count by UserName, client_ip, Country, City, Org \
| sort - count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.310(b),§164.310(c)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.32
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.33 — Minimum Necessary — Access Outside Active Care Team]
description = Minimum Necessary — Access Outside Active Care Team | UC 22.10.33 | regulation=hipaa-security | criticality=critical | clauses=§164.502(b), §164.514(d)
search = index=epic sourcetype="epic:audit" AccessType="PatientChart" earliest=-24h \
| lookup epic_active_careteam.csv PAT_ID USER_ID as member_id OUTPUT relation \
| where isnull(relation) \
| stats count dc(PAT_ID) as patients by USER_ID \
| where count>=5 \
| sort - count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.502(b),§164.514(d)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.33
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.34 — Break-Glass — Emergency Access Reason Codes & Post-Review]
description = Break-Glass — Emergency Access Reason Codes & Post-Review | UC 22.10.34 | regulation=hipaa-security | criticality=critical | clauses=§164.502(a)(2)(ii), §policy
search = index=epic sourcetype="epic:breakglass" earliest=-30d \
| eval has_reason=if(isnotnull(reason_code) AND reason_code!="","OK","MISSING") \
| join type=left session_id [ \
    search index=itsm sourcetype="snow:incident" earliest=-30d short_description="*break*glass*" \
    | rename number as attestation_ticket, sys_id as session_id ] \
| eval reviewed=if(isnotnull(attestation_ticket),"Yes","No") \
| where has_reason!="OK" OR reviewed="No" \
| table _time, USER_ID, PAT_ID, reason_code, has_reason, reviewed
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.502(a)(2)(ii),§policy
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.34
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.35 — Non-Treating Provider — Specialty Mismatch Chart Access]
description = Non-Treating Provider — Specialty Mismatch Chart Access | UC 22.10.35 | regulation=hipaa-security | criticality=high | clauses=§164.502(a)(1)
search = index=epic sourcetype="epic:audit" AccessType="PatientChart" earliest=-30d \
| lookup provider_specialty.csv USER_ID OUTPUT specialty_group \
| lookup patient_unit_map.csv PAT_ID OUTPUT unit_service_line \
| eval mismatch=if(isnotnull(specialty_group) AND NOT match(unit_service_line, specialty_group),1,0) \
| where mismatch=1 AND specialty_group IN ("BILLING","CODING","IT","HR") \
| stats dc(PAT_ID) as patients by USER_ID, specialty_group \
| where patients>=10 \
| sort - patients
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.502(a)(1)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.35
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.36 — Bulk ePHI Export — Clarity SQL / Caboodle Extract Volume Spike]
description = Bulk ePHI Export — Clarity SQL / Caboodle Extract Volume Spike | UC 22.10.36 | regulation=hipaa-security | criticality=critical | clauses=§164.312(b), §164.502(b)
search = index=mssql sourcetype="mssql:audit" earliest=-24h action_class="SELECT" \
| where match(lower(statement),"from\s+clarity\.(pat|patient|episodes)") OR match(lower(statement),"from\s+caboodle") \
| eval row_estimate=coalesce(rows,0) \
| bin _time span=1h \
| stats sum(row_estimate) as est_rows by session_server_principal_name, _time, client_ip \
| eventstats median(est_rows) as med by session_server_principal_name \
| where est_rows > med*20 AND est_rows>100000 \
| sort - est_rows
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(b),§164.502(b)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.36
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.37 — After-Hours ePHI Access — Billing Users on Inpatient Charts]
description = After-Hours ePHI Access — Billing Users on Inpatient Charts | UC 22.10.37 | regulation=hipaa-security | criticality=high | clauses=§164.502(b)
search = index=epic sourcetype="epic:audit" AccessType="PatientChart" earliest=-30d \
| eval hour=tonumber(strftime(_time,"%H")) \
| where hour>=22 OR hour<=5 \
| lookup hipaa_department_roles.csv USER_ID OUTPUT dept, after_hours_allowed \
| where dept="REVENUE_CYCLE" AND after_hours_allowed!="true" \
| stats dc(PAT_ID) as patients by USER_ID, dept \
| sort - patients
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.502(b)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.37
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.38 — Deceased Patient Records — Access After Death Date]
description = Deceased Patient Records — Access After Death Date | UC 22.10.38 | regulation=hipaa-security | criticality=high | clauses=§164.502(f), §policy
search = index=epic sourcetype="epic:audit" AccessType="PatientChart" earliest=-90d \
| lookup patient_death_index.csv PAT_ID OUTPUT death_dttm, restricted_flag \
| where isnotnull(death_dttm) AND _time > strptime(death_dttm,"%Y-%m-%d %H:%M:%S") \
| lookup post_mortem_allowed_roles.csv USER_ID OUTPUT allowed_role \
| where isnull(allowed_role) \
| stats count by USER_ID, PAT_ID \
| sort - count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.502(f),§policy
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.38
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.39 — VIP / High-Profile Patient — Elevated Access Monitoring]
description = VIP / High-Profile Patient — Elevated Access Monitoring | UC 22.10.39 | regulation=hipaa-security | criticality=critical | clauses=§164.502(a), §policy
search = index=epic sourcetype="epic:audit" AccessType="PatientChart" earliest=-30d \
| lookup vip_patients.csv PAT_ID OUTPUT vip_level, start_date, end_date \
| where isnotnull(vip_level) AND _time>=strptime(start_date,"%Y-%m-%d") AND _time<=strptime(end_date,"%Y-%m-%d") \
| lookup vip_care_team.csv PAT_ID USER_ID OUTPUT is_authorized \
| where isnull(is_authorized) \
| table _time, USER_ID, PAT_ID, vip_level, Activity
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.502(a),§policy
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.39
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.4 — Workforce Clearance — HR Hire vs AD Account Creation]
description = Workforce Clearance — HR Hire vs AD Account Creation | UC 22.10.4 | regulation=hipaa-security | criticality=high | clauses=§164.308(a)(3)
search = (index=windows EventCode IN ("4720","4722") TargetUserName="*") OR (index=hr sourcetype="workday:worker") \
| eval emp_id=coalesce(employee_id, TargetUserName) \
| eval event_type=if(EventCode="4720","AccountCreated",if(EventCode="4722","AccountEnabled","HRRecord")) \
| stats earliest(_time) as first_ad latest(_time) as last_hr by emp_id, event_type \
| join type=outer emp_id [ \
    search index=hr sourcetype="workday:worker" earliest=-180d \
    | eval clearance_epoch=strptime(effective_date,"%Y-%m-%d") \
    | table employee_id, clearance_status, clearance_epoch \
    | rename employee_id as emp_id ] \
| eval violation=if(first_ad < clearance_epoch AND clearance_status="Cleared","PreClearance_AD",null()) \
| where isnotnull(violation)
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(3)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.4
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.40 — Research Access — Chart Views Without Active IRB Consent Flag]
description = Research Access — Chart Views Without Active IRB Consent Flag | UC 22.10.40 | regulation=hipaa-security | criticality=critical | clauses=§164.502(a)(1), §164.512(i)
search = index=epic sourcetype="epic:audit" Activity="*Research*" OR Context="*Study*" earliest=-30d \
| rex field=Context "STUDY:(?<STUDY_ID>[^|]+)" \
| lookup irb_active_subjects.csv STUDY_ID PAT_ID OUTPUT consent_status, window_end \
| eval in_window=if(_time<strptime(window_end,"%Y-%m-%d"),1,0) \
| where consent_status!="Active" OR in_window=0 OR isnull(consent_status) \
| stats values(STUDY_ID) as studies dc(PAT_ID) as patients by USER_ID \
| sort - patients
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.502(a)(1),§164.512(i)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.40
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.41 — Accounting of Disclosures — Registry vs EHR-Logged Disclosures]
description = Accounting of Disclosures — Registry vs EHR-Logged Disclosures | UC 22.10.41 | regulation=hipaa-security | criticality=high | clauses=§164.528
search = index=privacy sourcetype="hipaa:disclosure_log" earliest=-30d \
| stats count by disclosure_id, PAT_ID, recipient_class \
| join type=left PAT_ID [ \
    search index=epic sourcetype="epic:disclosure_export" earliest=-30d \
    | stats latest(_time) as epic_export by PAT_ID ] \
| where isnull(epic_export) AND recipient_class IN ("LawEnforcement","Insurance","Employer") \
| table disclosure_id, PAT_ID, recipient_class, count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.528
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.41
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.42 — Patient Portal — Suspicious MyChart Password Reset & MFA Changes]
description = Patient Portal — Suspicious MyChart Password Reset & MFA Changes | UC 22.10.42 | regulation=hipaa-security | criticality=critical | clauses=§164.312(d), §164.530(c)
search = index=mychart sourcetype="epic:mychart_audit" earliest=-7d (action="PasswordReset" OR action="MFAEnrollmentChanged" OR action="EmailChanged") \
| stats earliest(_time) as first latest(_time) as last dc(src_ip) as src_dc values(action) as actions by user_mrn \
| where src_dc>=3 OR (last-first)<300 AND mvcount(actions)>=2 \
| sort - src_dc
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(d),§164.530(c)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.42
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.43 — Breach Discovery — Time-to-Detect from First PHI Indicator]
description = Breach Discovery — Time-to-Detect from First PHI Indicator | UC 22.10.43 | regulation=hipaa-security | criticality=critical | clauses=§164.404
search = `notable` HIPAA_Breach_Candidate=1 earliest=-90d \
| eval detect_time=_time \
| join IncidentId type=left [ \
    search index=dlp sourcetype="o365:dlp" earliest=-90d \
    | eval first_indicator=strptime(CreatedTime,"%Y-%m-%dT%H:%M:%SZ") \
    | stats min(first_indicator) as first_dlp by IncidentId ] \
| eval hours_to_detect=round((detect_time-first_dlp)/3600,2) \
| table IncidentId, first_dlp, detect_time, hours_to_detect, rule_name, status \
| sort - hours_to_detect
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.404
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.43
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.44 — Breach Risk Assessment — Four-Factor Documentation Tracking]
description = Breach Risk Assessment — Four-Factor Documentation Tracking | UC 22.10.44 | regulation=hipaa-security | criticality=critical | clauses=§164.402, §164.404
search = index=privacy sourcetype="hipaa:breach_ra" earliest=-365d \
| eval complete=if(isnotnull(factor1) AND isnotnull(factor2) AND isnotnull(factor3) AND isnotnull(factor4),"Yes","No") \
| where complete="No" OR isnull(preliminary_result) \
| eval days_open=round((now()-_time)/86400,1) \
| table incident_id, days_open, reviewer, factor1, factor2, factor3, factor4 \
| sort - days_open
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.402,§164.404
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.44
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.45 — Individual Notification — Letter Generation & Mailing Evidence]
description = Individual Notification — Letter Generation & Mailing Evidence | UC 22.10.45 | regulation=hipaa-security | criticality=critical | clauses=§164.404(b)
search = index=privacy sourcetype="hipaa:breach_notice" earliest=-120d \
| eval mailed_epoch=strptime(mailed_dttm,"%Y-%m-%d %H:%M:%S") \
| eval breach_discovered=strptime(breach_discovered_dttm,"%Y-%m-%d %H:%M:%S") \
| eval days_to_mail=round((mailed_epoch-breach_discovered)/86400,1) \
| where letter_status!="Mailed" OR days_to_mail>60 OR isnull(mail_tracking_id) \
| stats count by incident_id, letter_status \
| sort - count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.404(b)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.45
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.46 — HHS Secretary Notification — 500+ Individuals Threshold Watch]
description = HHS Secretary Notification — 500+ Individuals Threshold Watch | UC 22.10.46 | regulation=hipaa-security | criticality=critical | clauses=§164.408
search = index=privacy sourcetype="hipaa:breach_subject" earliest=-365d \
| where included_in_count="true" \
| stats dc(subject_token) as affected by incident_id \
| eval hhs_required=if(affected>=500,"Yes","No") \
| join incident_id [ \
    search index=privacy sourcetype="hipaa:hhs_filing" earliest=-365d \
    | stats latest(filed_dttm) as hhs_filed by incident_id ] \
| where hhs_required="Yes" AND isnull(hhs_filed) \
| table incident_id, affected, hhs_required
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.408
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.46
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.47 — Media Notification — Large-State Resident Threshold Tracking]
description = Media Notification — Large-State Resident Threshold Tracking | UC 22.10.47 | regulation=hipaa-security | criticality=critical | clauses=§164.406(c)
search = index=privacy sourcetype="hipaa:breach_subject" earliest=-365d included_in_count="true" \
| stats dc(subject_token) as affected by incident_id, state \
| lookup state_media_threshold.csv state OUTPUT threshold \
| eval media_needed=if(affected>=threshold,"Yes","No") \
| where media_needed="Yes" \
| join incident_id [ \
    search index=privacy sourcetype="hipaa:media_notice" earliest=-365d \
    | stats latest(published_dttm) as published by incident_id, state ] \
| where isnull(published) \
| table incident_id, state, affected, threshold
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.406(c)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.47
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.48 — Breach Log / Incident Register — Immutable Chronological Record]
description = Breach Log / Incident Register — Immutable Chronological Record | UC 22.10.48 | regulation=hipaa-security | criticality=high | clauses=§164.402, §policy
search = index=privacy sourcetype="hipaa:breach_register" earliest=-365d \
| stats latest(status) as last_status latest(_time) as last_update earliest(_time) as opened latest(incident_title) as incident_title values(owner) as owner by register_id \
| eval days_open=round((now()-opened)/86400,0) \
| where last_status IN ("Draft","Investigating") AND days_open>30 \
| table register_id, incident_title, last_status, owner, days_open, last_update \
| sort - days_open
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.402,§policy
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.48
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.49 — Breach Remediation — Control Implementation Evidence Post-Incident]
description = Breach Remediation — Control Implementation Evidence Post-Incident | UC 22.10.49 | regulation=hipaa-security | criticality=high | clauses=§164.308(a)(1)(ii)(A)
search = index=privacy sourcetype="hipaa:remediation_task" earliest=-180d status!="Closed" \
| eval due=strptime(target_date,"%Y-%m-%d") \
| join control_id [ \
    search index=metrics sourcetype="hipaa:control_metric" earliest=-1d \
    | stats latest(metric_value) as current_value by control_id ] \
| eval behind=if(now()>due AND current_value<100,1,0) \
| where behind=1 \
| table incident_id, control_id, target_date, current_value, owner
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(1)(ii)(A)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.49
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.5 — Termination — Access Revocation Within Policy Window]
description = Termination — Access Revocation Within Policy Window | UC 22.10.5 | regulation=hipaa-security | criticality=critical | clauses=§164.308(a)(3)(ii)(C)
search = index=hr sourcetype="workday:termination" earliest=-30d \
| eval term_epoch=strptime(term_date,"%Y-%m-%d %H:%M:%S") \
| table samaccountname, term_epoch \
| rename samaccountname as user \
| join type=inner user [ \
    search index=windows EventCode="4624" earliest=-30d \
    | eval user=mvindex(Security_ID,0) \
    | rex field=user "\\\\(?<sam>[^\\\\]+)$" \
    | eval user=coalesce(sam,user) \
    | table _time, user, WorkstationName, Logon_Type ] \
| where _time > term_epoch \
| eval hours_after_term=round((_time-term_epoch)/3600,2)
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(3)(ii)(C)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.5
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.50 — Annual Breach Reporting — Trend of Affected Individuals & Root Cause]
description = Annual Breach Reporting — Trend of Affected Individuals & Root Cause | UC 22.10.50 | regulation=hipaa-security | criticality=high | clauses=§164.408
search = index=privacy sourcetype="hipaa:breach_closeout" earliest=-800d confirmed_breach="true" \
| eval year=strftime(strptime(close_date,"%Y-%m-%d"),"%Y") \
| stats sum(affected_count) as individuals count as incidents by year, root_cause, vector \
| sort year, - individuals
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.408
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.50
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.51 — Business Associate Access — VPN/SSO Sessions Originating from BA Address Space]
description = Business Associate Access — VPN/SSO Sessions Originating from BA Address Space | UC 22.10.51 | regulation=hipaa-security | criticality=high | clauses=§164.308(b), §164.502(e)
search = index=citrix sourcetype="citrix:session" earliest=-7d PublishedApplication="*Epic*BAA*" \
| lookup ba_ip_ranges client_ip OUTPUT vendor_name, baa_id \
| where isnull(vendor_name) \
| iplocation client_ip \
| stats count dc(UserName) as distinct_users by client_ip, Country, Org, PublishedApplication \
| sort - count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(b),§164.502(e)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.51
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.52 — BAA Compliance Evidence — Control Attestations vs Technical Telemetry]
description = BAA Compliance Evidence — Control Attestations vs Technical Telemetry | UC 22.10.52 | regulation=hipaa-security | criticality=high | clauses=§164.308(b)(3), §164.502(e)
search = index=privacy sourcetype="hipaa:baa_attestation" earliest=-365d \
| where answer IN ("Yes","FullyImplemented") \
| join vendor_id, control_id [ \
    search index=metrics sourcetype="hipaa:ba_control_metric" earliest=-30d \
    | stats latest(measured_value) as measured by vendor_id, control_id ] \
| eval gap=if(isnull(measured) OR measured<95,"Evidence_Gap","OK") \
| where gap!="OK" \
| table vendor_id, control_id, answer, measured, gap
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(b)(3),§164.502(e)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.52
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.53 — Subcontractor Access — Downstream API Keys Touching ePHI Interfaces]
description = Subcontractor Access — Downstream API Keys Touching ePHI Interfaces | UC 22.10.53 | regulation=hipaa-security | criticality=critical | clauses=§164.502(e)
search = index=interop sourcetype="apigee:proxy" uri_path="/fhir/*" earliest=-30d \
| stats earliest(_time) as first_seen latest(_time) as last_seen dc(status) as status_codes by client_id, api_key \
| eval key_or_client=coalesce(client_id, api_key) \
| lookup ba_subcontractor_clients.csv key_or_client OUTPUT approved_vendor, subcontractor_name \
| where isnull(approved_vendor) \
| sort first_seen
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.502(e)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.53
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.54 — Third-Party Data Sharing — O365 Sharing Links to External Domains on PHI Libraries]
description = Third-Party Data Sharing — O365 Sharing Links to External Domains on PHI Libraries | UC 22.10.54 | regulation=hipaa-security | criticality=critical | clauses=§164.502(b), §164.514(e)
search = index=o365 sourcetype="ms:o365:management" Workload=SharePoint earliest=-7d Operation IN ("SharingSet","AnonymousLinkCreated","CompanyLinkCreated") \
| eval site_label=coalesce(SensitivityLabel, SiteSensitivity) \
| where match(lower(site_label),"clinical|phi|hipaa|restricted") \
| eval sharee_lower=lower(Sharee) \
| eval external=if(match(sharee_lower,"@yourhealthorg\.com$"),0,1) \
| where external=1 \
| stats count values(Sharee) as sharees by UserId, ObjectId, Operation, site_label \
| sort - count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.502(b),§164.514(e)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.54
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.55 — Cloud Service Provider — ePHI Hosting Admin Actions in AWS or Azure Audit]
description = Cloud Service Provider — ePHI Hosting Admin Actions in AWS or Azure Audit | UC 22.10.55 | regulation=hipaa-security | criticality=critical | clauses=§164.308(a)(1), §164.502(e)
search = index=aws sourcetype="aws:cloudtrail" earliest=-7d \
| rename userIdentity.arn as user_arn \
| eval account=coalesce(recipientAccountId, aws_account_id) \
| lookup aws_account_ephi_tags.csv account OUTPUT contains_ephi_backup \
| where contains_ephi_backup="true" \
| search eventName IN ("AssumeRole","GetObject","PutObject","DeleteObject","CreateSnapshot","RunInstances","ModifyDBInstance") \
| eval principal=mvindex(split(user_arn,"/"),-1) \
| stats count values(eventName) as actions by principal, account, sourceIPAddress \
| sort - count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(1),§164.502(e)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.55
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.6 — Security Awareness & Training — Phish Simulation Failure to ePHI Risk]
description = Security Awareness & Training — Phish Simulation Failure to ePHI Risk | UC 22.10.6 | regulation=hipaa-security | criticality=high | clauses=§164.308(a)(5)
search = index=security_awareness sourcetype="phish:sim" result="Clicked" earliest=-90d \
| stats count as fail_clicks by user \
| where fail_clicks>=3 \
| map maxsearches=50 search="index=epic sourcetype=\"epic:audit\" USER_ID=\"$user$\" AccessType IN (\"Report\",\"Export\") earliest=-90d | stats count as risky_epic_events by USER_ID" \
| sort - risky_epic_events
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(5)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.6
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.7 — Login Monitoring & Security Incident Procedures — Brute Force to Clinical SSO]
description = Login Monitoring & Security Incident Procedures — Brute Force to Clinical SSO | UC 22.10.7 | regulation=hipaa-security | criticality=critical | clauses=§164.308(a)(6)
search = index=ad sourcetype="WinEventLog:Security" EventCode="4625" earliest=-24h \
| eval user=TargetUserName \
| eval src=src_ip \
| stats count values(EventCode) as codes dc(ComputerName) as dc_used by user, src \
| where count>=15 AND dc_used>=3 \
| lookup hipaa_clinical_apps.csv src OUTPUT app_name \
| sort - count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(6)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.7
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.8 — Contingency Plan — Backup Job Success for ePHI Databases]
description = Contingency Plan — Backup Job Success for ePHI Databases | UC 22.10.8 | regulation=hipaa-security | criticality=critical | clauses=§164.308(a)(7)
search = index=backup sourcetype="commvault:job" earliest=-48h (database="CLARITY" OR database="CABOODLE" OR app="Epic*") \
| eval success=if(match(lower(status),"success|completed"),1,0) \
| stats sum(success) as ok_jobs count as total_jobs latest(_time) as last_job by database, client_name \
| eval health=if(ok_jobs=total_jobs,"OK","FAIL") \
| where health!="OK"
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(7)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.8
action.uc_compliance.param.regulation = hipaa-security

[UC-22.10.9 — Periodic Evaluation — Control Test Evidence Ingest]
description = Periodic Evaluation — Control Test Evidence Ingest | UC 22.10.9 | regulation=hipaa-security | criticality=high | clauses=§164.308(a)(8)
search = index=grc sourcetype="hipaa:periodic_test" earliest=-365d test_family="HIPAA_Technical" \
| eval period_month=strftime(_time,"%Y-%m") \
| stats latest(result) as last_result latest(_time) as last_run by control_id, period_month \
| where last_result!="Pass" \
| sort period_month, control_id
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(8)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.10.9
action.uc_compliance.param.regulation = hipaa-security

[UC-22.35.1 — Audit-log continuity: detect indexing gap indicating lost evidence]
description = Audit-log continuity: detect indexing gap indicating lost evidence | UC 22.35.1 | regulation=hipaa-security | criticality=critical | clauses=§164.312(b)
search = | tstats count as events where index=_internal source=*metrics.log* group=per_sourcetype_thruput earliest=-30m by sourcetype _time span=1m \
| eventstats avg(events) as avg_events, stdev(events) as stdev_events by sourcetype \
| where events=0 AND avg_events>0 \
| eval gap_seconds=60 \
| table _time sourcetype events avg_events stdev_events gap_seconds \
| sort - _time
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(b)
action.uc_compliance.param.versions = HIPAA@2013-final
action.uc_compliance.param.uc_id = 22.35.1
action.uc_compliance.param.regulation = hipaa-security

[UC-22.35.2 — Log tamper detection via write-once-read-many chain-of-custody]
description = Log tamper detection via write-once-read-many chain-of-custody | UC 22.35.2 | regulation=hipaa-security | criticality=critical | clauses=§164.312(c)(1)
search = | rest /services/cluster/master/buckets splunk_server=* | eval primary_hash=coalesce('primaries{}.bucket_flags.hash','bucket_hash') | stats values(primary_hash) as hashes dc(primary_hash) as uniques by index, bucket_id | where uniques>1 \
| eval divergence_detected_at=strftime(now(),"%Y-%m-%dT%H:%M:%SZ") \
| table divergence_detected_at index bucket_id hashes
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(c)(1)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.35.2
action.uc_compliance.param.regulation = hipaa-security

[UC-22.35.4 — Log signing chain integrity — cryptographic signature drift on evidence archive]
description = Log signing chain integrity — cryptographic signature drift on evidence archive | UC 22.35.4 | regulation=hipaa-security | criticality=critical | clauses=§164.312(c)(1)
search = index=compliance sourcetype=archive:signer earliest=-1h \
| eval block_parent_hash=coalesce(parent_hash,"GENESIS") \
| stats values(block_hash) as block_hashes latest(expected_parent_hash) as expected_parent by block_id archive_id \
| where expected_parent!=block_parent_hash AND block_parent_hash!="GENESIS" \
| table archive_id block_id block_parent_hash expected_parent block_hashes
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(c)(1)
action.uc_compliance.param.versions = HIPAA@2013-final
action.uc_compliance.param.uc_id = 22.35.4
action.uc_compliance.param.regulation = hipaa-security

[UC-22.35.5 — Search-head audit-trail completeness — deleted or rewritten search jobs]
description = Search-head audit-trail completeness — deleted or rewritten search jobs | UC 22.35.5 | regulation=hipaa-security | criticality=high | clauses=§164.312(b)
search = index=_audit action=search info=completed earliest=-24h \
| stats values(artifact_id) as artifact_ids earliest(_time) as first_seen latest(_time) as last_seen by search_id user \
| mvexpand artifact_ids \
| join type=outer artifact_ids [| rest /services/search/jobs splunk_server=* | fields sid dispatchState | rename sid as artifact_ids] \
| where isnull(dispatchState) AND (now()-last_seen)<86400 \
| table user search_id artifact_ids first_seen last_seen
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(b)
action.uc_compliance.param.versions = HIPAA@2013-final
action.uc_compliance.param.uc_id = 22.35.5
action.uc_compliance.param.regulation = hipaa-security

[UC-22.39.1 — Multi-regulator breach-notification SLA tracker (24h NIS2 / 72h GDPR / 72h HIPAA)]
description = Multi-regulator breach-notification SLA tracker (24h NIS2 / 72h GDPR / 72h HIPAA) | UC 22.39.1 | regulation=hipaa-security | criticality=critical | clauses=§164.308(a)(6)
search = | inputlookup incident_register.csv \
| where classification="breach" \
| eval applicable_regs=split(applicable_regs,",") \
| mvexpand applicable_regs \
| lookup sla_catalog.csv regulator as applicable_regs OUTPUT sla_hours \
| eval deadline=strptime(classified_at,"%Y-%m-%dT%H:%M:%SZ")+sla_hours*3600 \
| join incident_id applicable_regs [search index=notifications sourcetype=regulator:submission | fields incident_id applicable_regs tracking_id submitted_at] \
| eval status=case(isnull(tracking_id) AND now()>deadline, "breach", isnull(tracking_id) AND (deadline-now())<5400, "imminent", isnotnull(tracking_id), "submitted") \
| table incident_id applicable_regs deadline submitted_at status
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(6)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.39.1
action.uc_compliance.param.regulation = hipaa-security

[UC-22.39.3 — Data-subject breach communication timeline tracker (Art.34 / §164.404)]
description = Data-subject breach communication timeline tracker (Art.34 / §164.404) | UC 22.39.3 | regulation=hipaa-security | criticality=high | clauses=§164.404
search = index=comms sourcetype=breach:notification earliest=-90d \
| stats count(eval(status="delivered")) as delivered count(eval(status="bounced_handled")) as fallback count as total by campaign_id \
| eval ok=(delivered+fallback)/total \
| lookup breach_campaigns.csv campaign_id OUTPUT classification classified_at \
| eval age_days=round((now()-strptime(classified_at,"%Y-%m-%dT%H:%M:%SZ"))/86400,0) \
| where age_days>=50 AND ok<0.95 \
| table campaign_id classified_at age_days total delivered fallback ok
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.404
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.39.3
action.uc_compliance.param.regulation = hipaa-security

[UC-22.41.1 — Encryption-at-rest coverage gap — unencrypted storage with regulated data]
description = Encryption-at-rest coverage gap — unencrypted storage with regulated data | UC 22.41.1 | regulation=hipaa-security | criticality=critical | clauses=§164.312(a)(2)(iv)
search = | rest /services/cloud_storage/inventory splunk_server=* | where regulated_data="true" \
| stats values(encryption_state) as state by volume_id region tenant \
| where mvfind(state, "enabled")<0 \
| lookup data_classification.csv volume_id OUTPUT classification \
| table tenant region volume_id classification state
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(a)(2)(iv)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.41.1
action.uc_compliance.param.regulation = hipaa-security

[UC-22.41.2 — Certificate / TLS posture — weak cipher and expired-cert detection]
description = Certificate / TLS posture — weak cipher and expired-cert detection | UC 22.41.2 | regulation=hipaa-security | criticality=high | clauses=§164.312(e)(1)
search = index=tls sourcetype=tls:scan earliest=-1h \
| eval days_to_expiry=round((expires-now())/86400,0) \
| where days_to_expiry<21 OR protocol!="TLSv1.2" AND protocol!="TLSv1.3" OR NOT match(cipher, "^TLS_(AES|CHACHA20|ECDHE)") \
| table host days_to_expiry protocol cipher
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(e)(1)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.41.2
action.uc_compliance.param.regulation = hipaa-security

[UC-22.41.4 — TLS downgrade / legacy-cipher handshake spike]
description = TLS downgrade / legacy-cipher handshake spike | UC 22.41.4 | regulation=hipaa-security | criticality=high | clauses=§164.312(e)(1)
search = index=tls sourcetype=f5:tls OR sourcetype=nginx:tls OR sourcetype=haproxy:tls earliest=-1h \
| eval legacy=if(match(protocol,"TLSv1\.(0|1)") OR match(cipher,"3DES|RC4|NULL|DES|EXPORT"),1,0) \
| stats sum(legacy) as legacy_hs count as total_hs by host \
| eval legacy_pct=round(legacy_hs/total_hs*100,2) \
| where legacy_pct>0.5 AND total_hs>100 \
| table host legacy_hs total_hs legacy_pct
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(e)(1)
action.uc_compliance.param.versions = HIPAA@2013-final
action.uc_compliance.param.uc_id = 22.41.4
action.uc_compliance.param.regulation = hipaa-security

[UC-22.45.2 — Backup encryption and air-gap integrity — tamper detection on immutable storage]
description = Backup encryption and air-gap integrity — tamper detection on immutable storage | UC 22.45.2 | regulation=hipaa-security | criticality=critical | clauses=§164.308(a)(7)
search = index=backup sourcetype=immutable:events earliest=-1h (event=tamper OR event=lock_violation OR event=checksum_mismatch) \
| lookup backup_lifecycle.csv repo_id OUTPUT policy_id \
| where event!="lifecycle_expiry" OR isnull(policy_id) \
| table _time repo_id event detail policy_id
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(7)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.45.2
action.uc_compliance.param.regulation = hipaa-security

[UC-22.45.4 — Backup repository TLS posture — aged or weak-cipher endpoints]
description = Backup repository TLS posture — aged or weak-cipher endpoints | UC 22.45.4 | regulation=hipaa-security | criticality=medium | clauses=§164.312(e)(1)
search = index=tls sourcetype=tls:scan earliest=-24h \
| search host IN (`backup_repository_endpoints`) \
| eval days_to_expiry=round((expires-now())/86400,0) \
| where days_to_expiry<45 OR protocol="TLSv1.0" OR protocol="TLSv1.1" OR match(cipher,"3DES|RC4|NULL|DES|EXPORT") \
| table host days_to_expiry protocol cipher
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 3
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(e)(1)
action.uc_compliance.param.versions = HIPAA@2013-final
action.uc_compliance.param.uc_id = 22.45.4
action.uc_compliance.param.regulation = hipaa-security

[UC-22.46.1 — Mandatory security training — completion SLA by role]
description = Mandatory security training — completion SLA by role | UC 22.46.1 | regulation=hipaa-security | criticality=medium | clauses=§164.308(a)(5)
search = index=lms sourcetype=completion earliest=-365d course_id=security_awareness \
| stats max(_time) as last_completion by employee_id \
| lookup hr_roster.csv employee_id OUTPUT role status \
| where status="active" AND (isnull(last_completion) OR (now()-last_completion)>31536000) \
| table employee_id role last_completion
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 3
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(5)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.46.1
action.uc_compliance.param.regulation = hipaa-security

[UC-22.46.3 — Privileged-role specialist training — admins lacking annual deep-training]
description = Privileged-role specialist training — admins lacking annual deep-training | UC 22.46.3 | regulation=hipaa-security | criticality=medium | clauses=§164.308(a)(5)(ii)(A)
search = | rest /services/pam/vault-inventory splunk_server=* \
| fields user account_type \
| where account_type="privileged" \
| join user [search index=lms sourcetype=completion course_id=priv_security_training earliest=-365d | stats max(_time) as last_completion by user] \
| where (now()-last_completion)>31536000 OR isnull(last_completion) \
| table user account_type last_completion
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 3
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(5)(ii)(A)
action.uc_compliance.param.versions = HIPAA@2013-final
action.uc_compliance.param.uc_id = 22.46.3
action.uc_compliance.param.regulation = hipaa-security

[UC-22.46.5 — Developer data-handling training — prod-access engineers lacking training]
description = Developer data-handling training — prod-access engineers lacking training | UC 22.46.5 | regulation=hipaa-security | criticality=high | clauses=§164.308(a)(5)
search = | rest /services/identity/prod-access-groups splunk_server=* \
| fields user group scope \
| where scope="regulated" \
| join user [search index=lms sourcetype=completion course_id=data_handling_v* earliest=-365d | stats max(_time) as last_completion by user] \
| where (now()-last_completion)>31536000 OR isnull(last_completion) \
| table user group scope last_completion
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(5)
action.uc_compliance.param.versions = HIPAA@2013-final
action.uc_compliance.param.uc_id = 22.46.5
action.uc_compliance.param.regulation = hipaa-security

[UC-22.49.1 — Retention execution evidence — records past retention still present]
description = Retention execution evidence — records past retention still present | UC 22.49.1 | regulation=hipaa-security | criticality=high | clauses=§164.310(d)(1)
search = index=datastore sourcetype=retention:inventory earliest=-1d \
| lookup retention_policy.csv data_domain OUTPUT retention_period_days \
| eval age_days=round((now()-strptime(created_at,"%Y-%m-%dT%H:%M:%SZ"))/86400,0) \
| where age_days>retention_period_days AND litigation_hold!="true" \
| table store record_id data_domain age_days retention_period_days
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.310(d)(1)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.49.1
action.uc_compliance.param.regulation = hipaa-security

[UC-22.49.2 — Disposal workflow completion — failed disposals requiring manual review]
description = Disposal workflow completion — failed disposals requiring manual review | UC 22.49.2 | regulation=hipaa-security | criticality=high | clauses=§164.310(d)(1)
search = index=datastore sourcetype=disposal:job earliest=-7d \
| stats values(status) as states min(_time) as started max(_time) as last_update by job_id \
| where NOT match(states, "completed") AND (now()-started)>172800 \
| table job_id started last_update states
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.310(d)(1)
action.uc_compliance.param.versions = HIPAA Security@2013-final
action.uc_compliance.param.uc_id = 22.49.2
action.uc_compliance.param.regulation = hipaa-security

[UC-22.49.4 — Retention policy drift — system config vs policy catalogue]
description = Retention policy drift — system config vs policy catalogue | UC 22.49.4 | regulation=hipaa-security | criticality=high | clauses=§164.316(b)(2)
search = index=datastore sourcetype=retention:config earliest=-1d \
| lookup retention_policy.csv data_domain OUTPUT policy_days \
| eval drift_days=abs(configured_days-policy_days) \
| where drift_days>0 \
| table store data_domain configured_days policy_days drift_days
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.316(b)(2)
action.uc_compliance.param.versions = HIPAA@2013-final
action.uc_compliance.param.uc_id = 22.49.4
action.uc_compliance.param.regulation = hipaa-security

[UC-22.49.5 — Cryptographic erasure attestation — per-asset destruction evidence]
description = Cryptographic erasure attestation — per-asset destruction evidence | UC 22.49.5 | regulation=hipaa-security | criticality=high | clauses=§164.310(d)(2)(i)
search = index=assets sourcetype=asset:disposal earliest=-7d method=cryptographic_erasure \
| join asset_id [search index=kms action=DeleteKey earliest=-8d | fields asset_id key_id _time | rename _time as key_deleted_at] \
| eval delta=abs(_time-key_deleted_at) \
| where isnull(key_deleted_at) OR delta>86400 \
| table asset_id key_id _time key_deleted_at delta
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.310(d)(2)(i)
action.uc_compliance.param.versions = HIPAA@2013-final
action.uc_compliance.param.uc_id = 22.49.5
action.uc_compliance.param.regulation = hipaa-security

[UC-22.6.53 — ISO/IEC 27001:2022 Clause 7.2 — Competence evidence: role-based training completion]
description = ISO/IEC 27001:2022 Clause 7.2 — Competence evidence: role-based training completion | UC 22.6.53 | regulation=hipaa-security | criticality=medium | clauses=§164.308(a)(5)
search = index=hr sourcetype=hr:role_assignment earliest=-30d \
| stats latest(role) AS role BY user \
| lookup iso_27001_training_matrix.csv role OUTPUT required_modules \
| eval required_modules=split(required_modules,",") \
| join user \
  [search index=hr sourcetype=lms:completion status=completed earliest=-365d \
   | stats values(module) AS completed_modules BY user] \
| eval covered=mvmap(required_modules, if(isnotnull(mvfind(completed_modules,x)),1,0)) \
| eval compliance_pct=round(100*sum(covered)/mvcount(required_modules),0) \
| where compliance_pct<100 \
| table user role required_modules completed_modules compliance_pct
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 3
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.308(a)(5)
action.uc_compliance.param.versions = HIPAA Security Rule@2013-final
action.uc_compliance.param.uc_id = 22.6.53
action.uc_compliance.param.regulation = hipaa-security

[UC-22.8.31 — SOC 2 CC6.6 — Encryption-in-transit validation: cleartext protocols crossing the trust boundary]
description = SOC 2 CC6.6 — Encryption-in-transit validation: cleartext protocols crossing the trust boundary | UC 22.8.31 | regulation=hipaa-security | criticality=high | clauses=§164.312(e)(1)
search = index=stream sourcetype=stream:tls earliest=-30m \
| rename src_ip AS src dest_ip AS dest \
| lookup trust_zones.csv ip=src OUTPUT zone AS src_zone \
| lookup trust_zones.csv ip=dest OUTPUT zone AS dest_zone \
| where src_zone!=dest_zone \
| eval is_deprecated=if(tls_version IN ("SSLv3","TLS1.0","TLS1.1"),1,0) \
| where is_deprecated=1 OR protocol IN ("http","ftp","telnet","smtp") \
| stats count BY src dest dest_port protocol tls_version is_deprecated
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(e)(1)
action.uc_compliance.param.versions = HIPAA Security Rule@2013-final
action.uc_compliance.param.uc_id = 22.8.31
action.uc_compliance.param.regulation = hipaa-security

[UC-22.8.38 — SOC 2 C1.1 — Confidentiality: sensitive-data exposure at the egress boundary]
description = SOC 2 C1.1 — Confidentiality: sensitive-data exposure at the egress boundary | UC 22.8.38 | regulation=hipaa-security | criticality=high | clauses=§164.312(e)(1)
search = index=dlp sourcetype IN (symantec:dlp,microsoft:purview:dlp) severity IN ("high","critical") earliest=-30m \
| rename destination_ip AS dest policy_name AS dlp_policy \
| lookup approved_egress.csv dest OUTPUT approved \
| where approved!="yes" OR isnull(approved) \
| stats count BY dlp_policy severity user dest approved
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = §164.312(e)(1)
action.uc_compliance.param.versions = HIPAA Security Rule@2013-final
action.uc_compliance.param.uc_id = 22.8.38
action.uc_compliance.param.regulation = hipaa-security
