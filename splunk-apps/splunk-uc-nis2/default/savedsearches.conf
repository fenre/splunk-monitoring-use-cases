# -----------------------------------------------------------------
# GENERATED by scripts/generate_splunk_app.py — DO NOT EDIT BY HAND.
# Source of truth: use-cases/cat-*/uc-*.json + data/regulations.json.
# Re-run `python3 scripts/generate_splunk_app.py` after content edits.
# -----------------------------------------------------------------

[UC-22.2.1 — NIS2 Incident Detection and 24-Hour Early Warning Reporting]
description = NIS2 Incident Detection and 24-Hour Early Warning Reporting | UC 22.2.1 | regulation=nis2 | criticality=critical | clauses=Art.23
search = `notable` urgency IN ("high","critical") earliest=-3d \
| eval hours_open=round((now()-_time)/3600, 2) \
| eval t_minus_4h=if(hours_open>=20 AND hours_open<24 AND status!="Closed", 1, 0) \
| eval past_24h_open=if(hours_open>24 AND status!="Closed", 1, 0) \
| table _time, rule_name, urgency, status, owner, status_description, hours_open, t_minus_4h, past_24h_open \
| where t_minus_4h=1 OR past_24h_open=1 \
| sort - past_24h_open, - hours_open
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.23
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.1
action.uc_compliance.param.regulation = nis2

[UC-22.2.10 — NIS2 Cyber Hygiene and Training Compliance]
description = NIS2 Cyber Hygiene and Training Compliance | UC 22.2.10 | regulation=nis2 | criticality=medium | clauses=Art.21(2)(g)
search = | inputlookup nis2_training_completion.csv \
| eval days_since_training=round((now()-strptime(completion_date,"%Y-%m-%d"))/86400, 0) \
| eval overdue=if(days_since_training > 365 OR isnull(completion_date), 1, 0) \
| stats count as total_staff, sum(overdue) as overdue_count, avg(days_since_training) as avg_days_since \
| eval compliance_pct=round(100*(total_staff-overdue_count)/total_staff, 1) \
| table total_staff, overdue_count, compliance_pct, avg_days_since
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 3
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(g)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.10
action.uc_compliance.param.regulation = nis2

[UC-22.2.11 — NIS2 Cryptography and Encryption Policy Monitoring]
description = NIS2 Cryptography and Encryption Policy Monitoring | UC 22.2.11 | regulation=nis2 | criticality=high | clauses=Art.21(2)(h)
search = | tstats `summariesonly` count \
  from datamodel=Certificates.All_Certificates \
  where All_Certificates.ssl_end_time < relative_time(now(), "+30d") \
  by All_Certificates.ssl_subject_common_name All_Certificates.ssl_end_time All_Certificates.ssl_issuer_common_name \
| rename All_Certificates.* as * \
| eval days_until_expiry=round((ssl_end_time - now())/86400, 0) \
| eval status=case(days_until_expiry < 0, "EXPIRED", days_until_expiry < 14, "CRITICAL", days_until_expiry < 30, "WARNING") \
| sort days_until_expiry \
| table ssl_subject_common_name, ssl_issuer_common_name, days_until_expiry, status
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(h)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.11
action.uc_compliance.param.regulation = nis2

[UC-22.2.12 — NIS2 Multi-Factor Authentication and Secure Communications]
description = NIS2 Multi-Factor Authentication and Secure Communications | UC 22.2.12 | regulation=nis2 | criticality=critical | clauses=Art.21(2)(j)
search = index=auth OR index=azure OR index=okta earliest=-24h \
  sourcetype IN ("ms:aad:signin","OktaIM2:log","linux:auth","WinEventLog:Security") \
| eval user=coalesce(userPrincipalName, actor.alternateId, Account_Name, user) \
| eval mfa_used=case( \
    match(lower(_raw),"(?i)mfa|multifactor|two.?factor|2fa|totp|fido|webauthn|push.*approve"), 1, \
    match(lower(authenticationRequirement),"(?i)multi"), 1, \
    match(lower(factor),"(?i)push|totp|sms|call|webauthn"), 1, \
    1=1, 0) \
| eval is_admin=if(match(lower(user),"(?i)admin|svc-|service|root|system") OR match(lower(_raw),"(?i)privileged|admin.*role"), 1, 0) \
| stats count sum(mfa_used) as mfa_count by user, is_admin, sourcetype \
| eval mfa_pct=round(100*mfa_count/count, 1) \
| where (is_admin=1 AND mfa_pct < 100) OR (is_admin=0 AND mfa_pct < 80) \
| sort is_admin, mfa_pct \
| table user, is_admin, count, mfa_count, mfa_pct, sourcetype
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(j)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.12
action.uc_compliance.param.regulation = nis2

[UC-22.2.13 — NIS2 Asset Management and Configuration Baseline]
description = NIS2 Asset Management and Configuration Baseline | UC 22.2.13 | regulation=nis2 | criticality=high | clauses=Art.21(2)(i)
search = | tstats `summariesonly` dc(All_Traffic.dest_port) as ports, sum(All_Traffic.bytes) as total_bytes \
  from datamodel=Network_Traffic.All_Traffic \
  by All_Traffic.src \
| rename All_Traffic.src as src \
| lookup asset_lookup_by_str key AS src OUTPUT category, priority, owner, nt_host \
| where isnull(category) OR category="unknown" \
| sort - total_bytes \
| head 100 \
| table src, nt_host, category, owner, ports, total_bytes
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(i)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.13
action.uc_compliance.param.regulation = nis2

[UC-22.2.14 — NIS2 Human Resources Security — Joiner/Mover/Leaver Process]
description = NIS2 Human Resources Security — Joiner/Mover/Leaver Process | UC 22.2.14 | regulation=nis2 | criticality=high | clauses=Art.21(2)(i)
search = | inputlookup terminated_employees.csv \
| eval term_date=strptime(termination_date, "%Y-%m-%d") \
| eval days_since_term=round((now()-term_date)/86400, 0) \
| join type=left user [ \
    search index=auth OR index=windows OR index=azure earliest=-30d \
    | eval user=coalesce(Account_Name, userPrincipalName, user) \
    | stats latest(_time) as last_auth count as auth_count by user \
] \
| where isnotnull(last_auth) AND last_auth > term_date \
| eval days_active_after_term=round((last_auth - term_date)/86400, 0) \
| sort - days_active_after_term \
| table user, termination_date, days_since_term, days_active_after_term, auth_count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(i)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.14
action.uc_compliance.param.regulation = nis2

[UC-22.2.15 — NIS2 Secure System Acquisition and Development Lifecycle]
description = NIS2 Secure System Acquisition and Development Lifecycle | UC 22.2.15 | regulation=nis2 | criticality=high | clauses=Art.21(2)(e)
search = index=devops sourcetype IN ("github:webhook","gitlab:pipeline","jenkins:build") earliest=-30d \
| eval has_security_scan=if(match(lower(_raw),"(?i)sast|sca|snyk|sonar|trivy|semgrep|security.scan|code.scan"), 1, 0) \
| eval deployment=if(match(lower(_raw),"(?i)deploy|release|prod|production"), 1, 0) \
| stats count as total_builds, sum(has_security_scan) as scanned_builds, sum(deployment) as deployments by repository \
| eval scan_coverage=round(100*scanned_builds/total_builds, 1) \
| where scan_coverage < 80 OR (deployments > 0 AND scanned_builds = 0) \
| sort scan_coverage \
| table repository, total_builds, scanned_builds, scan_coverage, deployments
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(e)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.15
action.uc_compliance.param.regulation = nis2

[UC-22.2.16 — NIS2 Supply Chain Third-Party Risk Continuous Monitoring]
description = NIS2 Supply Chain Third-Party Risk Continuous Monitoring | UC 22.2.16 | regulation=nis2 | criticality=high | clauses=Art.21(2)(d)
search = | tstats `summariesonly` sum(All_Traffic.bytes_out) as bytes_out dc(All_Traffic.src) as internal_sources \
  from datamodel=Network_Traffic.All_Traffic \
  where All_Traffic.dest_category="supplier" \
  by All_Traffic.dest All_Traffic.app span=1d \
| rename All_Traffic.* as * \
| lookup supplier_risk_lookup dest OUTPUT supplier_name, risk_tier, last_assessment_date \
| eval days_since_assessment=round((now()-strptime(last_assessment_date,"%Y-%m-%d"))/86400, 0) \
| eval assessment_overdue=if(days_since_assessment > 365, 1, 0) \
| where bytes_out > 1073741824 OR assessment_overdue=1 OR internal_sources > 50 \
| sort - bytes_out \
| table dest, supplier_name, risk_tier, bytes_out, internal_sources, days_since_assessment, assessment_overdue
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(d)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.16
action.uc_compliance.param.regulation = nis2

[UC-22.2.17 — NIS2 Backup Management and Disaster Recovery Verification]
description = NIS2 Backup Management and Disaster Recovery Verification | UC 22.2.17 | regulation=nis2 | criticality=critical | clauses=Art.21(2)(c)
search = index=backup sourcetype IN ("veeam:backup","commvault:job","rubrik:event","aws:backup") earliest=-7d \
| eval job_status=lower(coalesce(status, result, job_result, state)) \
| eval success=if(match(job_status,"(?i)success|completed|ok"), 1, 0) \
| eval failed=if(match(job_status,"(?i)fail|error|warning|missed"), 1, 0) \
| stats sum(success) as successful, sum(failed) as failed, latest(_time) as last_backup by job_name, target_system \
| eval days_since_backup=round((now()-last_backup)/86400, 1) \
| eval backup_gap=if(days_since_backup > 1, "OVERDUE", "OK") \
| where failed > 0 OR backup_gap="OVERDUE" \
| sort - days_since_backup \
| table target_system, job_name, successful, failed, days_since_backup, backup_gap
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(c)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.17
action.uc_compliance.param.regulation = nis2

[UC-22.2.18 — NIS2 Network Security Monitoring and Anomaly Detection]
description = NIS2 Network Security Monitoring and Anomaly Detection | UC 22.2.18 | regulation=nis2 | criticality=critical | clauses=Art.21(2)(a)
search = | tstats `summariesonly` count sum(All_Traffic.bytes) as total_bytes dc(All_Traffic.dest_port) as dest_ports \
  from datamodel=Network_Traffic.All_Traffic \
  where NOT All_Traffic.dest_category IN ("internal_server","dns","ntp") \
  by All_Traffic.src All_Traffic.dest All_Traffic.action span=1h \
| rename All_Traffic.* as * \
| where dest_ports > 20 OR (action="blocked" AND count > 100) OR total_bytes > 10737418240 \
| lookup asset_lookup_by_str key AS src OUTPUT category as src_category, priority as src_priority \
| eval risk_signal=case( \
    dest_ports > 50, "port_scan", \
    action="blocked" AND count > 500, "brute_force", \
    total_bytes > 10737418240, "data_exfiltration", \
    isnull(src_category), "unknown_asset", \
    1=1, "anomaly") \
| sort - count \
| table _time, src, dest, src_category, risk_signal, dest_ports, count, total_bytes, action
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(a)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.18
action.uc_compliance.param.regulation = nis2

[UC-22.2.19 — NIS2 Cross-Border Incident Impact Assessment]
description = NIS2 Cross-Border Incident Impact Assessment | UC 22.2.19 | regulation=nis2 | criticality=high | clauses=Art.23(3)
search = `notable` urgency IN ("high","critical") status!="Closed" earliest=-7d \
| eval incident_assets=mvappend(src, dest, src_ip, dest_ip) \
| mvexpand incident_assets \
| lookup asset_lookup_by_str key AS incident_assets OUTPUT country, business_unit, category \
| iplocation incident_assets \
| eval asset_country=coalesce(country, Country) \
| where isnotnull(asset_country) \
| stats dc(asset_country) as countries_affected, values(asset_country) as affected_countries, dc(incident_assets) as assets_involved by rule_name, urgency \
| where countries_affected > 1 \
| eval cross_border_notification="REQUIRED — notify CSIRT in: " . mvjoin(affected_countries, ", ") \
| table rule_name, urgency, countries_affected, affected_countries, assets_involved, cross_border_notification
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.23(3)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.19
action.uc_compliance.param.regulation = nis2

[UC-22.2.2 — NIS2 Supply Chain Security Monitoring]
description = NIS2 Supply Chain Security Monitoring | UC 22.2.2 | regulation=nis2 | criticality=high | clauses=Art.21(2)(d)
search = index=pam sourcetype="cyberark:session" earliest=-24h \
| rex field=target_host "(?<target_domain>[a-zA-Z0-9][a-zA-Z0-9.-]+\.[a-zA-Z]{2,})$" \
| stats sum(duration_min) as total_min, dc(session_id) as sessions by user, target_host, target_domain, target_account \
| lookup threat_intel_domain_lookup domain AS target_domain OUTPUT description AS ti_description, weight AS ti_weight \
| where isnotnull(ti_weight) OR total_min>120 \
| sort - total_min
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(d)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.2
action.uc_compliance.param.regulation = nis2

[UC-22.2.20 — NIS2 Management Body Accountability and Governance Evidence]
description = NIS2 Management Body Accountability and Governance Evidence | UC 22.2.20 | regulation=nis2 | criticality=high | clauses=Art.20
search = | inputlookup nis2_governance_evidence.csv \
| eval evidence_date=strptime(date, "%Y-%m-%d") \
| eval days_since=round((now()-evidence_date)/86400, 0) \
| eval status=case( \
    days_since > 365, "OVERDUE", \
    days_since > 270, "DUE_SOON", \
    1=1, "CURRENT") \
| sort - days_since \
| table evidence_type, description, responsible_person, date, days_since, status
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.20
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.20
action.uc_compliance.param.regulation = nis2

[UC-22.2.21 — NIS2 Risk Analysis Evidence for Essential Entities]
description = NIS2 Risk Analysis Evidence for Essential Entities | UC 22.2.21 | regulation=nis2 | criticality=critical | clauses=Art.21(2)
search = | inputlookup nis2_entity_classification.csv WHERE entity_tier="essential" \
| rename asset_id as risk_object \
| join risk_object [ \
    search index=risk sourcetype="stash" earliest=-30d \
    | stats latest(risk_score) as residual_risk max(risk_score) as peak_risk by risk_object \
] \
| join risk_object [ \
    search index=tenable sourcetype="tenable:vuln" severity IN ("Critical","High") earliest=-14d \
    | stats dc(plugin_id) as open_critical_high by host \
    | rename host as risk_object \
] \
| eval analysis_stale=if(residual_risk > 80 OR open_critical_high > 5, "ESCALATE", "Monitor") \
| table risk_object, business_service, residual_risk, peak_risk, open_critical_high, analysis_stale \
| sort - residual_risk
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.21
action.uc_compliance.param.regulation = nis2

[UC-22.2.22 — NIS2 Risk Analysis Evidence for Important Entities]
description = NIS2 Risk Analysis Evidence for Important Entities | UC 22.2.22 | regulation=nis2 | criticality=high | clauses=Art.21(2)
search = | inputlookup nis2_entity_classification.csv WHERE entity_tier="important" \
| rename asset_hostname as host \
| join host [ \
    search index=qualys sourcetype="qualys:hostdetection" earliest=-30d \
    | eval age_days=round((now()-strptime(first_found_datetime,"%Y-%m-%dT%H:%M:%SZ"))/86400,0) \
    | where severity IN ("Critical","High") AND age_days > 30 \
    | stats dc(qid) as aged_findings by host \
] \
| where aged_findings > 0 \
| table host, business_service, aged_findings \
| sort - aged_findings
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.22
action.uc_compliance.param.regulation = nis2

[UC-22.2.23 — NIS2 Incident Handling Procedure Adherence and Playbook Execution]
description = NIS2 Incident Handling Procedure Adherence and Playbook Execution | UC 22.2.23 | regulation=nis2 | criticality=critical | clauses=Art.21(2)(b)
search = `notable` urgency IN ("high","critical") earliest=-30d \
| eval incident_id=rule_name."|"._time \
| join incident_id type=left [ \
    search index=itsm sourcetype="snow:incident" earliest=-30d \
    | eval incident_id=correlation_id \
    | eval stages_complete=mvcount(mvappend( \
        if(match(work_notes,"(?i)contain"),"contain",""), \
        if(match(work_notes,"(?i)erad"),"erad",""), \
        if(match(work_notes,"(?i)recover"),"recover",""), \
        if(match(work_notes,"(?i)lessons"),"lessons",""))) \
    | stats latest(stages_complete) as stages_complete by incident_id \
] \
| eval playbook_gap=if(stages_complete < 4, 1, 0) \
| where playbook_gap=1 AND status!="Closed" \
| table _time, rule_name, status, owner, stages_complete
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(b)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.23
action.uc_compliance.param.regulation = nis2

[UC-22.2.24 — NIS2 Business Continuity and ICT Continuity Evidence]
description = NIS2 Business Continuity and ICT Continuity Evidence | UC 22.2.24 | regulation=nis2 | criticality=critical | clauses=Art.21(2)(c)
search = | inputlookup nis2_bcp_test_register.csv \
| eval test_date_epoch=strptime(last_tabletop_date,"%Y-%m-%d") \
| eval months_since=round((now()-test_date_epoch)/2592000,1) \
| where months_since > 12 \
| join service_name [ \
    search index=itsi_summary earliest=-90d \
    | where severity IN ("Critical","High") \
    | stats sum(alert_duration) as downtime_seconds by service_name \
] \
| eval rto_seconds=rto_minutes*60 \
| eval rto_breach=if(downtime_seconds > rto_seconds, 1, 0) \
| table service_name, months_since, downtime_seconds, rto_seconds, rto_breach
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(c)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.24
action.uc_compliance.param.regulation = nis2

[UC-22.2.25 — NIS2 Supply Chain Security Assessment Coverage]
description = NIS2 Supply Chain Security Assessment Coverage | UC 22.2.25 | regulation=nis2 | criticality=high | clauses=Art.21(2)(d)
search = | inputlookup nis2_supplier_assessment_register.csv \
| eval assessment_due=strptime(next_assessment_date,"%Y-%m-%d") \
| eval overdue=if(now()>assessment_due,1,0) \
| join supplier_id [ \
    search index=github sourcetype="dependabot_alert" earliest=-30d \
    | stats dc(alert_number) as open_supply_chain_alerts by repo \
    | rename repo as supplier_code_repo \
] \
| where overdue=1 OR open_supply_chain_alerts > 10 \
| table supplier_name, tier, next_assessment_date, overdue, open_supply_chain_alerts
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(d)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.25
action.uc_compliance.param.regulation = nis2

[UC-22.2.26 — NIS2 Network Security Monitoring Coverage by Segment]
description = NIS2 Network Security Monitoring Coverage by Segment | UC 22.2.26 | regulation=nis2 | criticality=critical | clauses=Art.21(2)(a)
search = | inputlookup nis2_segment_coverage_expectations.csv \
| join segment_id [ \
    | tstats `summariesonly` count where index=netfw earliest=-24h by index \
    | eval segment_id=case( \
        match(index,"(?i)corp"),"CORP", \
        match(index,"(?i)dmz"),"DMZ", \
        1=1,"OTHER") \
    | stats sum(count) as events by segment_id \
] \
| eval expected_eps=expected_events_per_day/86400*3600 \
| eval coverage_gap=if(events < expected_eps*0.5, 1, 0) \
| where coverage_gap=1 \
| table segment_id, events, expected_eps, nis2_scope
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(a)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.26
action.uc_compliance.param.regulation = nis2

[UC-22.2.27 — NIS2 Vulnerability Disclosure Policy Operational Signals]
description = NIS2 Vulnerability Disclosure Policy Operational Signals | UC 22.2.27 | regulation=nis2 | criticality=high | clauses=Art.21(2)(e)
search = index=itsm sourcetype="snow:incident" category="*Vulnerability Disclosure*" earliest=-90d \
| eval opened=strptime(opened_at,"%Y-%m-%d %H:%M:%S") \
| eval triaged=strptime(work_start,"%Y-%m-%d %H:%M:%S") \
| eval triage_hours=round((triaged-opened)/3600,2) \
| eval sla_breach=if(triage_hours>48 OR isnull(triaged),1,0) \
| stats count as tickets sum(sla_breach) as breaches avg(triage_hours) as avg_triage \
| eval breach_pct=round(100*breaches/tickets,1) \
| table tickets, breaches, breach_pct, avg_triage
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(e)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.27
action.uc_compliance.param.regulation = nis2

[UC-22.2.28 — NIS2 Cyber Hygiene Practices — Baseline Control Compliance]
description = NIS2 Cyber Hygiene Practices — Baseline Control Compliance | UC 22.2.28 | regulation=nis2 | criticality=high | clauses=Art.21(2)(g)
search = | inputlookup nis2_asset_hygiene_expectations.csv \
| join asset_fqdn [ \
    search index=tenable sourcetype="tenable:sc:assets" earliest=-1d \
    | eval mfa_ok=if(match(plugins_text,"(?i)mfa|okta.verify"),1,0) \
    | eval edr_ok=if(match(plugins_text,"(?i)edr|defender|crowdstrike"),1,0) \
    | stats latest(mfa_ok) as mfa_ok latest(edr_ok) as edr_ok by dns_name \
    | rename dns_name as asset_fqdn \
] \
| eval hygiene_failures=if(mfa_ok==0,1,0) + if(edr_ok==0,1,0) \
| where hygiene_failures > 0 \
| table asset_fqdn, business_service, mfa_ok, edr_ok, hygiene_failures
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(g)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.28
action.uc_compliance.param.regulation = nis2

[UC-22.2.29 — NIS2 Cryptography Policy Compliance — TLS and Certificate Posture]
description = NIS2 Cryptography Policy Compliance — TLS and Certificate Posture | UC 22.2.29 | regulation=nis2 | criticality=high | clauses=Art.21(2)(h)
search = index=stream sourcetype="stream:tls" earliest=-24h \
| stats latest(tls_version) as tls_version latest(cipher) as cipher values(expiry_epoch) as cert_expiry by dest_ip dest_port \
| lookup nis2_scoped_service_map dest_ip OUTPUT service_name nis2_in_scope \
| where nis2_in_scope="true" \
| eval weak_tls=if(match(tls_version,"SSLv|TLSv1\.0|TLSv1\.1"),1,0) \
| eval cert_problem=if(cert_expiry < relative_time(now(),"+30d@d"),1,0) \
| where weak_tls=1 OR cert_problem=1 \
| table service_name, dest_ip, dest_port, tls_version, cipher, cert_problem
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(h)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.29
action.uc_compliance.param.regulation = nis2

[UC-22.2.3 — NIS2 Vulnerability Disclosure and Patch Management Tracking]
description = NIS2 Vulnerability Disclosure and Patch Management Tracking | UC 22.2.3 | regulation=nis2 | criticality=critical | clauses=Art.21(2)(e)
search = index=vulnerability sourcetype="tenable:vuln" state="Active" \
| eval host=coalesce(host, hostname, dns_name) \
| eval first_found=coalesce(first_found, first_seen) \
| eval age_days=round((now()-first_found)/86400, 1) \
| eval sla_days=case(severity="Critical",7, severity="High",30, 1=1,90) \
| eval sla_breach=if(age_days>sla_days, 1, 0) \
| stats count as open_vulns, max(age_days) as max_age by host, severity \
| where sla_breach=1 \
| sort - max_age \
| table host, severity, open_vulns, max_age, sla_days
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(e)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.3
action.uc_compliance.param.regulation = nis2

[UC-22.2.30 — NIS2 Human Resources Security Measures Evidence]
description = NIS2 Human Resources Security Measures Evidence | UC 22.2.30 | regulation=nis2 | criticality=high | clauses=Art.21(2)(i)
search = | inputlookup nis2_privileged_role_roster.csv \
| join user_id [ \
    search index=training earliest=-365d \
    | stats latest(completion_date) as last_training by user_id \
] \
| eval training_age=round((now()-strptime(last_training,"%Y-%m-%d"))/86400,0) \
| where training_age > 365 OR isnull(last_training) \
| table role_name, user_id, last_training, training_age
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(i)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.30
action.uc_compliance.param.regulation = nis2

[UC-22.2.31 — NIS2 Entity Classification Validation (Essential vs Important)]
description = NIS2 Entity Classification Validation (Essential vs Important) | UC 22.2.31 | regulation=nis2 | criticality=high | clauses=Art.2(1)
search = | inputlookup nis2_entity_classification.csv \
| join legal_entity_id [ \
    search index=cmdb earliest=-1d \
    | stats values(nis2_entity_tag) as cmdb_tags by legal_entity_id \
] \
| eval mismatch=if(NOT match(mvjoin(cmdb_tags,"|"), entity_tier), 1, 0) \
| where mismatch=1 OR isnull(cmdb_tags) \
| table legal_entity_id, entity_tier, cmdb_tags, mismatch
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.2(1)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.31
action.uc_compliance.param.regulation = nis2

[UC-22.2.32 — NIS2 Proportional Security Measure Verification by Tier]
description = NIS2 Proportional Security Measure Verification by Tier | UC 22.2.32 | regulation=nis2 | criticality=high | clauses=Art.21(2)
search = | inputlookup nis2_control_matrix.csv \
| join entity_tier [ \
    | inputlookup nis2_entity_classification.csv \
    | stats values(entity_tier) as entity_tiers by legal_entity_id \
    | mvexpand entity_tiers \
    | rename entity_tiers as entity_tier \
] \
| where required_for_tier="true" \
| join control_id [ \
    | rest /servicesNS/-/SplunkEnterpriseSecurity/saved/searches count=0 splunk_server=local \
    | search title="nis2_*" \
    | eval control_id=replace(title,"nis2_","") \
    | stats latest(disabled) as search_disabled by control_id \
] \
| where search_disabled=1 OR isnull(search_disabled) \
| table control_id, entity_tier, search_disabled
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.32
action.uc_compliance.param.regulation = nis2

[UC-22.2.33 — NIS2 Incident Reporting Timeline Compliance (24h Early Warning / 72h Notification)]
description = NIS2 Incident Reporting Timeline Compliance (24h Early Warning / 72h Notification) | UC 22.2.33 | regulation=nis2 | criticality=critical | clauses=Art.23
search = `notable` urgency IN ("high","critical") earliest=-14d \
| eval awareness_time=strptime(status_description,"(?i)awareness[:\\s]+(%Y-%m-%d %H:%M:%S)") \
| eval early_warning_sent=strptime(status_description,"(?i)early.warning[:\\s]+(%Y-%m-%d %H:%M:%S)") \
| eval notification_72h=strptime(status_description,"(?i)72h.notification[:\\s]+(%Y-%m-%d %H:%M:%S)") \
| eval hours_to_early=round((early_warning_sent-awareness_time)/3600,2) \
| eval hours_to_72=round((notification_72h-awareness_time)/3600,2) \
| where hours_to_early > 24 OR hours_to_72 > 72 OR isnull(early_warning_sent) \
| table rule_name, awareness_time, early_warning_sent, notification_72h, hours_to_early, hours_to_72
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.23
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.33
action.uc_compliance.param.regulation = nis2

[UC-22.2.34 — NIS2 Cross-Border Incident Coordination Task Tracking]
description = NIS2 Cross-Border Incident Coordination Task Tracking | UC 22.2.34 | regulation=nis2 | criticality=high | clauses=Art.23(3)
search = | inputlookup nis2_cross_border_incident_register.csv \
| join incident_id [ \
    search index=itsm sourcetype="snow:sc_task" short_description="*CSIRT*" earliest=-90d \
    | stats dc(short_description) as tasks_created sum(eval(case(state!="Closed",1,0))) as open_tasks by parent_incident \
    | rename parent_incident as incident_id \
] \
| eval expected_tasks=affected_member_states*2 \
| where open_tasks > 0 OR tasks_created < expected_tasks \
| table incident_id, affected_member_states, tasks_created, open_tasks, expected_tasks
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.23(3)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.34
action.uc_compliance.param.regulation = nis2

[UC-22.2.35 — NIS2 Supervisory Compliance Evidence Pack Readiness]
description = NIS2 Supervisory Compliance Evidence Pack Readiness | UC 22.2.35 | regulation=nis2 | criticality=high | clauses=Art.32-33
search = | inputlookup nis2_evidence_report_catalog.csv \
| join savedsearch_name [ \
    search index=_audit action=search earliest=-7d \
    | stats latest(total_run_time) as last_runtime latest(info) as result by savedsearch_name \
] \
| where isnull(last_runtime) OR match(result,"(?i)fail|error|skipped") \
| table savedsearch_name, owner, last_runtime, result
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.32-33
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.35
action.uc_compliance.param.regulation = nis2

[UC-22.2.36 — NIS2 OT Network Segmentation Validation]
description = NIS2 OT Network Segmentation Validation | UC 22.2.36 | regulation=nis2 | criticality=critical | clauses=Art.21(2)(a)
search = index=netfw earliest=-24h \
| lookup nis2_ot_segment_policy.csv src_zone dest_zone OUTPUT allowed \
| where allowed="false" \
| stats sum(bytes) as bytes count as sessions by src_zone dest_zone dest_port app \
| where sessions > 10 \
| sort - bytes \
| table src_zone, dest_zone, dest_port, app, bytes, sessions
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(a)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.36
action.uc_compliance.param.regulation = nis2

[UC-22.2.37 — NIS2 SCADA System Access Monitoring]
description = NIS2 SCADA System Access Monitoring | UC 22.2.37 | regulation=nis2 | criticality=critical | clauses=Art.21(2)(a)
search = (index=win_sec EventCode=4624 Logon_Type=10 OR Logon_Type=2) OR sourcetype="cyberark:session" earliest=-7d \
| eval host=coalesce(dest_nt_host, ComputerName, target_host) \
| lookup nis2_scada_asset_inventory.csv host OUTPUT scada_role plant_site \
| where scada_role IN ("HMI","ENG_WORKSTATION","SCADA_SERVER") \
| join host [ \
    | inputlookup nis2_scada_maintenance_windows.csv \
    | eval starttime=strptime(start,"%Y-%m-%d %H:%M:%S") \
    | eval endtime=strptime(end,"%Y-%m-%d %H:%M:%S") \
] \
| where _time < starttime OR _time > endtime \
| stats values(user) as users count by host plant_site \
| sort - count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(a)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.37
action.uc_compliance.param.regulation = nis2

[UC-22.2.38 — NIS2 Industrial Control System Patching and Change Evidence]
description = NIS2 Industrial Control System Patching and Change Evidence | UC 22.2.38 | regulation=nis2 | criticality=high | clauses=Art.21(2)(e)
search = index=ot_changes earliest=-90d \
| stats latest(firmware_version) as observed_fw latest(_time) as last_change by asset_id \
| lookup nis2_ot_patch_policy.csv asset_id OUTPUT required_fw_version last_approved_change_epoch \
| eval fw_compliant=if(observed_fw=required_fw_version,1,0) \
| eval change_stale=if(last_change < last_approved_change_epoch,1,0) \
| where fw_compliant=0 OR change_stale=1 \
| table asset_id, observed_fw, required_fw_version, last_change, change_stale
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(e)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.38
action.uc_compliance.param.regulation = nis2

[UC-22.2.39 — NIS2 OT Incident Detection — Process and Protocol Anomalies]
description = NIS2 OT Incident Detection — Process and Protocol Anomalies | UC 22.2.39 | regulation=nis2 | criticality=critical | clauses=Art.21(2)(f)
search = index=ot_protocol sourcetype="modbus:session" earliest=-7d \
| stats count dc(function_code) as fn_variety avg(register_span) as avg_span by plc_name cell_line \
| lookup nis2_ot_baseline.csv cell_line OUTPUT expected_fn_variety expected_span \
| eval anomaly=if(fn_variety > expected_fn_variety*2 OR avg_span > expected_span*3, 1, 0) \
| where anomaly=1 \
| table _time, plc_name, cell_line, fn_variety, expected_fn_variety, avg_span, expected_span
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(f)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.39
action.uc_compliance.param.regulation = nis2

[UC-22.2.4 — NIS2 Business Continuity and Crisis Management Monitoring]
description = NIS2 Business Continuity and Crisis Management Monitoring | UC 22.2.4 | regulation=nis2 | criticality=critical | clauses=Art.21(2)(c)
search = index=itsi_summary is_service_in_maintenance=0 earliest=-24h \
| eval rto_rpo_risk=if(severity_value>=3 OR health_score<70, 1, 0) \
| stats avg(health_score) as avg_health, \
        count(eval(rto_rpo_risk=1)) as breach_events \
    by service_name, kpi_name \
| where breach_events>0 OR avg_health<85 \
| sort - breach_events \
| table service_name, kpi_name, avg_health, breach_events
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(c)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.4
action.uc_compliance.param.regulation = nis2

[UC-22.2.40 — NIS2 Safety System Integrity Monitoring (SIL / SIS Interlocks)]
description = NIS2 Safety System Integrity Monitoring (SIL / SIS Interlocks) | UC 22.2.40 | regulation=nis2 | criticality=critical | clauses=Art.21(2)(c)
search = index=sis_audit earliest=-30d \
| lookup nis2_sis_critical_tags.csv tag_id OUTPUT sil_level plant_area \
| where match(lower(event_type),"(?i)bypass|force|override|interlock.disable") \
| join tag_id [ \
    search index=itsm sourcetype="snow:sc_task" short_description="*permit*" earliest=-30d \
    | stats latest(state) as permit_state by cmdb_ci \
    | rename cmdb_ci as tag_id \
] \
| where permit_state!="Closed" OR isnull(permit_state) \
| stats count by tag_id sil_level plant_area permit_state \
| sort - count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(c)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.40
action.uc_compliance.param.regulation = nis2

[UC-22.2.41 — NIS2 Management Body Cybersecurity Training Evidence]
description = NIS2 Management Body Cybersecurity Training Evidence | UC 22.2.41 | regulation=nis2 | criticality=high | clauses=Art.20
search = | inputlookup nis2_management_roster.csv \
| join person_id [ \
    search index=training course_code="NIS2_EXEC" earliest=-730d \
    | stats latest(completion_date) as last_completion by person_id \
] \
| eval days_since=if(isnotnull(last_completion), round((now()-strptime(last_completion,"%Y-%m-%d"))/86400,0), 9999) \
| where days_since > 365 OR isnull(last_completion) \
| table person_id, role, last_completion, days_since
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.20
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.41
action.uc_compliance.param.regulation = nis2

[UC-22.2.42 — NIS2 Board-Level Cyber Risk Reporting Distribution Audit]
description = NIS2 Board-Level Cyber Risk Reporting Distribution Audit | UC 22.2.42 | regulation=nis2 | criticality=high | clauses=Art.20
search = | inputlookup nis2_board_report_schedule.csv \
| join report_name [ \
    search index=_audit action=search earliest=-90d \
    | where match(info, "(?i)sendemail|pdf") \
    | stats latest(_time) as last_sent by report_name \
] \
| eval expected_cadence_days=case(frequency="monthly",35,frequency="quarterly",100,1=1,10) \
| eval days_since=round((now()-last_sent)/86400,0) \
| where days_since > expected_cadence_days OR isnull(last_sent) \
| table report_name, frequency, last_sent, days_since
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.20
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.42
action.uc_compliance.param.regulation = nis2

[UC-22.2.43 — NIS2 Annual Security Assessment Completion Tracking]
description = NIS2 Annual Security Assessment Completion Tracking | UC 22.2.43 | regulation=nis2 | criticality=high | clauses=Art.21(2)(f)
search = | inputlookup nis2_annual_assessment_register.csv \
| eval due_epoch=strptime(next_assessment_due,"%Y-%m-%d") \
| eval overdue=if(now()>due_epoch,1,0) \
| join legal_entity_id [ \
    search index=itsm short_description="*NIS2*annual*assessment*" earliest=-400d \
    | stats latest(closed_at) as last_closed by company \
    | rename company as legal_entity_id \
] \
| where overdue=1 \
| table legal_entity_id, assessment_type, next_assessment_due, last_closed, overdue
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(f)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.43
action.uc_compliance.param.regulation = nis2

[UC-22.2.44 — NIS2 Cooperation Group and Sector Information Sharing Participation]
description = NIS2 Cooperation Group and Sector Information Sharing Participation | UC 22.2.44 | regulation=nis2 | criticality=high | clauses=Art.14
search = | inputlookup nis2_cooperation_group_register.csv \
| join group_id [ \
    search index=itsm short_description="*ISAC*" OR short_description="*cooperation group*" earliest=-180d \
    | stats latest(state) as last_meeting_state latest(closed_at) as last_meeting_date by assignment_group \
    | rename assignment_group as group_id \
] \
| eval participation_gap=if(last_meeting_state!="Closed" OR isnull(last_meeting_date),1,0) \
| join group_id [ \
    search index=threat_intel earliest=-30d \
    | stats dc(indicator_id) as indicators_consumed by feed_name \
    | rename feed_name as group_id \
] \
| where participation_gap=1 OR indicators_consumed < 1000 \
| table group_id, last_meeting_state, last_meeting_date, indicators_consumed, participation_gap
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.14
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.44
action.uc_compliance.param.regulation = nis2

[UC-22.2.45 — NIS2 CSIRT Notification Compliance and Channel Health]
description = NIS2 CSIRT Notification Compliance and Channel Health | UC 22.2.45 | regulation=nis2 | criticality=critical | clauses=Art.23
search = index=reg_notify sourcetype="csirt:delivery" earliest=-90d \
| lookup nis2_csirt_endpoint_catalog.csv csirt_id OUTPUT required_template_version \
| eval template_ok=if(template_version=required_template_version,1,0) \
| eval delivery_ok=if(match(lower(delivery_status),"success|accepted|202"),1,0) \
| where delivery_ok=0 OR template_ok=0 \
| stats count by csirt_id delivery_status template_version required_template_version \
| sort - count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.23
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.45
action.uc_compliance.param.regulation = nis2

[UC-22.2.5 — NIS2 Network and Information Systems Access Control Audit]
description = NIS2 Network and Information Systems Access Control Audit | UC 22.2.5 | regulation=nis2 | criticality=critical | clauses=Art.21(2)(i)
search = index=windows sourcetype="WinEventLog:Security" EventCode IN (4624, 4625, 4672) earliest=-24h \
| eval auth_result=case(EventCode=4624,"success", EventCode=4625,"failure", EventCode=4672,"special_privileges", 1=1,"other") \
| eval after_hours=if(tonumber(strftime(_time,"%H"))<7 OR tonumber(strftime(_time,"%H"))>19, 1, 0) \
| stats count by EventCode, auth_result, Account_Name, dest, Logon_Type, after_hours \
| sort -count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(i)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.5
action.uc_compliance.param.regulation = nis2

[UC-22.2.6 — NIS2 Risk Analysis and Information System Security Policy Evidence]
description = NIS2 Risk Analysis and Information System Security Policy Evidence | UC 22.2.6 | regulation=nis2 | criticality=critical | clauses=Art.21(2)(a)
search = index=risk sourcetype="stash" earliest=-30d@d \
| stats latest(risk_score) as current_risk, max(risk_score) as peak_risk, dc(source) as contributing_detections by risk_object, risk_object_type \
| lookup asset_lookup_by_str key AS risk_object OUTPUT category, priority, owner \
| fillnull value="UNASSIGNED" owner category \
| where owner="UNASSIGNED" OR current_risk > 50 \
| sort - current_risk \
| table risk_object, risk_object_type, category, owner, current_risk, peak_risk, contributing_detections
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(a)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.6
action.uc_compliance.param.regulation = nis2

[UC-22.2.7 — NIS2 72-Hour Incident Notification Readiness]
description = NIS2 72-Hour Incident Notification Readiness | UC 22.2.7 | regulation=nis2 | criticality=critical | clauses=Art.23(2)
search = `notable` urgency IN ("high","critical") earliest=-7d \
| eval hours_elapsed=round((now()-_time)/3600, 2) \
| eval has_ioc=if(isnotnull(src) AND isnotnull(dest) AND isnotnull(signature), 1, 0) \
| eval has_severity_assessment=if(isnotnull(status_description) AND len(status_description)>20, 1, 0) \
| eval filing_72h_breach=if(hours_elapsed>72 AND status!="Closed" AND (has_ioc=0 OR has_severity_assessment=0), 1, 0) \
| eval approaching_72h=if(hours_elapsed>=48 AND hours_elapsed<72 AND (has_ioc=0 OR has_severity_assessment=0), 1, 0) \
| where filing_72h_breach=1 OR approaching_72h=1 \
| table _time, rule_name, urgency, status, owner, hours_elapsed, has_ioc, has_severity_assessment, filing_72h_breach, approaching_72h \
| sort - filing_72h_breach, - hours_elapsed
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.23(2)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.7
action.uc_compliance.param.regulation = nis2

[UC-22.2.8 — NIS2 One-Month Final Incident Report Tracking]
description = NIS2 One-Month Final Incident Report Tracking | UC 22.2.8 | regulation=nis2 | criticality=high | clauses=Art.23(4)
search = `notable` urgency IN ("high","critical") status="Closed" earliest=-60d \
| eval days_since_close=round((now()-_time)/86400, 1) \
| eval final_report_due=if(days_since_close>=30, 1, 0) \
| eval final_report_approaching=if(days_since_close>=21 AND days_since_close<30, 1, 0) \
| lookup pir_completion_lookup notable_id AS event_id OUTPUT pir_status, pir_date, root_cause_documented \
| fillnull value="NOT_SUBMITTED" pir_status \
| where (final_report_due=1 AND pir_status!="Complete") OR final_report_approaching=1 \
| table _time, rule_name, urgency, owner, days_since_close, pir_status, root_cause_documented, final_report_due \
| sort - final_report_due, - days_since_close
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.23(4)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.8
action.uc_compliance.param.regulation = nis2

[UC-22.2.9 — NIS2 Effectiveness Assessment of Cybersecurity Measures]
description = NIS2 Effectiveness Assessment of Cybersecurity Measures | UC 22.2.9 | regulation=nis2 | criticality=high | clauses=Art.21(2)(f)
search = | makeresults \
| eval measure="MFA_Coverage" \
| append [ \
    search index=_internal sourcetype=splunk_web_access earliest=-24h \
    | stats dc(user) as total_users dc(eval(if(match(_raw,"(?i)mfa|2fa|totp"),user,null()))) as mfa_users \
    | eval measure="MFA_Coverage", pct=round(100*mfa_users/total_users,1), target=95 \
] \
| append [ \
    search index=vulnerability sourcetype="tenable:vuln" state="Active" earliest=-30d \
    | eval sla_days=case(severity="Critical",7, severity="High",30, 1=1,90) \
    | eval age_days=round((now()-first_found)/86400,1) \
    | eval in_sla=if(age_days<=sla_days,1,0) \
    | stats avg(in_sla) as pct_raw \
    | eval measure="Patch_SLA_Compliance", pct=round(pct_raw*100,1), target=90 \
] \
| append [ \
    search index=itsi_summary is_service_in_maintenance=0 earliest=-7d \
    | stats avg(eval(if(health_score>=70,1,0))) as pct_raw by service_name \
    | stats avg(pct_raw) as pct_raw \
    | eval measure="Service_Availability", pct=round(pct_raw*100,1), target=99 \
] \
| where isnotnull(pct) \
| eval status=if(pct>=target,"PASS","FAIL") \
| table measure, pct, target, status
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(f)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.2.9
action.uc_compliance.param.regulation = nis2

[UC-22.3.42 — DORA Art.7 — ICT systems inventory completeness: unmanaged endpoints attached to financial services]
description = DORA Art.7 — ICT systems inventory completeness: unmanaged endpoints attached to financial services | UC 22.3.42 | regulation=nis2 | criticality=high | clauses=Art.21(2)(d)
search = | tstats summariesonly=t count FROM datamodel=Authentication WHERE Authentication.app IN ("payments","trading","settlement") BY Authentication.dest \
| rename Authentication.dest AS hostname \
| lookup financial_services_servers.csv hostname OUTPUT asset_id owner team \
| where isnull(asset_id) \
| eval inventory_gap_reason="not in ICT asset inventory" \
| table hostname inventory_gap_reason count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(d)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.3.42
action.uc_compliance.param.regulation = nis2

[UC-22.3.44 — DORA Art.17 — ICT incident classification timeliness: major-incident clock evidence]
description = DORA Art.17 — ICT incident classification timeliness: major-incident clock evidence | UC 22.3.44 | regulation=nis2 | criticality=critical | clauses=Art.23
search = index=soar sourcetype IN (servicenow:sir_incident,phantom:incident) tag::major_incident=true earliest=-1d \
| stats min(detected_at) AS detected_at min(classified_at) AS classified_at min(notified_at) AS notified_at min(intermediate_at) AS intermediate_at min(final_at) AS final_at BY incident_id \
| eval detect_to_classify_h=round((classified_at-detected_at)/3600,2) \
| eval classify_to_initial_h=round((notified_at-classified_at)/3600,2) \
| eval initial_to_inter_h=round((intermediate_at-notified_at)/3600,2) \
| eval inter_to_final_h=round((final_at-intermediate_at)/3600,2) \
| eval SLA_breached=if(detect_to_classify_h>24 OR classify_to_initial_h>24 OR initial_to_inter_h>72 OR inter_to_final_h>720,1,0) \
| table incident_id detected_at classified_at notified_at intermediate_at final_at detect_to_classify_h classify_to_initial_h initial_to_inter_h inter_to_final_h SLA_breached
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.23
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.3.44
action.uc_compliance.param.regulation = nis2

[UC-22.39.1 — Multi-regulator breach-notification SLA tracker (24h NIS2 / 72h GDPR / 72h HIPAA)]
description = Multi-regulator breach-notification SLA tracker (24h NIS2 / 72h GDPR / 72h HIPAA) | UC 22.39.1 | regulation=nis2 | criticality=critical | clauses=Art.23
search = | inputlookup incident_register.csv \
| where classification="breach" \
| eval applicable_regs=split(applicable_regs,",") \
| mvexpand applicable_regs \
| lookup sla_catalog.csv regulator as applicable_regs OUTPUT sla_hours \
| eval deadline=strptime(classified_at,"%Y-%m-%dT%H:%M:%SZ")+sla_hours*3600 \
| join incident_id applicable_regs [search index=notifications sourcetype=regulator:submission | fields incident_id applicable_regs tracking_id submitted_at] \
| eval status=case(isnull(tracking_id) AND now()>deadline, "breach", isnull(tracking_id) AND (deadline-now())<5400, "imminent", isnotnull(tracking_id), "submitted") \
| table incident_id applicable_regs deadline submitted_at status
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.23
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.39.1
action.uc_compliance.param.regulation = nis2

[UC-22.39.2 — Regulator-portal submission evidence — one-way API acknowledgement audit]
description = Regulator-portal submission evidence — one-way API acknowledgement audit | UC 22.39.2 | regulation=nis2 | criticality=high | clauses=Art.23
search = index=notifications sourcetype=regulator:submission earliest=-72h \
| stats values(tracking_id) as tracking_id min(_time) as submitted by incident_id regulator \
| join type=outer incident_id regulator [search index=notifications sourcetype=regulator:ack earliest=-72h | stats values(tracking_id) as ack_tracking_id max(_time) as acked by incident_id regulator] \
| where isnull(ack_tracking_id) OR (acked-submitted)>900 \
| table incident_id regulator tracking_id submitted acked
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.23
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.39.2
action.uc_compliance.param.regulation = nis2

[UC-22.39.4 — Cross-regulator consistency — divergent material facts across submissions]
description = Cross-regulator consistency — divergent material facts across submissions | UC 22.39.4 | regulation=nis2 | criticality=high | clauses=Art.23(4)
search = index=notifications sourcetype=regulator:submission earliest=-30d \
| stats values(affected_subjects) as subjects_set values(data_classes) as data_set dc(affected_subjects) as subjects_uniques dc(data_classes) as data_uniques by incident_id \
| where subjects_uniques>1 OR data_uniques>1 \
| table incident_id subjects_set data_set
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.23(4)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.39.4
action.uc_compliance.param.regulation = nis2

[UC-22.39.5 — Regulator-portal authentication failure during submission window]
description = Regulator-portal authentication failure during submission window | UC 22.39.5 | regulation=nis2 | criticality=high | clauses=Art.23(1)
search = index=idp sourcetype=okta:audit OR sourcetype=azuread:audit earliest=-15m result=failure \
| search user IN (`regulator_service_accounts`) \
| join user [| inputlookup incident_register.csv | where status="open" | fields user=regulator_user incident_id] \
| table _time user incident_id src_ip failure_reason
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.23(1)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.39.5
action.uc_compliance.param.regulation = nis2

[UC-22.41.2 — Certificate / TLS posture — weak cipher and expired-cert detection]
description = Certificate / TLS posture — weak cipher and expired-cert detection | UC 22.41.2 | regulation=nis2 | criticality=high | clauses=Art.21(2)(h)
search = index=tls sourcetype=tls:scan earliest=-1h \
| eval days_to_expiry=round((expires-now())/86400,0) \
| where days_to_expiry<21 OR protocol!="TLSv1.2" AND protocol!="TLSv1.3" OR NOT match(cipher, "^TLS_(AES|CHACHA20|ECDHE)") \
| table host days_to_expiry protocol cipher
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(h)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.41.2
action.uc_compliance.param.regulation = nis2

[UC-22.43.1 — Critical vulnerability SLA tracker — unpatched 30+ days with exploited-in-the-wild indicator]
description = Critical vulnerability SLA tracker — unpatched 30+ days with exploited-in-the-wild indicator | UC 22.43.1 | regulation=nis2 | criticality=critical | clauses=Art.21(2)(e)
search = index=vuln sourcetype=tenable:vm OR sourcetype=qualys:vm OR sourcetype=rapid7:vm earliest=-60d cvss_v3_base>=9.0 \
| stats min(_time) as first_seen max(_time) as last_seen values(cve) as cves by host \
| eval age_days=round((now()-first_seen)/86400,0) \
| where age_days>30 \
| join type=outer host cves [search index=vuln action=remediated] \
| where isnull(remediated_at) \
| table host cves first_seen age_days
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(e)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.43.1
action.uc_compliance.param.regulation = nis2

[UC-22.43.3 — Internet-facing asset × unpatched critical CVE]
description = Internet-facing asset × unpatched critical CVE | UC 22.43.3 | regulation=nis2 | criticality=critical | clauses=Art.21(2)(e)
search = index=vuln earliest=-24h cvss_v3_base>=9.0 \
| lookup external_asset_inventory.csv host OUTPUT external_exposed \
| where external_exposed="true" \
| stats min(_time) as first_seen values(cve) as cves by host \
| eval age_days=round((now()-first_seen)/86400,0) \
| where age_days>0 \
| table host cves age_days
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(e)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.43.3
action.uc_compliance.param.regulation = nis2

[UC-22.43.5 — SBOM vendor-component CVE exposure]
description = SBOM vendor-component CVE exposure | UC 22.43.5 | regulation=nis2 | criticality=high | clauses=Art.21(2)(d)
search = | inputlookup vendor_sbom.csv \
| lookup cve_feed.csv component version OUTPUT cve cvss_v3_base \
| where isnotnull(cve) AND cvss_v3_base>=7.0 \
| stats values(cve) as cves, max(cvss_v3_base) as max_cvss by vendor component version \
| table vendor component version cves max_cvss
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(d)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.43.5
action.uc_compliance.param.regulation = nis2

[UC-22.44.1 — Supplier attestation currency — stale SOC 2 / ISO 27001 reports for critical vendors]
description = Supplier attestation currency — stale SOC 2 / ISO 27001 reports for critical vendors | UC 22.44.1 | regulation=nis2 | criticality=high | clauses=Art.21(2)(d)
search = | inputlookup vrm_attestations.csv \
| where criticality IN ("critical","high") \
| eval days_to_expiry=round((strptime(expires,"%Y-%m-%d")-now())/86400,0) \
| where days_to_expiry<=30 \
| table vendor attestation_type expires days_to_expiry
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(d)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.44.1
action.uc_compliance.param.regulation = nis2

[UC-22.44.4 — Vendor access telemetry — principals active outside contracted hours/geos]
description = Vendor access telemetry — principals active outside contracted hours/geos | UC 22.44.4 | regulation=nis2 | criticality=high | clauses=Art.21(2)(d)
search = index=idp earliest=-1h (sourcetype=okta:audit OR sourcetype=azuread:audit) result=success \
| search user IN (`vendor_principals`) \
| `get_geolocation(src_ip)` \
| lookup vendor_contract.csv user OUTPUT allowed_hours allowed_countries \
| eval hour=strftime(_time,"%H"), weekday=strftime(_time,"%A"), window_ok=if(match(allowed_hours,weekday.":".hour),1,0), geo_ok=if(match(allowed_countries,country),1,0) \
| where window_ok=0 OR geo_ok=0 \
| table _time user country hour weekday allowed_countries allowed_hours
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(d)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.44.4
action.uc_compliance.param.regulation = nis2

[UC-22.44.5 — SBOM attestation completeness — critical vendors without signed SBOM]
description = SBOM attestation completeness — critical vendors without signed SBOM | UC 22.44.5 | regulation=nis2 | criticality=high | clauses=Art.21(2)(d)
search = | inputlookup vrm_attestations.csv \
| where criticality="critical" \
| lookup vendor_sbom_attestations.csv vendor OUTPUT sbom_sha256 signed signed_by signed_at \
| eval age_days=round((now()-strptime(signed_at,"%Y-%m-%d"))/86400,0) \
| where isnull(sbom_sha256) OR signed!="true" OR age_days>180 \
| table vendor criticality sbom_sha256 signed signed_by age_days
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(d)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.44.5
action.uc_compliance.param.regulation = nis2

[UC-22.45.5 — Business-continuity rehearsal evidence — BCP/DR exercise execution logged]
description = Business-continuity rehearsal evidence — BCP/DR exercise execution logged | UC 22.45.5 | regulation=nis2 | criticality=medium | clauses=Art.21(2)(c)
search = | inputlookup business_units.csv \
| lookup bcp_rehearsals.csv bu_id OUTPUT last_rehearsal_at signed_by \
| eval days_since=round((now()-strptime(last_rehearsal_at,"%Y-%m-%d"))/86400,0) \
| where isnull(last_rehearsal_at) OR days_since>cadence_days \
| table bu_id owner last_rehearsal_at days_since cadence_days
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 3
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(c)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.45.5
action.uc_compliance.param.regulation = nis2

[UC-22.46.1 — Mandatory security training — completion SLA by role]
description = Mandatory security training — completion SLA by role | UC 22.46.1 | regulation=nis2 | criticality=medium | clauses=Art.21(2)(g)
search = index=lms sourcetype=completion earliest=-365d course_id=security_awareness \
| stats max(_time) as last_completion by employee_id \
| lookup hr_roster.csv employee_id OUTPUT role status \
| where status="active" AND (isnull(last_completion) OR (now()-last_completion)>31536000) \
| table employee_id role last_completion
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 3
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(g)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.46.1
action.uc_compliance.param.regulation = nis2

[UC-22.46.2 — Phishing simulation efficacy — click-rate trend and repeat-clicker detection]
description = Phishing simulation efficacy — click-rate trend and repeat-clicker detection | UC 22.46.2 | regulation=nis2 | criticality=medium | clauses=Art.21(2)(g)
search = index=phishing sourcetype=knowbe4:campaign OR sourcetype=proofpoint:psa earliest=-180d \
| bin _time span=30d \
| stats count(eval(action="clicked")) as clicks count as total by _time \
| eval click_rate=round(clicks/total*100,2) \
| stats last(click_rate) as current avg(click_rate) as baseline \
| eval drift=current-baseline \
| where drift>3 \
| appendpipe [search index=phishing action=clicked earliest=-180d | stats dc(campaign_id) as clicks by employee | where clicks>=3]
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 3
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(g)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.46.2
action.uc_compliance.param.regulation = nis2

[UC-22.46.4 — Tabletop rehearsal evidence — IR plan exercise frequency]
description = Tabletop rehearsal evidence — IR plan exercise frequency | UC 22.46.4 | regulation=nis2 | criticality=medium | clauses=Art.21(2)(b)
search = | inputlookup ir_playbooks.csv \
| lookup ir_rehearsals.csv playbook_id OUTPUT last_rehearsal signed_by \
| eval days_since=round((now()-strptime(last_rehearsal,"%Y-%m-%d"))/86400,0) \
| where isnull(last_rehearsal) OR days_since>cadence_days \
| table playbook_id owner last_rehearsal days_since cadence_days
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 3
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21(2)(b)
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.46.4
action.uc_compliance.param.regulation = nis2

[UC-22.9.4 — Regulatory Incident Response Time Trending]
description = Regulatory Incident Response Time Trending | UC 22.9.4 | regulation=nis2 | criticality=medium | clauses=Art.23
search = `notable` earliest=-730d (tag="compliance" OR category="*compliance*" OR rule_name="*regulatory*" OR like(rule_name,"%compliance%")) \
| eval mttr_sec=if(isnotnull(closed_time) AND closed_time>_time, closed_time-_time, null()) \
| where isnotnull(mttr_sec) \
| timechart span=90d avg(mttr_sec) as avg_mttr_sec perc95(mttr_sec) as p95_mttr_sec \
| eval avg_mttr_h=avg_mttr_sec/3600 \
| trendline sma2(avg_mttr_sec) as mttr_trend \
| streamstats window=2 global=f first(avg_mttr_sec) as prev_q_mttr \
| eval vs_prev_q_pct=if(isnotnull(prev_q_mttr) AND prev_q_mttr>0,round(100*(avg_mttr_sec-prev_q_mttr)/prev_q_mttr,1),null()) \
| predict avg_mttr_sec as mttr_forecast algorithm=LLP future_timespan=2 period=4
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 3
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.23
action.uc_compliance.param.versions = NIS2@Directive (EU) 2022/2555
action.uc_compliance.param.uc_id = 22.9.4
action.uc_compliance.param.regulation = nis2
