# -----------------------------------------------------------------
# GENERATED by scripts/generate_splunk_app.py — DO NOT EDIT BY HAND.
# Source of truth: use-cases/cat-*/uc-*.json + data/regulations.json.
# Re-run `python3 scripts/generate_splunk_app.py` after content edits.
# -----------------------------------------------------------------

[UC-22.1.16 — GDPR Consent Withdrawal Processing Enforcement]
description = GDPR Consent Withdrawal Processing Enforcement | UC 22.1.16 | regulation=uk-gdpr | criticality=high | clauses=Art.18
search = | inputlookup gdpr_consent_withdrawals.csv WHERE withdrawal_date!="" \
| eval withdrawal_epoch=strptime(withdrawal_date, "%Y-%m-%d") \
| eval subject_hash=subject_identifier \
| join type=left subject_hash [ \
    search (index=email OR index=marketing) earliest=-30d \
    | eval subject_hash=coalesce(recipient_hash, subscriber_id, user_hash) \
    | where isnotnull(subject_hash) \
    | stats count as post_withdrawal_events, latest(_time) as last_processing_time by subject_hash \
] \
| where isnotnull(post_withdrawal_events) AND last_processing_time > withdrawal_epoch \
| eval days_processing_after_withdrawal=round((last_processing_time - withdrawal_epoch)/86400, 1) \
| sort - days_processing_after_withdrawal \
| table subject_hash, withdrawal_date, consent_purpose, post_withdrawal_events, days_processing_after_withdrawal
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.18
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.1.16
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.1.2 — GDPR Data Subject Access Request Fulfillment Tracking]
description = GDPR Data Subject Access Request Fulfillment Tracking | UC 22.1.2 | regulation=uk-gdpr | criticality=critical | clauses=Art.16
search = index=itsm (sourcetype="snow:sc_req_item" OR sourcetype="snow:incident") \
    (cat_item="*Subject Access*" OR short_description="*DSAR*" OR short_description="*data subject*") \
| eval opened_epoch=strptime(opened_at, "%Y-%m-%d %H:%M:%S") \
| eval closed_epoch=if(isnotnull(closed_at), strptime(closed_at, "%Y-%m-%d %H:%M:%S"), null()) \
| eval age_days=round((now()-opened_epoch)/86400, 1) \
| eval sla_met=if(isnotnull(closed_epoch) AND (closed_epoch-opened_epoch)<=2592000, "Met", "Missed") \
| eval open_breach=if(isnull(closed_epoch) AND age_days>30, "Open_SLA_Breach", null()) \
| table _time, number, sourcetype, state, age_days, sla_met, open_breach, short_description \
| sort - age_days
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.16
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.1.2
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.1.46 — GDPR Consent Mechanism Audit (Lawful Basis Alignment)]
description = GDPR Consent Mechanism Audit (Lawful Basis Alignment) | UC 22.1.46 | regulation=uk-gdpr | criticality=high | clauses=Art.21
search = (index=web sourcetype="js_tag") earliest=-24h \
| eval before_consent=if(consent_state="unknown" AND match(tag_category,"(?i)marketing|analytics"),1,0) \
| stats sum(before_consent) as early_nonessential_tags by site_id tag_vendor \
| where early_nonessential_tags > 100 \
| lookup gdpr_consent_policy.csv site_id OUTPUT required_banner_version \
| join site_id [ \
    search index=consent earliest=-24h \
    | stats latest(banner_version) as live_banner by site_id \
] \
| where live_banner != required_banner_version \
| table site_id, tag_vendor, early_nonessential_tags, live_banner, required_banner_version
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.21
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.1.46
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.35.1 — Audit-log continuity: detect indexing gap indicating lost evidence]
description = Audit-log continuity: detect indexing gap indicating lost evidence | UC 22.35.1 | regulation=uk-gdpr | criticality=critical | clauses=Art.32(1)(b)
search = | tstats count as events where index=_internal source=*metrics.log* group=per_sourcetype_thruput earliest=-30m by sourcetype _time span=1m \
| eventstats avg(events) as avg_events, stdev(events) as stdev_events by sourcetype \
| where events=0 AND avg_events>0 \
| eval gap_seconds=60 \
| table _time sourcetype events avg_events stdev_events gap_seconds \
| sort - _time
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.32(1)(b)
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.35.1
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.35.2 — Log tamper detection via write-once-read-many chain-of-custody]
description = Log tamper detection via write-once-read-many chain-of-custody | UC 22.35.2 | regulation=uk-gdpr | criticality=critical | clauses=Art.32
search = | rest /services/cluster/master/buckets splunk_server=* | eval primary_hash=coalesce('primaries{}.bucket_flags.hash','bucket_hash') | stats values(primary_hash) as hashes dc(primary_hash) as uniques by index, bucket_id | where uniques>1 \
| eval divergence_detected_at=strftime(now(),"%Y-%m-%dT%H:%M:%SZ") \
| table divergence_detected_at index bucket_id hashes
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.32
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.35.2
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.35.3 — Indexer replication lag exposing evidence to single-point failure]
description = Indexer replication lag exposing evidence to single-point failure | UC 22.35.3 | regulation=uk-gdpr | criticality=high | clauses=Art.32
search = | rest /services/cluster/master/indexes splunk_server=* | stats max(replication_factor_met) as rf_met max(search_factor_met) as sf_met by title | eval index=title, rf_met=coalesce(rf_met,0), sf_met=coalesce(sf_met,0) | where rf_met=0 OR sf_met=0
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.32
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.35.3
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.35.4 — Log signing chain integrity — cryptographic signature drift on evidence archive]
description = Log signing chain integrity — cryptographic signature drift on evidence archive | UC 22.35.4 | regulation=uk-gdpr | criticality=critical | clauses=Art.32(1)(b)
search = index=compliance sourcetype=archive:signer earliest=-1h \
| eval block_parent_hash=coalesce(parent_hash,"GENESIS") \
| stats values(block_hash) as block_hashes latest(expected_parent_hash) as expected_parent by block_id archive_id \
| where expected_parent!=block_parent_hash AND block_parent_hash!="GENESIS" \
| table archive_id block_id block_parent_hash expected_parent block_hashes
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.32(1)(b)
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.35.4
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.36.1 — DSAR fulfillment SLA tracker with verification evidence trail]
description = DSAR fulfillment SLA tracker with verification evidence trail | UC 22.36.1 | regulation=uk-gdpr | criticality=high | clauses=Art.12, Art.15
search = index=dsar sourcetype=onetrust:dsar OR sourcetype=servicenow:privacy earliest=-90d \
| stats min(_time) as received max(eval(case(status="verified",_time))) as verified max(eval(case(status="fulfilled",_time))) as fulfilled by ticket_id subject_token \
| eval days_open=round((coalesce(fulfilled,now())-received)/86400,1), sla_breach=if(isnull(fulfilled) AND days_open>25,1,0) \
| where sla_breach=1 \
| table ticket_id subject_token received verified days_open
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.12,Art.15
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.36.1
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.36.2 — Right-to-erasure propagation completeness across downstream systems]
description = Right-to-erasure propagation completeness across downstream systems | UC 22.36.2 | regulation=uk-gdpr | criticality=critical | clauses=Art.17, Art.17(2)
search = index=dsar subject_token=* action=erasure earliest=-180d \
| stats values(system) as acked_systems by subject_token ticket_id \
| lookup ropa_system_map.csv ticket_id OUTPUT systems_in_scope \
| eval missing=mvfilter(NOT match(acked_systems, system)) \
| where mvcount(missing)>0 \
| table ticket_id subject_token systems_in_scope acked_systems missing
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.17,Art.17(2)
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.36.2
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.36.3 — Portability export integrity — signed manifest verification]
description = Portability export integrity — signed manifest verification | UC 22.36.3 | regulation=uk-gdpr | criticality=medium | clauses=Art.20
search = index=dsar sourcetype=portability:export action=delivered earliest=-30d \
| join export_id [search index=dsar sourcetype=portability:manifest | fields export_id, manifest_hash] \
| where delivered_hash!=manifest_hash \
| table _time export_id subject_token delivered_hash manifest_hash
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 3
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.20
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.36.3
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.36.4 — DSAR identity-verification friction — failed-verification anomaly]
description = DSAR identity-verification friction — failed-verification anomaly | UC 22.36.4 | regulation=uk-gdpr | criticality=high | clauses=Art.12(6)
search = index=dsar sourcetype=onetrust:dsar OR sourcetype=servicenow:privacy action=verification earliest=-14d \
| eval pass=if(result="success",1,0), fail=if(result="failure",1,0) \
| stats sum(pass) as passes sum(fail) as failures values(ticket_id) as ticket_ids by subject_token \
| where failures>=3 AND passes<1 \
| table subject_token failures ticket_ids
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.12(6)
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.36.4
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.37.1 — Consent capture evidence freshness — stale-consent alerting]
description = Consent capture evidence freshness — stale-consent alerting | UC 22.37.1 | regulation=uk-gdpr | criticality=high | clauses=Art.6, Art.7
search = index=cmp sourcetype=onetrust:consent earliest=-400d \
| stats max(_time) as last_consent by subject_token purpose \
| eval consent_age_days=round((now()-last_consent)/86400,0) \
| where consent_age_days>365 \
| join subject_token [search index=app_activity earliest=-30d | stats count by subject_token | where count>0] \
| table subject_token purpose last_consent consent_age_days
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.6,Art.7
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.37.1
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.37.2 — Consent withdrawal propagation SLA — downstream stop-processing evidence]
description = Consent withdrawal propagation SLA — downstream stop-processing evidence | UC 22.37.2 | regulation=uk-gdpr | criticality=critical | clauses=Art.7
search = index=cmp action=withdraw earliest=-7d \
| join subject_token [search index=app_activity (lawful_basis=consent) earliest=-7d] \
| where _time>=withdrawal_time + 86400 \
| stats values(system) as systems_still_processing count by subject_token withdrawal_time \
| where count>0 \
| table withdrawal_time subject_token systems_still_processing count
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.7
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.37.2
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.37.4 — Purpose-limitation enforcement — processing not matching declared purpose]
description = Purpose-limitation enforcement — processing not matching declared purpose | UC 22.37.4 | regulation=uk-gdpr | criticality=high | clauses=Art.5(1)(b)
search = index=app_activity earliest=-1h token_purpose=* \
| lookup purpose_catalogue.csv system_id OUTPUT allowed_purposes \
| eval ok=if(match(allowed_purposes,token_purpose),1,0) \
| where ok=0 \
| stats count values(user) as users values(subject_token) as subjects by system_id token_purpose allowed_purposes \
| table system_id token_purpose allowed_purposes count users subjects
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.5(1)(b)
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.37.4
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.37.5 — IAB TCF consent string mutation without user interaction]
description = IAB TCF consent string mutation without user interaction | UC 22.37.5 | regulation=uk-gdpr | criticality=high | clauses=Art.7(1)
search = index=web sourcetype=access_combined earliest=-1h euconsent_v2=* \
| sort 0 session_id _time \
| streamstats current=f last(euconsent_v2) as prior_consent by session_id \
| where isnotnull(prior_consent) AND euconsent_v2!=prior_consent \
| join type=outer session_id _time [search index=cmp sourcetype=onetrust:cmp action=interaction earliest=-1h | fields session_id _time | rename _time as interaction_time] \
| where isnull(interaction_time) OR abs(_time-interaction_time)>60 \
| table _time session_id prior_consent euconsent_v2
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.7(1)
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.37.5
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.38.1 — Cross-border personal-data flow anomaly — egress to unsanctioned jurisdictions]
description = Cross-border personal-data flow anomaly — egress to unsanctioned jurisdictions | UC 22.38.1 | regulation=uk-gdpr | criticality=critical | clauses=Art.44, Art.46
search = | tstats summariesonly=t count from datamodel=Network_Traffic.All_Traffic where All_Traffic.tag=egress earliest=-1h by All_Traffic.dest, All_Traffic.src, All_Traffic.bytes_out \
| `get_geolocation(dest)` \
| lookup sanctioned_transfer_jurisdictions.csv country as dest_country OUTPUT scc_active adequacy_decision \
| where isnull(scc_active) AND isnull(adequacy_decision) \
| join src [search index=dlp tag=pii | stats values(classification) as pii_class by src] \
| table _time src dest dest_country pii_class bytes_out
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.44,Art.46
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.38.1
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.38.2 — SCC / adequacy decision reference freshness — stale-safeguard detector]
description = SCC / adequacy decision reference freshness — stale-safeguard detector | UC 22.38.2 | regulation=uk-gdpr | criticality=medium | clauses=Art.45, Art.46
search = | inputlookup transfer_register.csv \
| lookup scc_reference.csv template_id OUTPUT template_active effective_from superseded_by \
| eval age_days=round((now()-strptime(effective_from,"%Y-%m-%d"))/86400,0) \
| where NOT template_active OR age_days>1095 OR isnotnull(superseded_by) \
| table transfer_id destination template_id template_active age_days superseded_by
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 3
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.45,Art.46
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.38.2
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.38.3 — Data localization enforcement — regulated-data must-stay-in-region]
description = Data localization enforcement — regulated-data must-stay-in-region | UC 22.38.3 | regulation=uk-gdpr | criticality=high | clauses=Art.44
search = index=cloud_storage sourcetype=aws:s3:objectaccess OR sourcetype=azure:blob:access earliest=-1h \
| lookup data_classification.csv key=object_key OUTPUT classification regulated_scope \
| lookup localization_policy.csv regulated_scope OUTPUT allowed_regions \
| where isnotnull(regulated_scope) AND NOT match(bucket_region, allowed_regions) \
| table _time bucket bucket_region object_key classification regulated_scope allowed_regions
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.44
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.38.3
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.38.4 — Transfer Impact Assessment currency — stale Schrems II assessments]
description = Transfer Impact Assessment currency — stale Schrems II assessments | UC 22.38.4 | regulation=uk-gdpr | criticality=medium | clauses=Art.46
search = | inputlookup tia_register.csv \
| eval age_days=round((now()-strptime(last_reviewed,"%Y-%m-%d"))/86400,0) \
| where age_days>cadence_days \
| lookup transfer_register.csv tia_id OUTPUT transfer_id destination \
| table tia_id destination last_reviewed age_days cadence_days transfer_id
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 3
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.46
action.uc_compliance.param.versions = UK GDPR@post-Brexit,UK-GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.38.4
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.38.5 — Bulk regulated-data export targeting non-adequate jurisdiction]
description = Bulk regulated-data export targeting non-adequate jurisdiction | UC 22.38.5 | regulation=uk-gdpr | criticality=critical | clauses=Art.44, Art.45
search = index=db_audit action=export OR (index=cloud_storage action=PutObject bytes>1073741824) earliest=-10m \
| lookup data_classification.csv object_key OUTPUT classification regulated_scope \
| where isnotnull(regulated_scope) \
| `get_geolocation(dest_ip)` \
| lookup sanctioned_transfer_jurisdictions.csv country as dest_country OUTPUT scc_active adequacy_decision \
| where isnull(scc_active) AND isnull(adequacy_decision) \
| table _time user object_key classification regulated_scope dest_country bytes
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.44,Art.45
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.38.5
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.39.1 — Multi-regulator breach-notification SLA tracker (24h NIS2 / 72h GDPR / 72h HIPAA)]
description = Multi-regulator breach-notification SLA tracker (24h NIS2 / 72h GDPR / 72h HIPAA) | UC 22.39.1 | regulation=uk-gdpr | criticality=critical | clauses=Art.33
search = | inputlookup incident_register.csv \
| where classification="breach" \
| eval applicable_regs=split(applicable_regs,",") \
| mvexpand applicable_regs \
| lookup sla_catalog.csv regulator as applicable_regs OUTPUT sla_hours \
| eval deadline=strptime(classified_at,"%Y-%m-%dT%H:%M:%SZ")+sla_hours*3600 \
| join incident_id applicable_regs [search index=notifications sourcetype=regulator:submission | fields incident_id applicable_regs tracking_id submitted_at] \
| eval status=case(isnull(tracking_id) AND now()>deadline, "breach", isnull(tracking_id) AND (deadline-now())<5400, "imminent", isnotnull(tracking_id), "submitted") \
| table incident_id applicable_regs deadline submitted_at status
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.33
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.39.1
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.39.2 — Regulator-portal submission evidence — one-way API acknowledgement audit]
description = Regulator-portal submission evidence — one-way API acknowledgement audit | UC 22.39.2 | regulation=uk-gdpr | criticality=high | clauses=Art.33
search = index=notifications sourcetype=regulator:submission earliest=-72h \
| stats values(tracking_id) as tracking_id min(_time) as submitted by incident_id regulator \
| join type=outer incident_id regulator [search index=notifications sourcetype=regulator:ack earliest=-72h | stats values(tracking_id) as ack_tracking_id max(_time) as acked by incident_id regulator] \
| where isnull(ack_tracking_id) OR (acked-submitted)>900 \
| table incident_id regulator tracking_id submitted acked
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.33
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.39.2
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.39.3 — Data-subject breach communication timeline tracker (Art.34 / §164.404)]
description = Data-subject breach communication timeline tracker (Art.34 / §164.404) | UC 22.39.3 | regulation=uk-gdpr | criticality=high | clauses=Art.34
search = index=comms sourcetype=breach:notification earliest=-90d \
| stats count(eval(status="delivered")) as delivered count(eval(status="bounced_handled")) as fallback count as total by campaign_id \
| eval ok=(delivered+fallback)/total \
| lookup breach_campaigns.csv campaign_id OUTPUT classification classified_at \
| eval age_days=round((now()-strptime(classified_at,"%Y-%m-%dT%H:%M:%SZ"))/86400,0) \
| where age_days>=50 AND ok<0.95 \
| table campaign_id classified_at age_days total delivered fallback ok
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.34
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.39.3
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.39.4 — Cross-regulator consistency — divergent material facts across submissions]
description = Cross-regulator consistency — divergent material facts across submissions | UC 22.39.4 | regulation=uk-gdpr | criticality=high | clauses=Art.33(3)
search = index=notifications sourcetype=regulator:submission earliest=-30d \
| stats values(affected_subjects) as subjects_set values(data_classes) as data_set dc(affected_subjects) as subjects_uniques dc(data_classes) as data_uniques by incident_id \
| where subjects_uniques>1 OR data_uniques>1 \
| table incident_id subjects_set data_set
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.33(3)
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.39.4
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.41.1 — Encryption-at-rest coverage gap — unencrypted storage with regulated data]
description = Encryption-at-rest coverage gap — unencrypted storage with regulated data | UC 22.41.1 | regulation=uk-gdpr | criticality=critical | clauses=Art.32
search = | rest /services/cloud_storage/inventory splunk_server=* | where regulated_data="true" \
| stats values(encryption_state) as state by volume_id region tenant \
| where mvfind(state, "enabled")<0 \
| lookup data_classification.csv volume_id OUTPUT classification \
| table tenant region volume_id classification state
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 5
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.32
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.41.1
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.44.2 — Subprocessor inventory change — notification SLA to data controllers]
description = Subprocessor inventory change — notification SLA to data controllers | UC 22.44.2 | regulation=uk-gdpr | criticality=high | clauses=Art.28
search = index=vrm sourcetype=subprocessor:change earliest=-60d \
| join change_id [search index=vrm sourcetype=controller:notification | stats values(controller_id) as controllers_notified by change_id] \
| lookup subprocessor_scope.csv change_id OUTPUT controllers_in_scope notice_days \
| eval days_since=round((now()-_time)/86400,0), missing=mvfilter(NOT match(controllers_notified, controller_id)) \
| where days_since>=notice_days AND mvcount(missing)>0 \
| table change_id vendor controllers_in_scope controllers_notified missing
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.28
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.44.2
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.46.5 — Developer data-handling training — prod-access engineers lacking training]
description = Developer data-handling training — prod-access engineers lacking training | UC 22.46.5 | regulation=uk-gdpr | criticality=high | clauses=Art.32(4)
search = | rest /services/identity/prod-access-groups splunk_server=* \
| fields user group scope \
| where scope="regulated" \
| join user [search index=lms sourcetype=completion course_id=data_handling_v* earliest=-365d | stats max(_time) as last_completion by user] \
| where (now()-last_completion)>31536000 OR isnull(last_completion) \
| table user group scope last_completion
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.32(4)
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.46.5
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.49.1 — Retention execution evidence — records past retention still present]
description = Retention execution evidence — records past retention still present | UC 22.49.1 | regulation=uk-gdpr | criticality=high | clauses=Art.5
search = index=datastore sourcetype=retention:inventory earliest=-1d \
| lookup retention_policy.csv data_domain OUTPUT retention_period_days \
| eval age_days=round((now()-strptime(created_at,"%Y-%m-%dT%H:%M:%SZ"))/86400,0) \
| where age_days>retention_period_days AND litigation_hold!="true" \
| table store record_id data_domain age_days retention_period_days
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.5
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.49.1
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.49.2 — Disposal workflow completion — failed disposals requiring manual review]
description = Disposal workflow completion — failed disposals requiring manual review | UC 22.49.2 | regulation=uk-gdpr | criticality=high | clauses=Art.5
search = index=datastore sourcetype=disposal:job earliest=-7d \
| stats values(status) as states min(_time) as started max(_time) as last_update by job_id \
| where NOT match(states, "completed") AND (now()-started)>172800 \
| table job_id started last_update states
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.5
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.49.2
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.49.4 — Retention policy drift — system config vs policy catalogue]
description = Retention policy drift — system config vs policy catalogue | UC 22.49.4 | regulation=uk-gdpr | criticality=high | clauses=Art.5(1)(e)
search = index=datastore sourcetype=retention:config earliest=-1d \
| lookup retention_policy.csv data_domain OUTPUT policy_days \
| eval drift_days=abs(configured_days-policy_days) \
| where drift_days>0 \
| table store data_domain configured_days policy_days drift_days
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.5(1)(e)
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.49.4
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.49.5 — Cryptographic erasure attestation — per-asset destruction evidence]
description = Cryptographic erasure attestation — per-asset destruction evidence | UC 22.49.5 | regulation=uk-gdpr | criticality=high | clauses=Art.17
search = index=assets sourcetype=asset:disposal earliest=-7d method=cryptographic_erasure \
| join asset_id [search index=kms action=DeleteKey earliest=-8d | fields asset_id key_id _time | rename _time as key_deleted_at] \
| eval delta=abs(_time-key_deleted_at) \
| where isnull(key_deleted_at) OR delta>86400 \
| table asset_id key_id _time key_deleted_at delta
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 4
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.17
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.49.5
action.uc_compliance.param.regulation = uk-gdpr

[UC-22.8.39 — SOC 2 P1.1 — Privacy notice: consent-record freshness for privacy-notice version changes]
description = SOC 2 P1.1 — Privacy notice: consent-record freshness for privacy-notice version changes | UC 22.8.39 | regulation=uk-gdpr | criticality=medium | clauses=Art.7
search = index=consent sourcetype=onetrust:consent earliest=-14d \
| stats latest(consent_version) AS consent_version latest(_time) AS consent_time BY subject_id \
| lookup privacy_notice_versions.csv AS latest OUTPUT current_version \
| eval days_stale=round((now()-consent_time)/86400,0) \
| eval needs_refresh=if(consent_version!=current_version,1,0) \
| where needs_refresh=1 \
| table subject_id consent_version current_version days_stale needs_refresh
cron_schedule = */15 * * * *
dispatch.earliest_time = -30m@m
dispatch.latest_time = now
enableSched = 0
is_scheduled = 0
disabled = 1
alert.severity = 3
alert_condition = search count > 0
alert.track = 1
alert.suppress = 0
action.email = 0
action.logevent = 0
action.uc_compliance.param.clauses = Art.7
action.uc_compliance.param.versions = UK GDPR@post-Brexit
action.uc_compliance.param.uc_id = 22.8.39
action.uc_compliance.param.regulation = uk-gdpr
