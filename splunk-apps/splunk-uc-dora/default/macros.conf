# -----------------------------------------------------------------
# GENERATED by scripts/generate_splunk_app.py — DO NOT EDIT BY HAND.
# Source of truth: use-cases/cat-*/uc-*.json + data/regulations.json.
# Re-run `python3 scripts/generate_splunk_app.py` after content edits.
# -----------------------------------------------------------------

[uc_compliance_app]
definition = eventtype="uc_compliance_dora_uncategorised"
iseval = 0
description = Convenience macro: expands to the catch-all eventtype used by the dora compliance app.  Override to chain with your own filters.

[uc_compliance_window(1)]
args = lookback
definition = earliest=-$lookback$@m latest=now
iseval = 0
description = Parameterised lookback.  `uc_compliance_window(30m)` yields `earliest=-30m@m latest=now`.
