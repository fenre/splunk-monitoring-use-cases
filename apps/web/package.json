{
  "name": "@splunk-uc/web",
  "private": true,
  "version": "0.1.0-scaffold",
  "type": "module",
  "description": "Frontend rebuild scaffold for the Splunk Monitoring Use Cases catalog. See docs/adr/0013-frontend-rebuild-scaffold.md. The current production UI is the monolithic index.html at the repo root; this app is the future home for incremental migration (F16, F17).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "emit:legacy": "tsx scripts/emit-legacy.ts"
  },
  "devDependencies": {
    "tsx": "^4.22.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.13",
    "vitest": "^4.1.6"
  }
}
