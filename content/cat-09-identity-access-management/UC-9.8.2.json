{
  "$schema": "../../schemas/uc.schema.json",
  "id": "9.8.2",
  "title": "BeyondTrust Password Safe Credential Checkout and Check-In Cycle Time",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Operations",
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'BeyondTrust Password Safe Credential Checkout and Check-In Cycle Time' only. Does not replace full SIEM correlation, IdP cloud-native analytics, or regulatory attestation. Entities listed in `iam_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `iam_beyondtrust_password_safe_credential_checkout_an` (UC-9.8.2), SOC dashboard panel \"IAM — BeyondTrust Password Safe Credential Checkout and Check-In Cycle Time\", and weekly export to `index=audit_evidence` sourcetype=csv:iam_controls.",
  "controlTest": {
    "positiveScenario": "UC-9.8.2 positive (BeyondTrust, Password, Safe, Credential, Checkout): ingest synthetic events into `index=pam` sourcetype='beyondtrust:vault' where `checkout_ts`, `checkin_ts` grouped by `user`, `acct`, `sess` satisfies `hours_open` > 4 for 'BeyondTrust Password Safe Credential Checkout and Check-In Cycle Time'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-9.8.2 negative (Cycle Time): ingest benign `pam` / `beyondtrust:vault` events where `user`, `acct`, `sess` stays below `hours_open` > 4, or exclude the test entity via `iam_exception_register.csv`. Variant 7 confirms zero alert rows for 'BeyondTrust Password Safe Credential Checkout and Check-In Cycle Time'."
  },
  "dataSources": "`sourcetype=beyondtrust:vault` Password Safe checkout, check-in, and heartbeat events Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Add-on for BeyondTrust Password Safe or syslog/HEC parser to `beyondtrust:vault` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=pam sourcetype=\"beyondtrust:vault\" earliest=-7d\n| eval evt=lower(coalesce(event_type, EventType, action, Action, \"\"))\n| eval acct=coalesce(account_name, AccountName, credential, \"\")\n| eval user=coalesce(user, UserName, requester, \"\")\n| eval sess=coalesce(session_id, SessionID, checkout_id, CheckoutID, \"\")\n| eval t_checkout=if(match(evt,\"checkout\"),_time,null())\n| eval t_checkin=if(match(evt,\"checkin|check.in|return\"),_time,null())\n| stats min(t_checkout) as checkout_ts max(t_checkin) as checkin_ts by user acct sess\n| eval hours_open=round((checkin_ts-checkout_ts)/3600,2)\n| where hours_open>4 OR isnull(checkin_ts)\n| table checkout_ts checkin_ts user acct hours_open",
  "description": "Surfaces UC-9.8.2 — BeyondTrust, Password, Safe, Credential, Checkout — using `index=pam` sourcetype `beyondtrust:vault`, grouped by `user`, `acct`, `sess`. Outputs `checkout_ts`, `checkin_ts` each `the scheduled window`; alert when `hours_open` > 4. Primary focus: Cycle Time. Purpose-built for 'BeyondTrust Password Safe Credential Checkout and Check-In Cycle Time' in IAM workflows.",
  "value": "UC-9.8.2 operationalises 'BeyondTrust Password Safe Credential Checkout and Check-In Cycle Time' against `beyondtrust:vault` feeds so operators reduce account takeover risk, help-desk load, and audit findings without manual correlation.",
  "implementation": "(1) Align `event_type` strings to checkout and check-in pairs. (2) If `transaction` is heavy, pre-build session IDs in the TA. (3) Set max checkout SLA (for example four hours) by asset tier via lookup. (4) Auto-expire in Password Safe where supported. (5) Feed outliers to line managers monthly.",
  "visualization": "Histogram (duration distribution), table (longest checkouts), timeline.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw directory, IdP, PAM, or device feeds.",
  "references": [
    {
      "title": "BeyondTrust — Password Safe",
      "url": "https://www.beyondtrust.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Docs — transaction",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/Transaction",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `pam` (UC-9.8.2)** — Planned work for 'BeyondTrust Password Safe Credential Checkout and Check-In Cycle Time' shifts `checkout_ts`, `checkin_ts` over `span=1h` while `user`, `acct`, `sess` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `min` on `beyondtrust:vault`** — Scheduled automation elevates `acct` without crossing `hours_open` > 4 for UC-9.8.2. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `user`, `acct`, `sess`** — Health checks in `pam` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `beyondtrust:vault`** — First 48h baseline for UC-9.8.2 on `user` is unstable; window via `iam_exception_register.csv`.\n\n**Suppression mechanism:** `iam_exception_register.csv` keyed on `user`, `exception_reason`, `valid_until`.",
  "securityDomain": "access",
  "equipment": [
    "beyondtrust",
    "hashicorp",
    "syslog"
  ],
  "equipmentModels": [
    "hashicorp_vault"
  ],
  "status": "verified",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for BeyondTrust Password Safe or syslog/HEC parser to `beyondtrust:vault` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=beyondtrust:vault into pam. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`acct, hours_open, sess, t_checkin`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use identity and sign-in data in Splunk so we can notice unusual logins, access changes, and privileged use while it still matters — BeyondTrust Password Safe Credential Checkout and Check-In Cycle Time",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
