{
  "$schema": "../../schemas/uc.schema.json",
  "id": "9.8.4",
  "title": "BeyondTrust Privilege Elevation Request Denial Rate Monitoring",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Security",
    "Operations"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'BeyondTrust Privilege Elevation Request Denial Rate Monitoring' only. Does not replace full SIEM correlation, IdP cloud-native analytics, or regulatory attestation. Entities listed in `iam_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `iam_beyondtrust_privilege_elevation_request_denial_r` (UC-9.8.4), SOC dashboard panel \"IAM — BeyondTrust Privilege Elevation Request Denial Rate Monitoring\", and weekly export to `index=audit_evidence` sourcetype=csv:iam_controls.",
  "controlTest": {
    "positiveScenario": "UC-9.8.4 positive (BeyondTrust, Privilege, Elevation, Request, Denial): ingest synthetic events into `index=pam` sourcetype='beyondtrust:pam' where `count` grouped by `entity` satisfies `total` >= 10, or `deny_pct` > 50 for 'BeyondTrust Privilege Elevation Request Denial Rate Monitoring'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-9.8.4 negative (Rate Monitoring): ingest benign `pam` / `beyondtrust:pam` events where `entity` stays below `total` >= 10, or `deny_pct` > 50, or exclude the test entity via `iam_exception_register.csv`. Variant 4 confirms zero alert rows for 'BeyondTrust Privilege Elevation Request Denial Rate Monitoring'."
  },
  "dataSources": "`sourcetype=beyondtrust:pam` elevation, run-as, or approval workflow events with granted/denied outcomes Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Add-on for BeyondTrust Privilege Management / PRA mapped to `beyondtrust:pam` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=pam sourcetype=\"beyondtrust:pam\" earliest=-30d@d\n| eval res=coalesce(result, Result, outcome, Outcome, status, \"\")\n| eval user=coalesce(user, UserName, requester, \"\")\n| eval app=coalesce(application, Application, target_app, \"\")\n| bin _time span=1d\n| stats count(eval(match(lower(res),\"(?i)denied|reject|fail\"))) as denied count as total by _time user\n| eval deny_pct=round(100*denied/total,2)\n| where total>=10 AND deny_pct>50\n| sort -deny_pct",
  "description": "Monitors risk tied to BeyondTrust, Privilege, Elevation, Request, Denial through UC-9.8.4 on `index=pam` sourcetype `beyondtrust:pam`. Summarises `count` grouped by `entity` across `1d` before applying `total` >= 10, or `deny_pct` > 50. Operators tune 'BeyondTrust Privilege Elevation Request Denial Rate Monitoring' using UC-9.8.4 runbook steps.",
  "value": "Tracking `activity` via UC-9.8.4 on `beyondtrust:pam` feeds improves response to 'BeyondTrust Privilege Elevation Request Denial Rate Monitoring' and reduces account takeover risk, help-desk load, and audit findings.",
  "implementation": "(1) Standardize result field values across Windows and Mac agents. (2) Exclude known service accounts. (3) Baseline denial rates by team. (4) Cross-check with IdP risky sign-ins. (5) Offer self-service training when denial spikes follow application upgrades.",
  "visualization": "Line chart (denial % over time), bar chart (top users), heatmap (user × day).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw directory, IdP, PAM, or device feeds.",
  "references": [
    {
      "title": "BeyondTrust — Endpoint Privilege Management",
      "url": "https://www.beyondtrust.com/endpoint-privilege-management",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Docs — eval",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/Eval",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `pam` (UC-9.8.4)** — Planned work for 'BeyondTrust Privilege Elevation Request Denial Rate Monitoring' shifts `count` over `span=1d` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `beyondtrust:pam`** — Scheduled automation elevates `res` without crossing `total` >= 10 for UC-9.8.4. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `pam` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `beyondtrust:pam`** — First 48h baseline for UC-9.8.4 on `host` is unstable; window via `iam_exception_register.csv`.\n\n**Suppression mechanism:** `iam_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "securityDomain": "identity",
  "equipment": [
    "beyondtrust"
  ],
  "equipmentModels": [
    "beyondtrust_generic"
  ],
  "status": "verified",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for BeyondTrust Privilege Management / PRA mapped to `beyondtrust:pam` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=beyondtrust:pam into pam. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`deny_pct, total`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use identity and sign-in data in Splunk so we can notice unusual logins, access changes, and privileged use while it still matters — BeyondTrust Privilege Elevation Request Denial Rate Monitoring",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
