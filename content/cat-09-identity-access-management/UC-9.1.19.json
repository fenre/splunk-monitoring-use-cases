{
  "$schema": "../../schemas/uc.schema.json",
  "id": "9.1.19",
  "title": "LAPS Password Rotation Failures",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault",
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'LAPS Password Rotation Failures' only. Does not replace full SIEM correlation, IdP cloud-native analytics, or regulatory attestation. Entities listed in `iam_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `iam_laps_password_rotation_failures` (UC-9.1.19), SOC dashboard panel \"IAM — LAPS Password Rotation Failures\", and weekly export to `index=audit_evidence` sourcetype=csv:iam_controls.",
  "controlTest": {
    "positiveScenario": "UC-9.1.19 positive (LAPS, Password, Rotation, Failures): ingest synthetic events into `index=wineventlog` sourcetype='WinEventLog:Microsoft-Windows-LAPS/Operational' where `count` grouped by `ComputerName`, `EventCode`, `Message` satisfies `count` > 0 for 'LAPS Password Rotation Failures'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-9.1.19 negative (Rotation Failures): ingest benign `wineventlog` / `WinEventLog:Microsoft-Windows-LAPS/Operational` events where `ComputerName`, `EventCode`, `Message` stays below `count` > 0, or exclude the test entity via `iam_exception_register.csv`. Variant 4 confirms zero alert rows for 'LAPS Password Rotation Failures'."
  },
  "dataSources": "Operational log `Microsoft-Windows-LAPS/Operational` (Event IDs 10023, 10024, 10025, 10026), or legacy CSE events Collected via Splunk Add-on for Microsoft Windows (Splunkbase 742) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_windows` — Splunk Add-on for Microsoft Windows (Splunkbase 742)",
  "spl": "index=wineventlog sourcetype=\"WinEventLog:Microsoft-Windows-LAPS/Operational\" EventCode IN (10023,10024,10025,10026)\n| stats count by ComputerName, EventCode, Message\n| where count > 0\n| sort -count",
  "description": "Alerts on risk tied to LAPS, Password, Rotation, Failures through UC-9.1.19 on `index=wineventlog` sourcetype `WinEventLog:Microsoft-Windows-LAPS/Operational`. Summarises `count` grouped by `ComputerName`, `EventCode`, `Message` across `the scheduled window` before applying `count` > 0. Operators tune 'LAPS Password Rotation Failures' using UC-9.1.19 runbook steps.",
  "value": "Tracking `activity` via UC-9.1.19 on `WinEventLog:Microsoft-Windows-LAPS/Operational` feeds improves response to 'LAPS Password Rotation Failures' and reduces account takeover risk, help-desk load, and audit findings.",
  "implementation": "Forward LAPS Operational log from all domain-joined clients that use LAPS. Map Event IDs to rotation success/failure. Alert on repeated failures per OU or GPO scope. Correlate with GPO and network issues.",
  "visualization": "Table (hosts with failures), Bar chart (failures by OU), Single value (failed rotations 24h).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw directory, IdP, PAM, or device feeds.",
  "references": [
    {
      "title": "Splunk_TA_windows",
      "url": "https://splunkbase.splunk.com/app/742",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `wineventlog` (UC-9.1.19)** — Planned work for 'LAPS Password Rotation Failures' shifts `count` over `span=1h` while `ComputerName`, `EventCode`, `Message` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `WinEventLog:Microsoft-Windows-LAPS/Operational`** — Scheduled automation elevates `EventCode` without crossing `count` > 0 for UC-9.1.19. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `ComputerName`, `EventCode`, `Message`** — Health checks in `wineventlog` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `WinEventLog:Microsoft-Windows-LAPS/Operational`** — First 48h baseline for UC-9.1.19 on `ComputerName` is unstable; window via `iam_exception_register.csv`.\n\n**Suppression mechanism:** `iam_exception_register.csv` keyed on `ComputerName`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1552"
  ],
  "equipment": [
    "windows"
  ],
  "equipmentModels": [
    "windows_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_windows`.\n• Ensure the following data sources are available: Operational log `Microsoft-Windows-LAPS/Operational` (Event IDs 10023, 10024, 10025, 10026), or legacy CSE events.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nForward LAPS Operational log from all domain-joined clients that use LAPS. Map Event IDs to rotation success/failure. Alert on repeated failures per OU or GPO scope. Correlate with GPO and network issues.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=wineventlog sourcetype=\"WinEventLog:Microsoft-Windows-LAPS/Operational\" EventCode IN (10023,10024,10025,10026)\n| stats count by ComputerName, EventCode, Message\n| where count > 0\n| sort -count\n```\n\nUnderstanding this SPL\n\n**LAPS Password Rotation Failures** — Failed LAPS rotations leave predictable local admin passwords; attackers target stale LAPS attributes.\n\nDocumented **Data sources**: Operational log `Microsoft-Windows-LAPS/Operational` (Event IDs 10023, 10024, 10025, 10026), or legacy CSE events. **App/TA** (typical add-on context): `Splunk_TA_windows`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: wineventlog; **sourcetype**: WinEventLog:Microsoft-Windows-LAPS/Operational. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=wineventlog, sourcetype=\"WinEventLog:Microsoft-Windows-LAPS/Operational\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by ComputerName, EventCode, Message** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where count > 0` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nCompare with Event Viewer on domain controllers (or exported Security logs) and with Active Directory Users and Computers for the same objects and time window.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (hosts with failures), Bar chart (failures by OU), Single value (failed rotations 24h).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_windows` — Splunk Add-on for Microsoft Windows (Splunkbase 742)). Enable the modular input or HEC token that writes sourcetype=WinEventLog:Microsoft-Windows-LAPS into wineventlog. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`ComputerName, EventCode, Message`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use identity and sign-in data in Splunk so we can notice unusual logins, access changes, and privileged use while it still matters — LAPS Password Rotation Failures",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
