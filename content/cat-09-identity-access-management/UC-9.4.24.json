{
  "$schema": "../../schemas/uc.schema.json",
  "id": "9.4.24",
  "title": "BeyondTrust Privileged Sessions Started Outside Business Hours",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'BeyondTrust Privileged Sessions Started Outside Business Hours' only. Does not replace full SIEM correlation, IdP cloud-native analytics, or regulatory attestation. Entities listed in `iam_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `iam_beyondtrust_privileged_sessions_started_outside_` (UC-9.4.24), SOC dashboard panel \"IAM — BeyondTrust Privileged Sessions Started Outside Business Hours\", and weekly export to `index=audit_evidence` sourcetype=csv:iam_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=pam` sourcetype='beyondtrust:session' where `user` satisfies `hour` < 7 for UC-9.4.24 ('BeyondTrust Privileged Sessions Started Outside Business Hours'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `pam` / `beyondtrust:session` events where `user` stays below `hour` < 7, or exclude the test entity via `iam_exception_register.csv` with a valid window. Confirm zero alert rows for UC-9.4.24."
  },
  "dataSources": "`sourcetype=beyondtrust:session` (or normalized PAM session events forwarded from BeyondTrust) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "BeyondTrust TA / Splunk CIM Email & Authentication patterns (org deployment) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=pam sourcetype=\"beyondtrust:session\" earliest=-7d\n| eval user=coalesce(user, UserName, admin_user, \"\")\n| eval hour=strftime(_time, \"%H\")\n| eval dow=strftime(_time, \"%u\")\n| where (hour<7 OR hour>=19) OR dow IN (\"6\",\"7\")\n| stats count values(target_host) as targets by user\n| sort -count",
  "description": "Privileged PAM sessions that begin late night or on weekends warrant review because they align with ransomware operators and rogue administrators evading oversight.",
  "value": "Privileged PAM sessions that begin late night or on weekends warrant review because they align with ransomw.... Timely UC-9.4.24 alerting on 'BeyondTrust Privileged Sessions Started Outside Business Hours' reduces account takeover risk, help-desk load, and audit findings before manual triage or audit channels escalate.",
  "implementation": "Adjust business-hour boundaries and weekend definition for your locale. Map `target_host` field names to your parser. Exclude maintenance accounts via lookup. Pair with on-call schedules to reduce false positives.",
  "visualization": "Table (user × targets), timeline of off-hours sessions, pie (weekday vs weekend).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw directory, IdP, PAM, or device feeds.",
  "references": [
    {
      "title": "BeyondTrust Password Safe / Cloud Dashboard for Splunk",
      "url": "https://splunkbase.splunk.com/app/5574",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Lantern — privileged access monitoring patterns",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `pam`** — Planned work for 'BeyondTrust Privileged Sessions Started Outside Business Hours' shifts `values` over `span=1h` while `user` remains in-scope for UC-9.4.24. Cross-check CMDB CHG.\n\n2. **Batch `values` on `beyondtrust:session`** — Scheduled automation elevates `user` without crossing `hour` < 7. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `user`** — Health checks in `pam` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `beyondtrust:session`** — First 48h baseline for `user` is unstable; window via `iam_exception_register.csv`.\n\n**Suppression mechanism:** `iam_exception_register.csv` keyed on `user`, `exception_reason`, `valid_until`.",
  "equipment": [
    "beyondtrust"
  ],
  "equipmentModels": [
    "beyondtrust_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Install and configure: BeyondTrust TA / Splunk CIM Email & Authentication patterns (org deployment).\n• Data sources: `sourcetype=beyondtrust:session` (or normalized PAM session events forwarded from BeyondTrust).\n\nStep 1 — Configure data collection\nAdjust business-hour boundaries and weekend definition for your locale. Map `target_host` field names to your parser. Exclude maintenance accounts via lookup. Pair with on-call schedules to reduce false positives.\n\nStep 2 — Create the search and alert\n\n```spl\nindex=pam sourcetype=\"beyondtrust:session\" earliest=-7d\n| eval user=coalesce(user, UserName, admin_user, \"\")\n| eval hour=strftime(_time, \"%H\")\n| eval dow=strftime(_time, \"%u\")\n| where (hour<7 OR hour>=19) OR dow IN (\"6\",\"7\")\n| stats count values(target_host) as targets by user\n| sort -count\n```\n\nStep 3 — Validate\nCompare with BeyondTrust session history in the vendor console for the same users, targets, and timestamps.\n\nStep 4 — Operationalize\nAdd to a dashboard or alert; document the owner. Table (user × targets), timeline of off-hours sessions, pie (weekday vs weekend).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (BeyondTrust TA / Splunk CIM Email & Authentication patterns (org deployment) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=beyondtrust:session into pam. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`hour, user`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We flag when someone starts a privileged vault session at odd hours so you can review before off-hours access turns into unnoticed abuse.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
