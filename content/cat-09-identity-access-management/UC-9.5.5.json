{
  "$schema": "../../schemas/uc.schema.json",
  "id": "9.5.5",
  "title": "Okta Policy Modifications",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Okta Policy Modifications' only. Does not replace full SIEM correlation, IdP cloud-native analytics, or regulatory attestation. Entities listed in `iam_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `iam_okta_policy_modifications` (UC-9.5.5), SOC dashboard panel \"IAM — Okta Policy Modifications\", and weekly export to `index=audit_evidence` sourcetype=csv:iam_controls.",
  "controlTest": {
    "positiveScenario": "UC-9.5.5 positive (Okta, Policy, Modifications): ingest synthetic events into `index=okta` sourcetype='OktaIM2:log' where `count` grouped by `actor.alternateId`, `eventType`, `target{}.displayName` satisfies detection threshold for 'Okta Policy Modifications'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-9.5.5 negative (Policy Modifications): ingest benign `okta` / `OktaIM2:log` events where `actor.alternateId`, `eventType`, `target{}.displayName` stays below detection threshold, or exclude the test entity via `iam_exception_register.csv`. Variant 12 confirms zero alert rows for 'Okta Policy Modifications'."
  },
  "dataSources": "`sourcetype=OktaIM2:log` (`policy.*`) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_okta` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=okta sourcetype=\"OktaIM2:log\"\n| where like(eventType,\"policy.lifecycle%\") OR like(eventType,\"policy.rule%\")\n| stats count by actor.alternateId, eventType, target{}.displayName\n| sort -count",
  "description": "Surfaces UC-9.5.5 — Okta, Policy, Modifications — using `index=okta` sourcetype `OktaIM2:log`, grouped by `actor.alternateId`, `eventType`, `target{}.displayName`. Outputs `count` each `the scheduled window`; alert when configured threshold. Primary focus: Policy Modifications. Purpose-built for 'Okta Policy Modifications' in IAM workflows.",
  "value": "Tracking `activity` via UC-9.5.5 on `OktaIM2:log` feeds improves response to 'Okta Policy Modifications' and reduces account takeover risk, help-desk load, and audit findings.",
  "implementation": "Ingest policy lifecycle and rule events. Correlate with change tickets. Alert on any policy change outside maintenance windows or from non-admin service accounts. Snapshot policy names in a lookup for critical resources.",
  "visualization": "Table (policy, actor, target), Timeline (policy changes), Single value (changes in last 24h).",
  "cimModels": [
    "Change"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Change.All_Changes by All_Changes.action, All_Changes.object_category, All_Changes.user | sort - count",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Change` datasets (for example `Change.All_Changes`, `Change.Auditing_Changes`, or `Change.Account_Management` depending on your mapping) once audit and identity events are CIM-tagged; otherwise `cimSpl` tstats may return no rows.",
  "references": [
    {
      "title": "Splunk_TA_okta",
      "url": "https://splunkbase.splunk.com/app/6553",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Change",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Change",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `okta` (UC-9.5.5)** — Planned work for 'Okta Policy Modifications' shifts `count` over `span=1h` while `actor.alternateId`, `eventType`, `target{}.displayName` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `OktaIM2:log`** — Scheduled automation elevates `eventType` without crossing configured threshold for UC-9.5.5. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `actor.alternateId`, `eventType`, `target{}.displayName`** — Health checks in `okta` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `OktaIM2:log`** — First 48h baseline for UC-9.5.5 on `actor.alternateId` is unstable; window via `iam_exception_register.csv`.\n\n**Suppression mechanism:** `iam_exception_register.csv` keyed on `actor.alternateId`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1562.001",
    "T1098.003"
  ],
  "equipment": [
    "okta"
  ],
  "equipmentModels": [
    "okta_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_okta`.\n• Ensure the following data sources are available: `sourcetype=OktaIM2:log` (`policy.*`).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest policy lifecycle and rule events. Correlate with change tickets. Alert on any policy change outside maintenance windows or from non-admin service accounts. Snapshot policy names in a lookup for critical resources.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=okta sourcetype=\"OktaIM2:log\"\n| where like(eventType,\"policy.lifecycle%\") OR like(eventType,\"policy.rule%\")\n| stats count by actor.alternateId, eventType, target{}.displayName\n| sort -count\n```\n\nUnderstanding this SPL\n\n**Okta Policy Modifications** — Sign-on, MFA, and password policy edits can weaken security org-wide; detecting unauthorized or out-of-window changes is essential for governance.\n\nDocumented **Data sources**: `sourcetype=OktaIM2:log` (`policy.*`). **App/TA** (typical add-on context): `Splunk_TA_okta`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: okta; **sourcetype**: OktaIM2:log. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=okta, sourcetype=\"OktaIM2:log\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where like(eventType,\"policy.lifecycle%\") OR like(eventType,\"policy.rule%\")` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by actor.alternateId, eventType, target{}.displayName** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats summariesonly=t count from datamodel=Change.All_Changes by All_Changes.action, All_Changes.object_category, All_Changes.user | sort - count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Okta Policy Modifications** — Sign-on, MFA, and password policy edits can weaken security org-wide; detecting unauthorized or out-of-window changes is essential for governance.\n\nDocumented **Data sources**: `sourcetype=OktaIM2:log` (`policy.*`). **App/TA** (typical add-on context): `Splunk_TA_okta`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Change.All_Changes` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare Splunk results with the Okta admin console and System Log for the same users, outcomes, and time window, then adjust thresholds to normal org traffic.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (policy, actor, target), Timeline (policy changes), Single value (changes in last 24h).",
  "grandmaExplanation": "We use Okta sign-in records so we can see failed logins, risky sessions, and admin changes in one place.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
