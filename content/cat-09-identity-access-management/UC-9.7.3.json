{
  "$schema": "../../schemas/uc.schema.json",
  "id": "9.7.3",
  "title": "Privileged Account Activity Trending",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Privileged Account Activity Trending' only. Does not replace full SIEM correlation, IdP cloud-native analytics, or regulatory attestation. Entities listed in `iam_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `iam_privileged_account_activity_trending` (UC-9.7.3), SOC dashboard panel \"IAM — Privileged Account Activity Trending\", and weekly export to `index=audit_evidence` sourcetype=csv:iam_controls.",
  "controlTest": {
    "positiveScenario": "UC-9.7.3 positive (Privileged, Account, Activity, Trending): ingest synthetic events into `index=authentication` sourcetype='target_sourcetype' where `privileged_logons` grouped by `_time`, `Authentication.user` satisfies -30d for 'Privileged Account Activity Trending'. Validate macro `summariesonly`, lookup `privileged_users`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-9.7.3 negative (Activity Trending): ingest benign `authentication` / `target_sourcetype` events where `_time`, `Authentication.user` stays below -30d, or exclude the test entity via `iam_exception_register.csv`. Variant 10 confirms zero alert rows for 'Privileged Account Activity Trending'."
  },
  "dataSources": "`Authentication` data model; `privileged_users.csv` lookup (user, is_privileged) aligned with `Authentication.user` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Common Information Model (CIM), Splunk Add-on for Microsoft Windows (`Splunk_TA_windows`) — Universal Forwarder (Splunkbase 617)",
  "spl": "| tstats `summariesonly` count\n  from datamodel=Authentication.Authentication\n  where earliest=-30d@d latest=@d Authentication.action='success'\n  by _time span=1d Authentication.user\n| lookup privileged_users user AS \"Authentication.user\" OUTPUT is_privileged\n| where is_privileged=\"true\"\n| timechart span=1d sum(count) as privileged_logons\n| trendline sma7(privileged_logons) as priv_sma7\n| predict privileged_logons as priv_forecast algorithm=LLP future_timespan=7",
  "description": "Detects UC-9.7.3 — Privileged, Account, Activity, Trending — using datamodel `Authentication.Authentication`, grouped by `_time`, `Authentication.user`. Outputs `privileged_logons` each `1d`; alert when -30d. Filter chain includes macro `summariesonly`, lookup `privileged_users`. Purpose-built for 'Privileged Account Activity Trending' in IAM workflows.",
  "value": "For 'Privileged Account Activity Trending', UC-9.7.3 converts `privileged_logons` on `Authentication.Authentication` telemetry into actionable rows and reduces account takeover risk, help-desk load, and audit findings.",
  "implementation": "Build `privileged_users.csv` from Active Directory privileged groups, cloud Global Administrator roles, and break-glass accounts; refresh on a schedule. Require `Authentication.action='success'` to measure real sessions. Investigate sustained upward trends with parallel searches on source IP and workstation. Pair with change tickets to expected elevation work.",
  "visualization": "Line chart of privileged_logons with SMA; anomaly overlay if using MLTK or `anomalydetection`.",
  "cimModels": [
    "Authentication"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Authentication.Authentication by Authentication.user span=1d | sort - count",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Authentication.Authentication` dataset once identity and sign-in data are CIM-tagged; otherwise `cimSpl` tstats may return no rows.",
  "references": [
    {
      "title": "Splunk_TA_windows",
      "url": "https://splunkbase.splunk.com/app/742",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Authentication",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Authentication",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `authentication` (UC-9.7.3)** — Planned work for 'Privileged Account Activity Trending' shifts `privileged_logons` over `span=1d` while `_time`, `Authentication.user` remains in-scope. datamodel=Authentication.Authentication Applies macro `summariesonly`, lookup `privileged_users`. Cross-check CMDB CHG.\n\n2. **Batch `sum` on `configured_sourcetype`** — Scheduled automation elevates `Authentication.user` without crossing configured threshold for UC-9.7.3. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `_time`, `Authentication.user`** — Health checks in `authentication` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-9.7.3 on `_time` is unstable; join `privileged_users` prior to threshold.\n\n**Suppression mechanism:** `iam_exception_register.csv` keyed on `_time`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1078"
  ],
  "equipment": [
    "windows"
  ],
  "equipmentModels": [
    "windows_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Common Information Model (CIM), Splunk Add-on for Microsoft Windows (`Splunk_TA_windows`).\n• Ensure the following data sources are available: `Authentication` data model; `privileged_users.csv` lookup (user, is_privileged) aligned with `Authentication.user`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nBuild `privileged_users.csv` from Active Directory privileged groups, cloud Global Administrator roles, and break-glass accounts; refresh on a schedule. Require `Authentication.action='success'` to measure real sessions. Investigate sustained upward trends with parallel searches on source IP and workstation. Pair with change tickets to expected elevation work.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\n| tstats `summariesonly` count\n  from datamodel=Authentication.Authentication\n  where earliest=-30d@d latest=@d Authentication.action='success'\n  by _time span=1d Authentication.user\n| lookup privileged_users user AS \"Authentication.user\" OUTPUT is_privileged\n| where is_privileged=\"true\"\n| timechart span=1d sum(count) as privileged_logons\n| trendline sma7(privileged_logons) as priv_sma7\n| predict privileged_logons as priv_forecast algorithm=LLP future_timespan=7\n```\n\nUnderstanding this SPL\n\n**Privileged Account Activity Trending** — Privileged logon volume should be relatively stable; spikes can indicate credential theft, mass admin activity during an incident, or automation run amok. Trending over thirty days highlights gradual increases that point-in-time thresholds miss.\n\nDocumented **Data sources**: `Authentication` data model; `privileged_users.csv` lookup (user, is_privileged) aligned with `Authentication.user`. **App/TA** (typical add-on context): Splunk Common Information Model (CIM), Splunk Add-on for Microsoft Windows (`Splunk_TA_windows`). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Authentication.Authentication` — enable acceleration for that model.\n• Enriches events using `lookup` (lookup definition + optional OUTPUT fields).\n• Filters the current rows with `where is_privileged=\"true\"` — typically the threshold or rule expression for this monitoring goal.\n• `timechart` plots the metric over time using **span=1d** buckets — ideal for trending and alerting on this use case.\n• Pipeline stage (see **Privileged Account Activity Trending**): trendline sma7(privileged_logons) as priv_sma7\n• Pipeline stage (see **Privileged Account Activity Trending**): predict privileged_logons as priv_forecast algorithm=LLP future_timespan=7\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats summariesonly=t count from datamodel=Authentication.Authentication by Authentication.user span=1d | sort - count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Privileged Account Activity Trending** — Privileged logon volume should be relatively stable; spikes can indicate credential theft, mass admin activity during an incident, or automation run amok. Trending over thirty days highlights gradual increases that point-in-time thresholds miss.\n\nDocumented **Data sources**: `Authentication` data model; `privileged_users.csv` lookup (user, is_privileged) aligned with `Authentication.user`. **App/TA** (typical add-on context): Splunk Common Information Model (CIM), Splunk Add-on for Microsoft Windows (`Splunk_TA_windows`). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Authentication.Authentication` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare results with the authoritative identity source (directory, IdP, or PAM) for the same time range and with known change or maintenance tickets.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart of privileged_logons with SMA; anomaly overlay if using MLTK or `anomalydetection`.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Common Information Model (CIM), Splunk Add-on for Microsoft Windows (`Splunk_TA_windows`) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into index=main. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`Authentication.user, _time, is_privileged, user`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We track how sign-in and identity activity change over time so we can spot surges, drift, and seasonal norms without guessing.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
