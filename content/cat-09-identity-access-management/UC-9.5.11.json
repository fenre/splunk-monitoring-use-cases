{
  "$schema": "../../schemas/uc.schema.json",
  "id": "9.5.11",
  "title": "Impossible Travel Detection (Okta)",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Impossible Travel Detection (Okta)' only. Does not replace full SIEM correlation, IdP cloud-native analytics, or regulatory attestation. Entities listed in `iam_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `iam_impossible_travel_detection_okta` (UC-9.5.11), SOC dashboard panel \"IAM — Impossible Travel Detection (Okta)\", and weekly export to `index=audit_evidence` sourcetype=csv:iam_controls.",
  "controlTest": {
    "positiveScenario": "UC-9.5.11 positive (Impossible, Travel, Detection, (Okta)): ingest synthetic events into `index=okta` sourcetype='OktaIM2:log' where `count` grouped by `actor.alternateId` satisfies `delta_sec` > 0, or `delta_sec` < 3600 for 'Impossible Travel Detection (Okta)'. Validate eventType `user.authentication.sso`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-9.5.11 negative (Detection (Okta)): ingest benign `okta` / `OktaIM2:log` events where `actor.alternateId` stays below `delta_sec` > 0, or `delta_sec` < 3600, or exclude the test entity via `iam_exception_register.csv`. Variant 10 confirms zero alert rows for 'Impossible Travel Detection (Okta)'."
  },
  "dataSources": "`sourcetype=OktaIM2:log` (`user.session.start`, `user.authentication.sso`) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_okta` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=okta sourcetype=\"OktaIM2:log\" outcome.result=\"SUCCESS\" eventType=\"user.authentication.sso\"\n| sort 0 actor.alternateId _time\n| streamstats window=1 last(client.geographicalContext.country) as prev_country last(_time) as prev_time last(client.ipAddress) as prev_ip current(client.geographicalContext.country) as country by actor.alternateId\n| eval delta_sec=_time-prev_time\n| where delta_sec > 0 AND delta_sec < 3600 AND country!=prev_country AND isnotnull(prev_country)\n| table _time, actor.alternateId, prev_country, country, delta_sec, prev_ip, client.ipAddress",
  "description": "Detects UC-9.5.11 — Impossible, Travel, Detection, (Okta) — using `index=okta` sourcetype `OktaIM2:log`, grouped by `actor.alternateId`. Outputs `count` each `the scheduled window`; alert when `delta_sec` > 0, or `delta_sec` < 3600. Filter chain includes eventType `user.authentication.sso`. Purpose-built for 'Impossible Travel Detection (Okta)' in IAM workflows.",
  "value": "For 'Impossible Travel Detection (Okta)', UC-9.5.11 converts `activity` on `OktaIM2:log` feeds into actionable rows and reduces account takeover risk, help-desk load, and audit findings.",
  "implementation": "Use Okta geo fields (or enrich IP with `iplocation`). Tune minimum distance and maximum time windows. Exclude VPN and satellite egress via ASN lookups. Combine with Okta’s built-in impossible travel if licensed.",
  "visualization": "Table (user, country A → B, delta), Map (sequential points), Single value (impossible travel count per day).",
  "cimModels": [
    "Authentication"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Authentication.Authentication by Authentication.action, Authentication.user, Authentication.src | sort - count",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Authentication.Authentication` dataset once identity and sign-in data are CIM-tagged; otherwise `cimSpl` tstats may return no rows.",
  "references": [
    {
      "title": "Splunk_TA_okta",
      "url": "https://splunkbase.splunk.com/app/6553",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Authentication",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Authentication",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `okta` (UC-9.5.11)** — Planned work for 'Impossible Travel Detection (Okta)' shifts `count` over `span=1h` while `actor.alternateId` remains in-scope. eventType=user.authentication.sso Applies eventType `user.authentication.sso`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `OktaIM2:log`** — Scheduled automation elevates `delta_sec` without crossing `delta_sec` > 0 for UC-9.5.11. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `actor.alternateId`** — Health checks in `okta` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `OktaIM2:log`** — First 48h baseline for UC-9.5.11 on `actor.alternateId` is unstable; window via `iam_exception_register.csv`.\n\n**Suppression mechanism:** `iam_exception_register.csv` keyed on `actor.alternateId`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1078.004"
  ],
  "equipment": [
    "okta"
  ],
  "equipmentModels": [
    "okta_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_okta`.\n• Ensure the following data sources are available: `sourcetype=OktaIM2:log` (`user.session.start`, `user.authentication.sso`).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nUse Okta geo fields (or enrich IP with `iplocation`). Tune minimum distance and maximum time windows. Exclude VPN and satellite egress via ASN lookups. Combine with Okta’s built-in impossible travel if licensed.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=okta sourcetype=\"OktaIM2:log\" outcome.result=\"SUCCESS\" eventType=\"user.authentication.sso\"\n| sort 0 actor.alternateId _time\n| streamstats window=1 last(client.geographicalContext.country) as prev_country last(_time) as prev_time last(client.ipAddress) as prev_ip current(client.geographicalContext.country) as country by actor.alternateId\n| eval delta_sec=_time-prev_time\n| where delta_sec > 0 AND delta_sec < 3600 AND country!=prev_country AND isnotnull(prev_country)\n| table _time, actor.alternateId, prev_country, country, delta_sec, prev_ip, client.ipAddress\n```\n\nUnderstanding this SPL\n\n**Impossible Travel Detection (Okta)** — Two successful sessions from geolocations that cannot be reached in the elapsed time indicate credential theft or shared accounts.\n\nDocumented **Data sources**: `sourcetype=OktaIM2:log` (`user.session.start`, `user.authentication.sso`). **App/TA** (typical add-on context): `Splunk_TA_okta`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: okta; **sourcetype**: OktaIM2:log. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=okta, sourcetype=\"OktaIM2:log\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n• `streamstats` rolls up events into metrics; results are split **by actor.alternateId** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **delta_sec** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where delta_sec > 0 AND delta_sec < 3600 AND country!=prev_country AND isnotnull(prev_country)` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Impossible Travel Detection (Okta)**): table _time, actor.alternateId, prev_country, country, delta_sec, prev_ip, client.ipAddress\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats summariesonly=t count from datamodel=Authentication.Authentication by Authentication.action, Authentication.user, Authentication.src | sort - count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Impossible Travel Detection (Okta)** — Two successful sessions from geolocations that cannot be reached in the elapsed time indicate credential theft or shared accounts.\n\nDocumented **Data sources**: `sourcetype=OktaIM2:log` (`user.session.start`, `user.authentication.sso`). **App/TA** (typical add-on context): `Splunk_TA_okta`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Authentication.Authentication` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare Splunk results with the Okta admin console and System Log for the same users, outcomes, and time window, then adjust thresholds to normal org traffic.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (user, country A → B, delta), Map (sequential points), Single value (impossible travel count per day).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_okta` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=OktaIM2:log into okta. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`actor.alternateId, delta_sec`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We notice sign-ins that do not line up with how fast a person could move so we can catch stolen sessions or account sharing.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
