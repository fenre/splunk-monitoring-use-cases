{
  "$schema": "../../schemas/uc.schema.json",
  "id": "9.4.23",
  "title": "BeyondTrust Password Safe Denied or Failed Access Attempts",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "dataSources": "Password Safe security events forwarded to Splunk (`sourcetype=beyondtrust` or `source=password_safe`)",
  "app": "BeyondTrust Password Safe / Cloud Dashboard for Splunk (Splunkbase 5574)",
  "spl": "index=pam (sourcetype=beyondtrust OR source=password_safe) earliest=-24h\n| eval user=coalesce(UserName, user, User, \"\")\n| eval result=coalesce(Result, Status, Outcome, \"\")\n| where match(lower(result), \"(?i)deny|fail|block|invalid|unauthor\")\n OR match(lower(_raw), \"(?i)denied|failed|authentication failure\")\n| stats count values(result) as outcomes by user, src_ip\n| sort -count",
  "description": "Failed or denied Password Safe operations often precede brute force, policy violations, or attackers probing shared credentials.",
  "value": "Adds vault-layer detection that complements IdP sign-in logs—especially when attackers already possess a password but lack approval workflow.",
  "implementation": "Map `src_ip` when the connector includes it; some deployments require `rex` from `_raw`. Normalize result strings per BeyondTrust documentation. Tune out expected lockouts from scanners. Correlate repeated failures with MFA and SOAR playbooks.",
  "visualization": "Table (user × source IP), bar chart (outcomes), timeline.",
  "references": [
    {
      "title": "BeyondTrust Password Safe / Cloud Dashboard for Splunk",
      "url": "https://splunkbase.splunk.com/app/5574"
    },
    {
      "title": "BeyondTrust Splunk integration",
      "url": "https://docs.beyondtrust.com/bips/docs/bi-splunk-integration"
    }
  ],
  "equipment": [
    "beyondtrust"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases"
}
