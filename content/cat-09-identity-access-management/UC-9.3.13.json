{
  "$schema": "../../schemas/uc.schema.json",
  "id": "9.3.13",
  "title": "App Registration Secret Expiry",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability",
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'App Registration Secret Expiry' only. Does not replace full SIEM correlation, IdP cloud-native analytics, or regulatory attestation. Entities listed in `iam_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `iam_app_registration_secret_expiry` (UC-9.3.13), SOC dashboard panel \"IAM — App Registration Secret Expiry\", and weekly export to `index=audit_evidence` sourcetype=csv:iam_controls.",
  "controlTest": {
    "positiveScenario": "UC-9.3.13 positive (App, Registration, Secret, Expiry): ingest synthetic events into `index=azure` sourcetype='azure:graph:applications' where `count` grouped by `entity` satisfies `days_left` < 30, or `days_left` > 0 for 'App Registration Secret Expiry'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-9.3.13 negative (Secret Expiry): ingest benign `azure` / `azure:graph:applications` events where `entity` stays below `days_left` < 30, or `days_left` > 0, or exclude the test entity via `iam_exception_register.csv`. Variant 12 confirms zero alert rows for 'App Registration Secret Expiry'."
  },
  "dataSources": "Application credential inventory (`passwordCredentials.endDateTime`), audit when secret added Collected via Splunk Add-on for Microsoft Azure (Splunkbase 3110) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_microsoft-cloudservices`, Graph scripted input — Splunk Add-on for Microsoft Azure (Splunkbase 3110)",
  "spl": "index=azure sourcetype=\"azure:graph:applications\"\n| eval days_left=round((strptime(endDateTime,\"%Y-%m-%dT%H:%M:%SZ\")-now())/86400)\n| where days_left < 30 AND days_left > 0\n| table appId, displayName, days_left, endDateTime\n| sort days_left",
  "description": "Surfaces UC-9.3.13 — App, Registration, Secret, Expiry — using `index=azure` sourcetype `azure:graph:applications`, grouped by `entity`. Outputs `count` each `the scheduled window`; alert when `days_left` < 30, or `days_left` > 0. Primary focus: Secret Expiry. Purpose-built for 'App Registration Secret Expiry' in IAM workflows.",
  "value": "Tracking `activity` via UC-9.3.13 on `azure:graph:applications` feeds improves response to 'App Registration Secret Expiry' and reduces account takeover risk, help-desk load, and audit findings.",
  "implementation": "Schedule Graph export of app registrations with secrets/certificates. Alert at 30/14/7 days. Map apps to owners via lookup.",
  "visualization": "Table (expiring secrets), Single value (next expiry), Gauge (apps past due).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw directory, IdP, PAM, or device feeds.",
  "references": [
    {
      "title": "Splunk_TA_microsoft-cloudservices",
      "url": "https://splunkbase.splunk.com/app/3110",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `azure` (UC-9.3.13)** — Planned work for 'App Registration Secret Expiry' shifts `count` over `span=1h` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `azure:graph:applications`** — Scheduled automation elevates `days_left` without crossing `days_left` < 30 for UC-9.3.13. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `azure` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `azure:graph:applications`** — First 48h baseline for UC-9.3.13 on `host` is unstable; window via `iam_exception_register.csv`.\n\n**Suppression mechanism:** `iam_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1528"
  ],
  "equipment": [
    "azure",
    "m365"
  ],
  "equipmentModels": [
    "azure_generic",
    "m365_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_microsoft-cloudservices`, Graph scripted input.\n• Ensure the following data sources are available: Application credential inventory (`passwordCredentials.endDateTime`), audit when secret added.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nSchedule Graph export of app registrations with secrets/certificates. Alert at 30/14/7 days. Map apps to owners via lookup.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=azure sourcetype=\"azure:graph:applications\"\n| eval days_left=round((strptime(endDateTime,\"%Y-%m-%dT%H:%M:%SZ\")-now())/86400)\n| where days_left < 30 AND days_left > 0\n| table appId, displayName, days_left, endDateTime\n| sort days_left\n```\n\nUnderstanding this SPL\n\n**App Registration Secret Expiry** — Expired client secrets break automation and encourage long-lived secrets; proactive alerting avoids outages and insecure workarounds.\n\nDocumented **Data sources**: Application credential inventory (`passwordCredentials.endDateTime`), audit when secret added. **App/TA** (typical add-on context): `Splunk_TA_microsoft-cloudservices`, Graph scripted input. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: azure; **sourcetype**: azure:graph:applications. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=azure, sourcetype=\"azure:graph:applications\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **days_left** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where days_left < 30 AND days_left > 0` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **App Registration Secret Expiry**): table appId, displayName, days_left, endDateTime\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nCompare with Entra ID sign-in and audit logs in the Microsoft Entra or Azure portal for the same users, resources, and time window.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (expiring secrets), Single value (next expiry), Gauge (apps past due).",
  "grandmaExplanation": "We use identity and sign-in data in Splunk so we can notice unusual logins, access changes, and privileged use while it still matters — App Registration Secret Expiry",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
