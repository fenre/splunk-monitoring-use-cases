{
  "$schema": "../../schemas/uc.schema.json",
  "id": "9.5.4",
  "title": "Okta Admin Console Changes",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Okta Admin Console Changes' only. Does not replace full SIEM correlation, IdP cloud-native analytics, or regulatory attestation. Entities listed in `iam_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `iam_okta_admin_console_changes` (UC-9.5.4), SOC dashboard panel \"IAM — Okta Admin Console Changes\", and weekly export to `index=audit_evidence` sourcetype=csv:iam_controls.",
  "controlTest": {
    "positiveScenario": "UC-9.5.4 positive (Okta, Admin, Console, Changes): ingest synthetic events into `index=okta` sourcetype='OktaIM2:log' where `count` grouped by `actor.alternateId`, `eventType`, `client.ipAddress`, `displayMessage` satisfies detection threshold for 'Okta Admin Console Changes'. Validate eventType `user.session.access_admin_app`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-9.5.4 negative (Console Changes): ingest benign `okta` / `OktaIM2:log` events where `actor.alternateId`, `eventType`, `client.ipAddress`, `displayMessage` stays below detection threshold, or exclude the test entity via `iam_exception_register.csv`. Variant 13 confirms zero alert rows for 'Okta Admin Console Changes'."
  },
  "dataSources": "`sourcetype=OktaIM2:log` (`system.*`, `user.session.access_admin_app`, `resource.*`) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_okta` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=okta sourcetype=\"OktaIM2:log\" (eventType=\"user.session.access_admin_app\" OR like(eventType,\"system.org%\"))\n| stats count by actor.alternateId, eventType, client.ipAddress, displayMessage\n| sort -count",
  "description": "Surfaces UC-9.5.4 — Okta, Admin, Console, Changes — using `index=okta` sourcetype `OktaIM2:log`, grouped by `actor.alternateId`, `eventType`, `client.ipAddress`, `displayMessage`. Outputs `count` each `the scheduled window`; alert when configured threshold. Filter chain includes eventType `user.session.access_admin_app`. Purpose-built for 'Okta Admin Console Changes' in IAM workflows.",
  "value": "UC-9.5.4 gives IAM teams earlier visibility into Console Changes through `OktaIM2:log` feeds, lowering account takeover risk, help-desk load, and audit findings.",
  "implementation": "Capture all admin app sessions and high-privilege system events. Restrict alerts to production Okta orgs; exclude known automation actors. Store lookups for approved admins and compare. Alert on first-time admin access from new ASN or country.",
  "visualization": "Timeline (admin actions), Table (actor, event, IP), Bar chart (events by admin user).",
  "cimModels": [
    "Change"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Change.All_Changes by All_Changes.action, All_Changes.object_category, All_Changes.user | sort - count",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Change` datasets (for example `Change.All_Changes`, `Change.Auditing_Changes`, or `Change.Account_Management` depending on your mapping) once audit and identity events are CIM-tagged; otherwise `cimSpl` tstats may return no rows.",
  "references": [
    {
      "title": "Splunk_TA_okta",
      "url": "https://splunkbase.splunk.com/app/6553",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Change",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Change",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `okta` (UC-9.5.4)** — Planned work for 'Okta Admin Console Changes' shifts `count` over `span=1h` while `actor.alternateId`, `eventType`, `client.ipAddress`, `displayMessage` remains in-scope. eventType=user.session.access_admin_app Applies eventType `user.session.access_admin_app`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `OktaIM2:log`** — Scheduled automation elevates `eventType` without crossing configured threshold for UC-9.5.4. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `actor.alternateId`, `eventType`, `client.ipAddress`, `displayMessage`** — Health checks in `okta` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `OktaIM2:log`** — First 48h baseline for UC-9.5.4 on `actor.alternateId` is unstable; window via `iam_exception_register.csv`.\n\n**Suppression mechanism:** `iam_exception_register.csv` keyed on `actor.alternateId`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1098.003",
    "T1078.004"
  ],
  "equipment": [
    "okta"
  ],
  "equipmentModels": [
    "okta_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_okta`.\n• Ensure the following data sources are available: `sourcetype=OktaIM2:log` (`system.*`, `user.session.access_admin_app`, `resource.*`).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nCapture all admin app sessions and high-privilege system events. Restrict alerts to production Okta orgs; exclude known automation actors. Store lookups for approved admins and compare. Alert on first-time admin access from new ASN or country.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=okta sourcetype=\"OktaIM2:log\" (eventType=\"user.session.access_admin_app\" OR like(eventType,\"system.org%\"))\n| stats count by actor.alternateId, eventType, client.ipAddress, displayMessage\n| sort -count\n```\n\nUnderstanding this SPL\n\n**Okta Admin Console Changes** — Changes in the admin console affect global security posture; auditing who changed what supports SOC2/ISO investigations and insider-threat programs.\n\nDocumented **Data sources**: `sourcetype=OktaIM2:log` (`system.*`, `user.session.access_admin_app`, `resource.*`). **App/TA** (typical add-on context): `Splunk_TA_okta`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: okta; **sourcetype**: OktaIM2:log. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=okta, sourcetype=\"OktaIM2:log\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by actor.alternateId, eventType, client.ipAddress, displayMessage** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats summariesonly=t count from datamodel=Change.All_Changes by All_Changes.action, All_Changes.object_category, All_Changes.user | sort - count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Okta Admin Console Changes** — Changes in the admin console affect global security posture; auditing who changed what supports SOC2/ISO investigations and insider-threat programs.\n\nDocumented **Data sources**: `sourcetype=OktaIM2:log` (`system.*`, `user.session.access_admin_app`, `resource.*`). **App/TA** (typical add-on context): `Splunk_TA_okta`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Change.All_Changes` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare Splunk results with the Okta admin console and System Log for the same users, outcomes, and time window, then adjust thresholds to normal org traffic.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Timeline (admin actions), Table (actor, event, IP), Bar chart (events by admin user).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_okta` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=OktaIM2:log into okta. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`actor.alternateId, client.ipAddress, displayMessage, eventType`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use Okta sign-in records so we can see failed logins, risky sessions, and admin changes in one place.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
