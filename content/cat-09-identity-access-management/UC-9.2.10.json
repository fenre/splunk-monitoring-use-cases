{
  "$schema": "../../schemas/uc.schema.json",
  "id": "9.2.10",
  "title": "LDAPS Certificate Validation",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Availability"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'LDAPS Certificate Validation' only. Does not replace full SIEM correlation, IdP cloud-native analytics, or regulatory attestation. Entities listed in `iam_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `iam_ldaps_certificate_validation` (UC-9.2.10), SOC dashboard panel \"IAM — LDAPS Certificate Validation\", and weekly export to `index=audit_evidence` sourcetype=csv:iam_controls.",
  "controlTest": {
    "positiveScenario": "UC-9.2.10 positive (LDAPS, Certificate, Validation): ingest synthetic events into `index=wineventlog` sourcetype='WinEventLog:System' where `count` grouped by `ComputerName`, `EventCode`, `Message` satisfies detection threshold for 'LDAPS Certificate Validation'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-9.2.10 negative (Certificate Validation): ingest benign `wineventlog` / `WinEventLog:System` events where `ComputerName`, `EventCode`, `Message` stays below detection threshold, or exclude the test entity via `iam_exception_register.csv`. Variant 11 confirms zero alert rows for 'LDAPS Certificate Validation'."
  },
  "dataSources": "System log Schannel errors (36870, 36866), slapd TLS errors Collected via Splunk Add-on for Microsoft Windows (Splunkbase 742) modular input or scripted poll into the index named in the SPL.",
  "app": "Windows Schannel, OpenLDAP TLS logs — Splunk Add-on for Microsoft Windows (Splunkbase 742)",
  "spl": "index=wineventlog sourcetype=\"WinEventLog:System\" SourceName=\"Schannel\" EventCode IN (36870,36866)\n| stats count by ComputerName, EventCode, Message\n| sort -count",
  "description": "Surfaces UC-9.2.10 — LDAPS, Certificate, Validation — using `index=wineventlog` sourcetype `WinEventLog:System`, grouped by `ComputerName`, `EventCode`, `Message`. Outputs `count` each `the scheduled window`; alert when configured threshold. Primary focus: Certificate Validation. Purpose-built for 'LDAPS Certificate Validation' in IAM workflows.",
  "value": "UC-9.2.10 helps teams contain LDAPS, Certificate, Validation scenarios and reduce account takeover risk, help-desk load, and audit findings before incidents compound.",
  "implementation": "Forward Schannel and LDAP server TLS logs. Map to cert renewal runbook. Alert on spike in handshake failures after cert rotation.",
  "visualization": "Table (hosts with TLS errors), Timeline, Single value (LDAPS errors 24h).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw directory, IdP, PAM, or device feeds.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 742",
      "url": "https://splunkbase.splunk.com/app/742",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `wineventlog` (UC-9.2.10)** — Planned work for 'LDAPS Certificate Validation' shifts `count` over `span=1h` while `ComputerName`, `EventCode`, `Message` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `WinEventLog:System`** — Scheduled automation elevates `EventCode` without crossing configured threshold for UC-9.2.10. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `ComputerName`, `EventCode`, `Message`** — Health checks in `wineventlog` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `WinEventLog:System`** — First 48h baseline for UC-9.2.10 on `ComputerName` is unstable; window via `iam_exception_register.csv`.\n\n**Suppression mechanism:** `iam_exception_register.csv` keyed on `ComputerName`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1557"
  ],
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Windows Schannel, OpenLDAP TLS logs.\n• Ensure the following data sources are available: System log Schannel errors (36870, 36866), slapd TLS errors.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nForward Schannel and LDAP server TLS logs. Map to cert renewal runbook. Alert on spike in handshake failures after cert rotation.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=wineventlog sourcetype=\"WinEventLog:System\" SourceName=\"Schannel\" EventCode IN (36870,36866)\n| stats count by ComputerName, EventCode, Message\n| sort -count\n```\n\nUnderstanding this SPL\n\n**LDAPS Certificate Validation** — LDAPS clients failing TLS handshakes or cert validation indicate expired CAs, hostname mismatches, or MITM attempts.\n\nDocumented **Data sources**: System log Schannel errors (36870, 36866), slapd TLS errors. **App/TA** (typical add-on context): Windows Schannel, OpenLDAP TLS logs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: wineventlog; **sourcetype**: WinEventLog:System. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=wineventlog, sourcetype=\"WinEventLog:System\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by ComputerName, EventCode, Message** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nCompare with the directory server’s admin or audit view (bind DNs, result codes) for the same time range.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (hosts with TLS errors), Timeline, Single value (LDAPS errors 24h).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Windows Schannel, OpenLDAP TLS logs — Splunk Add-on for Microsoft Windows (Splunkbase 742)). Enable the modular input or HEC token that writes sourcetype=WinEventLog:System into wineventlog. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`ComputerName, EventCode, Message`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use identity and sign-in data in Splunk so we can notice unusual logins, access changes, and privileged use while it still matters — LDAPS Certificate Validation",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
