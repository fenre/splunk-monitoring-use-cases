{
  "$schema": "../../schemas/uc.schema.json",
  "id": "9.4.22",
  "title": "BeyondTrust Password Safe Credential Release Volume by Asset",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Security",
    "Audit"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'BeyondTrust Password Safe Credential Release Volume by Asset' only. Does not replace full SIEM correlation, IdP cloud-native analytics, or regulatory attestation. Entities listed in `iam_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `iam_beyondtrust_password_safe_credential_release_vol` (UC-9.4.22), SOC dashboard panel \"IAM — BeyondTrust Password Safe Credential Release Volume by Asset\", and weekly export to `index=audit_evidence` sourcetype=csv:iam_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=pam` sourcetype='beyondtrust' that satisfy the UC-9.4.22 SPL filters for 'BeyondTrust Password Safe Credential Release Volume by Asset'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `pam` / `beyondtrust` events outside the detection threshold, or exclude the test entity via `iam_exception_register.csv` with a valid window. Confirm zero alert rows for UC-9.4.22."
  },
  "dataSources": "Password Safe Splunk Event Forwarder `sourcetype=beyondtrust` or `source=password_safe`",
  "app": "BeyondTrust Password Safe / Cloud Dashboard for Splunk (Splunkbase 5574) with Splunk HTTP Event Collector",
  "spl": "index=pam (sourcetype=beyondtrust OR source=password_safe) earliest=-24h\n| eval user=coalesce(UserName, user, User, \"\")\n| eval asset=coalesce(SystemName, TargetSystem, ManagedAccount, host, \"\")\n| search user!=\"\" asset!=\"\"\n| where match(lower(_raw), \"(?i)password|credential|checkout|retrieve|release\")\n| stats count by user, asset\n| sort -count",
  "description": "Credential checkout and release activity grouped by user and managed system highlights overuse of shared break-glass accounts and unusual access to crown-jewel assets.",
  "value": "Gives IAM and server owners a quantitative view of vault usage beyond static session recordings, supporting access reviews and separation-of-duty reporting.",
  "implementation": "Align `index`, `sourcetype`, and `source` with BeyondTrust’s Splunk connector configuration. Replace the `_raw` keyword match with explicit `EventType` or `MessageType` fields once validated in your environment. Enrich `asset` with CMDB ownership. Suppress service automation accounts via lookup.",
  "visualization": "Table (user × asset), heatmap, timechart of daily checkouts.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw directory, IdP, PAM, or device feeds.",
  "references": [
    {
      "title": "BeyondTrust Password Safe / Cloud Dashboard for Splunk",
      "url": "https://splunkbase.splunk.com/app/5574",
      "retrieved": "2026-04-25"
    },
    {
      "title": "BeyondTrust Splunk integration (Event Forwarder)",
      "url": "https://docs.beyondtrust.com/bips/docs/bi-splunk-integration",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled IAM maintenance** — Planned directory or IdP change generates expected deviations for 'BeyondTrust Password Safe Credential Release Volume by Asset'. Cross-check change records.\n\n2. **Onboarding burst** — New accounts or apps produce atypical `pam` volume.\n\n3. **Monitoring synthetic bind checks** — Health probes from known scanner hosts.\n\n4. **Stale application credential** — Single entity dominates `beyondtrust` failures.\n\n**Suppression mechanism:** `iam_exception_register.csv` with entity, reason, valid_until.",
  "equipment": [
    "beyondtrust",
    "hardware_bmc"
  ],
  "equipmentModels": [
    "hardware_bmc_edac"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Install and configure: BeyondTrust Password Safe / Cloud Dashboard for Splunk (Splunkbase 5574) with Splunk HTTP Event Collector.\n• Data sources: Password Safe Splunk Event Forwarder `sourcetype=beyondtrust` or `source=password_safe`.\n\nStep 1 — Configure data collection\nAlign `index`, `sourcetype`, and `source` with BeyondTrust’s Splunk connector configuration. Replace the `_raw` keyword match with explicit `EventType` or `MessageType` fields once validated in your environment. Enrich `asset` with CMDB ownership. Suppress service automation accounts via lookup.\n\nStep 2 — Create the search and alert\n\n```spl\nindex=pam (sourcetype=beyondtrust OR source=password_safe) earliest=-24h\n| eval user=coalesce(UserName, user, User, \"\")\n| eval asset=coalesce(SystemName, TargetSystem, ManagedAccount, host, \"\")\n| search user!=\"\" asset!=\"\"\n| where match(lower(_raw), \"(?i)password|credential|checkout|retrieve|release\")\n| stats count by user, asset\n| sort -count\n```\n\nStep 3 — Validate\nCompare with BeyondTrust Password Safe reports and safe lists for the same users, assets, and time range.\n\nStep 4 — Operationalize\nAdd to a dashboard or alert; document the owner. Table (user × asset), heatmap, timechart of daily checkouts.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (BeyondTrust Password Safe / Cloud Dashboard for Splunk (Splunkbase 5574) with Splunk HTTP Event Collector). Enable the modular input or HEC token that writes sourcetype=beyondtrust into pam. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`asset, user`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We add up who pulled which vault passwords to which systems so you can see overuse of shared accounts and odd access to sensitive hosts during reviews.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
