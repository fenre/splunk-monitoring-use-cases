{
  "$schema": "../../schemas/uc.schema.json",
  "id": "9.4.25",
  "title": "BeyondTrust Privileged Session Duration Beyond Policy Threshold",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Security",
    "Operational"
  ],
  "splunkPillar": "Security",
  "dataSources": "`sourcetype=beyondtrust:session` with duration or start/end timestamps",
  "app": "BeyondTrust integrations feeding Splunk PAM index",
  "spl": "index=pam sourcetype=\"beyondtrust:session\" earliest=-24h\n| eval dur_min=coalesce(duration_min, round(duration_sec/60,1), round((SessionDurationSeconds)/60,1))\n| eval user=coalesce(user, UserName, admin_user, \"\")\n| eval target=coalesce(target_host, TargetHost, dest, \"\")\n| where isnotnull(dur_min) AND dur_min>120\n| stats max(dur_min) as max_min avg(dur_min) as avg_min by user, target\n| sort -max_min",
  "description": "Sessions that remain open for hours increase the window for credential theft, lateral movement, and unrecorded activity if recording fails mid-session.",
  "value": "Automates a control frequently checked only during audits, surfacing outliers before they appear in quarterly PAM reports.",
  "implementation": "Confirm which duration field your BeyondTrust parser provides; some feeds require computing `strptime` deltas between session start and end events. Set thresholds per asset class (120 minutes default). Join with change tickets for expected long maintenance.",
  "visualization": "Table (user × target × max duration), histogram of session lengths, timeline.",
  "references": [
    {
      "title": "BeyondTrust Password Safe / Cloud Dashboard for Splunk",
      "url": "https://splunkbase.splunk.com/app/5574"
    },
    {
      "title": "BeyondTrust product documentation hub",
      "url": "https://www.beyondtrust.com/docs"
    }
  ],
  "equipment": [
    "beyondtrust"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases"
}
