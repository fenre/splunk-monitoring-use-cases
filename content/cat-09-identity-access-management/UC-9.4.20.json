{
  "$schema": "../../schemas/uc.schema.json",
  "id": "9.4.20",
  "title": "PAM Agent Health Monitoring",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'PAM Agent Health Monitoring' only. Does not replace full SIEM correlation, IdP cloud-native analytics, or regulatory attestation. Entities listed in `iam_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `iam_pam_agent_health_monitoring` (UC-9.4.20), SOC dashboard panel \"IAM — PAM Agent Health Monitoring\", and weekly export to `index=audit_evidence` sourcetype=csv:iam_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=pam` sourcetype='cyberark:agent_heartbeat' that satisfy the UC-9.4.20 SPL filters for 'PAM Agent Health Monitoring'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `pam` / `cyberark:agent_heartbeat` events outside the detection threshold, or exclude the test entity via `iam_exception_register.csv` with a valid window. Confirm zero alert rows for UC-9.4.20."
  },
  "dataSources": "Agent heartbeat, service status, `Get-PMPServerHealth`-style scripted input Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "CyberArk component monitoring, SNMP/HEARTBEAT logs — Universal Forwarder (Splunkbase 617)",
  "spl": "index=pam sourcetype=\"cyberark:agent_heartbeat\"\n| stats latest(_time) as last_hb by agent_type, hostname\n| eval secs_since=now()-last_hb\n| where secs_since > 300\n| table agent_type, hostname, secs_since",
  "description": "CPM, PSM, and PVWA agents offline block rotation and session capture; distinct from vault binary health (UC-9.4.6).",
  "value": "Early 'PAM Agent Health Monitoring' detection reduces account takeover risk, help-desk load, and audit findings by giving IAM operators a Splunk-native signal before IdP lockout or breach reports arrive through manual channels.",
  "implementation": "Agents send heartbeat every 60s. Alert if no heartbeat >5 minutes. Auto-ticket remediation for PSM in production zones.",
  "visualization": "Status grid (agent × host), Single value (unhealthy agents), Line chart (heartbeat age).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw directory, IdP, PAM, or device feeds.",
  "references": [
    {
      "title": "Cisco Security Cloud",
      "url": "https://splunkbase.splunk.com/app/7404",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled IAM maintenance** — Planned directory or IdP change generates expected deviations for 'PAM Agent Health Monitoring'. Cross-check change records.\n\n2. **Onboarding burst** — New accounts or apps produce atypical `pam` volume.\n\n3. **Monitoring synthetic bind checks** — Health probes from known scanner hosts.\n\n4. **Stale application credential** — Single entity dominates `cyberark:agent_heartbeat` failures.\n\n**Suppression mechanism:** `iam_exception_register.csv` with entity, reason, valid_until.",
  "equipment": [
    "cyberark",
    "hashicorp",
    "snmp"
  ],
  "equipmentModels": [
    "hashicorp_vault",
    "snmp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: CyberArk component monitoring, SNMP/HEARTBEAT logs.\n• Ensure the following data sources are available: Agent heartbeat, service status, `Get-PMPServerHealth`-style scripted input.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nAgents send heartbeat every 60s. Alert if no heartbeat >5 minutes. Auto-ticket remediation for PSM in production zones.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=pam sourcetype=\"cyberark:agent_heartbeat\"\n| stats latest(_time) as last_hb by agent_type, hostname\n| eval secs_since=now()-last_hb\n| where secs_since > 300\n| table agent_type, hostname, secs_since\n```\n\nUnderstanding this SPL\n\n**PAM Agent Health Monitoring** — CPM, PSM, and PVWA agents offline block rotation and session capture; distinct from vault binary health (UC-9.4.6).\n\nDocumented **Data sources**: Agent heartbeat, service status, `Get-PMPServerHealth`-style scripted input. **App/TA** (typical add-on context): CyberArk component monitoring, SNMP/HEARTBEAT logs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: pam; **sourcetype**: cyberark:agent_heartbeat. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=pam, sourcetype=\"cyberark:agent_heartbeat\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by agent_type, hostname** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **secs_since** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where secs_since > 300` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **PAM Agent Health Monitoring**): table agent_type, hostname, secs_since\n\n\nStep 3 — Validate\nCompare with CyberArk PrivateArk/Password Vault Web Access (or BeyondTrust / vendor console) for the same sessions, vault activity, and alerts.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Status grid (agent × host), Single value (unhealthy agents), Line chart (heartbeat age).\n\nScripted input (generic example)\nThis use case relies on a scripted input. In the app's local/inputs.conf add a stanza such as:\n\n```ini\n[script://$SPLUNK_HOME/etc/apps/YourApp/bin/collect.sh]\ninterval = 300\nsourcetype = your_sourcetype\nindex = main\ndisabled = 0\n```\n\nThe script should print one event per line (e.g. key=value). Example minimal script (bash):\n\n```bash\n#!/usr/bin/env bash\n# Output metrics or events, one per line\necho \"metric=value timestamp=$(date +%s)\"\n```\n\nFor full details (paths, scheduling, permissions), see the Implementation guide: docs/implementation-guide.md",
  "grandmaExplanation": "We use identity and sign-in data in Splunk so we can notice unusual logins, access changes, and privileged use while it still matters — PAM Agent Health Monitoring",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
