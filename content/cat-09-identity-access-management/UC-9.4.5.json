{
  "$schema": "../../schemas/uc.schema.json",
  "id": "9.4.5",
  "title": "Suspicious Session Commands",
  "criticality": "critical",
  "difficulty": "beginner",
  "monitoringType": [
    "Security",
    "Performance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Suspicious Session Commands' only. Does not replace full SIEM correlation, IdP cloud-native analytics, or regulatory attestation. Entities listed in `iam_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `iam_suspicious_session_commands` (UC-9.4.5), SOC dashboard panel \"IAM — Suspicious Session Commands\", and weekly export to `index=audit_evidence` sourcetype=csv:iam_controls.",
  "controlTest": {
    "positiveScenario": "UC-9.4.5 positive (Suspicious, Session, Commands): ingest synthetic events into `index=pam` sourcetype='cyberark:psm_transcript' where `count` grouped by `entity` satisfies detection threshold for 'Suspicious Session Commands'. Validate filter `command IN (\"rm -rf\",\"format\",\"del /s\",\"DROP DATABASE\",\"shut`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-9.4.5 negative (Session Commands): ingest benign `pam` / `cyberark:psm_transcript` events where `entity` stays below detection threshold, or exclude the test entity via `iam_exception_register.csv`. Variant 11 confirms zero alert rows for 'Suspicious Session Commands'."
  },
  "dataSources": "PAM session recordings/keystroke logs Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "CyberArk PSM, BeyondTrust session monitoring — Universal Forwarder (Splunkbase 617)",
  "spl": "index=pam sourcetype=\"cyberark:psm_transcript\"\n| search command IN (\"rm -rf\",\"format\",\"del /s\",\"DROP DATABASE\",\"shutdown\",\"halt\",\"init 0\")\n| table _time, user, target_host, command, session_id",
  "description": "Detects UC-9.4.5 — Suspicious, Session, Commands — using `index=pam` sourcetype `cyberark:psm_transcript`, grouped by `entity`. Outputs `count` each `the scheduled window`; alert when configured threshold. Filter chain includes filter `command IN (\"rm -rf\",\"format\",\"del /s\",\"DROP DATABASE\",\"shut`. Purpose-built for 'Suspicious Session Commands' in IAM workflows.",
  "value": "UC-9.4.5 helps teams contain Suspicious, Session, Commands scenarios and reduce account takeover risk, help-desk load, and audit findings before incidents compound.",
  "implementation": "Enable PAM session recording and command logging. Parse keystroke transcripts. Alert immediately on high-risk commands (rm -rf, format, DROP DATABASE, etc.). Integrate with SOAR for automated session termination on critical detections.",
  "visualization": "Table (suspicious commands), Timeline (command events), Single value (high-risk commands today).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw directory, IdP, PAM, or device feeds.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `pam` (UC-9.4.5)** — Planned work for 'Suspicious Session Commands' shifts `count` over `span=1h` while `host` remains in-scope. Applies filter `command IN (\"rm -rf\",\"format\",\"del /s\",\"DROP DATABASE\",\"shut`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `cyberark:psm_transcript`** — Scheduled automation elevates `src` without crossing configured threshold for UC-9.4.5. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `pam` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `cyberark:psm_transcript`** — First 48h baseline for UC-9.4.5 on `host` is unstable; window via `iam_exception_register.csv`.\n\n**Suppression mechanism:** `iam_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1078",
    "T1003"
  ],
  "equipment": [
    "beyondtrust",
    "cyberark"
  ],
  "equipmentModels": [
    "beyondtrust_generic",
    "cyberark_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: CyberArk PSM, BeyondTrust session monitoring.\n• Ensure the following data sources are available: PAM session recordings/keystroke logs.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnable PAM session recording and command logging. Parse keystroke transcripts. Alert immediately on high-risk commands (rm -rf, format, DROP DATABASE, etc.). Integrate with SOAR for automated session termination on critical detections.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=pam sourcetype=\"cyberark:psm_transcript\"\n| search command IN (\"rm -rf\",\"format\",\"del /s\",\"DROP DATABASE\",\"shutdown\",\"halt\",\"init 0\")\n| table _time, user, target_host, command, session_id\n```\n\nUnderstanding this SPL\n\n**Suspicious Session Commands** — Detecting dangerous commands during privileged sessions enables real-time intervention before damage occurs.\n\nDocumented **Data sources**: PAM session recordings/keystroke logs. **App/TA** (typical add-on context): CyberArk PSM, BeyondTrust session monitoring. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: pam; **sourcetype**: cyberark:psm_transcript. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=pam, sourcetype=\"cyberark:psm_transcript\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Applies an explicit `search` filter to narrow the current result set.\n• Pipeline stage (see **Suspicious Session Commands**): table _time, user, target_host, command, session_id\n\n\nStep 3 — Validate\nCompare with CyberArk PrivateArk/Password Vault Web Access (or BeyondTrust / vendor console) for the same sessions, vault activity, and alerts.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (suspicious commands), Timeline (command events), Single value (high-risk commands today).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (CyberArk PSM, BeyondTrust session monitoring — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=cyberark:psm_transcript into pam. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, user, action`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use identity and sign-in data in Splunk so we can notice unusual logins, access changes, and privileged use while it still matters — Suspicious Session Commands",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
