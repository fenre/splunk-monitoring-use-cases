{
  "$schema": "../../schemas/uc.schema.json",
  "id": "9.4.7",
  "title": "Federated Identity Provider Health",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Federated Identity Provider Health' only. Does not replace full SIEM correlation, IdP cloud-native analytics, or regulatory attestation. Entities listed in `iam_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `iam_federated_identity_provider_health` (UC-9.4.7), SOC dashboard panel \"IAM — Federated Identity Provider Health\", and weekly export to `index=audit_evidence` sourcetype=csv:iam_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=iam` sourcetype='idp:health' where `idp_host` satisfies `latency` > 5000 for UC-9.4.7 ('Federated Identity Provider Health'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `iam` / `idp:health` events where `idp_host` stays below `latency` > 5000, or exclude the test entity via `iam_exception_register.csv` with a valid window. Confirm zero alert rows for UC-9.4.7."
  },
  "dataSources": "IdP health endpoints, federation error logs Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "IdP monitoring, SAML/OIDC audit logs — Universal Forwarder (Splunkbase 617)",
  "spl": "index=iam sourcetype=\"idp:health\"\n| stats latest(status) as status, latest(response_ms) as latency by idp_host, tenant\n| where status!=\"healthy\" OR latency > 5000\n| table idp_host, tenant, status, latency",
  "description": "IdP outages block all federated application access. Health monitoring ensures SSO availability and rapid incident response.",
  "value": "Early 'Federated Identity Provider Health' detection reduces account takeover risk, help-desk load, and audit findings by giving IAM operators a Splunk-native signal before IdP lockout or breach reports arrive through manual channels.",
  "implementation": "Poll IdP health endpoints (e.g., SAML metadata, OIDC discovery) every 60 seconds. Ingest federation errors from app and IdP logs. Alert on status unhealthy or latency >5s. Correlate with user-reported SSO issues.",
  "visualization": "Status grid (IdP × health), Single value (IdP uptime %), Line chart (latency trend).",
  "cimModels": [
    "Authentication"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Authentication.Authentication by Authentication.action, Authentication.user, Authentication.src | sort - count",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Authentication.Authentication` dataset once identity and sign-in data are CIM-tagged; otherwise `cimSpl` tstats may return no rows.",
  "references": [
    {
      "title": "CIM: Authentication",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Authentication",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `iam`** — Planned work for 'Federated Identity Provider Health' shifts `count` over `span=1h` while `idp_host` remains in-scope for UC-9.4.7. Cross-check CMDB CHG.\n\n2. **Batch `count` on `idp:health`** — Scheduled automation elevates `tenant` without crossing `latency` > 5000. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `idp_host`** — Health checks in `iam` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `idp:health`** — First 48h baseline for `idp_host` is unstable; window via `iam_exception_register.csv`.\n\n**Suppression mechanism:** `iam_exception_register.csv` keyed on `idp_host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: IdP monitoring, SAML/OIDC audit logs.\n• Ensure the following data sources are available: IdP health endpoints, federation error logs.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nPoll IdP health endpoints (e.g., SAML metadata, OIDC discovery) every 60 seconds. Ingest federation errors from app and IdP logs. Alert on status unhealthy or latency >5s. Correlate with user-reported SSO issues.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=iam sourcetype=\"idp:health\"\n| stats latest(status) as status, latest(response_ms) as latency by idp_host, tenant\n| where status!=\"healthy\" OR latency > 5000\n| table idp_host, tenant, status, latency\n```\n\nUnderstanding this SPL\n\n**Federated Identity Provider Health** — IdP outages block all federated application access. Health monitoring ensures SSO availability and rapid incident response.\n\nDocumented **Data sources**: IdP health endpoints, federation error logs. **App/TA** (typical add-on context): IdP monitoring, SAML/OIDC audit logs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: iam; **sourcetype**: idp:health. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=iam, sourcetype=\"idp:health\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by idp_host, tenant** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where status!=\"healthy\" OR latency > 5000` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Federated Identity Provider Health**): table idp_host, tenant, status, latency\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats summariesonly=t count from datamodel=Authentication.Authentication by Authentication.action, Authentication.user, Authentication.src | sort - count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Federated Identity Provider Health** — IdP outages block all federated application access. Health monitoring ensures SSO availability and rapid incident response.\n\nDocumented **Data sources**: IdP health endpoints, federation error logs. **App/TA** (typical add-on context): IdP monitoring, SAML/OIDC audit logs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Authentication.Authentication` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare results with the authoritative identity source (directory, IdP, or PAM) for the same time range and with known change or maintenance tickets.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Status grid (IdP × health), Single value (IdP uptime %), Line chart (latency trend).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (IdP monitoring, SAML/OIDC audit logs — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=idp:health into iam. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`idp_host, latency, tenant`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use identity and sign-in data in Splunk so we can notice unusual logins, access changes, and privileged use while it still matters — Federated Identity Provider Health",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
