{
  "$schema": "../../schemas/uc.schema.json",
  "id": "9.4.18",
  "title": "Emergency Break-Glass Account Usage",
  "criticality": "critical",
  "difficulty": "beginner",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Emergency Break-Glass Account Usage' only. Does not replace full SIEM correlation, IdP cloud-native analytics, or regulatory attestation. Entities listed in `iam_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `iam_emergency_break_glass_account_usage` (UC-9.4.18), SOC dashboard panel \"IAM — Emergency Break-Glass Account Usage\", and weekly export to `index=audit_evidence` sourcetype=csv:iam_controls.",
  "controlTest": {
    "positiveScenario": "UC-9.4.18 positive (Emergency, Break, Glass, Account, Usage): ingest synthetic events into `index=pam` sourcetype='cyberark:vault' where `count` grouped by `entity` satisfies detection threshold for 'Emergency Break-Glass Account Usage'. Validate lookup `soc_networks`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-9.4.18 negative (Account Usage): ingest benign `pam` / `cyberark:vault` events where `entity` stays below detection threshold, or exclude the test entity via `iam_exception_register.csv`. Variant 9 confirms zero alert rows for 'Emergency Break-Glass Account Usage'."
  },
  "dataSources": "PAM checkout for accounts tagged `emergency_only`, 4624 for same sAMAccountName Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk TA for CyberArk, AD Security logs — Universal Forwarder (Splunkbase 617)",
  "spl": "index=pam sourcetype=\"cyberark:vault\" account_tag=\"emergency_only\"\n| lookup soc_networks subnet OUTPUT network_name\n| where isnull(network_name)\n| table _time, user, account, client_ip, action\n| sort -_time",
  "description": "Detects UC-9.4.18 — Emergency, Break, Glass, Account, Usage — using `index=pam` sourcetype `cyberark:vault`, grouped by `entity`. Outputs `count` each `the scheduled window`; alert when configured threshold. Filter chain includes lookup `soc_networks`. Purpose-built for 'Emergency Break-Glass Account Usage' in IAM workflows.",
  "value": "Operators running UC-9.4.18 on `cyberark:vault` feeds shorten triage for Account Usage tied to 'Emergency Break-Glass Account Usage', lowering account takeover risk, help-desk load, and audit findings.",
  "implementation": "Define emergency accounts in PAM and AD. Alert on any checkout or interactive logon; require post-incident report within SLA. Correlate with major incident tickets.",
  "visualization": "Timeline (emergency usage), Table (detail), Single value (events outside SOC net).",
  "cimModels": [
    "Authentication"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Authentication.Authentication by Authentication.action, Authentication.user, Authentication.src | sort - count",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Authentication.Authentication` dataset once identity and sign-in data are CIM-tagged; otherwise `cimSpl` tstats may return no rows.",
  "references": [
    {
      "title": "CIM: Authentication",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Authentication",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `pam` (UC-9.4.18)** — Planned work for 'Emergency Break-Glass Account Usage' shifts `count` over `span=1h` while `host` remains in-scope. Applies lookup `soc_networks`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `cyberark:vault`** — Scheduled automation elevates `src` without crossing configured threshold for UC-9.4.18. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `pam` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `cyberark:vault`** — First 48h baseline for UC-9.4.18 on `host` is unstable; join `soc_networks` prior to threshold.\n\n**Suppression mechanism:** `iam_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1078.002",
    "T1078.004"
  ],
  "equipment": [
    "cyberark",
    "hashicorp"
  ],
  "equipmentModels": [
    "hashicorp_vault"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk TA for CyberArk, AD Security logs.\n• Ensure the following data sources are available: PAM checkout for accounts tagged `emergency_only`, 4624 for same sAMAccountName.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nDefine emergency accounts in PAM and AD. Alert on any checkout or interactive logon; require post-incident report within SLA. Correlate with major incident tickets.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=pam sourcetype=\"cyberark:vault\" account_tag=\"emergency_only\"\n| lookup soc_networks subnet OUTPUT network_name\n| where isnull(network_name)\n| table _time, user, account, client_ip, action\n| sort -_time\n```\n\nUnderstanding this SPL\n\n**Emergency Break-Glass Account Usage** — Real-time paging for emergency-only vault accounts beyond standard break-glass (UC-9.4.3)—includes usage from non-SOC networks.\n\nDocumented **Data sources**: PAM checkout for accounts tagged `emergency_only`, 4624 for same sAMAccountName. **App/TA** (typical add-on context): Splunk TA for CyberArk, AD Security logs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: pam; **sourcetype**: cyberark:vault. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=pam, sourcetype=\"cyberark:vault\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Enriches events using `lookup` (lookup definition + optional OUTPUT fields).\n• Filters the current rows with `where isnull(network_name)` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Emergency Break-Glass Account Usage**): table _time, user, account, client_ip, action\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats summariesonly=t count from datamodel=Authentication.Authentication by Authentication.action, Authentication.user, Authentication.src | sort - count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Emergency Break-Glass Account Usage** — Real-time paging for emergency-only vault accounts beyond standard break-glass (UC-9.4.3)—includes usage from non-SOC networks.\n\nDocumented **Data sources**: PAM checkout for accounts tagged `emergency_only`, 4624 for same sAMAccountName. **App/TA** (typical add-on context): Splunk TA for CyberArk, AD Security logs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Authentication.Authentication` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare with CyberArk PrivateArk/Password Vault Web Access (or BeyondTrust / vendor console) for the same sessions, vault activity, and alerts.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Timeline (emergency usage), Table (detail), Single value (events outside SOC net).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk TA for CyberArk, AD Security logs — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=cyberark:vault into pam. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`network_name, subnet`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use identity and sign-in data in Splunk so we can notice unusual logins, access changes, and privileged use while it still matters — Emergency Break-Glass Account Usage",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
