{
  "$schema": "../../schemas/uc.schema.json",
  "id": "9.2.2",
  "title": "Search Performance Degradation",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Search Performance Degradation' only. Does not replace full SIEM correlation, IdP cloud-native analytics, or regulatory attestation. Entities listed in `iam_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `iam_search_performance_degradation` (UC-9.2.2), SOC dashboard panel \"IAM — Search Performance Degradation\", and weekly export to `index=audit_evidence` sourcetype=csv:iam_controls.",
  "controlTest": {
    "positiveScenario": "UC-9.2.2 positive (Search, Performance, Degradation): ingest synthetic events into `index=ldap` sourcetype='openldap:access' where `avg_ms` grouped by `base_dn`, `filter` satisfies `elapsed_ms` > 1000 for 'Search Performance Degradation'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-9.2.2 negative (Performance Degradation): ingest benign `ldap` / `openldap:access` events where `base_dn`, `filter` stays below `elapsed_ms` > 1000, or exclude the test entity via `iam_exception_register.csv`. Variant 11 confirms zero alert rows for 'Search Performance Degradation'."
  },
  "dataSources": "OpenLDAP access log (search duration), 389 Directory access log Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "LDAP access log parsing — Universal Forwarder (Splunkbase 617)",
  "spl": "index=ldap sourcetype=\"openldap:access\" operation=\"SEARCH\"\n| where elapsed_ms > 1000\n| stats count, avg(elapsed_ms) as avg_ms by base_dn, filter\n| sort -avg_ms",
  "description": "Detects UC-9.2.2 — Search, Performance, Degradation — using `index=ldap` sourcetype `openldap:access`, grouped by `base_dn`, `filter`. Outputs `avg_ms` each `the scheduled window`; alert when `elapsed_ms` > 1000. Primary focus: Performance Degradation. Purpose-built for 'Search Performance Degradation' in IAM workflows.",
  "value": "UC-9.2.2 helps teams contain Search, Performance, Degradation scenarios and reduce account takeover risk, help-desk load, and audit findings before incidents compound.",
  "implementation": "Enable LDAP access logging with timing information. Parse search operations with duration. Alert on searches exceeding 1 second. Identify expensive filters (unindexed attributes, broad base DN). Recommend index creation.",
  "visualization": "Table (slow searches), Bar chart (avg duration by filter), Line chart (search latency trend).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw directory, IdP, PAM, or device feeds.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `ldap` (UC-9.2.2)** — Planned work for 'Search Performance Degradation' shifts `avg_ms` over `span=1h` while `base_dn`, `filter` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `openldap:access`** — Scheduled automation elevates `filter` without crossing `elapsed_ms` > 1000 for UC-9.2.2. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `base_dn`, `filter`** — Health checks in `ldap` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `openldap:access`** — First 48h baseline for UC-9.2.2 on `base_dn` is unstable; window via `iam_exception_register.csv`.\n\n**Suppression mechanism:** `iam_exception_register.csv` keyed on `base_dn`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: LDAP access log parsing.\n• Ensure the following data sources are available: OpenLDAP access log (search duration), 389 Directory access log.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnable LDAP access logging with timing information. Parse search operations with duration. Alert on searches exceeding 1 second. Identify expensive filters (unindexed attributes, broad base DN). Recommend index creation.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ldap sourcetype=\"openldap:access\" operation=\"SEARCH\"\n| where elapsed_ms > 1000\n| stats count, avg(elapsed_ms) as avg_ms by base_dn, filter\n| sort -avg_ms\n```\n\nUnderstanding this SPL\n\n**Search Performance Degradation** — Slow LDAP searches impact all applications relying on directory services for authentication and authorization.\n\nDocumented **Data sources**: OpenLDAP access log (search duration), 389 Directory access log. **App/TA** (typical add-on context): LDAP access log parsing. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ldap; **sourcetype**: openldap:access. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ldap, sourcetype=\"openldap:access\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where elapsed_ms > 1000` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by base_dn, filter** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nCompare with the directory server’s admin or audit view (bind DNs, result codes) for the same time range.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (slow searches), Bar chart (avg duration by filter), Line chart (search latency trend).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (LDAP access log parsing — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=openldap:access into ldap. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`base_dn, elapsed_ms, filter`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use identity and sign-in data in Splunk so we can notice unusual logins, access changes, and privileged use while it still matters — Search Performance Degradation",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
