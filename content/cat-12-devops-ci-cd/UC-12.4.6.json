{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.4.6",
  "title": "Pipeline Failure Root Cause Trending",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Pipeline Failure Root Cause Trending' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_pipeline_failure_root_cause_trending` (UC-12.4.6), SOC dashboard panel \"Operations — Pipeline Failure Root Cause Trending\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-12.4.6 positive (Pipeline, Failure, Root, Cause, Trending): ingest synthetic events into `index=cicd` sourcetype='jenkins:build' where `count` grouped by `cause`, `job_name` satisfies detection threshold for 'Pipeline Failure Root Cause Trending'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-12.4.6 negative (Cause Trending): ingest benign `cicd` / `jenkins:build` events where `cause`, `job_name` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 15 confirms zero alert rows for 'Pipeline Failure Root Cause Trending'."
  },
  "dataSources": "Pipeline run logs, failure reasons, stage outcomes Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Jenkins/GitHub Actions/Azure DevOps TAs — Universal Forwarder (Splunkbase 617)",
  "spl": "index=cicd sourcetype=\"jenkins:build\"\n| where result=\"FAILURE\"\n| rex field=message \"(?<cause>Timeout|OOM|Connection refused|AssertionError|dependency)\"\n| stats count by cause, job_name\n| sort -count",
  "description": "Surfaces UC-12.4.6 — Pipeline, Failure, Root, Cause, Trending — using `index=cicd` sourcetype `jenkins:build`, grouped by `cause`, `job_name`. Outputs `count` each `the scheduled window`; alert when configured threshold. Primary focus: Cause Trending. Purpose-built for 'Pipeline Failure Root Cause Trending' in Operations workflows.",
  "value": "UC-12.4.6 operationalises 'Pipeline Failure Root Cause Trending' against `jenkins:build` feeds so operators reduce operational risk and delayed incident response without manual correlation.",
  "implementation": "Parse failure messages and stack traces from CI logs. Classify by cause (test, env, dependency, timeout). Alert on spike in a specific cause. Report on top failure reasons by job and week.",
  "visualization": "Bar chart (failures by cause), Table (job × cause), Line chart (failure rate trend).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `cicd` (UC-12.4.6)** — Planned work for 'Pipeline Failure Root Cause Trending' shifts `count` over `span=1h` while `cause`, `job_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `jenkins:build`** — Scheduled automation elevates `job_name` without crossing configured threshold for UC-12.4.6. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `cause`, `job_name`** — Health checks in `cicd` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `jenkins:build`** — First 48h baseline for UC-12.4.6 on `cause` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `cause`, `exception_reason`, `valid_until`.",
  "equipment": [
    "azure",
    "github",
    "jenkins"
  ],
  "equipmentModels": [
    "azure_generic",
    "github_generic",
    "jenkins_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Jenkins/GitHub Actions/Azure DevOps TAs.\n• Ensure the following data sources are available: Pipeline run logs, failure reasons, stage outcomes.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nParse failure messages and stack traces from CI logs. Classify by cause (test, env, dependency, timeout). Alert on spike in a specific cause. Report on top failure reasons by job and week.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=cicd sourcetype=\"jenkins:build\"\n| where result=\"FAILURE\"\n| rex field=message \"(?<cause>Timeout|OOM|Connection refused|AssertionError|dependency)\"\n| stats count by cause, job_name\n| sort -count\n```\n\nUnderstanding this SPL\n\n**Pipeline Failure Root Cause Trending** — Recurring failure causes (e.g., flaky tests, env issues) slow delivery. Trending root causes supports targeted remediation and stability.\n\nDocumented **Data sources**: Pipeline run logs, failure reasons, stage outcomes. **App/TA** (typical add-on context): Jenkins/GitHub Actions/Azure DevOps TAs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: cicd; **sourcetype**: jenkins:build. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=cicd, sourcetype=\"jenkins:build\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where result=\"FAILURE\"` — typically the threshold or rule expression for this monitoring goal.\n• Extracts fields with `rex` (regular expression).\n• `stats` rolls up events into metrics; results are split **by cause, job_name** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with the GitHub audit log. Verify field extractions and index permissions.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Bar chart (failures by cause), Table (job × cause), Line chart (failure rate trend).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Jenkins/GitHub Actions/Azure DevOps TAs — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=jenkins:build into cicd. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`cause, job_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch this part of your delivery chain for risks and reliability so issues show up before they hit customers.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
