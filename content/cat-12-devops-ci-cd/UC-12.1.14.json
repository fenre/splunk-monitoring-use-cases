{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.1.14",
  "title": "Fork Network Suspicious Activity",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Fork Network Suspicious Activity' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_fork_network_suspicious_activity` (UC-12.1.14), SOC dashboard panel \"Operations — Fork Network Suspicious Activity\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-12.1.14 positive (Fork, Network, Suspicious, Activity): ingest synthetic events into `index=devops` sourcetype='github:meta' where `unique_actors` grouped by `repo_name`, `_time` satisfies `unique_actors` > 50, or `count` > 200 for 'Fork Network Suspicious Activity'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-12.1.14 negative (Suspicious Activity): ingest benign `devops` / `github:meta` events where `repo_name`, `_time` stays below `unique_actors` > 50, or `count` > 200, or exclude the test entity via `ops_exception_register.csv`. Variant 10 confirms zero alert rows for 'Fork Network Suspicious Activity'."
  },
  "dataSources": "`fork`, `create` events; `WatchEvent` bursts Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "GitHub audit + Events API — Universal Forwarder (Splunkbase 617)",
  "spl": "index=devops sourcetype=\"github:meta\" event_type IN (\"ForkEvent\",\"WatchEvent\")\n| bin _time span=1h\n| stats dc(actor_id) as unique_actors, count by repo_name, _time\n| where unique_actors > 50 OR count > 200\n| sort -count",
  "description": "Monitors UC-12.1.14 — Fork, Network, Suspicious, Activity — using `index=devops` sourcetype `github:meta`, grouped by `repo_name`, `_time`. Outputs `unique_actors` each `1h`; alert when `unique_actors` > 50, or `count` > 200. Primary focus: Suspicious Activity. Purpose-built for 'Fork Network Suspicious Activity' in Operations workflows.",
  "value": "For 'Fork Network Suspicious Activity', UC-12.1.14 converts `unique_actors` on `github:meta` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Ingest public repo events for crown-jewel repositories. Correlate with token scope changes. Feed threat intel on abusive ASNs.",
  "visualization": "Line chart (fork/star rate), Table (spikes), Geo map (actor country).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `devops` (UC-12.1.14)** — Planned work for 'Fork Network Suspicious Activity' shifts `unique_actors` over `span=1h` while `repo_name`, `_time` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `dc` on `github:meta`** — Scheduled automation elevates `_time` without crossing `unique_actors` > 50 for UC-12.1.14. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `repo_name`, `_time`** — Health checks in `devops` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `github:meta`** — First 48h baseline for UC-12.1.14 on `repo_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `repo_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "github"
  ],
  "equipmentModels": [
    "github_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: GitHub audit + Events API.\n• Ensure the following data sources are available: `fork`, `create` events; `WatchEvent` bursts.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest public repo events for crown-jewel repositories. Correlate with token scope changes. Feed threat intel on abusive ASNs.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=devops sourcetype=\"github:meta\" event_type IN (\"ForkEvent\",\"WatchEvent\")\n| bin _time span=1h\n| stats dc(actor_id) as unique_actors, count by repo_name, _time\n| where unique_actors > 50 OR count > 200\n| sort -count\n```\n\nUnderstanding this SPL\n\n**Fork Network Suspicious Activity** — Sudden stars/forks from new accounts or geo clusters can precede supply-chain attacks or leaked-token cloning.\n\nDocumented **Data sources**: `fork`, `create` events; `WatchEvent` bursts. **App/TA** (typical add-on context): GitHub audit + Events API. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: devops; **sourcetype**: github:meta. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=devops, sourcetype=\"github:meta\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by repo_name, _time** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where unique_actors > 50 OR count > 200` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with the GitHub audit log. Verify field extractions and index permissions.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (fork/star rate), Table (spikes), Geo map (actor country).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (GitHub audit + Events API — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=github:meta into devops. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, repo_name, unique_actors`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch this part of your delivery chain for risks and reliability so issues show up before they hit customers.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
