{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.1.7",
  "title": "GitHub Actions Workflow Run Time Trending",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'GitHub Actions Workflow Run Time Trending' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_github_actions_workflow_run_time_trending` (UC-12.1.7), SOC dashboard panel \"Operations — GitHub Actions Workflow Run Time Trending\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=devops` sourcetype='github:actions_run' where `workflow_name` satisfies `avg_duration` > 0 for UC-12.1.7 ('GitHub Actions Workflow Run Time Trending'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `devops` / `github:actions_run` events where `workflow_name` stays below `avg_duration` > 0, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-12.1.7."
  },
  "dataSources": "GitHub /repos/:owner/:repo/actions/runs Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom (GitHub API) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=devops sourcetype=\"github:actions_run\"\n| eval duration_min=round((updated_at_epoch-run_started_at_epoch)/60,1)\n| timechart span=1d avg(duration_min) as avg_duration, median(duration_min) as median_duration by workflow_name\n| where avg_duration > 0",
  "description": "Workflow duration growth over time indicates growing tech debt, resource constraints, or inefficient pipeline design. Early detection enables optimization before velocity degrades.",
  "value": "Early 'GitHub Actions Workflow Run Time Trending' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Poll GitHub Actions API for workflow runs. Ingest run metadata (workflow_name, status, run_started_at, updated_at) to Splunk HEC. Calculate duration per run. Track 7-day and 30-day rolling averages. Alert when avg duration increases >20% week-over-week. Correlate with runner capacity and job concurrency.",
  "visualization": "Line chart (workflow duration trend by workflow), Bar chart (avg duration by workflow), Table (slowest workflows this week), Single value (p95 duration).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `devops`** — Planned work for 'GitHub Actions Workflow Run Time Trending' shifts `avg` over `span=1d` while `workflow_name` remains in-scope for UC-12.1.7. Cross-check CMDB CHG.\n\n2. **Batch `avg` on `github:actions_run`** — Scheduled automation elevates `duration_min` without crossing `avg_duration` > 0. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `workflow_name`** — Health checks in `devops` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `github:actions_run`** — First 48h baseline for `workflow_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `workflow_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "github"
  ],
  "equipmentModels": [
    "github_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Custom (GitHub API).\n• Ensure the following data sources are available: GitHub /repos/:owner/:repo/actions/runs.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nPoll GitHub Actions API for workflow runs. Ingest run metadata (workflow_name, status, run_started_at, updated_at) to Splunk HEC. Calculate duration per run. Track 7-day and 30-day rolling averages. Alert when avg duration increases >20% week-over-week. Correlate with runner capacity and job concurrency.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=devops sourcetype=\"github:actions_run\"\n| eval duration_min=round((updated_at_epoch-run_started_at_epoch)/60,1)\n| timechart span=1d avg(duration_min) as avg_duration, median(duration_min) as median_duration by workflow_name\n| where avg_duration > 0\n```\n\nUnderstanding this SPL\n\n**GitHub Actions Workflow Run Time Trending** — Workflow duration growth over time indicates growing tech debt, resource constraints, or inefficient pipeline design. Early detection enables optimization before velocity degrades.\n\nDocumented **Data sources**: GitHub /repos/:owner/:repo/actions/runs. **App/TA** (typical add-on context): Custom (GitHub API). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: devops; **sourcetype**: github:actions_run. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=devops, sourcetype=\"github:actions_run\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **duration_min** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `timechart` plots the metric over time using **span=1d** buckets with a separate series **by workflow_name** — ideal for trending and alerting on this use case.\n• Filters the current rows with `where avg_duration > 0` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with the GitHub audit log. Verify field extractions and index permissions.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (workflow duration trend by workflow), Bar chart (avg duration by workflow), Table (slowest workflows this week), Single value (p95 duration).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Custom (GitHub API) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=github:actions_run into devops. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`avg_duration, workflow_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch pipeline time and spend so surprise cost or slowdowns do not derail delivery.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
