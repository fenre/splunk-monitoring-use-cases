{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.4.1",
  "title": "Terraform Plan/Apply Tracking",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Configuration"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Terraform Plan/Apply Tracking' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_terraform_plan_apply_tracking` (UC-12.4.1), SOC dashboard panel \"Operations — Terraform Plan/Apply Tracking\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=iac` sourcetype='terraform:run' where `entity` satisfies detection threshold for UC-12.4.1 ('Terraform Plan/Apply Tracking'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `iac` / `terraform:run` events where `entity` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-12.4.1."
  },
  "dataSources": "Terraform CLI output (plan/apply), Terraform Cloud run events Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Terraform Cloud API, CI/CD output parsing — Universal Forwarder (Splunkbase 617)",
  "spl": "index=iac sourcetype=\"terraform:run\"\n| table _time, workspace, user, action, resources_added, resources_changed, resources_destroyed, status\n| sort -_time",
  "description": "Every Terraform apply changes infrastructure. Full audit trail enables change management, impact analysis, and rollback decisions.",
  "value": "Early 'Terraform Plan/Apply Tracking' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Send Terraform run events to Splunk via HEC (from CI/CD pipeline or Terraform Cloud webhooks). Track resource changes per workspace. Alert on destroy operations. Correlate infrastructure changes with monitoring alerts.",
  "visualization": "Table (recent Terraform runs), Timeline (apply events), Bar chart (resource changes by workspace), Single value (applies today).",
  "cimModels": [
    "Change"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Change.All_Changes\n  by All_Changes.user All_Changes.object All_Changes.action span=1h\n| sort -count",
  "dataModelAcceleration": "Enable acceleration on Change.All_Changes before using this tstats search.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Change",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Change",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `iac`** — Planned work for 'Terraform Plan/Apply Tracking' shifts `count` over `span=1h` while `host` remains in-scope for UC-12.4.1. Cross-check CMDB CHG.\n\n2. **Batch `count` on `terraform:run`** — Scheduled automation elevates `src` without crossing configured threshold. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `iac` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `terraform:run`** — First 48h baseline for `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "hashicorp"
  ],
  "equipmentModels": [
    "hashicorp_terraform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Terraform Cloud API, CI/CD output parsing.\n• Ensure the following data sources are available: Terraform CLI output (plan/apply), Terraform Cloud run events.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nSend Terraform run events to Splunk via HEC (from CI/CD pipeline or Terraform Cloud webhooks). Track resource changes per workspace. Alert on destroy operations. Correlate infrastructure changes with monitoring alerts.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=iac sourcetype=\"terraform:run\"\n| table _time, workspace, user, action, resources_added, resources_changed, resources_destroyed, status\n| sort -_time\n```\n\nUnderstanding this SPL\n\n**Terraform Plan/Apply Tracking** — Every Terraform apply changes infrastructure. Full audit trail enables change management, impact analysis, and rollback decisions.\n\nDocumented **Data sources**: Terraform CLI output (plan/apply), Terraform Cloud run events. **App/TA** (typical add-on context): Terraform Cloud API, CI/CD output parsing. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: iac; **sourcetype**: terraform:run. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=iac, sourcetype=\"terraform:run\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Pipeline stage (see **Terraform Plan/Apply Tracking**): table _time, workspace, user, action, resources_added, resources_changed, resources_destroyed, status\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with Terraform Cloud audit and run history. Verify field extractions and index permissions.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (recent Terraform runs), Timeline (apply events), Bar chart (resource changes by workspace), Single value (applies today).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Terraform Cloud API, CI/CD output parsing — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=terraform:run into iac. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, user, action`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch infrastructure-as-code runs and policy so changes to systems stay visible and governed.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
