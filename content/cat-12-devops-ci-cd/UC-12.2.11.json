{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.2.11",
  "title": "GitLab CI Runner Availability",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'GitLab CI Runner Availability' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_gitlab_ci_runner_availability` (UC-12.2.11), SOC dashboard panel \"Operations — GitLab CI Runner Availability\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=cicd` sourcetype='gitlab:runners' where `entity` satisfies detection threshold for UC-12.2.11 ('GitLab CI Runner Availability'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `cicd` / `gitlab:runners` events where `entity` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-12.2.11."
  },
  "dataSources": "GitLab /api/v4/runners, runner logs Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom (GitLab API) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=cicd sourcetype=\"gitlab:runners\"\n| where active=\"false\" OR paused=\"true\" OR (status!=\"online\" AND status!=\"idle\")\n| table _time, runner_id, description, active, paused, status, contacted_at\n| sort -_time",
  "description": "Runner registration status and job queue time affect CI throughput. Offline or paused runners cause jobs to wait or fail; monitoring ensures runner fleet health.",
  "value": "Early 'GitLab CI Runner Availability' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Poll GitLab /api/v4/runners for runner list and status. Ingest active, paused, contacted_at. Optionally parse runner logs for connectivity errors. Alert when runner goes inactive or paused. Track job queue time from pipeline events. Report on runner utilization and availability SLA.",
  "visualization": "Table (inactive/paused runners), Single value (available runners), Status grid (runner × status), Line chart (job queue time trend).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `cicd`** — Planned work for 'GitLab CI Runner Availability' shifts `count` over `span=1h` while `host` remains in-scope for UC-12.2.11. Cross-check CMDB CHG.\n\n2. **Batch `count` on `gitlab:runners`** — Scheduled automation elevates `src` without crossing configured threshold. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `cicd` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `gitlab:runners`** — First 48h baseline for `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "gitlab"
  ],
  "equipmentModels": [
    "gitlab_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Custom (GitLab API).\n• Ensure the following data sources are available: GitLab /api/v4/runners, runner logs.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nPoll GitLab /api/v4/runners for runner list and status. Ingest active, paused, contacted_at. Optionally parse runner logs for connectivity errors. Alert when runner goes inactive or paused. Track job queue time from pipeline events. Report on runner utilization and availability SLA.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=cicd sourcetype=\"gitlab:runners\"\n| where active=\"false\" OR paused=\"true\" OR (status!=\"online\" AND status!=\"idle\")\n| table _time, runner_id, description, active, paused, status, contacted_at\n| sort -_time\n```\n\nUnderstanding this SPL\n\n**GitLab CI Runner Availability** — Runner registration status and job queue time affect CI throughput. Offline or paused runners cause jobs to wait or fail; monitoring ensures runner fleet health.\n\nDocumented **Data sources**: GitLab /api/v4/runners, runner logs. **App/TA** (typical add-on context): Custom (GitLab API). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: cicd; **sourcetype**: gitlab:runners. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=cicd, sourcetype=\"gitlab:runners\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where active=\"false\" OR paused=\"true\" OR (status!=\"online\" AND status!=\"idle\")` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **GitLab CI Runner Availability**): table _time, runner_id, description, active, paused, status, contacted_at\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with the GitLab audit log or pipeline UI. Verify field extractions and index permissions.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (inactive/paused runners), Single value (available runners), Status grid (runner × status), Line chart (job queue time trend).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Custom (GitLab API) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=gitlab:runners into cicd. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, user, action`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch GitLab runner and pipeline signals so broken jobs, storage, or runners do not stall your teams.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
