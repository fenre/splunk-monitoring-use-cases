{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.2.30",
  "title": "Argo CD ApplicationSet Generator Matrix Empty or Error",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault",
    "Operations"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Argo CD ApplicationSet Generator Matrix Empty or Error' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_argo_cd_applicationset_generator_matrix_empty_or` (UC-12.2.30), SOC dashboard panel \"Operations — Argo CD ApplicationSet Generator Matrix Empty or Error\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-12.2.30 positive (Argo, ApplicationSet, Generator, Matrix, Empty): ingest synthetic events into `index=devops` sourcetype='argocd:applicationset' where `count` grouped by `name`, `namespace` satisfies detection threshold for 'Argo CD ApplicationSet Generator Matrix Empty or Error'. Validate filter `\"ApplicationSet\" AND (\"error\" OR \"failed\" OR \"generator\")`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-12.2.30 negative (or Error): ingest benign `devops` / `argocd:applicationset` events where `name`, `namespace` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 2 confirms zero alert rows for 'Argo CD ApplicationSet Generator Matrix Empty or Error'."
  },
  "dataSources": "ApplicationSet controller logs as `argocd:applicationset` or filtered `argocd:server` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Argo CD ApplicationSet controller → Splunk via Kubernetes log pipeline — Universal Forwarder (Splunkbase 617)",
  "spl": "index=devops sourcetype=\"argocd:applicationset\" OR (sourcetype=\"argocd:server\" ApplicationSet*)\n| search \"ApplicationSet\" AND (\"error\" OR \"failed\" OR \"generator\")\n| stats latest(_time) as t, count by name, namespace\n| sort -count",
  "description": "Surfaces UC-12.2.30 for Argo, ApplicationSet, Generator, Matrix, Empty over `index=devops` sourcetype `argocd:applicationset` with thresholds configured threshold. Groups `count` by `name`, `namespace` each `the scheduled window`. Differentiated by filter `\"ApplicationSet\" AND (\"error\" OR \"failed\" OR \"generator\")`. Supports repeatable operations triage for 'Argo CD ApplicationSet Generator Matrix Empty or Error'.",
  "value": "For 'Argo CD ApplicationSet Generator Matrix Empty or Error', UC-12.2.30 converts `activity` on `argocd:applicationset` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Tag controller logs with cluster label; correlate with SCM API rate limits.",
  "visualization": "Table (ApplicationSet × error count), Single value (open failures).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "Argo CD Splunk integration notes",
      "url": "https://github.com/argoproj/argo-cd/wiki/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Infrastructure Monitoring App for Argo CD (GitHub)",
      "url": "https://github.com/splunk/Splunk-Infrastructure-Monitoring-App-for-ArgoCD",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `devops` (UC-12.2.30)** — Planned work for 'Argo CD ApplicationSet Generator Matrix Empty or Error' shifts `count` over `span=1h` while `name`, `namespace` remains in-scope. Applies filter `\"ApplicationSet\" AND (\"error\" OR \"failed\" OR \"generator\")`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `argocd:applicationset`** — Scheduled automation elevates `namespace` without crossing configured threshold for UC-12.2.30. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `name`, `namespace`** — Health checks in `devops` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `argocd:applicationset`** — First 48h baseline for UC-12.2.30 on `name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "argocd",
    "kubernetes"
  ],
  "equipmentModels": [
    "kubernetes_k8s"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Argo CD ApplicationSet controller → Splunk via Kubernetes log pipeline — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=argocd:applicationset, argocd:server into devops. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`name, namespace`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch GitOps sync and Argo signals so clusters stay aligned with the desired configuration in Git.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
