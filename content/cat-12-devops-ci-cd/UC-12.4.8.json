{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.4.8",
  "title": "Release Gate and Approval Lag",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Release Gate and Approval Lag' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_release_gate_and_approval_lag` (UC-12.4.8), SOC dashboard panel \"Operations — Release Gate and Approval Lag\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-12.4.8 positive (Release, Gate, and, Approval, Lag): ingest synthetic events into `index=cicd` sourcetype='release:gate' where `avg_wait`, `max_wait` grouped by `stage_name`, `environment` satisfies `avg_wait` > 3600 for 'Release Gate and Approval Lag'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-12.4.8 negative (Approval Lag): ingest benign `cicd` / `release:gate` events where `stage_name`, `environment` stays below `avg_wait` > 3600, or exclude the test entity via `ops_exception_register.csv`. Variant 3 confirms zero alert rows for 'Release Gate and Approval Lag'."
  },
  "dataSources": "Gate and approval timestamps from release pipelines Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Release management / pipeline TAs — Universal Forwarder (Splunkbase 617)",
  "spl": "index=cicd sourcetype=\"release:gate\"\n| eval wait_sec=approved_time - submitted_time\n| stats avg(wait_sec) as avg_wait, max(wait_sec) as max_wait by stage_name, environment\n| where avg_wait > 3600",
  "description": "Surfaces when Approval Lag criteria fire for 'Release Gate and Approval Lag' (UC-12.4.8) in `index=cicd` sourcetype `release:gate`. Pipeline aggregates `avg_wait`, `max_wait` by `stage_name`, `environment` over `the scheduled window`. Alert logic enforces `avg_wait` > 3600. Title terms: Release, Gate, and, Approval, Lag. Eliminates manual hunting for UC-12.4.8 conditions.",
  "value": "UC-12.4.8 helps teams contain Release, Gate, and, Approval, Lag scenarios and reduce operational risk and delayed incident response before incidents compound.",
  "implementation": "Ingest gate and approval events from Azure DevOps, Spinnaker, or similar. Compute wait time per stage. Alert when average wait exceeds threshold. Report on approval latency by stage and approver.",
  "visualization": "Bar chart (wait time by stage), Table (slow gates), Line chart (approval latency trend).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `cicd` (UC-12.4.8)** — Planned work for 'Release Gate and Approval Lag' shifts `avg_wait`, `max_wait` over `span=1h` while `stage_name`, `environment` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `release:gate`** — Scheduled automation elevates `environment` without crossing `avg_wait` > 3600 for UC-12.4.8. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `stage_name`, `environment`** — Health checks in `cicd` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `release:gate`** — First 48h baseline for UC-12.4.8 on `stage_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `stage_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "azure"
  ],
  "equipmentModels": [
    "azure_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Release management / pipeline TAs.\n• Ensure the following data sources are available: Gate and approval timestamps from release pipelines.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest gate and approval events from Azure DevOps, Spinnaker, or similar. Compute wait time per stage. Alert when average wait exceeds threshold. Report on approval latency by stage and approver.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=cicd sourcetype=\"release:gate\"\n| eval wait_sec=approved_time - submitted_time\n| stats avg(wait_sec) as avg_wait, max(wait_sec) as max_wait by stage_name, environment\n| where avg_wait > 3600\n```\n\nUnderstanding this SPL\n\n**Release Gate and Approval Lag** — Long approval or gate wait times delay releases. Monitoring gate duration and approval latency supports process improvement.\n\nDocumented **Data sources**: Gate and approval timestamps from release pipelines. **App/TA** (typical add-on context): Release management / pipeline TAs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: cicd; **sourcetype**: release:gate. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=cicd, sourcetype=\"release:gate\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **wait_sec** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by stage_name, environment** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where avg_wait > 3600` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with the vendor console or native job history for that tool. Verify field extractions and index permissions.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Bar chart (wait time by stage), Table (slow gates), Line chart (approval latency trend).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Release management / pipeline TAs — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=release:gate into cicd. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`avg_wait, environment, stage_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch this part of your delivery chain for risks and reliability so issues show up before they hit customers.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
