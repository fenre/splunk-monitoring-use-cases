{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.3.19",
  "title": "AWX Inventory Sync From Cloud Source Failures",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault",
    "Configuration"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'AWX Inventory Sync From Cloud Source Failures' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_awx_inventory_sync_from_cloud_source_failures` (UC-12.3.19), SOC dashboard panel \"Operations — AWX Inventory Sync From Cloud Source Failures\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-12.3.19 positive (AWX, Inventory, Sync, From, Cloud): ingest synthetic events into `index=automation` sourcetype='awx:job_events' where `count` grouped by `inventory_id`, `source` satisfies detection threshold for 'AWX Inventory Sync From Cloud Source Failures'. Validate filter `\"inventory\" AND (\"sync\" OR \"update\") AND (\"fail\" OR \"error\" `. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-12.3.19 negative (Source Failures): ingest benign `automation` / `awx:job_events` events where `inventory_id`, `source` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 15 confirms zero alert rows for 'AWX Inventory Sync From Cloud Source Failures'."
  },
  "dataSources": "AWX inventory update jobs and server logs Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "AWX external logging + optional Tower syslog forwarder — Universal Forwarder (Splunkbase 617)",
  "spl": "index=automation sourcetype=\"awx:job_events\" OR sourcetype=\"awx:server\"\n| search \"inventory\" AND (\"sync\" OR \"update\") AND (\"fail\" OR \"error\" OR \"403\" OR \"429\")\n| stats latest(_time) as last_err, count by inventory_id, source\n| sort -count",
  "description": "Alerts on UC-12.3.19 — AWX, Inventory, Sync, From, Cloud — using `index=automation` sourcetype `awx:job_events`, grouped by `inventory_id`, `source`. Outputs `count` each `the scheduled window`; alert when configured threshold. Filter chain includes filter `\"inventory\" AND (\"sync\" OR \"update\") AND (\"fail\" OR \"error\" `. Purpose-built for 'AWX Inventory Sync From Cloud Source Failures' in Operations workflows.",
  "value": "UC-12.3.19 operationalises 'AWX Inventory Sync From Cloud Source Failures' against `awx:job_events` feeds so operators reduce operational risk and delayed incident response without manual correlation.",
  "implementation": "Map `inventory_id` to human name via lookup. Watch for cloud API throttling.",
  "visualization": "Table (inventory × source), Single value (open failures).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "AWX logging integration (job events, activity stream)",
      "url": "https://github.com/ansible/awx/blob/devel/docs/logging_integration.md",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HEC — HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `automation` (UC-12.3.19)** — Planned work for 'AWX Inventory Sync From Cloud Source Failures' shifts `count` over `span=1h` while `inventory_id`, `source` remains in-scope. Applies filter `\"inventory\" AND (\"sync\" OR \"update\") AND (\"fail\" OR \"error\" `. Cross-check CMDB CHG.\n\n2. **Batch `count` on `awx:job_events`** — Scheduled automation elevates `source` without crossing configured threshold for UC-12.3.19. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `inventory_id`, `source`** — Health checks in `automation` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `awx:job_events`** — First 48h baseline for UC-12.3.19 on `inventory_id` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `inventory_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "syslog"
  ],
  "equipmentModels": [
    "syslog_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (AWX external logging + optional Tower syslog forwarder — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=awx:job_events, awx:server into automation. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`inventory_id, source`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch this part of your delivery chain for risks and reliability so issues show up before they hit customers.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
