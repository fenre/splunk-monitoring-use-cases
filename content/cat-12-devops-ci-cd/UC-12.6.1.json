{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.6.1",
  "title": "DORA Metrics Trending Dashboard",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'DORA Metrics Trending Dashboard' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_dora_metrics_trending_dashboard` (UC-12.6.1), SOC dashboard panel \"Operations — DORA Metrics Trending Dashboard\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=devops` sourcetype='github:issues' that satisfy the UC-12.6.1 SPL filters for 'DORA Metrics Trending Dashboard'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `devops` / `github:issues` events outside the detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-12.6.1."
  },
  "dataSources": "`index=devops` `sourcetype IN (\"github:workflow_run\",\"gitlab:pipeline\",\"jenkins:build\")`; production deploy tags on `environment` or `branch` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "GitHub/GitLab/Jenkins integrations, optional Splunk DORA or custom summary searches — Universal Forwarder (Splunkbase 617)",
  "spl": "index=devops sourcetype=\"github:issues\" earliest=-180d@d (label=\"incident\" OR priority=\"P1\")\n| eval created_ts=strptime(created_at,\"%Y-%m-%dT%H:%M:%SZ\")\n| eval closed_ts=strptime(closed_at,\"%Y-%m-%dT%H:%M:%SZ\")\n| eval restore_hr=round((closed_ts-created_ts)/3600,2)\n| where restore_hr>=0\n| eval _time=closed_ts\n| timechart span=1mon median(restore_hr) as mttr_restore_hours\n| trendline sma3(mttr_restore_hours) as mttr_trend",
  "description": "Deployment frequency, lead time, change failure rate, and restore time summarize delivery health; month-over-month trends show whether engineering investments actually improved flow or reliability.",
  "value": "Early 'DORA Metrics Trending Dashboard' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Rarely one query fits all four DORA metrics—implement four saved searches (or a data model) that align on calendar months and team tags. Map GitHub Actions, GitLab pipelines, and Jenkins jobs to “production deploy” using branch/environment rules. For change failure rate, count failed production workflows over successful attempts to prod in the same window. Restore time often comes from incident tooling (`index=itsm`) if not in GitHub issues—join on service name. Validate timestamps are UTC-consistent.",
  "visualization": "Four-panel executive dashboard (line charts per metric), Optional radar chart for normalized month scores, Table (monthly KPI table export).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'DORA Metrics Trending Dashboard'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `devops` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `ops_exception_register.csv` with entity, reason, valid_until.",
  "equipment": [
    "github",
    "gitlab",
    "jenkins"
  ],
  "equipmentModels": [
    "github_generic",
    "gitlab_generic",
    "jenkins_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: GitHub/GitLab/Jenkins integrations, optional Splunk DORA or custom summary searches.\n• Ensure the following data sources are available: `index=devops` `sourcetype IN (\"github:workflow_run\",\"gitlab:pipeline\",\"jenkins:build\")`; production deploy tags on `environment` or `branch`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nRarely one query fits all four DORA metrics—implement four saved searches (or a data model) that align on calendar months and team tags. Map GitHub Actions, GitLab pipelines, and Jenkins jobs to “production deploy” using branch/environment rules. For change failure rate, count failed production workflows over successful attempts to prod in the same window. Restore time often comes from incident tooling (`index=itsm`) if not in GitHub issues—join on service name. Validate timestamps are UTC-consi…\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=devops sourcetype=\"github:issues\" earliest=-180d@d (label=\"incident\" OR priority=\"P1\")\n| eval created_ts=strptime(created_at,\"%Y-%m-%dT%H:%M:%SZ\")\n| eval closed_ts=strptime(closed_at,\"%Y-%m-%dT%H:%M:%SZ\")\n| eval restore_hr=round((closed_ts-created_ts)/3600,2)\n| where restore_hr>=0\n| eval _time=closed_ts\n| timechart span=1mon median(restore_hr) as mttr_restore_hours\n| trendline sma3(mttr_restore_hours) as mttr_trend\n```\n\nUnderstanding this SPL\n\n**DORA Metrics Trending Dashboard** — Deployment frequency, lead time, change failure rate, and restore time summarize delivery health; month-over-month trends show whether engineering investments actually improved flow or reliability.\n\nDocumented **Data sources**: `index=devops` `sourcetype IN (\"github:workflow_run\",\"gitlab:pipeline\",\"jenkins:build\")`; production deploy tags on `environment` or `branch`. **App/TA** (typical add-on context): GitHub/GitLab/Jenkins integrations, optional Splunk DORA or custom summary searches. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: devops; **sourcetype**: github:issues. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=devops, sourcetype=\"github:issues\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **created_ts** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **closed_ts** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **restore_hr** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where restore_hr>=0` — typically the threshold or rule expression for this monitoring goal.\n• `eval` defines or adjusts **_time** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `timechart` plots the metric over time using **span=1mon** buckets — ideal for trending and alerting on this use case.\n• Pipeline stage (see **DORA Metrics Trending Dashboard**): trendline sma3(mttr_restore_hours) as mttr_trend\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with the GitHub and GitLab audit or activity views for the same time range. Verify field extractions and index permissions.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Four-panel executive dashboard (line charts per metric), Optional radar chart for normalized month scores, Table (monthly KPI table export).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (GitHub/GitLab/Jenkins integrations, optional Splunk DORA or custom summary searches — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=github:issues into devops. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`closed_at, created_at, restore_hr`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We track delivery tempo and stability so leaders see whether we are shipping safely and often enough.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
