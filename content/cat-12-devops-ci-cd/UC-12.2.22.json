{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.2.22",
  "title": "Mean Time to Recovery (MTTR)",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Mean Time to Recovery (MTTR)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_mean_time_to_recovery_mttr` (UC-12.2.22), SOC dashboard panel \"Operations — Mean Time to Recovery (MTTR)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-12.2.22 positive (Mean, Time, Recovery, (MTTR)): ingest synthetic events into `index=itsm` sourcetype='pagerduty:incident' where `avg_mttr` grouped by `service` satisfies `avg_mttr` > 60 for 'Mean Time to Recovery (MTTR)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-12.2.22 negative (Recovery (MTTR)): ingest benign `itsm` / `pagerduty:incident` events where `service` stays below `avg_mttr` > 60, or exclude the test entity via `ops_exception_register.csv`. Variant 9 confirms zero alert rows for 'Mean Time to Recovery (MTTR)'."
  },
  "dataSources": "Incident `created_at`, `resolved_at`; rollback `deploy_time` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "PagerDuty/Opsgenie, deployment events — Universal Forwarder (Splunkbase 617)",
  "spl": "index=itsm sourcetype=\"pagerduty:incident\"\n| eval mttr_min=(resolved_epoch-ack_epoch)/60\n| stats avg(mttr_min) as avg_mttr, p95(mttr_min) as p95_mttr by service\n| where avg_mttr > 60\n| sort -avg_mttr",
  "description": "Detects UC-12.2.22 — Mean, Time, Recovery, (MTTR) — using `index=itsm` sourcetype `pagerduty:incident`, grouped by `service`. Outputs `avg_mttr` each `the scheduled window`; alert when `avg_mttr` > 60. Primary focus: Recovery (MTTR). Purpose-built for 'Mean Time to Recovery (MTTR)' in Operations workflows.",
  "value": "Operators running UC-12.2.22 on `pagerduty:incident` feeds shorten triage for Recovery (MTTR) tied to 'Mean Time to Recovery (MTTR)', lowering operational risk and delayed incident response.",
  "implementation": "Correlate PD incidents with service. For deploy failures, measure time to healthy deploy. Executive review monthly.",
  "visualization": "Table (MTTR by service), Line chart (avg MTTR trend), Gauge (vs SLA).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `itsm` (UC-12.2.22)** — Planned work for 'Mean Time to Recovery (MTTR)' shifts `avg_mttr` over `span=1h` while `service` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `pagerduty:incident`** — Scheduled automation elevates `mttr_min` without crossing `avg_mttr` > 60 for UC-12.2.22. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `service`** — Health checks in `itsm` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `pagerduty:incident`** — First 48h baseline for UC-12.2.22 on `service` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `service`, `exception_reason`, `valid_until`.",
  "equipment": [
    "pagerduty"
  ],
  "equipmentModels": [
    "pagerduty_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: PagerDuty/Opsgenie, deployment events.\n• Ensure the following data sources are available: Incident `created_at`, `resolved_at`; rollback `deploy_time`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nCorrelate PD incidents with service. For deploy failures, measure time to healthy deploy. Executive review monthly.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=itsm sourcetype=\"pagerduty:incident\"\n| eval mttr_min=(resolved_epoch-ack_epoch)/60\n| stats avg(mttr_min) as avg_mttr, p95(mttr_min) as p95_mttr by service\n| where avg_mttr > 60\n| sort -avg_mttr\n```\n\nUnderstanding this SPL\n\n**Mean Time to Recovery (MTTR)** — Time from incident detection or deploy failure to successful restore—core DORA metric.\n\nDocumented **Data sources**: Incident `created_at`, `resolved_at`; rollback `deploy_time`. **App/TA** (typical add-on context): PagerDuty/Opsgenie, deployment events. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: itsm; **sourcetype**: pagerduty:incident. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=itsm, sourcetype=\"pagerduty:incident\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **mttr_min** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by service** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where avg_mttr > 60` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with the vendor console or native job history for that tool. Verify field extractions and index permissions.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (MTTR by service), Line chart (avg MTTR trend), Gauge (vs SLA).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (PagerDuty/Opsgenie, deployment events — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=pagerduty:incident into itsm. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`avg_mttr, service`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch this part of your delivery chain for risks and reliability so issues show up before they hit customers.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
