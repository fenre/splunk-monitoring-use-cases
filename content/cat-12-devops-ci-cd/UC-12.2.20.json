{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.2.20",
  "title": "Deployment Frequency Tracking",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Deployment Frequency Tracking' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_deployment_frequency_tracking` (UC-12.2.20), SOC dashboard panel \"Operations — Deployment Frequency Tracking\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-12.2.20 positive (Deployment, Frequency, Tracking): ingest synthetic events into `index=cicd` sourcetype='deployment_event' where `count` grouped by `_time`, `team`, `service` satisfies detection threshold for 'Deployment Frequency Tracking'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-12.2.20 negative (Frequency Tracking): ingest benign `cicd` / `deployment_event` events where `_time`, `team`, `service` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 7 confirms zero alert rows for 'Deployment Frequency Tracking'."
  },
  "dataSources": "Normalized deploy events with `service`, `team` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`deployment_event` HEC — Universal Forwarder (Splunkbase 617)",
  "spl": "index=cicd sourcetype=\"deployment_event\" environment=\"production\"\n| bin _time span=1d\n| stats count as deploys by _time, team, service\n| sort -deploys",
  "description": "Monitors UC-12.2.20 — Deployment, Frequency, Tracking — using `index=cicd` sourcetype `deployment_event`, grouped by `_time`, `team`, `service`. Outputs `count` each `1d`; alert when configured threshold. Primary focus: Frequency Tracking. Purpose-built for 'Deployment Frequency Tracking' in Operations workflows.",
  "value": "UC-12.2.20 operationalises 'Deployment Frequency Tracking' against `deployment_event` feeds so operators reduce operational risk and delayed incident response without manual correlation.",
  "implementation": "Tag all deploy pipelines. Weekly report to leadership. Compare to DORA elite thresholds (on-demand per day).",
  "visualization": "Line chart (deploys per day by team), Bar chart (by service), Single value (deploys 7d).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `cicd` (UC-12.2.20)** — Planned work for 'Deployment Frequency Tracking' shifts `count` over `span=1d` while `_time`, `team`, `service` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `deployment_event`** — Scheduled automation elevates `team` without crossing configured threshold for UC-12.2.20. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `_time`, `team`, `service`** — Health checks in `cicd` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `deployment_event`** — First 48h baseline for UC-12.2.20 on `_time` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `_time`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `deployment_event` HEC.\n• Ensure the following data sources are available: Normalized deploy events with `service`, `team`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nTag all deploy pipelines. Weekly report to leadership. Compare to DORA elite thresholds (on-demand per day).\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=cicd sourcetype=\"deployment_event\" environment=\"production\"\n| bin _time span=1d\n| stats count as deploys by _time, team, service\n| sort -deploys\n```\n\nUnderstanding this SPL\n\n**Deployment Frequency Tracking** — DORA dashboard slice—deployments per service per day with team and application tags (extends UC-12.2.3).\n\nDocumented **Data sources**: Normalized deploy events with `service`, `team`. **App/TA** (typical add-on context): `deployment_event` HEC. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: cicd; **sourcetype**: deployment_event. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=cicd, sourcetype=\"deployment_event\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by _time, team, service** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with the vendor console or native job history for that tool. Verify field extractions and index permissions.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (deploys per day by team), Bar chart (by service), Single value (deploys 7d).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`deployment_event` HEC — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=deployment_event into cicd. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, service, team`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We track delivery tempo and stability so leaders see whether we are shipping safely and often enough.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
