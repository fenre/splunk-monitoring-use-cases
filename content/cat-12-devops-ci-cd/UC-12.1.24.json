{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.1.24",
  "title": "GitLab Container Registry Garbage Collection and Blob Errors",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity",
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'GitLab Container Registry Garbage Collection and Blob Errors' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_gitlab_container_registry_garbage_collection_and` (UC-12.1.24), SOC dashboard panel \"Operations — GitLab Container Registry Garbage Collection and Blob Errors\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-12.1.24 positive (GitLab, Container, Registry, Garbage, Collection): ingest synthetic events into `index=devops` sourcetype='gitlab:registry' where `count` grouped by `host`, `message_snippet` satisfies detection threshold for 'GitLab Container Registry Garbage Collection and Blob Errors'. Validate filter `\"blob\" AND (\"delete\" OR \"gc\" OR \"garbage\" OR \"manifest unkno`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-12.1.24 negative (Blob Errors): ingest benign `devops` / `gitlab:registry` events where `host`, `message_snippet` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 9 confirms zero alert rows for 'GitLab Container Registry Garbage Collection and Blob Errors'."
  },
  "dataSources": "GitLab Registry or Rails logs with registry component (`sourcetype=gitlab:registry` normalized) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "GitLab Omnibus logs via Universal Forwarder; Registry stdout JSON if containerized — Universal Forwarder (Splunkbase 617)",
  "spl": "index=devops sourcetype=\"gitlab:registry\" OR (sourcetype=\"gitlab:application\" component=\"registry\")\n| search \"blob\" AND (\"delete\" OR \"gc\" OR \"garbage\" OR \"manifest unknown\" OR \"unknown blob\")\n| stats count, latest(_time) as last_seen by host, message_snippet\n| sort -count",
  "description": "Monitors UC-12.1.24 — GitLab, Container, Registry, Garbage, Collection — using `index=devops` sourcetype `gitlab:registry`, grouped by `host`, `message_snippet`. Outputs `count` each `the scheduled window`; alert when configured threshold. Filter chain includes filter `\"blob\" AND (\"delete\" OR \"gc\" OR \"garbage\" OR \"manifest unkno`. Purpose-built for 'GitLab Container Registry Garbage Collection and Blob Errors' in Operations workflows.",
  "value": "Operators running UC-12.1.24 on `gitlab:registry` feeds shorten triage for Blob Errors tied to 'GitLab Container Registry Garbage Collection and Blob Errors', lowering operational risk and delayed incident response.",
  "implementation": "Ship registry logs to Splunk; truncate `message_snippet` at ingest. Page on sustained error bursts correlated with disk alerts.",
  "visualization": "Bar chart (error categories), Timeline of registry errors.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "GitLab Add-on for Splunk",
      "url": "https://splunkbase.splunk.com/app/6848",
      "retrieved": "2026-04-25"
    },
    {
      "title": "GitLab CI/CD logs to Splunk (Lantern)",
      "url": "https://lantern.splunk.com/Data_Descriptors/GitLab/Getting_Gitlab_CICD_data_out_of_a_Gitlab_Pipeline_into_Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `devops` (UC-12.1.24)** — Planned work for 'GitLab Container Registry Garbage Collection and Blob Errors' shifts `count` over `span=1h` while `host`, `message_snippet` remains in-scope. Applies filter `\"blob\" AND (\"delete\" OR \"gc\" OR \"garbage\" OR \"manifest unkno`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `gitlab:registry`** — Scheduled automation elevates `message_snippet` without crossing configured threshold for UC-12.1.24. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`, `message_snippet`** — Health checks in `devops` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `gitlab:registry`** — First 48h baseline for UC-12.1.24 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "gitlab"
  ],
  "equipmentModels": [
    "gitlab_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (GitLab Omnibus logs via Universal Forwarder; Registry stdout JSON if containerized — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=gitlab:registry, gitlab:application into devops. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, message_snippet`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch packages and artifacts so supply-chain risk, storage, and policy checks stay under control.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
