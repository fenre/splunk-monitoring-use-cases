{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.5.4",
  "title": "GitHub Actions Workflow Failure Rate",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance",
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'GitHub Actions Workflow Failure Rate' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_github_actions_workflow_failure_rate` (UC-12.5.4), SOC dashboard panel \"Operations — GitHub Actions Workflow Failure Rate\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=devops` sourcetype='github:workflow_run' that satisfy the UC-12.5.4 SPL filters for 'GitHub Actions Workflow Failure Rate'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `devops` / `github:workflow_run` events outside the detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-12.5.4."
  },
  "dataSources": "`sourcetype=github:workflow_run`, `sourcetype=github:webhook` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "GitHub Audit Log / webhook forwarder, Splunk HTTP Event Collector — Universal Forwarder (Splunkbase 617)",
  "spl": "index=devops sourcetype=\"github:workflow_run\"\n| eval failed=if(conclusion IN (\"failure\",\"cancelled\",\"timed_out\"),1,0)\n| timechart span=1h sum(failed) as failures, count as runs\n| eval failure_rate=round(100*failures/runs,2)\n| fields _time, failure_rate, failures, runs",
  "description": "A rising workflow failure rate signals flaky pipelines, bad merges, or infra issues that slow delivery and can block production paths.",
  "value": "Early 'GitHub Actions Workflow Failure Rate' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Ingest workflow_run events with conclusion, workflow, branch, and repository. Compute failure rate over sliding windows per repo or default branch. Alert when failure_rate exceeds baseline or a fixed threshold. Exclude expected flaky jobs via labels when possible.",
  "visualization": "Line chart (failure rate), Stacked bar (conclusions), Single value (last 24h failure %), Table (top failing workflows).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'GitHub Actions Workflow Failure Rate'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `devops` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `ops_exception_register.csv` with entity, reason, valid_until.",
  "equipment": [
    "github"
  ],
  "equipmentModels": [
    "github_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: GitHub Audit Log / webhook forwarder, Splunk HTTP Event Collector.\n• Ensure the following data sources are available: `sourcetype=github:workflow_run`, `sourcetype=github:webhook`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest workflow_run events with conclusion, workflow, branch, and repository. Compute failure rate over sliding windows per repo or default branch. Alert when failure_rate exceeds baseline or a fixed threshold. Exclude expected flaky jobs via labels when possible.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=devops sourcetype=\"github:workflow_run\"\n| eval failed=if(conclusion IN (\"failure\",\"cancelled\",\"timed_out\"),1,0)\n| timechart span=1h sum(failed) as failures, count as runs\n| eval failure_rate=round(100*failures/runs,2)\n| fields _time, failure_rate, failures, runs\n```\n\nUnderstanding this SPL\n\n**GitHub Actions Workflow Failure Rate** — A rising workflow failure rate signals flaky pipelines, bad merges, or infra issues that slow delivery and can block production paths.\n\nDocumented **Data sources**: `sourcetype=github:workflow_run`, `sourcetype=github:webhook`. **App/TA** (typical add-on context): GitHub Audit Log / webhook forwarder, Splunk HTTP Event Collector. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: devops; **sourcetype**: github:workflow_run. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=devops, sourcetype=\"github:workflow_run\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **failed** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `timechart` plots the metric over time using **span=1h** buckets — ideal for trending and alerting on this use case.\n• `eval` defines or adjusts **failure_rate** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Keeps or drops fields with `fields` to shape columns and size.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with the GitHub audit log. Verify field extractions and index permissions.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (failure rate), Stacked bar (conclusions), Single value (last 24h failure %), Table (top failing workflows).",
  "grandmaExplanation": "We watch build outcomes so we spot quality or infrastructure problems before they become a release blocker.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
