{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.2.2",
  "title": "Build Duration Monitoring",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Build Duration Monitoring' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_build_duration_monitoring` (UC-12.2.2), SOC dashboard panel \"Operations — Build Duration Monitoring\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-12.2.2 positive (Build, Duration, Monitoring): ingest synthetic events into `index=cicd` sourcetype='jenkins:build' where `avg_build_time` grouped by `job_name` satisfies detection threshold for 'Build Duration Monitoring'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-12.2.2 negative (Duration Monitoring): ingest benign `cicd` / `jenkins:build` events where `job_name` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 3 confirms zero alert rows for 'Build Duration Monitoring'."
  },
  "dataSources": "Build start/end timestamps Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk App for Jenkins, CI/CD metrics — Universal Forwarder (Splunkbase 617)",
  "spl": "index=cicd sourcetype=\"jenkins:build\" result=\"SUCCESS\"\n| eval duration_min=round(duration/60000,1)\n| timechart span=1d avg(duration_min) as avg_build_time by job_name",
  "description": "Identifies when Duration Monitoring criteria fire for 'Build Duration Monitoring' (UC-12.2.2) in `index=cicd` sourcetype `jenkins:build`. Pipeline aggregates `avg_build_time` by `job_name` over `1d`. Alert logic enforces configured threshold. Title terms: Build, Duration, Monitoring. Eliminates manual hunting for UC-12.2.2 conditions.",
  "value": "UC-12.2.2 helps teams contain Build, Duration, Monitoring scenarios and reduce operational risk and delayed incident response before incidents compound.",
  "implementation": "Track build duration for all pipelines. Alert when duration exceeds historical average by >50%. Identify slow build steps. Correlate with infrastructure metrics (runner CPU, disk I/O) to find bottlenecks.",
  "visualization": "Line chart (build duration trend), Bar chart (avg duration by pipeline), Table (slowest builds today).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `cicd` (UC-12.2.2)** — Planned work for 'Build Duration Monitoring' shifts `avg_build_time` over `span=1d` while `job_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `jenkins:build`** — Scheduled automation elevates `duration_min` without crossing configured threshold for UC-12.2.2. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `job_name`** — Health checks in `cicd` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `jenkins:build`** — First 48h baseline for UC-12.2.2 on `job_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `job_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "jenkins"
  ],
  "equipmentModels": [
    "jenkins_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk App for Jenkins, CI/CD metrics.\n• Ensure the following data sources are available: Build start/end timestamps.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nTrack build duration for all pipelines. Alert when duration exceeds historical average by >50%. Identify slow build steps. Correlate with infrastructure metrics (runner CPU, disk I/O) to find bottlenecks.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=cicd sourcetype=\"jenkins:build\" result=\"SUCCESS\"\n| eval duration_min=round(duration/60000,1)\n| timechart span=1d avg(duration_min) as avg_build_time by job_name\n```\n\nUnderstanding this SPL\n\n**Build Duration Monitoring** — Increasing build times slow development velocity. Detection enables build optimization and infrastructure right-sizing.\n\nDocumented **Data sources**: Build start/end timestamps. **App/TA** (typical add-on context): Splunk App for Jenkins, CI/CD metrics. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: cicd; **sourcetype**: jenkins:build. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=cicd, sourcetype=\"jenkins:build\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **duration_min** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `timechart` plots the metric over time using **span=1d** buckets with a separate series **by job_name** — ideal for trending and alerting on this use case.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with Jenkins build history. Verify field extractions and index permissions.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (build duration trend), Bar chart (avg duration by pipeline), Table (slowest builds today).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk App for Jenkins, CI/CD metrics — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=jenkins:build into cicd. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`job_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch how long builds take so slow pipelines and flaky steps are visible before deadlines slip.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
