{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.6.4",
  "title": "Container Image Build Time Trending",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Container Image Build Time Trending' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_container_image_build_time_trending` (UC-12.6.4), SOC dashboard panel \"Operations — Container Image Build Time Trending\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=devops` sourcetype='target_sourcetype' that satisfy the UC-12.6.4 SPL filters for 'Container Image Build Time Trending'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `devops` / `target_sourcetype` events outside the detection threshold, or exclude the test entity via `operational_exceptions.csv` with a valid window. Confirm zero alert rows for UC-12.6.4."
  },
  "dataSources": "`index=devops` `sourcetype IN (\"gitlab:job\",\"github:workflow_job\",\"jenkins:build\")` filtered to `image`/`container`/`docker` job or stage names Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "GitLab CI, GitHub Actions, Jenkins Pipelines with Kaniko/buildkit logging — Universal Forwarder (Splunkbase 617)",
  "spl": "index=devops sourcetype IN (\"gitlab:job\",\"github:workflow_job\") earliest=-90d@d\n| eval job_lower=lower(coalesce(name,job_name,workflow_name))\n| where match(job_lower,\"(?i)image|container|docker|build.*push|kaniko|buildkit\")\n| eval dur_min=round(coalesce(duration_sec,duration)/60,2)\n| eval sprint=strftime(_time,\"%Y-W%V\")\n| stats avg(dur_min) as avg_build_min median(dur_min) as med_build_min by sprint, project\n| eventstats median(avg_build_min) as fleet_med by sprint\n| eval regression=if(avg_build_min > fleet_med*1.35,1,0)\n| sort sprint project",
  "description": "Longer image builds slow every downstream deploy; sprint-level trends catch Dockerfile regressions, bloated layers, or registry latency before they dominate CI budgets.",
  "value": "Early 'Container Image Build Time Trending' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Standardize job naming so filters stay reliable; alternatively maintain a lookup of pipeline IDs that produce images. Strip cache-hit jobs if duration is near-zero noise. Compare medians per repo against its own 8-sprint baseline to reduce cross-team skew. Pair with container registry pull latency dashboards when build times spike only in certain regions.",
  "visualization": "Line chart (average image build minutes by sprint), Bar chart (top regressing projects), Table (sprint, project, avg, median).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'Container Image Build Time Trending'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `devops` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `operational_exceptions.csv` with entity, reason, valid_until.",
  "equipment": [
    "docker",
    "github",
    "gitlab",
    "jenkins"
  ],
  "equipmentModels": [
    "docker_generic",
    "github_generic",
    "gitlab_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: GitLab CI, GitHub Actions, Jenkins Pipelines with Kaniko/buildkit logging.\n• Ensure the following data sources are available: `index=devops` `sourcetype IN (\"gitlab:job\",\"github:workflow_job\",\"jenkins:build\")` filtered to `image`/`container`/`docker` job or stage names.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nStandardize job naming so filters stay reliable; alternatively maintain a lookup of pipeline IDs that produce images. Strip cache-hit jobs if duration is near-zero noise. Compare medians per repo against its own 8-sprint baseline to reduce cross-team skew. Pair with container registry pull latency dashboards when build times spike only in certain regions.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=devops sourcetype IN (\"gitlab:job\",\"github:workflow_job\") earliest=-90d@d\n| eval job_lower=lower(coalesce(name,job_name,workflow_name))\n| where match(job_lower,\"(?i)image|container|docker|build.*push|kaniko|buildkit\")\n| eval dur_min=round(coalesce(duration_sec,duration)/60,2)\n| eval sprint=strftime(_time,\"%Y-W%V\")\n| stats avg(dur_min) as avg_build_min median(dur_min) as med_build_min by sprint, project\n| eventstats median(avg_build_min) as fleet_med by sprint\n| eval regression=if(avg_build_min > fleet_med*1.35,1,0)\n| sort sprint project\n```\n\nUnderstanding this SPL\n\n**Container Image Build Time Trending** — Longer image builds slow every downstream deploy; sprint-level trends catch Dockerfile regressions, bloated layers, or registry latency before they dominate CI budgets.\n\nDocumented **Data sources**: `index=devops` `sourcetype IN (\"gitlab:job\",\"github:workflow_job\",\"jenkins:build\")` filtered to `image`/`container`/`docker` job or stage names. **App/TA** (typical add-on context): GitLab CI, GitHub Actions, Jenkins Pipelines with Kaniko/buildkit logging. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: devops.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=devops, time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **job_lower** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where match(job_lower,\"(?i)image|container|docker|build.*push|kaniko|buildkit\")` — typically the threshold or rule expression for this monitoring goal.\n• `eval` defines or adjusts **dur_min** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **sprint** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by sprint, project** so each row reflects one combination of those dimensions.\n• `eventstats` rolls up events into metrics; results are split **by sprint** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **regression** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with the GitHub and GitLab audit or activity views for the same time range. Verify field extractions and index permissions.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (average image build minutes by sprint), Bar chart (top regressing projects), Table (sprint, project, avg, median).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (GitLab CI, GitHub Actions, Jenkins Pipelines with Kaniko/buildkit logging — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=gitlab:job, github:workflow_job into devops. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`project, regression, sprint`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch this part of your delivery chain for risks and reliability so issues show up before they hit customers.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
