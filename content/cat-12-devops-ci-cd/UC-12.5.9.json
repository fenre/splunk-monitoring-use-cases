{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.5.9",
  "title": "Kustomize Build Error Tracking",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault",
    "Configuration"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Kustomize Build Error Tracking' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_kustomize_build_error_tracking` (UC-12.5.9), SOC dashboard panel \"Operations — Kustomize Build Error Tracking\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-12.5.9 positive (Kustomize, Build, Error, Tracking): ingest synthetic events into `index=devops` sourcetype='fluxcd:controller' where `count` grouped by `project`, `pipeline_id`, `err_msg` satisfies detection threshold for 'Kustomize Build Error Tracking'. Validate filter `kustomize_build OR \"kustomize build\" OR \"error building kust`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-12.5.9 negative (Error Tracking): ingest benign `devops` / `fluxcd:controller` events where `project`, `pipeline_id`, `err_msg` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 12 confirms zero alert rows for 'Kustomize Build Error Tracking'."
  },
  "dataSources": "`sourcetype=gitlab:job`, `sourcetype=github:workflow_job`, `sourcetype=fluxcd:controller` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "CI pipeline logs, Flux kustomize-controller — Universal Forwarder (Splunkbase 617)",
  "spl": "index=devops (sourcetype=\"fluxcd:controller\" OR sourcetype=\"gitlab:job\" OR sourcetype=\"github:workflow_job\")\n| search kustomize_build OR \"kustomize build\" OR \"error building kustomize\"\n| rex field=_raw \"(?<err_msg>kustomize:.*|error:.*)\"\n| stats count by project, pipeline_id, err_msg\n| sort -count",
  "description": "Alerts on UC-12.5.9 — Kustomize, Build, Error, Tracking — using `index=devops` sourcetype `fluxcd:controller`, grouped by `project`, `pipeline_id`, `err_msg`. Outputs `count` each `the scheduled window`; alert when configured threshold. Filter chain includes filter `kustomize_build OR \"kustomize build\" OR \"error building kust`. Purpose-built for 'Kustomize Build Error Tracking' in Operations workflows.",
  "value": "Tracking `activity` via UC-12.5.9 on `fluxcd:controller` feeds improves response to 'Kustomize Build Error Tracking' and reduces operational risk and delayed incident response.",
  "implementation": "Capture stderr from CI jobs and Flux kustomize-controller when `kustomize build` runs. Extract file paths and duplicate key errors. Alert on any build failure on protected branches or for production overlays. Feed counts back to repo owners.",
  "visualization": "Table (build errors), Bar chart (errors by repo), Timeline (failure events).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `devops` (UC-12.5.9)** — Planned work for 'Kustomize Build Error Tracking' shifts `count` over `span=1h` while `project`, `pipeline_id`, `err_msg` remains in-scope. Applies filter `kustomize_build OR \"kustomize build\" OR \"error building kust`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `fluxcd:controller`** — Scheduled automation elevates `pipeline_id` without crossing configured threshold for UC-12.5.9. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `project`, `pipeline_id`, `err_msg`** — Health checks in `devops` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `fluxcd:controller`** — First 48h baseline for UC-12.5.9 on `project` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `project`, `exception_reason`, `valid_until`.",
  "equipment": [
    "github",
    "gitlab"
  ],
  "equipmentModels": [
    "github_generic",
    "gitlab_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: CI pipeline logs, Flux kustomize-controller.\n• Ensure the following data sources are available: `sourcetype=gitlab:job`, `sourcetype=github:workflow_job`, `sourcetype=fluxcd:controller`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nCapture stderr from CI jobs and Flux kustomize-controller when `kustomize build` runs. Extract file paths and duplicate key errors. Alert on any build failure on protected branches or for production overlays. Feed counts back to repo owners.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=devops (sourcetype=\"fluxcd:controller\" OR sourcetype=\"gitlab:job\" OR sourcetype=\"github:workflow_job\")\n| search kustomize_build OR \"kustomize build\" OR \"error building kustomize\"\n| rex field=_raw \"(?<err_msg>kustomize:.*|error:.*)\"\n| stats count by project, pipeline_id, err_msg\n| sort -count\n```\n\nUnderstanding this SPL\n\n**Kustomize Build Error Tracking** — Kustomize build failures block manifests from applying; early detection shortens fix time for overlay, patch, or base reference mistakes.\n\nDocumented **Data sources**: `sourcetype=gitlab:job`, `sourcetype=github:workflow_job`, `sourcetype=fluxcd:controller`. **App/TA** (typical add-on context): CI pipeline logs, Flux kustomize-controller. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: devops; **sourcetype**: fluxcd:controller, gitlab:job, github:workflow_job. Those sourcetypes align with what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=devops, sourcetype=\"fluxcd:controller\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Applies an explicit `search` filter to narrow the current result set.\n• Extracts fields with `rex` (regular expression).\n• `stats` rolls up events into metrics; results are split **by project, pipeline_id, err_msg** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with the GitHub and GitLab audit or activity views for the same time range. Verify field extractions and index permissions.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (build errors), Bar chart (errors by repo), Timeline (failure events).",
  "grandmaExplanation": "We watch Kubernetes release tooling so manifest or chart problems surface before users do.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
