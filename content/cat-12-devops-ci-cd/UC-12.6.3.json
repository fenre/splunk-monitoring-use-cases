{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.6.3",
  "title": "Build Queue Wait Time Trending",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Build Queue Wait Time Trending' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_build_queue_wait_time_trending` (UC-12.6.3), SOC dashboard panel \"Operations — Build Queue Wait Time Trending\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-12.6.3 positive (Build, Queue, Wait, Time, Trending): ingest synthetic events into `index=devops` sourcetype='jenkins:build' where `avg_wait_sec`, `med_wait` grouped by `entity` satisfies -30d for 'Build Queue Wait Time Trending'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-12.6.3 negative (Time Trending): ingest benign `devops` / `jenkins:build` events where `entity` stays below -30d, or exclude the test entity via `ops_exception_register.csv`. Variant 14 confirms zero alert rows for 'Build Queue Wait Time Trending'."
  },
  "dataSources": "`index=devops` `sourcetype=\"jenkins:queue\"` or `sourcetype=\"jenkins:build\"` with `queue_wait_ms`; `sourcetype=\"gitlab:job\"` with `queued_duration` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Jenkins metrics (`metrics` plugin), GitLab Runner metrics, GitHub Actions (job queue via API-derived events) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=devops (sourcetype=\"jenkins:build\" OR sourcetype=\"gitlab:job\") earliest=-30d@d\n| eval wait_sec=coalesce(queue_wait_ms/1000, queued_duration, queue_time_sec)\n| where isnotnull(wait_sec)\n| timechart span=1d avg(wait_sec) as avg_wait_sec p95(wait_sec) as p95_wait_sec\n| eval avg_wait_min=round(avg_wait_sec/60,2)\n| trendline sma7(avg_wait_min) as wait_trend\n| eventstats median(avg_wait_min) as med_wait\n| eval backlog_pressure=if(avg_wait_min > med_wait*1.5,1,0)",
  "description": "Detects UC-12.6.3 — Build, Queue, Wait, Time, Trending — using `index=devops` sourcetype `jenkins:build`, grouped by `entity`. Outputs `avg_wait_sec`, `med_wait` each `1d`; alert when -30d. Primary focus: Time Trending. Purpose-built for 'Build Queue Wait Time Trending' in Operations workflows.",
  "value": "Alerting on Build, Queue, Wait, Time, Trending through UC-12.6.3 reduces operational risk and delayed incident response by surfacing `avg_wait_sec`, `med_wait` early.",
  "implementation": "Ensure `wait_sec` excludes container provisioning if that is tracked separately. For GitHub-only shops, ingest workflow_job events with `queued_at` and `started_at` to derive wait. Split by `label` or `runner_group` to see constrained pools. Alert when p95 wait exceeds SLA for two consecutive days.",
  "visualization": "Line chart (average and p95 wait in minutes), Area chart (wait distribution bands), Single value (current p95 wait).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `devops` (UC-12.6.3)** — Planned work for 'Build Queue Wait Time Trending' shifts `avg_wait_sec`, `med_wait` over `span=1d` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `jenkins:build`** — Scheduled automation elevates `wait_sec` without crossing configured threshold for UC-12.6.3. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `devops` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `jenkins:build`** — First 48h baseline for UC-12.6.3 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "github",
    "gitlab",
    "jenkins"
  ],
  "equipmentModels": [
    "github_generic",
    "gitlab_generic",
    "jenkins_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Jenkins metrics (`metrics` plugin), GitLab Runner metrics, GitHub Actions (job queue via API-derived events).\n• Ensure the following data sources are available: `index=devops` `sourcetype=\"jenkins:queue\"` or `sourcetype=\"jenkins:build\"` with `queue_wait_ms`; `sourcetype=\"gitlab:job\"` with `queued_duration`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnsure `wait_sec` excludes container provisioning if that is tracked separately. For GitHub-only shops, ingest workflow_job events with `queued_at` and `started_at` to derive wait. Split by `label` or `runner_group` to see constrained pools. Alert when p95 wait exceeds SLA for two consecutive days.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=devops (sourcetype=\"jenkins:build\" OR sourcetype=\"gitlab:job\") earliest=-30d@d\n| eval wait_sec=coalesce(queue_wait_ms/1000, queued_duration, queue_time_sec)\n| where isnotnull(wait_sec)\n| timechart span=1d avg(wait_sec) as avg_wait_sec p95(wait_sec) as p95_wait_sec\n| eval avg_wait_min=round(avg_wait_sec/60,2)\n| trendline sma7(avg_wait_min) as wait_trend\n| eventstats median(avg_wait_min) as med_wait\n| eval backlog_pressure=if(avg_wait_min > med_wait*1.5,1,0)\n```\n\nUnderstanding this SPL\n\n**Build Queue Wait Time Trending** — Queue wait reflects runner capacity and pipeline fan-in; rising waits delay feedback and release trains even when job success rates look fine.\n\nDocumented **Data sources**: `index=devops` `sourcetype=\"jenkins:queue\"` or `sourcetype=\"jenkins:build\"` with `queue_wait_ms`; `sourcetype=\"gitlab:job\"` with `queued_duration`. **App/TA** (typical add-on context): Jenkins metrics (`metrics` plugin), GitLab Runner metrics, GitHub Actions (job queue via API-derived events). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: devops; **sourcetype**: jenkins:build, gitlab:job. Those sourcetypes align with what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=devops, sourcetype=\"jenkins:build\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **wait_sec** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where isnotnull(wait_sec)` — typically the threshold or rule expression for this monitoring goal.\n• `timechart` plots the metric over time using **span=1d** buckets — ideal for trending and alerting on this use case.\n• `eval` defines or adjusts **avg_wait_min** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Pipeline stage (see **Build Queue Wait Time Trending**): trendline sma7(avg_wait_min) as wait_trend\n• `eventstats` aggregates the pipeline (counts, distinct values, sums, percentiles, etc.) into fewer rows.\n• `eval` defines or adjusts **backlog_pressure** — often to normalize units, derive a ratio, or prepare for thresholds.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with the GitHub and GitLab audit or activity views for the same time range. Verify field extractions and index permissions.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (average and p95 wait in minutes), Area chart (wait distribution bands), Single value (current p95 wait).",
  "grandmaExplanation": "We watch queueing and wait time so developers are not stuck behind broken runners or clogged pipelines.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
