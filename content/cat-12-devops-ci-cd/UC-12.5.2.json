{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.5.2",
  "title": "ArgoCD Drift Detection",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Configuration",
    "Compliance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'ArgoCD Drift Detection' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_argocd_drift_detection` (UC-12.5.2), SOC dashboard panel \"Operations — ArgoCD Drift Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-12.5.2 positive (ArgoCD, Drift, Detection): ingest synthetic events into `index=devops` sourcetype='argocd:application' where `count` grouped by `name`, `namespace`, `sync_status`, `health_status` satisfies detection threshold for 'ArgoCD Drift Detection'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-12.5.2 negative (Drift Detection): ingest benign `devops` / `argocd:application` events where `name`, `namespace`, `sync_status`, `health_status` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 4 confirms zero alert rows for 'ArgoCD Drift Detection'."
  },
  "dataSources": "`sourcetype=argocd:application` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Add-on for Argo CD — Universal Forwarder (Splunkbase 617)",
  "spl": "index=devops sourcetype=\"argocd:application\"\n| where sync_status=\"OutOfSync\" OR health_status=\"Degraded\"\n| eval drift_indicator=if(sync_status=\"OutOfSync\",\"manifest_drift\",\"health_degraded\")\n| stats count by name, namespace, sync_status, health_status, drift_indicator\n| sort -count",
  "description": "Alerts on risk tied to ArgoCD, Drift, Detection through UC-12.5.2 on `index=devops` sourcetype `argocd:application`. Summarises `count` grouped by `name`, `namespace`, `sync_status`, `health_status` across `the scheduled window` before applying configured threshold. Operators tune 'ArgoCD Drift Detection' using UC-12.5.2 runbook steps.",
  "value": "Tracking `activity` via UC-12.5.2 on `argocd:application` feeds improves response to 'ArgoCD Drift Detection' and reduces operational risk and delayed incident response.",
  "implementation": "Poll or stream Argo CD application objects so `sync_status` and `health_status` are current. Treat sustained `OutOfSync` as drift unless an approved sync window applies. Alert with application, revision, and diff summary fields when available.",
  "visualization": "Table (drifted apps), Bar chart (drift by cluster/namespace), Line chart (drift count over time).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `devops` (UC-12.5.2)** — Planned work for 'ArgoCD Drift Detection' shifts `count` over `span=1h` while `name`, `namespace`, `sync_status`, `health_status` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `argocd:application`** — Scheduled automation elevates `namespace` without crossing configured threshold for UC-12.5.2. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `name`, `namespace`, `sync_status`, `health_status`** — Health checks in `devops` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `argocd:application`** — First 48h baseline for UC-12.5.2 on `name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "argocd"
  ],
  "equipmentModels": [
    "argocd_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Add-on for Argo CD.\n• Ensure the following data sources are available: `sourcetype=argocd:application`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nPoll or stream Argo CD application objects so `sync_status` and `health_status` are current. Treat sustained `OutOfSync` as drift unless an approved sync window applies. Alert with application, revision, and diff summary fields when available.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=devops sourcetype=\"argocd:application\"\n| where sync_status=\"OutOfSync\" OR health_status=\"Degraded\"\n| eval drift_indicator=if(sync_status=\"OutOfSync\",\"manifest_drift\",\"health_degraded\")\n| stats count by name, namespace, sync_status, health_status, drift_indicator\n| sort -count\n```\n\nUnderstanding this SPL\n\n**ArgoCD Drift Detection** — Live cluster drift from Git-defined manifests risks untracked changes and audit gaps; detecting drift prioritizes reconciliation before incidents or compliance findings.\n\nDocumented **Data sources**: `sourcetype=argocd:application`. **App/TA** (typical add-on context): Splunk Add-on for Argo CD. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: devops; **sourcetype**: argocd:application. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=devops, sourcetype=\"argocd:application\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where sync_status=\"OutOfSync\" OR health_status=\"Degraded\"` — typically the threshold or rule expression for this monitoring goal.\n• `eval` defines or adjusts **drift_indicator** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by name, namespace, sync_status, health_status, drift_indicator** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with the Argo CD UI and application details. Verify field extractions and index permissions.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (drifted apps), Bar chart (drift by cluster/namespace), Line chart (drift count over time).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for Argo CD — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=argocd:application into devops. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`drift_indicator, health_status, name, namespace`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch GitOps sync and Argo signals so clusters stay aligned with the desired configuration in Git.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
