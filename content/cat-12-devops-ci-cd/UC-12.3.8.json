{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.3.8",
  "title": "Artifact Retention Policy Compliance",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Compliance",
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Artifact Retention Policy Compliance' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_artifact_retention_policy_compliance` (UC-12.3.8), SOC dashboard panel \"Operations — Artifact Retention Policy Compliance\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=devops` sourcetype='artifactory:artifact_age' where `repository` satisfies detection threshold for UC-12.3.8 ('Artifact Retention Policy Compliance'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `devops` / `artifactory:artifact_age` events where `repository` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-12.3.8."
  },
  "dataSources": "Artifact `last_downloaded`, `created`, retention rule ID Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Artifactory/Nexus repository metadata API — Universal Forwarder (Splunkbase 617)",
  "spl": "index=devops sourcetype=\"artifactory:artifact_age\"\n| eval age_days=(now()-created_epoch)/86400\n| where age_days > retention_days + 7\n| stats count by repository, path\n| sort -count",
  "description": "Verifies cleanup jobs deleted snapshots per policy; stale artifacts past retention indicate failed garbage collection. Searches `index=devops` with sourcetype `artifactory:artifact_age` for UC-12.3.8, aggregating `count` by `repository` over `the scheduled window`. Alerts when grouped results cross configured threshold in the saved search. Gives Operations operators a repeatable signal for 'Artifact Retention Policy Compliance' without manual log review.",
  "value": "Early 'Artifact Retention Policy Compliance' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Compare artifact age to configured retention. Alert on drift >7 days past policy. Audit quarterly for legal hold exceptions.",
  "visualization": "Table (over-retained artifacts), Bar chart (by repo), Single value (non-compliant count).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `devops`** — Planned work for 'Artifact Retention Policy Compliance' shifts `count` over `span=1h` while `repository` remains in-scope for UC-12.3.8. Cross-check CMDB CHG.\n\n2. **Batch `count` on `artifactory:artifact_age`** — Scheduled automation elevates `path` without crossing configured threshold. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `repository`** — Health checks in `devops` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `artifactory:artifact_age`** — First 48h baseline for `repository` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `repository`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Artifactory/Nexus repository metadata API.\n• Ensure the following data sources are available: Artifact `last_downloaded`, `created`, retention rule ID.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nCompare artifact age to configured retention. Alert on drift >7 days past policy. Audit quarterly for legal hold exceptions.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=devops sourcetype=\"artifactory:artifact_age\"\n| eval age_days=(now()-created_epoch)/86400\n| where age_days > retention_days + 7\n| stats count by repository, path\n| sort -count\n```\n\nUnderstanding this SPL\n\n**Artifact Retention Policy Compliance** — Verifies cleanup jobs deleted snapshots per policy; stale artifacts past retention indicate failed garbage collection.\n\nDocumented **Data sources**: Artifact `last_downloaded`, `created`, retention rule ID. **App/TA** (typical add-on context): Artifactory/Nexus repository metadata API. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: devops; **sourcetype**: artifactory:artifact_age. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=devops, sourcetype=\"artifactory:artifact_age\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **age_days** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where age_days > retention_days + 7` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by repository, path** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with the repository admin UI and download logs. Verify field extractions and index permissions.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (over-retained artifacts), Bar chart (by repo), Single value (non-compliant count).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Artifactory/Nexus repository metadata API — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=artifactory:artifact_age into devops. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`path, repository`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch packages and artifacts so supply-chain risk, storage, and policy checks stay under control.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
