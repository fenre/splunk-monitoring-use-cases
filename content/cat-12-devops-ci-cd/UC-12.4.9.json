{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.4.9",
  "title": "Feature Flag and Experiment Rollout Monitoring",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Feature Flag and Experiment Rollout Monitoring' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_feature_flag_and_experiment_rollout_monitoring` (UC-12.4.9), SOC dashboard panel \"Operations — Feature Flag and Experiment Rollout Monitoring\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=app` sourcetype='feature_flag:eval' that satisfy the UC-12.4.9 SPL filters for 'Feature Flag and Experiment Rollout Monitoring'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `app` / `feature_flag:eval` events outside the detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-12.4.9."
  },
  "dataSources": "Flag evaluation logs, rollout events, error rates by flag Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Feature flag provider logs, app telemetry — Universal Forwarder (Splunkbase 617)",
  "spl": "index=app sourcetype=\"feature_flag:eval\"\n| bin _time span=1h\n| stats count, sum(eval(if(error=\"true\",1,0))) as errors by flag_name, variant, _time\n| eval error_rate=round((errors/count)*100, 2)\n| where error_rate > 5",
  "description": "Feature flags and experiments affect user experience. Monitoring rollout percentage and error rate per flag supports safe rollouts and rollback.",
  "value": "Early 'Feature Flag and Experiment Rollout Monitoring' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Ingest flag evaluation and error data. Track rollout % and error rate per flag/variant. Alert on error rate spike after rollout. Report on flag adoption and performance by variant.",
  "visualization": "Line chart (error rate by flag), Table (flags with high errors), Bar chart (rollout % by variant).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'Feature Flag and Experiment Rollout Monitoring'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `app` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `ops_exception_register.csv` with entity, reason, valid_until.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Feature flag provider logs, app telemetry.\n• Ensure the following data sources are available: Flag evaluation logs, rollout events, error rates by flag.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest flag evaluation and error data. Track rollout % and error rate per flag/variant. Alert on error rate spike after rollout. Report on flag adoption and performance by variant.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=app sourcetype=\"feature_flag:eval\"\n| bin _time span=1h\n| stats count, sum(eval(if(error=\"true\",1,0))) as errors by flag_name, variant, _time\n| eval error_rate=round((errors/count)*100, 2)\n| where error_rate > 5\n```\n\nUnderstanding this SPL\n\n**Feature Flag and Experiment Rollout Monitoring** — Feature flags and experiments affect user experience. Monitoring rollout percentage and error rate per flag supports safe rollouts and rollback.\n\nDocumented **Data sources**: Flag evaluation logs, rollout events, error rates by flag. **App/TA** (typical add-on context): Feature flag provider logs, app telemetry. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: app; **sourcetype**: feature_flag:eval. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=app, sourcetype=\"feature_flag:eval\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by flag_name, variant, _time** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **error_rate** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where error_rate > 5` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with the vendor console or native job history for that tool. Verify field extractions and index permissions.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (error rate by flag), Table (flags with high errors), Bar chart (rollout % by variant).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Feature flag provider logs, app telemetry — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=feature_flag:eval into app. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, error_rate, flag_name, variant`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch this part of your delivery chain for risks and reliability so issues show up before they hit customers.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
