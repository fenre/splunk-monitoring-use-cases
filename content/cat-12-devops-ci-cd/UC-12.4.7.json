{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.4.7",
  "title": "Container Image Build and Push Audit",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Container Image Build and Push Audit' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_container_image_build_and_push_audit` (UC-12.4.7), SOC dashboard panel \"Operations — Container Image Build and Push Audit\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=registry` sourcetype='registry:audit' that satisfy the UC-12.4.7 SPL filters for 'Container Image Build and Push Audit'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `registry` / `registry:audit` events outside the detection threshold, or exclude the test entity via `operational_exceptions.csv` with a valid window. Confirm zero alert rows for UC-12.4.7."
  },
  "dataSources": "Image push events, build logs, registry audit Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Registry and CI logs (ECR, ACR, Harbor, Docker) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=registry sourcetype=\"registry:audit\"\n| search action=push\n| stats latest(_time) as last_push, count by image_name, actor, tag\n| table image_name, tag, actor, last_push, count",
  "description": "Unauthorized or untagged image pushes can introduce risk. Auditing build and push events supports supply chain security and compliance.",
  "value": "Early 'Container Image Build and Push Audit' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Ingest registry audit and CI build events. Alert on push from unexpected identity or to production repo without tag policy. Report on image provenance and push frequency.",
  "visualization": "Table (push events), Timeline (pushes by image), Bar chart (pushes by actor).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'Container Image Build and Push Audit'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `registry` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `operational_exceptions.csv` with entity, reason, valid_until.",
  "equipment": [
    "docker"
  ],
  "equipmentModels": [
    "docker_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Registry and CI logs (ECR, ACR, Harbor, Docker).\n• Ensure the following data sources are available: Image push events, build logs, registry audit.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest registry audit and CI build events. Alert on push from unexpected identity or to production repo without tag policy. Report on image provenance and push frequency.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=registry sourcetype=\"registry:audit\"\n| search action=push\n| stats latest(_time) as last_push, count by image_name, actor, tag\n| table image_name, tag, actor, last_push, count\n```\n\nUnderstanding this SPL\n\n**Container Image Build and Push Audit** — Unauthorized or untagged image pushes can introduce risk. Auditing build and push events supports supply chain security and compliance.\n\nDocumented **Data sources**: Image push events, build logs, registry audit. **App/TA** (typical add-on context): Registry and CI logs (ECR, ACR, Harbor, Docker). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: registry; **sourcetype**: registry:audit. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=registry, sourcetype=\"registry:audit\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Applies an explicit `search` filter to narrow the current result set.\n• `stats` rolls up events into metrics; results are split **by image_name, actor, tag** so each row reflects one combination of those dimensions.\n• Pipeline stage (see **Container Image Build and Push Audit**): table image_name, tag, actor, last_push, count\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with the repository admin UI and download logs. Verify field extractions and index permissions.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (push events), Timeline (pushes by image), Bar chart (pushes by actor).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Registry and CI logs (ECR, ACR, Harbor, Docker) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=registry:audit into registry. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`actor, image_name, tag`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch this part of your delivery chain for risks and reliability so issues show up before they hit customers.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
