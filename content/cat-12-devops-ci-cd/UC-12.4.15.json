{
  "$schema": "../../schemas/uc.schema.json",
  "id": "12.4.15",
  "title": "Policy-as-Code Violation Trending",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Compliance",
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Policy-as-Code Violation Trending' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_policy_as_code_violation_trending` (UC-12.4.15), SOC dashboard panel \"Operations — Policy-as-Code Violation Trending\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-12.4.15 positive (Policy, Code, Violation, Trending): ingest synthetic events into `index=iac` sourcetype='opa:decision' where `count` grouped by `policy_name` satisfies detection threshold for 'Policy-as-Code Violation Trending'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-12.4.15 negative (Violation Trending): ingest benign `iac` / `opa:decision` events where `policy_name` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 12 confirms zero alert rows for 'Policy-as-Code Violation Trending'."
  },
  "dataSources": "`result=\"fail\"`, `policy_path`, `namespace` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "OPA decision logs, Terraform Cloud policy sets — Universal Forwarder (Splunkbase 617)",
  "spl": "index=iac sourcetype=\"opa:decision\"\n| where result=\"fail\"\n| timechart span=1d count by policy_name",
  "description": "Surfaces UC-12.4.15 — Policy, Code, Violation, Trending — using `index=iac` sourcetype `opa:decision`, grouped by `policy_name`. Outputs `count` each `1d`; alert when configured threshold. Primary focus: Violation Trending. Purpose-built for 'Policy-as-Code Violation Trending' in Operations workflows.",
  "value": "Tracking `activity` via UC-12.4.15 on `opa:decision` feeds improves response to 'Policy-as-Code Violation Trending' and reduces operational risk and delayed incident response.",
  "implementation": "Baseline failures per policy. Alert on 3× week-over-week spike. Run education before switching to hard-fail.",
  "visualization": "Line chart (violations over time), Bar chart (by policy), Table (top namespaces).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `iac` (UC-12.4.15)** — Planned work for 'Policy-as-Code Violation Trending' shifts `count` over `span=1d` while `policy_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `opa:decision`** — Scheduled automation elevates `src` without crossing configured threshold for UC-12.4.15. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `policy_name`** — Health checks in `iac` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `opa:decision`** — First 48h baseline for UC-12.4.15 on `policy_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `policy_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "hashicorp"
  ],
  "equipmentModels": [
    "hashicorp_terraform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: OPA decision logs, Terraform Cloud policy sets.\n• Ensure the following data sources are available: `result=\"fail\"`, `policy_path`, `namespace`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nBaseline failures per policy. Alert on 3× week-over-week spike. Run education before switching to hard-fail.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=iac sourcetype=\"opa:decision\"\n| where result=\"fail\"\n| timechart span=1d count by policy_name\n```\n\nUnderstanding this SPL\n\n**Policy-as-Code Violation Trending** — OPA, Sentinel, or Conftest denials over time—spikes after new policy rollout (extends UC-12.4.5).\n\nDocumented **Data sources**: `result=\"fail\"`, `policy_path`, `namespace`. **App/TA** (typical add-on context): OPA decision logs, Terraform Cloud policy sets. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: iac; **sourcetype**: opa:decision. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=iac, sourcetype=\"opa:decision\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where result=\"fail\"` — typically the threshold or rule expression for this monitoring goal.\n• `timechart` plots the metric over time using **span=1d** buckets with a separate series **by policy_name** — ideal for trending and alerting on this use case.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with Terraform Cloud audit and run history. Verify field extractions and index permissions.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (violations over time), Bar chart (by policy), Table (top namespaces).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (OPA decision logs, Terraform Cloud policy sets — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=opa:decision into iac. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`policy_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch this part of your delivery chain for risks and reliability so issues show up before they hit customers.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
