{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.14.1",
  "title": "OT Security Add-on Health and Configuration Status",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'OT Security Add-on Health and Configuration Status' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_ot_security_add_on_health_and_configuration_stat` (UC-10.14.1), SOC dashboard panel \"Security Infra — OT Security Add-on Health and Configuration Status\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.14.1 positive (Security, Add, Health, and, Configuration): ingest synthetic events into `index=_internal` sourcetype='target_sourcetype' where `count` grouped by `savedsearch_name`, `status` satisfies detection threshold for 'OT Security Add-on Health and Configuration Status'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.14.1 negative (Configuration Status): ingest benign `_internal` / `target_sourcetype` events where `savedsearch_name`, `status` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 11 confirms zero alert rows for 'OT Security Add-on Health and Configuration Status'."
  },
  "dataSources": "`index=_internal` scheduler/search logs, `index=ot_security` `sourcetype=\"ot:security:health\"`",
  "app": "OT Security Add-on for Splunk (Splunkbase 5151)",
  "spl": "index=_internal source=*scheduler.log status=failed savedsearch_name=\"OT*\"\n| stats count by savedsearch_name, status\n| append [\n    | rest /servicesNS/-/-/saved/searches search=\"OT*\"\n    | where disabled=1\n    | table title disabled\n  ]\n| table savedsearch_name, title, status, disabled, count",
  "description": "Detects UC-10.14.1 — Security, Add, Health, and, Configuration — using `index=_internal` sourcetype `configured sourcetype`, grouped by `savedsearch_name`, `status`. Outputs `count` each `the scheduled window`; alert when configured threshold. Primary focus: Configuration Status. Purpose-built for 'OT Security Add-on Health and Configuration Status' in Security Infra workflows.",
  "value": "UC-10.14.1 helps teams contain Security, Add, Health, and, Configuration scenarios and reduce missed threat campaigns, control regressions, and SOC SLA breaches before incidents compound.",
  "implementation": "Deploy Splunk Enterprise Security and the OT Security Add-on. Schedule a daily check for disabled or failing correlation searches. Alert when any OT-prefixed saved search has failed or been disabled.",
  "visualization": "Table (search name, status, last failure), Single value (failed OT searches count).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "OT Security Add-on for Splunk",
      "url": "https://splunkbase.splunk.com/app/5151",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Security",
      "url": "https://splunkbase.splunk.com/app/263",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `_internal` (UC-10.14.1)** — Planned work for 'OT Security Add-on Health and Configuration Status' shifts `count` over `span=1h` while `savedsearch_name`, `status` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `configured_sourcetype`** — Scheduled automation elevates `status` without crossing configured threshold for UC-10.14.1. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `savedsearch_name`, `status`** — Health checks in `_internal` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-10.14.1 on `savedsearch_name` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `savedsearch_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk OT Security Add-on"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: OT Security Add-on for Splunk (Splunkbase 5151).\n• Ensure the following data sources are available: `index=_internal` scheduler/search logs, `index=ot_security` `sourcetype=\"ot:security:health\"`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nDeploy Splunk Enterprise Security and the OT Security Add-on. Schedule a daily check for disabled or failing correlation searches. Alert when any OT-prefixed saved search has failed or been disabled.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=_internal source=*scheduler.log status=failed savedsearch_name=\"OT*\"\n| stats count by savedsearch_name, status\n| append [\n    | rest /servicesNS/-/-/saved/searches search=\"OT*\"\n    | where disabled=1\n    | table title disabled\n  ]\n| table savedsearch_name, title, status, disabled, count\n```\n\nUnderstanding this SPL\n\n**OT Security Add-on Health and Configuration Status** — Ensures OT Security Add-on correlation searches, lookups, and MITRE mappings stay healthy so process-safety detections do not silently fail.\n\nDocumented **Data sources**: `index=_internal` scheduler/search logs, `index=ot_security` `sourcetype=\"ot:security:health\"`. **App/TA** (typical add-on context): OT Security Add-on for Splunk (Splunkbase 5151). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: _internal.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=_internal. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by savedsearch_name, status** so each row reflects one combination of those dimensions.\n• Appends rows from a subsearch with `append`.\n• Pipeline stage (see **OT Security Add-on Health and Configuration Status**): table savedsearch_name, title, status, disabled, count\n\n\nStep 3 — Validate\nCompare failed or disabled searches in Splunk to the **OT Security Add-on** configuration UI and the **Saved Searches** list so names and schedules line up. Spot-check one failure against `scheduler.log` around the run time.\n\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (search name, status, last failure), Single value (failed OT searches count).\n",
  "grandmaExplanation": "We keep an eye on the tools that watch the plant so our safety and attack detections in OT do not quietly stop running.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
