{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.12.13",
  "title": "Algorithmic Trading Circuit Breaker",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Risk",
    "Availability"
  ],
  "splunkPillar": "Security",
  "industry": "Financial Services",
  "exclusions": "Covers the detection logic documented for 'Algorithmic Trading Circuit Breaker' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_algorithmic_trading_circuit_breaker` (UC-10.12.13), SOC dashboard panel \"Security Infra — Algorithmic Trading Circuit Breaker\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.12.13 positive (Algorithmic, Trading, Circuit, Breaker): ingest synthetic events into `index=trading` sourcetype='risk:circuit' where `values` grouped by `gateway`, `state`, `gateway]` satisfies `gap` > 30 for 'Algorithmic Trading Circuit Breaker'. Validate filter `index=trading sourcetype=\"fix:session\"`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.12.13 negative (Circuit Breaker): ingest benign `trading` / `risk:circuit` events where `gateway`, `state`, `gateway]` stays below `gap` > 30, or exclude the test entity via `soc_exception_register.csv`. Variant 14 confirms zero alert rows for 'Algorithmic Trading Circuit Breaker'."
  },
  "dataSources": "`sourcetype=\"risk:circuit\"`, `sourcetype=\"fix:session\"` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Risk engine, exchange gateway — Universal Forwarder (Splunkbase 617)",
  "spl": "index=trading sourcetype=\"risk:circuit\"\n| search state IN (\"HALT\",\"THROTTLE\",\"KILL\")\n| stats latest(_time) as tripped, values(strategy_id) as strategies by gateway, state\n| join max=1 gateway [| search index=trading sourcetype=\"fix:session\" | stats latest(heartbeat_gap) as gap by gateway]\n| where gap>30 OR isnotnull(tripped)",
  "description": "Surfaces UC-10.12.13 — Algorithmic, Trading, Circuit, Breaker — using `index=trading` sourcetype `risk:circuit`, grouped by `gateway`, `state`, `gateway]`. Outputs `values` each `the scheduled window`; alert when `gap` > 30. Filter chain includes filter `index=trading sourcetype=\"fix:session\"`. Purpose-built for 'Algorithmic Trading Circuit Breaker' in Security Infra workflows.",
  "value": "Alerting on Algorithmic, Trading, Circuit, Breaker through UC-10.12.13 reduces missed threat campaigns, control regressions, and SOC SLA breaches by surfacing `activity` early.",
  "implementation": "Log all manual overrides; post-mortem each halt with P&L and message counts.",
  "visualization": "Timeline (breaker events), Table (gateway state), Single value (strategies disabled).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable for the primary search: it relies on application- and industry-specific log formats, not a normalized CIM data model. Optional mapping to CIM, if you adopt it, is a separate follow-on project.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `trading` (UC-10.12.13)** — Planned work for 'Algorithmic Trading Circuit Breaker' shifts `values` over `span=1h` while `gateway`, `state`, `gateway]` remains in-scope. Applies filter `index=trading sourcetype=\"fix:session\"`. Cross-check CMDB CHG.\n\n2. **Batch `values` on `risk:circuit`** — Scheduled automation elevates `state` without crossing `gap` > 30 for UC-10.12.13. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `gateway`, `state`, `gateway]`** — Health checks in `trading` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `risk:circuit`** — First 48h baseline for UC-10.12.13 on `gateway` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `gateway`, `exception_reason`, `valid_until`.",
  "equipment": [
    "exchange"
  ],
  "equipmentModels": [
    "exchange_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Risk engine, exchange gateway.\n• Ensure the following data sources are available: `sourcetype=\"risk:circuit\"`, `sourcetype=\"fix:session\"`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nLog all manual overrides; post-mortem each halt with P&L and message counts.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=trading sourcetype=\"risk:circuit\"\n| search state IN (\"HALT\",\"THROTTLE\",\"KILL\")\n| stats latest(_time) as tripped, values(strategy_id) as strategies by gateway, state\n| join max=1 gateway [| search index=trading sourcetype=\"fix:session\" | stats latest(heartbeat_gap) as gap by gateway]\n| where gap>30 OR isnotnull(tripped)\n```\n\nUnderstanding this SPL\n\n**Algorithmic Trading Circuit Breaker** — Ensures volatility/gateway breakers trip correctly and strategies are flattened when limits hit.\n\nDocumented **Data sources**: `sourcetype=\"risk:circuit\"`, `sourcetype=\"fix:session\"`. **App/TA** (typical add-on context): Risk engine, exchange gateway. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: trading; **sourcetype**: risk:circuit. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=trading, sourcetype=\"risk:circuit\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Applies an explicit `search` filter to narrow the current result set.\n• `stats` rolls up events into metrics; results are split **by gateway, state** so each row reflects one combination of those dimensions.\n• Joins to a subsearch with `join` — set `max=` to match cardinality and avoid silent truncation.\n• Filters the current rows with `where gap>30 OR isnotnull(tripped)` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nRun the search on recent production data, spot-check a handful of results against the source system or operational runbooks (cash operations, EHR, payment gateway, etc.), and adjust thresholds to match your seasonal patterns and test windows so teams are not flooded with noise.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Timeline (breaker events), Table (gateway state), Single value (strategies disabled).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Risk engine, exchange gateway — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=risk:circuit, fix:session into trading. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`gap, gateway, gateway], state`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "In Financial Services, we pay close attention to \"Algorithmic Trading Circuit Breaker\" in our day-to-day work so that fraud, waste, and compliance issues do not slip by quietly. We help teams focus on the few situations that need a person, not every routine blip in the data.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
