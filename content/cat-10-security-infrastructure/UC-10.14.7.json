{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.14.7",
  "title": "Rogue Engineering Tool Execution",
  "criticality": "critical",
  "difficulty": "expert",
  "monitoringType": [
    "Security",
    "Change"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Rogue Engineering Tool Execution' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_rogue_engineering_tool_execution` (UC-10.14.7), SOC dashboard panel \"Security Infra — Rogue Engineering Tool Execution\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.14.7 positive (Rogue, Engineering, Tool, Execution): ingest synthetic events into `index=ot_security` sourcetype='ot:security:process' where `hosts` grouped by `process_name` satisfies detection threshold for 'Rogue Engineering Tool Execution'. Validate lookup `ot_approved_eng_tools`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.14.7 negative (Tool Execution): ingest benign `ot_security` / `ot:security:process` events where `process_name` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 4 confirms zero alert rows for 'Rogue Engineering Tool Execution'."
  },
  "dataSources": "`index=ot_security` host-based logs from engineering workstations, EDR forwarded to OT index",
  "app": "OT Security Add-on for Splunk (Splunkbase 5151)",
  "spl": "index=ot_security sourcetype=\"ot:security:process\"\n| lookup ot_approved_eng_tools process_name OUTPUT approved_tool\n| where isnull(approved_tool) AND match(process_name, \"(?i)studio|logix|step7|tia|codesys|unity\")\n| stats earliest(_time) as first_seen values(user) as users dc(host) as hosts by process_name\n| lookup ot_eng_workstations host OUTPUT approved_role\n| where isnull(approved_role)",
  "description": "Identifies risk tied to Rogue, Engineering, Tool, Execution through UC-10.14.7 on `index=ot_security` sourcetype `ot:security:process`. Summarises `hosts` grouped by `process_name` across `the scheduled window` before applying configured threshold. Operators tune 'Rogue Engineering Tool Execution' using UC-10.14.7 runbook steps.",
  "value": "Tracking `hosts` via UC-10.14.7 on `ot:security:process` feeds improves response to 'Rogue Engineering Tool Execution' and reduces missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Requires Splunk ES, OT Security Add-on, and workstation inventory. Tune for sanctioned tools and vendor remote support. Correlate with T0843 and change records.",
  "visualization": "Process execution timeline, Stacked bar by binary, Table linking host to user.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "OT Security Add-on for Splunk",
      "url": "https://splunkbase.splunk.com/app/5151",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `ot_security` (UC-10.14.7)** — Planned work for 'Rogue Engineering Tool Execution' shifts `hosts` over `span=1h` while `process_name` remains in-scope. Applies lookup `ot_approved_eng_tools`. Cross-check CMDB CHG.\n\n2. **Batch `values` on `ot:security:process`** — Scheduled automation elevates `src` without crossing configured threshold for UC-10.14.7. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `process_name`** — Health checks in `ot_security` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `ot:security:process`** — First 48h baseline for UC-10.14.7 on `process_name` is unstable; join `ot_approved_eng_tools` prior to threshold.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `process_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "dell_emc"
  ],
  "equipmentModels": [
    "dell_emc_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk OT Security Add-on"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: OT Security Add-on for Splunk (Splunkbase 5151).\n• Ensure the following data sources are available: `index=ot_security` host-based logs from engineering workstations, EDR forwarded to OT index.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nRequires Splunk ES, OT Security Add-on, and workstation inventory. Tune for sanctioned tools and vendor remote support. Correlate with T0843 and change records.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot_security sourcetype=\"ot:security:process\"\n| lookup ot_approved_eng_tools process_name OUTPUT approved_tool\n| where isnull(approved_tool) AND match(process_name, \"(?i)studio|logix|step7|tia|codesys|unity\")\n| stats earliest(_time) as first_seen values(user) as users dc(host) as hosts by process_name\n| lookup ot_eng_workstations host OUTPUT approved_role\n| where isnull(approved_role)\n```\n\nUnderstanding this SPL\n\n**Rogue Engineering Tool Execution** — Spots unauthorized engineering software sessions to field devices (T0843), reducing risk of malicious reprogramming.\n\nDocumented **Data sources**: `index=ot_security` host-based logs from engineering workstations, EDR forwarded to OT index. **App/TA** (typical add-on context): OT Security Add-on for Splunk (Splunkbase 5151). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot_security; **sourcetype**: ot:security:process. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot_security, sourcetype=\"ot:security:process\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Enriches events using `lookup` (lookup definition + optional OUTPUT fields).\n• Filters the current rows with `where isnull(approved_tool) AND match(process_name, \"(?i)studio|logix|step7|tia|codesys|unity\")` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by process_name** so each row reflects one combination of those dimensions.\n• Enriches events using `lookup` (lookup definition + optional OUTPUT fields).\n• Filters the current rows with `where isnull(approved_role)` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nCompare results to the **OT Security** add-on views, the **OT asset** inventory, and any **engineering change** or CMMS record. When you send ot-data through **Nozomi**, match the same **Nozomi Guardian** alert. When you use **Cyber Vision** sourcetype `cisco:cybervision`, line up the same event in the **Cyber Vision** UI. For raw ICS **syslog**, match the sensor or PLC console for that minute.\n\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Process execution timeline, Stacked bar by binary, Table linking host to user.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (OT Security Add-on for Splunk (Splunkbase 5151)). Enable the modular input or HEC token that writes sourcetype=ot:security:process into ot_security. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`approved_role, approved_tool, host, process_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We spot engineering software running where and when it should not, so only trusted techs reflash or reconfigure the plant.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
