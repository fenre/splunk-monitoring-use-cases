{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.11.83",
  "title": "Tanium Real-Time Question Response Times",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Tanium Real-Time Question Response Times' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_tanium_real_time_question_response_times` (UC-10.11.83), SOC dashboard panel \"Security Infra — Tanium Real-Time Question Response Times\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.11.83 positive (Tanium, Real, Time, Question, Response): ingest synthetic events into `index=endpoint` sourcetype='tanium:interact' where `count` grouped by `question_name` satisfies `p95` > 5000 for 'Tanium Real-Time Question Response Times'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.11.83 negative (Response Times): ingest benign `endpoint` / `tanium:interact` events where `question_name` stays below `p95` > 5000, or exclude the test entity via `soc_exception_register.csv`. Variant 9 confirms zero alert rows for 'Tanium Real-Time Question Response Times'."
  },
  "dataSources": "`sourcetype=tanium:interact` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_tanium` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=endpoint sourcetype=\"tanium:interact\"\n| stats perc95(response_ms) as p95 by question_name\n| where p95>5000",
  "description": "Alerts on UC-10.11.83 — Tanium, Real, Time, Question, Response — using `index=endpoint` sourcetype `tanium:interact`, grouped by `question_name`. Outputs `count` each `the scheduled window`; alert when `p95` > 5000. Primary focus: Response Times. Purpose-built for 'Tanium Real-Time Question Response Times' in Security Infra workflows.",
  "value": "Operators running UC-10.11.83 on `tanium:interact` feeds shorten triage for Response Times tied to 'Tanium Real-Time Question Response Times', lowering missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Monitor Interact latency for live investigations. Alert when platform or network degrades.",
  "visualization": "Line chart (p95 latency), Table (slow questions).",
  "cimModels": [
    "Performance"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Performance.CPU by Performance.host | sort - count",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Performance.CPU` dataset after normalizing this feed to the model; otherwise the `cimSpl` tstats search may return no rows.",
  "references": [
    {
      "title": "CIM: Performance",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Performance",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `endpoint` (UC-10.11.83)** — Planned work for 'Tanium Real-Time Question Response Times' shifts `count` over `span=1h` while `question_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `tanium:interact`** — Scheduled automation elevates `src` without crossing `p95` > 5000 for UC-10.11.83. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `question_name`** — Health checks in `endpoint` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `tanium:interact`** — First 48h baseline for UC-10.11.83 on `question_name` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `question_name`, `exception_reason`, `valid_until`.",
  "detectionType": "Anomaly",
  "securityDomain": "endpoint",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_tanium`.\n• Ensure the following data sources are available: `sourcetype=tanium:interact`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMonitor Interact latency for live investigations. Alert when platform or network degrades.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=endpoint sourcetype=\"tanium:interact\"\n| stats perc95(response_ms) as p95 by question_name\n| where p95>5000\n```\n\nUnderstanding this SPL\n\n**Tanium Real-Time Question Response Times** — Response times matter when Tanium is used for incident containment questions.\n\nDocumented **Data sources**: `sourcetype=tanium:interact`. **App/TA** (typical add-on context): `Splunk_TA_tanium`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: endpoint; **sourcetype**: tanium:interact. That sourcetype matches what this use case lists under Data sources.\n\n**Detection type** for this use case: Anomaly — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=endpoint, sourcetype=\"tanium:interact\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by question_name** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where p95>5000` — typically the threshold or rule expression for this monitoring goal.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats summariesonly=t count from datamodel=Performance.CPU by Performance.host | sort - count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Tanium Real-Time Question Response Times** — Response times matter when Tanium is used for incident containment questions.\n\nDocumented **Data sources**: `sourcetype=tanium:interact`. **App/TA** (typical add-on context): `Splunk_TA_tanium`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Detection type** for this use case: Anomaly — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Performance.CPU` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nRun the search over a window that includes both normal production traffic and, if you have one, a documented test (vendor sandbox, lab rule hit, or exercise). Compare a few sample events to the vendor’s own console or reports and tune indexes, fields, and thresholds so alerts are actionable before paging.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (p95 latency), Table (slow questions).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_tanium` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=tanium:interact into endpoint. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`p95, question_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use Tanium data and alerts to stay ahead of the risks described in \"Tanium Real-Time Question Response Times\". We help the team notice trouble early, while a situation is still small and containable.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
