{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.7.43",
  "title": "Okta MFA Exhaustion Hunt",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Okta MFA Exhaustion Hunt' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_okta_mfa_exhaustion_hunt` (UC-10.7.43), SOC dashboard panel \"Security Infra — Okta MFA Exhaustion Hunt\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.7.43 positive (Okta, MFA, Exhaustion, Hunt): ingest synthetic events into `index=target_index` sourcetype='target_sourcetype' where `successes`, `failures`, `pushes` grouped by `user`, `_time` satisfies `pushes` > 1, `pushes` > 1, `successes` > 0, or `pushes` > 3 for 'Okta MFA Exhaustion Hunt'. Validate macro `okta_mfa_exhaustion_hunt_filter`, filter `(pushes>1)`, eventType `core.user.factor.attempt_success`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.7.43 negative (Exhaustion Hunt): ingest benign `target_index` / `target_sourcetype` events where `user`, `_time` stays below `pushes` > 1, `pushes` > 1, `successes` > 0, or `pushes` > 3, or exclude the test entity via `soc_exception_register.csv`. Variant 4 confirms zero alert rows for 'Okta MFA Exhaustion Hunt'."
  },
  "dataSources": "Okta Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Security Essentials / ESCU — Universal Forwarder (Splunkbase 617)",
  "spl": "`okta` eventType=system.push.send_factor_verify_push OR ((legacyEventType=core.user.factor.attempt_success) AND (debugContext.debugData.factor=OKTA_VERIFY_PUSH)) OR ((legacyEventType=core.user.factor.attempt_fail) AND (debugContext.debugData.factor=OKTA_VERIFY_PUSH))\n      | stats count(eval(legacyEventType=\"core.user.factor.attempt_success\"))  as successes count(eval(legacyEventType=\"core.user.factor.attempt_fail\")) as failures count(eval(eventType=\"system.push.send_factor_verify_push\")) as pushes\n        BY user,_time\n      | stats latest(_time) as lasttime earliest(_time) as firsttime sum(successes) as successes sum(failures) as failures sum(pushes) as pushes\n        BY user\n      | eval seconds=lasttime-firsttime\n      | eval lasttime=strftime(lasttime, \"%c\")\n      | search (pushes>1)\n      | eval totalattempts=successes+failures\n      | eval finding=\"Normal authentication pattern\"\n      | eval finding=if(failures==pushes AND pushes>1,\"Authentication attempts not successful because multiple pushes denied\",finding)\n      | eval finding=if(totalattempts==0,\"Multiple pushes sent and ignored\",finding)\n      | eval finding=if(successes>0 AND pushes>3,\"Probably should investigate. Multiple pushes sent, eventual successful authentication!\",finding)\n      | `okta_mfa_exhaustion_hunt_filter`",
  "description": "Monitors risk tied to Okta, MFA, Exhaustion, Hunt through UC-10.7.43 on `index=target_index` sourcetype `configured sourcetype`. Summarises `successes`, `failures`, `pushes` grouped by `user`, `_time` across `the scheduled window` before applying `pushes` > 1, `pushes` > 1, `successes` > 0, or `pushes` > 3. Operators tune 'Okta MFA Exhaustion Hunt' using UC-10.7.43 runbook steps.",
  "value": "Tracking `successes`, `failures`, `pushes` via UC-10.7.43 on macro `okta_mfa_exhaustion_hunt_filter`, filter `(pushes>1)`, eventType `core.user.factor.attempt_success` improves response to 'Okta MFA Exhaustion Hunt' and reduces missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "ESCU Hunting detection — not designed for automated alerting. Run ad-hoc or on a low-frequency schedule from Splunk Enterprise Security for analyst-driven investigation. Requires Okta ingested and CIM-normalized.",
  "visualization": "Table, Timeline (from ESCU).",
  "cimModels": [
    "Authentication"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Authentication.Authentication by Authentication.user, Authentication.src, Authentication.action",
  "dataModelAcceleration": "Enable for Authentication, Authentication (dataset)",
  "references": [
    {
      "title": "https://developer.okta.com/docs/reference/api/event-types/?q=user.acount.lock",
      "url": "https://developer.okta.com/docs/reference/api/event-types/?q=user.acount.lock",
      "retrieved": "2026-04-25"
    },
    {
      "title": "https://splunkbase.splunk.com/app/6553",
      "url": "https://splunkbase.splunk.com/app/6553",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `target_index` (UC-10.7.43)** — Planned work for 'Okta MFA Exhaustion Hunt' shifts `successes`, `failures`, `pushes` over `span=1h` while `user`, `_time` remains in-scope. eventType=core.user.factor.attempt_success Applies macro `okta_mfa_exhaustion_hunt_filter`, filter `(pushes>1)`, eventType `core.user.factor.attempt_success`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `configured_sourcetype`** — Scheduled automation elevates `_time` without crossing `pushes` > 1 for UC-10.7.43. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `user`, `_time`** — Health checks in `target_index` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-10.7.43 on `user` is unstable; join `okta` prior to threshold.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `user`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1110"
  ],
  "detectionType": "Hunting",
  "equipment": [
    "kubernetes",
    "okta",
    "security_essentials"
  ],
  "equipmentModels": [
    "kubernetes_helm"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk Enterprise Security"
  ],
  "detailedImplementation": "Enterprise Security Detection Rule\n\n\"Okta MFA Exhaustion Hunt\" is a Hunting detection designed for analyst-driven threat hunting rather than automated alerting, sourced from the Splunk Enterprise Security Content Update (ESCU). Hunting detections are not intended for automated alerting. They are run on-demand or on a low-frequency schedule by security analysts investigating specific threat hypotheses.\n\nPrerequisites\n\n• Splunk Enterprise Security 7.x or later with the ES Content Update (ESCU) app installed and up to date.\n• Data sources: Okta. Must be ingested into Splunk and normalized to the Common Information Model (CIM) via the appropriate Technology Add-on.\n• MITRE ATT&CK: T1110. Review the ATT&CK matrix for adjacent techniques to identify detection coverage gaps in your environment.\n\nDeployment\n\n1. In Enterprise Security, navigate to Configure → Content → Content Management.\n2. Search for \"Okta MFA Exhaustion Hunt\" or filter by Analytic Story.\n3. Hunting detections are typically left disabled for automated scheduling. Instead, run them on-demand from the Search bar or schedule at low frequency (daily or weekly).\n4. Review results manually — hunting detections cast a wider net and expect analyst judgment to separate signal from noise.\n5. When results warrant further investigation, create a Notable Event manually or initiate your incident response workflow.\n\nTuning and False Positive Management\n\nKnown false positives for this detection: False positives may be present. Tune Okta and tune the analytic to ensure proper fidelity. Modify risk score as needed. Drop to anomaly until tuning is complete.\n\n• Hunting detections are expected to produce broader result sets that require analyst interpretation. Focus on refining the search scope (time range, specific hosts or users) rather than eliminating all noise.\n• Maintain a hunting journal documenting hypotheses tested, results found, and detection improvements identified.\n• Share high-fidelity findings with the detection engineering team to convert recurring hunting patterns into automated TTP or Anomaly detections.\n\nAnalyst Response Workflow\n\nWhen reviewing Hunting results:\n\n1. Run the search manually or review scheduled results. Evaluate each returned event against your threat hypothesis — not every result indicates compromise.\n2. Cross-reference findings with current threat intelligence, recent security advisories, and outputs from other detection sources.\n3. If suspicious activity is confirmed, create a Notable Event or escalate directly to your incident response process with the supporting evidence.\n4. Document the hunting exercise: hypothesis, data sources queried, time range, findings, and any detection engineering improvements identified.\n\nValidation\n\nConfirm the required data sources are flowing:\n\n```spl\n| tstats count where index=* by index, sourcetype | sort -count\n```\n\nVerify the detection is enabled and firing:\n\n```spl\nindex=_audit action=\"alert_fired\" ss_name=\"*\"\n| stats count by ss_name, trigger_time | sort -trigger_time\n```\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Security Essentials / ESCU — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into index=main. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, finding, pushes, successes`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use this 'Okta MFA Exhaustion Hunt' search to catch the exact behavior in the data we already collect, so the team can confirm or clear it in the right management console or identity store.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
