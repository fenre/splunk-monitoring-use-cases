{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.12.37",
  "title": "Omnichannel Order Routing Failures",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Operations"
  ],
  "splunkPillar": "Security",
  "industry": "Retail and E-Commerce",
  "exclusions": "Covers the detection logic documented for 'Omnichannel Order Routing Failures' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_omnichannel_order_routing_failures` (UC-10.12.37), SOC dashboard panel \"Security Infra — Omnichannel Order Routing Failures\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.12.37 positive (Omnichannel, Order, Routing, Failures): ingest synthetic events into `index=retail` sourcetype='oms:route' where `values` grouped by `failure_reason`, `orig_store` satisfies detection threshold for 'Omnichannel Order Routing Failures'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.12.37 negative (Routing Failures): ingest benign `retail` / `oms:route` events where `failure_reason`, `orig_store` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 1 confirms zero alert rows for 'Omnichannel Order Routing Failures'."
  },
  "dataSources": "`sourcetype=\"oms:route\"` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "OMS, DOM — Universal Forwarder (Splunkbase 617)",
  "spl": "index=retail sourcetype=\"oms:route\"\n| where route_status=\"FAIL\" OR promise_date=0\n| stats count, values(order_id) as orders by failure_reason, orig_store\n| sort -count",
  "description": "Detects Routing Failures activity in UC-10.12.37 ('Omnichannel Order Routing Failures') from `index=retail` sourcetype `oms:route`. Rolls up `values` by `failure_reason`, `orig_store` across `the scheduled window` and pages when configured threshold. Covers Omnichannel, Order, Routing, Failures. Security Infra operators treat UC-10.12.37 as the canonical signal.",
  "value": "Operators running UC-10.12.37 on `oms:route` feeds shorten triage for Routing Failures tied to 'Omnichannel Order Routing Failures', lowering missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Fallback routing rules; alert when failure rate exceeds 2% in any region for 15m.",
  "visualization": "Bar chart (reasons), Table (stores with failures), Map (regional heat).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable for the primary search: it relies on application- and industry-specific log formats, not a normalized CIM data model. Optional mapping to CIM, if you adopt it, is a separate follow-on project.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `retail` (UC-10.12.37)** — Planned work for 'Omnichannel Order Routing Failures' shifts `values` over `span=1h` while `failure_reason`, `orig_store` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `values` on `oms:route`** — Scheduled automation elevates `orig_store` without crossing configured threshold for UC-10.12.37. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `failure_reason`, `orig_store`** — Health checks in `retail` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `oms:route`** — First 48h baseline for UC-10.12.37 on `failure_reason` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `failure_reason`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: OMS, DOM.\n• Ensure the following data sources are available: `sourcetype=\"oms:route\"`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nFallback routing rules; alert when failure rate exceeds 2% in any region for 15m.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=retail sourcetype=\"oms:route\"\n| where route_status=\"FAIL\" OR promise_date=0\n| stats count, values(order_id) as orders by failure_reason, orig_store\n| sort -count\n```\n\nUnderstanding this SPL\n\n**Omnichannel Order Routing Failures** — Failed ship-from-store or DC allocation breaks promise dates and loyalty SLAs.\n\nDocumented **Data sources**: `sourcetype=\"oms:route\"`. **App/TA** (typical add-on context): OMS, DOM. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: retail; **sourcetype**: oms:route. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=retail, sourcetype=\"oms:route\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where route_status=\"FAIL\" OR promise_date=0` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by failure_reason, orig_store** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nRun the search on recent production data, spot-check a handful of results against the source system or operational runbooks (cash operations, EHR, payment gateway, etc.), and adjust thresholds to match your seasonal patterns and test windows so teams are not flooded with noise.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Bar chart (reasons), Table (stores with failures), Map (regional heat).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (OMS, DOM — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=oms:route into retail. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`failure_reason, orig_store`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "In Retail and E-Commerce, we pay close attention to \"Omnichannel Order Routing Failures\" in our day-to-day work so that fraud, waste, and compliance issues do not slip by quietly. We help teams focus on the few situations that need a person, not every routine blip in the data.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
