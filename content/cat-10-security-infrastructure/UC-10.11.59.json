{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.11.59",
  "title": "ZPA Connector Health",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'ZPA Connector Health' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_zpa_connector_health` (UC-10.11.59), SOC dashboard panel \"Security Infra — ZPA Connector Health\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.11.59 positive (ZPA, Connector, Health): ingest synthetic events into `index=network` sourcetype='zscaler:connector' where `count` grouped by `connector_name` satisfies detection threshold for 'ZPA Connector Health'. Validate filter `st!=\"ONLINE\"`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.11.59 negative (Connector Health): ingest benign `network` / `zscaler:connector` events where `connector_name` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 11 confirms zero alert rows for 'ZPA Connector Health'."
  },
  "dataSources": "`sourcetype=zscaler:connector` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_zscaler` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=network sourcetype=\"zscaler:connector\"\n| stats latest(status) as st by connector_name\n| search st!=\"ONLINE\"",
  "description": "Alerts on UC-10.11.59 — ZPA, Connector, Health — using `index=network` sourcetype `zscaler:connector`, grouped by `connector_name`. Outputs `count` each `the scheduled window`; alert when configured threshold. Filter chain includes filter `st!=\"ONLINE\"`. Purpose-built for 'ZPA Connector Health' in Security Infra workflows.",
  "value": "UC-10.11.59 helps teams contain ZPA, Connector, Health scenarios and reduce missed threat campaigns, control regressions, and SOC SLA breaches before incidents compound.",
  "implementation": "Monitor private service edge and app connector heartbeats. Page on offline connectors affecting access.",
  "visualization": "Table (connectors), Single value (offline count).",
  "cimModels": [
    "Network_Sessions"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Network_Sessions.All_Sessions by All_Sessions.action, All_Sessions.src, All_Sessions.user | sort - count",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Network_Sessions.All_Sessions` dataset after normalizing this feed to the model; otherwise the `cimSpl` tstats search may return no rows.",
  "references": [
    {
      "title": "CIM: Network_Sessions",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Network_Sessions",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `network` (UC-10.11.59)** — Planned work for 'ZPA Connector Health' shifts `count` over `span=1h` while `connector_name` remains in-scope. Applies filter `st!=\"ONLINE\"`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `zscaler:connector`** — Scheduled automation elevates `src` without crossing configured threshold for UC-10.11.59. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `connector_name`** — Health checks in `network` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `zscaler:connector`** — First 48h baseline for UC-10.11.59 on `connector_name` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `connector_name`, `exception_reason`, `valid_until`.",
  "detectionType": "Anomaly",
  "securityDomain": "network",
  "equipment": [
    "zscaler"
  ],
  "equipmentModels": [
    "zscaler_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_zscaler`.\n• Ensure the following data sources are available: `sourcetype=zscaler:connector`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMonitor private service edge and app connector heartbeats. Page on offline connectors affecting access.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=network sourcetype=\"zscaler:connector\"\n| stats latest(status) as st by connector_name\n| search st!=\"ONLINE\"\n```\n\nUnderstanding this SPL\n\n**ZPA Connector Health** — Connector health is prerequisite for ZPA private application reachability.\n\nDocumented **Data sources**: `sourcetype=zscaler:connector`. **App/TA** (typical add-on context): `Splunk_TA_zscaler`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: network; **sourcetype**: zscaler:connector. That sourcetype matches what this use case lists under Data sources.\n\n**Detection type** for this use case: Anomaly — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=network, sourcetype=\"zscaler:connector\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by connector_name** so each row reflects one combination of those dimensions.\n• Applies an explicit `search` filter to narrow the current result set.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats summariesonly=t count from datamodel=Network_Sessions.All_Sessions by All_Sessions.action, All_Sessions.src, All_Sessions.user | sort - count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**ZPA Connector Health** — Connector health is prerequisite for ZPA private application reachability.\n\nDocumented **Data sources**: `sourcetype=zscaler:connector`. **App/TA** (typical add-on context): `Splunk_TA_zscaler`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Detection type** for this use case: Anomaly — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Network_Sessions.All_Sessions` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nRun the search over a window that includes both normal production traffic and, if you have one, a documented test (vendor sandbox, lab rule hit, or exercise). Compare a few sample events to the vendor’s own console or reports and tune indexes, fields, and thresholds so alerts are actionable before paging.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (connectors), Single value (offline count).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_zscaler` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=zscaler:connector into network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`connector_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use Zscaler data and alerts to stay ahead of the risks described in \"ZPA Connector Health\". We help the team notice trouble early, while a situation is still small and containable.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
