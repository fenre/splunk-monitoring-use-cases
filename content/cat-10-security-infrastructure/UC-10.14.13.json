{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.14.13",
  "title": "OT Asset Risk Score Threshold Breach",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'OT Asset Risk Score Threshold Breach' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_ot_asset_risk_score_threshold_breach` (UC-10.14.13), SOC dashboard panel \"Security Infra — OT Asset Risk Score Threshold Breach\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.14.13 positive (Asset, Risk, Score, Threshold, Breach): ingest synthetic events into `index=ot_security` sourcetype='ot:security:risk' where `count` grouped by `asset_id` satisfies detection threshold for 'OT Asset Risk Score Threshold Breach'. Validate lookup `ot_risk_thresholds`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.14.13 negative (Threshold Breach): ingest benign `ot_security` / `ot:security:risk` events where `asset_id` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 4 confirms zero alert rows for 'OT Asset Risk Score Threshold Breach'."
  },
  "dataSources": "`index=ot_security` `sourcetype=\"ot:security:risk\"` ES risk framework outputs Indexes: ot_security. Sourcetypes: ot:security:risk. Key fields: asset_class, asset_id, max_risk. Validate field extraction in Search before saving the alert; compare event counts to the vendor admin console.",
  "app": "OT Security Add-on for Splunk (Splunkbase 5151)",
  "spl": "index=ot_security sourcetype=\"ot:security:risk\"\n| stats latest(risk_score) as risk by asset_id\n| lookup ot_risk_thresholds asset_class OUTPUT max_risk\n| where risk>max_risk\n| sort - risk\n| table asset_id, risk, max_risk",
  "description": "Identifies risk tied to Asset, Risk, Score, Threshold, Breach through UC-10.14.13 on `index=ot_security` sourcetype `ot:security:risk`. Summarises `count` grouped by `asset_id` across `the scheduled window` before applying configured threshold. Operators tune 'OT Asset Risk Score Threshold Breach' using UC-10.14.13 runbook steps.",
  "value": "Tracking `activity` via UC-10.14.13 on `ot:security:risk` feeds improves response to 'OT Asset Risk Score Threshold Breach' and reduces missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Configure Splunk ES risk notables with OT Security Add-on scoring models. Align thresholds with corporate risk appetite and NERC CIP-relevant assets.",
  "visualization": "Bubble chart (risk vs asset class), Prioritized table, Single value (breach count).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "OT Security Add-on for Splunk",
      "url": "https://splunkbase.splunk.com/app/5151",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `ot_security` (UC-10.14.13)** — Planned work for 'OT Asset Risk Score Threshold Breach' shifts `count` over `span=1h` while `asset_id` remains in-scope. Applies lookup `ot_risk_thresholds`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `ot:security:risk`** — Scheduled automation elevates `src` without crossing configured threshold for UC-10.14.13. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `asset_id`** — Health checks in `ot_security` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `ot:security:risk`** — First 48h baseline for UC-10.14.13 on `asset_id` is unstable; join `ot_risk_thresholds` prior to threshold.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `asset_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk OT Security Add-on"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: OT Security Add-on for Splunk (Splunkbase 5151).\n• Ensure the following data sources are available: `index=ot_security` `sourcetype=\"ot:security:risk\"` ES risk framework outputs.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nConfigure Splunk ES risk notables with OT Security Add-on scoring models. Align thresholds with corporate risk appetite and NERC CIP-relevant assets.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot_security sourcetype=\"ot:security:risk\"\n| stats latest(risk_score) as risk by asset_id\n| lookup ot_risk_thresholds asset_class OUTPUT max_risk\n| where risk>max_risk\n| sort - risk\n| table asset_id, risk, max_risk\n```\n\nUnderstanding this SPL\n\n**OT Asset Risk Score Threshold Breach** — Tracks cumulative OT asset risk from vulnerabilities and behavioral signals so operations can prioritize patching and isolation before compliance gaps widen.\n\nDocumented **Data sources**: `index=ot_security` `sourcetype=\"ot:security:risk\"` ES risk framework outputs. **App/TA** (typical add-on context): OT Security Add-on for Splunk (Splunkbase 5151). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot_security; **sourcetype**: ot:security:risk. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot_security, sourcetype=\"ot:security:risk\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by asset_id** so each row reflects one combination of those dimensions.\n• Enriches events using `lookup` (lookup definition + optional OUTPUT fields).\n• Filters the current rows with `where risk>max_risk` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n• Pipeline stage (see **OT Asset Risk Score Threshold Breach**): table asset_id, risk, max_risk\n\n\nStep 3 — Validate\nCompare failed or disabled searches in Splunk to the **OT Security Add-on** configuration UI and the **Saved Searches** list so names and schedules line up. Spot-check one failure against `scheduler.log` around the run time.\n\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Bubble chart (risk vs asset class), Prioritized table, Single value (breach count).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (OT Security Add-on for Splunk (Splunkbase 5151)). Enable the modular input or HEC token that writes sourcetype=ot:security:risk into ot_security. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`asset_class, asset_id, max_risk`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use asset risk figures so we know which pieces of the OT fleet look most dangerous when scores jump above what we expect.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
