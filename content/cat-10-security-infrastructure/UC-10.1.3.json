{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.1.3",
  "title": "C2 Communication Detection",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'C2 Communication Detection' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_c2_communication_detection` (UC-10.1.3), SOC dashboard panel \"Security Infra — C2 Communication Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.1.3 positive (Communication, Detection): ingest synthetic events into `index=pan` sourcetype='pan:threat' where `values` grouped by `src`, `src_user` satisfies -24h for 'C2 Communication Detection'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.1.3 negative (Communication Detection): ingest benign `pan` / `pan:threat` events where `src`, `src_user` stays below -24h, or exclude the test entity via `soc_exception_register.csv`. Variant 8 confirms zero alert rows for 'C2 Communication Detection'."
  },
  "dataSources": "Threat logs (C2 signatures), URL filtering (malware/C2 categories), DNS logs Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_paloalto`, threat intel feeds — Universal Forwarder (Splunkbase 617)",
  "spl": "index=pan sourcetype=\"pan:threat\" category=\"command-and-control\" earliest=-24h\n| stats count, values(dest) as c2_servers by src, src_user\n| sort -count",
  "description": "Surfaces UC-10.1.3 — Communication, Detection — using `index=pan` sourcetype `pan:threat`, grouped by `src`, `src_user`. Outputs `values` each `the scheduled window`; alert when -24h. Primary focus: Communication Detection. Purpose-built for 'C2 Communication Detection' in Security Infra workflows.",
  "value": "UC-10.1.3 accelerates insight into Communication Detection ('C2 Communication Detection') by tracking `activity` on `pan:threat` feeds, reducing missed threat campaigns, control regressions, and SOC SLA breaches before spreadsheet checks hide regressions.",
  "implementation": "Enable URL filtering and threat prevention with C2 categories. Forward to Splunk. Alert immediately on any C2 detection. Integrate with threat intel feeds for IP/domain enrichment. Trigger automated containment via SOAR.",
  "visualization": "Table (C2 detections with source/dest), Geo map (C2 server locations), Timeline (C2 events), Network diagram.",
  "cimModels": [
    "Intrusion_Detection"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Intrusion_Detection.IDS_Attacks\n  by IDS_Attacks.signature IDS_Attacks.severity IDS_Attacks.src IDS_Attacks.dest span=1h\n| sort -count",
  "references": [
    {
      "title": "https://attack.mitre.org/techniques/T1071",
      "url": "https://attack.mitre.org/techniques/T1071/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Intrusion_Detection",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Intrusion_Detection",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Approved vulnerability scanner** — Scanner `src` triggers high-severity signatures for 'C2 Communication Detection'. Join `scanner_ips` before threshold.\n\n2. **Signature definition update** — Vendor content push reclassifies benign traffic. Compare threat_id first_seen with content release date.\n\n3. **Red-team emulation** — Atomic test traffic from approved exercise hosts.\n\n4. **Backup replication traffic** — Off-hours bulk transfer matches lateral-movement heuristics.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `src`, `threat_id`, `valid_until`.",
  "mitreAttack": [
    "T1071"
  ],
  "detectionType": "TTP",
  "securityDomain": "network",
  "equipment": [
    "paloalto"
  ],
  "equipmentModels": [
    "paloalto_pan_firewall"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_paloalto`, threat intel feeds.\n• Ensure the following data sources are available: Threat logs (C2 signatures), URL filtering (malware/C2 categories), DNS logs.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnable URL filtering and threat prevention with C2 categories. Forward to Splunk. Alert immediately on any C2 detection. Integrate with threat intel feeds for IP/domain enrichment. Trigger automated containment via SOAR.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=pan sourcetype=\"pan:threat\" category=\"command-and-control\" earliest=-24h\n| stats count, values(dest) as c2_servers by src, src_user\n| sort -count\n```\n\nUnderstanding this SPL\n\n**C2 Communication Detection** — Command-and-control communication indicates active compromise. Detection enables containment before data exfiltration or lateral movement.\n\nDocumented **Data sources**: Threat logs (C2 signatures), URL filtering (malware/C2 categories), DNS logs. **App/TA** (typical add-on context): `Splunk_TA_paloalto`, threat intel feeds. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: pan; **sourcetype**: pan:threat. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Detection type** for this use case: TTP — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=pan, sourcetype=\"pan:threat\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by src, src_user** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` count\n  from datamodel=Intrusion_Detection.IDS_Attacks\n  by IDS_Attacks.signature IDS_Attacks.severity IDS_Attacks.src IDS_Attacks.dest span=1h\n| sort -count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**C2 Communication Detection** — Command-and-control communication indicates active compromise. Detection enables containment before data exfiltration or lateral movement.\n\nDocumented **Data sources**: Threat logs (C2 signatures), URL filtering (malware/C2 categories), DNS logs. **App/TA** (typical add-on context): `Splunk_TA_paloalto`, threat intel feeds. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Detection type** for this use case: TTP — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Intrusion_Detection.IDS_Attacks` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare with Panorama or the NGFW management console (Monitor → logs) for the same time window, then confirm field extractions and index permissions in Splunk.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (C2 detections with source/dest), Geo map (C2 server locations), Timeline (C2 events), Network diagram.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_paloalto`, threat intel feeds — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=pan:threat into pan. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`src, src_user`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for C2-style traffic at the firewall so we can contain active compromise while there is time to respond.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
