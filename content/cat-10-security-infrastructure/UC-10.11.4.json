{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.11.4",
  "title": "FortiGate Web Filter Category Blocks",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'FortiGate Web Filter Category Blocks' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_fortigate_web_filter_category_blocks` (UC-10.11.4), SOC dashboard panel \"Security Infra — FortiGate Web Filter Category Blocks\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=web` sourcetype='fortinet:fortigate_webfilter' that satisfy the UC-10.11.4 SPL filters for 'FortiGate Web Filter Category Blocks'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `web` / `fortinet:fortigate_webfilter` events outside the detection threshold, or exclude the test entity via `soc_exception_register.csv` with a valid window. Confirm zero alert rows for UC-10.11.4."
  },
  "dataSources": "`sourcetype=fortinet:fortigate_webfilter` Collected via Splunk Add-on for Fortinet FortiGate (Splunkbase 2846) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_fortinet` — Splunk Add-on for Fortinet FortiGate (Splunkbase 2846)",
  "spl": "index=web sourcetype=\"fortinet:fortigate_webfilter\" action=blocked\n| stats count by category, user, src\n| sort -count",
  "description": "Category blocks expose risky browsing, policy misuse, and potential malware download attempts.",
  "value": "Early 'FortiGate Web Filter Category Blocks' detection reduces missed threat campaigns, control regressions, and SOC SLA breaches by giving Security Infra operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Ingest explicit-proxy or flow-mode web filter logs. Join category IDs to human-readable names. Report top blocked categories and repeat offenders.",
  "visualization": "Bar chart (categories), Table (users with high block counts), Pie chart (category mix).",
  "cimModels": [
    "Web"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Web.Web by Web.src | sort - count",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Web.Web` dataset after normalizing this feed to the model; otherwise the `cimSpl` tstats search may return no rows.",
  "references": [
    {
      "title": "CIM: Web",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Web",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **SOC tuning or purple-team exercise** — Expected alert volume during rule validation for 'FortiGate Web Filter Category Blocks'. Match active CHG with `purple_team=true`.\n\n2. **Content deployment window** — TA or CIM update shifts field extractions on `index=web` `fortinet:fortigate_webfilter`.\n\n3. **Duplicate log ingestion** — HF/IDX misconfiguration doubles event counts.\n\n4. **Baseline seasonality** — Month-end batch jobs skew trending searches.\n\n**Suppression mechanism:** `soc_exception_register.csv` with entity, reason, valid_until.",
  "detectionType": "TTP",
  "securityDomain": "network",
  "equipment": [
    "fortinet"
  ],
  "equipmentModels": [
    "fortinet_fortigate"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_fortinet`.\n• Ensure the following data sources are available: `sourcetype=fortinet:fortigate_webfilter`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest explicit-proxy or flow-mode web filter logs. Join category IDs to human-readable names. Report top blocked categories and repeat offenders.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=web sourcetype=\"fortinet:fortigate_webfilter\" action=blocked\n| stats count by category, user, src\n| sort -count\n```\n\nUnderstanding this SPL\n\n**FortiGate Web Filter Category Blocks** — Category blocks expose risky browsing, policy misuse, and potential malware download attempts.\n\nDocumented **Data sources**: `sourcetype=fortinet:fortigate_webfilter`. **App/TA** (typical add-on context): `Splunk_TA_fortinet`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: web; **sourcetype**: fortinet:fortigate_webfilter. That sourcetype matches what this use case lists under Data sources.\n\n**Detection type** for this use case: TTP — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=web, sourcetype=\"fortinet:fortigate_webfilter\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by category, user, src** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats summariesonly=t count from datamodel=Web.Web by Web.src | sort - count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**FortiGate Web Filter Category Blocks** — Category blocks expose risky browsing, policy misuse, and potential malware download attempts.\n\nDocumented **Data sources**: `sourcetype=fortinet:fortigate_webfilter`. **App/TA** (typical add-on context): `Splunk_TA_fortinet`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Detection type** for this use case: TTP — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Web.Web` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nRun the search over a window that includes both normal production traffic and, if you have one, a documented test (vendor sandbox, lab rule hit, or exercise). Compare a few sample events to the vendor’s own console or reports and tune indexes, fields, and thresholds so alerts are actionable before paging.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Bar chart (categories), Table (users with high block counts), Pie chart (category mix).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_fortinet` — Splunk Add-on for Fortinet FortiGate (Splunkbase 2846)). Enable the modular input or HEC token that writes sourcetype=fortinet:fortigate_webfilter into web. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`category, src, user`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use FortiGate data and alerts to stay ahead of the risks described in \"FortiGate Web Filter Category Blocks\". We help the team notice trouble early, while a situation is still small and containable.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
