{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.14.18",
  "title": "NERC CIP System Security Management Patch Tracking",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Compliance",
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'NERC CIP System Security Management Patch Tracking' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_nerc_cip_system_security_management_patch_tracki` (UC-10.14.18), SOC dashboard panel \"Security Infra — NERC CIP System Security Management Patch Tracking\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "compliance": [
    {
      "regulation": "nerc-cip",
      "version": "current",
      "clause": "CIP-007-6 R2",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Auto-generated from legacy markdown 'Regulations' line during the per-UC migration. Requires SME review to fill in version, clause, mode, and assurance for NERC CIP.",
      "controlObjective": "Evidence that NERC CIP unknown is enforced — Splunk UC-10.14.18: NERC CIP System Security Management Patch Tracking.",
      "evidenceArtifact": "Saved search 'UC-10.14.18' running on index=ot_security vulnerability/patch feeds with bes_asset=1, archived to the restricted audit_evidence index (default 7-year retention). Auto-drafted — SME review required.",
      "obligationRef": "nerc-cip@current#CIP-007-6 R2",
      "requires_sme_review": true,
      "provenance": "maintainer"
    }
  ],
  "controlTest": {
    "positiveScenario": "UC-10.14.18 positive (NERC, CIP, System, Security, Management): ingest synthetic events into `index=ot_security` sourcetype='ot:security:patch' where `max_sev` grouped by `asset_id`, `hostname`, `deadline`, `applied` satisfies `max_sev` >= 7 for 'NERC CIP System Security Management Patch Tracking'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.14.18 negative (Patch Tracking): ingest benign `ot_security` / `ot:security:patch` events where `asset_id`, `hostname`, `deadline`, `applied` stays below `max_sev` >= 7, or exclude the test entity via `soc_exception_register.csv`. Variant 11 confirms zero alert rows for 'NERC CIP System Security Management Patch Tracking'."
  },
  "dataSources": "`index=ot_security` vulnerability/patch feeds with `bes_asset=1` Indexes: ot_security. Sourcetypes: ot:security:patch. Key fields: applied, asset_id, deadline, hostname, max_sev, overdue. Validate field extraction in Search before saving the alert; compare event counts to the vendor admin console.",
  "app": "OT Security Add-on for Splunk (Splunkbase 5151)",
  "spl": "index=ot_security sourcetype=\"ot:security:patch\" bes_asset=1\n| eval overdue=if(patch_available=1 AND now()>deadline AND applied=0, 1, 0)\n| stats values(cve) as cves max(severity_score) as max_sev by asset_id, hostname, deadline, applied\n| where overdue=1 OR (max_sev>=7 AND applied=0)\n| eval cip_standard=\"CIP-007\"\n| table cip_standard, asset_id, hostname, cves, max_sev, deadline, applied",
  "description": "Monitors UC-10.14.18 — NERC, CIP, System, Security, Management — using `index=ot_security` sourcetype `ot:security:patch`, grouped by `asset_id`, `hostname`, `deadline`, `applied`. Outputs `max_sev` each `the scheduled window`; alert when `max_sev` >= 7. Primary focus: Patch Tracking. Purpose-built for 'NERC CIP System Security Management Patch Tracking' in Security Infra workflows.",
  "value": "UC-10.14.18 helps teams contain NERC, CIP, System, Security, Management scenarios and reduce missed threat campaigns, control regressions, and SOC SLA breaches before incidents compound.",
  "implementation": "Integrate patch and vulnerability scanners with Splunk ES and OT Security Add-on for BES asset groups. Document evaluation intervals per CIP-007. Drive remediation tickets from overdue findings.",
  "visualization": "Gauge of patch compliance %, Timeline to deadlines, Treemap by severity.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "OT Security Add-on for Splunk",
      "url": "https://splunkbase.splunk.com/app/5151",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `ot_security` (UC-10.14.18)** — Planned work for 'NERC CIP System Security Management Patch Tracking' shifts `max_sev` over `span=1h` while `asset_id`, `hostname`, `deadline`, `applied` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `values` on `ot:security:patch`** — Scheduled automation elevates `hostname` without crossing `max_sev` >= 7 for UC-10.14.18. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `asset_id`, `hostname`, `deadline`, `applied`** — Health checks in `ot_security` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `ot:security:patch`** — First 48h baseline for UC-10.14.18 on `asset_id` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `asset_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk OT Security Add-on"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: OT Security Add-on for Splunk (Splunkbase 5151).\n• Ensure the following data sources are available: `index=ot_security` vulnerability/patch feeds with `bes_asset=1`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIntegrate patch and vulnerability scanners with Splunk ES and OT Security Add-on for BES asset groups. Document evaluation intervals per CIP-007. Drive remediation tickets from overdue findings.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot_security sourcetype=\"ot:security:patch\" bes_asset=1\n| eval overdue=if(patch_available=1 AND now()>deadline AND applied=0, 1, 0)\n| stats values(cve) as cves max(severity_score) as max_sev by asset_id, hostname, deadline, applied\n| where overdue=1 OR (max_sev>=7 AND applied=0)\n| eval cip_standard=\"CIP-007\"\n| table cip_standard, asset_id, hostname, cves, max_sev, deadline, applied\n```\n\nUnderstanding this SPL\n\n**NERC CIP System Security Management Patch Tracking** — Supports NERC CIP-007 by tracking patch status and timelines for BES Cyber Assets, reducing exploitable vulnerabilities on cyber systems.\n\nDocumented **Data sources**: `index=ot_security` vulnerability/patch feeds with `bes_asset=1`. **App/TA** (typical add-on context): OT Security Add-on for Splunk (Splunkbase 5151). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot_security; **sourcetype**: ot:security:patch. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot_security, sourcetype=\"ot:security:patch\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **overdue** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by asset_id, hostname, deadline, applied** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where overdue=1 OR (max_sev>=7 AND applied=0)` — typically the threshold or rule expression for this monitoring goal.\n• `eval` defines or adjusts **cip_standard** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Pipeline stage (see **NERC CIP System Security Management Patch Tracking**): table cip_standard, asset_id, hostname, cves, max_sev, deadline, applied\n\n\nStep 3 — Validate\nCompare failed or disabled searches in Splunk to the **OT Security Add-on** configuration UI and the **Saved Searches** list so names and schedules line up. Spot-check one failure against `scheduler.log` around the run time.\n\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Gauge of patch compliance %, Timeline to deadlines, Treemap by severity.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (OT Security Add-on for Splunk (Splunkbase 5151)). Enable the modular input or HEC token that writes sourcetype=ot:security:patch into ot_security. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`applied, asset_id, deadline, hostname`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We track patch and hardening work on critical systems so we can show the board and the regulator the real backlog, not a guess.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
