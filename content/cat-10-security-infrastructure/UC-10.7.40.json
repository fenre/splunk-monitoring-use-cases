{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.7.40",
  "title": "MCP Postgres Suspicious Query",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'MCP Postgres Suspicious Query' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_mcp_postgres_suspicious_query` (UC-10.7.40), SOC dashboard panel \"Security Infra — MCP Postgres Suspicious Query\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.7.40 positive (MCP, Postgres, Suspicious, Query): ingest synthetic events into `index=target_index` sourcetype='target_sourcetype' where `firstTime`, `lastTime`, `attack_diversity` grouped by `dest` satisfies detection threshold for 'MCP Postgres Suspicious Query'. Validate macro `mcp_postgres_suspicious_query_filter`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.7.40 negative (Suspicious Query): ingest benign `target_index` / `target_sourcetype` events where `dest` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 10 confirms zero alert rows for 'MCP Postgres Suspicious Query'."
  },
  "dataSources": "MCP Server Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Security Essentials / ESCU — Universal Forwarder (Splunkbase 617)",
  "spl": "`mcp_server` method=query direction=inbound\n    | eval dest=host\n    | eval query_lower=lower('params.query')\n    | eval suspicious_query='params.query'\n    | eval is_priv_escalation=if(like(query_lower, \"%update%users%role%admin%\") OR like(query_lower, \"%grant%admin%\") OR like(query_lower, \"%grant%superuser%\"), 1, 0)\n    | eval is_credential_theft=if(like(query_lower, \"%password%\") OR like(query_lower, \"%credential%\") OR like(query_lower, \"%api_key%\") OR like(query_lower, \"%secret%\"), 1, 0)\n    | eval is_recon=if(like(query_lower, \"%information_schema%\") OR like(query_lower, \"%pg_catalog%\") OR like(query_lower, \"%pg_tables%\") OR like(query_lower, \"%pg_user%\"), 1, 0)\n    | where is_priv_escalation=1 OR is_credential_theft=1 OR is_recon=1\n    | eval attack_type=case(\n        is_priv_escalation=1, \"Privilege Escalation\",\n        is_credential_theft=1, \"Credential Theft\",\n        is_recon=1, \"Schema Reconnaissance\",\n        1=1, \"Unknown\")\n    | stats count min(_time) as firstTime max(_time) as lastTime values(suspicious_query) as suspicious_queries values(attack_type) as attack_types dc(attack_type) as attack_diversity by dest\n    | `security_content_ctime(firstTime)`\n    | `security_content_ctime(lastTime)`\n    | table dest firstTime lastTime count suspicious_queries attack_types attack_diversity\n    | `mcp_postgres_suspicious_query_filter`",
  "description": "Identifies UC-10.7.40 — MCP, Postgres, Suspicious, Query — using `index=target_index` sourcetype `configured sourcetype`, grouped by `dest`. Outputs `firstTime`, `lastTime`, `attack_diversity` each `the scheduled window`; alert when configured threshold. Filter chain includes macro `mcp_postgres_suspicious_query_filter`. Purpose-built for 'MCP Postgres Suspicious Query' in Security Infra workflows.",
  "value": "For 'MCP Postgres Suspicious Query', UC-10.7.40 converts `firstTime`, `lastTime`, `attack_diversity` on macro `mcp_postgres_suspicious_query_filter` into actionable rows and reduces missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "ESCU Hunting detection — not designed for automated alerting. Run ad-hoc or on a low-frequency schedule from Splunk Enterprise Security for analyst-driven investigation. Requires MCP Server ingested and CIM-normalized.",
  "visualization": "Table, Timeline (from ESCU).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "https://splunkbase.splunk.com/app/8377",
      "url": "https://splunkbase.splunk.com/app/8377",
      "retrieved": "2026-04-25"
    },
    {
      "title": "https://www.nodejs-security.com/blog/the-tale-of-the-vulnerable-mcp-database-server",
      "url": "https://www.nodejs-security.com/blog/the-tale-of-the-vulnerable-mcp-database-server",
      "retrieved": "2026-04-25"
    },
    {
      "title": "https://www.splunk.com/en_us/blog/security/securing-ai-agents-model-context-protocol.html",
      "url": "https://www.splunk.com/en_us/blog/security/securing-ai-agents-model-context-protocol.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `target_index` (UC-10.7.40)** — Planned work for 'MCP Postgres Suspicious Query' shifts `firstTime`, `lastTime`, `attack_diversity` over `span=1h` while `dest` remains in-scope. Applies macro `mcp_postgres_suspicious_query_filter`. Cross-check CMDB CHG.\n\n2. **Batch `min` on `configured_sourcetype`** — Scheduled automation elevates `dest` without crossing configured threshold for UC-10.7.40. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `dest`** — Health checks in `target_index` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-10.7.40 on `dest` is unstable; join `mcp_server` prior to threshold.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `dest`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1555"
  ],
  "detectionType": "Hunting",
  "equipment": [
    "mssql",
    "postgresql",
    "security_essentials"
  ],
  "equipmentModels": [
    "postgresql_pg"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk Enterprise Security"
  ],
  "detailedImplementation": "Enterprise Security Detection Rule\n\n\"MCP Postgres Suspicious Query\" is a Hunting detection designed for analyst-driven threat hunting rather than automated alerting, sourced from the Splunk Enterprise Security Content Update (ESCU). Hunting detections are not intended for automated alerting. They are run on-demand or on a low-frequency schedule by security analysts investigating specific threat hypotheses.\n\nPrerequisites\n\n• Splunk Enterprise Security 7.x or later with the ES Content Update (ESCU) app installed and up to date.\n• Data sources: MCP Server. Must be ingested into Splunk and normalized to the Common Information Model (CIM) via the appropriate Technology Add-on.\n• MITRE ATT&CK: T1555. Review the ATT&CK matrix for adjacent techniques to identify detection coverage gaps in your environment.\n\nDeployment\n\n1. In Enterprise Security, navigate to Configure → Content → Content Management.\n2. Search for \"MCP Postgres Suspicious Query\" or filter by Analytic Story.\n3. Hunting detections are typically left disabled for automated scheduling. Instead, run them on-demand from the Search bar or schedule at low frequency (daily or weekly).\n4. Review results manually — hunting detections cast a wider net and expect analyst judgment to separate signal from noise.\n5. When results warrant further investigation, create a Notable Event manually or initiate your incident response workflow.\n\nTuning and False Positive Management\n\nKnown false positives for this detection: Legitimate database administrators performing user management tasks, ORM frameworks querying information_schema for schema validation, password reset functionality, and CI/CD pipelines running database migrations.\n\n• Hunting detections are expected to produce broader result sets that require analyst interpretation. Focus on refining the search scope (time range, specific hosts or users) rather than eliminating all noise.\n• Maintain a hunting journal documenting hypotheses tested, results found, and detection improvements identified.\n• Share high-fidelity findings with the detection engineering team to convert recurring hunting patterns into automated TTP or Anomaly detections.\n\nAnalyst Response Workflow\n\nWhen reviewing Hunting results:\n\n1. Run the search manually or review scheduled results. Evaluate each returned event against your threat hypothesis — not every result indicates compromise.\n2. Cross-reference findings with current threat intelligence, recent security advisories, and outputs from other detection sources.\n3. If suspicious activity is confirmed, create a Notable Event or escalate directly to your incident response process with the supporting evidence.\n4. Document the hunting exercise: hypothesis, data sources queried, time range, findings, and any detection engineering improvements identified.\n\nValidation\n\nConfirm the required data sources are flowing:\n\n```spl\n| tstats count where index=* by index, sourcetype | sort -count\n```\n\nVerify the detection is enabled and firing:\n\n```spl\nindex=_audit action=\"alert_fired\" ss_name=\"*\"\n| stats count by ss_name, trigger_time | sort -trigger_time\n```\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Security Essentials / ESCU — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into index=main. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`attack_type, dest, is_credential_theft, is_priv_escalation`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use this 'MCP Postgres Suspicious Query' search to catch the exact behavior in the data we already collect, so the team can confirm or clear it in the right management console or identity store.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
