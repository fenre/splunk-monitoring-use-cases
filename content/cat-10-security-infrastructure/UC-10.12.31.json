{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.12.31",
  "title": "POS Transaction Anomaly Detection",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fraud"
  ],
  "splunkPillar": "Security",
  "industry": "Retail and E-Commerce",
  "exclusions": "Covers the detection logic documented for 'POS Transaction Anomaly Detection' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_pos_transaction_anomaly_detection` (UC-10.12.31), SOC dashboard panel \"Security Infra — POS Transaction Anomaly Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=retail` sourcetype='pos:txn' where `store_id` satisfies `void_rate` > 0.15 for UC-10.12.31 ('POS Transaction Anomaly Detection'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `retail` / `pos:txn` events where `store_id` stays below `void_rate` > 0.15, or exclude the test entity via `soc_exception_register.csv` with a valid window. Confirm zero alert rows for UC-10.12.31."
  },
  "dataSources": "`sourcetype=\"pos:txn\"` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "POS gateway, store systems — Universal Forwarder (Splunkbase 617)",
  "spl": "index=retail sourcetype=\"pos:txn\"\n| eval is_void=if(txn_type IN (\"VOID\",\"RETURN\"),1,0)\n| stats sum(is_void) as voids, count as txns, sum(amount) as sales by store_id, terminal_id\n| eval void_rate=voids/txns\n| where void_rate>0.15 OR voids>30\n| sort -void_rate",
  "description": "Spikes in voids, returns, or manager overrides may indicate insider theft or terminal compromise. Searches `index=retail` with sourcetype `pos:txn` for UC-10.12.31, aggregating `sum` by `store_id` over `the scheduled window`. Alerts when grouped results cross `void_rate` > 0.15 in the saved search. Gives Security Infra operators a repeatable signal for 'POS Transaction Anomaly Detection' without manual log review.",
  "value": "Early 'POS Transaction Anomaly Detection' detection reduces missed threat campaigns, control regressions, and SOC SLA breaches by giving Security Infra operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Compare peer stores; video correlate with exception transactions; PCI-safe tokenization at ingest.",
  "visualization": "Table (hot terminals), Map (stores), Line chart (void rate trend).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable for the primary search: it relies on application- and industry-specific log formats, not a normalized CIM data model. Optional mapping to CIM, if you adopt it, is a separate follow-on project.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `retail`** — Planned work for 'POS Transaction Anomaly Detection' shifts `sum` over `span=1h` while `store_id` remains in-scope for UC-10.12.31. Cross-check CMDB CHG.\n\n2. **Batch `sum` on `pos:txn`** — Scheduled automation elevates `terminal_id` without crossing `void_rate` > 0.15. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `store_id`** — Health checks in `retail` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `pos:txn`** — First 48h baseline for `store_id` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `store_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: POS gateway, store systems.\n• Ensure the following data sources are available: `sourcetype=\"pos:txn\"`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nCompare peer stores; video correlate with exception transactions; PCI-safe tokenization at ingest.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=retail sourcetype=\"pos:txn\"\n| eval is_void=if(txn_type IN (\"VOID\",\"RETURN\"),1,0)\n| stats sum(is_void) as voids, count as txns, sum(amount) as sales by store_id, terminal_id\n| eval void_rate=voids/txns\n| where void_rate>0.15 OR voids>30\n| sort -void_rate\n```\n\nUnderstanding this SPL\n\n**POS Transaction Anomaly Detection** — Spikes in voids, returns, or manager overrides may indicate insider theft or terminal compromise.\n\nDocumented **Data sources**: `sourcetype=\"pos:txn\"`. **App/TA** (typical add-on context): POS gateway, store systems. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: retail; **sourcetype**: pos:txn. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=retail, sourcetype=\"pos:txn\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **is_void** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by store_id, terminal_id** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **void_rate** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where void_rate>0.15 OR voids>30` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nRun the search on recent production data, spot-check a handful of results against the source system or operational runbooks (cash operations, EHR, payment gateway, etc.), and adjust thresholds to match your seasonal patterns and test windows so teams are not flooded with noise.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (hot terminals), Map (stores), Line chart (void rate trend).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (POS gateway, store systems — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=pos:txn into retail. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`is_void, store_id, terminal_id, void_rate`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "In Retail and E-Commerce, we pay close attention to \"POS Transaction Anomaly Detection\" in our day-to-day work so that fraud, waste, and compliance issues do not slip by quietly. We help teams focus on the few situations that need a person, not every routine blip in the data.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
