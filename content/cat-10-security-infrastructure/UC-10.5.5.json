{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.5.5",
  "title": "DNS Security Events",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'DNS Security Events' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_dns_security_events` (UC-10.5.5), SOC dashboard panel \"Security Infra — DNS Security Events\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=dns_security` sourcetype='cisco:umbrella:dns' that satisfy the UC-10.5.5 SPL filters for 'DNS Security Events'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `dns_security` / `cisco:umbrella:dns` events outside the detection threshold, or exclude the test entity via `soc_exception_register.csv` with a valid window. Confirm zero alert rows for UC-10.5.5."
  },
  "dataSources": "Umbrella/DNS security logs Collected via Splunk Add-on for Cisco Security (Splunkbase 1352) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Add-on for Cisco Umbrella — Splunk Add-on for Cisco Security (Splunkbase 1352)",
  "spl": "index=dns_security sourcetype=\"cisco:umbrella:dns\" action=\"Blocked\"\n| stats count by internalIp, domain, categories\n| where match(categories,\"Malware|Command and Control|Phishing\")\n| sort -count",
  "description": "Blocked DNS queries to malicious domains indicate infection attempts or active compromise. Each block is a security win.",
  "value": "Early 'DNS Security Events' detection reduces missed threat campaigns, control regressions, and SOC SLA breaches by giving Security Infra operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Deploy DNS security (Umbrella, Zscaler). Forward blocked query logs to Splunk. Alert on blocks in malware/C2/phishing categories. Track affected internal IPs for investigation. Report on DNS security effectiveness.",
  "visualization": "Table (blocked domains with sources), Bar chart (blocks by category), Single value (unique blocked domains today).",
  "cimModels": [
    "Network_Resolution"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Network_Resolution.DNS\n  where DNS.action=\"blocked\"\n  by DNS.query DNS.src span=1h\n| sort -count",
  "references": [
    {
      "title": "CIM: Network_Resolution",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Network_Resolution",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Approved vulnerability scanner** — Scanner `src` triggers high-severity signatures for 'DNS Security Events'. Join `scanner_ips` before threshold.\n\n2. **Signature definition update** — Vendor content push reclassifies benign traffic. Compare threat_id first_seen with content release date.\n\n3. **Red-team emulation** — Atomic test traffic from approved exercise hosts.\n\n4. **Backup replication traffic** — Off-hours bulk transfer matches lateral-movement heuristics.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `src`, `threat_id`, `valid_until`.",
  "equipment": [
    "cisco",
    "zscaler"
  ],
  "equipmentModels": [
    "cisco_generic",
    "zscaler_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Add-on for Cisco Umbrella.\n• Ensure the following data sources are available: Umbrella/DNS security logs.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nDeploy DNS security (Umbrella, Zscaler). Forward blocked query logs to Splunk. Alert on blocks in malware/C2/phishing categories. Track affected internal IPs for investigation. Report on DNS security effectiveness.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=dns_security sourcetype=\"cisco:umbrella\" action=\"Blocked\"\n| stats count by internalIp, domain, categories\n| where match(categories,\"Malware|Command and Control|Phishing\")\n| sort -count\n```\n\nUnderstanding this SPL\n\n**DNS Security Events** — Blocked DNS queries to malicious domains indicate infection attempts or active compromise. Each block is a security win.\n\nDocumented **Data sources**: Umbrella/DNS security logs. **App/TA** (typical add-on context): Splunk Add-on for Cisco Umbrella. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: dns_security; **sourcetype**: cisco:umbrella. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=dns_security, sourcetype=\"cisco:umbrella\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by internalIp, domain, categories** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where match(categories,\"Malware|Command and Control|Phishing\")` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` count\n  from datamodel=Network_Resolution.DNS\n  where DNS.action=\"blocked\"\n  by DNS.query DNS.src span=1h\n| sort -count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**DNS Security Events** — Blocked DNS queries to malicious domains indicate infection attempts or active compromise. Each block is a security win.\n\nDocumented **Data sources**: Umbrella/DNS security logs. **App/TA** (typical add-on context): Splunk Add-on for Cisco Umbrella. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Network_Traffic.All_Traffic` — enable acceleration for that model.\n• `eval` defines or adjusts **bytes** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare with the Cisco Umbrella Investigate or reporting UI for the same time range; then confirm Splunk field extractions and access.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (blocked domains with sources), Bar chart (blocks by category), Single value (unique blocked domains today).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for Cisco Umbrella — Splunk Add-on for Cisco Security (Splunkbase 1352)). Enable the modular input or HEC token that writes sourcetype=cisco:umbrella:dns into dns_security. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`categories, domain, internalIp`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We review blocked and risky DNS security events so malware, phishing, and C2 do not get past the resolver unlogged.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
