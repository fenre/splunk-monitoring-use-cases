{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.13.28",
  "title": "Asset Risk Scoring (CIM)",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Risk"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Asset Risk Scoring (CIM)' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_asset_risk_scoring_cim` (UC-10.13.28), SOC dashboard panel \"Security Infra — Asset Risk Scoring (CIM)\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.13.28 positive (Asset, Risk, Scoring, (CIM)): ingest synthetic events into `index=security` sourcetype='target_sourcetype' where `risk_score`, `cve_count` grouped by `dest` satisfies detection threshold for 'Asset Risk Scoring (CIM)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.13.28 negative (Scoring (CIM)): ingest benign `security` / `target_sourcetype` events where `dest` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 12 confirms zero alert rows for 'Asset Risk Scoring (CIM)'."
  },
  "dataSources": "`dest`, `severity`, `cve` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Vulnerabilities CIM — Universal Forwarder (Splunkbase 617)",
  "spl": "index=security tag=vulnerability status=open\n| eval w=case(severity=\"critical\",10, severity=\"high\",7, severity=\"medium\",4, 1)\n| stats sum(w) as risk_score, dc(cve) as cve_count by dest\n| sort -risk_score\n| head 100",
  "description": "Surfaces UC-10.13.28 — Asset, Risk, Scoring, (CIM) — using `index=security` sourcetype `configured sourcetype`, grouped by `dest`. Outputs `risk_score`, `cve_count` each `the scheduled window`; alert when configured threshold. Primary focus: Scoring (CIM). Purpose-built for 'Asset Risk Scoring (CIM)' in Security Infra workflows.",
  "value": "Tracking `risk_score`, `cve_count` via UC-10.13.28 on UC-10.13.28 search results improves response to 'Asset Risk Scoring (CIM)' and reduces missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Blend with threat intel for exploited-in-wild CVEs (2× weight).",
  "visualization": "Table (top risk assets), Bubble chart.",
  "cimModels": [
    "Vulnerabilities"
  ],
  "cimSpl": "| tstats `summariesonly` dc(Vulnerabilities.cve) as cves from datamodel=Vulnerabilities.Vulnerabilities where Vulnerabilities.status=\"open\" by Vulnerabilities.dest, Vulnerabilities.severity",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Vulnerabilities.Vulnerabilities` dataset after normalizing this feed to the model; otherwise the `cimSpl` tstats search may return no rows.",
  "references": [
    {
      "title": "CIM: Vulnerabilities",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Vulnerabilities",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `security` (UC-10.13.28)** — Planned work for 'Asset Risk Scoring (CIM)' shifts `risk_score`, `cve_count` over `span=1h` while `dest` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `sum` on `configured_sourcetype`** — Scheduled automation elevates `w` without crossing configured threshold for UC-10.13.28. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `dest`** — Health checks in `security` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-10.13.28 on `dest` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `dest`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Vulnerabilities CIM.\n• Ensure the following data sources are available: `dest`, `severity`, `cve`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nBlend with threat intel for exploited-in-wild CVEs (2× weight).\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=security tag=vulnerability status=open\n| eval w=case(severity=\"critical\",10, severity=\"high\",7, severity=\"medium\",4, 1)\n| stats sum(w) as risk_score, dc(cve) as cve_count by dest\n| sort -risk_score\n| head 100\n```\n\nUnderstanding this SPL\n\n**Asset Risk Scoring (CIM)** — Aggregate open vulns per `dest` weighted by CVSS for top-risk hosts.\n\nDocumented **Data sources**: `dest`, `severity`, `cve`. **App/TA** (typical add-on context): Vulnerabilities CIM. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: security.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=security, tags. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **w** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by dest** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n• Limits the number of rows with `head`.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` dc(Vulnerabilities.cve) as cves from datamodel=Vulnerabilities.Vulnerabilities where Vulnerabilities.status=\"open\" by Vulnerabilities.dest, Vulnerabilities.severity\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Asset Risk Scoring (CIM)** — Aggregate open vulns per `dest` weighted by CVSS for top-risk hosts.\n\nDocumented **Data sources**: `dest`, `severity`, `cve`. **App/TA** (typical add-on context): Vulnerabilities CIM. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Vulnerabilities.Vulnerabilities` — enable acceleration for that model.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nAccelerate the CIM data model used in the optional tstats path, then run both the primary and `cimSpl` searches over a week that includes a known good baseline. Spot-check a few rows and tune thresholds; confirm no permission or tagging gap is hiding part of the population.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (top risk assets), Bubble chart.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Vulnerabilities CIM — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into security. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`dest, w`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use the same kind of check described for \"Asset Risk Scoring\" once our data is shaped in a common, repeatable way across products. We help the team see trouble early, compare different tools fairly, and avoid building a one-off report for every vendor.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
