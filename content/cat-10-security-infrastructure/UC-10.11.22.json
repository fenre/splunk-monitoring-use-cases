{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.11.22",
  "title": "Palo Alto Cortex XDR Incident Trending",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Palo Alto Cortex XDR Incident Trending' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_palo_alto_cortex_xdr_incident_trending` (UC-10.11.22), SOC dashboard panel \"Security Infra — Palo Alto Cortex XDR Incident Trending\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=endpoint` sourcetype='cortex:xdr_incident' where `severity` satisfies `count` > 0 for UC-10.11.22 ('Palo Alto Cortex XDR Incident Trending'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `endpoint` / `cortex:xdr_incident` events where `severity` stays below `count` > 0, or exclude the test entity via `soc_exception_register.csv` with a valid window. Confirm zero alert rows for UC-10.11.22."
  },
  "dataSources": "`sourcetype=cortex:xdr_incident` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_paloalto` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=endpoint sourcetype=\"cortex:xdr_incident\"\n| timechart span=1h count by severity\n| where count>0",
  "description": "XDR incident trending measures detection pipeline load and emerging attack types. Searches `index=endpoint` with sourcetype `cortex:xdr_incident` for UC-10.11.22, aggregating `count` by `severity` over `1h`. Alerts when grouped results cross `count` > 0 in the saved search. Gives Security Infra operators a repeatable signal for 'Palo Alto Cortex XDR Incident Trending' without manual log review.",
  "value": "Early 'Palo Alto Cortex XDR Incident Trending' detection reduces missed threat campaigns, control regressions, and SOC SLA breaches by giving Security Infra operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Ingest Cortex XDR incidents via API or SIEM export. Normalize severity and MITRE tactics. Trend open versus closed backlog.",
  "visualization": "Line chart (incidents), Stacked bar (severity).",
  "cimModels": [
    "Alerts"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Alerts.Alerts by Alerts.severity span=1h | sort - count",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Alerts.Alerts` dataset after normalizing this feed to the model; otherwise the `cimSpl` tstats search may return no rows.",
  "references": [
    {
      "title": "Splunk_TA_paloalto",
      "url": "https://splunkbase.splunk.com/app/2757",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Alerts",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Alerts",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `endpoint`** — Planned work for 'Palo Alto Cortex XDR Incident Trending' shifts `count` over `span=1h` while `severity` remains in-scope for UC-10.11.22. Cross-check CMDB CHG.\n\n2. **Batch `count` on `cortex:xdr_incident`** — Scheduled automation elevates `src` without crossing `count` > 0. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `severity`** — Health checks in `endpoint` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `cortex:xdr_incident`** — First 48h baseline for `severity` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `severity`, `exception_reason`, `valid_until`.",
  "detectionType": "TTP",
  "securityDomain": "endpoint",
  "equipment": [
    "paloalto"
  ],
  "equipmentModels": [
    "paloalto_pan_firewall"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_paloalto`.\n• Ensure the following data sources are available: `sourcetype=cortex:xdr_incident`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest Cortex XDR incidents via API or SIEM export. Normalize severity and MITRE tactics. Trend open versus closed backlog.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=endpoint sourcetype=\"cortex:xdr_incident\"\n| timechart span=1h count by severity\n| where count>0\n```\n\nUnderstanding this SPL\n\n**Palo Alto Cortex XDR Incident Trending** — XDR incident trending measures detection pipeline load and emerging attack types.\n\nDocumented **Data sources**: `sourcetype=cortex:xdr_incident`. **App/TA** (typical add-on context): `Splunk_TA_paloalto`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: endpoint; **sourcetype**: cortex:xdr_incident. That sourcetype matches what this use case lists under Data sources.\n\n**Detection type** for this use case: TTP — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=endpoint, sourcetype=\"cortex:xdr_incident\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `timechart` plots the metric over time using **span=1h** buckets with a separate series **by severity** — ideal for trending and alerting on this use case.\n• Filters the current rows with `where count>0` — typically the threshold or rule expression for this monitoring goal.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats summariesonly=t count from datamodel=Alerts.Alerts by Alerts.severity span=1h | sort - count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Palo Alto Cortex XDR Incident Trending** — XDR incident trending measures detection pipeline load and emerging attack types.\n\nDocumented **Data sources**: `sourcetype=cortex:xdr_incident`. **App/TA** (typical add-on context): `Splunk_TA_paloalto`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Detection type** for this use case: TTP — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Alerts.Alerts` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nRun the search over a window that includes both normal production traffic and, if you have one, a documented test (vendor sandbox, lab rule hit, or exercise). Compare a few sample events to the vendor’s own console or reports and tune indexes, fields, and thresholds so alerts are actionable before paging.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (incidents), Stacked bar (severity).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_paloalto` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=cortex:xdr_incident into endpoint. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`severity`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use Cortex XDR data and alerts to stay ahead of the risks described in \"Palo Alto Cortex XDR Incident Trending\". We help the team notice trouble early, while a situation is still small and containable.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
