{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.14.11",
  "title": "Safety Instrumented System (SIS) Bypass",
  "criticality": "critical",
  "difficulty": "expert",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Safety Instrumented System (SIS) Bypass' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_safety_instrumented_system_sis_bypass` (UC-10.14.11), SOC dashboard panel \"Security Infra — Safety Instrumented System (SIS) Bypass\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.14.11 positive (Safety, Instrumented, System, (SIS), Bypass): ingest synthetic events into `index=ot` sourcetype='ics:sis' where `values` grouped by `loop_id`, `asset`, `user` satisfies detection threshold for 'Safety Instrumented System (SIS) Bypass'. Validate lookup `ot_sis_authorized_changes`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.14.11 negative ((SIS) Bypass): ingest benign `ot` / `ics:sis` events where `loop_id`, `asset`, `user` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 9 confirms zero alert rows for 'Safety Instrumented System (SIS) Bypass'."
  },
  "dataSources": "`index=ot` `sourcetype=\"ics:sis\"` safety PLC diagnostics, alarm historians Indexes: ot. Sourcetypes: ics:sis. Key fields: asset, loop_id, permit_ok, user. Validate field extraction in Search before saving the alert; compare event counts to the vendor admin console.",
  "app": "OT Security Add-on for Splunk (Splunkbase 5151)",
  "spl": "index=ot sourcetype=\"ics:sis\"\n| where match(status_message, \"(?i)bypass|override|maintenance|manual|inhibit\")\n| lookup ot_sis_authorized_changes loop_id user OUTPUT permit_id valid_until\n| eval permit_ok=if(!isnull(permit_id) AND _time<=valid_until, 1, 0)\n| where permit_ok=0\n| stats earliest(_time) as first_seen values(status_message) as states by loop_id, asset, user",
  "description": "Surfaces UC-10.14.11 — Safety, Instrumented, System, (SIS), Bypass — using `index=ot` sourcetype `ics:sis`, grouped by `loop_id`, `asset`, `user`. Outputs `values` each `the scheduled window`; alert when configured threshold. Filter chain includes lookup `ot_sis_authorized_changes`. Purpose-built for 'Safety Instrumented System (SIS) Bypass' in Security Infra workflows.",
  "value": "Operators running UC-10.14.11 on `ics:sis` feeds shorten triage for (SIS) Bypass tied to 'Safety Instrumented System (SIS) Bypass', lowering missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Use Splunk ES and OT Safety data integrations. Require MOC/permit correlation. Escalate immediately to process safety and T0800 workflows.",
  "visualization": "State timeline per SIS loop, Alert list with permit linkage, Gauge of open bypasses.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "OT Security Add-on for Splunk",
      "url": "https://splunkbase.splunk.com/app/5151",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `ot` (UC-10.14.11)** — Planned work for 'Safety Instrumented System (SIS) Bypass' shifts `values` over `span=1h` while `loop_id`, `asset`, `user` remains in-scope. Applies lookup `ot_sis_authorized_changes`. Cross-check CMDB CHG.\n\n2. **Batch `values` on `ics:sis`** — Scheduled automation elevates `asset` without crossing configured threshold for UC-10.14.11. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `loop_id`, `asset`, `user`** — Health checks in `ot` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `ics:sis`** — First 48h baseline for UC-10.14.11 on `loop_id` is unstable; join `ot_sis_authorized_changes` prior to threshold.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `loop_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk OT Security Add-on"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: OT Security Add-on for Splunk (Splunkbase 5151).\n• Ensure the following data sources are available: `index=ot` `sourcetype=\"ics:sis\"` safety PLC diagnostics, alarm historians.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nUse Splunk ES and OT Safety data integrations. Require MOC/permit correlation. Escalate immediately to process safety and T0800 workflows.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot sourcetype=\"ics:sis\"\n| where match(status_message, \"(?i)bypass|override|maintenance|manual|inhibit\")\n| lookup ot_sis_authorized_changes loop_id user OUTPUT permit_id valid_until\n| eval permit_ok=if(!isnull(permit_id) AND _time<=valid_until, 1, 0)\n| where permit_ok=0\n| stats earliest(_time) as first_seen values(status_message) as states by loop_id, asset, user\n```\n\nUnderstanding this SPL\n\n**Safety Instrumented System (SIS) Bypass** — Monitors mode changes, jumpering, or overrides on SIS (T0800), directly tied to process safety and regulatory expectations.\n\nDocumented **Data sources**: `index=ot` `sourcetype=\"ics:sis\"` safety PLC diagnostics, alarm historians. **App/TA** (typical add-on context): OT Security Add-on for Splunk (Splunkbase 5151). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot; **sourcetype**: ics:sis. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot, sourcetype=\"ics:sis\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where match(status_message, \"(?i)bypass|override|maintenance|manual|inhibit\")` — typically the threshold or rule expression for this monitoring goal.\n• Enriches events using `lookup` (lookup definition + optional OUTPUT fields).\n• `eval` defines or adjusts **permit_ok** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where permit_ok=0` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by loop_id, asset, user** so each row reflects one combination of those dimensions.\n\n\nStep 3 — Validate\nCompare failed or disabled searches in Splunk to the **OT Security Add-on** configuration UI and the **Saved Searches** list so names and schedules line up. Spot-check one failure against `scheduler.log` around the run time.\n\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: State timeline per SIS loop, Alert list with permit linkage, Gauge of open bypasses.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (OT Security Add-on for Splunk (Splunkbase 5151)). Enable the modular input or HEC token that writes sourcetype=ics:sis into ot. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`asset, loop_id, permit_ok, user`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for safety or interlock systems being sidestepped in ways that only happen when someone tries to work around the guardrails.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
