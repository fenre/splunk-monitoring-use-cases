{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.8.2",
  "title": "Certificate Issuance Audit",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Certificate Issuance Audit' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_certificate_issuance_audit` (UC-10.8.2), SOC dashboard panel \"Security Infra — Certificate Issuance Audit\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=pki` sourcetype='adcs:audit' EventCode=4887 where `entity` satisfies detection threshold for UC-10.8.2 ('Certificate Issuance Audit'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `pki` / `adcs:audit` events where `entity` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv` with a valid window. Confirm zero alert rows for UC-10.8.2."
  },
  "dataSources": "CA audit logs (Microsoft AD CS, EJBCA, HashiCorp Vault PKI) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "CA server log forwarding — Universal Forwarder (Splunkbase 617)",
  "spl": "index=pki sourcetype=\"adcs:audit\"\n| search EventCode=4887\n| table _time, RequesterName, CertificateTemplate, SerialNumber, SubjectCN\n| sort -_time",
  "description": "Unauthorized certificate issuance from internal CAs can enable man-in-the-middle attacks. Audit trail supports compliance.",
  "value": "Early 'Certificate Issuance Audit' detection reduces missed threat campaigns, control regressions, and SOC SLA breaches by giving Security Infra operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Forward CA server audit logs to Splunk (Event ID 4887 for AD CS). Track all certificate issuance events. Alert on issuance from non-standard templates or by unauthorized requesters. Report on issuance volume and template usage.",
  "visualization": "Table (issued certificates), Timeline (issuance events), Bar chart (by template), Line chart (issuance volume trend).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `pki`** — Planned work for 'Certificate Issuance Audit' shifts `count` over `span=1h` while `host` remains in-scope for UC-10.8.2. Cross-check CMDB CHG.\n\n2. **Batch `count` on `adcs:audit` (EventCode=4887)** — Scheduled automation elevates `src` without crossing configured threshold. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `pki` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `adcs:audit`** — First 48h baseline for `host` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cert_pki",
    "hashicorp"
  ],
  "equipmentModels": [
    "cert_pki_adcs",
    "cert_pki_ejbca",
    "cert_pki_vault_pki",
    "hashicorp_vault"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: CA server log forwarding.\n• Ensure the following data sources are available: CA audit logs (Microsoft AD CS, EJBCA, HashiCorp Vault PKI).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nForward CA server audit logs to Splunk (Event ID 4887 for AD CS). Track all certificate issuance events. Alert on issuance from non-standard templates or by unauthorized requesters. Report on issuance volume and template usage.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=pki sourcetype=\"adcs:audit\"\n| search EventCode=4887\n| table _time, RequesterName, CertificateTemplate, SerialNumber, SubjectCN\n| sort -_time\n```\n\nUnderstanding this SPL\n\n**Certificate Issuance Audit** — Unauthorized certificate issuance from internal CAs can enable man-in-the-middle attacks. Audit trail supports compliance.\n\nDocumented **Data sources**: CA audit logs (Microsoft AD CS, EJBCA, HashiCorp Vault PKI). **App/TA** (typical add-on context): CA server log forwarding. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: pki; **sourcetype**: adcs:audit. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=pki, sourcetype=\"adcs:audit\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Applies an explicit `search` filter to narrow the current result set.\n• Pipeline stage (see **Certificate Issuance Audit**): table _time, RequesterName, CertificateTemplate, SerialNumber, SubjectCN\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nCompare with your CA’s audit or issuance log (e.g. AD CS, EJBCA, or Vault) for the same requester, template, and serial, then confirm field extractions in Splunk.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (issued certificates), Timeline (issuance events), Bar chart (by template), Line chart (issuance volume trend).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (CA server log forwarding — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=adcs:audit into pki. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, user, action`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We review who requested which certificates from our internal CAs so surprise or stolen issuance does not go unnoticed in Splunk or at audit time.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
