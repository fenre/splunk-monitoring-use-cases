{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.12.33",
  "title": "E-Commerce Bot Detection",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "industry": "Retail and E-Commerce",
  "exclusions": "Covers the detection logic documented for 'E-Commerce Bot Detection' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_e_commerce_bot_detection` (UC-10.12.33), SOC dashboard panel \"Security Infra — E-Commerce Bot Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.12.33 positive (Commerce, Bot, Detection): ingest synthetic events into `index=web` sourcetype='nginx:access' where `sess`, `avg_rt` grouped by `src` satisfies `count` > 200, `sess` < 5, or `avg_rt` < 50 for 'E-Commerce Bot Detection'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.12.33 negative (Bot Detection): ingest benign `web` / `nginx:access` events where `src` stays below `count` > 200, `sess` < 5, or `avg_rt` < 50, or exclude the test entity via `soc_exception_register.csv`. Variant 13 confirms zero alert rows for 'E-Commerce Bot Detection'."
  },
  "dataSources": "`sourcetype=\"nginx:access\"`, `sourcetype=\"akamai:siem\"` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "CDN/WAF, application logs — Universal Forwarder (Splunkbase 617)",
  "spl": "index=web sourcetype=\"nginx:access\" uri=\"/checkout*\"\n| eval bot_score=if(match(http_user_agent,\"(?i)(bot|crawler|python)\"),1,0)\n| stats count, dc(session_id) as sess, avg(request_time_ms) as avg_rt by src\n| where count>200 AND sess<5 AND avg_rt<50\n| sort -count",
  "description": "Alerts on UC-10.12.33 — Commerce, Bot, Detection — using `index=web` sourcetype `nginx:access`, grouped by `src`. Outputs `sess`, `avg_rt` each `the scheduled window`; alert when `count` > 200, `sess` < 5, or `avg_rt` < 50. Primary focus: Bot Detection. Purpose-built for 'E-Commerce Bot Detection' in Security Infra workflows.",
  "value": "UC-10.12.33 gives Security Infra teams earlier visibility into Bot Detection through `nginx:access` feeds, lowering missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Layer bot management (JS challenge, CAPTCHA); rate limit by ASN and fingerprint.",
  "visualization": "Table (suspicious IPs), Line chart (bot traffic share), Map (ASN concentration).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable for the primary search: it relies on application- and industry-specific log formats, not a normalized CIM data model. Optional mapping to CIM, if you adopt it, is a separate follow-on project.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `web` (UC-10.12.33)** — Planned work for 'E-Commerce Bot Detection' shifts `sess`, `avg_rt` over `span=1h` while `src` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `dc` on `nginx:access`** — Scheduled automation elevates `bot_score` without crossing `count` > 200 for UC-10.12.33. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `src`** — Health checks in `web` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `nginx:access`** — First 48h baseline for UC-10.12.33 on `src` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `src`, `exception_reason`, `valid_until`.",
  "equipment": [
    "nginx"
  ],
  "equipmentModels": [
    "nginx_open"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: CDN/WAF, application logs.\n• Ensure the following data sources are available: `sourcetype=\"nginx:access\"`, `sourcetype=\"akamai:siem\"`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nLayer bot management (JS challenge, CAPTCHA); rate limit by ASN and fingerprint.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=web sourcetype=\"nginx:access\" uri=\"/checkout*\"\n| eval bot_score=if(match(http_user_agent,\"(?i)(bot|crawler|python)\"),1,0)\n| stats count, dc(session_id) as sess, avg(request_time_ms) as avg_rt by src\n| where count>200 AND sess<5 AND avg_rt<50\n| sort -count\n```\n\nUnderstanding this SPL\n\n**E-Commerce Bot Detection** — Protects inventory, loyalty points, and checkout from scalping and credential stuffing.\n\nDocumented **Data sources**: `sourcetype=\"nginx:access\"`, `sourcetype=\"akamai:siem\"`. **App/TA** (typical add-on context): CDN/WAF, application logs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: web; **sourcetype**: nginx:access. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=web, sourcetype=\"nginx:access\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **bot_score** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by src** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where count>200 AND sess<5 AND avg_rt<50` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nRun the search on recent production data, spot-check a handful of results against the source system or operational runbooks (cash operations, EHR, payment gateway, etc.), and adjust thresholds to match your seasonal patterns and test windows so teams are not flooded with noise.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (suspicious IPs), Line chart (bot traffic share), Map (ASN concentration).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (CDN/WAF, application logs — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=nginx:access into web. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`avg_rt, bot_score, sess, src`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "In Retail and E-Commerce, we pay close attention to \"E-Commerce Bot Detection\" in our day-to-day work so that fraud, waste, and compliance issues do not slip by quietly. We help teams focus on the few situations that need a person, not every routine blip in the data.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
