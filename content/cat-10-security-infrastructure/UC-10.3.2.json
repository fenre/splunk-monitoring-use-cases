{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.3.2",
  "title": "Quarantine Action Monitoring",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'Quarantine Action Monitoring'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-10.3.2).",
  "evidence": "Saved search uc_10_3_2_quarantine_action_monitoring, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'Quarantine Action Monitoring'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-10.3.2.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "EDR remediation/action logs Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "EDR TA — Universal Forwarder (Splunkbase 617)",
  "spl": "index=edr sourcetype=\"crowdstrike:detection\"\n| stats count(eval(action=\"quarantined\")) as quarantined, count(eval(action=\"allowed\")) as allowed by severity\n| eval quarantine_rate=round(quarantined/(quarantined+allowed)*100,1)",
  "description": "Failed quarantine means malware remains active on the endpoint. Monitoring ensures automated remediation is working.",
  "value": "Operations and leadership rely on 'Quarantine Action Monitoring' to catch regressions before they become customer-visible outages or audit findings. Early signal reduces mean time to repair, avoids duplicate manual log review, and preserves evidence for post-incident review without waiting for a user-reported ticket.",
  "implementation": "Track EDR remediation actions (quarantine, kill process, isolate). Calculate quarantine success rate. Alert on failed quarantine actions. Follow up on \"allowed\" malware detections to ensure analyst review.",
  "visualization": "Pie chart (action distribution), Single value (quarantine success %), Table (failed quarantine events).",
  "cimModels": [
    "Intrusion_Detection",
    "Alerts"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Alerts.Alerts\n  by Alerts.severity Alerts.signature span=1h\n| where count>0\n| sort -count",
  "dataModelAcceleration": "Enable acceleration on the Alerts data model, dataset Alerts, before using this tstats search.",
  "references": [
    {
      "title": "CIM: Alerts",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Alerts",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "crowdstrike"
  ],
  "equipmentModels": [
    "crowdstrike_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: EDR TA.\n• Ensure the following data sources are available: EDR remediation/action logs.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nTrack EDR remediation actions (quarantine, kill process, isolate). Calculate quarantine success rate. Alert on failed quarantine actions. Follow up on \"allowed\" malware detections to ensure analyst review.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=edr sourcetype=\"crowdstrike:detection\"\n| stats count(eval(action=\"quarantined\")) as quarantined, count(eval(action=\"allowed\")) as allowed by severity\n| eval quarantine_rate=round(quarantined/(quarantined+allowed)*100,1)\n```\n\nUnderstanding this SPL\n\n**Quarantine Action Monitoring** — Failed quarantine means malware remains active on the endpoint. Monitoring ensures automated remediation is working.\n\nDocumented **Data sources**: EDR remediation/action logs. **App/TA** (typical add-on context): EDR TA. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: edr; **sourcetype**: crowdstrike:detection. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=edr, sourcetype=\"crowdstrike:detection\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by severity** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **quarantine_rate** — often to normalize units, derive a ratio, or prepare for thresholds.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` count\n  from datamodel=Alerts.Alerts\n  by Alerts.severity Alerts.signature span=1h\n| where count>0\n| sort -count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Quarantine Action Monitoring** — Failed quarantine means malware remains active on the endpoint. Monitoring ensures automated remediation is working.\n\nDocumented **Data sources**: EDR remediation/action logs. **App/TA** (typical add-on context): EDR TA. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Alerts.Alerts` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable acceleration on the Alerts data model, dataset Alerts, before using this tstats search.\n\n\nStep 3 — Validate\nCompare a sample in the EDR console (CrowdStrike Falcon, Microsoft Defender for Endpoint portal, Carbon Black, or SentinelOne), the Sysmon Operational log in Event Viewer, or `Get-MpThreatDetection` on Windows, as applicable.\n\nValidation (cross-check)\n\nCompare the same time range in your EDR console and confirm host, user, and technique line up. For Sysmon, open the same Event ID in Sysmon’s Operational log. For Microsoft Defender, run `Get-MpThreatDetection` in elevated PowerShell and match threat and time.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Pie chart (action distribution), Single value (quarantine success %), Table (failed quarantine events).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (EDR TA — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=crowdstrike:detection into edr. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`severity`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch whether quarantine and block steps actually run so malware is not still running because a fix action failed quietly in the background.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
