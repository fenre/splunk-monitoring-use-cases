{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.8.5",
  "title": "CT Log Monitoring",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'CT Log Monitoring' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_ct_log_monitoring` (UC-10.8.5), SOC dashboard panel \"Security Infra — CT Log Monitoring\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.8.5 positive (Log, Monitoring): ingest synthetic events into `index=certificates` sourcetype='ct_log' where `count` grouped by `entity` satisfies detection threshold for 'CT Log Monitoring'. Validate filter `NOT issuer IN (\"DigiCert*\",\"Let's Encrypt*\",\"Sectigo*\")`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.8.5 negative (Log Monitoring): ingest benign `certificates` / `ct_log` events where `entity` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 15 confirms zero alert rows for 'CT Log Monitoring'."
  },
  "dataSources": "Certificate Transparency log API Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom API input (crt.sh, CT log APIs) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=certificates sourcetype=\"ct_log\"\n| search NOT issuer IN (\"DigiCert*\",\"Let's Encrypt*\",\"Sectigo*\")\n| table _time, cn, issuer, serial, not_before, not_after\n| sort -_time",
  "description": "Alerts on UC-10.8.5 — Log, Monitoring — using `index=certificates` sourcetype `ct_log`, grouped by `entity`. Outputs `count` each `the scheduled window`; alert when configured threshold. Filter chain includes filter `NOT issuer IN (\"DigiCert*\",\"Let's Encrypt*\",\"Sectigo*\")`. Purpose-built for 'CT Log Monitoring' in Security Infra workflows.",
  "value": "UC-10.8.5 operationalises 'CT Log Monitoring' against `ct_log` feeds so operators reduce missed threat campaigns, control regressions, and SOC SLA breaches without manual correlation.",
  "implementation": "Poll CT log aggregators (crt.sh) for your domains daily. Maintain whitelist of approved issuers. Alert on certificates from unexpected CAs. Track issuance patterns for certificate lifecycle management.",
  "visualization": "Table (CT log entries), Timeline (issuance events), Bar chart (certs by issuer), Single value (unauthorized issuances).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `certificates` (UC-10.8.5)** — Planned work for 'CT Log Monitoring' shifts `count` over `span=1h` while `host` remains in-scope. Applies filter `NOT issuer IN (\"DigiCert*\",\"Let's Encrypt*\",\"Sectigo*\")`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `ct_log`** — Scheduled automation elevates `src` without crossing configured threshold for UC-10.8.5. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `certificates` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `ct_log`** — First 48h baseline for UC-10.8.5 on `host` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cert_pki"
  ],
  "equipmentModels": [
    "cert_pki_ct_logs",
    "cert_pki_letsencrypt",
    "cert_pki_sectigo"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Custom API input (crt.sh, CT log APIs).\n• Ensure the following data sources are available: Certificate Transparency log API.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nPoll CT log aggregators (crt.sh) for your domains daily. Maintain whitelist of approved issuers. Alert on certificates from unexpected CAs. Track issuance patterns for certificate lifecycle management.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=certificates sourcetype=\"ct_log\"\n| search NOT issuer IN (\"DigiCert*\",\"Let's Encrypt*\",\"Sectigo*\")\n| table _time, cn, issuer, serial, not_before, not_after\n| sort -_time\n```\n\nUnderstanding this SPL\n\n**CT Log Monitoring** — Certificate Transparency logs reveal all publicly-issued certificates for your domains. Detects unauthorized issuance by rogue or compromised CAs.\n\nDocumented **Data sources**: Certificate Transparency log API. **App/TA** (typical add-on context): Custom API input (crt.sh, CT log APIs). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: certificates; **sourcetype**: ct_log. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=certificates, sourcetype=\"ct_log\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Applies an explicit `search` filter to narrow the current result set.\n• Pipeline stage (see **CT Log Monitoring**): table _time, cn, issuer, serial, not_before, not_after\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nCompare with your CT log source or a public log viewer (e.g. crt.sh) for the same certificate or issuer, and confirm parsing in Splunk.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (CT log entries), Timeline (issuance events), Bar chart (certs by issuer), Single value (unauthorized issuances).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Custom API input (crt.sh, CT log APIs) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=ct_log into certificates. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, user, action`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch certificate transparency and issuance patterns so unexpected or fraudulent certificates are noticed early.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
