{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.4.3",
  "title": "URL Click Tracking",
  "criticality": "critical",
  "difficulty": "beginner",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'URL Click Tracking' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_url_click_tracking` (UC-10.4.3), SOC dashboard panel \"Security Infra — URL Click Tracking\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=email` sourcetype='o365:management:activity' that satisfy the UC-10.4.3 SPL filters for 'URL Click Tracking'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `email` / `o365:management:activity` events outside the detection threshold, or exclude the test entity via `soc_exception_register.csv` with a valid window. Confirm zero alert rows for UC-10.4.3."
  },
  "dataSources": "URL rewrite/protection logs, click tracking events Collected via Splunk Add-on for Microsoft Office 365 (Splunkbase 4054) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk_TA_MS_O365 (Safe Links), Proofpoint URL Defense — Splunk Add-on for Microsoft Office 365 (Splunkbase 4054)",
  "spl": "index=email (sourcetype=\"o365:management:activity\" Workload=Dlp) OR sourcetype=\"proofpoint:click\"\n| search verdict=\"malicious\" AND action=\"allowed\"\n| table _time, userPrincipalName, url, verdict, action",
  "description": "Tracks which users click malicious URLs in emails — the moment a phishing email becomes an active incident.",
  "value": "Early 'URL Click Tracking' detection reduces missed threat campaigns, control regressions, and SOC SLA breaches by giving Security Infra operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Enable Safe Links (M365) or URL Defense (Proofpoint). Ingest click tracking data. Alert immediately when a user clicks a malicious URL. Trigger automated password reset and endpoint scan. Track click-through rates for security awareness metrics.",
  "visualization": "Table (malicious URL clicks), Bar chart (clicks by user), Timeline (click events), Single value (clicks on malicious URLs today).",
  "cimModels": [
    "Email"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Email.All_Email\n  where All_Email.action=\"clicked\"\n  by All_Email.recipient All_Email.url span=1h\n| sort -count",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Email.All_Email` dataset once message and attachment fields are CIM-tagged; otherwise `cimSpl` tstats may return no rows.",
  "references": [
    {
      "title": "CIM: Email",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Email",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **SOC tuning or purple-team exercise** — Expected alert volume during rule validation for 'URL Click Tracking'. Match active CHG with `purple_team=true`.\n\n2. **Content deployment window** — TA or CIM update shifts field extractions on `index=email` `o365:management:activity`.\n\n3. **Duplicate log ingestion** — HF/IDX misconfiguration doubles event counts.\n\n4. **Baseline seasonality** — Month-end batch jobs skew trending searches.\n\n**Suppression mechanism:** `soc_exception_register.csv` with entity, reason, valid_until.",
  "equipment": [
    "m365",
    "proofpoint"
  ],
  "equipmentModels": [
    "m365_generic",
    "proofpoint_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk_TA_MS_O365 (Safe Links), Proofpoint URL Defense.\n• Ensure the following data sources are available: URL rewrite/protection logs, click tracking events.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnable Safe Links (M365) or URL Defense (Proofpoint). Ingest click tracking data. Alert immediately when a user clicks a malicious URL. Trigger automated password reset and endpoint scan. Track click-through rates for security awareness metrics.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=email sourcetype=\"ms:o365:dlp\" OR sourcetype=\"proofpoint:click\"\n| search verdict=\"malicious\" AND action=\"allowed\"\n| table _time, userPrincipalName, url, verdict, action\n```\n\nUnderstanding this SPL\n\n**URL Click Tracking** — Tracks which users click malicious URLs in emails — the moment a phishing email becomes an active incident.\n\nDocumented **Data sources**: URL rewrite/protection logs, click tracking events. **App/TA** (typical add-on context): Splunk_TA_MS_O365 (Safe Links), Proofpoint URL Defense. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: email; **sourcetype**: ms:o365:dlp, proofpoint:click. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=email, sourcetype=\"ms:o365:dlp\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Applies an explicit `search` filter to narrow the current result set.\n• Pipeline stage (see **URL Click Tracking**): table _time, userPrincipalName, url, verdict, action\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` count\n  from datamodel=Email.All_Email\n  where All_Email.action=\"clicked\"\n  by All_Email.recipient All_Email.url span=1h\n| sort -count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**URL Click Tracking** — Tracks which users click malicious URLs in emails — the moment a phishing email becomes an active incident.\n\nDocumented **Data sources**: URL rewrite/protection logs, click tracking events. **App/TA** (typical add-on context): Splunk_TA_MS_O365 (Safe Links), Proofpoint URL Defense. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Email.All_Email` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare search results in Splunk with the Microsoft 365 Defender portal (https://security.microsoft.com) and, where applicable, the Proofpoint TAP dashboard, Mimecast Personal Portal, or Cisco ESA `mail_logs`. Confirm events are present, fields parse correctly, and sample ratios match the vendor console for the same time range.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (malicious URL clicks), Bar chart (clicks by user), Timeline (click events), Single value (clicks on malicious URLs today).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk_TA_MS_O365 (Safe Links), Proofpoint URL Defense — Splunk Add-on for Microsoft Office 365 (Splunkbase 4054)). Enable the modular input or HEC token that writes sourcetype=o365:management:activity, proofpoint:click into email. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, user, action`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch when people click links that were scanned or rewritten in email, so we can respond if someone reaches a harmful page even when the message first looked acceptable.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
