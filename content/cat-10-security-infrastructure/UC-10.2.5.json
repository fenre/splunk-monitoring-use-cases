{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.2.5",
  "title": "Lateral Movement Detection",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Lateral Movement Detection' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_lateral_movement_detection` (UC-10.2.5), SOC dashboard panel \"Security Infra — Lateral Movement Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.2.5 positive (Lateral, Movement, Detection): ingest synthetic events into `index=ids` sourcetype='snort:alert' where `count` grouped by `src`, `dest`, `signature` satisfies detection threshold for 'Lateral Movement Detection'. Validate filter `category IN (\"attempted-admin\",\"trojan-activity\",\"policy-vio`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.2.5 negative (Movement Detection): ingest benign `ids` / `snort:alert` events where `src`, `dest`, `signature` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 9 confirms zero alert rows for 'Lateral Movement Detection'."
  },
  "dataSources": "IDS alerts from internal/east-west sensors Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "IDS TA (internal sensors) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=ids sourcetype=\"snort:alert\" sensor_zone=\"internal\"\n| search category IN (\"attempted-admin\",\"trojan-activity\",\"policy-violation\",\"misc-attack\")\n| stats count by src, dest, signature\n| sort -count",
  "description": "Monitors UC-10.2.5 — Lateral, Movement, Detection — using `index=ids` sourcetype `snort:alert`, grouped by `src`, `dest`, `signature`. Outputs `count` each `the scheduled window`; alert when configured threshold. Filter chain includes filter `category IN (\"attempted-admin\",\"trojan-activity\",\"policy-vio`. Purpose-built for 'Lateral Movement Detection' in Security Infra workflows.",
  "value": "Operators running UC-10.2.5 on `snort:alert` feeds shorten triage for Movement Detection tied to 'Lateral Movement Detection', lowering missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Deploy IDS sensors on internal network segments (not just perimeter). Forward alerts to Splunk. Alert on any high-severity internal detections. Correlate with AD authentication events and endpoint data for full attack chain visibility.",
  "visualization": "Network diagram (lateral movement paths), Table (internal IDS alerts), Timeline (attack progression).",
  "cimModels": [
    "Intrusion_Detection"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Intrusion_Detection.IDS_Attacks\n  by IDS_Attacks.src IDS_Attacks.dest IDS_Attacks.signature span=1h\n| sort -count",
  "dataModelAcceleration": "Enable Intrusion_Detection.IDS_Attacks acceleration; if `src_category` or internal-zone filters are not populated in your environment, add CIM tags or `where` clauses that match your east-west address ranges instead.",
  "references": [
    {
      "title": "CIM: Intrusion_Detection",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Intrusion_Detection",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `ids` (UC-10.2.5)** — Planned work for 'Lateral Movement Detection' shifts `count` over `span=1h` while `src`, `dest`, `signature` remains in-scope. Applies filter `category IN (\"attempted-admin\",\"trojan-activity\",\"policy-vio`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `snort:alert`** — Scheduled automation elevates `dest` without crossing configured threshold for UC-10.2.5. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `src`, `dest`, `signature`** — Health checks in `ids` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `snort:alert`** — First 48h baseline for UC-10.2.5 on `src` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `src`, `exception_reason`, `valid_until`.",
  "equipment": [
    "suricata"
  ],
  "equipmentModels": [
    "suricata_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: IDS TA (internal sensors).\n• Ensure the following data sources are available: IDS alerts from internal/east-west sensors.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nDeploy IDS sensors on internal network segments (not just perimeter). Forward alerts to Splunk. Alert on any high-severity internal detections. Correlate with AD authentication events and endpoint data for full attack chain visibility.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ids sourcetype=\"snort:alert\" sensor_zone=\"internal\"\n| search category IN (\"attempted-admin\",\"trojan-activity\",\"policy-violation\",\"misc-attack\")\n| stats count by src, dest, signature\n| sort -count\n```\n\nUnderstanding this SPL\n\n**Lateral Movement Detection** — IDS detections on internal network segments indicate an attacker has breached the perimeter and is moving laterally.\n\nDocumented **Data sources**: IDS alerts from internal/east-west sensors. **App/TA** (typical add-on context): IDS TA (internal sensors). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ids; **sourcetype**: snort:alert. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ids, sourcetype=\"snort:alert\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Applies an explicit `search` filter to narrow the current result set.\n• `stats` rolls up events into metrics; results are split **by src, dest, signature** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` count\n  from datamodel=Intrusion_Detection.IDS_Attacks\n  where IDS_Attacks.src_category=\"internal\" OR IDS_Attacks.dest_category=\"internal\"\n  by IDS_Attacks.src IDS_Attacks.dest IDS_Attacks.signature span=1h\n| sort -count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Lateral Movement Detection** — IDS detections on internal network segments indicate an attacker has breached the perimeter and is moving laterally.\n\nDocumented **Data sources**: IDS alerts from internal/east-west sensors. **App/TA** (typical add-on context): IDS TA (internal sensors). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Intrusion_Detection.IDS_Attacks` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare results with raw Snort or Suricata logs on the sensor, with Cisco Firepower Management Center, or with the IDS or IPS management console event view for the same time range. Confirm that severity and counts line up, then verify field extractions, tags, and index permissions in Splunk.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Network diagram (lateral movement paths), Table (internal IDS alerts), Timeline (attack progression).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (IDS TA (internal sensors) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=snort:alert into ids. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`dest, signature, src`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We look for known attack patterns on internal network paths so we can tell when someone is moving around inside the environment after a breach, not just knocking on the front door.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
