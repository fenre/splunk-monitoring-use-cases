{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.3.4",
  "title": "Behavioral Detection Alerts",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Behavioral Detection Alerts' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_behavioral_detection_alerts` (UC-10.3.4), SOC dashboard panel \"Security Infra — Behavioral Detection Alerts\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=edr` sourcetype='crowdstrike:detection' where `technique_id` satisfies detection threshold for UC-10.3.4 ('Behavioral Detection Alerts'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `edr` / `crowdstrike:detection` events where `technique_id` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv` with a valid window. Confirm zero alert rows for UC-10.3.4."
  },
  "dataSources": "EDR behavioral/heuristic alerts Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "EDR TA — Universal Forwarder (Splunkbase 617)",
  "spl": "index=edr sourcetype=\"crowdstrike:detection\" technique_id=\"T*\"\n| stats count by technique_id, tactic, hostname\n| sort -count",
  "description": "Behavioral detections catch attacks that bypass signatures (fileless malware, LOLBins, living-off-the-land). These are high-fidelity signals.",
  "value": "Early 'Behavioral Detection Alerts' detection reduces missed threat campaigns, control regressions, and SOC SLA breaches by giving Security Infra operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Ingest behavioral detection data. Map to MITRE ATT&CK framework (technique_id, tactic). Alert on high-confidence behavioral detections. Track most common techniques for threat intelligence and red team exercises.",
  "visualization": "MITRE ATT&CK heatmap, Table (behavioral detections), Bar chart (top techniques).",
  "cimModels": [
    "Intrusion_Detection",
    "Alerts"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Intrusion_Detection.IDS_Attacks\n  by IDS_Attacks.signature IDS_Attacks.severity IDS_Attacks.dest span=1h\n| where count>0\n| sort -count",
  "dataModelAcceleration": "Enable acceleration on the Intrusion_Detection data model, dataset IDS_Attacks, before using this tstats search.",
  "references": [
    {
      "title": "CIM: Intrusion_Detection",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Intrusion_Detection",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Alerts",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Alerts",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `edr`** — Planned work for 'Behavioral Detection Alerts' shifts `count` over `span=1h` while `technique_id` remains in-scope for UC-10.3.4. Cross-check CMDB CHG.\n\n2. **Batch `count` on `crowdstrike:detection`** — Scheduled automation elevates `tactic` without crossing configured threshold. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `technique_id`** — Health checks in `edr` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `crowdstrike:detection`** — First 48h baseline for `technique_id` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `technique_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "crowdstrike"
  ],
  "equipmentModels": [
    "crowdstrike_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: EDR TA.\n• Ensure the following data sources are available: EDR behavioral/heuristic alerts.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest behavioral detection data. Map to MITRE ATT&CK framework (technique_id, tactic). Alert on high-confidence behavioral detections. Track most common techniques for threat intelligence and red team exercises.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=edr sourcetype=\"crowdstrike:detection\" technique_id=\"T*\"\n| stats count by technique_id, tactic, hostname\n| sort -count\n```\n\nUnderstanding this SPL\n\n**Behavioral Detection Alerts** — Behavioral detections catch attacks that bypass signatures (fileless malware, LOLBins, living-off-the-land). These are high-fidelity signals.\n\nDocumented **Data sources**: EDR behavioral/heuristic alerts. **App/TA** (typical add-on context): EDR TA. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: edr; **sourcetype**: crowdstrike:detection. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=edr, sourcetype=\"crowdstrike:detection\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by technique_id, tactic, hostname** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` count\n  from datamodel=Intrusion_Detection.IDS_Attacks\n  by IDS_Attacks.signature IDS_Attacks.severity IDS_Attacks.dest span=1h\n| where count>0\n| sort -count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Behavioral Detection Alerts** — Behavioral detections catch attacks that bypass signatures (fileless malware, LOLBins, living-off-the-land). These are high-fidelity signals.\n\nDocumented **Data sources**: EDR behavioral/heuristic alerts. **App/TA** (typical add-on context): EDR TA. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Intrusion_Detection.IDS_Attacks` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable acceleration on the Intrusion_Detection data model, dataset IDS_Attacks, before using this tstats search.\n\n\nStep 3 — Validate\nCompare a sample in the EDR console (CrowdStrike Falcon, Microsoft Defender for Endpoint portal, Carbon Black, or SentinelOne), the Sysmon Operational log in Event Viewer, or `Get-MpThreatDetection` on Windows, as applicable.\n\nValidation (cross-check)\n\nCompare the same time range in your EDR console and confirm host, user, and technique line up. For Sysmon, open the same Event ID in Sysmon’s Operational log. For Microsoft Defender, run `Get-MpThreatDetection` in elevated PowerShell and match threat and time.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: MITRE ATT&CK heatmap, Table (behavioral detections), Bar chart (top techniques).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (EDR TA — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=crowdstrike:detection into edr. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`hostname, tactic, technique_id`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We lean on behavior-based rules on the endpoint to catch fileless and living-off-the-land tricks, not only a known-bad file name.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
