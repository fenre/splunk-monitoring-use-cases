{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.13.48",
  "title": "Installed Software Changes",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Installed Software Changes' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_installed_software_changes` (UC-10.13.48), SOC dashboard panel \"Security Infra — Installed Software Changes\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.13.48 positive (Installed, Software, Changes): ingest synthetic events into `index=security` sourcetype='target_sourcetype' where `count` grouped by `host`, `package_name`, `user` satisfies detection threshold for 'Installed Software Changes'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.13.48 negative (Software Changes): ingest benign `security` / `target_sourcetype` events where `host`, `package_name`, `user` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 10 confirms zero alert rows for 'Installed Software Changes'."
  },
  "dataSources": "Software inventory events Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "SCCM, Jamf inventory — Universal Forwarder (Splunkbase 617)",
  "spl": "index=security tag=endpoint tag=package action=install\n| where match(package_name,\"(?i)(miner|remote|vpn)\")\n| stats count by host, package_name, user",
  "description": "Surfaces UC-10.13.48 — Installed, Software, Changes — using `index=security` sourcetype `configured sourcetype`, grouped by `host`, `package_name`, `user`. Outputs `count` each `the scheduled window`; alert when configured threshold. Primary focus: Software Changes. Purpose-built for 'Installed Software Changes' in Security Infra workflows.",
  "value": "For 'Installed Software Changes', UC-10.13.48 converts `activity` on UC-10.13.48 search results into actionable rows and reduces missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Prod servers should use golden images; alert on non-approved titles.",
  "visualization": "Table (new software), Bar chart.",
  "cimModels": [
    "Endpoint"
  ],
  "cimSpl": "| tstats `summariesonly` dc(Processes.process_path) as paths from datamodel=Endpoint.Processes by Processes.dest, _time span=1d\n| where paths>30",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Endpoint.Processes` dataset after normalizing this feed to the model; otherwise the `cimSpl` tstats search may return no rows.",
  "references": [
    {
      "title": "CIM: Endpoint",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Endpoint",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `security` (UC-10.13.48)** — Planned work for 'Installed Software Changes' shifts `count` over `span=1h` while `host`, `package_name`, `user` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `configured_sourcetype`** — Scheduled automation elevates `package_name` without crossing configured threshold for UC-10.13.48. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`, `package_name`, `user`** — Health checks in `security` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-10.13.48 on `host` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: SCCM, Jamf inventory.\n• Ensure the following data sources are available: Software inventory events.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nProd servers should use golden images; alert on non-approved titles.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=security tag=endpoint tag=package action=install\n| where match(package_name,\"(?i)(miner|remote|vpn)\")\n| stats count by host, package_name, user\n```\n\nUnderstanding this SPL\n\n**Installed Software Changes** — New or rare `process_path` / binaries on servers — inventory or package logs mapped to Endpoint.\n\nDocumented **Data sources**: Software inventory events. **App/TA** (typical add-on context): SCCM, Jamf inventory. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: security.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=security, tags. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where match(package_name,\"(?i)(miner|remote|vpn)\")` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by host, package_name, user** so each row reflects one combination of those dimensions.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` dc(Processes.process_path) as paths from datamodel=Endpoint.Processes by Processes.dest, _time span=1d\n| where paths>30\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Installed Software Changes** — New or rare `process_path` / binaries on servers — inventory or package logs mapped to Endpoint.\n\nDocumented **Data sources**: Software inventory events. **App/TA** (typical add-on context): SCCM, Jamf inventory. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Endpoint.Processes` — enable acceleration for that model.\n• Filters the current rows with `where paths>30` — typically the threshold or rule expression for this monitoring goal.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nAccelerate the CIM data model used in the optional tstats path, then run both the primary and `cimSpl` searches over a week that includes a known good baseline. Spot-check a few rows and tune thresholds; confirm no permission or tagging gap is hiding part of the population.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (new software), Bar chart.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (SCCM, Jamf inventory — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into security. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, package_name, user`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use the same kind of check described for \"Installed Software Changes\" once our data is shaped in a common, repeatable way across products. We help the team see trouble early, compare different tools fairly, and avoid building a one-off report for every vendor.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
