{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.12.12",
  "title": "Order Execution Anomaly Detection",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Compliance",
    "Trading"
  ],
  "splunkPillar": "Security",
  "industry": "Financial Services",
  "exclusions": "Covers the detection logic documented for 'Order Execution Anomaly Detection' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_order_execution_anomaly_detection` (UC-10.12.12), SOC dashboard panel \"Security Infra — Order Execution Anomaly Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=trading` sourcetype='oms:order' that satisfy the UC-10.12.12 SPL filters for 'Order Execution Anomaly Detection'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `trading` / `oms:order` events outside the detection threshold, or exclude the test entity via `soc_exception_register.csv` with a valid window. Confirm zero alert rows for UC-10.12.12."
  },
  "dataSources": "`sourcetype=\"oms:order\"` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "OMS logs, exchange drop copies — Universal Forwarder (Splunkbase 617)",
  "spl": "index=trading sourcetype=\"oms:order\"\n| transaction client_order_id maxspan=2s\n| eval cancels=mvcount(if(action=\"cancel\",1,null()))\n| eval submits=mvcount(if(action=\"new\",1,null()))\n| where submits>10 AND cancels/submits>0.8\n| table _time, symbol, client_order_id, submits, cancels",
  "description": "Flags unusual order patterns (layering, spoofing precursors) for market abuse surveillance.",
  "value": "Early 'Order Execution Anomaly Detection' detection reduces missed threat campaigns, control regressions, and SOC SLA breaches by giving Security Infra operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Tune for legitimate market making; integrate with surveillance team playbooks and MAR/SEC retention.",
  "visualization": "Table (suspicious clusters), Timeline (order lifecycle), Bar chart (cancel ratio by desk).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable for the primary search: it relies on application- and industry-specific log formats, not a normalized CIM data model. Optional mapping to CIM, if you adopt it, is a separate follow-on project.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **SOC tuning or purple-team exercise** — Expected alert volume during rule validation for 'Order Execution Anomaly Detection'. Match active CHG with `purple_team=true`.\n\n2. **Content deployment window** — TA or CIM update shifts field extractions on `index=trading` `oms:order`.\n\n3. **Duplicate log ingestion** — HF/IDX misconfiguration doubles event counts.\n\n4. **Baseline seasonality** — Month-end batch jobs skew trending searches.\n\n**Suppression mechanism:** `soc_exception_register.csv` with entity, reason, valid_until.",
  "equipment": [
    "exchange"
  ],
  "equipmentModels": [
    "exchange_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: OMS logs, exchange drop copies.\n• Ensure the following data sources are available: `sourcetype=\"oms:order\"`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nTune for legitimate market making; integrate with surveillance team playbooks and MAR/SEC retention.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=trading sourcetype=\"oms:order\"\n| transaction client_order_id maxspan=2s\n| eval cancels=mvcount(if(action=\"cancel\",1,null()))\n| eval submits=mvcount(if(action=\"new\",1,null()))\n| where submits>10 AND cancels/submits>0.8\n| table _time, symbol, client_order_id, submits, cancels\n```\n\nUnderstanding this SPL\n\n**Order Execution Anomaly Detection** — Flags unusual order patterns (layering, spoofing precursors) for market abuse surveillance.\n\nDocumented **Data sources**: `sourcetype=\"oms:order\"`. **App/TA** (typical add-on context): OMS logs, exchange drop copies. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: trading; **sourcetype**: oms:order. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=trading, sourcetype=\"oms:order\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Groups related events into transactions — prefer `maxspan`/`maxpause`/`maxevents` for bounded memory.\n• `eval` defines or adjusts **cancels** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **submits** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where submits>10 AND cancels/submits>0.8` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Order Execution Anomaly Detection**): table _time, symbol, client_order_id, submits, cancels\n\n\nStep 3 — Validate\nRun the search on recent production data, spot-check a handful of results against the source system or operational runbooks (cash operations, EHR, payment gateway, etc.), and adjust thresholds to match your seasonal patterns and test windows so teams are not flooded with noise.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (suspicious clusters), Timeline (order lifecycle), Bar chart (cancel ratio by desk).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (OMS logs, exchange drop copies — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=oms:order into trading. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`submits`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "In Financial Services, we pay close attention to \"Order Execution Anomaly Detection\" in our day-to-day work so that fraud, waste, and compliance issues do not slip by quietly. We help teams focus on the few situations that need a person, not every routine blip in the data.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
