{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.8.37",
  "title": "EDR Tampering and Exclusion Detection",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'EDR Tampering and Exclusion Detection' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_edr_tampering_and_exclusion_detection` (UC-10.8.37), SOC dashboard panel \"Security Infra — EDR Tampering and Exclusion Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.8.37 positive (EDR, Tampering, and, Exclusion, Detection): ingest synthetic events into `index=endpoint` sourcetype='edr:agent' where `count` grouped by `entity` satisfies detection threshold for 'EDR Tampering and Exclusion Detection'. Validate filter `(event_type=\"tampering\" OR event_type=\"exclusion_added\" OR s`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.8.37 negative (Exclusion Detection): ingest benign `endpoint` / `edr:agent` events where `entity` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 5 confirms zero alert rows for 'EDR Tampering and Exclusion Detection'."
  },
  "dataSources": "EDR agent status, exclusion lists, Defender/AV events Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "EDR/AV agent logs, Windows Security events — Universal Forwarder (Splunkbase 617)",
  "spl": "index=endpoint (sourcetype=\"edr:agent\" OR sourcetype=\"defender:event\")\n| search (event_type=\"tampering\" OR event_type=\"exclusion_added\" OR status=\"disabled\")\n| table _time, host, event_type, user, path_or_rule\n| sort -_time",
  "description": "Monitors 'EDR Tampering and Exclusion Detection' patterns (UC-10.8.37) sourced from `index=endpoint` sourcetype `edr:agent`. Threshold bundle: configured threshold; aggregation grain: `entity`; metrics: `count`. Enrichment path: filter `(event_type=\"tampering\" OR event_type=\"exclusion_added\" OR s`.",
  "value": "UC-10.8.37 gives Security Infra teams earlier visibility into Exclusion Detection through `edr:agent` feeds, lowering missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Ingest EDR and AV agent events. Alert on tampering, new exclusions, or agent disabled. Require change approval for exclusions. Report on exclusion coverage by host.",
  "visualization": "Table (tampering/exclusion events), Timeline (events), Bar chart (by host).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `endpoint` (UC-10.8.37)** — Planned work for 'EDR Tampering and Exclusion Detection' shifts `count` over `span=1h` while `host` remains in-scope. Applies filter `(event_type=\"tampering\" OR event_type=\"exclusion_added\" OR s`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `edr:agent`** — Scheduled automation elevates `src` without crossing configured threshold for UC-10.8.37. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `endpoint` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `edr:agent`** — First 48h baseline for UC-10.8.37 on `host` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: EDR/AV agent logs, Windows Security events.\n• Ensure the following data sources are available: EDR agent status, exclusion lists, Defender/AV events.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest EDR and AV agent events. Alert on tampering, new exclusions, or agent disabled. Require change approval for exclusions. Report on exclusion coverage by host.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=endpoint (sourcetype=\"edr:agent\" OR sourcetype=\"defender:event\")\n| search (event_type=\"tampering\" OR event_type=\"exclusion_added\" OR status=\"disabled\")\n| table _time, host, event_type, user, path_or_rule\n| sort -_time\n```\n\nUnderstanding this SPL\n\n**EDR Tampering and Exclusion Detection** — Attackers often disable or exclude EDR/AV to evade detection. Detecting tampering or new exclusions ensures security controls remain active on endpoints.\n\nDocumented **Data sources**: EDR agent status, exclusion lists, Defender/AV events. **App/TA** (typical add-on context): EDR/AV agent logs, Windows Security events. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: endpoint; **sourcetype**: edr:agent, defender:event. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=endpoint, sourcetype=\"edr:agent\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Applies an explicit `search` filter to narrow the current result set.\n• Pipeline stage (see **EDR Tampering and Exclusion Detection**): table _time, host, event_type, user, path_or_rule\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nCompare with certificate authority records, an inventory of endpoints, or `openssl x509 -text` on a sample file so expiry and subject data in Splunk match the source of truth.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (tampering/exclusion events), Timeline (events), Bar chart (by host).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (EDR/AV agent logs, Windows Security events — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=edr:agent, defender:event into endpoint. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, user, action`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Attackers often disable or exclude /AV to evade detection — so we know when protection is off, out of date, or bypassed.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
