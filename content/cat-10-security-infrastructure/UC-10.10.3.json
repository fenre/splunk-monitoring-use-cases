{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.10.3",
  "title": "Stale detection identification (rules never firing 90+ days)",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Stale detection identification (rules never firing 90+ days)' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_stale_detection_identification_rules_never_firin` (UC-10.10.3), SOC dashboard panel \"Security Infra — Stale detection identification (rules never firing 90+ days)\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.10.3 positive (Stale, detection, identification, (rules, never): ingest synthetic events into `index=summary` sourcetype='detection_metrics' where `hits` grouped by `rule_name` satisfies `hits` < 1 for 'Stale detection identification (rules never firing 90+ days)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.10.3 negative (90+ days)): ingest benign `summary` / `detection_metrics` events where `rule_name` stays below `hits` < 1, or exclude the test entity via `soc_exception_register.csv`. Variant 13 confirms zero alert rows for 'Stale detection identification (rules never firing 90+ days)'."
  },
  "dataSources": "Summary index populated from savedsearch scheduler or ES introspection Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Enterprise Security / custom — Universal Forwarder (Splunkbase 617)",
  "spl": "index=summary sourcetype=detection_metrics earliest=-90d\n| stats sum(hit_count) as hits by rule_name\n| where hits=0 OR hits<1\n| sort rule_name",
  "description": "Detects UC-10.10.3 — Stale, detection, identification, (rules, never — using `index=summary` sourcetype `detection_metrics`, grouped by `rule_name`. Outputs `hits` each `the scheduled window`; alert when `hits` < 1. Primary focus: 90+ days). Purpose-built for 'Stale detection identification (rules never firing 90+ days)' in Security Infra workflows.",
  "value": "UC-10.10.3 gives Security Infra teams earlier visibility into 90+ days) through `detection_metrics` feeds, lowering missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Normalize field names to your Splunk ES version and index names. Use summary indexing or ES introspection where `detection_metrics` is not present. For YARA, deploy a scripted input or HEC to ingest scanner JSON.",
  "visualization": "Table, Single value, Time chart",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Enterprise Security",
      "url": "https://splunkbase.splunk.com/app/263",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk ES Content Management",
      "url": "https://docs.splunk.com/Documentation/ES/latest/Admin/ContentManagement",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `summary` (UC-10.10.3)** — Planned work for 'Stale detection identification (rules never firing 90+ days)' shifts `hits` over `span=1h` while `rule_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `sum` on `detection_metrics`** — Scheduled automation elevates `src` without crossing `hits` < 1 for UC-10.10.3. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `rule_name`** — Health checks in `summary` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `detection_metrics`** — First 48h baseline for UC-10.10.3 on `rule_name` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `rule_name`, `exception_reason`, `valid_until`.",
  "detectionType": "Operational metrics",
  "securityDomain": "soc_operations",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk Enterprise Security"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Enterprise Security / custom.\n• Ensure the following data sources are available: Summary index populated from savedsearch scheduler or ES introspection.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nNormalize field names to your Splunk ES version and index names. Use summary indexing or ES introspection where `detection_metrics` is not present. For YARA, deploy a scripted input or HEC to ingest scanner JSON.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=summary sourcetype=detection_metrics earliest=-90d\n| stats sum(hit_count) as hits by rule_name\n| where hits=0 OR hits<1\n| sort rule_name\n```\n\nUnderstanding this SPL\n\n**Stale detection identification (rules never firing 90+ days)** — Lists correlation searches with zero or near-zero hits over 90 days for retirement review.\n\nDocumented **Data sources**: Summary index populated from savedsearch scheduler or ES introspection. **App/TA** (typical add-on context): Splunk Enterprise Security / custom. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: summary; **sourcetype**: detection_metrics. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Detection type** for this use case: Operational metrics — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=summary, sourcetype=detection_metrics, time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by rule_name** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where hits=0 OR hits<1` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nIn **ES Content Management**, confirm the rule is enabled, scheduled, and scoped to indexes that have had traffic recently. Compare to a test-notable or manual run so “never fires” is not a permissions issue.\n\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table, Single value, Time chart\n\nScripted input (generic example)\nThis use case relies on a scripted input. In the app's local/inputs.conf add a stanza such as:\n\n```ini\n[script://$SPLUNK_HOME/etc/apps/YourApp/bin/collect.sh]\ninterval = 300\nsourcetype = your_sourcetype\nindex = main\ndisabled = 0\n```\n\nThe script should print one event per line (e.g. key=value). Example minimal script (bash):\n\n```bash\n#!/usr/bin/env bash\n# Output metrics or events, one per line\necho \"metric=value timestamp=$(date +%s)\"\n```\n\nFor full details (paths, scheduling, permissions), see the Implementation guide: docs/implementation-guide.md\n",
  "grandmaExplanation": "We find rules that have gone quiet so we are not holding broken glass while attackers walk past.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
