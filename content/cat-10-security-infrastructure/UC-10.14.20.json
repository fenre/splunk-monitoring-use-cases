{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.14.20",
  "title": "OT Network Baseline Deviation Detection",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Security",
    "Change"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'OT Network Baseline Deviation Detection' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_ot_network_baseline_deviation_detection` (UC-10.14.20), SOC dashboard panel \"Security Infra — OT Network Baseline Deviation Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=ot` sourcetype='ics:flow' where `src` satisfies detection threshold for UC-10.14.20 ('OT Network Baseline Deviation Detection'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `ot` / `ics:flow` events where `src` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv` with a valid window. Confirm zero alert rows for UC-10.14.20."
  },
  "dataSources": "`index=ot` NetFlow or Zeek `sourcetype=\"ics:flow\"`, baseline KV store Indexes: ot. Sourcetypes: ics:flow. Key fields: dest, first_seen_baseline, proto, sig, src, zone. Validate field extraction in Search before saving the alert; compare event counts to the vendor admin console.",
  "app": "OT Security Add-on for Splunk (Splunkbase 5151)",
  "spl": "index=ot sourcetype=\"ics:flow\"\n| eval sig=src.\"|\".dest.\"|\".dest_port.\"|\".proto\n| lookup ot_traffic_baseline sig OUTPUT first_seen_baseline\n| where isnull(first_seen_baseline)\n| bin _time span=1d\n| stats earliest(_time) as first_seen dc(dest_port) as new_ports by src, dest, proto\n| lookup ot_asset_inventory src OUTPUT zone\n| where zone=\"OT\"",
  "description": "Detects new protocols, ports, or connections relative to learned OT baselines (T0840), highlighting shadow connectivity and preparatory attack activity.",
  "value": "Early 'OT Network Baseline Deviation Detection' detection reduces missed threat campaigns, control regressions, and SOC SLA breaches by giving Security Infra operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Populate baselines via OT Security Add-on batch jobs after stable learning windows. Use Splunk ES for notable generation on first-seen edges. Pair with T0840 in threat hunting.",
  "visualization": "Chord graph of new edges, Timechart of first-seen flows, Table with MITRE tag.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "OT Security Add-on for Splunk",
      "url": "https://splunkbase.splunk.com/app/5151",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Network_Traffic",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Network_Traffic",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `ot`** — Planned work for 'OT Network Baseline Deviation Detection' shifts `dc` over `span=1d` while `src` remains in-scope for UC-10.14.20. Cross-check CMDB CHG.\n\n2. **Batch `dc` on `ics:flow`** — Scheduled automation elevates `dest` without crossing configured threshold. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `src`** — Health checks in `ot` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `ics:flow`** — First 48h baseline for `src` is unstable; join `ot_traffic_baseline` prior to threshold.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `src`, `exception_reason`, `valid_until`.",
  "equipment": [
    "netflow"
  ],
  "equipmentModels": [
    "netflow_netflow"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk OT Security Add-on"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: OT Security Add-on for Splunk (Splunkbase 5151).\n• Ensure the following data sources are available: `index=ot` NetFlow or Zeek `sourcetype=\"ics:flow\"`, baseline KV store.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nPopulate baselines via OT Security Add-on batch jobs after stable learning windows. Use Splunk ES for notable generation on first-seen edges. Pair with T0840 in threat hunting.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot sourcetype=\"ics:flow\"\n| eval sig=src.\"|\".dest.\"|\".dest_port.\"|\".proto\n| lookup ot_traffic_baseline sig OUTPUT first_seen_baseline\n| where isnull(first_seen_baseline)\n| bin _time span=1d\n| stats earliest(_time) as first_seen dc(dest_port) as new_ports by src, dest, proto\n| lookup ot_asset_inventory src OUTPUT zone\n| where zone=\"OT\"\n```\n\nUnderstanding this SPL\n\n**OT Network Baseline Deviation Detection** — Detects new protocols, ports, or connections relative to learned OT baselines (T0840), highlighting shadow connectivity and preparatory attack activity.\n\nDocumented **Data sources**: `index=ot` NetFlow or Zeek `sourcetype=\"ics:flow\"`, baseline KV store. **App/TA** (typical add-on context): OT Security Add-on for Splunk (Splunkbase 5151). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot; **sourcetype**: ics:flow. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot, sourcetype=\"ics:flow\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **sig** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Enriches events using `lookup` (lookup definition + optional OUTPUT fields).\n• Filters the current rows with `where isnull(first_seen_baseline)` — typically the threshold or rule expression for this monitoring goal.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by src, dest, proto** so each row reflects one combination of those dimensions.\n• Enriches events using `lookup` (lookup definition + optional OUTPUT fields).\n• Filters the current rows with `where zone=\"OT\"` — typically the threshold or rule expression for this monitoring goal.\n\nStep 3 — Validate\nCompare results to the **OT Security** add-on views, the **OT asset** inventory, and any **engineering change** or CMMS record. When you send ot-data through **Nozomi**, match the same **Nozomi Guardian** alert. When you use **Cyber Vision** sourcetype `cisco:cybervision`, line up the same event in the **Cyber Vision** UI. For raw ICS **syslog**, match the sensor or PLC console for that minute.\n\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Chord graph of new edges, Timechart of first-seen flows, Table with MITRE tag.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (OT Security Add-on for Splunk (Splunkbase 5151)). Enable the modular input or HEC token that writes sourcetype=ics:flow into ot. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`dest, first_seen_baseline, proto, sig`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We compare how the network usually behaves in the plant to today so a creeping change in traffic or talkers rings a bell early.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
