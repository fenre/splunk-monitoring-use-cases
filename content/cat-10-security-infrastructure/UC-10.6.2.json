{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.6.2",
  "title": "Mean Time to Remediation",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Mean Time to Remediation' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_mean_time_to_remediation` (UC-10.6.2), SOC dashboard panel \"Security Infra — Mean Time to Remediation\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.6.2 positive (Mean, Time, Remediation): ingest synthetic events into `index=vulnerability` sourcetype='tenable:vuln' where `avg_mttr`, `median_mttr` grouped by `severity` satisfies detection threshold for 'Mean Time to Remediation'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.6.2 negative (to Remediation): ingest benign `vulnerability` / `tenable:vuln` events where `severity` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 15 confirms zero alert rows for 'Mean Time to Remediation'."
  },
  "dataSources": "Scan results with first_seen and last_seen dates Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Vuln management TA — Universal Forwarder (Splunkbase 617)",
  "spl": "index=vulnerability sourcetype=\"tenable:vuln\" state=\"Fixed\"\n| eval mttr_days=round((fixed_date-first_seen)/86400)\n| stats avg(mttr_days) as avg_mttr, median(mttr_days) as median_mttr by severity",
  "description": "Alerts on UC-10.6.2 — Mean, Time, Remediation — using `index=vulnerability` sourcetype `tenable:vuln`, grouped by `severity`. Outputs `avg_mttr`, `median_mttr` each `the scheduled window`; alert when configured threshold. Primary focus: to Remediation. Purpose-built for 'Mean Time to Remediation' in Security Infra workflows.",
  "value": "UC-10.6.2 operationalises 'Mean Time to Remediation' against `tenable:vuln` feeds so operators reduce missed threat campaigns, control regressions, and SOC SLA breaches without manual correlation.",
  "implementation": "Track first_seen and fixed_date for each vulnerability. Calculate MTTR by severity. Report against SLA targets (Critical: 7d, High: 30d, Medium: 90d). Identify teams with consistently high MTTR for process improvement.",
  "visualization": "Bar chart (MTTR by severity), Line chart (MTTR trend), Table (SLA compliance by team).",
  "cimModels": [
    "Vulnerabilities"
  ],
  "cimSpl": "| tstats count from datamodel=Vulnerabilities.Vulnerabilities where Vulnerabilities.severity IN (\"critical\",\"high\") by Vulnerabilities.cve,Vulnerabilities.dest",
  "dataModelAcceleration": "Vulnerabilities.Vulnerabilities",
  "references": [
    {
      "title": "CIM: Vulnerabilities",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Vulnerabilities",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `vulnerability` (UC-10.6.2)** — Planned work for 'Mean Time to Remediation' shifts `avg_mttr`, `median_mttr` over `span=1h` while `severity` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `tenable:vuln`** — Scheduled automation elevates `mttr_days` without crossing configured threshold for UC-10.6.2. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `severity`** — Health checks in `vulnerability` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `tenable:vuln`** — First 48h baseline for UC-10.6.2 on `severity` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `severity`, `exception_reason`, `valid_until`.",
  "equipment": [
    "tenable"
  ],
  "equipmentModels": [
    "tenable_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Vuln management TA.\n• Ensure the following data sources are available: Scan results with first_seen and last_seen dates.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nTrack first_seen and fixed_date for each vulnerability. Calculate MTTR by severity. Report against SLA targets (Critical: 7d, High: 30d, Medium: 90d). Identify teams with consistently high MTTR for process improvement.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=vulnerability sourcetype=\"tenable:vuln\" state=\"Fixed\"\n| eval mttr_days=round((fixed_date-first_seen)/86400)\n| stats avg(mttr_days) as avg_mttr, median(mttr_days) as median_mttr by severity\n```\n\nUnderstanding this SPL\n\n**Mean Time to Remediation** — MTTR measures remediation efficiency. Long MTTR indicates process bottlenecks or resource constraints requiring management attention.\n\nDocumented **Data sources**: Scan results with first_seen and last_seen dates. **App/TA** (typical add-on context): Vuln management TA. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: vulnerability; **sourcetype**: tenable:vuln. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=vulnerability, sourcetype=\"tenable:vuln\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **mttr_days** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by severity** so each row reflects one combination of those dimensions.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats count from datamodel=Vulnerabilities.Vulnerabilities where Vulnerabilities.severity IN (\"critical\",\"high\") by Vulnerabilities.cve,Vulnerabilities.dest\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Mean Time to Remediation** — MTTR measures remediation efficiency. Long MTTR indicates process bottlenecks or resource constraints requiring management attention.\n\nDocumented **Data sources**: Scan results with first_seen and last_seen dates. **App/TA** (typical add-on context): Vuln management TA. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Vulnerabilities.Vulnerabilities` — enable acceleration for that model.\n• Groups by CVE and destination to align with per-finding and per-host triage in the vendor UIs.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare MTTR and remediation dates in Splunk with Tenable.io and Qualys VMDR (or Cloud Agent) vulnerability history for the same assets and CVEs. For sampled CVEs, cross-check with NIST NVD. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Bar chart (MTTR by severity), Line chart (MTTR trend), Table (SLA compliance by team).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Vuln management TA — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=tenable:vuln into vulnerability. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`severity`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We see how long it takes to fix reported flaws so we can tell when remediation is healthy, when a team is stuck, or when an approved exception explains the delay.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
