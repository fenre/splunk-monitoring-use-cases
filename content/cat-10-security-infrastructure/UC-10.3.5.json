{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.3.5",
  "title": "Endpoint Isolation Events",
  "criticality": "critical",
  "difficulty": "beginner",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Endpoint Isolation Events' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_endpoint_isolation_events` (UC-10.3.5), SOC dashboard panel \"Security Infra — Endpoint Isolation Events\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.3.5 positive (Endpoint, Isolation, Events): ingest synthetic events into `index=edr` sourcetype='crowdstrike:containment' where `count` grouped by `entity` satisfies detection threshold for 'Endpoint Isolation Events'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.3.5 negative (Isolation Events): ingest benign `edr` / `crowdstrike:containment` events where `entity` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 3 confirms zero alert rows for 'Endpoint Isolation Events'."
  },
  "dataSources": "EDR containment/isolation logs Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "EDR TA — Universal Forwarder (Splunkbase 617)",
  "spl": "index=edr sourcetype=\"crowdstrike:containment\"\n| table _time, hostname, action, initiated_by, reason\n| sort -_time",
  "description": "Monitors when Isolation Events criteria fire for 'Endpoint Isolation Events' (UC-10.3.5) in `index=edr` sourcetype `crowdstrike:containment`. Pipeline aggregates `count` by `entity` over `the scheduled window`. Alert logic enforces configured threshold. Title terms: Endpoint, Isolation, Events. Eliminates manual hunting for UC-10.3.5 conditions.",
  "value": "UC-10.3.5 helps teams contain Endpoint, Isolation, Events scenarios and reduce missed threat campaigns, control regressions, and SOC SLA breaches before incidents compound.",
  "implementation": "Track all isolation events (isolate, un-isolate). Alert on isolation events for awareness. Track isolation duration. Alert when endpoints remain isolated >24 hours without resolution. Maintain isolation audit trail.",
  "visualization": "Table (isolated endpoints), Timeline (isolation events), Single value (currently isolated count).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "CIM: Endpoint",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Endpoint",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `edr` (UC-10.3.5)** — Planned work for 'Endpoint Isolation Events' shifts `count` over `span=1h` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `crowdstrike:containment`** — Scheduled automation elevates `src` without crossing configured threshold for UC-10.3.5. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `edr` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `crowdstrike:containment`** — First 48h baseline for UC-10.3.5 on `host` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "crowdstrike"
  ],
  "equipmentModels": [
    "crowdstrike_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: EDR TA.\n• Ensure the following data sources are available: EDR containment/isolation logs.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nTrack all isolation events (isolate, un-isolate). Alert on isolation events for awareness. Track isolation duration. Alert when endpoints remain isolated >24 hours without resolution. Maintain isolation audit trail.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=edr sourcetype=\"crowdstrike:containment\"\n| table _time, hostname, action, initiated_by, reason\n| sort -_time\n```\n\nUnderstanding this SPL\n\n**Endpoint Isolation Events** — Isolation events indicate active incident response. Tracking ensures isolation is maintained and properly lifted when resolved.\n\nDocumented **Data sources**: EDR containment/isolation logs. **App/TA** (typical add-on context): EDR TA. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: edr; **sourcetype**: crowdstrike:containment. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=edr, sourcetype=\"crowdstrike:containment\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Pipeline stage (see **Endpoint Isolation Events**): table _time, hostname, action, initiated_by, reason\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nIn the EDR **containment** or **isolation** view (CrowdStrike Falcon, Microsoft Defender for Endpoint, Carbon Black, or SentinelOne), compare the same time window: host, who initiated, and reason. Match each row to an incident, playbook, or change ticket; isolation without a case is worth a follow-up, not a silent ignore.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (isolated endpoints), Timeline (isolation events), Single value (currently isolated count).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (EDR TA — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=crowdstrike:containment into edr. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, user, action`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We follow when machines get cut off from the network in an emergency so that isolation is only lifted when the problem is really cleared.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
