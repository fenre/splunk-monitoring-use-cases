{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.4.6",
  "title": "Email Volume Anomalies",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Email Volume Anomalies' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_email_volume_anomalies` (UC-10.4.6), SOC dashboard panel \"Security Infra — Email Volume Anomalies\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.4.6 positive (Email, Volume, Anomalies): ingest synthetic events into `index=email` sourcetype='ms:o365:messageTrace' where `avg_sent`, `stdev_sent` grouped by `SenderAddress` satisfies detection threshold for 'Email Volume Anomalies'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.4.6 negative (Volume Anomalies): ingest benign `email` / `ms:o365:messageTrace` events where `SenderAddress` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 7 confirms zero alert rows for 'Email Volume Anomalies'."
  },
  "dataSources": "Email message tracking logs (outbound) Collected via Splunk Add-on for Microsoft Office 365 (Splunkbase 4054) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk_TA_MS_O365, Splunk_TA_microsoft-exchange — Splunk Add-on for Microsoft Office 365 (Splunkbase 4054)",
  "spl": "index=email sourcetype=\"ms:o365:messageTrace\" Direction=\"Outbound\"\n| stats count by SenderAddress\n| eventstats avg(count) as avg_sent, stdev(count) as stdev_sent\n| where count > avg_sent + 3*stdev_sent\n| table SenderAddress, count, avg_sent",
  "description": "Surfaces UC-10.4.6 — Email, Volume, Anomalies — using `index=email` sourcetype `ms:o365:messageTrace`, grouped by `SenderAddress`. Outputs `avg_sent`, `stdev_sent` each `the scheduled window`; alert when configured threshold. Primary focus: Volume Anomalies. Purpose-built for 'Email Volume Anomalies' in Security Infra workflows.",
  "value": "UC-10.4.6 operationalises 'Email Volume Anomalies' against `ms:o365:messageTrace` feeds so operators reduce missed threat campaigns, control regressions, and SOC SLA breaches without manual correlation.",
  "implementation": "Track outbound email volume per sender. Baseline normal patterns. Alert when any sender exceeds 3× standard deviation. Correlate with sign-in events to detect compromised accounts. Report on top senders for capacity planning.",
  "visualization": "Bar chart (top senders), Line chart (outbound volume trend), Table (anomalous senders).",
  "cimModels": [
    "Email"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Email.All_Email\n  by All_Email.src_user All_Email.action span=1h\n| sort -count",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Email.All_Email` dataset once message and attachment fields are CIM-tagged; otherwise `cimSpl` tstats may return no rows.",
  "references": [
    {
      "title": "CIM: Email",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Email",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 4054",
      "url": "https://splunkbase.splunk.com/app/4054",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `email` (UC-10.4.6)** — Planned work for 'Email Volume Anomalies' shifts `avg_sent`, `stdev_sent` over `span=1h` while `SenderAddress` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `ms:o365:messageTrace`** — Scheduled automation elevates `src` without crossing configured threshold for UC-10.4.6. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `SenderAddress`** — Health checks in `email` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `ms:o365:messageTrace`** — First 48h baseline for UC-10.4.6 on `SenderAddress` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `SenderAddress`, `exception_reason`, `valid_until`.",
  "equipment": [
    "exchange",
    "m365"
  ],
  "equipmentModels": [
    "exchange_generic",
    "m365_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk_TA_MS_O365, Splunk_TA_microsoft-exchange.\n• Ensure the following data sources are available: Email message tracking logs (outbound).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nTrack outbound email volume per sender. Baseline normal patterns. Alert when any sender exceeds 3× standard deviation. Correlate with sign-in events to detect compromised accounts. Report on top senders for capacity planning.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=email sourcetype=\"ms:o365:messageTrace\" Direction=\"Outbound\"\n| stats count by SenderAddress\n| eventstats avg(count) as avg_sent, stdev(count) as stdev_sent\n| where count > avg_sent + 3*stdev_sent\n| table SenderAddress, count, avg_sent\n```\n\nUnderstanding this SPL\n\n**Email Volume Anomalies** — Unusual outbound email volumes may indicate compromised accounts used for spam/phishing or mass data exfiltration via email.\n\nDocumented **Data sources**: Email message tracking logs (outbound). **App/TA** (typical add-on context): Splunk_TA_MS_O365, Splunk_TA_microsoft-exchange. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: email; **sourcetype**: ms:o365:messageTrace. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=email, sourcetype=\"ms:o365:messageTrace\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by SenderAddress** so each row reflects one combination of those dimensions.\n• `eventstats` aggregates the pipeline (counts, distinct values, sums, percentiles, etc.) into fewer rows.\n• Filters the current rows with `where count > avg_sent + 3*stdev_sent` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Email Volume Anomalies**): table SenderAddress, count, avg_sent\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` count\n  from datamodel=Email.All_Email\n  by All_Email.src_user All_Email.action span=1h\n| sort -count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Email Volume Anomalies** — Unusual outbound email volumes may indicate compromised accounts used for spam/phishing or mass data exfiltration via email.\n\nDocumented **Data sources**: Email message tracking logs (outbound). **App/TA** (typical add-on context): Splunk_TA_MS_O365, Splunk_TA_microsoft-exchange. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Email.All_Email` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare search results in Splunk with the Microsoft 365 Defender portal (https://security.microsoft.com) and, where applicable, the Proofpoint TAP dashboard, Mimecast Personal Portal, or Cisco ESA `mail_logs`. Confirm events are present, fields parse correctly, and sample ratios match the vendor console for the same time range.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Bar chart (top senders), Line chart (outbound volume trend), Table (anomalous senders).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk_TA_MS_O365, Splunk_TA_microsoft-exchange — Splunk Add-on for Microsoft Office 365 (Splunkbase 4054)). Enable the modular input or HEC token that writes sourcetype=ms:o365:messageTrace into email. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`SenderAddress`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for unusual mail volume to and from users and systems, so sudden spikes from compromise, harvesting, or misconfiguration stand out from normal business traffic.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
