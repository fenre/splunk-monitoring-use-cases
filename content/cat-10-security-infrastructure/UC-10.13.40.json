{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.13.40",
  "title": "Unauthorized Change Detection",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Unauthorized Change Detection' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_unauthorized_change_detection` (UC-10.13.40), SOC dashboard panel \"Security Infra — Unauthorized Change Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.13.40 positive (Unauthorized, Change, Detection): ingest synthetic events into `index=security` sourcetype='target_sourcetype' where `count` grouped by `user`, `object_id` satisfies detection threshold for 'Unauthorized Change Detection'. Validate lookup `cab_authorized_users`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.13.40 negative (Change Detection): ingest benign `security` / `target_sourcetype` events where `user`, `object_id` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 0 confirms zero alert rows for 'Unauthorized Change Detection'."
  },
  "dataSources": "`object_id`, `user`, `change_type` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Change CIM + ITSM — Universal Forwarder (Splunkbase 617)",
  "spl": "index=security tag=change\n| lookup cab_authorized_users user OUTPUT authorized\n| where authorized!=\"true\" OR isnull(change_ticket)\n| stats count by user, object_id",
  "description": "Identifies 'Unauthorized Change Detection' (UC-10.13.40) using `index=security` sourcetype `configured sourcetype` via lookup `cab_authorized_users` by aggregating `count` grouped by `user`, `object_id` over `the scheduled window` and alerting when configured threshold. Keywords: Unauthorized, Change, Detection. Focuses on Change Detection behaviour so security infra teams can triage UC-10.13.40 without ad-hoc log review.",
  "value": "UC-10.13.40 accelerates insight into Change Detection ('Unauthorized Change Detection') by tracking `activity` on lookup `cab_authorized_users`, reducing missed threat campaigns, control regressions, and SOC SLA breaches before spreadsheet checks hide regressions.",
  "implementation": "Auto-severity for production `object_category`.",
  "visualization": "Table (unauthorized), Timeline.",
  "cimModels": [
    "Change"
  ],
  "cimSpl": "| tstats `summariesonly` count from datamodel=Change.All_Changes where All_Changes.user!=\"cicd_svc\" by All_Changes.user, All_Changes.object_id",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Change.All_Changes` dataset after normalizing this feed to the model; otherwise the `cimSpl` tstats search may return no rows.",
  "references": [
    {
      "title": "CIM: Change",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Change",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `security` (UC-10.13.40)** — Planned work for 'Unauthorized Change Detection' shifts `count` over `span=1h` while `user`, `object_id` remains in-scope. Applies lookup `cab_authorized_users`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `configured_sourcetype`** — Scheduled automation elevates `object_id` without crossing configured threshold for UC-10.13.40. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `user`, `object_id`** — Health checks in `security` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-10.13.40 on `user` is unstable; join `cab_authorized_users` prior to threshold.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `user`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Change CIM + ITSM.\n• Ensure the following data sources are available: `object_id`, `user`, `change_type`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nAuto-severity for production `object_category`.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=security tag=change\n| lookup cab_authorized_users user OUTPUT authorized\n| where authorized!=\"true\" OR isnull(change_ticket)\n| stats count by user, object_id\n```\n\nUnderstanding this SPL\n\n**Unauthorized Change Detection** — Changes without ticket ID or with `user` not in CAB roster — map `object_attrs` to CIM.\n\nDocumented **Data sources**: `object_id`, `user`, `change_type`. **App/TA** (typical add-on context): Change CIM + ITSM. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: security.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=security, tags. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Enriches events using `lookup` (lookup definition + optional OUTPUT fields).\n• Filters the current rows with `where authorized!=\"true\" OR isnull(change_ticket)` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by user, object_id** so each row reflects one combination of those dimensions.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` count from datamodel=Change.All_Changes where All_Changes.user!=\"cicd_svc\" by All_Changes.user, All_Changes.object_id\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Unauthorized Change Detection** — Changes without ticket ID or with `user` not in CAB roster — map `object_attrs` to CIM.\n\nDocumented **Data sources**: `object_id`, `user`, `change_type`. **App/TA** (typical add-on context): Change CIM + ITSM. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Change.All_Changes` — enable acceleration for that model.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nAccelerate the CIM data model used in the optional tstats path, then run both the primary and `cimSpl` searches over a week that includes a known good baseline. Spot-check a few rows and tune thresholds; confirm no permission or tagging gap is hiding part of the population.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (unauthorized), Timeline.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Change CIM + ITSM — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into security. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`authorized, object_id, user`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use the same kind of check described for \"Unauthorized Change Detection\" once our data is shaped in a common, repeatable way across products. We help the team see trouble early, compare different tools fairly, and avoid building a one-off report for every vendor.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
