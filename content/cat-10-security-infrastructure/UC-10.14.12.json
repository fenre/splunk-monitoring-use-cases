{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.14.12",
  "title": "Unauthorized VPN Tunnel into OT Network",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Unauthorized VPN Tunnel into OT Network' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_unauthorized_vpn_tunnel_into_ot_network` (UC-10.14.12), SOC dashboard panel \"Security Infra — Unauthorized VPN Tunnel into OT Network\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=ot_security` sourcetype='ot:security:vpn' that satisfy the UC-10.14.12 SPL filters for 'Unauthorized VPN Tunnel into OT Network'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `ot_security` / `ot:security:vpn` events outside the detection threshold, or exclude the test entity via `soc_exception_register.csv` with a valid window. Confirm zero alert rows for UC-10.14.12."
  },
  "dataSources": "`index=ot_security` VPN concentrator logs, firewall sessions Indexes: ot_security, ot. Sourcetypes: ot:security:vpn, ics:firewall. Key fields: assigned_ip, gateway, remote_ip, user. Validate field extraction in Search before saving the alert; compare event counts to the vendor admin console.",
  "app": "OT Security Add-on for Splunk (Splunkbase 5151)",
  "spl": "index=ot_security sourcetype=\"ot:security:vpn\" OR (index=ot sourcetype=\"ics:firewall\" dest_port IN (443, 1194, 4500))\n| lookup ot_approved_vpn_peers remote_ip user OUTPUT approved_site\n| where isnull(approved_site)\n| stats earliest(_time) as start latest(_time) as end by remote_ip, user, assigned_ip, gateway",
  "description": "Detects VPN or encrypted sessions into OT that violate perimeter policy (T0886), supporting regulatory and segmentation requirements.",
  "value": "Early 'Unauthorized VPN Tunnel into OT Network' detection reduces missed threat campaigns, control regressions, and SOC SLA breaches by giving Security Infra operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Maintain approved vendor and employee VPN profiles. Alert on split tunnel to OT, new device IDs. Map to T0886.",
  "visualization": "Timeline of VPN sessions, Table of unauthorized peers.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "OT Security Add-on for Splunk",
      "url": "https://splunkbase.splunk.com/app/5151",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Network_Traffic",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Network_Traffic",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **SOC tuning or purple-team exercise** — Expected alert volume during rule validation for 'Unauthorized VPN Tunnel into OT Network'. Match active CHG with `purple_team=true`.\n\n2. **Content deployment window** — TA or CIM update shifts field extractions on `index=ot_security` `ot:security:vpn`.\n\n3. **Duplicate log ingestion** — HF/IDX misconfiguration doubles event counts.\n\n4. **Baseline seasonality** — Month-end batch jobs skew trending searches.\n\n**Suppression mechanism:** `soc_exception_register.csv` with entity, reason, valid_until.",
  "equipment": [
    "m365"
  ],
  "equipmentModels": [
    "m365_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk OT Security Add-on"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: OT Security Add-on for Splunk (Splunkbase 5151).\n• Ensure the following data sources are available: `index=ot_security` VPN concentrator logs, firewall sessions.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMaintain approved vendor and employee VPN profiles. Alert on split tunnel to OT, new device IDs. Map to T0886.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot_security sourcetype=\"ot:security:vpn\" OR (index=ot sourcetype=\"ics:firewall\" dest_port IN (443, 1194, 4500))\n| lookup ot_approved_vpn_peers remote_ip user OUTPUT approved_site\n| where isnull(approved_site)\n| stats earliest(_time) as start latest(_time) as end by remote_ip, user, assigned_ip, gateway\n```\n\nUnderstanding this SPL\n\n**Unauthorized VPN Tunnel into OT Network** — Detects VPN or encrypted sessions into OT that violate perimeter policy (T0886), supporting regulatory and segmentation requirements.\n\nDocumented **Data sources**: `index=ot_security` VPN concentrator logs, firewall sessions. **App/TA** (typical add-on context): OT Security Add-on for Splunk (Splunkbase 5151). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot_security, ot; **sourcetype**: ot:security:vpn, ics:firewall. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot_security, index=ot, sourcetype=\"ot:security:vpn\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Enriches events using `lookup` (lookup definition + optional OUTPUT fields).\n• Filters the current rows with `where isnull(approved_site)` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by remote_ip, user, assigned_ip, gateway** so each row reflects one combination of those dimensions.\n\nStep 3 — Validate\nCompare failed or disabled searches in Splunk to the **OT Security Add-on** configuration UI and the **Saved Searches** list so names and schedules line up. Spot-check one failure against `scheduler.log` around the run time.\n\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Timeline of VPN sessions, Table of unauthorized peers.\n",
  "grandmaExplanation": "We look for hidden VPNs or tunnels into the plant that bypass the path we want every remote session to use.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
