{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.8.35",
  "title": "Container Runtime Security Event Correlation",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Container Runtime Security Event Correlation' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_container_runtime_security_event_correlation` (UC-10.8.35), SOC dashboard panel \"Security Infra — Container Runtime Security Event Correlation\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.8.35 positive (Container, Runtime, Security, Event, Correlation): ingest synthetic events into `index=containers` sourcetype='kube:audit' where `count` grouped by `pod`, `namespace`, `user`, `action` satisfies `count` > 10 for 'Container Runtime Security Event Correlation'. Validate filter `(reason=\"PrivilegedContainers\" OR action=\"exec\" OR severity=`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.8.35 negative (Event Correlation): ingest benign `containers` / `kube:audit` events where `pod`, `namespace`, `user`, `action` stays below `count` > 10, or exclude the test entity via `soc_exception_register.csv`. Variant 12 confirms zero alert rows for 'Container Runtime Security Event Correlation'."
  },
  "dataSources": "Container runtime events, K8s audit log, image registry Collected via Splunk Connect for Kubernetes (Splunkbase 5167) modular input or scripted poll into the index named in the SPL.",
  "app": "Container security TAs, Kubernetes audit logs — Splunk Connect for Kubernetes (Splunkbase 5167)",
  "spl": "index=containers (sourcetype=\"kube:audit\" OR sourcetype=\"container:runtime\")\n| search (reason=\"PrivilegedContainers\" OR action=\"exec\" OR severity=\"high\")\n| stats count by pod, namespace, user, action\n| where count > 10\n| sort -count",
  "description": "Monitors UC-10.8.35 — Container, Runtime, Security, Event, Correlation — using `index=containers` sourcetype `kube:audit`, grouped by `pod`, `namespace`, `user`, `action`. Outputs `count` each `the scheduled window`; alert when `count` > 10. Filter chain includes filter `(reason=\"PrivilegedContainers\" OR action=\"exec\" OR severity=`. Purpose-built for 'Container Runtime Security Event Correlation' in Security Infra workflows.",
  "value": "Tracking `activity` via UC-10.8.35 on `kube:audit` feeds improves response to 'Container Runtime Security Event Correlation' and reduces missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Ingest container runtime and K8s audit logs. Alert on privileged pod creation, exec into production namespaces, or high-severity runtime events. Correlate with image provenance and network flows.",
  "visualization": "Table (security events by pod), Timeline (runtime events), Bar chart (events by namespace).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 5167",
      "url": "https://splunkbase.splunk.com/app/5167",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `containers` (UC-10.8.35)** — Planned work for 'Container Runtime Security Event Correlation' shifts `count` over `span=1h` while `pod`, `namespace`, `user`, `action` remains in-scope. Applies filter `(reason=\"PrivilegedContainers\" OR action=\"exec\" OR severity=`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `kube:audit`** — Scheduled automation elevates `namespace` without crossing `count` > 10 for UC-10.8.35. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `pod`, `namespace`, `user`, `action`** — Health checks in `containers` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `kube:audit`** — First 48h baseline for UC-10.8.35 on `pod` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `pod`, `exception_reason`, `valid_until`.",
  "equipment": [
    "kubernetes"
  ],
  "equipmentModels": [
    "kubernetes_k8s"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Container security TAs, Kubernetes audit logs.\n• Ensure the following data sources are available: Container runtime events, K8s audit log, image registry.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest container runtime and K8s audit logs. Alert on privileged pod creation, exec into production namespaces, or high-severity runtime events. Correlate with image provenance and network flows.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=containers (sourcetype=\"kube:audit\" OR sourcetype=\"container:runtime\")\n| search (reason=\"PrivilegedContainers\" OR action=\"exec\" OR severity=\"high\")\n| stats count by pod, namespace, user, action\n| where count > 10\n| sort -count\n```\n\nUnderstanding this SPL\n\n**Container Runtime Security Event Correlation** — Container escape or privileged container abuse can lead to host compromise. Correlating runtime events with image and orchestrator data improves detection of container-based attacks.\n\nDocumented **Data sources**: Container runtime events, K8s audit log, image registry. **App/TA** (typical add-on context): Container security TAs, Kubernetes audit logs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: containers; **sourcetype**: kube:audit, container:runtime. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=containers, sourcetype=\"kube:audit\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Applies an explicit `search` filter to narrow the current result set.\n• `stats` rolls up events into metrics; results are split **by pod, namespace, user, action** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where count > 10` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nCompare with certificate authority records, an inventory of endpoints, or `openssl x509 -text` on a sample file so expiry and subject data in Splunk match the source of truth.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (security events by pod), Timeline (runtime events), Bar chart (events by namespace).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Container security TAs, Kubernetes audit logs — Splunk Connect for Kubernetes (Splunkbase 5167)). Enable the modular input or HEC token that writes sourcetype=kube:audit, container:runtime into containers. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`action, namespace, pod, user`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Container escape or privileged container abuse can lead to host compromise — so we know when protection is off, out of date, or bypassed.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
