{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.6.5",
  "title": "Exploitable Vulnerability Prioritization",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Exploitable Vulnerability Prioritization' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_exploitable_vulnerability_prioritization` (UC-10.6.5), SOC dashboard panel \"Security Infra — Exploitable Vulnerability Prioritization\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.6.5 positive (Exploitable, Vulnerability, Prioritization): ingest synthetic events into `index=vulnerability` sourcetype='tenable:vuln' where `count` grouped by `entity` satisfies `epss_score` > 0.5 for 'Exploitable Vulnerability Prioritization'. Validate lookup `cisa_kev.csv`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.6.5 negative (Vulnerability Prioritization): ingest benign `vulnerability` / `tenable:vuln` events where `entity` stays below `epss_score` > 0.5, or exclude the test entity via `soc_exception_register.csv`. Variant 15 confirms zero alert rows for 'Exploitable Vulnerability Prioritization'."
  },
  "dataSources": "Scan results + CISA KEV catalog + EPSS scores Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Vuln management TA + threat intel — Universal Forwarder (Splunkbase 617)",
  "spl": "index=vulnerability sourcetype=\"tenable:vuln\" severity=\"Critical\"\n| lookup cisa_kev.csv cve_id OUTPUT known_exploited, ransomware_associated\n| lookup epss_scores.csv cve_id OUTPUT epss_score\n| where known_exploited=\"Yes\" OR epss_score > 0.5\n| table hostname, cve_id, severity, epss_score, known_exploited, ransomware_associated\n| sort -epss_score",
  "description": "Monitors UC-10.6.5 — Exploitable, Vulnerability, Prioritization — using `index=vulnerability` sourcetype `tenable:vuln`, grouped by `entity`. Outputs `count` each `the scheduled window`; alert when `epss_score` > 0.5. Filter chain includes lookup `cisa_kev.csv`. Purpose-built for 'Exploitable Vulnerability Prioritization' in Security Infra workflows.",
  "value": "UC-10.6.5 operationalises 'Exploitable Vulnerability Prioritization' against `tenable:vuln` feeds so operators reduce missed threat campaigns, control regressions, and SOC SLA breaches without manual correlation.",
  "implementation": "Maintain CISA KEV and EPSS lookup tables (update weekly). Enrich vulnerability data with exploit intelligence. Prioritize vulnerabilities with known exploits and high EPSS scores. Alert immediately on new KEV vulnerabilities found in environment.",
  "visualization": "Table (exploitable vulns prioritized), Single value (KEV vulns in environment), Bar chart (EPSS distribution), Scatter plot (severity × EPSS).",
  "cimModels": [
    "Vulnerabilities"
  ],
  "cimSpl": "| tstats count from datamodel=Vulnerabilities.Vulnerabilities where Vulnerabilities.signature=\"*exploit*\" by Vulnerabilities.cve,Vulnerabilities.dest",
  "dataModelAcceleration": "Vulnerabilities.Vulnerabilities",
  "references": [
    {
      "title": "CIM: Vulnerabilities",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Vulnerabilities",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `vulnerability` (UC-10.6.5)** — Planned work for 'Exploitable Vulnerability Prioritization' shifts `count` over `span=1h` while `host` remains in-scope. Applies lookup `cisa_kev.csv`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `tenable:vuln`** — Scheduled automation elevates `src` without crossing `epss_score` > 0.5 for UC-10.6.5. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `vulnerability` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `tenable:vuln`** — First 48h baseline for UC-10.6.5 on `host` is unstable; join `cisa_kev.csv` prior to threshold.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "tenable"
  ],
  "equipmentModels": [
    "tenable_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Vuln management TA + threat intel.\n• Ensure the following data sources are available: Scan results + CISA KEV catalog + EPSS scores.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMaintain CISA KEV and EPSS lookup tables (update weekly). Enrich vulnerability data with exploit intelligence. Prioritize vulnerabilities with known exploits and high EPSS scores. Alert immediately on new KEV vulnerabilities found in environment.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=vulnerability sourcetype=\"tenable:vuln\" severity=\"Critical\"\n| lookup cisa_kev.csv cve_id OUTPUT known_exploited, ransomware_associated\n| lookup epss_scores.csv cve_id OUTPUT epss_score\n| where known_exploited=\"Yes\" OR epss_score > 0.5\n| table hostname, cve_id, severity, epss_score, known_exploited, ransomware_associated\n| sort -epss_score\n```\n\nUnderstanding this SPL\n\n**Exploitable Vulnerability Prioritization** — Not all vulnerabilities are equal — those with known exploits pose immediate risk. Prioritization focuses remediation on the highest-risk items.\n\nDocumented **Data sources**: Scan results + CISA KEV catalog + EPSS scores. **App/TA** (typical add-on context): Vuln management TA + threat intel. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: vulnerability; **sourcetype**: tenable:vuln. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=vulnerability, sourcetype=\"tenable:vuln\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Enriches events using `lookup` (lookup definition + optional OUTPUT fields).\n• Enriches events using `lookup` (lookup definition + optional OUTPUT fields).\n• Filters the current rows with `where known_exploited=\"Yes\" OR epss_score > 0.5` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Exploitable Vulnerability Prioritization**): table hostname, cve_id, severity, epss_score, known_exploited, ransomware_associated\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats count from datamodel=Vulnerabilities.Vulnerabilities where Vulnerabilities.signature=\"*exploit*\" by Vulnerabilities.cve,Vulnerabilities.dest\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Exploitable Vulnerability Prioritization** — Not all vulnerabilities are equal — those with known exploits pose immediate risk. Prioritization focuses remediation on the highest-risk items.\n\nDocumented **Data sources**: Scan results + CISA KEV catalog + EPSS scores. **App/TA** (typical add-on context): Vuln management TA + threat intel. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Vulnerabilities.Vulnerabilities` — enable acceleration for that model.\n• The `signature` match surfaces exploit-related findings; adjust the pattern if your CIM field maps vendor exploit flags differently.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare prioritized findings in Splunk with Tenable.io, Qualys VMDR, and with CISA KEV / NIST NVD and EPSS for the same CVEs. Reconcile lookup table refresh dates with alert spikes.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (exploitable vulns prioritized), Single value (KEV vulns in environment), Bar chart (EPSS distribution), Scatter plot (severity × EPSS).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Vuln management TA + threat intel — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=tenable:vuln into vulnerability. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`cve_id, epss_score, known_exploited, ransomware_associated`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We line up our scanner output with which issues are already being attacked in the wild so we fix what criminals would try first, not just what looks noisy on a list.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
