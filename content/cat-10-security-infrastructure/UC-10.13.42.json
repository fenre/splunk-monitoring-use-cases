{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.13.42",
  "title": "Configuration Drift Analysis",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Configuration Drift Analysis' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_configuration_drift_analysis` (UC-10.13.42), SOC dashboard panel \"Security Infra — Configuration Drift Analysis\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.13.42 positive (Configuration, Drift, Analysis): ingest synthetic events into `index=security` sourcetype='target_sourcetype' where `count` grouped by `object_id`, `user` satisfies `count` > 5 for 'Configuration Drift Analysis'. Validate lookup `snow_changes`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.13.42 negative (Drift Analysis): ingest benign `security` / `target_sourcetype` events where `object_id`, `user` stays below `count` > 5, or exclude the test entity via `soc_exception_register.csv`. Variant 13 confirms zero alert rows for 'Configuration Drift Analysis'."
  },
  "dataSources": "`object_id`, `object_attrs` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Change CIM, config mgmt — Universal Forwarder (Splunkbase 617)",
  "spl": "index=security tag=change change_type=update\n| stats count by object_id, user\n| where count>5\n| lookup snow_changes object_id OUTPUT ticket\n| where isnull(ticket)",
  "description": "Identifies UC-10.13.42 — Configuration, Drift, Analysis — using `index=security` sourcetype `configured sourcetype`, grouped by `object_id`, `user`. Outputs `count` each `the scheduled window`; alert when `count` > 5. Filter chain includes lookup `snow_changes`. Purpose-built for 'Configuration Drift Analysis' in Security Infra workflows.",
  "value": "UC-10.13.42 gives Security Infra teams earlier visibility into Drift Analysis through lookup `snow_changes`, lowering missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Integrate Ansible Tower job IDs as authorized change keys.",
  "visualization": "Table (drift objects), Bar chart.",
  "cimModels": [
    "Change"
  ],
  "cimSpl": "| tstats `summariesonly` count from datamodel=Change.All_Changes where All_Changes.change_type=\"update\" by All_Changes.object_id, All_Changes.user\n| where count>5",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Change.All_Changes` dataset after normalizing this feed to the model; otherwise the `cimSpl` tstats search may return no rows.",
  "references": [
    {
      "title": "CIM: Change",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Change",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `security` (UC-10.13.42)** — Planned work for 'Configuration Drift Analysis' shifts `count` over `span=1h` while `object_id`, `user` remains in-scope. Applies lookup `snow_changes`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `configured_sourcetype`** — Scheduled automation elevates `user` without crossing `count` > 5 for UC-10.13.42. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `object_id`, `user`** — Health checks in `security` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-10.13.42 on `object_id` is unstable; join `snow_changes` prior to threshold.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `object_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "ansible",
    "servicenow"
  ],
  "equipmentModels": [
    "ansible_generic",
    "servicenow_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Change CIM, config mgmt.\n• Ensure the following data sources are available: `object_id`, `object_attrs`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIntegrate Ansible Tower job IDs as authorized change keys.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=security tag=change change_type=update\n| stats count by object_id, user\n| where count>5\n| lookup snow_changes object_id OUTPUT ticket\n| where isnull(ticket)\n```\n\nUnderstanding this SPL\n\n**Configuration Drift Analysis** — Repeated `change_type=update` on same `object_id` without matching change record — drift vs. process.\n\nDocumented **Data sources**: `object_id`, `object_attrs`. **App/TA** (typical add-on context): Change CIM, config mgmt. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: security.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=security, tags. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by object_id, user** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where count>5` — typically the threshold or rule expression for this monitoring goal.\n• Enriches events using `lookup` (lookup definition + optional OUTPUT fields).\n• Filters the current rows with `where isnull(ticket)` — typically the threshold or rule expression for this monitoring goal.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` count from datamodel=Change.All_Changes where All_Changes.change_type=\"update\" by All_Changes.object_id, All_Changes.user\n| where count>5\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Configuration Drift Analysis** — Repeated `change_type=update` on same `object_id` without matching change record — drift vs. process.\n\nDocumented **Data sources**: `object_id`, `object_attrs`. **App/TA** (typical add-on context): Change CIM, config mgmt. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Change.All_Changes` — enable acceleration for that model.\n• Filters the current rows with `where count>5` — typically the threshold or rule expression for this monitoring goal.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nAccelerate the CIM data model used in the optional tstats path, then run both the primary and `cimSpl` searches over a week that includes a known good baseline. Spot-check a few rows and tune thresholds; confirm no permission or tagging gap is hiding part of the population.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (drift objects), Bar chart.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Change CIM, config mgmt — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into security. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`object_id, ticket, user`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use the same kind of check described for \"Configuration Drift Analysis\" once our data is shaped in a common, repeatable way across products. We help the team see trouble early, compare different tools fairly, and avoid building a one-off report for every vendor.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
