{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.15.2",
  "title": "Lateral Movement via Rare Destination Hosts (MLTK)",
  "criticality": "critical",
  "difficulty": "expert",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Lateral Movement via Rare Destination Hosts (MLTK)' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_lateral_movement_via_rare_destination_hosts_mltk` (UC-10.15.2), SOC dashboard panel \"Security Infra — Lateral Movement via Rare Destination Hosts (MLTK)\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.15.2 positive (Lateral, Movement, via, Rare, Destination): ingest synthetic events into `index=authentication` sourcetype='target_sourcetype' where `historical_dest_count` grouped by `_time`, `Authentication.user`, `Authentication.dest`, `user` satisfies detection threshold for 'Lateral Movement via Rare Destination Hosts (MLTK)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.15.2 negative (Hosts (MLTK)): ingest benign `authentication` / `target_sourcetype` events where `_time`, `Authentication.user`, `Authentication.dest`, `user` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 8 confirms zero alert rows for 'Lateral Movement via Rare Destination Hosts (MLTK)'."
  },
  "dataSources": "`index=main sourcetype=WinEventLog:Security` (EventCode=4624 Type 3/10), `index=network sourcetype=pan:traffic` Collected via Splunk Add-on for Microsoft Windows (Splunkbase 742) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Machine Learning Toolkit (MLTK), Splunk Enterprise Security (ES) — Splunk Add-on for Microsoft Windows (Splunkbase 742)",
  "spl": "| tstats `security_content_summariesonly` count FROM datamodel=Authentication\n    WHERE Authentication.action=\"success\" AND (Authentication.signature_id=4624)\n    BY _time span=1h, Authentication.user, Authentication.dest\n| `drop_dm_object_name(Authentication)`\n| stats earliest(_time) as first_seen latest(_time) as last_seen count by user, dest\n| join type=left user\n    [| tstats count FROM datamodel=Authentication WHERE Authentication.action=\"success\"\n        BY Authentication.user, Authentication.dest\n        | `drop_dm_object_name(Authentication)`\n        | stats dc(dest) as historical_dest_count values(dest) as historical_dests by user]\n| where NOT match(historical_dests, dest)\n| fit LogisticRegression fit_intercept=true \"is_lateral\" from count, historical_dest_count into lateral_movement_model\n| where 'predicted(is_lateral)' > 0.7\n| table first_seen, user, dest, count, historical_dest_count\n| sort -first_seen",
  "description": "Identifies UC-10.15.2 — Lateral, Movement, via, Rare, Destination — using datamodel `Authentication`, grouped by `_time`, `Authentication.user`, `Authentication.dest`, `user`. Outputs `historical_dest_count` each `1h`; alert when configured threshold. Primary focus: Hosts (MLTK). Purpose-built for 'Lateral Movement via Rare Destination Hosts (MLTK)' in Security Infra workflows.",
  "value": "UC-10.15.2 accelerates insight into Hosts (MLTK) ('Lateral Movement via Rare Destination Hosts (MLTK)') by tracking `historical_dest_count` on `Authentication` telemetry, reducing missed threat campaigns, control regressions, and SOC SLA breaches before spreadsheet checks hide regressions.",
  "implementation": "Maintain a rolling 90-day lookup of `user → dest` pairs using `outputlookup`. Compare each new authentication event against this baseline. First-seen pairs are candidate lateral movement. Enrich with ES asset data to prioritize connections to high-value targets (domain controllers, jump servers, database servers). Score with MLTK LogisticRegression or DensityFunction trained on known-good movement patterns. Feed results into RBA as risk events on both user and dest entities. Pair with T1021 and T1076 MITRE annotations for SOC triage context.",
  "visualization": "Network graph (user → dest with edge weight), Table (first-seen connections), Timeline (lateral movement sequence).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Enterprise Security",
      "url": "https://splunkbase.splunk.com/app/263",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Authentication",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Authentication",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **ESCU content update for UC-10.15.2** — Vendor macro `security_content_summariesonly` changes `historical_dest_count` on `_time`, `Authentication.user`, `Authentication.dest`, `user` for 'Lateral Movement via Rare Destination Hosts (MLTK)' without a real endpoint incident.  Compare Content Management `lastModified`.\n\n2. **TA field alias drift on `Authentication`** — Upgrade to the Endpoint datamodel TA renames ``_time`, `Authentication.user`, `Authentication.dest`, `user``; baseline ``historical_dest_count`` before re-enabling alerts.\n\n3. **Approved atomic test on `authentication`** — Red-team replays 'Lateral Movement via Rare Destination Hosts (MLTK)' detections from exercise hosts in `datamodel=Authentication` during scheduled pen-test window.\n\n4. **EDR bulk install on ``_time`, `Authentication.user`, `Authentication.dest`, `user``** — Mass ``historical_dest_count`` spike during agent rollout mimics 'Lateral Movement via Rare Destination Hosts (MLTK)'; suppress via `soc_exception_register.csv` keyed on deployment tag.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on ``_time`, `Authentication.user`, `Authentication.dest`, `user``, `search_name`, `valid_until`.",
  "mitreAttack": [
    "T1021",
    "T1076",
    "T1550"
  ],
  "detectionType": "user, dest",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk Enterprise Security"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Machine Learning Toolkit (MLTK), Splunk Enterprise Security (ES).\n• Ensure the following data sources are available: `index=main sourcetype=WinEventLog:Security` (EventCode=4624 Type 3/10), `index=network sourcetype=pan:traffic`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMaintain a rolling 90-day lookup of `user → dest` pairs using `outputlookup`. Compare each new authentication event against this baseline. First-seen pairs are candidate lateral movement. Enrich with ES asset data to prioritize connections to high-value targets (domain controllers, jump servers, database servers). Score with MLTK LogisticRegression or DensityFunction trained on known-good movement patterns. Feed results into RBA as risk events on both user and dest entities. Pair with T1021 and …\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\n| tstats `security_content_summariesonly` count FROM datamodel=Authentication\n    WHERE Authentication.action=\"success\" AND (Authentication.signature_id=4624)\n    BY _time span=1h, Authentication.user, Authentication.dest\n| `drop_dm_object_name(Authentication)`\n| stats earliest(_time) as first_seen latest(_time) as last_seen count by user, dest\n| join type=left user\n    [| tstats count FROM datamodel=Authentication WHERE Authentication.action=\"success\"\n        BY Authentication.user, Authentication.dest\n        | `drop_dm_object_name(Authentication)`\n        | stats dc(dest) as historical_dest_count values(dest) as historical_dests by user]\n| where NOT match(historical_dests, dest)\n| fit LogisticRegression fit_intercept=true \"is_lateral\" from count, historical_dest_count into lateral_movement_model\n| where predicted(is_lateral) > 0.7\n| table first_seen, user, dest, count, historical_dest_count\n| sort -first_seen\n```\n\nUnderstanding this SPL\n\n**Lateral Movement via Rare Destination Hosts (MLTK)** — Attackers moving laterally connect to hosts they have never accessed before. By modeling each user's typical destination set and flagging first-seen or statistically rare destinations, this detection catches post-compromise pivoting that static ACLs and signature rules cannot see.\n\nDocumented **Data sources**: `index=main sourcetype=WinEventLog:Security` (EventCode=4624 Type 3/10), `index=network sourcetype=pan:traffic`. **App/TA** (typical add-on context): Splunk Machine Learning Toolkit (MLTK), Splunk Enterprise Security (ES). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\n**Detection type** for this use case: user, dest — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Authentication` — enable acceleration for that model.\n• Invokes macro `drop_dm_object_name(Authentication)` — in Search, use the UI or expand to inspect the underlying SPL.\n• `stats` rolls up events into metrics; results are split **by user, dest** so each row reflects one combination of those dimensions.\n• Uses `tstats` against accelerated summaries for data model `Authentication` — enable acceleration for that model.\n• Filters the current rows with `where NOT match(historical_dests, dest)` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Lateral Movement via Rare Destination Hosts (MLTK)**): fit LogisticRegression fit_intercept=true \"is_lateral\" from count, historical_dest_count into lateral_movement_model\n• Filters the current rows with `where predicted(is_lateral) > 0.7` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Lateral Movement via Rare Destination Hosts (MLTK)**): table first_seen, user, dest, count, historical_dest_count\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nStep 3 — Validate\nCompare a handful of scored rows to **the MLTK experiment** that produced the model, or to a manual replay of the same SPL on a short window, and confirm `fit` / `apply` output matches. For side-by-side, compare notable or entity context to **the ES Incident Review** queue if you feed risk or tickets.\n\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Network graph (user → dest with edge weight), Table (first-seen connections), Timeline (lateral movement sequence).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Machine Learning Toolkit (MLTK), Splunk Enterprise Security (ES) — Splunk Add-on for Microsoft Windows (Splunkbase 742)). Enable the modular input or HEC token that writes sourcetype=see SPL into index=main. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`Authentication.dest, Authentication.user, _time, dest`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use baselines to flag rare host-to-host paths that can show lateral movement when someone hops outside their usual neighborhood.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
