{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.14.3",
  "title": "Default or Shared Credentials on OT Devices",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Default or Shared Credentials on OT Devices' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_default_or_shared_credentials_on_ot_devices` (UC-10.14.3), SOC dashboard panel \"Security Infra — Default or Shared Credentials on OT Devices\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.14.3 positive (Default, Shared, Credentials, Devices): ingest synthetic events into `index=ot_security` sourcetype='ics:auth' where `count` grouped by `src`, `dest_host`, `user`, `vendor` satisfies detection threshold for 'Default or Shared Credentials on OT Devices'. Validate lookup `ot_default_creds`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.14.3 negative (OT Devices): ingest benign `ot_security` / `ics:auth` events where `src`, `dest_host`, `user`, `vendor` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 7 confirms zero alert rows for 'Default or Shared Credentials on OT Devices'."
  },
  "dataSources": "`index=ot_security` `sourcetype=\"ics:auth\"` RADIUS/TACACS+, HMI/SCADA login logs",
  "app": "OT Security Add-on for Splunk (Splunkbase 5151)",
  "spl": "index=ot_security sourcetype=\"ics:auth\"\n| eval user=lower(trim(user))\n| lookup ot_default_creds user OUTPUT is_default\n| where is_default=1 OR match(user, \"admin|root|operator|guest\")\n| stats count by src, dest_host, user, vendor\n| sort - count",
  "description": "Alerts on UC-10.14.3 — Default, Shared, Credentials, Devices — using `index=ot_security` sourcetype `ics:auth`, grouped by `src`, `dest_host`, `user`, `vendor`. Outputs `count` each `the scheduled window`; alert when configured threshold. Filter chain includes lookup `ot_default_creds`. Purpose-built for 'Default or Shared Credentials on OT Devices' in Security Infra workflows.",
  "value": "UC-10.14.3 operationalises 'Default or Shared Credentials on OT Devices' against `ics:auth` feeds so operators reduce missed threat campaigns, control regressions, and SOC SLA breaches without manual correlation.",
  "implementation": "Integrate OT authentication logs with ES. Pair with password policy and account governance. Escalate repeated default-credential use referencing T0812.",
  "visualization": "Bar chart by user/asset, Timeline of default logins, Table with vendor and site.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "OT Security Add-on for Splunk",
      "url": "https://splunkbase.splunk.com/app/5151",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Authentication",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Authentication",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `ot_security` (UC-10.14.3)** — Planned work for 'Default or Shared Credentials on OT Devices' shifts `count` over `span=1h` while `src`, `dest_host`, `user`, `vendor` remains in-scope. Applies lookup `ot_default_creds`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `ics:auth`** — Scheduled automation elevates `dest_host` without crossing configured threshold for UC-10.14.3. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `src`, `dest_host`, `user`, `vendor`** — Health checks in `ot_security` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `ics:auth`** — First 48h baseline for UC-10.14.3 on `src` is unstable; join `ot_default_creds` prior to threshold.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `src`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk OT Security Add-on"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: OT Security Add-on for Splunk (Splunkbase 5151).\n• Ensure the following data sources are available: `index=ot_security` `sourcetype=\"ics:auth\"` RADIUS/TACACS+, HMI/SCADA login logs.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIntegrate OT authentication logs with ES. Pair with password policy and account governance. Escalate repeated default-credential use referencing T0812.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot_security sourcetype=\"ics:auth\"\n| eval user=lower(trim(user))\n| lookup ot_default_creds user OUTPUT is_default\n| where is_default=1 OR match(user, \"admin|root|operator|guest\")\n| stats count by src, dest_host, user, vendor\n| sort - count\n```\n\nUnderstanding this SPL\n\n**Default or Shared Credentials on OT Devices** — Surfaces authentication using vendor default or shared accounts on PLCs, RTUs, and HMIs (T0812), enabling trivial takeover and process manipulation.\n\nDocumented **Data sources**: `index=ot_security` `sourcetype=\"ics:auth\"` RADIUS/TACACS+, HMI/SCADA login logs. **App/TA** (typical add-on context): OT Security Add-on for Splunk (Splunkbase 5151). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot_security; **sourcetype**: ics:auth. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot_security, sourcetype=\"ics:auth\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **user** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Enriches events using `lookup` (lookup definition + optional OUTPUT fields).\n• Filters the current rows with `where is_default=1 OR match(user, \"admin|root|operator|guest\")` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by src, dest_host, user, vendor** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nStep 3 — Validate\nCompare results to the **OT Security** add-on views, the **OT asset** inventory, and any **engineering change** or CMMS record. When you send ot-data through **Nozomi**, match the same **Nozomi Guardian** alert. When you use **Cyber Vision** sourcetype `cisco:cybervision`, line up the same event in the **Cyber Vision** UI. For raw ICS **syslog**, match the sensor or PLC console for that minute.\n\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Bar chart by user/asset, Timeline of default logins, Table with vendor and site.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (OT Security Add-on for Splunk (Splunkbase 5151)). Enable the modular input or HEC token that writes sourcetype=ics:auth into ot_security. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`dest_host, is_default, src, user`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We look for default or shared logins on control gear so a generic password is not a skeleton key to the process.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
