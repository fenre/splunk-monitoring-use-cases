{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.16.12",
  "title": "Microsoft Defender Attack Surface Reduction (ASR) Rule Triggers from Event Hub",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Microsoft Defender Attack Surface Reduction (ASR) Rule Triggers from Event Hub' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_microsoft_defender_attack_surface_reduction_asr_` (UC-10.16.12), SOC dashboard panel \"Security Infra — Microsoft Defender Attack Surface Reduction (ASR) Rule Triggers from Event Hub\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.16.12 positive (Microsoft, Defender, Attack, Surface, Reduction): ingest synthetic events into `index=target_index` sourcetype='ms:defender:eventhub' where `count` grouped by `action`, `device` satisfies -24h for 'Microsoft Defender Attack Surface Reduction (ASR) Rule Triggers from Event Hub'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.16.12 negative (Event Hub): ingest benign `target_index` / `ms:defender:eventhub` events where `action`, `device` stays below -24h, or exclude the test entity via `soc_exception_register.csv`. Variant 12 confirms zero alert rows for 'Microsoft Defender Attack Surface Reduction (ASR) Rule Triggers from Event Hub'."
  },
  "dataSources": "Advanced hunting / device events ingested as `sourcetype=ms:defender:eventhub` (Azure Event Hub pipeline in Splunk Add-on for Microsoft Security)",
  "app": "Splunk Add-on for Microsoft Security (Splunkbase 6207), Azure Event Hub",
  "spl": "index=* sourcetype=\"ms:defender:eventhub\" earliest=-24h\n| eval action=coalesce(ActionType, action_type, \"\")\n| where like(action, \"%Asr%\") OR like(lower(action), \"%attack surface reduction%\")\n| eval device=coalesce(DeviceName, deviceName, host, dest)\n| stats count by action, device\n| sort -count",
  "description": "Alerts on UC-10.16.12 — Microsoft, Defender, Attack, Surface, Reduction — using `index=target_index` sourcetype `ms:defender:eventhub`, grouped by `action`, `device`. Outputs `count` each `the scheduled window`; alert when -24h. Primary focus: Event Hub. Purpose-built for 'Microsoft Defender Attack Surface Reduction (ASR) Rule Triggers from Event Hub' in Security Infra workflows.",
  "value": "Tracking `activity` via UC-10.16.12 on `ms:defender:eventhub` feeds improves response to 'Microsoft Defender Attack Surface Reduction (ASR) Rule Triggers from Event Hub' and reduces missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Configure the Event Hub modular input for Microsoft Defender advanced hunting per add-on documentation. Ensure ActionType is extracted; some builds nest fields under JSON—use `spath` if needed. Create lookups for approved ASR exclusions and subtract known-good paths. Start in audit mode baselines before alerting on block noise.",
  "visualization": "Bar chart (top ASR ActionType values), table (device × rule), timeline of trigger volume.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Add-on for Microsoft Security",
      "url": "https://splunkbase.splunk.com/app/6207",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Source types — Splunk Add-on for Microsoft Security",
      "url": "https://splunk.github.io/splunk-add-on-for-microsoft-365-defender/Sourcetypes/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `target_index` (UC-10.16.12)** — Planned work for 'Microsoft Defender Attack Surface Reduction (ASR) Rule Triggers from Event Hub' shifts `count` over `span=1h` while `action`, `device` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `ms:defender:eventhub`** — Scheduled automation elevates `device` without crossing configured threshold for UC-10.16.12. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `action`, `device`** — Health checks in `target_index` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `ms:defender:eventhub`** — First 48h baseline for UC-10.16.12 on `action` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `action`, `exception_reason`, `valid_until`.",
  "equipment": [
    "azure",
    "defender"
  ],
  "equipmentModels": [
    "azure_generic",
    "defender_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for Microsoft Security (Splunkbase 6207), Azure Event Hub). Enable the modular input or HEC token that writes sourcetype=ms:defender:eventhub into index=main. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`action, device`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We trend Attack Surface Reduction and related client blocks from the event stream so a policy wave or a noisy app shows up in the data.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
