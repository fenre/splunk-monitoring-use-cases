{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.1.5",
  "title": "SSL Decryption Coverage",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'SSL Decryption Coverage' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_ssl_decryption_coverage` (UC-10.1.5), SOC dashboard panel \"Security Infra — SSL Decryption Coverage\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=pan` sourcetype='pan:traffic' where `entity` satisfies detection threshold for UC-10.1.5 ('SSL Decryption Coverage'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `pan` / `pan:traffic` events where `entity` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv` with a valid window. Confirm zero alert rows for UC-10.1.5."
  },
  "dataSources": "Decryption statistics, traffic logs (encrypted vs decrypted flags) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_paloalto` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=pan sourcetype=\"pan:traffic\"\n| eval decrypted=if(flags LIKE \"%decrypt%\",1,0)\n| stats sum(decrypted) as decrypted_sessions, count as total_sessions\n| eval coverage_pct=round(decrypted_sessions/total_sessions*100,1)",
  "description": "Encrypted traffic that isn't inspected creates a blind spot. Measuring decryption coverage ensures security visibility. Searches `index=pan` with sourcetype `pan:traffic` for UC-10.1.5, aggregating `sum` by `entity` over `the scheduled window`. Alerts when grouped results cross configured threshold in the saved search. Gives Security Infra operators a repeatable signal for 'SSL Decryption Coverage' without manual log review.",
  "value": "Early 'SSL Decryption Coverage' detection reduces missed threat campaigns, control regressions, and SOC SLA breaches by giving Security Infra operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Enable decryption logging on NGFW. Track percentage of HTTPS traffic being decrypted. Identify exempted destinations and evaluate risk. Report coverage to security leadership. Target >80% coverage.",
  "visualization": "Single value (decryption coverage %), Pie chart (decrypted vs bypassed), Bar chart (top bypassed destinations).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk_TA_paloalto",
      "url": "https://splunkbase.splunk.com/app/2757",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Add-on for Palo Alto Networks (Splunkbase 4919)",
      "url": "https://splunkbase.splunk.com/app/4919",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Palo Alto Networks documentation",
      "url": "https://docs.paloaltonetworks.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Approved vulnerability scanner** — Scanner `src` triggers high-severity signatures for 'SSL Decryption Coverage'. Join `scanner_ips` before threshold.\n\n2. **Signature definition update** — Vendor content push reclassifies benign traffic. Compare threat_id first_seen with content release date.\n\n3. **Red-team emulation** — Atomic test traffic from approved exercise hosts.\n\n4. **Backup replication traffic** — Off-hours bulk transfer matches lateral-movement heuristics.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `src`, `threat_id`, `valid_until`.",
  "equipment": [
    "paloalto"
  ],
  "equipmentModels": [
    "paloalto_pan_firewall"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_paloalto`.\n• Ensure the following data sources are available: Decryption statistics, traffic logs (encrypted vs decrypted flags).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnable decryption logging on NGFW. Track percentage of HTTPS traffic being decrypted. Identify exempted destinations and evaluate risk. Report coverage to security leadership. Target >80% coverage.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=pan sourcetype=\"pan:traffic\"\n| eval decrypted=if(flags LIKE \"%decrypt%\",1,0)\n| stats sum(decrypted) as decrypted_sessions, count as total_sessions\n| eval coverage_pct=round(decrypted_sessions/total_sessions*100,1)\n```\n\nUnderstanding this SPL\n\n**SSL Decryption Coverage** — Encrypted traffic that isn't inspected creates a blind spot. Measuring decryption coverage ensures security visibility.\n\nDocumented **Data sources**: Decryption statistics, traffic logs (encrypted vs decrypted flags). **App/TA** (typical add-on context): `Splunk_TA_paloalto`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: pan; **sourcetype**: pan:traffic. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=pan, sourcetype=\"pan:traffic\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **decrypted** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` aggregates the pipeline (counts, distinct values, sums, percentiles, etc.) into fewer rows.\n• `eval` defines or adjusts **coverage_pct** — often to normalize units, derive a ratio, or prepare for thresholds.\n\n\nStep 3 — Validate\nCompare with Panorama or the NGFW management console (Monitor → logs) for the same time window, then confirm field extractions and index permissions in Splunk.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Single value (decryption coverage %), Pie chart (decrypted vs bypassed), Bar chart (top bypassed destinations).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_paloalto` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=pan:traffic into pan. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`decrypted`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We measure how much traffic the firewall decrypts and inspects so we are not blind to threats inside encrypted sessions.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
