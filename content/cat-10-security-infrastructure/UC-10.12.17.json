{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.12.17",
  "title": "HIPAA Transmission Security",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Security",
  "industry": "Healthcare",
  "exclusions": "Covers the detection logic documented for 'HIPAA Transmission Security' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_hipaa_transmission_security` (UC-10.12.17), SOC dashboard panel \"Security Infra — HIPAA Transmission Security\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "compliance": [
    {
      "regulation": "hipaa-security",
      "version": "2013-final",
      "clause": "§164.312(e)",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Auto-generated from legacy markdown 'Regulations' line during the per-UC migration. Requires SME review to fill in version, clause, mode, and assurance for HIPAA.",
      "controlObjective": "Evidence that HIPAA unknown is enforced — Splunk UC-10.12.17: HIPAA Transmission Security.",
      "evidenceArtifact": "Saved search 'UC-10.12.17' running on sourcetype=\"lb:access\", sourcetype=\"zeek:ssl\", archived to the restricted audit_evidence index (default 7-year retention). Auto-drafted — SME review required.",
      "obligationRef": "hipaa-security@2013-final#§164.312(e)",
      "requires_sme_review": true,
      "provenance": "maintainer"
    }
  ],
  "controlTest": {
    "positiveScenario": "UC-10.12.17 positive (HIPAA, Transmission, Security): ingest synthetic events into `index=network` sourcetype='zeek:ssl' where `count` grouped by `src`, `dest`, `version`, `cipher` satisfies detection threshold for 'HIPAA Transmission Security'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.12.17 negative (Transmission Security): ingest benign `network` / `zeek:ssl` events where `src`, `dest`, `version`, `cipher` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 5 confirms zero alert rows for 'HIPAA Transmission Security'."
  },
  "dataSources": "`sourcetype=\"lb:access\"`, `sourcetype=\"zeek:ssl\"` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Load balancer, TLS inspection (where lawful) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=network sourcetype=\"zeek:ssl\" (server_name=\"*fhir*\" OR server_name=\"*ccd*\")\n| where version IN (\"TLSv1\",\"TLSv1.1\") OR match(cipher, \"RC4|DES\")\n| stats count by src, dest, version, cipher\n| sort -count",
  "description": "Surfaces 'HIPAA Transmission Security' patterns (UC-10.12.17) sourced from `index=network` sourcetype `zeek:ssl`. Threshold bundle: configured threshold; aggregation grain: `src`, `dest`, `version`, `cipher`; metrics: `count`. Watch terms: HIPAA, Transmission, Security.",
  "value": "UC-10.12.17 gives Security Infra teams earlier visibility into Transmission Security through `zeek:ssl` feeds, lowering missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Block weak ciphers at edge; exception process for legacy devices with compensating controls.",
  "visualization": "Table (weak sessions), Pie chart (TLS version mix), Trend (remediation progress).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable for the primary search: it relies on application- and industry-specific log formats, not a normalized CIM data model. Optional mapping to CIM, if you adopt it, is a separate follow-on project.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "HIPAA Security Rule — HHS",
      "url": "https://www.hhs.gov/hipaa/for-professionals/security/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `network` (UC-10.12.17)** — Planned work for 'HIPAA Transmission Security' shifts `count` over `span=1h` while `src`, `dest`, `version`, `cipher` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `zeek:ssl`** — Scheduled automation elevates `dest` without crossing configured threshold for UC-10.12.17. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `src`, `dest`, `version`, `cipher`** — Health checks in `network` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `zeek:ssl`** — First 48h baseline for UC-10.12.17 on `src` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `src`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Load balancer, TLS inspection (where lawful).\n• Ensure the following data sources are available: `sourcetype=\"lb:access\"`, `sourcetype=\"zeek:ssl\"`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nBlock weak ciphers at edge; exception process for legacy devices with compensating controls.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=network sourcetype=\"zeek:ssl\" (server_name=\"*fhir*\" OR server_name=\"*ccd*\")\n| where version IN (\"TLSv1\",\"TLSv1.1\") OR match(cipher, \"RC4|DES\")\n| stats count by src, dest, version, cipher\n| sort -count\n```\n\nUnderstanding this SPL\n\n**HIPAA Transmission Security** — Detects cleartext PHI on protocols or TLS versions below policy (e.g., TLS 1.0).\n\nDocumented **Data sources**: `sourcetype=\"lb:access\"`, `sourcetype=\"zeek:ssl\"`. **App/TA** (typical add-on context): Load balancer, TLS inspection (where lawful). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: network; **sourcetype**: zeek:ssl. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=network, sourcetype=\"zeek:ssl\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where version IN (\"TLSv1\",\"TLSv1.1\") OR match(cipher, \"RC4|DES\")` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by src, dest, version, cipher** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nRun the search on recent production data, spot-check a handful of results against the source system or operational runbooks (cash operations, EHR, payment gateway, etc.), and adjust thresholds to match your seasonal patterns and test windows so teams are not flooded with noise.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (weak sessions), Pie chart (TLS version mix), Trend (remediation progress).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Load balancer, TLS inspection (where lawful) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=zeek:ssl into network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`cipher, dest, src, version`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "In Healthcare, we pay close attention to \"HIPAA Transmission Security\" in our day-to-day work so that fraud, waste, and compliance issues do not slip by quietly. We help teams focus on the few situations that need a person, not every routine blip in the data.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
