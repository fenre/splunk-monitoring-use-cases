{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.10.4",
  "title": "Detection rule runtime performance",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Detection rule runtime performance' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_detection_rule_runtime_performance` (UC-10.10.4), SOC dashboard panel \"Security Infra — Detection rule runtime performance\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.10.4 positive (Detection, rule, runtime, performance): ingest synthetic events into `index=_internal` sourcetype='target_sourcetype' where `avg_run_time`, `max_run_time` grouped by `savedsearch_name` satisfies `avg_run_time` > 60, or `run_count` < 1 for 'Detection rule runtime performance'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.10.4 negative (runtime performance): ingest benign `_internal` / `target_sourcetype` events where `savedsearch_name` stays below `avg_run_time` > 60, or `run_count` < 1, or exclude the test entity via `soc_exception_register.csv`. Variant 7 confirms zero alert rows for 'Detection rule runtime performance'."
  },
  "dataSources": "_internal scheduler logs Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Enterprise Security / custom — Universal Forwarder (Splunkbase 617)",
  "spl": "index=_internal source=*scheduler.log* savedsearch_name=*\n| stats avg(run_time) as avg_run_time max(run_time) as max_run_time count as run_count by savedsearch_name\n| where avg_run_time>60 OR run_count<1\n| sort - avg_run_time",
  "description": "Detects UC-10.10.4 — Detection, rule, runtime, performance — using `index=_internal` sourcetype `configured sourcetype`, grouped by `savedsearch_name`. Outputs `avg_run_time`, `max_run_time` each `the scheduled window`; alert when `avg_run_time` > 60, or `run_count` < 1. Primary focus: runtime performance. Purpose-built for 'Detection rule runtime performance' in Security Infra workflows.",
  "value": "UC-10.10.4 operationalises 'Detection rule runtime performance' against UC-10.10.4 search results so operators reduce missed threat campaigns, control regressions, and SOC SLA breaches without manual correlation.",
  "implementation": "Normalize field names to your Splunk ES version and index names. Use summary indexing or ES introspection where `detection_metrics` is not present. For YARA, deploy a scripted input or HEC to ingest scanner JSON.",
  "visualization": "Table, Single value, Time chart",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Enterprise Security",
      "url": "https://splunkbase.splunk.com/app/263",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk ES Content Management",
      "url": "https://docs.splunk.com/Documentation/ES/latest/Admin/ContentManagement",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `_internal` (UC-10.10.4)** — Planned work for 'Detection rule runtime performance' shifts `avg_run_time`, `max_run_time` over `span=1h` while `savedsearch_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `configured_sourcetype`** — Scheduled automation elevates `src` without crossing `avg_run_time` > 60 for UC-10.10.4. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `savedsearch_name`** — Health checks in `_internal` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-10.10.4 on `savedsearch_name` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `savedsearch_name`, `exception_reason`, `valid_until`.",
  "detectionType": "Operational metrics",
  "securityDomain": "soc_operations",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk Enterprise Security"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Enterprise Security / custom.\n• Ensure the following data sources are available: _internal scheduler logs.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nNormalize field names to your Splunk ES version and index names. Use summary indexing or ES introspection where `detection_metrics` is not present. For YARA, deploy a scripted input or HEC to ingest scanner JSON.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=_internal source=*scheduler.log* savedsearch_name=*\n| stats avg(run_time) as avg_run_time max(run_time) as max_run_time count as run_count by savedsearch_name\n| where avg_run_time>60 OR run_count<1\n| sort - avg_run_time\n```\n\nUnderstanding this SPL\n\n**Detection rule runtime performance** — Measures search duration and skipped runs to find expensive or failing detections.\n\nDocumented **Data sources**: _internal scheduler logs. **App/TA** (typical add-on context): Splunk Enterprise Security / custom. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: _internal.\n\n**Detection type** for this use case: Operational metrics — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=_internal. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by savedsearch_name** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where avg_run_time>60 OR run_count<1` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nCompare run-times to the **Search > Job** inspector and scheduled search history for the same window; remove outliers caused by ad-hoc backfills or large look-ups.\n\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table, Single value, Time chart\n\nScripted input (generic example)\nThis use case relies on a scripted input. In the app's local/inputs.conf add a stanza such as:\n\n```ini\n[script://$SPLUNK_HOME/etc/apps/YourApp/bin/collect.sh]\ninterval = 300\nsourcetype = your_sourcetype\nindex = main\ndisabled = 0\n```\n\nThe script should print one event per line (e.g. key=value). Example minimal script (bash):\n\n```bash\n#!/usr/bin/env bash\n# Output metrics or events, one per line\necho \"metric=value timestamp=$(date +%s)\"\n```\n\nFor full details (paths, scheduling, permissions), see the Implementation guide: docs/implementation-guide.md\n",
  "grandmaExplanation": "We watch how long our searches run so heavy or fragile rules do not blind us when seconds matter.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
