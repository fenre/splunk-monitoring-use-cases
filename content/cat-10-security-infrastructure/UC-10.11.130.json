{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.11.130",
  "title": "Check Point HTTPS Inspection Certificate Errors",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Check Point HTTPS Inspection Certificate Errors' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_check_point_https_inspection_certificate_errors` (UC-10.11.130), SOC dashboard panel \"Security Infra — Check Point HTTPS Inspection Certificate Errors\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.11.130 positive (Check, Point, HTTPS, Inspection, Certificate): ingest synthetic events into `index=firewall` sourcetype='cp_log' where `count` grouped by `logdesc`, `src`, `dst`, `category` satisfies -7d for 'Check Point HTTPS Inspection Certificate Errors'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.11.130 negative (Certificate Errors): ingest benign `firewall` / `cp_log` events where `logdesc`, `src`, `dst`, `category` stays below -7d, or exclude the test entity via `soc_exception_register.csv`. Variant 5 confirms zero alert rows for 'Check Point HTTPS Inspection Certificate Errors'."
  },
  "dataSources": "`sourcetype=cp_log` (HTTPS inspection logs) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_checkpoint` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=firewall sourcetype=\"cp_log\" earliest=-7d\n| where match(lower(product),\"(?i)https.?inspection|ssl.?inspection\") AND match(lower(logdesc),\"(?i)cert.*error|untrusted|expired|revoked|mismatch|validation.*fail\")\n| stats count by logdesc, src, dst, category\n| sort -count",
  "description": "Detects 'Check Point HTTPS Inspection Certificate Errors' patterns (UC-10.11.130) sourced from `index=firewall` sourcetype `cp_log`. Threshold bundle: -7d; aggregation grain: `logdesc`, `src`, `dst`, `category`; metrics: `count`. Watch terms: Check, Point, HTTPS, Inspection, Certificate.",
  "value": "UC-10.11.130 gives Security Infra teams earlier visibility into Certificate Errors through `cp_log` feeds, lowering missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Enable detailed HTTPS inspection logging. Track certificate error types and destinations. Alert on errors involving the inspection CA certificate (affects all users). Report on bypass-causing certificate issues. Correlate with help desk tickets for SSL errors.",
  "visualization": "Bar chart (errors by type), Table (destinations with errors), Line chart (error trend), Single value (errors today).",
  "cimModels": [
    "Network_Traffic"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Network_Traffic.All_Traffic by All_Traffic.src | sort - count",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Network_Traffic.All_Traffic` dataset after normalizing this feed to the model; otherwise the `cimSpl` tstats search may return no rows.",
  "references": [
    {
      "title": "Splunk_TA_checkpoint",
      "url": "https://splunkbase.splunk.com/app/2646",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Security",
      "url": "https://splunkbase.splunk.com/app/263",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Network_Traffic",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Network_Traffic",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `firewall` (UC-10.11.130)** — Planned work for 'Check Point HTTPS Inspection Certificate Errors' shifts `count` over `span=1h` while `logdesc`, `src`, `dst`, `category` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `cp_log`** — Scheduled automation elevates `src` without crossing configured threshold for UC-10.11.130. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `logdesc`, `src`, `dst`, `category`** — Health checks in `firewall` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `cp_log`** — First 48h baseline for UC-10.11.130 on `logdesc` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `logdesc`, `exception_reason`, `valid_until`.",
  "detectionType": "Anomaly",
  "securityDomain": "network",
  "equipment": [
    "checkpoint"
  ],
  "equipmentModels": [
    "checkpoint_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_checkpoint`.\n• Ensure the following data sources are available: `sourcetype=cp_log` (HTTPS inspection logs).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnable detailed HTTPS inspection logging. Track certificate error types and destinations. Alert on errors involving the inspection CA certificate (affects all users). Report on bypass-causing certificate issues. Correlate with help desk tickets for SSL errors.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=firewall sourcetype=\"cp_log\" earliest=-7d\n| where match(lower(product),\"(?i)https.?inspection|ssl.?inspection\") AND match(lower(logdesc),\"(?i)cert.*error|untrusted|expired|revoked|mismatch|validation.*fail\")\n| stats count by logdesc, src, dst, category\n| sort -count\n```\n\nUnderstanding this SPL\n\n**Check Point HTTPS Inspection Certificate Errors** — HTTPS inspection replaces server certificates with gateway-signed versions. Certificate errors — expired CA, untrusted root, or client certificate validation failures — break inspection and may cause user-facing errors or silent bypass of security inspection.\n\nDocumented **Data sources**: `sourcetype=cp_log` (HTTPS inspection logs). **App/TA** (typical add-on context): `Splunk_TA_checkpoint`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: firewall; **sourcetype**: cp_log. That sourcetype matches what this use case lists under Data sources.\n\n**Detection type** for this use case: Anomaly — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=firewall, sourcetype=\"cp_log\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where match(lower(product),\"(?i)https.?inspection|ssl.?inspection\") AND match(lower(logdesc),\"(?i)cert.*error|untrusted|exp…` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by logdesc, src, dst, category** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats summariesonly=t count from datamodel=Network_Traffic.All_Traffic by All_Traffic.src | sort - count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Check Point HTTPS Inspection Certificate Errors** — HTTPS inspection replaces server certificates with gateway-signed versions. Certificate errors — expired CA, untrusted root, or client certificate validation failures — break inspection and may cause user-facing errors or silent bypass of security inspection.\n\nDocumented **Data sources**: `sourcetype=cp_log` (HTTPS inspection logs). **App/TA** (typical add-on context): `Splunk_TA_checkpoint`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Detection type** for this use case: Anomaly — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Network_Traffic.All_Traffic` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nRun the search over a window that includes both normal production traffic and, if you have one, a documented test (vendor sandbox, lab rule hit, or exercise). Compare a few sample events to the vendor’s own console or reports and tune indexes, fields, and thresholds so alerts are actionable before paging.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Bar chart (errors by type), Table (destinations with errors), Line chart (error trend), Single value (errors today).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_checkpoint` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=cp_log into firewall. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`category, dst, logdesc, src`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use Check Point data and alerts to stay ahead of the risks described in \"Check Point HTTPS Inspection Certificate Errors\". We help the team notice trouble early, while a situation is still small and containable.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
