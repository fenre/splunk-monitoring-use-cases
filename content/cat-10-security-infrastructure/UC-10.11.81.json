{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.11.81",
  "title": "Tanium Asset Discovery Reconciliation",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Tanium Asset Discovery Reconciliation' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_tanium_asset_discovery_reconciliation` (UC-10.11.81), SOC dashboard panel \"Security Infra — Tanium Asset Discovery Reconciliation\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.11.81 positive (Tanium, Asset, Discovery, Reconciliation): ingest synthetic events into `index=endpoint` sourcetype='tanium:asset' where `count` grouped by `computer_id` satisfies detection threshold for 'Tanium Asset Discovery Reconciliation'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.11.81 negative (Discovery Reconciliation): ingest benign `endpoint` / `tanium:asset` events where `computer_id` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 4 confirms zero alert rows for 'Tanium Asset Discovery Reconciliation'."
  },
  "dataSources": "`sourcetype=tanium:asset` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_tanium` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=endpoint sourcetype=\"tanium:asset\"\n| stats latest(last_seen) as ls by computer_id\n| join max=1 computer_id [| inputlookup cmdb_assets]\n| where isnull(cmdb_id)",
  "description": "Monitors risk tied to Tanium, Asset, Discovery, Reconciliation through UC-10.11.81 on `index=endpoint` sourcetype `tanium:asset`. Summarises `count` grouped by `computer_id` across `the scheduled window` before applying configured threshold. Operators tune 'Tanium Asset Discovery Reconciliation' using UC-10.11.81 runbook steps.",
  "value": "Tracking `activity` via UC-10.11.81 on `tanium:asset` feeds improves response to 'Tanium Asset Discovery Reconciliation' and reduces missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Compare Tanium inventory to CMDB. Investigate unmanaged IDs as rogue or stale.",
  "visualization": "Table (unreconciled), Single value (gap count).",
  "cimModels": [
    "Compute_Inventory"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Compute_Inventory.Virtual_OS by Virtual_OS.dest, Virtual_OS.status | sort - count",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Compute_Inventory.Virtual_OS` dataset after normalizing this feed to the model; otherwise the `cimSpl` tstats search may return no rows.",
  "references": [
    {
      "title": "CIM: Compute_Inventory",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Compute_Inventory",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `endpoint` (UC-10.11.81)** — Planned work for 'Tanium Asset Discovery Reconciliation' shifts `count` over `span=1h` while `computer_id` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `tanium:asset`** — Scheduled automation elevates `src` without crossing configured threshold for UC-10.11.81. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `computer_id`** — Health checks in `endpoint` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `tanium:asset`** — First 48h baseline for UC-10.11.81 on `computer_id` is unstable; join `cmdb_assets]` prior to threshold.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `computer_id`, `exception_reason`, `valid_until`.",
  "detectionType": "Hunting",
  "securityDomain": "endpoint",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_tanium`.\n• Ensure the following data sources are available: `sourcetype=tanium:asset`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nCompare Tanium inventory to CMDB. Investigate unmanaged IDs as rogue or stale.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=endpoint sourcetype=\"tanium:asset\"\n| stats latest(last_seen) as ls by computer_id\n| join max=1 computer_id [| inputlookup cmdb_assets]\n| where isnull(cmdb_id)\n```\n\nUnderstanding this SPL\n\n**Tanium Asset Discovery Reconciliation** — Reconciliation ensures every managed asset appears in authoritative inventories.\n\nDocumented **Data sources**: `sourcetype=tanium:asset`. **App/TA** (typical add-on context): `Splunk_TA_tanium`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: endpoint; **sourcetype**: tanium:asset. That sourcetype matches what this use case lists under Data sources.\n\n**Detection type** for this use case: Hunting — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=endpoint, sourcetype=\"tanium:asset\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by computer_id** so each row reflects one combination of those dimensions.\n• Joins to a subsearch with `join` — set `max=` to match cardinality and avoid silent truncation.\n• Filters the current rows with `where isnull(cmdb_id)` — typically the threshold or rule expression for this monitoring goal.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats summariesonly=t count from datamodel=Compute_Inventory.Virtual_OS by Virtual_OS.dest, Virtual_OS.status | sort - count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Tanium Asset Discovery Reconciliation** — Reconciliation ensures every managed asset appears in authoritative inventories.\n\nDocumented **Data sources**: `sourcetype=tanium:asset`. **App/TA** (typical add-on context): `Splunk_TA_tanium`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Detection type** for this use case: Hunting — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Compute_Inventory.Virtual_OS` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nRun the search over a window that includes both normal production traffic and, if you have one, a documented test (vendor sandbox, lab rule hit, or exercise). Compare a few sample events to the vendor’s own console or reports and tune indexes, fields, and thresholds so alerts are actionable before paging.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (unreconciled), Single value (gap count).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_tanium` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=tanium:asset into endpoint. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`computer_id`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use Tanium data and alerts to stay ahead of the risks described in \"Tanium Asset Discovery Reconciliation\". We help the team notice trouble early, while a situation is still small and containable.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
