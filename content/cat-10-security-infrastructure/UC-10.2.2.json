{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.2.2",
  "title": "Top Targeted Hosts",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Top Targeted Hosts' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_top_targeted_hosts` (UC-10.2.2), SOC dashboard panel \"Security Infra — Top Targeted Hosts\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=ids` sourcetype='snort:alert' that satisfy the UC-10.2.2 SPL filters for 'Top Targeted Hosts'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `ids` / `snort:alert` events outside the detection threshold, or exclude the test entity via `soc_exception_register.csv` with a valid window. Confirm zero alert rows for UC-10.2.2."
  },
  "dataSources": "IDS/IPS alert logs (destination host) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "IDS/IPS TA — Universal Forwarder (Splunkbase 617)",
  "spl": "index=ids sourcetype=\"snort:alert\" priority<=2\n| stats count, dc(signature) as unique_sigs by dest\n| sort 20 -count\n| head 20",
  "description": "Identifies the most-attacked internal hosts, prioritizing vulnerability remediation and incident investigation.",
  "value": "Early 'Top Targeted Hosts' detection reduces missed threat campaigns, control regressions, and SOC SLA breaches by giving Security Infra operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Parse destination IP from IDS alerts. Aggregate by target host. Enrich with CMDB data (asset owner, criticality). Alert when a single host receives multiple high-severity alerts. Trigger vulnerability scan for top targets.",
  "visualization": "Table (top targeted hosts), Bar chart (alerts by host), Geo map (source attackers).",
  "cimModels": [
    "Intrusion_Detection"
  ],
  "cimSpl": "| tstats `summariesonly` count dc(IDS_Attacks.signature) as sig_count\n  from datamodel=Intrusion_Detection.IDS_Attacks\n  by IDS_Attacks.dest span=1h\n| sort -count",
  "dataModelAcceleration": "Enable Data Model Acceleration for Intrusion_Detection.IDS_Attacks; dest and signature need correct aliases from your snort, suricata, or pan:threat feed before the `cimSpl` tstats is trustworthy.",
  "references": [
    {
      "title": "CIM: Intrusion_Detection",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Intrusion_Detection",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **SOC tuning or purple-team exercise** — Expected alert volume during rule validation for 'Top Targeted Hosts'. Match active CHG with `purple_team=true`.\n\n2. **Content deployment window** — TA or CIM update shifts field extractions on `index=ids` `snort:alert`.\n\n3. **Duplicate log ingestion** — HF/IDX misconfiguration doubles event counts.\n\n4. **Baseline seasonality** — Month-end batch jobs skew trending searches.\n\n**Suppression mechanism:** `soc_exception_register.csv` with entity, reason, valid_until.",
  "equipment": [
    "suricata"
  ],
  "equipmentModels": [
    "suricata_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: IDS/IPS TA.\n• Ensure the following data sources are available: IDS/IPS alert logs (destination host).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nParse destination IP from IDS alerts. Aggregate by target host. Enrich with CMDB data (asset owner, criticality). Alert when a single host receives multiple high-severity alerts. Trigger vulnerability scan for top targets.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ids sourcetype=\"snort:alert\" priority<=2\n| stats count, dc(signature) as unique_sigs by dest\n| sort 20 -count\n| head 20\n```\n\nUnderstanding this SPL\n\n**Top Targeted Hosts** — Identifies the most-attacked internal hosts, prioritizing vulnerability remediation and incident investigation.\n\nDocumented **Data sources**: IDS/IPS alert logs (destination host). **App/TA** (typical add-on context): IDS/IPS TA. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ids; **sourcetype**: snort:alert. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ids, sourcetype=\"snort:alert\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by dest** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n• Limits the number of rows with `head`.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` count dc(IDS_Attacks.signature) as sig_count\n  from datamodel=Intrusion_Detection.IDS_Attacks\n  by IDS_Attacks.dest span=1h\n| sort -count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Top Targeted Hosts** — Identifies the most-attacked internal hosts, prioritizing vulnerability remediation and incident investigation.\n\nDocumented **Data sources**: IDS/IPS alert logs (destination host). **App/TA** (typical add-on context): IDS/IPS TA. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Intrusion_Detection.IDS_Attacks` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare results with raw Snort or Suricata logs on the sensor, with Cisco Firepower Management Center, or with the IDS or IPS management console event view for the same time range. Confirm that severity and counts line up, then verify field extractions, tags, and index permissions in Splunk.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (top targeted hosts), Bar chart (alerts by host), Geo map (source attackers).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (IDS/IPS TA — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=snort:alert into ids. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`dest, priority`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We see which internal systems get the most high-priority network threat hits so we can patch, investigate, and calm down the noisiest assets first.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
