{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.11.92",
  "title": "Palo Alto Prisma Access Tunnel and Portal Health",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Palo Alto Prisma Access Tunnel and Portal Health' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_palo_alto_prisma_access_tunnel_and_portal_health` (UC-10.11.92), SOC dashboard panel \"Security Infra — Palo Alto Prisma Access Tunnel and Portal Health\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.11.92 positive (Palo, Alto, Prisma, Access, Tunnel): ingest synthetic events into `index=cloud` sourcetype='prisma:access' where `count` grouped by `location`, `user` satisfies detection threshold for 'Palo Alto Prisma Access Tunnel and Portal Health'. Validate filter `ts!=\"up\"`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.11.92 negative (Portal Health): ingest benign `cloud` / `prisma:access` events where `location`, `user` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 13 confirms zero alert rows for 'Palo Alto Prisma Access Tunnel and Portal Health'."
  },
  "dataSources": "`sourcetype=prisma:access` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_paloalto` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=cloud sourcetype=\"prisma:access\"\n| stats latest(tunnel_status) as ts by location, user\n| search ts!=\"up\"",
  "description": "Surfaces UC-10.11.92 — Palo, Alto, Prisma, Access, Tunnel — using `index=cloud` sourcetype `prisma:access`, grouped by `location`, `user`. Outputs `count` each `the scheduled window`; alert when configured threshold. Filter chain includes filter `ts!=\"up\"`. Purpose-built for 'Palo Alto Prisma Access Tunnel and Portal Health' in Security Infra workflows.",
  "value": "UC-10.11.92 gives Security Infra teams earlier visibility into Portal Health through `prisma:access` feeds, lowering missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Ingest Prisma Access telemetry. Correlate with ISP issues for SASE troubleshooting.",
  "visualization": "Table (down users), Map (locations).",
  "cimModels": [
    "Network_Sessions"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Network_Sessions.All_Sessions by All_Sessions.user | sort - count",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Network_Sessions.All_Sessions` dataset after normalizing this feed to the model; otherwise the `cimSpl` tstats search may return no rows.",
  "references": [
    {
      "title": "Splunk_TA_paloalto",
      "url": "https://splunkbase.splunk.com/app/2757",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Network_Sessions",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Network_Sessions",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `cloud` (UC-10.11.92)** — Planned work for 'Palo Alto Prisma Access Tunnel and Portal Health' shifts `count` over `span=1h` while `location`, `user` remains in-scope. Applies filter `ts!=\"up\"`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `prisma:access`** — Scheduled automation elevates `user` without crossing configured threshold for UC-10.11.92. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `location`, `user`** — Health checks in `cloud` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `prisma:access`** — First 48h baseline for UC-10.11.92 on `location` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `location`, `exception_reason`, `valid_until`.",
  "detectionType": "Anomaly",
  "securityDomain": "network",
  "equipment": [
    "nutanix",
    "paloalto"
  ],
  "equipmentModels": [
    "paloalto_pan_firewall",
    "paloalto_prisma"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_paloalto`.\n• Ensure the following data sources are available: `sourcetype=prisma:access`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest Prisma Access telemetry. Correlate with ISP issues for SASE troubleshooting.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=cloud sourcetype=\"prisma:access\"\n| stats latest(tunnel_status) as ts by location, user\n| search ts!=\"up\"\n```\n\nUnderstanding this SPL\n\n**Palo Alto Prisma Access Tunnel and Portal Health** — Prisma Access health secures cloud-delivered firewall and ZTNA for distributed users.\n\nDocumented **Data sources**: `sourcetype=prisma:access`. **App/TA** (typical add-on context): `Splunk_TA_paloalto`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: cloud; **sourcetype**: prisma:access. That sourcetype matches what this use case lists under Data sources.\n\n**Detection type** for this use case: Anomaly — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=cloud, sourcetype=\"prisma:access\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by location, user** so each row reflects one combination of those dimensions.\n• Applies an explicit `search` filter to narrow the current result set.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats summariesonly=t count from datamodel=Network_Sessions.All_Sessions by All_Sessions.user | sort - count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Palo Alto Prisma Access Tunnel and Portal Health** — Prisma Access health secures cloud-delivered firewall and ZTNA for distributed users.\n\nDocumented **Data sources**: `sourcetype=prisma:access`. **App/TA** (typical add-on context): `Splunk_TA_paloalto`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Detection type** for this use case: Anomaly — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Network_Sessions.All_Sessions` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nRun the search over a window that includes both normal production traffic and, if you have one, a documented test (vendor sandbox, lab rule hit, or exercise). Compare a few sample events to the vendor’s own console or reports and tune indexes, fields, and thresholds so alerts are actionable before paging.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (down users), Map (locations).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_paloalto` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=prisma:access into cloud. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`location, user`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use Prisma Access data and alerts to stay ahead of the risks described in \"Palo Alto Prisma Access Tunnel and Portal Health\". We help the team notice trouble early, while a situation is still small and containable.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
