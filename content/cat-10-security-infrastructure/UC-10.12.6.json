{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.12.6",
  "title": "Card-Not-Present Transaction Spikes",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Fraud"
  ],
  "splunkPillar": "Security",
  "industry": "Financial Services",
  "exclusions": "Covers the detection logic documented for 'Card-Not-Present Transaction Spikes' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_card_not_present_transaction_spikes` (UC-10.12.6), SOC dashboard panel \"Security Infra — Card-Not-Present Transaction Spikes\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.12.6 positive (Card, Not, Present, Transaction, Spikes): ingest synthetic events into `index=payments` sourcetype='gateway:txn' where `gmv`, `baseline`, `s` grouped by `entity` satisfies detection threshold for 'Card-Not-Present Transaction Spikes'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.12.6 negative (Transaction Spikes): ingest benign `payments` / `gateway:txn` events where `entity` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 2 confirms zero alert rows for 'Card-Not-Present Transaction Spikes'."
  },
  "dataSources": "`sourcetype=\"gateway:txn\"`, `card_present=\"false\"` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Gateway TA, e-commerce platform — Universal Forwarder (Splunkbase 617)",
  "spl": "index=payments sourcetype=\"gateway:txn\" card_present=\"false\"\n| timechart span=1h sum(amount) as gmv, count as txns\n| eventstats avg(txns) as baseline, stdev(txns) as s\n| eval z=(txns-baseline)/s\n| where abs(z)>3",
  "description": "Identifies UC-10.12.6 for Card, Not, Present, Transaction, Spikes over `index=payments` sourcetype `gateway:txn` with thresholds configured threshold. Groups `gmv`, `baseline`, `s` by `entity` each `1h`. Centered on Transaction Spikes indicators. Supports repeatable security infra triage for 'Card-Not-Present Transaction Spikes'.",
  "value": "For 'Card-Not-Present Transaction Spikes', UC-10.12.6 converts `gmv`, `baseline`, `s` on `gateway:txn` feeds into actionable rows and reduces missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Segment by merchant and MCC; seasonal baselines for retail peaks. Correlate with 3DS failures and chargeback velocity.",
  "visualization": "Line chart (CNP volume vs. baseline), Single value (Z-score), Table (merchants over threshold).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable for the primary search: it relies on application- and industry-specific log formats, not a normalized CIM data model. Optional mapping to CIM, if you adopt it, is a separate follow-on project.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `payments` (UC-10.12.6)** — Planned work for 'Card-Not-Present Transaction Spikes' shifts `gmv`, `baseline`, `s` over `span=1h` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `sum` on `gateway:txn`** — Scheduled automation elevates `z` without crossing configured threshold for UC-10.12.6. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `payments` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `gateway:txn`** — First 48h baseline for UC-10.12.6 on `host` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Gateway TA, e-commerce platform.\n• Ensure the following data sources are available: `sourcetype=\"gateway:txn\"`, `card_present=\"false\"`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nSegment by merchant and MCC; seasonal baselines for retail peaks. Correlate with 3DS failures and chargeback velocity.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=payments sourcetype=\"gateway:txn\" card_present=\"false\"\n| timechart span=1h sum(amount) as gmv, count as txns\n| eventstats avg(txns) as baseline, stdev(txns) as s\n| eval z=(txns-baseline)/s\n| where abs(z)>3\n```\n\nUnderstanding this SPL\n\n**Card-Not-Present Transaction Spikes** — E-commerce CNP spikes often precede credential stuffing or BIN attacks.\n\nDocumented **Data sources**: `sourcetype=\"gateway:txn\"`, `card_present=\"false\"`. **App/TA** (typical add-on context): Gateway TA, e-commerce platform. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: payments; **sourcetype**: gateway:txn. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=payments, sourcetype=\"gateway:txn\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `timechart` plots the metric over time using **span=1h** buckets — ideal for trending and alerting on this use case.\n• `eventstats` aggregates the pipeline (counts, distinct values, sums, percentiles, etc.) into fewer rows.\n• `eval` defines or adjusts **z** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where abs(z)>3` — typically the threshold or rule expression for this monitoring goal.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nRun the search on recent production data, spot-check a handful of results against the source system or operational runbooks (cash operations, EHR, payment gateway, etc.), and adjust thresholds to match your seasonal patterns and test windows so teams are not flooded with noise.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (CNP volume vs. baseline), Single value (Z-score), Table (merchants over threshold).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Gateway TA, e-commerce platform — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=gateway:txn into payments. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, user, action`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "In Financial Services, we pay close attention to \"Card-Not-Present Transaction Spikes\" in our day-to-day work so that fraud, waste, and compliance issues do not slip by quietly. We help teams focus on the few situations that need a person, not every routine blip in the data.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
