{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.5.1",
  "title": "Blocked Category Trending",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Blocked Category Trending' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_blocked_category_trending` (UC-10.5.1), SOC dashboard panel \"Security Infra — Blocked Category Trending\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=proxy` sourcetype='cisco:umbrella:proxy' that satisfy the UC-10.5.1 SPL filters for 'Blocked Category Trending'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `proxy` / `cisco:umbrella:proxy` events outside the detection threshold, or exclude the test entity via `soc_exception_register.csv` with a valid window. Confirm zero alert rows for UC-10.5.1."
  },
  "dataSources": "SWG/proxy logs (URL category, action) Collected via Splunk Add-on for Cisco Security (Splunkbase 1352) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Add-on for Cisco Umbrella, TA-zscaler — Splunk Add-on for Cisco Security (Splunkbase 1352)",
  "spl": "index=proxy sourcetype=\"cisco:umbrella:proxy\" action=\"Blocked\"\n| top limit=20 categories",
  "description": "Trending blocked categories reveals user behavior patterns and informs acceptable use policy. Spikes may indicate infections.",
  "value": "Early 'Blocked Category Trending' detection reduces missed threat campaigns, control regressions, and SOC SLA breaches by giving Security Infra operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Forward SWG logs to Splunk. Track blocked requests by category over time. Identify trending categories. Alert on spikes in malware/phishing categories. Report on policy effectiveness.",
  "visualization": "Bar chart (top blocked categories), Line chart (blocks over time), Pie chart (block distribution).",
  "cimModels": [
    "Web"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Web.Web\n  where Web.action=\"blocked\"\n  by Web.url Web.user Web.src span=1h\n| sort -count",
  "references": [
    {
      "title": "CIM: Web",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Web",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **SOC tuning or purple-team exercise** — Expected alert volume during rule validation for 'Blocked Category Trending'. Match active CHG with `purple_team=true`.\n\n2. **Content deployment window** — TA or CIM update shifts field extractions on `index=proxy` `cisco:umbrella:proxy`.\n\n3. **Duplicate log ingestion** — HF/IDX misconfiguration doubles event counts.\n\n4. **Baseline seasonality** — Month-end batch jobs skew trending searches.\n\n**Suppression mechanism:** `soc_exception_register.csv` with entity, reason, valid_until.",
  "equipment": [
    "cisco",
    "zscaler"
  ],
  "equipmentModels": [
    "cisco_generic",
    "zscaler_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Add-on for Cisco Umbrella, TA-zscaler.\n• Ensure the following data sources are available: SWG/proxy logs (URL category, action).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nForward SWG logs to Splunk. Track blocked requests by category over time. Identify trending categories. Alert on spikes in malware/phishing categories. Report on policy effectiveness.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=proxy sourcetype=\"cisco:umbrella\" action=\"Blocked\"\n| top limit=20 categories\n```\n\nUnderstanding this SPL\n\n**Blocked Category Trending** — Trending blocked categories reveals user behavior patterns and informs acceptable use policy. Spikes may indicate infections.\n\nDocumented **Data sources**: SWG/proxy logs (URL category, action). **App/TA** (typical add-on context): Splunk Add-on for Cisco Umbrella, TA-zscaler. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: proxy; **sourcetype**: cisco:umbrella. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=proxy, sourcetype=\"cisco:umbrella\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `top` shows the most frequent field values (limit with an explicit `limit=` if needed).\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` count\n  from datamodel=Web.Web\n  where Web.action=\"blocked\"\n  by Web.url Web.user Web.src span=1h\n| sort -count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Blocked Category Trending** — Trending blocked categories reveals user behavior patterns and informs acceptable use policy. Spikes may indicate infections.\n\nDocumented **Data sources**: SWG/proxy logs (URL category, action). **App/TA** (typical add-on context): Splunk Add-on for Cisco Umbrella, TA-zscaler. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Network_Traffic.All_Traffic` — enable acceleration for that model.\n• `eval` defines or adjusts **bytes** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare with the Zscaler ZIA admin portal (web security, blocks, users) for the same time range; then confirm field extractions and index permissions in Splunk.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Bar chart (top blocked categories), Line chart (blocks over time), Pie chart (block distribution).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for Cisco Umbrella, TA-zscaler — Splunk Add-on for Cisco Security (Splunkbase 1352)). Enable the modular input or HEC token that writes sourcetype=cisco:umbrella:proxy into proxy. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, user, action`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We track blocked web categories at the secure web gateway so policy gaps, user behavior, and attack spikes are visible.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
