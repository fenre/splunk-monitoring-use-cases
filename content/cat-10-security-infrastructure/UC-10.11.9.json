{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.11.9",
  "title": "FortiGate SSL Inspection Bypass",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'FortiGate SSL Inspection Bypass' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_fortigate_ssl_inspection_bypass` (UC-10.11.9), SOC dashboard panel \"Security Infra — FortiGate SSL Inspection Bypass\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.11.9 positive (FortiGate, SSL, Inspection, Bypass): ingest synthetic events into `index=firewall` sourcetype='fortinet:fortigate_ssl' where `count` grouped by `bypass_reason`, `policyid`, `user` satisfies detection threshold for 'FortiGate SSL Inspection Bypass'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.11.9 negative (Inspection Bypass): ingest benign `firewall` / `fortinet:fortigate_ssl` events where `bypass_reason`, `policyid`, `user` stays below detection threshold, or exclude the test entity via `soc_exception_register.csv`. Variant 4 confirms zero alert rows for 'FortiGate SSL Inspection Bypass'."
  },
  "dataSources": "`sourcetype=fortinet:fortigate_ssl` Collected via Splunk Add-on for Fortinet FortiGate (Splunkbase 2846) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_fortinet` — Splunk Add-on for Fortinet FortiGate (Splunkbase 2846)",
  "spl": "index=firewall sourcetype=\"fortinet:fortigate_ssl\" bypass_reason=*\n| stats count by bypass_reason, policyid, user\n| sort -count",
  "description": "Alerts on risk tied to FortiGate, SSL, Inspection, Bypass through UC-10.11.9 on `index=firewall` sourcetype `fortinet:fortigate_ssl`. Summarises `count` grouped by `bypass_reason`, `policyid`, `user` across `the scheduled window` before applying configured threshold. Operators tune 'FortiGate SSL Inspection Bypass' using UC-10.11.9 runbook steps.",
  "value": "Tracking `activity` via UC-10.11.9 on `fortinet:fortigate_ssl` feeds improves response to 'FortiGate SSL Inspection Bypass' and reduces missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Log SSL inspection decisions including bypass reasons (user, category, tech). Review for excessive bypasses that reduce visibility.",
  "visualization": "Bar chart (bypass reasons), Table (policy, user), Line chart (trend).",
  "cimModels": [
    "Network_Traffic"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Network_Traffic.All_Traffic by All_Traffic.action, All_Traffic.src, All_Traffic.dest, All_Traffic.dest_port | sort - count",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Network_Traffic.All_Traffic` dataset after normalizing this feed to the model; otherwise the `cimSpl` tstats search may return no rows.",
  "references": [
    {
      "title": "CIM: Network_Traffic",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Network_Traffic",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 2846",
      "url": "https://splunkbase.splunk.com/app/2846",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `firewall` (UC-10.11.9)** — Planned work for 'FortiGate SSL Inspection Bypass' shifts `count` over `span=1h` while `bypass_reason`, `policyid`, `user` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `fortinet:fortigate_ssl`** — Scheduled automation elevates `policyid` without crossing configured threshold for UC-10.11.9. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `bypass_reason`, `policyid`, `user`** — Health checks in `firewall` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `fortinet:fortigate_ssl`** — First 48h baseline for UC-10.11.9 on `bypass_reason` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `bypass_reason`, `exception_reason`, `valid_until`.",
  "detectionType": "Hunting",
  "securityDomain": "network",
  "equipment": [
    "fortinet"
  ],
  "equipmentModels": [
    "fortinet_fortigate"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_fortinet`.\n• Ensure the following data sources are available: `sourcetype=fortinet:fortigate_ssl`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nLog SSL inspection decisions including bypass reasons (user, category, tech). Review for excessive bypasses that reduce visibility.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=firewall sourcetype=\"fortinet:fortigate_ssl\" bypass_reason=*\n| stats count by bypass_reason, policyid, user\n| sort -count\n```\n\nUnderstanding this SPL\n\n**FortiGate SSL Inspection Bypass** — SSL bypass monitoring balances privacy with ensuring encrypted threats are still inspected where required.\n\nDocumented **Data sources**: `sourcetype=fortinet:fortigate_ssl`. **App/TA** (typical add-on context): `Splunk_TA_fortinet`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: firewall; **sourcetype**: fortinet:fortigate_ssl. That sourcetype matches what this use case lists under Data sources.\n\n**Detection type** for this use case: Hunting — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=firewall, sourcetype=\"fortinet:fortigate_ssl\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by bypass_reason, policyid, user** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats summariesonly=t count from datamodel=Network_Traffic.All_Traffic by All_Traffic.action, All_Traffic.src, All_Traffic.dest, All_Traffic.dest_port | sort - count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**FortiGate SSL Inspection Bypass** — SSL bypass monitoring balances privacy with ensuring encrypted threats are still inspected where required.\n\nDocumented **Data sources**: `sourcetype=fortinet:fortigate_ssl`. **App/TA** (typical add-on context): `Splunk_TA_fortinet`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Detection type** for this use case: Hunting — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Network_Traffic.All_Traffic` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nRun the search over a window that includes both normal production traffic and, if you have one, a documented test (vendor sandbox, lab rule hit, or exercise). Compare a few sample events to the vendor’s own console or reports and tune indexes, fields, and thresholds so alerts are actionable before paging.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Bar chart (bypass reasons), Table (policy, user), Line chart (trend).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_fortinet` — Splunk Add-on for Fortinet FortiGate (Splunkbase 2846)). Enable the modular input or HEC token that writes sourcetype=fortinet:fortigate_ssl into firewall. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`bypass_reason, policyid, user`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use FortiGate data and alerts to stay ahead of the risks described in \"FortiGate SSL Inspection Bypass\". We help the team notice trouble early, while a situation is still small and containable.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
