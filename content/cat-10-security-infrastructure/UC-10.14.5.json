{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.14.5",
  "title": "Data Historian Compromise Indicators",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Data Historian Compromise Indicators' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_data_historian_compromise_indicators` (UC-10.14.5), SOC dashboard panel \"Security Infra — Data Historian Compromise Indicators\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=ot_security` sourcetype='ics:historian' where `_time` satisfies `rows` > 10000 for UC-10.14.5 ('Data Historian Compromise Indicators'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `ot_security` / `ics:historian` events where `_time` stays below `rows` > 10000, or exclude the test entity via `soc_exception_register.csv` with a valid window. Confirm zero alert rows for UC-10.14.5."
  },
  "dataSources": "`index=ot_security` `sourcetype=\"ics:historian\"` DB/OS logs for historian hosts Indexes: ot_security. Sourcetypes: ics:historian. Key fields: _time, clients, historian_host, rows. Validate field extraction in Search before saving the alert; compare event counts to the vendor admin console.",
  "app": "OT Security Add-on for Splunk (Splunkbase 5151)",
  "spl": "index=ot_security sourcetype=\"ics:historian\"\n| bin _time span=1h\n| stats count sum(rows_returned) as rows dc(client_ip) as clients by _time, historian_host\n| eventstats avg(count) as avg_count by historian_host\n| where count>avg_count*3 OR rows>10000 OR clients>5\n| table _time, historian_host, count, rows, clients, avg_count",
  "description": "Detects anomalous queries, bulk exports, or new clients against historian servers (T0815), which can hide sabotage or exfiltrate operational data.",
  "value": "Early 'Data Historian Compromise Indicators' detection reduces missed threat campaigns, control regressions, and SOC SLA breaches by giving Security Infra operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Whitelist backup and reporting hosts. Create thresholds for off-hours bulk reads and new ODBC/JDBC clients. Tag with T0815 for threat intel alignment.",
  "visualization": "Timechart of query volume vs baseline, Scatter of rows vs clients, Table of anomalous source IPs.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "OT Security Add-on for Splunk",
      "url": "https://splunkbase.splunk.com/app/5151",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `ot_security`** — Planned work for 'Data Historian Compromise Indicators' shifts `sum` over `span=1h` while `_time` remains in-scope for UC-10.14.5. Cross-check CMDB CHG.\n\n2. **Batch `sum` on `ics:historian`** — Scheduled automation elevates `historian_host` without crossing `rows` > 10000. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `_time`** — Health checks in `ot_security` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `ics:historian`** — First 48h baseline for `_time` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `_time`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk OT Security Add-on"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: OT Security Add-on for Splunk (Splunkbase 5151).\n• Ensure the following data sources are available: `index=ot_security` `sourcetype=\"ics:historian\"` DB/OS logs for historian hosts.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nWhitelist backup and reporting hosts. Create thresholds for off-hours bulk reads and new ODBC/JDBC clients. Tag with T0815 for threat intel alignment.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot_security sourcetype=\"ics:historian\"\n| bin _time span=1h\n| stats count sum(rows_returned) as rows dc(client_ip) as clients by _time, historian_host\n| eventstats avg(count) as avg_count by historian_host\n| where count>avg_count*3 OR rows>10000 OR clients>5\n| table _time, historian_host, count, rows, clients, avg_count\n```\n\nUnderstanding this SPL\n\n**Data Historian Compromise Indicators** — Detects anomalous queries, bulk exports, or new clients against historian servers (T0815), which can hide sabotage or exfiltrate operational data.\n\nDocumented **Data sources**: `index=ot_security` `sourcetype=\"ics:historian\"` DB/OS logs for historian hosts. **App/TA** (typical add-on context): OT Security Add-on for Splunk (Splunkbase 5151). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot_security; **sourcetype**: ics:historian. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot_security, sourcetype=\"ics:historian\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by _time, historian_host** so each row reflects one combination of those dimensions.\n• `eventstats` rolls up events into metrics; results are split **by historian_host** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where count>avg_count*3 OR rows>10000 OR clients>5` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Data Historian Compromise Indicators**): table _time, historian_host, count, rows, clients, avg_count\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare results to the **OT Security** add-on views, the **OT asset** inventory, and any **engineering change** or CMMS record. When you send ot-data through **Nozomi**, match the same **Nozomi Guardian** alert. When you use **Cyber Vision** sourcetype `cisco:cybervision`, line up the same event in the **Cyber Vision** UI. For raw ICS **syslog**, match the sensor or PLC console for that minute.\n\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Timechart of query volume vs baseline, Scatter of rows vs clients, Table of anomalous source IPs.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (OT Security Add-on for Splunk (Splunkbase 5151)). Enable the modular input or HEC token that writes sourcetype=ics:historian into ot_security. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, clients, historian_host, rows`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch the historian for odd bulk reads or changes so someone is not siphoning process secrets or history they should not take.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
