{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.10.14",
  "title": "YARA scan performance monitoring",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'YARA scan performance monitoring' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_yara_scan_performance_monitoring` (UC-10.10.14), SOC dashboard panel \"Security Infra — YARA scan performance monitoring\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.10.14 positive (YARA, scan, performance, monitoring): ingest synthetic events into `index=yara` sourcetype='yara:scan' where `avg_ms`, `max_ms` grouped by `host` satisfies `avg_ms` > 600000 for 'YARA scan performance monitoring'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.10.14 negative (performance monitoring): ingest benign `yara` / `yara:scan` events where `host` stays below `avg_ms` > 600000, or exclude the test entity via `soc_exception_register.csv`. Variant 5 confirms zero alert rows for 'YARA scan performance monitoring'."
  },
  "dataSources": "YARA scanner performance fields Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom TA / HEC — Universal Forwarder (Splunkbase 617)",
  "spl": "index=yara sourcetype=yara:scan\n| stats avg(scan_duration_ms) as avg_ms perc95(scan_duration_ms) as p95_ms max(scan_duration_ms) as max_ms by host\n| where avg_ms>600000",
  "description": "Surfaces 'YARA scan performance monitoring' patterns (UC-10.10.14) sourced from `index=yara` sourcetype `yara:scan`. Threshold bundle: `avg_ms` > 600000; aggregation grain: `host`; metrics: `avg_ms`, `max_ms`. Watch terms: YARA, scan, performance, monitoring.",
  "value": "UC-10.10.14 gives Security Infra teams earlier visibility into performance monitoring through `yara:scan` feeds, lowering missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Normalize field names to your Splunk ES version and index names. Use summary indexing or ES introspection where `detection_metrics` is not present. For YARA, deploy a scripted input or HEC to ingest scanner JSON.",
  "visualization": "Table, Time chart, Single value",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `yara` (UC-10.10.14)** — Planned work for 'YARA scan performance monitoring' shifts `avg_ms`, `max_ms` over `span=1h` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `yara:scan`** — Scheduled automation elevates `src` without crossing `avg_ms` > 600000 for UC-10.10.14. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `yara` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `yara:scan`** — First 48h baseline for UC-10.10.14 on `host` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "detectionType": "Operational metrics",
  "securityDomain": "soc_operations",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Custom TA / HEC.\n• Ensure the following data sources are available: YARA scanner performance fields.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nNormalize field names to your Splunk ES version and index names. Use summary indexing or ES introspection where `detection_metrics` is not present. For YARA, deploy a scripted input or HEC to ingest scanner JSON.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=yara sourcetype=yara:scan\n| stats avg(scan_duration_ms) as avg_ms perc95(scan_duration_ms) as p95_ms max(scan_duration_ms) as max_ms by host\n| where avg_ms>600000\n```\n\nUnderstanding this SPL\n\n**YARA scan native OS monitoring toolsing** — Tracks scan duration and CPU to detect scanner health issues.\n\nDocumented **Data sources**: YARA scanner performance fields. **App/TA** (typical add-on context): Custom TA / HEC. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: yara; **sourcetype**: yara:scan. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Detection type** for this use case: Operational metrics — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=yara, sourcetype=yara:scan. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by host** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where avg_ms>600000` — typically the threshold or rule expression for this monitoring goal.\n\nStep 3 — Validate\nCompare run duration to **scanner CPU and IO** on a slow day, and to prior full-scan baselines, before opening a product ticket.\n\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table, Time chart, Single value\n\nScripted input (generic example)\nThis use case relies on a scripted input. In the app's local/inputs.conf add a stanza such as:\n\n```ini\n[script://$SPLUNK_HOME/etc/apps/YourApp/bin/collect.sh]\ninterval = 300\nsourcetype = your_sourcetype\nindex = main\ndisabled = 0\n```\n\nThe script should print one event per line (e.g. key=value). Example minimal script (bash):\n\n```bash\n#!/usr/bin/env bash\n# Output metrics or events, one per line\necho \"metric=value timestamp=$(date +%s)\"\n```\n\nFor full details (paths, scheduling, permissions), see the Implementation guide: docs/implementation-guide.md\n",
  "grandmaExplanation": "We watch scan run-time and success so a broken agent or full disk does not look like a clean bill of health.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
