{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.12.14",
  "title": "FIX Protocol Session Health",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Security",
  "industry": "Financial Services",
  "exclusions": "Covers the detection logic documented for 'FIX Protocol Session Health' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_fix_protocol_session_health` (UC-10.12.14), SOC dashboard panel \"Security Infra — FIX Protocol Session Health\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.12.14 positive (FIX, Protocol, Session, Health): ingest synthetic events into `index=trading` sourcetype='fix:session' where `count` grouped by `SenderCompID`, `TargetCompID` satisfies `seq_gap` > 0 for 'FIX Protocol Session Health'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.12.14 negative (Session Health): ingest benign `trading` / `fix:session` events where `SenderCompID`, `TargetCompID` stays below `seq_gap` > 0, or exclude the test entity via `soc_exception_register.csv`. Variant 1 confirms zero alert rows for 'FIX Protocol Session Health'."
  },
  "dataSources": "`sourcetype=\"fix:session\"` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "QuickFIX / vendor bridge logs — Universal Forwarder (Splunkbase 617)",
  "spl": "index=trading sourcetype=\"fix:session\"\n| stats latest(MsgSeqNum) as last_in, latest(ExpectedSeqNum) as expected by SenderCompID, TargetCompID\n| eval seq_gap=abs(last_in-expected)\n| where seq_gap>0 OR LogonStatus!=\"LOGGED_ON\"\n| table SenderCompID, TargetCompID, LogonStatus, seq_gap",
  "description": "Detects Session Health activity in UC-10.12.14 ('FIX Protocol Session Health') from `index=trading` sourcetype `fix:session`. Rolls up `count` by `SenderCompID`, `TargetCompID` across `the scheduled window` and pages when `seq_gap` > 0. Covers FIX, Protocol, Session, Health. Security Infra operators treat UC-10.12.14 as the canonical signal.",
  "value": "Operators running UC-10.12.14 on `fix:session` feeds shorten triage for Session Health tied to 'FIX Protocol Session Health', lowering missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Alert on Logout, ResendRequest storms, or heartbeat miss (MsgType=0). Track test vs. prod sessions separately.",
  "visualization": "Status grid (sessions), Line chart (heartbeat RTT), Table (sequence gaps).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable for the primary search: it relies on application- and industry-specific log formats, not a normalized CIM data model. Optional mapping to CIM, if you adopt it, is a separate follow-on project.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `trading` (UC-10.12.14)** — Planned work for 'FIX Protocol Session Health' shifts `count` over `span=1h` while `SenderCompID`, `TargetCompID` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `fix:session`** — Scheduled automation elevates `TargetCompID` without crossing `seq_gap` > 0 for UC-10.12.14. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `SenderCompID`, `TargetCompID`** — Health checks in `trading` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `fix:session`** — First 48h baseline for UC-10.12.14 on `SenderCompID` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `SenderCompID`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: QuickFIX / vendor bridge logs.\n• Ensure the following data sources are available: `sourcetype=\"fix:session\"`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nAlert on Logout, ResendRequest storms, or heartbeat miss (MsgType=0). Track test vs. prod sessions separately.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=trading sourcetype=\"fix:session\"\n| stats latest(MsgSeqNum) as last_in, latest(ExpectedSeqNum) as expected by SenderCompID, TargetCompID\n| eval seq_gap=abs(last_in-expected)\n| where seq_gap>0 OR LogonStatus!=\"LOGGED_ON\"\n| table SenderCompID, TargetCompID, LogonStatus, seq_gap\n```\n\nUnderstanding this SPL\n\n**FIX Protocol Session Health** — FIX session drops or sequence gaps stop trading and breach venue SLAs.\n\nDocumented **Data sources**: `sourcetype=\"fix:session\"`. **App/TA** (typical add-on context): QuickFIX / vendor bridge logs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: trading; **sourcetype**: fix:session. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=trading, sourcetype=\"fix:session\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by SenderCompID, TargetCompID** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **seq_gap** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where seq_gap>0 OR LogonStatus!=\"LOGGED_ON\"` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **FIX Protocol Session Health**): table SenderCompID, TargetCompID, LogonStatus, seq_gap\n\n\nStep 3 — Validate\nRun the search on recent production data, spot-check a handful of results against the source system or operational runbooks (cash operations, EHR, payment gateway, etc.), and adjust thresholds to match your seasonal patterns and test windows so teams are not flooded with noise.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Status grid (sessions), Line chart (heartbeat RTT), Table (sequence gaps).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (QuickFIX / vendor bridge logs — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=fix:session into trading. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`SenderCompID, TargetCompID, seq_gap`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "In Financial Services, we pay close attention to \"FIX Protocol Session Health\" in our day-to-day work so that fraud, waste, and compliance issues do not slip by quietly. We help teams focus on the few situations that need a person, not every routine blip in the data.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
