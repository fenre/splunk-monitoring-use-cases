{
  "$schema": "../../schemas/uc.schema.json",
  "id": "10.11.70",
  "title": "Carbon Black Audit Log Analysis",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Carbon Black Audit Log Analysis' only. Does not supplant organization-wide SIEM playbooks for unrelated threat classes or regulatory attestation. Entities listed in `soc_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `security_infra_carbon_black_audit_log_analysis` (UC-10.11.70), SOC dashboard panel \"Security Infra — Carbon Black Audit Log Analysis\", and weekly export to `index=audit_evidence` sourcetype=csv:soc_controls.",
  "controlTest": {
    "positiveScenario": "UC-10.11.70 positive (Carbon, Black, Audit, Log, Analysis): ingest synthetic events into `index=audit` sourcetype='carbonblack:audit' where `count` grouped by `user`, `src` satisfies `count` > 10 for 'Carbon Black Audit Log Analysis'. Validate filter `(action=login_failed OR action=permission_denied)`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-10.11.70 negative (Log Analysis): ingest benign `audit` / `carbonblack:audit` events where `user`, `src` stays below `count` > 10, or exclude the test entity via `soc_exception_register.csv`. Variant 1 confirms zero alert rows for 'Carbon Black Audit Log Analysis'."
  },
  "dataSources": "`sourcetype=carbonblack:audit` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_vmware_carbonblack` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=audit sourcetype=\"carbonblack:audit\"\n| search (action=login_failed OR action=permission_denied)\n| stats count by user, src\n| where count>10",
  "description": "Monitors Log Analysis activity in UC-10.11.70 ('Carbon Black Audit Log Analysis') from `index=audit` sourcetype `carbonblack:audit`. Rolls up `count` by `user`, `src` across `the scheduled window` and pages when `count` > 10. Uses filter `(action=login_failed OR action=permission_denied)`. Security Infra operators treat UC-10.11.70 as the canonical signal.",
  "value": "Operators running UC-10.11.70 on `carbonblack:audit` feeds shorten triage for Log Analysis tied to 'Carbon Black Audit Log Analysis', lowering missed threat campaigns, control regressions, and SOC SLA breaches.",
  "implementation": "Ingest console and API audit logs. Alert on failed admin logins and API key abuse patterns.",
  "visualization": "Table (failed access), Timeline.",
  "cimModels": [
    "Authentication"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Authentication.Authentication by Authentication.user, Authentication.src | sort - count",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM `Authentication.Authentication` dataset after normalizing this feed to the model; otherwise the `cimSpl` tstats search may return no rows.",
  "references": [
    {
      "title": "CIM: Authentication",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Authentication",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `audit` (UC-10.11.70)** — Planned work for 'Carbon Black Audit Log Analysis' shifts `count` over `span=1h` while `user`, `src` remains in-scope. Applies filter `(action=login_failed OR action=permission_denied)`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `carbonblack:audit`** — Scheduled automation elevates `src` without crossing `count` > 10 for UC-10.11.70. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `user`, `src`** — Health checks in `audit` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `carbonblack:audit`** — First 48h baseline for UC-10.11.70 on `user` is unstable; window via `soc_exception_register.csv`.\n\n**Suppression mechanism:** `soc_exception_register.csv` keyed on `user`, `exception_reason`, `valid_until`.",
  "detectionType": "TTP",
  "securityDomain": "access",
  "equipment": [
    "vmware"
  ],
  "equipmentModels": [
    "vmware_ta_vmware"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_vmware_carbonblack`.\n• Ensure the following data sources are available: `sourcetype=carbonblack:audit`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest console and API audit logs. Alert on failed admin logins and API key abuse patterns.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=audit sourcetype=\"carbonblack:audit\"\n| search (action=login_failed OR action=permission_denied)\n| stats count by user, src\n| where count>10\n```\n\nUnderstanding this SPL\n\n**Carbon Black Audit Log Analysis** — Audit log analysis protects the EDR management plane from unauthorized access.\n\nDocumented **Data sources**: `sourcetype=carbonblack:audit`. **App/TA** (typical add-on context): `Splunk_TA_vmware_carbonblack`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: audit; **sourcetype**: carbonblack:audit. That sourcetype matches what this use case lists under Data sources.\n\n**Detection type** for this use case: TTP — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=audit, sourcetype=\"carbonblack:audit\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Applies an explicit `search` filter to narrow the current result set.\n• `stats` rolls up events into metrics; results are split **by user, src** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where count>10` — typically the threshold or rule expression for this monitoring goal.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats summariesonly=t count from datamodel=Authentication.Authentication by Authentication.user, Authentication.src | sort - count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Carbon Black Audit Log Analysis** — Audit log analysis protects the EDR management plane from unauthorized access.\n\nDocumented **Data sources**: `sourcetype=carbonblack:audit`. **App/TA** (typical add-on context): `Splunk_TA_vmware_carbonblack`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Detection type** for this use case: TTP — interpret thresholds and fields in that context.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Authentication.Authentication` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nRun the search over a window that includes both normal production traffic and, if you have one, a documented test (vendor sandbox, lab rule hit, or exercise). Compare a few sample events to the vendor’s own console or reports and tune indexes, fields, and thresholds so alerts are actionable before paging.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (failed access), Timeline.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_vmware_carbonblack` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=carbonblack:audit into audit. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`src, user`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use Carbon Black data and alerts to stay ahead of the risks described in \"Carbon Black Audit Log Analysis\". We help the team notice trouble early, while a situation is still small and containable.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
