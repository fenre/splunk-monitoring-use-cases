{
  "$schema": "../../schemas/uc.schema.json",
  "id": "16.4.10",
  "title": "Change Implementation Duration vs Plan",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Compliance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Change Implementation Duration vs Plan' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_change_implementation_duration_vs_plan` (UC-16.4.10), SOC dashboard panel \"Operations — Change Implementation Duration vs Plan\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-16.4.10 positive (Change, Implementation, Duration, Plan): ingest synthetic events into `index=itsm` sourcetype='snow:change_request' where `count` grouped by `entity` satisfies `planned_hrs` > 0, `actual_hrs` >= 0, or `overrun_pct` > 25 for 'Change Implementation Duration vs Plan'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-16.4.10 negative (vs Plan): ingest benign `itsm` / `snow:change_request` events where `entity` stays below `planned_hrs` > 0, `actual_hrs` >= 0, or `overrun_pct` > 25, or exclude the test entity via `ops_exception_register.csv`. Variant 14 confirms zero alert rows for 'Change Implementation Duration vs Plan'."
  },
  "dataSources": "`index=itsm` `sourcetype=\"snow:change_request\"` (`start_date`, `end_date`, `planned_start`, `planned_end`, `number`, `assignment_group`) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_snow` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=itsm sourcetype=\"snow:change_request\" state IN (\"Closed\",\"Implemented\") earliest=-60d\n| eval actual_hrs=round((end_date-start_date)/3600,2)\n| eval planned_hrs=round((planned_end-planned_start)/3600,2)\n| where planned_hrs>0 AND actual_hrs>=0\n| eval overrun_hrs=round(actual_hrs-planned_hrs,2)\n| eval overrun_pct=round(100*overrun_hrs/planned_hrs,1)\n| where overrun_pct>25\n| table number, short_description, assignment_group, planned_hrs, actual_hrs, overrun_pct\n| sort -overrun_pct",
  "description": "Alerts on UC-16.4.10 — Change, Implementation, Duration, Plan — using `index=itsm` sourcetype `snow:change_request`, grouped by `entity`. Outputs `count` each `the scheduled window`; alert when `planned_hrs` > 0, `actual_hrs` >= 0, or `overrun_pct` > 25. Primary focus: vs Plan. Purpose-built for 'Change Implementation Duration vs Plan' in Operations workflows.",
  "value": "Alerting on Change, Implementation, Duration, Plan through UC-16.4.10 reduces operational risk and delayed incident response by surfacing `activity` early.",
  "implementation": "(1) Convert string timestamps with `strptime` if your TA stores strings instead of epoch—mirror UC-16.4.2 field mappings; (2) Exclude emergency changes with documented extensions via a lookup; (3) Feed chronic overruns into release planning retrospectives.",
  "visualization": "Table (top overruns), Bar chart (overrun % by assignment group), Histogram (overrun distribution).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk_TA_snow",
      "url": "https://splunkbase.splunk.com/app/1928",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Change",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Change",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `itsm` (UC-16.4.10)** — Planned work for 'Change Implementation Duration vs Plan' shifts `count` over `span=1h` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `snow:change_request`** — Scheduled automation elevates `actual_hrs` without crossing `planned_hrs` > 0 for UC-16.4.10. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `itsm` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `snow:change_request`** — First 48h baseline for UC-16.4.10 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "servicenow"
  ],
  "equipmentModels": [
    "servicenow_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_snow`.\n• Ensure the following data sources are available: `index=itsm` `sourcetype=\"snow:change_request\"` (`start_date`, `end_date`, `planned_start`, `planned_end`, `number`, `assignment_group`).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\n(1) Convert string timestamps with `strptime` if your TA stores strings instead of epoch—mirror UC-16.4.2 field mappings; (2) Exclude emergency changes with documented extensions via a lookup; (3) Feed chronic overruns into release planning retrospectives.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=itsm sourcetype=\"snow:change_request\" state IN (\"Closed\",\"Implemented\") earliest=-60d\n| eval actual_hrs=round((end_date-start_date)/3600,2)\n| eval planned_hrs=round((planned_end-planned_start)/3600,2)\n| where planned_hrs>0 AND actual_hrs>=0\n| eval overrun_hrs=round(actual_hrs-planned_hrs,2)\n| eval overrun_pct=round(100*overrun_hrs/planned_hrs,1)\n| where overrun_pct>25\n| table number, short_description, assignment_group, planned_hrs, actual_hrs, overrun_pct\n| sort -overrun_pct\n```\n\nUnderstanding this SPL\n\n**Change Implementation Duration vs Plan** — Overruns against the approved implementation window increase collision risk and customer impact; comparing actual versus planned duration reinforces ITIL change scheduling and CAB commitments.\n\nDocumented **Data sources**: `index=itsm` `sourcetype=\"snow:change_request\"` (`start_date`, `end_date`, `planned_start`, `planned_end`, `number`, `assignment_group`). **App/TA** (typical add-on context): `Splunk_TA_snow`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: itsm; **sourcetype**: snow:change_request. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=itsm, sourcetype=\"snow:change_request\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **actual_hrs** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **planned_hrs** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where planned_hrs>0 AND actual_hrs>=0` — typically the threshold or rule expression for this monitoring goal.\n• `eval` defines or adjusts **overrun_hrs** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **overrun_pct** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where overrun_pct>25` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Change Implementation Duration vs Plan**): table number, short_description, assignment_group, planned_hrs, actual_hrs, overrun_pct\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats summariesonly=t count from datamodel=Change.All_Changes by All_Changes.action, All_Changes.object_category, All_Changes.user | sort - count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Change Implementation Duration vs Plan** — Overruns against the approved implementation window increase collision risk and customer impact; comparing actual versus planned duration reinforces ITIL change scheduling and CAB commitments.\n\nDocumented **Data sources**: `index=itsm` `sourcetype=\"snow:change_request\"` (`start_date`, `end_date`, `planned_start`, `planned_end`, `number`, `assignment_group`). **App/TA** (typical add-on context): `Splunk_TA_snow`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Change.All_Changes` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare with the ServiceNow change request list or form for the same records. Verify change numbers, states, and timing against our search. Use the same filters you expect operations to use in the list view.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (top overruns), Bar chart (overrun % by assignment group), Histogram (overrun distribution).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_snow` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=snow:change_request into itsm. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`actual_hrs, overrun_pct, planned_hrs`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We compare how long a change really took to what the plan said, so estimates, staffing, and customer comms get better next time.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
