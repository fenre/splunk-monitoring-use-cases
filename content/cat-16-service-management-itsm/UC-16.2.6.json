{
  "$schema": "../../schemas/uc.schema.json",
  "id": "16.2.6",
  "title": "CI Relationship Drift",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Configuration"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'CI Relationship Drift' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_ci_relationship_drift` (UC-16.2.6), SOC dashboard panel \"Operations — CI Relationship Drift\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-16.2.6 positive (Relationship, Drift): ingest synthetic events into `index=itsm` sourcetype='snow:cmdb_rel_ci' where `values` grouped by `parent`, `child`, `relationship_type` satisfies `change_days` > 1 for 'CI Relationship Drift'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-16.2.6 negative (Relationship Drift): ingest benign `itsm` / `snow:cmdb_rel_ci` events where `parent`, `child`, `relationship_type` stays below `change_days` > 1, or exclude the test entity via `ops_exception_register.csv`. Variant 0 confirms zero alert rows for 'CI Relationship Drift'."
  },
  "dataSources": "`sourcetype=snow:cmdb_rel_ci` or relationship table snapshots Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_snow` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=itsm sourcetype=\"snow:cmdb_rel_ci\" earliest=-7d@d\n| bucket _time span=1d\n| stats values(_time) as days by parent, child, relationship_type\n| eval change_days=mvcount(days)\n| where change_days>1\n| table parent, child, relationship_type, change_days",
  "description": "Identifies 'CI Relationship Drift' (UC-16.2.6) using `index=itsm` sourcetype `snow:cmdb_rel_ci` by aggregating `values` grouped by `parent`, `child`, `relationship_type` over `1d` and alerting when `change_days` > 1. Keywords: Relationship, Drift. Focuses on Relationship Drift behaviour so operations teams can triage UC-16.2.6 without ad-hoc log review.",
  "value": "UC-16.2.6 accelerates insight into Relationship Drift ('CI Relationship Drift') by tracking `activity` on `snow:cmdb_rel_ci` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Schedule a nightly `outputlookup cmdb_rel_baseline.csv` from `cmdb_rel_ci` and compare with `| diff` or `join` on parent+child+type for strict drift. The SPL above flags relationships with activity on multiple days in a week (churn). Alert on new parent/child pairs against a saved baseline lookup when available.",
  "visualization": "Table (drifted relationships), Single value (drift count), Timeline (relationship changes).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk_TA_snow",
      "url": "https://splunkbase.splunk.com/app/1928",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `itsm` (UC-16.2.6)** — Planned work for 'CI Relationship Drift' shifts `values` over `span=1d` while `parent`, `child`, `relationship_type` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `values` on `snow:cmdb_rel_ci`** — Scheduled automation elevates `child` without crossing `change_days` > 1 for UC-16.2.6. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `parent`, `child`, `relationship_type`** — Health checks in `itsm` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `snow:cmdb_rel_ci`** — First 48h baseline for UC-16.2.6 on `parent` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `parent`, `exception_reason`, `valid_until`.",
  "equipment": [
    "servicenow"
  ],
  "equipmentModels": [
    "servicenow_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_snow`.\n• Ensure the following data sources are available: `sourcetype=snow:cmdb_rel_ci` or relationship table snapshots.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nSchedule a nightly `outputlookup cmdb_rel_baseline.csv` from `cmdb_rel_ci` and compare with `| diff` or `join` on parent+child+type for strict drift. The SPL above flags relationships with activity on multiple days in a week (churn). Alert on new parent/child pairs against a saved baseline lookup when available.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=itsm sourcetype=\"snow:cmdb_rel_ci\" earliest=-7d@d\n| bucket _time span=1d\n| stats values(_time) as days by parent, child, relationship_type\n| eval change_days=mvcount(days)\n| where change_days>1\n| table parent, child, relationship_type, change_days\n```\n\nUnderstanding this SPL\n\n**CI Relationship Drift** — Detects when CI relationships change unexpectedly versus a baseline — supporting impact analysis integrity and unauthorized dependency changes.\n\nDocumented **Data sources**: `sourcetype=snow:cmdb_rel_ci` or relationship table snapshots. **App/TA** (typical add-on context): `Splunk_TA_snow`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: itsm; **sourcetype**: snow:cmdb_rel_ci. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=itsm, sourcetype=\"snow:cmdb_rel_ci\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by parent, child, relationship_type** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **change_days** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where change_days>1` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **CI Relationship Drift**): table parent, child, relationship_type, change_days\n\n\nStep 3 — Validate\nCompare with the ServiceNow configuration item or related CMDB view. Spot-check a few CIs, owners, and relationship fields, then confirm discovery dates if we measure freshness.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (drifted relationships), Single value (drift count), Timeline (relationship changes).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_snow` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=snow:cmdb_rel_ci into itsm. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`change_days, child, parent, relationship_type`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We notice when the real world and the book slowly disagree, so drift does not build until nobody trusts the data.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
