{
  "$schema": "../../schemas/uc.schema.json",
  "id": "16.2.23",
  "title": "Opsgenie Alert Fatigue — Repeated Close-Reopen Loops per Alias",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Operations",
    "Quality"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Opsgenie Alert Fatigue — Repeated Close-Reopen Loops per Alias' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_opsgenie_alert_fatigue_repeated_close_reopen_loo` (UC-16.2.23), SOC dashboard panel \"Operations — Opsgenie Alert Fatigue — Repeated Close-Reopen Loops per Alias\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-16.2.23 positive (Opsgenie, Alert, Fatigue, Repeated, Close): ingest synthetic events into `index=alerts` sourcetype='opsgenie:alert' where `action_dc` grouped by `alias`, `tinyId` satisfies `count` > 15, or `action_dc` >= 2 for 'Opsgenie Alert Fatigue — Repeated Close-Reopen Loops per Alias'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-16.2.23 negative (per Alias): ingest benign `alerts` / `opsgenie:alert` events where `alias`, `tinyId` stays below `count` > 15, or `action_dc` >= 2, or exclude the test entity via `ops_exception_register.csv`. Variant 6 confirms zero alert rows for 'Opsgenie Alert Fatigue — Repeated Close-Reopen Loops per Alias'."
  },
  "dataSources": "Opsgenie alert activity (`opsgenie:alert`) from OEC or integration logs Indexes: alerts. Sourcetypes: opsgenie:alert. Key fields: action_dc, alias, tinyId. Validate field extraction in Search before saving the alert; compare event counts to the vendor admin console.",
  "app": "Opsgenie for Splunk (Splunkbase 3759) forwarding alert JSON",
  "spl": "index=alerts sourcetype=\"opsgenie:alert\"\n| eval alias=coalesce(alias, message)\n| stats count, dc(action) as action_dc by alias, tinyId\n| where count>15 AND action_dc>=2\n| sort -count",
  "description": "Surfaces per Alias anomalies for UC-16.2.23 with window `the scheduled window` on `index=alerts` sourcetype `opsgenie:alert`. Combines `action_dc` by `alias`, `tinyId` and compares against `count` > 15, or `action_dc` >= 2. Mapped to keywords Opsgenie, Alert, Fatigue, Repeated, Close for dashboard routing.",
  "value": "Alerting on Opsgenie, Alert, Fatigue, Repeated, Close through UC-16.2.23 reduces operational risk and delayed incident response by surfacing `action_dc` early.",
  "implementation": "Include `action` field (Create, Close, Ack). Redact customer PII from `message`.",
  "visualization": "Bar chart (top aliases), Line chart (flap rate).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "PagerDuty App for Splunk",
      "url": "https://splunkbase.splunk.com/app/3013",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Opsgenie for Splunk",
      "url": "https://splunkbase.splunk.com/app/3759",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `alerts` (UC-16.2.23)** — Planned work for 'Opsgenie Alert Fatigue — Repeated Close-Reopen Loops per Alias' shifts `action_dc` over `span=1h` while `alias`, `tinyId` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `dc` on `opsgenie:alert`** — Scheduled automation elevates `tinyId` without crossing `count` > 15 for UC-16.2.23. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `alias`, `tinyId`** — Health checks in `alerts` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `opsgenie:alert`** — First 48h baseline for UC-16.2.23 on `alias` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `alias`, `exception_reason`, `valid_until`.",
  "equipment": [
    "hardware_bmc",
    "pagerduty"
  ],
  "equipmentModels": [
    "hardware_bmc_edac"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Install and configure: Opsgenie for Splunk (Splunkbase 3759) forwarding alert JSON.\n• Data: Opsgenie alert activity (`opsgenie:alert`) from OEC or integration logs.\n• For directory layout and inputs, see docs/implementation-guide.md.\n\nStep 1 — Configure data collection\nInclude `action` field (Create, Close, Ack). Redact customer PII from `message`.\n\nStep 2 — Create the search and alert\nRun the following search (then save as report or alert; tune time range and threshold):\n\n```spl\nindex=alerts sourcetype=\"opsgenie:alert\"\n| eval alias=coalesce(alias, message)\n| stats count, dc(action) as action_dc by alias, tinyId\n| where count>15 AND action_dc>=2\n| sort -count\n```\n\nUnderstanding this search\nFlapping integrations waste responder focus and distort MTTR metrics.\n\nStep 3 — Validate\nCompare with the PagerDuty incident timeline and service view for the same window. Reconcile a few incident numbers, state changes, and acknowledgement times. If anything disagrees, check export lag, time zone, and which API fields we map.\n\nStep 4 — Operationalize\nAdd to a dashboard, wire alert actions, and document ownership. Suggested view: Bar chart (top aliases), Line chart (flap rate).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Opsgenie for Splunk (Splunkbase 3759) forwarding alert JSON). Enable the modular input or HEC token that writes sourcetype=opsgenie:alert into alerts. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`action_dc, alias, tinyId`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We look for the same issue opening and closing in a loop, so we can fix a flaky monitor or a rule that will not let an issue rest.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
