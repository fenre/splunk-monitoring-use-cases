{
  "$schema": "../../schemas/uc.schema.json",
  "id": "16.1.30",
  "title": "Control-M Resource Pool Queue Depth Saturation",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity",
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Control-M Resource Pool Queue Depth Saturation' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_control_m_resource_pool_queue_depth_saturation` (UC-16.1.30), SOC dashboard panel \"Operations — Control-M Resource Pool Queue Depth Saturation\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-16.1.30 positive (Control, Resource, Pool, Queue, Depth): ingest synthetic events into `index=cicd` sourcetype='controlm:job' where `waiting_jobs` grouped by `controlm_server` satisfies `waiting_jobs` >= 10 for 'Control-M Resource Pool Queue Depth Saturation'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-16.1.30 negative (Depth Saturation): ingest benign `cicd` / `controlm:job` events where `controlm_server` stays below `waiting_jobs` >= 10, or exclude the test entity via `ops_exception_register.csv`. Variant 6 confirms zero alert rows for 'Control-M Resource Pool Queue Depth Saturation'."
  },
  "dataSources": "Control-M job status events including `resource_pool` and wait states Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Control-M/EM export with pool binding fields — Universal Forwarder (Splunkbase 617)",
  "spl": "index=cicd sourcetype=\"controlm:job\"\n| where status=\"Wait Resource\" OR status=\"Wait Host\"\n| stats dc(job_id) as waiting_jobs, values(resource_pool) as pools by controlm_server\n| where waiting_jobs>=10\n| sort -waiting_jobs",
  "description": "Identifies Depth Saturation anomalies for UC-16.1.30 with window `the scheduled window` on `index=cicd` sourcetype `controlm:job`. Combines `waiting_jobs` by `controlm_server` and compares against `waiting_jobs` >= 10. Mapped to keywords Control, Resource, Pool, Queue, Depth for dashboard routing.",
  "value": "Alerting on Control, Resource, Pool, Queue, Depth through UC-16.1.30 reduces operational risk and delayed incident response by surfacing `waiting_jobs` early.",
  "implementation": "Ensure `resource_pool` extracted from XML/JSON order definitions.",
  "visualization": "Single value (waiting_jobs), Bar chart (by pool).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "BMC Control-M Automation API overview",
      "url": "https://documents.bmc.com/supportu/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `cicd` (UC-16.1.30)** — Planned work for 'Control-M Resource Pool Queue Depth Saturation' shifts `waiting_jobs` over `span=1h` while `controlm_server` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `dc` on `controlm:job`** — Scheduled automation elevates `src` without crossing `waiting_jobs` >= 10 for UC-16.1.30. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `controlm_server`** — Health checks in `cicd` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `controlm:job`** — First 48h baseline for UC-16.1.30 on `controlm_server` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `controlm_server`, `exception_reason`, `valid_until`.",
  "equipment": [
    "controlm"
  ],
  "equipmentModels": [
    "controlm_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Install and configure: Control-M/EM export with pool binding fields.\n• Data: Control-M job status events including `resource_pool` and wait states.\n• For directory layout and inputs, see docs/implementation-guide.md.\n\nStep 1 — Configure data collection\nEnsure `resource_pool` extracted from XML/JSON order definitions.\n\nStep 2 — Create the search and alert\nRun the following search (then save as report or alert; tune time range and threshold):\n\n```spl\nindex=cicd sourcetype=\"controlm:job\"\n| where status=\"Wait Resource\" OR status=\"Wait Host\"\n| stats dc(job_id) as waiting_jobs, values(resource_pool) as pools by controlm_server\n| where waiting_jobs>=10\n| sort -waiting_jobs\n```\n\nUnderstanding this search\nResource pools backing up indicate host shortage or mis-sized concurrency limits.\n\nStep 3 — Validate\nCompare with the Control-M job plan and run history in the job scheduler. Reconcile job and folder names, start and end times, and queue or pool use for a few highlighted jobs.\n\nStep 4 — Operationalize\nAdd to a dashboard, wire alert actions, and document ownership. Suggested view: Single value (waiting_jobs), Bar chart (by pool).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Control-M/EM export with pool binding fields — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=controlm:job into cicd. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`controlm_server, waiting_jobs`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch pools and queues piling up, so we add capacity or spread work before one busy pipe blocks a whole class of jobs.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
