{
  "$schema": "../../schemas/uc.schema.json",
  "id": "16.5.2",
  "title": "Change Success Rate Trending",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Change Success Rate Trending' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_change_success_rate_trending` (UC-16.5.2), SOC dashboard panel \"Operations — Change Success Rate Trending\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-16.5.2 positive (Change, Success, Rate, Trending): ingest synthetic events into `index=itsm` sourcetype='snow:change_request' where `successes`, `med_qtr` grouped by `entity` satisfies -365d for 'Change Success Rate Trending'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-16.5.2 negative (Rate Trending): ingest benign `itsm` / `snow:change_request` events where `entity` stays below -365d, or exclude the test entity via `ops_exception_register.csv`. Variant 9 confirms zero alert rows for 'Change Success Rate Trending'."
  },
  "dataSources": "`index=itsm` `sourcetype=\"snow:change_request\"`; optional `sourcetype=jira:*` change records Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_snow` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=itsm sourcetype=\"snow:change_request\" earliest=-365d@d state=\"Closed\"\n| eval ok=if(match(lower(close_code),\"(?i)successful|success|complete\") OR match(lower(u_outcome),\"(?i)success\"),1,0)\n| timechart span=1w sum(ok) as successes count as total\n| eval success_pct=round(100*successes/nullif(total,0),1)\n| trendline sma8(success_pct) as success_pct_trend\n| eventstats median(success_pct) as med_qtr\n| eval vs_median=round(success_pct - med_qtr,1)",
  "description": "Surfaces UC-16.5.2 — Change, Success, Rate, Trending — using `index=itsm` sourcetype `snow:change_request`, grouped by `entity`. Outputs `successes`, `med_qtr` each `1w`; alert when -365d. Primary focus: Rate Trending. Purpose-built for 'Change Success Rate Trending' in Operations workflows.",
  "value": "Operators running UC-16.5.2 on `snow:change_request` feeds shorten triage for Rate Trending tied to 'Change Success Rate Trending', lowering operational risk and delayed incident response.",
  "implementation": "Normalize `close_code` and `u_outcome` from ServiceNow; treat emergency and standard changes consistently in scope. Exclude duplicate closure events by deduping on `number` and `sys_updated_on`. Align week boundaries to your CAB calendar. Alert when `success_pct` drops below target for two consecutive weeks or falls more than 10 points under the trailing quarterly median.",
  "visualization": "Line chart (weekly success % with trendline), Single value (rolling 13-week average), Table (worst assignment groups).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk_TA_snow",
      "url": "https://splunkbase.splunk.com/app/1928",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `itsm` (UC-16.5.2)** — Planned work for 'Change Success Rate Trending' shifts `successes`, `med_qtr` over `span=1w` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `sum` on `snow:change_request`** — Scheduled automation elevates `ok` without crossing configured threshold for UC-16.5.2. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `itsm` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `snow:change_request`** — First 48h baseline for UC-16.5.2 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "jira",
    "servicenow"
  ],
  "equipmentModels": [
    "jira_generic",
    "servicenow_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_snow`.\n• Ensure the following data sources are available: `index=itsm` `sourcetype=\"snow:change_request\"`; optional `sourcetype=jira:*` change records.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nNormalize `close_code` and `u_outcome` from ServiceNow; treat emergency and standard changes consistently in scope. Exclude duplicate closure events by deduping on `number` and `sys_updated_on`. Align week boundaries to your CAB calendar. Alert when `success_pct` drops below target for two consecutive weeks or falls more than 10 points under the trailing quarterly median.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=itsm sourcetype=\"snow:change_request\" earliest=-365d@d state=\"Closed\"\n| eval ok=if(match(lower(close_code),\"(?i)successful|success|complete\") OR match(lower(u_outcome),\"(?i)success\"),1,0)\n| timechart span=1w sum(ok) as successes count as total\n| eval success_pct=round(100*successes/nullif(total,0),1)\n| trendline sma8(success_pct) as success_pct_trend\n| eventstats median(success_pct) as med_qtr\n| eval vs_median=round(success_pct - med_qtr,1)\n```\n\nUnderstanding this SPL\n\n**Change Success Rate Trending** — A falling change success rate warns that testing, communication, or risk scoring is degrading before major outages; trending by week ties process fixes to measurable improvement.\n\nDocumented **Data sources**: `index=itsm` `sourcetype=\"snow:change_request\"`; optional `sourcetype=jira:*` change records. **App/TA** (typical add-on context): `Splunk_TA_snow`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: itsm; **sourcetype**: snow:change_request. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=itsm, sourcetype=\"snow:change_request\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **ok** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `timechart` plots the metric over time using **span=1w** buckets — ideal for trending and alerting on this use case.\n• `eval` defines or adjusts **success_pct** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Pipeline stage (see **Change Success Rate Trending**): trendline sma8(success_pct) as success_pct_trend\n• `eventstats` aggregates the pipeline (counts, distinct values, sums, percentiles, etc.) into fewer rows.\n• `eval` defines or adjusts **vs_median** — often to normalize units, derive a ratio, or prepare for thresholds.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare with the Jira issue page, board, or service project view for the same dates. Confirm issue keys, status, and assignment fields; reconcile timestamp fields you use in the report.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (weekly success % with trendline), Single value (rolling 13-week average), Table (worst assignment groups).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_snow` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=snow:change_request into itsm. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`ok`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We trend change outcomes over time, so we can see a culture or platform get safer—or slip—before a bad release week.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
