{
  "$schema": "../../schemas/uc.schema.json",
  "id": "16.2.18",
  "title": "Application CI Business Service Coverage",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Application CI Business Service Coverage' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_application_ci_business_service_coverage` (UC-16.2.18), SOC dashboard panel \"Operations — Application CI Business Service Coverage\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=itsm` sourcetype='snow:cmdb_ci' that satisfy the UC-16.2.18 SPL filters for 'Application CI Business Service Coverage'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `itsm` / `snow:cmdb_ci` events outside the detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-16.2.18."
  },
  "dataSources": "`index=itsm` `sourcetype=\"snow:cmdb_ci\"` (`ci_class`, `business_service`, `u_business_service`, `name`, `support_group`) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_snow` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=itsm sourcetype=\"snow:cmdb_ci\" earliest=-1d\n| where match(lower(ci_class),\"(?i)cmdb_ci_appl|application\")\n| eval bs=coalesce(business_service, u_business_service)\n| eval mapped=if(isnotnull(bs) AND bs!=\"\",1,0)\n| stats count as apps, sum(mapped) as mapped_apps by support_group\n| eval coverage_pct=round(100*mapped_apps/nullif(apps,0),1)\n| sort coverage_pct",
  "description": "Applications without a business service mapping weaken BIA-driven prioritization and incident impact statements; coverage metrics align the CMDB with ITIL service asset and configuration practices.",
  "value": "Early 'Application CI Business Service Coverage' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "(1) Adjust `ci_class` filters to your CMDB application table set; (2) Map whichever field holds the business service reference; (3) Set a minimum `apps` threshold before flagging low `coverage_pct` groups to avoid noise from tiny teams.",
  "visualization": "Bar chart (coverage % by support group), Table (unmapped application CIs), Gauge (overall coverage %).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk_TA_snow",
      "url": "https://splunkbase.splunk.com/app/1928",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'Application CI Business Service Coverage'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `itsm` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `ops_exception_register.csv` with entity, reason, valid_until.",
  "equipment": [
    "servicenow"
  ],
  "equipmentModels": [
    "servicenow_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_snow`.\n• Ensure the following data sources are available: `index=itsm` `sourcetype=\"snow:cmdb_ci\"` (`ci_class`, `business_service`, `u_business_service`, `name`, `support_group`).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\n(1) Adjust `ci_class` filters to your CMDB application table set; (2) Map whichever field holds the business service reference; (3) Set a minimum `apps` threshold before flagging low `coverage_pct` groups to avoid noise from tiny teams.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=itsm sourcetype=\"snow:cmdb_ci\" earliest=-1d\n| where match(lower(ci_class),\"(?i)cmdb_ci_appl|application\")\n| eval bs=coalesce(business_service, u_business_service)\n| eval mapped=if(isnotnull(bs) AND bs!=\"\",1,0)\n| stats count as apps, sum(mapped) as mapped_apps by support_group\n| eval coverage_pct=round(100*mapped_apps/nullif(apps,0),1)\n| sort coverage_pct\n```\n\nUnderstanding this SPL\n\n**Application CI Business Service Coverage** — Applications without a business service mapping weaken BIA-driven prioritization and incident impact statements; coverage metrics align the CMDB with ITIL service asset and configuration practices.\n\nDocumented **Data sources**: `index=itsm` `sourcetype=\"snow:cmdb_ci\"` (`ci_class`, `business_service`, `u_business_service`, `name`, `support_group`). **App/TA** (typical add-on context): `Splunk_TA_snow`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: itsm; **sourcetype**: snow:cmdb_ci. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=itsm, sourcetype=\"snow:cmdb_ci\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where match(lower(ci_class),\"(?i)cmdb_ci_appl|application\")` — typically the threshold or rule expression for this monitoring goal.\n• `eval` defines or adjusts **bs** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **mapped** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by support_group** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **coverage_pct** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nCompare with the ServiceNow configuration item or related CMDB view. Spot-check a few CIs, owners, and relationship fields, then confirm discovery dates if we measure freshness.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Bar chart (coverage % by support group), Table (unmapped application CIs), Gauge (overall coverage %).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_snow` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=snow:cmdb_ci into itsm. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`mapped, support_group`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We see which applications tie to which business service, so impact maps and priority lines stay true when we break or fix things.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
