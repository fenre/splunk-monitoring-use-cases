{
  "$schema": "../../schemas/uc.schema.json",
  "id": "16.3.11",
  "title": "Batch Job Schedule Compliance",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Operations"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Batch Job Schedule Compliance' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_batch_job_schedule_compliance` (UC-16.3.11), SOC dashboard panel \"Operations — Batch Job Schedule Compliance\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-16.3.11 positive (Batch, Job, Schedule, Compliance): ingest synthetic events into `index=batch` sourcetype='controlm:job' where `day_ok` grouped by `job_name`, `day` satisfies -7d for 'Batch Job Schedule Compliance'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-16.3.11 negative (Schedule Compliance): ingest benign `batch` / `controlm:job` events where `job_name`, `day` stays below -7d, or exclude the test entity via `ops_exception_register.csv`. Variant 10 confirms zero alert rows for 'Batch Job Schedule Compliance'."
  },
  "dataSources": "`sourcetype=controlm:job`, `sourcetype=autosys:job`, or `sourcetype=syslog` with scheduler tags Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "Control-M, Autosys, cron/syslog, mainframe SMF (custom) — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=batch sourcetype=\"controlm:job\" earliest=-7d@d\n| eval day=strftime(_time,\"%Y-%m-%d\")\n| eval ended_ok=if(match(lower(status),\"(?i)ended ok|success\"),1,0)\n| stats max(ended_ok) as day_ok by job_name, day\n| where day_ok=0\n| table job_name, day",
  "description": "Identifies UC-16.3.11 — Batch, Job, Schedule, Compliance — using `index=batch` sourcetype `controlm:job`, grouped by `job_name`, `day`. Outputs `day_ok` each `the scheduled window`; alert when -7d. Primary focus: Schedule Compliance. Purpose-built for 'Batch Job Schedule Compliance' in Operations workflows.",
  "value": "For 'Batch Job Schedule Compliance', UC-16.3.11 converts `day_ok` on `controlm:job` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Map vendor fields: `scheduled_time`, end status, job name. For cron, ingest start/stop lines and compare to `batch_schedule.csv` lookup (job, expected cron, max duration). Alert when `ran_ok=0` for a calendar day. Tune time zones.",
  "visualization": "Table (missed jobs), Calendar (job success by day), Line chart (miss rate trend).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 833",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `batch` (UC-16.3.11)** — Planned work for 'Batch Job Schedule Compliance' shifts `day_ok` over `span=1h` while `job_name`, `day` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `max` on `controlm:job`** — Scheduled automation elevates `day` without crossing configured threshold for UC-16.3.11. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `job_name`, `day`** — Health checks in `batch` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `controlm:job`** — First 48h baseline for UC-16.3.11 on `job_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `job_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "controlm",
    "syslog"
  ],
  "equipmentModels": [
    "controlm_generic",
    "syslog_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Control-M, Autosys, cron/syslog, mainframe SMF (custom).\n• Ensure the following data sources are available: `sourcetype=controlm:job`, `sourcetype=autosys:job`, or `sourcetype=syslog` with scheduler tags.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMap vendor fields: `scheduled_time`, end status, job name. For cron, ingest start/stop lines and compare to `batch_schedule.csv` lookup (job, expected cron, max duration). Alert when `ran_ok=0` for a calendar day. Tune time zones.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=batch sourcetype=\"controlm:job\" earliest=-7d@d\n| eval day=strftime(_time,\"%Y-%m-%d\")\n| eval ended_ok=if(match(lower(status),\"(?i)ended ok|success\"),1,0)\n| stats max(ended_ok) as day_ok by job_name, day\n| where day_ok=0\n| table job_name, day\n```\n\nUnderstanding this SPL\n\n**Batch Job Schedule Compliance** — Verifies scheduled batch jobs (ETL, billing, backups) started and finished within expected windows — catching silent scheduler failures before downstream SLAs break.\n\nDocumented **Data sources**: `sourcetype=controlm:job`, `sourcetype=autosys:job`, or `sourcetype=syslog` with scheduler tags. **App/TA** (typical add-on context): Control-M, Autosys, cron/syslog, mainframe SMF (custom). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: batch; **sourcetype**: controlm:job. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=batch, sourcetype=\"controlm:job\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **day** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **ended_ok** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by job_name, day** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where day_ok=0` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Batch Job Schedule Compliance**): table job_name, day\n\n\nStep 3 — Validate\nCompare with the Control-M job plan and run history in the job scheduler. Reconcile job and folder names, start and end times, and queue or pool use for a few highlighted jobs.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (missed jobs), Calendar (job success by day), Line chart (miss rate trend).",
  "grandmaExplanation": "We check whether work that must run on a schedule actually did, so finance, ops, and customers get files when they expect them.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
