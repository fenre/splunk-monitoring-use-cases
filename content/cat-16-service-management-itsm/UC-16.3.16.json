{
  "$schema": "../../schemas/uc.schema.json",
  "id": "16.3.16",
  "title": "Knowledge Article Effectiveness on Incidents",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Knowledge Article Effectiveness on Incidents' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_knowledge_article_effectiveness_on_incidents` (UC-16.3.16), SOC dashboard panel \"Operations — Knowledge Article Effectiveness on Incidents\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-16.3.16 positive (Knowledge, Article, Effectiveness, Incidents): ingest synthetic events into `index=itsm` sourcetype='snow:incident' where `med_mttr` grouped by `category`, `has_kb` satisfies `mttr_hrs` >= 0 for 'Knowledge Article Effectiveness on Incidents'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-16.3.16 negative (on Incidents): ingest benign `itsm` / `snow:incident` events where `category`, `has_kb` stays below `mttr_hrs` >= 0, or exclude the test entity via `ops_exception_register.csv`. Variant 8 confirms zero alert rows for 'Knowledge Article Effectiveness on Incidents'."
  },
  "dataSources": "`index=itsm` `sourcetype=\"snow:incident\"` (`knowledge`, `u_knowledge_article`, `resolved_at`, `opened_at`, `category`) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_snow` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=itsm sourcetype=\"snow:incident\" earliest=-90d state IN (\"closed\",\"resolved\",\"6\",\"7\")\n| eval has_kb=if(isnotnull(u_knowledge_article) AND u_knowledge_article!=\"\" OR match(lower(coalesce(knowledge,\"\")),\"true|yes|1\"),1,0)\n| eval mttr_hrs=round((resolved_at-opened_at)/3600,2)\n| where mttr_hrs>=0\n| stats median(mttr_hrs) as med_mttr, count as tickets by category, has_kb\n| xyseries category has_kb med_mttr\n| rename \"0\" as mttr_no_kb, \"1\" as mttr_with_kb\n| eval delta_hrs=round(mttr_no_kb-mttr_with_kb,2)\n| sort -delta_hrs",
  "description": "Surfaces UC-16.3.16 — Knowledge, Article, Effectiveness, Incidents — using `index=itsm` sourcetype `snow:incident`, grouped by `category`, `has_kb`. Outputs `med_mttr` each `the scheduled window`; alert when `mttr_hrs` >= 0. Primary focus: on Incidents. Purpose-built for 'Knowledge Article Effectiveness on Incidents' in Operations workflows.",
  "value": "UC-16.3.16 accelerates insight into on Incidents ('Knowledge Article Effectiveness on Incidents') by tracking `med_mttr` on `snow:incident` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "(1) Map the fields your instance uses for article linkage (`task_knowledge`, related lists ingested as multivalue); (2) If `xyseries` is sparse, pivot with `chart median(mttr_hrs) over category by has_kb` instead; (3) Share findings with knowledge coaches when `delta_hrs` is negligible for high-volume categories.",
  "visualization": "Grouped bar chart (median MTTR with vs without KB by category), Table (delta hours), Line chart (deflection-linked MTTR trend).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk_TA_snow",
      "url": "https://splunkbase.splunk.com/app/1928",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `itsm` (UC-16.3.16)** — Planned work for 'Knowledge Article Effectiveness on Incidents' shifts `med_mttr` over `span=1h` while `category`, `has_kb` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `median` on `snow:incident`** — Scheduled automation elevates `has_kb` without crossing `mttr_hrs` >= 0 for UC-16.3.16. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `category`, `has_kb`** — Health checks in `itsm` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `snow:incident`** — First 48h baseline for UC-16.3.16 on `category` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `category`, `exception_reason`, `valid_until`.",
  "equipment": [
    "servicenow"
  ],
  "equipmentModels": [
    "servicenow_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_snow`.\n• Ensure the following data sources are available: `index=itsm` `sourcetype=\"snow:incident\"` (`knowledge`, `u_knowledge_article`, `resolved_at`, `opened_at`, `category`).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\n(1) Map the fields your instance uses for article linkage (`task_knowledge`, related lists ingested as multivalue); (2) If `xyseries` is sparse, pivot with `chart median(mttr_hrs) over category by has_kb` instead; (3) Share findings with knowledge coaches when `delta_hrs` is negligible for high-volume categories.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=itsm sourcetype=\"snow:incident\" earliest=-90d state IN (\"closed\",\"resolved\",\"6\",\"7\")\n| eval has_kb=if(isnotnull(u_knowledge_article) AND u_knowledge_article!=\"\" OR match(lower(coalesce(knowledge,\"\")),\"true|yes|1\"),1,0)\n| eval mttr_hrs=round((resolved_at-opened_at)/3600,2)\n| where mttr_hrs>=0\n| stats median(mttr_hrs) as med_mttr, count as tickets by category, has_kb\n| xyseries category has_kb med_mttr\n| rename \"0\" as mttr_no_kb, \"1\" as mttr_with_kb\n| eval delta_hrs=round(mttr_no_kb-mttr_with_kb,2)\n| sort -delta_hrs\n```\n\nUnderstanding this SPL\n\n**Knowledge Article Effectiveness on Incidents** — Comparing incidents that reference knowledge articles versus those that do not shows whether the knowledge base reduces handle time and repeat contacts—core ITIL knowledge management feedback.\n\nDocumented **Data sources**: `index=itsm` `sourcetype=\"snow:incident\"` (`knowledge`, `u_knowledge_article`, `resolved_at`, `opened_at`, `category`). **App/TA** (typical add-on context): `Splunk_TA_snow`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: itsm; **sourcetype**: snow:incident. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=itsm, sourcetype=\"snow:incident\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **has_kb** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **mttr_hrs** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where mttr_hrs>=0` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by category, has_kb** so each row reflects one combination of those dimensions.\n• Pivots fields for charting with `xyseries`.\n• Renames fields with `rename` for clarity or joins.\n• `eval` defines or adjusts **delta_hrs** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nCompare with the ServiceNow knowledge list and a few live articles. Reconcile view counts, linked incidents, and article states with what we show in the search.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Grouped bar chart (median MTTR with vs without KB by category), Table (delta hours), Line chart (deflection-linked MTTR trend).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_snow` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=snow:incident into itsm. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`category, has_kb, mttr_hrs`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We see whether the right help is attached when we fix issues, so good writing turns into real time saved.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
