{
  "$schema": "../../schemas/uc.schema.json",
  "id": "16.3.7",
  "title": "Request Fulfillment and Approval Cycle Time",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Request Fulfillment and Approval Cycle Time' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_request_fulfillment_and_approval_cycle_time` (UC-16.3.7), SOC dashboard panel \"Operations — Request Fulfillment and Approval Cycle Time\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-16.3.7 positive (Request, Fulfillment, and, Approval, Cycle): ingest synthetic events into `index=itsm` sourcetype='request' where `avg_approval`, `avg_fulfill` grouped by `catalog_item`, `approval_group` satisfies detection threshold for 'Request Fulfillment and Approval Cycle Time'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-16.3.7 negative (Cycle Time): ingest benign `itsm` / `request` events where `catalog_item`, `approval_group` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 12 confirms zero alert rows for 'Request Fulfillment and Approval Cycle Time'."
  },
  "dataSources": "Request submitted, approval, and fulfillment timestamps Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "ITSM request, approval workflow logs — Universal Forwarder (Splunkbase 617)",
  "spl": "index=itsm sourcetype=\"request\"\n| where state=\"fulfilled\"\n| eval approval_hrs=(approval_time - submitted_time)/3600\n| eval fulfill_hrs=(fulfilled_time - approval_time)/3600\n| stats avg(approval_hrs) as avg_approval, avg(fulfill_hrs) as avg_fulfill by catalog_item, approval_group\n| table catalog_item, approval_group, avg_approval, avg_fulfill",
  "description": "Alerts on UC-16.3.7 — Request, Fulfillment, and, Approval, Cycle — using `index=itsm` sourcetype `request`, grouped by `catalog_item`, `approval_group`. Outputs `avg_approval`, `avg_fulfill` each `the scheduled window`; alert when configured threshold. Primary focus: Cycle Time. Purpose-built for 'Request Fulfillment and Approval Cycle Time' in Operations workflows.",
  "value": "Tracking `avg_approval`, `avg_fulfill` via UC-16.3.7 on `request` feeds improves response to 'Request Fulfillment and Approval Cycle Time' and reduces operational risk and delayed incident response.",
  "implementation": "Ingest request and approval lifecycle events. Compute approval and fulfillment duration. Alert when average exceeds target. Report on slow catalog items and approvers.",
  "visualization": "Table (cycle time by catalog item), Bar chart (approval vs fulfillment time), Line chart (trend).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `itsm` (UC-16.3.7)** — Planned work for 'Request Fulfillment and Approval Cycle Time' shifts `avg_approval`, `avg_fulfill` over `span=1h` while `catalog_item`, `approval_group` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `request`** — Scheduled automation elevates `approval_group` without crossing configured threshold for UC-16.3.7. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `catalog_item`, `approval_group`** — Health checks in `itsm` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `request`** — First 48h baseline for UC-16.3.7 on `catalog_item` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `catalog_item`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: ITSM request, approval workflow logs.\n• Ensure the following data sources are available: Request submitted, approval, and fulfillment timestamps.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest request and approval lifecycle events. Compute approval and fulfillment duration. Alert when average exceeds target. Report on slow catalog items and approvers.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=itsm sourcetype=\"request\"\n| where state=\"fulfilled\"\n| eval approval_hrs=(approval_time - submitted_time)/3600\n| eval fulfill_hrs=(fulfilled_time - approval_time)/3600\n| stats avg(approval_hrs) as avg_approval, avg(fulfill_hrs) as avg_fulfill by catalog_item, approval_group\n| table catalog_item, approval_group, avg_approval, avg_fulfill\n```\n\nUnderstanding this SPL\n\n**Request Fulfillment and Approval Cycle Time** — Long approval or fulfillment times delay service delivery. Monitoring cycle time supports process optimization and SLA for requests.\n\nDocumented **Data sources**: Request submitted, approval, and fulfillment timestamps. **App/TA** (typical add-on context): ITSM request, approval workflow logs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: itsm; **sourcetype**: request. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=itsm, sourcetype=\"request\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where state=\"fulfilled\"` — typically the threshold or rule expression for this monitoring goal.\n• `eval` defines or adjusts **approval_hrs** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **fulfill_hrs** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by catalog_item, approval_group** so each row reflects one combination of those dimensions.\n• Pipeline stage (see **Request Fulfillment and Approval Cycle Time**): table catalog_item, approval_group, avg_approval, avg_fulfill\n\n\nStep 3 — Validate\nCompare the same time range to your ITIL reporting dashboard or the primary view in your ticket and change system. Spot-check a few records, fields, and times in the source system, then confirm access and time zone for this search.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (cycle time by catalog item), Bar chart (approval vs fulfillment time), Line chart (trend).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (ITSM request, approval workflow logs — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=request into itsm. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`approval_group, catalog_item`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We time approvals and handoffs on requests, so we trim slow approvers and duplicate steps in the request path.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
