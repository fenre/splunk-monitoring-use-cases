{
  "$schema": "../../schemas/uc.schema.json",
  "id": "16.2.10",
  "title": "Undocumented Server Detection",
  "criticality": "critical",
  "difficulty": "beginner",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'Undocumented Server Detection'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-16.2.10).",
  "evidence": "Saved search uc_16_2_10_undocumented_server_detection, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'Undocumented Server Detection'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-16.2.10.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "`sourcetype=inventory` or `vmware:inv:vm`, `sourcetype=snow:cmdb_ci` Collected via Splunk Add-on for VMware ESXi Logs (Splunkbase 3684) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder inventory, AD, vCenter — Splunk Add-on for VMware ESXi Logs (Splunkbase 3684)",
  "spl": "index=inventory sourcetype=\"vmware:inv:vm\" earliest=-4h\n| eval host_key=lower(mvindex(split(name,\".\"),0))\n| stats latest(_time) as seen by host_key\n| join type=left max=1 host_key [ search index=itsm sourcetype=\"snow:cmdb_ci\" ci_class=\"cmdb_ci_server\" | eval host_key=lower(mvindex(split(name,\".\"),0)) | table host_key, sys_id ]\n| where isnull(sys_id)\n| table host_key, seen",
  "description": "Finds servers visible to monitoring or AD but missing from CMDB — classic gap for incident routing and security scope.",
  "value": "Operations and leadership rely on 'Undocumented Server Detection' to catch regressions before they become customer-visible outages or audit findings. Early signal reduces mean time to repair, avoids duplicate manual log review, and preserves evidence for post-incident review without waiting for a user-reported ticket.",
  "implementation": "Compare monitored/VM inventory hostnames (lowercased, short name) to CMDB server CIs. Tune for naming conventions (strip domain). Feed gaps to CMDB onboarding queue.",
  "visualization": "Table (undocumented hosts), Single value (gap count), Line chart (gap trend).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "servicenow",
    "vmware"
  ],
  "equipmentModels": [
    "vmware_esxi",
    "vmware_vcenter"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Universal Forwarder inventory, AD, vCenter.\n• Ensure the following data sources are available: `sourcetype=inventory` or `vmware:inv:vm`, `sourcetype=snow:cmdb_ci`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nCompare monitored/VM inventory hostnames (lowercased, short name) to CMDB server CIs. Tune for naming conventions (strip domain). Feed gaps to CMDB onboarding queue.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=inventory sourcetype=\"vmware:inv:vm\" earliest=-4h\n| eval host_key=lower(mvindex(split(name,\".\"),0))\n| stats latest(_time) as seen by host_key\n| join type=left max=1 host_key [ search index=itsm sourcetype=\"snow:cmdb_ci\" ci_class=\"cmdb_ci_server\" | eval host_key=lower(mvindex(split(name,\".\"),0)) | table host_key, sys_id ]\n| where isnull(sys_id)\n| table host_key, seen\n```\n\nUnderstanding this SPL\n\n**Undocumented Server Detection** — Finds servers visible to monitoring or AD but missing from CMDB — classic gap for incident routing and security scope.\n\nDocumented **Data sources**: `sourcetype=inventory` or `vmware:inv:vm`, `sourcetype=snow:cmdb_ci`. **App/TA** (typical add-on context): Splunk Universal Forwarder inventory, AD, vCenter. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: inventory; **sourcetype**: vmware:inv:vm. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=inventory, sourcetype=\"vmware:inv:vm\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **host_key** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by host_key** so each row reflects one combination of those dimensions.\n• Joins to a subsearch with `join` — set `max=` to match cardinality and avoid silent truncation.\n• Filters the current rows with `where isnull(sys_id)` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Undocumented Server Detection**): table host_key, seen\n\n\nStep 3 — Validate\nCompare with the ServiceNow configuration item or related CMDB view. Spot-check a few CIs, owners, and relationship fields, then confirm discovery dates if we measure freshness.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (undocumented hosts), Single value (gap count), Line chart (gap trend).",
  "grandmaExplanation": "We find systems that are live but not in the book, so we add them, own them, and protect them on purpose.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
