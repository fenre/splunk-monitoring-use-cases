{
  "$schema": "../../schemas/uc.schema.json",
  "id": "16.1.13",
  "title": "On-Call Escalation Frequency",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Operations"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'On-Call Escalation Frequency' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_on_call_escalation_frequency` (UC-16.1.13), SOC dashboard panel \"Operations — On-Call Escalation Frequency\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=oncall` sourcetype='target_sourcetype' where `entity` satisfies `escalation_count` > 0 for UC-16.1.13 ('On-Call Escalation Frequency'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `oncall` / `target_sourcetype` events where `entity` stays below `escalation_count` > 0, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-16.1.13."
  },
  "dataSources": "On-call platform API (incidents, escalations, acknowledgment times) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom (PagerDuty API, Opsgenie API, ITSM) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=oncall sourcetype IN (\"pagerduty:incidents\",\"opsgenie:alerts\")\n| eval escalated=if(escalation_count>0 OR escalation_policy_used=1, 1, 0)\n| eval ack_delay_mins=round((acknowledged_at-triggered_at)/60, 0)\n| timechart span=1d count as total, sum(escalated) as escalated\n| eval escalation_rate=round(escalated/total*100, 1)\n| where total>0",
  "description": "Rising escalation rate indicates team capacity or knowledge gaps. Unacknowledged or escalated incidents signal burnout risk and process bottlenecks. Supports on-call rotation tuning and training.",
  "value": "Early 'On-Call Escalation Frequency' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Ingest PagerDuty or Opsgenie incidents via REST API (scheduled input or scripted input). Map fields: `escalation_count`, `escalation_policy_used`, `acknowledged_at`, `triggered_at`. Compute escalation rate (escalated / total) per day or per service. Alert when escalation rate exceeds 15% over 7 days. Track acknowledgment time (SLA); alert when avg ack time exceeds 15 minutes for P1. Report by service and escalation policy to identify overloaded rotations.",
  "visualization": "Line chart (escalation rate trend), Bar chart (escalations by service), Single value (escalation rate %), Table (slowest-acknowledged incidents).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `oncall`** — Planned work for 'On-Call Escalation Frequency' shifts `sum` over `span=1d` while `host` remains in-scope for UC-16.1.13. Cross-check CMDB CHG.\n\n2. **Batch `sum` on `configured_sourcetype`** — Scheduled automation elevates `escalated` without crossing `escalation_count` > 0. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `oncall` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "pagerduty"
  ],
  "equipmentModels": [
    "pagerduty_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Custom (PagerDuty API, Opsgenie API, ITSM).\n• Ensure the following data sources are available: On-call platform API (incidents, escalations, acknowledgment times).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest PagerDuty or Opsgenie incidents via REST API (scheduled input or scripted input). Map fields: `escalation_count`, `escalation_policy_used`, `acknowledged_at`, `triggered_at`. Compute escalation rate (escalated / total) per day or per service. Alert when escalation rate exceeds 15% over 7 days. Track acknowledgment time (SLA); alert when avg ack time exceeds 15 minutes for P1. Report by service and escalation policy to identify overloaded rotations.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=oncall sourcetype IN (\"pagerduty:incidents\",\"opsgenie:alerts\")\n| eval escalated=if(escalation_count>0 OR escalation_policy_used=1, 1, 0)\n| eval ack_delay_mins=round((acknowledged_at-triggered_at)/60, 0)\n| timechart span=1d count as total, sum(escalated) as escalated\n| eval escalation_rate=round(escalated/total*100, 1)\n| where total>0\n```\n\nUnderstanding this SPL\n\n**On-Call Escalation Frequency** — Rising escalation rate indicates team capacity or knowledge gaps. Unacknowledged or escalated incidents signal burnout risk and process bottlenecks. Supports on-call rotation tuning and training.\n\nDocumented **Data sources**: On-call platform API (incidents, escalations, acknowledgment times). **App/TA** (typical add-on context): Custom (PagerDuty API, Opsgenie API, ITSM). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: oncall.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=oncall. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **escalated** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **ack_delay_mins** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `timechart` plots the metric over time using **span=1d** buckets — ideal for trending and alerting on this use case.\n• `eval` defines or adjusts **escalation_rate** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where total>0` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nCompare with the PagerDuty incident timeline and service view for the same window. Reconcile a few incident numbers, state changes, and acknowledgement times. If anything disagrees, check export lag, time zone, and which API fields we map.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (escalation rate trend), Bar chart (escalations by service), Single value (escalation rate %), Table (slowest-acknowledged incidents).\n\nScripted input (generic example)\nThis use case relies on a scripted input. In the app's local/inputs.conf add a stanza such as:\n\n```ini\n[script://$SPLUNK_HOME/etc/apps/YourApp/bin/collect.sh]\ninterval = 300\nsourcetype = your_sourcetype\nindex = main\ndisabled = 0\n```\n\nThe script should print one event per line (e.g. key=value). Example minimal script (bash):\n\n```bash\n#!/usr/bin/env bash\n# Output metrics or events, one per line\necho \"metric=value timestamp=$(date +%s)\"\n```\n\nFor full details (paths, scheduling, permissions), see the Implementation guide: docs/implementation-guide.md",
  "grandmaExplanation": "We track how often work jumps up the on-call chain, so we can see real pressure versus noisy or mis-tuned rules.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
