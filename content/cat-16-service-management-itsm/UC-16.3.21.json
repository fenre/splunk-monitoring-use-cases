{
  "$schema": "../../schemas/uc.schema.json",
  "id": "16.3.21",
  "title": "Jira Epic Without Open Child Issues for Extended Period",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Quality",
    "Governance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Jira Epic Without Open Child Issues for Extended Period' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_jira_epic_without_open_child_issues_for_extended` (UC-16.3.21), SOC dashboard panel \"Operations — Jira Epic Without Open Child Issues for Extended Period\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-16.3.21 positive (Jira, Epic, Without, Open, Child): ingest synthetic events into `index=itsm` sourcetype='jira:cloud:issues' where `count` grouped by `entity` satisfies `stale_d` > 14 for 'Jira Epic Without Open Child Issues for Extended Period'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-16.3.21 negative (Extended Period): ingest benign `itsm` / `jira:cloud:issues` events where `entity` stays below `stale_d` > 14, or exclude the test entity via `ops_exception_register.csv`. Variant 6 confirms zero alert rows for 'Jira Epic Without Open Child Issues for Extended Period'."
  },
  "dataSources": "Jira issues denormalized with `child_open_count` from ETL or lookup Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Nightly Jira REST export augmented with child counts in HEC — Universal Forwarder (Splunkbase 617)",
  "spl": "index=itsm sourcetype=\"jira:cloud:issues\" OR sourcetype=\"jira:issue\"\n| where issue_type=\"Epic\" AND epic_status IN (\"To Do\",\"In Progress\")\n| eval stale_d=(now()-updated_epoch)/86400\n| where stale_d > 14 AND child_open_count=0\n| table key, epic_name, project, stale_d\n| sort -stale_d",
  "description": "Monitors Extended Period anomalies for UC-16.3.21 with window `the scheduled window` on `index=itsm` sourcetype `jira:cloud:issues`. Combines `count` by `entity` and compares against `stale_d` > 14. Mapped to keywords Jira, Epic, Without, Open, Child for dashboard routing.",
  "value": "Alerting on Jira, Epic, Without, Open, Child through UC-16.3.21 reduces operational risk and delayed incident response by surfacing `activity` early.",
  "implementation": "Populate `child_open_count` via scheduled search into KV store lookup.",
  "visualization": "Table (epic list), Pie chart (by portfolio).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Add-on for Jira Cloud",
      "url": "https://splunkbase.splunk.com/app/6211",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Source types — Splunk Add-on for Jira Cloud",
      "url": "https://docs.splunk.com/Documentation/AddOns/released/JiraCloud/Sourcetypes",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `itsm` (UC-16.3.21)** — Planned work for 'Jira Epic Without Open Child Issues for Extended Period' shifts `count` over `span=1h` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `jira:cloud:issues`** — Scheduled automation elevates `stale_d` without crossing `stale_d` > 14 for UC-16.3.21. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `itsm` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `jira:cloud:issues`** — First 48h baseline for UC-16.3.21 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "jira"
  ],
  "equipmentModels": [
    "jira_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Install and configure: Nightly Jira REST export augmented with child counts in HEC.\n• Data: Jira issues denormalized with `child_open_count` from ETL or lookup.\n• For directory layout and inputs, see docs/implementation-guide.md.\n\nStep 1 — Configure data collection\nPopulate `child_open_count` via scheduled search into KV store lookup.\n\nStep 2 — Create the search and alert\nRun the following search (then save as report or alert; tune time range and threshold):\n\n```spl\nindex=itsm sourcetype=\"jira:cloud:issues\" OR sourcetype=\"jira:issue\"\n| where issue_type=\"Epic\" AND epic_status IN (\"To Do\",\"In Progress\")\n| eval stale_d=(now()-updated_epoch)/86400\n| where stale_d > 14 AND child_open_count=0\n| table key, epic_name, project, stale_d\n| sort -stale_d\n```\n\nUnderstanding this search\nOrphan epics mask portfolio reporting and waste capacity forecasts.\n\nStep 3 — Validate\nCompare with the Jira issue page, board, or service project view for the same dates. Confirm issue keys, status, and assignment fields; reconcile timestamp fields you use in the report.\n\nStep 4 — Operationalize\nAdd to a dashboard, wire alert actions, and document ownership. Suggested view: Table (epic list), Pie chart (by portfolio).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Nightly Jira REST export augmented with child counts in HEC — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=jira:cloud:issues, jira:issue into itsm. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`stale_d`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We notice an epic with no child work for a long stretch, so big promises are not left empty in the plan.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
