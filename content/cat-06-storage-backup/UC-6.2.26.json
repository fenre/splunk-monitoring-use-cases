{
  "$schema": "../../schemas/uc.schema.json",
  "id": "6.2.26",
  "title": "Ceph BlueStore compression ratio effectiveness by pool",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity",
    "Cost"
  ],
  "splunkPillar": "Observability",
  "dataSources": "Pool statistics in `ceph:pool` with `compression_*`, `stored`, `stored_raw`",
  "app": "Scripted input `ceph df detail -f json`",
  "spl": "index=storage sourcetype=\"ceph:pool\" earliest=-1h\n| eval stored=coalesce(stored_bytes, stored)\n| eval raw=coalesce(stored_raw_bytes, stored_raw)\n| eval ratio=if(raw>0, round(stored/raw,3), null())\n| where isnotnull(ratio) AND compression_mode!=\"none\"\n| timechart span=6h latest(ratio) as compress_ratio by pool_name",
  "description": "Compression that no longer yields ratio >1 wastes CPU and obscures capacity planning. Pool-level ratios highlight mis-set `compression_algorithm` or incompressible data.",
  "value": "Right-sizes CPU spend and improves effective $/TB for backup and archive pools.",
  "implementation": "Flatten `pools` array from `ceph df detail`. Join pool to application owner via lookup for chargeback.",
  "visualization": "Line chart (ratio by pool), table (pool, mode, ratio).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Ceph Documentation — monitoring",
      "url": "https://docs.ceph.com/en/latest/radosgw/"
    },
    {
      "title": "Red Hat Ceph Storage — troubleshooting",
      "url": "https://access.redhat.com/documentation/en-us/red_hat_ceph_storage/"
    }
  ],
  "equipment": [
    "ceph"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "9.x",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases"
}
