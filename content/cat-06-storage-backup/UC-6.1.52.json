{
  "$schema": "../../schemas/uc.schema.json",
  "id": "6.1.52",
  "title": "NetApp ONTAP SnapMirror lag versus configured recovery point objective",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Availability",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'NetApp ONTAP SnapMirror lag versus configured recovery point objective' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_netapp_ontap_snapmirror_lag_versus_configured_re` (UC-6.1.52), SOC dashboard panel \"Operations — NetApp ONTAP SnapMirror lag versus configured recovery point objective\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-6.1.52 positive (NetApp, ONTAP, SnapMirror, lag, versus): ingest synthetic events into `index=storage` sourcetype='netapp:ontap:snapmirror' where `max_lag`, `policy_rpo`, `breach` grouped by `relationship_name`, `destination_path`, `cluster_name` satisfies detection threshold for 'NetApp ONTAP SnapMirror lag versus configured recovery point objective'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-6.1.52 negative (point objective): ingest benign `storage` / `netapp:ontap:snapmirror` events where `relationship_name`, `destination_path`, `cluster_name` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 4 confirms zero alert rows for 'NetApp ONTAP SnapMirror lag versus configured recovery point objective'."
  },
  "dataSources": "SnapMirror relationship metrics in `netapp:ontap:snapmirror` (and related fields on `netapp:ontap:vol`) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Add-on for NetApp Data ONTAP (`TA-netapp_ontap`) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=storage sourcetype=\"netapp:ontap:snapmirror\"\n| eval lag_sec=coalesce(lag_time, relationship_lag_seconds, lag_seconds, if(isnotnull(lag_time_millis), round(lag_time_millis/1000,0), null()))\n| eval rpo_sec=coalesce(policy_rpo_seconds, rpo_seconds, 3600)\n| where isnotnull(lag_sec) AND lag_sec > rpo_sec\n| eval breach_ratio=round(lag_sec/rpo_sec,2)\n| stats max(lag_sec) as max_lag max(rpo_sec) as policy_rpo max(breach_ratio) as breach by relationship_name, destination_path, cluster_name\n| sort - max_lag",
  "description": "Surfaces risk tied to NetApp, ONTAP, SnapMirror, lag, versus through UC-6.1.52 on `index=storage` sourcetype `netapp:ontap:snapmirror`. Summarises `max_lag`, `policy_rpo`, `breach` grouped by `relationship_name`, `destination_path`, `cluster_name` across `the scheduled window` before applying configured threshold. Operators tune 'NetApp ONTAP SnapMirror lag versus configured recovery point objective' using UC-6.1.52 runbook steps.",
  "value": "Tracking `max_lag`, `policy_rpo`, `breach` via UC-6.1.52 on `netapp:ontap:snapmirror` feeds improves response to 'NetApp ONTAP SnapMirror lag versus configured recovery point objective' and reduces operational risk and delayed incident response.",
  "implementation": "Poll `/api/snapmirror/relationships` via the TA and extract `lag_time`, `policy`, and `healthy`. Convert ISO8601 lag to seconds if needed using `strptime`. Alert when lag exceeds `rpo_sec` for two consecutive samples; throttle during bulk resync using a `state` filter.",
  "visualization": "Scatter (RPO vs lag), timechart of lag by relationship, table of worst breaches.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Add-on for NetApp Data ONTAP (Splunkbase)",
      "url": "https://splunkbase.splunk.com/app/1664",
      "retrieved": "2026-04-25"
    },
    {
      "title": "SnapMirror overview",
      "url": "https://docs.netapp.com/us-en/ontap/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `storage` (UC-6.1.52)** — Planned work for 'NetApp ONTAP SnapMirror lag versus configured recovery point objective' shifts `max_lag`, `policy_rpo`, `breach` over `span=1h` while `relationship_name`, `destination_path`, `cluster_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `max` on `netapp:ontap:snapmirror`** — Scheduled automation elevates `destination_path` without crossing configured threshold for UC-6.1.52. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `relationship_name`, `destination_path`, `cluster_name`** — Health checks in `storage` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `netapp:ontap:snapmirror`** — First 48h baseline for UC-6.1.52 on `relationship_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `relationship_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "netapp"
  ],
  "equipmentModels": [
    "netapp_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "9.x",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for NetApp Data ONTAP (`TA-netapp_ontap`) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=netapp:ontap:snapmirror into storage. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`cluster_name, destination_path, relationship_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We track copy and mirror health so a planned outage or a bad link does not leave you with an old or broken remote copy when you need it most.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
