{
  "$schema": "../../schemas/uc.schema.json",
  "id": "6.1.50",
  "title": "NetApp ONTAP MetroCluster switchover and DR event correlation",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Availability",
    "Resilience"
  ],
  "splunkPillar": "Observability",
  "dataSources": "ONTAP EMS events indexed as `netapp:ontap:event` or `netapp:ontap:ems` from `TA-netapp_ontap` (syslog or REST EMS)",
  "app": "Splunk Add-on for NetApp Data ONTAP (`TA-netapp_ontap`, Splunkbase 1664)",
  "spl": "index=storage (sourcetype=\"netapp:ontap:event\" OR sourcetype=\"netapp:ontap:ems\")\n| search (message=\"*metrocluster*\" OR message=\"*MetroCluster*\" OR message=\"*switchover*\" OR EMSMessage=\"*metrocluster*\" OR EMSMessage=\"*switchover*\")\n| eval cluster=coalesce(cluster_name, cluster, cluster_uuid)\n| eval node=coalesce(node_name, node, origin_node)\n| table _time, cluster, node, severity, EMSMessage, message\n| sort - _time",
  "cimModels": [
    "N/A"
  ],
  "description": "Unplanned MetroCluster switchovers or failed negotiated switchovers strand I/O on one site and often precede extended outages. Rapid EMS correlation helps validate bridges, NVMe shelves, and mediator health before applications time out.",
  "value": "Reduces mean time to detect for site or fabric incidents affecting synchronous replication, protecting RTO for business-critical NAS and SAN workloads.",
  "implementation": "Install `TA-netapp_ontap` on a heavy forwarder. Enable EMS via `inputs.conf` (syslog from the cluster management LIF or scripted REST `/api/support/ems/events`). Normalize sourcetype to `netapp:ontap:event` or `netapp:ontap:ems` in `props.conf`. Schedule a 1-minute alert excluding maintenance windows from a CSV lookup.",
  "visualization": "Timeline by severity, table (cluster, node, message), single value (event count 15m).",
  "references": [
    {
      "title": "Splunk Add-on for NetApp Data ONTAP (Splunkbase)",
      "url": "https://splunkbase.splunk.com/app/1664"
    },
    {
      "title": "MetroCluster IP documentation",
      "url": "https://docs.netapp.com/us-en/ontap-metrocluster/"
    }
  ],
  "knownFalsePositives": "Short spikes during approved changes, maintenance windows, or known batch jobs can match the rule; confirm against the vendor console and change calendar.",
  "equipment": [
    "netapp",
    "syslog"
  ],
  "status": "draft",
  "grandmaExplanation": "We help you see how your arrays and related gear are doing before small issues turn into full outages or restore surprises.",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "9.x",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases"
}
