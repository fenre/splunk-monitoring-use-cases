{
  "$schema": "../../schemas/uc.schema.json",
  "id": "6.1.26",
  "title": "Deduplication Savings Ratio",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Deduplication Savings Ratio' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_deduplication_savings_ratio` (UC-6.1.26), SOC dashboard panel \"Operations — Deduplication Savings Ratio\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-6.1.26 positive (Deduplication, Savings, Ratio): ingest synthetic events into `index=storage` sourcetype='storage:dedupe' where `ratio` grouped by `aggregate_name` satisfies `ratio` < 30 for 'Deduplication Savings Ratio'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-6.1.26 negative (Savings Ratio): ingest benign `storage` / `storage:dedupe` events where `aggregate_name` stays below `ratio` < 30, or exclude the test entity via `ops_exception_register.csv`. Variant 12 confirms zero alert rows for 'Deduplication Savings Ratio'."
  },
  "dataSources": "Logical vs physical used, dedupe savings API fields Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Vendor TA (NetApp, Dell, Pure) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=storage sourcetype=\"storage:dedupe\"\n| eval savings_ratio=round((logical_used_bytes-physical_used_bytes)/nullif(logical_used_bytes,0)*100,1)\n| timechart span=1d avg(savings_ratio) as ratio by aggregate_name\n| where ratio < 30",
  "description": "Monitors UC-6.1.26 — Deduplication, Savings, Ratio — using `index=storage` sourcetype `storage:dedupe`, grouped by `aggregate_name`. Outputs `ratio` each `1d`; alert when `ratio` < 30. Primary focus: Savings Ratio. Purpose-built for 'Deduplication Savings Ratio' in Operations workflows.",
  "value": "Tracking `ratio` via UC-6.1.26 on `storage:dedupe` feeds improves response to 'Deduplication Savings Ratio' and reduces operational risk and delayed incident response.",
  "implementation": "Poll dedupe stats weekly or daily. Baseline savings ratio per aggregate. Alert on significant drop vs 30-day average (e.g., >20% relative drop).",
  "visualization": "Line chart (savings ratio over time), Table (aggregate, logical, physical, ratio), Single value (fleet average ratio).",
  "cimModels": [
    "Performance"
  ],
  "cimSpl": "| tstats `summariesonly` max(Performance.storage_used_percent) as used_pct\n  from datamodel=Performance where nodename=Performance.Storage\n  by Performance.host Performance.object span=1h\n| where used_pct > 80\n| sort - used_pct",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM Performance data model (Performance.Storage node / storage_used_percent) after vendor fields are aliased to CIM; otherwise the cimSpl tstats variant may return no rows.",
  "references": [
    {
      "title": "Cisco DC Networking Application for Splunk",
      "url": "https://splunkbase.splunk.com/app/7777",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Performance",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Performance",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `storage` (UC-6.1.26)** — Planned work for 'Deduplication Savings Ratio' shifts `ratio` over `span=1d` while `aggregate_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `storage:dedupe`** — Scheduled automation elevates `savings_ratio` without crossing `ratio` < 30 for UC-6.1.26. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `aggregate_name`** — Health checks in `storage` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `storage:dedupe`** — First 48h baseline for UC-6.1.26 on `aggregate_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `aggregate_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "netapp"
  ],
  "equipmentModels": [
    "netapp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Vendor TA (NetApp, Dell, Pure).\n• Ensure the following data sources are available: Logical vs physical used, dedupe savings API fields.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nPoll dedupe stats weekly or daily. Baseline savings ratio per aggregate. Alert on significant drop vs 30-day average (e.g., >20% relative drop).\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=storage sourcetype=\"storage:dedupe\"\n| eval savings_ratio=round((logical_used_bytes-physical_used_bytes)/nullif(logical_used_bytes,0)*100,1)\n| timechart span=1d avg(savings_ratio) as ratio by aggregate_name\n| where ratio < 30\n```\n\nUnderstanding this SPL\n\n**Deduplication Savings Ratio** — Deduplication ratio trending validates efficiency features and detects anomalies (sudden ratio drop may indicate new data types or misconfiguration).\n\nDocumented **Data sources**: Logical vs physical used, dedupe savings API fields. **App/TA** (typical add-on context): Vendor TA (NetApp, Dell, Pure). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: storage; **sourcetype**: storage:dedupe. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=storage, sourcetype=\"storage:dedupe\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **savings_ratio** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `timechart` plots the metric over time using **span=1d** buckets with a separate series **by aggregate_name** — ideal for trending and alerting on this use case.\n• Filters the current rows with `where ratio < 30` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nCompare the same metric, object name, and interval in the vendor or cloud console (array, backup, or object store) that is the source of truth for this feed.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Point on-call to the ONTAP or array runbook, Cisco SAN references, and SNMP/REST credentials already used in production—not generic platform steps only. Consider visualizations: Line chart (savings ratio over time), Table (aggregate, logical, physical, ratio), Single value (fleet average ratio).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Vendor TA (NetApp, Dell, Pure) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=storage:dedupe into storage. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`aggregate_name, ratio`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We help you see whether space savings features still behave the way you expect, so sudden shifts do not mean silent data or layout issues.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
