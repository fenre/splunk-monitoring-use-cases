{
  "$schema": "../../schemas/uc.schema.json",
  "id": "6.1.66",
  "title": "Pure Storage FlashArray host multipath path imbalance detection",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Pure Storage FlashArray host multipath path imbalance detection' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_pure_storage_flasharray_host_multipath_path_imba` (UC-6.1.66), SOC dashboard panel \"Operations — Pure Storage FlashArray host multipath path imbalance detection\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-6.1.66 positive (Pure, Storage, FlashArray, host, multipath): ingest synthetic events into `index=storage` sourcetype='purestorage:host' where `values` grouped by `h` satisfies `paths` < 2 for 'Pure Storage FlashArray host multipath path imbalance detection'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-6.1.66 negative (imbalance detection): ingest benign `storage` / `purestorage:host` events where `h` stays below `paths` < 2, or exclude the test entity via `ops_exception_register.csv`. Variant 4 confirms zero alert rows for 'Pure Storage FlashArray host multipath path imbalance detection'."
  },
  "dataSources": "Host connectivity records in `purestorage:host` joined to `purestorage:volume` path metadata when available Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Pure Storage Unified Add-on for Splunk (5513) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=storage sourcetype=\"purestorage:host\"\n| eval paths=coalesce(path_count, active_paths, multipath_paths)\n| eval h=coalesce(host_name, hostname, name)\n| where isnotnull(h)\n| stats latest(paths) as paths values(array_name) as arrays by h\n| where paths < 2 OR isnull(paths)\n| sort h",
  "description": "Surfaces risk tied to Pure, Storage, FlashArray, host, multipath through UC-6.1.66 on `index=storage` sourcetype `purestorage:host`. Summarises `values` grouped by `h` across `the scheduled window` before applying `paths` < 2. Operators tune 'Pure Storage FlashArray host multipath path imbalance detection' using UC-6.1.66 runbook steps.",
  "value": "Tracking `activity` via UC-6.1.66 on `purestorage:host` feeds improves response to 'Pure Storage FlashArray host multipath path imbalance detection' and reduces operational risk and delayed incident response.",
  "implementation": "Ensure host inventory REST feed is enabled. If `path_count` is absent, compute from multivalue `target_wwn` or `volume` connection lists via `mvcount` in a scheduled search. Alert integration with CMDB owner field.",
  "visualization": "Table (host, paths, arrays), pie chart (compliant vs non-compliant).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Pure Storage Unified Add-on for Splunk (Splunkbase)",
      "url": "https://splunkbase.splunk.com/app/5513",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Pure Storage Splunk reference",
      "url": "https://support.purestorage.com/Solutions/Splunk/Splunk_Reference/Array_Monitoring_on_Splunk_with_PureStorage_Unified_App_and_TA",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `storage` (UC-6.1.66)** — Planned work for 'Pure Storage FlashArray host multipath path imbalance detection' shifts `values` over `span=1h` while `h` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `values` on `purestorage:host`** — Scheduled automation elevates `paths` without crossing `paths` < 2 for UC-6.1.66. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `h`** — Health checks in `storage` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `purestorage:host`** — First 48h baseline for UC-6.1.66 on `h` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `h`, `exception_reason`, `valid_until`.",
  "equipment": [
    "pure_storage"
  ],
  "equipmentModels": [
    "pure_storage_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "9.x",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Pure Storage Unified Add-on for Splunk (5513) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=purestorage:host into storage. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`h, paths`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We help you see how your arrays and related gear are doing before small issues turn into full outages or restore surprises.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
