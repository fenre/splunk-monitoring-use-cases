{
  "$schema": "../../schemas/uc.schema.json",
  "id": "6.1.23",
  "title": "LUN Latency Trending",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'LUN Latency Trending' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_lun_latency_trending` (UC-6.1.23), SOC dashboard panel \"Operations — LUN Latency Trending\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-6.1.23 positive (LUN, Latency, Trending): ingest synthetic events into `index=storage` sourcetype='storage:lun_perf' where `count` grouped by `lun_id`, `array_name` satisfies `p95_read` > 20, or `p95_write` > 20 for 'LUN Latency Trending'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-6.1.23 negative (Latency Trending): ingest benign `storage` / `storage:lun_perf` events where `lun_id`, `array_name` stays below `p95_read` > 20, or `p95_write` > 20, or exclude the test entity via `ops_exception_register.csv`. Variant 13 confirms zero alert rows for 'LUN Latency Trending'."
  },
  "dataSources": "Array LUN performance API, VMware `disk.latency` per datastore Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Vendor TA, VMware vSphere performance (if LUN mapped) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=storage sourcetype=\"storage:lun_perf\"\n| timechart span=5m perc95(read_latency_ms) as p95_read, perc95(write_latency_ms) as p95_write by lun_id, array_name\n| where p95_read > 20 OR p95_write > 20",
  "description": "Identifies UC-6.1.23 — LUN, Latency, Trending — using `index=storage` sourcetype `storage:lun_perf`, grouped by `lun_id`, `array_name`. Outputs `count` each `5m`; alert when `p95_read` > 20, or `p95_write` > 20. Primary focus: Latency Trending. Purpose-built for 'LUN Latency Trending' in Operations workflows.",
  "value": "UC-6.1.23 gives Operations teams earlier visibility into Latency Trending through `storage:lun_perf` feeds, lowering operational risk and delayed incident response.",
  "implementation": "Ingest per-LUN latency at 5m granularity. Set SLA thresholds (e.g., p95 >20ms). Split by workload tier. Correlate with IOPS saturation.",
  "visualization": "Line chart (p95 read/write per LUN), Heatmap (LUN × hour), Table (worst LUNs).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `storage` (UC-6.1.23)** — Planned work for 'LUN Latency Trending' shifts `count` over `span=5m` while `lun_id`, `array_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `storage:lun_perf`** — Scheduled automation elevates `array_name` without crossing `p95_read` > 20 for UC-6.1.23. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `lun_id`, `array_name`** — Health checks in `storage` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `storage:lun_perf`** — First 48h baseline for UC-6.1.23 on `lun_id` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `lun_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "vmware"
  ],
  "equipmentModels": [
    "vmware_vsphere"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Vendor TA, VMware vSphere performance (if LUN mapped).\n• Ensure the following data sources are available: Array LUN performance API, VMware `disk.latency` per datastore.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest per-LUN latency at 5m granularity. Set SLA thresholds (e.g., p95 >20ms). Split by workload tier. Correlate with IOPS saturation.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=storage sourcetype=\"storage:lun_perf\"\n| timechart span=5m perc95(read_latency_ms) as p95_read, perc95(write_latency_ms) as p95_write by lun_id, array_name\n| where p95_read > 20 OR p95_write > 20\n```\n\nUnderstanding this SPL\n\n**LUN Latency Trending** — Per-LUN latency separates noisy neighbors and misaligned workloads from array-wide issues. Supports QoS and datastore placement decisions.\n\nDocumented **Data sources**: Array LUN performance API, VMware `disk.latency` per datastore. **App/TA** (typical add-on context): Vendor TA, VMware vSphere performance (if LUN mapped). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: storage; **sourcetype**: storage:lun_perf. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=storage, sourcetype=\"storage:lun_perf\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `timechart` plots the metric over time using **span=5m** buckets with a separate series **by lun_id, array_name** — ideal for trending and alerting on this use case.\n• Filters the current rows with `where p95_read > 20 OR p95_write > 20` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nCompare the same metric, object name, and interval in the vendor or cloud console (array, backup, or object store) that is the source of truth for this feed.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Point on-call to the ONTAP or array runbook, Cisco SAN references, and SNMP/REST credentials already used in production—not generic platform steps only. Consider visualizations: Line chart (p95 read/write per LUN), Heatmap (LUN × hour), Table (worst LUNs).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Vendor TA, VMware vSphere performance (if LUN mapped) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=storage:lun_perf into storage. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`array_name, lun_id, p95_read, p95_write`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch when disks or arrays slow down for your important workloads, so you can act before people notice a frozen app or missed deadlines.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
