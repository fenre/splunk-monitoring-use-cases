{
  "$schema": "../../schemas/uc.schema.json",
  "id": "6.2.47",
  "title": "TrueNAS NFS export latency and RPC slow operations",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'TrueNAS NFS export latency and RPC slow operations' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_truenas_nfs_export_latency_and_rpc_slow_operatio` (UC-6.2.47), SOC dashboard panel \"Operations — TrueNAS NFS export latency and RPC slow operations\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=storage` sourcetype='truenas:pool' where `client` satisfies detection threshold for UC-6.2.47 ('TrueNAS NFS export latency and RPC slow operations'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `storage` / `truenas:pool` events where `client` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-6.2.47."
  },
  "dataSources": "NFS server metrics or `truenas:pool` performance adjunct with NFS latency Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "TrueNAS reporting or `nfsstat` scripted to HEC — Universal Forwarder (Splunkbase 617)",
  "spl": "index=storage (sourcetype=\"truenas:pool\" OR sourcetype=\"truenas:alert\")\n| search nfs OR NFS\n| eval client=coalesce(client_ip, nfs_client)\n| eval lat_ms=coalesce(nfs_latency_ms, rpc_latency_ms)\n| timechart span=5m perc95(lat_ms) as p95_ms by client, export_path",
  "description": "Per-client NFS latency distinguishes overloaded clients from array-side CPU or ZIL contention affecting many hosts. Searches `index=storage` with sourcetype `truenas:pool` for UC-6.2.47, aggregating `count` by `client` over `5m`. Alerts when grouped results cross configured threshold in the saved search. Gives Operations operators a repeatable signal for 'TrueNAS NFS export latency and RPC slow operations' without manual log review.",
  "value": "Per-client NFS latency distinguishes overloaded clients from array-side CPU or ZIL contention affecting man.... Timely UC-6.2.47 alerting on 'TrueNAS NFS export latency and RPC slow operations' reduces operational risk and delayed incident response before manual triage or audit channels escalate.",
  "implementation": "If only server-side stats exist, pair with client `nfsiostat` forwarders for triangulation. Tag exports with workload class via lookup.",
  "visualization": "Heatmap client × export, line chart p95.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "TrueNAS SCALE API documentation",
      "url": "https://www.truenas.com/docs/scale/scaletutorials/toptoolbar/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "TrueNAS CORE/SCALE docs — alerts",
      "url": "https://www.truenas.com/docs/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `storage`** — Planned work for 'TrueNAS NFS export latency and RPC slow operations' shifts `count` over `span=5m` while `client` remains in-scope for UC-6.2.47. Cross-check CMDB CHG.\n\n2. **Batch `count` on `truenas:pool`** — Scheduled automation elevates `export_path` without crossing configured threshold. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `client`** — Health checks in `storage` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `truenas:pool`** — First 48h baseline for `client` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `client`, `exception_reason`, `valid_until`.",
  "equipment": [
    "truenas"
  ],
  "equipmentModels": [
    "truenas_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "9.x",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (TrueNAS reporting or `nfsstat` scripted to HEC — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=truenas:pool, truenas:alert into storage. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`client, export_path`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch when disks or arrays slow down for your important workloads, so you can act before people notice a frozen app or missed deadlines.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
