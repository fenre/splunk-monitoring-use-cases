{
  "$schema": "../../schemas/uc.schema.json",
  "id": "6.2.45",
  "title": "TrueNAS iSCSI target session disconnect and login failure events",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'TrueNAS iSCSI target session disconnect and login failure events' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_truenas_iscsi_target_session_disconnect_and_logi` (UC-6.2.45), SOC dashboard panel \"Operations — TrueNAS iSCSI target session disconnect and login failure events\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-6.2.45 positive (TrueNAS, iSCSI, target, session, disconnect): ingest synthetic events into `index=storage` sourcetype='truenas:alert' where `count` grouped by `hostname`, `tgt`, `initiator` satisfies -4h for 'TrueNAS iSCSI target session disconnect and login failure events'. Validate filter `iscsi OR iSCSI OR ctl OR \"target offline\" OR \"session termin`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-6.2.45 negative (failure events): ingest benign `storage` / `truenas:alert` events where `hostname`, `tgt`, `initiator` stays below -4h, or exclude the test entity via `ops_exception_register.csv`. Variant 15 confirms zero alert rows for 'TrueNAS iSCSI target session disconnect and login failure events'."
  },
  "dataSources": "SCSI target logs as `truenas:alert` or syslog with `ctl` / `iscsi` keywords Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Connect for Syslog or UF on TrueNAS SCALE — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=storage sourcetype=\"truenas:alert\" earliest=-4h\n| search iscsi OR iSCSI OR ctl OR \"target offline\" OR \"session terminate\"\n| eval tgt=coalesce(target_name, iscsi_target)\n| stats count as disc latest(_time) as last_seen by hostname, tgt, initiator\n| sort - disc",
  "description": "Monitors UC-6.2.45 — TrueNAS, iSCSI, target, session, disconnect — using `index=storage` sourcetype `truenas:alert`, grouped by `hostname`, `tgt`, `initiator`. Outputs `count` each `the scheduled window`; alert when -4h. Filter chain includes filter `iscsi OR iSCSI OR ctl OR \"target offline\" OR \"session termin`. Purpose-built for 'TrueNAS iSCSI target session disconnect and login failure events' in Operations workflows.",
  "value": "UC-6.2.45 operationalises 'TrueNAS iSCSI target session disconnect and login failure events' against `truenas:alert` feeds so operators reduce operational risk and delayed incident response without manual correlation.",
  "implementation": "Map TrueNAS alert severities to Splunk severity. If logs lack initiator IQN, augment with switch port counters in a correlated search.",
  "visualization": "Timeline, table (target, initiator, count).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "TrueNAS SCALE API documentation",
      "url": "https://www.truenas.com/docs/scale/scaletutorials/toptoolbar/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "TrueNAS CORE/SCALE docs — alerts",
      "url": "https://www.truenas.com/docs/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `storage` (UC-6.2.45)** — Planned work for 'TrueNAS iSCSI target session disconnect and login failure events' shifts `count` over `span=1h` while `hostname`, `tgt`, `initiator` remains in-scope. Applies filter `iscsi OR iSCSI OR ctl OR \"target offline\" OR \"session termin`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `truenas:alert`** — Scheduled automation elevates `tgt` without crossing configured threshold for UC-6.2.45. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `hostname`, `tgt`, `initiator`** — Health checks in `storage` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `truenas:alert`** — First 48h baseline for UC-6.2.45 on `hostname` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `hostname`, `exception_reason`, `valid_until`.",
  "equipment": [
    "syslog",
    "truenas"
  ],
  "equipmentModels": [
    "syslog_generic",
    "truenas_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "9.x",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Connect for Syslog or UF on TrueNAS SCALE — Splunk Add-on for Unix and Linux (Splunkbase 833)). Enable the modular input or HEC token that writes sourcetype=truenas:alert into storage. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`hostname, initiator, tgt`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We help you see how object storage is growing, who can reach it, and when something is exposed or mis-tagged, so cloud storage stays under control.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
