{
  "$schema": "../../schemas/uc.schema.json",
  "id": "6.2.24",
  "title": "CephFS MDS session cap recall and cache pressure",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Availability"
  ],
  "splunkPillar": "Observability",
  "dataSources": "MDS logs as `ceph:log` or MDS status JSON as `ceph:status`",
  "app": "Forwarder on MDS nodes; optional `ceph tell mds.* cache status` scripted input",
  "spl": "index=os sourcetype=\"ceph:log\" earliest=-4h\n| search \"mds.\" AND (\"cap\" OR \"recall\" OR \"session\")\n| rex field=_raw \"(?i)mds\\.(?<mds_id>[a-z0-9_]+)\"\n| stats count as cap_events by mds_id, host\n| where cap_events > 50\n| sort - cap_events",
  "cimModels": [
    "N/A"
  ],
  "description": "Aggressive cap recall usually means clients are hoarding caps or MDS cache is undersized, causing metadata latency for CephFS users.",
  "value": "Keeps shared filesystems responsive for HPC and EDA workloads without blind MDS reboots.",
  "implementation": "Split index: `os` for logs, `storage` for JSON status. Add client IP via `rex` when present to notify noisy tenants.",
  "visualization": "Table (MDS, events), line chart over time.",
  "references": [
    {
      "title": "Ceph Documentation — monitoring",
      "url": "https://docs.ceph.com/en/latest/radosgw/"
    },
    {
      "title": "Red Hat Ceph Storage — troubleshooting",
      "url": "https://access.redhat.com/documentation/en-us/red_hat_ceph_storage/"
    }
  ],
  "knownFalsePositives": "Short spikes during approved changes, maintenance windows, or known batch jobs can match the rule; confirm against the vendor console and change calendar.",
  "equipment": [
    "ceph"
  ],
  "status": "draft",
  "grandmaExplanation": "We watch who is allowed on the storage network and who just logged in, so stray servers or surprise changes on the fabric are harder to miss.",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "9.x",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases"
}
