{
  "$schema": "../../schemas/uc.schema.json",
  "id": "6.4.13",
  "title": "NFS Export Capacity and Client Load",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'NFS Export Capacity and Client Load' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_nfs_export_capacity_and_client_load` (UC-6.4.13), SOC dashboard panel \"Operations — NFS Export Capacity and Client Load\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-6.4.13 positive (NFS, Export, Capacity, and, Client): ingest synthetic events into `index=storage` sourcetype='nas:nfs_export' where `ops`, `pct` grouped by `export_path`, `host` satisfies `pct` > 85, or `ops` > 10000 for 'NFS Export Capacity and Client Load'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-6.4.13 negative (Client Load): ingest benign `storage` / `nas:nfs_export` events where `export_path`, `host` stays below `pct` > 85, or `ops` > 10000, or exclude the test entity via `ops_exception_register.csv`. Variant 0 confirms zero alert rows for 'NFS Export Capacity and Client Load'."
  },
  "dataSources": "Per-export bytes used, NFS op counters Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "NetApp/Isilon API, Linux `nfsstat`, `exportfs -v` metrics — Universal Forwarder (Splunkbase 617)",
  "spl": "index=storage sourcetype=\"nas:nfs_export\"\n| eval used_pct=round(used_bytes/capacity_bytes*100,1)\n| timechart span=5m sum(ops_per_sec) as ops, avg(used_pct) as pct by export_path, host\n| where pct > 85 OR ops > 10000",
  "description": "Alerts on 'NFS Export Capacity and Client Load' (UC-6.4.13) using `index=storage` sourcetype `nas:nfs_export` by aggregating `ops`, `pct` grouped by `export_path`, `host` over `5m` and alerting when `pct` > 85, or `ops` > 10000. Keywords: NFS, Export, Capacity, and, Client. Focuses on Client Load behaviour so operations teams can triage UC-6.4.13 without ad-hoc log review.",
  "value": "UC-6.4.13 accelerates insight into Client Load ('NFS Export Capacity and Client Load') by tracking `ops`, `pct` on `nas:nfs_export` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Poll export statistics from NAS API or aggregated nfsd metrics. Alert on high used % or abnormal ops vs baseline.",
  "visualization": "Table (export, used %, ops/s), Line chart (ops and capacity trend), Bar chart (top exports by ops).",
  "cimModels": [
    "Performance"
  ],
  "cimSpl": "| tstats `summariesonly` max(Performance.storage_used_percent) as used_pct\n  from datamodel=Performance where nodename=Performance.Storage\n  by Performance.host Performance.object span=1h\n| where used_pct > 80\n| sort - used_pct",
  "dataModelAcceleration": "Enable Data Model Acceleration for the CIM Performance data model (Performance.Storage node / storage_used_percent) after vendor fields are aliased to CIM; otherwise the cimSpl tstats variant may return no rows.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Performance",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Performance",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `storage` (UC-6.4.13)** — Planned work for 'NFS Export Capacity and Client Load' shifts `ops`, `pct` over `span=5m` while `export_path`, `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `sum` on `nas:nfs_export`** — Scheduled automation elevates `host` without crossing `pct` > 85 for UC-6.4.13. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `export_path`, `host`** — Health checks in `storage` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `nas:nfs_export`** — First 48h baseline for UC-6.4.13 on `export_path` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `export_path`, `exception_reason`, `valid_until`.",
  "equipment": [
    "dell_emc",
    "hardware_bmc",
    "netapp"
  ],
  "equipmentModels": [
    "hardware_bmc_ilo"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: NetApp/Isilon API, Linux `nfsstat`, `exportfs -v` metrics.\n• Ensure the following data sources are available: Per-export bytes used, NFS op counters.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nPoll export statistics from NAS API or aggregated nfsd metrics. Alert on high used % or abnormal ops vs baseline.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=storage sourcetype=\"nas:nfs_export\"\n| eval used_pct=round(used_bytes/capacity_bytes*100,1)\n| timechart span=5m sum(ops_per_sec) as ops, avg(used_pct) as pct by export_path, host\n| where pct > 85 OR ops > 10000\n```\n\nUnderstanding this SPL\n\n**NFS Export Capacity and Client Load** — Export-level capacity and NFS operations/sec highlight hot exports and approaching full filesystems on NAS heads.\n\nDocumented **Data sources**: Per-export bytes used, NFS op counters. **App/TA** (typical add-on context): NetApp/Isilon API, Linux `nfsstat`, `exportfs -v` metrics. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: storage; **sourcetype**: nas:nfs_export. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=storage, sourcetype=\"nas:nfs_export\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **used_pct** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `timechart` plots the metric over time using **span=5m** buckets with a separate series **by export_path, host** — ideal for trending and alerting on this use case.\n• Filters the current rows with `where pct > 85 OR ops > 10000` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nCompare the same metric, object name, and interval in the vendor or cloud console (array, backup, or object store) that is the source of truth for this feed.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Pair alerts with the file-server or security team runbook and change calendar. Consider visualizations: Table (export, used %, ops/s), Line chart (ops and capacity trend), Bar chart (top exports by ops).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (NetApp/Isilon API, Linux `nfsstat`, `exportfs -v` metrics — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=nas:nfs_export into storage. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`export_path, host, ops, pct`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch how full your storage is getting and give you time to add space or clean up snapshots and old data before an application or job suddenly stops working.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
