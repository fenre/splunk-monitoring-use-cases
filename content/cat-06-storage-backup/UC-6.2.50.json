{
  "$schema": "../../schemas/uc.schema.json",
  "id": "6.2.50",
  "title": "TrueNAS jail and plugin CPU memory utilization",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Operations"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'TrueNAS jail and plugin CPU memory utilization' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_truenas_jail_and_plugin_cpu_memory_utilization` (UC-6.2.50), SOC dashboard panel \"Operations — TrueNAS jail and plugin CPU memory utilization\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-6.2.50 positive (TrueNAS, jail, and, plugin, CPU): ingest synthetic events into `index=storage` sourcetype='truenas:jail' where `cpu_pct`, `mem_pct` grouped by `jail` satisfies `cpu` > 85, or `mem` > 90 for 'TrueNAS jail and plugin CPU memory utilization'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-6.2.50 negative (memory utilization): ingest benign `storage` / `truenas:jail` events where `jail` stays below `cpu` > 85, or `mem` > 90, or exclude the test entity via `ops_exception_register.csv`. Variant 15 confirms zero alert rows for 'TrueNAS jail and plugin CPU memory utilization'."
  },
  "dataSources": "Jail metrics indexed as `truenas:jail` from `/api/v2.0/jail` or middleware stats Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "REST poller + HEC — Universal Forwarder (Splunkbase 617)",
  "spl": "index=storage sourcetype=\"truenas:jail\" earliest=-1h\n| eval jail=coalesce(jail_name, name)\n| eval cpu=coalesce(cpu_percent, cpu_usage)\n| eval mem=coalesce(mem_percent, memory_usage_percent)\n| where cpu > 85 OR mem > 90\n| timechart span=15m max(cpu) as cpu_pct max(mem) as mem_pct by jail",
  "description": "Surfaces UC-6.2.50 — TrueNAS, jail, and, plugin, CPU — using `index=storage` sourcetype `truenas:jail`, grouped by `jail`. Outputs `cpu_pct`, `mem_pct` each `15m`; alert when `cpu` > 85, or `mem` > 90. Primary focus: memory utilization. Purpose-built for 'TrueNAS jail and plugin CPU memory utilization' in Operations workflows.",
  "value": "UC-6.2.50 operationalises 'TrueNAS jail and plugin CPU memory utilization' against `truenas:jail` feeds so operators reduce operational risk and delayed incident response without manual correlation.",
  "implementation": "If API lacks per-jail CPU, use `jexec` scripted samples. Tag jails with owner email in KV lookup.",
  "visualization": "Line chart (CPU/mem by jail), table (peak usage).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "TrueNAS SCALE API documentation",
      "url": "https://www.truenas.com/docs/scale/scaletutorials/toptoolbar/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "TrueNAS CORE/SCALE docs — alerts",
      "url": "https://www.truenas.com/docs/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `storage` (UC-6.2.50)** — Planned work for 'TrueNAS jail and plugin CPU memory utilization' shifts `cpu_pct`, `mem_pct` over `span=15m` while `jail` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `max` on `truenas:jail`** — Scheduled automation elevates `jail` without crossing `cpu` > 85 for UC-6.2.50. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `jail`** — Health checks in `storage` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `truenas:jail`** — First 48h baseline for UC-6.2.50 on `jail` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `jail`, `exception_reason`, `valid_until`.",
  "equipment": [
    "truenas"
  ],
  "equipmentModels": [
    "truenas_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "9.x",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (REST poller + HEC — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=truenas:jail into storage. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`cpu, jail, mem`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We help you see how object storage is growing, who can reach it, and when something is exposed or mis-tagged, so cloud storage stays under control.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
