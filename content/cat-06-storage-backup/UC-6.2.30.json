{
  "$schema": "../../schemas/uc.schema.json",
  "id": "6.2.30",
  "title": "Ceph OSD recovery and rebalance throughput versus cluster limits",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Operational"
  ],
  "splunkPillar": "Observability",
  "dataSources": "Cluster recovery flags in `ceph:status` (`recovering`, `recovering_bytes_per_sec`, `rebalancing`)",
  "app": "`ceph status -f json` scripted input",
  "spl": "index=storage sourcetype=\"ceph:status\" earliest=-4h\n| eval rbps=coalesce(recovering_bytes_per_sec, recovery_bytes_per_sec, recover_bps)\n| eval objs=coalesce(recovering_objects_per_sec, recovery_ops)\n| where match(_raw, \"recover\") OR match(_raw, \"rebalanc\") OR isnotnull(rbps)\n| timechart span=15m max(rbps) as recovery_bps max(objs) as recovery_ops by cluster_name",
  "description": "Recovery traffic competes with client IOPS. Monitoring rebalance throughput against configured limits validates throttling and explains user-visible latency during failures.",
  "value": "Lets operators choose between faster rebuild and application QoS during partial failures.",
  "implementation": "Parse `progress_events` if available in newer Ceph releases. Join with business-hours lookup to tighten limits interactively.",
  "visualization": "Area chart (recovery Bps), reference line for max osd recovery cap.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Ceph Documentation — monitoring",
      "url": "https://docs.ceph.com/en/latest/radosgw/"
    },
    {
      "title": "Red Hat Ceph Storage — troubleshooting",
      "url": "https://access.redhat.com/documentation/en-us/red_hat_ceph_storage/"
    }
  ],
  "equipment": [
    "ceph"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "9.x",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases"
}
