{
  "$schema": "../../schemas/uc.schema.json",
  "id": "21.8.10",
  "title": "Content Delivery Network Cache Hit Ratio",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "industry": "Telecommunications",
  "exclusions": "Covers the detection logic documented for 'Content Delivery Network Cache Hit Ratio' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `ops_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_content_delivery_network_cache_hit_ratio` (UC-21.8.10), SOC dashboard panel \"Operations — Content Delivery Network Cache Hit Ratio\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=telecom` sourcetype='cdn:performance' where `pop_id` satisfies `hit_ratio` < 85 for UC-21.8.10 ('Content Delivery Network Cache Hit Ratio'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `telecom` / `cdn:performance` events where `pop_id` stays below `hit_ratio` < 85, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-21.8.10."
  },
  "dataSources": "`index=telecom` `sourcetype=\"cdn:performance\"` (pop_id, cache_status, bytes_served, request_count) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "CDN raw logs / analytics API via HEC — Universal Forwarder (Splunkbase 617)",
  "spl": "index=telecom sourcetype=\"cdn:performance\"\n| eval hit=if(lower(cache_status) IN (\"hit\",\"tcp_hit\",\"mem_hit\"),1,0)\n| bin _time span=1h\n| stats sum(request_count) as reqs, sum(eval(if(hit=1,request_count,0))) as hit_reqs by pop_id, _time\n| eval hit_ratio=round(100*hit_reqs/nullif(reqs,0),2)\n| where hit_ratio < 85 AND reqs > 1000\n| table _time, pop_id, reqs, hit_reqs, hit_ratio",
  "description": "Low cache hit ratios increase origin load and subscriber latency; trending hit ratio by POP and content type guides cache sizing and TTL policy without analyzing subscriber CDRs.",
  "value": "Service and risk leaders use signals from Content Delivery Network Cache Hit Ratio to move crews, budget, and customer or regulator communications while the situation is still controllable, so the organization steers from live data rather than a post-incident report reduces operational risk and delayed incident response by surfacing actionable rows before manual triage or audit channels escalate the issue.",
  "implementation": "Map vendor cache hit tokens to `cache_status` in props. Exclude purge and error responses from denominator if tagged. Compare POPs to identify misconfigured origins. **Domain context:** CDN hit ratio varies by content type (live vs static) and TTL policy—compare POPs with similar traffic mix. **Splunk:** Extend `hit`/`cache_status` mapping for Fastly/Akamai/CloudFront variants (`MISS`, `STALE`, etc.); `reqs > 1000` avoids noise on low-traffic POPs.",
  "visualization": "Line chart (hit ratio by POP), Bar chart (worst POPs), Map (if geo coordinates available).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `telecom`** — Planned work for 'Content Delivery Network Cache Hit Ratio' shifts `sum` over `span=1h` while `pop_id` remains in-scope for UC-21.8.10. Cross-check CMDB CHG.\n\n2. **Batch `sum` on `cdn:performance`** — Scheduled automation elevates `_time` without crossing `hit_ratio` < 85. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `pop_id`** — Health checks in `telecom` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `cdn:performance`** — First 48h baseline for `pop_id` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `pop_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: CDN raw logs / analytics API via HEC.\n• Ensure the following data sources are available: `index=telecom` `sourcetype=\"cdn:performance\"` (pop_id, cache_status, bytes_served, request_count).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMap vendor cache hit tokens to `cache_status` in props. Exclude purge and error responses from denominator if tagged. Compare POPs to identify misconfigured origins. **Domain context:** CDN hit ratio varies by content type (live vs static) and TTL policy—compare POPs with similar traffic mix. **Splunk:** Extend `hit`/`cache_status` mapping for Fastly/Akamai/CloudFront variants (`MISS`, `STALE`, etc.); `reqs > 1000` avoids noise on low-traffic POPs.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=telecom sourcetype=\"cdn:performance\"\n| eval hit=if(lower(cache_status) IN (\"hit\",\"tcp_hit\",\"mem_hit\"),1,0)\n| bin _time span=1h\n| stats sum(request_count) as reqs, sum(eval(if(hit=1,request_count,0))) as hit_reqs by pop_id, _time\n| eval hit_ratio=round(100*hit_reqs/nullif(reqs,0),2)\n| where hit_ratio < 85 AND reqs > 1000\n| table _time, pop_id, reqs, hit_reqs, hit_ratio\n```\n\nUnderstanding this SPL\n\n**Content Delivery Network Cache Hit Ratio** — Low cache hit ratios increase origin load and subscriber latency; trending hit ratio by POP and content type guides cache sizing and TTL policy without analyzing subscriber CDRs.\n\nDocumented **Data sources**: `index=telecom` `sourcetype=\"cdn:performance\"` (pop_id, cache_status, bytes_served, request_count). **App/TA** (typical add-on context): CDN raw logs / analytics API via HEC. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: telecom; **sourcetype**: cdn:performance. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=telecom, sourcetype=\"cdn:performance\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **hit** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by pop_id, _time** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **hit_ratio** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where hit_ratio < 85 AND reqs > 1000` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Content Delivery Network Cache Hit Ratio**): table _time, pop_id, reqs, hit_reqs, hit_ratio\n\n\nStep 3 — Validate\nCompare KPIs or record counts with the EMS/performance management export or mediation control room view for the same element and interval. Align vendor time zones and collection periods before tuning thresholds.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (hit ratio by POP), Bar chart (worst POPs), Map (if geo coordinates available).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (CDN raw logs / analytics API via HEC — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=cdn:performance into telecom. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, hit, hit_ratio, pop_id`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch CDN hit ratios and origin load. We help you catch bad caching before video stalls spike.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
