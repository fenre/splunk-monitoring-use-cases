{
  "$schema": "../../schemas/uc.schema.json",
  "id": "21.6.3",
  "title": "In-Store Wi-Fi and Network Infrastructure Health",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "industry": "Retail and E-Commerce",
  "exclusions": "Covers the detection logic documented for 'In-Store Wi-Fi and Network Infrastructure Health' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `ops_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_in_store_wi_fi_and_network_infrastructure_health` (UC-21.6.3), SOC dashboard panel \"Operations — In-Store Wi-Fi and Network Infrastructure Health\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-21.6.3 positive (Store, and, Network, Infrastructure, Health): ingest synthetic events into `index=retail` sourcetype='wifi:controller' where `fails`, `avg_clients`, `max_cpu` grouped by `store_id`, `ap_name`, `_time` satisfies `avg_clients` > 0, `fails` > 20, `max_cpu` > 85, or `fail_rate` > 2 for 'In-Store Wi-Fi and Network Infrastructure Health'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-21.6.3 negative (Infrastructure Health): ingest benign `retail` / `wifi:controller` events where `store_id`, `ap_name`, `_time` stays below `avg_clients` > 0, `fails` > 20, `max_cpu` > 85, or `fail_rate` > 2, or exclude the test entity via `ops_exception_register.csv`. Variant 14 confirms zero alert rows for 'In-Store Wi-Fi and Network Infrastructure Health'."
  },
  "dataSources": "`index=retail` `sourcetype=\"wifi:controller\"` (store_id, ap_name, client_count, assoc_failures, cpu_pct) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Wi-Fi controller syslog / SNMP trap HEC — Universal Forwarder (Splunkbase 617)",
  "spl": "index=retail sourcetype=\"wifi:controller\"\n| bin _time span=5m\n| stats sum(assoc_failures) as fails, avg(client_count) as avg_clients, max(cpu_pct) as max_cpu by store_id, ap_name, _time\n| eval fail_rate=if(avg_clients>0, round(100*fails/avg_clients,2), fails)\n| where fails > 20 OR max_cpu > 85 OR fail_rate > 2\n| table _time, store_id, ap_name, fails, avg_clients, max_cpu, fail_rate",
  "description": "Detects UC-21.6.3 — Store, and, Network, Infrastructure, Health — using `index=retail` sourcetype `wifi:controller`, grouped by `store_id`, `ap_name`, `_time`. Outputs `fails`, `avg_clients`, `max_cpu` each `5m`; alert when `avg_clients` > 0, `fails` > 20, `max_cpu` > 85, or `fail_rate` > 2. Primary focus: Infrastructure Health. Purpose-built for 'In-Store Wi-Fi and Network Infrastructure Health' in Operations workflows.",
  "value": "Operations and risk leaders in Retail and E-Commerce use signals from In-Store Wi-Fi and Network Infrastructure Health to move crews, budget, and customer or regulator communications while the situation is still controllable, so the organization steers from live telemetry rather than a post-incident report.",
  "implementation": "Normalize vendor-specific fields in props/transforms; retain `ap_name` and `store_id` on every sample. Alert on controller CPU and on APs with repeated association failures compared to peers in the same store. **Domain context:** Guest Wi-Fi and mPOS share airtime—high `assoc_failures` during promotions may be RF contention, not controller fault. **Splunk:** `fail_rate` uses fails per avg_clients—validate denominator when client counts are sampled sparsely.",
  "visualization": "Time chart (assoc failures), Status grid (AP health), Line chart (controller CPU).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `retail` (UC-21.6.3)** — Planned work for 'In-Store Wi-Fi and Network Infrastructure Health' shifts `fails`, `avg_clients`, `max_cpu` over `span=5m` while `store_id`, `ap_name`, `_time` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `sum` on `wifi:controller`** — Scheduled automation elevates `ap_name` without crossing `avg_clients` > 0 for UC-21.6.3. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `store_id`, `ap_name`, `_time`** — Health checks in `retail` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `wifi:controller`** — First 48h baseline for UC-21.6.3 on `store_id` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `store_id`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1078"
  ],
  "equipment": [
    "snmp",
    "syslog"
  ],
  "equipmentModels": [
    "snmp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Wi-Fi controller syslog / SNMP trap HEC.\n• Ensure the following data sources are available: `index=retail` `sourcetype=\"wifi:controller\"` (store_id, ap_name, client_count, assoc_failures, cpu_pct).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nNormalize vendor-specific fields in props/transforms; retain `ap_name` and `store_id` on every sample. Alert on controller CPU and on APs with repeated association failures compared to peers in the same store. **Domain context:** Guest Wi-Fi and mPOS share airtime—high `assoc_failures` during promotions may be RF contention, not controller fault. **Splunk:** `fail_rate` uses fails per avg_clients—validate denominator when client counts are sampled sparsely.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=retail sourcetype=\"wifi:controller\"\n| bin _time span=5m\n| stats sum(assoc_failures) as fails, avg(client_count) as avg_clients, max(cpu_pct) as max_cpu by store_id, ap_name, _time\n| eval fail_rate=if(avg_clients>0, round(100*fails/avg_clients,2), fails)\n| where fails > 20 OR max_cpu > 85 OR fail_rate > 2\n| table _time, store_id, ap_name, fails, avg_clients, max_cpu, fail_rate\n```\n\nUnderstanding this SPL\n\n**In-Store Wi-Fi and Network Infrastructure Health** — mPOS, guest Wi-Fi, and IoT rely on store LAN/WLAN; tracking AP association failures and controller health prevents silent outages during promotions.\n\nDocumented **Data sources**: `index=retail` `sourcetype=\"wifi:controller\"` (store_id, ap_name, client_count, assoc_failures, cpu_pct). **App/TA** (typical add-on context): Wi-Fi controller syslog / SNMP trap HEC. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: retail; **sourcetype**: wifi:controller. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=retail, sourcetype=\"wifi:controller\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by store_id, ap_name, _time** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **fail_rate** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where fails > 20 OR max_cpu > 85 OR fail_rate > 2` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **In-Store Wi-Fi and Network Infrastructure Health**): table _time, store_id, ap_name, fails, avg_clients, max_cpu, fail_rate\n\n\nStep 3 — Validate\nReconcile amounts and counts with the POS journal or store close report for the same register and shift. Validate that returns, voids, and training transactions are tagged so they can be filtered if needed.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Time chart (assoc failures), Status grid (AP health), Line chart (controller CPU).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Wi-Fi controller syslog / SNMP trap HEC — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=wifi:controller into retail. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, ap_name, avg_clients, fail_rate`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch in-store Wi-Fi and branch network health. We help you catch pain before staff devices and hand scanners fail.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
