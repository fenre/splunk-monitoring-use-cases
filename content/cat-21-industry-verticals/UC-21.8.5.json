{
  "$schema": "../../schemas/uc.schema.json",
  "id": "21.8.5",
  "title": "Service Activation and Billing Mediation Latency",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "industry": "Telecommunications",
  "exclusions": "Covers the detection logic documented for 'Service Activation and Billing Mediation Latency' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `ops_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_service_activation_and_billing_mediation_latency` (UC-21.8.5), SOC dashboard panel \"Operations — Service Activation and Billing Mediation Latency\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-21.8.5 positive (Service, Activation, and, Billing, Mediation): ingest synthetic events into `index=telecom` sourcetype='mediation:event' where `avg_lat`, `max_q`, `vol` grouped by `_time` satisfies `avg_lat` > 120000, `p95_lat` > 300000, or `max_q` > 500000 for 'Service Activation and Billing Mediation Latency'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-21.8.5 negative (Mediation Latency): ingest benign `telecom` / `mediation:event` events where `_time` stays below `avg_lat` > 120000, `p95_lat` > 300000, or `max_q` > 500000, or exclude the test entity via `ops_exception_register.csv`. Variant 12 confirms zero alert rows for 'Service Activation and Billing Mediation Latency'."
  },
  "dataSources": "`index=telecom` `sourcetype=\"mediation:event\"` (batch_id, records_in, records_out, latency_ms, queue_depth) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Mediation platform logs via HEC — Universal Forwarder (Splunkbase 617)",
  "spl": "index=telecom sourcetype=\"mediation:event\"\n| bin _time span=5m\n| stats avg(latency_ms) as avg_lat, perc95(latency_ms) as p95_lat, max(queue_depth) as max_q, sum(records_in) as vol by _time\n| where avg_lat > 120000 OR p95_lat > 300000 OR max_q > 500000\n| eval avg_min=round(avg_lat/60000,2), p95_min=round(p95_lat/60000,2)\n| table _time, avg_min, p95_min, max_q, vol",
  "description": "Surfaces UC-21.8.5 — Service, Activation, and, Billing, Mediation — using `index=telecom` sourcetype `mediation:event`, grouped by `_time`. Outputs `avg_lat`, `max_q`, `vol` each `5m`; alert when `avg_lat` > 120000, `p95_lat` > 300000, or `max_q` > 500000. Primary focus: Mediation Latency. Purpose-built for 'Service Activation and Billing Mediation Latency' in Operations workflows.",
  "value": "Operations and risk leaders in Telecommunications use signals from Service Activation and Billing Mediation Latency to move crews, budget, and customer or regulator communications while the situation is still controllable, so the organization steers from live telemetry rather than a post-incident report.",
  "implementation": "Normalize timestamps to when batches complete, not file arrival. Alert on sustained queue growth with derivative search on `queue_depth`. Coordinate thresholds with billing close calendar. **Domain context:** Mediation/rating sits between network CDRs and billing—latency spikes near invoice close or rate plan changes are common; separate batch vs streaming pipelines in tagging. **Splunk:** Thresholds `120000`/`300000` ms are examples—tie to billing window SLAs; `avg_lat`/`p95_lat` on 5m buckets can be noisy for hourly batches.",
  "visualization": "Time chart (latency and queue depth), Area chart (records processed), Single value (backlog breach).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `telecom` (UC-21.8.5)** — Planned work for 'Service Activation and Billing Mediation Latency' shifts `avg_lat`, `max_q`, `vol` over `span=5m` while `_time` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `mediation:event`** — Scheduled automation elevates `avg_min` without crossing `avg_lat` > 120000 for UC-21.8.5. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `_time`** — Health checks in `telecom` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `mediation:event`** — First 48h baseline for UC-21.8.5 on `_time` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `_time`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1078"
  ],
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Mediation platform logs via HEC.\n• Ensure the following data sources are available: `index=telecom` `sourcetype=\"mediation:event\"` (batch_id, records_in, records_out, latency_ms, queue_depth).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nNormalize timestamps to when batches complete, not file arrival. Alert on sustained queue growth with derivative search on `queue_depth`. Coordinate thresholds with billing close calendar. **Domain context:** Mediation/rating sits between network CDRs and billing—latency spikes near invoice close or rate plan changes are common; separate batch vs streaming pipelines in tagging. **Splunk:** Thresholds `120000`/`300000` ms are examples—tie to billing window SLAs; `avg_lat`/`p95_lat` on 5m buckets …\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=telecom sourcetype=\"mediation:event\"\n| bin _time span=5m\n| stats avg(latency_ms) as avg_lat, perc95(latency_ms) as p95_lat, max(queue_depth) as max_q, sum(records_in) as vol by _time\n| where avg_lat > 120000 OR p95_lat > 300000 OR max_q > 500000\n| eval avg_min=round(avg_lat/60000,2), p95_min=round(p95_lat/60000,2)\n| table _time, avg_min, p95_min, max_q, vol\n```\n\nUnderstanding this SPL\n\n**Service Activation and Billing Mediation Latency** — Mediation pipelines must deliver rated records to billing within windows; end-to-end latency and backlog depth prevent revenue leakage and rating disputes distinct from raw CDR analytics use cases.\n\nDocumented **Data sources**: `index=telecom` `sourcetype=\"mediation:event\"` (batch_id, records_in, records_out, latency_ms, queue_depth). **App/TA** (typical add-on context): Mediation platform logs via HEC. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: telecom; **sourcetype**: mediation:event. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=telecom, sourcetype=\"mediation:event\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by _time** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where avg_lat > 120000 OR p95_lat > 300000 OR max_q > 500000` — typically the threshold or rule expression for this monitoring goal.\n• `eval` defines or adjusts **avg_min** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Pipeline stage (see **Service Activation and Billing Mediation Latency**): table _time, avg_min, p95_min, max_q, vol\n\n\nStep 3 — Validate\nCompare KPIs or record counts with the EMS/performance management export or mediation control room view for the same element and interval. Align vendor time zones and collection periods before tuning thresholds.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Time chart (latency and queue depth), Area chart (records processed), Single value (backlog breach).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Mediation platform logs via HEC — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=mediation:event into telecom. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, avg_lat, max_q, p95_lat`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch service activation and billing delays. We help you catch handoff breaks before revenue and NPS take a hit.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
