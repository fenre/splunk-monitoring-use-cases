{
  "$schema": "../../schemas/uc.schema.json",
  "id": "21.3.20",
  "title": "DIPS Arena User Authentication and SSO Monitoring",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Availability"
  ],
  "splunkPillar": "Security",
  "industry": "Healthcare",
  "exclusions": "Covers the detection logic documented for 'DIPS Arena User Authentication and SSO Monitoring' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `ops_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_dips_arena_user_authentication_and_sso_monitorin` (UC-21.3.20), SOC dashboard panel \"Operations — DIPS Arena User Authentication and SSO Monitoring\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=healthcare` sourcetype='dips:federation:auth' where `auth_flow` satisfies `fail_pct` > 10 for UC-21.3.20 ('DIPS Arena User Authentication and SSO Monitoring'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `healthcare` / `dips:federation:auth` events where `auth_flow` stays below `fail_pct` > 10, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-21.3.20."
  },
  "dataSources": "`index=healthcare` `sourcetype=\"dips:federation:auth\"` fields `auth_flow`, `result`, `user_type`, `client_id`, `response_time_ms`, `error_code` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "DIPS Federation Service logs via HEC or Universal Forwarder — Universal Forwarder (Splunkbase 617)",
  "spl": "index=healthcare sourcetype=\"dips:federation:auth\"\n| bin _time span=10m\n| stats sum(eval(if(result=\"success\", 1, 0))) as success,\n  sum(eval(if(result=\"failure\", 1, 0))) as failures,\n  dc(client_id) as unique_clients,\n  avg(response_time_ms) as avg_auth_ms by auth_flow, _time\n| eval fail_pct=round(failures/(success+failures)*100,1)\n| where fail_pct > 10 OR avg_auth_ms > 5000\n| table _time, auth_flow, success, failures, fail_pct, avg_auth_ms, unique_clients",
  "description": "DIPS Arena authentication flows through the DIPS Federation Service using OAuth/OIDC, with support for DIPS credentials, Microsoft Entra ID, and SMART on FHIR SSO. Authentication failures lock clinicians out of the EHR during critical care moments. Monitoring authentication success rates, latency, and failure patterns detects identity infrastructure issues before they cascade into widespread access disruptions.",
  "value": "Service and risk leaders use signals from DIPS Arena User Authentication and SSO Monitoring to move crews, budget, and customer or regulator communications while the situation is still controllable, so the organization steers from live data rather than a post-incident report reduces operational risk and delayed incident response by surfacing actionable rows before manual triage or audit channels escalate the issue.",
  "implementation": "Collect authentication event logs from the DIPS Federation Service. The service handles Authorization Code Flow (interactive clinician login), Client Credentials Flow (system integrations), and SMART on FHIR (embedded applications with SSO). Parse each event for authentication flow type, result (success/failure), error code, and response time. Alert on: authentication failure rate exceeding 10% within a 10-minute window, SSO response time exceeding 5 seconds (causes UI timeouts), repeated failures from a single client application (broken integration), or complete authentication service unavailability. Correlate with Microsoft Entra ID status if using federated identity.",
  "visualization": "Timechart (success vs failure by auth flow), Bar chart (failure reasons), Single value (current auth success rate).",
  "cimModels": [
    "Authentication"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Authentication.Authentication by Authentication.action, Authentication.user, Authentication.src span=10m | sort - count",
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "CIM: Authentication",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Authentication",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `healthcare`** — Planned work for 'DIPS Arena User Authentication and SSO Monitoring' shifts `sum` over `span=10m` while `auth_flow` remains in-scope for UC-21.3.20. Cross-check CMDB CHG.\n\n2. **Batch `sum` on `dips:federation:auth`** — Scheduled automation elevates `_time` without crossing `fail_pct` > 10. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `auth_flow`** — Health checks in `healthcare` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `dips:federation:auth`** — First 48h baseline for `auth_flow` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `auth_flow`, `exception_reason`, `valid_until`.",
  "equipment": [
    "m365"
  ],
  "equipmentModels": [
    "m365_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: DIPS Federation Service logs via HEC or Universal Forwarder.\n• Ensure the following data sources are available: `index=healthcare` `sourcetype=\"dips:federation:auth\"` fields `auth_flow`, `result`, `user_type`, `client_id`, `response_time_ms`, `error_code`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nCollect authentication event logs from the DIPS Federation Service. The service handles Authorization Code Flow (interactive clinician login), Client Credentials Flow (system integrations), and SMART on FHIR (embedded applications with SSO). Parse each event for authentication flow type, result (success/failure), error code, and response time. Alert on: authentication failure rate exceeding 10% within a 10-minute window, SSO response time exceeding 5 seconds (causes UI timeouts), repeated failur…\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=healthcare sourcetype=\"dips:federation:auth\"\n| bin _time span=10m\n| stats sum(eval(if(result=\"success\", 1, 0))) as success,\n  sum(eval(if(result=\"failure\", 1, 0))) as failures,\n  dc(client_id) as unique_clients,\n  avg(response_time_ms) as avg_auth_ms by auth_flow, _time\n| eval fail_pct=round(failures/(success+failures)*100,1)\n| where fail_pct > 10 OR avg_auth_ms > 5000\n| table _time, auth_flow, success, failures, fail_pct, avg_auth_ms, unique_clients\n```\n\nUnderstanding this SPL\n\n**DIPS Arena User Authentication and SSO Monitoring** — DIPS Arena authentication flows through the DIPS Federation Service using OAuth/OIDC, with support for DIPS credentials, Microsoft Entra ID, and SMART on FHIR SSO. Authentication failures lock clinicians out of the EHR during critical care moments. Monitoring authentication success rates, latency, and failure patterns detects identity infrastructure issues before they cascade into widespread access disruptions.\n\nDocumented **Data sources**: `index=healthcare` `sourcetype=\"dips:federation:auth\"` fields `auth_flow`, `result`, `user_type`, `client_id`, `response_time_ms`, `error_code`. **App/TA** (typical add-on context): DIPS Federation Service logs via HEC or Universal Forwarder. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: healthcare; **sourcetype**: dips:federation:auth. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=healthcare, sourcetype=\"dips:federation:auth\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by auth_flow, _time** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **fail_pct** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where fail_pct > 10 OR avg_auth_ms > 5000` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **DIPS Arena User Authentication and SSO Monitoring**): table _time, auth_flow, success, failures, fail_pct, avg_auth_ms, unique_clients\n\nStep 3 — Validate\nCompare failure rates, latency, and success counts with the identity service or federation console and Microsoft Entra sign-in audit for the same window. Spot-check a few indexed events against the DIPS (or EHR) vendor audit export so field names and flows line up with how clinicians actually sign in.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Timechart (success vs failure by auth flow), Bar chart (failure reasons), Single value (current auth success rate).",
  "grandmaExplanation": "We watch hospital sign-in and single sign-on health. We help you catch identity problems before clinicians are locked out at the worst moment.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
