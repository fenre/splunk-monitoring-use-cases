<!-- AUTO-GENERATED from UC-22.2.24.json — DO NOT EDIT -->

---
id: "22.2.24"
title: "NIS2 Business Continuity and ICT Continuity Evidence"
criticality: "critical"
splunkPillar: "IT Operations"
---

# UC-22.2.24 · NIS2 Business Continuity and ICT Continuity Evidence

## Description

Combines a register of when each in-scope service last proved its continuity plan (tabletop or drill) with measured outage seconds from ITSI summaries so you can see both testing cadence slip and real recoveries that exceeded the declared recovery-time objective.

## Value

Executives answering NIS2 questions need more than a policy binder—they need dated proof that exercises happened and that major outages stayed inside agreed time boxes; this automation stitches telemetry and ticket metadata into one reviewer-friendly table instead of three separate spreadsheets.

## Implementation

(1) Maintain `nis2_bcp_test_register.csv` under `lookups/` with one row per critical service and numeric `rto_minutes`. (2) Map `service_name` values to ITSI service IDs so `join` keys stay stable. (3) Install ITSI (1841); verify `index=itsi_summary` receives alert_duration populated by glass tables or deep dives. (4) Configure ServiceNow add-on (1928) `inputs.conf` modular input polling `/api/now/table/incident` into `index=itsm` as `sourcetype=snow:incident`. (5) Schedule `UC-22.2.24` weekly; email compliance when `months_since>12` or `rto_breach=1`.

## Detailed Implementation

Prerequisites
• Splunk IT Service Intelligence (Splunkbase 1841) with ITSI role `itoa_admin` or `itoa_analyst` for builders and a read-only `itsi_viewer` for auditors.
• KV store or CSV lookup `nis2_bcp_test_register.csv` in TA_LOOKUP app on search head; fields must include `service_name`, `last_tabletop_date`, `rto_minutes`.
• `index=itsi_summary` searchable for **90 days** minimum; confirm via Settings → Indexes.
• ServiceNow SaaS reachable over HTTPS; Splunk Add-on for ServiceNow (Splunkbase 1928) with OAuth storage in `passwords.conf`; inputs poll `https://<instance>.service-now.com/api/now/table/incident`.
• Optional: live `index=itsi` `sourcetype=itsi:service_health` for spot validation of scores against dashboards (Configuration → Services).

Step 1 — Configure data collection
In ITSI, ensure each NIS2-scoped service has KPIs tied to real entities; activate Episode Monitoring only where needed to avoid duplicate `alert_duration`.

On the ServiceNow add-on, create a modular input with filter `sysparm_query=category=ict_continuity^ORu_business_serviceISNOTEMPTY` (adapt to your CMDB fields). Force `sourcetype=snow:incident` in `props.conf` and extract `number`, `opened_at`, `resolved_at`, `u_priority`.

Upload the BCP register with `inputlookup` permissions limited to continuity team **RBAC** role `nis2_continuity`.

Step 2 — Create the search and alert
Run the join SPL nightly. The `months_since>12` arm surfaces stale drills; the ITSI subsearch sums `alert_duration` where severity is Critical/High—adapt severities to your thresholds. If `join` drops rows, switch to `| join type=left service_name` to keep overdue tests even without outages.

For richer proof, add a union clause scanning `index=itsm sourcetype=snow:incident category=ict_continuity` to list incident numbers beside RTO math.

Schedule weekly, Monday 06:00 local, earliest `-90d@d` for summaries, throttle on `service_name` for **7 days**.

Step 3 — Validate
Pick a service with known June drill: `inputlookup` should show `months_since` <12 and no row after filter. Inject a replayed ITSI episode of **600 seconds**; confirm `downtime_seconds` matches Episode Analytics UI within 5%.

Cross-check ServiceNow incident `resolved_at - opened_at` against ITSI duration; if they diverge >10%, fix timezone parsing (`strptime` with `%z`).

Step 4 — Operationalize
Dashboard “NIS2 continuity posture”: panel 1 overdue tests; panel 2 RTO breaches with drilldown to ServiceNow `number`; panel 3 timeline of `last_tabletop_date`.

Wire alert to Microsoft Teams governance channel plus JIRA Compliance project as optional webhook.

Step 5 — Troubleshooting
• **Empty itsi_summary** — verify Episode Aggregation saved searches running; check `scheduler.log` for skipped jobs. • **Join removes everything** — mismatch on `service_name` case; normalize with `| eval service_name=lower(service_name)` both sides. • **ServiceNow 401** — rotate OAuth refresh token in Storage Passwords. • **False RTO breach** — `alert_duration` includes maintenance; join `lookup change_calendar` and subtract approved windows with `eval downtime_seconds=max(downtime_seconds-maintenance_seconds,0)` using **24 hour** lookahead.


## SPL

```spl
| inputlookup nis2_bcp_test_register.csv
| eval test_date_epoch=strptime(last_tabletop_date,"%Y-%m-%d")
| eval months_since=round((now()-test_date_epoch)/2592000,1)
| where months_since > 12
| join service_name [
    search index=itsi_summary earliest=-90d
    | where severity IN ("Critical","High")
    | stats sum(alert_duration) as downtime_seconds by service_name
]
| eval rto_seconds=rto_minutes*60
| eval rto_breach=if(downtime_seconds > rto_seconds, 1, 0)
| table service_name, months_since, downtime_seconds, rto_seconds, rto_breach
```

## Visualization

Table (overdue tests and RTO breaches), Timeline (tests and incidents), Single value (services breaching RTO). Key fields: `downtime_seconds`, `months_since`, `rto_breach`, `rto_seconds`, `service_name`, `test_date_epoch`.

## References

- [Splunk ITSI](https://splunkbase.splunk.com/app/1841)
- [Splunk Add-on for ServiceNow](https://splunkbase.splunk.com/app/1928)
- [Directive (EU) 2022/2555 (NIS2)](https://eur-lex.europa.eu/eli/dir/2022/2555/oj)
- [Commission Implementing Regulation (EU) 2024/2690](https://eur-lex.europa.eu/eli/reg_impl/2024/2690/oj)
- [ENISA NIS2 Technical Implementation Guidance](https://www.enisa.europa.eu/publications/nis2-technical-implementation-guidance)
- [ITSI service health deep dives](https://docs.splunk.com/Documentation/ITSI/latest/Admin/Introduction)
