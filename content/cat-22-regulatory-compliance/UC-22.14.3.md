<!-- AUTO-GENERATED from UC-22.14.3.json — DO NOT EDIT -->

---
id: "22.14.3"
title: "Audit Storage Capacity and Index Growth Guardrails"
criticality: "high"
splunkPillar: "Security"
---

# UC-22.14.3 · Audit Storage Capacity and Index Growth Guardrails

> **Criticality:** High &middot; **Difficulty:** Intermediate &middot; **Pillar:** Security &middot; **Type:** Security, Compliance

*We detects indexes approaching disk or license limits that could cause loss of audit evidence, supporting AU-4 audit storage capacity management. That helps federal and agency security reviews match what the systems actually do day to day.*

---

## Description

Detects indexes approaching disk or license limits that could cause loss of audit evidence, supporting AU-4 audit storage capacity management.

## Value

Gives the responsible team auditor-ready evidence for NIST 800-53 compliance by automating regulatory compliance evidence. Replaces manual evidence gathering with a continuous Splunk search that produces structured, time-stamped artefacts suitable for regulatory examination and internal audit review.

## Implementation

(1) Run from a scheduled search with `rest` rights; (2) compare `size_gb` to business thresholds by index class; (3) integrate license usage from `_internal` for combined risk scoring; (4) open change requests before forced rolling.

## Evidence

Saved search results archived to the `audit_evidence` index with NIST 800-53 control tagging; scheduled PDF/CSV exports for auditor consumption; Splunk dashboard screenshots with timestamp watermarks capturing control operating effectiveness over the review period.

## Control test

### Positive scenario

Inject a synthetic test event that satisfies the detection criteria for 'Audit Storage Capacity and Index Growth Guardrails' — for example, create a log entry with all required fields populated to match the SPL filter conditions. Verify the search fires within the expected schedule interval, the alert populates the compliance dashboard, and the event routes correctly to the evidence index with proper NIST 800-53 control tagging.

### Negative scenario

Ingest a batch of test events that are structurally similar but explicitly do NOT meet the detection threshold — e.g., events with compliant values, timestamps outside the monitoring window, or source systems excluded by the scope filter. Verify the search produces zero results, no false-positive alerts fire, and the compliance posture score remains unaffected for the NIST 800-53 control objective.

## Detailed Implementation

## Prerequisites

- **Splunk**: Enterprise ≥9.2 or Splunk Cloud (current). Scheduling requires a search head not under memory pressure (check `index=_internal sourcetype=splunkd component=ProcessRunner` for OOM warnings before deploying a new scheduled search).
- **Regulatory mapping**: This detection evidences clause `AU-4`. The control objective is: "Evidence that NIST 800-53 AU-4 is enforced — Splunk UC-22.14.3: Audit Storage Capacity and Index Growth Guardrails." — every alert payload should include this text so downstream consumers know the compliance purpose.

## Step 1 — Configure data collection

Data collection is bespoke for this UC — no single canonical TA parses every source the detection relies on. Route data into the relevant indexes under the relevant sourcetypes, and document the ingestion path in the UC runbook so future engineers know where each field originates.


Four distinct operational considerations shape this detection's rollout:


- **Hint 1:** Run from a scheduled search with `rest` rights. Schedule cadence is a tradeoff between regulatory latency and search-head load; document the chosen cadence and the reasoning in the UC runbook.

- **Hint 2:** compare `size_gb` to business thresholds by index class. This is an operational consideration that influences either the detection's accuracy or its maintainability — document the approach chosen in the UC runbook.

- **Hint 3:** integrate license usage from `_internal` for combined risk scoring. This is an operational consideration that influences either the detection's accuracy or its maintainability — document the approach chosen in the UC runbook.

- **Hint 4:** open change requests before forced rolling. This is an operational consideration that influences either the detection's accuracy or its maintainability — document the approach chosen in the UC runbook.

## Step 2 — Implement the search

This search aggregates `count()` across the full search range, grouped by `the analytical dimensions`. For "Audit Storage Capacity and Index Growth Guardrails," the aggregation produces the result shape required by the NIST 800-53 control — each row is a summary record the regulation expects to be presentable at audit time. The aggregation approach is chosen to produce one result row per analytical unit, which is the typical requirement for compliance dashboarding and report generation.

Why aggregate at search time rather than rely on raw events: compliance reporting requires summarisation to be defensible and stable. A raw-event dashboard can show different counts depending on the exact second it was queried; an aggregated result rounded to the UC's schedule produces consistent numbers across the reporting period. If the downstream consumer (GRC platform, regulator submission) expects stable daily/weekly figures, schedule this search to write to a summary index and read from the summary for reports — that way the numbers quoted on Monday match what was quoted on Wednesday.

Cardinality vigilance: if `the analytical dimensions` has very high cardinality (e.g. grouping by a free-text `user_agent` or `url` field), the result set explodes and the search becomes slow. Check cardinality with `| stats dc(the analytical dimensions) as cardinality` before scheduling; if >100000 distinct groups, either narrow the search, pre-filter to the top N values, or redesign the search to aggregate at a coarser grain.

**Schedule configuration** (`savedsearches.conf`):
```ini
[UC-22.14.3: Audit Storage Capacity and Index Growth Guardrails]
cron_schedule = 0 */6 * * *
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
actions = summary_index
action.summary_index._name = summary_compliance
action.summary_index.marker = uc=22.14.3,reg=NIST_800-53
```

## Step 3 — Validate deployment

**(a) Field extraction check:** Run the following to confirm the TA is extracting every field the detection relies on:

```spl
index=the_index sourcetype=the_sourcetype earliest=-1h
| fieldsummary
| where count=0
| table field
```

Any row returned is a field the TA claims to extract but isn't appearing in recent data — it's either a rename upstream, a TA version mismatch, or a data-format change.

**(b) Required-field presence:** This detection specifically needs: `frozenTimePeriodInSecs`, `maxTotalDataSizeMB`, `retention_days`, `size_gb`, `title`. Run:

```spl
index=the_index sourcetype=the_sourcetype earliest=-1h
| head 100
| table frozenTimePeriodInSecs, maxTotalDataSizeMB, retention_days, size_gb, title
```

Every row should have non-null values for the fields the detection's `where` clause filters on. Nulls cause silent drops in the result set.

**(e) Negative test:** Generate a synthetic 'should not alert' event using the Splunk Sample Generator or a crafted `| makeresults` event and confirm the detection does NOT fire on it. A detection that fires on everything is equivalent to a detection that fires on nothing.

## Step 4 — Operationalize (dashboard and reporting)

Create a companion dashboard titled `UC-22.14.3: NIST 800-53 evidence` with the following panel layout:

**Panel 1 — Single-value KPI (`indexes over threshold`):** Place at top-left. Configure `underLabel` with the regulation clause (e.g. 'NIST 800-53 current violation count') and set a red colour range for any non-zero value. A zero reading is the steady state for a healthy control.

**Panel 2 — Drill-through table (`top consumers`):** Full-width bottom row. Enable row-level drilldown to a secondary dashboard that expands each violation with its raw events, timeline, and related context. This is the panel the auditor will actually read — optimise it for scannability (avoid huge wrap-around fields).

**Panel 3 — Distribution (`size by index`):** Shows the volumetric distribution across the primary dimension. Use `sort` so the worst offender is first — auditors focus on the tallest bar.


Time picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.

## Step 5 — Troubleshooting and regulation-specific caveats

**Search returns no results when violations are expected:** Verify time-window alignment, index existence, and field extractions in that order. Most 'missing results' are simpler configuration issues before they're detection-logic issues.

**Search times out:** Check the search job inspector (`Activity > Jobs > <this UC's last run> > Inspect`). The phase consuming the time (fetch, command, summary) indicates whether the issue is data-layer (too wide a window, unaccelerated datamodel), command-layer (expensive `transaction` or `eventstats`), or post-aggregation.


**NIST 800-53-specific failure modes:**

- Control baseline selection: NIST 800-53 has Low/Moderate/High/Privacy baselines via FIPS 199 categorisation. A detection written to a High baseline threshold produces false alarms against Low-categorised systems. Join with the system categorisation register `nist_800_53_system_categorization.csv` before thresholding.

- CA-7 continuous monitoring cadence: specifies 'ongoing' authorisation activities. Detections supporting CA-7 must produce metrics at the organisation's ConMon cadence (monthly is common for most agencies) — weekly fires are fine but not themselves evidence of monthly compliance.

- Control inheritance: in a cloud-hybrid deployment, some controls are inherited from the Cloud Service Provider. Detections must not flag 'missing' controls the CSP provides (documented in the SSP via Control Inheritance matrix); reference the SSP appendix and suppress matching findings.

- SP 800-53 Rev 5 restructure: controls were renumbered between Rev 4 and Rev 5. Detections that cite Rev 4 numbering (e.g. AC-2.12) may not match the SSP if the organisation has migrated. Store both Rev numbers in the UC metadata and reconcile.

## SPL

```spl
| rest /services/data/indexes splunk_server=local count=0
| search disabled=0 NOT title IN ("_*")
| eval size_gb=currentDBSizeMB/1024
| eval retention_days=round(frozenTimePeriodInSecs/86400,1)
| table title, size_gb, maxTotalDataSizeMB, retention_days, frozenTimePeriodInSecs
| sort - size_gb
```

## Visualization

Single value (indexes over threshold), Table (top consumers), Column chart (size by index). Key fields: `frozenTimePeriodInSecs`, `maxTotalDataSizeMB`, `retention_days`, `size_gb`, `title`.

## Known False Positives

1. **Authorised security assessment activities** — Planned security control assessments, continuous monitoring scans, and Plan of Action and Milestones (POA&M) validation tests generate expected control deviation signals that are part of the Assessment & Authorisation process.

2. **System authorisation boundary changes** — During ATO re-authorisation, system boundary expansions, or inheritance model updates, temporary control gaps may appear in the monitoring baseline until the updated security plan is approved by the Authorising Official.

3. **Interconnection Security Agreement (ISA) activity** — Data exchanges with authorised external systems operating under signed ISAs produce cross-boundary traffic that triggers network monitoring alerts but is covered by the formal interconnection agreement.

4. **FIPS 199 impact-level re-categorisation** — When system categorisation changes (e.g., Moderate to High), the transition period produces control baseline mismatches until the enhanced controls are fully implemented per the approved implementation schedule.

**Suppression mechanism:** Maintain a `nist_800_53_exception_filter` KV Store lookup containing approved assessment schedule entries, POA&M IDs, ISA identifiers, and system re-categorisation project IDs. The correlation search filters against this lookup and suppresses matches with valid exception entries.

## References

- [Splunk Lantern — use case library](https://lantern.splunk.com/)
- [NIST SP 800-53 Rev. 5](https://csrc.nist.gov/publications/detail/sp/800-53/rev-5/final)
- [NIST 800-53 Controls Catalog](https://csrc.nist.gov/projects/risk-management/sp800-53-controls/release-search)
- [SP 800-53 Rev. 5 (PDF)](https://nvlpubs.nist.gov/nistpubs/SpecialPublications/NIST.SP.800-53r5.pdf)
