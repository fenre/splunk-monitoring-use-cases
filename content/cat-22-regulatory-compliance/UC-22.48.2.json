{
  "$schema": "../../schemas/uc.schema.json",
  "id": "22.48.2",
  "title": "SoD violations via break-glass usage — emergency role abuse",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Compliance",
    "Security"
  ],
  "splunkPillar": "Security",
  "owner": "CISO",
  "controlFamily": "access-review-cadence",
  "exclusions": "Pairs with UC-22.40.2 for post-use approval.",
  "evidence": "Real-time 'breakglass_sod_violation' detects SoD violations during break-glass usage.",
  "compliance": [
    {
      "regulation": "sox-itgc",
      "version": "PCAOB AS 2201",
      "clause": "ITGC.AccessMgmt.SOD",
      "mode": "satisfies",
      "assurance": "partial",
      "assurance_rationale": "Break-glass that crosses SoD boundaries must be compensated; this UC generates the signal at session close.",
      "controlObjective": "Evidence that SOX ITGC ITGC.AccessMgmt.SOD (Segregation of duties) is enforced — Splunk UC-22.48.2: SoD violations via break-glass usage — emergency role abuse.",
      "evidenceArtifact": "Saved search 'UC-22.48.2' running on catalogue-defined data sources (see UC detailedImplementation), archived to the restricted audit_evidence index (default 7-year retention). Auto-drafted — SME review required.",
      "obligationRef": "sox-itgc@PCAOB AS 2201#ITGC.AccessMgmt.SOD",
      "requires_sme_review": false
    }
  ],
  "controlTest": {
    "positiveScenario": "UC-22.48.2 positive (SoD, violations, via, break, glass): ingest synthetic events into `index=pam` sourcetype='emergency:exception' where `values` grouped by `session_id`, `account` satisfies -24h for 'SoD violations via break-glass usage — emergency role abuse'. Validate lookup `sod_matrix.csv`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-22.48.2 negative (role abuse): ingest benign `pam` / `emergency:exception` events where `session_id`, `account` stays below -24h, or exclude the test entity via `compliance_exception_register.csv`. Variant 11 confirms zero alert rows for 'SoD violations via break-glass usage — emergency role abuse'."
  },
  "dataSources": "See subcategory preamble. — supplemented by authentication logs, system audit trails, and configuration management database (CMDB) asset inventory exports",
  "app": "Splunk Enterprise / Splunk Cloud Platform (Splunkbase)",
  "spl": "index=pam breakglass=true earliest=-24h\n| stats values(action_category) as actions by session_id account\n| mvexpand actions\n| lookup sod_matrix.csv action_a=actions OUTPUT toxic_against\n| mvexpand toxic_against\n| join session_id toxic_against [search index=pam breakglass=true earliest=-24h | eval hit=action_category]\n| join session_id [search index=grc sourcetype=emergency:exception | fields session_id exception_id]\n| where isnull(exception_id)\n| table session_id account actions toxic_against",
  "description": "Monitors UC-22.48.2 — SoD, violations, via, break, glass — using `index=pam` sourcetype `emergency:exception`, grouped by `session_id`, `account`. Outputs `values` each `the scheduled window`; alert when -24h. Filter chain includes lookup `sod_matrix.csv`. Purpose-built for 'SoD violations via break-glass usage — emergency role abuse' in Compliance workflows.",
  "value": "UC-22.48.2 helps teams contain SoD, violations, via, break, glass scenarios and reduce audit findings, regulatory gaps, and attestation delays before incidents compound.",
  "implementation": "(1) Tag PAM session actions into SoD categories; (2) maintain sod_matrix.csv; (3) require emergency exceptions to log to GRC; (4) run hourly. Indexes required: `index=grc`, `index=pam`; Sourcetypes: `sourcetype=emergency:exception`.",
  "visualization": "Table of sessions with SoD crossings, single-value 'SoD-compliant emergency sessions'. Key fields: `account`, `actions`, `hit`, `session_id`, `toxic_against`.",
  "cimModels": [
    "Authentication",
    "Change"
  ],
  "references": [
    {
      "url": "https://pcaobus.org/oversight/standards/auditing-standards/details/AS2201",
      "title": "PCAOB AS 2201",
      "retrieved": "2026-04-16"
    },
    {
      "url": "https://www.sec.gov/about/laws/soa2002.pdf",
      "title": "Sarbanes-Oxley Act of 2002 (PDF)"
    },
    {
      "url": "https://pcaobus.org/oversight/standards/auditing-standards",
      "title": "PCAOB — Auditing Standards"
    },
    {
      "url": "https://www.coso.org/guidance-on-ic",
      "title": "COSO — Internal Control Framework"
    }
  ],
  "knownFalsePositives": "1. **Authorised SOX ITGC control testing** — Internal audit exercises 'SoD violations via break-glass usage — emergency role abuse' (UC-22.48.2) on `session_id` during scheduled clause ITGC.AccessMgmt.SOD sampling. Match compliance programme calendar.\n\n2. **External assessor sampling** — QSA activity spikes `index=pam` `emergency:exception` without crossing evidence threshold. Cross-check engagement letter in `sox_itgc_exception_register.csv`.\n\n3. **Planned maintenance affecting control telemetry** — CAB change pauses ingestion to `pam` for `session_id`. Compare `_time` with active change record.\n\n4. **Third-party processor under contract** — BAA/DPA-covered vendor generates expected rows grouped by `session_id` during normal service delivery.\n\n**Suppression mechanism:** `sox_itgc_exception_register.csv` keyed on `session_id`, `exception_reason`, `valid_until`, joined before alert threshold.",
  "detectionType": "Correlation",
  "securityDomain": "identity",
  "requiredFields": [
    "account",
    "actions",
    "hit",
    "session_id",
    "toxic_against"
  ],
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "status": "community",
  "lastReviewed": "2026-04-16",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "N/A",
  "premiumApps": [],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Enterprise ≥9.2 or Splunk Cloud (current). Scheduling requires a search head not under memory pressure (check `index=_internal sourcetype=splunkd component=ProcessRunner` for OOM warnings before deploying a new scheduled search).\n• **Indexes**: `pam`, `grc` must exist on all indexers with retention matching the regulation's minimum (PCI: 1y, HIPAA: 6y, NIST 800-53: 1-3y per FIPS 199, SOX: 7y). Set `frozenTimePeriodInSecs` accordingly in `indexes.conf`.\n• **Search-head capacity**: Subsearch-based detections need `subsearch_maxtime` ≥ 60s and `subsearch_maxout` ≥ 50000 in `limits.conf`. For high-volume environments, tune `[search] max_searches_per_cpu` up from default.\n• **Regulatory mapping**: This detection evidences clause `ITGC.AccessMgmt.SOD`. The control objective is: \"Evidence that SOX ITGC ITGC.AccessMgmt.SOD (Segregation of duties) is enforced — Splunk UC-22.48.2: SoD violations via break-glass usage — emergency role abuse.\" — every alert payload should include this text so downstream consumers know the compliance purpose.\n\n## Step 1 — Configure data collection\n\nData collection is bespoke for this UC — no single canonical TA parses every source the detection relies on. Route data into `pam`, `grc` under `emergency:exception`, and document the ingestion path in the UC runbook so future engineers know where each field originates.\n\n\nFour distinct operational considerations shape this detection's rollout:\n\n\n• **Hint 1:** Tag PAM session actions into SoD categories. CIM-compliance tagging must be validated end-to-end: a downstream datamodel that does not accelerate because of a missing tag is a silent failure.\n\n• **Hint 2:** maintain sod_matrix.csv. The lookup table identified here is a long-lived compliance artifact; version it in source control and track every change with a pull-request review.\n\n• **Hint 3:** require emergency exceptions to log to GRC. This is an operational consideration that influences either the detection's accuracy or its maintainability — document the approach chosen in the UC runbook.\n\n• **Hint 4:** run hourly. This is an operational consideration that influences either the detection's accuracy or its maintainability — document the approach chosen in the UC runbook.\n\n## Step 2 — Implement the search\n\nFor \"SoD violations via break-glass usage — emergency role abuse,\" the aggregation produces the result shape required by the SOX ITGC control — each row is a summary record the regulation expects to be presentable at audit time.\n\nThis search correlates `index=pam` (sourcetype=`emergency:exception`) on the outer side with `index=grc` (sourcetype=`another`) on the inner side, joined on `session_id toxic_against` across the last 24 hours. `type=inner` means the search only returns rows where `session_id toxic_against` exists in both datasets. This is the correct choice when the detection requires presence in both sources — a left-join would include orphaned rows that cannot be evaluated against the compliance rule.\n\nJoin caveat: Splunk's `| join` subsearch is capped at `max_searches_per_cpu * subsearch_maxout` results (default: 50000). If the inner subsearch exceeds this, rows are silently dropped and the resulting report will under-count. Check `index=_internal sourcetype=splunkd log_level=WARN subsearch` for truncation warnings. For inner searches expected to return >10000 rows, rewrite as `| stats` with a larger base search and post-filter, or use `| tstats` if a CIM datamodel covers the data.\n\n**Schedule configuration** (`savedsearches.conf`):\n```ini\n[UC-22.48.2: SoD violations via break-glass usage — emergency role abuse]\ncron_schedule = 0 1 * * *\ndispatch.earliest_time = -24h\ndispatch.latest_time = now\nactions = summary_index\naction.summary_index._name = summary_compliance\naction.summary_index.marker = uc=22.48.2,reg=SOX_ITGC\n```\n\n## Step 3 — Validate deployment\n\n**(a) Field extraction check:** Run the following to confirm the TA is extracting every field the detection relies on:\n\n```spl\nindex=pam sourcetype=emergency:exception earliest=-1h\n| fieldsummary\n| where count=0\n| table field\n```\n\nAny row returned is a field the TA claims to extract but isn't appearing in recent data — it's either a rename upstream, a TA version mismatch, or a data-format change.\n\n**(b) Required-field presence:** This detection specifically needs: `account`, `actions`, `hit`, `session_id`, `toxic_against`. Run:\n\n```spl\nindex=pam sourcetype=emergency:exception earliest=-1h\n| head 100\n| table account, actions, hit, session_id, toxic_against\n```\n\nEvery row should have non-null values for the fields the detection's `where` clause filters on. Nulls cause silent drops in the result set.\n\n**(e) Negative test:** Generate a synthetic 'should not alert' event using the Splunk Sample Generator or a crafted `| makeresults` event and confirm the detection does NOT fire on it. A detection that fires on everything is equivalent to a detection that fires on nothing.\n\n## Step 4 — Operationalize (dashboard and reporting)\n\nCreate a companion dashboard titled `UC-22.48.2: SOX ITGC evidence` with the following panel layout:\n\n**Panel 1 — Table Of Sessions With Sod Crossings (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n**Panel 2 — Single-Value 'Sod-Compliant Emergency Sessions' (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting and regulation-specific caveats\n\n**No results when violations exist:** The most likely cause is subsearch truncation (see Step 2). Inspect `index=_internal sourcetype=splunkd component=searchprocessor log_level=WARN subsearch` for messages like 'Output result count exceeded 50000.'\n\n**Search times out:** Check the search job inspector (`Activity > Jobs > <this UC's last run> > Inspect`). The phase consuming the time (fetch, command, summary) indicates whether the issue is data-layer (too wide a window, unaccelerated datamodel), command-layer (expensive `transaction` or `eventstats`), or post-aggregation.\n\n\n**SOX ITGC-specific failure modes:**\n\n• Scope is financial-reporting systems: SOX ITGC covers systems that feed the general ledger. Detections on HR, research, or operational systems that do not touch GL are out of scope. Maintain `sox_in_scope_systems.csv`; suppress alerts on out-of-scope hosts.\n\n• Change management evidence: PCAOB AS 5 requires 'adequate' evidence of change controls. 'Adequate' is interpreted by the external auditor — typically this means the ticket exists, ticket was approved before change, approver != implementer, and change can be tied to a specific deployment record. Missing any one of these loses SOX compliance.\n\n• Segregation of duties (SoD): the famous 'one person cannot both create AND approve a payment' rule. SoD detections must use the HR authoritative role register, not AD groups, which accumulate historical access and drift.\n\n• Key reports (key reports classification): the external auditor designates 'key reports' — reports used in the financial-close process. Detections on report access only need to fire for key reports; non-key report access is noise.\n\n**Ecosystem integration:** Forward compliance detections to Splunk SOAR for automated incident triage and escalation playbooks. Enrich events with asset context from ServiceNow CMDB. Cross-reference with CrowdStrike Falcon or Microsoft Defender for Endpoint for host forensics and threat detection context. Integrate Okta or Azure AD identity context to determine if the acting user had appropriate authorisation. Feed validated findings into the GRC platform (e.g., RSA Archer, ServiceNow GRC, or AuditBoard) for regulatory compliance tracking and audit evidence management.",
  "grandmaExplanation": "We help you see when the same person could do two jobs that should be split, so errors and fraud are harder.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
