<!-- AUTO-GENERATED from UC-22.2.2.json — DO NOT EDIT -->

---
id: "22.2.2"
title: "NIS2 Supply Chain Security Monitoring"
criticality: "high"
splunkPillar: "Security"
---

# UC-22.2.2 · NIS2 Supply Chain Security Monitoring

## Description

Flags privileged vendor and supplier jump sessions when the landing domain matches a curated threat-intelligence watchlist or when a single operator spends an unusually long time in one supplier session, using CyberArk PSM session fields as the primary signal.

## Value

Reduces guesswork after a supplier breach headline: you get a short, dated list of who touched which supplier systems and whether those domains were already on your intelligence radar, so legal and procurement can prioritize calls without waiting for spreadsheets from four different teams.

## Implementation

(1) Install Splunk Add-on for CyberArk (2891) and confirm Vault/PSM logs land in `index=pam` with `sourcetype=cyberark:session`. (2) In Enterprise Security (263), export or sync `threat_intel_domain_lookup` from Intelligence Downloads or your STIX/TAXII workflow. (3) Maintain `vendor_asset_lookup` / CMDB-driven tags so `target_host` can be tied to a supplier record. (4) Save the SPL as a scheduled alert with a 24 hour window and throttle on `user,target_host`.

## Detailed Implementation

Prerequisites
• Splunk roles: analysts need `srchIndexesAllowed` to include `pam` and any supporting `itsm` / `vuln` indexes used for optional joins. Treat vendor session evidence as confidential — map saved searches to a restricted app context instead of `search`.
• Splunk Add-on for CyberArk (Splunkbase 2891) on heavy forwarders or intermediate tier that receives Vault/PSM JSON or CEF; confirm `props.conf` maps `sourcetype=cyberark:session` with fields `user`, `target_host`, `target_account`, `duration_min`, `session_id`.
• Splunk Enterprise Security (Splunkbase 263) on the search head if you rely on `threat_intel_domain_lookup`; otherwise build an equivalent KV store/CSV lookup under `lookups/` with matching field names.
• Network: ensure session logs arrive within 5 minutes of real time so a `earliest=-24h` window matches operator expectations.

Step 1 — Configure data collection
Enable the CyberArk TA inputs that ship PSM session data. On the collector, `splunk btool inputs list --debug | findstr /i cyberark` (Windows) or `grep -i cyberark $SPLUNK_HOME/etc/apps/TA-cyberark*/local/inputs.conf` (Linux) should show the modular input or HEC token you enabled. Route output to `index=pam` to keep privileged access segregated from generic security indexes.
In Settings > Data inputs, verify HEC or syslog listener capacity if PSM spikes during maintenance. Optionally mirror supplier asset inventory hourly from ServiceNow (1928) into `index=itsm` `sourcetype=snow:cmdb_rel_ci` so you can later `lookup` supplier names by configuration item.

Step 2 — Create the search and alert
Paste the documented SPL into Search & Reporting using a **Last 24 hours** preset to align with `earliest=-24h`. Save as `UC-22.2.2_vendor_session_threat_corr`.
Schedule: every **hour** with **time range** of last 24 hours, trigger if **number of results > 0**, throttle by fields `user` and `target_host` for **4 hours**.
If you add a Qualys context panel, clone the search and prepend a subsearch from `index=vuln` `sourcetype=qualys:hostdetection` — use Splunkbase 2964 field definitions from Data Summary, not assumed field names.

Step 3 — Validate
Pick a known CyberArk session from the vendor UI (PVWA or SIA) within the window. Run `index=pam sourcetype=cyberark:session session_id=<known>` and confirm `duration_min` matches operator notes within rounding.
Spot-check `rex` output: `| rex field=target_host "(?<target_domain>..."` should populate `target_domain` for FQDN targets; for IP-only targets expect null domains and tune with a CMDB `lookup` instead.
Validate TI enrichment by forcing a benign test domain into a dev lookup table and confirm the row appears when `weight` is non-null.

Step 4 — Operationalize
Publish a dashboard with three panels: (1) sortable table of violating sessions, (2) seven-day trend of `sessions` by supplier, (3) single-value KPI for count of distinct `user` values breaching the duration threshold. Wire drilldowns to open `index=pam` Raw Events for the chosen `session_id`.
Document a runbook: contact CyberArk owners first for session recordings, then vendor managers from CMDB. Archive weekly CSV exports to `index=audit_evidence` if your policy requires long retention outside the search tier.

Step 5 — Troubleshooting
• **No events** — modular input disabled in `inputs.conf` or wrong index; confirm `index=pam` on both input and props.
• **Blank target_domain** — targets logged as IP only; join `snow:cmdb_rel_ci` or a DNS enrichment lookup.
• **Lookup errors** — `threat_intel_domain_lookup` missing on non-ES search heads; copy lookup definition and CSV/KV store bundle.
• **Alert storms after TI feed change** — revert the feed version under ES Intelligence Downloads and add `summary_hash` dedup logic if vendors duplicate IOC rows.
• **False long durations** — clock skew between CyberArk and Splunk; compare `_time` drift with `eval lag=_indextime-_time | stats max(lag)`.

## SPL

```spl
index=pam sourcetype="cyberark:session" earliest=-24h
| rex field=target_host "(?<target_domain>[a-zA-Z0-9][a-zA-Z0-9.-]+\.[a-zA-Z]{2,})$"
| stats sum(duration_min) as total_min, dc(session_id) as sessions by user, target_host, target_domain, target_account
| lookup threat_intel_domain_lookup domain AS target_domain OUTPUT description AS ti_description, weight AS ti_weight
| where isnotnull(ti_weight) OR total_min>120
| sort - total_min
```

## Visualization

Table (sessions with TI hits, total_min, sessions), bar chart of minutes by supplier_domain, heatmap of user by hour-of-day.

## References

- [Splunk Add-on for CyberArk](https://splunkbase.splunk.com/app/2891)
- [Splunk Enterprise Security](https://splunkbase.splunk.com/app/263)
- [CIM: Authentication](https://docs.splunk.com/Documentation/CIM/latest/User/Authentication)
- [Directive (EU) 2022/2555 (NIS2)](https://eur-lex.europa.eu/eli/dir/2022/2555/oj)
- [Commission Implementing Regulation (EU) 2024/2690](https://eur-lex.europa.eu/eli/reg_impl/2024/2690/oj)
- [ENISA NIS2 Technical Implementation Guidance](https://www.enisa.europa.eu/publications/nis2-technical-implementation-guidance)
