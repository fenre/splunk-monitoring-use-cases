{
  "$schema": "../../schemas/uc.schema.json",
  "id": "22.4.19",
  "title": "CCPA/CPRA — Dark Patterns — Forced Navigation Loops on “Do Not Sell or Share” Choice Screen",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Security",
  "compliance": [
    {
      "regulation": "CCPA/CPRA",
      "version": "CPRA (as amended)",
      "clause": "§1798.120",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "CCPA/CPRA §1798.120 — Reviewed: SPL provides contributing evidence for this regulatory requirement. The search query directly addresses the monitoring objective specified by the clause.",
      "controlObjective": "Evidence that CCPA/CPRA §1798.120 is enforced — Splunk UC-22.4.19: CCPA/CPRA — Dark Patterns — Forced Navigation Loops on “Do Not Sell or Share” Choice Screen.",
      "evidenceArtifact": "Saved search 'UC-22.4.19' running on index=web sourcetype=\"ux:privacy_flow\" (session_id, step_name, sequence, _time), archived to the restricted audit_evidence index (default 7-year retention). Auto-drafted — SME review required.",
      "obligationRef": "ccpa@CPRA (as amended)#§1798.120",
      "requires_sme_review": false,
      "provenance": "maintainer"
    }
  ],
  "dataSources": "`index=web` `sourcetype=\"ux:privacy_flow\"` (session_id, step_name, sequence, _time)",
  "app": "Client-side telemetry HEC (privacy-safe event schema)",
  "spl": "index=web sourcetype=\"ux:privacy_flow\" earliest=-30d\n| sort 0 session_id, _time\n| streamstats window=1 global=f last(step_name) as prev_step by session_id\n| eval backtrack=if(step_name=\"dns_intro\" AND prev_step=\"dns_confirm\",1,0)\n| stats sum(backtrack) as loop_count by session_id\n| where loop_count>3\n| sort - loop_count",
  "description": "Dark patterns undermine consumer choice. Session paths that repeatedly bounce users away from the opt-out confirmation page without completion suggest manipulative UX—relevant to CPRA enforcement priorities on ease of exercise of rights.",
  "value": "Gives the responsible team auditor-ready evidence for CCPA/CPRA compliance by automating regulatory compliance evidence using indexes web and sourcetypes ux:privacy_flow. Replaces manual evidence gathering with a continuous Splunk search that produces structured, time-stamped artefacts suitable for regulatory examination and internal audit review.",
  "implementation": "(1) Instrument `step_name` consistently across web and app; (2) exclude A/B tests via `experiment_id` lookup; (3) product design reviews on outliers; (4) correlate with conversion metrics carefully—avoid accidental shaming; (5) legal escalation only on sustained campaigns. Indexes required: `index=web`; Sourcetypes: `sourcetype=ux:privacy_flow`.",
  "visualization": "Funnel diagram export, Table (sessions), Single value (sessions over threshold). Key fields: `backtrack`, `loop_count`, `session_id`.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/"
    }
  ],
  "knownFalsePositives": "Access requests from authorized regulatory inquiries, scheduled subject right campaigns, or outreach during privacy reviews.",
  "requiredFields": [
    "backtrack",
    "loop_count",
    "session_id"
  ],
  "lastReviewed": "2026-04-22",
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Client-side telemetry HEC (privacy-safe event schema).\n• Ensure the following data sources are available: `index=web` `sourcetype=\"ux:privacy_flow\"` (session_id, step_name, sequence, _time).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\n(1) Instrument `step_name` consistently across web and app; (2) exclude A/B tests via `experiment_id` lookup; (3) product design reviews on outliers; (4) correlate with conversion metrics carefully—avoid accidental shaming; (5) legal escalation only on sustained campaigns.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=web sourcetype=\"ux:privacy_flow\" earliest=-30d\n| sort 0 session_id, _time\n| streamstats window=1 global=f last(step_name) as prev_step by session_id\n| eval backtrack=if(step_name=\"dns_intro\" AND prev_step=\"dns_confirm\",1,0)\n| stats sum(backtrack) as loop_count by session_id\n| where loop_count>3\n| sort - loop_count\n```\n\nUnderstanding this SPL\n\n**CCPA/CPRA — Dark Patterns — Forced Navigation Loops on “Do Not Sell or Share” Choice Screen** — Dark patterns undermine consumer choice. Session paths that repeatedly bounce users away from the opt-out confirmation page without completion suggest manipulative UX—relevant to CPRA enforcement priorities on ease of exercise of rights.\n\nDocumented **Data sources**: `index=web` `sourcetype=\"ux:privacy_flow\"` (session_id, step_name, sequence, _time). **App/TA** (typical add-on context): Client-side telemetry HEC (privacy-safe event schema). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: web; **sourcetype**: ux:privacy_flow. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=web, sourcetype=\"ux:privacy_flow\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n• `streamstats` rolls up events into metrics; results are split **by session_id** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **backtrack** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by session_id** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where loop_count>3` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare with audit evidence in the source system (DLP, IDP, or application) and, if this use case underpins a control test, the ServiceNow GRC console or the evidence pack for a known time window.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Funnel diagram export, Table (sessions), Single value (sessions over threshold).",
  "grandmaExplanation": "We dark patterns undermine consumer choice.",
  "cimSpl": "",
  "dataModelAcceleration": "Not applicable"
}
