---
id: "22.3.44"
title: "DORA Art.17 — ICT incident classification timeliness: major-incident clock evidence"
status: "verified"
criticality: "critical"
splunkPillar: "Security"
---

# UC-22.3.44 · DORA Art.17 — ICT incident classification timeliness: major-incident clock evidence

## Description

Measures, per major ICT incident, the elapsed time between detection, classification, initial notification, intermediate notification, and final report. Any SLA breach against the DORA Art.17 + ESA RTS timeline (24h initial / 72h intermediate / 30d final) sets SLA_breached=1 and triggers a finding.

## Value

Supervisors under DORA request the per-incident clock evidence. This UC produces it automatically, so the entity can prove the notification workflow was operated within the required windows or identify the exact root cause of a miss.

## Implementation

(1) Normalise detected_at/classified_at/notified_at/intermediate_at/final_at in the IR tool; (2) tag major incidents at classification; (3) schedule UC every 5m; (4) SLA_breached=1 escalates to CISO + Head of IR; (5) monthly roll-up to Board; (6) annual tabletop exercise validates clock fidelity. Indexes required: `index=compliance`, `index=soar`, `index=ticketing`; Sourcetypes: `sourcetype=dora:notification.`, `sourcetype=jira:issue`, `sourcetype=phantom:incident`, `sourcetype=phantom:incident;`.

## Detailed Implementation

Prerequisites
• Install and configure the required add-on or app: Splunk SOAR, Splunk Add-on for ServiceNow.
• Ensure the following data sources are available: `index=soar` sourcetype=servicenow:sir_incident OR sourcetype=phantom:incident; `index=ticketing` sourcetype=jira:issue (IR project); regulator notification log `index=compliance` sourcetype=dora:notification..
• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md

Step 1 — Configure data collection
(1) Normalise detected_at/classified_at/notified_at/intermediate_at/final_at in the IR tool; (2) tag major incidents at classification; (3) schedule UC every 5m; (4) SLA_breached=1 escalates to CISO + Head of IR; (5) monthly roll-up to Board; (6) annual tabletop exercise validates clock fidelity.

Step 2 — Create the search and alert
Run the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):

```spl
index=soar sourcetype IN (servicenow:sir_incident,phantom:incident) tag::major_incident=true earliest=-1d
| stats min(detected_at) AS detected_at min(classified_at) AS classified_at min(notified_at) AS notified_at min(intermediate_at) AS intermediate_at min(final_at) AS final_at BY incident_id
| eval detect_to_classify_h=round((classified_at-detected_at)/3600,2)
| eval classify_to_initial_h=round((notified_at-classified_at)/3600,2)
| eval initial_to_inter_h=round((intermediate_at-notified_at)/3600,2)
| eval inter_to_final_h=round((final_at-intermediate_at)/3600,2)
| eval SLA_breached=if(detect_to_classify_h>24 OR classify_to_initial_h>24 OR initial_to_inter_h>72 OR inter_to_final_h>720,1,0)
| table incident_id detected_at classified_at notified_at intermediate_at final_at detect_to_classify_h classify_to_initial_h initial_to_inter_h inter_to_final_h SLA_breached
```

Understanding this SPL

**DORA Art.17 — ICT incident classification timeliness: major-incident clock evidence** — Supervisors under DORA request the per-incident clock evidence. This UC produces it automatically, so the entity can prove the notification workflow was operated within the required windows or identify the exact root cause of a miss.

Documented **Data sources**: `index=soar` sourcetype=servicenow:sir_incident OR sourcetype=phantom:incident; `index=ticketing` sourcetype=jira:issue (IR project); regulator notification log `index=compliance` sourcetype=dora:notification. **App/TA** (typical add-on context): Splunk SOAR, Splunk Add-on for ServiceNow. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.

The first pipeline stage scopes events using **index**: soar.

**Pipeline walkthrough**

• Scopes the data: index=soar, time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.
• `stats` aggregates events by **incident_id**, producing one row per unique combination for trend analysis and threshold evaluation.
• `eval` defines or adjusts **detect_to_classify_h** — applying business logic to classify, threshold, or derive compliance-relevant indicators.
• `eval` defines or adjusts **classify_to_initial_h** — applying business logic to classify, threshold, or derive compliance-relevant indicators.
• `eval` defines or adjusts **initial_to_inter_h** — applying business logic to classify, threshold, or derive compliance-relevant indicators.
• `eval` defines or adjusts **inter_to_final_h** — applying business logic to classify, threshold, or derive compliance-relevant indicators.
• `eval` defines or adjusts **SLA_breached** — applying business logic to classify, threshold, or derive compliance-relevant indicators.

Output fields: SLA_breached, classified_at, classify_to_initial_h, detect_to_classify_h, detected_at, final_at, incident_id, initial_to_inter_h.

Step 3 — Validate
Confirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.

Step 4 — Operationalize
Add the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Per-incident Gantt of phase durations, timechart of breaches per quarter, single value 'open incidents at risk of SLA breach in next 4h'.

## SPL

```spl
index=soar sourcetype IN (servicenow:sir_incident,phantom:incident) tag::major_incident=true earliest=-1d
| stats min(detected_at) AS detected_at min(classified_at) AS classified_at min(notified_at) AS notified_at min(intermediate_at) AS intermediate_at min(final_at) AS final_at BY incident_id
| eval detect_to_classify_h=round((classified_at-detected_at)/3600,2)
| eval classify_to_initial_h=round((notified_at-classified_at)/3600,2)
| eval initial_to_inter_h=round((intermediate_at-notified_at)/3600,2)
| eval inter_to_final_h=round((final_at-intermediate_at)/3600,2)
| eval SLA_breached=if(detect_to_classify_h>24 OR classify_to_initial_h>24 OR initial_to_inter_h>72 OR inter_to_final_h>720,1,0)
| table incident_id detected_at classified_at notified_at intermediate_at final_at detect_to_classify_h classify_to_initial_h initial_to_inter_h inter_to_final_h SLA_breached
```

## Visualization

Per-incident Gantt of phase durations, timechart of breaches per quarter, single value 'open incidents at risk of SLA breach in next 4h'. Key fields: `SLA_breached`, `classified_at`, `classify_to_initial_h`, `detect_to_classify_h`, `detected_at`, `final_at`.

## Known False Positives

Incidents re-classified as non-major after initial raise can show a misleading 'breach' until tag::major_incident is cleared — dedup on latest tag state before computing SLAs.

## References

- [DORA Regulation (EU) 2022/2554](https://eur-lex.europa.eu/eli/reg/2022/2554/oj)
- [NIS2 Directive (EU) 2022/2555](https://eur-lex.europa.eu/eli/dir/2022/2555/oj)
- [ISO/IEC 27001 2022](https://www.iso.org/standard/27001)
- [MITRE ATT&CK — T1090](https://attack.mitre.org/techniques/T1090/)
