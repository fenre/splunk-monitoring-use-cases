{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.7.59",
  "title": "Google Cloud Composer Failures by Dag",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Google Cloud Composer Failures by Dag' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_google_cloud_composer_failures_by_dag` (UC-24.7.59), SOC dashboard panel \"Operations — Google Cloud Composer Failures by Dag\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-24.7.59 positive (Google, Cloud, Composer, Failures, Dag): ingest synthetic events into `index=bi` sourcetype='gcp:composer' where `count` grouped by `dag_id` satisfies `failures` > 0 for 'Google Cloud Composer Failures by Dag'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-24.7.59 negative (by Dag): ingest benign `bi` / `gcp:composer` events where `dag_id` stays below `failures` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 3 confirms zero alert rows for 'Google Cloud Composer Failures by Dag'."
  },
  "dataSources": "Cloud Composer Airflow logs (`gcp:composer`). Collected via Splunk Add-on for Google Cloud Platform (Splunkbase 3084) modular input or scripted poll into the index named in the SPL.",
  "app": "Cloud Composer (managed Airflow) logs and metrics via the Google Cloud Add-on / Pub-Sub to HEC. — Splunk Add-on for Google Cloud Platform (Splunkbase 3084)",
  "spl": "index=bi sourcetype=gcp:composer state=\"failed\"\n| stats count as failures, latest(_time) as last_failure by dag_id\n| where failures>0\n| sort - failures",
  "description": "Surfaces when by Dag criteria fire for 'Google Cloud Composer Failures by Dag' (UC-24.7.59) in `index=bi` sourcetype `gcp:composer`. Pipeline aggregates `count` by `dag_id` over `the scheduled window`. Alert logic enforces `failures` > 0. Title terms: Google, Cloud, Composer, Failures, Dag. Eliminates manual hunting for UC-24.7.59 conditions.",
  "value": "UC-24.7.59 helps teams contain Google, Cloud, Composer, Failures, Dag scenarios and reduce operational risk and delayed incident response before incidents compound.",
  "implementation": "Ingest Google Cloud Composer failure events; schedule every 15 minutes and alert on failures for business-critical DAGs.",
  "visualization": "Table of DAG by failure count and last failure time.",
  "cimModels": [],
  "references": [
    {
      "title": "Cloud Composer — Monitoring",
      "url": "https://cloud.google.com/composer/docs/monitor-environments",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Cloud Composer — Airflow logs",
      "url": "https://cloud.google.com/composer/docs/composer-2/view-logs",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `bi` (UC-24.7.59)** — Planned work for 'Google Cloud Composer Failures by Dag' shifts `count` over `span=1h` while `dag_id` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `gcp:composer`** — Scheduled automation elevates `src` without crossing `failures` > 0 for UC-24.7.59. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `dag_id`** — Health checks in `bi` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `gcp:composer`** — First 48h baseline for UC-24.7.59 on `dag_id` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `dag_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "gcp"
  ],
  "equipmentModels": [
    "gcp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Cloud Composer (managed Airflow) logs and metrics via the Google Cloud Add-on / Pub-Sub to HEC. — Splunk Add-on for Google Cloud Platform (Splunkbase 3084)). Enable the modular input or HEC token that writes sourcetype=gcp:composer into bi. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`dag_id, failures`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We tell you exactly which parts of Google Cloud Composer are failing, not just that something is wrong, so the right thing gets fixed first.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
