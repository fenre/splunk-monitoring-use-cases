{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.11.16",
  "title": "MetricFlow Failures by Metric",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'MetricFlow Failures by Metric' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_metricflow_failures_by_metric` (UC-24.11.16), SOC dashboard panel \"Operations — MetricFlow Failures by Metric\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-24.11.16 positive (MetricFlow, Failures, Metric): ingest synthetic events into `index=bi` sourcetype='semantic_layer:query' where `count` grouped by `metric_name` satisfies `failures` > 0 for 'MetricFlow Failures by Metric'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-24.11.16 negative (by Metric): ingest benign `bi` / `semantic_layer:query` events where `metric_name` stays below `failures` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 13 confirms zero alert rows for 'MetricFlow Failures by Metric'."
  },
  "dataSources": "Semantic layer queries (`semantic_layer:query`). Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "dbt Semantic Layer / MetricFlow query logs via the JDBC/GraphQL API to HEC. — Universal Forwarder (Splunkbase 617)",
  "spl": "index=bi sourcetype=semantic_layer:query status=\"error\"\n| stats count as failures, latest(_time) as last_failure by metric_name\n| where failures>0\n| sort - failures",
  "description": "Alerts on UC-24.11.16 — MetricFlow, Failures, Metric — using `index=bi` sourcetype `semantic_layer:query`, grouped by `metric_name`. Outputs `count` each `the scheduled window`; alert when `failures` > 0. Primary focus: by Metric. Purpose-built for 'MetricFlow Failures by Metric' in Operations workflows.",
  "value": "UC-24.11.16 gives Operations teams earlier visibility into by Metric through `semantic_layer:query` feeds, lowering operational risk and delayed incident response.",
  "implementation": "Ingest MetricFlow failure events; schedule every 15 minutes and alert on failures for business-critical metrics.",
  "visualization": "Table of metric by failure count and last failure time.",
  "cimModels": [],
  "references": [
    {
      "title": "dbt Semantic Layer — MetricFlow",
      "url": "https://docs.getdbt.com/docs/build/about-metricflow",
      "retrieved": "2026-04-25"
    },
    {
      "title": "dbt Semantic Layer — APIs",
      "url": "https://docs.getdbt.com/docs/dbt-cloud-apis/sl-api-overview",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `bi` (UC-24.11.16)** — Planned work for 'MetricFlow Failures by Metric' shifts `count` over `span=1h` while `metric_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `semantic_layer:query`** — Scheduled automation elevates `src` without crossing `failures` > 0 for UC-24.11.16. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `metric_name`** — Health checks in `bi` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `semantic_layer:query`** — First 48h baseline for UC-24.11.16 on `metric_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `metric_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (dbt Semantic Layer / MetricFlow query logs via the JDBC/GraphQL API to HEC. — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=semantic_layer:query into bi. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`failures, metric_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We tell you exactly which parts of MetricFlow are failing, not just that something is wrong, so the right thing gets fixed first.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
