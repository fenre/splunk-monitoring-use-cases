{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.12.21",
  "title": "RudderStack Destination Freshness SLA",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Data Quality",
    "Operations"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'RudderStack Destination Freshness SLA' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_rudderstack_destination_freshness_sla` (UC-24.12.21), SOC dashboard panel \"Operations — RudderStack Destination Freshness SLA\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-24.12.21 positive (RudderStack, Destination, Freshness, SLA): ingest synthetic events into `index=bi` sourcetype='rudderstack:delivery' where `count` grouped by `destination_name` satisfies `hours_stale` > 6 for 'RudderStack Destination Freshness SLA'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-24.12.21 negative (Freshness SLA): ingest benign `bi` / `rudderstack:delivery` events where `destination_name` stays below `hours_stale` > 6, or exclude the test entity via `ops_exception_register.csv`. Variant 0 confirms zero alert rows for 'RudderStack Destination Freshness SLA'."
  },
  "dataSources": "RudderStack delivery events (`rudderstack:delivery`). Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "RudderStack delivery and failed-events metrics via the RudderStack API to HEC. — Universal Forwarder (Splunkbase 617)",
  "spl": "index=bi sourcetype=rudderstack:delivery status=\"delivered\"\n| stats latest(_time) as last_success by destination_name\n| eval hours_stale=round((now()-last_success)/3600,1)\n| where hours_stale>6\n| sort - hours_stale",
  "description": "Surfaces 'RudderStack Destination Freshness SLA' (UC-24.12.21) using `index=bi` sourcetype `rudderstack:delivery` by aggregating `count` grouped by `destination_name` over `the scheduled window` and alerting when `hours_stale` > 6. Keywords: RudderStack, Destination, Freshness, SLA. Focuses on Freshness SLA behaviour so operations teams can triage UC-24.12.21 without ad-hoc log review.",
  "value": "UC-24.12.21 accelerates insight into Freshness SLA ('RudderStack Destination Freshness SLA') by tracking `activity` on `rudderstack:delivery` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Ingest RudderStack success events; schedule hourly and alert when a critical destination exceeds its freshness window.",
  "visualization": "Table of destination by hours stale, freshness heat map.",
  "cimModels": [],
  "references": [
    {
      "title": "RudderStack — Monitoring",
      "url": "https://www.rudderstack.com/docs/dashboard-guides/data-governance/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RudderStack — API",
      "url": "https://www.rudderstack.com/docs/api/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `bi` (UC-24.12.21)** — Planned work for 'RudderStack Destination Freshness SLA' shifts `count` over `span=1h` while `destination_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `rudderstack:delivery`** — Scheduled automation elevates `hours_stale` without crossing `hours_stale` > 6 for UC-24.12.21. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `destination_name`** — Health checks in `bi` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `rudderstack:delivery`** — First 48h baseline for UC-24.12.21 on `destination_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `destination_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (RudderStack delivery and failed-events metrics via the RudderStack API to HEC. — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=rudderstack:delivery into bi. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`destination_name, hours_stale`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We check that each part of RudderStack has updated recently so nobody relies on old data.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
