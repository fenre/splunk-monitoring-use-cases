{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.12.6",
  "title": "Reverse-ETL Field Mapping Errors",
  "criticality": "low",
  "difficulty": "advanced",
  "monitoringType": [
    "Change",
    "Data Quality"
  ],
  "splunkPillar": "Observability",
  "dataSources": "Hightouch sync run events (`hightouch:sync`), Census sync history (`census:sync`), Segment/RudderStack destination delivery and error events, audience-refresh latency metrics via REST.",
  "app": "Hightouch / Census sync webhooks and REST APIs via HEC; Segment/RudderStack delivery events via HEC scripted inputs.",
  "spl": "index=dataops (sourcetype=hightouch:sync OR sourcetype=census:sync) event=mapping_error\n| stats count as errors, values(error_message) as messages by sync_name, field_name\n| where errors>0\n| sort - errors",
  "description": "Captures field-mapping errors in reverse-ETL syncs so a renamed or retyped warehouse column that breaks a destination mapping is caught quickly.",
  "value": "A mapping error can quietly stop a field from updating in the CRM; catching these keeps activated records complete and correct.",
  "implementation": "Ingest mapping-error events; schedule hourly; alert on new errors; review mappings after warehouse schema changes.",
  "visualization": "Table of mapping errors by sync/field, error timeline.",
  "cimModels": [],
  "references": [
    {
      "title": "Census — documentation",
      "url": "https://docs.getcensus.com/"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "grandmaExplanation": "We flag when a data field fails to map into a business tool, so records do not end up missing information."
}
