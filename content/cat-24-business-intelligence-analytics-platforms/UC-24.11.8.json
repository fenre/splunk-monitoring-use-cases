{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.11.8",
  "title": "MetricFlow Query Compilation Errors",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'MetricFlow Query Compilation Errors' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_metricflow_query_compilation_errors` (UC-24.11.8), SOC dashboard panel \"Operations — MetricFlow Query Compilation Errors\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-24.11.8 positive (MetricFlow, Query, Compilation, Errors): ingest synthetic events into `index=bi` sourcetype='semantic_layer:query' where `values` grouped by `metric_name` satisfies `errors` > 0 for 'MetricFlow Query Compilation Errors'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-24.11.8 negative (Compilation Errors): ingest benign `bi` / `semantic_layer:query` events where `metric_name` stays below `errors` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 7 confirms zero alert rows for 'MetricFlow Query Compilation Errors'."
  },
  "dataSources": "Semantic layer query logs (`cube:api`, `semantic_layer:query`), MetricFlow query events, and AtScale engine logs. Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Semantic-layer query and build logs (dbt Semantic Layer / MetricFlow, Cube, AtScale) via REST/GraphQL APIs and application logs to HEC. — Universal Forwarder (Splunkbase 617)",
  "spl": "index=bi sourcetype=semantic_layer:query engine=\"metricflow\" (status=\"error\" OR compilation_error=\"true\")\n| stats count as errors, values(error_message) as messages by metric_name\n| where errors>0\n| sort - errors",
  "description": "Detects UC-24.11.8 — MetricFlow, Query, Compilation, Errors — using `index=bi` sourcetype `semantic_layer:query`, grouped by `metric_name`. Outputs `values` each `the scheduled window`; alert when `errors` > 0. Primary focus: Compilation Errors. Purpose-built for 'MetricFlow Query Compilation Errors' in Operations workflows.",
  "value": "UC-24.11.8 operationalises 'MetricFlow Query Compilation Errors' against `semantic_layer:query` feeds so operators reduce operational risk and delayed incident response without manual correlation.",
  "implementation": "Ingest MetricFlow query logs; schedule hourly and alert on compilation errors for widely used metrics.",
  "visualization": "Table of metric by errors with messages.",
  "cimModels": [],
  "references": [
    {
      "title": "dbt Semantic Layer — MetricFlow",
      "url": "https://docs.getdbt.com/docs/build/about-metricflow",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Cube — Monitoring & logging",
      "url": "https://cube.dev/docs/product/deployment",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `bi` (UC-24.11.8)** — Planned work for 'MetricFlow Query Compilation Errors' shifts `values` over `span=1h` while `metric_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `values` on `semantic_layer:query`** — Scheduled automation elevates `src` without crossing `errors` > 0 for UC-24.11.8. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `metric_name`** — Health checks in `bi` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `semantic_layer:query`** — First 48h baseline for UC-24.11.8 on `metric_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `metric_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Semantic-layer query and build logs (dbt Semantic Layer / MetricFlow, Cube, AtScale) via REST/GraphQL APIs and application logs to HEC. — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=semantic_layer:query into bi. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`errors, metric_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We catch when the shared calculation engine cannot build a metric so the reports asking for it do not break silently.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
