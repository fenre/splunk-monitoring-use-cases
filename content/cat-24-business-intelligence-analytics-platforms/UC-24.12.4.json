{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.12.4",
  "title": "Activation Delivery Failures (Segment/RudderStack)",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Fault"
  ],
  "splunkPillar": "Observability",
  "dataSources": "Hightouch sync run events (`hightouch:sync`), Census sync history (`census:sync`), Segment/RudderStack destination delivery and error events, audience-refresh latency metrics via REST.",
  "app": "Hightouch / Census sync webhooks and REST APIs via HEC; Segment/RudderStack delivery events via HEC scripted inputs.",
  "spl": "index=dataops (sourcetype=segment:delivery OR sourcetype=rudderstack:delivery)\n| stats count as attempts, sum(eval(status=\"failed\")) as failures by destination, event_type\n| eval failure_rate=round(100*failures/attempts,1)\n| where failures>0\n| sort - failure_rate",
  "description": "Tracks event delivery from CDPs (Segment, RudderStack) to downstream destinations and flags destinations with high delivery-failure rates.",
  "value": "Failed event delivery breaks analytics and marketing automations downstream; per-destination failure rates show which integration to fix.",
  "implementation": "Ingest CDP delivery events; schedule hourly; alert on rising failure rates per destination.",
  "visualization": "Table by failure rate, timechart of delivery failures.",
  "cimModels": [],
  "references": [
    {
      "title": "Segment — Delivery Overview",
      "url": "https://segment.com/docs/connections/delivery-overview/"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "grandmaExplanation": "We watch the customer-data events sent to your marketing and analytics tools and flag when delivery to one keeps failing."
}
