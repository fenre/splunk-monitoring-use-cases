{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.11.1",
  "title": "Semantic Layer Query Errors",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Fault",
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Semantic Layer Query Errors' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_semantic_layer_query_errors` (UC-24.11.1), SOC dashboard panel \"Operations — Semantic Layer Query Errors\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=bi` sourcetype='dbt:semantic_layer' where `metric_name` satisfies `status` >= 500 for UC-24.11.1 ('Semantic Layer Query Errors'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `bi` / `dbt:semantic_layer` events where `metric_name` stays below `status` >= 500, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-24.11.1."
  },
  "dataSources": "dbt Semantic Layer / MetricFlow query logs, Cube API request logs and pre-aggregation build metrics (`cube:api`), AtScale engine query logs, LookML validation and metric-definition change events. Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "dbt Semantic Layer / MetricFlow and Cube query logs and APIs via HEC scripted inputs; AtScale engine logs; LookML content-validator output. — Universal Forwarder (Splunkbase 617)",
  "spl": "index=bi sourcetype=dbt:semantic_layer OR sourcetype=cube:api\n| stats count as queries, sum(eval(status>=500 OR result=\"error\")) as errors by metric_name\n| eval error_rate=round(100*errors/queries,1)\n| where errors>0\n| sort - error_rate",
  "description": "Tracks queries against the semantic/metrics layer and ranks metrics by error rate so a broken metric definition is caught before every consuming tool shows wrong or missing values.",
  "value": "Tracks queries against the semantic/metrics layer and ranks metrics by error rate so a broken metric defini.... Timely UC-24.11.1 alerting on 'Semantic Layer Query Errors' reduces operational risk and delayed incident response before manual triage or audit channels escalate.",
  "implementation": "Ingest semantic-layer/Cube query logs; schedule hourly; alert on error spikes per metric.",
  "visualization": "Table by error rate, timechart of query errors.",
  "cimModels": [],
  "references": [
    {
      "title": "dbt — About MetricFlow",
      "url": "https://docs.getdbt.com/docs/build/about-metricflow",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `bi`** — Planned work for 'Semantic Layer Query Errors' shifts `sum` over `span=1h` while `metric_name` remains in-scope for UC-24.11.1. Cross-check CMDB CHG.\n\n2. **Batch `sum` on `dbt:semantic_layer`** — Scheduled automation elevates `error_rate` without crossing `status` >= 500. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `metric_name`** — Health checks in `bi` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `dbt:semantic_layer`** — First 48h baseline for `metric_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `metric_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (dbt Semantic Layer / MetricFlow and Cube query logs and APIs via HEC scripted inputs; AtScale engine logs; LookML content-validator output. — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=dbt:semantic_layer, cube:api into bi. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`errors, metric_name, status`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch the shared definitions of your key numbers and flag when one breaks, so it does not go wrong everywhere at once.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
