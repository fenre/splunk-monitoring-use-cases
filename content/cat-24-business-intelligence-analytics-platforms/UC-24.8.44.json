{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.8.44",
  "title": "Google Dataflow Failures by Job",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Google Dataflow Failures by Job' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_google_dataflow_failures_by_job` (UC-24.8.44), SOC dashboard panel \"Operations — Google Dataflow Failures by Job\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-24.8.44 positive (Google, Dataflow, Failures, Job): ingest synthetic events into `index=bi` sourcetype='gcp:dataflow' where `count` grouped by `job_name` satisfies `failures` > 0 for 'Google Dataflow Failures by Job'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-24.8.44 negative (by Job): ingest benign `bi` / `gcp:dataflow` events where `job_name` stays below `failures` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 8 confirms zero alert rows for 'Google Dataflow Failures by Job'."
  },
  "dataSources": "Dataflow job status/metrics (`gcp:dataflow`). Collected via Splunk Add-on for Google Cloud Platform (Splunkbase 3084) modular input or scripted poll into the index named in the SPL.",
  "app": "Google Cloud Dataflow job metrics and logs via the Google Cloud Add-on for Splunk / Pub-Sub to HEC. — Splunk Add-on for Google Cloud Platform (Splunkbase 3084)",
  "spl": "index=bi sourcetype=gcp:dataflow job_state=\"JOB_STATE_FAILED\"\n| stats count as failures, latest(_time) as last_failure by job_name\n| where failures>0\n| sort - failures",
  "description": "Surfaces UC-24.8.44 — Google, Dataflow, Failures, Job — using `index=bi` sourcetype `gcp:dataflow`, grouped by `job_name`. Outputs `count` each `the scheduled window`; alert when `failures` > 0. Primary focus: by Job. Purpose-built for 'Google Dataflow Failures by Job' in Operations workflows.",
  "value": "UC-24.8.44 accelerates insight into by Job ('Google Dataflow Failures by Job') by tracking `activity` on `gcp:dataflow` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Ingest Google Dataflow failure events; schedule every 15 minutes and alert on failures for business-critical jobs.",
  "visualization": "Table of job by failure count and last failure time.",
  "cimModels": [],
  "references": [
    {
      "title": "Google Cloud Dataflow — Monitoring",
      "url": "https://cloud.google.com/dataflow/docs/guides/using-monitoring-intf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Google Cloud Dataflow — Troubleshooting",
      "url": "https://cloud.google.com/dataflow/docs/guides/troubleshooting-your-pipeline",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `bi` (UC-24.8.44)** — Planned work for 'Google Dataflow Failures by Job' shifts `count` over `span=1h` while `job_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `gcp:dataflow`** — Scheduled automation elevates `src` without crossing `failures` > 0 for UC-24.8.44. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `job_name`** — Health checks in `bi` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `gcp:dataflow`** — First 48h baseline for UC-24.8.44 on `job_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `job_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "gcp"
  ],
  "equipmentModels": [
    "gcp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Google Cloud Dataflow job metrics and logs via the Google Cloud Add-on for Splunk / Pub-Sub to HEC. — Splunk Add-on for Google Cloud Platform (Splunkbase 3084)). Enable the modular input or HEC token that writes sourcetype=gcp:dataflow into bi. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`failures, job_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We tell you exactly which parts of Google Dataflow are failing, not just that something is wrong, so the right thing gets fixed first.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
