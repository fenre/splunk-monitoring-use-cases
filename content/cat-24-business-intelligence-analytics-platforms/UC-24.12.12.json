{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.12.12",
  "title": "Hightouch Sync Sync Duration p95",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Hightouch Sync Sync Duration p95' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_hightouch_sync_sync_duration_p95` (UC-24.12.12), SOC dashboard panel \"Operations — Hightouch Sync Sync Duration p95\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-24.12.12 positive (Hightouch, Sync, Sync, Duration, p95): ingest synthetic events into `index=bi` sourcetype='hightouch:sync' where `avg_min` grouped by `sync_name` satisfies `p95_min` > 30 for 'Hightouch Sync Sync Duration p95'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-24.12.12 negative (Duration p95): ingest benign `bi` / `hightouch:sync` events where `sync_name` stays below `p95_min` > 30, or exclude the test entity via `ops_exception_register.csv`. Variant 11 confirms zero alert rows for 'Hightouch Sync Sync Duration p95'."
  },
  "dataSources": "Hightouch syncs (`hightouch:sync`). Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Hightouch sync run events via the Hightouch API/webhooks to HEC. — Universal Forwarder (Splunkbase 617)",
  "spl": "index=bi sourcetype=hightouch:sync status=\"success\"\n| eval dur_min=round(duration_ms/60000,2)\n| stats avg(dur_min) as avg_min, p95(dur_min) as p95_min, count as runs by sync_name\n| where p95_min>30\n| sort - p95_min",
  "description": "Alerts on UC-24.12.12 — Hightouch, Sync, Sync, Duration, p95 — using `index=bi` sourcetype `hightouch:sync`, grouped by `sync_name`. Outputs `avg_min` each `the scheduled window`; alert when `p95_min` > 30. Primary focus: Duration p95. Purpose-built for 'Hightouch Sync Sync Duration p95' in Operations workflows.",
  "value": "UC-24.12.12 helps teams contain Hightouch, Sync, Sync, Duration, p95 scenarios and reduce operational risk and delayed incident response before incidents compound.",
  "implementation": "Ingest Hightouch timing data; schedule daily over a trailing week and review the slowest syncs with their owners.",
  "visualization": "Table of sync by p95 sync duration, trend line.",
  "cimModels": [],
  "references": [
    {
      "title": "Hightouch — Sync monitoring",
      "url": "https://hightouch.com/docs/syncs/monitoring",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Hightouch — API reference",
      "url": "https://hightouch.com/docs/api-reference/introduction",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `bi` (UC-24.12.12)** — Planned work for 'Hightouch Sync Sync Duration p95' shifts `avg_min` over `span=1h` while `sync_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `hightouch:sync`** — Scheduled automation elevates `dur_min` without crossing `p95_min` > 30 for UC-24.12.12. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `sync_name`** — Health checks in `bi` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `hightouch:sync`** — First 48h baseline for UC-24.12.12 on `sync_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `sync_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Hightouch sync run events via the Hightouch API/webhooks to HEC. — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=hightouch:sync into bi. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`p95_min, sync_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We measure how slow the worst cases are in Hightouch so the sluggish ones get fixed before people give up on them.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
