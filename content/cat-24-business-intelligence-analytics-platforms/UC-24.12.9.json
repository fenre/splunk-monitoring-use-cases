{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.12.9",
  "title": "Activation Audience Freshness SLA",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Operations",
    "Data Quality"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Activation Audience Freshness SLA' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_activation_audience_freshness_sla` (UC-24.12.9), SOC dashboard panel \"Operations — Activation Audience Freshness SLA\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-24.12.9 positive (Activation, Audience, Freshness, SLA): ingest synthetic events into `index=bi` sourcetype='hightouch:sync' where `count` grouped by `sync_name`, `destination` satisfies `hours_stale` > 6 for 'Activation Audience Freshness SLA'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-24.12.9 negative (Freshness SLA): ingest benign `bi` / `hightouch:sync` events where `sync_name`, `destination` stays below `hours_stale` > 6, or exclude the test entity via `ops_exception_register.csv`. Variant 13 confirms zero alert rows for 'Activation Audience Freshness SLA'."
  },
  "dataSources": "Hightouch syncs (`hightouch:sync`), Census syncs (`census:sync`), Segment/RudderStack delivery events (`segment:delivery`, `rudderstack:delivery`). Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Reverse-ETL platform APIs and webhooks (Hightouch, Census, Segment, RudderStack) via HEC scripted inputs; sync run and delivery logs. — Universal Forwarder (Splunkbase 617)",
  "spl": "index=bi (sourcetype=hightouch:sync OR sourcetype=census:sync) status=\"completed\"\n| stats latest(_time) as last_sync by sync_name, destination\n| eval hours_stale=round((now()-last_sync)/3600,1)\n| where hours_stale>6\n| sort - hours_stale",
  "description": "Alerts on UC-24.12.9 — Activation, Audience, Freshness, SLA — using `index=bi` sourcetype `hightouch:sync`, grouped by `sync_name`, `destination`. Outputs `count` each `the scheduled window`; alert when `hours_stale` > 6. Primary focus: Freshness SLA. Purpose-built for 'Activation Audience Freshness SLA' in Operations workflows.",
  "value": "UC-24.12.9 gives Operations teams earlier visibility into Freshness SLA through `hightouch:sync` feeds, lowering operational risk and delayed incident response.",
  "implementation": "Ingest sync completion events; schedule hourly and alert when a revenue-critical sync exceeds its freshness window.",
  "visualization": "Table of sync by hours stale, freshness heat map.",
  "cimModels": [],
  "references": [
    {
      "title": "Hightouch — API & sync monitoring",
      "url": "https://hightouch.com/docs/syncs/monitoring",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Census — Observability",
      "url": "https://docs.getcensus.com/basics/observability",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `bi` (UC-24.12.9)** — Planned work for 'Activation Audience Freshness SLA' shifts `count` over `span=1h` while `sync_name`, `destination` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `hightouch:sync`** — Scheduled automation elevates `destination` without crossing `hours_stale` > 6 for UC-24.12.9. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `sync_name`, `destination`** — Health checks in `bi` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `hightouch:sync`** — First 48h baseline for UC-24.12.9 on `sync_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `sync_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Reverse-ETL platform APIs and webhooks (Hightouch, Census, Segment, RudderStack) via HEC scripted inputs; sync run and delivery logs. — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=hightouch:sync, census:sync into bi. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`destination, hours_stale, sync_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We check that the customer lists sent to your marketing and sales tools stay up to date.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
