{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.8.41",
  "title": "AWS DMS Failures by Task",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'AWS DMS Failures by Task' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_aws_dms_failures_by_task` (UC-24.8.41), SOC dashboard panel \"Operations — AWS DMS Failures by Task\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-24.8.41 positive (AWS, DMS, Failures, Task): ingest synthetic events into `index=bi` sourcetype='aws:dms:task' where `count` grouped by `replication_task` satisfies `failures` > 0 for 'AWS DMS Failures by Task'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-24.8.41 negative (by Task): ingest benign `bi` / `aws:dms:task` events where `replication_task` stays below `failures` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 11 confirms zero alert rows for 'AWS DMS Failures by Task'."
  },
  "dataSources": "AWS DMS replication tasks (`aws:dms:task`). Collected via Splunk Add-on for Amazon Web Services (Splunkbase 1876) modular input or scripted poll into the index named in the SPL.",
  "app": "AWS Database Migration Service task events via CloudWatch to the Splunk Add-on for AWS. — Splunk Add-on for Amazon Web Services (Splunkbase 1876)",
  "spl": "index=bi sourcetype=aws:dms:task status=\"failed\" OR status=\"stopped\"\n| stats count as failures, latest(_time) as last_failure by replication_task\n| where failures>0\n| sort - failures",
  "description": "Surfaces UC-24.8.41 — AWS, DMS, Failures, Task — using `index=bi` sourcetype `aws:dms:task`, grouped by `replication_task`. Outputs `count` each `the scheduled window`; alert when `failures` > 0. Primary focus: by Task. Purpose-built for 'AWS DMS Failures by Task' in Operations workflows.",
  "value": "UC-24.8.41 helps teams contain AWS, DMS, Failures, Task scenarios and reduce operational risk and delayed incident response before incidents compound.",
  "implementation": "Ingest AWS DMS failure events; schedule every 15 minutes and alert on failures for business-critical tasks.",
  "visualization": "Table of task by failure count and last failure time.",
  "cimModels": [],
  "references": [
    {
      "title": "AWS DMS — Monitoring",
      "url": "https://docs.aws.amazon.com/dms/latest/userguide/CHAP_Monitoring.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Add-on for AWS",
      "url": "https://splunkbase.splunk.com/app/1876",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `bi` (UC-24.8.41)** — Planned work for 'AWS DMS Failures by Task' shifts `count` over `span=1h` while `replication_task` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `aws:dms:task`** — Scheduled automation elevates `src` without crossing `failures` > 0 for UC-24.8.41. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `replication_task`** — Health checks in `bi` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `aws:dms:task`** — First 48h baseline for UC-24.8.41 on `replication_task` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `replication_task`, `exception_reason`, `valid_until`.",
  "equipment": [
    "aws"
  ],
  "equipmentModels": [
    "aws_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (AWS Database Migration Service task events via CloudWatch to the Splunk Add-on for AWS. — Splunk Add-on for Amazon Web Services (Splunkbase 1876)). Enable the modular input or HEC token that writes sourcetype=aws:dms:task into bi. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`failures, replication_task`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We tell you exactly which parts of AWS DMS are failing, not just that something is wrong, so the right thing gets fixed first.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
