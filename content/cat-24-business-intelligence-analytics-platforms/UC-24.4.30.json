{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.4.30",
  "title": "Qlik NPrinting Failures by Report",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Qlik NPrinting Failures by Report' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_qlik_nprinting_failures_by_report` (UC-24.4.30), SOC dashboard panel \"Operations — Qlik NPrinting Failures by Report\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-24.4.30 positive (Qlik, NPrinting, Failures, Report): ingest synthetic events into `index=bi` sourcetype='qlik:nprinting' where `count` grouped by `report_name` satisfies `failures` > 0 for 'Qlik NPrinting Failures by Report'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-24.4.30 negative (by Report): ingest benign `bi` / `qlik:nprinting` events where `report_name` stays below `failures` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 6 confirms zero alert rows for 'Qlik NPrinting Failures by Report'."
  },
  "dataSources": "Qlik NPrinting events (`qlik:nprinting`). Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Qlik NPrinting engine and scheduler logs via UF and the NPrinting API via HEC. — Universal Forwarder (Splunkbase 617)",
  "spl": "index=bi sourcetype=qlik:nprinting status=\"Failed\"\n| stats count as failures, latest(_time) as last_failure by report_name\n| where failures>0\n| sort - failures",
  "description": "Monitors by Report anomalies for UC-24.4.30 with window `the scheduled window` on `index=bi` sourcetype `qlik:nprinting`. Combines `count` by `report_name` and compares against `failures` > 0. Mapped to keywords Qlik, NPrinting, Failures, Report for dashboard routing.",
  "value": "Alerting on Qlik, NPrinting, Failures, Report through UC-24.4.30 reduces operational risk and delayed incident response by surfacing `activity` early.",
  "implementation": "Ingest Qlik NPrinting failure events; schedule every 15 minutes and alert on failures for business-critical reports.",
  "visualization": "Table of report by failure count and last failure time.",
  "cimModels": [],
  "references": [
    {
      "title": "Qlik — NPrinting",
      "url": "https://help.qlik.com/en-US/nprinting/latest/Content/NPrinting/Introduction/Introduction.htm",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Qlik — NPrinting API",
      "url": "https://help.qlik.com/en-US/nprinting/latest/Content/NPrinting/DeployingQVNprinting/API.htm",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `bi` (UC-24.4.30)** — Planned work for 'Qlik NPrinting Failures by Report' shifts `count` over `span=1h` while `report_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `qlik:nprinting`** — Scheduled automation elevates `src` without crossing `failures` > 0 for UC-24.4.30. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `report_name`** — Health checks in `bi` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `qlik:nprinting`** — First 48h baseline for UC-24.4.30 on `report_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `report_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Qlik NPrinting engine and scheduler logs via UF and the NPrinting API via HEC. — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=qlik:nprinting into bi. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`failures, report_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We tell you exactly which parts of Qlik NPrinting are failing, not just that something is wrong, so the right thing gets fixed first.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
