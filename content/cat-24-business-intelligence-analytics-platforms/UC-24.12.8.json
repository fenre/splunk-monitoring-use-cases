{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.12.8",
  "title": "Reverse-ETL Row Rejection Rate by Destination",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Data Quality"
  ],
  "splunkPillar": "Observability",
  "dataSources": "Hightouch syncs (`hightouch:sync`), Census syncs (`census:sync`), Segment/RudderStack delivery events (`segment:delivery`, `rudderstack:delivery`).",
  "app": "Reverse-ETL platform APIs and webhooks (Hightouch, Census, Segment, RudderStack) via HEC scripted inputs; sync run and delivery logs.",
  "spl": "index=bi (sourcetype=hightouch:sync OR sourcetype=census:sync)\n| stats sum(records_success) as ok, sum(records_rejected) as rejected by destination, sync_name\n| eval reject_pct=round(100*rejected/(ok+rejected),2)\n| where rejected>0\n| sort - reject_pct",
  "description": "Aggregates successfully synced versus rejected rows per reverse-ETL destination so silent field-validation and mapping rejections that starve operational tools are quantified.",
  "value": "Rejected rows mean the CRM or ad tool never gets the record; reject-rate tracking protects the operational value of activated data.",
  "implementation": "Ingest per-sync success/reject counts; schedule after each sync and alert when reject rate crosses a per-destination threshold.",
  "visualization": "Table of destination by reject rate, trend line.",
  "cimModels": [],
  "references": [
    {
      "title": "Hightouch — API & sync monitoring",
      "url": "https://hightouch.com/docs/syncs/monitoring"
    },
    {
      "title": "Census — Observability",
      "url": "https://docs.getcensus.com/basics/observability"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "grandmaExplanation": "We count the records that a destination tool refused so you know when good data is not making it through."
}
