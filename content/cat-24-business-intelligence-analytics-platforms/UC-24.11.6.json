{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.11.6",
  "title": "Metric Definition Consistency Drift",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Data Quality",
    "Configuration"
  ],
  "splunkPillar": "Observability",
  "dataSources": "Semantic layer query logs (`cube:api`, `semantic_layer:query`), MetricFlow query events, and AtScale engine logs.",
  "app": "Semantic-layer query and build logs (dbt Semantic Layer / MetricFlow, Cube, AtScale) via REST/GraphQL APIs and application logs to HEC.",
  "spl": "index=bi sourcetype=semantic_layer:query\n| stats dc(metric_definition_hash) as versions, values(caller) as callers by metric_name\n| where versions>1\n| sort - versions",
  "description": "Detects when the same governed metric is served with more than one definition hash across callers, catching the situation where a metric silently means different things in different tools.",
  "value": "A single source of truth is the whole point of a semantic layer; definition drift reintroduces the metric-inconsistency problem it was meant to solve.",
  "implementation": "Emit the resolved metric-definition hash with each semantic-layer query; schedule daily and alert when a metric shows multiple active definitions.",
  "visualization": "Table of metric by definition versions and callers.",
  "cimModels": [],
  "references": [
    {
      "title": "dbt Semantic Layer — MetricFlow",
      "url": "https://docs.getdbt.com/docs/build/about-metricflow"
    },
    {
      "title": "Cube — Monitoring & logging",
      "url": "https://cube.dev/docs/product/deployment"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "grandmaExplanation": "We make sure a business number like 'revenue' means exactly the same thing everywhere it is used."
}
