{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.2.4",
  "title": "Power BI Gateway Query Errors and Throughput",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance",
    "Fault"
  ],
  "splunkPillar": "Observability",
  "dataSources": "Power BI activity events (`o365:management:activity` with `Workload=PowerBI`), Power BI REST Admin API (`/admin/groups`, `/datasets/refreshes`, `/capacities/refreshables`), on-prem data gateway `Mashup`/`GatewayInfo` logs, Premium capacity metrics.",
  "app": "Splunk Add-on for Microsoft Cloud Services / Microsoft 365 (`Splunk_TA_microsoft-cloudservices`) for the Power BI activity log; Power BI REST Admin API and on-prem data gateway logs via HEC scripted inputs.",
  "spl": "index=bi sourcetype=powerbi:gateway:mashup\n| stats count as queries, sum(eval(isnotnull(errorMessage))) as errors, avg(durationMs) as avg_ms by dataSource\n| eval error_rate=round(100*errors/queries,1)\n| where errors>0\n| sort - error_rate",
  "description": "Analyses gateway Mashup query logs to surface data sources with high error rates or slow query times, pinpointing the connections degrading Power BI performance.",
  "value": "Gateway query errors translate directly into failed report loads; source-level error rates show which backend connection to fix first.",
  "implementation": "Ingest gateway Mashup logs; schedule hourly; alert on rising error rate per source. Correlate slow sources with the underlying database health.",
  "visualization": "Table by error_rate, timechart of gateway errors, latency distribution.",
  "cimModels": [],
  "references": [
    {
      "title": "Power BI — Track user activities (audit log)",
      "url": "https://learn.microsoft.com/power-bi/enterprise/service-admin-auditing"
    },
    {
      "title": "Power BI REST Admin API",
      "url": "https://learn.microsoft.com/rest/api/power-bi/admin"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "equipment": [
    "azure",
    "m365"
  ],
  "grandmaExplanation": "We watch the queries the Power BI bridge sends to your databases and flag the ones that fail or run slowly, so the weak link gets fixed."
}
