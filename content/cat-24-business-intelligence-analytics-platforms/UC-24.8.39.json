{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.8.39",
  "title": "Kafka Connect Failures by Connector",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "dataSources": "Kafka Connect status (`kafkaconnect:status`), JMX (`kafkaconnect:jmx`).",
  "app": "Kafka Connect REST status API and worker logs/JMX via HEC scripted inputs.",
  "spl": "index=bi sourcetype=kafkaconnect:status task_state=\"FAILED\" OR connector_state=\"FAILED\"\n| stats count as failures, latest(_time) as last_failure by connector\n| where failures>0\n| sort - failures",
  "description": "Counts Kafka Connect failures grouped by connector so operators immediately see which specific connectors are broken rather than a single global failure number.",
  "value": "Object-level failure attribution turns a vague 'Kafka Connect is failing' alert into an actionable list of exactly what to fix first.",
  "implementation": "Ingest Kafka Connect failure events; schedule every 15 minutes and alert on failures for business-critical connectors.",
  "visualization": "Table of connector by failure count and last failure time.",
  "cimModels": [],
  "references": [
    {
      "title": "Kafka Connect — REST API",
      "url": "https://kafka.apache.org/documentation/#connect_rest"
    },
    {
      "title": "Kafka Connect — Monitoring",
      "url": "https://kafka.apache.org/documentation/#connect_monitoring"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "equipment": [
    "kafka"
  ],
  "grandmaExplanation": "We tell you exactly which parts of Kafka Connect are failing, not just that something is wrong, so the right thing gets fixed first."
}
