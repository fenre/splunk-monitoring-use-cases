{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.8.11",
  "title": "Fivetran Sync Duration Trend",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance",
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Fivetran Sync Duration Trend' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_fivetran_sync_duration_trend` (UC-24.8.11), SOC dashboard panel \"Operations — Fivetran Sync Duration Trend\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-24.8.11 positive (Fivetran, Sync, Duration, Trend): ingest synthetic events into `index=bi` sourcetype='fivetran:log' where `avg_min` grouped by `connector_id` satisfies `p95_min` > 60 for 'Fivetran Sync Duration Trend'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-24.8.11 negative (Duration Trend): ingest benign `bi` / `fivetran:log` events where `connector_id` stays below `p95_min` > 60, or exclude the test entity via `ops_exception_register.csv`. Variant 2 confirms zero alert rows for 'Fivetran Sync Duration Trend'."
  },
  "dataSources": "Fivetran REST API connector status (`fivetran:connector`), log-service sync events (`fivetran:log`), and usage/MAR exports. Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Fivetran REST API (`/v1/connectors`, `/v1/metadata`) and Fivetran Platform Connector logs via HEC scripted inputs; Fivetran log-service events (`fivetran:log`). — Universal Forwarder (Splunkbase 617)",
  "spl": "index=bi sourcetype=fivetran:log event=\"sync_end\"\n| eval dur_min=round(duration_ms/60000,1)\n| stats avg(dur_min) as avg_min, p95(dur_min) as p95_min by connector_id\n| where p95_min>60\n| sort - p95_min",
  "description": "Detects UC-24.8.11 for Fivetran, Sync, Duration, Trend over `index=bi` sourcetype `fivetran:log` with thresholds `p95_min` > 60. Groups `avg_min` by `connector_id` each `the scheduled window`. Centered on Duration Trend indicators. Supports repeatable operations triage for 'Fivetran Sync Duration Trend'.",
  "value": "For 'Fivetran Sync Duration Trend', UC-24.8.11 converts `avg_min` on `fivetran:log` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Compute duration from sync_start/sync_end events; schedule daily and review the slowest connectors.",
  "visualization": "Table of connector by p95 duration, trend line.",
  "cimModels": [],
  "references": [
    {
      "title": "Fivetran — REST API reference",
      "url": "https://fivetran.com/docs/rest-api",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Fivetran — Platform Connector / logs",
      "url": "https://fivetran.com/docs/logs/fivetran-platform",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `bi` (UC-24.8.11)** — Planned work for 'Fivetran Sync Duration Trend' shifts `avg_min` over `span=1h` while `connector_id` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `fivetran:log`** — Scheduled automation elevates `dur_min` without crossing `p95_min` > 60 for UC-24.8.11. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `connector_id`** — Health checks in `bi` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `fivetran:log`** — First 48h baseline for UC-24.8.11 on `connector_id` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `connector_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Fivetran REST API (`/v1/connectors`, `/v1/metadata`) and Fivetran Platform Connector logs via HEC scripted inputs; Fivetran log-service events (`fivetran:log`). — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=fivetran:log into bi. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`connector_id, p95_min`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We track how long each data sync takes so the slow ones get attention before they make data arrive late.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
