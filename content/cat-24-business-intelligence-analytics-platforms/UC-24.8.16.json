{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.8.16",
  "title": "AWS Glue Crawler Failures",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Data Quality"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'AWS Glue Crawler Failures' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_aws_glue_crawler_failures` (UC-24.8.16), SOC dashboard panel \"Operations — AWS Glue Crawler Failures\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-24.8.16 positive (AWS, Glue, Crawler, Failures): ingest synthetic events into `index=bi` sourcetype='aws:cloudwatch' where `count` grouped by `crawler_name` satisfies detection threshold for 'AWS Glue Crawler Failures'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-24.8.16 negative (Crawler Failures): ingest benign `bi` / `aws:cloudwatch` events where `crawler_name` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 6 confirms zero alert rows for 'AWS Glue Crawler Failures'."
  },
  "dataSources": "AWS Glue job runs (`aws:glue:jobrun`), crawler events (`aws:cloudwatch`), and CloudTrail Glue API calls (`aws:cloudtrail`). Collected via Splunk Add-on for Amazon Web Services (Splunkbase 1876) modular input or scripted poll into the index named in the SPL.",
  "app": "AWS Glue job run and crawler events via CloudWatch/CloudTrail to the Splunk Add-on for AWS; Glue API `GetJobRuns` via HEC scripted inputs. — Splunk Add-on for Amazon Web Services (Splunkbase 1876)",
  "spl": "index=bi sourcetype=aws:cloudwatch source=\"*glue*\" (state=\"Failed\" OR crawlerState=\"Failed\")\n| stats count as failures, latest(_time) as last_fail by crawler_name\n| sort - failures",
  "description": "Identifies Crawler Failures anomalies for UC-24.8.16 with window `the scheduled window` on `index=bi` sourcetype `aws:cloudwatch`. Combines `count` by `crawler_name` and compares against configured threshold. Mapped to keywords AWS, Glue, Crawler, Failures for dashboard routing.",
  "value": "Alerting on AWS, Glue, Crawler, Failures through UC-24.8.16 reduces operational risk and delayed incident response by surfacing `activity` early.",
  "implementation": "Route Glue CloudWatch events through the Splunk Add-on for AWS; schedule hourly and alert on crawler failures for governed databases.",
  "visualization": "Table of crawler by failures, timechart.",
  "cimModels": [],
  "references": [
    {
      "title": "AWS Glue — Monitoring with CloudWatch",
      "url": "https://docs.aws.amazon.com/glue/latest/dg/monitor-cloudwatch.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Add-on for AWS",
      "url": "https://splunkbase.splunk.com/app/1876",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `bi` (UC-24.8.16)** — Planned work for 'AWS Glue Crawler Failures' shifts `count` over `span=1h` while `crawler_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `aws:cloudwatch`** — Scheduled automation elevates `src` without crossing configured threshold for UC-24.8.16. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `crawler_name`** — Health checks in `bi` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `aws:cloudwatch`** — First 48h baseline for UC-24.8.16 on `crawler_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `crawler_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "aws"
  ],
  "equipmentModels": [
    "aws_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (AWS Glue job run and crawler events via CloudWatch/CloudTrail to the Splunk Add-on for AWS; Glue API `GetJobRuns` via HEC scripted inputs. — Splunk Add-on for Amazon Web Services (Splunkbase 1876)). Enable the modular input or HEC token that writes sourcetype=aws:cloudwatch into bi. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`crawler_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch the jobs that keep the data lake's index up to date and flag failures so queries do not miss new data.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
