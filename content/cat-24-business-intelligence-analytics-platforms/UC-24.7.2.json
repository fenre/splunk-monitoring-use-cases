{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.7.2",
  "title": "Airflow Task Instance Retries and Failures",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault",
    "Reliability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Airflow Task Instance Retries and Failures' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_airflow_task_instance_retries_and_failures` (UC-24.7.2), SOC dashboard panel \"Operations — Airflow Task Instance Retries and Failures\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-24.7.2 positive (Airflow, Task, Instance, Retries, and): ingest synthetic events into `index=dataops` sourcetype='airflow:task' where `sum` grouped by `dag_id`, `task_id` satisfies `retries` > 3, or `failures` > 0 for 'Airflow Task Instance Retries and Failures'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-24.7.2 negative (and Failures): ingest benign `dataops` / `airflow:task` events where `dag_id`, `task_id` stays below `retries` > 3, or `failures` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 6 confirms zero alert rows for 'Airflow Task Instance Retries and Failures'."
  },
  "dataSources": "Airflow logs (`airflow:scheduler`, `airflow:task`), Airflow StatsD/OTel metrics, dbt `run_results.json`/`sources.json` (`dbt:run_results`), dbt Cloud API run history, Dagster/Prefect run events. Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Apache Airflow scheduler/worker logs and StatsD/OTel metrics via HEC; dbt run-results/manifest artifacts and dbt Cloud API via HEC scripted inputs; Dagster/Prefect event logs. — Universal Forwarder (Splunkbase 617)",
  "spl": "index=dataops sourcetype=airflow:task\n| stats sum(eval(state=\"up_for_retry\")) as retries, sum(eval(state=\"failed\")) as failures by dag_id, task_id\n| where retries>3 OR failures>0\n| sort - failures, - retries",
  "description": "Detects and Failures anomalies for UC-24.7.2 with window `the scheduled window` on `index=dataops` sourcetype `airflow:task`. Combines `sum` by `dag_id`, `task_id` and compares against `retries` > 3, or `failures` > 0. Mapped to keywords Airflow, Task, Instance, Retries, and for dashboard routing.",
  "value": "Alerting on Airflow, Task, Instance, Retries, and through UC-24.7.2 reduces operational risk and delayed incident response by surfacing `activity` early.",
  "implementation": "Ingest task logs; schedule hourly; alert on chronic retriers. Investigate flaky operators or upstream dependencies.",
  "visualization": "Table by task with retries/failures, timechart.",
  "cimModels": [],
  "references": [
    {
      "title": "Apache Airflow — Metrics",
      "url": "https://airflow.apache.org/docs/apache-airflow/stable/administration-and-deployment/logging-monitoring/metrics.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `dataops` (UC-24.7.2)** — Planned work for 'Airflow Task Instance Retries and Failures' shifts `sum` over `span=1h` while `dag_id`, `task_id` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `sum` on `airflow:task`** — Scheduled automation elevates `task_id` without crossing `retries` > 3 for UC-24.7.2. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `dag_id`, `task_id`** — Health checks in `dataops` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `airflow:task`** — First 48h baseline for UC-24.7.2 on `dag_id` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `dag_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "apache"
  ],
  "equipmentModels": [
    "apache_httpd"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Apache Airflow scheduler/worker logs and StatsD/OTel metrics via HEC; dbt run-results/manifest artifacts and dbt Cloud API via HEC scripted inputs; Dagster/Prefect event logs. — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=airflow:task into dataops. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`dag_id, failures, retries, task_id`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We spot the individual steps in a data workflow that keep stumbling, so the flaky one gets fixed instead of slowing everything.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
