{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.8.62",
  "title": "dlt Pipeline Freshness SLA",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Data Quality",
    "Operations"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'dlt Pipeline Freshness SLA' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_dlt_pipeline_freshness_sla` (UC-24.8.62), SOC dashboard panel \"Operations — dlt Pipeline Freshness SLA\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-24.8.62 positive (dlt, Pipeline, Freshness, SLA): ingest synthetic events into `index=bi` sourcetype='dlt:load' where `count` grouped by `pipeline_name` satisfies `hours_stale` > 12 for 'dlt Pipeline Freshness SLA'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-24.8.62 negative (Freshness SLA): ingest benign `bi` / `dlt:load` events where `pipeline_name` stays below `hours_stale` > 12, or exclude the test entity via `ops_exception_register.csv`. Variant 10 confirms zero alert rows for 'dlt Pipeline Freshness SLA'."
  },
  "dataSources": "dlt load info (`dlt:load`). Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "dlt (data load tool) pipeline trace and load-info events forwarded as JSON to HEC. — Universal Forwarder (Splunkbase 617)",
  "spl": "index=bi sourcetype=dlt:load status=\"completed\"\n| stats latest(_time) as last_success by pipeline_name\n| eval hours_stale=round((now()-last_success)/3600,1)\n| where hours_stale>12\n| sort - hours_stale",
  "description": "Identifies UC-24.8.62 — dlt, Pipeline, Freshness, SLA — using `index=bi` sourcetype `dlt:load`, grouped by `pipeline_name`. Outputs `count` each `the scheduled window`; alert when `hours_stale` > 12. Primary focus: Freshness SLA. Purpose-built for 'dlt Pipeline Freshness SLA' in Operations workflows.",
  "value": "For 'dlt Pipeline Freshness SLA', UC-24.8.62 converts `activity` on `dlt:load` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Ingest dlt success events; schedule hourly and alert when a critical pipeline exceeds its freshness window.",
  "visualization": "Table of pipeline by hours stale, freshness heat map.",
  "cimModels": [],
  "references": [
    {
      "title": "dlt — Documentation",
      "url": "https://dlthub.com/docs/intro",
      "retrieved": "2026-04-25"
    },
    {
      "title": "dlt — Monitoring & tracing",
      "url": "https://dlthub.com/docs/running-in-production/monitoring",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `bi` (UC-24.8.62)** — Planned work for 'dlt Pipeline Freshness SLA' shifts `count` over `span=1h` while `pipeline_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `dlt:load`** — Scheduled automation elevates `hours_stale` without crossing `hours_stale` > 12 for UC-24.8.62. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `pipeline_name`** — Health checks in `bi` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `dlt:load`** — First 48h baseline for UC-24.8.62 on `pipeline_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `pipeline_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (dlt (data load tool) pipeline trace and load-info events forwarded as JSON to HEC. — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=dlt:load into bi. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`hours_stale, pipeline_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We check that each part of dlt has updated recently so nobody relies on old data.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
