{
  "$schema": "../../schemas/uc.schema.json",
  "id": "24.7.46",
  "title": "Databricks Workflows Failures by Job",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Databricks Workflows Failures by Job' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_databricks_workflows_failures_by_job` (UC-24.7.46), SOC dashboard panel \"Operations — Databricks Workflows Failures by Job\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-24.7.46 positive (Databricks, Workflows, Failures, Job): ingest synthetic events into `index=bi` sourcetype='databricks:jobs' where `count` grouped by `job_name` satisfies `failures` > 0 for 'Databricks Workflows Failures by Job'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-24.7.46 negative (by Job): ingest benign `bi` / `databricks:jobs` events where `job_name` stays below `failures` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 11 confirms zero alert rows for 'Databricks Workflows Failures by Job'."
  },
  "dataSources": "Databricks job runs (`databricks:jobs`). Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Databricks Jobs API and system.lakeflow tables via HEC/DB Connect. — Universal Forwarder (Splunkbase 617)",
  "spl": "index=bi sourcetype=databricks:jobs result_state=\"FAILED\" OR result_state=\"TIMEDOUT\"\n| stats count as failures, latest(_time) as last_failure by job_name\n| where failures>0\n| sort - failures",
  "description": "Monitors UC-24.7.46 — Databricks, Workflows, Failures, Job — using `index=bi` sourcetype `databricks:jobs`, grouped by `job_name`. Outputs `count` each `the scheduled window`; alert when `failures` > 0. Primary focus: by Job. Purpose-built for 'Databricks Workflows Failures by Job' in Operations workflows.",
  "value": "UC-24.7.46 helps teams contain Databricks, Workflows, Failures, Job scenarios and reduce operational risk and delayed incident response before incidents compound.",
  "implementation": "Ingest Databricks Workflows failure events; schedule every 15 minutes and alert on failures for business-critical jobs.",
  "visualization": "Table of job by failure count and last failure time.",
  "cimModels": [],
  "references": [
    {
      "title": "Databricks — Jobs & orchestration",
      "url": "https://docs.databricks.com/aws/en/jobs/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Databricks — Monitor jobs",
      "url": "https://docs.databricks.com/aws/en/jobs/monitor",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `bi` (UC-24.7.46)** — Planned work for 'Databricks Workflows Failures by Job' shifts `count` over `span=1h` while `job_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `databricks:jobs`** — Scheduled automation elevates `src` without crossing `failures` > 0 for UC-24.7.46. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `job_name`** — Health checks in `bi` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `databricks:jobs`** — First 48h baseline for UC-24.7.46 on `job_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `job_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "db_connect"
  ],
  "equipmentModels": [
    "db_connect_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Databricks Jobs API and system.lakeflow tables via HEC/DB Connect. — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=databricks:jobs into bi. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`failures, job_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We tell you exactly which parts of Databricks Workflows are failing, not just that something is wrong, so the right thing gets fixed first.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
