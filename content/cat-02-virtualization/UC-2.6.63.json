{
  "$schema": "../../schemas/uc.schema.json",
  "id": "2.6.63",
  "title": "DaaS Autoscale Cloud Economics Tracking",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'DaaS Autoscale Cloud Economics Tracking'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-2.6.63).",
  "evidence": "Saved search uc_2_6_63_daas_autoscale_cloud_economics_tracking, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'DaaS Autoscale Cloud Economics Tracking'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-2.6.63.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "`index=cloud` or `index=azure` `sourcetype=\"azure:consume:export\"` with `cost`, `resource_id`, `tag_pool`, `meter`; `index=xd` `sourcetype=\"citrix:mc:autoscale\"` with `power_action`, `machine_count`, `session_count`, `host_pool`; `index=xd` `sourcetype=\"citrix:brokering:summary\"` for active sessions; optional FinOps CSV via `inputlookup`",
  "app": "Citrix Analytics Add-on for Splunk (Splunkbase 6280) for aggregated analytics data. For raw Cloud service data, use the Citrix Cloud System Log Add-on (open-source, see citrix/cc-system-log-addon-for-splunk repository) or poll the Monitor Service OData API (https://api.cloud.com/monitorodata) with a custom scripted input.",
  "spl": "index=cloud sourcetype=\"azure:consume:export\" (resource_type=\"*compute*\" OR resource_type=\"*virtual*\")\n| eval tag_pool=if(isnotnull(citrix_host_pool) AND citrix_host_pool!=\"\", citrix_host_pool, coalesce(resource_name, resource_id, \"unmapped\"))\n| bin _time span=1d\n| stats sum(coalesce(tonumber(cost), 0)) as daily_cost by _time, tag_pool\n| join type=left _time, tag_pool [\n  search index=xd (sourcetype=\"citrix:mc:autoscale\" OR sourcetype=\"citrix:brokering:summary\")\n  | eval tag_pool=coalesce(host_pool, delivery_group, catalog_name, \"unmapped\")\n  | bin _time span=1d\n  | stats max(session_count) as peak_sessions, latest(machine_count) as reported_machines by _time, tag_pool\n  ]\n| eval cost_per_session=if(peak_sessions>0, round(daily_cost/peak_sessions,3), null())\n| where isnotnull(daily_cost) AND daily_cost>0\n| table _time, tag_pool, daily_cost, peak_sessions, reported_machines, cost_per_session",
  "description": "DaaS autoscale can chase session demand, but public-cloud bills reflect clock time, instance families, and lingering powered-on capacity as much as user counts. Blending host-pool or delivery-group session peaks with tag-aligned cloud spend shows scale-out efficiency, expensive idle headroom, and cost-per-active-session trends. Finance and platform teams get a defensible way to right-size buffer percentages, change instance SKUs, or tune shutdown aggressiveness without only trusting static dashboards in the admin consoles.",
  "value": "Operations and leadership rely on 'DaaS Autoscale Cloud Economics Tracking' to catch regressions before they become customer-visible outages or audit findings. Early signal reduces mean time to repair, avoids duplicate manual log review, and preserves evidence for post-incident review without waiting for a user-reported ticket.",
  "implementation": "Tag or label cloud VMs with a stable `citrix_host_pool` value matching Splunk's brokering or MCS data. Ingest a daily (or hourly) cost feed with the same key. Build weekly reports: cost per session by pool, unused powered-on hours, and autoscale event counts versus cost deltas. Set soft alerts for sudden jumps in cost-per-session or sustained idle high-water marks after scale events. Engage FinOps to validate currency and amortization. Never alert on cost alone without a session denominator except for obvious billing anomalies. Document that bursty test traffic can skew short windows.",
  "visualization": "Line chart of cost per session by pool; stacked area of instance hours paid versus sessions; table of autoscale power actions and next-day cost impact.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Autoscale in Citrix DaaS",
      "url": "https://docs.citrix.com/en-us/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Microsoft Cost Management (export cost data to external tools)",
      "url": "https://learn.microsoft.com/en-us/azure/cost-management-billing/costs/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "azure",
    "citrix"
  ],
  "equipmentModels": [
    "citrix_cvad"
  ],
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Consistent resource tagging; billing API permissions; at least 14 days of both cost and session history before trusting ratios.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMap catalog or delivery group names in Citrix to tag keys. Fix any untagged SKUs in cloud before go-live to avoid a large unmapped spend bucket.\n\nStep 2 — Create the search and alert\nStart with a monthly executive summary; set anomaly alerts on week-over-week cost-per-session per pool using `anomalydetection` or static thresholds.\n\nStep 3 — Validate\nReconcile a single pool for one day against the cloud console subtotal; reconcile session peaks against Citrix Director or equivalent.\n\nStep 4 — Operationalize\nTie into FinOps and change management when autoscale policy or instance family changes are proposed.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Citrix Analytics Add-on for Splunk (Splunkbase 6280) for aggregated analytics data. For raw Cloud service data, use the Citrix Cloud System Log Add-on (open-source, see citrix/cc-system-log-addon-for-splunk repository) or poll the Monitor Service OData API (https://api.cloud.com/monitorodata) with a custom scripted input.). Enable the modular input or HEC token that writes sourcetype=azure:consume:export, citrix:mc:autoscale, citrix:brokering:summary into cloud, xd. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`], _time, cost_per_session, daily_cost`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We line up what the virtual desktops cost in the public cloud with how many people actually used them, so you can see waste from machines left running and tune the auto-sizing rules with numbers everyone trusts.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
