{
  "$schema": "../../schemas/uc.schema.json",
  "id": "2.6.31",
  "title": "Citrix Zone Topology and Zone Preference Failover",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Citrix Zone Topology and Zone Preference Failover' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_citrix_zone_topology_and_zone_preference_failove` (UC-2.6.31), SOC dashboard panel \"Linux — Citrix Zone Topology and Zone Preference Failover\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=xd` sourcetype='citrix:broker:events' where `_time` satisfies detection threshold for UC-2.6.31 ('Citrix Zone Topology and Zone Preference Failover'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `xd` / `citrix:broker:events` events where `_time` stays below detection threshold, or exclude the test entity via `linux_capacity_exceptions.csv` with a valid window. Confirm zero alert rows for UC-2.6.31."
  },
  "dataSources": "`index=xd` `sourcetype=\"citrix:broker:events\"` (zone change, brokering path, `ZoneName`, `PreferredController`, `Failover*`), `sourcetype=\"citrix:monitor:odata\"` for `Zones` / `Controllers` if collected; `sourcetype=\"citrix:netscaler:syslog\"` optional for GSLB/health probe correlation Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "Template for Citrix XenDesktop 7 (TA-XD7-Broker), Citrix Monitor Service OData API — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=xd sourcetype=\"citrix:broker:events\" (match(_raw, \"(?i)zone|failover|preferred|registration|chassis|data.?store|inter.?zone\") OR event_type IN (\"Zone*\", \"Registration\", \"Configuration\"))\n| eval zone=coalesce(ZoneName, zone_name, Zone)\n| eval path=if(match(_raw, \"(?i)failover|secondary|not.?preferred|alternate\"), \"failover_path\", if(match(_raw, \"(?i)preferred|primary|home.?zone\"), \"preferred_path\", \"other\"))\n| where isnotnull(zone) OR path!=\"other\"\n| bin _time span=5m\n| stats count, values(event_type) as event_types, dc(host) as controller_count by _time, zone, path, delivery_group\n| sort -_time, zone, path",
  "description": "Multi-zone CVAD sites route users to preferred zones; controllers and resources must register and broker in the right order. Unplanned failover traffic, inter-zone brokering storms, or machines registering outside their zone hint at network partitions, site misconfiguration, or loss of a preferred data path. Tracking zone-related broker events and preferred versus failover path selection shows topology stress before end-user latency spikes.",
  "value": "Early 'Citrix Zone Topology and Zone Preference Failover' detection reduces capacity incidents, patch regressions, and service outages by giving Linux operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Standardize `ZoneName` and delivery group in broker events. Create lookups for expected zone–delivery-group mappings. Alert when failover_path volume exceeds baseline, when zone membership churn appears, or when a zone has zero registered workers during business hours. Enrich with NetScaler or WAN metrics if you need proof of network cause.",
  "visualization": "Sankey or flow (preferred vs failover), Timeline (zone events), Table (anomalous delivery groups by zone).",
  "cimModels": [
    "Change"
  ],
  "references": [
    {
      "title": "Zones in Citrix Virtual Apps and Desktops",
      "url": "https://docs.citrix.com/en-us/citrix-virtual-apps-desktops/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 833",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Change",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Change",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `xd`** — Package managers spike `_time` metrics for 'Citrix Zone Topology and Zone Preference Failover'. Correlate with apt/yum logs in the same `citrix:broker:events` window.\n\n2. **Nightly backup I/O on `_time`** — Predictable `values` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `citrix:broker:events`** — Nagios/Zabbix polls `_time` and mimics sustained load without crossing capacity threshold.\n\n4. **New host onboarding** — `_time` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `_time`, `valid_until`.",
  "equipment": [
    "citrix",
    "syslog"
  ],
  "equipmentModels": [
    "citrix_cvad",
    "citrix_netscaler"
  ],
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Template for Citrix XenDesktop 7 (TA-XD7-Broker), optional Citrix Monitor Service OData API for `Zones`.\n• Ensure the following data sources are available: `sourcetype=\"citrix:broker:events\"` with zone and registration fields; optional `citrix:netscaler:syslog` for ADC.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nVerify broker event extractions for zone, registration, and failover terms. Add baseline searches for “normal” preferred-path volume per time-of-day. Document zone architecture and maintenance windows in a lookup.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; tune `event_type` and regex for your build):\n\n```spl\nindex=xd sourcetype=\"citrix:broker:events\" (match(_raw, \"(?i)zone|failover|preferred|registration|chassis|data.?store|inter.?zone\") OR event_type IN (\"Zone*\", \"Registration\", \"Configuration\"))\n| eval zone=coalesce(ZoneName, zone_name, Zone)\n| eval path=if(match(_raw, \"(?i)failover|secondary|not.?preferred|alternate\"), \"failover_path\", if(match(_raw, \"(?i)preferred|primary|home.?zone\"), \"preferred_path\", \"other\"))\n| where isnotnull(zone) OR path!=\"other\"\n| bin _time span=5m\n| stats count, values(event_type) as event_types, dc(host) as controller_count by _time, zone, path, delivery_group\n| sort -_time, zone, path\n```\n\nStep 3 — Validate\nCompare a known DR exercise or forced failover in lab to ensure events classify into preferred_path vs failover_path. Adjust regex if your logs use different wording.\n\nStep 4 — Operationalize\nSet adaptive thresholds; route zone-wide anomalies to the platform team. Add drilldowns to the Citrix and network dashboards for the affected zone.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Template for Citrix XenDesktop 7 (TA-XD7-Broker), Citrix Monitor Service OData API — Splunk Add-on for Unix and Linux (Splunkbase 833)). Enable the modular input or HEC token that writes sourcetype=citrix:broker:events into xd. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, delivery_group, path, zone`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We keep an eye on zone Topology and Zone Preference Failover and raise the alarm before it drags down real work or real outages start.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
