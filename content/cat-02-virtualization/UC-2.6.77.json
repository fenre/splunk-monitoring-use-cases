{
  "$schema": "../../schemas/uc.schema.json",
  "id": "2.6.77",
  "title": "Citrix Per-Application Perceived Performance (Startup vs Hang vs Network)",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'Citrix Per-Application Perceived Performance (Startup vs Hang vs Network)'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-2.6.77).",
  "evidence": "Saved search uc_2_6_77_citrix_per_application_perceived_perform, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'Citrix Per-Application Perceived Performance (Startup vs Hang vs Netwo'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-2.6.77.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "uberAgent: `sourcetype=\"uberAgent:Process:ProcessStartup\"` and `sourcetype=\"uberAgent:Application:Error\"` (if configured); `sourcetype=\"uberAgent:Network:Performance\"` (latency, loss); `sourcetype=\"citrix:broker:app_usage\"` (app title, `launch_to_ready_ms`); or Director OData for ICA RTT and application load time",
  "app": "uberAgent UXM (Splunkbase 1448) with Citrix templates; optional Citrix Monitor / Director export for the same fields",
  "spl": "index=xd (sourcetype=\"uberAgent:Process:ProcessStartup\" OR sourcetype=\"uberAgent:Network:Performance\" OR sourcetype=\"citrix:broker:app_usage\") earliest=-4h\n| eval app=coalesce(app_name, process_name, title, \"unknown\"), start_ms=tonumber(startup_ms), ica=tonumber(ica_rtt_ms), hang=if(match(_raw, \"(?i)not.?(responding)\"),1,0)\n| bin _time span=15m\n| stats median(start_ms) as med_start, median(ica) as med_ica, sum(hang) as hang_ev by _time, app, host\n| where med_start>10000 OR med_ica>100 OR hang_ev>0\n| table _time, app, host, med_start, med_ica, hang_ev",
  "description": "Not all “Citrix is slow” tickets are a network problem. Perceived slowness may be slow application startup, long UI busy states, or real ICA network delay. Combining process startup and hang signals from the VDA with network metrics and broker-reported app ready time splits accountability between packaging, the app itself, the profile, and the path between user and host.",
  "value": "Operations and leadership rely on 'Citrix Per-Application Perceived Performance (Startup vs Hang vs Network)' to catch regressions before they become customer-visible outages or audit findings. Early signal reduces mean time to repair, avoids duplicate manual log review, and preserves evidence for post-incident review without waiting for a user-reported ticket.",
  "implementation": "Standardize on one app name key (avoid publisher vs start-menu title drift). In uberAgent, enable process and network packs for gold images only first. If ICA RTT is missing in uberAgent, add broker or gateway RTT. Build three small alerts: p95 startup, p95 RTT, and not-responding process count, each routed to a different team owner.",
  "visualization": "Small multiples: one row per app with startup, hang count, and RTT; table: top hosts driving bad p95; overlay change markers on image or app version.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "uberAgent — Process and network metrics",
      "url": "https://splunkbase.splunk.com/app/1448",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Citrix — HDX and session performance",
      "url": "https://docs.citrix.com/en-us/citrix-virtual-apps-desktops/technical-overview/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "citrix"
  ],
  "equipmentModels": [
    "citrix_cvad",
    "citrix_uberagent"
  ],
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• `index=xd` with `uberAgent:Process:ProcessStartup`, `Network:Performance`, and `citrix:broker:app_usage`. UberAgent in gold image; broker exports `launch_to_ready_ms` and app title.\n\nStep 1 — Configure data collection\n`lookups/citrix_app_friendly_names.csv` maps `process_name` to `app` for LOB. Exclude backup/AV using a `lookup` column on noisy `process_name` (avoid brittle regexes on `app` alone). Pilot 2 LOB + Office. Verify `startup_ms` is milliseconds, not seconds.\n\nStep 2 — Create the search and alert\nBaseline p50/p95 per app 14d; replace static 10000/100 with lookup-driven SLO. Split alerts: p95 `med_start` to app team; p95 `med_ica` to network; `hang_ev` sustained 3x15m to EUC. Add `by host` to find noisy VDAs.\n\nStep 3 — Validate\nOn one ticket, align `_time` to Director/HDX and a screen cap showing not-responding vs high RTT. Confirm uberAgent and broker clocks within 1s.\n\nStep 4 — Operationalize\nDashboard: small-multiples for top 10 `app`. QBR: split accountability chart. Escalation: packaging vs net vs VDA. Re-baseline after uberAgent, VDA, or broker upgrade.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (uberAgent UXM (Splunkbase 1448) with Citrix templates; optional Citrix Monitor / Director export for the same fields). Enable the modular input or HEC token that writes sourcetype=uberAgent:Process:ProcessStartup, uberAgent:Network:Performance, citrix:broker:app_usage into xd. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, app, hang_ev, host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We break down whether the pain is a slow start, a frozen window, or a choppy line to the data center — so the app owner, the desktop team, and the line folks stop pointing fingers for the same ticket.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
