{
  "$schema": "../../schemas/uc.schema.json",
  "id": "2.3.8",
  "title": "Virtio Driver and Balloon Status in Guests",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Virtio Driver and Balloon Status in Guests' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_virtio_driver_and_balloon_status_in_guests` (UC-2.3.8), SOC dashboard panel \"Linux — Virtio Driver and Balloon Status in Guests\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-2.3.8 positive (Virtio, Driver, and, Balloon, Status): ingest synthetic events into `index=virtualization` sourcetype='kvm_balloon' where `count` grouped by `host`, `vm_name` satisfies `balloon_ratio` > 50 for 'Virtio Driver and Balloon Status in Guests'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-2.3.8 negative (in Guests): ingest benign `virtualization` / `kvm_balloon` events where `host`, `vm_name` stays below `balloon_ratio` > 50, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 12 confirms zero alert rows for 'Virtio Driver and Balloon Status in Guests'."
  },
  "dataSources": "QEMU guest agent, `virsh dommemstat` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom scripted input (guest agent or in-guest script) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=virtualization sourcetype=kvm_balloon host=*\n| stats latest(balloon_current_kb) as balloon_kb, latest(balloon_max_kb) as max_kb by host, vm_name\n| eval balloon_ratio=round(balloon_kb/max_kb*100, 1)\n| where balloon_ratio > 50\n| table host vm_name balloon_kb max_kb balloon_ratio",
  "description": "Monitors UC-2.3.8 — Virtio, Driver, and, Balloon, Status — using `index=virtualization` sourcetype `kvm_balloon`, grouped by `host`, `vm_name`. Outputs `count` each `the scheduled window`; alert when `balloon_ratio` > 50. Primary focus: in Guests. Purpose-built for 'Virtio Driver and Balloon Status in Guests' in Linux workflows.",
  "value": "Tracking `activity` via UC-2.3.8 on `kvm_balloon` feeds improves response to 'Virtio Driver and Balloon Status in Guests' and reduces capacity incidents, patch regressions, and service outages.",
  "implementation": "Use `virsh dommemstat` to get balloon current and maximum. High ratio indicates host is reclaiming memory from the VM. Alert when ratio >50% for critical VMs.",
  "visualization": "Table (VM, balloon KB, ratio), Line chart (balloon over time).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `virtualization`** — Package managers spike `host` metrics for 'Virtio Driver and Balloon Status in Guests'. Correlate with apt/yum logs in the same `kvm_balloon` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `count` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `kvm_balloon`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing `balloon_ratio` > 50.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Custom scripted input (guest agent or in-guest script).\n• Ensure the following data sources are available: QEMU guest agent, `virsh dommemstat`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nUse `virsh dommemstat` to get balloon current and maximum. High ratio indicates host is reclaiming memory from the VM. Alert when ratio >50% for critical VMs.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=virtualization sourcetype=kvm_balloon host=*\n| stats latest(balloon_current_kb) as balloon_kb, latest(balloon_max_kb) as max_kb by host, vm_name\n| eval balloon_ratio=round(balloon_kb/max_kb*100, 1)\n| where balloon_ratio > 50\n| table host vm_name balloon_kb max_kb balloon_ratio\n```\n\nUnderstanding this SPL\n\n**Virtio Driver and Balloon Status in Guests** — Virtio drivers and balloon driver improve I/O and allow memory reclamation. Missing or inactive drivers cause poor performance and overcommit issues.\n\nDocumented **Data sources**: QEMU guest agent, `virsh dommemstat`. **App/TA** (typical add-on context): Custom scripted input (guest agent or in-guest script). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: virtualization; **sourcetype**: kvm_balloon. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=virtualization, sourcetype=kvm_balloon. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by host, vm_name** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **balloon_ratio** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where balloon_ratio > 50` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Virtio Driver and Balloon Status in Guests**): table host vm_name balloon_kb max_kb balloon_ratio\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (VM, balloon KB, ratio), Line chart (balloon over time).",
  "grandmaExplanation": "We keep an eye on virtio Driver and Balloon Status in Guests and raise the alarm before it drags down real work or real outages start.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
