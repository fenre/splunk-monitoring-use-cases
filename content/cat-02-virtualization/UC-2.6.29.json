{
  "$schema": "../../schemas/uc.schema.json",
  "id": "2.6.29",
  "title": "Machine Catalog Image Pipeline Health",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Machine Catalog Image Pipeline Health' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_machine_catalog_image_pipeline_health` (UC-2.6.29), SOC dashboard panel \"Linux — Machine Catalog Image Pipeline Health\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=xd` sourcetype='citrix:monitor:odata' that satisfy the UC-2.6.29 SPL filters for 'Machine Catalog Image Pipeline Health'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `xd` / `citrix:monitor:odata` events outside the detection threshold, or exclude the test entity via `linux_capacity_exceptions.csv` with a valid window. Confirm zero alert rows for UC-2.6.29."
  },
  "dataSources": "`index=xd` `sourcetype=\"citrix:monitor:odata\"` with OData entity scoping to `MachineCatalog` (e.g. `ODataEntity=MachineCatalog` or `entity_type=MachineCatalog`); fields may include `Name`, `MasterImageVhd`, `ProvisioningType`, `LastApplyImageDate`, `UsedCount`, `PendingTaskCount` depending on your TA field mapping Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Citrix Monitor Service OData API, Template for Citrix XenDesktop 7 (TA-XD7-Broker) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=xd sourcetype=\"citrix:monitor:odata\" (ODataEntity=MachineCatalog OR entity_type=MachineCatalog OR Name=*)\n| eval master_age_days=if(isnotnull(LastImageUpdateTime) OR isnotnull(LastMasterImageTime), round((now()-coalesce(LastImageUpdateTime, LastMasterImageTime, _time)) / 86400, 1), null())\n| eval rollout_pending=coalesce(PendingImageRollout, PendingUpdateCount, 0)\n| where master_age_days > 90 OR rollout_pending > 0 OR match(coalesce(ProvisioningStatus, State, ErrorState), \"(?i)fail|error\")\n| table _time, Name, ProvisioningType, master_age_days, rollout_pending, ProvisioningStatus, State, ErrorState, MasterImageVhd, host\n| sort - master_age_days",
  "description": "Machine Catalog health depends on current master images, successful preparation or rollout jobs, and timely rollouts. Stale images (>90 days without refresh), pending rollouts stuck in queue, and provisioning errors reduce pool reliability and can leave machines on vulnerable or non-compliant images. Polling the Monitor `MachineCatalog` OData feed gives a single place to see catalog-level status when broker events do not list every field.",
  "value": "Early 'Machine Catalog Image Pipeline Health' detection reduces capacity incidents, patch regressions, and service outages by giving Linux operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Enable OData collection for Machine Catalog. Align field names to your add-on; use `fieldalias` in `props.conf` if the vendor uses `LastImageTime` instead of `LastMasterImageTime`. Set thresholds: image age 90+ days, any non-empty pending rollout counter for more than 24 hours, and any `Fail` in provisioning. Join to change tickets for image updates. Cross-check MCS/PVS UCs for prep failures on the same image name.",
  "visualization": "Table (catalogs at risk), Line chart (pending rollout trend), Single value (catalogs with stale images).",
  "cimModels": [
    "Endpoint"
  ],
  "references": [
    {
      "title": "Monitor Citrix with OData",
      "url": "https://docs.citrix.com/en-us/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Endpoint",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Endpoint",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday / unattended-upgrades** — CPU and disk I/O spike during package application for 'Machine Catalog Image Pipeline Health'. Correlate with apt/yum logs in same window.\n\n2. **Nightly backup or log-rotate job** — Predictable I/O burst on fixed cron schedule.\n\n3. **Synthetic monitoring probe** — Nagios/Zabbix agent generates periodic load.\n\n4. **New host onboarding** — Baseline metrics unstable first 48h on `xd` `citrix:monitor:odata`.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "citrix"
  ],
  "equipmentModels": [
    "citrix_cvad"
  ],
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Citrix Monitor Service OData API collector, Template for Citrix XenDesktop 7 (TA-XD7-Broker).\n• Ensure the following data sources are available: `index=xd` `sourcetype=\"citrix:monitor:odata\"` for `MachineCatalog`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nPoll the Monitor service with appropriate auth; verify `ODataEntity` or equivalent identifies Machine Catalog records. Add aliases for time and pending-count fields. Optionally enrich with a CSV of image owners and patch cycles.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust field names to match your feed):\n\n```spl\nindex=xd sourcetype=\"citrix:monitor:odata\" (ODataEntity=MachineCatalog OR entity_type=MachineCatalog OR Name=*)\n| eval master_age_days=if(isnotnull(LastImageUpdateTime) OR isnotnull(LastMasterImageTime), round((now()-coalesce(LastImageUpdateTime, LastMasterImageTime, _time)) / 86400, 1), null())\n| eval rollout_pending=coalesce(PendingImageRollout, PendingUpdateCount, 0)\n| where master_age_days > 90 OR rollout_pending > 0 OR match(coalesce(ProvisioningStatus, State, ErrorState), \"(?i)fail|error\")\n| table _time, Name, ProvisioningType, master_age_days, rollout_pending, ProvisioningStatus, State, ErrorState, MasterImageVhd, host\n| sort - master_age_days\n```\n\nStep 3 — Validate\nCompare results to Citrix Studio for a few catalogs. Confirm the 90-day threshold against your image lifecycle policy. Test alert with a non-production catalog.\n\nStep 4 — Operationalize\nSend weekly report to desktop engineering; page on `Fail` states or age breaches over policy. Link dashboard rows to your image build pipeline and vulnerability scanning.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Citrix Monitor Service OData API, Template for Citrix XenDesktop 7 (TA-XD7-Broker) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=citrix:monitor:odata into xd. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`master_age_days, rollout_pending`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We check whether the golden desktop images for your virtual machines are up to date and not stuck while updating — so you catch broken or outdated pools before they hurt real work.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
