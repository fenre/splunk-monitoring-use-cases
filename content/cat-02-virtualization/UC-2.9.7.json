{
  "$schema": "../../schemas/uc.schema.json",
  "id": "2.9.7",
  "title": "OpenStack Nova Scheduler Retry and Placement Filter Exhaustion Audit",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance",
    "Operational"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=openstack` `sourcetype=\"openstack:nova\"` — scheduler: `instance_uuid`, `retry`, `filter_name`, `reason`, `host`",
  "app": "nova-scheduler DEBUG/INFO lines with scheduler context",
  "spl": "index=openstack sourcetype=\"openstack:nova\" earliest=-4h\n| search match(lower(_raw), \"(?i)scheduler|retrying|no valid host|filter\")\n| eval r=tonumber(retry)\n| where r>=3\n| stats count as deep_retries, values(filter_name) as filters by host\n| sort - deep_retries",
  "description": "High scheduler retries mean builds queue while operators think capacity exists. This exposes filter misconfiguration or stale placement inventory.",
  "value": "Speeds time-to-build for tenants and reduces mysterious ERROR states.",
  "implementation": "Tune log verbosity carefully. Alert on retry>3 patterns. Correlate with `openstack:placement` inventory sync.",
  "visualization": "Timechart retry rate; table filters; instance samples.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "OpenStack Nova Scheduler",
      "url": "https://docs.openstack.org/nova/latest/user/filter-scheduler.html"
    }
  ],
  "equipment": [
    "openstack"
  ],
  "status": "verified",
  "lastReviewed": "2025-06-01",
  "splunkVersions": [
    "9.x",
    "8.x"
  ],
  "reviewer": "catalog-team",
  "grandmaExplanation": "We monitor the building blocks of your private cloud—computers, networks, disks, and logins—so new systems come up reliably and people are not locked out when something upstream hiccups.",
  "knownFalsePositives": "OpenStack metrics may swing during image builds, large migrations, or control-plane rolling updates; verify services are healthy in parallel before declaring data-plane failure."
}
