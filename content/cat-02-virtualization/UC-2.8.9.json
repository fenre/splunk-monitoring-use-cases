{
  "$schema": "../../schemas/uc.schema.json",
  "id": "2.8.9",
  "title": "oVirt GlusterFS Brick and Volume Health Signals",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Availability",
    "Fault"
  ],
  "splunkPillar": "IT Operations",
  "dataSources": "`index=ovirt` `sourcetype=\"ovirt:storagedomain\"` with `backend=gluster` OR dedicated `sourcetype=\"ovirt:gluster\"` — `volume`, `brick`, `state`, `rebalance_status`",
  "app": "Gluster CLI or gstatus JSON via scripted input; join to oVirt domain names",
  "spl": "index=ovirt (sourcetype=\"ovirt:storagedomain\" OR sourcetype=\"ovirt:gluster\") earliest=-1h\n| eval st=lower(coalesce(state, brick_state, status))\n| where match(st, \"(?i)down|faulty|degraded|unknown|out of sync\")\n| stats latest(_time) as t, values(volume) as vols, values(brick) as bricks by st\n| sort - t",
  "description": "Gluster-backed domains fail differently than block storage. Brick faults and split-brain signatures need early detection to avoid silent corruption risk.",
  "value": "Protects distributed storage-backed VMs from prolonged degraded modes.",
  "implementation": "Ingest gluster health more frequently than generic domain polls. Train operators on heal procedures. Correlate with network partitions.",
  "visualization": "Map of bricks; timeline degraded state; table volumes.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "GlusterFS with oVirt",
      "url": "https://www.ovirt.org/documentation/administration_guide/#gluster"
    }
  ],
  "equipment": [
    "ovirt"
  ],
  "status": "verified",
  "lastReviewed": "2025-06-01",
  "splunkVersions": [
    "9.x",
    "8.x"
  ],
  "reviewer": "catalog-team",
  "grandmaExplanation": "We keep an eye on the control panel that runs your virtual datacenter—who changed what, whether storage and hosts are healthy, and when something is about to strand running machines.",
  "knownFalsePositives": "AOS storage metrics can look worse during background heal, curator, or disk removal work; match alerts to Nutanix task progress and maintenance windows."
}
