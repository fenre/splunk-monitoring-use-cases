{
  "$schema": "../../schemas/uc.schema.json",
  "id": "2.6.54",
  "title": "RDS Licensing Validation for Multi-Session Hosts",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'RDS Licensing Validation for Multi-Session Hosts'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-2.6.54).",
  "evidence": "Saved search uc_2_6_54_rds_licensing_validation_for_multi_sessi, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'RDS Licensing Validation for Multi-Session Hosts'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-2.6.54.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "`index=windows` `sourcetype=\"WinEventLog:Application\"` with `Source=\"*TerminalServices*\"` or `Microsoft-Windows-TerminalServices-LSM*`, Windows license service events, Remote Desktop license server events (42, 4105, 22, 23, 25, 28); `index=windows` on the license server and `sourcetype=\"WinEventLog:RemoteDesktopServices*\"` where available; `index=xd` `sourcetype=\"citrix:broker:events\"` for multi-session brokering context Collected via Splunk Add-on for Microsoft Windows (Splunkbase 742) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Add-on for Microsoft Windows on session hosts and on Remote Desktop License Servers; optional scripted WMI or `Get-CimInstance` poll for `Win32_TerminalServiceSetting` and grace period on hosts — Splunk Add-on for Microsoft Windows (Splunkbase 742)",
  "spl": "index=windows (sourcetype=\"WinEventLog:Application\" OR sourcetype=\"WinEventLog:RemoteDesktopServices*\" OR source=\"*TerminalServices*\")\n| where EventCode IN (22, 23, 25, 28, 38, 4105) OR like(lower(_raw),\"%license%\") OR like(lower(_raw),\"%grace%\") OR like(lower(_raw),\"%remote desktop%\") OR like(lower(_raw),\"%rd licen%\")\n| eval kind=if(like(lower(_raw),\"%grace%\"),\"grace\", if(like(lower(_raw),\"%expir%\"),\"expiry\",\"license_event\"))\n| eval server=coalesce(license_server, LicenseServer, host)\n| bin _time span=1d\n| stats count as daily_events, values(EventCode) as codes, values(kind) as kinds by server, _time\n| where daily_events>0\n| sort - daily_events\n| table _time, server, daily_events, codes, kinds",
  "description": "Session hosts that offer multiple concurrent RDP and Citrix sessions need valid Remote Desktop Services client access licenses, healthy communication with the license server list, and clear visibility into per-device versus per-user mode and any grace period. A host in grace can appear healthy until a deadline passes and new sessions are refused. A broken license server list string — wrong DNS, firewall, or certificate — is a common misconfiguration. Collect license warnings from Application and the Remote Desktop service channels on each multi-session VDA, and aggregate the same on license servers. Pair with your Citrix per-user and Microsoft RDS-CAL entitlements in procurement, not in Splunk, but use Splunk to prove the runtime state matches policy.",
  "value": "Operations and leadership rely on 'RDS Licensing Validation for Multi-Session Hosts' to catch regressions before they become customer-visible outages or audit findings. Early signal reduces mean time to repair, avoids duplicate manual log review, and preserves evidence for post-incident review without waiting for a user-reported ticket.",
  "implementation": "Enable verbose Remote Desktop license logging in Windows where supported. Add a small scripted input to dump `HKEY_LOCAL_MACHINE\\SYSTEM\\CurrentControlSet\\Control\\Terminal Server\\RCM\\Licensing` or PowerShell `Get-RDLicense` output daily on license servers. Alert on any `grace` or `0-day` grace start, any event that says license server is unreachable, and any 4105 with severity error. Deduplicate license servers. Document which Citrix and Microsoft agreements cover which host pools. Tune out duplicate Windows noise per build.",
  "visualization": "Table of last license error per host, timechart of daily_events by server, single value of hosts in grace, network diagram optional with manual overlay.",
  "cimModels": [
    "Endpoint"
  ],
  "references": [
    {
      "title": "Remote Desktop Services and licensing (Microsoft Learn)",
      "url": "https://learn.microsoft.com/en-us/troubleshoot/windows-server/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Citrix — supported operating systems and RDS context",
      "url": "https://docs.citrix.com/en-us/citrix-virtual-apps-desktops/technical-overview/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "citrix"
  ],
  "equipmentModels": [
    "citrix_cvad"
  ],
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Windows add-on; reachability from forwarders to license servers; UDP/TCP 135 and RPC ranges documented.\n• Ensure the following data sources are available: Application and RDS-specific event logs; optional dedicated license server in `index=windows` with its own `host` value.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nBuild a `lookup` of expected license server FQDNs per site. Ingest the license server farm first — it is the source of truth for issuance errors. Ingest all multi-session session hosts. Merge Citrix and Microsoft terms of use in a runbook; Splunk is evidence, not a license calculator.\n\nStep 2 — Create the search and alert\nHarden `EventCode` lists per your Windows build. A parallel search for unreachability:\n\n```spl\nindex=windows \"Remote Desktop License Servers\" OR \"The Remote Desktop license server is not available\"\n| table _time, host, EventCode, Message\n```\n\n**RDS Licensing Validation for Multi-Session Hosts** — Require two matching events in 15 minutes before a page, to reduce one-off flaps. Join `host` to a lookup that classifies the machine as `multi_session=true` to avoid false positives on single-session VDI where RDS CAL rules differ.\n\nStep 3 — Validate\nStop the license service in a lab (not production) briefly and assert Splunk shows the follow-on events. Reconcile Event IDs to Microsoft’s table for your build.\n\nStep 4 — Operationalize\nAttach dashboards to the Windows license operations team, not only Citrix, because CAL issues are shared. Revisit when migrating license servers to new hardware or DNS aliases.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for Microsoft Windows on session hosts and on Remote Desktop License Servers; optional scripted WMI or `Get-CimInstance` poll for `Win32_TerminalServiceSetting` and grace period on hosts — Splunk Add-on for Microsoft Windows (Splunkbase 742)). Enable the modular input or HEC token that writes sourcetype=WinEventLog:Application, WinEventLog:RemoteDesktopServices into windows. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, daily_events, kind, server`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We make sure the shared Windows “terminal server” style licensing stays healthy on machines that run many people at once, so you are not suddenly blocked by grace periods or a dead license server list.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
