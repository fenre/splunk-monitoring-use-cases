{
  "$schema": "../../schemas/uc.schema.json",
  "id": "2.6.47",
  "title": "Workspace App Client Version Distribution",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Workspace App Client Version Distribution' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_workspace_app_client_version_distribution` (UC-2.6.47), SOC dashboard panel \"Linux — Workspace App Client Version Distribution\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-2.6.47 positive (Workspace, App, Client, Version, Distribution): ingest synthetic events into `index=xd` sourcetype='citrix:broker:events' where `users`, `hosts`, `total_sessions` grouped by `cv`, `platform` satisfies detection threshold for 'Workspace App Client Version Distribution'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-2.6.47 negative (Version Distribution): ingest benign `xd` / `citrix:broker:events` events where `cv`, `platform` stays below detection threshold, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 0 confirms zero alert rows for 'Workspace App Client Version Distribution'."
  },
  "dataSources": "`index=xd` `sourcetype=\"citrix:broker:events\"` (session or connection events that carry `ClientVersion`, `ClientProductId`, and `ClientAddress` or platform tags); `index=xd` `sourcetype=\"citrix:monitor:odata\"` `Sessions` resource if the broker event lacks detail Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Template for Citrix XenDesktop 7 (`TA-XD7-Broker`) and optional Citrix Monitor OData poller for session details — Universal Forwarder (Splunkbase 617)",
  "spl": "index=xd sourcetype=\"citrix:broker:events\" (event_type=\"SessionConnection\" OR event_type=\"ConnectionLogon\" OR event_type=\"SessionInfo\")\n| eval cv=coalesce(client_version, ClientVersion, workspace_version, \"unknown\")\n| eval platform=coalesce(client_platform, os_type, client_os, \"unknown\")\n| where cv!=\"unknown\"\n| stats count as sessions, dc(user) as users, dc(host) as hosts by cv, platform\n| eventstats sum(sessions) as total_sessions\n| eval pct=round(100 * sessions / total_sessions, 2)\n| sort - sessions\n| table cv, platform, users, sessions, pct",
  "description": "Alerts on 'Workspace App Client Version Distribution' (UC-2.6.47) using `index=xd` sourcetype `citrix:broker:events` by aggregating `users`, `hosts`, `total_sessions` grouped by `cv`, `platform` over `the scheduled window` and alerting when configured threshold. Keywords: Workspace, App, Client, Version, Distribution. Focuses on Version Distribution behaviour so linux teams can triage UC-2.6.47 without ad-hoc log review.",
  "value": "UC-2.6.47 accelerates insight into Version Distribution ('Workspace App Client Version Distribution') by tracking `users`, `hosts`, `total_sessions` on `citrix:broker:events` feeds, reducing capacity incidents, patch regressions, and service outages before spreadsheet checks hide regressions.",
  "implementation": "Ensure client version fields are present on at least one reliable event (often session start from broker or a Monitor OData `Sessions` backfill). Build a `lookup` of approved `client_version` per platform. Schedule a weekly or daily report, not an alert, unless a version is explicitly banned — then alert when `pct` for that version is nonzero. For executive views, show stacked percentage bars by platform. Feed the data into your software-asset and endpoint-management teams for package targeting.",
  "visualization": "Pie or treemap of versions, stacked bar by platform, table of versions with percent of sessions, optional single value for count of unapproved clients via lookup match.",
  "cimModels": [
    "Endpoint"
  ],
  "references": [
    {
      "title": "Citrix Workspace app lifecycle matrix",
      "url": "https://docs.citrix.com/en-us/citrix-workspace-app-for-windows/whats-new.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Session data from Monitor (context)",
      "url": "https://docs.citrix.com/en-us/citrix-virtual-apps-desktops/technical-overview/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `xd`** — Package managers spike `cv` metrics for 'Workspace App Client Version Distribution'. Correlate with apt/yum logs in the same `citrix:broker:events` window.\n\n2. **Nightly backup I/O on `cv`** — Predictable `dc` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `citrix:broker:events`** — Nagios/Zabbix polls `cv` and mimics sustained load without crossing capacity threshold.\n\n4. **New host onboarding** — `cv` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `cv`, `valid_until`.",
  "equipment": [
    "citrix"
  ],
  "equipmentModels": [
    "citrix_cvad"
  ],
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Template for Citrix XenDesktop 7 (`TA-XD7-Broker`); optional Citrix Monitor OData for richer session details.\n• Ensure the following data sources are available: broker events with client metadata; or OData `Sessions` with client version and device details.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nVerify field names with `| fieldsummary` in a 24-hour window. If only opaque build numbers arrive, add a `lookup` to friendly names. Deduplicate multiple events per logon to avoid double counting with `| dedup` on session and day where appropriate for reporting, but not for per-connection analytics.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report; adjust `event_type` list):\n\n```spl\nindex=xd sourcetype=\"citrix:broker:events\" (event_type=\"SessionConnection\" OR event_type=\"ConnectionLogon\")\n| eval cv=coalesce(client_version, ClientVersion, \"unknown\")\n| eval platform=coalesce(client_platform, client_os, \"unknown\")\n| where cv!=\"unknown\"\n| stats count as sessions, dc(user) as users by cv, platform\n| eventstats sum(sessions) as total_sessions\n| eval pct=round(100 * sessions / total_sessions, 2)\n| sort - sessions\n| table cv, platform, users, sessions, pct\n```\n\n**Workspace App Client Version Distribution** — Add `| inputlookup approved_workspace_clients` and `| where isnull(approved)` to flag rows that breach policy, if you maintain such a list.\n\nStep 3 — Validate\nCheck totals against a Citrix Director session export for a test day. Differences often come from unauthenticated or prelogon events — exclude those with a filter on event type if needed.\n\nStep 4 — Operationalize\nPublish the report to a portal, attach it to change windows for client upgrades, and re-run after any VPN or gateway change that alters the client path.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Template for Citrix XenDesktop 7 (`TA-XD7-Broker`) and optional Citrix Monitor OData poller for session details — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=citrix:broker:events into xd. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`cv, platform`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We list which app versions and device types are actually connecting, so you see who is behind on upgrades before support tickets and old bugs come back in waves.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
