{
  "$schema": "../../schemas/uc.schema.json",
  "id": "2.3.1",
  "title": "Guest VM Resource Monitoring",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Guest VM Resource Monitoring' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_guest_vm_resource_monitoring` (UC-2.3.1), SOC dashboard panel \"Linux — Guest VM Resource Monitoring\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-2.3.1 positive (Guest, Resource, Monitoring): ingest synthetic events into `index=virtualization` sourcetype='virsh_stats' where `count` grouped by `vm_name`, `host` satisfies detection threshold for 'Guest VM Resource Monitoring'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-2.3.1 negative (Resource Monitoring): ingest benign `virtualization` / `virsh_stats` events where `vm_name`, `host` stays below detection threshold, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 1 confirms zero alert rows for 'Guest VM Resource Monitoring'."
  },
  "dataSources": "Custom sourcetype from `virsh domstats` or `virt-top` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom scripted input (`virsh domstats`) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=virtualization sourcetype=virsh_stats\n| stats latest(cpu_pct) as cpu, latest(mem_used_mb) as memory by vm_name, host\n| sort -cpu",
  "description": "Alerts on Resource Monitoring activity in UC-2.3.1 ('Guest VM Resource Monitoring') from `index=virtualization` sourcetype `virsh_stats`. Rolls up `count` by `vm_name`, `host` across `the scheduled window` and pages when configured threshold. Covers Guest, Resource, Monitoring. Linux operators treat UC-2.3.1 as the canonical signal.",
  "value": "Operators running UC-2.3.1 on `virsh_stats` feeds shorten triage for Resource Monitoring tied to 'Guest VM Resource Monitoring', lowering capacity incidents, patch regressions, and service outages.",
  "implementation": "Create scripted input: `virsh domstats --cpu-total --balloon --interface --block`. Run every 60 seconds. Parse per-VM CPU time, balloon current, block read/write, and net rx/tx.",
  "visualization": "Table, Line chart per VM, Heatmap.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `virtualization`** — Package managers spike `vm_name` metrics for 'Guest VM Resource Monitoring'. Correlate with apt/yum logs in the same `virsh_stats` window.\n\n2. **Nightly backup I/O on `vm_name`** — Predictable `count` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `virsh_stats`** — Nagios/Zabbix polls `vm_name` and mimics sustained load without crossing capacity threshold.\n\n4. **New host onboarding** — `vm_name` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `vm_name`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Custom scripted input (`virsh domstats`).\n• Ensure the following data sources are available: Custom sourcetype from `virsh domstats` or `virt-top`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nCreate scripted input: `virsh domstats --cpu-total --balloon --interface --block`. Run every 60 seconds. Parse per-VM CPU time, balloon current, block read/write, and net rx/tx.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=virtualization sourcetype=virsh_stats\n| stats latest(cpu_pct) as cpu, latest(mem_used_mb) as memory by vm_name, host\n| sort -cpu\n```\n\nUnderstanding this SPL\n\n**Guest VM Resource Monitoring** — Per-VM resource tracking for capacity planning and performance troubleshooting in KVM environments.\n\nDocumented **Data sources**: Custom sourcetype from `virsh domstats` or `virt-top`. **App/TA** (typical add-on context): Custom scripted input (`virsh domstats`). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: virtualization; **sourcetype**: virsh_stats. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=virtualization, sourcetype=virsh_stats. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by vm_name, host** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table, Line chart per VM, Heatmap.\n\nScripted input (generic example)\nThis use case relies on a scripted input. In the app's local/inputs.conf add a stanza such as:\n\n```ini\n[script://$SPLUNK_HOME/etc/apps/YourApp/bin/collect.sh]\ninterval = 300\nsourcetype = your_sourcetype\nindex = main\ndisabled = 0\n```\n\nThe script should print one event per line (e.g. key=value). Example minimal script (bash):\n\n```bash\n#!/usr/bin/env bash\n# Output metrics or events, one per line\necho \"metric=value timestamp=$(date +%s)\"\n```\n\nFor full details (paths, scheduling, permissions), see the Implementation guide: docs/implementation-guide.md",
  "grandmaExplanation": "We keep an eye on how hard each virtual machine is working on the host (CPU and memory) and raise the alarm before it drags down real work or real outages start.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
