{
  "$schema": "../../schemas/uc.schema.json",
  "id": "2.6.43",
  "title": "Citrix Site Database Connectivity from Controllers",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Citrix Site Database Connectivity from Controllers' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_citrix_site_database_connectivity_from_controlle` (UC-2.6.43), SOC dashboard panel \"Linux — Citrix Site Database Connectivity from Controllers\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-2.6.43 positive (Citrix, Site, Database, Connectivity, from): ingest synthetic events into `index=windows` sourcetype='WinEventLog:Application' where `values` grouped by `host`, `_time` satisfies `evt_count` > 0 for 'Citrix Site Database Connectivity from Controllers'. Validate filter `(Message=\"*connection*\" OR Message=\"*timeout*\" OR Message=\"*`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-2.6.43 negative (from Controllers): ingest benign `windows` / `WinEventLog:Application` events where `host`, `_time` stays below `evt_count` > 0, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 10 confirms zero alert rows for 'Citrix Site Database Connectivity from Controllers'."
  },
  "dataSources": "`index=windows` `sourcetype=\"WinEventLog:Application\"` from Delivery Controllers for Citrix site database and configuration services; optional `index=xd` `sourcetype=\"citrix:broker:events\"` for correlated broker health, `EventCode` / `EventID` and `Message` text for connection timeouts and failure reasons Collected via Splunk Add-on for Microsoft Windows (Splunkbase 742) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Add-on for Microsoft Windows; Template for Citrix XenDesktop 7 (`TA-XD7-Broker`) for broker event normalization — Splunk Add-on for Microsoft Windows (Splunkbase 742)",
  "spl": "index=windows sourcetype=\"WinEventLog:Application\" (source=\"*Citrix*\" OR source=\"*Broker*\" OR Message=\"*site database*\" OR Message=\"*Site database*\")\n| search (Message=\"*connection*\" OR Message=\"*timeout*\" OR Message=\"*failed*\" OR Message=\"*unavailable*\") (Message=\"*database*\" OR Message=\"*SQL*\" OR Message=\"*data store*\")\n| bin _time span=5m\n| stats count as evt_count, values(EventCode) as event_codes, values(Message) as sample_msgs by host, _time\n| where evt_count > 0\n| sort -_time\n| table _time, host, event_codes, evt_count, sample_msgs",
  "description": "Identifies UC-2.6.43 — Citrix, Site, Database, Connectivity, from — using `index=windows` sourcetype `WinEventLog:Application`, grouped by `host`, `_time`. Outputs `values` each `5m`; alert when `evt_count` > 0. Filter chain includes filter `(Message=\"*connection*\" OR Message=\"*timeout*\" OR Message=\"*`. Purpose-built for 'Citrix Site Database Connectivity from Controllers' in Linux workflows.",
  "value": "For 'Citrix Site Database Connectivity from Controllers', UC-2.6.43 converts `activity` on `WinEventLog:Application` feeds into actionable rows and reduces capacity incidents, patch regressions, and service outages.",
  "implementation": "Forward Windows Application logs from every Delivery Controller. Add field extractions or `rex` to normalize database connection error text, SQL connectivity codes, and timeout indicators. Ingest or schedule-query `index=xd` broker events to correlate. Alert when any controller reports repeated site database connection failures in a five-minute window, or when a single error pattern exceeds your baseline. Suppress during planned database maintenance using a time-bound lookup. Document escalation to the DBA and Citrix site recovery runbooks.",
  "visualization": "Single value (open critical events), timechart of database-related errors by controller, table of recent error text with host, linked drilldown to broker event timeline.",
  "cimModels": [
    "Databases"
  ],
  "references": [
    {
      "title": "Citrix Databases — CVAD",
      "url": "https://docs.citrix.com/en-us/citrix-virtual-apps-desktops/technical-overview/databases.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "uberAgent UXM (optional correlation on endpoints)",
      "url": "https://splunkbase.splunk.com/app/1448",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `windows`** — Package managers spike `host` metrics for 'Citrix Site Database Connectivity from Controllers'. Correlate with apt/yum logs in the same `WinEventLog:Application` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `values` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `WinEventLog:Application`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing `evt_count` > 0.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "citrix"
  ],
  "equipmentModels": [
    "citrix_cvad"
  ],
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Add-on for Microsoft Windows; Template for Citrix XenDesktop 7 (`TA-XD7-Broker`) for `citrix:broker:events`.\n• Ensure the following data sources are available: `index=windows` `sourcetype=\"WinEventLog:Application\"` from Delivery Controllers; optional `index=xd` `sourcetype=\"citrix:broker:events\"` for correlation.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nDeploy universal forwarders on all Delivery Controllers. Verify Citrix FMA, Broker, and configuration logging emit database connection errors to Application. Tune `props.conf` to retain multiline `Message` where needed. Enable optional `index=xd` broker feed for the same time window. Map `host` to a stable controller name for paging.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range, `rex`, and `Message` match list as your event text requires):\n\n```spl\nindex=windows sourcetype=\"WinEventLog:Application\" (source=\"*Citrix*\" OR source=\"*Broker*\" OR Message=\"*site database*\" OR Message=\"*Site database*\")\n| search (Message=\"*connection*\" OR Message=\"*timeout*\" OR Message=\"*failed*\" OR Message=\"*unavailable*\") (Message=\"*database*\" OR Message=\"*SQL*\" OR Message=\"*data store*\")\n| bin _time span=5m\n| stats count as evt_count, values(EventCode) as event_codes, values(Message) as sample_msgs by host, _time\n| where evt_count > 0\n| sort -_time\n| table _time, host, event_codes, evt_count, sample_msgs\n```\n\n**Citrix Site Database Connectivity from Controllers** — The Citrix site database is the single source of truth for registrations, entitlements, and broker decisions. The SPL scopes controller Application logs, buckets time, and surfaces grouped failures per host. Join or append with `index=xd` broker searches if you need brokering-side confirmation.\n\nStep 3 — Validate\nReproduce a controlled test (e.g. firewall deny to SQL) in a lab and confirm events appear. Compare counts with SQL Server error logs and load balancer health. Ensure alert triggers only on sustained or repeated errors if noise is high.\n\nStep 4 — Operationalize\nRoute critical alerts to the Citrix and database on-call. Add a dashboard with five-minute error rate, top messages, and controller list. Update the runbook with RTO and verification steps (connectivity, disk, logins, failovers).",
  "grandmaExplanation": "We keep an eye on site Database Connectivity from Controllers and raise the alarm before it drags down real work or real outages start.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
