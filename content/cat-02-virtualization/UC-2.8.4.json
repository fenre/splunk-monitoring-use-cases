{
  "$schema": "../../schemas/uc.schema.json",
  "id": "2.8.4",
  "title": "oVirt VM Migration Failures and Timeout Events",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Fault"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=ovirt` `sourcetype=\"ovirt:vm\"` — migration: `vm_name`, `vm_id`, `src_host`, `dst_host`, `status`, `reason`",
  "app": "Engine event logs or REST audit mapped to `ovirt:vm`",
  "spl": "index=ovirt sourcetype=\"ovirt:vm\" earliest=-24h\n| eval st=lower(coalesce(status, migration_status))\n| where match(st, \"(?i)fail|error|timeout|cancel\")\n| eval reason=coalesce(reason, fail_reason, message)\n| stats count as fails, values(reason) as reasons, values(dst_host) as targets by vm_name, src_host\n| sort - fails",
  "description": "Failed migrations leave VMs on stressed hosts or mid-maintenance. Timeouts often indicate saturated migration networks or storage pressure.",
  "value": "Keeps maintenance and evacuation projects safe and reduces user-visible pauses.",
  "implementation": "Extract migration failure reasons. Alert on any failure in production clusters. Trend top reasons weekly for engineering backlog.",
  "visualization": "Pie of failure reasons; table vm/host; timechart failure rate.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "oVirt Migrating Virtual Machines",
      "url": "https://www.ovirt.org/documentation/administration_guide/#sect-Migrating_virtual_machines"
    }
  ],
  "equipment": [
    "ovirt"
  ],
  "status": "verified",
  "lastReviewed": "2025-06-01",
  "splunkVersions": [
    "9.x",
    "8.x"
  ],
  "reviewer": "catalog-team",
  "grandmaExplanation": "We keep an eye on the control panel that runs your virtual datacenter—who changed what, whether storage and hosts are healthy, and when something is about to strand running machines."
}
