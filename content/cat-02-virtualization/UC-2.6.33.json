{
  "$schema": "../../schemas/uc.schema.json",
  "id": "2.6.33",
  "title": "Citrix Autoscale Capacity Events",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Citrix Autoscale Capacity Events' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_citrix_autoscale_capacity_events` (UC-2.6.33), SOC dashboard panel \"Linux — Citrix Autoscale Capacity Events\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-2.6.33 positive (Citrix, Autoscale, Capacity, Events): ingest synthetic events into `index=xd` sourcetype='citrix:broker:events' where `machine_moves` grouped by `_time`, `dg`, `direction` satisfies `fail_hits` > 0, or `count` > 100 for 'Citrix Autoscale Capacity Events'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-2.6.33 negative (Capacity Events): ingest benign `xd` / `citrix:broker:events` events where `_time`, `dg`, `direction` stays below `fail_hits` > 0, or `count` > 100, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 13 confirms zero alert rows for 'Citrix Autoscale Capacity Events'."
  },
  "dataSources": "`index=xd` `sourcetype=\"citrix:broker:events\"` for autoscale, power, and schedule-related actions, `sourcetype=\"citrix:monitor:odata\"` for `Autoscale*`, `DeliveryGroup`, or `Session` machine counts; optional `sourcetype=\"citrix:vda:events\"` for power-on success after scale-out Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Template for Citrix XenDesktop 7 (TA-XD7-Broker), Citrix Monitor Service OData API — Universal Forwarder (Splunkbase 617)",
  "spl": "index=xd sourcetype=\"citrix:broker:events\" (match(_raw, \"(?i)autoscale|power.?on|turn.?on|turn.?off|scale.?in|scale.?out|power.?action|capaci\") OR event_type=Power* OR event_type=Autoscale*)\n| eval direction=if(match(_raw, \"(?i)scale.?out|turn.?on|add.*machine|increase\"), \"scale_out\", if(match(_raw, \"(?i)scale.?in|turn.?off|remove.*reduce\"), \"scale_in\", \"other\"))\n| eval dg=coalesce(delivery_group, DeliveryGroup, CatalogName)\n| eval success=if(match(coalesce(result, action_result, state), \"(?i)success|complete\"), 1, if(match(coalesce(result, action_result, state), \"(?i)fail|error|throttl|denied|pending\"), 0, null()))\n| bin _time span=15m\n| stats count, sum(eval(if(success=1,1,0))) as success_hits, sum(eval(if(success=0,1,0))) as fail_hits, dc(machine_name) as machine_moves by _time, dg, direction\n| where fail_hits>0 OR count>100\n| table _time, dg, direction, count, success_hits, fail_hits, machine_moves",
  "description": "Monitors UC-2.6.33 — Citrix, Autoscale, Capacity, Events — using `index=xd` sourcetype `citrix:broker:events`, grouped by `_time`, `dg`, `direction`. Outputs `machine_moves` each `15m`; alert when `fail_hits` > 0, or `count` > 100. Primary focus: Capacity Events. Purpose-built for 'Citrix Autoscale Capacity Events' in Linux workflows.",
  "value": "UC-2.6.33 gives Linux teams earlier visibility into Capacity Events through `citrix:broker:events` feeds, lowering capacity incidents, patch regressions, and service outages.",
  "implementation": "Confirm event strings for your CVAD/Cloud version. Build baselines of scale_out vs load per delivery group. Join OData `InUse*`, `Registered*`, and `Unassigned*`-style fields when available. Alert on high fail_hits, zero scale_out during a ramp when usage rises, and sustained unassigned high-water marks outside policy.",
  "visualization": "Column chart (scale events by direction), Timechart (in-use vs registered machines), Table (failed power actions with delivery group).",
  "cimModels": [
    "Change",
    "Performance"
  ],
  "references": [
    {
      "title": "Autoscale in Citrix DaaS",
      "url": "https://docs.citrix.com/en-us/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Change",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Change",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Performance",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Performance",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `xd`** — Package managers spike `_time` metrics for 'Citrix Autoscale Capacity Events'. Correlate with apt/yum logs in the same `citrix:broker:events` window.\n\n2. **Nightly backup I/O on `_time`** — Predictable `sum` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `citrix:broker:events`** — Nagios/Zabbix polls `_time` and mimics sustained load without crossing `fail_hits` > 0.\n\n4. **New host onboarding** — `_time` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `_time`, `valid_until`.",
  "equipment": [
    "citrix"
  ],
  "equipmentModels": [
    "citrix_cvad"
  ],
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Template for Citrix XenDesktop 7 (TA-XD7-Broker), Citrix Monitor Service OData API for capacity counters.\n• Ensure the following data sources are available: `sourcetype=\"citrix:broker:events\"` and optional `sourcetype=\"citrix:monitor:odata\"`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest all broker subsystems that log power and autoscale. Align delivery group and machine name fields. Load business-hours schedules from a CSV to compare actual scale actions to the intended plan.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust match strings for your site):\n\n```spl\nindex=xd sourcetype=\"citrix:broker:events\" (match(_raw, \"(?i)autoscale|power.?on|turn.?on|turn.?off|scale.?in|scale.?out|power.?action|capaci\") OR event_type=Power* OR event_type=Autoscale*)\n| eval direction=if(match(_raw, \"(?i)scale.?out|turn.?on|add.*machine|increase\"), \"scale_out\", if(match(_raw, \"(?i)scale.?in|turn.?off|remove.*reduce\"), \"scale_in\", \"other\"))\n| eval dg=coalesce(delivery_group, DeliveryGroup, CatalogName)\n| eval success=if(match(coalesce(result, action_result, state), \"(?i)success|complete\"), 1, if(match(coalesce(result, action_result, state), \"(?i)fail|error|throttl|denied|pending\"), 0, null()))\n| bin _time span=15m\n| stats count, sum(eval(if(success=1,1,0))) as success_hits, sum(eval(if(success=0,1,0))) as fail_hits, dc(machine_name) as machine_moves by _time, dg, direction\n| where fail_hits>0 OR count>100\n| table _time, dg, direction, count, success_hits, fail_hits, machine_moves\n```\n\nStep 3 — Validate\nReconcile counts with the Citrix admin console. Tune high `count` filter for large environments. Exclude maintenance windows with lookups.\n\nStep 4 — Operationalize\nFeed capacity reports to finops. Pair alerts with queue-depth UCs to separate broker from hypervisor bottlenecks.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Template for Citrix XenDesktop 7 (TA-XD7-Broker), Citrix Monitor Service OData API — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=citrix:broker:events into xd. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, dg, direction, fail_hits`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We follow when the system turns virtual computers on and off to match the day, and when that schedule slips or the machines fail to start — so you catch wasted power or a thin pool before it hurts real work.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
