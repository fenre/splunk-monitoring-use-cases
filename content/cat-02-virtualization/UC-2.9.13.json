{
  "$schema": "../../schemas/uc.schema.json",
  "id": "2.9.13",
  "title": "OpenStack Nova Live Migration Progress and Failure Reasons",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Availability"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=openstack` `sourcetype=\"openstack:nova\"` — `instance_uuid`, `src_host`, `dest_host`, `migration_type` (live), `progress_pct`, `status`",
  "app": "Nova compute logs with migration UUID context",
  "spl": "index=openstack sourcetype=\"openstack:nova\" earliest=-24h\n| eval mt=lower(coalesce(migration_type, type))\n| where match(mt, \"(?i)live\")\n| eval st=lower(status)\n| where st=\"failed\" OR tonumber(progress_pct)<100 AND now()-_time>3600\n| stats values(reason) as reasons by instance_uuid, src_host, dest_host",
  "description": "Live migrations should converge quickly; stalled jobs tie up CPU and network. Failures during maintenance windows risk unprotected hosts.",
  "value": "Supports non-disruptive hypervisor operations and capacity balancing.",
  "implementation": "Extract migration UUID. Alert on failure reason codes. Compare with Neutron MTU issues if TCP migration.",
  "visualization": "Timechart in-flight migrations; table failures; bandwidth overlay if metrics exist.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "OpenStack Nova Live Migration",
      "url": "https://docs.openstack.org/nova/latest/admin/live-migration-usage.html"
    }
  ],
  "equipment": [
    "openstack"
  ],
  "status": "verified",
  "lastReviewed": "2025-06-01",
  "splunkVersions": [
    "9.x",
    "8.x"
  ],
  "reviewer": "catalog-team",
  "grandmaExplanation": "We monitor the building blocks of your private cloud—computers, networks, disks, and logins—so new systems come up reliably and people are not locked out when something upstream hiccups.",
  "knownFalsePositives": "OpenStack metrics may swing during image builds, large migrations, or control-plane rolling updates; verify services are healthy in parallel before declaring data-plane failure."
}
