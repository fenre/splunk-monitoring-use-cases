{
  "$schema": "../../schemas/uc.schema.json",
  "id": "2.6.68",
  "title": "Citrix Endpoint Management Remote Wipe/Lock Action Tracking",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Citrix Endpoint Management Remote Wipe/Lock Action Tracking' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_citrix_endpoint_management_remote_wipe_lock_acti` (UC-2.6.68), SOC dashboard panel \"Linux — Citrix Endpoint Management Remote Wipe/Lock Action Tracking\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-2.6.68 positive (Citrix, Endpoint, Management, Remote, Wipe): ingest synthetic events into `index=xd` sourcetype='citrix:endpoint:mdm:command' where `count` grouped by `action`, `outcome` satisfies detection threshold for 'Citrix Endpoint Management Remote Wipe/Lock Action Tracking'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-2.6.68 negative (Action Tracking): ingest benign `xd` / `citrix:endpoint:mdm:command` events where `action`, `outcome` stays below detection threshold, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 6 confirms zero alert rows for 'Citrix Endpoint Management Remote Wipe/Lock Action Tracking'."
  },
  "dataSources": "`index=xd` `sourcetype=\"citrix:endpoint:mdm:command\"` with `action` (`wipe`, `lock`, `reset`, `unenroll`), `outcome` (`acknowledged`, `completed`, `failed`, `timeout`), `latency_ms`, `device_id`, `requester`, `incident_id`; CEM admin audit of remote actions if emitted separately to `sourcetype=\"citrix:endpoint:audit\"` Note: field names in SPL are suggested conventions for custom ingestion; actual field names depend on your parsing configuration in props.conf/transforms.conf.",
  "app": "No official Splunk TA for Citrix Endpoint Management. Ingest via syslog from XenMobile Server, or use the Citrix Analytics Add-on for Splunk (Splunkbase 6280) which imports CEM risk indicators from Citrix Analytics for Security. For on-premises XenMobile, forward syslog and JMX metrics via Universal Forwarder. Suggested custom sourcetypes follow the `citrix:endpoint:*` convention.",
  "spl": "index=xd sourcetype=\"citrix:endpoint:mdm:command\" action IN (\"wipe\",\"lock\",\"reset\",\"unenroll\")\n| eval ok=if(match(lower(coalesce(outcome, status, \"\")), \"(completed|acknowledged|success)\"), 1, 0)\n| eval late=if(tonumber(coalesce(latency_ms, 0))>120000, 1, 0)\n| where ok=0 OR late=1\n| eval action=upper(action)\n| timechart span=1h count by action, outcome\n| fillnull value=0",
  "description": "Surfaces Action Tracking anomalies for UC-2.6.68 with window `1h` on `index=xd` sourcetype `citrix:endpoint:mdm:command`. Combines `count` by `action`, `outcome` and compares against configured threshold. Mapped to keywords Citrix, Endpoint, Management, Remote, Wipe for dashboard routing.",
  "value": "Alerting on Citrix, Endpoint, Management, Remote, Wipe through UC-2.6.68 reduces capacity incidents, patch regressions, and service outages by surfacing `activity` early.",
  "implementation": "Ensure both the command result stream and a tamper-resistant admin audit of `requester` and business justification (ticket id) are present. Set RTO expectations (for example lock within two minutes on cellular). Page security operations on any `wipe` or `unenroll` that fails or exceeds latency SLO, with device last-seen time for triage. Weekly review of counts versus HR-driven terminations. Retain 13 months in line with HR and privacy counsel. Suppress test-lab device IDs. Never send full device payloads to a shared room without masking sensitive fields.",
  "visualization": "Gauge: success rate by action; timeline of long-running commands; table of failed devices with `requester` and `incident_id`.",
  "cimModels": [
    "Endpoint"
  ],
  "references": [
    {
      "title": "Device security actions (enterprise mobility — Apple/Android context)",
      "url": "https://docs.citrix.com/en-us/citrix-endpoint-management/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 6280",
      "url": "https://splunkbase.splunk.com/app/6280",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Endpoint",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Endpoint",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `xd`** — Package managers spike `action` metrics for 'Citrix Endpoint Management Remote Wipe/Lock Action Tracking'. Correlate with apt/yum logs in the same `citrix:endpoint:mdm:command` window.\n\n2. **Nightly backup I/O on `action`** — Predictable `count` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `citrix:endpoint:mdm:command`** — Nagios/Zabbix polls `action` and mimics sustained load without crossing capacity threshold.\n\n4. **New host onboarding** — `action` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `action`, `valid_until`.",
  "mitreAttack": [
    "T1485"
  ],
  "equipment": [
    "citrix",
    "syslog"
  ],
  "equipmentModels": [
    "citrix_generic",
    "syslog_generic"
  ],
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Legal and HR sign-off on logging requesters; CEM at a build that includes detailed command results; time sync.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nDeduplicate command retries. Map vendor-specific status strings to the four outcomes above. Hash device identifiers in lower environments.\n\nStep 2 — Create the search and alert\nRun a scheduled search every 5 minutes for failures. Route full-text evidence only to a restricted index role.\n\nStep 3 — Validate\nIn test, send lock and a selective wipe, confirm latency fields. For wipe, use a virtual lab or manufacturer reset workflow only.\n\nStep 4 — Operationalize\nQuarterly runbook drill with a paper incident; check that the audit trail is complete for auditors.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (No official Splunk TA for Citrix Endpoint Management. Ingest via syslog from XenMobile Server, or use the Citrix Analytics Add-on for Splunk (Splunkbase 6280) which imports CEM risk indicators from Citrix Analytics for Security. For on-premises XenMobile, forward syslog and JMX metrics via Universal Forwarder. Suggested custom sourcetypes follow the `citrix:endpoint:*` convention.). Enable the modular input or HEC token that writes sourcetype=citrix:endpoint:mdm:command into xd. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`action, late, ok, outcome`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We keep an eye on endpoint Management Remote Wipe/Lock Action and raise the alarm before it drags down real work or real outages start.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
