{
  "$schema": "../../schemas/uc.schema.json",
  "id": "2.7.1",
  "title": "Proxmox VE HA Manager Fencing and Live Migration Events",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Fault"
  ],
  "splunkPillar": "IT Operations",
  "exclusions": "Covers the detection logic documented for 'Proxmox VE HA Manager Fencing and Live Migration Events' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_proxmox_ve_ha_manager_fencing_and_live_migration` (UC-2.7.1), SOC dashboard panel \"Linux — Proxmox VE HA Manager Fencing and Live Migration Events\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=pve` sourcetype='proxmox:ha' where `node` satisfies detection threshold for UC-2.7.1 ('Proxmox VE HA Manager Fencing and Live Migration Events'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `pve` / `proxmox:ha` events where `node` stays below detection threshold, or exclude the test entity via `linux_capacity_exceptions.csv` with a valid window. Confirm zero alert rows for UC-2.7.1."
  },
  "dataSources": "`index=pve` `sourcetype=\"proxmox:ha\"` — `pve-ha-crm` / `pve-ha-lrm`: `node`, `vmid`, `action` (fence|migrate|relocate), `quorate`, `service` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder on Proxmox nodes (journald units `pve-ha-crm`, `pve-ha-lrm`) with `props.conf` for `proxmox:ha` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=pve sourcetype=\"proxmox:ha\" earliest=-24h\n| eval act=lower(coalesce(action, ha_action, op))\n| where match(act, \"(?i)fence|stonith|migrate|relocate\") OR match(lower(_raw), \"(?i)lost quorum|not quorate|fence\")\n| rex field=_raw \"(?i)VM\\s+(?<vmid_num>\\d+)\"\n| eval vmid=coalesce(vmid, vmid_num)\n| stats count as ev, values(act) as actions, latest(quorate) as last_quorate by node, vmid\n| sort - ev",
  "description": "HA fencing and forced migrations are the last resort for guest availability on Proxmox clusters. When these events cluster in time or correlate with lost quorum, operators risk split-brain, repeated restarts, or guests left unmanaged during node failure.",
  "value": "HA fencing and forced migrations are the last resort for guest availability on Proxmox clusters. Timely UC-2.7.1 alerting on 'Proxmox VE HA Manager Fencing and Live Migration Events' reduces capacity incidents, patch regressions, and service outages before manual triage or audit channels escalate.",
  "implementation": "Forward HA manager logs from every cluster member with a consistent sourcetype. Use FIELDALIAS to normalize `action` and `quorate`. Create a correlation search for fence bursts per node and for `quorate=false` transitions. Tie alerts to your hardware management channel when STONITH is involved.",
  "visualization": "Single value (active HA incidents), timechart of fence/migrate rate, table by node and vmid.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Proxmox VE HA Manager",
      "url": "https://pve.proxmox.com/pve-docs/chapter-ha-manager.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `pve`** — Package managers spike `node` metrics for 'Proxmox VE HA Manager Fencing and Live Migration Events'. Correlate with apt/yum logs in the same `proxmox:ha` window.\n\n2. **Nightly backup I/O on `node`** — Predictable `values` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `proxmox:ha`** — Nagios/Zabbix polls `node` and mimics sustained load without crossing capacity threshold.\n\n4. **New host onboarding** — `node` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `node`, `valid_until`.",
  "equipment": [
    "proxmox"
  ],
  "equipmentModels": [
    "proxmox_generic"
  ],
  "status": "verified",
  "lastReviewed": "2025-06-01",
  "splunkVersions": [
    "9.x",
    "8.x"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Universal Forwarder on Proxmox nodes (journald units `pve-ha-crm`, `pve-ha-lrm`) with `props.conf` for `proxmox:ha` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=proxmox:ha into pve. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`node, vmid`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We keep an eye on vE HA Manager Fencing and Live Migration Events and raise the alarm before it drags down real work or real outages start.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
