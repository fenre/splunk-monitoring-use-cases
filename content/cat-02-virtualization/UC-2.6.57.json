{
  "$schema": "../../schemas/uc.schema.json",
  "id": "2.6.57",
  "title": "Citrix Cloud Connector Deep Health (HealthData API)",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Citrix Cloud Connector Deep Health (HealthData API)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_citrix_cloud_connector_deep_health_healthdata_ap` (UC-2.6.57), SOC dashboard panel \"Linux — Citrix Cloud Connector Deep Health (HealthData API)\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-2.6.57 positive (Citrix, Cloud, Connector, Deep, Health): ingest synthetic events into `index=xd` sourcetype='citrix:cloudconnector:healthdata' where `failed_deps` grouped by `host`, `connector_id`, `resource_location` satisfies `cpu` > 90 for 'Citrix Cloud Connector Deep Health (HealthData API)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-2.6.57 negative ((HealthData API)): ingest benign `xd` / `citrix:cloudconnector:healthdata` events where `host`, `connector_id`, `resource_location` stays below `cpu` > 90, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 12 confirms zero alert rows for 'Citrix Cloud Connector Deep Health (HealthData API)'."
  },
  "dataSources": "`index=xd` `sourcetype=\"citrix:cloudconnector:healthdata\"` with `connector_id`, `cpu_percent`, `alert_state`, `cloud_registration`, `time_sync_status`, `dependency_check`, `failed_outbound` fields parsed from the Citrix HealthData API or Cloud Connector local health snapshots forwarded on a short interval; complementary `sourcetype=\"citrix:cloudconnector\"` for baseline connectivity from UC-2.6.16 Note: field names in SPL are suggested conventions for custom ingestion; actual field names depend on your parsing configuration in props.conf/transforms.conf.",
  "app": "Citrix Analytics Add-on for Splunk (Splunkbase 6280) for aggregated analytics data. For raw Cloud service data, use the Citrix Cloud System Log Add-on (open-source, see citrix/cc-system-log-addon-for-splunk repository) or poll the Monitor Service OData API (https://api.cloud.com/monitorodata) with a custom scripted input.",
  "spl": "index=xd sourcetype=\"citrix:cloudconnector:healthdata\"\n| eval reg_ok=if(match(lower(coalesce(cloud_registration, registration_status, \"\")), \"(registered|ok|success)\"), 1, 0)\n| eval sync_ok=if(match(lower(coalesce(time_sync_status, ntp_status, \"\")), \"(synced|ok|in\\ssync)\"), 1, 0)\n| eval dep_ok=if(tonumber(coalesce(failed_outbound, failed_dependencies, 0))=0, 1, 0)\n| eval cpu=tonumber(coalesce(cpu_percent, cpu, 0))\n| where reg_ok=0 OR sync_ok=0 OR dep_ok=0 OR cpu>90 OR like(lower(coalesce(alert_state, health_alert, \"\")), \"%fail%\") OR like(lower(coalesce(alert_state, health_alert, \"\")), \"%error%\")\n| stats latest(cpu) as cpu_pct, latest(alert_state) as alert_state, latest(cloud_registration) as registration, latest(time_sync_status) as time_sync, max(failed_outbound) as failed_deps by host, connector_id, resource_location\n| sort - cpu_pct",
  "description": "Monitors UC-2.6.57 — Citrix, Cloud, Connector, Deep, Health — using `index=xd` sourcetype `citrix:cloudconnector:healthdata`, grouped by `host`, `connector_id`, `resource_location`. Outputs `failed_deps` each `the scheduled window`; alert when `cpu` > 90. Primary focus: (HealthData API). Purpose-built for 'Citrix Cloud Connector Deep Health (HealthData API)' in Linux workflows.",
  "value": "Tracking `failed_deps` via UC-2.6.57 on `citrix:cloudconnector:healthdata` feeds improves response to 'Citrix Cloud Connector Deep Health (HealthData API)' and reduces capacity incidents, patch regressions, and service outages.",
  "implementation": "Deploy a least-privilege scheduled collector on each Cloud Connector (or a shared runner that iterates member hosts) that calls the HealthData API and emits JSON events every one to five minutes. Normalize numeric CPU and map alert flags to a small enum. Create correlation searches that ignore brief CPU spikes under two minutes. Require dual-connector hot-spares: alert when the worst two hosts in a site both show dependency failures. Retain 30 days of history for post-incident review. Co-watch with 2.6.16 so disconnections and deep health anomalies appear on one dashboard.",
  "visualization": "Connector matrix (CPU, registration, NTP, dependency failures); sparklines of failed outbound tests; overlay with VDA registration errors in the same resource location.",
  "cimModels": [
    "Application_State"
  ],
  "references": [
    {
      "title": "Citrix Cloud Connector — system and connectivity requirements",
      "url": "https://docs.citrix.com/en-us/citrix-cloud/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 6280",
      "url": "https://splunkbase.splunk.com/app/6280",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `xd`** — Package managers spike `host` metrics for 'Citrix Cloud Connector Deep Health (HealthData API)'. Correlate with apt/yum logs in the same `citrix:cloudconnector:healthdata` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `max` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `citrix:cloudconnector:healthdata`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing `cpu` > 90.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "citrix"
  ],
  "equipmentModels": [
    "citrix_generic"
  ],
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Cloud Connector version supported for HealthData-style health exports; service account with rights to read local health endpoints if applicable.\n• `index=xd` receiving `citrix:cloudconnector:healthdata` with stable host, connector_id, and resource_location keys.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nImplement the scripted input or lightweight agent that calls the health endpoint and posts one line per snapshot. Verify clocks on collectors match domain time for meaningful NTP fields.\n\nStep 2 — Create the search and alert\nTriage with the primary SPL, then add suppressions for maintenance windows. Pair alerts with the simpler connectivity search from 2.6.16 to avoid duplicate pages for the same outage.\n\nStep 3 — Validate\nInduce a controlled outbound block in a lab and confirm failed dependency fields populate. Skew a VM clock in test and assert time_sync flips not OK.\n\nStep 4 — Operationalize\nDocument escalation from connector host OS health to this deep health, and add capacity playbooks when CPU stays above threshold across polling windows.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Citrix Analytics Add-on for Splunk (Splunkbase 6280) for aggregated analytics data. For raw Cloud service data, use the Citrix Cloud System Log Add-on (open-source, see citrix/cc-system-log-addon-for-splunk repository) or poll the Monitor Service OData API (https://api.cloud.com/monitorodata) with a custom scripted input.). Enable the modular input or HEC token that writes sourcetype=citrix:cloudconnector:healthdata into xd. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`connector_id, cpu, dep_ok, host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We go beyond a simple on-or-off check for the link to the cloud and look at time sync, heavy processor load, and failed dependency checks, so you fix the hidden strain before people lose access or machines stop registering.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
