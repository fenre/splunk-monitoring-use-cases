{
  "$schema": "../../schemas/uc.schema.json",
  "id": "2.6.50",
  "title": "VDA BSOD and Machine Stability Tracking",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'VDA BSOD and Machine Stability Tracking' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_vda_bsod_and_machine_stability_tracking` (UC-2.6.50), SOC dashboard panel \"Linux — VDA BSOD and Machine Stability Tracking\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-2.6.50 positive (VDA, BSOD, and, Machine, Stability): ingest synthetic events into `index=windows` sourcetype='WinEventLog:System' EventCode=1001 where `values` grouped by `host`, `_time` satisfies `bugcheck` > 0, or `instabilities` > 0 for 'VDA BSOD and Machine Stability Tracking'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-2.6.50 negative (Stability Tracking): ingest benign `windows` / `WinEventLog:System` events where `host`, `_time` stays below `bugcheck` > 0, or `instabilities` > 0, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 10 confirms zero alert rows for 'VDA BSOD and Machine Stability Tracking'."
  },
  "dataSources": "`index=windows` (session hosts) `sourcetype=\"WinEventLog:System\"` bugcheck events, unexpected shutdowns, kernel power events; `index=xd` `sourcetype=\"citrix:vda:events\"` for VDA and agent service restarts tied to host instability; `index=uberagent` `sourcetype=\"uberAgent:Machine:Boot\"` and related stability or boot sourcetypes for unexpected reboots and stop codes normalized by uberAgent",
  "app": "Splunk Add-on for Microsoft Windows; uberAgent UXM (Splunkbase 1448); optional Template for Citrix XenDesktop 7 for `citrix:vda:events`",
  "spl": "index=windows sourcetype=\"WinEventLog:System\" (EventCode=1001 OR EventCode=41 OR EventCode=6008)\n| rex field=Message max_match=0 \"(?<bugcheck>0x[0-9A-Fa-f]+)\"\n| append [ search index=uberagent sourcetype=\"uberAgent:Machine:Boot\" unexpected_reboot=1 | eval EventCode=9999 | eval host=coalesce(host, dest_host) ]\n| bin _time span=1d\n| stats count as instabilities, values(EventCode) as event_codes, values(bugcheck) as stop_codes by host, _time\n| where instabilities>0\n| sort - instabilities\n| table _time, host, instabilities, event_codes, stop_codes",
  "description": "Surfaces UC-2.6.50 — VDA, BSOD, and, Machine, Stability — using `index=windows` sourcetype `WinEventLog:System`, grouped by `host`, `_time`. Outputs `values` each `1d`; alert when `bugcheck` > 0, or `instabilities` > 0. Primary focus: Stability Tracking. Purpose-built for 'VDA BSOD and Machine Stability Tracking' in Linux workflows.",
  "value": "For 'VDA BSOD and Machine Stability Tracking', UC-2.6.50 converts `activity` on `WinEventLog:System` feeds into actionable rows and reduces capacity incidents, patch regressions, and service outages.",
  "implementation": "Ingest the full System channel from all session hosts. For bugcheck 1001, parse `Message` to extract the stop code. Join `host` to a CMDB or lookup that supplies `catalog_name` and `delivery_group`. In uberAgent, confirm unexpected reboots flow with the same `host` key. Alert when any host has more than one bugcheck in seven days, or when a new stop code appears in more than 10% of a catalog in a week. Exclude planned reboot windows via a change lookup. For GPU images, add NVIDIA or AMD field extractions in a child search.",
  "visualization": "Choropleth of stability rate by data center, bar chart of top stop codes, timeline of restarts, table of worst hosts with catalog and patch level.",
  "cimModels": [
    "Endpoint"
  ],
  "references": [
    {
      "title": "Windows bug check reference (Microsoft Learn)",
      "url": "https://learn.microsoft.com/en-us/windows-hardware/drivers/debugger/bug-check-code-reference2",
      "retrieved": "2026-04-25"
    },
    {
      "title": "uberAgent unexpected reboots",
      "url": "https://liquidware.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `windows`** — Package managers spike `host` metrics for 'VDA BSOD and Machine Stability Tracking'. Correlate with apt/yum logs in the same `WinEventLog:System` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `values` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `WinEventLog:System`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing `bugcheck` > 0.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "citrix"
  ],
  "equipmentModels": [
    "citrix_cvad",
    "citrix_uberagent"
  ],
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Add-on for Microsoft Windows on all session hosts; uberAgent; optional VDA log feed.\n• Ensure the following data sources are available: `WinEventLog:System` with 1001, 41, 6008, and 1074 where applicable; `index=uberagent` boot and stability; optional `index=xd` `citrix:vda:events`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nStandardize time zones. Map `host` to asset inventory. Truncate or mask kernel dumps paths in `Message` if sensitive. If `append` is heavy, materialize a summary index on a 15-minute schedule instead of real-time for wide fleets.\n\nStep 2 — Create the search and alert\nSimplify the primary SPL in production — the example is illustrative. A typical pattern is:\n\n```spl\nindex=windows sourcetype=\"WinEventLog:System\" EventCode=1001\n| eval stop_code=if(match(_raw, \"(0x[A-Fa-f0-9]{8})\"), mvindex( match(_raw, \"(0x[A-Fa-f0-9]{8})\" ),1), null())\n| stats count by host, stop_code\n| where count>0\n```\n\n**VDA BSOD and Machine Stability Tracking** — Union with `index=uberagent` unexpected reboots on `host` in a `join` or `append` and deduplicate the same second.\n\nStep 3 — Validate\nInduce a controlled bugcheck in a non-production pool if policy allows, or use historical data from a known patch regression. Check that 41 and 1001 do not double count the same event.\n\nStep 4 — Operationalize\nSend weekly top-stop-code review to the desktop engineering team, attach to image lifecycle gates, and block promotion when stability KPIs regress.",
  "grandmaExplanation": "We count sudden blue screens and surprise restarts on your shared desktop machines, so you see a bad patch or driver before a whole building keeps dropping connection during work.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
