{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.13.5",
  "title": "Device Health by Site Hierarchy",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Availability"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=catalyst sourcetype=cisco:dnac:devicehealth`. from the `devicehealth` modular input in the Cisco Catalyst Add-on (Splunkbase 7538) polling `GET /dna/intent/api/v1/device-health`. Default poll interval: 900s (15m). Volume: ~1 event per managed device per poll. RBAC: NETWORK-ADMIN-ROLE. Key fields: `overallHealth` (integer 0-100), `reachabilityHealth` (string), `deviceName` (string), `deviceType` (string), `siteId` (string), `platformId` (string), `managementIpAddress` (string), `softwareVersion` (string).",
  "app": "`Cisco Catalyst Add-on for Splunk` (Splunkbase 7538)",
  "spl": "index=catalyst sourcetype=\"cisco:dnac:devicehealth\" | stats avg(overallHealth) as avg_health count as device_count count(eval(overallHealth<50)) as unhealthy_count by siteId | eval unhealthy_pct=round(unhealthy_count*100/device_count,1) | sort -unhealthy_pct | head 20",
  "description": "Aggregates device health scores by Catalyst Center site hierarchy (area, building, floor), identifying locations with systemic network problems.",
  "value": "Pinpoints which physical locations have the worst network health, enabling site-specific remediation and resource allocation.",
  "implementation": "Prerequisite: UC-5.13.1 live with `siteId` and device health in `index=catalyst`. Enrich with a site name lookup if only IDs are present. Add this panel to regional operations dashboards; schedule a weekly report for the top 20 sites by unhealthy percentage.",
  "visualization": "Table (top 20 sites with avg_health, counts, unhealthy_pct), bar chart, choropleth or tile map if you join geo metadata.",
  "references": [
    {
      "title": "Splunkbase app 7538",
      "url": "https://splunkbase.splunk.com/app/7538"
    },
    {
      "title": "Catalyst Center API docs",
      "url": "https://developer.cisco.com/docs/catalyst-center/"
    },
    {
      "title": "Catalyst Center Integration Guide",
      "url": "docs/guides/catalyst-center.md"
    },
    {
      "title": "Catalyst Center Device Health API — Cisco DevNet",
      "url": "https://developer.cisco.com/docs/catalyst-center/#!get-overall-network-device-health"
    }
  ],
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_catalyst_center"
  ],
  "knownFalsePositives": "**Tiny branch site with 2–3 devices where a single device failure dominates the average.** A remote office with only a router and a switch will show 50% unhealthy if either device has issues. Distinguish by checking `device_count` for the site — if it is under 5, the percentage is statistically unreliable. Suppress by using absolute device counts (`unhealthy_count>=3`) for sites with fewer than 10 devices, and percentage thresholds only for larger sites.\n\n**Regional power event or construction affecting a single site.** A building power outage or construction-related network disruption affects all devices at one `siteId` simultaneously. Distinguish by checking whether all devices at the site went unhealthy at the same time, and whether neighboring sites are unaffected. This is a real event — do not suppress, but annotate the dashboard with the site-specific context.\n\n**Site hierarchy restructuring changing siteId associations.** When Catalyst Center's site hierarchy is reorganized (floors moved between buildings, sites renamed), `siteId` values change, breaking trend continuity and causing apparent new-site spikes. Distinguish by correlating with `index=catalyst sourcetype=\"cisco:dnac:audit:logs\"` for site hierarchy changes. Suppress by tracking sites by both `siteId` (UUID, stable) and `siteName` (display name, may change), and alerting on siteId continuity.\n\n**Devices not yet assigned to sites appearing as orphaned or under a catch-all.** Newly discovered devices that have not been placed in the site hierarchy may cluster under a \"Global\" or \"Unassigned\" pseudo-site, inflating that site's device count and potentially lowering its health average. Distinguish by checking `index=catalyst sourcetype=\"cisco:dnac:site:topology\"` for devices with no site assignment. Suppress by filtering the catch-all site from per-site health dashboards.",
  "detailedImplementation": "Prerequisites\n• **UC-5.13.1** with **`siteId`** on `cisco:dnac:devicehealth` (Catalyst **site hierarchy** must be populated in **Design > Network Hierarchy** and synced to inventory).\n• Cisco Catalyst Add-on (7538), **devicehealth** input, **`NETWORK-ADMIN-ROLE`** or **`SUPER-ADMIN-ROLE`**.\n• A **lookup** from **`siteId`** to **building name** is strongly recommended for tickets and reports.\n• See `docs/implementation-guide.md`.\n\nStep 1 — Configure data collection\n• **Intent API:** `GET /dna/intent/api/v1/device-health` via **devicehealth** modular input, sourcetype `cisco:dnac:devicehealth`.\n• **Key field:** `siteId` (UUID or string as emitted)—confirm in **one raw event**; missing `siteId` means devices are unassigned in Catalyst **hierarchy**.\n• **Poll:** default **900s**; for **head 20** weekly reports, **widen** the time range to **7d** to smooth single bad polls.\n\nStep 2 — Create the search and alert\n```spl\nindex=catalyst sourcetype=\"cisco:dnac:devicehealth\" | stats avg(overallHealth) as avg_health count as device_count count(eval(overallHealth<50)) as unhealthy_count by siteId | eval unhealthy_pct=round(unhealthy_count*100/device_count,1) | sort -unhealthy_pct | head 20\n```\n\nUnderstanding this SPL\n• **Per-site** aggregation answers “**where** is the pain?”; **`head 20`** focuses leadership on the **worst** sites—**increase to 50** or **remove** `head` if you use **filters** (region) instead.\n• **`<50` unhealthy** matches common **Poor** bands; align with your **SLO** per site **tier** (hospital vs. warehouse) if you add a **lookup** for stricter floors.\n• Cross-check **`device_count`**: a site with **2** devices and **1** unhealthy is **50%**—interpret with care.\n\n**Pipeline walkthrough**\n• `stats` by **`siteId`**: **avg** health, population, and **count** below threshold.\n• `eval` **percentage**; `sort` then **`head 20`** for a **regional** hot list.\n\nStep 3 — Validate\n• Join **`siteId`** to Catalyst **UI** **site** names; spot-check **unhealthy** devices at one site in **Assurance** vs. Splunk row.\n• Compare **Top 20** **device totals** to **sum of site inventory** for the same scope—gaps point to **unassigned** devices.\n• **`| where device_count>10`** in a test search to see how ranking changes for **noise** from small sites.\n\nStep 4 — Operationalize\n• **Dashboard:** **table** with **lookup**-enriched **site name**, **`avg_health`**, **`unhealthy_pct`**, **`device_count`**; default **Last 7 days** for exec reviews; **24h** for NOC.\n• **Scheduled report:** weekly **PDF** to regional leads; **drill** to **UC-5.13.1** and **device** inventory for the same `siteId`.\n• **Optional alert:** when **`unhealthy_pct`** for a **tier-1** site in a **lookup** exceeds **X%** with **min device_count**—avoid paging on every small branch blip.\n\n• **Validate:** compare against the Catalyst Center Assurance dashboard — pick 3-5 devices and verify health scores match within a single poll window.\nStep 5 — Troubleshooting\n• **All `siteId` = null:** fix **hierarchy** assignment in Catalyst and **re-inventory**; confirm TA sends the same field name.\n• **Site missing from top 20 but users complain:** the issue may be **client**-side—use **UC-5.13.9+**; or devices may sit in a **different** `siteId` than users expect (VPN, SD-WAN).\n• **Rankings shift every poll:** API instability or **duplicate** `deviceName` across sites—dedupe logic from **UC-5.13.1** notes applies.\n• **No devicehealth events arriving:** verify the `devicehealth` input is enabled, credentials are valid, and check `index=_internal` for API error or endpoint timeout messages from the TA.\n• **NULL or zero values in health fields:** confirm Assurance licensing (DNA Advantage required) and that Assurance analytics is enabled for the site in Catalyst Center.\n• **Fewer devices than expected:** check the service account role and virtual domain scope; credential access denied errors in TA logs indicate insufficient permissions.",
  "grandmaExplanation": "We compare network health across all your buildings and sites, so you can see which locations need attention first.",
  "prerequisiteUseCases": [
    "UC-5.13.1"
  ],
  "wave": "walk"
}
