{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.5.23",
  "title": "Versa Networks SD-WAN Path Quality and Routing Decisions",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance",
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Versa Networks SD-WAN Path Quality and Routing Decisions' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_versa_networks_sd_wan_path_quality_and_routing_d` (UC-5.5.23), SOC dashboard panel \"Operations — Versa Networks SD-WAN Path Quality and Routing Decisions\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.5.23 positive (Versa, Networks, WAN, Path, Quality): ingest synthetic events into `index=sdwan` sourcetype='versa:sdwan' where `count` grouped by `branch_name`, `circuit_name`, `event_type`, `sla_policy` satisfies detection threshold for 'Versa Networks SD-WAN Path Quality and Routing Decisions'. Validate filter `event_type=\"sla_violation\" OR event_type=\"path_switch\"`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.5.23 negative (Routing Decisions): ingest benign `sdwan` / `versa:sdwan` events where `branch_name`, `circuit_name`, `event_type`, `sla_policy` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 2 confirms zero alert rows for 'Versa Networks SD-WAN Path Quality and Routing Decisions'."
  },
  "dataSources": "Versa FlexVNF appliance syslog (`sourcetype=versa:sdwan`) and structured Versa Director REST API output. Key fields: `event_type` (`sla_violation`, `path_switch`, `tunnel_state_change`), `branch_name`, `circuit_name`, `sla_policy`, `path_quality_score`. Versa Analytics provides historical baselines that the alert can lean on without rolling its own statistical state. Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "Versa Director REST API (custom scripted input or HEC push), Versa FlexVNF syslog (raw forwarder). No vendor Splunkbase TA at the time of authoring; integration is a Versa-supplied Python sample plus a custom `props.conf` / `transforms.conf` pair. — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=sdwan sourcetype=\"versa:sdwan\"\n| search event_type=\"sla_violation\" OR event_type=\"path_switch\"\n| stats count by branch_name, circuit_name, event_type, sla_policy\n| sort - count",
  "description": "Monitors UC-5.5.23 for Versa, Networks, WAN, Path, Quality over `index=sdwan` sourcetype `versa:sdwan` with thresholds configured threshold. Groups `count` by `branch_name`, `circuit_name`, `event_type`, `sla_policy` each `the scheduled window`. Differentiated by filter `event_type=\"sla_violation\" OR event_type=\"path_switch\"`. Supports repeatable operations triage for 'Versa Networks SD-WAN Path Quality and Routing Decisions'.",
  "value": "For 'Versa Networks SD-WAN Path Quality and Routing Decisions', UC-5.5.23 converts `activity` on `versa:sdwan` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Forward Versa FlexVNF syslog to Splunk over UDP / TCP 514. Poll the Versa Director API on a 5-minute schedule for structured SLA violation and path-steering data. Alert when SLA violations exceed a per-site, per-circuit threshold over a rolling 1-hour window — the noise floor varies dramatically by transport and by SLA policy.",
  "visualization": "Table (SLA violations by branch / circuit), Timeline (path-switch events over time), Line chart (circuit quality scores per circuit, multi-circuit overlay).",
  "cimModels": [
    "Network_Traffic"
  ],
  "references": [
    {
      "title": "Versa Networks documentation",
      "url": "https://docs.versa-networks.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Versa Director API guide",
      "url": "https://docs.versa-networks.com/Solutions/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `sdwan` (UC-5.5.23)** — Planned work for 'Versa Networks SD-WAN Path Quality and Routing Decisions' shifts `count` over `span=1h` while `branch_name`, `circuit_name`, `event_type`, `sla_policy` remains in-scope. Applies filter `event_type=\"sla_violation\" OR event_type=\"path_switch\"`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `versa:sdwan`** — Scheduled automation elevates `circuit_name` without crossing configured threshold for UC-5.5.23. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `branch_name`, `circuit_name`, `event_type`, `sla_policy`** — Health checks in `sdwan` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `versa:sdwan`** — First 48h baseline for UC-5.5.23 on `branch_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `branch_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "syslog",
    "versa"
  ],
  "equipmentModels": [
    "versa_analytics",
    "versa_csg_1000",
    "versa_csg_2000",
    "versa_csg_3000",
    "versa_director"
  ],
  "status": "community",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Versa Director REST API (custom scripted input or HEC push), Versa FlexVNF syslog (raw forwarder). No vendor Splunkbase TA at the time of authoring; integration is a Versa-supplied Python sample plus a custom `props.conf` / `transforms.conf` pair. — Splunk Add-on for Unix and Linux (Splunkbase 833)). Enable the modular input or HEC token that writes sourcetype=versa:sdwan into sdwan. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`branch_name, circuit_name, event_type, sla_policy`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We track whether the company's branch offices are getting the network performance they were promised. When the link gets too slow or unreliable for an important application, the SD-WAN switches the traffic to a different path. We watch both the problem and the fix so the network team knows the system is doing its job.",
  "prerequisiteUseCases": [],
  "wave": "walk"
}
