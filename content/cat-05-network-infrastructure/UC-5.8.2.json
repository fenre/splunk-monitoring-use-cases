{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.8.2",
  "title": "Meraki Organization Monitoring",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Meraki Organization Monitoring' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_meraki_organization_monitoring` (UC-5.8.2), SOC dashboard panel \"Operations — Meraki Organization Monitoring\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.8.2 positive (Meraki, Organization, Monitoring): ingest synthetic events into `index=meraki` sourcetype='meraki:devicesavailabilities' where `sum` grouped by `network.id`, `network.name` satisfies `offline_count` > 0, or `alerting_count` > 0 for 'Meraki Organization Monitoring'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.8.2 negative (Organization Monitoring): ingest benign `meraki` / `meraki:devicesavailabilities` events where `network.id`, `network.name` stays below `offline_count` > 0, or `alerting_count` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 11 confirms zero alert rows for 'Meraki Organization Monitoring'."
  },
  "dataSources": "Splunk_TA_cisco_meraki (Splunkbase #5580): Devices Availabilities input (sourcetype=meraki:devicesavailabilities, daily, TA v3.3+) and Organization Networks input (sourcetype=meraki:organizationsnetworks, daily) for network metadata.",
  "app": "`Cisco Meraki Add-on for Splunk` (Splunkbase 5580)",
  "spl": "index=meraki sourcetype=\"meraki:devicesavailabilities\" earliest=-1h\n| stats count as device_count,\n        sum(eval(if(status=\"online\",1,0))) as online_count,\n        sum(eval(if(status=\"offline\",1,0))) as offline_count,\n        sum(eval(if(status=\"alerting\",1,0))) as alerting_count\n         by network.id, network.name\n| eval pct_online = round(online_count*100/device_count, 1)\n| where offline_count > 0 OR alerting_count > 0\n| sort pct_online",
  "description": "Surfaces UC-5.8.2 — Meraki, Organization, Monitoring — using `index=meraki` sourcetype `meraki:devicesavailabilities`, grouped by `network.id`, `network.name`. Outputs `sum` each `the scheduled window`; alert when `offline_count` > 0, or `alerting_count` > 0. Primary focus: Organization Monitoring. Purpose-built for 'Meraki Organization Monitoring' in Operations workflows.",
  "value": "UC-5.8.2 helps teams contain Meraki, Organization, Monitoring scenarios and reduce operational risk and delayed incident response before incidents compound.",
  "implementation": "1. Enable Devices Availabilities and Organization Networks inputs in Splunk_TA_cisco_meraki. The Availabilities input returns one event per device with status (online/offline/dormant/alerting), productType, network.{id,name}. 2. Aggregate per network for a per-site availability dashboard. 3. For multi-org consolidation, configure one Organization input per Meraki tenancy and tag events with the org name; the TA's input wizard prompts for org name when adding.",
  "visualization": "Map (device locations), Table, Status grid, Single value (offline count).",
  "cimModels": [],
  "references": [
    {
      "title": "Splunkbase app 5580",
      "url": "https://splunkbase.splunk.com/app/5580",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Add-on for Cisco Meraki (Splunkbase 5631)",
      "url": "https://splunkbase.splunk.com/app/5631",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Cisco Security documentation",
      "url": "https://www.cisco.com/c/en/us/support/security/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `meraki` (UC-5.8.2)** — Planned work for 'Meraki Organization Monitoring' shifts `sum` over `span=1h` while `network.id`, `network.name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `sum` on `meraki:devicesavailabilities`** — Scheduled automation elevates `network.name` without crossing `offline_count` > 0 for UC-5.8.2. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `network.id`, `network.name`** — Health checks in `meraki` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `meraki:devicesavailabilities`** — First 48h baseline for UC-5.8.2 on `network.id` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `network.id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco",
    "snmp",
    "syslog"
  ],
  "equipmentModels": [
    "cisco_meraki",
    "snmp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Cisco Meraki Add-on for Splunk` (Splunkbase 5580).\n• Ensure the following data sources are available: Splunk_TA_cisco_meraki (Splunkbase #5580): Devices Availabilities input (sourcetype=meraki:devicesavailabilities, daily, TA v3.3+) and Organization Networks input (sourcetype=meraki:organizationsnetworks, daily) for network metadata..\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\n1. Enable Devices Availabilities and Organization Networks inputs in Splunk_TA_cisco_meraki. The Availabilities input returns one event per device with status (online/offline/dormant/alerting), productType, network.{id,name}. 2. Aggregate per network for a per-site availability dashboard. 3. For multi-org consolidation, configure one Organization input per Meraki tenancy and tag events with the org name; the TA's input wizard prompts for org name when adding.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=meraki sourcetype=\"meraki:devicesavailabilities\" earliest=-1h\n| stats count as device_count,\n        sum(eval(if(status=\"online\",1,0))) as online_count,\n        sum(eval(if(status=\"offline\",1,0))) as offline_count,\n        sum(eval(if(status=\"alerting\",1,0))) as alerting_count\n         by network.id, network.name\n| eval pct_online = round(online_count*100/device_count, 1)\n| where offline_count > 0 OR alerting_count > 0\n| sort pct_online\n```\n\nUnderstanding this SPL\n\n**Meraki Organization Monitoring** — Network operations teams maintain a unified view of Meraki device health across all organizations and networks, detecting site outages (offline MX), degraded coverage (offline APs), and alerting conditions with offline duration tracking.\n\nDocumented **Data sources**: Splunk_TA_cisco_meraki (Splunkbase #5580): Devices Availabilities input (sourcetype=meraki:devicesavailabilities, daily, TA v3.3+) and Organization Networks input (sourcetype=meraki:organizationsnetworks, daily) for network metadata. **App/TA** (typical add-on context): `Cisco Meraki Add-on for Splunk` (Splunkbase 5580). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: meraki; **sourcetype**: meraki:devicesavailabilities. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=meraki, sourcetype=\"meraki:devicesavailabilities\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by network.id, network.name** so each row reflects one combination of those dimensions (useful for per-host, per-user, or per-entity comparisons for this use case).\n• `eval` defines or adjusts **pct_online** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where offline_count > 0 OR alerting_count > 0` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Map (device locations), Table, Status grid, Single value (offline count).",
  "grandmaExplanation": "We help you see when Meraki gear goes offline or unhealthy across sites so the team can fix it before everyone loses the network in that building.",
  "hardware": "Cisco Meraki MX64, MX67, MX68, MX75, MX84, MX85, MX95, MX100, MX105, MX250, MX450, Cisco Meraki MR36, MR44, MR46, MR56, MR57, MR76, MR78, MR86, Cisco Meraki MS120, MS125, MS130, MS210, MS225, MS250, MS350, MS390",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
