{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.8.9",
  "title": "SSL/TLS Certificate Expiration Tracking (Meraki)",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'SSL/TLS Certificate Expiration Tracking (Meraki)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_ssl_tls_certificate_expiration_tracking_meraki` (UC-5.8.9), SOC dashboard panel \"Operations — SSL/TLS Certificate Expiration Tracking (Meraki)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=meraki` sourcetype='meraki:assurancealerts' where `scope.devices{}.serial` satisfies detection threshold for UC-5.8.9 ('SSL/TLS Certificate Expiration Tracking (Meraki)'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `meraki` / `meraki:assurancealerts` events where `scope.devices{}.serial` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-5.8.9."
  },
  "dataSources": "Splunk_TA_cisco_meraki (Splunkbase #5580): Assurance Alerts input (sourcetype=meraki:assurancealerts). NOTE: the Meraki Dashboard API does NOT expose certificate validity dates for managed devices; the only signal available is the assurance-alert feed when a certificate-related issue is detected. For branch firewalls hosting public services, monitor your CA portal (Let's Encrypt, Sectigo, DigiCert) directly or use a TLS scanner like ssl-checker / Splunk Sslscan TA.",
  "app": "`Cisco Meraki Add-on for Splunk` (Splunkbase 5580)",
  "spl": "index=meraki sourcetype=\"meraki:assurancealerts\"\n    (title=\"*certificate*\" OR title=\"*TLS*\" OR title=\"*SSL*\"\n     OR title=\"*expir*\")\n    earliest=-7d\n| stats count as alert_count,\n        values(title) as cert_alerts,\n        latest(severity) as severity\n         by scope.devices{}.serial, scope.devices{}.name, network.name\n| sort - alert_count",
  "description": "Monitors SSL certificate expiration dates on all network devices to prevent outages. Searches `index=meraki` with sourcetype `meraki:assurancealerts` for UC-5.8.9, aggregating `values` by `scope.devices{}.serial` over `the scheduled window`. Alerts when grouped results cross configured threshold in the saved search. Gives Operations operators a repeatable signal for 'SSL/TLS Certificate Expiration Tracking (Meraki)' without manual log review.",
  "value": "Monitors SSL certificate expiration dates on all network devices to prevent outages. Timely UC-5.8.9 alerting on 'SSL/TLS Certificate Expiration Tracking (Meraki)' reduces operational risk and delayed incident response before manual triage or audit channels escalate.",
  "implementation": "1. Enable the Assurance Alerts input in Splunk_TA_cisco_meraki. 2. Filter alert titles for certificate / TLS / SSL / expiry keywords. 3. For proactive monitoring deploy a TLS expiry scanner (e.g. the Splunk Sslscan TA or a custom curl + openssl modular input) against your branch endpoints, and trigger a Splunk alert ~30 days before expiry.",
  "visualization": "Expiration countdown gauge; timeline of expiring certs; alert table.",
  "cimModels": [],
  "references": [
    {
      "title": "Splunkbase app 5580",
      "url": "https://splunkbase.splunk.com/app/5580",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Add-on for Cisco Meraki (Splunkbase 5631)",
      "url": "https://splunkbase.splunk.com/app/5631",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Cisco Security documentation",
      "url": "https://www.cisco.com/c/en/us/support/security/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `meraki`** — Planned work for 'SSL/TLS Certificate Expiration Tracking (Meraki)' shifts `values` over `span=1h` while `scope.devices{}.serial` remains in-scope for UC-5.8.9. Cross-check CMDB CHG.\n\n2. **Batch `values` on `meraki:assurancealerts`** — Scheduled automation elevates `scope.devices{}.name` without crossing configured threshold. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `scope.devices{}.serial`** — Health checks in `meraki` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `meraki:assurancealerts`** — First 48h baseline for `scope.devices{}.serial` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `scope.devices{}.serial`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cert_pki",
    "cisco",
    "syslog"
  ],
  "equipmentModels": [
    "cert_pki_letsencrypt",
    "cert_pki_sectigo",
    "cisco_meraki"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Cisco Meraki Add-on for Splunk` (Splunkbase 5580).\n• Ensure the following data sources are available: Splunk_TA_cisco_meraki (Splunkbase #5580): Assurance Alerts input (sourcetype=meraki:assurancealerts). NOTE: the Meraki Dashboard API does NOT expose certificate validity dates for managed devices; the only signal available is the assurance-alert feed when a certificate-related issue is detected. For branch firewalls hosting public services, monitor your CA portal (Let's Encrypt, Sectigo, DigiCert) directly or use a TLS scanner like ssl-checker / Splunk Sslscan TA..\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\n1. Enable the Assurance Alerts input in Splunk_TA_cisco_meraki. 2. Filter alert titles for certificate / TLS / SSL / expiry keywords. 3. For proactive monitoring deploy a TLS expiry scanner (e.g. the Splunk Sslscan TA or a custom curl + openssl modular input) against your branch endpoints, and trigger a Splunk alert ~30 days before expiry.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=meraki sourcetype=\"meraki:assurancealerts\"\n    (title=\"*certificate*\" OR title=\"*TLS*\" OR title=\"*SSL*\"\n     OR title=\"*expir*\")\n    earliest=-7d\n| stats count as alert_count,\n        values(title) as cert_alerts,\n        latest(severity) as severity\n         by scope.devices{}.serial, scope.devices{}.name, network.name\n| sort - alert_count\n```\n\nUnderstanding this SPL\n\n**SSL/TLS Certificate Expiration Tracking (Meraki)** — Network operations teams track SSL/TLS certificate expiration across all network device management interfaces and Meraki appliances, preventing certificate-related management access failures and identifying weak cryptographic configurations.\n\nDocumented **Data sources**: Splunk_TA_cisco_meraki (Splunkbase #5580): Assurance Alerts input (sourcetype=meraki:assurancealerts). NOTE: the Meraki Dashboard API does NOT expose certificate validity dates for managed devices; the only signal available is the assurance-alert feed when a certificate-related issue is detected. For branch firewalls hosting public services, monitor your CA portal (Let's Encrypt, Sectigo, DigiCert) directly or use a TLS scanner like ssl-checker / Splunk Sslscan TA. **App/TA** (typical add-on context): `Cisco Meraki Add-on for Splunk` (Splunkbase 5580). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: meraki; **sourcetype**: meraki:assurancealerts. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=meraki, sourcetype=\"meraki:assurancealerts\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by scope.devices{}.serial, scope.devices{}.name, network.name** so each row reflects one combination of those dimensions (useful for per-host, per-user, or per-entity comparisons for this use case).\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Expiration countdown gauge; timeline of expiring certs; alert table.",
  "grandmaExplanation": "We warn you before Meraki dashboard certificates age out, so the browser and API checks keep working and nobody gets stuck with scary warnings.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
