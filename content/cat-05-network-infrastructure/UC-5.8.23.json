{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.8.23",
  "title": "Dashboard Configuration and Export Backup (Meraki)",
  "criticality": "low",
  "difficulty": "beginner",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Dashboard Configuration and Export Backup (Meraki)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_dashboard_configuration_and_export_backup_meraki` (UC-5.8.23), SOC dashboard panel \"Operations — Dashboard Configuration and Export Backup (Meraki)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.8.23 positive (Dashboard, Configuration, and, Export, Backup): ingest synthetic events into `index=meraki` sourcetype='meraki:audit' where `values` grouped by `organizationId`, `networkName` satisfies `days_since_last` > 30 for 'Dashboard Configuration and Export Backup (Meraki)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.8.23 negative (Backup (Meraki)): ingest benign `meraki` / `meraki:audit` events where `organizationId`, `networkName` stays below `days_since_last` > 30, or exclude the test entity via `ops_exception_register.csv`. Variant 7 confirms zero alert rows for 'Dashboard Configuration and Export Backup (Meraki)'."
  },
  "dataSources": "Splunk_TA_cisco_meraki (Splunkbase #5580): Audit input (sourcetype=meraki:audit, daily). NOTE: the Meraki Dashboard does not provide a native 'config backup' API endpoint; backups are typically performed by exporting GET /networks/{networkId}/configTemplates and per-product config endpoints via a custom script. The audit log records when an admin performs these actions.",
  "app": "`Cisco Meraki Add-on for Splunk` (Splunkbase 5580)",
  "spl": "index=meraki sourcetype=\"meraki:audit\" earliest=-30d\n    (page=\"Backup\" OR label=\"*backup*\" OR action=\"*backup*\"\n     OR page=\"Configuration sync\" OR label=\"*export*\")\n| stats latest(_time) as last_action,\n        values(adminName) as performed_by,\n        values(label) as actions\n         by organizationId, networkName\n| eval days_since_last = round((now() - last_action)/86400, 0)\n| where isnull(last_action) OR days_since_last > 30\n| sort - days_since_last",
  "description": "Identifies UC-5.8.23 — Dashboard, Configuration, and, Export, Backup — using `index=meraki` sourcetype `meraki:audit`, grouped by `organizationId`, `networkName`. Outputs `values` each `the scheduled window`; alert when `days_since_last` > 30. Primary focus: Backup (Meraki). Purpose-built for 'Dashboard Configuration and Export Backup (Meraki)' in Operations workflows.",
  "value": "UC-5.8.23 operationalises 'Dashboard Configuration and Export Backup (Meraki)' against `meraki:audit` feeds so operators reduce operational risk and delayed incident response without manual correlation.",
  "implementation": "1. Enable the Audit input in Splunk_TA_cisco_meraki. 2. Filter audit entries for configuration export / template backup activity. 3. The query identifies organizations or networks where no backup-related admin action has happened in the last 30 days. 4. For an automated backup, run a scheduled script that calls GET /networks/{networkId}/appliance/firewall/l3FirewallRules, /wireless/ssids, /switch/accessPolicies etc. and stores the output in version control. The Audit input gives you visibility into manual exports performed via the Meraki Dashboard UI.",
  "visualization": "Last backup timestamp by org; backup recency gauge; backup history timeline.",
  "cimModels": [],
  "references": [
    {
      "title": "Splunkbase app 5580",
      "url": "https://splunkbase.splunk.com/app/5580",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Add-on for Cisco Meraki (Splunkbase 5631)",
      "url": "https://splunkbase.splunk.com/app/5631",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Cisco Security documentation",
      "url": "https://www.cisco.com/c/en/us/support/security/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `meraki` (UC-5.8.23)** — Planned work for 'Dashboard Configuration and Export Backup (Meraki)' shifts `values` over `span=1h` while `organizationId`, `networkName` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `values` on `meraki:audit`** — Scheduled automation elevates `networkName` without crossing `days_since_last` > 30 for UC-5.8.23. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `organizationId`, `networkName`** — Health checks in `meraki` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `meraki:audit`** — First 48h baseline for UC-5.8.23 on `organizationId` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `organizationId`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco",
    "syslog"
  ],
  "equipmentModels": [
    "cisco_meraki"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Cisco Meraki Add-on for Splunk` (Splunkbase 5580).\n• Ensure the following data sources are available: Splunk_TA_cisco_meraki (Splunkbase #5580): Audit input (sourcetype=meraki:audit, daily). NOTE: the Meraki Dashboard does not provide a native 'config backup' API endpoint; backups are typically performed by exporting GET /networks/{networkId}/configTemplates and per-product config endpoints via a custom script. The audit log records when an admin performs these actions..\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\n1. Enable the Audit input in Splunk_TA_cisco_meraki. 2. Filter audit entries for configuration export / template backup activity. 3. The query identifies organizations or networks where no backup-related admin action has happened in the last 30 days. 4. For an automated backup, run a scheduled script that calls GET /networks/{networkId}/appliance/firewall/l3FirewallRules, /wireless/ssids, /switch/accessPolicies etc. and stores the output in version control. The Audit input gives you visibility i…\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=meraki sourcetype=\"meraki:audit\" earliest=-30d\n    (page=\"Backup\" OR label=\"*backup*\" OR action=\"*backup*\"\n     OR page=\"Configuration sync\" OR label=\"*export*\")\n| stats latest(_time) as last_action,\n        values(adminName) as performed_by,\n        values(label) as actions\n         by organizationId, networkName\n| eval days_since_last = round((now() - last_action)/86400, 0)\n| where isnull(last_action) OR days_since_last > 30\n| sort - days_since_last\n```\n\nUnderstanding this SPL\n\n**Dashboard Configuration and Export Backup (Meraki)** — Network operations teams maintain indexed Meraki configuration backups in Splunk for disaster recovery, audit compliance, and configuration drift detection across all networks and configuration sections.\n\nDocumented **Data sources**: Splunk_TA_cisco_meraki (Splunkbase #5580): Audit input (sourcetype=meraki:audit, daily). NOTE: the Meraki Dashboard does not provide a native 'config backup' API endpoint; backups are typically performed by exporting GET /networks/{networkId}/configTemplates and per-product config endpoints via a custom script. The audit log records when an admin performs these actions. **App/TA** (typical add-on context): `Cisco Meraki Add-on for Splunk` (Splunkbase 5580). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: meraki; **sourcetype**: meraki:audit. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=meraki, sourcetype=\"meraki:audit\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by organizationId, networkName** so each row reflects one combination of those dimensions (useful for per-host, per-user, or per-entity comparisons for this use case).\n• `eval` defines or adjusts **days_since_last** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where isnull(last_action) OR days_since_last > 30` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Last backup timestamp by org; backup recency gauge; backup history timeline.",
  "grandmaExplanation": "We help you back up and track Meraki dashboard exports, so a bad change does not erase a good layout when you need it most.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
