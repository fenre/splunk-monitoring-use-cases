{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.8.26",
  "title": "CDN Origin Hit Rate and Cache Efficiency (CloudFront / Akamai / Fastly)",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'CDN Origin Hit Rate and Cache Efficiency (CloudFront / Akamai / Fastly)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_cdn_origin_hit_rate_and_cache_efficiency_cloudfr` (UC-5.8.26), SOC dashboard panel \"Operations — CDN Origin Hit Rate and Cache Efficiency (CloudFront / Akamai / Fastly)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.8.26 positive (CDN, Origin, Hit, Rate, and): ingest synthetic events into `index=cdn` sourcetype='aws:cloudfront:accesslogs' where `count` grouped by `cs_uri_stem` satisfies `hit_rate` < 80, or `errors` > 10 for 'CDN Origin Hit Rate and Cache Efficiency (CloudFront / Akamai / Fastly)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.8.26 negative (/ Fastly)): ingest benign `cdn` / `aws:cloudfront:accesslogs` events where `cs_uri_stem` stays below `hit_rate` < 80, or `errors` > 10, or exclude the test entity via `ops_exception_register.csv`. Variant 15 confirms zero alert rows for 'CDN Origin Hit Rate and Cache Efficiency (CloudFront / Akamai / Fastly)'."
  },
  "dataSources": "AWS CloudFront access logs (`sourcetype=aws:cloudfront:accesslogs`), Akamai DataStream (`sourcetype=akamai:datastream`), and Fastly Real-Time Log Streaming (`sourcetype=fastly:cdn`). Each stream carries the cache-decision metadata that distinguishes HIT vs MISS vs ERROR at the edge: `x_edge_result_type` for CloudFront, `cacheStatus` for Akamai DataStream, and `cache_status` for Fastly. Hit ratio at edge is the single best indicator of CDN configuration health.",
  "app": "Splunk Add-on for AWS (`Splunk_TA_aws`, Splunkbase 1876) for CloudFront; Akamai DataStream syslog (raw forwarder, no Splunkbase TA); Fastly Real-Time Log Streaming via HEC (no Splunkbase TA, configured directly in Fastly UI)",
  "spl": "index=cdn sourcetype=\"aws:cloudfront:accesslogs\"\n| eval cache_result=case(x_edge_result_type=\"Hit\",\"HIT\", x_edge_result_type=\"Miss\",\"MISS\", x_edge_result_type=\"Error\",\"ERROR\", 1=1,\"OTHER\")\n| stats count as total, count(eval(cache_result=\"HIT\")) as hits, count(eval(cache_result=\"MISS\")) as misses, count(eval(cache_result=\"ERROR\")) as errors by cs_uri_stem\n| eval hit_rate=round(hits/total*100,2)\n| where hit_rate < 80 OR errors > 10\n| sort - total",
  "description": "Detects UC-5.8.26 — CDN, Origin, Hit, Rate, and — using `index=cdn` sourcetype `aws:cloudfront:accesslogs`, grouped by `cs_uri_stem`. Outputs `count` each `the scheduled window`; alert when `hit_rate` < 80, or `errors` > 10. Primary focus: / Fastly). Purpose-built for 'CDN Origin Hit Rate and Cache Efficiency (CloudFront / Akamai / Fastly)' in Operations workflows.",
  "value": "UC-5.8.26 operationalises 'CDN Origin Hit Rate and Cache Efficiency (CloudFront / Akamai / Fastly)' against `aws:cloudfront:accesslogs` feeds so operators reduce operational risk and delayed incident response without manual correlation.",
  "implementation": "Enable CloudFront access logging to S3 and ingest via Splunk Add-on for AWS. For Akamai, configure DataStream to forward to Splunk via syslog (or to a Kinesis sink the AWS TA can read). For Fastly, configure Real-Time Log Streaming to a Splunk HEC endpoint. Alert on cache hit rate drops below 80% or elevated edge error rates.",
  "visualization": "Line chart (cache hit rate over time, multiple PoPs overlaid), Table (low-efficiency URIs sorted by total volume), Pie chart (HIT/MISS/ERROR distribution per CDN provider).",
  "cimModels": [
    "Web"
  ],
  "references": [
    {
      "title": "Splunk Add-on for AWS (Splunkbase app 1876)",
      "url": "https://splunkbase.splunk.com/app/1876",
      "retrieved": "2026-04-25"
    },
    {
      "title": "AWS CloudFront access log documentation",
      "url": "https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/AccessLogs.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Akamai DataStream",
      "url": "https://techdocs.akamai.com/datastream2/docs",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Fastly Real-Time Log Streaming",
      "url": "https://docs.fastly.com/en/guides/about-fastlys-realtime-log-streaming-features",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `cdn` (UC-5.8.26)** — Planned work for 'CDN Origin Hit Rate and Cache Efficiency (CloudFront / Akamai / Fastly)' shifts `count` over `span=1h` while `cs_uri_stem` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `aws:cloudfront:accesslogs`** — Scheduled automation elevates `cache_result` without crossing `hit_rate` < 80 for UC-5.8.26. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `cs_uri_stem`** — Health checks in `cdn` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `aws:cloudfront:accesslogs`** — First 48h baseline for UC-5.8.26 on `cs_uri_stem` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `cs_uri_stem`, `exception_reason`, `valid_until`.",
  "equipment": [
    "akamai",
    "asterisk",
    "aws",
    "fastly",
    "syslog"
  ],
  "equipmentModels": [
    "akamai_datastream",
    "aws_cloudfront",
    "fastly_cdn"
  ],
  "status": "community",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for AWS (`Splunk_TA_aws`, Splunkbase 1876) for CloudFront; Akamai DataStream syslog (raw forwarder, no Splunkbase TA); Fastly Real-Time Log Streaming via HEC (no Splunkbase TA, configured directly in Fastly UI)). Enable the modular input or HEC token that writes sourcetype=aws:cloudfront:accesslogs into cdn. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`cache_result, cs_uri_stem, errors, hit_rate`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch how often our content delivery network (the cache layer that serves images and videos closer to users) actually finds what users ask for in its memory versus going back to the original servers. When the cache hit rate drops or errors climb, we alert before users notice slow pages or before our origin servers get overloaded.",
  "prerequisiteUseCases": [],
  "wave": "walk"
}
