{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.18.3",
  "title": "RSVP-TE Tunnel State and Bandwidth Reservation",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Capacity",
    "Performance",
    "Operations"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'RSVP-TE Tunnel State and Bandwidth Reservation'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-5.18.3).",
  "evidence": "Saved search uc_5_18_3_rsvp_te_tunnel_state_and_bandwidth_reser, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'RSVP-TE Tunnel State and Bandwidth Reservation'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-5.18.3.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "Detailed: `index=network`; syslog RSVP reservation failures (`INSUFFICIENT`, `preemption`, `BW unavailable`), SNMP OID walks (`mplsTunnel*` MIB tables where exposed), streaming telemetry `allocated_bandwidth`, `setup_priority`, `holding_priority`; Cisco IOS-XR model-driven telemetry paths under Cisco-IOS-XR-mpls-te-oper. Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "Cisco Networks Add-on for Splunk (`TA-cisco_ios`), Splunk Add-on for Juniper (`Splunk_TA_juniper`), Nokia SR OS streaming telemetry over gRPC/gNMI normalized to JSON lines. — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=network earliest=-24h@h latest=now\n| eval st=lower(coalesce(sourcetype,_sourcetype,\"\"))\n| eval msg=lower(_raw)\n| eval rsvp_te=match(msg,\"rsvp\") AND match(msg,\"(?:tunnel|ero|explicit|rro|soft.?preempt|bandwidth|bw|reservation|ero.?hop|session.*(?:fail|error)|(?:path|resv).*(?:err|tear))\")\n| where rsvp_te=1 OR match(st,\"telemetry\") AND match(msg,\"\\\"tunnel\\\"|mpls.?te|traffic.?eng\")\n| rex field=_raw max_match=0 \"(?i)bandwidth[^0-9]*(?<bw_kbps>[0-9]+)\\s*(kbps|kb/s)?\"\n| rex field=_raw max_match=0 \"(?i)(?:tunnel.?id|tunnel)\\s*[:=]?\\s*(?<tunnel_id>[0-9]+)\"\n| rex field=_raw max_match=0 \"(?i)(?:setup|hold).*priority[^0-9]*(?<prio>[0-7])\"\n| stats values(msg) as sample_messages latest(bw_kbps) as last_bw latest(prio) as last_prio count by host tunnel_id\n| eval bw_mbps=round(if(isnotnull(last_bw),tonumber(last_bw)/1024,null()),2)\n| sort - count",
  "description": "Splunk aggregates RSVP-TE signaling failures, bandwidth shortage messages, and telemetry snapshots so constrained TE trunks and preemption storms become visible before admission control blocks new wholesale circuits.",
  "value": "Planning and NOC teams defend committed information rates on constrained bundles because Splunk highlights when reservations fail, priorities preempt lower classes, or tunnel bandwidth diverges from engineered DS-TE templates.",
  "implementation": "Combine syslog RSVP errors with periodic telemetry pulls of tunnel bandwidth; normalize Mbps; alert on INSUFFICIENT_RESOURCES strings or when telemetry shows allocated bandwidth exceeding interface policy caps minus headroom.",
  "visualization": "Dashboard Studio: KPI row for tunnels with reservation failures; dual-axis timechart of signaled bandwidth vs interface cap; table (`host`, `tunnel_id`, `bw_mbps`, `sample_messages`) with severity coloring.",
  "references": [
    {
      "title": "Cisco IOS XR Traffic Engineering Configuration Guide",
      "url": "https://www.cisco.com/c/en/us/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Juniper RSVP User Guide — MPLS Traffic Engineering",
      "url": "https://www.juniper.net/documentation/us/en/software/junos/mpls/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "IETF RFC 3209 — RSVP-TE Extensions",
      "url": "https://www.rfc-editor.org/rfc/rfc3209",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "mitreAttack": [
    "N/A (capacity monitoring)"
  ],
  "detectionType": "Baseline",
  "securityDomain": "network",
  "equipment": [
    "asterisk",
    "cisco",
    "juniper",
    "snmp",
    "syslog"
  ],
  "equipmentModels": [
    "cisco_asr_1000",
    "cisco_asr_9000",
    "cisco_ios",
    "juniper_mx",
    "snmp_generic"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• DS-TE / TE-class policies documented with numeric priorities and interface maximum reservable bandwidth.\n• Tunnel naming convention encoded in lookup `te_tunnel_inventory.csv` with endpoints and CIR.\n\nStep 1 — Instrument RSVP failures\nEnable MPLS TE syslog at severity capturing `%ROUTING-RSVP-4-*` style insufficient bandwidth and preemption notifications on Cisco; Junos `traceoptions` only where syslog bridging exists—prefer telemetry for steady-state.\n\nStep 2 — Telemetry normalization\nFor IOS-XR OpenConfig or native sensor paths, map `tunnel-name`, `signaled-bandwidth`, `state`; land JSON with sourcetype `telemetry:mpls`. Juniper network-agent subscriptions similar.\n\nStep 3 — Saved correlation\nSearch `rsvp_te_bw_reserve_issues`: failure syslog OR `(bw_mbps > nominal_cap_mbps * 0.95)` joined via lookup on `tunnel_id`.\n\nStep 4 — Validation\nDuring lab admission tests, attempt tunnel setup exceeding RSVP maximum—ensure Splunk captures RSVP PathErr reason code matching CLI `show rsvp reservation`.\n\nStep 5 — Visualization & governance\nExecutive dashboard shows top congested POP pairs; monthly review tunes thresholds against upgrade Capex triggers.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Cisco Networks Add-on for Splunk (`TA-cisco_ios`), Splunk Add-on for Juniper (`Splunk_TA_juniper`), Nokia SR OS streaming telemetry over gRPC/gNMI normalized to JSON lines. — Splunk Add-on for Unix and Linux (Splunkbase 833)). Enable the modular input or HEC token that writes sourcetype=see SPL into network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, tunnel_id`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We track when reserved highway lanes on our backbone fail to open or get bumped by higher-priority trucks. That helps planners see squeezes before promised speeds to customers quietly shrink.",
  "prerequisiteUseCases": [],
  "wave": "walk"
}
