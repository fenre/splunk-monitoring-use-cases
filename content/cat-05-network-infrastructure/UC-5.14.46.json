{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.14.46",
  "title": "Traefik ACME / Let's Encrypt Renewal Errors",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Security"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Traefik ACME / Let's Encrypt Renewal Errors' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_traefik_acme_let_s_encrypt_renewal_errors` (UC-5.14.46), SOC dashboard panel \"Operations — Traefik ACME / Let's Encrypt Renewal Errors\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=proxy` sourcetype='traefik:log' where `_raw` satisfies detection threshold for UC-5.14.46 ('Traefik ACME / Let's Encrypt Renewal Errors'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `proxy` / `traefik:log` events where `_raw` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-5.14.46."
  },
  "dataSources": "`index=proxy` `sourcetype=traefik:log` ACME client lines Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes — Universal Forwarder (Splunkbase 617)",
  "spl": "index=proxy sourcetype=\"traefik:log\"\n| regex _raw=\"(?i)(acme|letsencrypt).*(error|fail|unable)\"\n| stats latest(_time) as last_seen count by _raw\n| sort - count\n| head 20",
  "description": "TLS expiry outages are preventable with log monitoring. This use case monitors the condition described in the title using the SPL and data sources documented on this page.",
  "value": "TLS expiry outages are preventable with log monitoring. Timely UC-5.14.46 alerting on 'Traefik ACME / Let's Encrypt Renewal Errors' reduces operational risk and delayed incident response before manual triage or audit channels escalate.",
  "implementation": "Prefer DNS challenge in complex networks; monitor cert NotAfter via separate exporter.",
  "visualization": "Time series for rates and latency, top/dedup for hotspots, single-value alerts on health thresholds.",
  "cimModels": [],
  "references": [
    {
      "title": "Vendor documentation",
      "url": "https://doc.traefik.io/traefik/https/acme/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `proxy`** — Planned work for 'Traefik ACME / Let's Encrypt Renewal Errors' shifts `count` over `span=1h` while `_raw` remains in-scope for UC-5.14.46. Cross-check CMDB CHG.\n\n2. **Batch `count` on `traefik:log`** — Scheduled automation elevates `src` without crossing configured threshold. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `_raw`** — Health checks in `proxy` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `traefik:log`** — First 48h baseline for `_raw` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `_raw`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cert_pki",
    "syslog",
    "traefik"
  ],
  "equipmentModels": [
    "cert_pki_letsencrypt"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "Prerequisites\n* Traefik logs (syslog or file) with ACME/Let's Encrypt events. Data in `index=proxy` with `sourcetype=traefik:log`. Key events: certificate renewal, challenge failures, rate limit errors, account registration.\n* ACME: Traefik automatically obtains and renews TLS certificates from Let's Encrypt (or other ACME CAs). Certificates are renewed 30 days before expiry (configurable). Failures to renew mean certificates expire, causing HTTPS errors. Common issues: DNS challenge failures, HTTP challenge port 80 not reachable, rate limiting.\n\nStep 1 -- Configure data collection\n```yaml\n# traefik.yml\ncertificatesResolvers:\n  letsencrypt:\n    acme:\n      email: admin@example.com\n      storage: /etc/traefik/acme.json\n      httpChallenge:\n        entryPoint: web\n```\nVerify:\n```spl\nindex=proxy sourcetype=\"traefik:log\" earliest=-24h\n| where match(_raw, \"(?i)acme|letsencrypt|certificate|renewal|challenge\")\n| stats count by _raw | head 20\n```\n\nStep 2 -- Create the search and alert\n\n**Primary search -- ACME renewal error detection:**\n```spl\nindex=proxy sourcetype=\"traefik:log\" earliest=-24h\n| where match(_raw, \"(?i)acme|letsencrypt|certificate|renewal|challenge\")\n| eval acme_event=case(match(_raw, \"(?i)unable.*obtain|challenge.*fail|error.*acme|acme.*error\"), \"RENEWAL_FAILURE\", match(_raw, \"(?i)rate.*limit|too many\"), \"RATE_LIMITED\", match(_raw, \"(?i)dns.*challenge.*fail\"), \"DNS_CHALLENGE_FAIL\", match(_raw, \"(?i)http.*challenge.*fail|validation.*fail\"), \"HTTP_CHALLENGE_FAIL\", match(_raw, \"(?i)certificate.*obtained|renewal.*success|new certificate\"), \"RENEWAL_SUCCESS\", match(_raw, \"(?i)certificate.*expir\"), \"EXPIRY_WARNING\", 1==1, \"OTHER\")\n| where acme_event != \"OTHER\" AND acme_event != \"RENEWAL_SUCCESS\"\n| rex \"domain.*?(?<domain>[a-zA-Z0-9.-]+\\.[a-z]{2,})\"\n| stats count as events values(domain) as affected_domains by acme_event\n| eval severity=case(acme_event=\"RENEWAL_FAILURE\", \"CRITICAL -- certificate renewal failed\", acme_event=\"RATE_LIMITED\", \"HIGH -- rate limiting by CA\", acme_event=\"DNS_CHALLENGE_FAIL\", \"HIGH -- DNS challenge failed\", acme_event=\"HTTP_CHALLENGE_FAIL\", \"HIGH -- HTTP-01 challenge failed\", acme_event=\"EXPIRY_WARNING\", \"WARNING -- certificate near expiry\", 1==1, \"INFO\")\n| sort severity, -events\n```\n\nStep 3 -- Validate\n(a) Check ACME storage: `cat /etc/traefik/acme.json | jq '.Certificates[].domain'` -- lists managed certificates.\n(b) Verify HTTP challenge: `curl http://<host>/.well-known/acme-challenge/test` -- should be reachable.\n(c) Check certificate expiry: `echo | openssl s_client -servername <domain> -connect <host>:443 | openssl x509 -noout -dates`.\n\nStep 4 -- Operationalize\nDashboard (\"Traefik -- ACME/TLS\"):\n* Row 1 -- Single-value: \"Renewal failures (24h)\", \"Rate limit events\", \"Expiring certs\".\n* Row 2 -- ACME event timeline.\n* Row 3 -- Affected domains list.\n\nAlerting:\n* Critical (renewal failure): certificate will expire if not fixed.\n* High (rate limited): CA blocking requests -- wait or check domain count.\n\nStep 5 -- Troubleshooting\n\n* **HTTP-01 challenge failure** -- CA cannot reach port 80 on the domain. Check: (1) port 80 is open in firewall, (2) Traefik is listening on port 80, (3) DNS A record points to the correct IP, (4) no other service intercepting `/.well-known/acme-challenge/`.\n\n* **Rate limited by CA** -- Let's Encrypt limits: 50 certificates per registered domain per week, 5 duplicate certificates per week. Wait for the rate limit window to pass or use staging endpoint for testing.\n\n* **DNS challenge failure** -- DNS provider API credentials may be wrong or expired. Check the provider-specific environment variables (e.g., Cloudflare API token, Route53 keys).",
  "grandmaExplanation": "We watch traefik acme / let's encrypt renewal errors and catch issues early, before they turn into outages for the people who rely on the network.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
