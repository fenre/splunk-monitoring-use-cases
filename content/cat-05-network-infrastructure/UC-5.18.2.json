{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.18.2",
  "title": "LDP Neighbor Adjacency Monitoring",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Fault",
    "Operations"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'LDP Neighbor Adjacency Monitoring'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-5.18.2).",
  "evidence": "Saved search uc_5_18_2_ldp_neighbor_adjacency_monitoring, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'LDP Neighbor Adjacency Monitoring'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-5.18.2.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "Detailed: `index=network`; `sourcetype` — `cisco:ios`, `cisco:ios_xr`, `cisco:ios_xe`, `juniper:junos`, `nokia:sros`; syslog covering LDP session events (`%LDP-*`, `%ROUTING-LDP-*`, Junos `rpd_ldp_*`, `ldpDown`, `ldpUp`, Nokia `tmnxPlcyLdPi*`/`svplsLdPi*`-style facility strings); optional SNMP TRAP-PDU `mplsLdpSessionDown`. Fields: `neighbor`, `interface`, `tcp_socket`, `reason`, `graceful_restart`. Collected via Splunk Add-on for Cisco Security (Splunkbase 1352) modular input or scripted poll into the index named in the SPL.",
  "app": "Cisco Networks Add-on for Splunk (`TA-cisco_ios`), Splunk Add-on for Juniper (`Splunk_TA_juniper`), Nokia SR OS syslog integrations with supplemental FIELDALIAS for neighbor router-id. — Splunk Add-on for Cisco Security (Splunkbase 1352)",
  "spl": "index=network earliest=-24h@h latest=now\n| eval st=lower(coalesce(sourcetype,_sourcetype,\"\"))\n| where match(st,\"cisco:ios|cisco:ios_xr|cisco:ios_xe|juniper:junos|nokia\")\n| eval msg=lower(_raw)\n| eval is_ldp=if(match(msg,\"\\\\bldp\\\\b\") OR match(msg,\"label.?distribution\") OR match(_raw,\"LDP-\"),1,0)\n| where is_ldp=1 AND match(msg,\"(?:neighbor|adjacency|session|peer).*(?:down|up|reset|closed|timeout|hold.?time|fd|tcp)|(?:down|lost).*ldp|ldp.*(?:fail|error)|graceful.?restart.*(?:fail|complete)\")\n| rex field=_raw max_match=0 \"(?i)(?:neighbor|peer)[=: \\t]+(?<neighbor>[0-9.]+|[0-9a-f:]+)\"\n| rex field=_raw max_match=0 \"(?i)(?:interface|if)\\s*[:=]?\\s*(?<ifname>[^\\s,]+)\"\n| stats count earliest(_time) as first_seen latest(_time) as last_seen values(neighbor) as peers values(ifname) as ifaces by host st\n| sort - count",
  "description": "Splunk tracks LDP hello and TCP session churn across PE and P nodes so partial mesh loss or GR timeouts appear as structured neighbor-down bursts rather than buried lines inside verbose routing daemon logs.",
  "value": "Label distribution continuity protects VPN and Internet prefixes alike because ops teams detect dying TCP adjacencies before liberal label retention masks blackholes and before PIC edge cases strand stale LFIB entries on upstream hops.",
  "implementation": "Ensure LDP session change syslog reaches the network index, normalize IPv4/IPv6 router IDs into `neighbor`, alert when distinct peer loss exceeds threshold per PE within ten minutes, and enrich with NetBox CMDB roles.",
  "visualization": "Dashboard Studio: KPI for PEs with LDP downs in 24h; stacked bar of sessions by `host`; drilldown table (`host`, `peers`, `ifaces`, `count`) linked to raw events.",
  "references": [
    {
      "title": "Cisco IOS Configuration Guides — MPLS Layer 3 VPNs (LDP fundamentals)",
      "url": "https://www.cisco.com/c/en/us/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Juniper Junos MPLS User Guide — LDP Overview",
      "url": "https://www.juniper.net/documentation/us/en/software/junos/mpls/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "IETF RFC 5036 — LDP Specification",
      "url": "https://www.rfc-editor.org/rfc/rfc5036",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "mitreAttack": [
    "N/A (availability monitoring)"
  ],
  "detectionType": "Baseline",
  "securityDomain": "network",
  "equipment": [
    "cisco",
    "juniper",
    "snmp",
    "syslog"
  ],
  "equipmentModels": [
    "cisco_asr_1000",
    "cisco_asr_9000",
    "cisco_ios",
    "juniper_mx",
    "snmp_generic"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Accurate CMDB linking management IPs on loopbacks to device names forwarded as syslog `host`.\n• Baseline of expected LDP peers per platform (full mesh vs targeted).\n\nStep 1 — Logging posture\nCisco IOS-XE: confirm `%LDP-5-NBRCHG` or equivalent informational messages export to collectors; IOS-XR validate `mpls ldp neighbor` events via `logging discriminator`. Junos: ensure `system syslog user * any notice` captures `rpd` LDP messages without drowning in BGP noise—optionally filter facility in relay. Nokia SR OS: mirror router-id and peer events at `major` or higher per operations policy.\n\nStep 2 — Parsing\nAugment TA extractions with `rex` for neighbor router-id and optional GR reason codes; map interface names for targeted sessions.\n\nStep 3 — Saved search\n`ldp_neighbor_down_surge`: alert if `dc(peers)`≥2 per host in five minutes or single peer matches critical tier lookup `tier1_ldp_peers.csv`.\n\nStep 4 — CLI validation\nOn alarm: Cisco `show mpls ldp neighbor`; XR `show ldp neighbor`; Junos `show ldp neighbor`; Nokia `show router ldp session`; compare states and uptime to Splunk event timestamps.\n\nStep 5 — Runbook tie-in\nDashboard drilldown shows raw messages and suggested bounce/recovery steps; post-mortems attach Splunk search URLs as incident evidence when GR timers masked flap bursts.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Cisco Networks Add-on for Splunk (`TA-cisco_ios`), Splunk Add-on for Juniper (`Splunk_TA_juniper`), Nokia SR OS syslog integrations with supplemental FIELDALIAS for neighbor router-id. — Splunk Add-on for Cisco Security (Splunkbase 1352)). Enable the modular input or HEC token that writes sourcetype=see SPL into network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, is_ldp, st`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We keep an eye on the handshake lines between our big routers that share sticker labels for packets. When those friendships drop, we shout early so nobody ships mail down a hallway that suddenly lost its name tags.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
