{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.14.16",
  "title": "Varnish Workspace Client Overflow Detection",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Fault",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Varnish Workspace Client Overflow Detection' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_varnish_workspace_client_overflow_detection` (UC-5.14.16), SOC dashboard panel \"Operations — Varnish Workspace Client Overflow Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.14.16 positive (Varnish, Workspace, Client, Overflow, Detection): ingest synthetic events into `index=proxy` sourcetype='varnish:log' where `count` grouped by `host` satisfies `count` >= 1 for 'Varnish Workspace Client Overflow Detection'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.14.16 negative (Overflow Detection): ingest benign `proxy` / `varnish:log` events where `host` stays below `count` >= 1, or exclude the test entity via `ops_exception_register.csv`. Variant 6 confirms zero alert rows for 'Varnish Workspace Client Overflow Detection'."
  },
  "dataSources": "`index=proxy` `sourcetype=varnish:log` stderr/syslog workspace errors Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=proxy sourcetype=\"varnish:log\"\n| regex _raw=\"(?i)workspace.*overflow|WS.*overflow\"\n| stats count by host\n| where count >= 1",
  "description": "Detects Overflow Detection anomalies for UC-5.14.16 with window `the scheduled window` on `index=proxy` sourcetype `varnish:log`. Combines `count` by `host` and compares against `count` >= 1. Mapped to keywords Varnish, Workspace, Client, Overflow, Detection for dashboard routing.",
  "value": "Alerting on Varnish, Workspace, Client, Overflow, Detection through UC-5.14.16 reduces operational risk and delayed incident response by surfacing `activity` early.",
  "implementation": "Raise `workspace_client` and hunt oversized cookies or headers.",
  "visualization": "Time series for rates and latency, top/dedup for hotspots, single-value alerts on health thresholds.",
  "cimModels": [],
  "references": [
    {
      "title": "Vendor documentation",
      "url": "https://varnish-cache.org/docs/trunk/reference/varnishd.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 833",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `proxy` (UC-5.14.16)** — Planned work for 'Varnish Workspace Client Overflow Detection' shifts `count` over `span=1h` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `varnish:log`** — Scheduled automation elevates `src` without crossing `count` >= 1 for UC-5.14.16. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `proxy` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `varnish:log`** — First 48h baseline for UC-5.14.16 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "syslog",
    "varnish"
  ],
  "equipmentModels": [
    "syslog_generic",
    "varnish_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "Prerequisites\n* Varnish statistics for workspace memory. Key counters: `MAIN.ws_client_overflow`, `MAIN.ws_backend_overflow`, `MAIN.ws_thread_overflow`. Data in `index=proxy` with `sourcetype=varnish:stats`.\n* Workspaces: Varnish allocates fixed-size memory buffers per request (client workspace) and per backend request (backend workspace). When a request's headers or processing data exceed the workspace size, Varnish returns a 500 error. Common causes: very long URLs, many cookies, large headers.\n\nStep 1 -- Configure data collection\nSame as UC-5.14.11. Verify:\n```spl\nindex=proxy sourcetype=\"varnish:stats\" earliest=-4h\n| spath input=_raw path=\"MAIN.ws_client_overflow.value\" output=client_overflow\n| stats latest(client_overflow) as overflows\n```\n\nStep 2 -- Create the search and alert\n\n**Primary search -- Workspace overflow detection:**\n```spl\nindex=proxy sourcetype=\"varnish:stats\" earliest=-4h\n| spath input=_raw path=\"MAIN.ws_client_overflow.value\" output=client_overflow\n| spath input=_raw path=\"MAIN.ws_backend_overflow.value\" output=backend_overflow\n| spath input=_raw path=\"MAIN.ws_thread_overflow.value\" output=thread_overflow\n| eval client_overflow=tonumber(client_overflow), backend_overflow=tonumber(backend_overflow), thread_overflow=tonumber(thread_overflow)\n| bin _time span=5m\n| stats latest(client_overflow) as client_of latest(backend_overflow) as backend_of latest(thread_overflow) as thread_of by _time\n| streamstats current=f last(client_of) as prev_client last(backend_of) as prev_backend last(thread_of) as prev_thread\n| eval client_rate=client_of - prev_client\n| eval backend_rate=backend_of - prev_backend\n| eval thread_rate=thread_of - prev_thread\n| eval total_rate=client_rate + backend_rate + thread_rate\n| where total_rate > 0\n| eval severity=case(client_rate > 100, \"CRITICAL -- heavy client workspace overflows\", backend_rate > 100, \"CRITICAL -- heavy backend workspace overflows\", total_rate > 10, \"HIGH -- workspace overflows occurring\", 1==1, \"WARNING\")\n| table _time, client_rate, backend_rate, thread_rate, severity\n```\n\nStep 3 -- Validate\n(a) `varnishstat -1 | grep ws_.*overflow`.\n(b) Send a request with very large headers: `curl -H \"X-Long: $(python3 -c 'print(\"A\"*100000)')\" http://<varnish>/` -- should trigger client workspace overflow.\n(c) Check current workspace sizes: `varnishadm param.show workspace_client` and `workspace_backend`.\n\nStep 4 -- Operationalize\nDashboard (\"Varnish -- Workspace Health\"):\n* Row 1 -- Single-value: \"Client overflows (/5m)\", \"Backend overflows (/5m)\".\n* Row 2 -- Overflow rate timechart.\n\nAlerting:\n* Critical (overflow rate > 100/5m): significant request failures.\n* Warning (any overflow): investigate affected requests.\n\nStep 5 -- Troubleshooting\n\n* **Client workspace overflow** -- Request headers too large. Increase: `varnishadm param.set workspace_client 128k` (default 64k). Investigate what's sending large headers (analytics cookies, JWT tokens in headers).\n\n* **Backend workspace overflow** -- Backend response headers too large. Increase: `varnishadm param.set workspace_backend 128k`. Common cause: backends setting many `Set-Cookie` headers.\n\n* **After increasing workspace, memory usage grows** -- Each workspace is allocated per thread. More workspace per thread = more total memory. Calculate: threads * workspace_size. Ensure server has capacity.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes — Splunk Add-on for Unix and Linux (Splunkbase 833)). Enable the modular input or HEC token that writes sourcetype=varnish:log into proxy. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch varnish workspace client overflow detection and catch issues early, before they turn into outages for the people who rely on the network.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
