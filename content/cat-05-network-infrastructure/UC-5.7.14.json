{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.7.14",
  "title": "Geo-IP Flow Enrichment and Unexpected Country Detection",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Compliance",
    "Risk"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Geo-IP Flow Enrichment and Unexpected Country Detection' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_geo_ip_flow_enrichment_and_unexpected_country_de` (UC-5.7.14), SOC dashboard panel \"Operations — Geo-IP Flow Enrichment and Unexpected Country Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.7.14 positive (Geo, Flow, Enrichment, and, Unexpected): ingest synthetic events into `index=netflow` sourcetype='target_sourcetype' where `bytes`, `internal_hosts`, `external_ips` grouped by `dest` satisfies -4h for 'Geo-IP Flow Enrichment and Unexpected Country Detection'. Validate lookup `expected_peer_countries.csv`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.7.14 negative (Country Detection): ingest benign `netflow` / `target_sourcetype` events where `dest` stays below -4h, or exclude the test entity via `ops_exception_register.csv`. Variant 12 confirms zero alert rows for 'Geo-IP Flow Enrichment and Unexpected Country Detection'."
  },
  "dataSources": "`index=netflow` sourcetype `netflow` or `stream:ipfix` with external-facing `dest` (or `dest_ip`) for outbound sessions; post-ingest enrichment via props transforms calling MaxMind–style lookups or inline `iplocation` at search time for fields `Country`, `City`, `lat`, `lon`.",
  "app": "Splunk Add-on for NetFlow (Splunkbase 1838); Splunk Premium Lookup — MaxMind or bundled geography collections",
  "spl": "index=netflow earliest=-4h NOT (cidrmatch(\"10.0.0.0/8\", dest) OR cidrmatch(\"192.168.0.0/16\", dest) OR cidrmatch(\"172.16.0.0/12\", dest))\n| iplocation prefix=geo_ dest\n| lookup expected_peer_countries.csv country AS geo_Country OUTPUT allowed\n| where isnull(allowed)\n| stats sum(bytes) as bytes dc(src) as internal_hosts dc(dest) as external_ips values(geo_Country) as countries values(geo_City) as cities\n  by dest\n| eval mb=round(bytes/1048576,2)\n| sort -bytes\n| head 50",
  "description": "Alerts on UC-5.7.14 — Geo, Flow, Enrichment, and, Unexpected — using `index=netflow` sourcetype `configured sourcetype`, grouped by `dest`. Outputs `bytes`, `internal_hosts`, `external_ips` each `the scheduled window`; alert when -4h. Filter chain includes lookup `expected_peer_countries.csv`. Purpose-built for 'Geo-IP Flow Enrichment and Unexpected Country Detection' in Operations workflows.",
  "value": "Tracking `bytes`, `internal_hosts`, `external_ips` via UC-5.7.14 on lookup `expected_peer_countries.csv` improves response to 'Geo-IP Flow Enrichment and Unexpected Country Detection' and reduces operational risk and delayed incident response.",
  "implementation": "Schedule recurring geography database updates; maintain `expected_peer_countries.csv`; filter RFC1918 and carrier-grade Network Address Translation ranges; tune byte and host-count floors.",
  "visualization": "Map visualization of weighted points; table of country, bytes, internal_hosts; pie chart of bytes by continent.",
  "references": [
    {
      "title": "Splunk Documentation — iplocation command",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/Iplocation",
      "retrieved": "2026-04-25"
    },
    {
      "title": "MaxMind GeoIP documentation",
      "url": "https://dev.maxmind.com/geoip/docs/databases",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `netflow` (UC-5.7.14)** — Planned work for 'Geo-IP Flow Enrichment and Unexpected Country Detection' shifts `bytes`, `internal_hosts`, `external_ips` over `span=1h` while `dest` remains in-scope. Applies lookup `expected_peer_countries.csv`. Cross-check CMDB CHG.\n\n2. **Batch `sum` on `configured_sourcetype`** — Scheduled automation elevates `mb` without crossing configured threshold for UC-5.7.14. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `dest`** — Health checks in `netflow` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-5.7.14 on `dest` is unstable; join `expected_peer_countries.csv` prior to threshold.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `dest`, `exception_reason`, `valid_until`.",
  "equipment": [
    "netflow"
  ],
  "equipmentModels": [
    "netflow_ipfix",
    "netflow_netflow",
    "netflow_sflow"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Legal-approved list of countries permitted for routine business traffic; separate lists for sensitive subnets.\n• Accurate MaxMind or equivalent database mapped through `transforms.conf` as automatic lookups or invoked explicitly.\n• Consensus whether to evaluate source geography for inbound denial-of-service contexts—this UC focuses on outbound discovery.\n\nStep 1 — Configure data collection\nPrefer indexer-time enrichment for volume efficiency; if search-time `iplocation` is used, restrict summaries with transforms or summaries-only accelerated searches.\n\nStep 2 — Create the search\nExtend SPL with `| lookup sensitive_src_segments.csv src OUTPUT tier` and multiply thresholds when tier equals restricted. Add threat-intelligence joins sparingly to avoid cartesian explosion.\n\nStep 3 — Validate\nPick three known Software-as-a-Service endpoints across approved regions and confirm absence from alerts. Synthetic tests via authorized cloud shells should appear once then be allowlisted.\n\nStep 4 — Operationalize\nDashboard tabs split by business unit using asset lookups; automated ticketing includes geo_city and autonomous-system summaries when available.\n\nStep 5 — Troubleshooting\nCarrier proxies and virtual private network egress nodes skew geography—maintain `vpn_exit_ips.csv`. Satellite offices may route through unexpected countries legitimately; annotate lookups with effective dates.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for NetFlow (Splunkbase 1838); Splunk Premium Lookup — MaxMind or bundled geography collections). Enable the modular input or HEC token that writes sourcetype=see SPL into netflow. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`allowed, country, dest`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We tag outside destinations with where in the world they sit and compare that to the places we normally do business. When something lands outside that familiar list, we look closer without reading every line ourselves.",
  "prerequisiteUseCases": [],
  "wave": "walk"
}
