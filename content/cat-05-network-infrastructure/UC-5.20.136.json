{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.20.136",
  "title": "IPv6 NDP Redirect Message Abuse Detection",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'IPv6 NDP Redirect Message Abuse Detection'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-5.20.136).",
  "evidence": "Saved search uc_5_20_136_ipv6_ndp_redirect_message_abuse_detectio, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'IPv6 NDP Redirect Message Abuse Detection'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-5.20.136.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "`index=network sourcetype=cisco:ios` or `sourcetype=zeek:conn` — Detects abuse of ICMPv6 Redirect messages (type 137). Redirect messages tell a host to use a different next-hop for a specific destination. An attacker can forge redirects to perform man-in-the-middle attacks, redirecting a victim's traffic through the attacker's machine. Collected via Splunk Add-on for Cisco Security (Splunkbase 1352) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Enterprise Security — Splunk Add-on for Cisco Security (Splunkbase 1352)",
  "spl": "index=network (sourcetype=\"cisco:ios\" OR sourcetype=\"zeek:conn\" OR sourcetype=\"suricata:alert\") earliest=-24h\n  (\"redirect\" AND (\"ICMPv6\" OR \"icmpv6\" OR \"type.*137\"))\n| rex field=_raw \"(?:target|new.?hop)\\s*(?<redirect_target>[0-9a-fA-F:]+)\"\n| rex field=_raw \"(?:src|from)\\s*(?<redirect_source>[0-9a-fA-F:]+)\"\n| stats count as redirects dc(redirect_target) as unique_targets by host, redirect_source\n| eval severity=case(\n    redirects > 50, \"HIGH — excessive ICMPv6 redirects from \" . redirect_source . \" — possible MITM\",\n    unique_targets > 10, \"HIGH — redirects for many targets from single source — systematic attack\",\n    redirects > 10, \"MEDIUM — elevated redirect rate from \" . redirect_source,\n    1=1, null())\n| where isnotnull(severity)\n| sort -redirects",
  "description": "Detects abuse of ICMPv6 Redirect messages (type 137) used for man-in-the-middle attacks. An attacker forges redirect messages to reroute a victim's traffic through the attacker's machine. Legitimate redirects are rare in modern networks — most can be disabled without impact.",
  "value": "ICMPv6 redirects are a classic MITM vector. Because redirects change a host's routing table entry for a specific destination, they allow surgical traffic interception. Unlike RA spoofing (which affects all traffic), redirect attacks target specific flows. Most enterprise networks should disable redirect acceptance on hosts and filter redirects at switches.",
  "implementation": "Monitor for ICMPv6 type 137 messages. Alert on high redirect rates or redirects from unauthorized sources.",
  "visualization": "(1) Table: redirect sources and targets. (2) Single-value: redirect count. (3) Timeline: redirect events.",
  "references": [
    {
      "title": "RFC 4861 — Neighbor Discovery for IPv6 (§8 — Redirect)",
      "url": "https://www.rfc-editor.org/rfc/rfc4861#section-8",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RFC 4890 — Recommendations for Filtering ICMPv6 Messages (redirect is MAY DROP)",
      "url": "https://www.rfc-editor.org/rfc/rfc4890",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "cisco",
    "suricata",
    "zeek"
  ],
  "equipmentModels": [
    "cisco_catalyst_9000",
    "cisco_ios",
    "suricata_ids",
    "zeek_sensor"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Network sensor or router logging for ICMPv6.\n\nStep 1 — Disable redirect acceptance on hosts:\nLinux: `sysctl -w net.ipv6.conf.all.accept_redirects=0`\nWindows: `netsh interface ipv6 set global icmpredirects=disabled`\n\nStep 2 — Monitor for redirect messages on the network.\n\nStep 3 — Validate: Attempt to send a forged redirect. Verify detection fires.\n\nStep 4 — Operationalize\n**Dashboard:** ICMPv6 redirect monitoring. **Alert:** Redirects from unauthorized source — high.\n\nStep 5 — Troubleshooting\n• Disable redirect acceptance on all endpoints to eliminate the attack vector entirely.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Enterprise Security — Splunk Add-on for Cisco Security (Splunkbase 1352)). Enable the modular input or HEC token that writes sourcetype=cisco:ios, zeek:conn, suricata:alert into network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, redirect_source, redirects, severity`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Normally, the post office tells you 'there's a closer mailbox for that destination.' But a bad actor can forge these redirect notices to make your letters go through their hands first. We watch for suspicious redirect notices, especially from people who aren't real post office workers.",
  "prerequisiteUseCases": [
    "UC-5.20.21",
    "UC-5.20.131"
  ],
  "wave": "walk"
}
