{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.14.45",
  "title": "Traefik Middleware Time Share of Request Duration",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=proxy` `sourcetype=traefik:access` JSON access (Traefik v2+ fields)",
  "app": "Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes",
  "spl": "index=proxy sourcetype=\"traefik:access\"\n| eval mw=tonumber(MiddlewareDuration), total=tonumber(Duration)\n| eval mw_pct=if(total>0, round(100*mw/total,1), null())\n| where mw_pct > 25\n| timechart span=5m perc95(mw_pct) by RouterName",
  "description": "Auth and compression should not dominate latency budgets.",
  "value": "Guides middleware ordering and tuning.",
  "implementation": "If fields absent, export OpenTelemetry traces with span attributes for middleware.",
  "visualization": "Time series for rates and latency, top/dedup for hotspots, single-value alerts on health thresholds.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Vendor documentation",
      "url": "https://doc.traefik.io/traefik/observability/access-logs/"
    }
  ],
  "equipment": [
    "opentelemetry",
    "syslog",
    "traefik"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "grandmaExplanation": "We watch traefik middleware time share of request duration and catch issues early, before they turn into outages for the people who rely on the network.",
  "knownFalsePositives": "Edge cases: multi-vendor mixes and ad hoc feeds can include lab traffic and partial visibility that mimics issues until scopes are defined. Tuning tip: match this to «Traefik Middleware Time Share of Request Duration» and exclude change windows, scans, and lab VLANs you already expect."
}
