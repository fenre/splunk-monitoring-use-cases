{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.3.35",
  "title": "Citrix ADC AAA Audit Trail and Command Logging",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Citrix ADC AAA Audit Trail and Command Logging' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_citrix_adc_aaa_audit_trail_and_command_logging` (UC-5.3.35), SOC dashboard panel \"Operations — Citrix ADC AAA Audit Trail and Command Logging\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=netscaler` sourcetype='citrix:netscaler:syslog' that satisfy the UC-5.3.35 SPL filters for 'Citrix ADC AAA Audit Trail and Command Logging'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `netscaler` / `citrix:netscaler:syslog` events outside the detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-5.3.35."
  },
  "dataSources": "`index=netscaler` `sourcetype=\"citrix:netscaler:syslog\"` — AAA, audit, `NITRO` or `API` command, `nsconfig` save, `set`, `add`, `rm` actions, `CLI`, `GUI` login, admin usernames, source IP Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Add-on for Citrix NetScaler (Splunk_TA_citrix-netscaler) — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=netscaler sourcetype=\"citrix:netscaler:syslog\" (\"audit\" OR NITRO OR \"nsconfig\" OR \"cmd\" OR \"set \" OR \"add \" OR \"rm \" OR \"save ns config\" OR \"Command\" OR \"local\" OR API)\n| eval admin=coalesce(adc_user, admin_user, user, \"unknown\")\n| eval action=if(match(_raw, \"(?i)save[\\\\s_]+ns[\\\\s_]+config\"),\"config_save\",if(match(_raw, \"(?i)NITRO|Rest|API|HTTP\"),\"api\",\"cli_gui\"))\n| bin _time span=1h\n| stats count as cmds, values(action) as actions, dc(_raw) as unique_patterns by _time, host, admin\n| where cmds>0\n| sort - cmds\n| table _time, host, admin, actions, unique_patterns, cmds",
  "description": "Administrative changes on a Citrix ADC (CLI, GUI, and NITRO API) are security- and compliance-relevant. Retaining a tamper-resistant audit trail of who did what, when, from where—and whether configuration was saved—supports investigations, break-glass reviews, and control frameworks that expect full accountability for network edge devices.",
  "value": "Security and compliance teams track Citrix ADC administrative command execution with risk classification, detecting destructive operations, unauthorized changes, and failed admin login attempts.",
  "implementation": "Enable audit logging, command logging, and API access logging on the ADC; ensure administrators cannot disable logging without a separate control. Forward to `index=netscaler` with role-based read restrictions in Splunk. Retention per policy. Consider streaming critical commands to a write-once store. Alert on new admin accounts, off-hours `save ns config` bursts, or API keys used from new subnets.",
  "visualization": "Table: recent high-risk commands, user timeline, count of `save` events per day, map of source IP (if approved).",
  "cimModels": [],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Authentication.Authentication\n  by Authentication.user Authentication.action Authentication.src Authentication.app span=1h\n| sort -count",
  "dataModelAcceleration": "Enable acceleration on the Authentication data model, dataset Authentication, before using this tstats search.",
  "references": [
    {
      "title": "Splunk Documentation: Splunk Add-on for Citrix NetScaler",
      "url": "https://docs.splunk.com/Documentation/AddOns/released/CitrixNetScaler/CitrixNetScaler",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Authentication",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Authentication",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Citrix ADC — Auditing and logging",
      "url": "https://docs.citrix.com/en-us/citrix-adc/current-release/system/audit-logging.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'Citrix ADC AAA Audit Trail and Command Logging'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `netscaler` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `ops_exception_register.csv` with entity, reason, valid_until.",
  "mitreAttack": [
    "T1078",
    "T1098"
  ],
  "equipment": [
    "citrix",
    "syslog"
  ],
  "equipmentModels": [
    "citrix_netscaler"
  ],
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n* Splunk Add-on for Citrix NetScaler (`Splunk_TA_citrix-netscaler`, Splunkbase 2770). Citrix ADC audit/command logs. Key fields: `username`, `command`, `source_ip`, `timestamp`, `result` (SUCCESS/FAILURE), `session_id`.\n* AAA audit trail tracks: (1) all CLI commands executed (nsconfig, add/remove/set), (2) GUI actions, (3) API calls, (4) admin login/logout. Essential for: change management, compliance, security investigation, troubleshooting config changes that caused issues.\n\nStep 1 -- Configure data collection\nEnable command logging:\n```\nset audit syslogParams -logLevel ALL\nadd audit syslogAction splunk_audit <splunk_ip> -logLevel ALL -loglevel ALL\nadd audit syslogPolicy splunk_audit_policy ns_true splunk_audit\nbind system global splunk_audit_policy -priority 1\n```\nVerify:\n```spl\nindex=netscaler sourcetype=\"citrix:netscaler:syslog\" (\"CMD_EXECUTED\" OR \"LOGIN\" OR \"LOGOUT\" OR \"nsconfig\" OR \"save config\") earliest=-24h\n| stats count by host\n```\n\nStep 2 -- Create the search and alert\n\n**Primary search -- Admin activity audit trail:**\n```spl\nindex=netscaler sourcetype=\"citrix:netscaler:syslog\" (\"CMD_EXECUTED\" OR \"command\" OR \"LOGIN\" OR \"LOGOUT\" OR \"nsconfig\") earliest=-24h\n| eval user=coalesce(username, user, admin_user)\n| eval cmd=coalesce(command, cmd)\n| eval src=coalesce(source_ip, client_ip)\n| eval cmd_risk=case(match(cmd, \"(?i)(rm |remove |unbind |clear |reset )\"), \"HIGH_RISK -- destructive command\", match(cmd, \"(?i)(set |add |bind ).*ssl\"), \"MEDIUM -- SSL config change\", match(cmd, \"(?i)(set |add |bind ).*lb\"), \"MEDIUM -- LB config change\", match(cmd, \"(?i)save config\"), \"INFO -- config saved\", match(cmd, \"(?i)(login|logout)\"), \"INFO -- session event\", 1==1, \"LOW\")\n| stats count as commands dc(cmd) as unique_cmds values(cmd_risk) as risk_levels latest(cmd) as last_cmd by user, src\n| where match(mvjoin(risk_levels, \",\"), \"HIGH_RISK|MEDIUM\")\n| sort -commands\n```\n\n**Unauthorized access detection:**\n```spl\nindex=netscaler sourcetype=\"citrix:netscaler:syslog\" (\"LOGIN\" AND (\"fail\" OR \"invalid\" OR \"denied\")) earliest=-4h\n| eval user=coalesce(username, user)\n| eval src=coalesce(source_ip, client_ip)\n| stats count as failures dc(src) as source_ips by user\n| where failures > 3\n| sort -failures\n```\n\nStep 3 -- Validate\n(a) Execute a command on the ADC and verify it appears in Splunk with user context.\n(b) Attempt login with wrong credentials and verify the failure is logged.\n(c) Verify that all admin users appear in the audit trail.\n\nStep 4 -- Operationalize\nDashboard (\"Citrix ADC -- Audit Trail\"):\n* Row 1 -- Single-value: \"Admin commands (24h)\", \"Unique admins\", \"High-risk commands\", \"Failed logins\".\n* Row 2 -- Admin activity table with risk classification.\n* Row 3 -- Failed login attempts.\n\nAlerting:\n* High (destructive command on prod ADC outside change window): unauthorized change.\n* Warning (> 3 failed admin logins from same source): brute force attempt.\n\nStep 5 -- Troubleshooting\n\n* **No audit events** -- Check: (1) audit syslog policy is bound to system global, (2) syslog action destination is correct, (3) log level includes ALL.\n\n* **Command detail truncated** -- Long commands may be truncated in syslog. Increase message length: `set audit syslogParams -maxLogDataSizeToHold 4096`.\n\n* **Correlating config change with outage** -- Use the audit trail timestamp to identify which command was executed before an outage. Common culprits: `save config` after `unbind` or `remove` commands.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for Citrix NetScaler (Splunk_TA_citrix-netscaler) — Splunk Add-on for Unix and Linux (Splunkbase 833)). Enable the modular input or HEC token that writes sourcetype=citrix:netscaler:syslog into netscaler. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, action, admin, cmds`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We keep an audit line for shell and high-privilege work on the same box so who touched what, and when, is plain in one trail.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
