{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.1.45",
  "title": "Switch CPU and Memory Utilization (Meraki MS)",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Switch CPU and Memory Utilization (Meraki MS)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_switch_cpu_and_memory_utilization_meraki_ms` (UC-5.1.45), SOC dashboard panel \"Operations — Switch CPU and Memory Utilization (Meraki MS)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=meraki` sourcetype='meraki:assurancealerts' where `device_serial` satisfies `alert_count` > 0 for UC-5.1.45 ('Switch CPU and Memory Utilization (Meraki MS)'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `meraki` / `meraki:assurancealerts` events where `device_serial` stays below `alert_count` > 0, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-5.1.45."
  },
  "dataSources": "Splunk_TA_cisco_meraki (Splunkbase #5580): Assurance Alerts input (sourcetype=meraki:assurancealerts, hourly, OAuth scope dashboard:general:telemetry:read). The TA preserves Meraki's nested JSON paths: `deviceType` is the model-family code (`MS` for switches, NOT `switch`), device identity is `scope.devices{}.serial` / `scope.devices{}.name`, network identity is `network.name` / `network.id`, and `dismissedAt` carries the literal string `\"null\"` (not a Splunk NULL) when an alert is still open. Note: the Meraki Dashboard API does NOT expose per-switch CPU or memory counters via this TA. If you need raw CPU/memory telemetry, deploy SNMP polling (Splunk SNMP modular input) against the switch directly using IF-MIB / CISCO-PROCESS-MIB OIDs.",
  "app": "`Cisco Meraki Add-on for Splunk` (Splunkbase 5580)",
  "spl": "index=meraki sourcetype=\"meraki:assurancealerts\"\n    deviceType=\"MS\"\n    (categoryType=\"device\" OR categoryType=\"connectivity\")\n    earliest=-24h\n| rename scope.devices{}.serial as device_serial,\n         scope.devices{}.name as device_name,\n         network.name as network_name\n| stats count as alert_count,\n        values(title) as alert_titles,\n        latest(severity) as severity,\n        latest(dismissedAt) as dismissed_at\n         by device_serial, device_name, network_name\n| search dismissed_at=\"null\"\n| where alert_count > 0\n| sort - alert_count",
  "description": "Monitors switch hardware resources to prevent performance degradation or device failure. Searches `index=meraki` with sourcetype `meraki:assurancealerts` for UC-5.1.45, aggregating `values` by `device_serial` over `the scheduled window`. Alerts when grouped results cross `alert_count` > 0 in the saved search. Gives Operations operators a repeatable signal for 'Switch CPU and Memory Utilization (Meraki MS)' without manual log review.",
  "value": "Monitors switch hardware resources to prevent performance degradation or device failure. Timely UC-5.1.45 alerting on 'Switch CPU and Memory Utilization (Meraki MS)' reduces operational risk and delayed incident response before manual triage or audit channels escalate.",
  "implementation": "1. Enable the Assurance Alerts input in Splunk_TA_cisco_meraki. The TA polls GET /organizations/{orgId}/assurance/alerts hourly and emits one event per alert. The relevant fields are: `deviceType` (model code: MR/MS/MX/MV/MT/MG), `scope.devices{}.serial`, `scope.devices{}.name`, `network.name`, `network.id`, `categoryType`, `title`, `severity`, `dismissedAt`. 2. Filter to `deviceType=\"MS\"` for switch-specific issues — the API uses uppercase model codes, NOT the lowercase product type. Performance issues (high CPU, memory pressure, queue drops) surface as device alerts with categoryType=device or =connectivity. 3. `dismissedAt` carries the literal string `\"null\"` while the alert is open, so use `search dismissed_at=\"null\"` instead of `where isnull(dismissed_at)`. 4. For continuous CPU/memory graphs, supplement with SNMP polling against the switch's management IP using OIDs from CISCO-PROCESS-MIB (cpmCPUTotal5minRev) and CISCO-MEMORY-POOL-MIB — the Meraki TA does NOT carry these counters.",
  "visualization": "Gauge charts for CPU/memory; time-series trends; capacity planning dashboard.",
  "cimModels": [],
  "references": [
    {
      "title": "Splunkbase app 5580",
      "url": "https://splunkbase.splunk.com/app/5580",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Add-on for Cisco Meraki (Splunkbase 5631)",
      "url": "https://splunkbase.splunk.com/app/5631",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Cisco Security documentation",
      "url": "https://www.cisco.com/c/en/us/support/security/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `meraki`** — Planned work for 'Switch CPU and Memory Utilization (Meraki MS)' shifts `values` over `span=1h` while `device_serial` remains in-scope for UC-5.1.45. Cross-check CMDB CHG.\n\n2. **Batch `values` on `meraki:assurancealerts`** — Scheduled automation elevates `device_name` without crossing `alert_count` > 0. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `device_serial`** — Health checks in `meraki` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `meraki:assurancealerts`** — First 48h baseline for `device_serial` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `device_serial`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco",
    "snmp"
  ],
  "equipmentModels": [
    "cisco_meraki",
    "snmp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Cisco Meraki Add-on for Splunk` (Splunkbase 5580).\n• Ensure the following data sources are available: Splunk_TA_cisco_meraki (Splunkbase #5580): Assurance Alerts input (sourcetype=meraki:assurancealerts, hourly, OAuth scope dashboard:general:telemetry:read). The TA preserves Meraki's nested JSON paths: `deviceType` is the model-family code (`MS` for switches, NOT `switch`), device identity is `scope.devices{}.serial` / `scope.devices{}.name`, network identity is `network.name` / `network.id`, and `dismissedAt` carries the literal string `\"null\"` (not a Splunk NULL) when an alert is still open. Note: the Meraki Dashboard API does NOT expose per-switch CPU or memory counters via this TA. If you need raw CPU/memory telemetry, deploy SNMP polling (Splunk SNMP modular input) against the switch directly using IF-MIB / CISCO-PROCESS-MIB OIDs..\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\n1. Enable the Assurance Alerts input in Splunk_TA_cisco_meraki. The TA polls GET /organizations/{orgId}/assurance/alerts hourly and emits one event per alert. The relevant fields are: `deviceType` (model code: MR/MS/MX/MV/MT/MG), `scope.devices{}.serial`, `scope.devices{}.name`, `network.name`, `network.id`, `categoryType`, `title`, `severity`, `dismissedAt`. 2. Filter to `deviceType=\"MS\"` for switch-specific issues — the API uses uppercase model codes, NOT the lowercase product type. Performanc…\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=meraki sourcetype=\"meraki:assurancealerts\"\n    deviceType=\"MS\"\n    (categoryType=\"device\" OR categoryType=\"connectivity\")\n    earliest=-24h\n| rename scope.devices{}.serial as device_serial,\n         scope.devices{}.name as device_name,\n         network.name as network_name\n| stats count as alert_count,\n        values(title) as alert_titles,\n        latest(severity) as severity,\n        latest(dismissedAt) as dismissed_at\n         by device_serial, device_name, network_name\n| search dismissed_at=\"null\"\n| where alert_count > 0\n| sort - alert_count\n```\n\nUnderstanding this SPL\n\n**Switch CPU and Memory Utilization (Meraki MS)** — Operations teams monitor Meraki MS switch CPU and memory utilization, detecting resource exhaustion that affects management plane responsiveness and control plane processing.\n\nDocumented **Data sources**: Splunk_TA_cisco_meraki (Splunkbase #5580): Assurance Alerts input (sourcetype=meraki:assurancealerts, hourly, OAuth scope dashboard:general:telemetry:read). The TA preserves Meraki's nested JSON paths: `deviceType` is the model-family code (`MS` for switches, NOT `switch`), device identity is `scope.devices{}.serial` / `scope.devices{}.name`, network identity is `network.name` / `network.id`, and `dismissedAt` carries the literal string `\"null\"` (not a Splunk NULL) when an alert is still open. Note: the Meraki Dashboard API does NOT expose per-switch CPU or memory counters via this TA. If you need raw CPU/memory telemetry, deploy SNMP polling (Splunk SNMP modular input) against the switch directly using IF-MIB / CISCO-PROCESS-MIB OIDs. **App/TA** (typical add-on context): `Cisco Meraki Add-on for Splunk` (Splunkbase 5580). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: meraki; **sourcetype**: meraki:assurancealerts. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=meraki, sourcetype=\"meraki:assurancealerts\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Renames fields with `rename` for clarity or joins.\n• `stats` rolls up events into metrics; results are split **by device_serial, device_name, network_name** so each row reflects one combination of those dimensions (useful for per-host, per-user, or per-entity comparisons for this use case).\n• Applies an explicit `search` filter to narrow the current result set.\n• Filters the current rows with `where alert_count > 0` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Gauge charts for CPU/memory; time-series trends; capacity planning dashboard.",
  "grandmaExplanation": "We help you know early when something looks wrong with switch cpu and memory utilization so the team can act before it grows into a bigger outage.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
