{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.20.115",
  "title": "IPv6-to-IPv4 Protocol Translation (NAT64/SIIT) Health Monitoring",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Availability",
    "Performance"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers the detection logic documented for 'IPv6-to-IPv4 Protocol Translation (NAT64/SIIT) Health Monitoring' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_ipv6_to_ipv4_protocol_translation_nat64_siit_hea` (UC-5.20.115), SOC dashboard panel \"Operations — IPv6-to-IPv4 Protocol Translation (NAT64/SIIT) Health Monitoring\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=network` sourcetype='cisco:ios' where `host` satisfies `events` > 100 for UC-5.20.115 ('IPv6-to-IPv4 Protocol Translation (NAT64/SIIT) Health Monitoring'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `network` / `cisco:ios` events where `host` stays below `events` > 100, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-5.20.115."
  },
  "dataSources": "`index=network sourcetype=cisco:ios` or `sourcetype=cisco:iosxe` or `sourcetype=juniper:junos` — Monitors stateful NAT64 (RFC 6146) and stateless SIIT (RFC 7915) protocol translation gateways that enable IPv6-only clients to communicate with IPv4-only servers. These translators are critical infrastructure during the IPv6 transition — when they fail, IPv6-only clients lose access to all IPv4-only content.\n\n**NAT64/SIIT operational context:**\n\n1. **Stateful NAT64.** Maintains per-session state mapping between IPv6 and IPv4 addresses. Session table can exhaust under high traffic or DoS. Used with DNS64 for transparent operation.\n2. **Stateless NAT64 (SIIT).** 1:1 mapping between IPv6 and IPv4 addresses. No session state — deterministic translation. Used for servers that need stable addresses in both protocols.\n3. **DNS64.** Synthesises AAAA records for IPv4-only domains by embedding the IPv4 address in the well-known prefix 64:ff9b::/96. Essential companion to NAT64.\n4. **464XLAT (RFC 6877).** Client-side translation (CLAT) combined with NAT64 (PLAT) for applications that require literal IPv4 addresses. Used in mobile networks.\n5. **Session table exhaustion.** Stateful NAT64 has a finite session table. Under DDoS or high legitimate load, the table fills and new translations are dropped.\n6. **Translation failures.** Some protocols embed IP addresses in the payload (FTP, SIP). These require Application Layer Gateways (ALGs) that understand both IPv4 and IPv6. Collected via Splunk Add-on for Cisco Security (Splunkbase 1352) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Enterprise — Splunk Add-on for Cisco Security (Splunkbase 1352)",
  "spl": "index=network (sourcetype=\"cisco:ios\" OR sourcetype=\"cisco:iosxe\" OR sourcetype=\"juniper:junos\") earliest=-4h\n  (\"%NAT64\" OR \"%NAT-6\" OR \"nat64\" OR \"stateful64\" OR \"SIIT\" OR \"464XLAT\")\n| eval nat64_event=case(\n    match(_raw, \"(?i)session.*table.*full|translation.*limit|pool.*exhaust\"), \"SESSION_EXHAUSTION\",\n    match(_raw, \"(?i)translation.*fail|nat64.*drop|cannot.*translate\"), \"TRANSLATION_FAILURE\",\n    match(_raw, \"(?i)pool.*low|address.*running|threshold\"), \"POOL_LOW\",\n    match(_raw, \"(?i)session.*create|translation.*created\"), \"SESSION_CREATED\",\n    match(_raw, \"(?i)hairpin|v6v6\"), \"HAIRPIN\",\n    1=1, \"OTHER\")\n| stats count as events by host, nat64_event\n| eval severity=case(\n    nat64_event=\"SESSION_EXHAUSTION\", \"CRITICAL — NAT64 session table full — IPv6-only clients losing IPv4 access\",\n    nat64_event=\"TRANSLATION_FAILURE\" AND events > 100, \"HIGH — translation failures (\" . events . \") — protocol or ALG issue\",\n    nat64_event=\"POOL_LOW\", \"WARNING — NAT64 IPv4 address pool running low\",\n    nat64_event=\"HAIRPIN\" AND events > 50, \"MEDIUM — NAT64 hairpinning detected (performance impact)\",\n    1=1, null())\n| where isnotnull(severity)\n| sort -events",
  "description": "Monitors NAT64 (RFC 6146), SIIT (RFC 7915), and 464XLAT (RFC 6877) protocol translation gateways for session table exhaustion, translation failures, IPv4 address pool depletion, and performance degradation. These translators are critical infrastructure for IPv6-only networks that must communicate with IPv4-only services.",
  "value": "Monitors NAT64 (RFC 6146), SIIT (RFC 7915), and 464XLAT (RFC 6877) protocol translation gateways for sessio.... Timely UC-5.20.115 alerting on 'IPv6-to-IPv4 Protocol Translation (NAT64/SIIT) Health Monitoring' reduces operational risk and delayed incident response before manual triage or audit channels escalate.",
  "implementation": "Monitor NAT64 session table utilisation, translation success/failure rates, IPv4 pool utilisation, and DNS64 synthesis health. Alert on session exhaustion and translation failures.",
  "visualization": "(1) Gauge: NAT64 session table utilization. (2) Timechart: translations per second. (3) Single-value: translation failure rate. (4) Table: NAT64 devices with health status.",
  "references": [
    {
      "title": "RFC 6146 — Stateful NAT64: Network Address and Protocol Translation from IPv6 Clients to IPv4 Servers",
      "url": "https://www.rfc-editor.org/rfc/rfc6146",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RFC 6147 — DNS64: DNS Extensions for Network Address Translation from IPv6 Clients to IPv4 Servers",
      "url": "https://www.rfc-editor.org/rfc/rfc6147",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RFC 6877 — 464XLAT: Combination of Stateful and Stateless Translation",
      "url": "https://www.rfc-editor.org/rfc/rfc6877",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `network`** — Planned work for 'IPv6-to-IPv4 Protocol Translation (NAT64/SIIT) Health Monitoring' shifts `count` over `span=1h` while `host` remains in-scope for UC-5.20.115. Cross-check CMDB CHG.\n\n2. **Batch `count` on `cisco:ios`** — Scheduled automation elevates `nat64_event` without crossing `events` > 100. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `network` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `cisco:ios`** — First 48h baseline for `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco",
    "jool",
    "juniper"
  ],
  "equipmentModels": [
    "cisco_asr_1000",
    "cisco_catalyst_8000",
    "cisco_ios",
    "jool_nat64",
    "juniper_mx"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• NAT64 gateway deployed (Cisco ASR/C8000, Juniper MX, or Linux Jool).\n• DNS64 resolver configured.\n• NAT64 logging enabled.\n\nStep 1 — Configure NAT64 monitoring\n\n**Cisco IOS-XE NAT64 configuration and logging:**\n```\nnat64 prefix stateful 64:ff9b::/96\nnat64 v4 pool NAT64-POOL 203.0.113.0 203.0.113.255\nnat64 v6v4 static 2001:db8::1 203.0.113.1\n!\nnat64 translation timeout tcp 3600\nnat64 translation timeout udp 300\n!\nlogging buffered 8192 informational\n```\n\n**Session table monitoring (SNMP or CLI polling):**\n```\nshow nat64 statistics\nshow nat64 translations\n```\nPoll via scripted input and send to Splunk.\n\n**DNS64 configuration (BIND 9):**\n```\noptions {\n    dns64 64:ff9b::/96 {\n        clients { any; };\n        mapped { !rfc1918; any; };\n        suffix ::;\n    };\n};\n```\n\nStep 2 — Create monitoring searches\n\n**NAT64 session table utilization:**\n```spl\nindex=network (sourcetype=\"cisco:ios\" OR sourcetype=\"cisco:iosxe\") earliest=-1h\n  \"nat64\" AND (\"session\" OR \"translation\")\n| rex field=_raw \"active\\s+(?<active_sessions>\\d+)\"\n| rex field=_raw \"(?:max|limit)\\s+(?<max_sessions>\\d+)\"\n| eval utilization=round(tonumber(active_sessions) / tonumber(max_sessions) * 100, 1)\n| stats latest(utilization) as current_pct latest(active_sessions) as sessions by host\n| eval status=case(\n    current_pct > 90, \"CRITICAL — session table \" . current_pct . \"% full\",\n    current_pct > 75, \"WARNING — session table \" . current_pct . \"% full\",\n    1=1, \"OK — \" . current_pct . \"%\")\n```\n\n**DNS64 synthesis verification:**\n```spl\nindex=dns sourcetype=\"named:querylog\" earliest=-4h\n  \"dns64\" OR \"64:ff9b\"\n| eval synthesised=if(match(_raw, \"64:ff9b\"), 1, 0)\n| stats count as total sum(synthesised) as dns64_responses\n| eval dns64_pct=round(dns64_responses / total * 100, 1)\n| eval status=if(dns64_responses > 0, \"DNS64 active — \" . dns64_responses . \" synthesised responses\", \"DNS64 NOT WORKING — no synthesised responses\")\n```\n\nStep 3 — Validate\n(a) **End-to-end test.** From an IPv6-only client, access an IPv4-only website. Verify the connection succeeds via NAT64/DNS64.\n\n(b) **DNS64 test.** `dig AAAA ipv4only.arpa @<dns64-resolver>`. Should return `64:ff9b::c000:aa` (the well-known NAT64 test domain).\n\n(c) **Session table test.** Generate high connection volume through NAT64. Monitor session table growth and verify it doesn't exceed limits.\n\nStep 4 — Operationalize\n\n**Dashboard** (\"IPv6 — NAT64/DNS64 Health\"):\n• Row 1 — Gauges: NAT64 session table utilization per device.\n• Row 2 — Timechart: translations per second.\n• Row 3 — Single-values: translation failures, pool utilization.\n• Row 4 — DNS64 synthesis status.\n\n**Alert 1:** Session table >90% — critical.\n**Alert 2:** IPv4 pool utilization >90% — critical.\n**Alert 3:** Translation failures >100/hour — high.\n\nStep 5 — Troubleshooting\n\n• **Session table full.** Reduce TCP timeout (`nat64 translation timeout tcp 1800`). Enable session logging to identify top consumers. Consider adding more IPv4 pool addresses or deploying additional NAT64 instances.\n\n• **DNS64 not synthesising.** Verify the DNS64 prefix matches the NAT64 prefix. Check that `dns64` is enabled in BIND configuration. Test with `dig AAAA ipv4only.arpa`.\n\n• **Application failures through NAT64.** Some applications embed IPv4 addresses in payloads (FTP, SIP, RTSP). These require ALGs. If an ALG is not available, consider running the application in dual-stack mode or using 464XLAT on the client.",
  "grandmaExplanation": "When someone using only the new address system (IPv6) wants to talk to someone who only understands the old address system (IPv4), they need a translator in the middle — like an interpreter at the United Nations.",
  "prerequisiteUseCases": [
    "UC-5.20.49",
    "UC-5.20.53"
  ],
  "wave": "run"
}
