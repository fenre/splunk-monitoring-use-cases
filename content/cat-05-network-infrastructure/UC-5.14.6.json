{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.14.6",
  "title": "HAProxy HTTP Compression Ratio Effectiveness",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Cost"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'HAProxy HTTP Compression Ratio Effectiveness' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_haproxy_http_compression_ratio_effectiveness` (UC-5.14.6), SOC dashboard panel \"Operations — HAProxy HTTP Compression Ratio Effectiveness\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=proxy` sourcetype='haproxy:http' where `frontend` satisfies `cin` > 0 for UC-5.14.6 ('HAProxy HTTP Compression Ratio Effectiveness'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `proxy` / `haproxy:http` events where `frontend` stays below `cin` > 0, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-5.14.6."
  },
  "dataSources": "`index=proxy` `sourcetype=haproxy:http` with `comp_in`/`comp_out` from custom `log-format` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes — Universal Forwarder (Splunkbase 617)",
  "spl": "index=proxy sourcetype=\"haproxy:http\"\n| eval cin=tonumber(comp_in), cout=tonumber(comp_out)\n| eval ratio=if(cin>0, round(100*cout/cin,1), null())\n| timechart span=1h avg(ratio) by frontend",
  "description": "Compression reduces egress cost until misconfiguration disables it for large JSON. Searches `index=proxy` with sourcetype `haproxy:http` for UC-5.14.6, aggregating `avg` by `frontend` over `1h`. Alerts when grouped results cross `cin` > 0 in the saved search. Gives Operations operators a repeatable signal for 'HAProxy HTTP Compression Ratio Effectiveness' without manual log review.",
  "value": "Compression reduces egress cost until misconfiguration disables it for large JSON. Timely UC-5.14.6 alerting on 'HAProxy HTTP Compression Ratio Effectiveness' reduces operational risk and delayed incident response before manual triage or audit channels escalate.",
  "implementation": "Add compression byte fields per HAProxy docs; document field order for parsers.",
  "visualization": "Time series for rates and latency, top/dedup for hotspots, single-value alerts on health thresholds.",
  "cimModels": [],
  "references": [
    {
      "title": "Vendor documentation",
      "url": "https://docs.haproxy.org/2.8/configuration.html#compression",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `proxy`** — Planned work for 'HAProxy HTTP Compression Ratio Effectiveness' shifts `avg` over `span=1h` while `frontend` remains in-scope for UC-5.14.6. Cross-check CMDB CHG.\n\n2. **Batch `avg` on `haproxy:http`** — Scheduled automation elevates `cin` without crossing `cin` > 0. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `frontend`** — Health checks in `proxy` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `haproxy:http`** — First 48h baseline for `frontend` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `frontend`, `exception_reason`, `valid_until`.",
  "equipment": [
    "haproxy",
    "syslog"
  ],
  "equipmentModels": [
    "haproxy_generic",
    "syslog_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "Prerequisites\n* HAProxy HTTP logs with connection counts and queue depth. Key fields: `actconn` (active connections), `feconn` (frontend connections), `beconn` (backend connections), `srv_conn` (server connections), `srv_queue` (server queue depth), `backend_queue` (backend queue depth).\n* HAProxy connection limits: `maxconn` (global and per-server). When connections exceed maxconn, new requests are queued. Queue depth > 0 indicates saturation. If the queue fills, HAProxy returns 503.\n\nStep 1 -- Configure data collection\nLog format with connection counters:\n```\n# haproxy.cfg\ndefaults\n    log-format \"%ci:%cp [%t] %ft %b/%s %Tq/%Tw/%Tc/%Tr/%Tt %ST %B %CC %CS %tsc %ac/%fc/%bc/%sc/%rc %sq/%bq\"\n```\nThe `%ac/%fc/%bc/%sc/%rc` block = active/frontend/backend/server/retries connections. `%sq/%bq` = server queue/backend queue depth.\n\nVerify:\n```spl\nindex=proxy sourcetype=\"haproxy:http\" earliest=-4h\n| stats avg(actconn) as avg_active avg(srv_queue) as avg_queue by backend\n| sort -avg_active\n```\n\nStep 2 -- Create the search and alert\n\n**Primary search -- Connection saturation and queue buildup:**\n```spl\nindex=proxy sourcetype=\"haproxy:http\" earliest=-4h\n| eval active=tonumber(actconn)\n| eval be_conn=tonumber(beconn)\n| eval s_queue=tonumber(srv_queue)\n| eval b_queue=tonumber(backend_queue)\n| bin _time span=5m\n| stats avg(active) as avg_active max(active) as max_active avg(be_conn) as avg_backend_conn avg(s_queue) as avg_srv_queue max(s_queue) as max_srv_queue avg(b_queue) as avg_be_queue count as requests by _time, backend\n| eval queue_pct=round(100*avg_srv_queue/requests, 2)\n| eval saturation=case(max_srv_queue > 100, \"CRITICAL -- deep queue (max=\".max_srv_queue.\")\", avg_srv_queue > 10, \"HIGH -- sustained queuing\", avg_be_queue > 5, \"WARNING -- backend queue building\", 1==1, \"OK\")\n| where saturation != \"OK\"\n| table _time, backend, avg_active, avg_backend_conn, avg_srv_queue, max_srv_queue, saturation\n```\n\nStep 3 -- Validate\n(a) Check HAProxy stats page: `qcur` (current queued), `qlimit`, `scur` (current sessions).\n(b) HAProxy runtime API: `echo \"show stat\" | socat stdio /var/run/haproxy/admin.sock | cut -d, -f1,2,5,18,30,31` -- shows queue and session counters.\n(c) Load test with more connections than `maxconn` and verify queue growth.\n\nStep 4 -- Operationalize\nDashboard (\"HAProxy -- Connection Saturation\"):\n* Row 1 -- Single-value: \"Active connections\", \"Max queue depth\", \"Backends queuing\".\n* Row 2 -- Connection vs queue depth timechart.\n* Row 3 -- Per-backend saturation table.\n\nAlerting:\n* Critical (queue depth > 100 sustained): immediate capacity issue.\n* Warning (queue depth > 10 sustained): approaching capacity.\n\nStep 5 -- Troubleshooting\n\n* **Queue building on specific servers** -- Server's `maxconn` is too low or server is slow. Check: `maxconn` per server in haproxy.cfg. Also check if server's keepalive connections are exhausted.\n\n* **Queue on entire backend** -- All servers are saturated. Add more backend servers or increase capacity. Check if a single slow endpoint is causing all connections to pile up.\n\n* **High active connections but no queue** -- System is handling load but approaching limits. Monitor trend and plan capacity.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=haproxy:http into proxy. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`cin, frontend, ratio`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch haproxy http compression ratio effectiveness and catch issues early, before they turn into outages for the people who rely on the network.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
