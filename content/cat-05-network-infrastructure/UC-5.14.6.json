{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.14.6",
  "title": "HAProxy HTTP Compression Ratio Effectiveness",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Cost"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=proxy` `sourcetype=haproxy:http` with `comp_in`/`comp_out` from custom `log-format`",
  "app": "Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes",
  "spl": "index=proxy sourcetype=\"haproxy:http\"\n| eval cin=tonumber(comp_in), cout=tonumber(comp_out)\n| eval ratio=if(cin>0, round(100*cout/cin,1), null())\n| timechart span=1h avg(ratio) by frontend",
  "description": "Compression reduces egress cost until misconfiguration disables it for large JSON.",
  "value": "Quantifies CDN and origin offload ROI.",
  "implementation": "Add compression byte fields per HAProxy docs; document field order for parsers.",
  "visualization": "Time series for rates and latency, top/dedup for hotspots, single-value alerts on health thresholds.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Vendor documentation",
      "url": "https://docs.haproxy.org/2.8/configuration.html#compression"
    }
  ],
  "equipment": [
    "haproxy",
    "syslog"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "grandmaExplanation": "We verify compression saves bandwidth as expected."
}
