{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.14.42",
  "title": "Envoy ext_authz Latency and Denials",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=proxy` `sourcetype=envoy:access` JSON with duration and flags",
  "app": "Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes",
  "spl": "index=proxy sourcetype=\"envoy:access\"\n| eval dur=tonumber(duration_ms)\n| where response_code IN (401,403) OR match(response_flags, \"UAEX\")\n| timechart span=5m perc95(dur) as p95_ms count by cluster_name",
  "description": "Authz latency becomes user latency on every request.",
  "value": "Keeps zero-trust gates fast and observable.",
  "implementation": "Avoid logging tokens; use separate debug index for authz bodies.",
  "visualization": "Time series for rates and latency, top/dedup for hotspots, single-value alerts on health thresholds.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Vendor documentation",
      "url": "https://www.envoyproxy.io/docs/envoy/latest/configuration/http/http_filters/ext_authz_filter"
    }
  ],
  "equipment": [
    "envoy",
    "syslog"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "grandmaExplanation": "We watch envoy ext_authz latency and denials and catch issues early, before they turn into outages for the people who rely on the network.",
  "knownFalsePositives": "Edge cases: multi-vendor mixes and ad hoc feeds can include lab traffic and partial visibility that mimics issues until scopes are defined. Tuning tip: match this to «Envoy ext_authz Latency and Denials» and exclude change windows, scans, and lab VLANs you already expect."
}
