{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.9.14",
  "title": "DNS Resolution Time Trending",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'DNS Resolution Time Trending' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_dns_resolution_time_trending` (UC-5.9.14), SOC dashboard panel \"Operations — DNS Resolution Time Trending\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.9.14 positive (DNS, Resolution, Time, Trending): ingest synthetic events into `index=target_index` sourcetype='target_sourcetype' where `avg_dns_s`, `max_dns_s` grouped by `dns.question.name`, `server.address` satisfies `avg_dns_ms` > 200 for 'DNS Resolution Time Trending'. Validate macro `stream_index`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.9.14 negative (Time Trending): ingest benign `target_index` / `target_sourcetype` events where `dns.question.name`, `server.address` stays below `avg_dns_ms` > 200, or exclude the test entity via `ops_exception_register.csv`. Variant 11 confirms zero alert rows for 'DNS Resolution Time Trending'."
  },
  "dataSources": "`index=thousandeyes_metrics sourcetype=cisco:thousandeyes:metric` from the Tests Stream — Metrics input in `ta_cisco_thousandeyes` (Splunkbase 7719). The OTel v2 metric `dns.lookup.duration` reports the time to resolve the domain name in **seconds** (same unit convention as `network.latency`). A typical fast resolution is 0.005–0.020 seconds (5–20 ms). Values above 0.200 seconds (200 ms) indicate a problem. Resource attributes: `thousandeyes.test.type` (`dns-server`), `dns.question.name`, `server.address`, `thousandeyes.source.agent.name`.",
  "app": "`ta_cisco_thousandeyes` — Cisco ThousandEyes App for Splunk ([Splunkbase 7719](https://splunkbase.splunk.com/app/7719)), Cisco Supported.",
  "spl": "`stream_index` thousandeyes.test.type=\"dns-server\"\n| stats avg(dns.lookup.duration) as avg_dns_s max(dns.lookup.duration) as max_dns_s by dns.question.name, server.address\n| eval avg_dns_ms=round(avg_dns_s*1000,1), max_dns_ms=round(max_dns_s*1000,1)\n| where avg_dns_ms > 200\n| sort -avg_dns_ms",
  "description": "Identifies UC-5.9.14 — DNS, Resolution, Time, Trending — using `index=target_index` sourcetype `configured sourcetype`, grouped by `dns.question.name`, `server.address`. Outputs `avg_dns_s`, `max_dns_s` each `the scheduled window`; alert when `avg_dns_ms` > 200. Filter chain includes macro `stream_index`. Purpose-built for 'DNS Resolution Time Trending' in Operations workflows.",
  "value": "UC-5.9.14 helps teams contain DNS, Resolution, Time, Trending scenarios and reduce operational risk and delayed incident response before incidents compound.",
  "implementation": "Uses the same DNS Server test data as UC-5.9.13. No additional test configuration needed. The `dns.lookup.duration` metric is reported alongside `dns.lookup.availability` in the same events. Schedule as a dashboard trending panel with an alert for sustained resolution times above 200 ms.",
  "visualization": "(1) Timechart: `| timechart span=5m avg(dns.lookup.duration) as avg_dns_s by dns.question.name | eval avg_dns_ms=round(avg_dns_s*1000,1)` — shows resolution time trends per domain. Step-changes indicate DNS infrastructure events. (2) Table: domain, server, avg resolution (ms), max resolution (ms) — sorted worst-first. (3) Single value: count of domains with avg resolution > 200 ms (yellow) or > 500 ms (red). (4) Comparison bar chart: resolution time by DNS server for the same domain (compare your internal resolver vs Cloudflare vs Google — see UC-5.9.16).",
  "references": [
    {
      "title": "Cisco ThousandEyes App for Splunk (Splunkbase 7719)",
      "url": "https://splunkbase.splunk.com/app/7719",
      "retrieved": "2026-04-25"
    },
    {
      "title": "ThousandEyes DNS Server Test — Resolution time metrics",
      "url": "https://docs.thousandeyes.com/product-documentation/internet-and-wan-monitoring/tests/dns-tests/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Google Web Performance — DNS lookup impact on page load time",
      "url": "https://web.dev/articles/performance-http2",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `target_index` (UC-5.9.14)** — Planned work for 'DNS Resolution Time Trending' shifts `avg_dns_s`, `max_dns_s` over `span=1h` while `dns.question.name`, `server.address` remains in-scope. Applies macro `stream_index`. Cross-check CMDB CHG.\n\n2. **Batch `avg` on `configured_sourcetype`** — Scheduled automation elevates `server.address` without crossing `avg_dns_ms` > 200 for UC-5.9.14. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `dns.question.name`, `server.address`** — Health checks in `target_index` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-5.9.14 on `dns.question.name` is unstable; join `stream_index` prior to threshold.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `dns.question.name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_thousandeyes"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-02",
  "splunkVersions": [
    "9.1+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• All prerequisites from UC-5.9.13 apply — DNS Server tests configured, Tests Stream enabled, DNS data flowing.\n• **Resolution time baselines:** Establish expected resolution times per domain × server combination over 7 days before setting thresholds. Cached lookups: 1–5 ms. Uncached lookups to local authoritative: 10–50 ms. Uncached lookups through recursive chain: 50–200 ms. Anything above 200 ms sustained is almost always a problem.\n\nStep 1 — Configure data collection\nSame as UC-5.9.13. The `dns.lookup.duration` metric is reported in the same events as `dns.lookup.availability`.\n\nVerify:\n```spl\nindex=thousandeyes_metrics thousandeyes.test.type=\"dns-server\" earliest=-30m\n| stats avg(dns.lookup.duration) as avg_dur by dns.question.name, server.address\n| eval avg_dur_ms = round(avg_dur * 1000, 1)\n```\n\nStep 2 — Create the search and alert\n**Trending search (dashboard):**\n```spl\n`stream_index` thousandeyes.test.type=\"dns-server\"\n| timechart span=5m avg(dns.lookup.duration) as avg_dns_s by dns.question.name\n| foreach avg_dns_s* [eval <<FIELD>>=round(<<FIELD>>*1000,1)]\n```\n\n**Alert search:**\n```spl\n`stream_index` thousandeyes.test.type=\"dns-server\"\n| stats avg(dns.lookup.duration) as avg_dns_s max(dns.lookup.duration) as max_dns_s by dns.question.name, server.address\n| eval avg_dns_ms=round(avg_dns_s*1000,1), max_dns_ms=round(max_dns_s*1000,1)\n| where avg_dns_ms > 200\n| sort -avg_dns_ms\n```\n\n**Understanding this SPL**\n\n`dns.lookup.duration` — in OTel v2, this is in **seconds**. A value of `0.015` means 15 ms. The `eval` converts to milliseconds for human readability.\n\n`where avg_dns_ms > 200` — 200 ms is the threshold where DNS latency becomes perceptible to users. For web-heavy workloads, tighten to 100 ms. For internal applications where DNS resolution is a small fraction of total latency, relax to 500 ms.\n\nWhy `avg` AND `max`: average tells you the sustained DNS quality; max tells you the worst single lookup. If avg is 20 ms but max is 2000 ms, you have occasional DNS timeouts (1 query timing out and being retried) that aren't visible in the average but cause individual user requests to take 2+ seconds.\n\n**Percentile variant** (more nuanced than avg/max):\n```spl\n`stream_index` thousandeyes.test.type=\"dns-server\"\n| stats p50(dns.lookup.duration) as p50_s p95(dns.lookup.duration) as p95_s p99(dns.lookup.duration) as p99_s by dns.question.name, server.address\n| eval p50_ms=round(p50_s*1000,1), p95_ms=round(p95_s*1000,1), p99_ms=round(p99_s*1000,1)\n| where p95_ms > 200\n| sort -p95_ms\n```\nP95 at 200 ms means 5% of lookups take longer than 200 ms — this catches intermittent DNS slowdowns that avg misses.\n\n**Scheduling:** cron `*/15 * * * *`, time range `-1h to now`. Throttle by `dns.question.name` + `server.address` for 4 hours.\n\nStep 3 — Validate\n(a) **Cross-reference ThousandEyes UI.** The ThousandEyes DNS Server view shows resolution time per agent. Compare the numbers in Splunk to the UI.\n\n(b) **Manual query timing.** From a machine with access to the DNS server: `dig @<server> <domain> +stats` — look at the \"Query time:\" line at the bottom. Compare with what ThousandEyes/Splunk reports. Note: `dig` measures from the client; ThousandEyes measures from the agent. Network latency between the agent and the DNS server is included in the ThousandEyes measurement.\n\n(c) **Cache vs uncached comparison.** Run two `dig` queries in succession. The first (uncached) will be slower; the second (cached) should be faster. ThousandEyes queries may hit cache or not depending on TTL and query timing.\n\nStep 4 — Operationalize\n**Dashboard** (add as a row in the UC-5.9.13 \"DNS Health\" dashboard):\n• Timechart: resolution time per domain over 24 hours. Add a reference line at 200 ms.\n• Table: domain | server | avg resolution (ms) | p95 (ms) | max (ms) — sorted by p95 descending.\n• Comparison bar chart: resolution time per DNS server for a selected domain (dropdown token).\n\n**Runbook** (owner: DNS team):\n1. **Resolution time increased suddenly.** Check if the DNS server was restarted (cold cache). Wait for one TTL cycle and re-check.\n2. **Sustained high resolution time.** Check DNS server CPU and query rate. If CPU is high, the server needs more capacity or query rate reduction (tune TTL upward).\n3. **High resolution time from specific agents only.** The problem is network latency between those agents and the DNS server, not the DNS server itself. Correlate with UC-5.9.1.\n4. **Resolution time increases at specific times of day.** DNS server is overloaded during peak hours. Add more DNS capacity or use DNS load balancing.\n\nStep 5 — Troubleshooting\n\n• **Resolution time seems unrealistically low (< 1 ms for all queries)** — The agent may be using a local DNS cache (systemd-resolved, dnsmasq) that intercepts queries before they reach the configured DNS server. This means you're measuring cache response time, not actual DNS server response time. Check the agent's DNS configuration.\n\n• **Resolution time is exactly 0** — The metric may not be populated. Check field presence: `| fieldsummary | search field=dns.lookup.duration`.\n\n• **All common troubleshooting** — See UC-5.9.13 and UC-5.9.1 Step 5.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`ta_cisco_thousandeyes` — Cisco ThousandEyes App for Splunk ([Splunkbase 7719](https://splunkbase.splunk.com/app/7719)), Cisco Supported.). Enable the modular input or HEC token that writes sourcetype=see SPL into index=main. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`avg_dns_ms, dns.question.name, server.address`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We measure how long it takes to look up our domain names on the internet, because even though you don't see it, a slow reference list adds delay to every single click and page load.",
  "prerequisiteUseCases": [
    "UC-5.9.13"
  ],
  "wave": "walk"
}
