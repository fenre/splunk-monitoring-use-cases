{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.20.119",
  "title": "IPv6 Multicast Scoping and Boundary Enforcement",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers the detection logic documented for 'IPv6 Multicast Scoping and Boundary Enforcement' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_ipv6_multicast_scoping_and_boundary_enforcement` (UC-5.20.119), SOC dashboard panel \"Operations — IPv6 Multicast Scoping and Boundary Enforcement\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.20.119 positive (IPv6, Multicast, Scoping, and, Boundary): ingest synthetic events into `index=network` sourcetype='cisco:ios' where `count` grouped by `host`, `scope_violation` satisfies -24h for 'IPv6 Multicast Scoping and Boundary Enforcement'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.20.119 negative (Boundary Enforcement): ingest benign `network` / `cisco:ios` events where `host`, `scope_violation` stays below -24h, or exclude the test entity via `ops_exception_register.csv`. Variant 10 confirms zero alert rows for 'IPv6 Multicast Scoping and Boundary Enforcement'."
  },
  "dataSources": "`index=network sourcetype=cisco:ios` or `sourcetype=cisco:iosxe` — Monitors IPv6 multicast scope enforcement. IPv6 multicast addresses encode their scope in bits 12-15: interface-local (1), link-local (2), admin-local (4), site-local (5), organization-local (8), global (e). Traffic with site-local multicast scope (ff05::) MUST NOT cross site boundaries. Monitors for scope violations where scoped multicast crosses boundaries. Collected via Splunk Add-on for Cisco Security (Splunkbase 1352) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Enterprise — Splunk Add-on for Cisco Security (Splunkbase 1352)",
  "spl": "index=network (sourcetype=\"cisco:ios\" OR sourcetype=\"cisco:iosxe\") earliest=-24h\n  (\"multicast\" OR \"ff05\" OR \"ff08\" OR \"mcast\" OR \"PIM\")\n| eval scope_violation=case(\n    match(_raw, \"ff05:\") AND match(_raw, \"(?i)forward.*site.?boundary|cross.?site\"), \"SITE-LOCAL scope crossing site boundary\",\n    match(_raw, \"ff02:\") AND match(_raw, \"(?i)routed|forward\"), \"LINK-LOCAL scope being routed (MUST NOT route)\",\n    match(_raw, \"ff01:\") AND match(_raw, \"(?i)forward\"), \"INTERFACE-LOCAL scope forwarded (MUST NOT)\",\n    1=1, null())\n| where isnotnull(scope_violation)\n| stats count as events by host, scope_violation\n| eval severity=\"HIGH — multicast scope violation: \" . scope_violation\n| sort -events",
  "description": "Alerts on UC-5.20.119 — IPv6, Multicast, Scoping, and, Boundary — using `index=network` sourcetype `cisco:ios`, grouped by `host`, `scope_violation`. Outputs `count` each `the scheduled window`; alert when -24h. Primary focus: Boundary Enforcement. Purpose-built for 'IPv6 Multicast Scoping and Boundary Enforcement' in Operations workflows.",
  "value": "For 'IPv6 Multicast Scoping and Boundary Enforcement', UC-5.20.119 converts `activity` on `cisco:ios` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Monitor multicast routing for scope violations. Verify multicast boundary filters are correctly configured at scope boundaries.",
  "visualization": "(1) Table: scope violations by type and device. (2) Single-value: violation count. (3) Map: site boundaries with scope enforcement status.",
  "references": [
    {
      "title": "RFC 4291 — IP Version 6 Addressing Architecture (§2.7 — multicast scoping)",
      "url": "https://www.rfc-editor.org/rfc/rfc4291#section-2.7",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RFC 7346 — IPv6 Multicast Address Scopes",
      "url": "https://www.rfc-editor.org/rfc/rfc7346",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `network` (UC-5.20.119)** — Planned work for 'IPv6 Multicast Scoping and Boundary Enforcement' shifts `count` over `span=1h` while `host`, `scope_violation` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `cisco:ios`** — Scheduled automation elevates `scope_violation` without crossing configured threshold for UC-5.20.119. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`, `scope_violation`** — Health checks in `network` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `cisco:ios`** — First 48h baseline for UC-5.20.119 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco",
    "juniper"
  ],
  "equipmentModels": [
    "cisco_catalyst_9000",
    "cisco_ios",
    "juniper_mx"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Multicast routing (PIMv6) deployed.\n• Multicast scope boundaries configured.\n\nStep 1 — Configure multicast boundaries\n\n**Cisco IOS-XE multicast scope boundary:**\n```\ninterface GigabitEthernet0/0/0\n description Site boundary\n ipv6 multicast boundary block source\n ipv6 multicast boundary scope 5\n```\n\nStep 2 — Create monitoring searches\n\n**Scope boundary enforcement audit:**\n```spl\nindex=network sourcetype=\"cisco:*:config\" earliest=-7d\n| dedup host\n| eval has_mcast_boundary=if(match(_raw, \"(?i)multicast boundary\"), 1, 0)\n| stats count as total sum(has_mcast_boundary) as with_boundary\n| eval coverage=round(with_boundary / total * 100, 1) . \"%\"\n```\n\nStep 3 — Validate\nSend a site-local multicast packet (ff05::1) at a site boundary router. Verify it is NOT forwarded across the boundary.\n\nStep 4 — Operationalize\n**Dashboard:** Multicast scope compliance. **Alert:** Any scope violation — high.\n\nStep 5 — Troubleshooting\n• Missing scope boundary: Add `ipv6 multicast boundary scope 5` to site border interfaces.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Enterprise — Splunk Add-on for Cisco Security (Splunkbase 1352)). Enable the modular input or HEC token that writes sourcetype=cisco:ios, cisco:iosxe into network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, scope_violation`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Some messages on our network are meant for everyone in the same room (link-local), everyone in the same building (site-local), or everyone in the whole company (organization). We make sure that room messages don't accidentally get broadcast to the whole building, and building messages don't leak out to other buildings.",
  "prerequisiteUseCases": [
    "UC-5.20.109"
  ],
  "wave": "walk"
}
