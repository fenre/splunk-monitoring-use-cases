{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.20.43",
  "title": "OSPFv3 Adjacency Monitoring and IPv6 Routing Stability",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "IT Operations",
  "exclusions": "Covers the detection logic documented for 'OSPFv3 Adjacency Monitoring and IPv6 Routing Stability' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_ospfv3_adjacency_monitoring_and_ipv6_routing_sta` (UC-5.20.43), SOC dashboard panel \"Operations — OSPFv3 Adjacency Monitoring and IPv6 Routing Stability\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.20.43 positive (OSPFv3, Adjacency, Monitoring, and, IPv6): ingest synthetic events into `index=network` sourcetype='cisco:ios' where `count` grouped by `host`, `neighbor_id`, `interface`, `ospf_pid` satisfies `adjacency_losses` > 3, or `adjacency_ups` > 3 for 'OSPFv3 Adjacency Monitoring and IPv6 Routing Stability'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.20.43 negative (Routing Stability): ingest benign `network` / `cisco:ios` events where `host`, `neighbor_id`, `interface`, `ospf_pid` stays below `adjacency_losses` > 3, or `adjacency_ups` > 3, or exclude the test entity via `ops_exception_register.csv`. Variant 7 confirms zero alert rows for 'OSPFv3 Adjacency Monitoring and IPv6 Routing Stability'."
  },
  "dataSources": "`index=network sourcetype=cisco:ios` containing OSPFv3 adjacency change syslog messages. OSPFv3 (RFC 5340) is the IPv6 version of OSPF, replacing OSPFv2 for IPv6 routing. While OSPFv2 carries IPv4 routes inside LSAs using IPv4 addressing, OSPFv3 uses link-local addresses for neighbor communication and carries IPv6 prefixes in separate LSA types (Type 8 Intra-Area-Prefix-LSA, Type 9 Inter-Area-Prefix-LSA). Key differences:\n\n1. **Link-local next-hops** — OSPFv3 peers using link-local addresses (FE80::/10), not global addresses\n2. **Per-link operation** — OSPFv3 runs per-link, not per-subnet (multiple instances per interface possible)\n3. **Authentication** — OSPFv3 uses IPsec (AH/ESP) for authentication, not MD5 in the OSPF header\n4. **Address families** — OSPFv3 can carry both IPv4 and IPv6 routes via Address Family (AF) support (RFC 5838)\n\nAdjacency events:\n- `%OSPFv3-5-ADJCHG: Process <pid> Nbr <router-id> on <intf> from FULL to DOWN` — adjacency lost\n- `%OSPFv3-5-ADJCHG: Process <pid> Nbr <router-id> on <intf> from LOADING to FULL` — adjacency established\n\nOSPFv3-specific failure modes (not present in OSPFv2):\n- IPsec authentication mismatch (replaces MD5)\n- Link-local address conflict or misconfiguration\n- Instance ID mismatch (new in OSPFv3)\n- IPv6 disabled on the interface but OSPFv3 configured",
  "app": "Splunk Add-on for Cisco IOS (`Splunk_TA_cisco-ios`, Splunkbase 2812).",
  "spl": "index=network sourcetype=\"cisco:ios\" \"%OSPFv3\" \"ADJCHG\" earliest=-24h\n| rex field=_raw \"Process\\s+(?<ospf_pid>\\d+).*Nbr\\s+(?<neighbor_id>[\\d.]+)\\s+on\\s+(?<interface>\\S+)\\s+from\\s+(?<from_state>\\S+)\\s+to\\s+(?<to_state>\\S+)\"\n| eval severity=case(\n    to_state=\"DOWN\", \"CRITICAL — adjacency lost\",\n    to_state=\"FULL\", \"INFO — adjacency established\",\n    match(to_state, \"INIT|2WAY|EXSTART|EXCHANGE|LOADING\"), \"WARNING — adjacency transitioning\",\n    1=1, \"INFO\")\n| eval is_down=if(to_state=\"DOWN\", 1, 0)\n| eval is_full=if(to_state=\"FULL\", 1, 0)\n| stats count as events count(eval(is_down=1)) as adjacency_losses count(eval(is_full=1)) as adjacency_ups by host, neighbor_id, interface, ospf_pid\n| eval flapping=if(adjacency_losses > 3 AND adjacency_ups > 3, \"FLAPPING\", \"stable\")\n| sort -adjacency_losses",
  "description": "Alerts on UC-5.20.43 — OSPFv3, Adjacency, Monitoring, and, IPv6 — using `index=network` sourcetype `cisco:ios`, grouped by `host`, `neighbor_id`, `interface`, `ospf_pid`. Outputs `count` each `the scheduled window`; alert when `adjacency_losses` > 3, or `adjacency_ups` > 3. Primary focus: Routing Stability. Purpose-built for 'OSPFv3 Adjacency Monitoring and IPv6 Routing Stability' in Operations workflows.",
  "value": "UC-5.20.43 operationalises 'OSPFv3 Adjacency Monitoring and IPv6 Routing Stability' against `cisco:ios` feeds so operators reduce operational risk and delayed incident response without manual correlation.",
  "implementation": "Collect OSPFv3 ADJCHG syslog events from all routers. Track adjacency state transitions. Alert on adjacency losses and flapping. Correlate with interface events and IPsec errors.",
  "visualization": "(1) Table: current OSPFv3 adjacency events with state transition and severity. (2) Timechart: adjacency ups and downs over 24 hours. (3) Single-value: total active adjacency losses. (4) Network topology: OSPFv3 adjacency map (nodes=routers, edges=adjacencies, red=down).",
  "references": [
    {
      "title": "RFC 5340 — OSPF for IPv6",
      "url": "https://www.rfc-editor.org/rfc/rfc5340",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RFC 5838 — Support of Address Families in OSPFv3 (dual-stack AF support)",
      "url": "https://www.rfc-editor.org/rfc/rfc5838",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RFC 4552 — Authentication/Confidentiality for OSPFv3 (IPsec for OSPFv3)",
      "url": "https://www.rfc-editor.org/rfc/rfc4552",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `network` (UC-5.20.43)** — Planned work for 'OSPFv3 Adjacency Monitoring and IPv6 Routing Stability' shifts `count` over `span=1h` while `host`, `neighbor_id`, `interface`, `ospf_pid` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `cisco:ios`** — Scheduled automation elevates `neighbor_id` without crossing `adjacency_losses` > 3 for UC-5.20.43. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`, `neighbor_id`, `interface`, `ospf_pid`** — Health checks in `network` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `cisco:ios`** — First 48h baseline for UC-5.20.43 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "arista",
    "cisco",
    "exchange",
    "juniper",
    "syslog"
  ],
  "equipmentModels": [
    "arista_eos",
    "cisco_ios",
    "cisco_iosxe",
    "cisco_nxos",
    "juniper_junos"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• OSPFv3 deployed on IPv6-enabled interfaces.\n• Syslog forwarding at severity 5 (notification) for ADJCHG events.\n• Optionally, SNMP polling of ospfv3NbrState for continuous adjacency monitoring.\n\nStep 1 — Configure data collection\n\n**Cisco IOS-XE — ensure OSPFv3 adjacency logging:**\n```\nrouter ospfv3 1\n address-family ipv6 unicast\n  log-adjacency-changes detail\n```\n`detail` logs all state transitions, not just to/from FULL. This is essential for detecting stuck states.\n\n**Juniper Junos:**\n```\nset protocols ospf3 traceoptions flag state\n```\n\n**Verification:**\n```spl\nindex=network sourcetype=\"cisco:ios\" \"%OSPFv3\" earliest=-7d\n| stats count by host\n```\n\nStep 2 — Create the search and alert\n\n**Adjacency loss alert:**\n```spl\nindex=network sourcetype=\"cisco:ios\" \"%OSPFv3\" \"ADJCHG\" \"to DOWN\" earliest=-1h\n| rex field=_raw \"Nbr\\s+(?<neighbor_id>[\\d.]+)\\s+on\\s+(?<interface>\\S+)\"\n| eval alert_text=\"OSPFv3 adjacency lost: \" . host . \" → \" . neighbor_id . \" on \" . interface\n| table _time, host, neighbor_id, interface, alert_text\n```\nTrigger: any adjacency to DOWN.\n\n**Adjacency flapping detection:**\n```spl\nindex=network sourcetype=\"cisco:ios\" \"%OSPFv3\" \"ADJCHG\" earliest=-4h\n| rex field=_raw \"Nbr\\s+(?<neighbor_id>[\\d.]+)\\s+on\\s+(?<interface>\\S+)\\s+from\\s+(?<from_state>\\S+)\\s+to\\s+(?<to_state>\\S+)\"\n| stats count as transitions dc(to_state) as distinct_states by host, neighbor_id, interface\n| where transitions > 6\n| eval alert=\"OSPFv3 flapping: \" . transitions . \" state changes in 4 hours for \" . host . \" ↔ \" . neighbor_id\n```\n\n**IPsec authentication failure correlation:**\n```spl\nindex=network sourcetype=\"cisco:ios\" (\"%OSPFv3\" \"ADJCHG\") OR (\"%CRYPTO-4\" AND \"ipsec\" AND \"fail\")\n  earliest=-4h\n| eval event_type=case(\n    match(_raw, \"OSPFv3.*ADJCHG\"), \"OSPFv3_ADJCHG\",\n    match(_raw, \"CRYPTO.*ipsec.*fail\"), \"IPsec_FAILURE\",\n    1=1, \"OTHER\")\n| transaction host maxspan=5m maxpause=2m\n| search event_type=\"OSPFv3_ADJCHG\" AND event_type=\"IPsec_FAILURE\"\n| eval diagnosis=\"OSPFv3 adjacency failure correlated with IPsec authentication error — check IPsec SPI and keys\"\n```\n\nStep 3 — Validate\n(a) **Simulate adjacency loss.** Shut down an OSPFv3 interface (`shutdown` on an OSPFv3-enabled interface). Verify the ADJCHG to DOWN appears in Splunk.\n\n(b) **Simulate flapping.** Rapidly toggle an interface. Verify the flapping detection fires.\n\n(c) **IPsec mismatch.** Change the IPsec key on one side of an OSPFv3 adjacency. Verify the adjacency fails and the IPsec correlation search identifies the root cause.\n\nStep 4 — Operationalize\n\n**Dashboard** (\"IPv6 — OSPFv3 Routing Health\"):\n• Row 1 — Single-value: total OSPFv3 adjacencies, current DOWN count, flapping pairs.\n• Row 2 — Timechart: adjacency state changes over 24 hours.\n• Row 3 — Table: current adjacency losses with host, neighbor, interface, and duration.\n• Row 4 — Correlation: OSPFv3 events alongside IPsec errors and interface events.\n\n**Scheduling:** Adjacency loss alert real-time. Flapping detection every 30 minutes.\n\n**Runbook:**\n1. Adjacency DOWN: check physical link (`show interface`), check OSPFv3 neighbor (`show ospfv3 neighbor`), check IPsec SA (`show crypto ipsec sa`).\n2. Stuck in EXSTART/EXCHANGE: MTU mismatch between peers. Check interface MTU on both sides.\n3. IPsec authentication failure: verify matching SPI, algorithm, and key on both sides. OSPFv3 uses Transport Mode ESP or AH.\n4. Instance ID mismatch: verify `instance-id` matches on both sides of the link.\n\nStep 5 — Troubleshooting\n\n• **OSPFv3 vs OSPFv2 events** — Ensure the search specifically matches `%OSPFv3` and not `%OSPF-5-ADJCHG` (which is OSPFv2). Both may exist on dual-stack routers.\n\n• **Link-local peering complicates identification** — OSPFv3 peers using link-local addresses. The syslog shows the Router ID (IPv4 format) but the actual peering address is link-local. To identify which physical interface is affected, use the `on <interface>` field from the syslog.\n\n• **Address Family (AF) mode** — In AF mode (RFC 5838), OSPFv3 can carry both IPv4 and IPv6 routes. An adjacency failure in AF mode affects both address families simultaneously. The syslog format may differ: `%OSPFv3-5-ADJCHG: Process 1 (address-family ipv6) Nbr ...`.",
  "grandmaExplanation": "The routers that carry IPv6 traffic need to be good neighbours — they constantly check on each other to share road maps. If a neighbour stops responding, the others have to quickly reroute traffic around the gap. We watch for these 'neighbour down' events because they mean some roads are suddenly closed and traffic might be taking a longer detour — or worse, getting lost.",
  "prerequisiteUseCases": [],
  "wave": "walk"
}
