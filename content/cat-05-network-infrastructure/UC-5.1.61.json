{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.1.61",
  "title": "Arista EOS Agent Health Monitoring (Arista)",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Arista EOS Agent Health Monitoring (Arista)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_arista_eos_agent_health_monitoring_arista` (UC-5.1.61), SOC dashboard panel \"Operations — Arista EOS Agent Health Monitoring (Arista)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.1.61 positive (Arista, EOS, Agent, Health, Monitoring): ingest synthetic events into `index=network` sourcetype='arista:eos' where `values` grouped by `host` satisfies `restarts` > 0 for 'Arista EOS Agent Health Monitoring (Arista)'. Validate filter `ProcMgr OR \"ProcMgr-worker\" OR \"Agent.*restart\" OR \"restarte`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.1.61 negative (Monitoring (Arista)): ingest benign `network` / `arista:eos` events where `host` stays below `restarts` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 1 confirms zero alert rows for 'Arista EOS Agent Health Monitoring (Arista)'."
  },
  "dataSources": "`sourcetype=arista:eos` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`arista:eos` via SC4S, syslog — Universal Forwarder (Splunkbase 617)",
  "spl": "index=network sourcetype=\"arista:eos\"\n| search ProcMgr OR \"ProcMgr-worker\" OR \"Agent.*restart\" OR \"restarted\" OR \"%AGENT-\"\n| rex field=_raw \"(?i)(?<agent_name>[A-Za-z0-9_\\-]+)\\s+agent.*restart\"\n| stats count as restarts, values(agent_name) as agents by host\n| where restarts > 0\n| sort -restarts",
  "description": "Alerts on Monitoring (Arista) activity in UC-5.1.61 ('Arista EOS Agent Health Monitoring (Arista)') from `index=network` sourcetype `arista:eos`. Rolls up `values` by `host` across `the scheduled window` and pages when `restarts` > 0. Uses filter `ProcMgr OR \"ProcMgr-worker\" OR \"Agent.*restart\" OR \"restarte`. Operations operators treat UC-5.1.61 as the canonical signal.",
  "value": "Operators running UC-5.1.61 on `arista:eos` feeds shorten triage for Monitoring (Arista) tied to 'Arista EOS Agent Health Monitoring (Arista)', lowering operational risk and delayed incident response.",
  "implementation": "Create a baseline of allowed occasional restarts per major version; alert when any host exceeds threshold per day or when critical agents (e.g., Stp, Bgp, Route) restart. Attach EOS version from inventory. Open problem ticket when restarts cluster after a specific feature toggle.",
  "visualization": "Table of hosts with agent restart counts; bar chart by agent name; sparkline of restarts over time.",
  "cimModels": [],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `network` (UC-5.1.61)** — Planned work for 'Arista EOS Agent Health Monitoring (Arista)' shifts `values` over `span=1h` while `host` remains in-scope. Applies filter `ProcMgr OR \"ProcMgr-worker\" OR \"Agent.*restart\" OR \"restarte`. Cross-check CMDB CHG.\n\n2. **Batch `values` on `arista:eos`** — Scheduled automation elevates `src` without crossing `restarts` > 0 for UC-5.1.61. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `network` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `arista:eos`** — First 48h baseline for UC-5.1.61 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "syslog"
  ],
  "equipmentModels": [
    "syslog_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n* Arista EOS agent health data from syslog or eAPI. Data in `index=arista` or `index=network` with `sourcetype=arista:eos`. Key syslog: `AGENT-5-*`, `%AGENT-6-INITIALIZED`, `%AGENT-3-CRASHED`. eAPI: `show agent`.\n* Arista EOS agents: modular software architecture where each protocol/feature runs as an independent agent (Stp, Rib, Bgp, Ospf, Mlag, Acl, etc.). Agent crash or restart indicates software issue. Agent health is critical for switch functionality.\n\nStep 1 -- Configure data collection\n```\n# Arista EOS -- agent logging is enabled by default\nlogging host <splunk-ip>\nlogging trap informational\n```\nVerify:\n```spl\nindex=arista earliest=-30d\n| where match(_raw, \"(?i)AGENT|agent.*crash|agent.*restart|agent.*init|agent.*term\")\n| stats count by host\n```\n\nStep 2 -- Create the search and alert\n\n**Primary search -- EOS agent health monitoring:**\n```spl\nindex=arista earliest=-30d\n| where match(_raw, \"(?i)AGENT|agent.*crash|agent.*restart|agent.*init|agent.*term|agent.*fail\")\n| eval device=coalesce(host, device_name)\n| rex field=_raw \"(?i)Agent\\s+'?(?<agent_name>[A-Za-z]+)'?\"\n| eval agent=coalesce(agent_name, \"unknown\")\n| eval agent_event=case(\n    match(_raw, \"(?i)crash|core|abort|segfault\"), \"CRASH\",\n    match(_raw, \"(?i)restart|recover\"), \"RESTART\",\n    match(_raw, \"(?i)init|start\"), \"INITIALIZED\",\n    match(_raw, \"(?i)term|stop|exit\"), \"TERMINATED\",\n    1==1, \"AGENT_EVENT\")\n| eval is_critical_agent=if(match(agent, \"(?i)Stp|Rib|Bgp|Ospf|Mlag|Intf|Fib|Acl|Arp\"), \"YES\", \"NO\")\n| stats count as events count(eval(agent_event=\"CRASH\")) as crashes count(eval(agent_event=\"RESTART\")) as restarts values(agent) as affected_agents by device\n| eval severity=case(\n    crashes > 0, \"CRITICAL -- agent crash: \".mvjoin(affected_agents, \", \"),\n    restarts > 3, \"WARNING -- frequent agent restarts\",\n    1==1, \"OK\")\n| where severity != \"OK\"\n| sort severity\n```\n\nStep 3 -- Validate\n(a) CLI: `show agent` -- list all agents and their status.\n(b) CLI: `show agent <name> logs` -- agent-specific log.\n(c) CLI: `dir /var/core/` -- check for crash core files.\n\nStep 4 -- Operationalize\nDashboard (\"Arista -- Agent Health\"):\n* Row 1 -- Single-value: \"Agent crashes (30d)\", \"Agent restarts\", \"Devices affected\".\n* Row 2 -- Agent crash/restart timeline.\n\nAlert: Critical (agent crash): collect core file, open Arista TAC case.\n\nStep 5 -- Troubleshooting\n\n* **Agent crash** -- Collect core file: `dir /var/core/`. Open TAC case with core file and `show tech-support`. Consider upgrading EOS to a release with the bug fix.\n\n* **Frequent agent restarts** -- May indicate memory leak or resource exhaustion. Monitor: `show processes top` for memory usage trends. Check EOS release notes for known issues.\n\n* **Critical agent down** -- Stp, Rib, or Bgp agent failure affects forwarding. If not auto-recovered, try: `agent <name> restart`. Plan maintenance window for firmware upgrade.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`arista:eos` via SC4S, syslog — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=arista:eos into network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, restarts`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We help you know early when something looks wrong with arista eos agent health monitoring so the team can act before it grows into a bigger outage.",
  "hardware": "All Arista 7000/7100/7200/7300/7500/7800 series",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
