{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.13.19",
  "title": "Network Health by Site (Area/Building)",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Availability"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=catalyst sourcetype=cisco:dnac:devicehealth` from the `devicehealth` modular input in `TA_cisco_catalyst` (Splunkbase 7538). Uses per-device health data grouped by `siteId` to compute a per-site aggregate — more granular than the global `networkhealth` feed (UC-5.13.16) which has no site dimension. Enriched with `catalyst_site_lookup` for human-readable site names (UC-5.13.5 / UC-5.13.51).",
  "app": "`TA_cisco_catalyst` ([Splunkbase 7538](https://splunkbase.splunk.com/app/7538)), Cisco Supported, 1.x+.",
  "spl": "index=catalyst sourcetype=\"cisco:dnac:devicehealth\"\n| where overallHealth > 0\n| stats avg(overallHealth) as avg_health count as device_count count(eval(overallHealth<50)) as bad_count by siteId\n| eval bad_pct=round(bad_count*100/device_count,1)\n| lookup catalyst_site_lookup siteId OUTPUT siteName\n| eval site_label=coalesce(siteName, siteId)\n| sort -bad_pct\n| head 20",
  "description": "Ranks Catalyst Center sites by aggregate device health, showing which buildings and campuses have the worst infrastructure scores — enabling operations to prioritise site-level remediation over fleet-wide changes that may not address localised problems like a failing UPS, overloaded IDF, or deferred cabling maintenance.",
  "value": "UC-5.13.16 gives you one campus-wide number. This UC breaks it into *where*. A campus score of 82 may hide the fact that headquarters is at 95 while the branch office in Building C is at 55 — because Building C's HVAC failed last week and the switches are thermal-throttling. Without the site split, you'd tune RRM globally when you only need to call facilities for one building. The ranked list also identifies sites for proactive infrastructure surveys before users start complaining, and tracks whether a site-specific remediation (switch refresh, UPS replacement) actually moved the needle.",
  "implementation": "Uses the `devicehealth` feed (same as UC-5.13.1), grouped by `siteId`. For site name resolution, use the `catalyst_site_lookup` from UC-5.13.5 or UC-5.13.51. Schedule as weekly report for regional operations.",
  "visualization": "(1) Bar chart: top 20 sites by `bad_pct`, red/yellow/green colour coding. (2) Table: site_label | device_count | avg_health | bad_count | bad_pct — drilldown to UC-5.13.1 filtered by `siteId`. (3) Optional choropleth map if geo-coordinates are available. (4) Timechart variant: `| timechart span=1d avg(overallHealth) by siteId` for per-site trending.",
  "references": [
    {
      "title": "Cisco Catalyst Add-on for Splunk (Splunkbase 7538)",
      "url": "https://splunkbase.splunk.com/app/7538"
    },
    {
      "title": "Catalyst Center Intent API — Device Health endpoint",
      "url": "https://developer.cisco.com/docs/catalyst-center/#!get-overall-network-device-health"
    },
    {
      "title": "Catalyst Center Integration Guide (this repo)",
      "url": "docs/guides/catalyst-center.md"
    },
    {
      "title": "Catalyst Center Site Topology API",
      "url": "https://developer.cisco.com/docs/catalyst-center/#!get-site-topology"
    }
  ],
  "knownFalsePositives": "**Site hierarchy reorganisation moving devices between siteIds.** When Catalyst Center's hierarchy is restructured, devices may shift between siteIds, creating phantom improvements at the old site and phantom degradation at the new one. Distinguish by correlating with `index=catalyst sourcetype=\"cisco:dnac:audit:logs\"` for hierarchy changes. Suppress by tracking by `deviceName` (stable) when investigating anomalies.\n\n**Small-population sites producing volatile percentages.** A remote branch with 3 devices where 1 is unhealthy shows 33% bad — alarming but a single device. Distinguish by checking `device_count`. Suppress by filtering `| where device_count >= 5` for the ranked chart.\n\n**All devices at a site showing null siteId.** Devices in the unassigned 'Global' site aggregate into one misleading bucket. Distinguish by checking whether the `siteId` is the root UUID. Suppress by filtering `| where siteId != \"<global-root-uuid>\"` or by assigning all devices to proper sites in Catalyst Center.\n\n**Maintenance window at one site dragging its health down temporarily.** During a firmware upgrade at Building A, all its devices reload and show poor health. Distinguish by correlating with ITSM change records. Suppress by annotating the chart with maintenance windows.",
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_catalyst_center"
  ],
  "status": "verified",
  "lastReviewed": "2026-04-30",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• UC-5.13.1 (Device Health Overview) operational — same `devicehealth` data feed.\n• `siteId` populated in device health events (always present in Catalyst Center 2.3.5+ for assigned devices).\n• `catalyst_site_lookup` created per UC-5.13.5 or UC-5.13.51 for human-readable site names.\n• Note: the `networkhealth` sourcetype (UC-5.13.16) is a cluster-wide aggregate with no `siteId` dimension. For per-site network health, this UC uses `devicehealth` grouped by `siteId` — a different approach that gives site-level granularity.\n\nStep 1 — Configure data collection\nSame `devicehealth` input as UC-5.13.1. Confirm `siteId` is populated per site:\n```spl\nindex=catalyst sourcetype=\"cisco:dnac:devicehealth\" earliest=-1h\n| stats dc(deviceName) as devices by siteId\n| sort -devices\n```\n\nStep 2 — Create the search and report\n```spl\nindex=catalyst sourcetype=\"cisco:dnac:devicehealth\"\n| where overallHealth > 0\n| stats avg(overallHealth) as avg_health count as device_count count(eval(overallHealth<50)) as bad_count by siteId\n| eval bad_pct=round(bad_count*100/device_count,1)\n| lookup catalyst_site_lookup siteId OUTPUT siteName\n| eval site_label=coalesce(siteName, siteId)\n| sort -bad_pct\n| head 20\n```\n\nWhy use `devicehealth` instead of `networkhealth`: the `networkhealth` API returns a single cluster-wide aggregate without a `siteId` dimension. To get per-site health, you must aggregate per-device scores from `devicehealth` by `siteId`. This gives a different (but complementary) view: UC-5.13.16 is the official Assurance composite score; this UC is a device-health-only aggregate per location.\n\nWhy `where overallHealth > 0` before aggregation: filters Assurance recomputation artifacts that would drag site averages down with zeros.\n\nWhy `bad_pct` as the ranking metric: `avg_health` can be misleading — a site with 49 healthy devices and 1 critically sick device averages 97. `bad_pct` surfaces the concentration of problems regardless of site size.\n\nSchedule as Report: weekly (cron `0 7 * * 1`), output to PDF for the regional ops meeting.\n\nStep 3 — Validate\n(a) Sum `device_count` across all sites. Should match UC-5.13.1's total device count (minus any with null `siteId`).\n\n(b) Pick the worst site. Drill to UC-5.13.1 filtered by that `siteId`. Verify the devices are genuinely unhealthy in **Catalyst Center > Assurance > Health > Device** filtered by that site.\n\n(c) Cross-reference with UC-5.13.5 (Device Health by Site) — should produce similar rankings since both use the same data feed grouped by `siteId`.\n\n(d) Compare with UC-5.13.13 (Client Health by Site) — do the worst sites for device health also have the worst client health? If yes, infrastructure problem. If device health is fine but client health is poor at a site, the issue is wireless/DHCP, not equipment.\n\n(e) Verify site name resolution: all `site_label` values should be building names, not UUIDs.\n\nStep 4 — Operationalize\nDashboard placement:\n• On the Network Health dashboard below UC-5.13.16's single-value gauge.\n• Bar chart: top 20 sites by `bad_pct`, colour-coded.\n• Table with drilldown to UC-5.13.1 filtered by site.\n• Time-picker: \"Last 1 hour\" for current state, \"Last 7 days\" for weekly review.\n\nRunbook (owner: Regional Operations):\n1. Identify the site with the highest `bad_pct`.\n2. Check whether the bad devices share a common characteristic (same `deviceType`, same `platformId`, same firmware version).\n3. If concentrated by type → fleet-level issue (firmware bug). If mixed types → site-level issue (power, cooling, cabling).\n4. Cross-reference with UC-5.13.13 (Client Health by Site) to assess user impact.\n5. Track site rankings month-over-month. Sites consistently in the bottom 5 are candidates for infrastructure investment.\n\nStep 5 — Troubleshooting\n\n• **All devices grouped under one siteId** — hierarchy is flat. Fix in Catalyst Center.\n\n• **Site names show as UUIDs** — lookup is empty. Regenerate per UC-5.13.5.\n\n• **A site shows 100% bad_pct** — genuine site-wide failure (power outage, upstream link down). Verify in Catalyst Center and escalate.\n\n• **Per-site averages don't match the campus-wide score from UC-5.13.16** — expected. UC-5.13.16 uses the Assurance-computed composite (weighted by device role and health dimensions). This UC uses a simple `avg(overallHealth)` per site. The two views complement each other.\n\n• **New site appears with poor health** — recently created site with newly assigned devices. Assurance needs 24-48 hours to build baselines.\n\n• **Search is slow** — with 10,000+ devices, narrowing to `earliest=-20m` gives a single-poll snapshot. For trending, use summary indexing.\n\n• **One site dominates with very high device count** — headquarters. Consider normalising by using `bad_pct` rather than `bad_count`.\n\n• **Device count at a site doesn't match Catalyst Center Inventory for that site** — check virtual domain scope of the service account.",
  "grandmaExplanation": "We rank every building and campus by how healthy the network equipment inside it is. The locations with the most infrastructure problems show up at the top, so your team knows exactly which building needs a visit — whether it is to fix a failing switch, replace a broken power supply, or call the building electrician.",
  "prerequisiteUseCases": [
    "UC-5.13.1"
  ],
  "wave": "walk"
}
