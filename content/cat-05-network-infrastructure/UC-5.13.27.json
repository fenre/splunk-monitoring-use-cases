{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.13.27",
  "title": "Issue Volume Anomaly Detection",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Anomaly"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=catalyst sourcetype=cisco:dnac:issue`. from the `issue` modular input in the Cisco Catalyst Add-on (Splunkbase 7538) polling `GET /dna/intent/api/v1/issues`. Default poll interval: 900s (15m). Volume: ~1 event per active issue per poll. RBAC: NETWORK-ADMIN-ROLE. Key fields: `priority` (string), `category` (string), `name` (string), `deviceName` (string), `status` (string), `issueId` (string), `siteId` (string), `lastOccurenceTime` (epoch milliseconds).",
  "app": "`Cisco Catalyst Add-on for Splunk` (Splunkbase 7538)",
  "spl": "index=catalyst sourcetype=\"cisco:dnac:issue\" | bin _time span=4h | stats count as issue_count by _time | eventstats avg(issue_count) as baseline stdev(issue_count) as stdev_issues | where issue_count > (baseline + 2*stdev_issues) AND stdev_issues > 0 | eval deviation=round((issue_count-baseline)/stdev_issues,1) | sort -deviation",
  "description": "Detects statistically unusual spikes in assurance issue volume that may indicate a network event, failed change, or emerging attack.",
  "value": "A sudden surge in issues often correlates with a failed change, configuration push, or emerging infrastructure problem. Anomaly detection catches these faster than fixed thresholds.",
  "implementation": "Enable the `issue` input. Run over a time window with enough 4h buckets to estimate a stable baseline; tune the `2*stdev` multiplier for sensitivity. Use alongside change calendars for triage context.",
  "visualization": "Timechart of issue_count with reference lines, table of anomalous buckets with deviation score.",
  "references": [
    {
      "title": "Splunkbase app 7538",
      "url": "https://splunkbase.splunk.com/app/7538"
    },
    {
      "title": "Catalyst Center API docs",
      "url": "https://developer.cisco.com/docs/catalyst-center/"
    },
    {
      "title": "Catalyst Center Integration Guide",
      "url": "docs/guides/catalyst-center.md"
    },
    {
      "title": "Catalyst Center Issues API — Cisco DevNet",
      "url": "https://developer.cisco.com/docs/catalyst-center/#!issues"
    }
  ],
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_catalyst_center"
  ],
  "knownFalsePositives": "**Planned maintenance window creating a volume spike that triggers the anomaly detector.** Issue volume increases during maintenance as devices reload and generate connectivity issues. Distinguish by correlating with ITSM change records or audit logs for scheduled maintenance. Suppress by excluding maintenance windows from the baseline: compute `eventstats` only on non-maintenance time buckets.\n\n**Catalyst Center Assurance engine restart re-detecting all current issues.** After a Catalyst Center restart or upgrade, the Assurance engine may re-detect all active issues, creating a burst that appears anomalous. Distinguish by checking whether the spike coincides with a Catalyst Center platform event. Suppress by using `| stats dc(issueId)` per time bucket instead of event count, which prevents re-detection from inflating the volume.\n\n**Seasonal or weekly usage pattern creating periodic volume variations.** Network issue volume may follow predictable patterns (higher during business hours, lower on weekends). If the baseline window is too short, normal variations appear anomalous. Distinguish by comparing the current pattern to the same day-of-week in previous weeks. Suppress by using a 7+ day baseline window and day-of-week-aware statistics.\n\n**New Assurance rule or category enabled creating a one-time volume increase.** When Catalyst Center adds new issue detection rules (via upgrade or configuration change), previously undetected conditions are now flagged, creating a one-time volume increase. Distinguish by checking whether new `name` values appear that were not present before. Suppress by establishing a new baseline after the detection rule change.",
  "detailedImplementation": "Prerequisites\n• **issue** data flowing (`cisco:dnac:issue`); **UC-5.13.22** to sanity-check a normal **4h** **timechart** before **stats**-based **anomaly** work.\n• **Same-window** **eventstats** baseline as **UC-5.13.20** (network health): **changing** the **time** picker **changes** the **baseline**—set **lookback** policy in the runbook (**14d** or **30d** typical).\n• **Dedup wisdom:** if each poll re-sends **all** open issues, **raw** **count** is meaningless—**fix** **ingest** first or **pre-aggregate** **`| bin ... | stats dc(issueId) as issue_count by _time`** in a v2 **macro**.\n• `docs/implementation-guide.md` for **search** **cost** ( **eventstats** over long windows).\n\nStep 1 — Configure data collection\n• **Input:** **issue**; **interval** aligned with NOC’s comfort on **stale** **issues**.\n• **Key:** stable **`issueId`** in raw events for any future **dedup** design.\n\nStep 2 — Create the search or alert\n```spl\nindex=catalyst sourcetype=\"cisco:dnac:issue\" | bin _time span=4h | stats count as issue_count by _time | eventstats avg(issue_count) as baseline stdev(issue_count) as stdev_issues | where issue_count > (baseline + 2*stdev_issues) AND stdev_issues > 0 | eval deviation=round((issue_count-baseline)/stdev_issues,1) | sort -deviation\n```\n\nUnderstanding this SPL (2-sigma on bucket counts)\n• **Over-dispersed** data (many **zeros**, rare **huge** hours) can break **Gaussian** intuition—treat as **sensitivity** **triage**, not proof of a sev-1 on its own.\n• For chatty networks, require (baseline + 3 × stdev_issues) instead of 2 × stdev, or lengthen the time window to stabilize stdev.\n• **Parallel** `| timechart count` to see **genuine** **bursts** vs **ingest** **drop/restart** **artifacts**.\n\n**Pipeline walkthrough**\n• **4h** **buckets** align with **UC-5.13.22** for story consistency with leadership.\n\nStep 3 — Validate\n• **Change** **calendar** overlay: a **known** **bad** **Saturday** **cut** should show **rows**; if **not**, **sensitivity** is too low or **count** is **wrong** (dedup problem).\n• **Compare** to **Catalyst** **UI** **issue** **counts** in the same **4h** **UTC** **window**.\n\nStep 4 — Operationalize\n• **Alert** to a Slack/Teams **investigate** channel with a **Catalyst Assurance** link; page only when paired with **device** or **client** health drops (see **UC-5.13.1**, **3**, **5**, and **9** in this set).\n• **Table** **panel** of **deviation** for **postmortem** **Appendix**.\n\n• **Validate:** compare against the Catalyst Center Assurance Issues dashboard — verify P1/P2 counts match for the same time window.\nStep 5 — Troubleshooting\n• **Hundreds of buckets:** **stdev** too small; **lengthen** lookback or **increase** **span** to **1d** for **strategic** **noise** **reduction**.\n• **No buckets:** **count** is **0** in **all** **bins**—**ingest** **failure**; check **sourcetype** **first**.\n• **No issue events arriving:** verify the `issue` input is enabled and check `index=_internal` for API error or endpoint timeout messages from the TA.\n• **Missing fields or empty values:** some issue fields vary by Catalyst Center version; run `| fieldsummary` to identify NULL or zero values that need `fillnull`.\n• **Fewer events than the Catalyst Center UI shows:** the API returns only active issues by default; if credential access is denied, check the service account RBAC role.",
  "grandmaExplanation": "We watch how many issues Catalyst Center is logging in each time bucket and call out the hours when that pile is unusually high compared with the rest of the period you are looking at.",
  "prerequisiteUseCases": [
    "UC-5.13.21",
    "UC-5.13.22"
  ],
  "wave": "run"
}
