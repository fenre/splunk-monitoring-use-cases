{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.14.49",
  "title": "Traefik Mutual TLS Client Certificate Errors",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Audit"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=proxy` `sourcetype=traefik:access` with TLS fields",
  "app": "Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes",
  "spl": "index=proxy sourcetype=\"traefik:access\"\n| where DownstreamStatus==401 OR match(_raw, \"(?i)(unknown ca|certificate required|handshake failure)\")\n| stats count by RouterName, ClientAddr\n| where count > 15",
  "description": "mTLS ecosystems break silently when CAs rotate.",
  "value": "Keeps partner B2B APIs reliable.",
  "implementation": "Configure `tls.options.clientAuthType`; never log private keys or full chains in access logs.",
  "visualization": "Time series for rates and latency, top/dedup for hotspots, single-value alerts on health thresholds.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Vendor documentation",
      "url": "https://doc.traefik.io/traefik/https/tls/"
    }
  ],
  "equipment": [
    "syslog",
    "traefik"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "grandmaExplanation": "We watch traefik mutual tls client certificate errors and catch issues early, before they turn into outages for the people who rely on the network.",
  "knownFalsePositives": "Edge cases: multi-vendor mixes and ad hoc feeds can include lab traffic and partial visibility that mimics issues until scopes are defined. Tuning tip: match this to «Traefik Mutual TLS Client Certificate Errors» and exclude change windows, scans, and lab VLANs you already expect."
}
