{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.13.12",
  "title": "Client Health by SSID and VLAN",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=catalyst sourcetype=cisco:dnac:clienthealth`. from the `clienthealth` modular input in the Cisco Catalyst Add-on (Splunkbase 7538) polling `GET /dna/intent/api/v1/client-health`. Default poll interval: 900s (15m). Volume: ~3 events per poll (ALL, WIRED, WIRELESS categories). RBAC: NETWORK-ADMIN-ROLE. Key fields: `scoreCategory` (string), `value` (integer 0-100), `clientCount` (integer), `scoreDetail{}` (array/object).",
  "app": "`Cisco Catalyst Add-on for Splunk` (Splunkbase 7538)",
  "spl": "index=catalyst sourcetype=\"cisco:dnac:clienthealth\" | spath output=scores path=scoreDetail{} | mvexpand scores | spath input=scores | search scoreCategory.scoreCategory=\"*\" | stats avg(scoreCategory.value) as avg_health sum(clientCount) as total_clients by scoreCategory.scoreCategory | sort -total_clients",
  "description": "Breaks down client health by SSID and VLAN to identify specific wireless networks or network segments with poor performance.",
  "value": "Different SSIDs serve different purposes (corporate, guest, IoT). Isolating health by SSID reveals which user communities are most affected.",
  "implementation": "Prerequisite: UC-5.13.9. Use `spath` to normalize nested `scoreDetail` so each segment name becomes a row; confirm Catalyst Center is emitting the segment identifiers you expect (sometimes encoded in `scoreCategory`). If SSID and VLAN are split across other APIs, consider a `lookup` join in a later version of this panel.",
  "visualization": "Table (scoreCategory, avg_health, total_clients), bar chart of total_clients, optional heat map when pivoted with site in another panel.",
  "references": [
    {
      "title": "Splunkbase app 7538",
      "url": "https://splunkbase.splunk.com/app/7538"
    },
    {
      "title": "Catalyst Center API docs",
      "url": "https://developer.cisco.com/docs/catalyst-center/"
    },
    {
      "title": "Catalyst Center Integration Guide",
      "url": "docs/guides/catalyst-center.md"
    },
    {
      "title": "Catalyst Center Client Health API — Cisco DevNet",
      "url": "https://developer.cisco.com/docs/catalyst-center/#!get-overall-client-health"
    }
  ],
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_catalyst_center"
  ],
  "knownFalsePositives": "**SSID or VLAN renaming causing apparent new entries in the breakdown.** If an SSID is renamed or a VLAN description changes in the network configuration, the health-by-SSID breakdown will show the old and new names as separate entries. Distinguish by checking whether client counts shifted from one SSID/VLAN name to another at the same time. Suppress by maintaining an `ssid_aliases` lookup that maps old and new SSID names to a canonical name.\n\n**Guest SSID with naturally lower health scores skewing the overall breakdown.** Guest SSIDs typically serve devices with weaker radios, older Wi-Fi standards, or poor signal quality, resulting in inherently lower health scores. Distinguish by comparing guest SSID health scores to corporate SSID scores — a 10-15 point difference is typical and expected. Suppress by setting per-SSID health thresholds in a `ssid_health_baselines` lookup rather than using a single global threshold.\n\n**VLAN reassignment during network segmentation project.** During a microsegmentation or VRF migration project, clients may move between VLANs, causing the per-VLAN health breakdown to shift without an actual health problem. Distinguish by correlating with `index=catalyst sourcetype=\"cisco:dnac:audit:logs\"` for VLAN or network profile changes. Suppress by monitoring per-VLAN trends over a 7-day window rather than alerting on single-poll changes.\n\n**AP radio band steering moving clients between 2.4GHz and 5GHz SSIDs.** Band steering features may move clients between bands, changing the per-SSID client count and potentially affecting the average health score per SSID. Distinguish by checking whether client count shifts between 2.4GHz and 5GHz variants of the same SSID. Suppress by aggregating both bands into a single SSID entry for health reporting.",
  "detailedImplementation": "Prerequisites\n• **UC-5.13.9** working: `clienthealth` input, sourcetype `cisco:dnac:clienthealth`, and visible nested `scoreDetail` in at least one raw event.\n• Cisco Catalyst Add-on (7538); Assurance client health licensed. The TA polls the Intent API (client health) on a default 900s interval.\n• SSID vs VLAN: Catalyst may encode segment or WLAN identity inside nested `scoreCategory` fields—the exact path varies by release. Validate field names in raw JSON before relying on the panel for exec reporting.\n• See `docs/implementation-guide.md` and `docs/guides/catalyst-center.md`.\n\nStep 1 — Configure data collection\n• Enable the `clienthealth` modular input; destination index `catalyst`.\n• If `scoreCategory` is nested, use `spath input=scores` after `mvexpand` so `scoreCategory.value` and the label field used in `by` are populated.\n\nStep 2 — Create the search and alert\n```spl\nindex=catalyst sourcetype=\"cisco:dnac:clienthealth\" | spath output=scores path=scoreDetail{} | mvexpand scores | spath input=scores | search scoreCategory.scoreCategory=\"*\" | stats avg(scoreCategory.value) as avg_health sum(clientCount) as total_clients by scoreCategory.scoreCategory | sort -total_clients\n```\n\nUnderstanding this SPL\n• The `spath` / `mvexpand` / `spath input=scores` pattern matches the flattening approach in UC-5.13.9 but targets nested `scoreCategory.*` for segment labels in your build.\n• The `search scoreCategory.scoreCategory=\"*\"` clause drops empty labels; replace with a fixed list of production SSIDs if test WLANs add noise.\n• Sorting by `total_clients` surfaces high-traffic wireless segments first; read alongside `avg_health` so a tiny guest SSID with few clients does not dominate the story by mistake.\n\n**Pipeline walkthrough**\n• `mvexpand` on `scoreDetail` rows, then `stats` by the SSID/segment name field, summing client counts and averaging the health value for that slice.\n\nStep 3 — Validate\n• Capture one event and confirm the nested fields used in the `stats` match what Catalyst Center shows under Client health for a named SSID in the same time range.\n• If the search returns no rows, compare to UC-5.13.9’s `spath path=scoreDetail{}` path—your payload may use different key names; adjust field paths accordingly.\n\nStep 4 — Operationalize\n• Use a table (segment name, avg_health, total_clients) on a Wi-Fi or client experience dashboard; optional site or building token if you add `siteId` enrichment in another panel.\n• For poor rows, the runbook should point to WLC config, RADIUS policy, and Catalyst issues apps—not this search alone.\n\nStep 5 — Troubleshooting\n• Null or missing `scoreCategory.value`: inspect one `scores` string after `mvexpand`; update `spath` or field names after TA or Catalyst upgrades.\n• Duplicate or doubled client counts: dedupe on poll time and device, or narrow the time range if the same client health batch ingests twice.\n• **No clienthealth events arriving:** verify the `clienthealth` input is enabled and check `index=_internal` for API error or endpoint timeout messages from the TA.\n• **NULL or zero values in health scores:** confirm clients are connected and visible in Catalyst Center Assurance; empty values indicate no clients in that category.\n• **Fewer events than expected:** check for API throttling (HTTP 429); credential access denied errors in TA logs indicate insufficient permissions.",
  "grandmaExplanation": "We break down network health by each wireless network name, so you can see if the guest network, the corporate network, or the IoT network is the one causing trouble.",
  "prerequisiteUseCases": [
    "UC-5.13.9"
  ],
  "wave": "walk"
}
