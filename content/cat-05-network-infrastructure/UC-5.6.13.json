{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.6.13",
  "title": "Failed DHCP Assignments and IP Pool Exhaustion (Meraki)",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Failed DHCP Assignments and IP Pool Exhaustion (Meraki)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_failed_dhcp_assignments_and_ip_pool_exhaustion_m` (UC-5.6.13), SOC dashboard panel \"Operations — Failed DHCP Assignments and IP Pool Exhaustion (Meraki)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.6.13 positive (Failed, DHCP, Assignments, and, Pool): ingest synthetic events into `index=meraki` sourcetype='meraki' where `values` grouped by `host`, `failure_type` satisfies `failure_count` > 5 for 'Failed DHCP Assignments and IP Pool Exhaustion (Meraki)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.6.13 negative (Exhaustion (Meraki)): ingest benign `meraki` / `meraki` events where `host`, `failure_type` stays below `failure_count` > 5, or exclude the test entity via `ops_exception_register.csv`. Variant 3 confirms zero alert rows for 'Failed DHCP Assignments and IP Pool Exhaustion (Meraki)'."
  },
  "dataSources": "SC4S Meraki vendor pack (sourcetype=meraki) receiving Meraki MX/MS syslog. DHCP events appear as type=events with message bodies 'dhcp no offers for mac <mac>', 'dhcp lease of ip <ip> from server mac <mac> for client mac <mac>', and 'Blocked DHCP server response from <mac> on VLAN <id>'.",
  "app": "`Cisco Meraki Add-on for Splunk` (Splunkbase 5580) | Optional alternate path: Splunk Connect for Syslog (SC4S) with the Meraki vendor pack ingests Meraki MX/MS/MR appliance syslog as sourcetype=\"meraki\" (does not require the API TA).",
  "spl": "index=meraki sourcetype=\"meraki\" type=events\n    (\"dhcp no offers\" OR \"dhcp lease\" OR \"DHCP NACK\" OR \"Blocked DHCP\")\n    earliest=-24h\n| rex \"for mac (?<client_mac>[0-9A-Fa-f:]+)\"\n| rex \"host = (?<dhcp_server>[\\d\\.]+)\"\n| eval failure_type = case(\n    match(_raw, \"dhcp no offers\"), \"no_offers\",\n    match(_raw, \"DHCP NACK\"), \"nack\",\n    match(_raw, \"Blocked DHCP\"), \"blocked_rogue\",\n    1=1, \"lease\")\n| stats count as failure_count,\n        values(client_mac) as failed_clients,\n        values(dhcp_server) as servers\n         by host, failure_type\n| where failure_type != \"lease\" AND failure_count > 5\n| sort - failure_count",
  "description": "Surfaces when Exhaustion (Meraki) criteria fire for 'Failed DHCP Assignments and IP Pool Exhaustion (Meraki)' (UC-5.6.13) in `index=meraki` sourcetype `meraki`. Pipeline aggregates `values` by `host`, `failure_type` over `the scheduled window`. Alert logic enforces `failure_count` > 5. Title terms: Failed, DHCP, Assignments, and, Pool. Eliminates manual hunting for UC-5.6.13 conditions.",
  "value": "UC-5.6.13 helps teams contain Failed, DHCP, Assignments, and, Pool scenarios and reduce operational risk and delayed incident response before incidents compound.",
  "implementation": "1. Configure SC4S for Meraki MX/MS syslog and enable Appliance + Switch event logs in Meraki Dashboard. 2. 'dhcp no offers' indicates the DHCP server pool is exhausted or unreachable; 'DHCP NACK' indicates a lease conflict; 'Blocked DHCP' indicates DHCP guard caught a rogue server. 3. Threshold per AP/switch and trigger Splunk alerts on sustained exhaustion. 4. For real-time pool sizing use GET /networks/{networkId}/appliance/vlans (VLAN reservation/exclusion ranges) with a custom modular input — that endpoint is not yet polled by the TA.",
  "visualization": "Table of DHCP failures by AP; time-series showing failure spike; alert dashboard.",
  "cimModels": [
    "Network_Sessions"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Network_Sessions.DHCP\n  by DHCP.mac DHCP.ip DHCP.action span=1h\n| where count>0\n| sort -count",
  "dataModelAcceleration": "Enable acceleration for the Network_Sessions data model (DHCP dataset) before relying on tstats here.",
  "references": [
    {
      "title": "Splunkbase app 5580",
      "url": "https://splunkbase.splunk.com/app/5580",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Cisco Security documentation",
      "url": "https://www.cisco.com/c/en/us/support/security/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `meraki` (UC-5.6.13)** — Planned work for 'Failed DHCP Assignments and IP Pool Exhaustion (Meraki)' shifts `values` over `span=1h` while `host`, `failure_type` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `values` on `meraki`** — Scheduled automation elevates `failure_type` without crossing `failure_count` > 5 for UC-5.6.13. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`, `failure_type`** — Health checks in `meraki` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `meraki`** — First 48h baseline for UC-5.6.13 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco",
    "syslog"
  ],
  "equipmentModels": [
    "cisco_meraki"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Cisco Meraki Add-on for Splunk` (Splunkbase 5580) | Optional alternate path: Splunk Connect for Syslog (SC4S) with the Meraki vendor pack ingests Meraki MX/MS/MR appliance syslog as sourcetype=\"meraki\" (does not require the API TA)..\n• Ensure the following data sources are available: SC4S Meraki vendor pack (sourcetype=meraki) receiving Meraki MX/MS syslog. DHCP events appear as type=events with message bodies 'dhcp no offers for mac <mac>', 'dhcp lease of ip <ip> from server mac <mac> for client mac <mac>', and 'Blocked DHCP server response from <mac> on VLAN <id>'..\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\n1. Configure SC4S for Meraki MX/MS syslog and enable Appliance + Switch event logs in Meraki Dashboard. 2. 'dhcp no offers' indicates the DHCP server pool is exhausted or unreachable; 'DHCP NACK' indicates a lease conflict; 'Blocked DHCP' indicates DHCP guard caught a rogue server. 3. Threshold per AP/switch and trigger Splunk alerts on sustained exhaustion. 4. For real-time pool sizing use GET /networks/{networkId}/appliance/vlans (VLAN reservation/exclusion ranges) with a custom modular input …\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=meraki sourcetype=\"meraki\" type=events\n    (\"dhcp no offers\" OR \"dhcp lease\" OR \"DHCP NACK\" OR \"Blocked DHCP\")\n    earliest=-24h\n| rex \"for mac (?<client_mac>[0-9A-Fa-f:]+)\"\n| rex \"host = (?<dhcp_server>[\\d\\.]+)\"\n| eval failure_type = case(\n    match(_raw, \"dhcp no offers\"), \"no_offers\",\n    match(_raw, \"DHCP NACK\"), \"nack\",\n    match(_raw, \"Blocked DHCP\"), \"blocked_rogue\",\n    1=1, \"lease\")\n| stats count as failure_count,\n        values(client_mac) as failed_clients,\n        values(dhcp_server) as servers\n         by host, failure_type\n| where failure_type != \"lease\" AND failure_count > 5\n| sort - failure_count\n```\n\nUnderstanding this SPL\n\n**Failed DHCP Assignments and IP Pool Exhaustion (Meraki)** — Network operations teams monitoring Meraki environments detect DHCP assignment failures and pool exhaustion per site/subnet, enabling proactive pool expansion before users lose network connectivity.\n\nDocumented **Data sources**: SC4S Meraki vendor pack (sourcetype=meraki) receiving Meraki MX/MS syslog. DHCP events appear as type=events with message bodies 'dhcp no offers for mac <mac>', 'dhcp lease of ip <ip> from server mac <mac> for client mac <mac>', and 'Blocked DHCP server response from <mac> on VLAN <id>'. **App/TA** (typical add-on context): `Cisco Meraki Add-on for Splunk` (Splunkbase 5580) | Optional alternate path: Splunk Connect for Syslog (SC4S) with the Meraki vendor pack ingests Meraki MX/MS/MR appliance syslog as sourcetype=\"meraki\" (does not require the API TA). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: meraki; **sourcetype**: meraki. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=meraki, sourcetype=\"meraki\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Extracts fields with `rex` (regular expression).\n• Extracts fields with `rex` (regular expression).\n• `eval` defines or adjusts **failure_type** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by host, failure_type** so each row reflects one combination of those dimensions (useful for per-host, per-user, or per-entity comparisons for this use case).\n• Filters the current rows with `where failure_type != \"lease\" AND failure_count > 5` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` count\n  from datamodel=Network_Sessions.DHCP\n  by DHCP.mac DHCP.ip DHCP.action span=1h\n| where count>0\n| sort -count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Failed DHCP Assignments and IP Pool Exhaustion (Meraki)** — Network operations teams monitoring Meraki environments detect DHCP assignment failures and pool exhaustion per site/subnet, enabling proactive pool expansion before users lose network connectivity.\n\nDocumented **Data sources**: SC4S Meraki vendor pack (sourcetype=meraki) receiving Meraki MX/MS syslog. DHCP events appear as type=events with message bodies 'dhcp no offers for mac <mac>', 'dhcp lease of ip <ip> from server mac <mac> for client mac <mac>', and 'Blocked DHCP server response from <mac> on VLAN <id>'. **App/TA** (typical add-on context): `Cisco Meraki Add-on for Splunk` (Splunkbase 5580) | Optional alternate path: Splunk Connect for Syslog (SC4S) with the Meraki vendor pack ingests Meraki MX/MS/MR appliance syslog as sourcetype=\"meraki\" (does not require the API TA). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Network_Sessions.DHCP` — enable acceleration for that model.\n• Filters the current rows with `where count>0` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table of DHCP failures by AP; time-series showing failure spike; alert dashboard.",
  "grandmaExplanation": "We help you know when address pools are filling up or leases look wrong, so new phones and laptops can still get on the network when they need to.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
