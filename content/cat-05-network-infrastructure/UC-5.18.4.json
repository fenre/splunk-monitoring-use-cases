{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.18.4",
  "title": "PE-CE BGP Session Health (VRF-Aware)",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Fault",
    "Operations"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'PE-CE BGP Session Health (VRF-Aware)'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-5.18.4).",
  "evidence": "Saved search uc_5_18_4_pe_ce_bgp_session_health_vrf_aware, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'PE-CE BGP Session Health (VRF-Aware)'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-5.18.4.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "Detailed: `index=network`; BGP syslog (`%BGP-*`, Junos `BGP_*`, Nokia `tmnxBgpi*`/`bgpNeighbor*`), include `vrf`, `vpn-ipv4`, `vpn-ipv6`, `route-distinguisher`, `neighbor`, `state`, `reset`, `hold`, `notification` codes; SNMP BGP FSM traps optional. Collected via Splunk Add-on for Cisco Security (Splunkbase 1352) modular input or scripted poll into the index named in the SPL.",
  "app": "Cisco Networks Add-on for Splunk (`TA-cisco_ios`), Splunk Add-on for Juniper (`Splunk_TA_juniper`), Nokia SR OS BGP syslog parsers. — Splunk Add-on for Cisco Security (Splunkbase 1352)",
  "spl": "index=network earliest=-24h@h latest=now\n| eval st=lower(coalesce(sourcetype,_sourcetype,\"\"))\n| where match(st,\"cisco:ios|cisco:ios_xr|cisco:ios_xe|juniper:junos|nokia\")\n| eval msg=lower(_raw)\n| eval vrf_like=match(msg,\"\\\\bvrf\\\\b|vpnv[46]|vpn.?ipv[46]|route.?distinguisher|address.?family.*vpn|rd:?\\\\s*[0-9]+:[0-9]+\")\n| eval bgp_sess=match(msg,\"\\\\bbgp\\\\b\") AND match(msg,\"neighbor|peer|session\")\n| where bgp_sess=1 AND (vrf_like=1 OR match(msg,\"\\\\bvrf\\\\b\")) AND match(msg,\"(?:down|idle|active|reset|notification|hold.?time|dead|flap|(?:state).*(?:chang))\")\n| rex field=_raw max_match=0 \"(?i)vrf[^A-Za-z0-9_/:-]*(?<vrf>[A-Za-z0-9_.:-]+)\"\n| rex field=_raw max_match=0 \"(?i)(?:neighbor|peer)[=: \\t]+(?<neighbor>[0-9.]+|[0-9a-f:]+|%?[A-Za-z0-9/.:_-]+)\"\n| rex field=_raw max_match=0 \"(?i)(?:rd|route.?distinguisher)\\s*[:=]?\\s*(?<rd>[0-9:.]+)\"\n| stats count earliest(_time) as first_seen latest(_time) as last_seen values(neighbor) as peers values(rd) as rds by host vrf\n| sort - count",
  "description": "Splunk highlights PE-CE BGP session transitions inside MPLS Layer-3 VPN contexts so customer-facing VRF adjacency loss, notification storms, and hold-timer expiry surface with neighbor and RD identifiers attached.",
  "value": "Enterprise service desks shrink mean-time-to-know because Splunk separates noisy global BGP churn from contract-critical CE peers per VPN, letting tier-one engineers page on RD-qualified outages instead of hunting ambiguous `%BGP-5-ADJCHANGE` lines.",
  "implementation": "Tag inbound syslog with customer-VPN lookups on RD or VRF name, require vrf-aware regex filters to exclude Internet table churn, alert when distinct CE peers per PE exceed playbook thresholds within fifteen minutes.",
  "visualization": "Dashboard Studio: KPI for impacted VPNs; heatmap by `vrf` × `host`; table (`host`, `vrf`, `peers`, `rds`, `count`) filtered by SLA tier token.",
  "references": [
    {
      "title": "Cisco MPLS Layer 3 VPNs Configuration Guide — BGP PE-CE",
      "url": "https://www.cisco.com/c/en/us/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Juniper BGP Layer 3 VPNs Feature Guide",
      "url": "https://www.juniper.net/documentation/us/en/software/junos/bgp/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "IETF RFC 4364 — BGP/MPLS IP VPNs",
      "url": "https://www.rfc-editor.org/rfc/rfc4364",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "mitreAttack": [
    "N/A (availability monitoring)"
  ],
  "detectionType": "Baseline",
  "securityDomain": "network",
  "equipment": [
    "cisco",
    "juniper",
    "snmp",
    "syslog"
  ],
  "equipmentModels": [
    "cisco_asr_1000",
    "cisco_asr_9000",
    "cisco_ios",
    "juniper_mx",
    "snmp_generic"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Golden lookup `vpn_customer_map.csv` joining `rd`, `vrf`, SLA tier, and escalation contacts.\n• CE addressing scheme documented so link-local % interfaces decode consistently.\n\nStep 1 — Logging verification\nOn Cisco: confirm PE logs include VRF name with BGP neighbor messages (`address-family` aware). IOS-XR often prefixes `%ROUTING-BGP-*` with VPN context—capture full text. Junos: validate `rpd` exports include routing-instance. Nokia: verify BGP instance naming matches customer VPN service IDs.\n\nStep 2 — Field extraction\nNormalize `vrf` casing; strip `%vrfname` Cisco interface placeholders via secondary `rex`; validate RD pattern `(asn:index)`.\n\nStep 3 — Saved search\n`pe_ce_bgp_vpn_health`: alert when `count`≥2 per `(host,vrf)` or when peer matches `tier1` lookup.\n\nStep 4 — CLI validation\nCompare Splunk peer list to operational state: Cisco `show bgp vpnv4 unicast vrf X summary`; Junos `show bgp summary instance Y`; Nokia `show router bgp neighbor` under correct service routing instance.\n\nStep 5 — Dashboard handoff\nEmbed drilldown to traceroute/LSP health panels (cross-link UC-5.18.1 saved searches) so triage distinguishes CE-side modem faults from core issues.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Cisco Networks Add-on for Splunk (`TA-cisco_ios`), Splunk Add-on for Juniper (`Splunk_TA_juniper`), Nokia SR OS BGP syslog parsers. — Splunk Add-on for Cisco Security (Splunkbase 1352)). Enable the modular input or HEC token that writes sourcetype=see SPL into network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, vrf`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch the handshake between our edge router and each customer’s box inside their private slice of our network. When that handshake stumbles, we know which customer slice blinked—not just that ‘something somewhere’ coughed.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
