{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.4.15",
  "title": "SSID Performance Ranking and Trend Analysis (Meraki MR)",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'SSID Performance Ranking and Trend Analysis (Meraki MR)'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-5.4.15).",
  "evidence": "Saved search uc_5_4_15_ssid_performance_ranking_and_trend_analy, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'SSID Performance Ranking and Trend Analysis (Meraki MR)'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-5.4.15.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "Splunk_TA_cisco_meraki (Splunkbase #5580): Wireless Packet Loss by Device input (sourcetype=meraki:wirelessdevicespacketlossbydevice, TA v3+, OAuth scope wireless:telemetry:read). NOTE: per-SSID throughput, retry rate, and connection time are NOT exposed by the polled API; for those, use webhooks or the per-network wireless health endpoints (not currently in the TA).",
  "app": "`Cisco Meraki Add-on for Splunk` (Splunkbase 5580)",
  "spl": "index=meraki sourcetype=\"meraki:wirelessdevicespacketlossbydevice\" earliest=-24h\n| stats avg(downstream.lossPercentage) as avg_dl_loss,\n        avg(upstream.lossPercentage) as avg_ul_loss,\n        avg(downstream.total) as avg_dl_packets,\n        avg(upstream.total) as avg_ul_packets\n         by device.serial, device.name, network.name\n| eval health_score = round(100 - ((avg_dl_loss + avg_ul_loss) / 2), 1)\n| sort health_score",
  "description": "Compares performance across multiple SSIDs to identify underperforming networks and optimize deployment strategy.",
  "value": "Network operations teams rank Meraki SSID performance across all sites using a composite score (success rate, signal quality, latency), enabling cross-site comparison and targeted wireless optimization.",
  "implementation": "1. Enable the Wireless Packet Loss by Device input in Splunk_TA_cisco_meraki. The TA polls GET /organizations/{orgId}/wireless/devices/packetLoss/byDevice daily and emits one event per AP with downstream.{lossPercentage,total} and upstream.* fields. 2. Aggregate per network for a coarse 'wireless health' indicator. 3. Per-SSID metrics need either webhook ingestion (client_connection_changed) with alertData.ssid grouping, or a custom modular input that calls GET /networks/{networkId}/wireless/ssids/.../latencyStats.",
  "visualization": "Bar chart comparing SSID performance; sparklines for trend; scorecard showing top/bottom performers.",
  "cimModels": [],
  "references": [
    {
      "title": "Splunkbase app 5580",
      "url": "https://splunkbase.splunk.com/app/5580",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_meraki"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Cisco Meraki Add-on for Splunk` (Splunkbase 5580).\n• Ensure the following data sources are available: Splunk_TA_cisco_meraki (Splunkbase #5580): Wireless Packet Loss by Device input (sourcetype=meraki:wirelessdevicespacketlossbydevice, TA v3+, OAuth scope wireless:telemetry:read). NOTE: per-SSID throughput, retry rate, and connection time are NOT exposed by the polled API; for those, use webhooks or the per-network wireless health endpoints (not currently in the TA)..\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\n1. Enable the Wireless Packet Loss by Device input in Splunk_TA_cisco_meraki. The TA polls GET /organizations/{orgId}/wireless/devices/packetLoss/byDevice daily and emits one event per AP with downstream.{lossPercentage,total} and upstream.* fields. 2. Aggregate per network for a coarse 'wireless health' indicator. 3. Per-SSID metrics need either webhook ingestion (client_connection_changed) with alertData.ssid grouping, or a custom modular input that calls GET /networks/{networkId}/wireless/ssi…\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=meraki sourcetype=\"meraki:wirelessdevicespacketlossbydevice\" earliest=-24h\n| stats avg(downstream.lossPercentage) as avg_dl_loss,\n        avg(upstream.lossPercentage) as avg_ul_loss,\n        avg(downstream.total) as avg_dl_packets,\n        avg(upstream.total) as avg_ul_packets\n         by device.serial, device.name, network.name\n| eval health_score = round(100 - ((avg_dl_loss + avg_ul_loss) / 2), 1)\n| sort health_score\n```\n\nUnderstanding this SPL\n\n**SSID Performance Ranking and Trend Analysis (Meraki MR)** — Network operations teams rank Meraki SSID performance across all sites using a composite score (success rate, signal quality, latency), enabling cross-site comparison and targeted wireless optimization.\n\nDocumented **Data sources**: Splunk_TA_cisco_meraki (Splunkbase #5580): Wireless Packet Loss by Device input (sourcetype=meraki:wirelessdevicespacketlossbydevice, TA v3+, OAuth scope wireless:telemetry:read). NOTE: per-SSID throughput, retry rate, and connection time are NOT exposed by the polled API; for those, use webhooks or the per-network wireless health endpoints (not currently in the TA). **App/TA** (typical add-on context): `Cisco Meraki Add-on for Splunk` (Splunkbase 5580). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: meraki; **sourcetype**: meraki:wirelessdevicespacketlossbydevice. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=meraki, sourcetype=\"meraki:wirelessdevicespacketlossbydevice\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by device.serial, device.name, network.name** so each row reflects one combination of those dimensions (useful for per-host, per-user, or per-entity comparisons for this use case).\n• `eval` defines or adjusts **health_score** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Bar chart comparing SSID performance; sparklines for trend; scorecard showing top/bottom performers.",
  "grandmaExplanation": "We watch ssid performance ranking and trend analysis (meraki mr) so we notice problems while they are still small, and we can fix them before Wi-Fi users get dropped calls or dead spots.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
