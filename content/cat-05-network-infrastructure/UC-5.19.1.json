{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.19.1",
  "title": "Ansible Playbook/Job Failure Rate and Duration Trending",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Operations",
    "Reliability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'Ansible Playbook/Job Failure Rate and Duration Trending'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-5.19.1).",
  "evidence": "Saved search uc_5_19_1_ansible_playbook_job_failure_rate_and_du, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'Ansible Playbook/Job Failure Rate and Duration Trending'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-5.19.1.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "Detailed: `index=iac` or `index=main` (org-standard); `sourcetype` examples — `ansible:result`, `ansible:callback`, `awx:job_events`, `tower:job_events`, `automationcontroller:jobs` (JSON from AWX/Tower API export or HEC); normalize `job_id`, `job_template`, `playbook`, `status` (failed/changed/ok), `duration_sec`, `forks`, `environment`, `inventory`. Optional correlation with Git commit SHA from CI metadata. Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Ansible Automation Platform / AWX audit via Splunk HTTP Event Collector; Ansible Tower logging forwarder; Splunk Add-on for Ansible (community) or custom props for job event JSON; optional ARA (Ansible Run Analysis) exporter. — Universal Forwarder (Splunkbase 617)",
  "spl": "index IN (\"iac\",\"main\",\"ansible\")\n| eval st=lower(coalesce(sourcetype,_sourcetype,\"\"))\n| where match(st,\"ansible|awx|tower|automationcontroller\")\n| eval status_norm=lower(trim(coalesce(status,changed_status,final_status,job_status,\"\")))\n| eval failed=if(match(status_norm,\"fail|error|canceled|cancelled|unreachable\"),1,0)\n| eval dur=tonumber(coalesce(duration_sec,duration,elapsed_time,job_elapsed))\n| eval jt=coalesce(job_template,template_name,\"unknown\")\n| bin _time span=1h\n| stats count as jobs sum(failed) as failed_jobs avg(dur) as avg_dur perc95(dur) as p95_dur by _time, jt, st\n| eval fail_rate=round(100*failed_jobs/nullif(jobs,0),2)\n| where jobs>=3 AND (fail_rate>=10 OR p95_dur>3600)\n| sort -fail_rate,-p95_dur",
  "description": "Splunk rolls up Ansible Automation Platform and AWX/Tower job telemetry into hourly failure ratios and duration percentiles per template so brittle playbooks, credential regressions, or slow network pushes surface before change windows stack up unresolved incidents.",
  "value": "Network and platform teams stabilize automation-led rollouts because recurring playbook failures and runtime creep trigger proactive fixes instead of discovering broken convergence only after devices drift or rollback scripts fire during production incidents.",
  "implementation": "Forward normalized job completion JSON with explicit duration seconds and terminal status; retain job_template identifiers; run hourly; tune minimum job counts and failure-rate thresholds per environment.",
  "visualization": "Dashboard Studio: KPI tiles for overall failure % and worst template; `splunk.timechart` of fail_rate by job_template; table (`jt`,`jobs`,`failed_jobs`,`fail_rate`,`p95_dur`) sorted by impact.",
  "references": [
    {
      "title": "Red Hat Ansible Automation Platform — Logging and aggregation",
      "url": "https://docs.redhat.com/en/documentation/red_hat_ansible_automation_platform/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Ansible AWX — Integration overview",
      "url": "https://ansible.readthedocs.io/projects/awx/en/latest/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Lantern — observability patterns",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "mitreAttack": [
    "N/A (operations monitoring)"
  ],
  "detectionType": "Baseline",
  "securityDomain": "network",
  "equipment": [
    "ansible",
    "dell_emc",
    "linux"
  ],
  "equipmentModels": [
    "ansible AWX job event stream (HEC JSON)",
    "linux_automation_runner"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• AWX/Tower logging enabled for job lifecycle events (started, running, successful, failed, canceled) with consistent timestamps (UTC).\n• Service account HEC token scoped to `iac` index; TLS termination validated.\n\nStep 1 — Normalize payloads\nMap AWX `job_events` summary records or Tower `/api/v2/jobs/` polling exports into flat fields: `job_id`, `job_template`, `status`, `started`, `finished`, `duration_sec`. Strip ANSI from stdout fields before indexing.\n\nStep 2 — props.conf\nAssign `KV_MODE=json` for structured callbacks; set `TIME_PREFIX` if `_time` skews; create field aliases `duration`→`duration_sec` when vendors differ.\n\nStep 3 — Saved search\nSave SPL as `ansible_job_fail_rate_duration_trend`; alert when hourly `fail_rate`≥10% for templates with ≥3 executions or when `p95_dur` exceeds baseline ×1.5 from lookup `ansible_runtime_baseline.csv`.\n\nStep 4 — Validate\nReplay a known failing playbook in lab; confirm failed increment and duration extraction versus AWX UI job metrics within one-minute skew.\n\nStep 5 — Operationalize\nDashboard: stacked column of failure rate by template; overlay line chart of p95 duration; drilldown saved search to raw `_raw` for playbook stderr correlation.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Ansible Automation Platform / AWX audit via Splunk HTTP Event Collector; Ansible Tower logging forwarder; Splunk Add-on for Ansible (community) or custom props for job event JSON; optional ARA (Ansible Run Analysis) exporter. — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into index=main. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, fail_rate, failed, jobs`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We track whether our batch updates to routers and switches finish on time and how often they fail. When failures or slowdowns bunch up, we fix the recipes before the whole network ends up half updated.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
