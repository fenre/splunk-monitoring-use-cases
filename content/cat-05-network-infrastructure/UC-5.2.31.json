{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.2.31",
  "title": "Application Visibility and Network Application Trending (Meraki MX)",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Application Visibility and Network Application Trending (Meraki MX)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_application_visibility_and_network_application_t` (UC-5.2.31), SOC dashboard panel \"Operations — Application Visibility and Network Application Trending (Meraki MX)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.2.31 positive (Application, Visibility, and, Network, Application): ingest synthetic events into `index=meraki` sourcetype='meraki' where `app_bytes` grouped by `application`, `application_category` satisfies detection threshold for 'Application Visibility and Network Application Trending (Meraki MX)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.2.31 negative ((Meraki MX)): ingest benign `meraki` / `meraki` events where `application`, `application_category` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 7 confirms zero alert rows for 'Application Visibility and Network Application Trending (Meraki MX)'."
  },
  "dataSources": "`sourcetype=meraki (type=flows OR type=firewall) application=*` | Alternate ingest: Splunk Connect for Syslog (SC4S) Meraki vendor pack — points the Meraki dashboard at an SC4S receiver and produces sourcetype=\"meraki\" syslog events (free-form text extracted with rex). Use when you don't want to deploy the polling API TA.",
  "app": "`Cisco Meraki Add-on for Splunk` (Splunkbase 5580) | Optional alternate path: Splunk Connect for Syslog (SC4S) with the Meraki vendor pack ingests Meraki MX/MS/MR appliance syslog as sourcetype=\"meraki\" (does not require the API TA).",
  "spl": "index=meraki sourcetype=\"meraki\" (type=flows OR type=firewall) application=*\n| stats sum(bytes) as app_bytes, count as flow_count by application, application_category\n| eval app_bandwidth_pct=round(app_bytes*100/sum(app_bytes), 2)\n| sort - app_bytes\n| head 20",
  "description": "Surfaces UC-5.2.31 — Application, Visibility, and, Network, Application — using `index=meraki` sourcetype `meraki`, grouped by `application`, `application_category`. Outputs `app_bytes` each `the scheduled window`; alert when configured threshold. Primary focus: (Meraki MX). Purpose-built for 'Application Visibility and Network Application Trending (Meraki MX)' in Operations workflows.",
  "value": "UC-5.2.31 operationalises 'Application Visibility and Network Application Trending (Meraki MX)' against `meraki` feeds so operators reduce operational risk and delayed incident response without manual correlation.",
  "implementation": "Extract application field from flow logs. Aggregate by app and category.",
  "visualization": "App bandwidth pie chart; top apps bar chart; bandwidth timeline by app.",
  "cimModels": [],
  "cimSpl": "| tstats `summariesonly` sum(All_Traffic.bytes_in) as bytes_in sum(All_Traffic.bytes_out) as bytes_out\n  from datamodel=Network_Traffic.All_Traffic\n  by All_Traffic.src All_Traffic.dest All_Traffic.app span=1h\n| eval bytes=bytes_in+bytes_out\n| sort -bytes",
  "dataModelAcceleration": "Enable acceleration on the Network_Traffic data model, dataset All_Traffic, before using this tstats search.",
  "references": [
    {
      "title": "Splunkbase app 5580",
      "url": "https://splunkbase.splunk.com/app/5580",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Network_Traffic",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Network_Traffic",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `meraki` (UC-5.2.31)** — Planned work for 'Application Visibility and Network Application Trending (Meraki MX)' shifts `app_bytes` over `span=1h` while `application`, `application_category` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `sum` on `meraki`** — Scheduled automation elevates `application_category` without crossing configured threshold for UC-5.2.31. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `application`, `application_category`** — Health checks in `meraki` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `meraki`** — First 48h baseline for UC-5.2.31 on `application` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `application`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco",
    "syslog"
  ],
  "equipmentModels": [
    "cisco_meraki"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Cisco Meraki Add-on for Splunk` (Splunkbase 5580) | Optional alternate path: Splunk Connect for Syslog (SC4S) with the Meraki vendor pack ingests Meraki MX/MS/MR appliance syslog as sourcetype=\"meraki\" (does not require the API TA)..\n• Ensure the following data sources are available: `sourcetype=meraki (type=flows OR type=firewall) application=*` | Alternate ingest: Splunk Connect for Syslog (SC4S) Meraki vendor pack — points the Meraki dashboard at an SC4S receiver and produces sourcetype=\"meraki\" syslog events (free-form text extracted with rex). Use when you don't want to deploy the polling API TA..\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nExtract application field from flow logs. Aggregate by app and category.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=meraki sourcetype=\"meraki\" (type=flows OR type=firewall) application=*\n| stats sum(bytes) as app_bytes, count as flow_count by application, application_category\n| eval app_bandwidth_pct=round(app_bytes*100/sum(app_bytes), 2)\n| sort - app_bytes\n| head 20\n```\n\nUnderstanding this SPL\n\n**Application Visibility and Network Application Trending (Meraki MX)** — Operations teams trend Meraki MX application visibility data to identify bandwidth-dominant applications and track usage patterns for capacity planning and policy enforcement.\n\nDocumented **Data sources**: `sourcetype=meraki (type=flows OR type=firewall) application=*` | Alternate ingest: Splunk Connect for Syslog (SC4S) Meraki vendor pack — points the Meraki dashboard at an SC4S receiver and produces sourcetype=\"meraki\" syslog events (free-form text extracted with rex). Use when you don't want to deploy the polling API TA. **App/TA** (typical add-on context): `Cisco Meraki Add-on for Splunk` (Splunkbase 5580) | Optional alternate path: Splunk Connect for Syslog (SC4S) with the Meraki vendor pack ingests Meraki MX/MS/MR appliance syslog as sourcetype=\"meraki\" (does not require the API TA). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: meraki; **sourcetype**: meraki. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=meraki, sourcetype=\"meraki\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by application, application_category** so each row reflects one combination of those dimensions (useful for per-host, per-user, or per-entity comparisons for this use case).\n• `eval` defines or adjusts **app_bandwidth_pct** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n• Limits the number of rows with `head`.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` sum(All_Traffic.bytes_in) as bytes_in sum(All_Traffic.bytes_out) as bytes_out\n  from datamodel=Network_Traffic.All_Traffic\n  by All_Traffic.src All_Traffic.dest All_Traffic.app span=1h\n| eval bytes=bytes_in+bytes_out\n| sort -bytes\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Application Visibility and Network Application Trending (Meraki MX)** — Operations teams trend Meraki MX application visibility data to identify bandwidth-dominant applications and track usage patterns for capacity planning and policy enforcement.\n\nDocumented **Data sources**: `sourcetype=meraki (type=flows OR type=firewall) application=*` | Alternate ingest: Splunk Connect for Syslog (SC4S) Meraki vendor pack — points the Meraki dashboard at an SC4S receiver and produces sourcetype=\"meraki\" syslog events (free-form text extracted with rex). Use when you don't want to deploy the polling API TA. **App/TA** (typical add-on context): `Cisco Meraki Add-on for Splunk` (Splunkbase 5580) | Optional alternate path: Splunk Connect for Syslog (SC4S) with the Meraki vendor pack ingests Meraki MX/MS/MR appliance syslog as sourcetype=\"meraki\" (does not require the API TA). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Network_Traffic.All_Traffic` — enable acceleration for that model.\n• `eval` defines or adjusts **bytes** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: App bandwidth pie chart; top apps bar chart; bandwidth timeline by app.",
  "grandmaExplanation": "We trend which applications and traffic types dominate so heavy cloud use, file shares, and video do not take you by surprise.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
