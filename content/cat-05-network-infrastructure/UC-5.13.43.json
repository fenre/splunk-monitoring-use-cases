{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.13.43",
  "title": "Client Connection Failure Analysis",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Fault"
  ],
  "splunkPillar": "Observability",
  "dataSources": "index=catalyst, sourcetype cisco:dnac:client (Catalyst Center client data; fields connectionStatus, onboardingStatus, failureReason, connectionType, ssid)",
  "app": "`Cisco Catalyst Add-on for Splunk` (Splunkbase 7538)",
  "spl": "index=catalyst sourcetype=\"cisco:dnac:client\" (connectionStatus=\"FAILED\" OR onboardingStatus=\"FAILED\") | stats count as failure_count by failureReason, connectionType, ssid | sort -failure_count",
  "description": "Analyzes client connection failures by reason, connection type, and SSID to identify the root cause of connectivity problems.",
  "value": "Connection failures frustrate users and generate helpdesk tickets. Categorizing failures by reason (auth, DHCP, association) points directly to the failing infrastructure component.",
  "implementation": "Enable the `client` input in the Cisco Catalyst TA pointing to `index=catalyst`. The TA polls client detail data from the Catalyst Center Intent API every 60 minutes. Key fields: `connectionStatus`, `onboardingStatus`, `failureReason`, `connectionType`, `ssid`, `macAddress`.",
  "visualization": "Table (failureReason, connectionType, ssid, failure_count), bar chart of top failure reasons, alert when new dominant failure class appears.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunkbase app 7538",
      "url": "https://splunkbase.splunk.com/app/7538"
    },
    {
      "title": "Catalyst Center API docs",
      "url": "https://developer.cisco.com/docs/catalyst-center/"
    },
    {
      "title": "Catalyst Center Integration Guide",
      "url": "docs/guides/catalyst-center.md"
    }
  ],
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_sdwan"
  ],
  "knownFalsePositives": "Pilots, MDM re-enrolments, and guest captive-portal self-service can fill the same failureReason buckets as a major outage. Hourly poller cadence can batch several short failures into a larger count than a live Catalyst “last 5 minutes” view, so the numbers look worse than a front-line triage would expect.",
  "detailedImplementation": "Prerequisites\n• Cisco Catalyst Add-on (7538) with `cisco:dnac:client` including `connectionStatus`, `onboardingStatus`, and `failureReason` in failed-path events.\n• Enumerate the exact values your controller uses (FAILED vs `Failure` or numeric codes) so the `OR` block does not miss rows.\n• `docs/implementation-guide.md`.\n\nStep 1 — Configure data collection\n• MAC-level client feed is PII; scope dashboards to NOC/Wireless teams.\n\nStep 2 — Failure counts by reason and segment\n```spl\nindex=catalyst sourcetype=\"cisco:dnac:client\" (connectionStatus=\"FAILED\" OR onboardingStatus=\"FAILED\") | stats count as failure_count by failureReason, connectionType, ssid | sort -failure_count\n```\n\nUnderstanding this SPL (dominant failure class)\n**Client Connection Failure Analysis** — Surfaces the top **failureReason** strings and whether they are wired or wireless, and which **SSID** is implicated, so you can hand off to the right team (RADIUS, DHCP, RF).\n\n**Pipeline walkthrough**\n• `FAILED` on connection or onboarding → `stats` by `failureReason`, `connectionType`, `ssid` → `sort` by count.\n\nStep 3 — Validate\n• Sample top `failureReason` in Splunk, then locate the same reason class in the Catalyst **Clients** or **Troubleshooting** UI for a known bad session.\n• If `failureReason` is null, the TA or Catalyst may map errors elsewhere—`fieldsummary failureReason` and inspect raw JSON.\n\nStep 4 — Operationalize\n• Alert on new failure signature in the last 24h (add `| rare failureReason` in a second panel) to catch a fresh radius misconfiguration before ticket volume explodes. Include `connectionType` and `ssid` in the alert payload for context.\n\nStep 5 — Troubleshooting\n• All failures missing: status field names differ in your version—broaden to `*fail*` on raw `_raw` in lab only, then add proper `props`.\n• Mass failures during controller upgrade: suppress alerts during that window; correlate to change records.\n",
  "grandmaExplanation": "We analyze why people fail to connect to the network and group the problems by cause — bad password, no IP address, or weak signal — so you know what to fix.",
  "prerequisiteUseCases": [
    "UC-5.13.40"
  ],
  "wave": "walk"
}
