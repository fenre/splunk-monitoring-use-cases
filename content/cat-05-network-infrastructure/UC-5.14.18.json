{
  "$schema": "../../schemas/uc.schema.json",
  "cimModels": [
    "N/A"
  ],
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "status": "draft",
  "splunkPillar": "Observability",
  "id": "5.14.18",
  "title": "Varnish Backend Connection Reuse Signals",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "dataSources": "`index=proxy` `sourcetype=varnish:stats` backend connection counters",
  "app": "Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes",
  "spl": "index=proxy sourcetype=\"varnish:stats\"\n| eval conn=tonumber(backend_conn), reuse=tonumber(backend_recycle)\n| eval reuse_ratio=if(conn>0, round(100*reuse/conn,1), null())\n| timechart span=15m avg(reuse_ratio) by host",
  "description": "Poor reuse increases latency and CPU on both sides.",
  "value": "Guides keep-alive and TLS session settings.",
  "implementation": "Low reuse may indicate TLS handshake storms toward origin.",
  "visualization": "Time series for rates and latency, top/dedup for hotspots, single-value alerts on health thresholds.",
  "references": [
    {
      "title": "Vendor documentation",
      "url": "https://varnish-cache.org/docs/trunk/reference/varnishstat.html"
    }
  ],
  "equipment": [
    "varnish"
  ],
  "grandmaExplanation": "We verify warm backend connections are reused."
}
