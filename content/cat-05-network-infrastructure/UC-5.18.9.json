{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.18.9",
  "title": "Pseudowire (L2VPN) Status Monitoring",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Fault",
    "Operations"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'Pseudowire (L2VPN) Status Monitoring'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-5.18.9).",
  "evidence": "Saved search uc_5_18_9_pseudowire_l2vpn_status_monitoring, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'Pseudowire (L2VPN) Status Monitoring'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-5.18.9.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "Detailed: `index=network`; syslog for pseudowire VC state (`%Pseudowire`, `%L2VPN`, `%AC-*`, Junos `rpd_l2circuit_*`, `rpd_l2vpn_*`, Nokia `svcVpls*`/`sdp-bind*` narratives); SNMP PW-STANDARD-MIB `pwOperStatus`; telemetry VC counters. Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "Cisco Networks Add-on for Splunk (`TA-cisco_ios`), Splunk Add-on for Juniper; SNMP Modular Input for `pwOperStatus`; Nokia SR OS operational logs. — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=network earliest=-24h@h latest=now\n| eval msg=lower(_raw)\n| eval pw=match(msg,\"(?:pseudowire|pw.?id|l2circuit|l2vpn|(?:ethernet|virtual.?circuit)|(?:vpws|vpls).*?(?:vc|pseudo|routed.?pseudo)|(?:mpls.?)?(?:xc|cross.?connect).*pw)\")\n| eval bad=match(msg,\"(?:down|standby|(?:not.?)?(?:operational|active)|(?:detach|withdraw)|(?:signal.?)?(?:fail|mismatch)|(?:mtu).*(?:mismatch)|(?:remote.?)?(?:label).*(?:(?:mis)?match)|(?:oam)?(?:alarm)|(?:bd.?status).*?(?:down))\")\n| where pw=1 AND bad=1\n| rex field=_raw max_match=0 \"(?i)(?:vc.?id|vcid|pseudo.?wire.?id)\\s*[:=]?\\s*(?<vc_id>[0-9]+)\"\n| rex field=_raw max_match=0 \"(?i)(?:neighbor|peer)\\s*[:=]?\\s*(?<neighbor>[0-9.]+|[0-9a-f:]+)\"\n| rex field=_raw max_match=0 \"(?i)(?:interface|ac)\\s*[:=]?\\s*(?<ac_if>[^\\s,]+)\"\n| stats count earliest(_time) as first_seen latest(_time) as last_seen values(neighbor) as peers values(ac_if) as ac_ports values(vc_id) as vc_ids by host sourcetype\n| sort - count",
  "description": "Splunk watches pseudowire attachment circuits and VC signaling so Ethernet wholesale handoffs, mobile backhaul emulations, and VPWS extensions raise alarms when VC IDs drift, MTUs mismatch, or remote labels disappear.",
  "value": "Business wholesale revenue stays defendable because operations correlates customer-visible Carrier Ethernet outages with precise pseudowire identifiers instead of guessing among dozens of VPN attachment interfaces.",
  "implementation": "Land PW syslog plus fifteen-minute SNMP polls for `pwOperStatus.down`, join on `vc_id`, enrich with customer circuit IDs via lookup, alert when VC stays non-forwarding > three polling intervals.",
  "visualization": "Dashboard Studio: KPI for down pseudowires; Sankey optional via stats prep; table (`host`, `vc_ids`, `peers`, `ac_ports`, `count`).",
  "references": [
    {
      "title": "Cisco Layer 2 VPNs Configuration Guide — Pseudowire",
      "url": "https://www.cisco.com/c/en/us/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Juniper Layer 2 Circuits and VPNs User Guide",
      "url": "https://www.juniper.net/documentation/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "IETF RFC 4762 — VPLS Using LDP Signaling",
      "url": "https://www.rfc-editor.org/rfc/rfc4762",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "mitreAttack": [
    "N/A (availability monitoring)"
  ],
  "detectionType": "Baseline",
  "securityDomain": "network",
  "equipment": [
    "cisco",
    "juniper",
    "snmp",
    "syslog"
  ],
  "equipmentModels": [
    "cisco_asr_1000",
    "cisco_asr_9000",
    "cisco_ios",
    "juniper_mx",
    "snmp_generic"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Circuit inventory CSV tying `vc_id`, endpoints, customer VLAN, and handoff port.\n• Known-good MTU template list.\n\nStep 1 — Logging coverage\nCisco ME/ASR families: ensure pseudowire status transitions emit syslog not solely SNMP traps if traps filtered upstream.\n\nStep 2 — SNMP augmentation\nPoll `pwOperStatus`, `pwRemotePwID`, `pwAttachedPwIndex` via SC4SNMP profiles.\n\nStep 3 — SPL correlation\nJoin syslog events within ±300s of SNMP down transitions using `vc_id` key.\n\nStep 4 — CLI validation\n`show l2vpn xconnect`, Junos `show l2circuit connections`, Nokia `show service sdp-using` commands confirm Splunk narrative.\n\nStep 5 — Lifecycle\nQuarterly audit stale VC IDs still alerting—retire suppress entries after migrations.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Cisco Networks Add-on for Splunk (`TA-cisco_ios`), Splunk Add-on for Juniper; SNMP Modular Input for `pwOperStatus`; Nokia SR OS operational logs. — Splunk Add-on for Unix and Linux (Splunkbase 833)). Enable the modular input or HEC token that writes sourcetype=see SPL into network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, sourcetype`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We babysit the pretend Ethernet cords stretched across the country inside our pipes. When those cords fray or lose their pairing tags, we ring the bell before wholesale buyers lose phone or cloud links.",
  "prerequisiteUseCases": [],
  "wave": "walk"
}
