{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.2.48",
  "title": "Check Point Policy Install and Publish Tracking (Check Point)",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Configuration"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Check Point Policy Install and Publish Tracking (Check Point)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_check_point_policy_install_and_publish_tracking_` (UC-5.2.48), SOC dashboard panel \"Operations — Check Point Policy Install and Publish Tracking (Check Point)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.2.48 positive (Check, Point, Policy, Install, and): ingest synthetic events into `index=checkpoint` sourcetype='cp_log' where `values` grouped by `administrator`, `target_gateway` satisfies -30d for 'Check Point Policy Install and Publish Tracking (Check Point)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.2.48 negative ((Check Point)): ingest benign `checkpoint` / `cp_log` events where `administrator`, `target_gateway` stays below -30d, or exclude the test entity via `ops_exception_register.csv`. Variant 13 confirms zero alert rows for 'Check Point Policy Install and Publish Tracking (Check Point)'."
  },
  "dataSources": "`sourcetype=cp_log` (audit/admin logs), SmartConsole audit trail Indexes: checkpoint. Sourcetypes: cp_log. Key fields: administrator, target_gateway. Validate field extraction in Search before saving the alert; compare event counts to the vendor admin console.",
  "app": "`Splunk_TA_checkpoint` (Splunkbase 5402), Check Point App for Splunk (Splunkbase 4293), CCX Add-on for Checkpoint Smart-1 Cloud (Splunkbase 7259)",
  "spl": "index=checkpoint sourcetype=\"cp_log\" earliest=-30d\n| where match(lower(product),\"(?i)smartconsole|smartcenter|management\") AND match(lower(operation),\"(?i)install|publish|verify\")\n| stats count earliest(_time) as first latest(_time) as last values(operation) as ops by administrator, target_gateway\n| sort -last",
  "description": "Monitors UC-5.2.48 — Check, Point, Policy, Install, and — using `index=checkpoint` sourcetype `cp_log`, grouped by `administrator`, `target_gateway`. Outputs `values` each `the scheduled window`; alert when -30d. Primary focus: (Check Point). Purpose-built for 'Check Point Policy Install and Publish Tracking (Check Point)' in Operations workflows.",
  "value": "UC-5.2.48 gives Operations teams earlier visibility into (Check Point) through `cp_log` feeds, lowering operational risk and delayed incident response.",
  "implementation": "Forward management audit logs via Log Exporter. Track policy install duration (publish → install complete). Alert on install failures or partial installs (some gateways succeeded, others failed). Require ITSM ticket IDs in SmartConsole session descriptions for audit correlation. Report on policy change frequency by admin and gateway.",
  "visualization": "Table (recent policy installs), Timeline (publish/install events), Bar chart (installs by admin), Single value (failed installs this week).",
  "cimModels": [],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Change.All_Changes\n  by All_Changes.user All_Changes.object All_Changes.action span=1h\n| sort -count",
  "dataModelAcceleration": "Enable acceleration on the Change data model, dataset All_Changes, before using this tstats search.",
  "references": [
    {
      "title": "Check Point App for Splunk",
      "url": "https://splunkbase.splunk.com/app/4293",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Change",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Change",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CCX Add-on for Checkpoint Smart-1 Cloud",
      "url": "https://splunkbase.splunk.com/app/7259",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 5402",
      "url": "https://splunkbase.splunk.com/app/5402",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `checkpoint` (UC-5.2.48)** — Planned work for 'Check Point Policy Install and Publish Tracking (Check Point)' shifts `values` over `span=1h` while `administrator`, `target_gateway` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `values` on `cp_log`** — Scheduled automation elevates `target_gateway` without crossing configured threshold for UC-5.2.48. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `administrator`, `target_gateway`** — Health checks in `checkpoint` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `cp_log`** — First 48h baseline for UC-5.2.48 on `administrator` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `administrator`, `exception_reason`, `valid_until`.",
  "equipment": [
    "checkpoint"
  ],
  "equipmentModels": [
    "checkpoint_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n* Check Point policy install and publish audit logs. Data in `index=checkpoint` or `index=firewall` with `sourcetype=cp_log`. Key fields: `product` (SmartConsole, Management Server), `action` (Install Policy, Publish), `administrator`, `policy_name`, `target`, `status`, `rule_count`.\n* Check Point policy workflow: (1) Admin edits rules in SmartConsole, (2) Publishes changes (creates audit trail), (3) Installs policy to gateway(s). Each step generates audit logs. Failed installs leave gateways on previous policy version. CLI: `show-sessions`, `install-policy`.\n\nStep 1 -- Configure data collection\n```\n# Check Point Management Server -- enable audit logging\n# SmartConsole > Manage & Settings > Logs & Masters\n# Log policy install and configuration changes\n# Ensure \"Audit\" blade is enabled\n\n# Log Exporter (captures audit and system logs)\ncp_log_export add name splunk_audit target-server <splunk-ip> target-port 514 protocol udp format syslog\ncp_log_export set name splunk_audit read-mode audit\ncp_log_export restart name splunk_audit\n```\nVerify:\n```spl\nindex=checkpoint sourcetype=\"cp_log\" earliest=-30d\n| where match(action, \"(?i)install.*policy|publish|policy.*install\") OR match(product, \"(?i)SmartConsole|Audit\")\n| stats count by action, administrator, status\n```\n\nStep 2 -- Create the search and alert\n\n**Primary search -- Policy install and publish audit trail:**\n```spl\nindex=checkpoint sourcetype=\"cp_log\" earliest=-30d\n| where match(action, \"(?i)install.*policy|publish|policy.*install|accept.*changes\")\n| eval admin=coalesce(administrator, user, src_user_name)\n| eval policy=coalesce(policy_name, policy, rule_name)\n| eval target_gw=coalesce(target, dst, gateway)\n| eval install_status=case(\n    match(status, \"(?i)succeeded|success|completed\"), \"SUCCESS\",\n    match(status, \"(?i)failed|error|timeout\"), \"FAILED\",\n    match(status, \"(?i)warning|partial\"), \"PARTIAL\",\n    1==1, \"UNKNOWN\")\n| eval change_type=case(\n    match(action, \"(?i)publish\"), \"PUBLISH\",\n    match(action, \"(?i)install\"), \"INSTALL\",\n    1==1, \"OTHER\")\n| stats count as events count(eval(install_status=\"FAILED\")) as failures count(eval(install_status=\"SUCCESS\")) as successes values(target_gw) as targets by admin, policy, change_type, install_status\n| eval severity=case(\n    install_status=\"FAILED\", \"CRITICAL -- policy install FAILED\",\n    install_status=\"PARTIAL\", \"WARNING -- partial policy install\",\n    change_type=\"INSTALL\" AND match(admin, \"(?i)unknown|system|api\"), \"WARNING -- non-interactive policy install\",\n    1==1, \"INFO\")\n| eval last_time=strftime(now(), \"%Y-%m-%d %H:%M:%S\")\n| where severity != \"INFO\" OR change_type=\"INSTALL\"\n| sort severity, -events\n```\n\nStep 3 -- Validate\n(a) SmartConsole: Logs & Monitor > Audit -- verify install events appear.\n(b) CLI: `show-gateways` -- check policy date on each gateway.\n(c) Verify: `show-sessions limit 5` -- recent management sessions.\n\nStep 4 -- Operationalize\nDashboard (\"Check Point -- Policy Management\"):\n* Row 1 -- Single-value: \"Policy installs (30d)\", \"Failed installs\", \"Administrators active\".\n* Row 2 -- Policy install timeline.\n* Row 3 -- Audit trail table (admin, action, gateway, status).\n\nAlert: Critical (policy install failed): gateway running stale policy, investigate immediately.\n\nStep 5 -- Troubleshooting\n\n* **Policy install failed** -- Common causes: (1) SIC (Secure Internal Communication) certificate issue between management and gateway, (2) disk space on gateway, (3) rule conflict or invalid object reference. Check: `cpca_client lscert` for SIC, and SmartConsole install log for detailed error.\n\n* **Unauthorized policy change** -- Review audit log for admin identity. Cross-reference with change management tickets. Consider requiring approval for policy installs via SmartWorkflow.\n\n* **Policy install timeout** -- Large rulebases (>10K rules) or slow management-to-gateway links. Consider: optimizing rulebase, using policy packages, or increasing timeout.",
  "grandmaExplanation": "We follow policy install and publish steps so a bad push, a late night edit, or a missing approval is not invisible when auditors ask why.",
  "hardware": "Check Point Quantum 6200/6400/6600/6800/7000/16200/16600/26000/28000, Check Point Quantum Maestro, Check Point CloudGuard Network, Smart-1 Cloud (management)",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
