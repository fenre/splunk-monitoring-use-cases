{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.20.116",
  "title": "IPv6 Dual-Stack Monitoring Parity Audit",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Compliance"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers the detection logic documented for 'IPv6 Dual-Stack Monitoring Parity Audit' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_ipv6_dual_stack_monitoring_parity_audit` (UC-5.20.116), SOC dashboard panel \"Operations — IPv6 Dual-Stack Monitoring Parity Audit\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.20.116 positive (IPv6, Dual, Stack, Monitoring, Parity): ingest synthetic events into `index=target_index` sourcetype='target_sourcetype' where `count` grouped by `sourcetype` satisfies `ipv4_events` > 1000, `ipv6_pct` < 1, `ipv4_events` > 1000, or `expected` > 10 for 'IPv6 Dual-Stack Monitoring Parity Audit'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.20.116 negative (Parity Audit): ingest benign `target_index` / `target_sourcetype` events where `sourcetype` stays below `ipv4_events` > 1000, `ipv6_pct` < 1, `ipv4_events` > 1000, or `expected` > 10, or exclude the test entity via `ops_exception_register.csv`. Variant 0 confirms zero alert rows for 'IPv6 Dual-Stack Monitoring Parity Audit'."
  },
  "dataSources": "`index=network` — Audits whether IPv6 monitoring coverage matches IPv4 across all operational domains: firewall logging, flow data, IDS/IPS inspection, syslog, SNMP polling, synthetic testing, and application monitoring. RFC 9099 §2.6 explicitly requires operational monitoring parity between IPv4 and IPv6.\n\n**The monitoring parity problem:**\n\nMost organisations deploy IPv6 but leave their monitoring toolchain configured for IPv4 only. This creates a 'shadow network' where IPv6 traffic flows unmonitored. Common parity gaps include:\n1. Firewalls logging IPv4 traffic but not IPv6 (or logging with fewer fields).\n2. NetFlow/IPFIX exporting IPv4 templates but missing IPv6 templates.\n3. IDS/IPS rules covering IPv4 attack patterns but lacking IPv6 equivalents.\n4. SNMP monitoring of IPv4 interfaces but not IPv6 interface counters.\n5. Synthetic monitoring (ping, HTTP checks) running over IPv4 only.\n6. Application logs recording IPv4 client addresses but truncating or omitting IPv6.\n7. SIEM correlation rules referencing IPv4 addresses without IPv6 equivalents. Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Enterprise — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=* earliest=-24h\n| eval has_ipv6_src=if(match(src, \":\"), 1, 0)\n| eval has_ipv4_src=if(match(src, \"^\\d+\\.\\d+\\.\\d+\\.\\d+$\"), 1, 0)\n| stats count as total count(eval(has_ipv6_src=1)) as ipv6_events count(eval(has_ipv4_src=1)) as ipv4_events by sourcetype\n| eval ipv6_pct=round(ipv6_events / max(total, 1) * 100, 1)\n| eval parity_status=case(\n    ipv6_events=0 AND ipv4_events > 1000, \"NO IPv6 — this sourcetype has zero IPv6 events despite significant IPv4 volume\",\n    ipv6_pct < 1 AND ipv4_events > 1000, \"MINIMAL IPv6 — only \" . ipv6_pct . \"% IPv6 (expected >10% in dual-stack environment)\",\n    ipv6_pct < 10 AND ipv4_events > 1000, \"LOW IPv6 — \" . ipv6_pct . \"% IPv6 (investigate possible logging gap)\",\n    1=1, \"OK — \" . ipv6_pct . \"% IPv6\")\n| where ipv4_events > 1000 AND ipv6_pct < 10\n| sort ipv6_pct\n| table sourcetype, total, ipv4_events, ipv6_events, ipv6_pct, parity_status",
  "description": "Detects 'IPv6 Dual-Stack Monitoring Parity Audit' (UC-5.20.116) using `index=target_index` sourcetype `configured sourcetype` by aggregating `count` grouped by `sourcetype` over `the scheduled window` and alerting when `ipv4_events` > 1000, `ipv6_pct` < 1, `ipv4_events` > 1000, or `expected` > 10. Keywords: IPv6, Dual, Stack, Monitoring, Parity. Focuses on Parity Audit behaviour so operations teams can triage UC-5.20.116 without ad-hoc log review.",
  "value": "UC-5.20.116 accelerates insight into Parity Audit ('IPv6 Dual-Stack Monitoring Parity Audit') by tracking `activity` on UC-5.20.116 search results, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Compare IPv6 vs IPv4 event volumes across all sourcetypes. Identify sourcetypes with IPv4 traffic but no IPv6. Generate a parity remediation report.",
  "visualization": "(1) Table: sourcetypes with parity status. (2) Bar chart: IPv6 percentage by sourcetype. (3) Single-value: sourcetypes with zero IPv6 (the gap count). (4) Trend: parity improvement over time.",
  "references": [
    {
      "title": "RFC 9099 — Operational Security Considerations for IPv6 Networks (§2.6 — Monitoring)",
      "url": "https://www.rfc-editor.org/rfc/rfc9099",
      "retrieved": "2026-04-25"
    },
    {
      "title": "NIST SP 800-119 — Guidelines for the Secure Deployment of IPv6 (§6 — monitoring)",
      "url": "https://csrc.nist.gov/publications/detail/sp/800-119/final",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `target_index` (UC-5.20.116)** — Planned work for 'IPv6 Dual-Stack Monitoring Parity Audit' shifts `count` over `span=1h` while `sourcetype` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `configured_sourcetype`** — Scheduled automation elevates `has_ipv6_src` without crossing `ipv4_events` > 1000 for UC-5.20.116. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `sourcetype`** — Health checks in `target_index` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-5.20.116 on `sourcetype` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `sourcetype`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco",
    "netflow",
    "paloalto",
    "snmp",
    "splunk",
    "syslog",
    "zeek"
  ],
  "equipmentModels": [
    "cisco_catalyst_9000",
    "netflow_netflow",
    "paloalto_pa",
    "snmp_generic",
    "splunk_enterprise",
    "zeek_sensor"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Dual-stack network deployment.\n• Multiple sourcetypes in Splunk covering different monitoring domains.\n\nStep 1 — Establish baseline\n\nRun the main SPL search to identify all sourcetypes with parity gaps. This creates the initial remediation backlog.\n\n**Detailed parity analysis by monitoring domain:**\n```spl\n| makeresults\n| eval domain=mvappend(\n    \"Firewall logging (pan:traffic, cisco:asa, cisco:ftd)\",\n    \"Flow data (netflow, sflow, ipfix)\",\n    \"IDS/IPS (suricata:alert, snort:alert, zeek:notice)\",\n    \"Network device syslog (cisco:ios, juniper:junos)\",\n    \"DNS (zeek:dns, named:querylog)\",\n    \"Web server (access_combined, iis)\",\n    \"Authentication (cisco:ise, radius)\",\n    \"SNMP (snmp:trap, snmp:interface)\")\n| mvexpand domain\n| eval expected_ipv6=\"yes\"\n| eval actual_ipv6=\"check manually\"\n| table domain, expected_ipv6, actual_ipv6\n```\n\nStep 2 — Deep-dive per sourcetype\n\n**Firewall parity (most critical):**\n```spl\nindex=network sourcetype=\"pan:traffic\" earliest=-24h\n| eval ip_ver=case(\n    match(src, \":\") OR match(dest, \":\"), \"IPv6\",\n    match(src, \"^\\d+\\.\"), \"IPv4\",\n    1=1, \"Unknown\")\n| stats count as events dc(src) as unique_sources dc(dest) as unique_dests by ip_ver\n| eval total_events=events\n| eval pct=round(events / sum(events) * 100, 1)\n```\n\n**Flow data parity:**\n```spl\nindex=network sourcetype=\"netflow\" earliest=-24h\n| eval ip_ver=if(match(src, \":\") OR match(dest, \":\"), \"IPv6\", \"IPv4\")\n| stats sum(bytes) as total_bytes count as flows by ip_ver\n| eval pct=round(flows / sum(flows) * 100, 1)\n```\n\nStep 3 — Validate\n(a) **Generate IPv6 traffic.** From a test host, generate known IPv6 traffic (HTTP, DNS, SSH). Verify each sourcetype captures the IPv6 events with full field fidelity.\n\n(b) **Field fidelity check.** For each sourcetype, verify that IPv6 events have the same fields as IPv4 events (src, dest, port, protocol, action, bytes).\n\n(c) **Synthetic test.** If synthetic monitoring is IPv4-only, add IPv6 synthetic tests for each monitored service.\n\nStep 4 — Operationalize\n\n**Dashboard** (\"IPv6 — Monitoring Parity\"):\n• Row 1 — Single-values: sourcetypes with zero IPv6, total parity gap count.\n• Row 2 — Table: sourcetypes sorted by IPv6 percentage.\n• Row 3 — Bar chart: IPv6 percentage by sourcetype.\n• Row 4 — Trend: parity improvement over time (weekly snapshots).\n\n**Scheduled report:** Monthly. Send to security team with remediation priorities.\n\n**Remediation priorities:**\n1. Firewall logging (most critical — security inspection).\n2. Flow data (traffic analysis and forensics).\n3. IDS/IPS (attack detection).\n4. DNS (IPv6 resolution monitoring).\n5. Authentication (user attribution).\n6. Web server logs (application monitoring).\n7. SNMP (infrastructure monitoring).\n\nStep 5 — Troubleshooting\n\n• **Firewall not logging IPv6.** Verify firewall policies include IPv6 address objects. Some firewalls require separate IPv6 rules or 'any' rules that explicitly include IPv6.\n\n• **NetFlow missing IPv6 templates.** Verify the NetFlow exporter is configured to export IPv6 templates. On Cisco IOS: `flow exporter EXPORT / ip flow-export version 9 / ip flow-export template ipv6`. See UC-5.20.58.\n\n• **Web server log format.** Some web servers truncate IPv6 addresses in logs. Verify the log format supports full IPv6 addresses (128-bit). Apache: use `%a` or `%{REMOTE_ADDR}e`.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Enterprise — Splunk Add-on for Unix and Linux (Splunkbase 833)). Enable the modular input or HEC token that writes sourcetype=see SPL into index=main. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`expected, has_ipv4_src, has_ipv6_src, ipv4_events`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We have security cameras, alarm systems, and visitor logs that watch everything coming and going through the old address system (IPv4). We need to make sure we have the exact same security cameras and alarm systems watching the new address system (IPv6). This tool checks every security system to make sure IPv6 isn't a blind spot — because burglars will always go through the unwatched door.",
  "prerequisiteUseCases": [
    "UC-5.20.1",
    "UC-5.20.58"
  ],
  "wave": "walk"
}
