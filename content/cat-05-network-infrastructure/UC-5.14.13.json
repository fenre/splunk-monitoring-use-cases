{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.14.13",
  "title": "Varnish Grace and Keep Serving Stale Content Frequency",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Reliability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Varnish Grace and Keep Serving Stale Content Frequency' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_varnish_grace_and_keep_serving_stale_content_fre` (UC-5.14.13), SOC dashboard panel \"Operations — Varnish Grace and Keep Serving Stale Content Frequency\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=proxy` sourcetype='varnish:vsl' that satisfy the UC-5.14.13 SPL filters for 'Varnish Grace and Keep Serving Stale Content Frequency'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `proxy` / `varnish:vsl` events outside the detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-5.14.13."
  },
  "dataSources": "`index=proxy` `sourcetype=varnish:vsl` grace or HFM related VSL lines Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes — Universal Forwarder (Splunkbase 617)",
  "spl": "index=proxy sourcetype=\"varnish:vsl\"\n| regex _raw=\"(?i)(grace|stale|hit-for-pass)\"\n| bin _time span=15m\n| stats count by host, _time",
  "description": "Stale serving shields origins but can confuse content owners if unexplained. This use case monitors the condition described in the title using the SPL and data sources documented on this page.",
  "value": "Operations teams track how often Varnish serves stale objects via grace mode, identifying when TTLs are too short or backends too slow to refresh objects before they expire.",
  "implementation": "Tune thresholds against editorial SLOs; document VCL `grace`/`keep`.",
  "visualization": "Time series for rates and latency, top/dedup for hotspots, single-value alerts on health thresholds.",
  "cimModels": [],
  "references": [
    {
      "title": "Vendor documentation",
      "url": "https://varnish-cache.org/docs/trunk/users-guide/vcl-grace.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'Varnish Grace and Keep Serving Stale Content Frequency'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `proxy` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `ops_exception_register.csv` with entity, reason, valid_until.",
  "equipment": [
    "syslog",
    "varnish"
  ],
  "equipmentModels": [
    "syslog_generic",
    "varnish_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "Prerequisites\n* Varnish logs with grace/stale-serving indicators. Key fields: `TTL`, `Grace`, `Keep`, `VCL_call` with `hit` on expired objects. Data in `index=proxy` with `sourcetype=varnish:log`.\n* Grace mode: when an object's TTL expires but is within the grace period, Varnish serves the stale object immediately and triggers a background fetch. This avoids waiting for the backend. `beresp.grace` sets the grace window. `beresp.keep` sets how long to keep the object for conditional requests (IMS/If-None-Match).\n* Frequent grace serving indicates backends are slow or TTLs are too short relative to request frequency.\n\nStep 1 -- Configure data collection\nVCL grace configuration:\n```\nsub vcl_backend_response {\n    set beresp.ttl = 300s;\n    set beresp.grace = 3600s;\n    set beresp.keep = 86400s;\n}\n```\nVerify:\n```spl\nindex=proxy sourcetype=\"varnish:log\" earliest=-4h\n| where match(_raw, \"(?i)grace|stale|HIT.*expired|hit-for-miss\")\n| stats count by _raw | head 20\n```\n\nStep 2 -- Create the search and alert\n\n**Primary search -- Grace/stale serving frequency:**\n```spl\nindex=proxy (sourcetype=\"varnish:log\" OR sourcetype=\"varnish:access\") earliest=-4h\n| eval is_grace=if(match(_raw, \"(?i)grace|HIT.*expired|stale-while-revalidate\"), 1, 0)\n| eval is_normal_hit=if(match(hitmiss, \"(?i)^hit$\") AND is_grace=0, 1, 0)\n| eval is_miss=if(match(hitmiss, \"(?i)miss\"), 1, 0)\n| bin _time span=15m\n| stats sum(is_grace) as grace_hits sum(is_normal_hit) as fresh_hits sum(is_miss) as misses count as total by _time\n| eval grace_pct=round(100*grace_hits/total, 2)\n| eval severity=case(grace_pct > 20, \"HIGH -- >20% requests served stale\", grace_pct > 10, \"WARNING -- >10% stale\", 1==1, \"OK\")\n| where severity != \"OK\"\n| table _time, total, fresh_hits, grace_hits, grace_pct, misses, severity\n```\n\nStep 3 -- Validate\n(a) Set a very short TTL (e.g., 5s) with long grace (1h) on a test object. Request it after TTL expires -- should be served from grace.\n(b) `varnishlog -q \"TTL\" -i TTL` -- shows TTL decisions including grace.\n(c) Verify background fetches are succeeding after grace hits.\n\nStep 4 -- Operationalize\nDashboard (\"Varnish -- Grace & Stale Serving\"):\n* Row 1 -- Single-value: \"Grace hit rate (%)\", \"Stale objects served (4h)\".\n* Row 2 -- Grace vs fresh hit ratio timechart.\n\nAlerting:\n* Warning (grace_pct > 20% for > 30 min): most cached content is stale.\n\nStep 5 -- Troubleshooting\n\n* **High grace serving** -- TTLs are too short or backends are too slow to refresh in time. Options: (1) increase `beresp.ttl`, (2) optimize backend response time, (3) use background fetches (`beresp.grace`) to smooth the impact.\n\n* **Grace hit but no background fetch** -- Varnish may be rate-limiting backend requests. Check `MAIN.backend_busy` and `MAIN.backend_conn` stats.\n\n* **Keep vs grace confusion** -- `grace` = serve stale while fetching. `keep` = retain object for conditional (304) requests even after grace expires. Both should be set for optimal cache behavior.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=varnish:vsl into proxy. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch varnish grace and keep serving stale content frequency and catch issues early, before they turn into outages for the people who rely on the network.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
