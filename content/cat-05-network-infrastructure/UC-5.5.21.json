{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.5.21",
  "title": "VMware VeloCloud Orchestrator Tunnel Health",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'VMware VeloCloud Orchestrator Tunnel Health' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_vmware_velocloud_orchestrator_tunnel_health` (UC-5.5.21), SOC dashboard panel \"Operations — VMware VeloCloud Orchestrator Tunnel Health\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=sdwan` sourcetype='velocloud:link' that satisfy the UC-5.5.21 SPL filters for 'VMware VeloCloud Orchestrator Tunnel Health'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `sdwan` / `velocloud:link` events outside the detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-5.5.21."
  },
  "dataSources": "VMware VeloCloud Orchestrator REST API (`/monitoring/aggregate/edge/link`) polled by a custom scripted input, normalised into `sourcetype=velocloud:link`. Per-link metrics include `latencyMsRx`, `latencyMsTx`, `jitterMsRx`, `lossPctRx`, and the link-state enum (`UP` / `DOWN` / `STANDBY`). Edge identity is carried in `edgeName` and `edgeId`; per-link identity in `linkName` and `linkLogicalId`. Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "VMware VeloCloud Orchestrator REST API (custom scripted input or HEC push). No Splunkbase TA; `Splunk_TA_vmware` covers vCenter only and not VeloCloud. — Universal Forwarder (Splunkbase 617)",
  "spl": "index=sdwan sourcetype=\"velocloud:link\"\n| stats avg(latencyMsRx) as rx_latency, avg(latencyMsTx) as tx_latency, avg(jitterMsRx) as jitter, avg(lossPctRx) as loss_pct by edgeName, linkName\n| where rx_latency > 100 OR jitter > 30 OR loss_pct > 1\n| sort - rx_latency",
  "description": "Polls the VMware VeloCloud Orchestrator API for per-link latency, jitter, and loss across the SD-WAN fabric, then aggregates by edge appliance and link. Surfaces transports whose receive-direction latency exceeds 100 ms, whose jitter crosses 30 ms, or whose loss climbs above 1% — the conventional thresholds that put real-time voice and video at risk.",
  "value": "VMware SD-WAN (VeloCloud) sits on the WAN edge for thousands of branch sites in retail, manufacturing, and logistics. Without orchestrator-side polling, branch outages depend on someone at the branch noticing — a 2-hour mean-time-to-detect that retail customers cannot accept on a sale day. This UC turns the orchestrator's own per-link telemetry into a Splunk-native alerting surface so the NOC sees the degradation before the branch staff do, and lets capacity planning trend WAN quality across the entire estate.",
  "implementation": "Configure a scripted input (Python or PowerShell) to poll the VeloCloud Orchestrator `/monitoring/aggregate/edge/link` endpoint at 5-minute intervals. Store the API key in `passwords.conf` (NEVER inline). Normalise the link-state enum (UP / DOWN / STANDBY) into a consistent value before alerting; the Orchestrator returns slightly different shapes depending on edge model and software version.",
  "visualization": "Line chart (latency / jitter per edge over time), Table (top-N degraded links sorted by loss), Status grid (per-site link health, coloured red / amber / green).",
  "cimModels": [
    "Network_Traffic"
  ],
  "references": [
    {
      "title": "VMware SD-WAN documentation",
      "url": "https://docs.vmware.com/en/VMware-SD-WAN/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "VeloCloud Orchestrator API reference",
      "url": "https://docs.vmware.com/en/VMware-SD-WAN/4.5/vmware-sd-wan-api-reference/GUID-D4FC6FAC-D053-4FE3-A09F-D5547BFD7CA5.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'VMware VeloCloud Orchestrator Tunnel Health'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `sdwan` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `ops_exception_register.csv` with entity, reason, valid_until.",
  "equipment": [
    "velocloud",
    "vmware"
  ],
  "equipmentModels": [
    "velocloud_edge_3400",
    "velocloud_edge_3800",
    "velocloud_edge_510",
    "velocloud_edge_520",
    "velocloud_edge_540",
    "velocloud_edge_610",
    "velocloud_edge_620",
    "velocloud_edge_640",
    "velocloud_edge_680",
    "vmware_ta_vmware",
    "vmware_vcenter"
  ],
  "status": "community",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (VMware VeloCloud Orchestrator REST API (custom scripted input or HEC push). No Splunkbase TA; `Splunk_TA_vmware` covers vCenter only and not VeloCloud. — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=velocloud:link into sdwan. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`edgeName, jitter, linkName, loss_pct`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch the network connections between branch offices and headquarters. When voice or video calls start sounding choppy because a link is dropping data or running slow, we catch it before the branch employees have to call the help desk.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
