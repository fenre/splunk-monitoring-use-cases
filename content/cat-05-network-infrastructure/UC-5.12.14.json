{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.12.14",
  "title": "SBC (Session Border Controller) Registration Trending",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability",
    "Security"
  ],
  "splunkPillar": "Observability",
  "industry": "Telecommunications",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'SBC (Session Border Controller) Registration Trending'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-5.12.14).",
  "evidence": "Saved search uc_5_12_14_sbc_session_border_controller_registrati, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'SBC (Session Border Controller) Registration Trending'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-5.12.14.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "`index=voip` or `index=network` `sourcetype=\"cisco:ios\"` / `sourcetype=\"sip:sbc\"` with SIP METHOD=REGISTER, response codes (`sip_response_code`), `contact`, `user_agent`, source IPs; Cisco CUBE syslog `%SIP-4-REGISTER`; Ribbon/AudioCodes JSON syslog equivalents; optional Splunk Stream SIP capture normalized to same schema. Collected via Splunk Add-on for Cisco Security (Splunkbase 1352) modular input or scripted poll into the index named in the SPL.",
  "app": "Cisco IOS/CUBE syslog TA, vendor SBC syslog feeds, Splunk Stream (optional) — Splunk Add-on for Cisco Security (Splunkbase 1352)",
  "spl": "index=voip (sourcetype=\"sip:sbc\" OR sourcetype=\"cisco:ios\")\n| search REGISTER\n| eval success=if(match(sip_response_code,\"^(200|202)$\"),1,0)\n| bin _time span=15m\n| stats sum(success) as ok count as attempts dc(contact) as distinct_contacts by _time, host\n| eval fail_rate=if(attempts>0, round(100*(attempts-ok)/attempts,2), 0)\n| where fail_rate>5 OR attempts>1000\n| sort _time",
  "description": "Measures SIP REGISTER attempt volumes, success ratios, and distinct endpoint footprint per SBC to expose credential attacks, TLS migration regressions, or registrar outages before phones mass-unregister.",
  "value": "Security and voice ops detect brute-force REGISTER storms, certificate rollover failures, or stale expiry timers early—preserving inbound reachability for BYOD softphones and SIP trunk survivability options.",
  "implementation": "Extract METHOD and response reliably via transforms; whitelist maintenance subnets; alert on fail_rate >5% over fifteen minutes or sudden 401/403 bursts; retain hashed AOR identifiers if privacy policy requires minimization.",
  "visualization": "Timechart REGISTER attempts vs successes; single-value current fail_rate; top user_agent table for fingerprinting rogue scanners.",
  "cimModels": [],
  "references": [
    {
      "title": "RFC 3261 — SIP REGISTER refreshes overview",
      "url": "https://www.rfc-editor.org/rfc/rfc3261",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "cisco",
    "stream",
    "syslog"
  ],
  "equipmentModels": [
    "cisco_cucm",
    "cisco_ios"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• SIP syslog from each SBC cluster member with consistent timestamp source (prefer hardware clock).\n• Dictionary mapping `host` → `site`, `carrier_proxy`, `tls_profile_version`.\n• Understanding of normal registration refresh intervals (typically 300–3600 seconds).\n\nStep 1 — Normalize SIP events\nEnsure REGISTER rows carry `sip_response_code`, `sip_method`, `contact`, `authorization_identity` if logged.\n\nStep 2 — Baseline steady state\nCompute hourly median attempts per site; seasonal adjustments for retail handset rollouts.\n\nStep 3 — Detection logic\nAlert when fifteen-minute failure ratio exceeds baseline + three sigma or absolute >8%; parallel threshold on distinct_contacts drop >40% indicating partial outage.\n\nStep 4 — Visualization\nStacked success vs failure over time; geomap of REGISTER sources when anomalies correlate to geography.\n\nStep 5 — Response\nValidate TLS certificate expiry on SBC; replay PCAP subset if Stream enabled; coordinate with IdP if REGISTER carries OAuth-derived credentials.\n\nExtended troubleshooting\nCarrier-backed registrations may aggregate behind single Contact leading to low dc(contact)—switch KPI to unique Call-ID. NAT rebinding events mimic churn; compare UDP vs TCP transports separately.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Cisco IOS/CUBE syslog TA, vendor SBC syslog feeds, Splunk Stream (optional) — Splunk Add-on for Cisco Security (Splunkbase 1352)). Enable the modular input or HEC token that writes sourcetype=sip:sbc, cisco:ios into voip. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, attempts, fail_rate, host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We track phones checking in with the border gatekeeper so we notice if they suddenly cannot log on, whether from a bad password rush or a broken security certificate.",
  "prerequisiteUseCases": [],
  "telcoUseCase": "SIP endpoint registration assurance",
  "wave": "crawl"
}
