{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.20.140",
  "title": "IPv6 Flow Label Misuse and Covert Channel Detection (RFC 6437)",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'IPv6 Flow Label Misuse and Covert Channel Detection (RFC 6437)'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-5.20.140).",
  "evidence": "Saved search uc_5_20_140_ipv6_flow_label_misuse_and_covert_channe, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'IPv6 Flow Label Misuse and Covert Channel Detection (RFC 6437)'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-5.20.140.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "`index=network sourcetype=netflow` — Detects misuse of the IPv6 Flow Label field (20 bits in the IPv6 header, RFC 6437) for covert channel communication. The Flow Label should be set uniformly random per flow; non-random patterns, encoded data, or zero values on all flows may indicate either misconfiguration or deliberate covert channel use. Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Enterprise Security — Universal Forwarder (Splunkbase 617)",
  "spl": "index=network sourcetype=\"netflow\" earliest=-24h\n| eval is_ipv6=if(match(src, \":\") OR match(dest, \":\"), 1, 0)\n| where is_ipv6=1\n| eval flow_label=if(isnotnull(flow_label), flow_label, 0)\n| stats count as flows dc(flow_label) as unique_labels avg(flow_label) as avg_label stdev(flow_label) as stdev_label by src\n| eval label_ratio=round(unique_labels/flows, 4)\n| eval anomaly=case(\n    unique_labels=1 AND flows > 100 AND flow_label=0, \"ALL_ZERO — \" . flows . \" flows with flow_label=0 — legacy behaviour or misconfiguration\",\n    label_ratio < 0.01 AND flows > 100, \"LOW_DIVERSITY — only \" . unique_labels . \" labels across \" . flows . \" flows — possible encoding\",\n    stdev_label < 100 AND flows > 100, \"LOW_ENTROPY — flow labels have suspiciously low randomness\",\n    1=1, null())\n| where isnotnull(anomaly)\n| sort -flows",
  "description": "Detects misuse of the IPv6 Flow Label field for covert channel communication. The 20-bit Flow Label should be uniformly random per flow (RFC 6437). Non-random patterns indicate either misconfiguration or deliberate covert channel encoding. Also detects all-zero Flow Labels, which while valid, reduce QoS effectiveness.",
  "value": "The IPv6 Flow Label is a 20-bit field that traverses the network unchanged by routers. This makes it an attractive covert channel for data exfiltration or C2 communication. Because most security tools ignore the Flow Label, it can carry encoded data undetected. Monitoring Flow Label entropy identifies both covert channels and misconfigured stacks that don't set Flow Labels properly.",
  "implementation": "Analyze Flow Label diversity and entropy per source. Alert on anomalous patterns.",
  "visualization": "(1) Histogram: Flow Label distribution. (2) Table: sources with anomalous labels. (3) Single-value: % of flows with zero labels.",
  "references": [
    {
      "title": "RFC 6437 — IPv6 Flow Label Specification",
      "url": "https://www.rfc-editor.org/rfc/rfc6437",
      "retrieved": "2026-04-25"
    },
    {
      "title": "IPv6 Flow Labels as Covert Channel — Academic Research",
      "url": "https://doi.org/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "cisco",
    "juniper",
    "netflow"
  ],
  "equipmentModels": [
    "cisco_catalyst_9000",
    "juniper_mx",
    "netflow_netflow"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• NetFlow/IPFIX exporting Flow Label field.\n• Note: Not all NetFlow implementations export the Flow Label.\n\nStep 1 — Verify NetFlow exports include Flow Label.\n\nStep 2 — Baseline normal Flow Label distribution.\n\nStep 3 — Validate: Generate test traffic with known Flow Labels. Verify detection.\n\nStep 4 — Operationalize\n**Dashboard:** Flow Label analysis. **Alert:** Low-entropy Flow Labels — investigate.\n\nStep 5 — Troubleshooting\n• Zero Flow Labels: Usually harmless but indicate the source stack doesn't implement RFC 6437. Update OS/firmware.\n• Encoded data in labels: Investigate the source for malware or unauthorized tools.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Enterprise Security — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=netflow into network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`anomaly, flow_label, flows, is_ipv6`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Every IPv6 packet has a small label, like a shipping tag. It should be a random number for each conversation. If someone is writing secret messages on these tags instead of random numbers, they could be smuggling data out of the network. We check that these tags look properly random and flag any suspicious patterns.",
  "prerequisiteUseCases": [
    "UC-5.20.98"
  ],
  "wave": "run"
}
