{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.13.37",
  "title": "Devices Affected by Active Advisories",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Vulnerability"
  ],
  "splunkPillar": "Security",
  "owner": "Head of IT Operations",
  "controlFamily": "regulation-specific",
  "compliance": [
    {
      "regulation": "NIST-800-53",
      "version": "Rev. 5",
      "clause": "SI-2",
      "mode": "satisfies",
      "assurance": "partial",
      "assurance_rationale": "Tracks Cisco PSIRT security advisories affecting managed devices, providing evidence of vulnerability awareness and remediation tracking as required by NIST 800-53 Flaw Remediation.",
      "controlObjective": "Demonstrates continuous monitoring of vendor-published security advisories for all managed network infrastructure.",
      "evidenceArtifact": "Saved search over index=catalyst sourcetype=cisco:dnac:securityadvisory scheduled daily; advisory counts and affected device lists exported to compliance dashboard."
    },
    {
      "regulation": "NIST-800-53",
      "version": "Rev. 5",
      "clause": "RA-5",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Contributes to vulnerability scanning by tracking known vendor-published vulnerabilities, though it does not replace active scanning tools.",
      "controlObjective": "Provides a vendor-authoritative vulnerability feed for network infrastructure to complement active vulnerability scanning.",
      "evidenceArtifact": "Dashboard panel showing advisory severity distribution and affected device counts, refreshed with each TA poll cycle."
    }
  ],
  "dataSources": "`index=catalyst sourcetype=cisco:dnac:securityadvisory` from the `securityadvisory` modular input in `TA_cisco_catalyst` (Splunkbase 7538). Same data feed as UC-5.13.34. Per-device advisory mapping: each event links an `advisoryId` to the affected `deviceId` or device list.",
  "app": "`TA_cisco_catalyst` ([Splunkbase 7538](https://splunkbase.splunk.com/app/7538)), Cisco Supported, 1.x+.",
  "spl": "index=catalyst sourcetype=\"cisco:dnac:securityadvisory\" (severity=\"CRITICAL\" OR severity=\"HIGH\")\n| stats latest(advisoryTitle) as title latest(severity) as sev by advisoryId, deviceId\n| lookup catalyst_device_lookup deviceId OUTPUT deviceName, platformId, softwareVersion\n| table advisoryId, sev, title, deviceName, platformId, softwareVersion\n| sort sev, advisoryId, deviceName",
  "description": "Lists every device affected by each active security advisory, showing which specific switches, routers, and WLCs need firmware upgrades or workarounds — transforming the abstract advisory count from UC-5.13.34 into a concrete device-level remediation plan.",
  "value": "UC-5.13.34 says '5 CRITICAL advisories affect 47 devices.' This UC says WHICH 47 devices, what firmware they're running, and what version they need. That's the difference between a vulnerability report and a remediation work order. The per-device list is what the firmware upgrade team needs to build the SWIM distribution plan in Catalyst Center, and what the security team needs for risk-based prioritisation (internet-facing core switch vs. internal access switch).",
  "implementation": "Same `securityadvisory` input as UC-5.13.34. The API should return device-level data within advisory events. If device IDs are returned instead of names, join with `index=catalyst sourcetype=\"cisco:dnac:device\"` for hostname enrichment.",
  "visualization": "(1) Table: advisoryId, severity, advisoryTitle, deviceName, platformId, softwareVersion — drilldown to Catalyst Center Device 360. (2) Count: devices affected by CRITICAL advisories (red ≥ 1). (3) Pivot: `| stats dc(advisoryId) as advisory_count by deviceName | sort -advisory_count` — most-exposed devices first.",
  "references": [
    {
      "title": "Splunkbase app 7538",
      "url": "https://splunkbase.splunk.com/app/7538"
    },
    {
      "title": "Catalyst Center API docs",
      "url": "https://developer.cisco.com/docs/catalyst-center/"
    },
    {
      "title": "Catalyst Center Integration Guide",
      "url": "docs/guides/catalyst-center.md"
    },
    {
      "title": "Cisco PSIRT Security Advisories",
      "url": "https://sec.cloudapps.cisco.com/security/center/publicationListing.x"
    }
  ],
  "knownFalsePositives": "**Same device appearing in multiple advisories inflating the affected-device count.** A device running vulnerable software may be affected by multiple PSIRTs simultaneously, appearing multiple times in the per-device advisory view. Distinguish by using `dc(advisoryId)` per device to understand the distinct advisory count. No suppression needed — each advisory is a separate finding.\n\n**Lab or non-production device affected by critical advisories.** Lab devices running intentionally outdated firmware may be affected by many advisories that do not warrant operational urgency. Distinguish by checking whether the device is in a non-production `siteId`. Suppress by filtering lab devices from the operational advisory dashboard using a `catalyst_excluded_devices` lookup.\n\n**Device UUID change after RMA making the same physical device appear as a new affected device.** After RMA replacement, the new device has a different `deviceId` but the same `hostname`. The advisory may show both the old and new UUIDs as affected. Distinguish by checking whether `hostname` matches between old and new `deviceId` values. Suppress by correlating with inventory data to deduplicate by `hostname`.\n\n**Advisory scan incomplete for large inventories.** In environments with thousands of devices, the PSIRT scan may not complete within a single poll cycle, showing an incomplete list of affected devices. Distinguish by checking whether `dc(deviceId)` increases between consecutive polls for the same `advisoryId`. Suppress by waiting for the count to stabilize (2-3 poll cycles) before using the affected-device list for operational decisions.",
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_catalyst_center"
  ],
  "detailedImplementation": "Prerequisites\n• UC-5.13.34 (PSIRT Overview) must be operational — same data feed.\n• For device name enrichment, maintain a `catalyst_device_lookup` from `index=catalyst sourcetype=\"cisco:dnac:device\" | stats latest(hostname) as deviceName latest(platformId) as platformId latest(softwareVersion) as softwareVersion by id | rename id as deviceId | outputlookup catalyst_device_lookup`.\n\nStep 1 — Configure data collection\nSame `securityadvisory` input as UC-5.13.34. The API returns advisory-to-device mappings.\n\nStep 2 — Create the search and report\n```spl\nindex=catalyst sourcetype=\"cisco:dnac:securityadvisory\" (severity=\"CRITICAL\" OR severity=\"HIGH\")\n| stats latest(advisoryTitle) as title latest(severity) as sev by advisoryId, deviceId\n| lookup catalyst_device_lookup deviceId OUTPUT deviceName, platformId, softwareVersion\n| table advisoryId, sev, title, deviceName, platformId, softwareVersion\n| sort sev, advisoryId, deviceName\n```\n\nWhy `by advisoryId, deviceId`: creates one row per advisory-device pair — the unit of remediation work. Each row is 'this device needs this patch.'\n\nWhy include `softwareVersion`: shows the CURRENT firmware version. The security team can see whether the device is close to the fixed version (minor upgrade) or far behind (major upgrade effort).\n\nVariant — most-exposed devices:\n```spl\nindex=catalyst sourcetype=\"cisco:dnac:securityadvisory\" (severity=\"CRITICAL\" OR severity=\"HIGH\")\n| stats dc(advisoryId) as advisory_count values(advisoryId) as advisories by deviceId\n| lookup catalyst_device_lookup deviceId OUTPUT deviceName, platformId, softwareVersion\n| sort -advisory_count\n| head 20\n```\n\nStep 3 — Validate\n(a) Pick a CRITICAL advisory. Open **Catalyst Center > Security Advisories > [advisory] > Affected Devices**. Compare the device list with Splunk.\n(b) Verify `softwareVersion` is current by spot-checking a device.\n(c) Confirm the `catalyst_device_lookup` is up to date.\n\nStep 4 — Operationalize\n• Export as CSV for the firmware upgrade team's SWIM distribution plan.\n• Track per-advisory remediation with UC-5.13.38.\n• Prioritise internet-facing and CDE-scoped devices using a `device_exposure` lookup.\n\nRunbook:\n1. For each CRITICAL advisory row: check if a fixed version is available (from UC-5.13.34).\n2. Group affected devices by `platformId` (same device family = same upgrade image).\n3. Create a SWIM upgrade task in Catalyst Center targeting the affected devices.\n4. Schedule the upgrade during the next maintenance window.\n5. Track remediation with UC-5.13.38.\n\nStep 5 — Troubleshooting\n\n• **`deviceId` instead of `deviceName` in results** — the `catalyst_device_lookup` is empty. Regenerate it from device inventory data.\n\n• **Advisory shows 0 affected devices in Splunk but non-zero in Catalyst Center** — the advisory-device mapping may be in a nested array that needs `spath | mvexpand`. Check the raw event structure.\n\n• **Same device appears for many advisories** — the device is running old firmware affected by multiple PSIRTs. A single firmware upgrade may remediate multiple advisories.\n\n• **`softwareVersion` is null** — the device lookup is incomplete. Ensure the `cisco:dnac:device` input is running.\n\n• **More devices in Splunk than Catalyst Center** — time window difference. Narrow the Splunk search to `-2h` for the current snapshot.\n\n• **Lookup is stale** — schedule the `outputlookup` search daily to keep device metadata current.\n\n• **Want to see MEDIUM advisories too** — remove the severity filter. But expect a much larger result set.\n\n• **Advisory-device mapping not in the API response** — some TA versions may not extract per-device detail from the advisory endpoint. Check with `| head 1 | spath` for the response structure.",
  "grandmaExplanation": "We list exactly which network devices are affected by each security weakness, so your team knows precisely which boxes need a software update — not just 'we have a vulnerability' but 'these 12 specific switches in Building A need to be upgraded to version X.'",
  "prerequisiteUseCases": [
    "UC-5.13.34"
  ],
  "wave": "walk",
  "status": "verified",
  "lastReviewed": "2026-04-30",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases"
}
