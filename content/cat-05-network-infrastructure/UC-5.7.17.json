{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.7.17",
  "title": "Asymmetric Routing Detection via Flow Analysis",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Fault",
    "Performance",
    "Operations"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Asymmetric Routing Detection via Flow Analysis' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_asymmetric_routing_detection_via_flow_analysis` (UC-5.7.17), SOC dashboard panel \"Operations — Asymmetric Routing Detection via Flow Analysis\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.7.17 positive (Asymmetric, Routing, Detection, via, Flow): ingest synthetic events into `index=netflow` sourcetype='target_sourcetype' where `bytes_lr`, `bytes_rl`, `total_bytes` grouped by `left`, `right` satisfies `mb_total` > 50, `imbalance` > 0.80, `bytes_lr` > 0, or `bytes_rl` > 0 for 'Asymmetric Routing Detection via Flow Analysis'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.7.17 negative (Flow Analysis): ingest benign `netflow` / `target_sourcetype` events where `left`, `right` stays below `mb_total` > 50, `imbalance` > 0.80, `bytes_lr` > 0, or `bytes_rl` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 4 confirms zero alert rows for 'Asymmetric Routing Detection via Flow Analysis'."
  },
  "dataSources": "`index=netflow` with bidirectional visibility preferred (`sourcetype=netflow` or `stream:ipfix`) containing normalized `src`, `dest`, `bytes`, `packets`; deploy at chokepoints capturing both forward and return paths or synthesize pairs across mirrored collectors with caution.",
  "app": "Splunk Add-on for NetFlow (Splunkbase 1838)",
  "spl": "index=netflow earliest=-4h\n| eval left=min(src,dest), right=max(src,dest)\n| eval fwd_bytes=if(src=left, bytes, 0), rev_bytes=if(src=right, bytes, 0)\n| stats sum(fwd_bytes) as bytes_lr sum(rev_bytes) as bytes_rl sum(bytes) as total_bytes sum(packets) as pkts by left right\n| eval imbalance=abs(bytes_lr-bytes_rl)/(bytes_lr+bytes_rl+1)\n| eval mb_total=round(total_bytes/1048576,2)\n| where mb_total>50 AND imbalance>0.80 AND bytes_lr>0 AND bytes_rl>0\n| sort -imbalance\n| head 75",
  "description": "Alerts on risk tied to Asymmetric, Routing, Detection, via, Flow through UC-5.7.17 on `index=netflow` sourcetype `configured sourcetype`. Summarises `bytes_lr`, `bytes_rl`, `total_bytes` grouped by `left`, `right` across `the scheduled window` before applying `mb_total` > 50, `imbalance` > 0.80, `bytes_lr` > 0, or `bytes_rl` > 0. Operators tune 'Asymmetric Routing Detection via Flow Analysis' using UC-5.7.17 runbook steps.",
  "value": "Tracking `bytes_lr`, `bytes_rl`, `total_bytes` via UC-5.7.17 on UC-5.7.17 search results improves response to 'Asymmetric Routing Detection via Flow Analysis' and reduces operational risk and delayed incident response.",
  "implementation": "Ensure overlapping collectors cover both directions or annotate partial visibility per site; tune minimum-megabyte and imbalance ratios; document legitimate multicast and satellite asymmetry exceptions.",
  "visualization": "Chord-like table of left/right with imbalance bar; dual-axis chart bytes_lr versus bytes_rl; map interfaces when fields exist.",
  "references": [
    {
      "title": "RFC 791 — Internet Protocol (routing asymmetry context)",
      "url": "https://www.rfc-editor.org/rfc/rfc791",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk — Traffic mirroring and NetFlow best practices",
      "url": "https://docs.splunk.com/Documentation/NetFlow/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `netflow` (UC-5.7.17)** — Planned work for 'Asymmetric Routing Detection via Flow Analysis' shifts `bytes_lr`, `bytes_rl`, `total_bytes` over `span=1h` while `left`, `right` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `min` on `configured_sourcetype`** — Scheduled automation elevates `right` without crossing `mb_total` > 50 for UC-5.7.17. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `left`, `right`** — Health checks in `netflow` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-5.7.17 on `left` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `left`, `exception_reason`, `valid_until`.",
  "equipment": [
    "netflow"
  ],
  "equipmentModels": [
    "netflow_ipfix",
    "netflow_netflow",
    "netflow_sflow"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Topology maps identifying multipath equal-cost routes and Network Address Translation boundaries.\n• Agreement that sampled NetFlow introduces statistical noise—use higher byte floors on busy links.\n• Optional verification dataset from traceroute utilities during calibration.\n\nStep 1 — Configure data collection\nPlace exporters on core-facing interfaces rather than access-edge-only vantage points when diagnosing campus asymmetry. Record ingress and egress interface identifiers if available for drilldowns.\n\nStep 2 — Create the search\nAugment with `| lookup asym_allowlist.csv left right OUTPUT reason` to suppress known satellite feeds. Add parallel panel counting pairs where `bytes_rl==0` indicating missing reverse visibility.\n\nStep 3 — Validate\nIntroduce controlled asymmetric routing in lab using policy-based routing and verify detection within the observation window.\n\nStep 4 — Operationalize\nIntegrate alerts with topology-aware ticketing; attach recommended Border Gateway Protocol or static-route checks per pair classification.\n\nStep 5 — Troubleshooting\nSymmetric hashing across link bundles may split flows unevenly in sampling—raise thresholds on aggregated uplinks. Timestamp skew between collectors falsifies pairing—monitor `_time` deltas.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for NetFlow (Splunkbase 1838)). Enable the modular input or HEC token that writes sourcetype=see SPL into netflow. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`bytes_lr, bytes_rl, fwd_bytes, imbalance`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We weigh traffic leaving point A toward B against traffic leaving B toward A on the same conversation. When one side almost disappears while the other roars, something on the path may be lopsided or we may only see half the road.",
  "prerequisiteUseCases": [],
  "wave": "walk"
}
