{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.13.13",
  "title": "Client Health by Site",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Availability"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=catalyst sourcetype=cisco:dnac:clienthealth`. from the `clienthealth` modular input in the Cisco Catalyst Add-on (Splunkbase 7538) polling `GET /dna/intent/api/v1/client-health`. Default poll interval: 900s (15m). Volume: ~3 events per poll (ALL, WIRED, WIRELESS categories). RBAC: NETWORK-ADMIN-ROLE. Key fields: `scoreCategory` (string), `value` (integer 0-100), `clientCount` (integer), `scoreDetail{}` (array/object).",
  "app": "`Cisco Catalyst Add-on for Splunk` (Splunkbase 7538)",
  "spl": "index=catalyst sourcetype=\"cisco:dnac:clienthealth\" | stats avg(scoreDetail{}.scoreCategory.value) as avg_health sum(scoreDetail{}.clientCount) as total_clients by siteId | eval health_status=case(avg_health>=75,\"Healthy\",avg_health>=50,\"Fair\",1==1,\"Poor\") | sort avg_health",
  "description": "Aggregates client health metrics by Catalyst Center site, enabling location-based comparison of user experience quality.",
  "value": "Comparing client health across sites reveals whether problems are localized (single building) or systemic (infrastructure-wide), guiding remediation priority.",
  "implementation": "Requires UC-5.13.9 and site-level `siteId` in each event. If only UUIDs are present, add a `lookup` to friendly site names. Run during business hours to compare user populations fairly; for 24/7 sites, add a time-of-day trellis or a separate night-shift view.",
  "visualization": "Table (siteId, avg_health, total_clients, health_status), bar chart, map if joined with geo for campus locations.",
  "references": [
    {
      "title": "Splunkbase app 7538",
      "url": "https://splunkbase.splunk.com/app/7538"
    },
    {
      "title": "Catalyst Center API docs",
      "url": "https://developer.cisco.com/docs/catalyst-center/"
    },
    {
      "title": "Catalyst Center Integration Guide",
      "url": "docs/guides/catalyst-center.md"
    },
    {
      "title": "Catalyst Center Client Health API — Cisco DevNet",
      "url": "https://developer.cisco.com/docs/catalyst-center/#!get-overall-client-health"
    }
  ],
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_catalyst_center"
  ],
  "knownFalsePositives": "**Tiny branch site with few clients where a single poor client dominates the average.** A remote site with 5 connected clients will show low average health if even one client has a poor score. Distinguish by checking `total_clients` for the site — averages from sites with fewer than 10 clients are statistically unreliable. Suppress by using a minimum client count threshold: `| where total_clients>=10` for percentage-based health reporting.\n\n**Site hierarchy change reassigning clients to different siteId values.** When Catalyst Center's site hierarchy is reorganized, clients may temporarily appear under a different `siteId`, causing apparent health shifts per site. Distinguish by correlating with `index=catalyst sourcetype=\"cisco:dnac:audit:logs\"` for site hierarchy modifications. Suppress by tracking by siteId (UUID, stable) rather than site name.\n\n**Temporary Wi-Fi outage at one site from AP maintenance or power event.** An AP restart or site-level power event causes all wireless clients at that site to disconnect, creating a spike in poor health. Distinguish by checking whether the health drop is localized to one site and whether AP health data (`index=catalyst sourcetype=\"cisco:dnac:devicehealth\" deviceType=\"*AP*\"`) shows concurrent issues. This is a real event — do not suppress, but annotate the alert with site-specific context.\n\n**VPN or remote-access clients not associated with any physical site.** Remote workers connecting via VPN may have their client health reported without a valid `siteId`, appearing under a catch-all site that skews the per-site breakdown. Distinguish by checking whether low-health sites have VPN-related SSIDs or connection types. Suppress by filtering VPN client connections from site-level health dashboards.",
  "detailedImplementation": "Prerequisites\n• **UC-5.13.9** and client health data flowing to `cisco:dnac:clienthealth`.\n• `siteId` on events (Catalyst site hierarchy in Design and inventory sync). Add a **lookup** from `siteId` to building or campus name for humans.\n• Cisco Catalyst Add-on (7538); `clienthealth` input. Nested `scoreDetail` field paths can differ by build—`scoreDetail{}.scoreCategory.value` is convenient when multivalue paths work; if not, use the same `spath` flattening as UC-5.13.9 and aggregate after.\n• See `docs/implementation-guide.md`.\n\nStep 1 — Configure data collection\n• Intent client-health API via TA `clienthealth` modular input; default 900s poll.\n• Confirm `siteId` is non-null for production sites; unassigned clients skew “unknown” site buckets.\n\nStep 2 — Create the search and alert\n```spl\nindex=catalyst sourcetype=\"cisco:dnac:clienthealth\" | stats avg(scoreDetail{}.scoreCategory.value) as avg_health sum(scoreDetail{}.clientCount) as total_clients by siteId | eval health_status=case(avg_health>=75,\"Healthy\",avg_health>=50,\"Fair\",1==1,\"Poor\") | sort avg_health\n```\n\nUnderstanding this SPL\n• Multivalue `scoreDetail{}` paths roll up all nested categories in one `stats`—if your data shape does not support parallel arrays, switch to a flattened subsearch (UC-5.13.9 pattern) and then `stats by siteId`.\n• `health_status` uses 75/50 cutoffs; align to your SLOs or to device-health bands used in UC-5.13.1 for executive consistency.\n• `sort avg_health` ascending shows worst user experience at the top; pair with `total_clients` to avoid overreacting to a small branch.\n\n**Pipeline walkthrough**\n• `stats by siteId` → average of reported health values and sum of client counts; `eval` labels bands; `sort` for triage order.\n\nStep 3 — Validate\n• Compare a single site’s row to Catalyst Center Client health for that site in the same window.\n• `| fieldsummary siteId` to ensure coverage; if many nulls, fix hierarchy in Catalyst before trusting rankings.\n\nStep 4 — Operationalize\n• Regional dashboard: table of site (name from lookup), avg_health, total_clients, health_status. Drill to UC-5.13.5 for device-level health and UC-5.13.1 where needed.\n• Optional: trellis or separate panels for “large campus” vs “branch” using a site-tier lookup to avoid false prioritization.\n\nStep 5 — Troubleshooting\n• Wildly wrong averages: often multivalue `scoreDetail` is not parallel—flatten first, then re-aggregate.\n• Good Splunk, bad UI: time zone, site filter in TA, or different Assurance scope in the console versus global Splunk index.\n• **No clienthealth events arriving:** verify the `clienthealth` input is enabled and check `index=_internal` for API error or endpoint timeout messages from the TA.\n• **NULL or zero values in health scores:** confirm clients are connected and visible in Catalyst Center Assurance; empty values indicate no clients in that category.\n• **Fewer events than expected:** check for API throttling (HTTP 429); credential access denied errors in TA logs indicate insufficient permissions.",
  "grandmaExplanation": "We compare how users at different buildings and offices experience the network, so you can see which locations need help first.",
  "prerequisiteUseCases": [
    "UC-5.13.9"
  ],
  "wave": "walk"
}
