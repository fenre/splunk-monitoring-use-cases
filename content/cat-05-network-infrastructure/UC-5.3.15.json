{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.3.15",
  "title": "Citrix ADC SSL Certificate Expiration Monitoring (NetScaler)",
  "criticality": "critical",
  "difficulty": "beginner",
  "monitoringType": [
    "Compliance",
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Citrix ADC SSL Certificate Expiration Monitoring (NetScaler)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_citrix_adc_ssl_certificate_expiration_monitoring` (UC-5.3.15), SOC dashboard panel \"Operations — Citrix ADC SSL Certificate Expiration Monitoring (NetScaler)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=network` sourcetype='citrix:netscaler:ssl' where `certkey_name` satisfies `days_left` < 90 for UC-5.3.15 ('Citrix ADC SSL Certificate Expiration Monitoring (NetScaler)'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `network` / `citrix:netscaler:ssl` events where `certkey_name` stays below `days_left` < 90, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-5.3.15."
  },
  "dataSources": "`index=network` `sourcetype=\"citrix:netscaler:ssl\"` fields `certkey_name`, `days_to_expiry`, `subject`, `issuer`, `serial`, `bound_vserver` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom scripted input polling Citrix ADC NITRO API — Universal Forwarder (Splunkbase 617)",
  "spl": "index=network sourcetype=\"citrix:netscaler:ssl\"\n| stats latest(days_to_expiry) as days_left, latest(subject) as subject, latest(issuer) as issuer, values(bound_vserver) as bound_to by certkey_name, host\n| where days_left < 90\n| eval urgency=case(days_left<=7, \"CRITICAL\", days_left<=30, \"HIGH\", days_left<=90, \"MEDIUM\", 1=1, \"LOW\")\n| sort days_left\n| table certkey_name, days_left, urgency, subject, issuer, bound_to, host",
  "description": "SSL certificates on Citrix ADC terminate HTTPS connections for all web applications behind the load balancer. An expired certificate causes browser warnings or complete connection failures for all users. The NITRO API exposes `daystoexpiration` for every bound SSL certificate, enabling automated alerting well before expiry. Certificate expiry outages are among the most preventable yet impactful failures in production environments.",
  "value": "SSL certificates on Citrix ADC terminate HTTPS connections for all web applications behind the load balancer. Timely UC-5.3.15 alerting on 'Citrix ADC SSL Certificate Expiration Monitoring (NetScaler)' reduces operational risk and delayed incident response before manual triage or audit channels escalate.",
  "implementation": "Create a scripted input that polls the NITRO API `sslcertkey` resource on each ADC. The API returns `certkey` name, `subject`, `issuer`, `serial`, `clientcertnotbefore`, `clientcertnotafter`, `daystoexpiration`, and `expirymonitor` status. Also enable the built-in `expirymonitor` on the ADC with a `notificationperiod` (10–100 days). Run the scripted input daily. Alert at 90 days (plan renewal), 30 days (action required), 7 days (critical), and immediately when `daystoexpiration` reaches 0. Track all certificates bound to vServers — unbound certificates can be ignored or flagged for cleanup.",
  "visualization": "Table (certificates sorted by expiry), Single value (certificates expiring within 30 days), Gauge (soonest expiry).",
  "cimModels": [],
  "references": [
    {
      "title": "CIM: Certificates",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Certificates",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `network`** — Planned work for 'Citrix ADC SSL Certificate Expiration Monitoring (NetScaler)' shifts `values` over `span=1h` while `certkey_name` remains in-scope for UC-5.3.15. Cross-check CMDB CHG.\n\n2. **Batch `values` on `citrix:netscaler:ssl`** — Scheduled automation elevates `host` without crossing `days_left` < 90. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `certkey_name`** — Health checks in `network` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `citrix:netscaler:ssl`** — First 48h baseline for `certkey_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `certkey_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "citrix"
  ],
  "equipmentModels": [
    "citrix_netscaler"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n* Splunk Add-on for Citrix NetScaler (`Splunk_TA_citrix-netscaler`, Splunkbase 2770). Citrix ADC syslog or NITRO API providing certificate data. Key fields: `cert_name`, `expiry_date`, `days_to_expiry`, `issuer`, `subject`, `serial`. Citrix ADC syslog message: \"Certificate <name> is going to expire in <N> days\" (message code EVENT_CERT_EXP).\n\nStep 1 -- Configure data collection\nCitrix ADC generates certificate expiry warnings at 30 days and 7 days by default. Optionally poll NITRO API: `GET /nitro/v1/config/sslcertkey` for all certificate metadata. Verify:\n```spl\nindex=netscaler sourcetype=\"citrix:netscaler:syslog\" (\"certificate\" AND (\"expir\" OR \"expire\")) earliest=-30d\n| stats count by host\n```\n\nStep 2 -- Create the search and alert\n\n**Primary search -- Certificate expiry monitoring:**\n```spl\nindex=netscaler (sourcetype=\"citrix:netscaler:syslog\" OR sourcetype=\"citrix:netscaler:perf\") earliest=-7d\n| where match(_raw, \"(?i)(certificate|cert|ssl).*(expir|expire|expired)\")\n| rex \"(?i)(?:certificate|cert)\\s+(?<cert_name>\\S+).*?(?<days_left>\\d+)\\s*day\"\n| eval days_left=coalesce(days_to_expiry, days_left)\n| where isnotnull(days_left)\n| lookup citrix_cert_inventory.csv cert_name OUTPUT vserver, application, owner, auto_renew\n| eval urgency=case(tonumber(days_left) < 0, \"EXPIRED\", tonumber(days_left) < 7, \"CRITICAL\", tonumber(days_left) < 30, \"WARNING\", tonumber(days_left) < 90, \"PLAN\", 1==1, \"OK\")\n| where urgency != \"OK\"\n| table host, cert_name, application, vserver, owner, days_left, urgency, auto_renew\n| sort days_left\n```\n\nStep 3 -- Validate\n(a) On ADC CLI: `show ssl certkey` -- compare expiry dates with Splunk.\n(b) Identify a certificate expiring within 30 days and verify it appears.\n(c) Check certificate bindings: `show ssl vserver <vs>` -- verify the correct cert is bound.\n\nStep 4 -- Operationalize\nDashboard (\"Citrix ADC -- SSL Certificate Lifecycle\"):\n* Row 1 -- Single-value: \"Expired\", \"< 7 days\", \"< 30 days\", \"Total certs\".\n* Row 2 -- Certificate expiry table with owner and auto-renewal status.\n\nAlerting:\n* Critical (cert expired or < 7 days): immediate renewal.\n* Warning (cert < 30 days): schedule renewal.\n* Info (weekly): certificate report.\n\nStep 5 -- Troubleshooting\n\n* **Certificate renewed but ADC still shows old** -- After uploading a new cert, you must update the binding: `update ssl certkey <name> -cert <new_file> -key <new_key>`. If HA pair, sync: `force ha sync`.\n\n* **Wildcard cert expiring** -- Wildcard certs are often shared across many vservers. Use the lookup to identify all affected applications and coordinate renewal.\n\n* **Let's Encrypt / ACME certs** -- If using automated renewal, verify the automation is running. Citrix ADC supports ACME protocol natively in recent firmware.",
  "grandmaExplanation": "We look at time left on listener certificates in one place on the same gear so renewals are planned, not an outage story.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
