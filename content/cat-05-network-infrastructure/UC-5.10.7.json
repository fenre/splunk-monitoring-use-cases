{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.10.7",
  "title": "SIP Trunk Health and Call Setup Success Rate",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "industry": "Telecommunications",
  "exclusions": "Covers the detection logic documented for 'SIP Trunk Health and Call Setup Success Rate' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_sip_trunk_health_and_call_setup_success_rate` (UC-5.10.7), SOC dashboard panel \"Operations — SIP Trunk Health and Call Setup Success Rate\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.10.7 positive (SIP, Trunk, Health, and, Call): ingest synthetic events into `index=telecom_sip` sourcetype='stream:sip' where `unique_sessions` grouped by `_time`, `dest` satisfies `reply_code` >= 400, `reply_code` < 700, `invites` >= 50, or `cs_sr_pct` < 97 for 'SIP Trunk Health and Call Setup Success Rate'. Validate lookup `sip_trunks.csv`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.10.7 negative (Success Rate): ingest benign `telecom_sip` / `stream:sip` events where `_time`, `dest` stays below `reply_code` >= 400, `reply_code` < 700, `invites` >= 50, or `cs_sr_pct` < 97, or exclude the test entity via `ops_exception_register.csv`. Variant 12 confirms zero alert rows for 'SIP Trunk Health and Call Setup Success Rate'."
  },
  "dataSources": "`index=telecom_sip` (or your Stream SIP index) with `sourcetype=stream:sip`; optionally augment with `dest`, `src`, `reply_code`, `call_id`, `method`, `dest_zone`, carrier/trunk tagging via CSV lookup keyed by trunk endpoint IPs. Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk App for Stream` (Splunkbase #1809) plus optional `sip_trunks.csv` lookup for carrier/trunk labels. — Universal Forwarder (Splunkbase 617)",
  "spl": "index=telecom_sip sourcetype=\"stream:sip\" method=\"INVITE\"\n| bin _time span=15m\n| stats count as invites,\n        sum(eval(if(reply_code IN (180,183,200),1,0))) as progressing_ok,\n        sum(eval(if((reply_code>=400 AND reply_code<700) OR reply_code IN (403,404,408,486,487,503,603),1,0))) as invite_failures,\n        sum(eval(if(reply_code==487,1,0))) as canceled_before_answer,\n        dc(call_id) as unique_sessions\n        by _time, dest\n| eval cs_sr_pct=round(100*(invites-invite_failures)/invites,2)\n| eval progressing_pct=round(100*progressing_ok/invites,2)\n| where invites>=50 AND (cs_sr_pct < 97 OR progressing_pct < 92)\n| lookup sip_trunks.csv dest_ip as dest OUTPUT carrier_name trunk_group\n| eval trunk_label=coalesce(carrier_name,dest)\n| table _time trunk_label invites progressing_pct cs_sr_pct invite_failures canceled_before_answer\n| sort -invite_failures",
  "description": "Identifies UC-5.10.7 — SIP, Trunk, Health, and, Call — using `index=telecom_sip` sourcetype `stream:sip`, grouped by `_time`, `dest`. Outputs `unique_sessions` each `15m`; alert when `reply_code` >= 400, `reply_code` < 700, `invites` >= 50, or `cs_sr_pct` < 97. Filter chain includes lookup `sip_trunks.csv`. Purpose-built for 'SIP Trunk Health and Call Setup Success Rate' in Operations workflows.",
  "value": "Tracking `unique_sessions` via UC-5.10.7 on `stream:sip` feeds improves response to 'SIP Trunk Health and Call Setup Success Rate' and reduces operational risk and delayed incident response.",
  "implementation": "Point Stream at bilateral SIP signaling toward PSTN/SBC trunk IPs; normalize INVITE events into `telecom_sip`; attach trunk/carrier labels via lookup; schedule the fifteen-minute aggregation; tune minimum invites-per-window so sparse routes do not page falsely.",
  "visualization": "Row 1: single-value tiles per carrier for fifteen-minute CS-SR with traffic-light thresholds; Row 2: timechart of progressing_pct and cs_sr_pct stacked per trunk; Row 3: table of failures by SIP response code with drill to Stream packet capture IDs if stored.",
  "cimModels": [],
  "references": [
    {
      "title": "Splunkbase — Splunk App for Stream",
      "url": "https://splunkbase.splunk.com/app/1809",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RFC 3261 — SIP: Session Initiation Protocol",
      "url": "https://www.rfc-editor.org/rfc/rfc3261",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `telecom_sip` (UC-5.10.7)** — Planned work for 'SIP Trunk Health and Call Setup Success Rate' shifts `unique_sessions` over `span=15m` while `_time`, `dest` remains in-scope. Applies lookup `sip_trunks.csv`. Cross-check CMDB CHG.\n\n2. **Batch `sum` on `stream:sip`** — Scheduled automation elevates `dest` without crossing `reply_code` >= 400 for UC-5.10.7. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `_time`, `dest`** — Health checks in `telecom_sip` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `stream:sip`** — First 48h baseline for UC-5.10.7 on `_time` is unstable; join `sip_trunks.csv` prior to threshold.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `_time`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco",
    "stream"
  ],
  "equipmentModels": [
    "cisco_ios",
    "stream_forwarder"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Splunk App for Stream (Splunkbase 1809) 8.x forwarders tapping trunk-facing VLAN or ERSPAN with symmetric bidirectional captures so responses arrive with requests.\n• Inventory CSV `sip_trunks.csv` mapping signaling endpoints (`dest_ip`), carrier-facing trunk_group identifiers, commercial carrier_name, and routing class (national/international/satellite) when thresholds differ.\n• Written interconnect KPI targets from acceptance testing—many carriers quote combined setup-success objectives excluding intentionally busy subscribers; align SPL exclusions accordingly.\n• Enough license capacity for sustained INVITE telemetry—historically multiple TB/day on busy PSTN breakout farms.\n\nStep 1 — Configure Stream SIP extraction focusing on `method=INVITE`. Validate `reply_code`, `call_id`, `dest`, `_time`, and `setup_delay` fields via fifteen-minute verification searches comparing counts against session border controller logs.\n\nStep 2 — Land events in `index=telecom_sip` with sourcetype `stream:sip`; automate lookup synchronization whenever trunk IPs change after migrations or wholesale resale rearrangements.\n\nStep 3 — Implement scheduled searches computing progressing_pct (share receiving 180/183/200 first-leg responses) and cs_sr_pct (share avoiding hard 4xx/5xx/6xx failures including canceled attempts). Require `invites>=50` per bucket to suppress noise on dormant routes; optionally stratify thresholds using lookup `call_type`.\n\nStep 4 — Operational dashboard couples KPI tiles, fifteen-minute trend charts per trunk_group, and Rapid Investigation drilldown launching raw events constrained by `call_id`. Tie Splunk alerts to ticketing via webhook carrying trunk_label and rolling averages.\n\nStep 5 — Troubleshooting: asymmetric taps yield artificial failures—validate ERSPAN ACL directionality; lab trunks should be filtered with explicit dest exclusions; when audio suffers despite healthy counters, pivot to UC-5.10.6 post-dial delay analytics and RTP loss panels.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk App for Stream` (Splunkbase #1809) plus optional `sip_trunks.csv` lookup for carrier/trunk labels. — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=stream:sip into telecom_sip. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, carrier_name trunk_group, cs_sr_pct, dest`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We count how many calls actually get through each outside line we buy so when one supplier quietly starts dropping setups we see it on a chart instead of hearing angry stories days later.",
  "prerequisiteUseCases": [],
  "telcoUseCase": "IMS interconnect acceptance & KPI enforcement",
  "wave": "walk"
}
