{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.7.13",
  "title": "Traffic Matrix Baseline Deviation Detection",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Anomaly",
    "Performance",
    "Security"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'Traffic Matrix Baseline Deviation Detection'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-5.7.13).",
  "evidence": "Saved search uc_5_7_13_traffic_matrix_baseline_deviation_detect, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'Traffic Matrix Baseline Deviation Detection'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-5.7.13.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "`index=netflow` with `sourcetype=netflow` or `stream:ipfix` containing `src`, `dest`, `bytes`, optional `src_interface`, `dest_interface`; enrich with `cidrmatch` or `lookup zone_matrix.csv` mapping addresses to site or metro buckets before aggregation.",
  "app": "Splunk Add-on for NetFlow (Splunkbase 1838); Splunk Machine Learning Toolkit optional for robust baselines",
  "spl": "index=netflow earliest=-8h\n| lookup zone_matrix.csv ip as src OUTPUT zone as src_zone\n| lookup zone_matrix.csv ip as dest OUTPUT zone as dest_zone\n| where isnotnull(src_zone) AND isnotnull(dest_zone)\n| bin _time span=1h\n| stats sum(bytes) as bytes by _time src_zone dest_zone\n| eventstats median(bytes) as med by src_zone dest_zone\n| eval deviation=if(med>0, abs(bytes-med)/med, 0)\n| where bytes>104857600 AND deviation>2\n| sort - deviation\n| head 100",
  "description": "Measures hourly bytes between zone pairs against a trailing median derived from the same search window to spotlight pairs whose volume jumped or collapsed compared with recent behavior.",
  "value": "Operators catch routing leaks, bulk data movement, or failed reroutes early; executives gain a simple east-west versus north-south imbalance signal without deploying new agents beyond existing flow exporters.",
  "implementation": "Maintain authoritative zone lookups; run hourly matrix stats; tune deviation multiplier and minimum-byte guardrails per environment; integrate alerts with change-management calendars.",
  "visualization": "Heatmap of zones × zones with deviation score; companion single-value for count of hot pairs; timeline overlay of change tickets.",
  "references": [
    {
      "title": "Splunk Blog — Building Network Insights",
      "url": "https://www.splunk.com/en_us/blog/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "NIST SP 800-53 AU-6 — Audit Record Review",
      "url": "https://csrc.nist.gov/projects/cprt/catalog#/control?version=SP_800_53_5_1_1&control=AU-6",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "netflow"
  ],
  "equipmentModels": [
    "netflow_ipfix",
    "netflow_netflow",
    "netflow_sflow"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Reliable `zone_matrix.csv` with CIDR keys covering internal and partner prefixes; refresh weekly from your internet protocol address management source.\n• At least fourteen days of retained netflow data for seasonal baselines when moving beyond the inline median example.\n• Agreement on minimum-byte thresholds so tiny flows do not noise the alert channel.\n\nStep 1 — Configure data collection\nConfirm exporters preserve internal addressing without excessive summarization. If Network Address Translation hides sources, attach post-address-allocation fields via intermediate syslog correlation.\n\nStep 2 — Create the search\nMaterialize a summary index `traffic_matrix_hourly` using `collect` or scheduled transforms for scale. Replace inline `eventstats` with `| join type=left src_zone dest_zone [|inputlookup matrix_baseline_weekly]` when baselines are precomputed.\n\nStep 3 — Validate\nReplay a known maintenance failover that shifts metro traffic and verify the affected pair exceeds deviation threshold within one hour while unaffected pairs remain quiet.\n\nStep 4 — Operationalize\nColor a chord or sankey visualization from the stats output; attach recommended triage steps distinguishing symmetric denial-of-service floods from single-direction copy jobs.\n\nStep 5 — Troubleshooting\nSudden widespread deviation often indicates a broken lookup rather than the network—spot-check `| stats count by src_zone`. Clock skew across exporters widens artificial deltas; enforce Network Time Protocol discipline.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for NetFlow (Splunkbase 1838); Splunk Machine Learning Toolkit optional for robust baselines). Enable the modular input or HEC token that writes sourcetype=see SPL into netflow. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, bytes, dest_zone, deviation`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We compare how much traffic usually flows between each pair of sites with how much moved recently. When one corridor suddenly spikes or dries up, we raise a hand before customers feel it.",
  "prerequisiteUseCases": [],
  "wave": "run"
}
