{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.10.10",
  "title": "BGP Community and AS-Path Anomaly from Upstream Providers",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Security",
    "Anomaly"
  ],
  "splunkPillar": "Observability",
  "industry": "Telecommunications",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'BGP Community and AS-Path Anomaly from Upstream Providers'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-5.10.10).",
  "evidence": "Saved search uc_5_10_10_bgp_community_and_as_path_anomaly_from_u, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'BGP Community and AS-Path Anomaly from Upstream Providers'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-5.10.10.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "`index=network` syslog from IOS (`sourcetype=cisco:ios`) and Junos (`sourcetype=juniper:junos`) with BGP update/detail logging enabled—messages containing `COMMUNITY`, `EXTENDED_COMMUNITY`, `AS_PATH`, `path`, or `%BGP` keywords. Supplement with routing telemetry feeds if available. Maintain lookups `expected_upstream_communities.csv` (neighbor IP → allowed community regex list) and `transit_as_allowlist.csv` (upstream peer → permitted transit autonomous system numbers). Collected via Splunk Add-on for Cisco Security (Splunkbase 1352) modular input or scripted poll into the index named in the SPL.",
  "app": "`TA-cisco_ios`, `Splunk_TA_juniper`, or universal SC4S syslog parsing. — Splunk Add-on for Cisco Security (Splunkbase 1352)",
  "spl": "index=network earliest=-4h (sourcetype=\"cisco:ios\" OR sourcetype=\"juniper:junos\")\n    (\"%BGP\" OR \"BGP-5\" OR \"bgp_\" OR \"COMMUNITY\" OR \"Community\" OR \"community\" OR \"AS PATH\" OR \"AS_PATH\")\n| rex field=_raw \"neighbor\\s+(?<bgp_neighbor>[0-9a-fA-F:\\.]+)\"\n| rex field=_raw max_match=20 \"(?i)community(?:[^0-9a-fA-F:\\s]|\\s)+(?<comm_raw>[0-9a-fA-F:\\s,.]+)\"\n| rex field=_raw \"(?i)(?:AS[_ ]?PATH|ASPATH|As-Path)\\s*:\\s*(?<as_path>[0-9\\s\\.]+)\"\n| lookup expected_upstream_communities.csv bgp_neighbor OUTPUT allowed_community_regex transit_allow_regex\n| eval path_tokens=split(trim(as_path),\" \")\n| eval path_len=if(isnotnull(path_tokens), mvcount(path_tokens), 0)\n| eval unexpected_comm=if(isnotnull(allowed_community_regex) AND len(comm_raw)>2 AND NOT match(comm_raw,allowed_community_regex),1,0)\n| eval path_join=mvjoin(path_tokens,\" \")\n| eval unexpected_transit=if(isnotnull(transit_allow_regex) AND len(path_join)>2 AND NOT match(path_join,transit_allow_regex),1,0)\n| eval anomaly=case(unexpected_comm==1,\"COMMUNITY_MISMATCH\", unexpected_transit==1,\"ASPATH_MISMATCH\", path_len>12,\"LONG_PATH\", true(),null())\n| where isnotnull(anomaly)\n| stats count by host, bgp_neighbor, anomaly, comm_raw, path_join\n| sort -count",
  "description": "Parses carrier-facing BGP syslog updates for communities and ordered AS paths, comparing observed attributes against curated allowlists per upstream neighbor to flag unexpected tagging, opaque reroutes, or path-length inflation indicative of policy mistakes or upstream leaks.",
  "value": "Peering engineers gain automated audit evidence when wholesale providers prepend unexpected transit segments or stamp foreign communities—shortening mean-time-to-comprehension during billing disputes, route leaks, or mitigation actions like selective de-peering.",
  "implementation": "Enable BGP update logging at sane rates on edge routers; throttle Splunk ingestion via filters dropping repetitive withdraw churn; maintain CSV lookups synchronized from IRR/peering DB exports; alert on community mismatch before ASPATH if noise dominates.",
  "visualization": "Timeline with anomaly icons; Sankey-style AS path explorer fed by stats; table listing neighbor, anomaly class, sample community string, last occurrence.",
  "cimModels": [],
  "references": [
    {
      "title": "RFC 4271 — A Border Gateway Protocol 4 (BGP-4)",
      "url": "https://www.rfc-editor.org/rfc/rfc4271",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RFC 1997 — BGP Communities Attribute",
      "url": "https://www.rfc-editor.org/rfc/rfc1997",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "cisco",
    "dell_emc",
    "juniper",
    "syslog"
  ],
  "equipmentModels": [
    "cisco_ios",
    "juniper_mx"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Stable syslog forwarding with millisecond timestamps and consistent timezone normalization.\n• Documented policy stating which standard communities (including informational versus action communities) each upstream may attach.\n• IRR or internal source-of-truth exporting regex-friendly allowlists.\n• Splunk knowledge manager owning CSV lifecycle with Git-backed change control.\n\nStep 1 — Enable selective BGP logging features (`bgp log-neighbor-changes`, Junos `traceoptions`) limiting verbosity to avoid control-plane CPU exhaustion—consult vendor caps.\n\nStep 2 — Normalize sourcetypes; extract `comm_raw`, flattened AS sequence, and neighbor identifiers via `rex`; collapse whitespace on Junos formats differing from Cisco.\n\nStep 3 — Layer lookups translating neighbor IPs into regex constraints; implement dual alerts—warning for community drift, critical when ASPATH violates transit contract.\n\nStep 4 — Dashboard overlays anomaly timeline with BGP session state panels from UC-5.10.15 for holistic carrier correlation.\n\nStep 5 — Troubleshooting: ephemeral debugging communities during migrations trigger benign alerts—annotate maintenance CSV column `suppress_until`; overly greedy regex causes misses—test with `regex` command interactively; missing logs often mean rate-limited drops—verify router buffer statistics.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`TA-cisco_ios`, `Splunk_TA_juniper`, or universal SC4S syslog parsing. — Splunk Add-on for Cisco Security (Splunkbase 1352)). Enable the modular input or HEC token that writes sourcetype=cisco:ios, juniper:junos into network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`allowed_community_regex transit_allow_regex, anomaly, bgp_neighbor, comm_raw`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We compare the little stickers and directions carriers put on internet routes against what we agreed so weird detours or sneaky tags show up before they mess up billing or security.",
  "prerequisiteUseCases": [],
  "telcoUseCase": "Upstream routing policy assurance",
  "wave": "run"
}
