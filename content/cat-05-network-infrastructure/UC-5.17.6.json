{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.17.6",
  "title": "De-duplication and Header Stripping Statistics",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Data Quality",
    "Performance",
    "Operations"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'De-duplication and Header Stripping Statistics' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_de_duplication_and_header_stripping_statistics` (UC-5.17.6), SOC dashboard panel \"Operations — De-duplication and Header Stripping Statistics\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.17.6 positive (duplication, and, Header, Stripping, Statistics): ingest synthetic events into `index=visibility` sourcetype='target_sourcetype' where `avg_dedup`, `peak_dedup`, `strips` grouped by `_time`, `vendor`, `feat_host`, `feature_profile` satisfies `dedup_baseline` > 1, or `strips` > 1000000 for 'De-duplication and Header Stripping Statistics'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.17.6 negative (Stripping Statistics): ingest benign `visibility` / `target_sourcetype` events where `_time`, `vendor`, `feat_host`, `feature_profile` stays below `dedup_baseline` > 1, or `strips` > 1000000, or exclude the test entity via `ops_exception_register.csv`. Variant 14 confirms zero alert rows for 'De-duplication and Header Stripping Statistics'."
  },
  "dataSources": "Detailed: `index=visibility`; feature telemetry such as `gigamon:gigasmart`, `keysight:advanced_features`, `cpacket:analytics` exposing `dedup_removed_pct`, `strip_vlan_count`, `inner_ip_exposed`, `timestamp_normalized_events`, `payload_collapsed_frames`. Pair with tool-ingest byte counters (IDS/NDR) to sanity-check that stripping did not overshoot legitimate encapsulations. Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Gigamon GigaSMART application intelligence integrations, Keysight Vision advanced processing modules, generic syslog parsers plus Splunk-field extractions for JSON feature counters. — Universal Forwarder (Splunkbase 617)",
  "spl": "index=visibility earliest=-24h\n| eval v=lower(sourcetype)\n| eval vendor=case(match(v,\"gigamon\"),\"Gigamon\",match(v,\"keysight|ixia|vision\"),\"Keysight\",match(v,\"cpacket|cstor\"),\"cPacket\",\"other\")\n| where vendor!=\"other\"\n| eval feat_host=coalesce(host,chassis_name,\"unknown\")\n| eval dedup_pct=tonumber(coalesce(dedup_removed_pct,payload_dedup_ratio*100,null()))\n| eval strip_events=tonumber(coalesce(vlan_strip_count,header_strip_events,stripped_frames))\n| bin _time span=1h\n| stats avg(dedup_pct) as avg_dedup max(dedup_pct) as peak_dedup sum(strip_events) as strips by _time vendor feat_host feature_profile\n| streamstats global=f window=24 avg(avg_dedup) as dedup_baseline by vendor feat_host feature_profile\n| eval dedup_anomaly=if(peak_dedup>(dedup_baseline*3) AND dedup_baseline>1,1,0)\n| eval strip_spike=if(strips>1000000,1,0)\n| where dedup_anomaly=1 OR strip_spike=1\n| sort - peak_dedup vendor feat_host _time",
  "description": "Surfaces UC-5.17.6 — duplication, and, Header, Stripping, Statistics — using `index=visibility` sourcetype `configured sourcetype`, grouped by `_time`, `vendor`, `feat_host`, `feature_profile`. Outputs `avg_dedup`, `peak_dedup`, `strips` each `1h`; alert when `dedup_baseline` > 1, or `strips` > 1000000. Primary focus: Stripping Statistics. Purpose-built for 'De-duplication and Header Stripping Statistics' in Operations workflows.",
  "value": "Alerting on duplication, and, Header, Stripping, Statistics through UC-5.17.6 reduces operational risk and delayed incident response by surfacing `avg_dedup`, `peak_dedup`, `strips` early.",
  "implementation": "Document acceptable dedup ceilings per deployment tier; join Splunk results weekly with PCAP spot-checks on mirrored spans to validate retained headers match policy.",
  "visualization": "Stacked area chart of strips vs `_time`; overlaid line for avg_dedup; annotations from change-management lookup when profile hashes shift.",
  "references": [
    {
      "title": "Splunk Documentation — About summary indexing",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Indexer/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Gigamon Documentation — GigaSMART overview",
      "url": "https://docs.gigamon.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "IETF RFC 7014 — IP Flow Information Export (IPFIX) considerations for visibility",
      "url": "https://www.rfc-editor.org/rfc/rfc7014",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `visibility` (UC-5.17.6)** — Planned work for 'De-duplication and Header Stripping Statistics' shifts `avg_dedup`, `peak_dedup`, `strips` over `span=1h` while `_time`, `vendor`, `feat_host`, `feature_profile` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `configured_sourcetype`** — Scheduled automation elevates `vendor` without crossing `dedup_baseline` > 1 for UC-5.17.6. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `_time`, `vendor`, `feat_host`, `feature_profile`** — Health checks in `visibility` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-5.17.6 on `_time` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `_time`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "N/A (data hygiene monitoring)"
  ],
  "detectionType": "Anomaly",
  "securityDomain": "network",
  "equipment": [
    "apcon",
    "cpacket",
    "f5",
    "garland",
    "gigamon",
    "keysight",
    "syslog"
  ],
  "equipmentModels": [
    "apcon_visibility_platform",
    "cpacket_cstor",
    "f5_asm",
    "garland_network_tap",
    "gigamon_gigavue",
    "keysight_vision"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Signed architecture standards stating which encapsulations must survive to tools.\n• Lab PCAP corpus replay harness feeding identical traffic through profiles under test.\n• Namespaced `feature_profile` identifiers matching Git-managed templates.\n\nStep 1 — Enable telemetry\nActivate vendor counters for dedup sessions and strip engines; ensure logs include profile revision hashes after each deployment pipeline run.\n\nStep 2 — Normalize percentages\nConvert ratios to 0–100 scale consistently; ignore samples where denominator counters reset mid-hour.\n\nStep 3 — Build Splunk content\nMaterialize summary index `visibility_feat_hourly` from the rolling stats for twelve-month trending without re-scanning raw noisy streams.\n\nStep 4 — Validate\nReplay PCAP through staging appliance toggling dedup on/off—ensure Splunk deltas align with wireshark difference summaries within agreed tolerance.\n\nStep 5 — Operationalize\nAlert only after sustained anomalies (three consecutive hours) to ignore transient microbursts; dashboard annotate recent change tickets pulled via REST lookup.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Gigamon GigaSMART application intelligence integrations, Keysight Vision advanced processing modules, generic syslog parsers plus Splunk-field extractions for JSON feature counters. — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into visibility. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, dedup_anomaly, dedup_baseline, feat_host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Before copies reach our watchers, machines sometimes remove repeats or peel off labels so the picture is clearer—but peel too much and we lose the story. We watch those knobs so trimming stays helpful instead of accidentally hiding clues.",
  "prerequisiteUseCases": [],
  "wave": "walk"
}
