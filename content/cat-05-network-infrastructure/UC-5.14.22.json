{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.14.22",
  "title": "Varnish Gzip Compression Ratio by Content Type",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Cost"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Varnish Gzip Compression Ratio by Content Type' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_varnish_gzip_compression_ratio_by_content_type` (UC-5.14.22), SOC dashboard panel \"Operations — Varnish Gzip Compression Ratio by Content Type\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=proxy` sourcetype='varnish:vsl' where `ctype` satisfies detection threshold for UC-5.14.22 ('Varnish Gzip Compression Ratio by Content Type'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `proxy` / `varnish:vsl` events where `ctype` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-5.14.22."
  },
  "dataSources": "`index=proxy` `sourcetype=varnish:vsl` response header lines (sampled) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes — Universal Forwarder (Splunkbase 617)",
  "spl": "index=proxy sourcetype=\"varnish:vsl\"\n| rex field=_raw \"Content-Type:\\s+(?<ctype>[^\\s;]+)\"\n| stats count by ctype",
  "description": "JSON and text compress better than images; mis-tuned filters waste CPU. This use case monitors the condition described in the title using the SPL and data sources documented on this page.",
  "value": "JSON and text compress better than images; mis-tuned filters waste CPU. Timely UC-5.14.22 alerting on 'Varnish Gzip Compression Ratio by Content Type' reduces operational risk and delayed incident response before manual triage or audit channels escalate.",
  "implementation": "Pair with byte counters if available; sample to control volume.",
  "visualization": "Time series for rates and latency, top/dedup for hotspots, single-value alerts on health thresholds.",
  "cimModels": [],
  "references": [
    {
      "title": "Vendor documentation",
      "url": "https://varnish-cache.org/docs/trunk/users-guide/compression.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `proxy`** — Planned work for 'Varnish Gzip Compression Ratio by Content Type' shifts `count` over `span=1h` while `ctype` remains in-scope for UC-5.14.22. Cross-check CMDB CHG.\n\n2. **Batch `count` on `varnish:vsl`** — Scheduled automation elevates `src` without crossing configured threshold. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `ctype`** — Health checks in `proxy` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `varnish:vsl`** — First 48h baseline for `ctype` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `ctype`, `exception_reason`, `valid_until`.",
  "equipment": [
    "syslog",
    "varnish"
  ],
  "equipmentModels": [
    "syslog_generic",
    "varnish_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "Prerequisites\n* Varnish access logs with response size and content-type, plus `Accept-Encoding` and `Content-Encoding` headers. Key fields: `bytes_read`, `content_type`, `content_encoding`. Data in `index=proxy` with `sourcetype=varnish:access`.\n* Varnish gzip compression: enabled with `set beresp.do_gzip = true;` in `vcl_backend_response`. Varnish can compress responses before storing (objects stored compressed) and decompress for clients that don't support gzip. Key benefits: reduced storage usage and bandwidth. Compression ratio = uncompressed_size / compressed_size.\n\nStep 1 -- Configure data collection\nVCL compression:\n```\nsub vcl_backend_response {\n    if (beresp.http.Content-Type ~ \"(?i)(text/|application/json|application/javascript|application/xml)\") {\n        set beresp.do_gzip = true;\n    }\n}\n```\nLog format with compression info:\n```\nvarnishncsa -F '... %{Content-Type}o %{Content-Encoding}o %b ...'\n```\nVerify:\n```spl\nindex=proxy sourcetype=\"varnish:access\" earliest=-4h\n| eval is_gzipped=if(match(content_encoding, \"(?i)gzip\"), 1, 0)\n| stats count sum(is_gzipped) as gzipped by content_type\n| eval gzip_pct=round(100*gzipped/count, 1)\n| sort -count\n```\n\nStep 2 -- Create the search and alert\n\n**Primary search -- Compression ratio by content type:**\n```spl\nindex=proxy sourcetype=\"varnish:access\" earliest=-4h\n| eval response_bytes=tonumber(bytes_read)\n| eval is_compressed=if(match(content_encoding, \"(?i)gzip|br|deflate\"), 1, 0)\n| eval content_group=case(match(content_type, \"text/html\"), \"text/html\", match(content_type, \"application/json\"), \"application/json\", match(content_type, \"application/javascript|text/javascript\"), \"javascript\", match(content_type, \"text/css\"), \"text/css\", match(content_type, \"image/\"), \"image (skip)\", match(content_type, \"video/|audio/\"), \"media (skip)\", 1==1, \"other\")\n| where content_group != \"image (skip)\" AND content_group != \"media (skip)\"\n| stats count as responses sum(response_bytes) as total_bytes avg(response_bytes) as avg_bytes sum(is_compressed) as compressed_count by content_group\n| eval compression_rate=round(100*compressed_count/responses, 1)\n| eval total_mb=round(total_bytes/1048576, 1)\n| eval savings_potential=if(compression_rate < 50 AND match(content_group, \"text|json|javascript|css\"), \"HIGH -- \".content_group.\" should be compressed\", \"OK\")\n| sort -total_bytes\n| table content_group, responses, total_mb, avg_bytes, compression_rate, savings_potential\n```\n\nStep 3 -- Validate\n(a) Request with gzip: `curl -H \"Accept-Encoding: gzip\" -s -o /dev/null -w \"%{size_download}\" https://<varnish>/page`.\n(b) Request without: `curl -s -o /dev/null -w \"%{size_download}\" https://<varnish>/page`.\n(c) Calculate ratio: uncompressed/compressed. Typical HTML ratio: 4-8x.\n\nStep 4 -- Operationalize\nDashboard (\"Varnish -- Compression\"):\n* Row 1 -- Single-value: \"Compression rate (%)\", \"Bandwidth saved (est)\", \"Uncompressed text types\".\n* Row 2 -- Compression by content type table.\n\nStep 5 -- Troubleshooting\n\n* **Text content not compressed** -- Check: (1) `beresp.do_gzip = true` in VCL for the content type, (2) client sent `Accept-Encoding: gzip`, (3) content isn't already compressed by backend.\n\n* **Images/media being compressed** -- Gzipping already-compressed formats (JPEG, PNG, MP4) wastes CPU with no benefit. Exclude: `if (beresp.http.Content-Type !~ \"image|video|audio\")`.\n\n* **High CPU from compression** -- If Varnish CPU is high and compression is enabled, consider: (1) compress at the backend instead, (2) use `beresp.do_gzip` only for objects that will be served many times (high hit ratio), (3) reduce compression level.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=varnish:vsl into proxy. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`ctype`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch varnish gzip compression ratio by content type and catch issues early, before they turn into outages for the people who rely on the network.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
