{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.18.1",
  "title": "LSP (Label Switched Path) State Changes and Failures",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Availability",
    "Fault",
    "Operations"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'LSP (Label Switched Path) State Changes and Failures' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_lsp_label_switched_path_state_changes_and_failur` (UC-5.18.1), SOC dashboard panel \"Operations — LSP (Label Switched Path) State Changes and Failures\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=network` sourcetype='target_sourcetype' where `host` satisfies `count` >= 1 for UC-5.18.1 ('LSP (Label Switched Path) State Changes and Failures'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `network` / `target_sourcetype` events where `host` stays below `count` >= 1, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-5.18.1."
  },
  "dataSources": "Detailed: `index=network` (org-standard); `sourcetype` examples — `cisco:ios`, `cisco:ios_xr`, `cisco:ios_xe`, `juniper:junos`, `nokia:sros`; MPLS and RSVP syslog mnemonics (`RSVP_*`, `MPLS_*`, `TE-*`, `RSVP-*`, `mpls_lsp`, `rpath`, `tunnel`), SNMP traps for tunnel/LSP down, and streaming telemetry (`telemetry.mpls`, Junos `sensor`/gNMI LSP state). Normalize `host`, `vrf`, `tunnel_id`, `lsp_name`, `fec`, `ingress`, `egress`, `state`, `reason`. Collected via Splunk Add-on for Cisco Security (Splunkbase 1352) modular input or scripted poll into the index named in the SPL.",
  "app": "Cisco Networks Add-on for Splunk (`TA-cisco_ios`), Splunk Add-on for Juniper (`Splunk_TA_juniper`), Nokia SR OS syslog/telemetry parsers (custom TA or HEC JSON); optional Splunk Connect for Syslog for structured RFC5424. — Splunk Add-on for Cisco Security (Splunkbase 1352)",
  "spl": "index=network earliest=-24h@h latest=now\n| eval st=lower(coalesce(sourcetype,_sourcetype,\"\"))\n| where match(st,\"cisco:ios|cisco:ios_xr|cisco:ios_xe|juniper:junos|nokia:sros|nokia\")\n| eval msg=lower(_raw)\n| eval hit=case(\n    match(msg,\"lsp.*down|lsp.*removed|mpls.*lsp.*(?:fail|error)|tunnel.*(?:down|tear)|rsvp.*(?:path.*(?:error|tear)|session.*down)|signalling.*interrupted|backup.*active|primary.*(?:fail|switch)|fast.?reroute|sbfd.*(?:down|fail)|bfd.*(?:mpls|tunnel).*down\"),1,\n    match(msg,\"(?:mpls.?te|traffic.?eng).*tunnel.*(?:chang|state|down|up)\") AND match(msg,\"(?:chang|transition|state|down|up|fail)\"),1,\n    match(msg,\"ldp.*(?:lsp|fec).*\") AND match(msg,\"(?:withdraw|delete|fail|down)\"),1,\n    match(msg,\"junos.*(?:lsp|rsvp|mpls).*\") AND match(msg,\"(?:down|fail|error|tear|switchover)\"),1,\n    match(msg,\"(?:7750|timos|sros).*?(?:lsp|tunnel|mpls).*\") AND match(msg,\"(?:down|fail|clear|major|critical)\"),1,\n    0)\n| where hit=1\n| rex field=_raw max_match=0 \"(?i)(?:tunnel|lsp|pw)\\s*[:=]?\\s*(?<lsp_name>[^\\s,]+)\"\n| rex field=_raw max_match=0 \"(?i)(?:fec|prefix)\\s*[:=]?\\s*(?<fec>[0-9./:]+)\"\n| rex field=_raw max_match=0 \"(?i)vrf[_:\\s]+(?<vrf>[^\\s,]+)\"\n| stats count earliest(_time) as first_seen latest(_time) as last_seen values(lsp_name) as lsps values(fec) as fecs values(vrf) as vrfs by host st\n| where count>=1\n| sort - count",
  "description": "Splunk correlates MPLS and RSVP syslog streams so operator-visible label-path transitions, teardowns, protection switchovers, and signaling errors surface within minutes instead of waiting on customer trouble tickets after silent reroutes exhaust backup capacity.",
  "value": "Splunk correlates MPLS and RSVP syslog streams so operator-visible label-path transitions, teardowns, prote.... Timely UC-5.18.1 alerting on 'LSP (Label Switched Path) State Changes and Failures' reduces operational risk and delayed incident response before manual triage or audit channels escalate.",
  "implementation": "Land PE syslog with severity ≥informational for RSVP-TE and MPLS subsystems, normalize tunnel/LSP identifiers, schedule the search every five minutes for spikes by host, and route alerts to the carrier NOC with topology context.",
  "visualization": "Dashboard Studio: KPI row for PEs with LSP failures in 24h; middle `splunk.timechart` of event count by `host`; bottom table (`host`, `st`, `lsps`, `fecs`, `first_seen`, `last_seen`) with drilldown to verbose messages.",
  "references": [
    {
      "title": "Cisco IOS XR MPLS Configuration Guide — MPLS Traffic Engineering",
      "url": "https://www.cisco.com/c/en/us/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Juniper MPLS Applications User Guide — RSVP Signaling",
      "url": "https://www.juniper.net/documentation/us/en/software/junos/mpls/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Nokia InfoProducts — SR OS documentation portal",
      "url": "https://infoproducts.nokia.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `network`** — Planned work for 'LSP (Label Switched Path) State Changes and Failures' shifts `values` over `span=1h` while `host` remains in-scope for UC-5.18.1. Cross-check CMDB CHG.\n\n2. **Batch `values` on `configured_sourcetype`** — Scheduled automation elevates `st` without crossing `count` >= 1. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `network` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "N/A (availability monitoring)"
  ],
  "detectionType": "Baseline",
  "securityDomain": "network",
  "equipment": [
    "cisco",
    "juniper",
    "snmp",
    "syslog"
  ],
  "equipmentModels": [
    "cisco_asr_1000",
    "cisco_asr_9000",
    "cisco_ios",
    "juniper_mx",
    "snmp_generic"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Inventory mapping `host` to POP, role (P/PE), software train (IOS-XE, IOS-XR, Junos, SR OS), and maintenance windows.\n• Clock sync (PTP/NTP) verified so `_time` aligns with EMS correlators.\n\nStep 1 — Enable verbose MPLS/RSVP logging\nOn Cisco IOS-XR: `logging events level informational` under MPLS TE if policy allows; capture `%ROUTING-RSVP-*`, `%MPLS_*-TE-*`, and BFD session logs for TE tunnels. On Junos: commit `set protocols rsvp traceoptions file rsvp.log size 10m files 5` plus `set protocols mpls traceoptions file mpls.log` in lab first, then tune flag severity for production syslog export. On Nokia SR OS: enable `log syslog` for TIMOS MPLS/RSVP facility at `major` default, escalate `critical` for tunnel-down.\n\nStep 2 — Ingest and parse\nForward to Splunk via TCP syslog or HEC; in `props.conf` assign `LINE_BREAKER` for multi-line IOS-XR messages; extract `tunnel-id`, endpoint addresses, and `lsp-name` with vendor TA extractions or supplemental `REPORT-mpls_lsp` transforms.\n\nStep 3 — Saved search\nSave SPL as `mpls_lsp_state_failures_24h`; alert when `count`≥3 distinct failures per `host` in fifteen minutes or when message matches explicit “tunnel down” without matching suppress lookup `mpls_maintenance.csv`.\n\nStep 4 — Validate\nDuring a controlled ISIS metric tweak in lab, compare Splunk event timestamps to `show mpls traffic-eng tunnels brief` / `show rsvp session` / Junos `show mpls lsp extensive` / Nokia `show router rsvp session`; discrepancies >60s imply buffering or timezone skew.\n\nStep 5 — Operationalize\nDashboard: timeline by POP, drilldown to raw `_raw`; annotate planned fiber events via lookup-driven suppression; weekly review false-positive regex tuning with SME sign-off.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Cisco Networks Add-on for Splunk (`TA-cisco_ios`), Splunk Add-on for Juniper (`Splunk_TA_juniper`), Nokia SR OS syslog/telemetry parsers (custom TA or HEC JSON); optional Splunk Connect for Syslog for structured RFC5424. — Splunk Add-on for Cisco Security (Splunkbase 1352)). Enable the modular input or HEC token that writes sourcetype=see SPL into network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`hit, host, st`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch the carrier backbone’s special fast lanes so when one tears down or flips to a backup, we know right away. That way crews fix breaks before customers only notice silent slowdowns or dropped priority traffic.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
