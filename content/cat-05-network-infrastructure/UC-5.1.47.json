{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.1.47",
  "title": "Trunk Link Utilization and Performance (Meraki MS)",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Trunk Link Utilization and Performance (Meraki MS)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_trunk_link_utilization_and_performance_meraki_ms` (UC-5.1.47), SOC dashboard panel \"Operations — Trunk Link Utilization and Performance (Meraki MS)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=meraki` sourcetype='meraki:switchportsbyswitch' where `serial` satisfies `down_trunks` > 0 for UC-5.1.47 ('Trunk Link Utilization and Performance (Meraki MS)'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `meraki` / `meraki:switchportsbyswitch` events where `serial` stays below `down_trunks` > 0, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-5.1.47."
  },
  "dataSources": "Splunk_TA_cisco_meraki (Splunkbase #5580): Switch Ports by Switch input (sourcetype=meraki:switchportsbyswitch, daily, TA v3.2+, OAuth scope switch:config:read). Per-port utilization counters are NOT exposed by the polling API; use the Webhook Logs (HEC) input + Meraki alert profile 'switch port status changed' or 'switch port flapping' for live trunk-flap detection.",
  "app": "`Cisco Meraki Add-on for Splunk` (Splunkbase 5580)",
  "spl": "index=meraki sourcetype=\"meraki:switchportsbyswitch\" earliest=-24h\n| spath path=ports{} output=port_arr\n| mvexpand port_arr\n| spath input=port_arr\n| where type=\"trunk\" AND enabled=\"true\"\n| stats count as trunk_port_count,\n        values(name) as trunk_port_names,\n        sum(eval(if(status=\"Connected\",0,1))) as down_trunks\n         by serial, name, network.name\n| where down_trunks > 0\n| sort - down_trunks",
  "description": "Monitors inter-switch and uplink trunk utilization to identify bandwidth constraints. Searches `index=meraki` with sourcetype `meraki:switchportsbyswitch` for UC-5.1.47, aggregating `values` by `serial` over `the scheduled window`. Alerts when grouped results cross `down_trunks` > 0 in the saved search. Gives Operations operators a repeatable signal for 'Trunk Link Utilization and Performance (Meraki MS)' without manual log review.",
  "value": "Monitors inter-switch and uplink trunk utilization to identify bandwidth constraints. Timely UC-5.1.47 alerting on 'Trunk Link Utilization and Performance (Meraki MS)' reduces operational risk and delayed incident response before manual triage or audit channels escalate.",
  "implementation": "1. Enable the Switch Ports by Switch input. The TA polls GET /organizations/{orgId}/switch/ports/bySwitch daily and emits one event per switch with a ports[] array of {portId, name, type, enabled, status, vlan, allowedVlans, ...}. 2. Filter where type=trunk to identify uplink ports between switches. 3. For live link-down or utilization, configure a Meraki webhook alert profile and ingest via the Webhook Logs (HEC) input.",
  "visualization": "Trunk link utilization heatmap; timeline showing peak demand; capacity planning chart.",
  "cimModels": [],
  "references": [
    {
      "title": "Splunkbase app 5580",
      "url": "https://splunkbase.splunk.com/app/5580",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Add-on for Cisco Meraki (Splunkbase 5631)",
      "url": "https://splunkbase.splunk.com/app/5631",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Cisco Security documentation",
      "url": "https://www.cisco.com/c/en/us/support/security/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `meraki`** — Planned work for 'Trunk Link Utilization and Performance (Meraki MS)' shifts `values` over `span=1h` while `serial` remains in-scope for UC-5.1.47. Cross-check CMDB CHG.\n\n2. **Batch `values` on `meraki:switchportsbyswitch`** — Scheduled automation elevates `name` without crossing `down_trunks` > 0. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `serial`** — Health checks in `meraki` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `meraki:switchportsbyswitch`** — First 48h baseline for `serial` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `serial`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_meraki"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Cisco Meraki Add-on for Splunk` (Splunkbase 5580).\n• Ensure the following data sources are available: Splunk_TA_cisco_meraki (Splunkbase #5580): Switch Ports by Switch input (sourcetype=meraki:switchportsbyswitch, daily, TA v3.2+, OAuth scope switch:config:read). Per-port utilization counters are NOT exposed by the polling API; use the Webhook Logs (HEC) input + Meraki alert profile 'switch port status changed' or 'switch port flapping' for live trunk-flap detection..\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\n1. Enable the Switch Ports by Switch input. The TA polls GET /organizations/{orgId}/switch/ports/bySwitch daily and emits one event per switch with a ports[] array of {portId, name, type, enabled, status, vlan, allowedVlans, ...}. 2. Filter where type=trunk to identify uplink ports between switches. 3. For live link-down or utilization, configure a Meraki webhook alert profile and ingest via the Webhook Logs (HEC) input.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=meraki sourcetype=\"meraki:switchportsbyswitch\" earliest=-24h\n| spath path=ports{} output=port_arr\n| mvexpand port_arr\n| spath input=port_arr\n| where type=\"trunk\" AND enabled=\"true\"\n| stats count as trunk_port_count,\n        values(name) as trunk_port_names,\n        sum(eval(if(status=\"Connected\",0,1))) as down_trunks\n         by serial, name, network.name\n| where down_trunks > 0\n| sort - down_trunks\n```\n\nUnderstanding this SPL\n\n**Trunk Link Utilization and Performance (Meraki MS)** — Network engineers monitor Meraki MS trunk link utilization, detecting inter-switch uplink saturation that impacts all downstream VLANs and devices.\n\nDocumented **Data sources**: Splunk_TA_cisco_meraki (Splunkbase #5580): Switch Ports by Switch input (sourcetype=meraki:switchportsbyswitch, daily, TA v3.2+, OAuth scope switch:config:read). Per-port utilization counters are NOT exposed by the polling API; use the Webhook Logs (HEC) input + Meraki alert profile 'switch port status changed' or 'switch port flapping' for live trunk-flap detection. **App/TA** (typical add-on context): `Cisco Meraki Add-on for Splunk` (Splunkbase 5580). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: meraki; **sourcetype**: meraki:switchportsbyswitch. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=meraki, sourcetype=\"meraki:switchportsbyswitch\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Extracts structured paths (JSON/XML) with `spath`.\n• Expands multivalue fields with `mvexpand` — use `limit=` to cap row explosion.\n• Extracts structured paths (JSON/XML) with `spath`.\n• Filters the current rows with `where type=\"trunk\" AND enabled=\"true\"` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by serial, name, network.name** so each row reflects one combination of those dimensions (useful for per-host, per-user, or per-entity comparisons for this use case).\n• Filters the current rows with `where down_trunks > 0` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Trunk link utilization heatmap; timeline showing peak demand; capacity planning chart.",
  "grandmaExplanation": "We help you know early when something looks wrong with trunk link utilization and performance so the team can act before it grows into a bigger outage.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
