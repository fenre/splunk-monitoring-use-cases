{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.14.28",
  "title": "Squid HTTP Access Denied by ACL",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Audit"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Squid HTTP Access Denied by ACL' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_squid_http_access_denied_by_acl` (UC-5.14.28), SOC dashboard panel \"Operations — Squid HTTP Access Denied by ACL\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.14.28 positive (Squid, HTTP, Access, Denied, ACL): ingest synthetic events into `index=proxy` sourcetype='squid:access' where `count` grouped by `acl`, `dst_domain`, `src_ip` satisfies detection threshold for 'Squid HTTP Access Denied by ACL'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.14.28 negative (by ACL): ingest benign `proxy` / `squid:access` events where `acl`, `dst_domain`, `src_ip` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 1 confirms zero alert rows for 'Squid HTTP Access Denied by ACL'."
  },
  "dataSources": "`index=proxy` `sourcetype=squid:access` with ACL name field if configured Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes — Universal Forwarder (Splunkbase 617)",
  "spl": "index=proxy sourcetype=\"squid:access\"\n| where match(code, \"TCP_DENIED|ERR_ACCESS_DENIED|NONE_ABORTED\")\n| stats count by acl, dst_domain, src_ip\n| sort - count\n| head 40",
  "description": "Alerts on by ACL activity in UC-5.14.28 ('Squid HTTP Access Denied by ACL') from `index=proxy` sourcetype `squid:access`. Rolls up `count` by `acl`, `dst_domain`, `src_ip` across `the scheduled window` and pages when configured threshold. Covers Squid, HTTP, Access, Denied, ACL. Operations operators treat UC-5.14.28 as the canonical signal.",
  "value": "Operators running UC-5.14.28 on `squid:access` feeds shorten triage for by ACL tied to 'Squid HTTP Access Denied by ACL', lowering operational risk and delayed incident response.",
  "implementation": "Add ACL tag to `access_log format` (Squid 4+); sanitize sensitive domains in dashboards.",
  "visualization": "Time series for rates and latency, top/dedup for hotspots, single-value alerts on health thresholds.",
  "cimModels": [],
  "references": [
    {
      "title": "Vendor documentation",
      "url": "http://www.squid-cache.org/Doc/config/http_access/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `proxy` (UC-5.14.28)** — Planned work for 'Squid HTTP Access Denied by ACL' shifts `count` over `span=1h` while `acl`, `dst_domain`, `src_ip` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `squid:access`** — Scheduled automation elevates `dst_domain` without crossing configured threshold for UC-5.14.28. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `acl`, `dst_domain`, `src_ip`** — Health checks in `proxy` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `squid:access`** — First 48h baseline for UC-5.14.28 on `acl` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `acl`, `exception_reason`, `valid_until`.",
  "equipment": [
    "squid",
    "syslog"
  ],
  "equipmentModels": [
    "squid_generic",
    "syslog_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "Prerequisites\n* Squid access logs with TCP_DENIED status and ACL information. Data in `index=proxy` with `sourcetype=squid:access`. Key fields: `squid_request_status=TCP_DENIED`, `http_status_code=403`, `request_url`, `src_ip` (client IP).\n* ACL-based access control: Squid ACLs (`acl` + `http_access allow/deny`) control which clients can access which destinations. Denied requests return TCP_DENIED/403. High denial rates may indicate: (1) policy too restrictive, (2) unauthorized access attempts, (3) malware trying to reach C2 servers.\n\nStep 1 -- Configure data collection\n```\n# squid.conf\nacl allowed_sites dstdomain .example.com .internal.corp\nhttp_access allow allowed_sites\nhttp_access deny all\n```\nVerify:\n```spl\nindex=proxy sourcetype=\"squid:access\" squid_request_status=\"TCP_DENIED\" earliest=-4h\n| stats count by src_ip, request_url\n| sort -count | head 20\n```\n\nStep 2 -- Create the search and alert\n\n**Primary search -- ACL denial analysis by client and destination:**\n```spl\nindex=proxy sourcetype=\"squid:access\" earliest=-4h\n| where match(squid_request_status, \"(?i)DENIED\")\n| rex field=request_url \"https?://(?<denied_domain>[^/]+)\"\n| bin _time span=15m\n| stats count as denials dc(src_ip) as affected_clients dc(denied_domain) as unique_domains values(denied_domain) as sample_domains by _time\n| eval severity=case(denials > 500, \"HIGH -- heavy denial volume\", affected_clients > 50, \"WARNING -- many clients being denied\", unique_domains > 100, \"WARNING -- diverse denied destinations (possible malware)\", 1==1, \"OK\")\n| where severity != \"OK\"\n| table _time, denials, affected_clients, unique_domains, sample_domains, severity\n```\n\n**Top denied clients:**\n```spl\nindex=proxy sourcetype=\"squid:access\" squid_request_status=\"TCP_DENIED\" earliest=-4h\n| rex field=request_url \"https?://(?<denied_domain>[^/]+)\"\n| stats count as denials dc(denied_domain) as unique_domains values(denied_domain) as top_domains by src_ip\n| sort -denials | head 20\n```\n\nStep 3 -- Validate\n(a) Request a blocked URL: `curl -x http://<squid>:3128 http://blocked.example.com/` -- should get 403.\n(b) Verify the denial appears in the access log with TCP_DENIED.\n(c) `squidclient mgr:counters | grep denied` -- shows denial counts.\n\nStep 4 -- Operationalize\nDashboard (\"Squid -- Access Denials\"):\n* Row 1 -- Single-value: \"Denials (4h)\", \"Denied clients\", \"Denied domains\".\n* Row 2 -- Denial trending timechart.\n* Row 3 -- Top denied clients and domains tables.\n\nAlerting:\n* High (single client > 500 denials/hr): possible malware or misconfigured app.\n* Warning (denials to > 100 unique domains from single client): suspicious C2 behavior.\n\nStep 5 -- Troubleshooting\n\n* **Legitimate traffic being denied** -- Review ACL order (Squid processes ACLs top-down, first match wins). Use `squid -k parse` to validate config syntax. Check `http_access` order.\n\n* **High denials from single client** -- Investigate: (1) malware attempting outbound connections, (2) application misconfigured with wrong proxy settings, (3) browser extensions trying to reach analytics/ad networks.\n\n* **Denied requests to internal hosts** -- Squid is blocking internal traffic. Ensure internal ranges are in an `acl localnet` with `http_access allow localnet`.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=squid:access into proxy. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`acl, dst_domain, src_ip`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch squid http access denied by acl and catch issues early, before they turn into outages for the people who rely on the network.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
