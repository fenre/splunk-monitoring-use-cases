{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.13.31",
  "title": "Compliance by Rule/Policy Category",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Compliance",
    "Configuration"
  ],
  "splunkPillar": "Security",
  "owner": "Head of IT Operations",
  "controlFamily": "log-source-completeness",
  "compliance": [
    {
      "regulation": "NIST-800-53",
      "version": "Rev. 5",
      "clause": "CM-6",
      "mode": "satisfies",
      "assurance": "partial",
      "assurance_rationale": "Monitors device configuration compliance status against Catalyst Center policies, providing evidence of configuration baseline enforcement.",
      "controlObjective": "Breaks non-compliance out by `complianceType` so the organisation can see which control settings fail most often, supporting focused remediation under CM-6.",
      "evidenceArtifact": "Saved search or dashboard panel `UC-5.13.31` (violations and affected device counts by category) with drilldown; attach to the CM evidence pack for each review period."
    },
    {
      "regulation": "PCI-DSS",
      "version": "v4.0",
      "clause": "1.1.1",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Demonstrates network device configuration compliance monitoring as part of firewall and router configuration standards.",
      "controlObjective": "Clarifies which classes of device configuration (for example image versus running config) drive failures, helping prove coverage of 1.1.1 in practice.",
      "evidenceArtifact": "Dashboard export and optional lookup table of remediation owners per `complianceType` for QSA walkthroughs."
    }
  ],
  "dataSources": "`index=catalyst sourcetype=cisco:dnac:compliance`. from the `compliance` modular input in the Cisco Catalyst Add-on (Splunkbase 7538) polling `GET /dna/intent/api/v1/compliance`. Default poll interval: 3600s (1h). Volume: ~1 event per device per poll. RBAC: NETWORK-ADMIN-ROLE. Key fields: `complianceStatus` (string), `complianceType` (string), `deviceName` (string), `deviceUuid` (string), `managementIpAddress` (string), `lastUpdateTime` (epoch milliseconds). CIM data model: Change.",
  "app": "`Cisco Catalyst Add-on for Splunk` (Splunkbase 7538)",
  "spl": "index=catalyst sourcetype=\"cisco:dnac:compliance\" complianceStatus=\"NON_COMPLIANT\" | stats count as violations dc(deviceName) as affected_devices by complianceType | sort -violations",
  "description": "Breaks down non-compliance by policy rule category to identify which compliance areas (running config, image, PSIRT, etc.) have the most violations.",
  "value": "Not all compliance violations are equal. Breaking down by rule category reveals whether the problem is configuration drift, outdated images, or unpatched vulnerabilities.",
  "implementation": "Enable the `compliance` input. If `complianceType` is nested, use `spath` or field aliases from the TA so category names appear consistently.",
  "visualization": "Bar chart (violations by complianceType), table with affected device counts.",
  "references": [
    {
      "title": "Splunkbase app 7538",
      "url": "https://splunkbase.splunk.com/app/7538"
    },
    {
      "title": "Catalyst Center API docs",
      "url": "https://developer.cisco.com/docs/catalyst-center/"
    },
    {
      "title": "Catalyst Center Integration Guide",
      "url": "docs/guides/catalyst-center.md"
    },
    {
      "title": "Catalyst Center Compliance API — Cisco DevNet",
      "url": "https://developer.cisco.com/docs/catalyst-center/#!get-compliance-status"
    }
  ],
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_catalyst_center"
  ],
  "knownFalsePositives": "**Single compliance type dominating the violation count due to template update.** After a golden template update for one compliance type (e.g., RUNNING_CONFIG), that type may show a spike in violations while other types remain stable. Distinguish by checking whether the spike in one `complianceType` coincides with a template change. This is a real finding — the violations are genuine, but the cause is the template change, not device drift.\n\n**PSIRT compliance type showing violations for advisories with no available fix.** Devices flagged as non-compliant for PSIRT advisories where no fixed software version is available cannot be remediated. Distinguish by checking the advisory's `fixedVersions` — if empty, the device cannot be patched. Suppress by separating PSIRT violations with available fixes from those without, and tracking the latter in a risk-accepted dashboard.\n\n**IMAGE compliance violations for devices intentionally running non-golden versions.** Pilot or canary devices running a newer firmware version than the golden image will show as IMAGE non-compliant. Distinguish by checking whether the device's running version is newer than the target version. Suppress by maintaining a `catalyst_swim_exceptions` lookup for devices intentionally on non-golden firmware.\n\n**EOX compliance type flagging devices within their support window.** Some devices may be incorrectly flagged for EOX compliance if the EOX database in Catalyst Center is not current. Distinguish by verifying the device's EOX status against the official Cisco EOX API. Suppress by cross-referencing with the current Cisco EOX announcements.",
  "detailedImplementation": "Prerequisites\n• Cisco Catalyst Add-on for Splunk (Splunkbase 7538) with the **compliance** modular input writing sourcetype `cisco:dnac:compliance` to `index=catalyst`.\n• Service account: **`SUPER-ADMIN-ROLE`** or **`NETWORK-ADMIN-ROLE`** (or the roles your Cisco TA documents for compliance Intent API read access); verify the controller virtual domain/scope includes the sites you expect.\n• Field names and nesting for `complianceType` and `complianceStatus` can vary by Catalyst Center release—sample one **raw** JSON event and confirm the TA extraction before building dashboards.\n• `docs/implementation-guide.md` and `docs/guides/catalyst-center.md` for install paths, index sizing, and credential handling.\n\nStep 1 — Configure data collection\n• **Intent API (typical):** device compliance results as exposed in your TA version; the add-on normalises to `cisco:dnac:compliance`.\n• **TA input:** **compliance**; confirm the destination **index** and **sourcetype** match this UC (`cisco:dnac:compliance`).\n• **Poll interval:** follow the Cisco TA default (often 15–60 minutes); tighter intervals mean fresher `NON_COMPLIANT` counts but more API load.\n• **Key fields to validate in Search:** `complianceType`, `complianceStatus` (`COMPLIANT` / `NON_COMPLIANT` or your build’s values), `deviceName`.\n• If `complianceType` is nested in JSON, add **field extractions** or `spath` in your knowledge objects so category names are stable in panels.\n\nStep 2 — Create the search and report\n```spl\nindex=catalyst sourcetype=\"cisco:dnac:compliance\" complianceStatus=\"NON_COMPLIANT\" | stats count as violations dc(deviceName) as affected_devices by complianceType | sort -violations\n```\n\nUnderstanding this SPL (what the alert condition really is)\n**Compliance by Rule/Policy Category** — Ranks *which classes* of policy fail most, not *which* single device to fix first (use `UC-5.13.33` for row-level detail).\n• The filter on `complianceStatus=\"NON_COMPLIANT\"` is intentional: passing devices are excluded so the bar chart is actionable.\n• `dc(deviceName)` is an **approximate** count of impacted devices; if the TA emits multiple rows per device per poll, deduplicate in a stricter panel (`latest` by key) before trusting exact headcount.\n• Sort `-violations` orders the noisiest **complianceType** first for program-level prioritisation (image/PSIRT/config families).\n\n**Pipeline walkthrough**\n• Non-compliant rows only → `stats` of violation **events** and **distinct** hostnames by category → **sort** for executive or engineering review.\n\nStep 3 — Validate (completeness vs Catalyst Center)\n• In Catalyst **Network Assurance / Compliance** (wording varies by version), open the same site scope and compare the top **categories** to this chart within one poll window.\n• In Splunk: `| tstats count WHERE index=catalyst sourcetype=\"cisco:dnac:compliance\" by complianceType` to confirm all expected **types** (running-config, image, etc.) appear.\n• `| timechart count` on the sourcetype to catch silent **ingest gaps** (zero events across hours).\n\nStep 4 — Operationalize\n• **Dashboard:** bar of **violations** by `complianceType` with a drilldown to a **detail** search (`deviceName` list) and link to the Catalyst **device** or **compliance** view.\n• **Change correlation:** add a `notes` or **ITSM** lookup for scheduled policy pushes; spike the panel after approved changes to avoid false escalations from expected waves of `NON_COMPLIANT` results.\n• **Access:** `deviceName` and policy text can be sensitive—restrict the app or dashboard to **NetEng / SecOps** roles.\n\nStep 5 — Troubleshooting\n• **No `cisco:dnac:compliance` data:** re-enable the **compliance** input, verify the **Catalyst** base URL, credentials, and **role**; scan `splunkd.log` for REST **403/5xx** from the input worker.\n• **Empty or odd `complianceType`:** inspect **raw** JSON; add **EXTRACT** or `spath` until `by complianceType` is populated.\n• **Count explosion:** duplicate **poll** rows—use `| stats ... by complianceType, deviceName` in a stricter view or add **dedup** keyed on a TA-provided `lastUpdated` field if present.\n• **Mismatch vs UI:** check **Catalyst** site/scope, **RMA** renames, and **stale** inventory; Splunk can only reflect what the **Intent API** returned on the last successful poll.\n• **No compliance events arriving:** verify the `compliance` input is enabled and check `index=_internal` for API error or endpoint timeout messages from the TA.\n• **NULL values in compliance status:** devices in the middle of a compliance check show IN_PROGRESS; empty values indicate the compliance engine has not run for that device.\n• **Fewer devices than expected:** check the service account role; credential access denied errors indicate the account lacks NETWORK-ADMIN-ROLE.",
  "grandmaExplanation": "We show you exactly which types of rules are being broken most often — is it configuration settings, outdated software, or security patches that are missing?",
  "prerequisiteUseCases": [
    "UC-5.13.28"
  ],
  "wave": "walk"
}
