{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.13.33",
  "title": "Compliance Violation Detail Drill-Down",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Compliance",
    "Configuration"
  ],
  "splunkPillar": "Security",
  "owner": "Head of IT Operations",
  "controlFamily": "log-source-completeness",
  "compliance": [
    {
      "regulation": "NIST-800-53",
      "version": "Rev. 5",
      "clause": "CM-6",
      "mode": "satisfies",
      "assurance": "partial",
      "assurance_rationale": "Monitors device configuration compliance status against Catalyst Center policies, providing evidence of configuration baseline enforcement.",
      "controlObjective": "Surfaces the exact rule failure, message, and suggested fix per device to support prompt remediation consistent with configuration settings (CM-6).",
      "evidenceArtifact": "Report `UC-5.13.33` (tabular violation list) and optional CSV for ticket bulk upload; keep with other CM-6 evidence for assessor review."
    },
    {
      "regulation": "PCI-DSS",
      "version": "v4.0",
      "clause": "1.1.1",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Demonstrates network device configuration compliance monitoring as part of firewall and router configuration standards.",
      "controlObjective": "Gives assessors the concrete non-conformities and remediation text for in-scope device configuration, supporting 1.1.1 in operation.",
      "evidenceArtifact": "Dashboard table or saved search with violationType and remediationAction fields; include sample tickets closed against each class of finding."
    }
  ],
  "dataSources": "`index=catalyst sourcetype=cisco:dnac:compliance`. from the `compliance` modular input in the Cisco Catalyst Add-on (Splunkbase 7538) polling `GET /dna/intent/api/v1/compliance`. Default poll interval: 3600s (1h). Volume: ~1 event per device per poll. RBAC: NETWORK-ADMIN-ROLE. Key fields: `complianceStatus` (string), `complianceType` (string), `deviceName` (string), `deviceUuid` (string), `managementIpAddress` (string), `lastUpdateTime` (epoch milliseconds). CIM data model: Change.",
  "app": "`Cisco Catalyst Add-on for Splunk` (Splunkbase 7538)",
  "spl": "index=catalyst sourcetype=\"cisco:dnac:compliance\" complianceStatus=\"NON_COMPLIANT\" | spath output=violations path=violations{} | mvexpand violations | spath input=violations | table deviceName complianceType violationType violationMessage remediationAction | sort deviceName complianceType",
  "description": "Provides detailed violation-level information for non-compliant devices, including the specific rule violated, the violation message, and suggested remediation actions.",
  "value": "Operations teams need actionable detail to remediate violations. This drill-down provides the specific violation and recommended fix for each non-compliant device.",
  "implementation": "Enable the `compliance` input. Confirm the TA normalises `violations` (JSON or multivalue) as expected. Adjust `spath` paths if the payload structure differs in your version.",
  "visualization": "Table (deviceName, complianceType, violationType, violationMessage, remediationAction) with drilldowns.",
  "references": [
    {
      "title": "Splunkbase app 7538",
      "url": "https://splunkbase.splunk.com/app/7538"
    },
    {
      "title": "Catalyst Center API docs",
      "url": "https://developer.cisco.com/docs/catalyst-center/"
    },
    {
      "title": "Catalyst Center Integration Guide",
      "url": "docs/guides/catalyst-center.md"
    },
    {
      "title": "Catalyst Center Compliance API — Cisco DevNet",
      "url": "https://developer.cisco.com/docs/catalyst-center/#!get-compliance-status"
    }
  ],
  "knownFalsePositives": "**Compliance violation detail showing expected configuration differences for lab devices.** Lab devices may have intentional configuration deviations that generate violation details. Distinguish by checking `deviceName` against lab naming conventions. Suppress by filtering lab devices from the violation detail drill-down.\n\n**Large number of violation entries for a single device due to template strictness.** A very detailed golden template with many checked elements may generate dozens of violation entries for a single device, making the drill-down appear overwhelming. Distinguish by grouping violations by `deviceName` and counting unique violation types. Suppress by showing a summary count per device with drill-down to individual violations, rather than listing every violation flat.\n\n**Violation details changing between polls due to partial compliance remediation.** A device may have some violations fixed between polls, causing the violation list to change. Distinguish by comparing violation details across consecutive polls for the same device. No suppression needed — the changing list reflects actual remediation progress.\n\n**Nested JSON violation structure requiring careful spath expansion.** The compliance violation detail may use nested JSON arrays that require `| spath output=violations path=violations{} | mvexpand violations` to flatten. Incomplete spath expansion may show incomplete or missing violation details. Distinguish by running `| spath` on the raw event to verify the full structure. This is a query issue — fix the SPL spath path.",
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_catalyst_center"
  ],
  "detailedImplementation": "Prerequisites\n• Cisco Catalyst Add-on (7538) with **compliance** → `cisco:dnac:compliance` in `index=catalyst`.\n• One **raw** `NON_COMPLIANT` event in hand: confirm the JSON path to **`violations`** (array of objects, depth, and field names for `violationType` / `remediationAction`).\n• **Performance:** `mvexpand` can be heavy at scale—**limit** the time range or pre-filter to a **site** or **complianceType** in busy estates.\n• `docs/implementation-guide.md` for app layout and any performance notes on the compliance input.\n\nStep 1 — Configure data collection\n• If the built-in `spath` in this sample does not match your payload, replace `path=violations{}` with the correct `spath` or **KV** extraction; keep **field** names **consistent** in `props` so dashboards do not break on upgrade.\n\nStep 2 — Create the search and table\n```spl\nindex=catalyst sourcetype=\"cisco:dnac:compliance\" complianceStatus=\"NON_COMPLIANT\" | spath output=violations path=violations{} | mvexpand violations | spath input=violations | table deviceName complianceType violationType violationMessage remediationAction | sort deviceName complianceType\n```\n\nUnderstanding this SPL (operator drilldown)\n**Violation detail** — This is a **row-per-finding** view for **remediation** tickets, not a **fleet health** score (see `UC-5.13.31` for roll-ups).\n• If `violationType` is empty, your inner `spath` may need a different sub-path—use **Job Inspector** in Search on one **mvexpand**d row to debug.\n\n**Pipeline walkthrough**\n• `NON_COMPLIANT` only → `spath` **violations** array → `mvexpand` to individual findings → `spath` inner fields → **table** for export to **Excel** or **ITSM**.\n\nStep 3 — Validate\n• Pick a device in **Catalyst** **Compliance** detail and line up **message** and **remediation** text to one Splunk **row**.\n• `| where isnotnull(violationMessage) | head 1` in **Verbose** to confirm multiline text is not truncated by **LINE_BREAKER** settings.\n• **Vendor UI parity:** open **Catalyst Center > Inventory > Compliance** for the same time window; verify non-compliant device counts match.\nStep 4 — Operationalize\n• **Dashboard:** two-step drill—start from **bar** by `complianceType` (`UC-5.13.31`), token drill to this **detail** search filtered on `complianceType` and **site**.\n• **Export:** `outputcsv` for bulk ticket creation; **redact** hostnames for **untrusted** tickets.\nStep 5 — Troubleshooting\n• **Empty table after mvexpand:** the **violations** path is wrong, or the TA flattened violations into **separate** events already—simplify the SPL to `| table violations` first.\n• **Exploding row counts:** one API row per rule **per** rule check—**dedup** on `deviceName+violationType+_time` if the TA re-sends the same string every **poll**.\n• **No data:** follow **compliance** input and **Catalyst** **role** steps from `UC-5.13.31` troubleshooting.\n• **Catalyst** shows a finding Splunk does not: **time range** and **virtual domain** do not include that site, or the device **left** inventory mid-window.\n• If data is not arriving for `cisco:dnac:compliance`, check that the `compliance` input is enabled in the TA configuration and that the Catalyst Center API credentials have not expired.\n",
  "grandmaExplanation": "We give your team the exact details of what is wrong with each non-compliant device and what to do to fix it.",
  "prerequisiteUseCases": [
    "UC-5.13.29"
  ],
  "wave": "walk"
}
