{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.14.15",
  "title": "Varnish ESI Fragment Error Rate",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Varnish ESI Fragment Error Rate' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_varnish_esi_fragment_error_rate` (UC-5.14.15), SOC dashboard panel \"Operations — Varnish ESI Fragment Error Rate\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=proxy` sourcetype='varnish:vsl' that satisfy the UC-5.14.15 SPL filters for 'Varnish ESI Fragment Error Rate'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `proxy` / `varnish:vsl` events outside the detection threshold, or exclude the test entity via `operational_exceptions.csv` with a valid window. Confirm zero alert rows for UC-5.14.15."
  },
  "dataSources": "`index=proxy` `sourcetype=varnish:vsl` ESI parser errors Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes — Universal Forwarder (Splunkbase 617)",
  "spl": "index=proxy sourcetype=\"varnish:vsl\"\n| regex _raw=\"(?i)(ESI.*error|include.*failed)\"\n| stats count by host\n| where count > 10",
  "description": "ESI failures create partial pages that are hard to spot in averages. This use case monitors the condition described in the title using the SPL and data sources documented on this page.",
  "value": "Operations teams detect Varnish ESI fragment fetch failures and XML parsing errors that cause broken page assembly for end users.",
  "implementation": "Limit verbose ESI debug to non-prod; aggregate counts in prod.",
  "visualization": "Time series for rates and latency, top/dedup for hotspots, single-value alerts on health thresholds.",
  "cimModels": [],
  "references": [
    {
      "title": "Vendor documentation",
      "url": "https://varnish-cache.org/docs/trunk/users-guide/esi.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'Varnish ESI Fragment Error Rate'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `proxy` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `operational_exceptions.csv` with entity, reason, valid_until.",
  "equipment": [
    "syslog",
    "varnish"
  ],
  "equipmentModels": [
    "syslog_generic",
    "varnish_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "Prerequisites\n* Varnish logs with ESI (Edge Side Includes) processing events. Key counters: `MAIN.esi_errors`, `MAIN.esi_warnings`. ESI log tags: `ESI_xmlerror`, `Error`. Data in `index=proxy` with `sourcetype=varnish:log` or `sourcetype=varnish:stats`.\n* ESI: Varnish parses `<esi:include src=\"/fragment\"/>` tags in responses and fetches each fragment separately, allowing independent caching of page components. ESI errors occur when: (1) fragment fetch fails, (2) XML parsing error in ESI tags, (3) infinite include loop, (4) fragment returns non-200 status.\n\nStep 1 -- Configure data collection\nEnable ESI in VCL:\n```\nsub vcl_backend_response {\n    if (beresp.http.Content-Type ~ \"text/html\") {\n        set beresp.do_esi = true;\n    }\n}\n```\nVerify:\n```spl\nindex=proxy (sourcetype=\"varnish:log\" OR sourcetype=\"varnish:stats\") earliest=-4h\n| where match(_raw, \"(?i)esi_error|ESI_xmlerror|esi.*fail|esi.*warning\")\n| stats count\n```\n\nStep 2 -- Create the search and alert\n\n**Primary search -- ESI error analysis:**\n```spl\nindex=proxy (sourcetype=\"varnish:log\" OR sourcetype=\"varnish:stats\") earliest=-4h\n| eval is_stats=if(sourcetype=\"varnish:stats\", 1, 0)\n| eval esi_event=case(match(_raw, \"(?i)ESI_xmlerror\"), \"XML_PARSE_ERROR\", match(_raw, \"(?i)esi.*failed.*fetch|esi.*no backend\"), \"FRAGMENT_FETCH_FAIL\", match(_raw, \"(?i)esi.*include.*loop\"), \"INCLUDE_LOOP\", match(_raw, \"(?i)esi_error\"), \"ESI_ERROR\", match(_raw, \"(?i)esi_warning\"), \"ESI_WARNING\", 1==1, null())\n| where isnotnull(esi_event)\n| rex \"src=\"(?<fragment_url>[^\"]+)\"\"\n| stats count as errors dc(fragment_url) as unique_fragments values(fragment_url) as fragments by esi_event\n| eval severity=case(esi_event=\"INCLUDE_LOOP\", \"CRITICAL -- recursive ESI loop\", errors > 100, \"HIGH -- frequent ESI errors\", 1==1, \"WARNING\")\n| sort severity, -errors\n```\n\nStep 3 -- Validate\n(a) Create a test page with `<esi:include src=\"/test-fragment\"/>` and a failing fragment endpoint.\n(b) `varnishstat -1 | grep esi` -- shows ESI error/warning counters.\n(c) `varnishlog -q \"ESI_xmlerror\"` -- live ESI errors.\n\nStep 4 -- Operationalize\nDashboard (\"Varnish -- ESI Health\"):\n* Row 1 -- Single-value: \"ESI errors (4h)\", \"Failed fragments\", \"XML parse errors\".\n* Row 2 -- ESI error breakdown by type.\n\nAlerting:\n* Critical (include loop detected): can cause resource exhaustion.\n* High (ESI errors > 100/hr): page rendering affected.\n\nStep 5 -- Troubleshooting\n\n* **XML parse error** -- The parent page has malformed ESI tags. Check: properly closed tags, no nested `<esi:include>` in attributes, valid XML characters.\n\n* **Fragment fetch failure** -- The fragment URL is unreachable. Check: (1) fragment backend is healthy, (2) URL path is correct relative to Varnish, (3) fragment backend has capacity.\n\n* **Performance impact** -- Each ESI include is a separate internal request. Many fragments per page multiply latency. Consider reducing fragments or increasing TTLs on frequently used fragments.",
  "grandmaExplanation": "We watch varnish esi fragment error rate and catch issues early, before they turn into outages for the people who rely on the network.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
