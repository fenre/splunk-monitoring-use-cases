{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.14.44",
  "title": "Traefik Service Health Check Failures",
  "criticality": "critical",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Traefik Service Health Check Failures' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_traefik_service_health_check_failures` (UC-5.14.44), SOC dashboard panel \"Operations — Traefik Service Health Check Failures\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.14.44 positive (Traefik, Service, Health, Check, Failures): ingest synthetic events into `index=proxy` sourcetype='traefik:log' where `count` grouped by `service_name` satisfies detection threshold for 'Traefik Service Health Check Failures'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.14.44 negative (Check Failures): ingest benign `proxy` / `traefik:log` events where `service_name` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 10 confirms zero alert rows for 'Traefik Service Health Check Failures'."
  },
  "dataSources": "`index=proxy` `sourcetype=traefik:log` JSON application logs Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes — Universal Forwarder (Splunkbase 617)",
  "spl": "index=proxy sourcetype=\"traefik:log\"\n| regex _raw=\"(?i)(Health check failed|Status.*DOWN|Server.*Unhealthy)\"\n| stats count by service_name\n| sort - count",
  "description": "Detects UC-5.14.44 — Traefik, Service, Health, Check, Failures — using `index=proxy` sourcetype `traefik:log`, grouped by `service_name`. Outputs `count` each `the scheduled window`; alert when configured threshold. Primary focus: Check Failures. Purpose-built for 'Traefik Service Health Check Failures' in Operations workflows.",
  "value": "For 'Traefik Service Health Check Failures', UC-5.14.44 converts `activity` on `traefik:log` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Ship container stdout via sidecar; set log level INFO in prod.",
  "visualization": "Time series for rates and latency, top/dedup for hotspots, single-value alerts on health thresholds.",
  "cimModels": [],
  "references": [
    {
      "title": "Vendor documentation",
      "url": "https://doc.traefik.io/traefik/routing/services/#health-check",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `proxy` (UC-5.14.44)** — Planned work for 'Traefik Service Health Check Failures' shifts `count` over `span=1h` while `service_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `traefik:log`** — Scheduled automation elevates `src` without crossing configured threshold for UC-5.14.44. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `service_name`** — Health checks in `proxy` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `traefik:log`** — First 48h baseline for UC-5.14.44 on `service_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `service_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "syslog",
    "traefik"
  ],
  "equipmentModels": [
    "syslog_generic",
    "traefik_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "Prerequisites\n* Traefik access logs and metrics. Key metrics: `traefik_service_server_up` (gauge, 0/1 per server), `traefik_service_requests_total` (with code label showing 5xx). Access log fields: `ServiceName`, `OriginStatus`, `DownstreamStatus`, `Duration`.\n* Traefik health checks: configured per service with `healthCheck.path`, `healthCheck.interval`, `healthCheck.timeout`. When a server fails health checks, Traefik stops routing traffic to it. Unlike HAProxy, Traefik integrates with service discovery (Docker, Kubernetes, Consul) so servers can also disappear from the pool when containers stop.\n\nStep 1 -- Configure data collection\nDynamic config (file provider or Docker labels):\n```yaml\n# dynamic config\nhttp:\n  services:\n    my-service:\n      loadBalancer:\n        healthCheck:\n          path: /health\n          interval: 10s\n          timeout: 5s\n        servers:\n        - url: http://10.0.0.1:8080\n        - url: http://10.0.0.2:8080\n```\nVerify:\n```spl\nindex=proxy sourcetype=\"traefik:metrics\" earliest=-4h\n| where match(metric_name, \"service_server_up\")\n| stats latest(metric_value) as status by service, url\n| where status=0\n```\n\nStep 2 -- Create the search and alert\n\n**Primary search -- Service health check failure detection:**\n```spl\nindex=proxy sourcetype=\"traefik:metrics\" earliest=-4h\n| where match(metric_name, \"service_server_up\")\n| eval is_down=if(metric_value=0, 1, 0)\n| stats latest(metric_value) as status latest(_time) as last_check sum(is_down) as down_checks count as total_checks by service, url\n| eval health=if(status=0, \"DOWN\", \"UP\")\n| eval severity=case(health=\"DOWN\", \"CRITICAL -- server is DOWN\", down_checks > 5 AND health=\"UP\", \"WARNING -- recent flapping (\".down_checks.\" failures)\", 1==1, \"OK\")\n| where severity != \"OK\"\n| table service, url, health, down_checks, total_checks, last_check, severity\n```\n\n**From access logs (backend errors):**\n```spl\nindex=proxy sourcetype=\"traefik:access\" earliest=-4h\n| where OriginStatus >= 500\n| stats count as errors by ServiceName, ServiceAddr\n| sort -errors\n```\n\nStep 3 -- Validate\n(a) Traefik dashboard: `http://<traefik>:8080/dashboard/` -- shows service health status.\n(b) Traefik API: `curl http://localhost:8080/api/http/services` -- lists services with server statuses.\n(c) Stop a backend server and verify it is marked DOWN.\n\nStep 4 -- Operationalize\nDashboard (\"Traefik -- Service Health\"):\n* Row 1 -- Single-value: \"DOWN servers\", \"Total servers\", \"Services with failures\".\n* Row 2 -- Server health status table.\n* Row 3 -- Backend error rate timechart.\n\nAlerting:\n* Critical (server DOWN): traffic no longer being routed to this server.\n* Warning (frequent flapping): server intermittently failing health checks.\n\nStep 5 -- Troubleshooting\n\n* **Server DOWN but application works** -- Health check path may differ from application path. Verify: `healthCheck.path` is correct and returns 2xx.\n\n* **All servers DOWN for a service** -- Traefik will return 503 for all requests to this service. Check: (1) backend network connectivity, (2) common dependency failure, (3) DNS resolution.\n\n* **Health checks passing but backend errors** -- Health check is too basic. Use an endpoint that tests full application stack (DB, cache, dependencies).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=traefik:log into proxy. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`service_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch traefik service health check failures and catch issues early, before they turn into outages for the people who rely on the network.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
