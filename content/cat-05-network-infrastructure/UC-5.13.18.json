{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.13.18",
  "title": "Network Health Degradation Alerting",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Fault"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=catalyst sourcetype=cisco:dnac:networkhealth`. from the `networkhealth` modular input in the Cisco Catalyst Add-on (Splunkbase 7538) polling `GET /dna/intent/api/v1/network-health`. Default poll interval: 900s (15m). Volume: ~1 event per poll (aggregate network score). RBAC: NETWORK-ADMIN-ROLE. Key fields: `healthScore` (integer 0-100), `goodCount` (integer), `badCount` (integer), `totalCount` (integer).",
  "app": "`Cisco Catalyst Add-on for Splunk` (Splunkbase 7538)",
  "spl": "index=catalyst sourcetype=\"cisco:dnac:networkhealth\" | stats latest(healthScore) as health_score latest(badCount) as unhealthy_devices latest(totalCount) as total_devices by _time | where health_score < 70 OR unhealthy_devices > 5 | eval unhealthy_pct=round(unhealthy_devices*100/total_devices,1) | table _time health_score unhealthy_devices total_devices unhealthy_pct",
  "description": "Triggers alerts when the network health score drops below acceptable thresholds or when the count of unhealthy devices exceeds limits.",
  "value": "A network health score below 70 typically indicates multiple concurrent issues. Alerting on this composite metric catches systemic problems that individual device alerts might miss.",
  "implementation": "Requires UC-5.13.16 running so composite scores and counts are flowing. Save as a 15–30 minute alert with a non-zero trigger. Tune 70/5/percent math for your org; some campuses are noisier and need higher bad-count headroom, while others are stricter on percentage.",
  "visualization": "Table of alert rows with unhealthy_pct, single value of worst recent health_score, link to a dynamic drilldown to UC-5.13.1/3.",
  "references": [
    {
      "title": "Splunkbase app 7538",
      "url": "https://splunkbase.splunk.com/app/7538"
    },
    {
      "title": "Catalyst Center API docs",
      "url": "https://developer.cisco.com/docs/catalyst-center/"
    },
    {
      "title": "Catalyst Center Integration Guide",
      "url": "docs/guides/catalyst-center.md"
    },
    {
      "title": "Catalyst Center Network Health API — Cisco DevNet",
      "url": "https://developer.cisco.com/docs/catalyst-center/#!get-overall-network-health"
    }
  ],
  "knownFalsePositives": "**Planned maintenance window causing temporary health degradation below alert threshold.** When multiple devices are upgraded simultaneously, the aggregate health score may drop below the alerting threshold for the duration of the maintenance window. Distinguish by correlating with ITSM change records or `index=catalyst sourcetype=\"cisco:dnac:audit:logs\"` for scheduled maintenance. Suppress by using a `catalyst_maintenance_windows` lookup and suppressing alerts where `_time` falls within a scheduled window.\n\n**Single critical device failure triggering the alert while the rest of the network is healthy.** A core router or distribution switch failure can drop the health score below the threshold even though 95%+ of devices are healthy. Distinguish by checking `unhealthy_devices` vs `total_devices` — if `unhealthy_devices<=2` but health drops sharply, it is a single-device issue. Do not suppress — this is a real alert, but include the specific device context in the notification.\n\n**Assurance recomputation causing brief health score drop that triggers the alert.** During recomputation, `healthScore` may briefly hit 0 or a very low value. If the alert uses `latest()` without multi-poll persistence, a single recomputation event can fire the alert. Distinguish by checking whether the alert fires at the same time each hour. Suppress by requiring the health score to be below threshold on 2+ consecutive polls: use a summary index or `| eventstats` to compare adjacent data points.\n\n**New site or device batch added to Catalyst Center lowering the aggregate health.** When a new site with devices in varied health states is onboarded, the aggregate health may drop temporarily. Distinguish by checking `total_devices` — if it increased significantly in the current period, the health drop may reflect the new devices' condition. Suppress by requiring `total_devices` to be stable (within ±5%) over the alerting window.",
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_catalyst_center"
  ],
  "detailedImplementation": "Prerequisites\n• Cisco Catalyst Add-on for Splunk (Splunkbase 7538) with the **networkhealth** modular input writing sourcetype `cisco:dnac:networkhealth` to `index=catalyst` (this is the **Assurance summary** feed, not per-device `cisco:dnac:devicehealth`).\n• UC-5.13.16 should already prove summary events with `healthScore`, `badCount`, and `totalCount` in Search before you page anyone.\n• Agree on **thresholds** (example **<70** and **>5** unhealthy) with NOC; document exceptions for small fleets where one bad device is a high percentage but not an enterprise incident.\n• `docs/implementation-guide.md` for credential storage and the forwarder/SH that runs the **networkhealth** input.\n\nStep 1 — Configure data collection\n• **Intent API:** `GET /dna/intent/api/v1/network-health` (Assurance top-level network summary).\n• **TA input name:** **networkhealth**; sourcetype `cisco:dnac:networkhealth`, index `catalyst`.\n• **Default interval:** **900 seconds**; align alert schedule with **poll cadence** so you do not alert on a partially ingested window.\n• **Key fields:** `healthScore` (0–100), `badCount`, `totalCount`—confirm names match your TA version on a single raw event.\n\nStep 2 — Create the search and alert\n```spl\nindex=catalyst sourcetype=\"cisco:dnac:networkhealth\" | stats latest(healthScore) as health_score latest(badCount) as unhealthy_devices latest(totalCount) as total_devices by _time | where health_score < 70 OR unhealthy_devices > 5 | eval unhealthy_pct=round(unhealthy_devices*100/total_devices,1) | table _time health_score unhealthy_devices total_devices unhealthy_pct\n```\n\nUnderstanding this SPL (dual gate, time bucketing)\n• **`where health_score < 70 OR unhealthy_devices > 5`** catches a **softer score** and a **count-based** “many devices are bad” pattern; tune **70** and **5** to your headcount and risk appetite.\n• **`stats` by `_time`:** if multiple events share a timestamp, consider **`| dedup _time |`** first or collapse to the **last poll** in the window; otherwise you may get duplicate table rows in tests.\n• **`unhealthy_pct`** helps ticket text; guard **`total_devices=0`** in a v2 to avoid division noise during Assurance initialization.\n\n**Pipeline walkthrough**\n• `latest()` picks the most recent Assured summary fields per time key.\n• The filter and percent roll up a single operational story for a war-room view alongside **UC-5.13.1** and **UC-5.13.3** for device detail.\n\nStep 3 — Validate\n• Compare firing rows to **Catalyst Center > Assurance > Network health** for the same **controller scope** and **15–30 minute** window.\n• During a **staged** incident (half the campuses upgraded), expect **unhealthy_devices** to rise—confirm the alert still matches leadership’s definition of “degraded.”\n• Run **`| timechart count`** on `cisco:dnac:networkhealth` to catch silent ingest gaps before relying on the alert in production.\n\nStep 4 — Operationalize (alerting)\n• **Schedule:** every **15–30 minutes** with a **Last 30–45 minutes** window to overlap at least one **900s** poll and reduce edge misses.\n• **Trigger:** number of results **> 0**; optional **throttle** on composite score bands so one long bridge outage does not re-page every 15 minutes—track one parent incident in ITSM instead.\n• **Actions:** include **`unhealthy_pct`**, link to the **Catalyst** Assurance summary and drill to **device health** (UC-5.13.1) for device-level work queues.\n\nStep 5 — Troubleshooting\n• **No `cisco:dnac:networkhealth` data:** enable the **networkhealth** input, re-check **base URL** and **API role**, and confirm index routing.\n• **All-clear in Splunk but UI is red:** wrong **Catalyst** instance in the add-on, **virtual domain** scope mismatch, or **stale** credentials—re-auth and compare raw JSON to the UI.\n• **Alert never clears:** `totalCount` or **bad** counts may be **stale**; restart the modular input after TA upgrades and re-validate `fieldsummary` for numeric fields.\n• If data is not arriving for `cisco:dnac:networkhealth`, check that the `networkhealth` input is enabled in the TA configuration and that the Catalyst Center API credentials have not expired.\n",
  "grandmaExplanation": "We warn you when the big picture health score for the whole network falls too low or too many devices look unhealthy at once, so you treat it like a real incident, not a single blip on one box.",
  "prerequisiteUseCases": [
    "UC-5.13.16"
  ],
  "wave": "walk"
}
