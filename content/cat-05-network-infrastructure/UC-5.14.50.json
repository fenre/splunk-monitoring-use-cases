{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.14.50",
  "title": "Traefik Dynamic Configuration Reload Events",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Change",
    "Operational"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=proxy` `sourcetype=traefik:log` provider notifications",
  "app": "Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes",
  "spl": "index=proxy sourcetype=\"traefik:log\"\n| regex _raw=\"(?i)(Configuration loaded|Provider event|Reloaded)\"\n| stats count by _raw\n| sort - count\n| head 30",
  "description": "Reload storms during incidents explain sudden behavior changes.",
  "value": "Supports blameless postmortems with timestamps.",
  "implementation": "Correlate with GitOps commits; alert only on error-level reload failures in prod.",
  "visualization": "Time series for rates and latency, top/dedup for hotspots, single-value alerts on health thresholds.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Vendor documentation",
      "url": "https://doc.traefik.io/traefik/providers/overview/"
    }
  ],
  "equipment": [
    "syslog",
    "traefik"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "grandmaExplanation": "We watch traefik dynamic configuration reload events and catch issues early, before they turn into outages for the people who rely on the network.",
  "knownFalsePositives": "Edge cases: multi-vendor mixes and ad hoc feeds can include lab traffic and partial visibility that mimics issues until scopes are defined. Tuning tip: match this to «Traefik Dynamic Configuration Reload Events» and exclude change windows, scans, and lab VLANs you already expect."
}
