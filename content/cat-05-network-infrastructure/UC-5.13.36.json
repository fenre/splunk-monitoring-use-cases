{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.13.36",
  "title": "Advisory Trending and New Advisory Detection",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Vulnerability"
  ],
  "splunkPillar": "Security",
  "owner": "Head of IT Operations",
  "controlFamily": "regulation-specific",
  "compliance": [
    {
      "regulation": "NIST-800-53",
      "version": "Rev. 5",
      "clause": "SI-2",
      "mode": "satisfies",
      "assurance": "partial",
      "assurance_rationale": "Tracks Cisco PSIRT security advisories affecting managed devices, providing evidence of vulnerability awareness and remediation tracking as required by NIST 800-53 Flaw Remediation.",
      "controlObjective": "Demonstrates continuous monitoring of vendor-published security advisories for all managed network infrastructure.",
      "evidenceArtifact": "Saved search over index=catalyst sourcetype=cisco:dnac:securityadvisory scheduled daily; advisory counts and affected device lists exported to compliance dashboard."
    },
    {
      "regulation": "NIST-800-53",
      "version": "Rev. 5",
      "clause": "RA-5",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Contributes to vulnerability scanning by tracking known vendor-published vulnerabilities, though it does not replace active scanning tools.",
      "controlObjective": "Provides a vendor-authoritative vulnerability feed for network infrastructure to complement active vulnerability scanning.",
      "evidenceArtifact": "Dashboard panel showing advisory severity distribution and affected device counts, refreshed with each TA poll cycle."
    }
  ],
  "dataSources": "`index=catalyst sourcetype=cisco:dnac:securityadvisory`. from the `securityadvisory` modular input in the Cisco Catalyst Add-on (Splunkbase 7538) polling `GET /dna/intent/api/v1/security-advisory/advisory`. Default poll interval: 3600s (1h). Volume: ~1 event per advisory-device pair. RBAC: NETWORK-ADMIN-ROLE. Key fields: `advisoryId` (string), `advisoryTitle` (string), `severity` (string), `cveId` (string), `deviceCount` (integer), `deviceId` (string), `fixedVersions` (array of strings), `publicationDate` (string). CIM data model: Vulnerabilities.",
  "app": "`Cisco Catalyst Add-on for Splunk` (Splunkbase 7538)",
  "spl": "index=catalyst sourcetype=\"cisco:dnac:securityadvisory\" | stats earliest(_time) as first_seen latest(_time) as last_seen dc(deviceId) as affected_devices by advisoryId, advisoryTitle, severity | eval first_seen_date=strftime(first_seen, \"%Y-%m-%d\") | where first_seen > relative_time(now(), \"-7d\") | sort severity -affected_devices",
  "description": "Identifies newly detected security advisories in the last 7 days, enabling rapid assessment of new vulnerabilities affecting the managed network.",
  "value": "New advisories require immediate triage. Detecting them as soon as Catalyst Center reports them ensures the vulnerability management process starts promptly.",
  "implementation": "Enable the `securityadvisory` input. The `where first_seen` window compares the earliest event per advisory to `now()`. Adjust the `-7d` lookback to match your patch SLAs.",
  "visualization": "Table of new advisories (first_seen_date, affected_devices), timechart of first_seen counts.",
  "references": [
    {
      "title": "Splunkbase app 7538",
      "url": "https://splunkbase.splunk.com/app/7538"
    },
    {
      "title": "Catalyst Center API docs",
      "url": "https://developer.cisco.com/docs/catalyst-center/"
    },
    {
      "title": "Catalyst Center Integration Guide",
      "url": "docs/guides/catalyst-center.md"
    },
    {
      "title": "Cisco PSIRT Security Advisories",
      "url": "https://sec.cloudapps.cisco.com/security/center/publicationListing.x"
    }
  ],
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_catalyst_center"
  ],
  "knownFalsePositives": "**PSIRT database update generating apparent new advisory detections.** When Catalyst Center updates its PSIRT database, previously unknown advisories may appear for the first time even though the vulnerabilities are not new. Distinguish by checking the advisory's `publicationDate` — if it was published months ago but just detected, the PSIRT database was recently updated. Suppress by alerting on advisories where `publicationDate` is within the last 30 days as genuinely new, and treating older publications as database-update catches.\n\n**Advisory reclassification changing severity between polls.** Cisco may update the severity of a published PSIRT (e.g., upgrading from HIGH to CRITICAL after exploitation is confirmed). Catalyst Center reflects this change. Distinguish by checking whether the `severity` changed for the same `advisoryId` between consecutive polls. Do not suppress — severity upgrades are operationally important.\n\n**Same advisory appearing for different device UUIDs as more devices are scanned.** The initial scan may not cover all devices; subsequent polls include additional affected devices for the same advisory. Distinguish by tracking `dc(deviceId)` per `advisoryId` over time — an increasing count is normal as the scan completes. Suppress by waiting for the full scan to complete (typically 24 hours) before considering the advisory trending stable.\n\n**Advisory with zero affected devices remaining in the feed.** After all affected devices are patched, the advisory may remain in the feed with `deviceCount=0` until Catalyst Center removes it. Distinguish by checking `deviceCount=0`. Suppress by filtering `| where deviceCount>0` for active advisory dashboards.",
  "detailedImplementation": "Prerequisites\n• Cisco Catalyst Add-on for Splunk (Splunkbase 7538) with the **securityadvisory** modular input enabled to `index=catalyst`, `sourcetype=cisco:dnac:securityadvisory`.\n• Catalyst Center **2.3.5+** with PSIRT integration enabled (Catalyst Center automatically checks for advisories against the managed inventory).\n• RBAC: service account with **NETWORK-ADMIN-ROLE** for security advisory data access.\n• At least 30 days of historical advisory data for trend analysis.\n\nStep 1 — Configure data collection\nThe TA polls `GET /dna/intent/api/v1/security-advisory/advisory` at the configured interval (default 3600 seconds). Each event represents one security advisory detected by Catalyst Center for the managed device inventory.\n\nKey fields:\n• `advisoryId`: Cisco advisory identifier (e.g., \"cisco-sa-20230301-iosxe\").\n• `severity`: advisory severity — CRITICAL, HIGH, MEDIUM, LOW, INFORMATIONAL.\n• `deviceCount`: number of managed devices affected by this advisory.\n• `publicationDate`: date the advisory was published by Cisco.\n• `cveId`: associated CVE identifier(s).\n\nStep 2 — Create the search and alert\n\n```spl\nindex=catalyst sourcetype=\"cisco:dnac:securityadvisory\"\n| stats earliest(_time) as first_detected latest(_time) as last_seen latest(deviceCount) as current_affected latest(severity) as severity by advisoryId, cveId\n| eval days_since_first=round((now()-first_detected)/86400,1)\n| eval is_new=if(days_since_first <= 7, \"New\", \"Known\")\n| eval trend=case(is_new=\"New\" AND severity IN (\"CRITICAL\",\"HIGH\"), \"New Critical/High\", is_new=\"New\", \"New Advisory\", 1==1, \"Known Advisory\")\n| sort trend, -days_since_first\n```\n\nUnderstanding this SPL:\n• **`earliest(_time) as first_detected`**: When Splunk first saw this advisory — indicates when the advisory was detected for this environment.\n• **`is_new=if(days_since_first <= 7, ...)`**: Flags advisories first detected within the last 7 days as \"New.\"\n• **`trend`**: Prioritizes new critical/high advisories for immediate attention while tracking known advisories for ongoing remediation.\n\nStep 3 — Validate\n• **UI comparison:** Open Catalyst Center > Assurance > Security Advisories. Compare the total advisory count, severity distribution, and new detections with the Splunk results.\n• **Publication date check:** For new detections, verify `publicationDate` against the Cisco PSIRT portal to confirm whether the advisory is genuinely new or was recently added to the Catalyst Center database.\n\nStep 4 — Operationalize\n• **Dashboard:** New advisory timeline (last 30 days), severity distribution pie chart, and a table of new critical/high advisories with affected device counts.\n• **Alert:** Trigger immediately when a new CRITICAL advisory is detected (`is_new=\"New\" AND severity=\"CRITICAL\"`). Route to the security team.\n• **SLA tracking:** Measure time from advisory detection to remediation completion using UC-5.13.38.\n\nStep 5 — Troubleshoot\n• **Database update surfacing old advisories as \"new\":** After a Catalyst Center PSIRT database update, previously unknown advisories appear. Check `publicationDate` — if it's months old, the advisory was just added to the local database.\n• **Advisory count discrepancy with Cisco PSIRT portal:** Catalyst Center only shows advisories affecting managed devices. The PSIRT portal shows all advisories regardless of inventory.\n• **INFORMATIONAL advisories creating noise:** Filter `| where severity!=\"INFORMATIONAL\"` for operational dashboards.\n• **No securityadvisory events arriving:** verify the `securityadvisory` input is enabled and check `index=_internal` for API error or endpoint timeout messages from the TA.\n• **Missing fields or zero values in severity:** some advisory fields are only populated for certain PSIRT types; NULL values for `fixedVersions` indicate no patch is available.\n• **Fewer records than expected:** the PSIRT scan may still be running for large inventories; if credential access is denied, check the service account permissions.",
  "grandmaExplanation": "We catch new security warnings as soon as they appear and show you which ones are new this week so nothing gets missed.",
  "prerequisiteUseCases": [
    "UC-5.13.34"
  ],
  "wave": "walk"
}
