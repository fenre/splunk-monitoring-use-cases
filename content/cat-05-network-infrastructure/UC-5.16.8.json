{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.16.8",
  "title": "High-Latency Path Detection (Pre/Post Optimization Delta)",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance",
    "Anomaly",
    "Operations"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'High-Latency Path Detection (Pre/Post Optimization Delta)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_high_latency_path_detection_pre_post_optimizatio` (UC-5.16.8), SOC dashboard panel \"Operations — High-Latency Path Detection (Pre/Post Optimization Delta)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.16.8 positive (High, Latency, Path, Detection, (Pre): ingest synthetic events into `index=wanop` sourcetype='target_sourcetype' where `avg_pre_ms`, `avg_post_ms`, `avg_delta_ms` grouped by `vendor`, `app`, `site_code` satisfies `pre_ms` >= 120, `delta_ms` < 10, `pre_ms` >= 120, or `avg_pre_ms` >= 250 for 'High-Latency Path Detection (Pre/Post Optimization Delta)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.16.8 negative (Optimization Delta)): ingest benign `wanop` / `target_sourcetype` events where `vendor`, `app`, `site_code` stays below `pre_ms` >= 120, `delta_ms` < 10, `pre_ms` >= 120, or `avg_pre_ms` >= 250, or exclude the test entity via `ops_exception_register.csv`. Variant 13 confirms zero alert rows for 'High-Latency Path Detection (Pre/Post Optimization Delta)'."
  },
  "dataSources": "Detailed: `index=wanop OR index=network`; synthetic probes or embedded latency telemetry capturing RTT before and after optimization layers (`latency_before_ms`, `latency_after_ms`), SteelHead path-selection logs (`riverbed:steelhead`), EdgeConnect tunnel metrics (`silverpeak:*`), Citrix WANOP performance traps (`citrix:sdwan*`), blended ZDX last-mile versus cloud measurements (`zscaler:zdx`) when diagnosing SaaS legs interacting with regional WAN optimizers. Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Add-on for Zscaler for ZDX feeds; appliance syslog/TAs; optional ThousandEyes or SPL ping scripted inputs feeding identical field names for apples-to-apples comparisons. — Universal Forwarder (Splunkbase 617)",
  "spl": "index=wanop OR index=network earliest=-24h latest=now\n| eval v=lower(sourcetype)\n| eval vendor=case(match(v,\"riverbed|steelhead\"),\"Riverbed SteelHead\",match(v,\"silverpeak|edgeconnect\"),\"Silver Peak EdgeConnect\",match(v,\"citrix\"),\"Citrix SD-WAN WANOP\",match(v,\"zdx|zscaler\"),\"Zscaler Digital Experience\",\"other\")\n| where vendor!=\"other\"\n| eval pre_ms=tonumber(coalesce(latency_before_ms,rtt_inner_ms,wano_inner_rtt))\n| eval post_ms=tonumber(coalesce(latency_after_ms,rtt_outer_ms,wano_outer_rtt))\n| eval delta_ms=if(isnotnull(pre_ms) AND isnotnull(post_ms), pre_ms-post_ms, null())\n| eval app=coalesce(application,app_name,target_saas,\"unspecified\")\n| where isnotnull(pre_ms) AND pre_ms>=120\n| eval stalled_opt=if(isnull(delta_ms),1,if(delta_ms<10 AND pre_ms>=120,1,0))\n| where stalled_opt=1\n| stats avg(pre_ms) as avg_pre_ms avg(post_ms) as avg_post_ms avg(delta_ms) as avg_delta_ms count as samples dc(host) as appliance_hops by vendor app site_code\n| eval severity=case(avg_pre_ms>=250,\"critical\",avg_pre_ms>=180,\"high\",\"medium\")\n| sort - avg_pre_ms vendor app\n| head 200",
  "description": "Surfaces UC-5.16.8 — High, Latency, Path, Detection, (Pre — using `index=wanop` sourcetype `configured sourcetype`, grouped by `vendor`, `app`, `site_code`. Outputs `avg_pre_ms`, `avg_post_ms`, `avg_delta_ms` each `the scheduled window`; alert when `pre_ms` >= 120, `delta_ms` < 10, `pre_ms` >= 120, or `avg_pre_ms` >= 250. Primary focus: Optimization Delta). Purpose-built for 'High-Latency Path Detection (Pre/Post Optimization Delta)' in Operations workflows.",
  "value": "UC-5.16.8 gives Operations teams earlier visibility into Optimization Delta) through UC-5.16.8 search results, lowering operational risk and delayed incident response.",
  "implementation": "Normalize probes every five minutes per site, maintain adaptive baseline via `predict`, suppress alerts during carrier-declared maintenance windows stored in lookup.",
  "visualization": "Scatter plot avg_pre_ms vs avg_delta_ms with quadrant thresholds; companion table listing worst apps per vendor.",
  "references": [
    {
      "title": "Splunk Lantern — Synthetic monitoring patterns",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Zscaler Help — ZDX Administration Guide",
      "url": "https://help.zscaler.com/zdx",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `wanop` (UC-5.16.8)** — Planned work for 'High-Latency Path Detection (Pre/Post Optimization Delta)' shifts `avg_pre_ms`, `avg_post_ms`, `avg_delta_ms` over `span=1h` while `vendor`, `app`, `site_code` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `configured_sourcetype`** — Scheduled automation elevates `app` without crossing `pre_ms` >= 120 for UC-5.16.8. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `vendor`, `app`, `site_code`** — Health checks in `wanop` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-5.16.8 on `vendor` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `vendor`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "N/A (latency observability)"
  ],
  "detectionType": "Anomaly",
  "securityDomain": "network",
  "equipment": [
    "cisco",
    "citrix",
    "riverbed",
    "silverpeak",
    "syslog",
    "zscaler"
  ],
  "equipmentModels": [
    "cisco_thousandeyes",
    "citrix_sdwan_wanop",
    "riverbed_steelhead",
    "silverpeak_edgeconnect",
    "zscaler_zdx"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Consistent probe endpoints across branches referencing identical SaaS or DC targets.\n• Documentation labeling measurement insertion points (LAN vs WAN side taps).\n• Cooperation from carriers when escalating backbone latency proofs.\n\nStep 1 — Configure data collection\nDeploy lightweight synthetic agents writing JSON latency tuples via HEC; mirror appliance-native latency feeds into same sourcetype namespace.\n\nStep 2 — Create the search and alert\nMaterialize SPL as `wanopt_latency_delta_gap`; alert when samples>=12/hour and avg_delta_ms<=5 while avg_pre_ms>=180.\n\nStep 3 — Validate\nCompare to traceroute archives and carrier PM tickets—ensure Splunk aligns with carrier MTTR narratives.\n\nStep 4 — Operationalize\nDashboard overlays map site_code coloring severity with drilldown traceroute runbook buttons.\n\nStep 5 — Troubleshooting\n**UDP vs TCP probes:** reconcile methodology.**Satellite backhaul:** elevate thresholds ethically.**DNS geo-steering:** rotating destinations confuse aggregates—anchor FQDN.**ZDX-only legs:** confirm optimizers actually sit in path via CMDB.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for Zscaler for ZDX feeds; appliance syslog/TAs; optional ThousandEyes or SPL ping scripted inputs feeding identical field names for apples-to-apples comparisons. — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into wanop, network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`app, avg_pre_ms, delta_ms, pre_ms`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We measure travel time before and after the shortcut is applied. When the trip still feels endless even after the shortcut, something on the road—or the shortcut itself—is wrong, and we flag it before folks blame the computers at their desks.",
  "prerequisiteUseCases": [],
  "wave": "walk"
}
