{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.10.9",
  "title": "ISP Peering Point Saturation Detection",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "industry": "Telecommunications",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'ISP Peering Point Saturation Detection'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-5.10.9).",
  "evidence": "Saved search uc_5_10_9_isp_peering_point_saturation_detection, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'ISP Peering Point Saturation Detection'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-5.10.9.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "`index=netflow` with normalized fields `bytes`, `host` (exporter/router), and egress interface identifier (`dst_inf_name`, `output_snmp`, or equivalent depending on TA—alias to `dst_inf_name` in props); optional `dst_as`/`src_as` enrichment. CSV lookup `peering_interfaces.csv` keyed by `peer_key` (`host|dst_inf_name`) supplying `peer_name`, `pop`, engineered `nominal_bps`, `peer_as`. UC-5.10.8 SNMP data fills gaps when templates omit interface names. Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk Stream` NetFlow/IPFIX collector, `Splunk Connect for Syslog` forwarding templates, or vendor TA (e.g., Cisco TA) normalizing flow records. — Universal Forwarder (Splunkbase 617)",
  "spl": "index=netflow earliest=-35m\n| bin _time span=5m\n| stats sum(eval(bytes*8)) as bits by _time, host, dst_inf_name\n| eval peer_key=host.\"|\".dst_inf_name\n| lookup peering_interfaces.csv peer_key OUTPUT peer_name pop nominal_bps peer_as\n| where isnotnull(peer_name)\n| eval interval_sec=300\n| eval bps=bits/interval_sec\n| eventstats avg(bps) as baseline by peer_name\n| eval util_pct=if(isnotnull(nominal_bps) AND nominal_bps>0, round(100*bps/nominal_bps,2), null())\n| eval baseline_spike=if(isnotnull(baseline) AND baseline>0 AND bps>(baseline*1.35), 1, 0)\n| where util_pct>80 OR baseline_spike=1\n| stats latest(util_pct) as util_pct latest(bps) as current_bps latest(baseline) as baseline_bps latest(peer_as) as peer_as by peer_name, pop\n| sort -util_pct",
  "description": "Aggregates five-minute flow volume per ISP hand-off or peering interface to expose sustained saturation—either versus engineered nominal capacity or versus a thirty-minute adaptive baseline—highlighting choke points before packet loss manifests on subscriber services.",
  "value": "Peering coordinators gain quantitative justification for augmenting ports, shifting traffic across diverse paths, or invoking contracted burst clauses—often avoiding reactive finger-pointing during sporting or retail traffic spikes.",
  "implementation": "Ensure NetFlow/IPFIX templates export interface IDs resolvable to names; maintain lookup tying exporter+interface to carriers; blend SNMP when flows lack counters; tune nominal_bps yearly after upgrades.",
  "visualization": "Treemap of peers sized by bits; alert table listing peer_name, util_pct, baseline delta; correlation panel with UC-5.10.8 SNMP chart for same interface.",
  "cimModels": [],
  "references": [
    {
      "title": "Splunk Docs — NetFlow data onboard",
      "url": "https://docs.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RFC 7011 — IPFIX Protocol Specification",
      "url": "https://www.rfc-editor.org/rfc/rfc7011",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "cisco",
    "juniper",
    "netflow",
    "snmp",
    "stream",
    "syslog"
  ],
  "equipmentModels": [
    "cisco_ios",
    "juniper_mx",
    "netflow_netflow",
    "snmp_generic",
    "stream_forwarder"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• NetFlow v9/IPFIX exporters on provider-edge routers with templates exposing OUTPUT_INTERFACE/SOURCE_INTERFACE IDs plus optional SNMP mapping tables.\n• Splunk Technology Add-on or Stream pipeline translating numeric interface indexes into human-readable `dst_inf_name` strings aligned with peering documentation.\n• Authoritative engineering workbook listing nominal capacity per interconnect (sometimes lower than line rate due to policers).\n• Baseline observation window (≥14 days) before trusting adaptive spike ratios.\n\nStep 1 — Normalize sourcetypes so `bytes` counts octets per flow and `_time` reflects flow end; deduplicate when multiple collectors mirror identical exports.\n\nStep 2 — Bucket traffic into five-minute spans per exporter/interface pair, multiply summed octets by eight to obtain bits, divide by three hundred seconds for average bps.\n\nStep 3 — Enrich with `peering_interfaces.csv`; compute engineered utilization when `nominal_bps` exists, otherwise rely on `baseline_spike` comparing against thirty-minute rolling averages per peer.\n\nStep 4 — Visualization stacks Top-N saturated peers, overlays SNMP discard counters (optional join), and publishes automated carrier-facing PDF summaries.\n\nStep 5 — Troubleshooting: sampled flows miss microbursts—spot-check with switchingASIC telemetry; confirm bidirectional exports since asymmetric configs blind one direction; update lookups whenever circuits relocate between routers.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk Stream` NetFlow/IPFIX collector, `Splunk Connect for Syslog` forwarding templates, or vendor TA (e.g., Cisco TA) normalizing flow records. — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into netflow. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, baseline, baseline_spike, dst_inf_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We measure how busy each front door to the internet becomes so our team adds lanes or spreads traffic before videos start buffering for everyone.",
  "prerequisiteUseCases": [],
  "telcoUseCase": "Peering capacity assurance",
  "wave": "walk"
}
