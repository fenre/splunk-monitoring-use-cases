{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.20.120",
  "title": "IPv6 Address Stability and Interface ID Tracking (RFC 7217 / RFC 8981)",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'IPv6 Address Stability and Interface ID Tracking (RFC 7217 / RFC 8981)'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-5.20.120).",
  "evidence": "Saved search uc_5_20_120_ipv6_address_stability_and_interface_id, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'IPv6 Address Stability and Interface ID Tracking (RFC 7217 / RFC 8981)'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-5.20.120.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "`index=network` or `index=os` — Tracks IPv6 interface identifier generation methods across the network: deprecated EUI-64 (embeds MAC address), RFC 7217 stable OPAQUE identifiers, and RFC 8981 temporary addresses. Understanding which method each host uses is critical for privacy compliance (GDPR — see UC-5.20.93), forensic correlation, and address tracking.\n\n**Interface ID methods:**\n1. **EUI-64 (deprecated by RFC 8064).** Embeds the host's MAC address in the IPv6 address. Creates a persistent identifier that can track devices across networks.\n2. **RFC 7217 stable OPAQUE.** Generates a stable-within-a-network address that changes when the host moves to a different network. Does not embed the MAC. Recommended for server/infrastructure use.\n3. **RFC 8981 temporary addresses.** Short-lived addresses that change periodically (default: every few hours). Used for outbound connections to prevent tracking. Challenging for forensic correlation. Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Enterprise — Universal Forwarder (Splunkbase 617)",
  "spl": "index=network earliest=-24h\n| eval is_ipv6=if(match(src, \":\"), 1, 0)\n| where is_ipv6=1\n| eval iid_type=case(\n    match(src, \"[Ff][Ff][Ff][Ee]\"), \"EUI-64 (deprecated — contains MAC address)\",\n    match(src, \"::[0-9a-fA-F]{1,4}$\") AND len(replace(src, \"[^:]\", \"\")) < 5, \"Likely static/manual\",\n    1=1, \"OPAQUE or temporary (RFC 7217/8981)\")\n| stats dc(src) as unique_addresses count as events by iid_type\n| eval pct=round(unique_addresses / sum(unique_addresses) * 100, 1)\n| table iid_type, unique_addresses, pct, events",
  "description": "Tracks IPv6 interface identifier generation methods across the network: deprecated EUI-64 (privacy risk), RFC 7217 stable OPAQUE (recommended), and RFC 8981 temporary addresses (privacy-preserving but forensically challenging). Understanding the distribution helps assess privacy compliance and forensic capability.",
  "value": "EUI-64 addresses embed the device's MAC address, creating a persistent tracking identifier that violates GDPR privacy requirements (per the Breyer ruling). RFC 7217 provides stability without exposing the MAC. RFC 8981 temporary addresses provide maximum privacy but make forensic correlation difficult. Understanding the mix helps balance privacy and security needs.",
  "implementation": "Analyse IPv6 source addresses for EUI-64 patterns (::ffXX:XXXX:XXXX with ff:fe in the middle). Track the distribution of address types over time.",
  "visualization": "(1) Pie chart: address type distribution. (2) Table: hosts using EUI-64 (remediation targets). (3) Trend: EUI-64 usage declining over time.",
  "references": [
    {
      "title": "RFC 7217 — A Method for Generating Semantically Opaque Interface Identifiers",
      "url": "https://www.rfc-editor.org/rfc/rfc7217",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RFC 8981 — Temporary Address Extensions for Stateless Address Autoconfiguration in IPv6",
      "url": "https://www.rfc-editor.org/rfc/rfc8981",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RFC 8064 — Recommendation on Stable IPv6 Interface Identifiers",
      "url": "https://www.rfc-editor.org/rfc/rfc8064",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "apple",
    "cisco",
    "linux",
    "microsoft"
  ],
  "equipmentModels": [
    "cisco_catalyst_9000",
    "linux",
    "macos",
    "windows_10_11"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• IPv6 traffic logs in Splunk.\n\nStep 1 — Configure data collection\nNo special configuration needed — this UC analyses existing traffic data.\n\nStep 2 — Create monitoring searches\n\n**EUI-64 device identification (remediation targets):**\n```spl\nindex=network earliest=-7d\n| eval is_eui64=if(match(src, \"[Ff][Ff][Ff][Ee]\"), 1, 0)\n| where is_eui64=1\n| rex field=src \"(?<prefix>[0-9a-fA-F:]+):(?<iid_high>[0-9a-fA-F]{1,4}):[Ff][Ff][Ff][Ee]:(?<iid_low>[0-9a-fA-F:]+)$\"\n| eval mac_oui=replace(iid_high . \":ff:fe:\" . iid_low, \"(..)(..):ff:fe:(..)(..)\", \"\\1:\\2:XX:XX:\\3:\\4\")\n| stats dc(src) as addresses count as events by mac_oui\n| sort -events\n```\n\nStep 3 — Validate\nCheck a known device's IPv6 address. If it contains ff:fe, it's using EUI-64.\n\nStep 4 — Operationalize\n**Dashboard:** Address type distribution. **Alert:** EUI-64 percentage increasing — regression.\n\nStep 5 — Troubleshooting\n• **Disabling EUI-64 on Linux:** `sysctl -w net.ipv6.conf.all.addr_gen_mode=2` (RFC 7217 stable privacy).\n• **Windows:** RFC 7217 is default since Windows 10 1703.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Enterprise — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`iid_type, is_ipv6`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Old-style IPv6 addresses embedded your device's unique hardware number (like a license plate), so anyone could track where you go. Modern addresses are like changing disguises — they look different on each network. We check how many devices on our network still have the old trackable addresses and need to be updated to the new anonymous style.",
  "prerequisiteUseCases": [
    "UC-5.20.6",
    "UC-5.20.93"
  ],
  "wave": "walk"
}
