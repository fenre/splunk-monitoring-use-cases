{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.13.15",
  "title": "Client Health Category Breakdown (Good/Fair/Poor/Idle/New)",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=catalyst sourcetype=cisco:dnac:clienthealth`. from the `clienthealth` modular input in the Cisco Catalyst Add-on (Splunkbase 7538) polling `GET /dna/intent/api/v1/client-health`. Default poll interval: 900s (15m). Volume: ~3 events per poll (ALL, WIRED, WIRELESS categories). RBAC: NETWORK-ADMIN-ROLE. Key fields: `scoreCategory` (string), `value` (integer 0-100), `clientCount` (integer), `scoreDetail{}` (array/object).",
  "app": "`Cisco Catalyst Add-on for Splunk` (Splunkbase 7538)",
  "spl": "index=catalyst sourcetype=\"cisco:dnac:clienthealth\" | spath output=categories path=scoreDetail{} | mvexpand categories | spath input=categories | stats latest(clientCount) as count by scoreCategory | eventstats sum(count) as total | eval pct=round(count*100/total,1) | table scoreCategory count pct | sort -count",
  "description": "Provides a detailed breakdown of client health across all Catalyst Center health categories (good, fair, poor, idle, nodata, new), showing the full distribution of client experience.",
  "value": "The full category breakdown goes beyond simple healthy/unhealthy to show idle clients (potential AP capacity waste), new clients (onboarding quality), and nodata clients (monitoring gaps).",
  "implementation": "Build after UC-5.13.9 when `scoreDetail` is reliable. Use `eventstats sum(count) as total` with `eval pct=round(count*100/total,1)` so percentages share one denominator and total 100 per snapshot; for multi-snapshot data, pre-dedup with `stats latest` per category. Place as a static panel next to the UC-5.13.9 headline tiles.",
  "visualization": "100% stacked bar or pie of pct by scoreCategory, table with count and pct, trellis of category mix over sites if you later join `siteId`.",
  "references": [
    {
      "title": "Splunkbase app 7538",
      "url": "https://splunkbase.splunk.com/app/7538"
    },
    {
      "title": "Catalyst Center API docs",
      "url": "https://developer.cisco.com/docs/catalyst-center/"
    },
    {
      "title": "Catalyst Center Integration Guide",
      "url": "docs/guides/catalyst-center.md"
    },
    {
      "title": "Catalyst Center Client Health API — Cisco DevNet",
      "url": "https://developer.cisco.com/docs/catalyst-center/#!get-overall-client-health"
    }
  ],
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_catalyst_center"
  ],
  "knownFalsePositives": "**Assurance category definitions changing between Catalyst Center versions.** The categories reported by the client health API (Good, Fair, Poor, Idle, New) may change or subdivide between Catalyst Center versions, causing apparent shifts in the breakdown. Distinguish by checking whether a new category appears coincident with a Catalyst Center upgrade. Suppress by mapping categories to a standardized set using a `client_health_categories` lookup.\n\n**Idle clients counted during off-hours or weekends.** During off-hours, many devices (IP phones, printers, IoT) remain connected but idle, inflating the IDLE category count. This is expected behavior, not a health problem. Distinguish by comparing the category breakdown during business hours vs off-hours. Suppress by adding time-based filtering to the dashboard: `| where date_hour>=8 AND date_hour<=18` for business-hours-only views.\n\n**New device deployment wave inflating the NEW category.** A rollout of new laptops, IoT devices, or phones creates a temporary spike in the NEW category. Distinguish by checking whether the NEW count increase correlates with a known device deployment. Suppress by allowing the NEW category to be elevated for 48 hours after a known deployment event.\n\n**Client with fluctuating health score oscillating between Good and Fair categories.** A client near the threshold boundary may flip between categories on each poll, creating noise in the breakdown. Distinguish by checking whether specific `macAddress` entries appear in different categories across consecutive polls. Suppress by using hysteresis in the categorization — require a client to remain in a category for 2+ polls before counting it in that category.",
  "detailedImplementation": "Prerequisites\n• **UC-5.13.9** in place: reliable `cisco:dnac:clienthealth` and visible `scoreDetail` arrays in raw data.\n• Cisco Catalyst Add-on (7538); `clienthealth` input; `GET /dna/intent/api/v1/client-health` behind the scenes.\n• Use full `scoreDetail{}` objects with `spath` and `mvexpand` so Good/Fair/Poor/Idle/New/nodata rows extract consistently across TA versions.\n• See `docs/implementation-guide.md`.\n\nStep 1 — Configure data collection\n• Default 900s poll; ensure Assurance client analytics is on for the sites you report.\n• Category enum strings (GOOD, POOR, IDLE, etc.) may vary—run `| stats values(scoreCategory)` after flattening before publishing executive labels.\n\nStep 2 — Create the search and alert\n```spl\nindex=catalyst sourcetype=\"cisco:dnac:clienthealth\" | spath output=categories path=scoreDetail{} | mvexpand categories | spath input=categories | stats latest(clientCount) as count by scoreCategory | eventstats sum(count) as total | eval pct=round(count*100/total,1) | table scoreCategory count pct | sort -count\n```\n\nUnderstanding this SPL\n• `latest(clientCount) by scoreCategory` takes the most recent population per health bucket in the time window; if multiple polls are present, narrow the time picker to “last poll” or pre-dedupe with `| stats latest(_time) as t by scoreCategory` pattern if you need a single snapshot.\n• `eventstats sum(count) as total` gives one denominator for percentage so all buckets on the same table sum to 100% for that snapshot.\n• `sort -count` lists the largest segments first; compare next to UC-5.13.9 headline “wired vs wireless” when storytelling.\n\n**Pipeline walkthrough**\n• Flatten nested JSON → per-category `clientCount` → percent of whole.\n\nStep 3 — Validate\n• Compare category totals to Catalyst Center Client health pie or table for the same scope and time; allow one poll skew.\n• If percentages do not add to ~100%, duplicate polls or double `mvexpand` may be present—`dedup` on `_raw`+`_time` in a test.\n\nStep 4 — Operationalize\n• 100% stacked bar or table beside UC-5.13.9; use for NOC and exec “experience mix” reviews. When nodata is high, open inventory and client telemetry coverage, not just RF tuning.\n• Optional: clone panel with `by siteId` in a pre-search if the feed is site-scoped per event.\n\nStep 5 — Troubleshooting\n• Category missing after upgrade: Cisco renamed enums—rebuild `values(scoreCategory)` and update the dashboard labels.\n• All nodata: client health not enabled for site or `scoreDetail` empty in JSON—revisit clienthealth input and API permissions.\n• **No clienthealth events arriving:** verify the `clienthealth` input is enabled and check `index=_internal` for API error or endpoint timeout messages from the TA.\n• **NULL or zero values in health scores:** confirm clients are connected and visible in Catalyst Center Assurance; empty values indicate no clients in that category.\n• **Fewer events than expected:** check for API throttling (HTTP 429); credential access denied errors in TA logs indicate insufficient permissions.",
  "grandmaExplanation": "We break down every connected user into health buckets — good, fair, poor, idle, and new — so you understand exactly what everyone is experiencing.",
  "prerequisiteUseCases": [
    "UC-5.13.9"
  ],
  "wave": "walk"
}
