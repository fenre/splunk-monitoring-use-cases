{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.13.34",
  "title": "Security Advisory (PSIRT) Overview",
  "criticality": "critical",
  "difficulty": "beginner",
  "monitoringType": [
    "Security",
    "Vulnerability"
  ],
  "splunkPillar": "Security",
  "owner": "Head of IT Operations",
  "controlFamily": "regulation-specific",
  "compliance": [
    {
      "regulation": "NIST-800-53",
      "version": "Rev. 5",
      "clause": "SI-2",
      "mode": "satisfies",
      "assurance": "partial",
      "assurance_rationale": "Tracks Cisco PSIRT security advisories affecting managed devices, providing evidence of vulnerability awareness and remediation tracking as required by NIST 800-53 Flaw Remediation.",
      "controlObjective": "Demonstrates continuous monitoring of vendor-published security advisories for all managed network infrastructure.",
      "evidenceArtifact": "Saved search over index=catalyst sourcetype=cisco:dnac:securityadvisory scheduled daily; advisory counts and affected device lists exported to compliance dashboard."
    },
    {
      "regulation": "NIST-800-53",
      "version": "Rev. 5",
      "clause": "RA-5",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Contributes to vulnerability scanning by tracking known vendor-published vulnerabilities, though it does not replace active scanning tools.",
      "controlObjective": "Provides a vendor-authoritative vulnerability feed for network infrastructure to complement active vulnerability scanning.",
      "evidenceArtifact": "Dashboard panel showing advisory severity distribution and affected device counts, refreshed with each TA poll cycle."
    }
  ],
  "dataSources": "`index=catalyst sourcetype=cisco:dnac:securityadvisory` from the `securityadvisory` modular input in `TA_cisco_catalyst` (Splunkbase 7538). The TA polls `GET /dna/intent/api/v1/security-advisory/advisory` using token auth. Default poll interval: 3600s (1h). Volume: ~1 event per advisory-device pair per poll × ~700 bytes. Key fields: `advisoryId` (Cisco advisory ID), `advisoryTitle` (human-readable title), `severity` (CRITICAL, HIGH, MEDIUM, LOW, INFORMATIONAL), `cveId` (CVE identifier), `deviceCount` (affected devices), `fixedVersions` (array of patched firmware versions), `publicationDate` (advisory publish date). CIM data model: Vulnerabilities.",
  "app": "`TA_cisco_catalyst` ([Splunkbase 7538](https://splunkbase.splunk.com/app/7538)), Cisco Supported, 1.x+.",
  "spl": "index=catalyst sourcetype=\"cisco:dnac:securityadvisory\"\n| stats dc(advisoryId) as unique_advisories dc(cveId) as unique_cves sum(deviceCount) as total_affected by severity\n| sort case(severity=\"CRITICAL\",1,severity=\"HIGH\",2,severity=\"MEDIUM\",3,severity=\"LOW\",4,1==1,5)",
  "description": "Provides a complete overview of all Cisco security advisories (PSIRTs) affecting devices managed by Catalyst Center, showing advisory severity, CVE identifiers, affected device counts, and available fixed versions — the network security team's starting point for vulnerability management across the managed infrastructure.",
  "value": "Catalyst Center automatically maps Cisco PSIRTs to your device inventory based on running firmware versions. This UC centralises that mapping in Splunk so the security team can see all active advisories across the fleet in one view, prioritise by severity and blast radius (affected device count), and track which advisories have available fixes. Without this, the security team must log into Catalyst Center's GUI per-advisory — with it, they have a single Pareto table that answers 'what are our biggest vulnerability exposures right now?' and drives the firmware upgrade roadmap.",
  "implementation": "Install `TA_cisco_catalyst` (Splunkbase 7538). Enable the `securityadvisory` input (Inputs → Create → Security Advisory: account `catcenter-prod`, index `catalyst`, interval `3600`). The TA polls the PSIRT advisory endpoint hourly. Schedule a weekly report for the security operations review.",
  "visualization": "(1) Table: advisoryId, advisoryTitle, severity, cveId, deviceCount, fixedVersions — sorted by severity then deviceCount. (2) Bar chart: advisory count by severity (CRITICAL red, HIGH orange, MEDIUM yellow, LOW grey). (3) Single value: count of CRITICAL/HIGH advisories with affected devices (red ≥ 1). (4) Drilldown to UC-5.13.37 (affected devices per advisory) for remediation targeting.",
  "references": [
    {
      "title": "Splunkbase app 7538",
      "url": "https://splunkbase.splunk.com/app/7538"
    },
    {
      "title": "Catalyst Center API docs",
      "url": "https://developer.cisco.com/docs/catalyst-center/"
    },
    {
      "title": "Catalyst Center Integration Guide",
      "url": "docs/guides/catalyst-center.md"
    },
    {
      "title": "Cisco PSIRT Security Advisories",
      "url": "https://sec.cloudapps.cisco.com/security/center/publicationListing.x"
    }
  ],
  "knownFalsePositives": "**Same advisory re-polled on every cycle inflating the event count.** The securityadvisory API returns all current advisories on each poll. If you count events rather than distinct `advisoryId` values, counts inflate with each poll cycle. Distinguish by checking whether `| stats dc(advisoryId)` is significantly lower than `| stats count`. Suppress by using `dc(advisoryId)` for advisory metrics instead of event count.\n\n**INFORMATIONAL severity advisories with zero affected devices.** Catalyst Center may include INFORMATIONAL advisories in the feed that do not affect any devices in the managed inventory. These can inflate the total advisory count. Distinguish by checking `deviceCount` — if 0, the advisory is informational only. Suppress by filtering `| where deviceCount>0` for operationally relevant advisory dashboards.\n\n**Advisory impact classification still in progress.** When Catalyst Center first detects a new PSIRT advisory, it may still be classifying the impact (which devices are affected, which versions are vulnerable). During this period, the advisory appears with incomplete data. Distinguish by checking whether `deviceCount` is null or changes between polls. Suppress by allowing 24 hours after a new advisory's `first_seen` before including it in operational dashboards.\n\n**Catalyst Center PSIRT database update re-scanning and surfacing previously cleared advisories.** When Catalyst Center updates its PSIRT database, it re-scans the inventory and may re-detect advisories that were previously assessed and accepted. Distinguish by checking whether the advisory's `advisoryId` appeared in earlier data with a resolution status. Suppress by maintaining a `catalyst_advisory_exceptions` lookup for risk-accepted advisories.",
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_catalyst_center"
  ],
  "detailedImplementation": "Prerequisites\n• `TA_cisco_catalyst` (Splunkbase 7538) ≥1.0 installed on Search Heads AND the Heavy Forwarder running inputs.\n• Catalyst Center **2.3.5+** with Security Advisories feature enabled. The feature maps Cisco PSIRT publications against your device inventory's firmware versions automatically.\n• Service account with **NETWORK-ADMIN-ROLE** (minimum for advisory data).\n• Network: HTTPS (TCP 443) from Splunk HF to Catalyst Center management IP/FQDN.\n• Splunk role: users need `srchIndexesAllowed = catalyst`. Security operations users should have a dedicated role (`network_security_analyst`).\n• License headroom: the `securityadvisory` sourcetype generates ~1 event per advisory-device pair per poll × 700 bytes. A fleet affected by 20 advisories across 200 devices: ~4,000 events/poll × 24 polls/day ≈ 2.7 MB/day.\n• CIM: this sourcetype maps to the **Vulnerabilities** data model. Verify CIM tagging with `| search eventtype=cisco_dnac_advisory tag=vulnerability` after installation.\n\nStep 1 — Configure data collection\nEnable the `securityadvisory` input:\n\n| Setting | Value |\n|---------|-------|\n| Input type | Security Advisory |\n| Account | `catcenter-prod` |\n| Index | `catalyst` |\n| Interval | `3600` (hourly — advisory data changes slowly; daily polling is also acceptable) |\n\nThe TA polls `GET /dna/intent/api/v1/security-advisory/advisory`. Each advisory is matched against devices running affected firmware versions. The response includes the advisory metadata (ID, title, severity, CVEs) and the count/list of affected devices.\n\nVerification:\n```spl\nindex=catalyst sourcetype=\"cisco:dnac:securityadvisory\" earliest=-2h\n| stats dc(advisoryId) as advisories, sum(deviceCount) as affected_devices\n```\nCompare the advisory count with **Catalyst Center > Security Advisories** overview. They should match.\n\nStep 2 — Create the search and report\n```spl\nindex=catalyst sourcetype=\"cisco:dnac:securityadvisory\"\n| stats dc(advisoryId) as unique_advisories dc(cveId) as unique_cves sum(deviceCount) as total_affected by severity\n| sort case(severity=\"CRITICAL\",1,severity=\"HIGH\",2,severity=\"MEDIUM\",3,severity=\"LOW\",4,1==1,5)\n```\n\nWhy `dc(advisoryId)` not `count`: the same advisory appears in every poll. `dc(advisoryId)` gives the actual number of distinct active advisories per severity level.\n\nWhy `sum(deviceCount)`: shows the total blast radius per severity. `total_affected` for CRITICAL severity answers 'how many devices are exposed to critical vulnerabilities right now?'\n\nWhy custom `sort` with `case()`: sorts by severity in operational priority order (CRITICAL first) rather than alphabetically.\n\nFor the detailed advisory list (more actionable):\n```spl\nindex=catalyst sourcetype=\"cisco:dnac:securityadvisory\"\n| stats latest(advisoryTitle) as title latest(cveId) as cve latest(deviceCount) as devices latest(fixedVersions) as fixes by advisoryId, severity\n| where devices > 0\n| sort case(severity=\"CRITICAL\",1,severity=\"HIGH\",2,severity=\"MEDIUM\",3,1==1,4) -devices\n```\n\nSchedule as Report: weekly (cron `0 7 * * 1`), output to PDF for the security operations review.\n\nStep 3 — Validate\n(a) Compare the advisory count and severity distribution with **Catalyst Center > Security Advisories**. The counts should match.\n\n(b) Pick a specific CRITICAL advisory from the results. Verify in Catalyst Center that the `deviceCount` and `fixedVersions` match.\n\n(c) Cross-reference a CVE identifier with the Cisco PSIRT portal (sec.cloudapps.cisco.com) to verify the advisory is genuine and the severity is correctly classified.\n\n(d) Check CIM mapping: `| tstats count from datamodel=Vulnerabilities by Vulnerabilities.severity`. If no results, the CIM tags need configuration on the Search Head.\n\n(e) Vendor UI parity: open **Catalyst Center > Security Advisories** and compare the overall severity breakdown with the Splunk search results.\n\nStep 4 — Operationalize\nDashboard placement (on a \"Network Vulnerability Management\" dashboard):\n• Table: advisory list sorted by severity and device count.\n• Bar chart: advisory count by severity.\n• Single value: CRITICAL + HIGH advisories with devices > 0.\n• Drilldown: click an advisory → UC-5.13.37 (affected devices).\n\nRunbook (owner: Network Security Operations):\n1. Review the weekly advisory report. Focus on CRITICAL and HIGH advisories with `deviceCount > 0`.\n2. For each CRITICAL advisory: check `fixedVersions` — is a patched firmware available? If yes, escalate to the firmware upgrade team (UC-5.13.56). If no fix is available, check for workarounds in the Cisco PSIRT publication.\n3. For HIGH advisories: assess whether the affected devices are internet-facing, in the CDE (PCI), or in critical infrastructure. Prioritise accordingly.\n4. Track remediation progress with UC-5.13.38 (Advisory Remediation Progress).\n5. Generate a monthly vulnerability report for compliance evidence (NIST SI-2 / RA-5).\n\nStep 5 — Troubleshooting\n\n• **No advisory events** — the `securityadvisory` input is not enabled, or the Catalyst Center doesn't have Security Advisories configured. Check **Catalyst Center > Security Advisories** to verify the feature is available.\n\n• **Advisory count in Splunk > Catalyst Center GUI** — the GUI may filter by severity or only show advisories with affected devices. The TA may include all advisories including those with `deviceCount=0`. Filter with `| where deviceCount > 0`.\n\n• **`deviceCount` is 0 for all advisories** — Catalyst Center hasn't matched advisories to your device inventory. This requires accurate firmware version reporting via device discovery. Check **Catalyst Center > Provision > Inventory** for firmware version accuracy.\n\n• **`fixedVersions` is empty** — Cisco hasn't released a fix for this advisory yet. Track it for workaround application in the meantime.\n\n• **CIM Vulnerabilities model shows no data** — the TA's `tags.conf` may not include the `vulnerability` tag for advisory events. Add it to `$SPLUNK_HOME/etc/apps/TA_cisco_catalyst/local/tags.conf`.\n\n• **Same advisory appears multiple times in the table** — the search window covers multiple polls. Use `| stats latest() by advisoryId` to deduplicate.\n\n• **New advisory appears with very high deviceCount** — a newly published CRITICAL PSIRT affecting a common platform can hit hundreds of devices. This is a genuine exposure — escalate immediately.\n\n• **Advisory severity doesn't match the CVE CVSS score** — Cisco assigns its own severity to PSIRTs, which may differ from NIST NVD CVSS scores. Use Cisco's severity for operational prioritisation; reference CVSS for compliance reporting.",
  "grandmaExplanation": "We show you all the known security weaknesses that affect your network equipment, ranked by how serious they are and how many devices are at risk. This helps your security team focus on patching the most dangerous vulnerabilities first.",
  "prerequisiteUseCases": [],
  "wave": "crawl",
  "status": "verified",
  "lastReviewed": "2026-04-30",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases"
}
