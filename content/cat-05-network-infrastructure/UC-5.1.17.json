{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.1.17",
  "title": "Duplex Mismatch Detection",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Duplex Mismatch Detection' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_duplex_mismatch_detection` (UC-5.1.17), SOC dashboard panel \"Operations — Duplex Mismatch Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.1.17 positive (Duplex, Mismatch, Detection): ingest synthetic events into `index=network` sourcetype='cisco:ios' where `count` grouped by `host`, `local_intf`, `remote_device`, `remote_intf` satisfies detection threshold for 'Duplex Mismatch Detection'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.1.17 negative (Mismatch Detection): ingest benign `network` / `cisco:ios` events where `host`, `local_intf`, `remote_device`, `remote_intf` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 4 confirms zero alert rows for 'Duplex Mismatch Detection'."
  },
  "dataSources": "`sourcetype=cisco:ios`, `sourcetype=snmp:interface` Collected via Splunk Add-on for Cisco Security (Splunkbase 1352) modular input or scripted poll into the index named in the SPL.",
  "app": "SNMP Modular Input, IF-MIB, `TA-cisco_ios`, `Splunk_TA_juniper`, `arista:eos` via SC4S, HPE Aruba CX syslog — Splunk Add-on for Cisco Security (Splunkbase 1352)",
  "spl": "index=network sourcetype=\"cisco:ios\" \"%CDP-4-DUPLEX_MISMATCH\"\n| rex \"duplex mismatch discovered on (?<local_intf>\\S+).*with (?<remote_device>\\S+) (?<remote_intf>\\S+)\"\n| stats count latest(_time) as last_seen by host, local_intf, remote_device, remote_intf\n| sort -last_seen",
  "description": "Surfaces risk tied to Duplex, Mismatch, Detection through UC-5.1.17 on `index=network` sourcetype `cisco:ios`. Summarises `count` grouped by `host`, `local_intf`, `remote_device`, `remote_intf` across `the scheduled window` before applying configured threshold. Operators tune 'Duplex Mismatch Detection' using UC-5.1.17 runbook steps.",
  "value": "Tracking `activity` via UC-5.1.17 on `cisco:ios` feeds improves response to 'Duplex Mismatch Detection' and reduces operational risk and delayed incident response.",
  "implementation": "Enable CDP/LLDP on all interfaces. Monitor syslog for duplex mismatch messages. Cross-reference with SNMP interface counters showing late collisions.",
  "visualization": "Table (local device/interface → remote device/interface), Alert list.",
  "cimModels": [],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 1352",
      "url": "https://splunkbase.splunk.com/app/1352",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Cisco Security documentation",
      "url": "https://www.cisco.com/c/en/us/support/security/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `network` (UC-5.1.17)** — Planned work for 'Duplex Mismatch Detection' shifts `count` over `span=1h` while `host`, `local_intf`, `remote_device`, `remote_intf` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `cisco:ios`** — Scheduled automation elevates `local_intf` without crossing configured threshold for UC-5.1.17. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`, `local_intf`, `remote_device`, `remote_intf`** — Health checks in `network` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `cisco:ios`** — First 48h baseline for UC-5.1.17 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco",
    "snmp",
    "syslog"
  ],
  "equipmentModels": [
    "cisco_ios",
    "snmp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    {
      "name": "Splunk User Behavior Analytics",
      "displayName": "Splunk User Behavior Analytics",
      "note": "UBA"
    }
  ],
  "detailedImplementation": "Prerequisites\n* Interface speed/duplex syslog and SNMP data. Data in `index=network` with `sourcetype=cisco:ios` or SNMP data. Key fields: `ifSpeed`, `ifDuplex`, syslog messages about auto-negotiation results.\n* Duplex mismatch: one side of a link is full-duplex, the other half-duplex. Causes late collisions, CRC errors, and degraded throughput. Typically occurs when one side has auto-negotiation disabled (hardcoded) and the other auto-negotiates. Very common cause of intermittent packet loss.\n\nStep 1 -- Configure data collection\n```\n# SNMP polling for interface speed/duplex\n# OID: dot3StatsDuplexStatus (.1.3.6.1.2.1.10.7.2.1.19)\n# Values: 1=unknown, 2=halfDuplex, 3=fullDuplex\n\n# Cisco syslog: %LINK-3-UPDOWN includes speed/duplex info\n# %CDP-4-DUPLEX_MISMATCH is key indicator\n```\nVerify:\n```spl\nindex=network earliest=-24h\n| where match(_raw, \"(?i)duplex.*mismatch|DUPLEX_MISMATCH|half.?duplex|auto.*negot\")\n| stats count by host\n```\n\nStep 2 -- Create the search and alert\n\n**Primary search -- Duplex mismatch detection:**\n```spl\nindex=network earliest=-24h\n| where match(_raw, \"(?i)DUPLEX_MISMATCH|duplex.*mismatch|half.?duplex|a-half|auto.*negot.*half\")\n| rex field=_raw \"(?i)(?:port|interface|Port)\\s+(?<interface>\\S+)\"\n| rex field=_raw \"(?i)(?:neighbor|peer|remote).*?(?<neighbor_port>\\S+)\"\n| eval device=coalesce(host, device_name)\n| eval iface=coalesce(interface, port)\n| stats count as events latest(_time) as last_seen by device, iface, neighbor_port\n| eval severity=\"WARNING -- duplex mismatch detected on \".iface\n| eval last_seen_time=strftime(last_seen, \"%Y-%m-%d %H:%M:%S\")\n| table device, iface, neighbor_port, events, last_seen_time, severity\n| sort -events\n```\n\n**Secondary search -- Half-duplex interface inventory:**\n```spl\nindex=network earliest=-4h\n| eval duplex=coalesce(dot3StatsDuplexStatus, duplex, if(match(_raw, \"(?i)half\"), \"half\", if(match(_raw, \"(?i)full\"), \"full\", null())))\n| where duplex=\"half\" OR duplex=\"2\"\n| eval device=coalesce(host, device_name)\n| eval interface=coalesce(ifName, interface, port)\n| stats count latest(_time) as last_seen by device, interface\n| eval severity=\"INFO -- interface running half-duplex\"\n| table device, interface, last_seen, severity\n```\n\nStep 3 -- Validate\n(a) CLI: `show interface status` -- check speed/duplex for all ports.\n(b) CLI: `show cdp neighbors detail` -- check CDP duplex mismatch warnings.\n(c) Correlate with CRC/input error trending (UC-5.1.2, UC-5.1.21).\n\nStep 4 -- Operationalize\nDashboard (\"Network -- Duplex Status\"):\n* Row 1 -- Single-value: \"Duplex mismatches\", \"Half-duplex interfaces\".\n* Row 2 -- Duplex mismatch table with interface details.\n\nAlert: Warning (duplex mismatch detected): fix immediately to prevent packet loss.\n\nStep 5 -- Troubleshooting\n\n* **Fix duplex mismatch** -- Best practice: set both ends to auto-negotiate OR both ends to hardcoded same speed/duplex. Never mix auto and hardcoded. CLI: `interface <intf>` then `speed auto` and `duplex auto`.\n\n* **Correlate with errors** -- Duplex mismatch causes: late collisions (on full-duplex side), FCS/CRC errors, runts. Check `show interface <intf>` error counters.\n\n* **Why auto-negotiation fails** -- Some older devices or specific cabling (crossover vs straight-through) may not auto-negotiate correctly. Hardcode both ends to match.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (SNMP Modular Input, IF-MIB, `TA-cisco_ios`, `Splunk_TA_juniper`, `arista:eos` via SC4S, HPE Aruba CX syslog — Splunk Add-on for Cisco Security (Splunkbase 1352)). Enable the modular input or HEC token that writes sourcetype=cisco:ios into network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, local_intf, remote_device, remote_intf`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We help you know early when something looks wrong with duplex mismatch detection so the team can act before it grows into a bigger outage.",
  "hardware": "Cisco Catalyst 9200/9300/9400/9500/9600, ISR 1100/4000, ASR 1000; Juniper EX2300, EX3400, EX4300, EX4400, EX4600, QFX5100/5120/5200/5220, MX204/304/480, SRX300/SRX1500/SRX4100/SRX4200; Arista 7050X3, 7060X, 7260X3, 7280R3, 7500R3; HPE Aruba CX 6100/6200/6300/6400, CX 8320/8325/8360",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
