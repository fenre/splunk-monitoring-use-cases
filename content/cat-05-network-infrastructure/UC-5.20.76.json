{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.20.76",
  "title": "IPv6 PMTUD Manipulation Attack Detection",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'IPv6 PMTUD Manipulation Attack Detection' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_ipv6_pmtud_manipulation_attack_detection` (UC-5.20.76), SOC dashboard panel \"Operations — IPv6 PMTUD Manipulation Attack Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=network` sourcetype='cisco:ios' that satisfy the UC-5.20.76 SPL filters for 'IPv6 PMTUD Manipulation Attack Detection'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `network` / `cisco:ios` events outside the detection threshold, or exclude the test entity via `operational_exceptions.csv` with a valid window. Confirm zero alert rows for UC-5.20.76."
  },
  "dataSources": "`index=network sourcetype=netflow` or `sourcetype=zeek:conn` or `sourcetype=cisco:ios` containing ICMPv6 Packet Too Big messages and TCP retransmission data. Path MTU Discovery (PMTUD) manipulation is an IPv6-specific attack where an attacker sends forged ICMPv6 Packet Too Big (Type 2) messages with a fraudulently low MTU value:\n\n1. **Attack mechanism.** The attacker sends a crafted ICMPv6 PTB message to a host, claiming the path MTU is very low (e.g., 1280 bytes, the IPv6 minimum). The host reduces its Maximum Segment Size (MSS) for the target connection, dramatically reducing throughput.\n2. **Impact.** Throughput drops to a fraction of normal because each TCP segment carries only 1220 bytes of payload instead of the typical 1440+ bytes. The connection remains functional but is extremely slow — a performance degradation attack.\n3. **Detection signals:** (a) PTB messages from sources not on the actual path between the two endpoints. (b) Sudden MSS reduction in TCP connections without corresponding path changes. (c) PTB messages claiming an MTU below 1280 (invalid per RFC 8200 — IPv6 minimum is 1280).\n4. **Atomic fragment attack (RFC 8021).** A special case where PTB claims MTU < 1280, causing some implementations to generate atomic fragments (packets with a Fragment Header but only one fragment). RFC 8021 documents that this should not happen. Collected via Splunk Add-on for Cisco Security (Splunkbase 1352) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Add-on for Cisco IOS. Splunk Add-on for Zeek/Corelight. — Splunk Add-on for Cisco Security (Splunkbase 1352)",
  "spl": "index=network (sourcetype=\"cisco:ios\" OR sourcetype=\"netflow\" OR sourcetype=\"zeek:conn\") (\"Packet Too Big\" OR \"icmpv6_type=2\" OR \"packetTooBig\") earliest=-4h\n| rex field=_raw \"(?:mtu|MTU)\\s*=?\\s*(?<claimed_mtu>\\d+)\"\n| rex field=_raw \"(?:src|source|from)\\s*=?\\s*(?<ptb_source>[0-9a-fA-F:.]+)\"\n| rex field=_raw \"(?:dst|dest|to)\\s*=?\\s*(?<ptb_dest>[0-9a-fA-F:.]+)\"\n| eval claimed_mtu=tonumber(claimed_mtu)\n| eval anomaly=case(\n    claimed_mtu < 1280, \"CRITICAL — MTU below IPv6 minimum (1280). This is ALWAYS an attack (RFC 8021).\",\n    claimed_mtu < 1300 AND claimed_mtu >= 1280, \"WARNING — MTU at IPv6 minimum. Verify path contains a 1280-byte link.\",\n    1=1, null())\n| where isnotnull(anomaly)\n| stats count as ptb_count first(_time) as first last(_time) as last by ptb_source, ptb_dest, claimed_mtu, anomaly\n| sort -ptb_count",
  "description": "Detects Path MTU Discovery manipulation attacks where an attacker sends forged ICMPv6 Packet Too Big messages with fraudulently low MTU values to degrade connection throughput. PTB messages claiming an MTU below the IPv6 minimum of 1280 bytes are definitively invalid per RFC 8021. PTB messages with very low MTU values from sources not on the actual network path between endpoints are highly suspicious.",
  "value": "PMTUD manipulation is a subtle attack — the connection continues to work but at dramatically reduced throughput. Users report 'the network is slow' but not 'the network is down,' making diagnosis difficult. Because IPv6 has no router-level fragmentation (unlike IPv4), PMTUD is the only mechanism for adapting to path MTU constraints. Forging PTB messages exploits this mandatory trust relationship. Detecting forged PTB messages catches this attack before it impacts application performance.",
  "implementation": "Monitor ICMPv6 PTB messages for fraudulent MTU values. Verify PTB source addresses are on the actual path between endpoints. Alert on MTU values below 1280 (always invalid). Correlate with TCP retransmission patterns.",
  "visualization": "(1) Alert table: PTB messages with invalid/suspicious MTU values. (2) Scatter plot: claimed MTU vs PTB source (path validation). (3) Timechart: PTB message rate. (4) Table: connections experiencing sudden MSS reduction.",
  "references": [
    {
      "title": "RFC 8021 — Generation of IPv6 Atomic Fragments Considered Harmful",
      "url": "https://www.rfc-editor.org/rfc/rfc8021",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RFC 8201 — Path MTU Discovery for IP version 6 (PMTUD specification)",
      "url": "https://www.rfc-editor.org/rfc/rfc8201",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'IPv6 PMTUD Manipulation Attack Detection'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `network` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `operational_exceptions.csv` with entity, reason, valid_until.",
  "equipment": [
    "cisco",
    "corelight",
    "juniper",
    "netflow",
    "paloaltonetworks",
    "zeek"
  ],
  "equipmentModels": [
    "cisco_ios",
    "cisco_iosxe",
    "corelight_sensor",
    "netflow_netflow",
    "paloalto_panos",
    "zeek_sensor"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• ICMPv6 PTB messages visible in syslog, NetFlow, or Zeek logs.\n• Network topology knowledge to validate PTB source addresses against actual path.\n• TCP performance metrics to correlate with MSS reduction.\n\nStep 1 — Configure data collection\n\n**Cisco IOS-XE — ICMPv6 logging:**\nICMPv6 PTB messages are logged when `debug ipv6 icmp` is enabled (not recommended for production) or via ACL logging:\n```\nipv6 access-list MONITOR_PTB\n permit icmp any any packet-too-big log\n permit ipv6 any any\n```\n\n**Zeek/Corelight — ICMPv6 analysis:**\nZeek logs ICMP connections in conn.log. PTB messages appear as ICMP connections with specific type/code fields.\n\n**NetFlow/IPFIX:**\nIPFIX can export ICMPv6 type/code in the icmpTypeCodeIPv6 (IE#139) field. Verify this field is included in the flow record.\n\n**Verification:**\n```spl\nindex=network (\"Packet Too Big\" OR \"packetTooBig\" OR \"icmpv6_type=2\") earliest=-7d\n| stats count by host\n```\n\nStep 2 — Create the search and alert\n\n**Invalid MTU detection (<1280):**\n```spl\nindex=network (\"Packet Too Big\" OR \"packetTooBig\" OR \"icmpv6_type=2\") earliest=-1h\n| rex field=_raw \"(?:mtu|MTU)\\s*=?\\s*(?<mtu>\\d+)\"\n| where tonumber(mtu) < 1280\n| eval alert=\"DEFINITIVE ATTACK: PTB with MTU=\" . mtu . \" — below IPv6 minimum (1280). RFC 8021 explicitly calls this harmful.\"\n| table _time, host, mtu, alert\n```\nTrigger: any event. MTU < 1280 is ALWAYS invalid in IPv6.\n\n**Suspicious PTB source validation:**\n```spl\nindex=network (\"Packet Too Big\" OR \"icmpv6_type=2\") earliest=-4h\n| rex field=_raw \"(?:src|source|from)\\s*=?\\s*(?<ptb_src>[0-9a-fA-F:.]+)\"\n| rex field=_raw \"(?:mtu|MTU)\\s*=?\\s*(?<mtu>\\d+)\"\n| lookup known_transit_routers.csv ipv6_address as ptb_src OUTPUT router_name\n| where isnull(router_name)\n| eval warning=\"PTB from unknown source \" . ptb_src . \" (MTU=\" . mtu . \") — not a known transit router. Possible spoofed PTB.\"\n| table _time, ptb_src, mtu, warning\n```\nLegitimate PTB messages come from routers on the actual path. A PTB from an unknown source is suspicious.\n\n**Repeated PTB to same target (sustained attack):**\n```spl\nindex=network (\"Packet Too Big\" OR \"icmpv6_type=2\") earliest=-1h\n| rex field=_raw \"(?:dst|dest|to)\\s*=?\\s*(?<target>[0-9a-fA-F:.]+)\"\n| rex field=_raw \"(?:mtu|MTU)\\s*=?\\s*(?<mtu>\\d+)\"\n| stats count as ptb_count dc(mtu) as mtu_values by target\n| where ptb_count > 20\n| eval attack=\"Sustained PMTUD attack: \" . ptb_count . \" PTB messages to \" . target . \" in 1 hour\"\n```\n\nStep 3 — Validate\n(a) **Invalid MTU test.** Use Scapy to craft a PTB with MTU=1000 and send to a test host. Verify the invalid MTU alert fires.\n\n(b) **Path validation.** On a path with a known transit router, verify PTB messages from that router do NOT trigger the unknown-source alert.\n\n(c) **Normal PTB.** On a path with a VPN tunnel (legitimate reduced MTU), verify PTB messages are classified correctly as legitimate.\n\nStep 4 — Operationalize\n\n**Dashboard** (\"IPv6 — PMTUD Security\"):\n• Row 1 — Alert: invalid MTU PTB messages (always attack).\n• Row 2 — Table: suspicious PTB from unknown sources.\n• Row 3 — Timechart: PTB rate per target host.\n• Row 4 — Table: known PTB sources (transit routers) vs unknown sources.\n\n**Scheduling:** Invalid MTU real-time. Suspicious source every 15 minutes. Sustained attack hourly.\n\n**Runbook:**\n1. MTU < 1280: block the PTB source IP. If the source is spoofed, deploy BCP 38 anti-spoofing.\n2. Unknown PTB source: verify the source is not a recently-added transit router. If unknown, block and investigate.\n3. Sustained attack: implement ICMPv6 rate limiting on the target host. Consider deploying PMTUD path validation (host-side).\n\nStep 5 — Troubleshooting\n\n• **PMTUD path cache** — Hosts maintain a path MTU cache. After a PMTUD attack, the reduced MTU persists in the cache even after the attack stops. Clear the path MTU cache on affected hosts to restore performance.\n\n• **PLPMTUD (RFC 8899)** — Packetization Layer PMTUD validates PTB messages by probing the path. Hosts that implement PLPMTUD are more resistant to PMTUD manipulation because they verify the claimed MTU independently.\n\n• **Rate limiting PTB** — Excessive PTB rate limiting can break legitimate PMTUD. Apply rate limits conservatively — 1-10 PTB per second per destination is typically sufficient for legitimate operation.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for Cisco IOS. Splunk Add-on for Zeek/Corelight. — Splunk Add-on for Cisco Security (Splunkbase 1352)). Enable the modular input or HEC token that writes sourcetype=cisco:ios, netflow, zeek:conn into network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`anomaly, claimed_mtu, ptb_dest, ptb_source`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "When sending a large package, the delivery service may come back and say 'Your package is too big for the tunnel on the route — make it smaller.' If a trickster sends this message falsely, you'll start wrapping everything in tiny packages, making your deliveries very slow even though the route is actually fine.",
  "prerequisiteUseCases": [
    "UC-5.20.38"
  ],
  "wave": "run"
}
