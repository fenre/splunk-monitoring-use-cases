{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.13.20",
  "title": "Network Health Score Anomaly Detection",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Anomaly"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=catalyst sourcetype=cisco:dnac:networkhealth`. from the `networkhealth` modular input in the Cisco Catalyst Add-on (Splunkbase 7538) polling `GET /dna/intent/api/v1/network-health`. Default poll interval: 900s (15m). Volume: ~1 event per poll (aggregate network score). RBAC: NETWORK-ADMIN-ROLE. Key fields: `healthScore` (integer 0-100), `goodCount` (integer), `badCount` (integer), `totalCount` (integer).",
  "app": "`Cisco Catalyst Add-on for Splunk` (Splunkbase 7538)",
  "spl": "index=catalyst sourcetype=\"cisco:dnac:networkhealth\" | bin _time span=1h | stats avg(healthScore) as health by _time | eventstats avg(health) as baseline stdev(health) as stdev_health | where health < (baseline - 2*stdev_health) AND stdev_health > 0 | eval deviation=round((baseline-health)/stdev_health,1) | sort -deviation",
  "description": "Applies statistical anomaly detection to the network health score, identifying significant deviations from the historical baseline that may indicate emerging issues.",
  "value": "Fixed thresholds for network health may not account for normal variations. Anomaly detection catches unusual drops relative to your network's specific baseline.",
  "implementation": "Requires UC-5.13.16 and enough history for UC-5.13.17 to establish variance. The `eventstats` here runs across the whole result set, so use a long lookback and watch for missing hours that can inflate `stdev`. Consider seasonal baselines in a follow-on if the network’s score drifts with quarters or school terms.",
  "visualization": "Table of anomalous hours with deviation, overlay plot of `health` with a ribbon for baseline minus two sigma, combined alert card.",
  "references": [
    {
      "title": "Splunkbase app 7538",
      "url": "https://splunkbase.splunk.com/app/7538"
    },
    {
      "title": "Catalyst Center API docs",
      "url": "https://developer.cisco.com/docs/catalyst-center/"
    },
    {
      "title": "Catalyst Center Integration Guide",
      "url": "docs/guides/catalyst-center.md"
    },
    {
      "title": "Catalyst Center Network Health API — Cisco DevNet",
      "url": "https://developer.cisco.com/docs/catalyst-center/#!get-overall-network-health"
    }
  ],
  "knownFalsePositives": "**Planned maintenance causing health score to deviate from the statistical baseline.** During firmware upgrades or network changes, the health score drops below the normal operating range, triggering a statistical anomaly. Distinguish by correlating with `index=catalyst sourcetype=\"cisco:dnac:audit:logs\"` for planned change activity. Suppress by excluding known maintenance windows from the baseline computation: use `| where NOT (maintenance_flag=1)` before computing `eventstats`.\n\n**Seasonal network usage pattern creating periodic deviations.** Network health may follow predictable patterns (lower during business hours, higher on weekends) that appear anomalous compared to a flat 7-day baseline. Distinguish by checking whether the same pattern occurs at the same time in previous weeks. Suppress by using a time-aware baseline: compute separate baselines for business hours and off-hours, or use day-of-week segmented statistics.\n\n**Assurance recomputation introducing brief outliers that inflate standard deviation.** A single poll returning `healthScore=0` during recomputation will widen the standard deviation and may cause subsequent normal scores to appear anomalous. Distinguish by checking whether the outlier event has `healthScore=0` exactly (characteristic of API recomputation). Suppress by adding `| where healthScore>0` before computing the baseline, or by using median and interquartile range instead of mean and standard deviation.\n\n**Major inventory change shifting the health score distribution.** Adding a large batch of devices (e.g., a new building coming online) changes the health score composition. The statistical baseline computed from prior data becomes invalid. Distinguish by tracking `totalCount` alongside `healthScore` — if the count changed significantly, the baseline needs resetting. Suppress by requiring a minimum of 48 hours of data after a significant inventory change before re-enabling anomaly detection.",
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_catalyst_center"
  ],
  "detailedImplementation": "Prerequisites\n• **networkhealth** feed with continuous `cisco:dnac:networkhealth` in `index=catalyst` (Cisco Catalyst Add-on 7538).\n• **UC-5.13.16** and **UC-5.13.17** to validate the basic score and a simple trend before you add statistics.\n• **Rule of thumb:** use a **7–30 day** lookback in tests so `eventstats` has enough **hourly** buckets; a **single day** window makes **baseline** and **stdev** unstable.\n• Document that this technique uses **all rows in the search job** for **baseline/stdev**—it is a **tactical** NOC view, not a pre-trained **ML** model.\n• `docs/implementation-guide.md` for scheduled search limits and **search head** load.\n\nStep 1 — Configure data collection\n• **API:** `GET /dna/intent/api/v1/network-health`; **input** **networkhealth**; **sourcetype** `cisco:dnac:networkhealth`.\n• **Interval:** **900s** typical; if you have **sub-hour** re-emits, `avg(healthScore)` in an hour still behaves sensibly, but check for **duplicate** timestamps in validation.\n• **Key field:** `healthScore` must be **numeric** (`| where isnum(healthScore)` in QA).\n\nStep 2 — Create the search and report\n```spl\nindex=catalyst sourcetype=\"cisco:dnac:networkhealth\" | bin _time span=1h | stats avg(healthScore) as health by _time | eventstats avg(health) as baseline stdev(health) as stdev_health | where health < (baseline - 2*stdev_health) AND stdev_health > 0 | eval deviation=round((baseline-health)/stdev_health,1) | sort -deviation\n```\n\nUnderstanding this SPL (same-window stats, 2-sigma, caveats)\n• **`eventstats` baseline/stdev** are computed from **all hourly rows returned**; changing the time picker **changes** the “normal” line—set expectations in the runbook.\n• **Guard `stdev_health > 0`** avoids divide-by-near-zero noise when the score is **flat** for days.\n• **Tune** the **2x** multiplier to **2.5x** or **3x** if leadership sees too many “yellow” hours during ordinary volatility.\n• **Missing hours:** if **ingest** drops, a single low hour can look anomalous—pair with a **concurrent** `timechart` of `count` to detect **gaps**.\n\n**Pipeline walkthrough**\n• Hourly `bin` + `stats` build one **health** per bucket.\n• **Z-score** style filter surfaces **unusual** lows for triage with **UC-5.13.18** (threshold alerts) and **UC-5.13.16** (executive view).\n\nStep 3 — Validate\n• **Known incident week:** the search should return **rows** in hours you already documented; if not, the multiplier or lookback is too loose.\n• **`| timechart count`** in parallel: zero-hour gaps should **not** be treated as “health dropped to null.”\n• After **Catalyst** major upgrade, re-baseline: scoring changes can **shift** the whole curve.\n• **Vendor UI parity:** open **Catalyst Center > Assurance > Network > Health** for the same time window; the overall health score should match within 1-2 points.\nStep 4 — Operationalize\n• **Dashboard:** table of anomalous hours **plus** a **line** chart of **health** with **shaded** band for **baseline − 2σ** (clone this search into two panels).\n• **Alerting:** schedule **2× daily** for **review** or **on-demand**; many teams **do not** page on **statistical** health alone without **UC-5.13.18** also firing—document that policy.\nStep 5 — Troubleshooting\n• **Hundreds of rows:** baseline window too short or **stdev** very small after a long flatline—lengthen **earliest** or increase **span** to **4h** to match **UC-5.13.17** cadence.\n• **No rows ever:** `healthScore` stringified or **ingest** missing—`fieldsummary` and raw JSON.\n• **All rows after TA upgrade:** **field** rename in add-on; compare release notes and fix **props**.\n• If data is not arriving for `cisco:dnac:networkhealth`, check that the `networkhealth` input is enabled in the TA configuration and that the Catalyst Center API credentials have not expired.\n",
  "grandmaExplanation": "We watch the usual rhythm of that single network health number and only raise a hand when an hour is unusually low compared with your own past, not just compared with a one-size-fits-all rule.",
  "prerequisiteUseCases": [
    "UC-5.13.16",
    "UC-5.13.17"
  ],
  "wave": "run"
}
