{
  "$schema": "../../schemas/uc.schema.json",
  "cimModels": [
    "N/A"
  ],
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "status": "draft",
  "splunkPillar": "Observability",
  "id": "5.14.3",
  "title": "HAProxy Queue Time vs Response Time Saturation Ratio",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance",
    "Capacity"
  ],
  "dataSources": "`index=proxy` `sourcetype=haproxy:http` timed fields `Tw` (queue) and `Tr` (response) in TCP log format",
  "app": "Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes",
  "spl": "index=proxy sourcetype=\"haproxy:http\"\n| eval Tw=tonumber(Wait), Tr=tonumber(Response)\n| eval q_ratio=if(Tr>0, round(Tw/Tr,3), null())\n| where Tw > 50 AND q_ratio > 0.5\n| timechart span=5m perc95(q_ratio) by backend",
  "description": "Queue-heavy latency means the proxy or servers cannot dequeue fast enough even if app CPU is fine.",
  "value": "Directs tuning at maxconn, threads, or origin scale instead of micro-optimizing code alone.",
  "implementation": "Confirm millisecond units in `props.conf`. Tune ratio threshold per API; pair with server `cur_sess`.",
  "visualization": "Time series for rates and latency, top/dedup for hotspots, single-value alerts on health thresholds.",
  "references": [
    {
      "title": "Vendor documentation",
      "url": "https://www.haproxy.com/blog/the-four-golden-signals-for-haproxy/"
    }
  ],
  "equipment": [
    "haproxy"
  ],
  "grandmaExplanation": "We tell if delays are from waiting in HAProxy versus slow application code."
}
