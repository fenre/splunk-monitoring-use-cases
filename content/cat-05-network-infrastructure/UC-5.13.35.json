{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.13.35",
  "title": "Critical/High PSIRT Alerting",
  "criticality": "critical",
  "difficulty": "beginner",
  "monitoringType": [
    "Security",
    "Vulnerability"
  ],
  "splunkPillar": "Security",
  "owner": "Head of IT Operations",
  "controlFamily": "regulation-specific",
  "compliance": [
    {
      "regulation": "NIST-800-53",
      "version": "Rev. 5",
      "clause": "SI-2",
      "mode": "satisfies",
      "assurance": "partial",
      "assurance_rationale": "Tracks Cisco PSIRT security advisories affecting managed devices, providing evidence of vulnerability awareness and remediation tracking as required by NIST 800-53 Flaw Remediation.",
      "controlObjective": "Demonstrates continuous monitoring of vendor-published security advisories for all managed network infrastructure.",
      "evidenceArtifact": "Saved search over index=catalyst sourcetype=cisco:dnac:securityadvisory scheduled daily; advisory counts and affected device lists exported to compliance dashboard."
    },
    {
      "regulation": "NIST-800-53",
      "version": "Rev. 5",
      "clause": "RA-5",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Contributes to vulnerability scanning by tracking known vendor-published vulnerabilities, though it does not replace active scanning tools.",
      "controlObjective": "Provides a vendor-authoritative vulnerability feed for network infrastructure to complement active vulnerability scanning.",
      "evidenceArtifact": "Dashboard panel showing advisory severity distribution and affected device counts, refreshed with each TA poll cycle."
    },
    {
      "regulation": "PCI-DSS",
      "version": "v4.0",
      "clause": "6.3.1",
      "mode": "satisfies",
      "assurance": "partial",
      "assurance_rationale": "Alerts on critical and high severity vendor security advisories, supporting PCI DSS requirements to identify and rank vulnerabilities.",
      "controlObjective": "Ensures critical vendor-published vulnerabilities are flagged within hours of detection by Catalyst Center.",
      "evidenceArtifact": "Alert action on UC-5.13.35 firing for severity CRITICAL/HIGH advisories; alert history in triggered alerts log."
    }
  ],
  "dataSources": "`index=catalyst sourcetype=cisco:dnac:securityadvisory`. from the `securityadvisory` modular input in the Cisco Catalyst Add-on (Splunkbase 7538) polling `GET /dna/intent/api/v1/security-advisory/advisory`. Default poll interval: 3600s (1h). Volume: ~1 event per advisory-device pair. RBAC: NETWORK-ADMIN-ROLE. Key fields: `advisoryId` (string), `advisoryTitle` (string), `severity` (string), `cveId` (string), `deviceCount` (integer), `deviceId` (string), `fixedVersions` (array of strings), `publicationDate` (string). CIM data model: Vulnerabilities.",
  "app": "`Cisco Catalyst Add-on for Splunk` (Splunkbase 7538)",
  "spl": "index=catalyst sourcetype=\"cisco:dnac:securityadvisory\" (severity=\"CRITICAL\" OR severity=\"HIGH\") | stats dc(deviceId) as affected_devices values(cveId) as cves latest(advisoryId) as advisory by severity, advisoryTitle | sort severity -affected_devices",
  "description": "Alerts on critical and high severity PSIRTs affecting managed devices, with the count of affected devices and associated CVEs.",
  "value": "Critical and high PSIRTs require immediate remediation. Alerting with affected device counts helps prioritize patching based on blast radius.",
  "implementation": "Enable the `securityadvisory` input. Use as a scheduled alert with low latency for CRITICAL, and include advisoryTitle and cves in the payload for analysts.",
  "visualization": "Table (affected_devices, cves, advisory by severity and title), top-N list for P1 remediation.",
  "references": [
    {
      "title": "Splunkbase app 7538",
      "url": "https://splunkbase.splunk.com/app/7538"
    },
    {
      "title": "Catalyst Center API docs",
      "url": "https://developer.cisco.com/docs/catalyst-center/"
    },
    {
      "title": "Catalyst Center Integration Guide",
      "url": "docs/guides/catalyst-center.md"
    },
    {
      "title": "Cisco PSIRT Security Advisories",
      "url": "https://sec.cloudapps.cisco.com/security/center/publicationListing.x"
    }
  ],
  "knownFalsePositives": "**Critical/High advisory affecting only lab or end-of-life devices.** A CRITICAL or HIGH PSIRT advisory may affect devices that are in a lab environment or have already been scheduled for decommissioning. Distinguish by checking whether the affected `deviceId` values correspond to devices in non-production `siteId` values or devices flagged for EOL. Suppress by maintaining a `catalyst_excluded_devices` lookup and filtering those devices from the critical alerting, while tracking them in a separate risk dashboard.\n\n**Advisory with compensating controls already in place.** The vulnerability may require specific network conditions (e.g., exposed management interface) that are already mitigated by ACLs, segmentation, or other compensating controls. Distinguish by reviewing the advisory's exploitation conditions and verifying whether the compensating controls exist in the network. Suppress by maintaining a `catalyst_advisory_exceptions` lookup with advisoryId, deviceName, compensating control, and review date.\n\n**Catalyst Center severity differs from published PSIRT base score.** Catalyst Center may assess a different severity than the published PSIRT based on local context (device exposure, software configuration). Distinguish by comparing the `severity` field in Splunk with the authoritative PSIRT listing at `https://sec.cloudapps.cisco.com/security/center/publicationListing.x`. No suppression needed — use the authoritative source for final risk assessment.\n\n**Advisory re-detected after Catalyst Center PSIRT database update.** A database update may re-surface previously assessed advisories. Distinguish by checking the advisory's `first_seen` timestamp in Splunk against the database update date. Suppress by tracking `| stats earliest(_time) as first_seen by advisoryId` and alerting only on advisories with `first_seen` within the current reporting window.",
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_catalyst_center"
  ],
  "detailedImplementation": "Prerequisites\n• Cisco Catalyst Add-on (7538) with **securityadvisory** input → `cisco:dnac:securityadvisory` in `index=catalyst`.\n• `GET /dna/intent/api/v1/security-advisory/advisory` (Catalyst/TA default ~**3600s** poll); confirm **Catalyst** and **Cisco** PSIRT are enabled for your account.\n• Run `fieldsummary` on `deviceId` vs `deviceName`—this SPL uses `deviceId`; if your build only has names, change the `dc()` field accordingly.\n• `docs/implementation-guide.md` and `docs/guides/catalyst-center.md`.\n\nStep 1 — Configure data collection\n• **Security Advisory** data is **Catalyst**-scoped to **managed** inventory; it does not replace org-wide **Qualys/Defender** coverage.\n• **Default interval:** one hour is typical; sub-hour often adds API load for little gain unless you have an emergency bridge.\n\nStep 2 — Create the search and **alert** (high priority)\n```spl\nindex=catalyst sourcetype=\"cisco:dnac:securityadvisory\" (severity=\"CRITICAL\" OR severity=\"HIGH\") | stats dc(deviceId) as affected_devices values(cveId) as cves latest(advisoryId) as advisory by severity, advisoryTitle | sort severity -affected_devices\n```\n\nUnderstanding this SPL (blast radius, not a CVSS calculator)\n**Critical/High PSIRT** — Triage list for **VulnMgmt**; combine with your **CAB** and **EOL** data before mandatory reboots in change freeze.\n• `sort severity -affected_devices` is a **heuristic**; **Cisco** may sort **severity** lexicographically in some UIs—verify **CRITICAL** > **HIGH** in your `sort` if you need strict ordering (sometimes use **eval** of numeric rank).\n\n**Pipeline walkthrough**\n• High/Critical only → per **`severity, advisoryTitle`** → **dc(deviceId)** and **values(cveId)** for ticket body.\n\nStep 3 — Validate\n• Open **Catalyst** (or the linked **Cisco** advisory) for one row: **`deviceCount` / affected list** and **CVEs** should align within one **poll**.\n• In Splunk, check whether multiple rows per `advisoryId` exist; if so, the **count** of raw events is **not** the same as **`dc(deviceId)`**—this is expected.\n• **Vendor UI parity:** open **Catalyst Center > Security > Advisories** for the same time window; verify critical/high advisory counts match.\nStep 4 — Operationalize (alerting)\n• **Schedule** every **hour** (or 15m during active PSIRT) with time range **Last 2h** to survive clock skew. **Throttle** to **1 ticket per `advisoryId` per 24h** with **separate** zero-results **recovery** in ITSM (optional).\n• **Enrich** with a **CMDB** lookup: filter `deviceId` to **prod**; send **P1** if **>0** **prod** and **0** test.\nStep 5 — Troubleshooting\n• **Fires on empty inventory:** `deviceId` may be null for some **informational** advisories—tighten `| where isnotnull(deviceId) AND affected_devices>0` if needed.\n• **Stuck old advisories:** `latest(advisoryId)` in this SPL is a **per-group** field—pair with a **Catalyst** **fixed** version field in a **v2** panel for **true** closure status.\n• **401/403 on collection:** re-check the **Catalyst** **API** **role** and **token**; review **Add-on** **logs** on the inputs host.\n• **Catalyst lags cisco.com:** treat **Cisco TAC/PSIRT** as final on **exploit** status when the portal and **Catalyst** differ.\n",
  "grandmaExplanation": "We immediately flag the most dangerous security vulnerabilities found on your network devices and tell you how many devices are at risk.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
