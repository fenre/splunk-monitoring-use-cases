{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.3.37",
  "title": "Citrix ADC Pooled Licensing Utilization",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Capacity",
    "Compliance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Citrix ADC Pooled Licensing Utilization' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_citrix_adc_pooled_licensing_utilization` (UC-5.3.37), SOC dashboard panel \"Operations — Citrix ADC Pooled Licensing Utilization\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.3.37 positive (Citrix, ADC, Pooled, Licensing, Utilization): ingest synthetic events into `index=netscaler` sourcetype='citrix:netscaler:syslog' where `max_pool`, `peak_vcpu`, `peak_thr` grouped by `_time`, `host`, `pool_name` satisfies `pool_pct` > 90, or `max_pool` > 80 for 'Citrix ADC Pooled Licensing Utilization'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.3.37 negative (Licensing Utilization): ingest benign `netscaler` / `citrix:netscaler:syslog` events where `_time`, `host`, `pool_name` stays below `pool_pct` > 90, or `max_pool` > 80, or exclude the test entity via `ops_exception_register.csv`. Variant 10 confirms zero alert rows for 'Citrix ADC Pooled Licensing Utilization'."
  },
  "dataSources": "`index=netscaler` `sourcetype=\"citrix:netscaler:syslog\"` or `sourcetype=\"citrix:netscaler:perf\"` from NITRO — `license` pool usage percent, vCPU, throughput entitlement, `pool` name, `grace` or `expir` Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Add-on for Citrix NetScaler (Splunk_TA_citrix-netscaler) — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=netscaler (sourcetype=\"citrix:netscaler:syslog\" OR sourcetype=\"citrix:netscaler:perf\") (\"pooled\" OR \"license pool\" OR vCPU OR throughput OR entitlement OR CBM OR \"ceiling\" OR grace OR expir)\n| eval pool_pct=coalesce(license_pool_use_pct, pooled_license_use_pct, 0), vcpu=coalesce(allocated_vcpus, 0), thr_mbps=coalesce(throughput_mbps, 0)\n| eval capacity_flag=if(pool_pct>90 OR match(_raw,\"(?i)grace|violation|denied\"),1,0)\n| bin _time span=1h\n| stats max(pool_pct) as max_pool, max(vcpu) as peak_vcpu, max(thr_mbps) as peak_thr, max(capacity_flag) as risk by _time, host, pool_name\n| where max_pool>80 OR risk=1\n| table _time, host, pool_name, max_pool, peak_vcpu, peak_thr, risk",
  "description": "Surfaces UC-5.3.37 — Citrix, ADC, Pooled, Licensing, Utilization — using `index=netscaler` sourcetype `citrix:netscaler:syslog`, grouped by `_time`, `host`, `pool_name`. Outputs `max_pool`, `peak_vcpu`, `peak_thr` each `1h`; alert when `pool_pct` > 90, or `max_pool` > 80. Primary focus: Licensing Utilization. Purpose-built for 'Citrix ADC Pooled Licensing Utilization' in Operations workflows.",
  "value": "For 'Citrix ADC Pooled Licensing Utilization', UC-5.3.37 converts `max_pool`, `peak_vcpu`, `peak_thr` on `citrix:netscaler:syslog` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Ingest NITRO license and pooled-capacity output via TA scripted input into `citrix:netscaler:perf` and capture syslog warnings from `ns` `license` `pool`. Set thresholds at 80% (planning) and 90% (urgent) for pool use. Log proof-of-use reports with Splunk for quarterly true-ups. Add alerts for grace-period entry or license server connectivity loss (where applicable).",
  "visualization": "Gauge: pool use percent, line chart: vCPU and throughput, table: top consumers by `host` and `pool_name`.",
  "cimModels": [],
  "references": [
    {
      "title": "Splunk Documentation: Splunk Add-on for Citrix NetScaler",
      "url": "https://docs.splunk.com/Documentation/AddOns/released/CitrixNetScaler/CitrixNetScaler",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Citrix — Licensing and pooled capacity",
      "url": "https://www.citrix.com/support/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `netscaler` (UC-5.3.37)** — Planned work for 'Citrix ADC Pooled Licensing Utilization' shifts `max_pool`, `peak_vcpu`, `peak_thr` over `span=1h` while `_time`, `host`, `pool_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `max` on `citrix:netscaler:syslog`** — Scheduled automation elevates `host` without crossing `pool_pct` > 90 for UC-5.3.37. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `_time`, `host`, `pool_name`** — Health checks in `netscaler` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `citrix:netscaler:syslog`** — First 48h baseline for UC-5.3.37 on `_time` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `_time`, `exception_reason`, `valid_until`.",
  "equipment": [
    "citrix",
    "syslog"
  ],
  "equipmentModels": [
    "citrix_netscaler"
  ],
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n* Splunk Add-on for Citrix NetScaler (`Splunk_TA_citrix-netscaler`, Splunkbase 2770). Citrix ADC pooled licensing data from Citrix ADM or NITRO stats. Key fields: `license_type`, `allocated_bandwidth`, `used_bandwidth`, `available_bandwidth`, `instance_count`, `expiry_date`.\n* Citrix Pooled Licensing: instead of per-appliance licenses, bandwidth is allocated from a central pool in Citrix ADM. Instances check out bandwidth on startup. If the pool is exhausted, new instances can't start or existing instances may lose bandwidth.\n\nStep 1 -- Configure data collection\nCitrix ADM manages the license pool. Poll ADM API or collect ADM syslog. Verify:\n```spl\nindex=netscaler (sourcetype=\"citrix:netscaler:syslog\" OR sourcetype=\"citrix:netscaler:perf\") (\"license\" OR \"bandwidth\" OR \"pool\" OR \"expir\") earliest=-7d\n| where match(_raw, \"(?i)(license|bandwidth|pool|allocat)\")\n| stats count by host\n```\n\nStep 2 -- Create the search and alert\n\n**Primary search -- License pool utilization:**\n```spl\nindex=netscaler (sourcetype=\"citrix:netscaler:syslog\" OR sourcetype=\"citrix:netscaler:perf\") (\"license\" OR \"bandwidth pool\" OR \"pooled\") earliest=-4h\n| eval total_bw=coalesce(allocated_bandwidth, total_pool_bandwidth)\n| eval used_bw=coalesce(used_bandwidth, consumed_bandwidth)\n| eval available=coalesce(available_bandwidth, remaining_bandwidth)\n| eval instances=coalesce(instance_count, licensed_instances)\n| eval expiry=coalesce(expiry_date, license_expiry)\n| stats latest(total_bw) as total latest(used_bw) as used latest(available) as available latest(instances) as instances latest(expiry) as expiry by host\n| eval utilization_pct=if(total > 0, round(100*used/total, 1), null())\n| eval days_to_expiry=if(isnotnull(expiry), round((strptime(expiry, \"%Y-%m-%d\") - now()) / 86400, 0), null())\n| eval status=case(utilization_pct > 90, \"CRITICAL -- pool nearly exhausted\", utilization_pct > 75, \"WARNING -- high utilization\", days_to_expiry < 30 AND isnotnull(days_to_expiry), \"WARNING -- license expiring soon\", 1==1, \"OK\")\n| where status != \"OK\"\n```\n\nStep 3 -- Validate\n(a) Check Citrix ADM: Infrastructure > Pooled Licensing -- compare utilization.\n(b) Verify each instance's allocated bandwidth: ADM > Infrastructure > Instances.\n(c) Check license expiry dates.\n\nStep 4 -- Operationalize\nDashboard (\"Citrix ADC -- License Pool\"):\n* Row 1 -- Single-value: \"Pool utilization %\", \"Total bandwidth (Gbps)\", \"Available (Gbps)\", \"Instances\", \"Days to expiry\".\n* Row 2 -- License status with expiry alerts.\n\nAlerting:\n* Critical (pool utilization > 90%): cannot provision new instances.\n* Warning (license expiring < 30 days): renewal needed.\n\nStep 5 -- Troubleshooting\n\n* **Pool exhausted** -- Instances may have checked out more bandwidth than needed. Review per-instance allocation in ADM and reduce over-provisioned instances.\n\n* **Instance can't start** -- Insufficient pool bandwidth. Either free bandwidth from another instance or purchase additional pool capacity.\n\n* **License expired** -- Grace period typically applies. Contact Citrix for renewal. Instances will continue running but may lose features.",
  "grandmaExplanation": "We look at shared license use over time on the same platform so a quiet threshold day does not turn into a hard stop with no warning.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
