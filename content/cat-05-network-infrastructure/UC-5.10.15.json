{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.10.15",
  "title": "Carrier BGP Session Failover and Prefix Acceptance Tracking",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Availability",
    "Resilience"
  ],
  "splunkPillar": "Observability",
  "industry": "Telecommunications",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'Carrier BGP Session Failover and Prefix Acceptance Tracking'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-5.10.15).",
  "evidence": "Saved search uc_5_10_15_carrier_bgp_session_failover_and_prefix, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'Carrier BGP Session Failover and Prefix Acceptance Tracking'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-5.10.15.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "`index=network sourcetype=snmp:cisco_bgp` (or vendor-normalized `snmp:bgpPeerEntry`) exposing `bgpPeerRemoteAddr`, `bgpPeerState` (IANA: 6=Established), `cbgpPeerAcceptedPrefixes` / `cbgpPeerDeniedPrefixes` from CISCO-BGP4-MIB; correlated syslog `%BGP-5-ADJCHANGE` (`sourcetype=cisco:ios`) for flap timeline. Lookup `carrier_bgp_peers.csv` stores `peer_ip`, `carrier`, `site`, `baseline_accepted_min`, `criticality_tier`, optional `max_transition_delta` (SNMP counter threshold for `bgpPeerFsmEstablishedTransitions` spikes). Collected via Splunk Add-on for Cisco Security (Splunkbase 1352) modular input or scripted poll into the index named in the SPL.",
  "app": "SNMP Modular Input with CISCO-BGP4-MIB walks plus Cisco IOS syslog forwarding. — Splunk Add-on for Cisco Security (Splunkbase 1352)",
  "spl": "index=network sourcetype=\"snmp:cisco_bgp\" earliest=-30m\n| stats latest(bgpPeerState) as peer_state latest(cbgpPeerAcceptedPrefixes) as accepted latest(cbgpPeerDeniedPrefixes) as denied latest(bgpPeerFsmEstablishedTransitions) as transitions by host, bgpPeerRemoteAddr\n| lookup carrier_bgp_peers.csv peer_ip as bgpPeerRemoteAddr OUTPUT carrier site baseline_accepted_min criticality_tier max_transition_delta\n| eval peer_state_num=tonumber(peer_state)\n| eval established=(peer_state_num==6 OR lower(peer_state)==\"established\")\n| eval prefix_gap=isnotnull(baseline_accepted_min) AND accepted < baseline_accepted_min\n| eval session_alarm=NOT established\n| eval transition_spike=isnotnull(max_transition_delta) AND transitions>max_transition_delta\n| where session_alarm OR prefix_gap OR transition_spike\n| eval severity=case(prefix_gap AND session_alarm,\"critical\", session_alarm,\"high\", prefix_gap,\"medium\", transition_spike,\"medium\", true(), \"low\")\n| table host bgpPeerRemoteAddr carrier site peer_state accepted denied transitions baseline_accepted_min severity criticality_tier\n| sort peer_state accepted",
  "description": "Combines SNMP BGP finite-state-machine status with accepted-prefix counters on carrier-facing peers—detecting simultaneous session impairment and silent partial reroutes when upstream filters shrink announcements without fully tearing sessions.",
  "value": "Architecture teams prove whether redundant uplinks actually carried expected prefixes after failover drills or carrier policy tweaks—closing blind spots where BGP stays Established yet accepted routes drop below engineered minima.",
  "implementation": "Poll SNMP every 60–120s; baseline `baseline_accepted_min` using steady-state observations per season; pair with syslog flap detection for root-cause narratives; throttle alerts on maintenance-flagged peers via lookup column.",
  "visualization": "Dual-axis chart of accepted prefixes and peer_state code; sankey from carrier→site→peer list filtered to alarms; single-value of Established peers below quota.",
  "cimModels": [],
  "references": [
    {
      "title": "CISCO-BGP4-MIB — BGP peer metrics reference",
      "url": "https://www.cisco.com/c/en/us/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Lantern — SNMP onboarding best practices",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "cisco",
    "snmp",
    "syslog"
  ],
  "equipmentModels": [
    "cisco_ios",
    "cisco_isr_4000",
    "snmp_generic"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• CISCO-BGP4-MIB enabled on PE devices with SNMP community or token scoped read-only.\n• Known-good accepted-prefix counts per peer reflecting full IPv4/IPv6 families—maintain separate rows per AFI when dual-stack.\n• Splunk Transform guaranteeing numeric typing (`accepted`, `denied`).\n• Capacity planning for SNMP walks across hundreds of peers—consider Directed SNMP proxies.\n\nStep 1 — Normalize oid outputs into `snmp:cisco_bgp` sourcetype via TA props/transforms.\n\nStep 2 — Populate baseline CSV using thirty-day percentiles excluding maintenance spikes.\n\nStep 3 — Compose alerting: critical merges session_alarm + prefix_gap; informational logs gradual monotone declines >15% without crossing absolute floor.\n\nStep 4 — Visualization overlays transitions counter deltas versus syslog-derived flap intervals.\n\nStep 5 — Troubleshooting: vendor MIB omissions omit prefix counters—fallback to streaming telemetry gRPC export; vrf-aware peers require appended rd keys in lookup schema.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (SNMP Modular Input with CISCO-BGP4-MIB walks plus Cisco IOS syslog forwarding. — Splunk Add-on for Cisco Security (Splunkbase 1352)). Enable the modular input or HEC token that writes sourcetype=snmp:cisco_bgp into network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`bgpPeerRemoteAddr, carrier site baseline_accepted_min criticality_tier max_transition_delta, host, peer_ip`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We check both whether the handshake line to our internet supplier stays up and whether we still receive the full bundle of directions we expect, so a half-working backup link cannot hide quietly.",
  "prerequisiteUseCases": [],
  "telcoUseCase": "WAN BGP resilience validation",
  "wave": "walk"
}
