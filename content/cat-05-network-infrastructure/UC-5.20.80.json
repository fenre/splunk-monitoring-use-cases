{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.20.80",
  "title": "RFC 8504 IPv6 Node Requirements Compliance Audit",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers the detection logic documented for 'RFC 8504 IPv6 Node Requirements Compliance Audit' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_rfc_8504_ipv6_node_requirements_compliance_audit` (UC-5.20.80), SOC dashboard panel \"Operations — RFC 8504 IPv6 Node Requirements Compliance Audit\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.20.80 positive (RFC, 8504, IPv6, Node, Requirements): ingest synthetic events into `index=network` sourcetype='cisco:ios' where `slaac`, `eui64`, `dad` grouped by `host` satisfies -30d for 'RFC 8504 IPv6 Node Requirements Compliance Audit'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.20.80 negative (Compliance Audit): ingest benign `network` / `cisco:ios` events where `host` stays below -30d, or exclude the test entity via `ops_exception_register.csv`. Variant 4 confirms zero alert rows for 'RFC 8504 IPv6 Node Requirements Compliance Audit'."
  },
  "dataSources": "`index=network sourcetype=cisco:ios` or `sourcetype=syslog` — host and router behaviour validated against RFC 8504 (IPv6 Node Requirements, obsoletes RFC 6434). RFC 8504 specifies the minimum protocol support every IPv6 host and router MUST implement. Non-compliant devices may cause subtle connectivity failures. Key requirements:\n\n**Host requirements (§5):**\n• MUST support SLAAC (RFC 4862) and generate SLAAC addresses from stable identifiers (RFC 7217), NOT EUI-64\n• MUST support DHCPv6 client (at minimum stateless, RFC 8415)\n• MUST perform DAD before using any address\n• MUST process ICMPv6 Packet Too Big (Type 2) for PMTUD\n• MUST support NDP (NS/NA/RS/RA processing)\n• SHOULD implement first-hop security mechanisms\n\n**Router requirements (§6):**\n• MUST implement NDP router behaviour (RFC 4861)\n• MUST support prefix advertisement via RA\n• MUST implement ICMPv6 error generation\n• MUST support IPv6 forwarding with proper hop limit decrement\n• SHOULD implement RA Guard on access-facing interfaces Collected via Splunk Add-on for Cisco Security (Splunkbase 1352) modular input or scripted poll into the index named in the SPL.",
  "app": "Multiple — compliance verification across all IPv6 devices — Splunk Add-on for Cisco Security (Splunkbase 1352)",
  "spl": "index=network (sourcetype=\"cisco:ios\" OR sourcetype=\"syslog\" OR sourcetype=\"cisco:iosxe\") earliest=-30d\n| eval has_slaac=if(match(_raw, \"(?i)SLAAC|autoconfig|ipv6 address autoconfig\"), 1, 0)\n| eval has_eui64=if(match(_raw, \"(?i)eui-64|EUI64\"), 1, 0)\n| eval has_dad=if(match(_raw, \"(?i)DAD|duplicate.address|dad-attempts\"), 1, 0)\n| eval has_pmtud=if(match(_raw, \"(?i)packet.too.big|icmpv6.*type.?2|path.mtu\"), 1, 0)\n| eval has_ndp=if(match(_raw, \"(?i)neighbor.discovery|ipv6.nd|NDP\"), 1, 0)\n| stats max(has_slaac) as slaac max(has_eui64) as eui64 max(has_dad) as dad max(has_pmtud) as pmtud max(has_ndp) as ndp by host\n| eval rfc8504_issues=mvappend(\n    if(has_eui64=1, \"EUI-64 in use — RFC 8504 §5.1 requires RFC 7217 stable identifiers\", null()),\n    if(slaac=0 AND ndp=0, \"No SLAAC or NDP evidence — possible IPv6 misconfiguration\", null()),\n    if(dad=0, \"No DAD evidence — RFC 8504 §5 requires DAD\", null()))\n| where isnotnull(rfc8504_issues)\n| table host, eui64, slaac, dad, pmtud, ndp, rfc8504_issues\n| sort -eui64",
  "description": "Monitors risk tied to RFC, 8504, IPv6, Node, Requirements through UC-5.20.80 on `index=network` sourcetype `cisco:ios`. Summarises `slaac`, `eui64`, `dad` grouped by `host` across `the scheduled window` before applying -30d. Operators tune 'RFC 8504 IPv6 Node Requirements Compliance Audit' using UC-5.20.80 runbook steps.",
  "value": "Tracking `slaac`, `eui64`, `dad` via UC-5.20.80 on `cisco:ios` feeds improves response to 'RFC 8504 IPv6 Node Requirements Compliance Audit' and reduces operational risk and delayed incident response.",
  "implementation": "Analyse syslog, configuration, and flow data for evidence of RFC 8504-compliant behaviour. Flag EUI-64 address usage, missing DAD, and PMTUD failures. Report non-compliant devices for remediation.",
  "visualization": "(1) Single-value: EUI-64 device count (target: 0). (2) Table: non-compliant devices with specific violations. (3) Pie chart: address generation method distribution (EUI-64 vs stable identifiers vs privacy extensions). (4) Trend: EUI-64 elimination progress over time.",
  "references": [
    {
      "title": "RFC 8504 — IPv6 Node Requirements (current, replaces RFC 6434)",
      "url": "https://www.rfc-editor.org/rfc/rfc8504",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RFC 7217 — A Method for Generating Semantically Opaque Interface Identifiers with IPv6 SLAAC",
      "url": "https://www.rfc-editor.org/rfc/rfc7217",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RFC 8064 — Recommendation on Stable IPv6 Interface Identifiers (deprecates EUI-64)",
      "url": "https://www.rfc-editor.org/rfc/rfc8064",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `network` (UC-5.20.80)** — Planned work for 'RFC 8504 IPv6 Node Requirements Compliance Audit' shifts `slaac`, `eui64`, `dad` over `span=1h` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `max` on `cisco:ios`** — Scheduled automation elevates `has_slaac` without crossing configured threshold for UC-5.20.80. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `network` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `cisco:ios`** — First 48h baseline for UC-5.20.80 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco",
    "juniper",
    "linux",
    "paloaltonetworks",
    "syslog",
    "windows"
  ],
  "equipmentModels": [
    "cisco_ios",
    "cisco_iosxe",
    "linux_server",
    "windows_server"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• IPv6 syslog and configuration data from hosts and routers.\n• NDP cache polling (UC-5.20.16) for address pattern analysis.\n• Understanding of RFC 8504 requirement categories (MUST, SHOULD, MAY).\n\nStep 1 — Configure data collection\n\n**EUI-64 detection from NDP cache or flow data:**\n```spl\nindex=network (sourcetype=\"netflow\" OR sourcetype=\"cisco:ios\") earliest=-24h\n| rex field=_raw \"(?<ipv6_addr>[0-9a-fA-F]{1,4}(:[0-9a-fA-F]{1,4}){7})\"\n| rex field=ipv6_addr \"::?[0-9a-fA-F]{1,4}:(?<iid_high>[0-9a-fA-F]{1,4}):(?<iid_mid>[0-9a-fA-F]{1,4}):(?<iid_low>[0-9a-fA-F]{1,4})$\"\n| eval is_eui64=if(match(iid_mid, \"(?i)^ff[Ff][Ee]$\"), 1, 0)\n| where is_eui64=1\n| eval embedded_mac=replace(iid_high . \":\" . \"ff:fe:\" . iid_low, \"(..)(..)\", \"\\1:\\2\")\n| stats count as events dc(ipv6_addr) as unique_addresses by host, embedded_mac\n| sort -unique_addresses\n```\nEUI-64 addresses contain `ff:fe` in the middle of the 64-bit interface identifier. This pattern uniquely identifies EUI-64 usage.\n\n**Verification:**\n```spl\nindex=network sourcetype=\"cisco:ios\" \"%IPV6-4-DUPLICATE\" | stats count by host\n```\nThis confirms DAD is operational and logging on devices where it triggers.\n\nStep 2 — Create compliance assessment\n\n**RFC 8504 compliance scorecard:**\n```spl\n| inputlookup ipv6_device_inventory.csv\n| eval slaac_compliant=if(address_method!=\"eui64\", 1, 0)\n| eval dad_enabled=if(dad_status=\"enabled\", 1, 0)\n| eval pmtud_working=if(pmtud_status=\"working\", 1, 0)\n| eval stable_identifiers=if(address_method=\"rfc7217\" OR address_method=\"privacy_extensions\", 1, 0)\n| eval score=slaac_compliant + dad_enabled + pmtud_working + stable_identifiers\n| eval grade=case(\n    score=4, \"FULLY COMPLIANT\",\n    score=3, \"MOSTLY COMPLIANT\",\n    score <= 2, \"NON-COMPLIANT\")\n| table host, device_type, address_method, dad_status, pmtud_status, grade\n| sort grade\n```\n\nStep 3 — Validate\n(a) **EUI-64 verification.** On a sample device using EUI-64 (ff:fe in IID), verify the MAC address extraction matches the actual interface MAC.\n\n(b) **DAD test.** On a test segment, configure two hosts with the same IPv6 address. Verify `%IPV6-4-DUPLICATE` messages appear in Splunk.\n\n(c) **PMTUD test.** From a test host, `ping6 -s 1500 -M do <remote>` across a path with lower MTU. Verify ICMPv6 Packet Too Big is received and path MTU adapts.\n\nStep 4 — Operationalize\n\n**Dashboard** (\"IPv6 — RFC 8504 Node Compliance\"):\n• Row 1 — Single-value: EUI-64 device count (target: 0), non-compliant device count.\n• Row 2 — Pie chart: address generation method distribution.\n• Row 3 — Table: non-compliant devices with specific RFC 8504 violations.\n• Row 4 — Trend: EUI-64 elimination progress over 12 months.\n\n**Alert:** New EUI-64 address detected — trigger on first occurrence of ff:fe pattern in new source address.\n\n**Runbook:**\n1. EUI-64 on Windows: Verify `netsh interface ipv6 set global randomizeidentifiers=enabled` is set.\n2. EUI-64 on Linux: Verify `net.ipv6.conf.all.addr_gen_mode=3` (stable-privacy, RFC 7217).\n3. EUI-64 on Cisco: Configure `ipv6 address autoconfig default` which uses RFC 7217 on modern IOS versions.\n4. EUI-64 on macOS: Privacy extensions are enabled by default since macOS 10.12.\n\nStep 5 — Troubleshooting\n\n• **Difficulty distinguishing address generation methods.** RFC 7217 stable identifiers look random but are deterministic. Privacy extensions (RFC 8981) are random and rotate. EUI-64 contains ff:fe. Only EUI-64 can be definitively identified by pattern; stable vs privacy requires tracking address persistence over time.\n\n• **Link-local EUI-64.** Many devices use EUI-64 for link-local addresses even when using stable identifiers for global addresses. Link-local EUI-64 is lower risk (not routed) but still reveals MAC to local segment.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Multiple — compliance verification across all IPv6 devices — Splunk Add-on for Cisco Security (Splunkbase 1352)). Enable the modular input or HEC token that writes sourcetype=cisco:ios, syslog, cisco:iosxe into network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`has_dad, has_eui64, has_ndp, has_pmtud`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "There's a rule book (RFC 8504) that says every device on the new postal system (IPv6) must follow certain minimum standards — like having a proper letterbox, checking for duplicate addresses, and not putting your home address on the outside of every letter. We check each device against the rule book and flag any that aren't following the minimum standards.",
  "prerequisiteUseCases": [
    "UC-5.20.13",
    "UC-5.20.16"
  ],
  "wave": "run"
}
