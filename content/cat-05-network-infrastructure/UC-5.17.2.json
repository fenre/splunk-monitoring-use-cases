{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.17.2",
  "title": "Packet Drop Rate on Filter/Map Rules",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Fault",
    "Data Quality",
    "Operations"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Packet Drop Rate on Filter/Map Rules' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_packet_drop_rate_on_filter_map_rules` (UC-5.17.2), SOC dashboard panel \"Operations — Packet Drop Rate on Filter/Map Rules\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=visibility` sourcetype='target_sourcetype' where `_time` satisfies `peak_pps` > 1000 for UC-5.17.2 ('Packet Drop Rate on Filter/Map Rules'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `visibility` / `target_sourcetype` events where `_time` stays below `peak_pps` > 1000, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-5.17.2."
  },
  "dataSources": "Detailed: `index=visibility` / `index=network`; `sourcetype` patterns — `gigamon:map`, `gigamon:stats`, `keysight:filter`, `ixia:rule`, `apcon:policy`; syslog traps carrying `drop_pkts`, `drop_pct`, `rule_id`, `map_name`, `cause_buffer_full|acl_miss|parser_error`. Prefer counters normalized to drops-per-second or drops-per-million packets for comparability across chassis generations. Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "Vendor REST/syslog integrations for Gigamon Flow Mapping/GigaVUE OS, Keysight Vision filters, APCON policy engine; optional Splunk Supporting Add-on for SNMP when drops exported only as MIB counters. — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=visibility OR index=network earliest=-8h\n| eval v=lower(sourcetype)\n| eval vendor=case(match(v,\"gigamon\"),\"Gigamon\",match(v,\"keysight|ixia|vision\"),\"Keysight\",match(v,\"apcon\"),\"APCON\",\"other\")\n| where vendor!=\"other\"\n| eval rule_key=coalesce(rule_id,map_name,filter_name,policy_rule,\"unknown\")\n| eval drops_pps=tonumber(coalesce(drop_pps,drops_per_sec,packet_drop_rate))\n| eval drops_total=tonumber(coalesce(drop_pkts,dropped_packets,map_drop_count))\n| eval pmpp=tonumber(coalesce(drops_per_million,dpm))\n| bin _time span=5m\n| stats sum(drops_total) as sum_drops max(drops_pps) as peak_pps avg(pmpp) as avg_pmpp dc(host) as chassis_hits by _time vendor rule_key\n| eval severity=case(peak_pps>1000 OR avg_pmpp>500,\"critical\",peak_pps>200 OR avg_pmpp>100,\"high\",peak_pps>0 OR sum_drops>0,\"medium\",\"info\")\n| where severity IN (\"critical\",\"high\")\n| sort - peak_pps vendor rule_key _time",
  "description": "Five-minute Splunk rollups attribute rising packet discard counters to specific broker filter or map identifiers so engineering teams know whether buffering, ACL tightening, or malformed rules—not vague chassis alarms—drive visibility loss.",
  "value": "Five-minute Splunk rollups attribute rising packet discard counters to specific broker filter or map identi.... Timely UC-5.17.2 alerting on 'Packet Drop Rate on Filter/Map Rules' reduces operational risk and delayed incident response before manual triage or audit channels escalate.",
  "implementation": "Ensure every drop-bearing event carries stable `rule_key`; baseline benign drops with a weekly median lookup; alert only when peak_pps or drops-per-million exceeds curated thresholds per vendor tier.",
  "visualization": "Heatmap of sum_drops by rule_key vs `_time`; companion single-value for critical severity count; table drilldown showing vendor, chassis, and linked change ticket.",
  "references": [
    {
      "title": "Splunk Documentation — stats command reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/Stats",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Gigamon Documentation — Visibility and analytics nodes",
      "url": "https://docs.gigamon.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "APCON — Network visibility and security fabric",
      "url": "https://www.apcon.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `visibility`** — Planned work for 'Packet Drop Rate on Filter/Map Rules' shifts `sum` over `span=5m` while `_time` remains in-scope for UC-5.17.2. Cross-check CMDB CHG.\n\n2. **Batch `sum` on `configured_sourcetype`** — Scheduled automation elevates `vendor` without crossing `peak_pps` > 1000. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `_time`** — Health checks in `visibility` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for `_time` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `_time`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "N/A (fault monitoring)"
  ],
  "detectionType": "Anomaly",
  "securityDomain": "network",
  "equipment": [
    "apc_dc",
    "apcon",
    "cpacket",
    "garland",
    "gigamon",
    "keysight",
    "snmp",
    "syslog"
  ],
  "equipmentModels": [
    "apcon_visibility_platform",
    "cpacket_cstor",
    "garland_network_tap",
    "gigamon_gigavue",
    "keysight_vision",
    "snmp_apc",
    "snmp_generic"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Exported rule catalog (`rule_key`, owner, business justification, expected drop budget).\n• Understanding whether counters reset on rule edits—document rollover semantics per vendor OS.\n• Capacity for five-minute retention searches without dominating search quota.\n\nStep 1 — Configure data collection\nTurn on structured syslog or REST polling that emits both instantaneous and cumulative drop counters; prefer timestamps from the appliance, not the collector edge.\n\nStep 2 — Normalize counters\nConvert burst counters to `drops_pps` using adjacent samples where vendors only publish totals; guard divide-by-zero when intervals stall.\n\nStep 3 — Build saved search\nMaterialize `pktbrk_map_drop_hotrules` with acceleration-friendly summary when cardinality explodes beyond fifty thousand rule keys—pre-aggregate per chassis hourly.\n\nStep 4 — Validate\nInject lab traffic through a sacrificial map known to drop synthetic flows; confirm Splunk surfaces the expected `rule_key` within two poll intervals.\n\nStep 5 — Operationalize\nWire alerts to SecOps with direct links to vendor edit screens; attach recommended actions (widen buffer, split map, add tool port) based on historical runbooks stored in a Splunk lookup.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Vendor REST/syslog integrations for Gigamon Flow Mapping/GigaVUE OS, Keysight Vision filters, APCON policy engine; optional Splunk Supporting Add-on for SNMP when drops exported only as MIB counters. — Splunk Add-on for Unix and Linux (Splunkbase 833)). Enable the modular input or HEC token that writes sourcetype=see SPL into visibility, network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, avg_pmpp, peak_pps, rule_key`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Sometimes the copying machinery quietly throws away little pieces of traffic when rules are too tight or boxes run out of room. We track which rule caused the waste so the right person can fix it before our watchers miss something real.",
  "prerequisiteUseCases": [],
  "wave": "walk"
}
