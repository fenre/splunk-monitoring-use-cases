{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.20.83",
  "title": "Dual-Stack VPN IPv6 Traffic Leakage Detection (RFC 7359)",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers the detection logic documented for 'Dual-Stack VPN IPv6 Traffic Leakage Detection (RFC 7359)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_dual_stack_vpn_ipv6_traffic_leakage_detection_rf` (UC-5.20.83), SOC dashboard panel \"Operations — Dual-Stack VPN IPv6 Traffic Leakage Detection (RFC 7359)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.20.83 positive (Dual, Stack, VPN, IPv6, Traffic): ingest synthetic events into `index=network` sourcetype='pan:traffic' where `unique_dests`, `leaked_bytes` grouped by `src`, `vpn_user`, `egress` satisfies -4h for 'Dual-Stack VPN IPv6 Traffic Leakage Detection (RFC 7359)'. Validate lookup `vpn_client_assignments.csv`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.20.83 negative ((RFC 7359)): ingest benign `network` / `pan:traffic` events where `src`, `vpn_user`, `egress` stays below -4h, or exclude the test entity via `ops_exception_register.csv`. Variant 9 confirms zero alert rows for 'Dual-Stack VPN IPv6 Traffic Leakage Detection (RFC 7359)'."
  },
  "dataSources": "`index=network sourcetype=pan:traffic` or `sourcetype=netflow` — detection of IPv6 traffic bypassing VPN tunnels. RFC 7359 documents a critical security issue: when a device connects to a VPN, the VPN client typically captures all IPv4 traffic and routes it through the encrypted tunnel. However, many VPN clients do NOT capture IPv6 traffic. This means:\n\n1. **DNS leakage.** The device's OS sends AAAA DNS queries over the local (non-VPN) IPv6 connection, exposing browsing activity to the local network.\n2. **Split tunneling by default.** IPv6 traffic goes directly to the internet while IPv4 goes through the VPN, creating a surveillance-free path.\n3. **Happy Eyeballs makes it worse.** RFC 8305 connection racing means the OS will prefer IPv6 (the non-VPN path) when an AAAA record exists, bypassing the VPN for dual-stack services.\n4. **Coffee shop attacks.** An attacker providing IPv6 connectivity via rogue RA can redirect all dual-stack traffic away from the VPN tunnel.\n\n**Detection approach:** Compare IPv6 traffic sources before/during VPN sessions. IPv6 traffic from a VPN client's physical interface (not the VPN tunnel interface) during an active VPN session indicates leakage. Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Palo Alto Networks Add-on / Cisco Add-on / NetFlow Add-on — Universal Forwarder (Splunkbase 617)",
  "spl": "index=network (sourcetype=\"pan:traffic\" OR sourcetype=\"netflow\") earliest=-4h\n| eval is_ipv6=if(match(src, \":\") OR match(dest, \":\"), 1, 0)\n| where is_ipv6=1\n| lookup vpn_client_assignments.csv src_ip as src OUTPUT vpn_user, vpn_status, tunnel_interface\n| where vpn_status=\"connected\" AND isnotnull(vpn_user)\n| eval egress=coalesce(egress_interface, outbound_if)\n| eval leaking=if(egress != tunnel_interface, 1, 0)\n| where leaking=1\n| stats count as leaked_flows dc(dest) as unique_dests sum(bytes) as leaked_bytes by src, vpn_user, egress\n| eval alert=\"VPN user \" . vpn_user . \" has \" . leaked_flows . \" IPv6 flows (\" . round(leaked_bytes/1024, 0) . \" KB) bypassing VPN tunnel via \" . egress\n| sort -leaked_flows",
  "description": "Identifies UC-5.20.83 — Dual, Stack, VPN, IPv6, Traffic — using `index=network` sourcetype `pan:traffic`, grouped by `src`, `vpn_user`, `egress`. Outputs `unique_dests`, `leaked_bytes` each `the scheduled window`; alert when -4h. Filter chain includes lookup `vpn_client_assignments.csv`. Purpose-built for 'Dual-Stack VPN IPv6 Traffic Leakage Detection (RFC 7359)' in Operations workflows.",
  "value": "Operators running UC-5.20.83 on `pan:traffic` feeds shorten triage for (RFC 7359) tied to 'Dual-Stack VPN IPv6 Traffic Leakage Detection (RFC 7359)', lowering operational risk and delayed incident response.",
  "implementation": "Correlate VPN session data with IPv6 flow data. Identify IPv6 flows from VPN clients that exit through the physical interface rather than the VPN tunnel interface. Alert on any IPv6 traffic that bypasses the VPN.",
  "visualization": "(1) Single-value: number of VPN users with IPv6 leakage. (2) Table: per-user leakage detail with flow count and data volume. (3) Timechart: leaked IPv6 flows over time. (4) Pie chart: leaked traffic by destination category.",
  "references": [
    {
      "title": "RFC 7359 — Layer 3 Virtual Private Network (VPN) Tunnel Traffic Leakages in Dual-Stack Hosts/Networks",
      "url": "https://www.rfc-editor.org/rfc/rfc7359",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RFC 8305 — Happy Eyeballs Version 2 (exacerbates VPN leakage by preferring IPv6)",
      "url": "https://www.rfc-editor.org/rfc/rfc8305",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RFC 9099 — Operational Security Considerations for IPv6 Networks (§2.7.3 — VPN dual-stack considerations)",
      "url": "https://www.rfc-editor.org/rfc/rfc9099",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `network` (UC-5.20.83)** — Planned work for 'Dual-Stack VPN IPv6 Traffic Leakage Detection (RFC 7359)' shifts `unique_dests`, `leaked_bytes` over `span=1h` while `src`, `vpn_user`, `egress` remains in-scope. Applies lookup `vpn_client_assignments.csv`. Cross-check CMDB CHG.\n\n2. **Batch `dc` on `pan:traffic`** — Scheduled automation elevates `vpn_user` without crossing configured threshold for UC-5.20.83. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `src`, `vpn_user`, `egress`** — Health checks in `network` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `pan:traffic`** — First 48h baseline for UC-5.20.83 on `src` is unstable; join `vpn_client_assignments.csv` prior to threshold.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `src`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco",
    "fortinet",
    "netflow",
    "paloalto",
    "paloaltonetworks",
    "zeek"
  ],
  "equipmentModels": [
    "cisco_asa",
    "cisco_ftd",
    "netflow_netflow",
    "paloalto_pan_firewall",
    "paloalto_panos"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• VPN session log data (Cisco AnyConnect, GlobalProtect, FortiClient).\n• Firewall or flow data showing both tunnel and non-tunnel interfaces.\n• Understanding of VPN client IPv6 capabilities.\n\nStep 1 — Configure data collection\n\n**Create VPN client assignment lookup (auto-populated from VPN logs):**\n```spl\nindex=network (sourcetype=\"pan:globalprotect\" OR sourcetype=\"cisco:asa\" OR sourcetype=\"cisco:ftd\") \"VPN\" \"connected\"\n| rex field=_raw \"user=(?<vpn_user>\\S+)\"\n| rex field=_raw \"(?:assigned|tunnel).?(?:ip|address)\\s*=?\\s*(?<tunnel_ip>[0-9a-fA-F:.]+)\"\n| rex field=_raw \"(?:public|real).?(?:ip|address)\\s*=?\\s*(?<public_ip>[0-9a-fA-F:.]+)\"\n| eval vpn_status=\"connected\"\n| eval tunnel_interface=\"tunnel.1\"\n| table vpn_user, public_ip, tunnel_ip, vpn_status, tunnel_interface\n| outputlookup vpn_client_assignments.csv\n```\n\n**Detection search — DNS leakage via IPv6:**\n```spl\nindex=network (sourcetype=\"pan:traffic\" OR sourcetype=\"cisco:asa\") dest_port=53 earliest=-1h\n| eval is_ipv6_dns=if(match(src, \":\") AND match(dest, \":\"), 1, 0)\n| where is_ipv6_dns=1\n| lookup vpn_client_assignments.csv public_ip as src OUTPUT vpn_user, vpn_status\n| where vpn_status=\"connected\"\n| stats count as dns_queries dc(dest) as unique_resolvers by src, vpn_user\n| eval alert=\"VPN user \" . vpn_user . \" sending \" . dns_queries . \" DNS queries over IPv6 (outside VPN tunnel) to \" . unique_resolvers . \" resolvers\"\n```\n\n**Verification:**\n```spl\nindex=network sourcetype=\"pan:globalprotect\" \"connected\" | stats count by user | head 10\n```\n\nStep 2 — Create detection dashboard\n\n**Full VPN leakage detection:**\n```spl\nindex=network (sourcetype=\"pan:traffic\" OR sourcetype=\"netflow\") earliest=-4h\n| eval is_ipv6=if(match(src, \":\") OR match(dest, \":\"), 1, 0)\n| where is_ipv6=1\n| lookup vpn_client_assignments.csv public_ip as src OUTPUT vpn_user, vpn_status\n| where vpn_status=\"connected\"\n| eval leaked_dest_type=case(\n    dest_port=53, \"DNS (reveals browsing activity)\",\n    dest_port=443, \"HTTPS (reveals sites visited)\",\n    dest_port=80, \"HTTP (content exposed)\",\n    1=1, \"Other (port \" . dest_port . \")\")\n| stats count as flows sum(bytes) as bytes dc(dest) as unique_dests by vpn_user, leaked_dest_type\n| sort -flows\n```\n\nStep 3 — Validate\n(a) **Controlled test.** Connect a test device to VPN. Enable IPv6 on the local interface. Browse to a dual-stack website (google.com). Check whether flows appear on the local (non-VPN) interface in IPv6.\n\n(b) **DNS leak test.** While connected to VPN, run `nslookup -type=AAAA google.com` and observe which DNS resolver responds. If it's not the VPN DNS, leakage is confirmed.\n\n(c) **RA injection test.** On a lab network with a VPN client, inject a rogue RA providing IPv6 connectivity. Verify the VPN client's traffic shifts to IPv6 (bypassing the tunnel).\n\nStep 4 — Operationalize\n\n**Dashboard** (\"IPv6 — VPN Traffic Leakage\"):\n• Row 1 — Single-value: users with active IPv6 VPN leakage (target: 0).\n• Row 2 — Table: per-user leakage detail with traffic classification.\n• Row 3 — Timechart: leaked flows over time.\n• Row 4 — Pie chart: leakage by traffic type (DNS, HTTPS, other).\n\n**Alert:** Any VPN user with IPv6 DNS leakage — immediate notification. DNS leakage reveals all browsing activity regardless of HTTPS encryption.\n\n**Remediation options:**\n1. **VPN client configuration.** Configure VPN client to tunnel IPv6 (Cisco AnyConnect: `ipv6-addr-only` split-tunnel policy).\n2. **Disable IPv6 on VPN adapter.** Less ideal but effective: disable IPv6 on the physical adapter when VPN connects.\n3. **Full tunnel mode.** Use full-tunnel VPN that captures all traffic (both IPv4 and IPv6).\n4. **Network-level mitigation.** Deploy RA Guard on network segments used by VPN clients to prevent rogue RA attacks.\n\nStep 5 — Troubleshooting\n\n• **VPN platform variations.** Each VPN platform handles IPv6 differently. Cisco AnyConnect supports IPv6 tunnel transport but may not tunnel IPv6 traffic by default. Palo Alto GlobalProtect requires explicit IPv6 tunnel configuration. WireGuard tunnels both by default if configured.\n\n• **Mobile devices.** iOS and Android VPN clients have different IPv6 handling. iOS Always-On VPN with IKEv2 tunnels IPv6 by default. Android depends on the VPN app.\n\n• **Correlation timing.** VPN session start/end times may not align precisely with flow data timestamps. Allow a 5-minute tolerance window around VPN session boundaries.",
  "grandmaExplanation": "When you use a secret tunnel (VPN) to send your letters privately, some of your letters might accidentally go through the regular postal system (IPv6) instead of the secret tunnel. A spy at the coffee shop could read those letters. We check for any letters that escaped the secret tunnel and went out the normal way.",
  "prerequisiteUseCases": [
    "UC-5.20.57",
    "UC-5.20.58"
  ],
  "wave": "run"
}
