{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.14.43",
  "title": "Traefik Entrypoint Open Connections",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Traefik Entrypoint Open Connections' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_traefik_entrypoint_open_connections` (UC-5.14.43), SOC dashboard panel \"Operations — Traefik Entrypoint Open Connections\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.14.43 positive (Traefik, Entrypoint, Open, Connections): ingest synthetic events into `index=proxy` sourcetype='traefik:metrics' where `max` grouped by `entrypoint` satisfies detection threshold for 'Traefik Entrypoint Open Connections'. Validate filter `metric_name=\"*entrypoint*open_connections*\" OR match(metric_`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.14.43 negative (Open Connections): ingest benign `proxy` / `traefik:metrics` events where `entrypoint` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 14 confirms zero alert rows for 'Traefik Entrypoint Open Connections'."
  },
  "dataSources": "`index=proxy` `sourcetype=traefik:metrics` Prometheus scrape Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder, Splunk Connect for Syslog, or HTTP Event Collector; `props.conf`/`transforms.conf` for listed sourcetypes — Universal Forwarder (Splunkbase 617)",
  "spl": "index=proxy sourcetype=\"traefik:metrics\"\n| search metric_name=\"*entrypoint*open_connections*\" OR match(metric_name, \"traefik_entrypoint_open_connections\")\n| eval v=tonumber(metric_value)\n| timechart span=1m max(v) by entrypoint",
  "description": "Alerts on UC-5.14.43 — Traefik, Entrypoint, Open, Connections — using `index=proxy` sourcetype `traefik:metrics`, grouped by `entrypoint`. Outputs `max` each `1m`; alert when configured threshold. Filter chain includes filter `metric_name=\"*entrypoint*open_connections*\" OR match(metric_`. Purpose-built for 'Traefik Entrypoint Open Connections' in Operations workflows.",
  "value": "Alerting on Traefik, Entrypoint, Open, Connections through UC-5.14.43 reduces operational risk and delayed incident response by surfacing `activity` early.",
  "implementation": "Enable `--metrics.prometheus`; label series by entrypoint. Use OTel if multi-tenant.",
  "visualization": "Time series for rates and latency, top/dedup for hotspots, single-value alerts on health thresholds.",
  "cimModels": [],
  "references": [
    {
      "title": "Vendor documentation",
      "url": "https://doc.traefik.io/traefik/observability/metrics/prometheus/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `proxy` (UC-5.14.43)** — Planned work for 'Traefik Entrypoint Open Connections' shifts `max` over `span=1m` while `entrypoint` remains in-scope. Applies filter `metric_name=\"*entrypoint*open_connections*\" OR match(metric_`. Cross-check CMDB CHG.\n\n2. **Batch `max` on `traefik:metrics`** — Scheduled automation elevates `v` without crossing configured threshold for UC-5.14.43. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `entrypoint`** — Health checks in `proxy` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `traefik:metrics`** — First 48h baseline for UC-5.14.43 on `entrypoint` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `entrypoint`, `exception_reason`, `valid_until`.",
  "equipment": [
    "prometheus",
    "syslog",
    "traefik"
  ],
  "equipmentModels": [
    "prometheus_generic",
    "syslog_generic",
    "traefik_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "Prerequisites\n* Traefik access logs and/or Prometheus metrics forwarded to Splunk. Data in `index=proxy` with `sourcetype=traefik:access` (JSON or CLF access log) or `sourcetype=traefik:metrics` (Prometheus scrape). Key metrics: `traefik_entrypoint_open_connections` (gauge, labeled by entrypoint and protocol), `traefik_entrypoint_requests_total`.\n* Entrypoints: Traefik's network listeners (e.g., `:80/http`, `:443/https`). Open connections is the number of active TCP connections on each entrypoint. High values indicate: (1) legitimate traffic surge, (2) slowloris/connection exhaustion attack, (3) connection leak from clients not closing.\n\nStep 1 -- Configure data collection\nEnable Traefik access log and metrics:\n```yaml\n# traefik.yml (static config)\naccessLog:\n  filePath: /var/log/traefik/access.log\n  format: json\n  fields:\n    headers:\n      defaultMode: keep\n\nmetrics:\n  prometheus:\n    entryPoint: metrics\n    addEntryPointsLabels: true\n    addServicesLabels: true\n\nentryPoints:\n  web:\n    address: \":80\"\n  websecure:\n    address: \":443\"\n  metrics:\n    address: \":8082\"\n```\nVerify:\n```spl\nindex=proxy sourcetype=\"traefik:metrics\" earliest=-4h\n| where match(metric_name, \"entrypoint_open_connections\")\n| stats latest(metric_value) as open_conns by entrypoint\n```\n\nStep 2 -- Create the search and alert\n\n**Primary search -- Open connections per entrypoint:**\n```spl\nindex=proxy sourcetype=\"traefik:metrics\" earliest=-4h\n| where match(metric_name, \"entrypoint_open_connections\")\n| bin _time span=5m\n| stats avg(metric_value) as avg_open max(metric_value) as max_open by _time, entrypoint\n| eval severity=case(max_open > 10000, \"CRITICAL -- >10K open connections\", max_open > 5000, \"HIGH -- >5K connections\", avg_open > 2000, \"WARNING -- sustained high connections\", 1==1, \"OK\")\n| where severity != \"OK\"\n| table _time, entrypoint, avg_open, max_open, severity\n```\n\n**From access log (connection counting):**\n```spl\nindex=proxy sourcetype=\"traefik:access\" earliest=-1h\n| bin _time span=5m\n| stats dc(ClientAddr) as unique_clients count as requests by _time, entrypoint\n| table _time, entrypoint, unique_clients, requests\n```\n\nStep 3 -- Validate\n(a) Traefik API dashboard: `http://<traefik>:8080/api/entrypoints` -- shows entrypoint config.\n(b) Compare with `curl http://localhost:8082/metrics | grep entrypoint_open`.\n(c) Load test with many concurrent connections and verify gauge increases.\n\nStep 4 -- Operationalize\nDashboard (\"Traefik -- Entrypoint Connections\"):\n* Row 1 -- Single-value per entrypoint: \"Open connections\", \"Unique clients\", \"Requests/sec\".\n* Row 2 -- Connection count timechart.\n\nAlerting:\n* Critical (> 10K open connections): potential exhaustion or attack.\n* High (> 5K sustained): capacity planning needed.\n\nStep 5 -- Troubleshooting\n\n* **Connections growing but requests flat** -- Clients are opening connections without sending requests (idle/keepalive). Tune: `transport.respondingTimeouts.idleTimeout` (default 180s) in Traefik config.\n\n* **Sudden connection spike** -- Check for: (1) DDoS/slowloris attack (many connections from few IPs), (2) bot scraping, (3) legitimate traffic event. Examine: per-IP connection counts.\n\n* **Connections not decreasing** -- Connection leak. Check: (1) backend is sending responses, (2) no timeout misconfiguration, (3) client-side keepalive settings.",
  "grandmaExplanation": "We watch traefik entrypoint open connections and catch issues early, before they turn into outages for the people who rely on the network.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
