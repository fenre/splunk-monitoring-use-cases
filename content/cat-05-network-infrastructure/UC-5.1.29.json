{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.1.29",
  "title": "ARP Table Size Trending",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'ARP Table Size Trending' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_arp_table_size_trending` (UC-5.1.29), SOC dashboard panel \"Operations — ARP Table Size Trending\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=network` sourcetype='snmp:arp' where `host` satisfies `util_pct` > 70 for UC-5.1.29 ('ARP Table Size Trending'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `network` / `snmp:arp` events where `host` stays below `util_pct` > 70, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-5.1.29."
  },
  "dataSources": "ipNetToMediaTable entries count, show arp count Collected via Splunk Add-on for Cisco Security (Splunkbase 1352) modular input or scripted poll into the index named in the SPL.",
  "app": "SNMP modular input, `TA-cisco_ios`, `Splunk_TA_juniper`, `arista:eos` via SC4S, HPE Aruba CX syslog — Splunk Add-on for Cisco Security (Splunkbase 1352)",
  "spl": "index=network sourcetype=snmp:arp OR sourcetype=cisco:ios:arp\n| eval arp_count=coalesce(arp_entries, arp_count, 0)\n| stats latest(arp_count) as current_arp by host\n| lookup arp_limit host OUTPUT max_arp\n| eval util_pct=round(current_arp/max_arp*100,1)\n| where util_pct > 70\n| table host current_arp max_arp util_pct",
  "description": "ARP table approaching hardware limits; can cause connectivity failures. This use case monitors the condition described in the title using the SPL and data sources documented on this page.",
  "value": "ARP table approaching hardware limits; can cause connectivity failures. Timely UC-5.1.29 alerting on 'ARP Table Size Trending' reduces operational risk and delayed incident response before manual triage or audit channels escalate.",
  "implementation": "Poll ipNetToMediaTable (count rows) or parse `show ip arp` / `show arp` output via scripted input. Create lookup with device→max_arp (from vendor specs). Alert when utilization exceeds 70%.",
  "visualization": "Line chart (ARP count over time), Gauge (utilization), Table.",
  "cimModels": [],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 1352",
      "url": "https://splunkbase.splunk.com/app/1352",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Cisco Security documentation",
      "url": "https://www.cisco.com/c/en/us/support/security/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `network`** — Planned work for 'ARP Table Size Trending' shifts `count` over `span=1h` while `host` remains in-scope for UC-5.1.29. Cross-check CMDB CHG.\n\n2. **Batch `count` on `snmp:arp`** — Scheduled automation elevates `arp_count` without crossing `util_pct` > 70. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `network` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `snmp:arp`** — First 48h baseline for `host` is unstable; join `arp_limit` prior to threshold.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco",
    "snmp",
    "syslog"
  ],
  "equipmentModels": [
    "cisco_ios",
    "snmp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    {
      "name": "Splunk User Behavior Analytics",
      "displayName": "Splunk User Behavior Analytics",
      "note": "UBA"
    }
  ],
  "detailedImplementation": "Prerequisites\n* ARP table size data from SNMP or CLI scripted input. SNMP OID: ipNetToMediaEntry count, or `show arp | count` via scripted input. Data in `index=network` with SNMP or `sourcetype=network:arp:stats`.\n* ARP table exhaustion causes devices to drop new ARP entries, breaking connectivity. Large ARP tables indicate large flat networks (L2 broadcast domains), scanning activity, or ARP storms.\n\nStep 1 -- Configure data collection\n```\n[script:///opt/splunk/etc/apps/network_mon/bin/arp_table_size.sh]\ninterval = 300\nsourcetype = network:arp:stats\nindex = network\n\n# arp_table_size.sh\n#!/bin/bash\necho \"device=$(hostname)\"\narp_count=$(show ip arp | wc -l 2>/dev/null || arp -an | wc -l)\necho \"arp_entries=$arp_count\"\n```\nVerify:\n```spl\nindex=network sourcetype=\"network:arp:stats\" earliest=-4h | stats latest(arp_entries) by device\n```\n\nStep 2 -- Create the search and alert\n\n**Primary search -- ARP table size trending:**\n```spl\nindex=network earliest=-7d\n| eval arp_count=tonumber(coalesce(arp_entries, arp_table_size))\n| eval device=coalesce(device, host, device_name)\n| where isnotnull(arp_count)\n| bin _time span=1h\n| stats avg(arp_count) as avg_entries max(arp_count) as max_entries by _time, device\n| eventstats avg(avg_entries) as baseline_avg stdev(avg_entries) as stdev_entries by device\n| eval z_score=if(stdev_entries > 0, round((avg_entries - baseline_avg)/stdev_entries, 2), 0)\n| eval severity=case(\n    max_entries > 4000, \"CRITICAL -- ARP table very large (>4000 entries)\",\n    z_score > 3, \"WARNING -- abnormal ARP table growth\",\n    max_entries > 2000, \"INFO -- ARP table elevated\",\n    1==1, \"OK\")\n| where severity != \"OK\"\n| table _time, device, avg_entries, max_entries, z_score, severity\n| sort severity, -max_entries\n```\n\nStep 3 -- Validate\n(a) CLI: `show ip arp | count` or `show ip arp summary` -- current ARP table size.\n(b) Check ARP timeout: `show ip arp timeout` -- default 4 hours on Cisco.\n(c) Verify ARP table limit: hardware-dependent.\n\nStep 4 -- Operationalize\nDashboard (\"Network -- ARP Table\"):\n* Row 1 -- Single-value: \"Max ARP entries\", \"Devices > 2000 entries\".\n* Row 2 -- ARP table size timechart.\n\nAlert: Critical (>4000 ARP entries): investigate cause.\n\nStep 5 -- Troubleshooting\n\n* **Large flat L2 domain** -- All hosts in the broadcast domain generate ARP entries. Consider: subnet segmentation, VRFs, or moving to routed access layer.\n\n* **ARP storm from scanning** -- Check for new entries from same source MAC. May indicate network scanner or worm. Apply DHCP snooping and DAI.\n\n* **ARP timeout too long** -- Default 4 hours may keep stale entries. Consider reducing: `arp timeout <seconds>` on interface.",
  "grandmaExplanation": "We help you know early when something looks wrong with arp table size trending so the team can act before it grows into a bigger outage.",
  "hardware": "Cisco Catalyst 9200/9300/9400/9500/9600, ISR 1100/4000, ASR 1000; Juniper EX2300, EX3400, EX4300, EX4400, EX4600, QFX5100/5120/5200/5220, MX204/304/480, SRX300/SRX1500/SRX4100/SRX4200; Arista 7050X3, 7060X, 7260X3, 7280R3, 7500R3; HPE Aruba CX 6100/6200/6300/6400, CX 8320/8325/8360",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
