{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.2.12",
  "title": "NAT Pool Exhaustion",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'NAT Pool Exhaustion' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_nat_pool_exhaustion` (UC-5.2.12), SOC dashboard panel \"Operations — NAT Pool Exhaustion\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.2.12 positive (NAT, Pool, Exhaustion): ingest synthetic events into `index=firewall` sourcetype='target_sourcetype' where `count` grouped by `host`, `nat_pool` satisfies detection threshold for 'NAT Pool Exhaustion'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.2.12 negative (Pool Exhaustion): ingest benign `firewall` / `target_sourcetype` events where `host`, `nat_pool` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 0 confirms zero alert rows for 'NAT Pool Exhaustion'."
  },
  "dataSources": "Firewall NAT/system logs Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_paloalto`, `TA-fortinet_fortigate`, Cisco Secure Firewall Add-on, `Splunk_TA_juniper` (SRX), syslog — Universal Forwarder (Splunkbase 617)",
  "spl": "index=firewall (\"NAT\" OR \"nat\") (\"exhausted\" OR \"allocation failed\" OR \"out of\")\n| stats count by host, nat_pool | sort -count",
  "description": "Identifies 'NAT Pool Exhaustion' (UC-5.2.12) using `index=firewall` sourcetype `configured sourcetype` by aggregating `count` grouped by `host`, `nat_pool` over `the scheduled window` and alerting when configured threshold. Keywords: NAT, Pool, Exhaustion. Focuses on Pool Exhaustion behaviour so operations teams can triage UC-5.2.12 without ad-hoc log review.",
  "value": "UC-5.2.12 accelerates insight into Pool Exhaustion ('NAT Pool Exhaustion') by tracking `activity` on UC-5.2.12 search results, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Forward firewall logs. Monitor NAT table usage. Alert on exhaustion messages or >80% utilization.",
  "visualization": "Gauge per pool, Table, Events timeline.",
  "cimModels": [],
  "references": [
    {
      "title": "Splunk_TA_paloalto",
      "url": "https://splunkbase.splunk.com/app/2757",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Cisco Security documentation",
      "url": "https://www.cisco.com/c/en/us/support/security/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Palo Alto Networks documentation",
      "url": "https://docs.paloaltonetworks.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `firewall` (UC-5.2.12)** — Planned work for 'NAT Pool Exhaustion' shifts `count` over `span=1h` while `host`, `nat_pool` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `configured_sourcetype`** — Scheduled automation elevates `nat_pool` without crossing configured threshold for UC-5.2.12. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`, `nat_pool`** — Health checks in `firewall` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-5.2.12 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco",
    "fortinet",
    "paloalto",
    "syslog"
  ],
  "equipmentModels": [
    "cisco_firepower",
    "fortinet_fortigate",
    "paloalto_pan_firewall"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n* Firewall NAT/system logs in `index=firewall`. Key events: NAT pool exhaustion, PAT allocation failures, NAT session creation failures. Palo Alto: `pan:system` with NAT events. Fortinet: `fgt_event` with NAT messages. Key fields: `nat_pool`, `nat_rule`, `sessions_used`, `sessions_max`.\n* NAT pool exhaustion: when all available IP:port combinations in a NAT pool are used, new outbound connections fail. PAT (Port Address Translation) typically allows ~64K ports per IP. For large environments, multiple NAT IPs are needed.\n\nStep 1 -- Configure data collection\n**Palo Alto:**\n```\nshow running nat-rule-ippool  # shows pool utilization\nshow running ippool  # shows allocated IPs and port ranges\n```\nForward system events that include NAT resource warnings via syslog.\nVerify:\n```spl\nindex=firewall earliest=-4h\n| where match(_raw, \"(?i)NAT.*(exhaust|fail|alloc|pool|limit|resource|oversubscr)\")\n| stats count by host, _raw\n| head 20\n```\n\nStep 2 -- Create the search and alert\n\n**Primary search -- NAT pool exhaustion detection:**\n```spl\nindex=firewall earliest=-4h\n| where match(_raw, \"(?i)NAT.*(exhaust|fail|alloc.*fail|pool.*full|limit.*reach|resource.*low|oversubscr|no.*available)\")\n| eval nat_event=case(match(_raw, \"(?i)exhaust|pool.*full|no.*available\"), \"POOL_EXHAUSTED\", match(_raw, \"(?i)alloc.*fail\"), \"ALLOCATION_FAILED\", match(_raw, \"(?i)limit.*reach|oversubscr\"), \"NEAR_LIMIT\", match(_raw, \"(?i)resource.*low\"), \"LOW_RESOURCES\", 1==1, \"OTHER\")\n| rex \"pool\\s+(?<nat_pool>[\\w.-]+)\"\n| rex \"rule\\s+(?<nat_rule>[\\w.-]+)\"\n| stats count as events latest(_time) as last_event by host, nat_event, nat_pool\n| eval severity=case(nat_event=\"POOL_EXHAUSTED\", \"CRITICAL -- NAT pool exhausted, connections failing\", nat_event=\"ALLOCATION_FAILED\" AND events > 100, \"HIGH -- frequent allocation failures\", nat_event=\"NEAR_LIMIT\", \"WARNING -- approaching limit\", 1==1, \"INFO\")\n| where severity != \"INFO\"\n| sort severity, -events\n```\n\n**Session count by NAT pool (if available from API polling):**\n```spl\nindex=firewall sourcetype=\"pan:system:resource\" earliest=-4h\n| eval pool_pct=round(100*tonumber(sessions_used)/tonumber(sessions_max), 1)\n| where pool_pct > 70\n| stats latest(pool_pct) as utilization latest(sessions_used) as used latest(sessions_max) as max by nat_pool, host\n| sort -utilization\n```\n\nStep 3 -- Validate\n(a) Palo Alto: `show running nat-rule-ippool` -- check allocation percentage.\n(b) Fortinet: `diagnose firewall iprope list 100004` -- shows NAT pool utilization.\n(c) Verify by checking if users report internet access failures that correlate with NAT exhaustion events.\n\nStep 4 -- Operationalize\nDashboard (\"Firewall -- NAT Pool Health\"):\n* Row 1 -- Gauge: \"NAT pool utilization %\" per pool.\n* Row 2 -- NAT exhaustion events timeline.\n\nAlerting:\n* Critical (pool exhausted): outbound connections failing immediately.\n* High (allocation failures > 100/hr): capacity issue.\n* Warning (pool > 80%): plan additional NAT IPs.\n\nStep 5 -- Troubleshooting\n\n* **NAT pool exhausted** -- Immediate: add more IP addresses to the NAT pool. Long-term: (1) identify top session consumers (specific hosts using excessive outbound connections), (2) reduce session timeouts for short-lived connections, (3) investigate for infected hosts opening many sessions.\n\n* **Single host consuming most NAT sessions** -- May indicate: (1) P2P software, (2) malware with many C2 connections, (3) aggressive web crawler. Investigate the host.\n\n* **NAT pool size planning** -- Rule of thumb: each public IP provides ~60K concurrent PAT sessions. For 10K users with average 5 concurrent connections, you need at least 1 NAT IP. Peak loads need 2-3x.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_paloalto`, `TA-fortinet_fortigate`, Cisco Secure Firewall Add-on, `Splunk_TA_juniper` (SRX), syslog — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into firewall. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, nat_pool`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We look for public address pools running out so new sessions still get out to the internet during busy days and big projects.",
  "hardware": "Cisco Secure Firewall 3110/3120/3130/3140, Firepower 1010/1120/1140/1150, Firepower 2110/2120/2130/2140, FMC; Palo Alto PA-220/PA-440/PA-450/PA-460/PA-3200/PA-5200/PA-5400/PA-7000, Panorama; Fortinet FortiGate 40F/60F/80F/100F/200F/400F/600F/1000F/1800F/2600F/3500F/3700F/4400F, FortiManager; Juniper SRX300/SRX320/SRX340/SRX345/SRX550/SRX1500/SRX4100/SRX4200/SRX4600/SRX5400/SRX5600/SRX5800 (FMC)",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
