{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.2.52",
  "title": "Check Point Anti-Spoofing Violations (Check Point)",
  "criticality": "critical",
  "difficulty": "beginner",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Check Point Anti-Spoofing Violations (Check Point)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_check_point_anti_spoofing_violations_check_point` (UC-5.2.52), SOC dashboard panel \"Operations — Check Point Anti-Spoofing Violations (Check Point)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-5.2.52 positive (Check, Point, Anti, Spoofing, Violations): ingest synthetic events into `index=firewall` sourcetype='cp_log' where `count` grouped by `src`, `inzone`, `outzone`, `rule_name` satisfies -24h for 'Check Point Anti-Spoofing Violations (Check Point)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-5.2.52 negative ((Check Point)): ingest benign `firewall` / `cp_log` events where `src`, `inzone`, `outzone`, `rule_name` stays below -24h, or exclude the test entity via `ops_exception_register.csv`. Variant 6 confirms zero alert rows for 'Check Point Anti-Spoofing Violations (Check Point)'."
  },
  "dataSources": "`sourcetype=cp_log` (firewall logs with anti-spoofing drops) Indexes: firewall. Sourcetypes: cp_log. Key fields: inzone, orig, outzone, rule_name, src. Validate field extraction in Search before saving the alert; compare event counts to the vendor admin console.",
  "app": "`Splunk_TA_checkpoint` (Splunkbase 5402), Check Point App for Splunk (Splunkbase 4293), CCX Add-on for Checkpoint Smart-1 Cloud (Splunkbase 7259)",
  "spl": "index=firewall sourcetype=\"cp_log\" earliest=-24h\n| where match(lower(action),\"(?i)drop\") AND match(lower(logdesc),\"(?i)anti.?spoof|spoofing\")\n| stats count by src, inzone, outzone, rule_name, orig\n| sort -count",
  "description": "Identifies (Check Point) anomalies for UC-5.2.52 with window `the scheduled window` on `index=firewall` sourcetype `cp_log`. Combines `count` by `src`, `inzone`, `outzone`, `rule_name` and compares against -24h. Mapped to keywords Check, Point, Anti, Spoofing, Violations for dashboard routing.",
  "value": "Alerting on Check, Point, Anti, Spoofing, Violations through UC-5.2.52 reduces operational risk and delayed incident response by surfacing `activity` early.",
  "implementation": "Forward firewall drop logs including anti-spoofing events. Map `inzone` and `outzone` to topology to distinguish misconfiguration from attacks. Alert on new source IPs triggering anti-spoofing. Correlate with routing changes. Tune anti-spoofing topology definitions after legitimate asymmetric routing is identified.",
  "visualization": "Table (spoofing violations by source), Bar chart (violations by interface/zone), Line chart (violation trend), Map (source geo if available).",
  "cimModels": [],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Network_Traffic.All_Traffic\n  by All_Traffic.src All_Traffic.dest All_Traffic.action All_Traffic.dvc span=1h\n| where count>0\n| sort -count",
  "dataModelAcceleration": "Enable acceleration on the Network_Traffic data model, dataset All_Traffic, before using this tstats search.",
  "references": [
    {
      "title": "Check Point App for Splunk",
      "url": "https://splunkbase.splunk.com/app/4293",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Network_Traffic",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Network_Traffic",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CCX Add-on for Checkpoint Smart-1 Cloud",
      "url": "https://splunkbase.splunk.com/app/7259",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 5402",
      "url": "https://splunkbase.splunk.com/app/5402",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `firewall` (UC-5.2.52)** — Planned work for 'Check Point Anti-Spoofing Violations (Check Point)' shifts `count` over `span=1h` while `src`, `inzone`, `outzone`, `rule_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `cp_log`** — Scheduled automation elevates `inzone` without crossing configured threshold for UC-5.2.52. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `src`, `inzone`, `outzone`, `rule_name`** — Health checks in `firewall` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `cp_log`** — First 48h baseline for UC-5.2.52 on `src` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `src`, `exception_reason`, `valid_until`.",
  "equipment": [
    "checkpoint"
  ],
  "equipmentModels": [
    "checkpoint_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n* Check Point anti-spoofing violation logs. Data in `index=checkpoint` with `sourcetype=cp_log`. Key fields: `action` (Drop/Reject), `src`, `dst`, `ifname`, `rule_name`, `product`, `description`.\n* Anti-spoofing: validates that packets arriving on an interface have source IPs consistent with the interface's defined topology (network behind the interface). Configured per-interface in Gateway Properties > Topology. When a packet's source IP doesn't match the expected network for the ingress interface, it's dropped as a spoofing attempt. Also prevents internal IP addresses from appearing on external interfaces.\n\nStep 1 -- Configure data collection\n```\n# SmartConsole -- enable anti-spoofing\n# Gateway Properties > Network Management > Topology\n# For each interface:\n#   Define topology (Internal/External)\n#   Set anti-spoofing to \"Prevent\" or \"Detect\"\n#   Define the network behind the interface\n# Enable logging for anti-spoofing drops\n\n# Ensure \"Spoofed packets\" logging is enabled:\n# Global Properties > Log and Alert > Anti Spoofing\n```\nVerify:\n```spl\nindex=checkpoint sourcetype=\"cp_log\" earliest=-7d\n| where match(_raw, \"(?i)spoof|anti.?spoof\") OR match(action, \"(?i)drop\") AND match(description, \"(?i)spoof\")\n| stats count by ifname, src\n| sort -count | head 20\n```\n\nStep 2 -- Create the search and alert\n\n**Primary search -- Anti-spoofing violation monitoring:**\n```spl\nindex=checkpoint sourcetype=\"cp_log\" earliest=-24h\n| where match(_raw, \"(?i)spoof|anti.?spoof\") OR (match(action, \"(?i)drop\") AND match(description, \"(?i)spoof\"))\n| eval src=coalesce(src, source_address, src_ip)\n| eval dst=coalesce(dst, destination_address, dest_ip)\n| eval iface=coalesce(ifname, interface, inzone)\n| eval device=coalesce(origin, host)\n| iplocation src prefix=src_\n| stats count as violations dc(src) as unique_sources dc(dst) as unique_targets values(src) as source_ips by device, iface\n| eval severity=case(\n    violations > 1000, \"CRITICAL -- massive spoofing attempt (\".violations.\" violations)\",\n    unique_sources > 20, \"WARNING -- distributed spoofing from \".unique_sources.\" sources\",\n    violations > 100, \"WARNING -- sustained spoofing activity\",\n    1==1, \"INFO\")\n| where severity != \"INFO\"\n| sort severity, -violations\n```\n\nStep 3 -- Validate\n(a) SmartConsole: Gateway > Topology -- verify anti-spoofing is \"Prevent\" on all interfaces.\n(b) SmartConsole: Logs & Monitor -- filter for anti-spoofing events.\n(c) CLI: `fw ctl zdebug + drop` -- check for anti-spoofing drops in real time.\n\nStep 4 -- Operationalize\nDashboard (\"Check Point -- Anti-Spoofing\"):\n* Row 1 -- Single-value: \"Spoofing violations (24h)\", \"Unique sources\", \"Interfaces affected\".\n* Row 2 -- Anti-spoofing violation timeline.\n* Row 3 -- Top spoofed source IPs with geolocation.\n\nAlert: Critical (>1000 violations in 1 hour): active spoofing attack or misconfiguration.\n\nStep 5 -- Troubleshooting\n\n* **False positives from asymmetric routing** -- Anti-spoofing drops legitimate traffic when return path uses a different interface. Solution: adjust topology definition to include the additional network, or use `do not check packets from` exception.\n\n* **False positives after network changes** -- Adding new subnets behind an interface requires updating the topology definition. Failing to update causes anti-spoofing drops for the new subnet. Run: `fetch topology` in SmartConsole.\n\n* **Anti-spoofing not preventing** -- Verify mode is \"Prevent\" not \"Detect\" for production interfaces. Check: Gateway Properties > Topology > anti-spoofing setting per interface.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_checkpoint` (Splunkbase 5402), Check Point App for Splunk (Splunkbase 4293), CCX Add-on for Checkpoint Smart-1 Cloud (Splunkbase 7259)). Enable the modular input or HEC token that writes sourcetype=cp_log into firewall. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`inzone, orig, outzone, rule_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We count anti-spoofing hits so bad addressing, late routing change, and miswired segments surface before they turn into silent drops.",
  "hardware": "Check Point Quantum 6200/6400/6600/6800/7000/16200/16600/26000/28000, Check Point Quantum Maestro, Check Point CloudGuard Network, Smart-1 Cloud (management)",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
