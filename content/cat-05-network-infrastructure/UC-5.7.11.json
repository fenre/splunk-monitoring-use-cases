{
  "$schema": "../../schemas/uc.schema.json",
  "id": "5.7.11",
  "title": "sFlow Sampling Rate Validation and Collector Health",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Data Quality",
    "Operations",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'sFlow Sampling Rate Validation and Collector Health' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_sflow_sampling_rate_validation_and_collector_hea` (UC-5.7.11), SOC dashboard panel \"Operations — sFlow Sampling Rate Validation and Collector Health\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=netflow` sourcetype='stream:netflow' that satisfy the UC-5.7.11 SPL filters for 'sFlow Sampling Rate Validation and Collector Health'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `netflow` / `stream:netflow` events outside the detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-5.7.11."
  },
  "dataSources": "`index=netflow` (or `network_flow`) with `sourcetype=stream:netflow`, `netflow`, or vendor-normalized sFlow from the Splunk Add-on for NetFlow (Splunkbase 1838); expect datagram-level fields such as `sample_pool`, `sampling_rate`, `mean_skip_count`, `agent`, `src`, `dest`, `bytes`, `packets`, `protocol`, and ingress interface identifiers when templates expose them.",
  "app": "Splunk Add-on for NetFlow (Splunkbase 1838)",
  "spl": "index=netflow sourcetype=\"stream:netflow\" OR sourcetype=\"netflow\"\n| bin _time span=15m\n| stats count dc(agent) as agents dc(src) as uniq_src dc(dest) as uniq_dest,\n    values(sampling_rate) as sampling_rates,\n    avg(if(isnum(sampling_rate), sampling_rate, null())) as avg_sampling_rate,\n    sum(bytes) as total_bytes\n  by _time, host\n| eval sampling_rates_mv=mvjoin(sampling_rates, \",\")\n| where isnull(avg_sampling_rate) OR mvcount(sampling_rates)>3 OR count<10\n| sort _time\n| head 200",
  "description": "Compares observed sFlow sampling metadata and record cadence per agent and time bucket against expectations so gaps, wild swings in declared sampling rate, or silent collectors surface before capacity or security views drift.",
  "value": "Capacity and observability owners keep normalized byte and packet estimates trustworthy for trending and chargeback, reduce blind spots when agents reboot or templates change, and shorten mean time to detect collector saturation or misconfigured sampling.",
  "implementation": "Land sFlow in Splunk with sampling-related information elements preserved; chart sampling_rate variance per agent; alert on missing buckets or conflicting rates across interfaces.",
  "visualization": "Row 1: timechart of record count per collector host; Row 2: table of agent, avg_sampling_rate, sampling_rates_mv, total_bytes; Row 3: scatter of uniq_src vs count to spot starvation.",
  "references": [
    {
      "title": "Splunk Documentation: Get Started with NetFlow",
      "url": "https://docs.splunk.com/Documentation/NetFlow/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RFC 3176 — InMon Corporation's sFlow",
      "url": "https://www.rfc-editor.org/rfc/rfc3176",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'sFlow Sampling Rate Validation and Collector Health'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `netflow` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `ops_exception_register.csv` with entity, reason, valid_until.",
  "equipment": [
    "netflow"
  ],
  "equipmentModels": [
    "netflow_ipfix",
    "netflow_netflow",
    "netflow_sflow"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-03",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Heavy Forwarder or intermediate collector running the Splunk Add-on for NetFlow (1838) with sFlow enabled on UDP ports documented for your deployment; agents on switches and routers exporting sFlow version 5.\n• Documented target sampling policy per site (for example 1:512 on edge, 1:4096 on core) and list of agent management addresses expected to send datagrams.\n• Baseline understanding that sFlow exports packet samples and counter samples; byte totals are statistical estimates—health checks focus on consistency of sampling metadata and steady datagram arrival, not absolute precision.\n\nStep 1 — Configure data collection\nVerify agents resolve to stable `agent` or `exporter` fields and that `sampling_rate` (or equivalent) is extracted from counter-sample records. Enable DEBUG temporarily on the forwarder if templates omit sampling attributes; confirm firewall paths allow bidirectional UDP from every agent subnet to the collector cluster.\n\nStep 2 — Create the search\nClone the primary SPL into a scheduled report per collector `host`. Extend with `lookup sflow_policy.csv agent OUTPUT expected_rate` and flag `abs(avg_sampling_rate-expected_rate)>expected_rate*0.25`. Add a companion search `| timechart span=15m count by agent` for volume collapse detection.\n\nStep 3 — Validate\nDuring a maintenance window, change sampling on one lab switch and confirm the dashboard reflects the new `sampling_rate` within two buckets. Simulate collector downtime and verify the missing-bucket alert fires.\n\nStep 4 — Operationalize\nPublish a dashboard row for agents over threshold, a single-value tile for collectors with zero events in the prior hour, and weekly CSV exports for auditors proving sampling metadata continuity.\n\nStep 5 — Troubleshooting\nIf `sampling_rate` is always null, inspect IPFIX/sFlow template decoding logs from the add-on and upgrade to a build that includes your vendor enterprise fields. Bursts of duplicate agents often indicate network address translation in front of the collector—normalize using syslog or DHCP correlation.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for NetFlow (Splunkbase 1838)). Enable the modular input or HEC token that writes sourcetype=stream:netflow, netflow into netflow. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch whether our traffic sampling stays steady over time. When the equipment quietly changes how much it samples or stops sending numbers, we notice quickly so our charts and alarms stay honest.",
  "prerequisiteUseCases": [],
  "wave": "walk"
}
