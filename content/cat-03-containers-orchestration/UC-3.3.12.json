{
  "$schema": "../../schemas/uc.schema.json",
  "id": "3.3.12",
  "title": "Project Resource Quota Exhaustion",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`sourcetype=openshift:resourcequota`",
  "app": "`oc get resourcequota -A -o json` scripted input",
  "spl": "index=openshift sourcetype=\"openshift:resourcequota\"\n| eval cpu_pct=round(used_cpu/hard_cpu*100,1), mem_pct=round(used_memory/hard_memory*100,1), pods_pct=round(used_pods/hard_pods*100,1)\n| where cpu_pct>85 OR mem_pct>85 OR pods_pct>85\n| table namespace quota_name cpu_pct mem_pct pods_pct\n| sort -cpu_pct",
  "cimModels": [
    "N/A"
  ],
  "description": "OpenShift projects enforce ResourceQuotas for CPU, memory, and pod count. When quotas approach exhaustion, new deployments and scaling operations fail silently.",
  "value": "OpenShift projects enforce ResourceQuotas for CPU, memory, and pod count. When quotas approach exhaustion, new deployments and scaling operations fail silently.",
  "grandmaExplanation": "We watch each project's resource budget so you know before teams run out of room to deploy new workloads.",
  "implementation": "Scripted input: `oc get resourcequota -A -o json`. Parse `status.hard` and `status.used` for CPU, memory, and pods. Run every 300 seconds. Alert when any resource exceeds 85% of quota.",
  "visualization": "Table (namespace, quota, resource, used, hard, pct), Gauge per resource, Heatmap by namespace.",
  "references": [
    {
      "title": "OpenShift ResourceQuotas documentation",
      "url": "https://docs.openshift.com/container-platform/latest/applications/quotas/quotas-setting-per-project.html"
    }
  ],
  "equipmentModels": [
    "kubernetes_openshift"
  ]
}
