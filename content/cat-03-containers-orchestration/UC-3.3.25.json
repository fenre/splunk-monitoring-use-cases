{
  "$schema": "../../schemas/uc.schema.json",
  "id": "3.3.25",
  "title": "LimitRange Enforcement Tracking",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Capacity",
    "Compliance"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`sourcetype=openshift:limitrange`, `sourcetype=kube:events`",
  "app": "`oc get limitrange -A -o json` scripted input, event forwarding",
  "spl": "index=openshift sourcetype=\"kube:events\" reason=\"FailedCreate\"\n| search \"forbidden: exceeded quota\" OR \"must be less than or equal to\" OR \"LimitRange\"\n| stats count by namespace, involvedObject.kind, involvedObject.name, message\n| sort -count",
  "description": "LimitRanges set default and maximum resource requests/limits per container or pod in a namespace. When workloads are rejected for exceeding these limits, deployments fail. Tracking enforcement events helps teams right-size their resource requests.",
  "value": "LimitRanges set default and maximum resource requests/limits per container or pod in a namespace. When workloads are rejected for exceeding these limits, deployments fail. Tracking enforcement events helps teams right-size their resource requests.",
  "implementation": "Forward OpenShift events. Filter for FailedCreate events with LimitRange or quota-related messages. Also periodically export LimitRange definitions to compare against actual workload requests. Alert on repeated failures per namespace.",
  "visualization": "Table (namespace, workload, message), Bar chart by namespace, Timeline of rejections.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "OpenShift LimitRange documentation",
      "url": "https://docs.openshift.com/container-platform/latest/nodes/clusters/nodes-cluster-limit-ranges.html"
    }
  ],
  "equipment": [
    "kubernetes"
  ],
  "equipmentModels": [
    "kubernetes_openshift"
  ],
  "grandmaExplanation": "We watch for applications that ask for too many resources and get rejected, helping teams fix their resource requirements before production deployments."
}
