{
  "$schema": "../../schemas/uc.schema.json",
  "id": "3.3.19",
  "title": "Ingress Controller Errors",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`sourcetype=openshift:haproxy`, `sourcetype=openshift:ingresscontroller`",
  "app": "OpenShift HAProxy log forwarding, `oc get ingresscontroller -n openshift-ingress-operator -o json` scripted input",
  "spl": "index=openshift sourcetype=\"openshift:haproxy\"\n| where status_code>=500\n| bin _time span=5m\n| stats count as errors by backend_name, status_code, _time\n| where errors>10\n| sort -errors",
  "cimModels": [
    "N/A"
  ],
  "description": "The OpenShift Ingress Controller (HAProxy-based router) handles all external traffic into the cluster. 5xx errors, backend connection failures, and route misconfigurations directly impact application availability.",
  "value": "The OpenShift Ingress Controller (HAProxy-based router) handles all external traffic into the cluster. 5xx errors, backend connection failures, and route misconfigurations directly impact application availability.",
  "grandmaExplanation": "We watch the front door of your cluster so you know when visitors are getting error pages instead of your applications.",
  "implementation": "Forward HAProxy access logs from the router pods or expose HAProxy stats. Parse status codes, backend names, and response times. Alert when 5xx error rate exceeds threshold per backend. Also monitor IngressController object for Degraded condition.",
  "visualization": "Timechart (5xx rate by backend), Table (backend, status, count), Error rate single value.",
  "references": [
    {
      "title": "OpenShift Ingress Operator documentation",
      "url": "https://docs.openshift.com/container-platform/latest/networking/ingress-operator.html"
    }
  ],
  "equipmentModels": [
    "kubernetes_openshift"
  ]
}
