{
  "$schema": "../../schemas/uc.schema.json",
  "id": "13.2.10",
  "title": "Fluentd / Fluent Bit Buffer Overflow",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault",
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Fluentd / Fluent Bit Buffer Overflow' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_fluentd_fluent_bit_buffer_overflow` (UC-13.2.10), SOC dashboard panel \"Operations — Fluentd / Fluent Bit Buffer Overflow\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-13.2.10 positive (Fluentd, Fluent, Bit, Buffer, Overflow): ingest synthetic events into `index=fluent` sourcetype='target_sourcetype' where `count` grouped by `host`, `plugin_id`, `output_plugin` satisfies `buffer_total_limit` > 0, `buffer_queue_length` > 0, `buffer_usage_pct` > 80, or `buffer_queue_length` < 1000 for 'Fluentd / Fluent Bit Buffer Overflow'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-13.2.10 negative (Buffer Overflow): ingest benign `fluent` / `target_sourcetype` events where `host`, `plugin_id`, `output_plugin` stays below `buffer_total_limit` > 0, `buffer_queue_length` > 0, `buffer_usage_pct` > 80, or `buffer_queue_length` < 1000, or exclude the test entity via `ops_exception_register.csv`. Variant 6 confirms zero alert rows for 'Fluentd / Fluent Bit Buffer Overflow'."
  },
  "dataSources": "Fluentd /api/plugins.json, Fluent Bit /api/v1/metrics Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom (Fluentd monitoring agent, Fluent Bit metrics) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=fluent sourcetype IN (\"fluentd:plugins\", \"fluentbit:metrics\")\n| eval buffer_usage_pct=if(isnum(buffer_queue_length) AND buffer_total_limit>0, round(buffer_queue_length/buffer_total_limit*100,1), null())\n| where buffer_queue_length > 0 AND (buffer_usage_pct > 80 OR buffer_total_limit - buffer_queue_length < 1000)\n| stats latest(buffer_queue_length) as queue_depth, latest(buffer_total_limit) as limit, latest(buffer_usage_pct) as pct by host, plugin_id, output_plugin\n| sort -pct",
  "description": "Detects Buffer Overflow anomalies for UC-13.2.10 with window `the scheduled window` on `index=fluent` sourcetype `configured sourcetype`. Combines `count` by `host`, `plugin_id`, `output_plugin` and compares against `buffer_total_limit` > 0, `buffer_queue_length` > 0, `buffer_usage_pct` > 80, or `buffer_queue_length` < 1000. Mapped to keywords Fluentd, Fluent, Bit, Buffer, Overflow for dashboard routing.",
  "value": "Alerting on Fluentd, Fluent, Bit, Buffer, Overflow through UC-13.2.10 reduces operational risk and delayed incident response by surfacing `activity` early.",
  "implementation": "For Fluentd, enable monitoring agent and poll `/api/plugins.json` (or use `in_monitor_agent`). For Fluent Bit, enable HTTP server and poll `/api/v1/metrics`. Ingest buffer_queue_length, buffer_total_limit, retry_count, and emit_count. Alert when buffer usage exceeds 80% or when retries spike. Correlate with downstream (Elasticsearch, Splunk) ingestion latency. Tune buffer size, flush interval, or add more workers.",
  "visualization": "Table (plugins with high buffer usage), Gauge (buffer fill % per output), Line chart (buffer depth over time), Bar chart (retries by plugin).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `fluent` (UC-13.2.10)** — Planned work for 'Fluentd / Fluent Bit Buffer Overflow' shifts `count` over `span=1h` while `host`, `plugin_id`, `output_plugin` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `configured_sourcetype`** — Scheduled automation elevates `plugin_id` without crossing `buffer_total_limit` > 0 for UC-13.2.10. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`, `plugin_id`, `output_plugin`** — Health checks in `fluent` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-13.2.10 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "elasticsearch",
    "log_pipeline"
  ],
  "equipmentModels": [
    "elasticsearch_es"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Custom (Fluentd monitoring agent, Fluent Bit metrics).\n• Ensure the following data sources are available: Fluentd /api/plugins.json, Fluent Bit /api/v1/metrics.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nFor Fluentd, enable monitoring agent and poll `/api/plugins.json` (or use `in_monitor_agent`). For Fluent Bit, enable HTTP server and poll `/api/v1/metrics`. Ingest buffer_queue_length, buffer_total_limit, retry_count, and emit_count. Alert when buffer usage exceeds 80% or when retries spike. Correlate with downstream (Elasticsearch, Splunk) ingestion latency. Tune buffer size, flush interval, or add more workers.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=fluent sourcetype IN (\"fluentd:plugins\", \"fluentbit:metrics\")\n| eval buffer_usage_pct=if(isnum(buffer_queue_length) AND buffer_total_limit>0, round(buffer_queue_length/buffer_total_limit*100,1), null())\n| where buffer_queue_length > 0 AND (buffer_usage_pct > 80 OR buffer_total_limit - buffer_queue_length < 1000)\n| stats latest(buffer_queue_length) as queue_depth, latest(buffer_total_limit) as limit, latest(buffer_usage_pct) as pct by host, plugin_id, output_plugin\n| sort -pct\n```\n\nUnderstanding this SPL\n\n**Fluentd / Fluent Bit Buffer Overflow** — Log forwarding buffer full, data at risk of being dropped.\n\nDocumented **Data sources**: Fluentd /api/plugins.json, Fluent Bit /api/v1/metrics. **App/TA** (typical add-on context): Custom (Fluentd monitoring agent, Fluent Bit metrics). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: fluent.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=fluent. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **buffer_usage_pct** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where buffer_queue_length > 0 AND (buffer_usage_pct > 80 OR buffer_total_limit - buffer_queue_length < 1000)` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by host, plugin_id, output_plugin** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nCompare with agent status, destination health, and a short local log sample on one host, then broaden if the pattern holds.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (plugins with high buffer usage), Gauge (buffer fill % per output), Line chart (buffer depth over time), Bar chart (retries by plugin).",
  "grandmaExplanation": "We read the forwarder and buffer logs for stress and give-ups so we see log loss risk before disks or patience run out.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
