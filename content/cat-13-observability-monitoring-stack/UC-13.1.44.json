{
  "$schema": "../../schemas/uc.schema.json",
  "id": "13.1.44",
  "title": "App Version Consistency Across SHC (operational inventory)",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Configuration"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'App Version Consistency Across SHC (operational inventory)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_app_version_consistency_across_shc_operational_i` (UC-13.1.44), SOC dashboard panel \"Operations — App Version Consistency Across SHC (operational inventory)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=inventory` sourcetype='splunk:shc_app_inventory' where `app_name` satisfies `ver_count` > 1 for UC-13.1.44 ('App Version Consistency Across SHC (operational inventory)'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `inventory` / `splunk:shc_app_inventory` events where `app_name` stays below `ver_count` > 1, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-13.1.44."
  },
  "dataSources": "`| rest /services/apps/local` per member, `sourcetype=splunk:shc_app_inventory` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "SHC, REST — Universal Forwarder (Splunkbase 617)",
  "spl": "index=inventory sourcetype=\"splunk:shc_app_inventory\"\n| stats values(app_version) as ver by app_name, member\n| eventstats dc(ver) as ver_count by app_name\n| where ver_count > 1\n| table app_name, member, ver",
  "description": "Mixed app versions on SHC members cause bundle skew and intermittent UI or search errors. Searches `index=inventory` with sourcetype `splunk:shc_app_inventory` for UC-13.1.44, aggregating `values` by `app_name` over `the scheduled window`. Alerts when grouped results cross `ver_count` > 1 in the saved search. Gives Operations operators a repeatable signal for 'App Version Consistency Across SHC (operational inventory)' without manual log review.",
  "value": "Early 'App Version Consistency Across SHC (operational inventory)' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Push inventory script via `runshellscript` or external job. Alert on mismatch for production apps. Exclude dev-only apps via lookup.",
  "visualization": "Matrix (app × member version), Table (mismatched apps).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `inventory`** — Planned work for 'App Version Consistency Across SHC (operational inventory)' shifts `values` over `span=1h` while `app_name` remains in-scope for UC-13.1.44. Cross-check CMDB CHG.\n\n2. **Batch `values` on `splunk:shc_app_inventory`** — Scheduled automation elevates `member` without crossing `ver_count` > 1. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `app_name`** — Health checks in `inventory` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `splunk:shc_app_inventory`** — First 48h baseline for `app_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `app_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: SHC, REST.\n• Ensure the following data sources are available: `| rest /services/apps/local` per member, `sourcetype=splunk:shc_app_inventory`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nPush inventory script via `runshellscript` or external job. Alert on mismatch for production apps. Exclude dev-only apps via lookup.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=inventory sourcetype=\"splunk:shc_app_inventory\"\n| stats values(app_version) as ver by app_name, member\n| eventstats dc(ver) as ver_count by app_name\n| where ver_count > 1\n| table app_name, member, ver\n```\n\nUnderstanding this SPL\n\n**App Version Consistency Across SHC (operational inventory)** — Mixed app versions on SHC members cause bundle skew and intermittent UI or search errors.\n\nDocumented **Data sources**: `| rest /services/apps/local` per member, `sourcetype=splunk:shc_app_inventory`. **App/TA** (typical add-on context): SHC, REST. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: inventory; **sourcetype**: splunk:shc_app_inventory. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=inventory, sourcetype=\"splunk:shc_app_inventory\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by app_name, member** so each row reflects one combination of those dimensions.\n• `eventstats` rolls up events into metrics; results are split **by app_name** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where ver_count > 1` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **App Version Consistency Across SHC (operational inventory)**): table app_name, member, ver\n\n\n\nStep 3 — Validate\nRun the search for a time range you can explain. Compare with the closest Grafana dashboard, APM UI, or Splunk monitoring console, and spot-check a few raw rows for fields that matter.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Matrix (app × member version), Table (mismatched apps).",
  "grandmaExplanation": "We watch platform health and capacity for search, indexing, and data delivery so the system we use for answers does not go dark or slow first.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
