{
  "$schema": "../../schemas/uc.schema.json",
  "id": "13.1.42",
  "title": "I/O Wait Bottleneck Detection (_introspection)",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'I/O Wait Bottleneck Detection (_introspection)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_i_o_wait_bottleneck_detection_introspection` (UC-13.1.42), SOC dashboard panel \"Operations — I/O Wait Bottleneck Detection (_introspection)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=_introspection` sourcetype='splunk_resource_usage' where `host` satisfies `io_wait_pct` > 25 for UC-13.1.42 ('I/O Wait Bottleneck Detection (_introspection)'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `_introspection` / `splunk_resource_usage` events where `host` stays below `io_wait_pct` > 25, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-13.1.42."
  },
  "dataSources": "`index=_introspection` `sourcetype=splunk_resource_usage` (disk I/O fields) / host metrics Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Monitoring Console, OTel/node_exporter — Universal Forwarder (Splunkbase 617)",
  "spl": "index=_introspection sourcetype=splunk_resource_usage\n| where data.io_wait_pct > 25 OR data.disk_busy_pct > 80\n| timechart span=5m avg(data.io_wait_pct) by host",
  "description": "High I/O wait on indexers correlates with slow searches, bucket replication, and ingestion lag. Searches `index=_introspection` with sourcetype `splunk_resource_usage` for UC-13.1.42, aggregating `avg` by `host` over `5m`. Alerts when grouped results cross `io_wait_pct` > 25 in the saved search. Gives Operations operators a repeatable signal for 'I/O Wait Bottleneck Detection (_introspection)' without manual log review.",
  "value": "Early 'I/O Wait Bottleneck Detection (_introspection)' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Field names depend on platform; normalize in `props`. Correlate with storage latency metrics from SAN/NVMe. Alert when sustained `io_wait` exceeds threshold.",
  "visualization": "Line chart (iowait %), Table (hosts with disk saturation).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `_introspection`** — Planned work for 'I/O Wait Bottleneck Detection (_introspection)' shifts `avg` over `span=5m` while `host` remains in-scope for UC-13.1.42. Cross-check CMDB CHG.\n\n2. **Batch `avg` on `splunk_resource_usage`** — Scheduled automation elevates `src` without crossing `io_wait_pct` > 25. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `_introspection` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `splunk_resource_usage`** — First 48h baseline for `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Monitoring Console, OTel/node_exporter.\n• Ensure the following data sources are available: `index=_introspection` `sourcetype=splunk_resource_usage` (disk I/O fields) / host metrics.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nField names depend on platform; normalize in `props`. Correlate with storage latency metrics from SAN/NVMe. Alert when sustained `io_wait` exceeds threshold.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=_introspection sourcetype=splunk_resource_usage\n| where data.io_wait_pct > 25 OR data.disk_busy_pct > 80\n| timechart span=5m avg(data.io_wait_pct) by host\n```\n\nUnderstanding this SPL\n\n**I/O Wait Bottleneck Detection (_introspection)** — High I/O wait on indexers correlates with slow searches, bucket replication, and ingestion lag.\n\nDocumented **Data sources**: `index=_introspection` `sourcetype=splunk_resource_usage` (disk I/O fields) / host metrics. **App/TA** (typical add-on context): Monitoring Console, OTel/node_exporter. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: _introspection; **sourcetype**: splunk_resource_usage. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=_introspection, sourcetype=splunk_resource_usage. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where data.io_wait_pct > 25 OR data.disk_busy_pct > 80` — typically the threshold or rule expression for this monitoring goal.\n• `timechart` plots the metric over time using **span=5m** buckets with a separate series **by host** — ideal for trending and alerting on this use case.\n\n\nStep 3 — Validate\nRun the search over calm and busy windows. Compare with the Splunk monitoring console and relevant health and introspection views, and spot-check key fields in sample events.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (iowait %), Table (hosts with disk saturation).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Monitoring Console, OTel/node_exporter — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=splunk_resource_usage into _introspection. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`disk_busy_pct, host, io_wait_pct`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We track machine and scheduler stress on the search side so a heavy job or a bad pattern does not starve the rest of the work.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
