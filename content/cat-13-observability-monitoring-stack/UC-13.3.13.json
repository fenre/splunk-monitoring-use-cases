{
  "$schema": "../../schemas/uc.schema.json",
  "id": "13.3.13",
  "title": "Grafana Datasource Health",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Grafana Datasource Health' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_grafana_datasource_health` (UC-13.3.13), SOC dashboard panel \"Operations — Grafana Datasource Health\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-13.3.13 positive (Grafana, Datasource, Health): ingest synthetic events into `index=grafana` sourcetype='grafana:datasource' where `avg_ms` grouped by `datasource_name`, `datasource_type`, `host` satisfies `response_time_ms` > 3000 for 'Grafana Datasource Health'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-13.3.13 negative (Datasource Health): ingest benign `grafana` / `grafana:datasource` events where `datasource_name`, `datasource_type`, `host` stays below `response_time_ms` > 3000, or exclude the test entity via `ops_exception_register.csv`. Variant 2 confirms zero alert rows for 'Grafana Datasource Health'."
  },
  "dataSources": "Grafana /api/datasources/proxy/:id, health check endpoint Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom (Grafana API) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=grafana sourcetype=\"grafana:datasource\"\n| where status!=\"success\" OR response_time_ms > 3000\n| stats count, avg(response_time_ms) as avg_ms, values(error_message) as errors by datasource_name, datasource_type, host\n| sort -count",
  "description": "Detects UC-13.3.13 for Grafana, Datasource, Health over `index=grafana` sourcetype `grafana:datasource` with thresholds `response_time_ms` > 3000. Groups `avg_ms` by `datasource_name`, `datasource_type`, `host` each `the scheduled window`. Centered on Datasource Health indicators. Supports repeatable operations triage for 'Grafana Datasource Health'.",
  "value": "For 'Grafana Datasource Health', UC-13.3.13 converts `avg_ms` on `grafana:datasource` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Use scripted input or scheduled search to call Grafana `/api/datasources` and `/api/datasources/proxy/:id/health` (or datasource-specific health endpoints). Ingest response status, latency, and error messages. Alternatively, parse Grafana server logs for datasource query errors. Alert when any datasource fails health check or when error rate exceeds threshold. Track which dashboards use each datasource for impact assessment.",
  "visualization": "Table (unhealthy datasources with errors), Status grid (datasource × status), Single value (healthy datasource count), Line chart (datasource latency trend).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `grafana` (UC-13.3.13)** — Planned work for 'Grafana Datasource Health' shifts `avg_ms` over `span=1h` while `datasource_name`, `datasource_type`, `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `grafana:datasource`** — Scheduled automation elevates `datasource_type` without crossing `response_time_ms` > 3000 for UC-13.3.13. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `datasource_name`, `datasource_type`, `host`** — Health checks in `grafana` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `grafana:datasource`** — First 48h baseline for UC-13.3.13 on `datasource_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `datasource_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "grafana"
  ],
  "equipmentModels": [
    "grafana_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Custom (Grafana API).\n• Ensure the following data sources are available: Grafana /api/datasources/proxy/:id, health check endpoint.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nUse scripted input or scheduled search to call Grafana `/api/datasources` and `/api/datasources/proxy/:id/health` (or datasource-specific health endpoints). Ingest response status, latency, and error messages. Alternatively, parse Grafana server logs for datasource query errors. Alert when any datasource fails health check or when error rate exceeds threshold. Track which dashboards use each datasource for impact assessment.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=grafana sourcetype=\"grafana:datasource\"\n| where status!=\"success\" OR response_time_ms > 3000\n| stats count, avg(response_time_ms) as avg_ms, values(error_message) as errors by datasource_name, datasource_type, host\n| sort -count\n```\n\nUnderstanding this SPL\n\n**Grafana Datasource Health** — Datasource connectivity and query errors; broken dashboards.\n\nDocumented **Data sources**: Grafana /api/datasources/proxy/:id, health check endpoint. **App/TA** (typical add-on context): Custom (Grafana API). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: grafana; **sourcetype**: grafana:datasource. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=grafana, sourcetype=\"grafana:datasource\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where status!=\"success\" OR response_time_ms > 3000` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by datasource_name, datasource_type, host** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nCompare the series with the Grafana dashboards and, where relevant, Prometheus, Loki, or target health for the same interval.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (unhealthy datasources with errors), Status grid (datasource × status), Single value (healthy datasource count), Line chart (datasource latency trend).\n\nScripted input (generic example)\nThis use case relies on a scripted input. In the app's local/inputs.conf add a stanza such as:\n\n```ini\n[script://$SPLUNK_HOME/etc/apps/YourApp/bin/collect.sh]\ninterval = 300\nsourcetype = your_sourcetype\nindex = main\ndisabled = 0\n```\n\nThe script should print one event per line (e.g. key=value). Example minimal script (bash):\n\n```bash\n#!/usr/bin/env bash\n# Output metrics or events, one per line\necho \"metric=value timestamp=$(date +%s)\"\n```\n\nFor full details (paths, scheduling, permissions), see the Implementation guide: docs/implementation-guide.md",
  "grandmaExplanation": "We watch HTTP data intake so broken or busy endpoints do not starve the rest of our monitoring.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
