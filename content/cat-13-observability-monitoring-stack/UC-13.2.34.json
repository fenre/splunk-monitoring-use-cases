{
  "$schema": "../../schemas/uc.schema.json",
  "id": "13.2.34",
  "title": "Episode MTTR Analysis by Service Tier",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Episode MTTR Analysis by Service Tier' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_episode_mttr_analysis_by_service_tier` (UC-13.2.34), SOC dashboard panel \"Operations — Episode MTTR Analysis by Service Tier\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-13.2.34 positive (Episode, MTTR, Analysis, Service, Tier): ingest synthetic events into `index=itsi_grouped_alerts` sourcetype='target_sourcetype' where `avg_mttr`, `median_mttr` grouped by `tier` satisfies `severity` >= 6, or `severity` >= 4 for 'Episode MTTR Analysis by Service Tier'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-13.2.34 negative (Service Tier): ingest benign `itsi_grouped_alerts` / `target_sourcetype` events where `tier` stays below `severity` >= 6, or `severity` >= 4, or exclude the test entity via `ops_exception_register.csv`. Variant 3 confirms zero alert rows for 'Episode MTTR Analysis by Service Tier'."
  },
  "dataSources": "`itsi_grouped_alerts` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk ITSI — Universal Forwarder (Splunkbase 617)",
  "spl": "index=itsi_grouped_alerts status=5\n| eval create_time=strptime(create_time, \"%Y-%m-%dT%H:%M:%S\")\n| eval close_time=strptime(mod_time, \"%Y-%m-%dT%H:%M:%S\")\n| eval mttr_minutes=round((close_time - create_time) / 60, 1)\n| eval tier=case(\n    severity>=6, \"Tier 1 - Critical\",\n    severity>=4, \"Tier 2 - Important\",\n    1=1, \"Tier 3 - Internal\"\n)\n| stats avg(mttr_minutes) as avg_mttr median(mttr_minutes) as median_mttr count by tier\n| sort tier",
  "description": "Alerts on when Service Tier criteria fire for 'Episode MTTR Analysis by Service Tier' (UC-13.2.34) in `index=itsi_grouped_alerts` sourcetype `configured sourcetype`. Pipeline aggregates `avg_mttr`, `median_mttr` by `tier` over `the scheduled window`. Alert logic enforces `severity` >= 6, or `severity` >= 4. Title terms: Episode, MTTR, Analysis, Service, Tier. Eliminates manual hunting for UC-13.2.34 conditions.",
  "value": "UC-13.2.34 helps teams contain Episode, MTTR, Analysis, Service, Tier scenarios and reduce operational risk and delayed incident response before incidents compound.",
  "implementation": "Define service tiers based on business impact (severity mapping). Track MTTR per tier over time. Set targets: Tier 1 < 15 min, Tier 2 < 60 min, Tier 3 < 4 hours. Analyze outliers to identify process gaps. Correlate MTTR with time-of-day and team assignment for resource optimization.",
  "visualization": "Bar chart (avg MTTR by tier), Line chart (MTTR trend over weeks), Table (slowest-resolved episodes).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk ITSI",
      "url": "https://splunkbase.splunk.com/app/1841",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `itsi_grouped_alerts` (UC-13.2.34)** — Planned work for 'Episode MTTR Analysis by Service Tier' shifts `avg_mttr`, `median_mttr` over `span=1h` while `tier` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `configured_sourcetype`** — Scheduled automation elevates `create_time` without crossing `severity` >= 6 for UC-13.2.34. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `tier`** — Health checks in `itsi_grouped_alerts` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-13.2.34 on `tier` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `tier`, `exception_reason`, `valid_until`.",
  "equipment": [
    "itsi"
  ],
  "equipmentModels": [
    "itsi_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    {
      "name": "Splunk ITSI",
      "displayName": "Splunk IT Service Intelligence",
      "note": "ITSI"
    }
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk ITSI.\n• Ensure the following data sources are available: `itsi_grouped_alerts`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nDefine service tiers based on business impact (severity mapping). Track MTTR per tier over time. Set targets: Tier 1 < 15 min, Tier 2 < 60 min, Tier 3 < 4 hours. Analyze outliers to identify process gaps. Correlate MTTR with time-of-day and team assignment for resource optimization.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=itsi_grouped_alerts status=5\n| eval create_time=strptime(create_time, \"%Y-%m-%dT%H:%M:%S\")\n| eval close_time=strptime(mod_time, \"%Y-%m-%dT%H:%M:%S\")\n| eval mttr_minutes=round((close_time - create_time) / 60, 1)\n| eval tier=case(\n    severity>=6, \"Tier 1 - Critical\",\n    severity>=4, \"Tier 2 - Important\",\n    1=1, \"Tier 3 - Internal\"\n)\n| stats avg(mttr_minutes) as avg_mttr median(mttr_minutes) as median_mttr count by tier\n| sort tier\n```\n\nUnderstanding this SPL\n\n**Episode MTTR Analysis by Service Tier** — Breaking MTTR down by service tier (Tier 1 critical, Tier 2 important, Tier 3 internal) reveals whether high-priority services get faster resolution and identifies process bottlenecks.\n\nDocumented **Data sources**: `itsi_grouped_alerts`. **App/TA** (typical add-on context): Splunk ITSI. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: itsi_grouped_alerts.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=itsi_grouped_alerts. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **create_time** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **close_time** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **mttr_minutes** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **tier** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by tier** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nRun the search when you know the service or episode context. Compare with the ITSI Service Analyzer, episode or notable views, and Glass Tables or drill-downs for the same time range.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Bar chart (avg MTTR by tier), Line chart (MTTR trend over weeks), Table (slowest-resolved episodes).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk ITSI — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into itsi_grouped_alerts. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`create_time, mod_time, severity, tier`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We see how long it takes to calm trouble by how important a service is so we can tell whether priority really drives speed.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
