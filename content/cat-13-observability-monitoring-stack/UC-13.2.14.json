{
  "$schema": "../../schemas/uc.schema.json",
  "id": "13.2.14",
  "title": "Glass Table SLA Breaches",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Glass Table SLA Breaches' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_glass_table_sla_breaches` (UC-13.2.14), SOC dashboard panel \"Operations — Glass Table SLA Breaches\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-13.2.14 positive (Glass, Table, SLA, Breaches): ingest synthetic events into `index=itsi_summary` sourcetype='target_sourcetype' where `count` grouped by `service_name`, `kpi_name` satisfies `severity_value` >= 4 for 'Glass Table SLA Breaches'. Validate lookup `sla_targets`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-13.2.14 negative (SLA Breaches): ingest benign `itsi_summary` / `target_sourcetype` events where `service_name`, `kpi_name` stays below `severity_value` >= 4, or exclude the test entity via `ops_exception_register.csv`. Variant 13 confirms zero alert rows for 'Glass Table SLA Breaches'."
  },
  "dataSources": "ITSI KPIs, `itsi_summary`, SLA lookup Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk ITSI Glass Tables — Universal Forwarder (Splunkbase 617)",
  "spl": "index=itsi_summary\n| lookup sla_targets service_name OUTPUT kpi_name, sla_target\n| where health_score < sla_target OR severity_value>=4\n| stats count by service_name, kpi_name\n| sort -count",
  "description": "Monitors UC-13.2.14 — Glass, Table, SLA, Breaches — using `index=itsi_summary` sourcetype `configured sourcetype`, grouped by `service_name`, `kpi_name`. Outputs `count` each `the scheduled window`; alert when `severity_value` >= 4. Filter chain includes lookup `sla_targets`. Purpose-built for 'Glass Table SLA Breaches' in Operations workflows.",
  "value": "UC-13.2.14 gives Operations teams earlier visibility into SLA Breaches through lookup `sla_targets`, lowering operational risk and delayed incident response.",
  "implementation": "Maintain `sla_targets` lookup with minimum health score or max severity per service. Drive Glass Table color thresholds from the same search. Alert when executive-facing services breach SLA for >15 minutes.",
  "visualization": "Glass Table (SLA status), KPI ticker (breached services), Table (breach duration).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk ITSI",
      "url": "https://splunkbase.splunk.com/app/1841",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `itsi_summary` (UC-13.2.14)** — Planned work for 'Glass Table SLA Breaches' shifts `count` over `span=1h` while `service_name`, `kpi_name` remains in-scope. Applies lookup `sla_targets`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `configured_sourcetype`** — Scheduled automation elevates `kpi_name` without crossing `severity_value` >= 4 for UC-13.2.14. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `service_name`, `kpi_name`** — Health checks in `itsi_summary` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-13.2.14 on `service_name` is unstable; join `sla_targets` prior to threshold.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `service_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "itsi"
  ],
  "equipmentModels": [
    "itsi_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    {
      "name": "Splunk ITSI",
      "displayName": "Splunk IT Service Intelligence",
      "note": "ITSI"
    }
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk ITSI Glass Tables.\n• Ensure the following data sources are available: ITSI KPIs, `itsi_summary`, SLA lookup.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMaintain `sla_targets` lookup with minimum health score or max severity per service. Drive Glass Table color thresholds from the same search. Alert when executive-facing services breach SLA for >15 minutes.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=itsi_summary\n| lookup sla_targets service_name OUTPUT kpi_name, sla_target\n| where health_score < sla_target OR severity_value>=4\n| stats count by service_name, kpi_name\n| sort -count\n```\n\nUnderstanding this SPL\n\n**Glass Table SLA Breaches** — Glass Tables for NOC must reflect SLA-backed KPIs; breaches on the wallboard drive incident prioritization.\n\nDocumented **Data sources**: ITSI KPIs, `itsi_summary`, SLA lookup. **App/TA** (typical add-on context): Splunk ITSI Glass Tables. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: itsi_summary.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=itsi_summary. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Enriches events using `lookup` (lookup definition + optional OUTPUT fields).\n• Filters the current rows with `where health_score < sla_target OR severity_value>=4` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by service_name, kpi_name** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nRun the search when you know the service or episode context. Compare with the ITSI Service Analyzer, episode or notable views, and Glass Tables or drill-downs for the same time range.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Glass Table (SLA status), KPI ticker (breached services), Table (breach duration).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk ITSI Glass Tables — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into itsi_summary. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`kpi_name, service_name, severity_value, sla_target`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We track service health, episodes, and related signals in one workspace so the team can see what broke and who owns it without juggling every console.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
