{
  "$schema": "../../schemas/uc.schema.json",
  "id": "13.2.25",
  "title": "MLTK Custom Anomaly Detection on KPI Data",
  "criticality": "medium",
  "difficulty": "expert",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'MLTK Custom Anomaly Detection on KPI Data' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_mltk_custom_anomaly_detection_on_kpi_data` (UC-13.2.25), SOC dashboard panel \"Operations — MLTK Custom Anomaly Detection on KPI Data\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-13.2.25 positive (MLTK, Custom, Anomaly, Detection, KPI): ingest synthetic events into `index=itsi_summary` sourcetype='target_sourcetype' where `avg` grouped by `kpi_name` satisfies `isOutlier` > 0 for 'MLTK Custom Anomaly Detection on KPI Data'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-13.2.25 negative (KPI Data): ingest benign `itsi_summary` / `target_sourcetype` events where `kpi_name` stays below `isOutlier` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 6 confirms zero alert rows for 'MLTK Custom Anomaly Detection on KPI Data'."
  },
  "dataSources": "`itsi_summary`, MLTK models Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk ITSI, Splunk Machine Learning Toolkit (MLTK) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=itsi_summary is_service_aggregate=0 kpi_name IN (\"CPU Utilization\", \"Memory Usage\", \"Response Time\")\n| timechart span=5m avg(alert_value) by kpi_name\n| fit DensityFunction \"CPU Utilization\" \"Memory Usage\" \"Response Time\" into itsi_multivariate_model\n| where isOutlier > 0",
  "description": "Monitors KPI Data anomalies for UC-13.2.25 with window `5m` on `index=itsi_summary` sourcetype `configured sourcetype`. Combines `avg` by `kpi_name` and compares against `isOutlier` > 0. Mapped to keywords MLTK, Custom, Anomaly, Detection, KPI for dashboard routing.",
  "value": "Alerting on MLTK, Custom, Anomaly, Detection, KPI through UC-13.2.25 reduces operational risk and delayed incident response by surfacing `activity` early.",
  "implementation": "Extract KPI data from `itsi_summary`. Build MLTK models (DensityFunction for outlier detection, RandomForestRegressor for prediction). Create residual KPIs: predicted vs actual values. Feed MLTK output back as ITSI KPIs for service health scoring. Retrain models monthly or on significant infrastructure changes.",
  "visualization": "Scatter plot (multi-dimensional KPI space with outliers highlighted), Line chart (residual KPI over time).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk ITSI",
      "url": "https://splunkbase.splunk.com/app/1841",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `itsi_summary` (UC-13.2.25)** — Planned work for 'MLTK Custom Anomaly Detection on KPI Data' shifts `avg` over `span=5m` while `kpi_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `configured_sourcetype`** — Scheduled automation elevates `src` without crossing `isOutlier` > 0 for UC-13.2.25. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `kpi_name`** — Health checks in `itsi_summary` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-13.2.25 on `kpi_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `kpi_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "itsi"
  ],
  "equipmentModels": [
    "itsi_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    {
      "name": "Splunk ITSI",
      "displayName": "Splunk IT Service Intelligence",
      "note": "ITSI"
    }
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk ITSI, Splunk Machine Learning Toolkit (MLTK).\n• Ensure the following data sources are available: `itsi_summary`, MLTK models.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nExtract KPI data from `itsi_summary`. Build MLTK models (DensityFunction for outlier detection, RandomForestRegressor for prediction). Create residual KPIs: predicted vs actual values. Feed MLTK output back as ITSI KPIs for service health scoring. Retrain models monthly or on significant infrastructure changes.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=itsi_summary is_service_aggregate=0 kpi_name IN (\"CPU Utilization\", \"Memory Usage\", \"Response Time\")\n| timechart span=5m avg(alert_value) by kpi_name\n| fit DensityFunction \"CPU Utilization\" \"Memory Usage\" \"Response Time\" into itsi_multivariate_model\n| where isOutlier > 0\n```\n\nUnderstanding this SPL\n\n**MLTK Custom Anomaly Detection on KPI Data** — Combining MLTK with ITSI KPI data enables custom anomaly models that detect multi-dimensional patterns (e.g., CPU+memory+latency correlation) impossible with single-KPI thresholds.\n\nDocumented **Data sources**: `itsi_summary`, MLTK models. **App/TA** (typical add-on context): Splunk ITSI, Splunk Machine Learning Toolkit (MLTK). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: itsi_summary.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=itsi_summary. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `timechart` plots the metric over time using **span=5m** buckets with a separate series **by kpi_name** — ideal for trending and alerting on this use case.\n• Pipeline stage (see **MLTK Custom Anomaly Detection on KPI Data**): fit DensityFunction \"CPU Utilization\" \"Memory Usage\" \"Response Time\" into itsi_multivariate_model\n• Filters the current rows with `where isOutlier > 0` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nRun the search when you know the service or episode context. Compare with the ITSI Service Analyzer, episode or notable views, and Glass Tables or drill-downs for the same time range.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Scatter plot (multi-dimensional KPI space with outliers highlighted), Line chart (residual KPI over time).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk ITSI, Splunk Machine Learning Toolkit (MLTK) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into itsi_summary. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`isOutlier, kpi_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We track service health, episodes, and related signals in one workspace so the team can see what broke and who owns it without juggling every console.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
