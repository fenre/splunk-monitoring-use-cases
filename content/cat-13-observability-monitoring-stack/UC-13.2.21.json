{
  "$schema": "../../schemas/uc.schema.json",
  "id": "13.2.21",
  "title": "Content Pack Deployment Health (Monitoring and Alerting)",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Content Pack Deployment Health (Monitoring and Alerting)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_content_pack_deployment_health_monitoring_and_al` (UC-13.2.21), SOC dashboard panel \"Operations — Content Pack Deployment Health (Monitoring and Alerting)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=_internal` sourcetype='scheduler' where `savedsearch_name` satisfies `failed` > 0 for UC-13.2.21 ('Content Pack Deployment Health (Monitoring and Alerting)'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `_internal` / `scheduler` events where `savedsearch_name` stays below `failed` > 0, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-13.2.21."
  },
  "dataSources": "`index=_internal sourcetype=scheduler`, `itsi_tracked_alerts`, `itsi_grouped_alerts` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk ITSI, DA-ITSI-CP-Monitoring-and-Alerting — Universal Forwarder (Splunkbase 617)",
  "spl": "index=_internal sourcetype=scheduler app=\"DA-ITSI-CP-Monitoring-and-Alerting\"\n| stats count(eval(status=\"success\")) as success count(eval(status=\"skipped\")) as skipped count(eval(status!=\"success\" AND status!=\"skipped\")) as failed by savedsearch_name\n| eval health=if(failed>0 OR skipped>success, \"degraded\", \"healthy\")\n| sort -failed -skipped",
  "description": "The Monitoring and Alerting content pack provides pre-built correlation searches and aggregation policies. Tracking deployment health ensures these critical components remain functional.",
  "value": "Early 'Content Pack Deployment Health (Monitoring and Alerting)' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Install the Monitoring and Alerting content pack via ITSI UI. Enable correlation searches incrementally. Monitor the lookup generator reports (schedule daily). Track notable event flow rates to confirm the pipeline is working. Alert on correlation search failures or skipped executions.",
  "visualization": "Table (search name, status, skip rate), Single value (healthy/degraded count).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk ITSI",
      "url": "https://splunkbase.splunk.com/app/1841",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `_internal`** — Planned work for 'Content Pack Deployment Health (Monitoring and Alerting)' shifts `count` over `span=1h` while `savedsearch_name` remains in-scope for UC-13.2.21. Cross-check CMDB CHG.\n\n2. **Batch `count` on `scheduler`** — Scheduled automation elevates `health` without crossing `failed` > 0. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `savedsearch_name`** — Health checks in `_internal` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `scheduler`** — First 48h baseline for `savedsearch_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `savedsearch_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "itsi"
  ],
  "equipmentModels": [
    "itsi_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    {
      "name": "Splunk ITSI",
      "displayName": "Splunk IT Service Intelligence",
      "note": "ITSI"
    }
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk ITSI, DA-ITSI-CP-Monitoring-and-Alerting.\n• Ensure the following data sources are available: `index=_internal sourcetype=scheduler`, `itsi_tracked_alerts`, `itsi_grouped_alerts`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nInstall the Monitoring and Alerting content pack via ITSI UI. Enable correlation searches incrementally. Monitor the lookup generator reports (schedule daily). Track notable event flow rates to confirm the pipeline is working. Alert on correlation search failures or skipped executions.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=_internal sourcetype=scheduler app=\"DA-ITSI-CP-Monitoring-and-Alerting\"\n| stats count(eval(status=\"success\")) as success count(eval(status=\"skipped\")) as skipped count(eval(status!=\"success\" AND status!=\"skipped\")) as failed by savedsearch_name\n| eval health=if(failed>0 OR skipped>success, \"degraded\", \"healthy\")\n| sort -failed -skipped\n```\n\nUnderstanding this SPL\n\n**Content Pack Deployment Health (Monitoring and Alerting)** — The Monitoring and Alerting content pack provides pre-built correlation searches and aggregation policies. Tracking deployment health ensures these critical components remain functional.\n\nDocumented **Data sources**: `index=_internal sourcetype=scheduler`, `itsi_tracked_alerts`, `itsi_grouped_alerts`. **App/TA** (typical add-on context): Splunk ITSI, DA-ITSI-CP-Monitoring-and-Alerting. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: _internal; **sourcetype**: scheduler. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=_internal, sourcetype=scheduler. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by savedsearch_name** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **health** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nRun the search over calm and busy windows. Compare with the Splunk monitoring console and relevant health and introspection views, and spot-check key fields in sample events.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (search name, status, skip rate), Single value (healthy/degraded count).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk ITSI, DA-ITSI-CP-Monitoring-and-Alerting — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=scheduler into _internal. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`failed, health, savedsearch_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We track service health, episodes, and related signals in one workspace so the team can see what broke and who owns it without juggling every console.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
