{
  "$schema": "../../schemas/uc.schema.json",
  "id": "13.2.33",
  "title": "Business Service SLA Composite Scoring",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Business Service SLA Composite Scoring' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_business_service_sla_composite_scoring` (UC-13.2.33), SOC dashboard panel \"Operations — Business Service SLA Composite Scoring\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-13.2.33 positive (Business, Service, SLA, Composite, Scoring): ingest synthetic events into `index=itsi_summary` sourcetype='target_sourcetype' where `daily_health`, `days_met` grouped by `_time`, `service_name` satisfies `daily_health` >= 70 for 'Business Service SLA Composite Scoring'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-13.2.33 negative (Composite Scoring): ingest benign `itsi_summary` / `target_sourcetype` events where `_time`, `service_name` stays below `daily_health` >= 70, or exclude the test entity via `ops_exception_register.csv`. Variant 1 confirms zero alert rows for 'Business Service SLA Composite Scoring'."
  },
  "dataSources": "`itsi_summary`, SLA definitions Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk ITSI — Universal Forwarder (Splunkbase 617)",
  "spl": "index=itsi_summary is_service_aggregate=1\n  service_name IN (\"Payment Gateway\", \"Customer Portal\", \"API Platform\")\n| bin _time span=1d\n| stats avg(health_score) as daily_health by _time service_name\n| eval sla_met=if(daily_health >= 70, 1, 0)\n| stats sum(sla_met) as days_met count as total_days by service_name\n| eval sla_pct=round(days_met/total_days*100, 3)\n| eval sla_target=99.9\n| eval sla_status=if(sla_pct >= sla_target, \"MET\", \"BREACHED\")",
  "description": "Detects Composite Scoring activity in UC-13.2.33 ('Business Service SLA Composite Scoring') from `index=itsi_summary` sourcetype `configured sourcetype`. Rolls up `daily_health`, `days_met` by `_time`, `service_name` across `1d` and pages when `daily_health` >= 70. Covers Business, Service, SLA, Composite, Scoring. Operations operators treat UC-13.2.33 as the canonical signal.",
  "value": "Operators running UC-13.2.33 on UC-13.2.33 search results shorten triage for Composite Scoring tied to 'Business Service SLA Composite Scoring', lowering operational risk and delayed incident response.",
  "implementation": "Define SLA targets per business service (e.g., 99.9% availability). Map ITSI health score thresholds to SLA compliance (health >= 70 = available). Calculate daily/monthly/quarterly SLA metrics. Use Glass Tables for executive dashboards showing SLA status. Alert on projected SLA breach based on error budget burn rate. Integrate with ITSM for SLA violation reporting.",
  "visualization": "Glass Table (SLA dashboard), Single value (current SLA %), Gauge (error budget remaining), Table (service SLA history).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk ITSI",
      "url": "https://splunkbase.splunk.com/app/1841",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `itsi_summary` (UC-13.2.33)** — Planned work for 'Business Service SLA Composite Scoring' shifts `daily_health`, `days_met` over `span=1d` while `_time`, `service_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `configured_sourcetype`** — Scheduled automation elevates `service_name` without crossing `daily_health` >= 70 for UC-13.2.33. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `_time`, `service_name`** — Health checks in `itsi_summary` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-13.2.33 on `_time` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `_time`, `exception_reason`, `valid_until`.",
  "equipment": [
    "itsi"
  ],
  "equipmentModels": [
    "itsi_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    {
      "name": "Splunk ITSI",
      "displayName": "Splunk IT Service Intelligence",
      "note": "ITSI"
    }
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk ITSI.\n• Ensure the following data sources are available: `itsi_summary`, SLA definitions.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nDefine SLA targets per business service (e.g., 99.9% availability). Map ITSI health score thresholds to SLA compliance (health >= 70 = available). Calculate daily/monthly/quarterly SLA metrics. Use Glass Tables for executive dashboards showing SLA status. Alert on projected SLA breach based on error budget burn rate. Integrate with ITSM for SLA violation reporting.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=itsi_summary is_service_aggregate=1\n  service_name IN (\"Payment Gateway\", \"Customer Portal\", \"API Platform\")\n| bin _time span=1d\n| stats avg(health_score) as daily_health by _time service_name\n| eval sla_met=if(daily_health >= 70, 1, 0)\n| stats sum(sla_met) as days_met count as total_days by service_name\n| eval sla_pct=round(days_met/total_days*100, 3)\n| eval sla_target=99.9\n| eval sla_status=if(sla_pct >= sla_target, \"MET\", \"BREACHED\")\n```\n\nUnderstanding this SPL\n\n**Business Service SLA Composite Scoring** — Composite SLA scores aggregate ITSI health data across services to produce contractual SLA metrics (e.g., 99.9% availability), directly supporting customer and executive reporting.\n\nDocumented **Data sources**: `itsi_summary`, SLA definitions. **App/TA** (typical add-on context): Splunk ITSI. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: itsi_summary.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=itsi_summary. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by _time service_name** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **sla_met** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by service_name** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **sla_pct** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **sla_target** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **sla_status** — often to normalize units, derive a ratio, or prepare for thresholds.\n\n\nStep 3 — Validate\nRun the search when you know the service or episode context. Compare with the ITSI Service Analyzer, episode or notable views, and Glass Tables or drill-downs for the same time range.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Glass Table (SLA dashboard), Single value (current SLA %), Gauge (error budget remaining), Table (service SLA history).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk ITSI — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into itsi_summary. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, daily_health, service_name, sla_met`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We track service health, episodes, and related signals in one workspace so the team can see what broke and who owns it without juggling every console.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
