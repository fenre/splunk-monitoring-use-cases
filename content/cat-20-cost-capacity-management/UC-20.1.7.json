{
  "$schema": "../../schemas/uc.schema.json",
  "id": "20.1.7",
  "title": "Spot/Preemptible Instance Tracking",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Spot/Preemptible Instance Tracking' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_spot_preemptible_instance_tracking` (UC-20.1.7), SOC dashboard panel \"Operations — Spot/Preemptible Instance Tracking\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-20.1.7 positive (Spot, Preemptible, Instance, Tracking): ingest synthetic events into `index=aws` sourcetype='aws:cloudtrail' where `count` grouped by `requestParameters.instanceId`, `requestParameters.instanceType`, `userIdentity.arn` satisfies detection threshold for 'Spot/Preemptible Instance Tracking'. Validate lookup `spot_savings`, filter `eventName=\"BidEvictedEvent\" OR eventName=\"SpotInstanceInterr`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-20.1.7 negative (Instance Tracking): ingest benign `aws` / `aws:cloudtrail` events where `requestParameters.instanceId`, `requestParameters.instanceType`, `userIdentity.arn` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 2 confirms zero alert rows for 'Spot/Preemptible Instance Tracking'."
  },
  "dataSources": "EC2 spot instance events, billing data Collected via Splunk Add-on for Amazon Web Services (Splunkbase 1876) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk Add-on for AWS`, EC2 event logs — Splunk Add-on for Amazon Web Services (Splunkbase 1876)",
  "spl": "index=aws sourcetype=\"aws:cloudtrail\"\n| search eventName=\"BidEvictedEvent\" OR eventName=\"SpotInstanceInterruption\"\n| stats count as interruptions by requestParameters.instanceId, requestParameters.instanceType, userIdentity.arn\n| lookup spot_savings instance_id as requestParameters.instanceId OUTPUT on_demand_cost, spot_cost\n| eval savings=on_demand_cost-spot_cost\n| eval savings_pct=round((savings/on_demand_cost)*100, 1)\n| table requestParameters.instanceId, requestParameters.instanceType, interruptions, on_demand_cost, spot_cost, savings_pct",
  "description": "Monitors UC-20.1.7 for Spot, Preemptible, Instance, Tracking over `index=aws` sourcetype `aws:cloudtrail` with thresholds configured threshold. Groups `count` by `requestParameters.instanceId`, `requestParameters.instanceType`, `userIdentity.arn` each `the scheduled window`. Differentiated by lookup `spot_savings`, filter `eventName=\"BidEvictedEvent\" OR eventName=\"SpotInstanceInterr`. Supports repeatable operations triage for 'Spot/Preemptible Instance Tracking'.",
  "value": "For 'Spot/Preemptible Instance Tracking', UC-20.1.7 converts `activity` on `aws:cloudtrail` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Track spot instance lifecycle events via CloudTrail. Monitor interruption frequency by instance type and AZ. Calculate savings vs on-demand pricing. Alert on interruption rate spikes affecting critical workloads. Report monthly spot savings to justify continued spot adoption.",
  "visualization": "Bar chart (interruptions by type), Timechart (interruption frequency), Single value (monthly spot savings), Table (instance interruption details).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Add-on for AWS",
      "url": "https://splunkbase.splunk.com/app/1876",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `aws` (UC-20.1.7)** — Planned work for 'Spot/Preemptible Instance Tracking' shifts `count` over `span=1h` while `requestParameters.instanceId`, `requestParameters.instanceType`, `userIdentity.arn` remains in-scope. Applies lookup `spot_savings`, filter `eventName=\"BidEvictedEvent\" OR eventName=\"SpotInstanceInterr`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `aws:cloudtrail`** — Scheduled automation elevates `requestParameters.instanceType` without crossing configured threshold for UC-20.1.7. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `requestParameters.instanceId`, `requestParameters.instanceType`, `userIdentity.arn`** — Health checks in `aws` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `aws:cloudtrail`** — First 48h baseline for UC-20.1.7 on `requestParameters.instanceId` is unstable; join `spot_savings` prior to threshold.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `requestParameters.instanceId`, `exception_reason`, `valid_until`.",
  "equipment": [
    "aws"
  ],
  "equipmentModels": [
    "aws_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk Add-on for AWS`, EC2 event logs.\n• Ensure the following data sources are available: EC2 spot instance events, billing data.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nTrack spot instance lifecycle events via CloudTrail. Monitor interruption frequency by instance type and AZ. Calculate savings vs on-demand pricing. Alert on interruption rate spikes affecting critical workloads. Report monthly spot savings to justify continued spot adoption.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=aws sourcetype=\"aws:cloudtrail\"\n| search eventName=\"BidEvictedEvent\" OR eventName=\"SpotInstanceInterruption\"\n| stats count as interruptions by requestParameters.instanceId, requestParameters.instanceType, userIdentity.arn\n| lookup spot_savings instance_id as requestParameters.instanceId OUTPUT on_demand_cost, spot_cost\n| eval savings=on_demand_cost-spot_cost\n| eval savings_pct=round((savings/on_demand_cost)*100, 1)\n| table requestParameters.instanceId, requestParameters.instanceType, interruptions, on_demand_cost, spot_cost, savings_pct\n```\n\nUnderstanding this SPL\n\n**Spot/Preemptible Instance Tracking** — Spot instances offer significant savings (60-90%) but can be interrupted. Tracking interruptions, savings achieved, and workload placement ensures teams maximize savings while maintaining application resilience.\n\nDocumented **Data sources**: EC2 spot instance events, billing data. **App/TA** (typical add-on context): `Splunk Add-on for AWS`, EC2 event logs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: aws; **sourcetype**: aws:cloudtrail. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=aws, sourcetype=\"aws:cloudtrail\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Applies an explicit `search` filter to narrow the current result set.\n• `stats` rolls up events into metrics; results are split **by requestParameters.instanceId, requestParameters.instanceType, userIdentity.arn** so each row reflects one combination of those dimensions.\n• Enriches events using `lookup` (lookup definition + optional OUTPUT fields).\n• `eval` defines or adjusts **savings** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **savings_pct** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Pipeline stage (see **Spot/Preemptible Instance Tracking**): table requestParameters.instanceId, requestParameters.instanceType, interruptions, on_demand_cost, spot_cost, savings_pct\n\n\nStep 3 — Validate\ncompare with Cost Explorer; compare with the cloud billing console for the same scope, accounts, and time range.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Bar chart (interruptions by type), Timechart (interruption frequency), Single value (monthly spot savings), Table (instance interruption details).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk Add-on for AWS`, EC2 event logs — Splunk Add-on for Amazon Web Services (Splunkbase 1876)). Enable the modular input or HEC token that writes sourcetype=aws:cloudtrail into aws. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`instance_id, on_demand_cost, requestParameters.instanceId, requestParameters.instanceType`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Cost and capacity tools watch how much we spend on cloud and the data center, and where we have spare or strained capacity.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
