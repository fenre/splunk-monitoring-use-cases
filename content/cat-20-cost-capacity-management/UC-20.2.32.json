{
  "$schema": "../../schemas/uc.schema.json",
  "id": "20.2.32",
  "title": "GPU Pool Utilization for ML Workload Capacity",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Capacity",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'GPU Pool Utilization for ML Workload Capacity' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_gpu_pool_utilization_for_ml_workload_capacity` (UC-20.2.32), SOC dashboard panel \"Operations — GPU Pool Utilization for ML Workload Capacity\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-20.2.32 positive (GPU, Pool, Utilization, for, Workload): ingest synthetic events into `index=ml_infra` sourcetype='dcgm:gpu' where `avg_sm`, `fleet_avg`, `fleet_p95` grouped by `_time`, `host`, `gpu_index` satisfies `fleet_avg` < 35, `fleet_p95` < 70, or `fleet_p95` > 92 for 'GPU Pool Utilization for ML Workload Capacity'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-20.2.32 negative (Workload Capacity): ingest benign `ml_infra` / `dcgm:gpu` events where `_time`, `host`, `gpu_index` stays below `fleet_avg` < 35, `fleet_p95` < 70, or `fleet_p95` > 92, or exclude the test entity via `ops_exception_register.csv`. Variant 10 confirms zero alert rows for 'GPU Pool Utilization for ML Workload Capacity'."
  },
  "dataSources": "`index=ml_infra` `sourcetype=\"dcgm:gpu\"` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "NVIDIA DCGM exporter, Kubernetes GPU metrics, cloud GPU monitoring — Universal Forwarder (Splunkbase 617)",
  "spl": "index=ml_infra sourcetype=\"dcgm:gpu\" earliest=-7d\n| eval util=tonumber(coalesce(gpu_sm_utilization, sm_util_pct))\n| bin _time span=1h\n| stats avg(util) as avg_sm perc95(util) as p95_sm by _time, host, gpu_index\n| stats avg(avg_sm) as fleet_avg max(p95_sm) as fleet_p95 by host\n| eval underused=if(fleet_avg<35 AND fleet_p95<70,1,0)\n| where underused=1 OR fleet_p95>92\n| table host, fleet_avg, fleet_p95",
  "description": "Monitors UC-20.2.32 — GPU, Pool, Utilization, for, Workload — using `index=ml_infra` sourcetype `dcgm:gpu`, grouped by `_time`, `host`, `gpu_index`. Outputs `avg_sm`, `fleet_avg`, `fleet_p95` each `1h`; alert when `fleet_avg` < 35, `fleet_p95` < 70, or `fleet_p95` > 92. Primary focus: Workload Capacity. Purpose-built for 'GPU Pool Utilization for ML Workload Capacity' in Operations workflows.",
  "value": "For 'GPU Pool Utilization for ML Workload Capacity', UC-20.2.32 converts `avg_sm`, `fleet_avg`, `fleet_p95` on `dcgm:gpu` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "(1) Deploy DCGM on GPU nodes and normalize `gpu_index`. (2) Tag hosts with workload type such as training versus inference. (3) Right-size node pools when underused persists fourteen days; scale out when fleet_p95 exceeds ninety-two.",
  "visualization": "Box plot (utilization distribution), Table (underused hosts), Timechart (job queue depth if ingested).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `ml_infra` (UC-20.2.32)** — Planned work for 'GPU Pool Utilization for ML Workload Capacity' shifts `avg_sm`, `fleet_avg`, `fleet_p95` over `span=1h` while `_time`, `host`, `gpu_index` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `dcgm:gpu`** — Scheduled automation elevates `host` without crossing `fleet_avg` < 35 for UC-20.2.32. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `_time`, `host`, `gpu_index`** — Health checks in `ml_infra` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `dcgm:gpu`** — First 48h baseline for UC-20.2.32 on `_time` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `_time`, `exception_reason`, `valid_until`.",
  "equipment": [
    "kubernetes"
  ],
  "equipmentModels": [
    "kubernetes_k8s"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: NVIDIA DCGM exporter, Kubernetes GPU metrics, cloud GPU monitoring.\n• Ensure the following data sources are available: `index=ml_infra` `sourcetype=\"dcgm:gpu\"`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\n(1) Deploy DCGM on GPU nodes and normalize `gpu_index`. (2) Tag hosts with workload type such as training versus inference. (3) Right-size node pools when underused persists fourteen days; scale out when fleet_p95 exceeds ninety-two.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ml_infra sourcetype=\"dcgm:gpu\" earliest=-7d\n| eval util=tonumber(coalesce(gpu_sm_utilization, sm_util_pct))\n| bin _time span=1h\n| stats avg(util) as avg_sm perc95(util) as p95_sm by _time, host, gpu_index\n| stats avg(avg_sm) as fleet_avg max(p95_sm) as fleet_p95 by host\n| eval underused=if(fleet_avg<35 AND fleet_p95<70,1,0)\n| where underused=1 OR fleet_p95>92\n| table host, fleet_avg, fleet_p95\n```\n\nUnderstanding this SPL\n\n**GPU Pool Utilization for ML Workload Capacity** — GPU nodes are expensive; low average utilization wastes capital while queue spikes delay training service levels. Tracking streaming-multiprocessor utilization and peak load by host informs autoscaler bounds and purchase versus spot decisions.\n\nDocumented **Data sources**: `index=ml_infra` `sourcetype=\"dcgm:gpu\"`. **App/TA** (typical add-on context): NVIDIA DCGM exporter, Kubernetes GPU metrics, cloud GPU monitoring. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ml_infra; **sourcetype**: dcgm:gpu. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ml_infra, sourcetype=\"dcgm:gpu\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **util** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by _time, host, gpu_index** so each row reflects one combination of those dimensions.\n• `stats` rolls up events into metrics; results are split **by host** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **underused** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where underused=1 OR fleet_p95>92` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **GPU Pool Utilization for ML Workload Capacity**): table host, fleet_avg, fleet_p95\n\n\nStep 3 — Validate\ncompare with the cloud billing console for the same scope, accounts, and time range.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Box plot (utilization distribution), Table (underused hosts), Timechart (job queue depth if ingested).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (NVIDIA DCGM exporter, Kubernetes GPU metrics, cloud GPU monitoring — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=dcgm:gpu into ml_infra. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, fleet_avg, fleet_p95, gpu_index`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Cost and capacity tools watch how much we spend on cloud and the data center, and where we have spare or strained capacity.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
