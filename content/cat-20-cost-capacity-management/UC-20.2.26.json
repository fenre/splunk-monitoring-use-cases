{
  "$schema": "../../schemas/uc.schema.json",
  "id": "20.2.26",
  "title": "Kubernetes Namespace Resource Quota Pressure",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Kubernetes Namespace Resource Quota Pressure' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_kubernetes_namespace_resource_quota_pressure` (UC-20.2.26), SOC dashboard panel \"Operations — Kubernetes Namespace Resource Quota Pressure\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-20.2.26 positive (Kubernetes, Namespace, Resource, Quota, Pressure): ingest synthetic events into `index=kubernetes` sourcetype='kube:quota' where `count` grouped by `cluster`, `namespace` satisfies `cpu_headroom_pct` < 15, or `mem_headroom_pct` < 15 for 'Kubernetes Namespace Resource Quota Pressure'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-20.2.26 negative (Quota Pressure): ingest benign `kubernetes` / `kube:quota` events where `cluster`, `namespace` stays below `cpu_headroom_pct` < 15, or `mem_headroom_pct` < 15, or exclude the test entity via `ops_exception_register.csv`. Variant 2 confirms zero alert rows for 'Kubernetes Namespace Resource Quota Pressure'."
  },
  "dataSources": "`index=kubernetes` `sourcetype=\"kube:quota\"` Collected via Splunk Connect for Kubernetes (Splunkbase 5167) modular input or scripted poll into the index named in the SPL.",
  "app": "Kubernetes metrics TA, Prometheus exporter, OpenTelemetry — Splunk Connect for Kubernetes (Splunkbase 5167)",
  "spl": "index=kubernetes sourcetype=\"kube:quota\" earliest=-1h\n| eval cpu_req=tonumber(coalesce(cpu_requests_cores, cpu_requests))\n| eval cpu_lim=tonumber(coalesce(cpu_hard_quota_cores, cpu_hard_limit))\n| eval mem_req=tonumber(coalesce(memory_requests_gib, mem_requests_gib))\n| eval mem_lim=tonumber(coalesce(memory_hard_quota_gib, mem_hard_limit_gib))\n| eval cpu_headroom_pct=round(100*(cpu_lim-cpu_req)/nullif(cpu_lim,0),1)\n| eval mem_headroom_pct=round(100*(mem_lim-mem_req)/nullif(mem_lim,0),1)\n| where cpu_headroom_pct<15 OR mem_headroom_pct<15\n| stats latest(cpu_headroom_pct) as cpu_head latest(mem_headroom_pct) as mem_head by cluster, namespace\n| sort cluster, cpu_head",
  "description": "Monitors UC-20.2.26 for Kubernetes, Namespace, Resource, Quota, Pressure over `index=kubernetes` sourcetype `kube:quota` with thresholds `cpu_headroom_pct` < 15, or `mem_headroom_pct` < 15. Groups `count` by `cluster`, `namespace` each `the scheduled window`. Centered on Quota Pressure indicators. Supports repeatable operations triage for 'Kubernetes Namespace Resource Quota Pressure'.",
  "value": "For 'Kubernetes Namespace Resource Quota Pressure', UC-20.2.26 converts `activity` on `kube:quota` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "(1) Ingest kube-state-metrics quota objects or a vendor CMDB export with limits and allocated requests. (2) Alert when headroom stays below 15% for six hours. (3) Pair with cost data (UC-20.1.16) before raising quotas on idle namespaces.",
  "visualization": "Heatmap (namespace × resource headroom), Table (clusters at risk), Gauge (worst namespace headroom).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 5167",
      "url": "https://splunkbase.splunk.com/app/5167",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `kubernetes` (UC-20.2.26)** — Planned work for 'Kubernetes Namespace Resource Quota Pressure' shifts `count` over `span=1h` while `cluster`, `namespace` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `kube:quota`** — Scheduled automation elevates `namespace` without crossing `cpu_headroom_pct` < 15 for UC-20.2.26. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `cluster`, `namespace`** — Health checks in `kubernetes` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `kube:quota`** — First 48h baseline for UC-20.2.26 on `cluster` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `cluster`, `exception_reason`, `valid_until`.",
  "equipment": [
    "kubernetes",
    "opentelemetry",
    "prometheus"
  ],
  "equipmentModels": [
    "kubernetes_k8s"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Kubernetes metrics TA, Prometheus exporter, OpenTelemetry.\n• Ensure the following data sources are available: `index=kubernetes` `sourcetype=\"kube:quota\"`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\n(1) Ingest kube-state-metrics quota objects or a vendor CMDB export with limits and allocated requests. (2) Alert when headroom stays below 15% for six hours. (3) Pair with cost data (UC-20.1.16) before raising quotas on idle namespaces.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=kubernetes sourcetype=\"kube:quota\" earliest=-1h\n| eval cpu_req=tonumber(coalesce(cpu_requests_cores, cpu_requests))\n| eval cpu_lim=tonumber(coalesce(cpu_hard_quota_cores, cpu_hard_limit))\n| eval mem_req=tonumber(coalesce(memory_requests_gib, mem_requests_gib))\n| eval mem_lim=tonumber(coalesce(memory_hard_quota_gib, mem_hard_limit_gib))\n| eval cpu_headroom_pct=round(100*(cpu_lim-cpu_req)/nullif(cpu_lim,0),1)\n| eval mem_headroom_pct=round(100*(mem_lim-mem_req)/nullif(mem_lim,0),1)\n| where cpu_headroom_pct<15 OR mem_headroom_pct<15\n| stats latest(cpu_headroom_pct) as cpu_head latest(mem_headroom_pct) as mem_head by cluster, namespace\n| sort cluster, cpu_head\n```\n\nUnderstanding this SPL\n\n**Kubernetes Namespace Resource Quota Pressure** — Hard quotas stop deployments during peak traffic, causing revenue-impacting outages. Tracking requested versus hard limits for CPU, memory, and persistent volume claims per namespace lets platform teams expand quotas or reclaim unused requests before developers hit the wall.\n\nDocumented **Data sources**: `index=kubernetes` `sourcetype=\"kube:quota\"`. **App/TA** (typical add-on context): Kubernetes metrics TA, Prometheus exporter, OpenTelemetry. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: kubernetes; **sourcetype**: kube:quota. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=kubernetes, sourcetype=\"kube:quota\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **cpu_req** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **cpu_lim** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **mem_req** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **mem_lim** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **cpu_headroom_pct** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **mem_headroom_pct** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where cpu_headroom_pct<15 OR mem_headroom_pct<15` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by cluster, namespace** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\ncompare with the cloud billing console for the same scope, accounts, and time range.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Heatmap (namespace × resource headroom), Table (clusters at risk), Gauge (worst namespace headroom).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Kubernetes metrics TA, Prometheus exporter, OpenTelemetry — Splunk Connect for Kubernetes (Splunkbase 5167)). Enable the modular input or HEC token that writes sourcetype=kube:quota into kubernetes. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`cluster, cpu_headroom_pct, mem_headroom_pct, namespace`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Cost and capacity tools watch how much we spend on cloud and the data center, and where we have spare or strained capacity.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
