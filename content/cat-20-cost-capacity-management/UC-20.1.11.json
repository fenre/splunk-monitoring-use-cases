{
  "$schema": "../../schemas/uc.schema.json",
  "id": "20.1.11",
  "title": "Spot Instance Interruption Rate",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Cost",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Spot Instance Interruption Rate' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_spot_instance_interruption_rate` (UC-20.1.11), SOC dashboard panel \"Operations — Spot Instance Interruption Rate\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-20.1.11 positive (Spot, Instance, Interruption, Rate): ingest synthetic events into `index=aws` sourcetype='aws:cloudtrail' where `instance_hours`, `interruptions`, `ih` grouped by `entity` satisfies -30d for 'Spot Instance Interruption Rate'. Validate filter `lineItem_UsageType=\"*SpotUsage*\"`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-20.1.11 negative (Interruption Rate): ingest benign `aws` / `aws:cloudtrail` events where `entity` stays below -30d, or exclude the test entity via `ops_exception_register.csv`. Variant 1 confirms zero alert rows for 'Spot Instance Interruption Rate'."
  },
  "dataSources": "`aws:cloudtrail` Spot events, instance-hours from CUR Collected via Splunk Add-on for Amazon Web Services (Splunkbase 1876) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk Add-on for AWS`, CloudTrail — Splunk Add-on for Amazon Web Services (Splunkbase 1876)",
  "spl": "index=aws sourcetype=\"aws:cloudtrail\" earliest=-30d\n| search eventName=\"SpotInstanceInterruption\"\n| stats count as intr\n| append [ search index=cloud_billing sourcetype=\"aws:billing:cur\" earliest=-30d\n  | search lineItem_UsageType=\"*SpotUsage*\"\n  | stats sum(lineItem_NormalizedUsageAmount) as instance_hours ]\n| stats sum(intr) as interruptions sum(instance_hours) as ih\n| eval intr_per_1k=round(1000*interruptions/nullif(ih,0),2)",
  "description": "Alerts on Interruption Rate activity in UC-20.1.11 ('Spot Instance Interruption Rate') from `index=aws` sourcetype `aws:cloudtrail`. Rolls up `instance_hours`, `interruptions`, `ih` by `entity` across `the scheduled window` and pages when -30d. Uses filter `lineItem_UsageType=\"*SpotUsage*\"`. Operations operators treat UC-20.1.11 as the canonical signal.",
  "value": "Operators running UC-20.1.11 on `aws:cloudtrail` feeds shorten triage for Interruption Rate tied to 'Spot Instance Interruption Rate', lowering operational risk and delayed incident response.",
  "implementation": "Align instance-hour denominator from CUR `NormalizedUsageAmount`. Alert when `intr_per_1k` exceeds baseline for stateful tiers.",
  "visualization": "Single value (interruptions per 1k hours), Line chart (rate trend), Table (by AZ).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Add-on for AWS",
      "url": "https://splunkbase.splunk.com/app/1876",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `aws` (UC-20.1.11)** — Planned work for 'Spot Instance Interruption Rate' shifts `instance_hours`, `interruptions`, `ih` over `span=1h` while `host` remains in-scope. Applies filter `lineItem_UsageType=\"*SpotUsage*\"`. Cross-check CMDB CHG.\n\n2. **Batch `sum` on `aws:cloudtrail`** — Scheduled automation elevates `intr_per_1k` without crossing configured threshold for UC-20.1.11. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `aws` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `aws:cloudtrail`** — First 48h baseline for UC-20.1.11 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "aws"
  ],
  "equipmentModels": [
    "aws_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk Add-on for AWS`, CloudTrail.\n• Ensure the following data sources are available: `aws:cloudtrail` Spot events, instance-hours from CUR.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nAlign instance-hour denominator from CUR `NormalizedUsageAmount`. Alert when `intr_per_1k` exceeds baseline for stateful tiers.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=aws sourcetype=\"aws:cloudtrail\" earliest=-30d\n| search eventName=\"SpotInstanceInterruption\"\n| stats count as intr\n| append [ search index=cloud_billing sourcetype=\"aws:billing:cur\" earliest=-30d\n  | search lineItem_UsageType=\"*SpotUsage*\"\n  | stats sum(lineItem_NormalizedUsageAmount) as instance_hours ]\n| stats sum(intr) as interruptions sum(instance_hours) as ih\n| eval intr_per_1k=round(1000*interruptions/nullif(ih,0),2)\n```\n\nUnderstanding this SPL\n\n**Spot Instance Interruption Rate** — Interruptions per 1,000 instance-hours by pool and AZ — extends raw event counts (UC-20.1.7) with a **rate** for SLO tracking.\n\nDocumented **Data sources**: `aws:cloudtrail` Spot events, instance-hours from CUR. **App/TA** (typical add-on context): `Splunk Add-on for AWS`, CloudTrail. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: aws; **sourcetype**: aws:cloudtrail. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=aws, sourcetype=\"aws:cloudtrail\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Applies an explicit `search` filter to narrow the current result set.\n• `stats` aggregates the pipeline (counts, distinct values, sums, percentiles, etc.) into fewer rows.\n• Appends rows from a subsearch with `append`.\n• `stats` aggregates the pipeline (counts, distinct values, sums, percentiles, etc.) into fewer rows.\n• `eval` defines or adjusts **intr_per_1k** — often to normalize units, derive a ratio, or prepare for thresholds.\n\n\nStep 3 — Validate\ncompare with Cost Explorer; compare with the cloud billing console for the same scope, accounts, and time range.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Single value (interruptions per 1k hours), Line chart (rate trend), Table (by AZ).",
  "grandmaExplanation": "Cost and capacity tools watch how much we spend on cloud and the data center, and where we have spare or strained capacity.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
