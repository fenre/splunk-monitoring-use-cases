{
  "$schema": "../../schemas/uc.schema.json",
  "id": "20.2.24",
  "title": "Cloud Cost Anomaly with Seasonal Decomposition (MLTK)",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Cost"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Cloud Cost Anomaly with Seasonal Decomposition (MLTK)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_cloud_cost_anomaly_with_seasonal_decomposition_m` (UC-20.2.24), SOC dashboard panel \"Operations — Cloud Cost Anomaly with Seasonal Decomposition (MLTK)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-20.2.24 positive (Cloud, Cost, Anomaly, with, Seasonal): ingest synthetic events into `index=cloud` sourcetype='target_sourcetype' where `daily_cost` grouped by `_time`, `service_name`, `account_id` satisfies detection threshold for 'Cloud Cost Anomaly with Seasonal Decomposition (MLTK)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-20.2.24 negative (Decomposition (MLTK)): ingest benign `cloud` / `target_sourcetype` events where `_time`, `service_name`, `account_id` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 2 confirms zero alert rows for 'Cloud Cost Anomaly with Seasonal Decomposition (MLTK)'."
  },
  "dataSources": "`index=cloud sourcetype=aws:billing` or `sourcetype=azure:costmanagement` or `sourcetype=gcp:billing` Collected via Splunk Add-on for Amazon Web Services (Splunkbase 1876) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Machine Learning Toolkit (MLTK), Splunk Add-on for AWS / Azure / GCP — Splunk Add-on for Amazon Web Services (Splunkbase 1876)",
  "spl": "index=cloud sourcetype IN (\"aws:billing\",\"azure:costmanagement\",\"gcp:billing\")\n| bin _time span=1d\n| stats sum(cost) as daily_cost by _time, service_name, account_id\n| eval dow=strftime(_time, \"%A\"), dom=strftime(_time, \"%d\")\n| fit StateSpaceForecast daily_cost holdback=0 forecast_k=14 conf_interval=95 by service_name into cost_seasonal_model\n| eval residual=daily_cost - 'predicted(daily_cost)'\n| eval pct_deviation=round(100*residual/nullif('predicted(daily_cost)', 0), 1)\n| where abs(pct_deviation) > 25 OR daily_cost > 'upper95(predicted(daily_cost))'\n| table _time, service_name, account_id, daily_cost, \"predicted(daily_cost)\", pct_deviation\n| sort -pct_deviation",
  "description": "Identifies UC-20.2.24 for Cloud, Cost, Anomaly, with, Seasonal over `index=cloud` sourcetype `configured sourcetype` with thresholds configured threshold. Groups `daily_cost` by `_time`, `service_name`, `account_id` each `1d`. Centered on Decomposition (MLTK) indicators. Supports repeatable operations triage for 'Cloud Cost Anomaly with Seasonal Decomposition (MLTK)'.",
  "value": "For 'Cloud Cost Anomaly with Seasonal Decomposition (MLTK)', UC-20.2.24 converts `daily_cost` on UC-20.2.24 search results into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Ingest cloud billing data daily from CUR (AWS), Cost Management exports (Azure), or BigQuery billing export (GCP). Train StateSpaceForecast models per service that capture weekly seasonality (weekend dips) and monthly patterns (month-end peaks). Forecast 14 days ahead with 95% confidence intervals. Alert FinOps teams when actual cost exceeds the upper confidence bound or deviates more than 25% from the seasonal prediction. Include account-level drill-down to identify the specific workload driving the anomaly. Retrain models monthly. Pair with UC-20.1.13 for budget variance context and UC-20.1.18 for orphaned resource identification when cost spikes correlate with new resources.",
  "visualization": "Area chart (actual vs forecast with confidence band), Table (anomalous services), Bar chart (top cost deviations by service).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Add-on for AWS",
      "url": "https://splunkbase.splunk.com/app/1876",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `cloud` (UC-20.2.24)** — Planned work for 'Cloud Cost Anomaly with Seasonal Decomposition (MLTK)' shifts `daily_cost` over `span=1d` while `_time`, `service_name`, `account_id` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `sum` on `configured_sourcetype`** — Scheduled automation elevates `service_name` without crossing configured threshold for UC-20.2.24. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `_time`, `service_name`, `account_id`** — Health checks in `cloud` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-20.2.24 on `_time` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `_time`, `exception_reason`, `valid_until`.",
  "equipment": [
    "aws",
    "azure",
    "gcp"
  ],
  "equipmentModels": [
    "aws_generic",
    "azure_generic",
    "gcp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Machine Learning Toolkit (MLTK), Splunk Add-on for AWS / Azure / GCP.\n• Ensure the following data sources are available: `index=cloud sourcetype=aws:billing` or `sourcetype=azure:costmanagement` or `sourcetype=gcp:billing`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest cloud billing data daily from CUR (AWS), Cost Management exports (Azure), or BigQuery billing export (GCP). Train StateSpaceForecast models per service that capture weekly seasonality (weekend dips) and monthly patterns (month-end peaks). Forecast 14 days ahead with 95% confidence intervals. Alert FinOps teams when actual cost exceeds the upper confidence bound or deviates more than 25% from the seasonal prediction. Include account-level drill-down to identify the specific workload drivin…\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=cloud sourcetype IN (\"aws:billing\",\"azure:costmanagement\",\"gcp:billing\")\n| bin _time span=1d\n| stats sum(cost) as daily_cost by _time, service_name, account_id\n| eval dow=strftime(_time, \"%A\"), dom=strftime(_time, \"%d\")\n| fit StateSpaceForecast daily_cost holdback=0 forecast_k=14 conf_interval=95 by service_name into cost_seasonal_model\n| eval residual=daily_cost - 'predicted(daily_cost)'\n| eval pct_deviation=round(100*residual/nullif('predicted(daily_cost)', 0), 1)\n| where abs(pct_deviation) > 25 OR daily_cost > 'upper95(predicted(daily_cost))'\n| table _time, service_name, account_id, daily_cost, \"predicted(daily_cost)\", pct_deviation\n| sort -pct_deviation\n```\n\nUnderstanding this SPL\n\n**Cloud Cost Anomaly with Seasonal Decomposition (MLTK)** — Cloud costs follow predictable weekly and monthly cycles — batch jobs on weekends, month-end reporting spikes, quarterly compliance scans. Static thresholds generate noise during normal peaks and miss slow-growth anomalies during quiet periods. By decomposing cost into seasonal, trend, and residual components with MLTK, this detection flags true anomalies against the expected cost shape for that specific day and hour.\n\nDocumented **Data sources**: `index=cloud sourcetype=aws:billing` or `sourcetype=azure:costmanagement` or `sourcetype=gcp:billing`. **App/TA** (typical add-on context): Splunk Machine Learning Toolkit (MLTK), Splunk Add-on for AWS / Azure / GCP. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: cloud.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=cloud. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by _time, service_name, account_id** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **dow** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Pipeline stage (see **Cloud Cost Anomaly with Seasonal Decomposition (MLTK)**): fit StateSpaceForecast daily_cost holdback=0 forecast_k=14 conf_interval=95 by service_name into cost_seasonal_model\n• `eval` defines or adjusts **residual** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **pct_deviation** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where abs(pct_deviation) > 25 OR daily_cost > 'upper95(predicted(daily_cost))'` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Cloud Cost Anomaly with Seasonal Decomposition (MLTK)**): table _time, service_name, account_id, daily_cost, \"predicted(daily_cost)\", pct_deviation\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\ncompare with Cost Explorer; compare with Azure Cost Management; compare with the GCP billing console; compare with the cloud billing console for the same scope, accounts, and time range.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Area chart (actual vs forecast with confidence band), Table (anomalous services), Bar chart (top cost deviations by service).",
  "grandmaExplanation": "Cost and capacity tools watch how much we spend on cloud and the data center, and where we have spare or strained capacity.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
