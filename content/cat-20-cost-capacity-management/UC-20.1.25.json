{
  "$schema": "../../schemas/uc.schema.json",
  "id": "20.1.25",
  "title": "NAT Gateway and VPC Endpoint Egress Cost Concentration",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Cost",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'NAT Gateway and VPC Endpoint Egress Cost Concentration' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_nat_gateway_and_vpc_endpoint_egress_cost_concent` (UC-20.1.25), SOC dashboard panel \"Operations — NAT Gateway and VPC Endpoint Egress Cost Concentration\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-20.1.25 positive (NAT, Gateway, and, VPC, Endpoint): ingest synthetic events into `index=cloud_billing` sourcetype='aws:billing:cur' where `nat_vpc_cost`, `usage_qty` grouped by `lineItem_UsageAccountId`, `lineItem_UsageType`, `resource` satisfies -30d for 'NAT Gateway and VPC Endpoint Egress Cost Concentration'. Validate filter `lineItem_ProductCode=\"AmazonEC2\" (lineItem_UsageType=\"NatGat`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-20.1.25 negative (Cost Concentration): ingest benign `cloud_billing` / `aws:billing:cur` events where `lineItem_UsageAccountId`, `lineItem_UsageType`, `resource` stays below -30d, or exclude the test entity via `ops_exception_register.csv`. Variant 7 confirms zero alert rows for 'NAT Gateway and VPC Endpoint Egress Cost Concentration'."
  },
  "dataSources": "`index=cloud_billing` `sourcetype=\"aws:billing:cur\"` Collected via Splunk Add-on for Amazon Web Services (Splunkbase 1876) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk Add-on for AWS`, VPC Flow Logs (optional correlation) — Splunk Add-on for Amazon Web Services (Splunkbase 1876)",
  "spl": "index=cloud_billing sourcetype=\"aws:billing:cur\" earliest=-30d\n| eval cost=tonumber(lineItem_UnblendedCost)\n| search lineItem_ProductCode=\"AmazonEC2\" (lineItem_UsageType=\"NatGateway*\" OR lineItem_UsageType=\"VpcEndpoint*\")\n| eval resource=coalesce(lineItem_ResourceId, resourceId)\n| stats sum(cost) as nat_vpc_cost sum(lineItem_UsageAmount) as usage_qty by lineItem_UsageAccountId, lineItem_UsageType, resource\n| sort -nat_vpc_cost\n| head 50",
  "description": "Identifies UC-20.1.25 — NAT, Gateway, and, VPC, Endpoint — using `index=cloud_billing` sourcetype `aws:billing:cur`, grouped by `lineItem_UsageAccountId`, `lineItem_UsageType`, `resource`. Outputs `nat_vpc_cost`, `usage_qty` each `the scheduled window`; alert when -30d. Filter chain includes filter `lineItem_ProductCode=\"AmazonEC2\" (lineItem_UsageType=\"NatGat`. Purpose-built for 'NAT Gateway and VPC Endpoint Egress Cost Concentration' in Operations workflows.",
  "value": "UC-20.1.25 operationalises 'NAT Gateway and VPC Endpoint Egress Cost Concentration' against `aws:billing:cur` feeds so operators reduce operational risk and delayed incident response without manual correlation.",
  "implementation": "(1) Tag NAT gateways and endpoints with owning application. (2) Join top resources to flow log aggregates if available. (3) Prioritize architecture reviews for the top 10 resources by trailing 30-day cost.",
  "visualization": "Table (top NAT/VPC-endpoint resources), Bar chart (cost by usage type), Pie chart (share by account).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Add-on for AWS",
      "url": "https://splunkbase.splunk.com/app/1876",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `cloud_billing` (UC-20.1.25)** — Planned work for 'NAT Gateway and VPC Endpoint Egress Cost Concentration' shifts `nat_vpc_cost`, `usage_qty` over `span=1h` while `lineItem_UsageAccountId`, `lineItem_UsageType`, `resource` remains in-scope. Applies filter `lineItem_ProductCode=\"AmazonEC2\" (lineItem_UsageType=\"NatGat`. Cross-check CMDB CHG.\n\n2. **Batch `sum` on `aws:billing:cur`** — Scheduled automation elevates `lineItem_UsageType` without crossing configured threshold for UC-20.1.25. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `lineItem_UsageAccountId`, `lineItem_UsageType`, `resource`** — Health checks in `cloud_billing` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `aws:billing:cur`** — First 48h baseline for UC-20.1.25 on `lineItem_UsageAccountId` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `lineItem_UsageAccountId`, `exception_reason`, `valid_until`.",
  "equipment": [
    "aws"
  ],
  "equipmentModels": [
    "aws_generic",
    "m365_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk Add-on for AWS`, VPC Flow Logs (optional correlation).\n• Ensure the following data sources are available: `index=cloud_billing` `sourcetype=\"aws:billing:cur\"`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\n(1) Tag NAT gateways and endpoints with owning application. (2) Join top resources to flow log aggregates if available. (3) Prioritize architecture reviews for the top 10 resources by trailing 30-day cost.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=cloud_billing sourcetype=\"aws:billing:cur\" earliest=-30d\n| eval cost=tonumber(lineItem_UnblendedCost)\n| search lineItem_ProductCode=\"AmazonEC2\" (lineItem_UsageType=\"NatGateway*\" OR lineItem_UsageType=\"VpcEndpoint*\")\n| eval resource=coalesce(lineItem_ResourceId, resourceId)\n| stats sum(cost) as nat_vpc_cost sum(lineItem_UsageAmount) as usage_qty by lineItem_UsageAccountId, lineItem_UsageType, resource\n| sort -nat_vpc_cost\n| head 50\n```\n\nUnderstanding this SPL\n\n**NAT Gateway and VPC Endpoint Egress Cost Concentration** — Managed NAT and interface endpoints bill per GB processed; a single chatty microservice behind NAT can dominate networking spend. Ranking usage types tied to NAT Gateway and PrivateLink highlights candidates for VPC endpoint redesign, caching, or regional consolidation.\n\nDocumented **Data sources**: `index=cloud_billing` `sourcetype=\"aws:billing:cur\"`. **App/TA** (typical add-on context): `Splunk Add-on for AWS`, VPC Flow Logs (optional correlation). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: cloud_billing; **sourcetype**: aws:billing:cur. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=cloud_billing, sourcetype=\"aws:billing:cur\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **cost** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Applies an explicit `search` filter to narrow the current result set.\n• `eval` defines or adjusts **resource** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by lineItem_UsageAccountId, lineItem_UsageType, resource** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n• Limits the number of rows with `head`.\n\n\nStep 3 — Validate\ncompare with Cost Explorer; compare with the cloud billing console for the same scope, accounts, and time range.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (top NAT/VPC-endpoint resources), Bar chart (cost by usage type), Pie chart (share by account).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk Add-on for AWS`, VPC Flow Logs (optional correlation) — Splunk Add-on for Amazon Web Services (Splunkbase 1876)). Enable the modular input or HEC token that writes sourcetype=aws:billing:cur into cloud_billing. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`lineItem_UsageAccountId, lineItem_UsageType, resource`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Cost and capacity tools watch how much we spend on cloud and the data center, and where we have spare or strained capacity.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
