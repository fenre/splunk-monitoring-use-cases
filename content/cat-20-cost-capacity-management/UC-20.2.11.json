{
  "$schema": "../../schemas/uc.schema.json",
  "id": "20.2.11",
  "title": "Unused and Orphaned Resource Cost Attribution",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Cost"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Unused and Orphaned Resource Cost Attribution' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_unused_and_orphaned_resource_cost_attribution` (UC-20.2.11), SOC dashboard panel \"Operations — Unused and Orphaned Resource Cost Attribution\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-20.2.11 positive (Unused, and, Orphaned, Resource, Cost): ingest synthetic events into `index=cloud_cost` sourcetype='resource:inventory' where `waste_cost` grouped by `resource_type`, `account_id` satisfies `last_used_days` > 30, or `cost` > 0 for 'Unused and Orphaned Resource Cost Attribution'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-20.2.11 negative (Cost Attribution): ingest benign `cloud_cost` / `resource:inventory` events where `resource_type`, `account_id` stays below `last_used_days` > 30, or `cost` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 1 confirms zero alert rows for 'Unused and Orphaned Resource Cost Attribution'."
  },
  "dataSources": "Resource list with last used, cost, tags Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Cloud resource inventory, cost allocation tags — Universal Forwarder (Splunkbase 617)",
  "spl": "index=cloud_cost sourcetype=\"resource:inventory\"\n| where (last_used_days > 30 OR state=\"stopped\") AND cost > 0\n| stats sum(cost) as waste_cost, count by resource_type, account_id\n| sort -waste_cost\n| table resource_type, account_id, count, waste_cost",
  "description": "Identifies Cost Attribution activity in UC-20.2.11 ('Unused and Orphaned Resource Cost Attribution') from `index=cloud_cost` sourcetype `resource:inventory`. Rolls up `waste_cost` by `resource_type`, `account_id` across `the scheduled window` and pages when `last_used_days` > 30, or `cost` > 0. Covers Unused, and, Orphaned, Resource, Cost. Operations operators treat UC-20.2.11 as the canonical signal.",
  "value": "Operators running UC-20.2.11 on `resource:inventory` feeds shorten triage for Cost Attribution tied to 'Unused and Orphaned Resource Cost Attribution', lowering operational risk and delayed incident response.",
  "implementation": "Combine resource inventory (with last-used or state) and cost data. Flag idle or stopped resources older than threshold. Report on waste by type and account. Drive cleanup campaigns.",
  "visualization": "Table (waste by type and account), Bar chart (waste cost by resource type), Single value (total waste).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `cloud_cost` (UC-20.2.11)** — Planned work for 'Unused and Orphaned Resource Cost Attribution' shifts `waste_cost` over `span=1h` while `resource_type`, `account_id` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `sum` on `resource:inventory`** — Scheduled automation elevates `account_id` without crossing `last_used_days` > 30 for UC-20.2.11. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `resource_type`, `account_id`** — Health checks in `cloud_cost` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `resource:inventory`** — First 48h baseline for UC-20.2.11 on `resource_type` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `resource_type`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Cloud resource inventory, cost allocation tags.\n• Ensure the following data sources are available: Resource list with last used, cost, tags.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nCombine resource inventory (with last-used or state) and cost data. Flag idle or stopped resources older than threshold. Report on waste by type and account. Drive cleanup campaigns.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=cloud_cost sourcetype=\"resource:inventory\"\n| where (last_used_days > 30 OR state=\"stopped\") AND cost > 0\n| stats sum(cost) as waste_cost, count by resource_type, account_id\n| sort -waste_cost\n| table resource_type, account_id, count, waste_cost\n```\n\nUnderstanding this SPL\n\n**Unused and Orphaned Resource Cost Attribution** — Unused disks, idle instances, and orphaned snapshots drive waste. Attributing cost to these resources supports cleanup and chargeback.\n\nDocumented **Data sources**: Resource list with last used, cost, tags. **App/TA** (typical add-on context): Cloud resource inventory, cost allocation tags. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: cloud_cost; **sourcetype**: resource:inventory. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=cloud_cost, sourcetype=\"resource:inventory\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where (last_used_days > 30 OR state=\"stopped\") AND cost > 0` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by resource_type, account_id** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n• Pipeline stage (see **Unused and Orphaned Resource Cost Attribution**): table resource_type, account_id, count, waste_cost\n\n\nStep 3 — Validate\ncompare with the cloud billing console for the same scope, accounts, and time range.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (waste by type and account), Bar chart (waste cost by resource type), Single value (total waste).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Cloud resource inventory, cost allocation tags — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=resource:inventory into cloud_cost. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`account_id, cost, last_used_days, resource_type`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Cost and capacity tools watch how much we spend on cloud and the data center, and where we have spare or strained capacity.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
