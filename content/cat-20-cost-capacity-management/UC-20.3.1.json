{
  "$schema": "../../schemas/uc.schema.json",
  "id": "20.3.1",
  "title": "SaaS License Utilization (Assigned vs Active)",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity",
    "Compliance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'SaaS License Utilization (Assigned vs Active)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_saas_license_utilization_assigned_vs_active` (UC-20.3.1), SOC dashboard panel \"Operations — SaaS License Utilization (Assigned vs Active)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=saas` sourcetype='license:usage' that satisfy the UC-20.3.1 SPL filters for 'SaaS License Utilization (Assigned vs Active)'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `saas` / `license:usage` events outside the detection threshold, or exclude the test entity via `operational_exceptions.csv` with a valid window. Confirm zero alert rows for UC-20.3.1."
  },
  "dataSources": "`sourcetype=license:usage`, `sourcetype=o365:reporting` Collected via Splunk Add-on for Microsoft Office 365 (Splunkbase 4054) modular input or scripted poll into the index named in the SPL.",
  "app": "Microsoft Entra ID Add-on, Okta Splunk App, Salesforce TA — Splunk Add-on for Microsoft Office 365 (Splunkbase 4054)",
  "spl": "index=saas sourcetype=\"license:usage\"\n| eval assigned=coalesce(licenses_assigned,0), active=coalesce(active_users_30d,0)\n| eval utilization_pct=round(100*active/nullif(assigned,0),1)\n| where utilization_pct < 70 OR active < assigned*0.5\n| stats latest(assigned) as assigned latest(active) as active latest(utilization_pct) as util_pct by product, sku, cost_center\n| sort util_pct",
  "description": "Paying for assigned-but-unused seats wastes budget; comparing entitlements to real sign-in or activity highlights reclaim and right-size opportunities before renewals.",
  "value": "Early 'SaaS License Utilization (Assigned vs Active)' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Ingest monthly license assignment exports and last-sign-in or 30-day active user counts from IdP or vendor admin APIs. Schedule weekly jobs; join on `sku`/`product`. Alert when utilization drops below policy thresholds. Feed reclamation workflows with user lists.",
  "visualization": "Bar chart (utilization % by product), Table (reclaim candidates), Single value (wasted seat estimate).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'SaaS License Utilization (Assigned vs Active)'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `saas` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `operational_exceptions.csv` with entity, reason, valid_until.",
  "equipment": [
    "m365",
    "okta"
  ],
  "equipmentModels": [
    "m365_generic",
    "okta_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Microsoft Entra ID Add-on, Okta Splunk App, Salesforce TA.\n• Ensure the following data sources are available: `sourcetype=license:usage`, `sourcetype=o365:reporting`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest monthly license assignment exports and last-sign-in or 30-day active user counts from IdP or vendor admin APIs. Schedule weekly jobs; join on `sku`/`product`. Alert when utilization drops below policy thresholds. Feed reclamation workflows with user lists.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=saas sourcetype=\"license:usage\"\n| eval assigned=coalesce(licenses_assigned,0), active=coalesce(active_users_30d,0)\n| eval utilization_pct=round(100*active/nullif(assigned,0),1)\n| where utilization_pct < 70 OR active < assigned*0.5\n| stats latest(assigned) as assigned latest(active) as active latest(utilization_pct) as util_pct by product, sku, cost_center\n| sort util_pct\n```\n\nUnderstanding this SPL\n\n**SaaS License Utilization (Assigned vs Active)** — Paying for assigned-but-unused seats wastes budget; comparing entitlements to real sign-in or activity highlights reclaim and right-size opportunities before renewals.\n\nDocumented **Data sources**: `sourcetype=license:usage`, `sourcetype=o365:reporting`. **App/TA** (typical add-on context): Microsoft Entra ID Add-on, Okta Splunk App, Salesforce TA. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: saas; **sourcetype**: license:usage. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=saas, sourcetype=\"license:usage\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **assigned** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **utilization_pct** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where utilization_pct < 70 OR active < assigned*0.5` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by product, sku, cost_center** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\ncompare with the cloud billing console for the same scope, accounts, and time range.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Bar chart (utilization % by product), Table (reclaim candidates), Single value (wasted seat estimate).",
  "grandmaExplanation": "Cost and capacity tools watch how much we spend on cloud and the data center, and where we have spare or strained capacity.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
