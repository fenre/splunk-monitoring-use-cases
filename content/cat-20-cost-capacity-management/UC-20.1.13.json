{
  "$schema": "../../schemas/uc.schema.json",
  "id": "20.1.13",
  "title": "Cost Anomaly by Cloud Service",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Anomaly"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Cost Anomaly by Cloud Service' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_cost_anomaly_by_cloud_service` (UC-20.1.13), SOC dashboard panel \"Operations — Cost Anomaly by Cloud Service\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-20.1.13 positive (Cost, Anomaly, Cloud, Service): ingest synthetic events into `index=cloud_billing` sourcetype='aws:billing:cur' where `daily`, `med`, `sd` grouped by `lineItem_ProductCode` satisfies `sd` > 0 for 'Cost Anomaly by Cloud Service'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-20.1.13 negative (Cloud Service): ingest benign `cloud_billing` / `aws:billing:cur` events where `lineItem_ProductCode` stays below `sd` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 5 confirms zero alert rows for 'Cost Anomaly by Cloud Service'."
  },
  "dataSources": "CUR, Azure cost export Collected via Splunk Add-on for Amazon Web Services (Splunkbase 1876) modular input or scripted poll into the index named in the SPL.",
  "app": "Billing export TAs — Splunk Add-on for Amazon Web Services (Splunkbase 1876)",
  "spl": "index=cloud_billing sourcetype=\"aws:billing:cur\" earliest=-60d\n| eval cost=tonumber(lineItem_UnblendedCost)\n| timechart span=1d sum(cost) as daily by lineItem_ProductCode\n| untable _time lineItem_ProductCode daily\n| eventstats median(daily) as med, stdev(daily) as sd by lineItem_ProductCode\n| eval z=if(sd>0, (daily-med)/sd, 0)\n| where abs(z)>3\n| table _time, lineItem_ProductCode, daily, med, z",
  "description": "Identifies 'Cost Anomaly by Cloud Service' patterns (UC-20.1.13) sourced from `index=cloud_billing` sourcetype `aws:billing:cur`. Threshold bundle: `sd` > 0; aggregation grain: `lineItem_ProductCode`; metrics: `daily`, `med`, `sd`. Watch terms: Cost, Anomaly, Cloud, Service.",
  "value": "UC-20.1.13 gives Operations teams earlier visibility into Cloud Service through `aws:billing:cur` feeds, lowering operational risk and delayed incident response.",
  "implementation": "Requires 60d history. Exclude credits via `lineItem_LineItemType`. Page on |z|>3 for top services.",
  "visualization": "Table (service anomalies), Line chart (daily vs median), Single value (open anomalies).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 1876",
      "url": "https://splunkbase.splunk.com/app/1876",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `cloud_billing` (UC-20.1.13)** — Planned work for 'Cost Anomaly by Cloud Service' shifts `daily`, `med`, `sd` over `span=1d` while `lineItem_ProductCode` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `sum` on `aws:billing:cur`** — Scheduled automation elevates `cost` without crossing `sd` > 0 for UC-20.1.13. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `lineItem_ProductCode`** — Health checks in `cloud_billing` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `aws:billing:cur`** — First 48h baseline for UC-20.1.13 on `lineItem_ProductCode` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `lineItem_ProductCode`, `exception_reason`, `valid_until`.",
  "equipment": [
    "azure"
  ],
  "equipmentModels": [
    "azure_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Billing export TAs.\n• Ensure the following data sources are available: CUR, Azure cost export.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nRequires 60d history. Exclude credits via `lineItem_LineItemType`. Page on |z|>3 for top services.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=cloud_billing sourcetype=\"aws:billing:cur\" earliest=-60d\n| eval cost=tonumber(lineItem_UnblendedCost)\n| timechart span=1d sum(cost) as daily by lineItem_ProductCode\n| untable _time lineItem_ProductCode daily\n| eventstats median(daily) as med, stdev(daily) as sd by lineItem_ProductCode\n| eval z=if(sd>0, (daily-med)/sd, 0)\n| where abs(z)>3\n| table _time, lineItem_ProductCode, daily, med, z\n```\n\nUnderstanding this SPL\n\n**Cost Anomaly by Cloud Service** — z-score or median-absolute-deviation of **daily cost per `lineItem_ProductCode`** — tighter scope than account-level UC-20.1.2.\n\nDocumented **Data sources**: CUR, Azure cost export. **App/TA** (typical add-on context): Billing export TAs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: cloud_billing; **sourcetype**: aws:billing:cur. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=cloud_billing, sourcetype=\"aws:billing:cur\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **cost** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `timechart` plots the metric over time using **span=1d** buckets with a separate series **by lineItem_ProductCode** — ideal for trending and alerting on this use case.\n• Pipeline stage (see **Cost Anomaly by Cloud Service**): untable _time lineItem_ProductCode daily\n• `eventstats` rolls up events into metrics; results are split **by lineItem_ProductCode** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **z** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where abs(z)>3` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Cost Anomaly by Cloud Service**): table _time, lineItem_ProductCode, daily, med, z\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\ncompare with Cost Explorer; compare with Azure Cost Management; compare with the cloud billing console for the same scope, accounts, and time range.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (service anomalies), Line chart (daily vs median), Single value (open anomalies).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Billing export TAs — Splunk Add-on for Amazon Web Services (Splunkbase 1876)). Enable the modular input or HEC token that writes sourcetype=aws:billing:cur into cloud_billing. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`lineItem_ProductCode, sd, z`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Cost and capacity tools watch how much we spend on cloud and the data center, and where we have spare or strained capacity.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
