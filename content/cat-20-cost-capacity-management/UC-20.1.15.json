{
  "$schema": "../../schemas/uc.schema.json",
  "id": "20.1.15",
  "title": "Data Transfer Cost Attribution by Tag",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Cost"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Data Transfer Cost Attribution by Tag' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_data_transfer_cost_attribution_by_tag` (UC-20.1.15), SOC dashboard panel \"Operations — Data Transfer Cost Attribution by Tag\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-20.1.15 positive (Data, Transfer, Cost, Attribution, Tag): ingest synthetic events into `index=cloud_billing` sourcetype='aws:billing:cur' where `xfer_cost` grouped by `app`, `lineItem_UsageType` satisfies detection threshold for 'Data Transfer Cost Attribution by Tag'. Validate filter `lineItem_ProductCode=\"AmazonEC2\" (lineItem_UsageType=\"*DataT`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-20.1.15 negative (by Tag): ingest benign `cloud_billing` / `aws:billing:cur` events where `app`, `lineItem_UsageType` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 8 confirms zero alert rows for 'Data Transfer Cost Attribution by Tag'."
  },
  "dataSources": "`aws:billing:cur` Collected via Splunk Add-on for Amazon Web Services (Splunkbase 1876) modular input or scripted poll into the index named in the SPL.",
  "app": "CUR with resource tags — Splunk Add-on for Amazon Web Services (Splunkbase 1876)",
  "spl": "index=cloud_billing sourcetype=\"aws:billing:cur\"\n| search lineItem_ProductCode=\"AmazonEC2\" (lineItem_UsageType=\"*DataTransfer*\" OR lineItem_UsageType=\"*Bytes*\")\n| eval cost=tonumber(lineItem_UnblendedCost)\n| eval app=coalesce(resourceTags_user_Application,\"untagged\")\n| stats sum(cost) as xfer_cost by app, lineItem_UsageType\n| sort -xfer_cost",
  "description": "Identifies UC-20.1.15 — Data, Transfer, Cost, Attribution, Tag — using `index=cloud_billing` sourcetype `aws:billing:cur`, grouped by `app`, `lineItem_UsageType`. Outputs `xfer_cost` each `the scheduled window`; alert when configured threshold. Filter chain includes filter `lineItem_ProductCode=\"AmazonEC2\" (lineItem_UsageType=\"*DataT`. Purpose-built for 'Data Transfer Cost Attribution by Tag' in Operations workflows.",
  "value": "UC-20.1.15 accelerates insight into by Tag ('Data Transfer Cost Attribution by Tag') by tracking `xfer_cost` on `aws:billing:cur` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Requires tags on resources generating egress; untagged flows appear as `untagged`. Reconcile with VPC Flow to owners.",
  "visualization": "Stacked bar (transfer $ by app), Table (top untagged), Pie chart (egress by tag).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 1876",
      "url": "https://splunkbase.splunk.com/app/1876",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `cloud_billing` (UC-20.1.15)** — Planned work for 'Data Transfer Cost Attribution by Tag' shifts `xfer_cost` over `span=1h` while `app`, `lineItem_UsageType` remains in-scope. Applies filter `lineItem_ProductCode=\"AmazonEC2\" (lineItem_UsageType=\"*DataT`. Cross-check CMDB CHG.\n\n2. **Batch `sum` on `aws:billing:cur`** — Scheduled automation elevates `lineItem_UsageType` without crossing configured threshold for UC-20.1.15. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `app`, `lineItem_UsageType`** — Health checks in `cloud_billing` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `aws:billing:cur`** — First 48h baseline for UC-20.1.15 on `app` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `app`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: CUR with resource tags.\n• Ensure the following data sources are available: `aws:billing:cur`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nRequires tags on resources generating egress; untagged flows appear as `untagged`. Reconcile with VPC Flow to owners.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=cloud_billing sourcetype=\"aws:billing:cur\"\n| search lineItem_ProductCode=\"AmazonEC2\" (lineItem_UsageType=\"*DataTransfer*\" OR lineItem_UsageType=\"*Bytes*\")\n| eval cost=tonumber(lineItem_UnblendedCost)\n| eval app=coalesce(resourceTags_user_Application,\"untagged\")\n| stats sum(cost) as xfer_cost by app, lineItem_UsageType\n| sort -xfer_cost\n```\n\nUnderstanding this SPL\n\n**Data Transfer Cost Attribution by Tag** — Allocates data transfer line items to `resourceTags_user_*` for chargeback — extends aggregate transfer analysis (UC-20.1.8).\n\nDocumented **Data sources**: `aws:billing:cur`. **App/TA** (typical add-on context): CUR with resource tags. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: cloud_billing; **sourcetype**: aws:billing:cur. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=cloud_billing, sourcetype=\"aws:billing:cur\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Applies an explicit `search` filter to narrow the current result set.\n• `eval` defines or adjusts **cost** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **app** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by app, lineItem_UsageType** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\ncompare with Cost Explorer; compare with the cloud billing console for the same scope, accounts, and time range.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Stacked bar (transfer $ by app), Table (top untagged), Pie chart (egress by tag).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (CUR with resource tags — Splunk Add-on for Amazon Web Services (Splunkbase 1876)). Enable the modular input or HEC token that writes sourcetype=aws:billing:cur into cloud_billing. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`app, lineItem_UsageType`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Cost and capacity tools watch how much we spend on cloud and the data center, and where we have spare or strained capacity.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
