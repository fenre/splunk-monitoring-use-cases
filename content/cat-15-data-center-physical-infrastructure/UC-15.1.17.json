{
  "$schema": "../../schemas/uc.schema.json",
  "id": "15.1.17",
  "title": "UPS Self-Test Failure",
  "criticality": "critical",
  "difficulty": "beginner",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not judge battery impedance projections, UPS firmware CVE posture, outlet-level PDU metrics, or predictive cooling correlations—those stay separate catalogue tracks requiring additional datasets.",
  "evidence": "Export the saved search \"UPS Self-Test Failure\" PDF weekly plus underlying `index=power` event samples filtered to failing `ups_name` values for auditors proving diagnostics monitoring existed.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=power` sourcetype='snmp:ups:selftest' where `entity` satisfies detection threshold for UC-15.1.17 ('UPS Self-Test Failure'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `power` / `snmp:ups:selftest` events where `entity` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-15.1.17."
  },
  "dataSources": "Route polled UPS-MIB style self-test telemetry into `index=power` with `sourcetype=\"snmp:ups:selftest\"`, commonly emitted after configuring SNMP polling through Splunk SNMP Modular Input (**Splunkbase app ID 1537**) alongside field mappings shipped with **UPS SNMP (upsTestResultsSummary)**—confirm your deployed package keeps those sourcetype aliases consistent. Mandatory extracted dimensions for this UC include enumerated **`upsTestResultsSummary`**, outcome **`test_result`**, asset **`ups_name`**, and discriminator **`test_type`** alongside `_time`.",
  "app": "UPS SNMP (upsTestResultsSummary)",
  "spl": "index=power sourcetype=\"snmp:ups:selftest\"\n| where test_result!=\"passed\" OR upsTestResultsSummary!=\"donePass\"\n| table _time, ups_name, test_type, test_result, upsTestResultsSummary",
  "description": "This detection consumes SNMP-derived UPS diagnostics normalized into `sourcetype=\"snmp:ups:selftest\"`, highlighting rows where `upsTestResultsSummary` is not `donePass` or `test_result` is not `passed`—patterns that accompany aborted battery or inverter qualification cycles.",
  "value": "This detection consumes SNMP-derived UPS diagnostics normalized into `sourcetype=\"snmp:ups:selftest\"`, high.... Timely UC-15.1.17 alerting on 'UPS Self-Test Failure' reduces operational risk and delayed incident response before manual triage or audit channels escalate.",
  "implementation": "Stand up SNMP polling or trap normalization so each rack UPS posts self-test summaries on a steady cadence, map them to `snmp:ups:selftest`, then schedule a saved search or alert that fires whenever failures breach your maintenance thresholds and routes tickets into your battery-replacement workflow.",
  "visualization": "Primary panel: sortable Table keyed by `ups_name`, `_time`, `test_type`, `test_result`, `upsTestResultsSummary` with conditional formatting on failures; companion Single Value showing distinct failing assets over `-24h`; optional Timeline overlay comparing successful versus failed diagnostics streams per site.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "SNMP Modular Input — Splunkbase",
      "url": "https://splunkbase.splunk.com/app/1537"
    },
    {
      "title": "Splunk Lantern — Operational analytics guidance",
      "url": "https://lantern.splunk.com/"
    },
    {
      "title": "RFC 1628 — UPS Management Information Base",
      "url": "https://www.rfc-editor.org/info/rfc1628"
    },
    {
      "title": "Splunk Documentation — Configure alerting on scheduled searches",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Alert/AlertWizardWalkthrough"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `power`** — Planned work for 'UPS Self-Test Failure' shifts `count` over `span=1h` while `host` remains in-scope for UC-15.1.17. Cross-check CMDB CHG.\n\n2. **Batch `count` on `snmp:ups:selftest`** — Scheduled automation elevates `src` without crossing configured threshold. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `power` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `snmp:ups:selftest`** — First 48h baseline for `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "snmp"
  ],
  "equipmentModels": [
    "snmp_generic",
    "snmp_ups"
  ],
  "detailedImplementation": "Prerequisites\n• Deploy **UPS SNMP (upsTestResultsSummary)** so MIB-derived UPS diagnostics land with the sourcetype aliases referenced below; pair it with SNMP Modular Input (**Splunkbase app ID 1537**) when you need hosted polling credentials managed inside Splunk.\n• Grant Splunk administrators plus facility/power SMEs the ability to edit modular-input definitions (`inputs.conf`) while restricting alert acknowledgement to the `power_operator` RBAC role group.\n• Validate SNMP reachability from the Universal Forwarder tier using timed polls every 300 seconds minimum so `_time` aligns with physical test completion.\n\nStep 1 — Configure collection\n• Target `index=power` for retention parity with other facility telemetry and bind outputs to `sourcetype=\"snmp:ups:selftest\"` exactly as your TA documents.\n• Ensure extracts populate `upsTestResultsSummary`, `test_result`, `ups_name`, and `test_type`; missing keys indicate incomplete MIB transforms upstream.\n• Document enterprise SNMP credentials via Splunk credential stores—not plaintext props—to satisfy rotation policies.\n\nStep 2 — Author detection logic\nPaste the catalogue SPL into Search, constrain `dispatch.earliest_time` to `-24h@h` while tuning, then promote to alert mode with `-15m` rolling windows once stable.\nCompare `_time` versus `_indextime` to detect ingestion backlog skew.\n\nStep 3 — Validate\nSpot-check failing rows against the UPS local panel or embedded web summary for the same `ups_name`, epoch, and test identifier.\nCross-verify SNMP trap floods versus polled facts before acknowledging incidents.\n\nStep 4 — Operationalize\nPublish the failing-event table plus a sparkline history panel on an Observability dashboard, attach webhook routing for facility queues, and annotate known maintenance windows via lookups keyed by asset.",
  "grandmaExplanation": "We watch the physical data center for power, cooling, environment, and physical access.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
