{
  "$schema": "../../schemas/uc.schema.json",
  "id": "15.1.8",
  "title": "Generator Runtime and Fuel Level",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Generator Runtime and Fuel Level' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_generator_runtime_and_fuel_level` (UC-15.1.8), SOC dashboard panel \"Operations — Generator Runtime and Fuel Level\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-15.1.8 positive (Generator, Runtime, and, Fuel, Level): ingest synthetic events into `index=power` sourcetype='generator:telemetry' where `count` grouped by `generator_id` satisfies `fuel_pct` < 30 for 'Generator Runtime and Fuel Level'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-15.1.8 negative (Fuel Level): ingest benign `power` / `generator:telemetry` events where `generator_id` stays below `fuel_pct` < 30, or exclude the test entity via `ops_exception_register.csv`. Variant 8 confirms zero alert rows for 'Generator Runtime and Fuel Level'."
  },
  "dataSources": "Generator controller telemetry (fuel_level_pct, run_hours, engine_status) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom (generator controller SNMP/Modbus, BMS integration) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=power sourcetype=\"generator:telemetry\"\n| stats latest(fuel_level_pct) as fuel_pct, latest(run_hours) as run_hrs, latest(engine_status) as status by generator_id\n| where fuel_pct < 30 OR status!=\"standby\" AND status!=\"running\"\n| table generator_id, fuel_pct, run_hrs, status",
  "description": "Surfaces UC-15.1.8 — Generator, Runtime, and, Fuel, Level — using `index=power` sourcetype `generator:telemetry`, grouped by `generator_id`. Outputs `count` each `the scheduled window`; alert when `fuel_pct` < 30. Primary focus: Fuel Level. Purpose-built for 'Generator Runtime and Fuel Level' in Operations workflows.",
  "value": "UC-15.1.8 accelerates insight into Fuel Level ('Generator Runtime and Fuel Level') by tracking `activity` on `generator:telemetry` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Integrate generator controller via SNMP or Modbus. Ingest fuel_level_pct, run_hours, engine_status (standby, running, fault). Poll every 5–15 minutes. Alert on low fuel (<30%), engine fault, or unexpected runtime. Correlate with utility outage events. Report fuel consumption rate during run events.",
  "visualization": "Gauge (fuel level per generator), Table (generator status), Line chart (fuel level trend), Single value (lowest fuel %).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `power` (UC-15.1.8)** — Planned work for 'Generator Runtime and Fuel Level' shifts `count` over `span=1h` while `generator_id` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `generator:telemetry`** — Scheduled automation elevates `src` without crossing `fuel_pct` < 30 for UC-15.1.8. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `generator_id`** — Health checks in `power` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `generator:telemetry`** — First 48h baseline for UC-15.1.8 on `generator_id` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `generator_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "modbus",
    "snmp"
  ],
  "equipmentModels": [
    "snmp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Custom (generator controller SNMP/Modbus, BMS integration).\n• Ensure the following data sources are available: Generator controller telemetry (fuel_level_pct, run_hours, engine_status).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIntegrate generator controller via SNMP or Modbus. Ingest fuel_level_pct, run_hours, engine_status (standby, running, fault). Poll every 5–15 minutes. Alert on low fuel (<30%), engine fault, or unexpected runtime. Correlate with utility outage events. Report fuel consumption rate during run events.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=power sourcetype=\"generator:telemetry\"\n| stats latest(fuel_level_pct) as fuel_pct, latest(run_hours) as run_hrs, latest(engine_status) as status by generator_id\n| where fuel_pct < 30 OR status!=\"standby\" AND status!=\"running\"\n| table generator_id, fuel_pct, run_hrs, status\n```\n\nUnderstanding this SPL\n\n**Generator Runtime and Fuel Level** — Diesel generator monitoring for extended outages; fuel level and run hours ensure generators can sustain operations during prolonged utility failures.\n\nDocumented **Data sources**: Generator controller telemetry (fuel_level_pct, run_hours, engine_status). **App/TA** (typical add-on context): Custom (generator controller SNMP/Modbus, BMS integration). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: power; **sourcetype**: generator:telemetry. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=power, sourcetype=\"generator:telemetry\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by generator_id** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where fuel_pct < 30 OR status!=\"standby\" AND status!=\"running\"` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Generator Runtime and Fuel Level**): table generator_id, fuel_pct, run_hrs, status\n\n\nStep 3 — Validate\nCompare with the PDU UI or SNMP poll for the same devices, readings, and time range.\n\n\nStep 4 — Operationalize\nStep 4 — Operationalize\nStep 4 — Operationalize\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Gauge (fuel level per generator), Table (generator status), Line chart (fuel level trend), Single value (lowest fuel %).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Custom (generator controller SNMP/Modbus, BMS integration) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=generator:telemetry into power. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`fuel_pct, generator_id`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch the physical data center for power, cooling, environment, and physical access.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
