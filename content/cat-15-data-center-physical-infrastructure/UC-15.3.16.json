{
  "$schema": "../../schemas/uc.schema.json",
  "id": "15.3.16",
  "title": "Camera Feed Loss and Recording Gap Detection",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Exclude sandbox streams, unmanned staging cages, or drone pilot kits mapped via `site=sandbox-*`; dedicate alternate indexes for retail demo feeds so they never inherit production alerting thresholds.",
  "evidence": "Export the scheduled search job inspector plus a CSV of triggering rows (`camera_id`, `site`, `recording_gap_sec`, `_time`) and attach the matching VMS operator-console screenshot for auditor review.",
  "controlTest": {
    "positiveScenario": "UC-15.3.16 positive (Camera, Feed, Loss, and, Recording): ingest synthetic events into `index=physical` sourcetype='vms:stream_health' where `count` grouped by `camera_id`, `site` satisfies `recording_gap_sec` > 60 for 'Camera Feed Loss and Recording Gap Detection'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-15.3.16 negative (Gap Detection): ingest benign `physical` / `vms:stream_health` events where `camera_id`, `site` stays below `recording_gap_sec` > 60, or exclude the test entity via `ops_exception_register.csv`. Variant 2 confirms zero alert rows for 'Camera Feed Loss and Recording Gap Detection'."
  },
  "dataSources": "Poll your facility Video Management System REST endpoints through REST API Modular Input (Splunkbase app ID 1747) or deliver equivalent JSON probes via HTTP Event Collector `/services/collector/event`, landing telemetry on `index=physical` as `sourcetype=\"vms:stream_health\"` with Splunk-parsed fields `fps`, `keyframes`, `recording_continuity`, `recording_gap_sec`, `stream_state`, `camera_id`, and `site`; align poll intervals (commonly five minutes, tighter during commissioning) with vendor rate limits.",
  "app": "VMS API, stream health probes",
  "spl": "index=physical sourcetype=\"vms:stream_health\"\n| where fps=0 OR recording_gap_sec > 60 OR stream_state=\"lost\"\n| stats latest(recording_gap_sec) as gap by camera_id, site\n| sort -gap",
  "description": "Detects UC-15.3.16 for Camera, Feed, Loss, and, Recording over `index=physical` sourcetype `vms:stream_health` with thresholds `recording_gap_sec` > 60. Groups `count` by `camera_id`, `site` each `the scheduled window`. Centered on Gap Detection indicators. Supports repeatable operations triage for 'Camera Feed Loss and Recording Gap Detection'.",
  "value": "For 'Camera Feed Loss and Recording Gap Detection', UC-15.3.16 converts `activity` on `vms:stream_health` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Normalize each camera stream into `vms:stream_health` samples, retain FPS, keyframe cadence, recorder continuity counters, and explicit gap seconds from the VMS API or probe tier, then schedule the bundled SPL every five minutes with suppression keyed by `camera_id` and maintenance tags.",
  "visualization": "Primary Splunk table sorted by `recording_gap_sec` with conditional formatting for stream loss; paired timeline chart plotting `fps` and `recording_gap_sec` per `camera_id`; optional choropleth or SVG floor-plan overlay keyed by `site` for guard dispatch.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "REST API Modular Input on Splunkbase (1747)",
      "url": "https://splunkbase.splunk.com/app/1747"
    },
    {
      "title": "Splunk Docs — Configure alerts in Splunk Web",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Alert/AlertWorkflow"
    },
    {
      "title": "Splunk Docs — Send HTTP Event Collector data to Splunk Enterprise",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    },
    {
      "title": "Splunk Lantern — Operations monitoring guidance",
      "url": "https://lantern.splunk.com/"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `physical` (UC-15.3.16)** — Planned work for 'Camera Feed Loss and Recording Gap Detection' shifts `count` over `span=1h` while `camera_id`, `site` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `vms:stream_health`** — Scheduled automation elevates `site` without crossing `recording_gap_sec` > 60 for UC-15.3.16. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `camera_id`, `site`** — Health checks in `physical` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `vms:stream_health`** — First 48h baseline for UC-15.3.16 on `camera_id` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `camera_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "REST API Modular Input add-on (Splunkbase 1747)",
    "VMS API with stream health probes (RTSP/HLS monitoring context)"
  ],
  "detailedImplementation": "Prerequisites\n• Install REST API Modular Input (Splunkbase 1747) on a Heavy Forwarder that can reach the HTTPS management plane of your VMS, or approve an internal collector that forwards compact JSON through HEC without embedding secrets in dashboards.\n• Map vendor responses into shared keys (`fps`, `keyframes`, `recording_continuity`, `recording_gap_sec`, `stream_state`, `camera_id`, `site`) before routing to `index=physical` / `sourcetype=\"vms:stream_health\"`; document the exact REST paths (for example `/api/streams/status` or vendor-published equivalents) in the facility integration runbook.\n• Grant operators a dedicated RBAC role such as `physical_security_analyst` read-only access to `index=physical`, and reserve `edit_alert_actions` for the automation service account.\n• Follow docs/implementation-guide.md for standard `inputs.conf` placement, credential storage, and upgrade sequencing.\n\nStep 1 — Configure data collection\n• Create modular-input stanzas that issue timed HTTPS GET/POST calls every five minutes (`cron` example `*/5 * * * *`) during steady state and every one to two minutes while new racks are commissioned.\n• Stamp `_time` from the appliance-reported observation time inside the payload so gaps reflect recorder truth, not Splunk ingestion lag alone.\n• If probes cannot hit the VMS directly, relay stream-health metrics through an approved intermediary that preserves the same sourcetype and field names.\n\nStep 2 — Create the search and alert\nExecute the UC SPL (saved as report first) across a `-15m` rolling window aligned to the modular-input cadence, then promote to a scheduled alert with digest mode enabled so multiple degraded cameras batch into one notification.\n\n```spl\nindex=physical sourcetype=\"vms:stream_health\"\n| where fps=0 OR recording_gap_sec > 60 OR stream_state=\"lost\"\n| stats latest(recording_gap_sec) as gap by camera_id, site\n| sort -gap\n```\n\nUnderstanding this SPL\n• The base search pins `index=physical` and `sourcetype=\"vms:stream_health\"`, matching the normalized telemetry described under Data sources.\n• The `where` clause isolates true streaming/recording failures from benign idle scenes by combining FPS collapse, explicit gap seconds beyond sixty, or vendor-declared `stream_state=\"lost\"`.\n• `stats` rolls up the worst `recording_gap_sec` per `camera_id` and `site` so dashboards highlight entire aisles or halls rather than duplicate polling noise.\n• `sort -gap` surfaces the longest outages first for incident bridges.\n\nStep 3 — Validate\n• Compare Splunk rows against the VMS operator console for the same `camera_id` interval and capture screenshots for evidence packs.\n• Replay anonymized JSON fixtures on a dev indexer to confirm field spelling (`recording_gap_sec` vs alternate vendor labels) before promoting to production inputs.\n\nStep 4 — Operationalize and troubleshoot\n• Publish a Dashboard Studio or Classic XML panel pairing the alert table with a timeline chart keyed by `_time` and `camera_id`.\n• If alerts spike during planned maintenance, temporarily narrow the search with Splunk `lookup` masks sourced from the change-management calendar, and document roll-forward steps when inputs stall (firewall rule, TLS certificate expiry, throttled API tokens).",
  "grandmaExplanation": "We watch the physical data center for power, cooling, environment, and physical access.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
