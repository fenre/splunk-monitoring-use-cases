{
  "$schema": "../../schemas/uc.schema.json",
  "id": "15.3.27",
  "title": "Video Stream Connection Errors and Quality Issues (Meraki MV)",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'Video Stream Connection Errors and Quality Issues (Meraki MV)'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-15.3.27).",
  "evidence": "Saved search uc_15_3_27_video_stream_connection_errors_and_quali, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'Video Stream Connection Errors and Quality Issues (Meraki MV)'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-15.3.27.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "`index=meraki sourcetype=meraki:assurancealerts` from `cisco_meraki_assurance_alerts` plus `index=meraki sourcetype=meraki:webhook` from `cisco_meraki_webhook` in `Splunk_TA_cisco_meraki` (Splunkbase 5580). Stream-quality issues surface as assurance alerts; specific connection errors come via webhook.",
  "app": "`Cisco Meraki Add-on for Splunk` (Splunkbase 5580)",
  "spl": "index=meraki (sourcetype=\"meraki:assurancealerts\" OR sourcetype=\"meraki:webhook\")\n| where deviceType=\"MV\" OR like('scope.devices{}.productType', \"%camera%\") OR like(deviceType, \"%camera%\")\n| stats count as alert_count, latest(_time) as last_alert by scope.devices{}.name, type, severity\n| where alert_count > 1\n| sort - alert_count",
  "description": "Detects video stream connection failures that prevent remote viewing or recording.",
  "value": "Operations and leadership rely on 'Video Stream Connection Errors and Quality Issues (Meraki MV)' to catch regressions before they become customer-visible outages or audit findings. Early signal reduces mean time to repair, avoids duplicate manual log review, and preserves evidence for post-incident review without waiting for a user-reported ticket.",
  "implementation": "Use `cisco_meraki_assurance_alerts` (default 1h poll) for camera-related assurance alerts (stream connection issues, RTSP failures, NVR communication errors). For real-time per-event alerts also configure `cisco_meraki_webhook` and subscribe to camera alert types in the Dashboard.",
  "visualization": "Connection error timeline; affected camera list; error type breakdown.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunkbase app 5580",
      "url": "https://splunkbase.splunk.com/app/5580",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_meraki"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Cisco Meraki Add-on for Splunk` (Splunkbase 5580).\n• Ensure the following data sources are available: `index=meraki sourcetype=meraki:assurancealerts` from `cisco_meraki_assurance_alerts` plus `index=meraki sourcetype=meraki:webhook` from `cisco_meraki_webhook` in `Splunk_TA_cisco_meraki` (Splunkbase 5580). Stream-quality issues surface as assurance alerts; specific connection errors come via webhook..\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nUse `cisco_meraki_assurance_alerts` (default 1h poll) for camera-related assurance alerts (stream connection issues, RTSP failures, NVR communication errors). For real-time per-event alerts also configure `cisco_meraki_webhook` and subscribe to camera alert types in the Dashboard.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=meraki (sourcetype=\"meraki:assurancealerts\" OR sourcetype=\"meraki:webhook\")\n| where deviceType=\"MV\" OR like('scope.devices{}.productType', \"%camera%\") OR like(deviceType, \"%camera%\")\n| stats count as alert_count, latest(_time) as last_alert by scope.devices{}.name, type, severity\n| where alert_count > 1\n| sort - alert_count\n```\n\nUnderstanding this SPL\n\n**Video Stream Connection Errors and Quality Issues (Meraki MV)** — Detects video stream connection failures that prevent remote viewing or recording.\n\nDocumented **Data sources**: `index=meraki sourcetype=meraki:assurancealerts` from `cisco_meraki_assurance_alerts` plus `index=meraki sourcetype=meraki:webhook` from `cisco_meraki_webhook` in `Splunk_TA_cisco_meraki` (Splunkbase 5580). Stream-quality issues surface as assurance alerts; specific connection errors come via webhook. **App/TA** (typical add-on context): `Cisco Meraki Add-on for Splunk` (Splunkbase 5580). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: meraki; **sourcetype**: meraki:assurancealerts, meraki:webhook. Those sourcetypes align with what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=meraki, sourcetype=\"meraki:assurancealerts\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where deviceType=\"MV\" OR like('scope.devices{}.productType', \"%camera%\") OR like(deviceType, \"%camera%\")` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by scope.devices{}.name, type, severity** so each row reflects one combination of those dimensions (useful for per-host, per-user, or per-entity comparisons for this use case).\n• Filters the current rows with `where alert_count > 1` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Connection error timeline; affected camera list; error type breakdown.",
  "grandmaExplanation": "We watch the physical data center for power, cooling, environment, and physical access.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
