{
  "$schema": "../../schemas/uc.schema.json",
  "id": "15.3.4",
  "title": "Camera System Health",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not certify video retention legal holds, perform pixel-level video analytics, or replace the VMS/NVR native health console; carrier WAN or building LAN faults outside camera or recorder control need separate network operations workflows unless those signals are already ingested elsewhere.",
  "evidence": "Retain the scheduled search or alert output listing `camera_id`, `location`, `connection_status`, `recording_status`, and `last_frame`, the Splunk job identifier, and a VMS/NVR screenshot or export for the same interval showing matching device state.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=physical` sourcetype='vms:camera_status' where `entity` satisfies detection threshold for UC-15.3.4 ('Camera System Health'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `physical` / `vms:camera_status` events where `entity` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-15.3.4."
  },
  "dataSources": "Video management system logs carrying **camera status** and **recording status** normalized to `index=physical` with `sourcetype=\"vms:camera_status\"` and extracted fields **`connection_status`**, **`recording_status`**, **`camera_id`**, **`location`**, and **`last_frame`**. Typical paths: NVR/VMS **syslog** into Splunk Connect for Syslog ([Splunkbase app 4740](https://splunkbase.splunk.com/app/4740)) or a Heavy Forwarder `inputs.conf` TCP/syslog stanza; optional HTTPS **`POST`** to Splunk HEC **`/services/collector/event`** when the VMS exposes JSON status APIs polled by a scripted or modular input.",
  "app": "NVR/VMS syslog or API",
  "spl": "index=physical sourcetype=\"vms:camera_status\"\n| where recording_status!=\"recording\" OR connection_status!=\"connected\"\n| table camera_id, location, connection_status, recording_status, last_frame",
  "description": "Detects data-center surveillance cameras that stop sending heartbeats or stop recording to the Video Management System by correlating connection and recording-state fields—so aisles, entries, and cages are not unknowingly unwatched.",
  "value": "Detects data-center surveillance cameras that stop sending heartbeats or stop recording to the Video Manage.... Timely UC-15.3.4 alerting on 'Camera System Health' reduces operational risk and delayed incident response before manual triage or audit channels escalate.",
  "implementation": "Normalize VMS/NVR syslog or API camera-health events onto `index=physical` as `vms:camera_status`, validate `connection_status` and `recording_status` literals against the vendor UI, schedule the catalog SPL every five minutes with suppression per `camera_id`, and tune thresholds or lookups for maintenance and standby modes before paging physical security.",
  "visualization": "Heatmap or status grid of `camera_id` × (`connection_status`, `recording_status`); sortable Table of offline or non-recording cameras with `location` and `last_frame`; Single value KPI for percentage of cameras in `recording`; timechart of degraded-count trends for executive summaries.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable — rely on raw `index=physical` `sourcetype=\"vms:camera_status\"` searches because no accelerated CIM node is assumed for generic VMS telemetry; revisit only if you FIELDALIAS camera attributes into a curated data model after volume review.",
  "references": [
    {
      "title": "Splunk Connect for Syslog (Splunkbase 4740)",
      "url": "https://splunkbase.splunk.com/app/4740"
    },
    {
      "title": "Splunk Documentation — Forward data to Splunk Enterprise",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/ForwardingData"
    },
    {
      "title": "Splunk Documentation — Create and manage alerts",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Alert/AlertWorkflows"
    },
    {
      "title": "Splunk Documentation — REST endpoint POST stream/collector/event (HEC)",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `physical`** — Planned work for 'Camera System Health' shifts `count` over `span=1h` while `host` remains in-scope for UC-15.3.4. Cross-check CMDB CHG.\n\n2. **Batch `count` on `vms:camera_status`** — Scheduled automation elevates `src` without crossing configured threshold. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `physical` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `vms:camera_status`** — First 48h baseline for `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cctv",
    "syslog"
  ],
  "equipmentModels": [
    "Network Video Recorder (NVR) hosting recorded camera streams for data-center halls",
    "Video Management System head-end exporting syslog or HTTPS status to Splunk"
  ],
  "detailedImplementation": "Prerequisites\n• Confirm the deployment pattern described in **App**: **NVR/VMS syslog or API**—either syslog from the recorder/VMS head-end or scheduled HTTPS calls to vendor status endpoints that forward into Splunk.\n• Ingest must land on **`index=physical`** with **`sourcetype=\"vms:camera_status\"`** and stable extractions for **`connection_status`**, **`recording_status`**, **`camera_id`**, **`location`**, and **`last_frame`**; spot-check Search in a `-15m@m` window before enabling alerts.\n• Prefer Splunk Connect for Syslog ([Splunkbase 4740](https://splunkbase.splunk.com/app/4740)) or an `inputs.conf` listener on a Heavy Forwarder with TLS and a dedicated sourcetype override; for API bridges, document the HTTPS path, poll interval (for example **`*/5 * * * *`**), and any auth token vaulting—never paste secrets into props.\n• RBAC: scope a role such as **`physical_security_analyst`** to **`index=physical`** only and grant **`schedule_search`** to the service account that owns the scheduled detection.\n• Clock skew: require NTP on cameras/NVR/VMS so **`last_frame`** and `_time` comparisons match operator consoles.\n\nStep 1 — Configure data collection\n• Map each **`camera_id`** to **`location`** strings (hall, cage, mantrap) consistently with CAD floor plans used by guards.\n• If multiple sites share one index, add **`site`** or **`campus`** at ingest via **`transforms.conf`** routing from source host or subnet.\n• For API feeds, confirm the modular or scripted input writes the same field names the catalog SPL expects, or clone the search with your aliased fields.\n\nStep 2 — Create the search and alert\nSave the catalog SPL as a report, then clone to a scheduled alert with **`cron_schedule=*/5 * * * *`**, **`dispatch.earliest_time=-5m`**, **`dispatch.latest_time=now`**, and suppression keyed on **`camera_id`** for at least one interval to limit duplicate notifications during flapping links.\n\n```spl\nindex=physical sourcetype=\"vms:camera_status\"\n| where recording_status!=\"recording\" OR connection_status!=\"connected\"\n| table camera_id, location, connection_status, recording_status, last_frame\n```\n\nUnderstanding this SPL\n• **`index=physical`** and **`sourcetype=\"vms:camera_status\"`** must match your props/TAs; rename only if your build standard differs—keep Data sources and SPL aligned.\n• **`where recording_status!=\"recording\" OR connection_status!=\"connected\"`** surfaces cameras that lost heartbeats or stopped persisting segments—tune literals to your vendor vocabulary (`online`/`offline`, `ok`/`fail`) via a lookup if needed.\n• **`table`** projects operator columns for ticketing and dispatch to guards or smart-hands vendors.\n\nStep 3 — Validate\nPick two cameras in the VMS/NVR UI—one healthy, one deliberately disabled in a maintenance window—and compare connection and recording badges to Splunk rows for the same interval; export a CSV from the VMS if auditors require side-by-side evidence.\n\nStep 4 — Operationalize and troubleshoot\nPublish a dashboard with a status matrix by **`location`**, a table of failing **`camera_id`** values, and a single-value panel for percentage recording; wire alert actions (email, webhook, PagerDuty) per your physical-security runbook. If noise spikes, check syslog queue depth on the forwarder, HEC ACK failures on **`/services/collector/event`**, or mass maintenance flags from the VMS that should drive lookup-based suppressions.",
  "grandmaExplanation": "We watch the physical data center for power, cooling, environment, and physical access.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
