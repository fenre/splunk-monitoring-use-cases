{
  "$schema": "../../schemas/uc.schema.json",
  "id": "15.3.24",
  "title": "Motion Detection Events and Alert Volume Analysis (Meraki MV)",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=meraki sourcetype=meraki:webhook` from `cisco_meraki_webhook` (HEC, real-time) OR `index=meraki sourcetype=meraki:webhooklogs:api` from `cisco_meraki_webhook_logs` (polled) in `Splunk_TA_cisco_meraki` (Splunkbase 5580). Motion-detection events are delivered via Meraki Dashboard webhooks (Network-wide > Alerts); they are NOT in the polled device-API surface. Subscribe to `Camera motion alerts` in the alert profile.",
  "app": "`Cisco Meraki Add-on for Splunk` (Splunkbase 5580)",
  "spl": "index=meraki sourcetype IN (\"meraki:webhook\",\"meraki:webhooklogs:api\") (alertType=\"motionAlert\" OR alertTypeId=\"motion_alert\")\n| spath\n| eval device_name=coalesce('alertData.deviceName', 'data.deviceName', deviceName)\n| timechart span=1h count as motion_events by device_name limit=10",
  "description": "Analyzes motion detection event patterns to optimize camera sensitivity and reduce false alerts.",
  "value": "Analyzes motion detection event patterns to optimize camera sensitivity and reduce false alerts.",
  "implementation": "Motion detection alerts are NOT in the polled device-API surface of `Splunk_TA_cisco_meraki`. Two paths: (1) `cisco_meraki_webhook` HEC input (real-time, sourcetype `meraki:webhook`) — requires HEC token and a webhook URL configured in the Meraki Dashboard; (2) `cisco_meraki_webhook_logs` polled input (sourcetype `meraki:webhooklogs:api`) — simpler to set up but capped to the Meraki webhook-logs retention window. This SPL queries both. Subscribe to `Motion alert` alert types per camera or zone in the Dashboard.",
  "visualization": "Motion detection timeline; heat map by time of day; camera comparison chart.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunkbase app 5580",
      "url": "https://splunkbase.splunk.com/app/5580"
    }
  ],
  "knownFalsePositives": "Sensor anomalies during cleaning, calibration, or planned environmental changes.",
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_meraki"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Cisco Meraki Add-on for Splunk` (Splunkbase 5580).\n• Ensure the following data sources are available: `index=meraki sourcetype=meraki:webhook` from `cisco_meraki_webhook` (HEC, real-time) OR `index=meraki sourcetype=meraki:webhooklogs:api` from `cisco_meraki_webhook_logs` (polled) in `Splunk_TA_cisco_meraki` (Splunkbase 5580). Motion-detection events are delivered via Meraki Dashboard webhooks (Network-wide > Alerts); they are NOT in the polled device-API surface. Subscribe to `Camera motion alerts` in the alert profile..\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMotion detection alerts are NOT in the polled device-API surface of `Splunk_TA_cisco_meraki`. Two paths: (1) `cisco_meraki_webhook` HEC input (real-time, sourcetype `meraki:webhook`) — requires HEC token and a webhook URL configured in the Meraki Dashboard; (2) `cisco_meraki_webhook_logs` polled input (sourcetype `meraki:webhooklogs:api`) — simpler to set up but capped to the Meraki webhook-logs retention window. This SPL queries both. Subscribe to `Motion alert` alert types per camera or zone i…\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=meraki sourcetype IN (\"meraki:webhook\",\"meraki:webhooklogs:api\") (alertType=\"motionAlert\" OR alertTypeId=\"motion_alert\")\n| spath\n| eval device_name=coalesce('alertData.deviceName', 'data.deviceName', deviceName)\n| timechart span=1h count as motion_events by device_name limit=10\n```\n\nUnderstanding this SPL\n\n**Motion Detection Events and Alert Volume Analysis (Meraki MV)** — Analyzes motion detection event patterns to optimize camera sensitivity and reduce false alerts.\n\nDocumented **Data sources**: `index=meraki sourcetype=meraki:webhook` from `cisco_meraki_webhook` (HEC, real-time) OR `index=meraki sourcetype=meraki:webhooklogs:api` from `cisco_meraki_webhook_logs` (polled) in `Splunk_TA_cisco_meraki` (Splunkbase 5580). Motion-detection events are delivered via Meraki Dashboard webhooks (Network-wide > Alerts); they are NOT in the polled device-API surface. Subscribe to `Camera motion alerts` in the alert profile. **App/TA** (typical add-on context): `Cisco Meraki Add-on for Splunk` (Splunkbase 5580). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: meraki.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=meraki. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Extracts structured paths (JSON/XML) with `spath`.\n• `eval` defines or adjusts **device_name** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `timechart` plots the metric over time using **span=1h** buckets with a separate series **by device_name limit=10** — ideal for trending and alerting on this use case.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Motion detection timeline; heat map by time of day; camera comparison chart.",
  "grandmaExplanation": "We watch the physical data center for power, cooling, environment, and physical access."
}
