{
  "$schema": "../../schemas/uc.schema.json",
  "id": "15.1.27",
  "title": "APC UPS Input Line Voltage Sag with Transfer-to-Battery Events",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault",
    "Physical"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Drop hosts listed in planned-maintenance lookups, DR-drill asset tags, or harmonic-only monitoring classes until battery-transfer SNMP OIDs are confirmed for those assets.",
  "evidence": "Exports of the saved search over a 30-day window plus PDF timeline panels showing voltage nadirs matched to facility work orders and breaker labels for audit reviews.",
  "controlTest": {
    "positiveScenario": "Replay lab SNMP polls for host lab-ups01 where upsAdvInputLineVoltage reports 102 V on three consecutive five-minute samples and upsLastTransferReason begins reporting on-battery; confirm the detection search returns an event with host, min_vin, and xfer_events populated in the alerting payload.",
    "negativeScenario": "Feed steady 120 V polls with upsLastTransferReason locked to normal utility for a full day and verify the search job result count stays at zero unless a maintenance suppression token is injected, proving thresholds are not misfiring on clean power."
  },
  "dataSources": "Poll and trap APC-compatible UPS SNMP variables into `index=power` via Splunk Connect for SNMP (Splunkbase app ID 6266) or the SNMP Modular Input (Splunkbase app ID 1537); standardize `sourcetype=snmp:apc:ups` for scheduled walks and optionally merge parallel trap streams carrying UPS MIB OIDs under `sourcetype=snmp:trap`. Primary extracted readings include numeric `upsAdvInputLineVoltage`, enumerated `upsLastTransferReason`, and status bits from `upsBasicOutputStatus`.",
  "app": "Splunk Connect for SNMP (Splunkbase #6266), SNMP Modular Input (Splunkbase #1537)",
  "spl": "index=power sourcetype=snmp:apc:ups host=*\n| eval vin=tonumber(upsAdvInputLineVoltage), reason=coalesce(upsLastTransferReason, transfer_reason)\n| where vin < 108 OR vin > 132 OR match(toString(reason), \"(?i)transfer|battery|boost|trim\")\n| bin _time span=15m\n| stats count as xfer_events, min(vin) as min_vin by _time, host\n| where xfer_events > 0 OR min_vin < 108",
  "description": "Facility UPS SNMP telemetry exposes input voltage excursions and automatic transfers that typically precede accelerated battery wear. Watching polled UPS-MIB readings together with trap-fed transfer counters ties recurring on-battery episodes to upstream feeder quality rather than assuming defective UPS hardware.",
  "value": "We steer remediation toward electrical distribution, harmonic mitigation, and ATS tuning when chronically depressed input voltage or abnormal transfer reasons cluster by rack row or feeder, cutting unnecessary battery swaps and tightening readiness during generator ride-through exercises.",
  "implementation": "Normalize nominal voltage using lookup `ups_nominal_voltage` keyed on `host`. For North American 120 V service, alert when `upsAdvInputLineVoltage` stays below 108 V across two SNMP polls from Splunk Connect for SNMP (#6266) or the SNMP Modular Input (#1537), or when `upsLastTransferReason` increments on the trap path. Decode `upsBasicOutputStatus` when traps expose on-battery bits, and stash raw poll latency in an eval field so null walks are not mistaken for sag events.",
  "visualization": "Timeline (transfer events), Line chart (input voltage), Table (sites with chronic sags).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "APC — UPS management and SNMP application note",
      "url": "https://www.apc.com/us/en/faqs/FA156148/"
    },
    {
      "title": "SNMP Modular Input (Splunkbase 1537)",
      "url": "https://splunkbase.splunk.com/app/1537"
    },
    {
      "title": "Splunk Connect for SNMP (Splunkbase 6266)",
      "url": "https://splunkbase.splunk.com/app/6266"
    },
    {
      "title": "IETF RFC 1628 — UPS Management Information Base",
      "url": "https://www.rfc-editor.org/rfc/rfc1628.html"
    }
  ],
  "knownFalsePositives": "Scenario A — Weekly Generator Bank B load exercises: APC UPS units record `upsLastTransferReason` shifts during ATS transfer tests; distinguish with `generator_test_window` lookup hours or suppress hosts tagged `planned_gen_test=true`.\nScenario B — PDU-N09-A maintenance power rotations: Controlled phase loss mimics sag; distinguish using change-ticket correlation and require two offending 15-minute buckets before paging.\nScenario C — SNMP walk timeout from remote IDF closets: Missing `upsAdvInputLineVoltage` appears as zero; distinguish by demanding non-null polls in both the current and prior interval via `fillnull` guards.\nScenario D — Harmonic buck/boost trimming without battery transfer: Elevated THD keeps UPS in trim mode but never increments transfer counters; distinguish by routing those hosts to harmonic-only monitoring and excluding them from transfer-based correlation until separate OIDs exist.",
  "equipment": [
    "snmp"
  ],
  "equipmentModels": [
    "snmp_generic",
    "snmp_ups"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Install Splunk Connect for SNMP (Splunkbase #6266) or SNMP Modular Input (Splunkbase #1537) with SNMPv3 credentials confined to password managers—not embedded in dashboards.\n• Land polled UPS MIB walks and SNMP traps in `index=power`; tag poll documents `sourcetype=snmp:apc:ups` and trap payloads `sourcetype=snmp:trap`, using a saved-search macro or `append` only when both paths exist.\n• Publish CSV lookup `ups_nominal_voltage` keyed by `host` (or UPS management IP) to map nominal 120 V vs 230 V fleets and expected high/low thresholds.\n\nConfiguration\n• Configure the modular input or SC4SNMP poller to walk `upsAdvInputLineVoltage`, `upsLastTransferReason`, and `upsBasicOutputStatus` on a five-minute cadence aligned to facility change windows.\n• Schedule the supervisory saved search with `cron_schedule=*/5 * * * *`, `dispatch.earliest_time=-15m`, and `dispatch.latest_time=now` so each run covers at least two poll cycles.\n• Apply least-privilege RBAC: assign a `power_ops` role that may search only `index=power` and attach the same index allow-list to the scheduled search owner on the SHC captain.\n\nOperations\n1) Normalize thresholds from `ups_nominal_voltage` (for example flag sustained readings under 108 V on 120 V service across two successive polls).\n2) Overlay transfer spikes on facility feeder labels to isolate breaker, transformer tap, or harmonic filter work.\n3) Ingest planned maintenance markers (CMDB CSV or KV lookup) to suppress scheduled generator or ATS exercises.\n4) Feed downstream battery-cycle dashboards from the same `index=power` stream to prove cycle counts fall after upstream electrical repairs.\n\nHandover\n• Document SNMP community or USM rotation in the runbook and map on-call escalation to facility electrical partners when min line voltage breaches SLO for thirty minutes.\n\nStep 1 — Configure data collection\n\nInstall and configure the upstream Splunk app or TA: Splunk Connect for SNMP (Splunkbase #6266), SNMP Modular Input (Splunkbase #1537).\n\n**Expected data sources:** Poll and trap APC-compatible UPS SNMP variables into `index=power` via Splunk Connect for SNMP (Splunkbase app ID 6266) or the SNMP Modular Input (Splunkbase app ID 1537); standardize `sourcetype=snmp:apc:ups` for scheduled walks and optionally merge parallel trap streams carrying UPS MIB OIDs under `sourcetype=snmp:trap`. Primary extracted readings include numeric `upsAdvInputLineVoltage`, enumerated `upsLastTransferReason`, and status bits from `upsBasicOutputStatus`.\n\n**Indexes referenced by the SPL:** `power`\n**Sourcetypes referenced by the SPL:** `snmp:apc:ups`, `snmp:trap`\n\nAfter enabling the input, confirm event arrival on a search head with:\n\n```spl\nindex=* sourcetype=\"snmp:apc:ups\" earliest=-15m | head 5\n```\n\nTune the input's polling interval, batch size, and field extraction to match the volume and latency requirements of the alert. Keep an operational baseline of expected events-per-minute so anomalous drops in ingest can be detected separately from anomalous behaviour.\n\n\nStep 2 — Create the search and alert\n\nUse the SPL on the use-case page as the starting point. Schedule it as a saved search (typically every 5–15 minutes over a rolling lookback equal to the alert's intended detection window). Tune the threshold (`where`/`stats` clauses) to your environment's normal baseline before enabling alert actions. For high-volume sources, use `tstats` against the accelerated data model when possible.\n\nStep 3 — Validate\n\nValidate the search against a known-good event window:\n\n1. Run the SPL over a recent time range where the monitored condition is known to be present (or absent).\n2. Cross-check field values against the vendor's UI or CLI (controller dashboard, REST API response, syslog log file on the device).\n3. Confirm CIM field extractions resolve by running `| datamodel <model> search | head 1` and inspecting the relevant fields.\n4. Walk a single event through the alert action to verify PagerDuty / Slack / email rendering before enabling production alerting.\n\nStep 5 — Troubleshoot\n\nCommon failure modes for this use case:\n\n• **No events arriving** — Confirm the upstream input on the forwarder or HEC endpoint is enabled and reachable from the data-collection tier. Inspect `$SPLUNK_HOME/var/log/splunk/splunkd.log` for input errors. Cross-check with `| tstats count where index=power sourcetype=snmp:apc:ups earliest=-1h`.\n\n• **Field extraction returning null** — Inspect the relevant `props.conf` / `transforms.conf` for the sourcetype on the search head and indexer. A vendor TA upgrade or a corporate hardening script can break the regex. Compare against the TA's default stanza using `splunk btool props list --debug | grep snmp:apc:ups`.\n\n• **Search returns no rows even when the condition is present** — The role running the search may lack `srchIndexesAllowed` for the underlying index. Run `| rest splunk_server=local /servicesNS/-/-/authorization/roles | search title=`role_name` | table title srchIndexesAllowed`, replacing the role name with the role that owns the saved search.\n\n• **High false-positive rate** — Maintain an exception lookup keyed on the dominant noise field (host, user, src, process_name, etc.) and join it to the alert with `| lookup <exceptions>.csv key OUTPUT in_exceptions | where in_exceptions!=\"yes\"`. Prefer narrow per-key suppressions over wholesale disabling.\n\n• **Alert fires repeatedly during expected maintenance windows** — Maintain a `maintenance_windows` lookup and join it against the alert: `| lookup maintenance_windows host OUTPUT in_window | where in_window!=\"yes\"`. Keep the lookup in source control next to the saved-search definitions.\n\n• **Alert never fires even when the dashboard shows the condition** — The alert's scheduled time range probably doesn't match the search. Verify cron schedule and earliest/latest tokens align with the intended detection window.\n",
  "grandmaExplanation": "We watch the physical data center for power, cooling, environment, and physical access.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
