{
  "$schema": "../../schemas/uc.schema.json",
  "id": "15.1.16",
  "title": "Breaker Panel Load Balancing",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not replace infrared thermography, certified arc-flash studies, or National Electrical Code compliance audits—only operational visibility on already-modeled `panel:phase` telemetry and Splunk-side thresholds.",
  "evidence": "Archive the alert artifact or scheduled export showing `_time`, `panel_id`, per-phase amps, computed `imbalance`, and screenshot or export from the PQ analyzer for the identical window plus the service request that documents any load change.",
  "controlTest": {
    "positiveScenario": "UC-15.1.16 positive (Breaker, Panel, Load, Balancing): ingest synthetic events into `index=power` sourcetype='panel:phase' where `max` grouped by `entity` satisfies detection threshold for 'Breaker Panel Load Balancing'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-15.1.16 negative (Load Balancing): ingest benign `power` / `panel:phase` events where `entity` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 10 confirms zero alert rows for 'Breaker Panel Load Balancing'."
  },
  "dataSources": "Normalize three-phase branch or panelboard metering into `index=power` as `sourcetype=\"panel:phase\"` with extracted fields `phase_a_amps`, `phase_b_amps`, `phase_c_amps`, plus `panel_id` and `rated_amps` so the imbalance math in the catalog SPL resolves. Typical paths mirror the Panelboard metering and PQ analyzer context in App: poll current transducer or meter outputs through SNMP Modular Input (Splunkbase app ID 1537) or Splunk Connect for SNMP (Splunkbase app ID 6266) with `inputs.conf` trap or poll stanzas, push sub-minute aggregates through `http://<heavy-forwarder>:8088/services/collector/event` HEC tokens, or run a scripted modular input that reads the same values the PQ tool already publishes to CSV.",
  "app": "Panelboard metering, PQ analyzer",
  "spl": "index=power sourcetype=\"panel:phase\"\n| eval max_p=max(phase_a_amps, phase_b_amps, phase_c_amps), min_p=min(phase_a_amps, phase_b_amps, phase_c_amps), imbalance=max_p-min_p\n| where imbalance > rated_amps*0.15\n| table panel_id, phase_a_amps, phase_b_amps, phase_c_amps, imbalance",
  "description": "Monitors UC-15.1.16 — Breaker, Panel, Load, Balancing — using `index=power` sourcetype `panel:phase`, grouped by `entity`. Outputs `max` each `the scheduled window`; alert when configured threshold. Primary focus: Load Balancing. Purpose-built for 'Breaker Panel Load Balancing' in Operations workflows.",
  "value": "For 'Breaker Panel Load Balancing', UC-15.1.16 converts `activity` on `panel:phase` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Normalize branch-board CT or PQ-feed currents into `panel:phase`, maintain accurate `rated_amps` per `panel_id`, schedule the imbalance SPL every five minutes against `-5m`, tune the imbalance percentage macro for dense single-phase racks, and correlate alerts with rack migration tickets before dispatching electricians.",
  "visualization": "Primary: clustered column chart comparing `phase_a_amps`, `phase_b_amps`, `phase_c_amps` per `panel_id` for the latest five-minute slice. Secondary: table sorted by descending `imbalance` with color rules. Tertiary: single-value KPI counting panels breaching threshold in the selected window with drill-down to raw events.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "SNMP Modular Input add-on (Splunkbase 1537)",
      "url": "https://splunkbase.splunk.com/app/1537"
    },
    {
      "title": "Splunk Docs — Manage SNMP data",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/ManagingSNMPdata"
    },
    {
      "title": "Splunk Docs — Set up and use HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    },
    {
      "title": "Splunk Lantern — operations best practices library",
      "url": "https://lantern.splunk.com/"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `power` (UC-15.1.16)** — Planned work for 'Breaker Panel Load Balancing' shifts `max` over `span=1h` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `max` on `panel:phase`** — Scheduled automation elevates `max_p` without crossing configured threshold for UC-15.1.16. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `power` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `panel:phase`** — First 48h baseline for UC-15.1.16 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "snmp"
  ],
  "equipmentModels": [
    "panelboard_metering",
    "pq_analyzer",
    "snmp_generic"
  ],
  "detailedImplementation": "Prerequisites\n• Confirm the ingestion path tied to Panelboard metering and PQ analyzer lands on `index=power` with `sourcetype=\"panel:phase\"`; verify `props.conf` extracts `phase_a_amps`, `phase_b_amps`, `phase_c_amps`, `panel_id`, and `rated_amps` at search time.\n• Deploy collectors on a Heavy Forwarder class host: SNMP Modular Input (Splunkbase app ID 1537) for SNMP/OID polling, optionally Splunk Connect for SNMP (Splunkbase app ID 6266) for trap heavy sites, or an HEC endpoint in `inputs.conf` receiving JSON from a DCIM exporter—document the exact stanza names in your runbook.\n• Align device clocks to NTP so `_time` matches the PQ dashboard; use Interactive Search over `-24h@h` to confirm numeric fields are populated before alerting.\n• RBAC: grant `facility_power_rw` roles search/filter rights limited to `index=power` and schedule_saved_search capability only on the dedicated svc_facilities_scheduler service account.\n\nStep 1 — Configure thresholds\nBaseline each `panel_id` with actual rated frame amps (`rated_amps`) sourced from CMDB or asset tags; codify imbalance allowance (example catalog default 15%) in a macro `panel_imbalance_pct` so operators tune without rewriting SPL.\n\nStep 2 — Build and schedule the detection\nPaste the UC SPL into Search, validate skewed versus balanced panels, then save as alert `cron_schedule=*/5 * * * *` with `dispatch.earliest_time=-5m` and `dispatch.latest_time=now` so oscillations surface within one polling cadence.\n\nStep 3 — Validate against native tooling\nCompare Splunk-derived imbalance rows with the PQ analyzer UI or parallel SNMP poll on the same interval; reconcile differences caused by rounding before enabling paging.\n\nStep 4 — Operationalize\nPublish paired panels on a Dashboard Studio board (phase amp bars plus imbalance table), wire alert actions to email or webhook per facility policy, log tuning decisions referencing `panel_id` thresholds, and rehearse suppression macros keyed on planned maintenance CSV lookups.",
  "grandmaExplanation": "We watch the physical data center for power, cooling, environment, and physical access.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
