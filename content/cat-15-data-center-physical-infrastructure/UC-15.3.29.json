{
  "$schema": "../../schemas/uc.schema.json",
  "id": "15.3.29",
  "title": "Night Mode Effectiveness and Low-Light Performance (Meraki MV)",
  "criticality": "low",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Night Mode Effectiveness and Low-Light Performance (Meraki MV)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_night_mode_effectiveness_and_low_light_performan` (UC-15.3.29), SOC dashboard panel \"Operations — Night Mode Effectiveness and Low-Light Performance (Meraki MV)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-15.3.29 positive (Night, Mode, Effectiveness, and, Low): ingest synthetic events into `index=meraki` sourcetype='target_sourcetype' where `count` grouped by `device_name`, `period` satisfies `hour` >= 22, `hour` < 6, `hour` >= 18, or `hour` < 22 for 'Night Mode Effectiveness and Low-Light Performance (Meraki MV)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-15.3.29 negative ((Meraki MV)): ingest benign `meraki` / `target_sourcetype` events where `device_name`, `period` stays below `hour` >= 22, `hour` < 6, `hour` >= 18, or `hour` < 22, or exclude the test entity via `ops_exception_register.csv`. Variant 11 confirms zero alert rows for 'Night Mode Effectiveness and Low-Light Performance (Meraki MV)'."
  },
  "dataSources": "`index=meraki sourcetype=meraki:audit` from `cisco_meraki_audit` plus `index=meraki sourcetype=meraki:webhook` (HEC) or `meraki:webhooklogs:api` (polled) from `Splunk_TA_cisco_meraki` (Splunkbase 5580). Camera night-mode quality is not exposed in the TA; this UC uses motion-alert volume per hour-of-day as a proxy.",
  "app": "`Cisco Meraki Add-on for Splunk` (Splunkbase 5580)",
  "spl": "index=meraki sourcetype IN (\"meraki:webhook\",\"meraki:webhooklogs:api\") (alertType=\"motionAlert\" OR alertTypeId=\"motion_alert\")\n| spath\n| eval device_name=coalesce('alertData.deviceName', 'data.deviceName', deviceName)\n| eval hour=tonumber(strftime(_time, \"%H\"))\n| eval period=case(hour>=22 OR hour<6, \"Night (22-06)\", hour>=18 AND hour<22, \"Evening (18-22)\", hour>=6 AND hour<10, \"Morning (06-10)\", true(), \"Day (10-18)\")\n| stats count as motion_alerts by device_name, period\n| sort device_name, period",
  "description": "Alerts on UC-15.3.29 — Night, Mode, Effectiveness, and, Low — using `index=meraki` sourcetype `configured sourcetype`, grouped by `device_name`, `period`. Outputs `count` each `the scheduled window`; alert when `hour` >= 22, `hour` < 6, `hour` >= 18, or `hour` < 22. Primary focus: (Meraki MV). Purpose-built for 'Night Mode Effectiveness and Low-Light Performance (Meraki MV)' in Operations workflows.",
  "value": "UC-15.3.29 helps teams contain Night, Mode, Effectiveness, and, Low scenarios and reduce operational risk and delayed incident response before incidents compound.",
  "implementation": "The Splunk_TA_cisco_meraki TA does not expose per-camera quality scores or night-mode effectiveness directly. Two practical paths: (1) audit-based — track admin changes to camera quality settings via `meraki:audit`; (2) webhook-based (this SPL) — bucket motion alerts by hour-of-day to spot cameras whose night-mode behaviour is anomalous (e.g. far more or far fewer motion alerts than peers during 22:00-06:00).",
  "visualization": "Night mode quality gauge; low-light performance timeline; affected camera list.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunkbase app 5580",
      "url": "https://splunkbase.splunk.com/app/5580",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Add-on for Cisco Meraki (Splunkbase 5631)",
      "url": "https://splunkbase.splunk.com/app/5631",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Cisco Security documentation",
      "url": "https://www.cisco.com/c/en/us/support/security/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `meraki` (UC-15.3.29)** — Planned work for 'Night Mode Effectiveness and Low-Light Performance (Meraki MV)' shifts `count` over `span=1h` while `device_name`, `period` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `configured_sourcetype`** — Scheduled automation elevates `period` without crossing `hour` >= 22 for UC-15.3.29. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `device_name`, `period`** — Health checks in `meraki` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-15.3.29 on `device_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `device_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_meraki"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Cisco Meraki Add-on for Splunk` (Splunkbase 5580).\n• Ensure the following data sources are available: `index=meraki sourcetype=meraki:audit` from `cisco_meraki_audit` plus `index=meraki sourcetype=meraki:webhook` (HEC) or `meraki:webhooklogs:api` (polled) from `Splunk_TA_cisco_meraki` (Splunkbase 5580). Camera night-mode quality is not exposed in the TA; this UC uses motion-alert volume per hour-of-day as a proxy..\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nThe Splunk_TA_cisco_meraki TA does not expose per-camera quality scores or night-mode effectiveness directly. Two practical paths: (1) audit-based — track admin changes to camera quality settings via `meraki:audit`; (2) webhook-based (this SPL) — bucket motion alerts by hour-of-day to spot cameras whose night-mode behaviour is anomalous (e.g. far more or far fewer motion alerts than peers during 22:00-06:00).\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=meraki sourcetype IN (\"meraki:webhook\",\"meraki:webhooklogs:api\") (alertType=\"motionAlert\" OR alertTypeId=\"motion_alert\")\n| spath\n| eval device_name=coalesce('alertData.deviceName', 'data.deviceName', deviceName)\n| eval hour=tonumber(strftime(_time, \"%H\"))\n| eval period=case(hour>=22 OR hour<6, \"Night (22-06)\", hour>=18 AND hour<22, \"Evening (18-22)\", hour>=6 AND hour<10, \"Morning (06-10)\", true(), \"Day (10-18)\")\n| stats count as motion_alerts by device_name, period\n| sort device_name, period\n```\n\nUnderstanding this SPL\n\n**Night Mode Effectiveness and Low-Light Performance (Meraki MV)** — Monitors camera performance in low-light conditions to ensure night surveillance effectiveness.\n\nDocumented **Data sources**: `index=meraki sourcetype=meraki:audit` from `cisco_meraki_audit` plus `index=meraki sourcetype=meraki:webhook` (HEC) or `meraki:webhooklogs:api` (polled) from `Splunk_TA_cisco_meraki` (Splunkbase 5580). Camera night-mode quality is not exposed in the TA; this UC uses motion-alert volume per hour-of-day as a proxy. **App/TA** (typical add-on context): `Cisco Meraki Add-on for Splunk` (Splunkbase 5580). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: meraki.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=meraki. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Extracts structured paths (JSON/XML) with `spath`.\n• `eval` defines or adjusts **device_name** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **hour** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **period** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by device_name, period** so each row reflects one combination of those dimensions (useful for per-host, per-user, or per-entity comparisons for this use case).\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Night mode quality gauge; low-light performance timeline; affected camera list.",
  "grandmaExplanation": "We watch the physical data center for power, cooling, environment, and physical access.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
