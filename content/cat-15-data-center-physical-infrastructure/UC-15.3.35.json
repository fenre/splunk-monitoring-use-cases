{
  "$schema": "../../schemas/uc.schema.json",
  "id": "15.3.35",
  "title": "After-Hours Wireless Presence Detection",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'After-Hours Wireless Presence Detection' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_after_hours_wireless_presence_detection` (UC-15.3.35), SOC dashboard panel \"Operations — After-Hours Wireless Presence Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-15.3.35 positive (After, Hours, Wireless, Presence, Detection): ingest synthetic events into `index=cisco_spaces` sourcetype='cisco:spaces:presence' where `count` grouped by `macAddress`, `owner`, `department`, `building` satisfies `hour` < 6, `hour` > 20, or `day` > 5 for 'After-Hours Wireless Presence Detection'. Validate lookup `known_devices.csv`, eventType `DEVICE_ENTRY`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-15.3.35 negative (Presence Detection): ingest benign `cisco_spaces` / `cisco:spaces:presence` events where `macAddress`, `owner`, `department`, `building` stays below `hour` < 6, `hour` > 20, or `day` > 5, or exclude the test entity via `ops_exception_register.csv`. Variant 14 confirms zero alert rows for 'After-Hours Wireless Presence Detection'."
  },
  "dataSources": "Cisco Spaces Firehose API (DEVICE_PRESENCE events — entry/exit with timestamps) Indexes: cisco_spaces. Sourcetypes: cisco:spaces:presence. Key fields: authorized_afterhours, building, day, department, floor, hour, macAddress, owner. Validate field extraction in Search before saving the alert; compare event counts to the vendor admin console.",
  "app": "`Spaces Add-On for Splunk` (Splunkbase 8485), Cisco Spaces Firehose API",
  "spl": "index=cisco_spaces sourcetype=\"cisco:spaces:presence\" eventType=\"DEVICE_ENTRY\"\n| eval hour=strftime(_time, \"%H\")\n| eval day=strftime(_time, \"%u\")\n| where (hour<6 OR hour>20) OR day>5\n| lookup known_devices.csv macAddress OUTPUT owner, department, authorized_afterhours\n| where authorized_afterhours!=\"yes\" OR isnull(authorized_afterhours)\n| stats count as entries, earliest(_time) as first_seen, latest(_time) as last_seen by macAddress, owner, department, building, floor, zoneName\n| sort -count\n| table macAddress, owner, department, building, floor, zoneName, first_seen, last_seen, entries",
  "description": "Alerts on UC-15.3.35 — After, Hours, Wireless, Presence, Detection — using `index=cisco_spaces` sourcetype `cisco:spaces:presence`, grouped by `macAddress`, `owner`, `department`, `building`. Outputs `count` each `the scheduled window`; alert when `hour` < 6, `hour` > 20, or `day` > 5. Filter chain includes lookup `known_devices.csv`, eventType `DEVICE_ENTRY`. Purpose-built for 'After-Hours Wireless Presence Detection' in Operations workflows.",
  "value": "Alerting on After, Hours, Wireless, Presence, Detection through UC-15.3.35 reduces operational risk and delayed incident response by surfacing `activity` early.",
  "implementation": "Configure Cisco Spaces Firehose API to stream DEVICE_PRESENCE events with entry/exit notifications. Build a known devices lookup by correlating MAC addresses with user identities from ISE or Active Directory (via 802.1X authentication records). Define business hours per building/zone (some facilities operate 24/7). Maintain an authorized_afterhours list for security, janitorial, and operations staff. Alert on unknown devices or unauthorized users detected after hours in sensitive zones. Integrate with badge access logs for cross-validation.",
  "visualization": "Floor plan map (after-hours device locations), Timeline (presence events outside business hours), Table (unauthorized after-hours presence), Bar chart (after-hours detections by zone).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunkbase app 8485",
      "url": "https://splunkbase.splunk.com/app/8485",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Authentication",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Authentication",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `cisco_spaces` (UC-15.3.35)** — Planned work for 'After-Hours Wireless Presence Detection' shifts `count` over `span=1h` while `macAddress`, `owner`, `department`, `building` remains in-scope. eventType=DEVICE_ENTRY Applies lookup `known_devices.csv`, eventType `DEVICE_ENTRY`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `cisco:spaces:presence`** — Scheduled automation elevates `owner` without crossing `hour` < 6 for UC-15.3.35. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `macAddress`, `owner`, `department`, `building`** — Health checks in `cisco_spaces` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `cisco:spaces:presence`** — First 48h baseline for UC-15.3.35 on `macAddress` is unstable; join `known_devices.csv` prior to threshold.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `macAddress`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_spaces"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Spaces Add-On for Splunk` (Splunkbase 8485), Cisco Spaces Firehose API.\n• Ensure the following data sources are available: Cisco Spaces Firehose API (DEVICE_PRESENCE events — entry/exit with timestamps).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nConfigure Cisco Spaces Firehose API to stream DEVICE_PRESENCE events with entry/exit notifications. Build a known devices lookup by correlating MAC addresses with user identities from ISE or Active Directory (via 802.1X authentication records). Define business hours per building/zone (some facilities operate 24/7). Maintain an authorized_afterhours list for security, janitorial, and operations staff. Alert on unknown devices or unauthorized users detected after hours in sensitive zones. Integrat…\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=cisco_spaces sourcetype=\"cisco:spaces:presence\" eventType=\"DEVICE_ENTRY\"\n| eval hour=strftime(_time, \"%H\")\n| eval day=strftime(_time, \"%u\")\n| where (hour<6 OR hour>20) OR day>5\n| lookup known_devices.csv macAddress OUTPUT owner, department, authorized_afterhours\n| where authorized_afterhours!=\"yes\" OR isnull(authorized_afterhours)\n| stats count as entries, earliest(_time) as first_seen, latest(_time) as last_seen by macAddress, owner, department, building, floor, zoneName\n| sort -count\n| table macAddress, owner, department, building, floor, zoneName, first_seen, last_seen, entries\n```\n\nUnderstanding this SPL\n\n**After-Hours Wireless Presence Detection** — Detects Wi-Fi-connected devices present in buildings or restricted zones outside of business hours. Correlates with badge access data and employee schedules to identify unauthorized presence — potential tailgating, after-hours theft, or social engineering. Unlike badge-only systems which only detect entry, wireless presence confirms ongoing physical presence. Supports investigations by providing device MAC, location, and duration of after-hours presence. Particularly…\n\nDocumented **Data sources**: Cisco Spaces Firehose API (DEVICE_PRESENCE events — entry/exit with timestamps). **App/TA** (typical add-on context): `Spaces Add-On for Splunk` (Splunkbase 8485), Cisco Spaces Firehose API. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: cisco_spaces; **sourcetype**: cisco:spaces:presence. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=cisco_spaces, sourcetype=\"cisco:spaces:presence\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **hour** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **day** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where (hour<6 OR hour>20) OR day>5` — typically the threshold or rule expression for this monitoring goal.\n• Enriches events using `lookup` (lookup definition + optional OUTPUT fields).\n• Filters the current rows with `where authorized_afterhours!=\"yes\" OR isnull(authorized_afterhours)` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by macAddress, owner, department, building, floor, zoneName** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n• Pipeline stage (see **After-Hours Wireless Presence Detection**): table macAddress, owner, department, building, floor, zoneName, first_seen, last_seen, entries\n\nStep 3 — Validate\nCompare with the BMS console for the same equipment, sensors, and time range.\n\n\nStep 4 — Operationalize\nStep 4 — Operationalize\nStep 4 — Operationalize\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Floor plan map (after-hours device locations), Timeline (presence events outside business hours), Table (unauthorized after-hours presence), Bar chart (after-hours detections by zone).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Spaces Add-On for Splunk` (Splunkbase 8485), Cisco Spaces Firehose API). Enable the modular input or HEC token that writes sourcetype=cisco:spaces:presence into cisco_spaces. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`authorized_afterhours, building, day, department`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch the physical data center for power, cooling, environment, and physical access.",
  "hardware": "Cisco Meraki MR36, MR44, MR46, MR56, MR57, MR76, MR78, MR86",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
