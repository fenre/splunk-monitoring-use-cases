{
  "$schema": "../../schemas/uc.schema.json",
  "id": "15.1.13",
  "title": "Transfer Switch Events",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not certify NEC separation distances, substitute arc-flash engineering studies, or replace vendor ATS commissioning reports—only correlates telemetry already modeled as sourcetype \"ats:events\".",
  "evidence": "Retain exported alert results showing _time, ats_id, event, source_side, duration_ms plus screenshots or SNMP MIB polls proving feeder state during the window for retroactive audits.",
  "controlTest": {
    "positiveScenario": "UC-15.1.13 positive (Transfer, Switch, Events): ingest synthetic events into `index=power` sourcetype='ats:events' where `count` grouped by `entity` satisfies `duration_ms` > 500 for 'Transfer Switch Events'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-15.1.13 negative (Switch Events): ingest benign `power` / `ats:events` events where `entity` stays below `duration_ms` > 500, or exclude the test entity via `ops_exception_register.csv`. Variant 1 confirms zero alert rows for 'Transfer Switch Events'."
  },
  "dataSources": "Land ATS telemetry on index=power as sourcetype=\"ats:events\" with Splunk-extracted dimensions ats_id, event (for example transfer_to_gen, retransfer_to_utility, test), source_side, duration_ms, and the relay or trap source field referenced in props. Typical collection paths are SNMP traps or polled OIDs from the ATS SNMP/BMS integration described in App, dry-contact closures into a syslog or HEC bridge, Splunk Connect for SNMP (Splunkbase app ID 6266), or SNMP Modular Input (Splunkbase app ID 1537) on a Heavy Forwarder with UDP/162 trap stanzas in inputs.conf.",
  "app": "ATS SNMP/BMS, relay contacts",
  "spl": "index=power sourcetype=\"ats:events\"\n| where event IN (\"transfer_to_gen\",\"retransfer_to_utility\",\"test\") OR duration_ms > 500\n| table _time, ats_id, event, source_side, duration_ms\n| sort -_time",
  "description": "Identifies Switch Events activity in UC-15.1.13 ('Transfer Switch Events') from `index=power` sourcetype `ats:events`. Rolls up `count` by `entity` across `the scheduled window` and pages when `duration_ms` > 500. Covers Transfer, Switch, Events. Operations operators treat UC-15.1.13 as the canonical signal.",
  "value": "Operators running UC-15.1.13 on `ats:events` feeds shorten triage for Switch Events tied to 'Transfer Switch Events', lowering operational risk and delayed incident response.",
  "implementation": "Normalize ATS SNMP/BMS or dry-contact relay signals into ats:events, keep management-interface clocks aligned to NTP, run the catalog SPL on index=power with tunable duration_ms thresholds, and correlate hits with upstream utility or generator maintenance windows before opening an incident.",
  "visualization": "Primary: Timeline colored by event across each ats_id for shift reviews. Secondary: Table sorted by latest transfer per ATS with sparkline-ready duration_ms. KPI: Single-value panel counting qualifying transitions over `-24h` with drill-down to raw events.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Connect for SNMP (Splunkbase 6266)",
      "url": "https://splunkbase.splunk.com/app/6266"
    },
    {
      "title": "SNMP Modular Input add-on (Splunkbase 1537)",
      "url": "https://splunkbase.splunk.com/app/1537"
    },
    {
      "title": "Splunk Docs — Configure SNMP traps",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/ConfigureSNMPTraps"
    },
    {
      "title": "Splunk Docs — Monitor SNMP Data",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/ManagingSNMPdata"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `power` (UC-15.1.13)** — Planned work for 'Transfer Switch Events' shifts `count` over `span=1h` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `ats:events`** — Scheduled automation elevates `src` without crossing `duration_ms` > 500 for UC-15.1.13. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `power` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `ats:events`** — First 48h baseline for UC-15.1.13 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "snmp",
    "syslog"
  ],
  "equipmentModels": [
    "snmp_generic"
  ],
  "detailedImplementation": "Prerequisites\n• Deploy collection that matches App: ATS SNMP/BMS traps/polls plus optional dry-contact relays—prefer Splunk Connect for SNMP (Splunkbase app ID 6266) or SNMP Modular Input (Splunkbase app ID 1537) on a Heavy Forwarder that owns UDP/162 trap listeners declared in inputs.conf, forwarding into index=power with sourcetype=\"ats:events\".\n• Validate props/transforms extract ats_id, event, source_side, duration_ms, and trap identity fields before alerting; spot-check Interactive Search over `-24h@h` for field completeness.\n• RBAC: grant facility engineers a scoped role such as power_facility_ops read/search on index=power only; dedicate a scheduler account with schedule_search for the saved detection.\n• Document ATS-specific enumerated event tokens your normalization emits so Splunk admins map trap OID payloads consistently.\n\nStep 1 — Configure data collection\nMap SNMP trap bindings or relay closures into structured rows with `_time` anchored to device-reported timestamps; if relays arrive via syslog, tune LINE_BREAKER/TIME_PREFIX so `_time` reflects switch action, not forwarder receipt lag.\n\nStep 2 — Create the search and alert\nPaste the UC SPL into Search, verify rows for known transfers and drills, then save as an alert with cron_schedule `*/5 * * * *` paired to dispatch.earliest_time `-5m` so oscillations surface quickly without overlapping windows.\n\nStep 3 — Validate\nCross-check Splunk rows against the ATS local status indication or an SNMP poll/walk from the same management VLAN for the same ats_id during the `_time` window; reconcile discrepancies before promoting to production schedules.\n\nStep 4 — Operationalize\nPublish the query on an operational dashboard (timeline plus sortable table), route alert actions through your paging policy, record tuning thresholds for duration_ms and allowed event vocabulary in the facility runbook, and rehearse DR drills with suppression macros keyed on maintenance CSV lookups.",
  "grandmaExplanation": "We watch the physical data center for power, cooling, environment, and physical access.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
