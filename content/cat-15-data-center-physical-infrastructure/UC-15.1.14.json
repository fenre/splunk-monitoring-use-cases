{
  "$schema": "../../schemas/uc.schema.json",
  "id": "15.1.14",
  "title": "Power Factor Monitoring",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not certify IEEE-compliant harmonic mitigation designs, replace calibrated PQ meters, or close utility tariff disputes—Splunk only evidences telemetry breaches requiring electrical engineers to act in the field.",
  "evidence": "Export the scheduled **`PQ-PowerFactor-Breach`** alert plus backing **`timechart`** panel screenshot showing **`index=power`** **`power_meter:pq`** **`feed_id`** drilldown retained **`90` days** in the ops evidence SharePoint.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=power` sourcetype='power_meter:pq' where `feed_id` satisfies `pf` < 0.92 for UC-15.1.14 ('Power Factor Monitoring'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `power` / `power_meter:pq` events where `feed_id` stays below `pf` < 0.92, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-15.1.14."
  },
  "dataSources": "**Power meter SNMP (PQ meters)** paths normalize into **`index=power`** with **`sourcetype=\"power_meter:pq\"`**, exposing extracted numeric **`pf`**, **`kw`**, **`kvar`**, and harmonic proxies mapped as **`thd`** / **`thd_pct`** plus dimensional **`feed_id`**. SNMP Modular Input deployments commonly cite Splunkbase 1537 when translating enterprise PQ MIB walks into those fields; validate OID-to-field mappings against each meter family before trusting thresholds.",
  "app": "Power meter SNMP (PQ meters)",
  "spl": "index=power sourcetype=\"power_meter:pq\"\n| where pf < 0.92 OR thd_pct > 8\n| timechart span=15m avg(pf) as avg_pf by feed_id",
  "description": "Monitors SNMP-polled PQ telemetry on main distribution feeds so Splunk surfaces sustained lagging power factor, harmonic distortion creep, and per-feed divergence across feed_id before breaker thermal margins or tariff clauses are breached.",
  "value": "Monitors SNMP-polled PQ telemetry on main distribution feeds so Splunk surfaces sustained lagging power fac.... Timely UC-15.1.14 alerting on 'Power Factor Monitoring' reduces operational risk and delayed incident response before manual triage or audit channels escalate.",
  "implementation": "Configure SNMP polls so every monitored MSB feed lands **`feed_id`**, **`pf`**, **`thd_pct`**, **`kw`**, and **`kvar`** at least every five minutes into **`power_meter:pq`**. Align **`pf`** alert thresholds with the utility reactive-demand rider or internal PF guarantee (often 0.92 leading/lagging). Layer optional capacitor-step states only when that auxiliary sourcetype already exists—otherwise trend PF alone and annotate remediation tickets manually.",
  "visualization": "Dashboard Studio dark layout: background **`splunk.rectangle`** bands separating KPI strip from trends; **`splunk.singlevalue`** panels bind worst-case **`pf`** and **`thd_pct`** with **`majorField`** overrides; **`splunk.line`** charts plot **`avg_pf`** **`by feed_id`** using **`seriesColors`** aligned to facility branding; **`splunk.table`** lists breached **`feed_id`**, latest **`kw`**, **`kvar`**, and SLA countdown timers; optional **`splunk.markdown`** interprets tariff clauses beside the charts.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable — PQ SNMP metrics remain raw structured logs rather than accelerated CIM nodes for this observability UC.",
  "references": [
    {
      "title": "SNMP Modular Input (Splunkbase app 1537)",
      "url": "https://splunkbase.splunk.com/app/1537"
    },
    {
      "title": "Splunk Lantern — operational analytics guidance",
      "url": "https://lantern.splunk.com/"
    },
    {
      "title": "Splunk Documentation — SNMP network monitoring overview",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/MonitorNetworks"
    },
    {
      "title": "RFC 3416 — Protocol Operations for SNMP",
      "url": "https://www.rfc-editor.org/rfc/rfc3416.html"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `power`** — Planned work for 'Power Factor Monitoring' shifts `avg` over `span=15m` while `feed_id` remains in-scope for UC-15.1.14. Cross-check CMDB CHG.\n\n2. **Batch `avg` on `power_meter:pq`** — Scheduled automation elevates `src` without crossing `pf` < 0.92. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `feed_id`** — Health checks in `power` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `power_meter:pq`** — First 48h baseline for `feed_id` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `feed_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "snmp"
  ],
  "equipmentModels": [
    "snmp_generic"
  ],
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud search heads grant **`power_ops`** (or equivalent facilities-engineering role) read rights on **`index=power`** plus capability to schedule alerts every fifteen minutes.\n• Deploy **Power meter SNMP (PQ meters)** ingestion (cross-check Splunkbase 1537 SNMP Modular Input patterns when applicable) so SNMP modular inputs write **`sourcetype=\"power_meter:pq\"`** events carrying **`pf`**, **`thd_pct`**, **`kw`**, **`kvar`**, and **`feed_id`**; capture props/transform notes in **`inputs.conf`** alongside any **`TRANSFORMS-`** stanza that normalizes harmonic counters.\n• Document SNMP community or SNMPv3 `authPriv` secrets outside Splunk—reference **`docs/implementation-guide.md`** for directory layout on heavy forwarders versus Cloud Collect.\n\nStep 1 — Configure data collection\n• Poll main switchboards and PDU incomers at **`300` seconds** or faster during commissioning, relaxing toward **`900` seconds** when harmonic drift stabilizes.\n• Stamp **`feed_id`** consistently with electrical one-line labels (for example **`MSB-L3-A`** versus **`PDU-HPC-POD21`**).\n• When capacitor banks emit auxiliary SNMP traps or discrete registers, mirror those inputs under a sibling sourcetype before correlating bank switching noise with PF dips.\n\nStep 2 — Create the search and alert\nReuse the catalogue SPL unchanged—saved-search **`dispatch.earliest_time=-35m@m`** pairs cleanly with **`cron_schedule=*/15 * * * *`** so each fifteen-minute **`span=15m`** bucket completes before evaluation.\nPin **`relation`** alerts to utility-contract **`pf`** floors (often **`0.92`**) and harmonic ceilings aligned with **`thd_pct`** distortion budgets negotiated with the transmission operator.\n\nStep 3 — Validate\nCompare Splunk **`avg_pf`** for each **`feed_id`** against the handheld PQ analyzer or rack **`PDU`** dashboard during the same **`±15m`** window to confirm SNMP scaling factors.\nSpot-check **`kw`** versus **`kvar`** triangles so **`pf`** recomputes manually within published meter tolerance.\n\nStep 4 — Operationalize\nPublish a Dashboard Studio row combining **`splunk.line`** PF trends, **`splunk.singlevalue`** gauges for worst **`feed_id`**, and **`splunk.table`** listings sorted by harmonic severity.\nRoute Sev2 pages through webhook routing rules tied to electrical supervisor queues with **`alert.suppress.fields=feed_id`** set to **`30m`** minimum.\n\nStep 5 — Troubleshoot common failures\n• **Failure:** SNMP modular input timeouts yield **`missing pf`** extractions—inspect **`splunkd.log`** on the polling worker for **`SNMP`** handshake retries and widen **`timeout`** seconds before blaming meters.\n• **Failure:** **`role`** **`permission`** misconfiguration hides **`index=power`** during scheduled searches—grant `indexes_edit` viewers read-only clones instead of sharing admin passwords.\n• **Failure:** Firmware refreshes reorder MIB branches producing **`NULL thd_pct`**—reload transforms and replay baseline **`feed_id`** samples until Splunk matches vendor PQ screenshots.",
  "grandmaExplanation": "We watch the physical data center for power, cooling, environment, and physical access.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
