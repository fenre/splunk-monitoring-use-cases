{
  "$schema": "../../schemas/uc.schema.json",
  "id": "15.1.20",
  "title": "PDU Branch Circuit Alerts",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'PDU Branch Circuit Alerts' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_pdu_branch_circuit_alerts` (UC-15.1.20), SOC dashboard panel \"Operations — PDU Branch Circuit Alerts\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-15.1.20 positive (PDU, Branch, Circuit, Alerts): ingest synthetic events into `index=power` sourcetype='snmp:pdu:branch' where `count` grouped by `entity` satisfies `pct` > 80 for 'PDU Branch Circuit Alerts'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-15.1.20 negative (Circuit Alerts): ingest benign `power` / `snmp:pdu:branch` events where `entity` stays below `pct` > 80, or exclude the test entity via `ops_exception_register.csv`. Variant 8 confirms zero alert rows for 'PDU Branch Circuit Alerts'."
  },
  "dataSources": "`sourcetype=\"snmp:pdu:branch\"` (branch_id, branch_amps, branch_rated_amps) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Smart PDU branch monitoring — Universal Forwarder (Splunkbase 617)",
  "spl": "index=power sourcetype=\"snmp:pdu:branch\"\n| eval pct=round(branch_amps/branch_rated_amps*100,1)\n| where pct > 80 OR branch_status=\"alarm\"\n| table pdu_id, branch_id, branch_amps, branch_rated_amps, pct\n| sort -pct",
  "description": "Identifies UC-15.1.20 — PDU, Branch, Circuit, Alerts — using `index=power` sourcetype `snmp:pdu:branch`, grouped by `entity`. Outputs `count` each `the scheduled window`; alert when `pct` > 80. Primary focus: Circuit Alerts. Purpose-built for 'PDU Branch Circuit Alerts' in Operations workflows.",
  "value": "UC-15.1.20 accelerates insight into Circuit Alerts ('PDU Branch Circuit Alerts') by tracking `activity` on `snmp:pdu:branch` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Map branches to rack groups. Alert at 80% sustained. Correlate with planned equipment adds.",
  "visualization": "Bar chart (branch load %), Table (branches at risk).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `power` (UC-15.1.20)** — Planned work for 'PDU Branch Circuit Alerts' shifts `count` over `span=1h` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `snmp:pdu:branch`** — Scheduled automation elevates `pct` without crossing `pct` > 80 for UC-15.1.20. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `power` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `snmp:pdu:branch`** — First 48h baseline for UC-15.1.20 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "snmp"
  ],
  "equipmentModels": [
    "snmp_generic",
    "snmp_pdu"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Smart PDU branch monitoring.\n• Ensure the following data sources are available: `sourcetype=\"snmp:pdu:branch\"` (branch_id, branch_amps, branch_rated_amps).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMap branches to rack groups. Alert at 80% sustained. Correlate with planned equipment adds.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=power sourcetype=\"snmp:pdu:branch\"\n| eval pct=round(branch_amps/branch_rated_amps*100,1)\n| where pct > 80 OR branch_status=\"alarm\"\n| table pdu_id, branch_id, branch_amps, branch_rated_amps, pct\n| sort -pct\n```\n\nUnderstanding this SPL\n\n**PDU Branch Circuit Alerts** — Branch breakers trip before mains; per-branch monitoring catches overload before rack outage.\n\nDocumented **Data sources**: `sourcetype=\"snmp:pdu:branch\"` (branch_id, branch_amps, branch_rated_amps). **App/TA** (typical add-on context): Smart PDU branch monitoring. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: power; **sourcetype**: snmp:pdu:branch. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=power, sourcetype=\"snmp:pdu:branch\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **pct** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where pct > 80 OR branch_status=\"alarm\"` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **PDU Branch Circuit Alerts**): table pdu_id, branch_id, branch_amps, branch_rated_amps, pct\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nCompare with the PDU UI or SNMP poll for the same devices, readings, and time range.\n\n\nStep 4 — Operationalize\nStep 4 — Operationalize\nStep 4 — Operationalize\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Bar chart (branch load %), Table (branches at risk).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Smart PDU branch monitoring — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=snmp:pdu:branch into power. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`pct`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch the physical data center for power, cooling, environment, and physical access.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
