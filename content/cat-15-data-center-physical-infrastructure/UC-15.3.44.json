{
  "$schema": "../../schemas/uc.schema.json",
  "id": "15.3.44",
  "title": "PTZ Camera Preset Home and Auto-Flip Failure Monitoring",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault",
    "Physical Security"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not certify video chain-of-custody for legal evidence beyond log correlation, does not replace VMS-native health dashboards for storage or network throughput, and does not cover fixed-lens cameras lacking PTZ telemetry.",
  "evidence": "Splunk saved search or alert history for PTZ homing failures plus linked dashboard panels on `index=physical` showing per-`camera_id` `error_code` rollups exported as PDF or scheduled CSV to the physical security evidence share.",
  "controlTest": {
    "positiveScenario": "UC-15.3.44 positive (PTZ, Camera, Preset, Home, and): ingest synthetic events into `index=physical` sourcetype='cctv:ptz' where `count` grouped by `host`, `camera_id`, `error_code` satisfies `count` > 5 for 'PTZ Camera Preset Home and Auto-Flip Failure Monitoring'. Validate filter `preset OR Home OR timeout OR motor OR stall OR \"PTZ error\"`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-15.3.44 negative (Failure Monitoring): ingest benign `physical` / `cctv:ptz` events where `host`, `camera_id`, `error_code` stays below `count` > 5, or exclude the test entity via `ops_exception_register.csv`. Variant 3 confirms zero alert rows for 'PTZ Camera Preset Home and Auto-Flip Failure Monitoring'."
  },
  "dataSources": "Land Axis, Hanwha, Hikvision SDK diagnostics or Milestone Recording Server bridges into `index=physical` using **`sourcetype=cctv:ptz`** (and companion **`sourcetype=nvr:diagnostic`** lines where the VMS emits PTZ health separately) with Splunk-extracted fields **`preset`**, **`home`**, **`timeout`**, **`motor`**, and **`error_code`** tied to **`camera_id`**. Typical ingest uses syslog/TLS forwarded through **Splunk Connect for Syslog** ([Splunkbase app **4740**](https://splunkbase.splunk.com/app/4740)), Heavy Forwarder `inputs.conf` listeners, or **`POST /services/collector/event`** HEC JSON when SDK exporters emit structured payloads.",
  "app": "Axis, Hanwha, Hikvision PTZ SDK logs via syslog, or Milestone device logs",
  "spl": "index=physical sourcetype=cctv:ptz OR (sourcetype=nvr:diagnostic PTZ)\n| search preset OR Home OR timeout OR motor OR stall OR \"PTZ error\"\n| stats count by host, camera_id, error_code\n| where count > 5\n| sort - count",
  "description": "Surfaces when Failure Monitoring criteria fire for 'PTZ Camera Preset Home and Auto-Flip Failure Monitoring' (UC-15.3.44) in `index=physical` sourcetype `cctv:ptz`. Pipeline aggregates `count` by `host`, `camera_id`, `error_code` over `the scheduled window`. Alert logic enforces `count` > 5. SPL signature: filter `preset OR Home OR timeout OR motor OR stall OR \"PTZ error\"`. Eliminates manual hunting for UC-15.3.44 conditions.",
  "value": "UC-15.3.44 helps teams contain PTZ, Camera, Preset, Home, and scenarios and reduce operational risk and delayed incident response before incidents compound.",
  "implementation": "Tier diagnostics verbosity on noisy Axis/Hanwha/Hikvision bodies first; normalize vendor integers into **`error_code`** via transforms lookup tables Milestone administrators align with Device Pack release notes. Schedule the hourly aggregation saved search under the physical-security service account, alert when Splunk counts failures/hour stay above five for two rolling hours per camera, and optionally enrich outdoor domes with approved weather CSV lookups gated behind RBAC.",
  "visualization": "Primary `splunk.table` of latest hour: `camera_id`, `site`, `error_code`, failure count, linked drilldown to raw `cctv:ptz` events; companion `splunk.line` trending failures per camera; optional `splunk.singlevalue` KPI for cameras breaching threshold; geo context only when CMDB coordinates exist.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Connect for Syslog (Splunkbase 4740)",
      "url": "https://splunkbase.splunk.com/app/4740"
    },
    {
      "title": "Splunk Docs — Forwarding and receiving syslog",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/ForwardingReception"
    },
    {
      "title": "Axis Developer Documentation — VAPIX",
      "url": "https://developer.axis.com/"
    },
    {
      "title": "Milestone Developer — integrations and APIs",
      "url": "https://developer.milestonesys.com/"
    },
    {
      "title": "ONVIF — Open Network Video Interface Forum",
      "url": "https://www.onvif.org/"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `physical` (UC-15.3.44)** — Planned work for 'PTZ Camera Preset Home and Auto-Flip Failure Monitoring' shifts `count` over `span=1h` while `host`, `camera_id`, `error_code` remains in-scope. Applies filter `preset OR Home OR timeout OR motor OR stall OR \"PTZ error\"`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `cctv:ptz`** — Scheduled automation elevates `camera_id` without crossing `count` > 5 for UC-15.3.44. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`, `camera_id`, `error_code`** — Health checks in `physical` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `cctv:ptz`** — First 48h baseline for UC-15.3.44 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cctv",
    "syslog"
  ],
  "equipmentModels": [
    "Axis Q6075-LE",
    "Hanwha Vision XNP-6320",
    "Hikvision DS-2DE4425IW-AE",
    "Milestone XProtect Corporate Recording Server"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Approved syslog/TLS or HEC path from Milestone Recording Server, Axis Companion/Device Manager feeds, Hanwha diagnostic bridges, or Hikvision SDK exporters into Splunk Cloud or Splunk Enterprise—Splunk Connect for Syslog ([Splunkbase app 4740](https://splunkbase.splunk.com/app/4740)) on a hardened Heavy Forwarder is the usual pattern alongside documented **`inputs.conf`** UDP/TCP stanzas.\n• Require normalized events on **`index=physical`** with **`sourcetype=cctv:ptz`** plus optional **`sourcetype=nvr:diagnostic`** rows referencing the same **`camera_id`**; extracted tokens must include **`preset`**, **`home`**, **`timeout`**, **`motor`**, **`error_code`**, and **`host`** before promoting alerting.\n• RBAC: constrain dashboards and scheduled searches to a **`physical_security_analyst`** (or similarly scoped) role with search/filter limits on **`index=physical`** only; store integration secrets outside SPL literals.\n• CMMS linkage fields (`work_order_id`, `vendor_case`) simplify tuning once mechanical remediation loops mature.\n\nStep 1 — Configure collection\nForward verbose PTZ traces during calibration windows only; otherwise rely on Milestone audit bridges or Axis/Hanwha/Hikvision SDK heartbeat logs at vendor-documented intervals. For JSON-capable exporters, bind **`POST /services/collector/event`** HEC tokens with explicit sourcetype overrides so multi-line syslog fragmentation cannot split **`PTZ error`** strings.\n\nStep 2 — Build validation searches\nBefore enabling automation, run ad hoc searches over **`index=physical`** **`sourcetype=cctv:ptz`** with **`camera_id`** split-by counts inside a **`-24h`** window to capture baseline jitter when icing or lubrication cycles occur; repeat against **`sourcetype=nvr:diagnostic`** if Milestone duplicates alarms.\n\nStep 3 — Validate against consoles\nCross-check spikes against Milestone Smart Client playback timelines and Axis/Hanwha/Hikvision maintenance utilities so Splunk cardinality mirrors observed mechanical faults rather than benign UI retries.\n\nStep 4 — Operationalize\nPublish dashboard drilldowns (camera × **`error_code`**, hourly failure sparklines, geo overlays fed by CMDB latitude/longitude lookups) and route breaches into CMMS tickets referencing stored Splunk SID URLs as forensic breadcrumbs.",
  "grandmaExplanation": "We watch the physical data center for power, cooling, environment, and physical access.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
