<!-- AUTO-GENERATED from UC-15.3.25.json — DO NOT EDIT -->

---
id: "15.3.25"
title: "Camera Video Quality Score and Stream Health (Meraki MV)"
criticality: "medium"
splunkPillar: "Observability"
---

# UC-15.3.25 · Camera Video Quality Score and Stream Health (Meraki MV)

> **Criticality:** Medium &middot; **Difficulty:** Intermediate &middot; **Pillar:** Observability &middot; **Type:** Availability

*We watch the physical data center for power, cooling, environment, and physical access.*

---

## Description

Monitors video quality metrics to identify network or hardware issues affecting video feeds.

## Value

Monitors video quality metrics to identify network or hardware issues affecting video feeds.

## Implementation

The Splunk_TA_cisco_meraki TA does not expose a per-camera quality score. The most reliable proxy for cloud-archive stream health is the camera's uplink loss and latency, polled via `cisco_meraki_devices_uplinks_loss_and_latency`. Loss > 1% or latency > 100ms generally degrades cloud video continuity. For deeper quality metrics, configure webhook integration and subscribe to `Camera offline` and `Camera unable to upload to cloud` alerts.

## Detailed Implementation

### Prerequisites
- Install and configure the required add-on or app: `Cisco Meraki Add-on for Splunk` (Splunkbase 5580).
- Ensure the following data sources are available: `index=meraki sourcetype=meraki:devicesavailabilities productType="camera"` plus `index=meraki sourcetype=meraki:devicesuplinkslossandlatency` from `Splunk_TA_cisco_meraki` (Splunkbase 5580). The TA does not expose a per-camera `quality_score`; this UC infers stream health from camera availability + uplink loss/latency to the cloud archive..
- For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md

### Step 1 — Configure data collection
The Splunk_TA_cisco_meraki TA does not expose a per-camera quality score. The most reliable proxy for cloud-archive stream health is the camera's uplink loss and latency, polled via `cisco_meraki_devices_uplinks_loss_and_latency`. Loss > 1% or latency > 100ms generally degrades cloud video continuity. For deeper quality metrics, configure webhook integration and subscribe to `Camera offline` and `Camera unable to upload to cloud` alerts.

### Step 2 — Create the search and alert
Run the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):

```spl
index=meraki sourcetype="meraki:devicesuplinkslossandlatency"
| stats avg(lossPercent) as avg_loss_pct, avg(latencyMs) as avg_latency_ms, max(latencyMs) as peak_latency_ms by serial, "network.name"
| where avg_loss_pct > 1 OR avg_latency_ms > 100
| eval stream_health=case(avg_loss_pct > 5 OR avg_latency_ms > 250, "POOR", avg_loss_pct > 1 OR avg_latency_ms > 100, "FAIR", true(), "GOOD")
| sort - avg_loss_pct
```

#### Understanding this SPL

**Camera Video Quality Score and Stream Health (Meraki MV)** — Monitors video quality metrics to identify network or hardware issues affecting video feeds.

Documented **Data sources**: `index=meraki sourcetype=meraki:devicesavailabilities productType="camera"` plus `index=meraki sourcetype=meraki:devicesuplinkslossandlatency` from `Splunk_TA_cisco_meraki` (Splunkbase 5580). The TA does not expose a per-camera `quality_score`; this UC infers stream health from camera availability + uplink loss/latency to the cloud archive. **App/TA** (typical add-on context): `Cisco Meraki Add-on for Splunk` (Splunkbase 5580). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.

The first pipeline stage scopes events using **index**: meraki; **sourcetype**: meraki:devicesuplinkslossandlatency. That sourcetype matches what this use case lists under Data sources.

**Pipeline walkthrough**

- Scopes the data: index=meraki, sourcetype="meraki:devicesuplinkslossandlatency". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.
- `stats` rolls up events into metrics; results are split **by serial, "network.name"** so each row reflects one combination of those dimensions (useful for per-host, per-user, or per-entity comparisons for this use case).
- Filters the current rows with `where avg_loss_pct > 1 OR avg_latency_ms > 100` — typically the threshold or rule expression for this monitoring goal.
- `eval` defines or adjusts **stream_health** — often to normalize units, derive a ratio, or prepare for thresholds.
- Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.


### Step 3 — Validate
Confirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.

### Step 4 — Operationalize
Add the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Quality score gauge per camera; quality trend line; affected camera list.

## SPL

```spl
index=meraki sourcetype="meraki:devicesuplinkslossandlatency"
| stats avg(lossPercent) as avg_loss_pct, avg(latencyMs) as avg_latency_ms, max(latencyMs) as peak_latency_ms by serial, "network.name"
| where avg_loss_pct > 1 OR avg_latency_ms > 100
| eval stream_health=case(avg_loss_pct > 5 OR avg_latency_ms > 250, "POOR", avg_loss_pct > 1 OR avg_latency_ms > 100, "FAIR", true(), "GOOD")
| sort - avg_loss_pct
```

## Visualization

Quality score gauge per camera; quality trend line; affected camera list.

## Known False Positives

Sensor anomalies during cleaning, calibration, or planned environmental changes.

## References

- [Splunkbase app 5580](https://splunkbase.splunk.com/app/5580)
