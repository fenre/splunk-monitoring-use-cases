{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.1.48",
  "title": "EV Charging Station Availability and Utilization",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'EV Charging Station Availability and Utilization' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_ev_charging_station_availability_and_utilization` (UC-14.1.48), SOC dashboard panel \"Operations — EV Charging Station Availability and Utilization\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.1.48 positive (Charging, Station, Availability, and, Utilization): ingest synthetic events into `index=building` sourcetype='bms:ev_charging' where `avg_duration`, `total_energy_kwh`, `active_stations` grouped by `facility`, `_time` satisfies detection threshold for 'EV Charging Station Availability and Utilization'. Validate lookup `ev_station_inventory.csv`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.1.48 negative (and Utilization): ingest benign `building` / `bms:ev_charging` events where `facility`, `_time` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 4 confirms zero alert rows for 'EV Charging Station Availability and Utilization'."
  },
  "dataSources": "`sourcetype=bms:ev_charging` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "OCPP-compatible charging management system (ChargePoint, Blink, ABB), custom API input — Universal Forwarder (Splunkbase 617)",
  "spl": "index=building sourcetype=\"bms:ev_charging\"\n| eval session_duration_hrs=round(session_duration_min/60, 1), energy_kwh=round(energy_delivered_wh/1000, 1)\n| bin _time span=1d\n| stats count as sessions, avg(session_duration_hrs) as avg_duration, sum(energy_kwh) as total_energy_kwh, dc(station_id) as active_stations, count(eval(status=\"faulted\")) as faulted_count by facility, _time\n| lookup ev_station_inventory.csv facility OUTPUTNEW total_stations\n| eval utilization_pct=round(active_stations*100/total_stations, 1)\n| table _time, facility, sessions, avg_duration, total_energy_kwh, active_stations, total_stations, utilization_pct, faulted_count",
  "description": "Identifies risk tied to Charging, Station, Availability, and, Utilization through UC-14.1.48 on `index=building` sourcetype `bms:ev_charging`. Summarises `avg_duration`, `total_energy_kwh`, `active_stations` grouped by `facility`, `_time` across `1d` before applying configured threshold. Operators tune 'EV Charging Station Availability and Utilization' using UC-14.1.48 runbook steps.",
  "value": "Tracking `avg_duration`, `total_energy_kwh`, `active_stations` via UC-14.1.48 on `bms:ev_charging` feeds improves response to 'EV Charging Station Availability and Utilization' and reduces operational risk and delayed incident response.",
  "implementation": "Connect to charging management platform via OCPP or vendor API. Track session starts, energy delivered, and station faults. Monitor utilization rates. Alert on faulted stations. Feed energy data to demand management.",
  "visualization": "Station availability map; energy delivery trend; utilization by time of day; faulted station alerts; session duration histogram.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable (no CIM or Operational_Telemetry data model in this SPL).",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `building` (UC-14.1.48)** — Planned work for 'EV Charging Station Availability and Utilization' shifts `avg_duration`, `total_energy_kwh`, `active_stations` over `span=1d` while `facility`, `_time` remains in-scope. Applies lookup `ev_station_inventory.csv`. Cross-check CMDB CHG.\n\n2. **Batch `avg` on `bms:ev_charging`** — Scheduled automation elevates `_time` without crossing configured threshold for UC-14.1.48. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `facility`, `_time`** — Health checks in `building` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `bms:ev_charging`** — First 48h baseline for UC-14.1.48 on `facility` is unstable; join `ev_station_inventory.csv` prior to threshold.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `facility`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: OCPP-compatible charging management system (ChargePoint, Blink, ABB), custom API input.\n• Ensure the following data sources are available: `sourcetype=bms:ev_charging`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nConnect to charging management platform via OCPP or vendor API. Track session starts, energy delivered, and station faults. Monitor utilization rates. Alert on faulted stations. Feed energy data to demand management.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=building sourcetype=\"bms:ev_charging\"\n| eval session_duration_hrs=round(session_duration_min/60, 1), energy_kwh=round(energy_delivered_wh/1000, 1)\n| bin _time span=1d\n| stats count as sessions, avg(session_duration_hrs) as avg_duration, sum(energy_kwh) as total_energy_kwh, dc(station_id) as active_stations, count(eval(status=\"faulted\")) as faulted_count by facility, _time\n| lookup ev_station_inventory.csv facility OUTPUTNEW total_stations\n| eval utilization_pct=round(active_stations*100/total_stations, 1)\n| table _time, facility, sessions, avg_duration, total_energy_kwh, active_stations, total_stations, utilization_pct, faulted_count\n```\n\nUnderstanding this SPL\n\n**EV Charging Station Availability and Utilization** — EV charging infrastructure is rapidly growing in commercial buildings. Monitoring station availability, session duration, and energy delivery helps right-size charging capacity, identify broken stations, and plan electrical load management.\n\nDocumented **Data sources**: `sourcetype=bms:ev_charging`. **App/TA** (typical add-on context): OCPP-compatible charging management system (ChargePoint, Blink, ABB), custom API input. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: building; **sourcetype**: bms:ev_charging. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=building, sourcetype=\"bms:ev_charging\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **session_duration_hrs** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by facility, _time** so each row reflects one combination of those dimensions.\n• Enriches events using `lookup` (lookup definition + optional OUTPUT fields).\n• `eval` defines or adjusts **utilization_pct** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Pipeline stage (see **EV Charging Station Availability and Utilization**): table _time, facility, sessions, avg_duration, total_energy_kwh, active_stations, total_stations, utilization_pct, faulted_count\n\n\nStep 3 — Validate\nRun the search in a known window. Spot-check a few events against the BMS console (for example Niagara, Tridium, or Honeywell) and confirm zones, values, and alarms line up. Verify field names and that the right roles can search the index.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Station availability map; energy delivery trend; utilization by time of day; faulted station alerts; session duration histogram.",
  "grandmaExplanation": "We watch how your building systems behave. We help you know if something is off with who can charge and whether stations are working before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
