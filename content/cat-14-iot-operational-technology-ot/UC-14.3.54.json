{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.3.54",
  "title": "Multi-Zone Temperature Gradient Monitoring for Optimal Environment",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Multi-Zone Temperature Gradient Monitoring for Optimal Environment' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_multi_zone_temperature_gradient_monitoring_for_o` (UC-14.3.54), SOC dashboard panel \"Operations — Multi-Zone Temperature Gradient Monitoring for Optimal Environment\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=target_index` sourcetype='target_sourcetype' that satisfy the UC-14.3.54 SPL filters for 'Multi-Zone Temperature Gradient Monitoring for Optimal Environment'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `target_index` / `target_sourcetype` events outside the detection threshold, or exclude the test entity via `operational_exceptions.csv` with a valid window. Confirm zero alert rows for UC-14.3.54."
  },
  "dataSources": "`index=edge-hub-data` metric_name=temperature, `sourcetype=edge_hub` zone=* Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Edge Hub (multiple temperature sensors via MQTT or external probes), Splunk OT Intelligence — Universal Forwarder (Splunkbase 617)",
  "spl": "| mstats avg(temperature) as zone_temp by zone, _time span=5m\n| stats avg(zone_temp) as avg_zone_temp by zone\n| eventstats avg(avg_zone_temp) as facility_avg_temp\n| eval temp_offset=(avg_zone_temp - facility_avg_temp)\n| stats max(abs(temp_offset)) as max_gradient by zone\n| where max_gradient > 3\n| eval gradient_alert=\"HVAC_IMBALANCE\"",
  "description": "Monitors temperature gradients across facility zones to optimize HVAC distribution and detect unequal cooling/heating.",
  "value": "Early 'Multi-Zone Temperature Gradient Monitoring for Optimal Environment' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Deploy Edge Hub in central location with MQTT subscriptions to external temperature sensors in multiple zones (Advanced Settings → MQTT Subscriptions). Or use external probes connected to I²C port (3.5mm jack). Configure 5-minute polling to capture HVAC response dynamics. Acceptable temperature gradient: ±1-2°C across zones. Gradient > 3°C indicates HVAC distribution issue (blocked duct, stuck valve). Store zone temperatures in edge-hub-data metrics. Implement trend analysis: if gradient increasing over days = duct blockage. If gradient constant but offset = thermostat miscalibration. Alert HVAC maintenance if gradient exceeds threshold.",
  "visualization": "Zone temperature heatmap, gradient trend, HVAC balance status.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable for CIM; ensure metrics index access and mstats permissions for the indexes in the search.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'Multi-Zone Temperature Gradient Monitoring for Optimal Environment'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `target index` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `operational_exceptions.csv` with entity, reason, valid_until.",
  "equipment": [
    "edge_hub",
    "m365",
    "mqtt"
  ],
  "equipmentModels": [
    "edge_hub_generic",
    "m365_generic",
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk Edge Hub",
    "Splunk OT Intelligence"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Edge Hub (multiple temperature sensors via MQTT or external probes), Splunk OT Intelligence.\n• Ensure the following data sources are available: `index=edge-hub-data` metric_name=temperature, `sourcetype=edge_hub` zone=*.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nDeploy Edge Hub in central location with MQTT subscriptions to external temperature sensors in multiple zones (Advanced Settings → MQTT Subscriptions). Or use external probes connected to I²C port (3.5mm jack). Configure 5-minute polling to capture HVAC response dynamics. Acceptable temperature gradient: ±1-2°C across zones. Gradient > 3°C indicates HVAC distribution issue (blocked duct, stuck valve). Store zone temperatures in edge-hub-data metrics. Implement trend analysis: if gradient increas…\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\n| mstats avg(temperature) as zone_temp by zone, _time span=5m\n| stats avg(zone_temp) as avg_zone_temp by zone\n| eventstats avg(avg_zone_temp) as facility_avg_temp\n| eval temp_offset=(avg_zone_temp - facility_avg_temp)\n| stats max(abs(temp_offset)) as max_gradient by zone\n| where max_gradient > 3\n| eval gradient_alert=\"HVAC_IMBALANCE\"\n```\n\nUnderstanding this SPL\n\n**Multi-Zone Temperature Gradient Monitoring for Optimal Environment** — Monitors temperature gradients across facility zones to optimize HVAC distribution and detect unequal cooling/heating.\n\nDocumented **Data sources**: `index=edge-hub-data` metric_name=temperature, `sourcetype=edge_hub` zone=*. **App/TA** (typical add-on context): Splunk Edge Hub (multiple temperature sensors via MQTT or external probes), Splunk OT Intelligence. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\n**Pipeline walkthrough**\n\n• Uses `mstats` to query metrics indexes (pre-aggregated metric data).\n• `stats` rolls up events into metrics; results are split **by zone** so each row reflects one combination of those dimensions.\n• `eventstats` aggregates the pipeline (counts, distinct values, sums, percentiles, etc.) into fewer rows.\n• `eval` defines or adjusts **temp_offset** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by zone** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where max_gradient > 3` — typically the threshold or rule expression for this monitoring goal.\n• `eval` defines or adjusts **gradient_alert** — often to normalize units, derive a ratio, or prepare for thresholds.\n\n\nNote: for mstats, confirm the metrics indexes in the search exist and your role can run mstats. This is separate from CIM data model acceleration.\n\nStep 3 — Validate\nConfirm metric or event volume in the time range, then compare trends and device status to the Splunk Edge Hub admin UI or the mobile app for the same devices. Verify metric names, hosts, and time skew.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Zone temperature heatmap, gradient trend, HVAC balance status.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Edge Hub (multiple temperature sensors via MQTT or external probes), Splunk OT Intelligence — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into index=main. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, max_gradient, zone`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch what edge sensors report from the physical world. We help you know if something is off with uncomfortable or unsafe hot and cold spots before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
