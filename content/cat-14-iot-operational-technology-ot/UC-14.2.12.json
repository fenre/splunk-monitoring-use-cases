{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.2.12",
  "title": "Historian Data Integrity",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Historian Data Integrity' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_historian_data_integrity` (UC-14.2.12), SOC dashboard panel \"Operations — Historian Data Integrity\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.2.12 positive (Historian, Data, Integrity): ingest synthetic events into `index=ot` sourcetype='historian:point' where `count` grouped by `tag_name`, `gap_sec` satisfies `gap_sec` > 300 for 'Historian Data Integrity'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.2.12 negative (Data Integrity): ingest benign `ot` / `historian:point` events where `tag_name`, `gap_sec` stays below `gap_sec` > 300, or exclude the test entity via `ops_exception_register.csv`. Variant 11 confirms zero alert rows for 'Historian Data Integrity'."
  },
  "dataSources": "`sourcetype=\"historian:point\"` (value, quality, ts) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "PI / OPC-UA historian export — Universal Forwarder (Splunkbase 617)",
  "spl": "index=ot sourcetype=\"historian:point\"\n| sort 0 + _time\n| streamstats window=2 global=prev\n| eval gap_sec=_time-prev_ts\n| where gap_sec > 300 OR quality!=\"Good\"\n| stats count by tag_name, gap_sec",
  "description": "Alerts on UC-14.2.12 — Historian, Data, Integrity — using `index=ot` sourcetype `historian:point`, grouped by `tag_name`, `gap_sec`. Outputs `count` each `the scheduled window`; alert when `gap_sec` > 300. Primary focus: Data Integrity. Purpose-built for 'Historian Data Integrity' in Operations workflows.",
  "value": "UC-14.2.12 helps teams contain Historian, Data, Integrity scenarios and reduce operational risk and delayed incident response before incidents compound.",
  "implementation": "Ingest quality codes and source timestamps. Alert on gap > SLA or bad quality >10% of samples per tag group.",
  "visualization": "Line chart (insert rate), Table (tags with gaps), Single value (data quality %).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable for CIM; ensure metrics index access and mstats permissions for the indexes in the search.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `ot` (UC-14.2.12)** — Planned work for 'Historian Data Integrity' shifts `count` over `span=1h` while `tag_name`, `gap_sec` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `historian:point`** — Scheduled automation elevates `gap_sec` without crossing `gap_sec` > 300 for UC-14.2.12. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `tag_name`, `gap_sec`** — Health checks in `ot` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `historian:point`** — First 48h baseline for UC-14.2.12 on `tag_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `tag_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "opcua"
  ],
  "equipmentModels": [
    "opcua_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: PI / OPC-UA historian export.\n• Ensure the following data sources are available: `sourcetype=\"historian:point\"` (value, quality, ts).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest quality codes and source timestamps. Alert on gap > SLA or bad quality >10% of samples per tag group.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot sourcetype=\"historian:point\"\n| sort 0 + _time\n| streamstats window=2 global=prev\n| eval gap_sec=_time-prev_ts\n| where gap_sec > 300 OR quality!=\"Good\"\n| stats count by tag_name, gap_sec\n```\n\nUnderstanding this SPL\n\n**Historian Data Integrity** — Gaps or duplicated timestamps in historian feeds break batch quality and regulatory reporting.\n\nDocumented **Data sources**: `sourcetype=\"historian:point\"` (value, quality, ts). **App/TA** (typical add-on context): PI / OPC-UA historian export. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot; **sourcetype**: historian:point. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot, sourcetype=\"historian:point\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n• `streamstats` aggregates the pipeline (counts, distinct values, sums, percentiles, etc.) into fewer rows.\n• `eval` defines or adjusts **gap_sec** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where gap_sec > 300 OR quality!=\"Good\"` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by tag_name, gap_sec** so each row reflects one combination of those dimensions.\n\n\nNote: for mstats, confirm the metrics indexes in the search exist and your role can run mstats. This is separate from CIM data model acceleration.\n\nStep 3 — Validate\nRun the search when the process is in a known state. Compare key fields and alarm states to the SCADA or DCS HMI and, where relevant, the engineering tool for the PLC. Confirm sourcetypes and field extractions match the vendor feed.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (insert rate), Table (tags with gaps), Single value (data quality %).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (PI / OPC-UA historian export — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=historian:point into ot. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`gap_sec, tag_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch your plant control systems and field data. We help you know if something is off with the history of process values the plant trusts before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
