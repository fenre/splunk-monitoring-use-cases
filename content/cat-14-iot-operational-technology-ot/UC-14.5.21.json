{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.5.21",
  "title": "MQTT Topic Tree Depth and Fan-Out Analysis",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'MQTT Topic Tree Depth and Fan-Out Analysis' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_mqtt_topic_tree_depth_and_fan_out_analysis` (UC-14.5.21), SOC dashboard panel \"Operations — MQTT Topic Tree Depth and Fan-Out Analysis\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=ot` sourcetype='mqtt:message' that satisfy the UC-14.5.21 SPL filters for 'MQTT Topic Tree Depth and Fan-Out Analysis'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `ot` / `mqtt:message` events outside the detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-14.5.21."
  },
  "dataSources": "`index=ot` `sourcetype=\"mqtt:message\"` fields `topic` Indexes: ot. Sourcetypes: mqtt:message. Key fields: host, root. Validate field extraction in Search before saving the alert; compare event counts to the vendor admin console.",
  "app": "MQTT Modular Input (Splunkbase 1890), HiveMQ Splunk Extension",
  "spl": "index=ot sourcetype=\"mqtt:message\"\n| eval depth=mvcount(split(topic,\"/\"))\n| eval parts=split(topic,\"/\")\n| eval root=mvindex(parts,0)\n| stats count as msgs, dc(topic) as unique_topics, max(depth) as max_depth, avg(depth) as avg_depth by root, host\n| sort - unique_topics",
  "description": "Deep topic hierarchies and high fan-out increase broker memory and ACL evaluation costs; trending complexity helps right-size clusters and topic design reviews.",
  "value": "Early 'MQTT Topic Tree Depth and Fan-Out Analysis' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "For very high message rates, sample or pre-aggregate in HiveMQ metrics. Exclude test topics. Pair with ACL audit if unauthorized deep topics appear.",
  "visualization": "Bar chart (unique topics by root prefix), Histogram (depth distribution), Table (top fan-out roots).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "MQTT Modular Input",
      "url": "https://splunkbase.splunk.com/app/1890",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'MQTT Topic Tree Depth and Fan-Out Analysis'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `ot` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `ops_exception_register.csv` with entity, reason, valid_until.",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: MQTT Modular Input (Splunkbase 1890), HiveMQ Splunk Extension.\n• Ensure the following data sources are available: `index=ot` `sourcetype=\"mqtt:message\"` fields `topic`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nFor very high message rates, sample or pre-aggregate in HiveMQ metrics. Exclude test topics. Pair with ACL audit if unauthorized deep topics appear.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot sourcetype=\"mqtt:message\"\n| eval depth=mvcount(split(topic,\"/\"))\n| eval parts=split(topic,\"/\")\n| eval root=mvindex(parts,0)\n| stats count as msgs, dc(topic) as unique_topics, max(depth) as max_depth, avg(depth) as avg_depth by root, host\n| sort - unique_topics\n```\n\nUnderstanding this SPL\n\n**MQTT Topic Tree Depth and Fan-Out Analysis** — Deep topic hierarchies and high fan-out increase broker memory and ACL evaluation costs; trending complexity helps right-size clusters and topic design reviews.\n\nDocumented **Data sources**: `index=ot` `sourcetype=\"mqtt:message\"` fields `topic`. **App/TA** (typical add-on context): MQTT Modular Input (Splunkbase 1890), HiveMQ Splunk Extension. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot; **sourcetype**: mqtt:message. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot, sourcetype=\"mqtt:message\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **depth** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **parts** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **root** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by root, host** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nStep 3 — Validate\nRun the search over a period with known traffic, then compare with the MQTT broker UI or `mosquitto_sub` for the same topics and clients. Spot-check counts against broker access and metrics logs.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Bar chart (unique topics by root prefix), Histogram (depth distribution), Table (top fan-out roots).",
  "grandmaExplanation": "We size topic tree depth and fan-out so a messy namespace or runaway auto-topics do not get impossible to run.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
