{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.5.14",
  "title": "MQTT TLS Handshake and Cipher Compliance",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'MQTT TLS Handshake and Cipher Compliance' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_mqtt_tls_handshake_and_cipher_compliance` (UC-14.5.14), SOC dashboard panel \"Operations — MQTT TLS Handshake and Cipher Compliance\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.5.14 positive (MQTT, TLS, Handshake, and, Cipher): ingest synthetic events into `index=ot` sourcetype='mqtt_tls_log' where `count` grouped by `cipher`, `protocol`, `reason`, `modern_tls` satisfies detection threshold for 'MQTT TLS Handshake and Cipher Compliance'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.5.14 negative (Cipher Compliance): ingest benign `ot` / `mqtt_tls_log` events where `cipher`, `protocol`, `reason`, `modern_tls` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 11 confirms zero alert rows for 'MQTT TLS Handshake and Cipher Compliance'."
  },
  "dataSources": "Broker or proxy TLS logs (handshake, cipher) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "MQTT broker (TLS log), reverse proxy logs — Universal Forwarder (Splunkbase 617)",
  "spl": "index=ot sourcetype=mqtt_tls_log (\"handshake failed\" OR \"certificate verify\" OR \"TLS\")\n| rex \"cipher=(?<cipher>\\S+)|protocol=(?<protocol>\\S+)\"\n| eval modern_tls=if(protocol IN (\"TLSv1.2\",\"TLSv1.3\"), \"yes\", \"no\"),\n       strong_cipher=if(match(cipher, \"^(TLS_AES_|TLS_CHACHA20_|ECDHE-.*-GCM|ECDHE-.*-CHACHA20)\"), \"yes\", \"no\")\n| stats count by cipher, protocol, reason, modern_tls, strong_cipher\n| where modern_tls!=\"yes\" OR strong_cipher!=\"yes\" OR reason=\"failed\"\n| table cipher protocol reason count",
  "description": "Alerts on UC-14.5.14 — MQTT, TLS, Handshake, and, Cipher — using `index=ot` sourcetype `mqtt_tls_log`, grouped by `cipher`, `protocol`, `reason`, `modern_tls`. Outputs `count` each `the scheduled window`; alert when configured threshold. Primary focus: Cipher Compliance. Purpose-built for 'MQTT TLS Handshake and Cipher Compliance' in Operations workflows.",
  "value": "UC-14.5.14 helps teams contain MQTT, TLS, Handshake, and, Cipher scenarios and reduce operational risk and delayed incident response before incidents compound.",
  "implementation": "Enable TLS logging on MQTT broker or proxy. Ingest handshake success/failure and negotiated cipher/protocol. Alert on handshake failure or use of non-approved ciphers (e.g. block TLS 1.0/1.1 and weak ciphers).",
  "visualization": "Table (cipher, protocol, failures), Timeline (handshake events), Single value (TLS failures).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `ot` (UC-14.5.14)** — Planned work for 'MQTT TLS Handshake and Cipher Compliance' shifts `count` over `span=1h` while `cipher`, `protocol`, `reason`, `modern_tls` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `mqtt_tls_log`** — Scheduled automation elevates `protocol` without crossing configured threshold for UC-14.5.14. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `cipher`, `protocol`, `reason`, `modern_tls`** — Health checks in `ot` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `mqtt_tls_log`** — First 48h baseline for UC-14.5.14 on `cipher` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `cipher`, `exception_reason`, `valid_until`.",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: MQTT broker (TLS log), reverse proxy logs.\n• Ensure the following data sources are available: Broker or proxy TLS logs (handshake, cipher).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnable TLS logging on MQTT broker or proxy. Ingest handshake success/failure and negotiated cipher/protocol. Alert on handshake failure or use of non-approved ciphers (e.g. block TLS 1.0/1.1 and weak ciphers).\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot sourcetype=mqtt_tls_log (\"handshake failed\" OR \"certificate verify\" OR \"TLS\")\n| rex \"cipher=(?<cipher>\\S+)|protocol=(?<protocol>\\S+)\"\n| eval modern_tls=if(protocol IN (\"TLSv1.2\",\"TLSv1.3\"), \"yes\", \"no\"),\n       strong_cipher=if(match(cipher, \"^(TLS_AES_|TLS_CHACHA20_|ECDHE-.*-GCM|ECDHE-.*-CHACHA20)\"), \"yes\", \"no\")\n| stats count by cipher, protocol, reason, modern_tls, strong_cipher\n| where modern_tls!=\"yes\" OR strong_cipher!=\"yes\" OR reason=\"failed\"\n| table cipher protocol reason count\n```\n\nUnderstanding this SPL\n\n**MQTT TLS Handshake and Cipher Compliance** — TLS handshake failures or weak ciphers indicate misconfiguration or downgrade attacks. Monitoring ensures encrypted MQTT and policy compliance.\n\nDocumented **Data sources**: Broker or proxy TLS logs (handshake, cipher). **App/TA** (typical add-on context): MQTT broker (TLS log), reverse proxy logs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot; **sourcetype**: mqtt_tls_log. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot, sourcetype=mqtt_tls_log. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Extracts fields with `rex` (regular expression).\n• `eval` defines or adjusts **modern_tls** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by cipher, protocol, reason, modern_tls, strong_cipher** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where modern_tls!=\"yes\" OR strong_cipher!=\"yes\" OR reason=\"failed\"` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **MQTT TLS Handshake and Cipher Compliance**): table cipher protocol reason count\n\nStep 3 — Validate\nRun the search over a period with known traffic, then compare with the MQTT broker UI or `mosquitto_sub` for the same topics and clients. Spot-check counts against broker access and metrics logs.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (cipher, protocol, failures), Timeline (handshake events), Single value (TLS failures).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (MQTT broker (TLS log), reverse proxy logs — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=mqtt_tls_log into ot. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`cipher, modern_tls, protocol, reason`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We track secure handshakes and ciphers on the broker so weak or wrong setups do not pass unnoticed.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
