{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.1.24",
  "title": "Sensor Connectivity and Heartbeat Monitoring (Meraki MT)",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Sensor Connectivity and Heartbeat Monitoring (Meraki MT)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_sensor_connectivity_and_heartbeat_monitoring_mer` (UC-14.1.24), SOC dashboard panel \"Operations — Sensor Connectivity and Heartbeat Monitoring (Meraki MT)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.1.24 positive (Sensor, Connectivity, and, Heartbeat, Monitoring): ingest synthetic events into `index=meraki` sourcetype='meraki:sensorreadingshistory' where `count` grouped by `serial`, `\"network.name\"` satisfies `hours_since_reading` > 2 for 'Sensor Connectivity and Heartbeat Monitoring (Meraki MT)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.1.24 negative ((Meraki MT)): ingest benign `meraki` / `meraki:sensorreadingshistory` events where `serial`, `\"network.name\"` stays below `hours_since_reading` > 2, or exclude the test entity via `ops_exception_register.csv`. Variant 7 confirms zero alert rows for 'Sensor Connectivity and Heartbeat Monitoring (Meraki MT)'."
  },
  "dataSources": "`index=meraki sourcetype=meraki:sensorreadingshistory` from the `cisco_meraki_sensor_readings_history` input plus `index=meraki sourcetype=meraki:devicesavailabilities` from `cisco_meraki_devices_availabilities` in `Splunk_TA_cisco_meraki` (Splunkbase 5580). Used to detect sensors that have stopped reporting.",
  "app": "`Cisco Meraki Add-on for Splunk` (Splunkbase 5580)",
  "spl": "index=meraki sourcetype=\"meraki:sensorreadingshistory\"\n| stats latest(_time) as last_reading by serial, \"network.name\"\n| eval hours_since_reading=round((now()-last_reading)/3600, 1)\n| where hours_since_reading > 2\n| eval last_reading_human=strftime(last_reading, \"%Y-%m-%d %H:%M:%S\")\n| sort - hours_since_reading",
  "description": "Monitors UC-14.1.24 — Sensor, Connectivity, and, Heartbeat, Monitoring — using `index=meraki` sourcetype `meraki:sensorreadingshistory`, grouped by `serial`, `\"network.name\"`. Outputs `count` each `the scheduled window`; alert when `hours_since_reading` > 2. Primary focus: (Meraki MT). Purpose-built for 'Sensor Connectivity and Heartbeat Monitoring (Meraki MT)' in Operations workflows.",
  "value": "UC-14.1.24 operationalises 'Sensor Connectivity and Heartbeat Monitoring (Meraki MT)' against `meraki:sensorreadingshistory` feeds so operators reduce operational risk and delayed incident response without manual correlation.",
  "implementation": "Track when each MT sensor last reported. Sensors transmit periodically (every few minutes for live metrics, hourly for slow-change ones); >2 hours without any reading typically means the sensor has disconnected, lost LoRa coverage, or the gateway (MR/MV) is offline. Cross-reference with `meraki:devicesavailabilities` for the parent gateway state.",
  "visualization": "Sensor status table; last heartbeat timeline; offline sensor list.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable (no CIM or Operational_Telemetry data model in this SPL).",
  "references": [
    {
      "title": "Splunkbase app 5580",
      "url": "https://splunkbase.splunk.com/app/5580",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Add-on for Cisco Meraki (Splunkbase 5631)",
      "url": "https://splunkbase.splunk.com/app/5631",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Cisco Security documentation",
      "url": "https://www.cisco.com/c/en/us/support/security/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `meraki` (UC-14.1.24)** — Planned work for 'Sensor Connectivity and Heartbeat Monitoring (Meraki MT)' shifts `count` over `span=1h` while `serial`, `\"network.name\"` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `meraki:sensorreadingshistory`** — Scheduled automation elevates `\"network.name\"` without crossing `hours_since_reading` > 2 for UC-14.1.24. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `serial`, `\"network.name\"`** — Health checks in `meraki` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `meraki:sensorreadingshistory`** — First 48h baseline for UC-14.1.24 on `serial` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `serial`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_meraki"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Cisco Meraki Add-on for Splunk` (Splunkbase 5580).\n• Ensure the following data sources are available: `index=meraki sourcetype=meraki:sensorreadingshistory` from the `cisco_meraki_sensor_readings_history` input plus `index=meraki sourcetype=meraki:devicesavailabilities` from `cisco_meraki_devices_availabilities` in `Splunk_TA_cisco_meraki` (Splunkbase 5580). Used to detect sensors that have stopped reporting..\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nTrack when each MT sensor last reported. Sensors transmit periodically (every few minutes for live metrics, hourly for slow-change ones); >2 hours without any reading typically means the sensor has disconnected, lost LoRa coverage, or the gateway (MR/MV) is offline. Cross-reference with `meraki:devicesavailabilities` for the parent gateway state.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=meraki sourcetype=\"meraki:sensorreadingshistory\"\n| stats latest(_time) as last_reading by serial, \"network.name\"\n| eval hours_since_reading=round((now()-last_reading)/3600, 1)\n| where hours_since_reading > 2\n| eval last_reading_human=strftime(last_reading, \"%Y-%m-%d %H:%M:%S\")\n| sort - hours_since_reading\n```\n\nUnderstanding this SPL\n\n**Sensor Connectivity and Heartbeat Monitoring (Meraki MT)** — Ensures all sensors maintain connectivity and operational status.\n\nDocumented **Data sources**: `index=meraki sourcetype=meraki:sensorreadingshistory` from the `cisco_meraki_sensor_readings_history` input plus `index=meraki sourcetype=meraki:devicesavailabilities` from `cisco_meraki_devices_availabilities` in `Splunk_TA_cisco_meraki` (Splunkbase 5580). Used to detect sensors that have stopped reporting. **App/TA** (typical add-on context): `Cisco Meraki Add-on for Splunk` (Splunkbase 5580). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: meraki; **sourcetype**: meraki:sensorreadingshistory. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=meraki, sourcetype=\"meraki:sensorreadingshistory\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by serial, \"network.name\"** so each row reflects one combination of those dimensions (useful for per-host, per-user, or per-entity comparisons for this use case).\n• `eval` defines or adjusts **hours_since_reading** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where hours_since_reading > 2` — typically the threshold or rule expression for this monitoring goal.\n• `eval` defines or adjusts **last_reading_human** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Sensor status table; last heartbeat timeline; offline sensor list.",
  "grandmaExplanation": "We watch how your building systems behave. We help you know if something is off with this area: sensor connectivity and heartbeat monitoring before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
