{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.4.3",
  "title": "Asset Tracking",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Asset Tracking' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_asset_tracking` (UC-14.4.3), SOC dashboard panel \"Operations — Asset Tracking\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.4.3 positive (Asset, Tracking): ingest synthetic events into `index=iot` sourcetype='asset_tracking' where `count` grouped by `asset_id`, `asset_type` satisfies `hours_since` > 24 for 'Asset Tracking'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.4.3 negative (Asset Tracking): ingest benign `iot` / `asset_tracking` events where `asset_id`, `asset_type` stays below `hours_since` > 24, or exclude the test entity via `ops_exception_register.csv`. Variant 13 confirms zero alert rows for 'Asset Tracking'."
  },
  "dataSources": "GPS/BLE beacon data, RFID events Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom API input, BLE/GPS data — Universal Forwarder (Splunkbase 617)",
  "spl": "index=iot sourcetype=\"asset_tracking\"\n| stats latest(location) as current_location, latest(_time) as last_seen by asset_id, asset_type\n| eval hours_since=round((now()-last_seen)/3600,1)\n| where hours_since > 24",
  "description": "Identifies UC-14.4.3 — Asset, Tracking — using `index=iot` sourcetype `asset_tracking`, grouped by `asset_id`, `asset_type`. Outputs `count` each `the scheduled window`; alert when `hours_since` > 24. Primary focus: Asset Tracking. Purpose-built for 'Asset Tracking' in Operations workflows.",
  "value": "UC-14.4.3 gives Operations teams earlier visibility into Asset Tracking through `asset_tracking` feeds, lowering operational risk and delayed incident response.",
  "implementation": "Ingest asset tracking data from GPS/BLE/RFID systems. Track asset locations and movement patterns. Alert when high-value assets leave designated zones. Report on asset utilization by location.",
  "visualization": "Map (asset locations), Table (asset inventory with location), Timeline (asset movement), Single value (assets not reporting).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable (no CIM or Operational_Telemetry data model in this SPL).",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `iot` (UC-14.4.3)** — Planned work for 'Asset Tracking' shifts `count` over `span=1h` while `asset_id`, `asset_type` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `asset_tracking`** — Scheduled automation elevates `asset_type` without crossing `hours_since` > 24 for UC-14.4.3. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `asset_id`, `asset_type`** — Health checks in `iot` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `asset_tracking`** — First 48h baseline for UC-14.4.3 on `asset_id` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `asset_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Custom API input, BLE/GPS data.\n• Ensure the following data sources are available: GPS/BLE beacon data, RFID events.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest asset tracking data from GPS/BLE/RFID systems. Track asset locations and movement patterns. Alert when high-value assets leave designated zones. Report on asset utilization by location.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=iot sourcetype=\"asset_tracking\"\n| stats latest(location) as current_location, latest(_time) as last_seen by asset_id, asset_type\n| eval hours_since=round((now()-last_seen)/3600,1)\n| where hours_since > 24\n```\n\nUnderstanding this SPL\n\n**Asset Tracking** — Real-time asset location reduces search time, prevents loss, and enables utilization optimization.\n\nDocumented **Data sources**: GPS/BLE beacon data, RFID events. **App/TA** (typical add-on context): Custom API input, BLE/GPS data. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: iot; **sourcetype**: asset_tracking. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=iot, sourcetype=\"asset_tracking\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by asset_id, asset_type** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **hours_since** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where hours_since > 24` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nCompare device counts, last-seen times, and health fields to the IoT platform console (for example AWS IoT, Azure IoT Hub, or Google Cloud IoT) for the same period. Verify API polling intervals and that offline rules match your operational definition.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Map (asset locations), Table (asset inventory with location), Timeline (asset movement), Single value (assets not reporting).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Custom API input, BLE/GPS data — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=asset_tracking into iot. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`asset_id, asset_type, hours_since`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch your connected devices and the messages they send. We help you know if something is off with this area: asset before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
