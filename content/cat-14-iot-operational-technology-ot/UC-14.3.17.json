{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.3.17",
  "title": "Equipment Alignment & Vibration Analysis via Gyroscope",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Equipment Alignment & Vibration Analysis via Gyroscope' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_equipment_alignment_vibration_analysis_via_gyros` (UC-14.3.17), SOC dashboard panel \"Operations — Equipment Alignment & Vibration Analysis via Gyroscope\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.3.17 positive (Equipment, Alignment, Vibration, Analysis, via): ingest synthetic events into `index=target_index` sourcetype='target_sourcetype' where `gx`, `gy`, `gz` grouped by `host`, `equipment_id`, `_time` satisfies `avg_rot` > 50, or `std_rot` > 10 for 'Equipment Alignment & Vibration Analysis via Gyroscope'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.3.17 negative (via Gyroscope): ingest benign `target_index` / `target_sourcetype` events where `host`, `equipment_id`, `_time` stays below `avg_rot` > 50, or `std_rot` > 10, or exclude the test entity via `ops_exception_register.csv`. Variant 8 confirms zero alert rows for 'Equipment Alignment & Vibration Analysis via Gyroscope'."
  },
  "dataSources": "`index=edge-hub-data` metric_name=gyro_x OR gyro_y OR gyro_z, `sourcetype=edge_hub` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Edge Hub (6-axis gyroscope), Splunk OT Intelligence — Universal Forwarder (Splunkbase 617)",
  "spl": "| mstats avg(gyro_x) as gx, avg(gyro_y) as gy, avg(gyro_z) as gz by host, equipment_id, _time span=1m\n| eval rotation_magnitude=sqrt((gx^2 + gy^2 + gz^2))\n| eval z_axis_dominant=if(abs(gz) > abs(gx) AND abs(gz) > abs(gy), \"YES\", \"NO\")\n| stats avg(rotation_magnitude) as avg_rot, stdev(rotation_magnitude) as std_rot by equipment_id\n| where (avg_rot > 50) AND (std_rot > 10)",
  "description": "Monitors UC-14.3.17 — Equipment, Alignment, Vibration, Analysis, via — using `index=target_index` sourcetype `configured sourcetype`, grouped by `host`, `equipment_id`, `_time`. Outputs `gx`, `gy`, `gz` each `1m`; alert when `avg_rot` > 50, or `std_rot` > 10. Primary focus: via Gyroscope. Purpose-built for 'Equipment Alignment & Vibration Analysis via Gyroscope' in Operations workflows.",
  "value": "UC-14.3.17 accelerates insight into via Gyroscope ('Equipment Alignment & Vibration Analysis via Gyroscope') by tracking `gx`, `gy`, `gz` on UC-14.3.17 search results, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Mount Edge Hub at equipment bearing or motor coupling with gyroscope Z-axis aligned to equipment rotation axis. Collect 30-day baseline for expected rotation rate and variation. Use built-in kNN anomaly detection to flag unexpected rotational patterns (e.g., gyroscopic precession from misalignment). For precision industrial environments, integrate with OPC-UA PLC (port 4840) to read encoder data for ground-truth validation. Local 3M backlog ensures all rotation events are captured.",
  "visualization": "Rotation rate trend, z-axis dominance heatmap, misalignment risk gauge.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable for CIM; ensure metrics index access and mstats permissions for the indexes in the search.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `target_index` (UC-14.3.17)** — Planned work for 'Equipment Alignment & Vibration Analysis via Gyroscope' shifts `gx`, `gy`, `gz` over `span=1m` while `host`, `equipment_id`, `_time` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `configured_sourcetype`** — Scheduled automation elevates `equipment_id` without crossing `avg_rot` > 50 for UC-14.3.17. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`, `equipment_id`, `_time`** — Health checks in `target_index` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-14.3.17 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "edge_hub",
    "opcua"
  ],
  "equipmentModels": [
    "edge_hub_generic",
    "opcua_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk Edge Hub",
    "Splunk OT Intelligence"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Edge Hub (6-axis gyroscope), Splunk OT Intelligence.\n• Ensure the following data sources are available: `index=edge-hub-data` metric_name=gyro_x OR gyro_y OR gyro_z, `sourcetype=edge_hub`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMount Edge Hub at equipment bearing or motor coupling with gyroscope Z-axis aligned to equipment rotation axis. Collect 30-day baseline for expected rotation rate and variation. Use built-in kNN anomaly detection to flag unexpected rotational patterns (e.g., gyroscopic precession from misalignment). For precision industrial environments, integrate with OPC-UA PLC (port 4840) to read encoder data for ground-truth validation. Local 3M backlog ensures all rotation events are captured.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\n| mstats avg(gyro_x) as gx, avg(gyro_y) as gy, avg(gyro_z) as gz by host, equipment_id, _time span=1m\n| eval rotation_magnitude=sqrt((gx^2 + gy^2 + gz^2))\n| eval z_axis_dominant=if(abs(gz) > abs(gx) AND abs(gz) > abs(gy), \"YES\", \"NO\")\n| stats avg(rotation_magnitude) as avg_rot, stdev(rotation_magnitude) as std_rot by equipment_id\n| where (avg_rot > 50) AND (std_rot > 10)\n```\n\nUnderstanding this SPL\n\n**Equipment Alignment & Vibration Analysis via Gyroscope** — Monitors rotational alignment of rotating equipment to predict misalignment-induced failures.\n\nDocumented **Data sources**: `index=edge-hub-data` metric_name=gyro_x OR gyro_y OR gyro_z, `sourcetype=edge_hub`. **App/TA** (typical add-on context): Splunk Edge Hub (6-axis gyroscope), Splunk OT Intelligence. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\n**Pipeline walkthrough**\n\n• Uses `mstats` to query metrics indexes (pre-aggregated metric data).\n• `eval` defines or adjusts **rotation_magnitude** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **z_axis_dominant** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by equipment_id** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where (avg_rot > 50) AND (std_rot > 10)` — typically the threshold or rule expression for this monitoring goal.\n\n\nNote: for mstats, confirm the metrics indexes in the search exist and your role can run mstats. This is separate from CIM data model acceleration.\n\nStep 3 — Validate\nConfirm metric or event volume in the time range, then compare trends and device status to the Splunk Edge Hub admin UI or the mobile app for the same devices. Verify metric names, hosts, and time skew.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Rotation rate trend, z-axis dominance heatmap, misalignment risk gauge.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Edge Hub (6-axis gyroscope), Splunk OT Intelligence — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into index=main. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, avg_rot, equipment_id, host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch what edge sensors report from the physical world. We help you know if something is off with shaking that might mean a bearing, balance, or mount problem before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
