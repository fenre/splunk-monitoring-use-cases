{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.4.9",
  "title": "Gateway and Edge Node Resource Utilization",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Gateway and Edge Node Resource Utilization' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_gateway_and_edge_node_resource_utilization` (UC-14.4.9), SOC dashboard panel \"Operations — Gateway and Edge Node Resource Utilization\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.4.9 positive (Gateway, and, Edge, Node, Resource): ingest synthetic events into `index=iot` sourcetype='gateway:metrics' where `count` grouped by `gateway_id` satisfies `cpu` > 80, `mem` > 85, or `queue` > 1000 for 'Gateway and Edge Node Resource Utilization'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.4.9 negative (Resource Utilization): ingest benign `iot` / `gateway:metrics` events where `gateway_id` stays below `cpu` > 80, `mem` > 85, or `queue` > 1000, or exclude the test entity via `ops_exception_register.csv`. Variant 7 confirms zero alert rows for 'Gateway and Edge Node Resource Utilization'."
  },
  "dataSources": "Gateway CPU, memory, disk, message queue depth Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Edge/gateway metrics, SNMP or agent — Universal Forwarder (Splunkbase 617)",
  "spl": "index=iot sourcetype=\"gateway:metrics\"\n| stats latest(cpu_pct) as cpu, latest(mem_pct) as mem, latest(queue_depth) as queue by gateway_id\n| where cpu > 80 OR mem > 85 OR queue > 1000\n| table gateway_id, cpu, mem, queue",
  "description": "Identifies UC-14.4.9 — Gateway, and, Edge, Node, Resource — using `index=iot` sourcetype `gateway:metrics`, grouped by `gateway_id`. Outputs `count` each `the scheduled window`; alert when `cpu` > 80, `mem` > 85, or `queue` > 1000. Primary focus: Resource Utilization. Purpose-built for 'Gateway and Edge Node Resource Utilization' in Operations workflows.",
  "value": "UC-14.4.9 operationalises 'Gateway and Edge Node Resource Utilization' against `gateway:metrics` feeds so operators reduce operational risk and delayed incident response without manual correlation.",
  "implementation": "Collect gateway and edge node metrics via agent or SNMP. Alert when CPU/memory or queue exceeds threshold. Report on utilization trend and top-loaded gateways. Plan scale-out before saturation.",
  "visualization": "Table (gateways over threshold), Gauge (queue depth), Line chart (utilization trend).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable (no CIM or Operational_Telemetry data model in this SPL).",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `iot` (UC-14.4.9)** — Planned work for 'Gateway and Edge Node Resource Utilization' shifts `count` over `span=1h` while `gateway_id` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `gateway:metrics`** — Scheduled automation elevates `src` without crossing `cpu` > 80 for UC-14.4.9. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `gateway_id`** — Health checks in `iot` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `gateway:metrics`** — First 48h baseline for UC-14.4.9 on `gateway_id` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `gateway_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "snmp"
  ],
  "equipmentModels": [
    "snmp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Edge/gateway metrics, SNMP or agent.\n• Ensure the following data sources are available: Gateway CPU, memory, disk, message queue depth.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nCollect gateway and edge node metrics via agent or SNMP. Alert when CPU/memory or queue exceeds threshold. Report on utilization trend and top-loaded gateways. Plan scale-out before saturation.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=iot sourcetype=\"gateway:metrics\"\n| stats latest(cpu_pct) as cpu, latest(mem_pct) as mem, latest(queue_depth) as queue by gateway_id\n| where cpu > 80 OR mem > 85 OR queue > 1000\n| table gateway_id, cpu, mem, queue\n```\n\nUnderstanding this SPL\n\n**Gateway and Edge Node Resource Utilization** — Overloaded gateways drop telemetry or delay processing. Monitoring CPU, memory, and queue depth ensures edge reliability and capacity planning.\n\nDocumented **Data sources**: Gateway CPU, memory, disk, message queue depth. **App/TA** (typical add-on context): Edge/gateway metrics, SNMP or agent. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: iot; **sourcetype**: gateway:metrics. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=iot, sourcetype=\"gateway:metrics\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by gateway_id** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where cpu > 80 OR mem > 85 OR queue > 1000` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Gateway and Edge Node Resource Utilization**): table gateway_id, cpu, mem, queue\n\n\nStep 3 — Validate\nCompare device counts, last-seen times, and health fields to the IoT platform console (for example AWS IoT, Azure IoT Hub, or Google Cloud IoT) for the same period. Verify API polling intervals and that offline rules match your operational definition.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (gateways over threshold), Gauge (queue depth), Line chart (utilization trend).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Edge/gateway metrics, SNMP or agent — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=gateway:metrics into iot. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`cpu, gateway_id, mem, queue`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch your connected devices and the messages they send. We help you know if something is off with this area: gateway and edge node resource utilization before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
