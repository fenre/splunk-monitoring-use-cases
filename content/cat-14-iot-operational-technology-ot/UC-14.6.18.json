{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.6.18",
  "title": "BACnet Who-Is Broadcast Storm Detection",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Performance",
    "Fault"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'BACnet Who-Is Broadcast Storm Detection' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_bacnet_who_is_broadcast_storm_detection` (UC-14.6.18), SOC dashboard panel \"Operations — BACnet Who-Is Broadcast Storm Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=ot` sourcetype='zeek:bacnet:json' where `_time` satisfies `count` > 200 for UC-14.6.18 ('BACnet Who-Is Broadcast Storm Detection'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `ot` / `zeek:bacnet:json` events where `_time` stays below `count` > 200, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-14.6.18."
  },
  "dataSources": "`index=ot` `sourcetype=\"zeek:bacnet:json\"` (`bacnet_discovery.log`: `pdu_service`, `device_id_number`, `source_h`, `destination_h`)",
  "app": "TA for Zeek (Splunkbase 5466), Corelight App for Splunk (Splunkbase 3884)",
  "spl": "index=ot sourcetype=\"zeek:bacnet:json\" pdu_service IN (\"who-is\",\"i-am\",\"who_is\",\"i_am\")\n| bin _time span=1m\n| stats count dc(source_h) as distinct_sources by _time destination_h\n| where count>200 OR distinct_sources>20\n| table _time destination_h count distinct_sources",
  "description": "Excessive Who-Is/I-Am discovery floods MS/TP-to-IP bridges and can indicate misconfigured devices, loops, or active scanning that degrades BMS responsiveness.",
  "value": "Early 'BACnet Who-Is Broadcast Storm Detection' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Ingest discovery logs from Zeek on BACnet/IP VLANs. Set thresholds per campus; investigate sources with high Who-Is rates and verify router/broadcast management settings.",
  "visualization": "Area chart (Who-Is rate per minute), Table (spikes), Pie chart (share by pdu_service).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "TA for Zeek",
      "url": "https://splunkbase.splunk.com/app/5466",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Corelight App for Splunk",
      "url": "https://splunkbase.splunk.com/app/3884",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `ot`** — Planned work for 'BACnet Who-Is Broadcast Storm Detection' shifts `dc` over `span=1m` while `_time` remains in-scope for UC-14.6.18. Cross-check CMDB CHG.\n\n2. **Batch `dc` on `zeek:bacnet:json`** — Scheduled automation elevates `destination_h` without crossing `count` > 200. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `_time`** — Health checks in `ot` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `zeek:bacnet:json`** — First 48h baseline for `_time` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `_time`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: TA for Zeek (Splunkbase 5466), Corelight App for Splunk (Splunkbase 3884).\n• Ensure the following data sources are available: `index=ot` `sourcetype=\"zeek:bacnet:json\"` (`bacnet_discovery.log`: `pdu_service`, `device_id_number`, `source_h`, `destination_h`).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest discovery logs from Zeek on BACnet/IP VLANs. Set thresholds per campus; investigate sources with high Who-Is rates and verify router/broadcast management settings.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot sourcetype=\"zeek:bacnet:json\" pdu_service IN (\"who-is\",\"i-am\",\"who_is\",\"i_am\")\n| bin _time span=1m\n| stats count dc(source_h) as distinct_sources by _time destination_h\n| where count>200 OR distinct_sources>20\n| table _time destination_h count distinct_sources\n```\n\nUnderstanding this SPL\n\n**BACnet Who-Is Broadcast Storm Detection** — Excessive Who-Is/I-Am discovery floods MS/TP-to-IP bridges and can indicate misconfigured devices, loops, or active scanning that degrades BMS responsiveness.\n\nDocumented **Data sources**: `index=ot` `sourcetype=\"zeek:bacnet:json\"` (`bacnet_discovery.log`: `pdu_service`, `device_id_number`, `source_h`, `destination_h`). **App/TA** (typical add-on context): TA for Zeek (Splunkbase 5466), Corelight App for Splunk (Splunkbase 3884). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot; **sourcetype**: zeek:bacnet:json. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot, sourcetype=\"zeek:bacnet:json\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by _time destination_h** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where count>200 OR distinct_sources>20` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **BACnet Who-Is Broadcast Storm Detection**): table _time destination_h count distinct_sources\n\nStep 3 — Validate\nRun the search for a short window and compare key fields with Zeek or Corelight protocol logs on the sensor for the same flow. Reconcile function codes, endpoints, and time order against a small known-good capture if needed.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Area chart (Who-Is rate per minute), Table (spikes), Pie chart (share by pdu_service).",
  "grandmaExplanation": "We use deep packet inspection to catch BACnet discovery storms so broadcast noise does not hide real trouble.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
