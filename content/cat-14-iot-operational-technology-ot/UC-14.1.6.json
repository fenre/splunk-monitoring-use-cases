{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.1.6",
  "title": "Environmental Compliance",
  "criticality": "critical",
  "difficulty": "beginner",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Environmental Compliance' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_environmental_compliance` (UC-14.1.6), SOC dashboard panel \"Operations — Environmental Compliance\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.1.6 positive (Environmental, Compliance): ingest synthetic events into `index=environment` sourcetype='sensor:environmental' where `count` grouped by `entity` satisfies `temp_f` > 80, `temp_f` < 64, `humidity_pct` > 60, or `humidity_pct` < 40 for 'Environmental Compliance'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.1.6 negative (Environmental Compliance): ingest benign `environment` / `sensor:environmental` events where `entity` stays below `temp_f` > 80, `temp_f` < 64, `humidity_pct` > 60, or `humidity_pct` < 40, or exclude the test entity via `ops_exception_register.csv`. Variant 11 confirms zero alert rows for 'Environmental Compliance'."
  },
  "dataSources": "Environmental sensors (temperature, humidity, differential pressure) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Environmental sensor inputs (SNMP, MQTT) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=environment sourcetype=\"sensor:environmental\"\n| where temp_f > 80 OR temp_f < 64 OR humidity_pct > 60 OR humidity_pct < 40\n| table _time, zone, sensor, temp_f, humidity_pct",
  "description": "Surfaces UC-14.1.6 — Environmental, Compliance — using `index=environment` sourcetype `sensor:environmental`, grouped by `entity`. Outputs `count` each `the scheduled window`; alert when `temp_f` > 80, `temp_f` < 64, `humidity_pct` > 60, or `humidity_pct` < 40. Primary focus: Environmental Compliance. Purpose-built for 'Environmental Compliance' in Operations workflows.",
  "value": "UC-14.1.6 helps teams contain Environmental, Compliance scenarios and reduce operational risk and delayed incident response before incidents compound.",
  "implementation": "Deploy environmental sensors per ASHRAE guidelines. Ingest via SNMP or MQTT. Alert immediately on out-of-range conditions. Log compliance data for audit. Track seasonal patterns for cooling optimization.",
  "visualization": "Heatmap (zone × temperature), Line chart (temp/humidity trend), Single value (zones in compliance %), Gauge (current temp per zone).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable (no CIM or Operational_Telemetry data model in this SPL).",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `environment` (UC-14.1.6)** — Planned work for 'Environmental Compliance' shifts `count` over `span=1h` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `sensor:environmental`** — Scheduled automation elevates `src` without crossing `temp_f` > 80 for UC-14.1.6. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `environment` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `sensor:environmental`** — First 48h baseline for UC-14.1.6 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "mqtt",
    "snmp"
  ],
  "equipmentModels": [
    "snmp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Environmental sensor inputs (SNMP, MQTT).\n• Ensure the following data sources are available: Environmental sensors (temperature, humidity, differential pressure).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nDeploy environmental sensors per ASHRAE guidelines. Ingest via SNMP or MQTT. Alert immediately on out-of-range conditions. Log compliance data for audit. Track seasonal patterns for cooling optimization.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=environment sourcetype=\"sensor:environmental\"\n| where temp_f > 80 OR temp_f < 64 OR humidity_pct > 60 OR humidity_pct < 40\n| table _time, zone, sensor, temp_f, humidity_pct\n```\n\nUnderstanding this SPL\n\n**Environmental Compliance** — Temperature/humidity exceedances in data centers risk equipment damage; in labs they invalidate experiments. Compliance monitoring is mandatory.\n\nDocumented **Data sources**: Environmental sensors (temperature, humidity, differential pressure). **App/TA** (typical add-on context): Environmental sensor inputs (SNMP, MQTT). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: environment; **sourcetype**: sensor:environmental. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=environment, sourcetype=\"sensor:environmental\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where temp_f > 80 OR temp_f < 64 OR humidity_pct > 60 OR humidity_pct < 40` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Environmental Compliance**): table _time, zone, sensor, temp_f, humidity_pct\n\n\nStep 3 — Validate\nRun the search in a known window. Spot-check a few events against the BMS console (for example Niagara, Tridium, or Honeywell) and confirm zones, values, and alarms line up. Verify field names and that the right roles can search the index.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Heatmap (zone × temperature), Line chart (temp/humidity trend), Single value (zones in compliance %), Gauge (current temp per zone).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Environmental sensor inputs (SNMP, MQTT) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=sensor:environmental into environment. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`humidity_pct, temp_f`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch how your building systems behave. We help you know if something is off with room conditions your rules and equipment need before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
