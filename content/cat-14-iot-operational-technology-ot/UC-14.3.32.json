{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.3.32",
  "title": "Modbus TCP Register Monitoring for Industrial Equipment",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Modbus TCP Register Monitoring for Industrial Equipment' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_modbus_tcp_register_monitoring_for_industrial_eq` (UC-14.3.32), SOC dashboard panel \"Operations — Modbus TCP Register Monitoring for Industrial Equipment\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.3.32 positive (Modbus, TCP, Register, Monitoring, for): ingest synthetic events into `index=edge-hub-logs` sourcetype='edge_hub' where `count` grouped by `modbus_register_name`, `modbus_device_ip` satisfies `latest_val` >= 210, `latest_val` <= 250, `latest_val` >= 0, or `latest_val` <= 100 for 'Modbus TCP Register Monitoring for Industrial Equipment'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.3.32 negative (Industrial Equipment): ingest benign `edge-hub-logs` / `edge_hub` events where `modbus_register_name`, `modbus_device_ip` stays below `latest_val` >= 210, `latest_val` <= 250, `latest_val` >= 0, or `latest_val` <= 100, or exclude the test entity via `ops_exception_register.csv`. Variant 2 confirms zero alert rows for 'Modbus TCP Register Monitoring for Industrial Equipment'."
  },
  "dataSources": "`index=edge-hub-logs` sourcetype=edge_hub modbus_register, `index=edge-hub-data` modbus_metric Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Edge Hub (Modbus TCP client), Splunk OT Intelligence — Universal Forwarder (Splunkbase 617)",
  "spl": "index=edge-hub-logs sourcetype=edge_hub modbus_register\n| regex modbus_register_name=\"^(voltage|current|frequency)\"\n| stats latest(register_value) as latest_val by modbus_register_name, modbus_device_ip\n| eval register_healthy=case(\n    modbus_register_name=\"voltage\" AND latest_val >= 210 AND latest_val <= 250, \"YES\",\n    modbus_register_name=\"current\" AND latest_val >= 0 AND latest_val <= 100, \"YES\",\n    modbus_register_name=\"frequency\" AND latest_val >= 49 AND latest_val <= 51, \"YES\",\n    1=1, \"NO\")\n| where register_healthy=\"NO\"",
  "description": "Surfaces UC-14.3.32 for Modbus, TCP, Register, Monitoring, for over `index=edge-hub-logs` sourcetype `edge_hub` with thresholds `latest_val` >= 210, `latest_val` <= 250, `latest_val` >= 0, or `latest_val` <= 100. Groups `count` by `modbus_register_name`, `modbus_device_ip` each `the scheduled window`. Centered on Industrial Equipment indicators. Supports repeatable operations triage for 'Modbus TCP Register Monitoring for Industrial Equipment'.",
  "value": "For 'Modbus TCP Register Monitoring for Industrial Equipment', UC-14.3.32 converts `activity` on `edge_hub` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Configure Modbus TCP in Advanced Settings → Modbus tab with equipment IP/port (default 502). Define register map (coils, discrete inputs, holding registers, input registers) with OID aliases for readability. Configure polling interval (10-30 seconds typical) and register read strategy (optimized batching). Store register values in edge-hub-logs (events) or as metrics in edge-hub-data. Map register indices to human-readable tags (e.g., 0x1234→\"VFD_Speed_Hz\"). Local SQLite backlog stores 100K Modbus events for offline resilience.",
  "visualization": "Register value trend, equipment health gauge, Modbus gateway connection status.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable (no CIM or Operational_Telemetry data model in this SPL).",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `edge-hub-logs` (UC-14.3.32)** — Planned work for 'Modbus TCP Register Monitoring for Industrial Equipment' shifts `count` over `span=1h` while `modbus_register_name`, `modbus_device_ip` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `edge_hub`** — Scheduled automation elevates `modbus_device_ip` without crossing `latest_val` >= 210 for UC-14.3.32. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `modbus_register_name`, `modbus_device_ip`** — Health checks in `edge-hub-logs` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `edge_hub`** — First 48h baseline for UC-14.3.32 on `modbus_register_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `modbus_register_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "edge_hub",
    "modbus"
  ],
  "equipmentModels": [
    "edge_hub_generic",
    "modbus_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk Edge Hub",
    "Splunk OT Intelligence"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Edge Hub (Modbus TCP client), Splunk OT Intelligence.\n• Ensure the following data sources are available: `index=edge-hub-logs` sourcetype=edge_hub modbus_register, `index=edge-hub-data` modbus_metric.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nConfigure Modbus TCP in Advanced Settings → Modbus tab with equipment IP/port (default 502). Define register map (coils, discrete inputs, holding registers, input registers) with OID aliases for readability. Configure polling interval (10-30 seconds typical) and register read strategy (optimized batching). Store register values in edge-hub-logs (events) or as metrics in edge-hub-data. Map register indices to human-readable tags (e.g., 0x1234→\"VFD_Speed_Hz\"). Local SQLite backlog stores 100K Modb…\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=edge-hub-logs sourcetype=edge_hub modbus_register\n| regex modbus_register_name=\"^(voltage|current|frequency)\"\n| stats latest(register_value) as latest_val by modbus_register_name, modbus_device_ip\n| eval register_healthy=case(\n    modbus_register_name=\"voltage\" AND latest_val >= 210 AND latest_val <= 250, \"YES\",\n    modbus_register_name=\"current\" AND latest_val >= 0 AND latest_val <= 100, \"YES\",\n    modbus_register_name=\"frequency\" AND latest_val >= 49 AND latest_val <= 51, \"YES\",\n    1=1, \"NO\")\n| where register_healthy=\"NO\"\n```\n\nUnderstanding this SPL\n\n**Modbus TCP Register Monitoring for Industrial Equipment** — Monitors equipment operational parameters via Modbus registers without requiring specialized data collection agents.\n\nDocumented **Data sources**: `index=edge-hub-logs` sourcetype=edge_hub modbus_register, `index=edge-hub-data` modbus_metric. **App/TA** (typical add-on context): Splunk Edge Hub (Modbus TCP client), Splunk OT Intelligence. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: edge-hub-logs; **sourcetype**: edge_hub. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=edge-hub-logs, sourcetype=edge_hub. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters rows matching a pattern with `regex`.\n• `stats` rolls up events into metrics; results are split **by modbus_register_name, modbus_device_ip** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **register_healthy** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where register_healthy=\"NO\"` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nConfirm metric or event volume in the time range, then compare trends and device status to the Splunk Edge Hub admin UI or the mobile app for the same devices. Verify metric names, hosts, and time skew.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Register value trend, equipment health gauge, Modbus gateway connection status.",
  "grandmaExplanation": "We watch what edge sensors report from the physical world. We help you know if something is off with dust in the air that can bother lungs before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
