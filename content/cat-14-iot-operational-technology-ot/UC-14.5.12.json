{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.5.12",
  "title": "OPC-UA Subscription Latency and Sampling Overrun",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'OPC-UA Subscription Latency and Sampling Overrun' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_opc_ua_subscription_latency_and_sampling_overrun` (UC-14.5.12), SOC dashboard panel \"Operations — OPC-UA Subscription Latency and Sampling Overrun\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.5.12 positive (OPC, Subscription, Latency, and, Sampling): ingest synthetic events into `index=edge-hub-data` sourcetype='opcua_metrics' where `avg_latency`, `overruns` grouped by `host`, `subscription_id` satisfies `avg_latency` > 500, or `overruns` > 0 for 'OPC-UA Subscription Latency and Sampling Overrun'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.5.12 negative (Sampling Overrun): ingest benign `edge-hub-data` / `opcua_metrics` events where `host`, `subscription_id` stays below `avg_latency` > 500, or `overruns` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 15 confirms zero alert rows for 'OPC-UA Subscription Latency and Sampling Overrun'."
  },
  "dataSources": "Connector metrics (subscription latency, overrun count) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "OPC-UA gateway, Edge Hub OPC-UA connector — Universal Forwarder (Splunkbase 617)",
  "spl": "index=edge-hub-data OR index=ot sourcetype=opcua_metrics\n| stats avg(subscription_latency_ms) as avg_latency, sum(sampling_overrun_count) as overruns by host, subscription_id\n| where avg_latency > 500 OR overruns > 0\n| table host subscription_id avg_latency overruns",
  "description": "Detects UC-14.5.12 — OPC, Subscription, Latency, and, Sampling — using `index=edge-hub-data` sourcetype `opcua_metrics`, grouped by `host`, `subscription_id`. Outputs `avg_latency`, `overruns` each `the scheduled window`; alert when `avg_latency` > 500, or `overruns` > 0. Primary focus: Sampling Overrun. Purpose-built for 'OPC-UA Subscription Latency and Sampling Overrun' in Operations workflows.",
  "value": "UC-14.5.12 operationalises 'OPC-UA Subscription Latency and Sampling Overrun' against `opcua_metrics` feeds so operators reduce operational risk and delayed incident response without manual correlation.",
  "implementation": "If gateway or Edge Hub exposes subscription latency and overrun metrics, ingest them. Alert when latency exceeds 500 ms or overrun count is non-zero. Reduce sampling rate or add resources if persistent.",
  "visualization": "Line chart (latency by subscription), Table (overruns by subscription), Single value (max latency).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `edge-hub-data` (UC-14.5.12)** — Planned work for 'OPC-UA Subscription Latency and Sampling Overrun' shifts `avg_latency`, `overruns` over `span=1h` while `host`, `subscription_id` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `opcua_metrics`** — Scheduled automation elevates `subscription_id` without crossing `avg_latency` > 500 for UC-14.5.12. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`, `subscription_id`** — Health checks in `edge-hub-data` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `opcua_metrics`** — First 48h baseline for UC-14.5.12 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "edge_hub",
    "opcua"
  ],
  "equipmentModels": [
    "edge_hub_generic",
    "opcua_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk Edge Hub"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: OPC-UA gateway, Edge Hub OPC-UA connector.\n• Ensure the following data sources are available: Connector metrics (subscription latency, overrun count).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIf gateway or Edge Hub exposes subscription latency and overrun metrics, ingest them. Alert when latency exceeds 500 ms or overrun count is non-zero. Reduce sampling rate or add resources if persistent.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=edge-hub-data OR index=ot sourcetype=opcua_metrics\n| stats avg(subscription_latency_ms) as avg_latency, sum(sampling_overrun_count) as overruns by host, subscription_id\n| where avg_latency > 500 OR overruns > 0\n| table host subscription_id avg_latency overruns\n```\n\nUnderstanding this SPL\n\n**OPC-UA Subscription Latency and Sampling Overrun** — High subscription latency or sampling overrun (server missing sample interval) degrades real-time visibility. Tracking supports connector and server tuning.\n\nDocumented **Data sources**: Connector metrics (subscription latency, overrun count). **App/TA** (typical add-on context): OPC-UA gateway, Edge Hub OPC-UA connector. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: edge-hub-data, ot; **sourcetype**: opcua_metrics. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=edge-hub-data, index=ot, sourcetype=opcua_metrics. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by host, subscription_id** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where avg_latency > 500 OR overruns > 0` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **OPC-UA Subscription Latency and Sampling Overrun**): table host subscription_id avg_latency overruns\n\nStep 3 — Validate\nRun the search for a time range where you know sessions were active, then compare the same nodes and clients in an industrial client such as UA Expert. Spot-check certificate and session fields against the server.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (latency by subscription), Table (overruns by subscription), Single value (max latency).",
  "grandmaExplanation": "We time subscriptions and sampling on the industrial server so late or overrun polls do not erode the picture of the process.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
