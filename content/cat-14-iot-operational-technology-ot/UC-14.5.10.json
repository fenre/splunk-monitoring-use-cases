{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.5.10",
  "title": "Edge Hub Local Storage and SQLite Backlog",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Edge Hub Local Storage and SQLite Backlog' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_edge_hub_local_storage_and_sqlite_backlog` (UC-14.5.10), SOC dashboard panel \"Operations — Edge Hub Local Storage and SQLite Backlog\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.5.10 positive (Edge, Hub, Local, Storage, and): ingest synthetic events into `index=edge-hub-health` sourcetype='edge_hub' where `count` grouped by `host` satisfies `disk_pct` > 85, or `backlog` > 2000000 for 'Edge Hub Local Storage and SQLite Backlog'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.5.10 negative (SQLite Backlog): ingest benign `edge-hub-health` / `edge_hub` events where `host` stays below `disk_pct` > 85, or `backlog` > 2000000, or exclude the test entity via `ops_exception_register.csv`. Variant 5 confirms zero alert rows for 'Edge Hub Local Storage and SQLite Backlog'."
  },
  "dataSources": "`index=edge-hub-health` (disk_usage, backlog_count) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Edge Hub (device health) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=edge-hub-health sourcetype=edge_hub\n| stats latest(disk_usage) as disk_pct, latest(backlog_count) as backlog by host\n| where disk_pct > 85 OR backlog > 2000000\n| table host disk_pct backlog _time",
  "description": "Alerts on 'Edge Hub Local Storage and SQLite Backlog' patterns (UC-14.5.10) sourced from `index=edge-hub-health` sourcetype `edge_hub`. Threshold bundle: `disk_pct` > 85, or `backlog` > 2000000; aggregation grain: `host`; metrics: `count`. Watch terms: Edge, Hub, Local, Storage, and.",
  "value": "UC-14.5.10 gives Operations teams earlier visibility into SQLite Backlog through `edge_hub` feeds, lowering operational risk and delayed incident response.",
  "implementation": "Edge Hub reports disk and backlog to edge-hub-health. Alert when disk exceeds 85% or backlog approaches device limit (e.g. 2M). Plan connectivity and storage upgrades before saturation.",
  "visualization": "Gauge (disk %), Line chart (backlog over time), Table (devices near limit).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `edge-hub-health` (UC-14.5.10)** — Planned work for 'Edge Hub Local Storage and SQLite Backlog' shifts `count` over `span=1h` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `edge_hub`** — Scheduled automation elevates `src` without crossing `disk_pct` > 85 for UC-14.5.10. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `edge-hub-health` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `edge_hub`** — First 48h baseline for UC-14.5.10 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "edge_hub"
  ],
  "equipmentModels": [
    "edge_hub_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk Edge Hub"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Edge Hub (device health).\n• Ensure the following data sources are available: `index=edge-hub-health` (disk_usage, backlog_count).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEdge Hub reports disk and backlog to edge-hub-health. Alert when disk exceeds 85% or backlog approaches device limit (e.g. 2M). Plan connectivity and storage upgrades before saturation.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=edge-hub-health sourcetype=edge_hub\n| stats latest(disk_usage) as disk_pct, latest(backlog_count) as backlog by host\n| where disk_pct > 85 OR backlog > 2000000\n| table host disk_pct backlog _time\n```\n\nUnderstanding this SPL\n\n**Edge Hub Local Storage and SQLite Backlog** — Edge Hub stores backlog in SQLite when disconnected. Disk usage and backlog size must stay within limits (e.g. 3M sensor points) to avoid data loss and device lockup.\n\nDocumented **Data sources**: `index=edge-hub-health` (disk_usage, backlog_count). **App/TA** (typical add-on context): Splunk Edge Hub (device health). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: edge-hub-health; **sourcetype**: edge_hub. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=edge-hub-health, sourcetype=edge_hub. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by host** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where disk_pct > 85 OR backlog > 2000000` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Edge Hub Local Storage and SQLite Backlog**): table host disk_pct backlog _time\n\nStep 3 — Validate\nRun the search over a health or backlog window you understand, then compare with the Splunk Edge Hub admin views and local health on the edge host. Reconcile queue, backlog, and service fields line by line.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Gauge (disk %), Line chart (backlog over time), Table (devices near limit).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Edge Hub (device health) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=edge_hub into edge-hub-health. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`backlog, disk_pct, host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch local storage and backlogs on the edge box so a full disk or stuck queue does not block messaging into our platform.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
