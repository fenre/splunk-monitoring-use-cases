{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.5.18",
  "title": "HiveMQ Retained Message Store Growth",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'HiveMQ Retained Message Store Growth' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_hivemq_retained_message_store_growth` (UC-14.5.18), SOC dashboard panel \"Operations — HiveMQ Retained Message Store Growth\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=ot` sourcetype='hivemq:metrics' where `host` satisfies detection threshold for UC-14.5.18 ('HiveMQ Retained Message Store Growth'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `ot` / `hivemq:metrics` events where `host` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-14.5.18."
  },
  "dataSources": "`index=ot` `sourcetype=\"hivemq:metrics\"` retained message counters Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "HiveMQ Splunk Extension (metrics to Splunk) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=ot sourcetype=\"hivemq:metrics\"\n| eval m=lower(coalesce(metric_name, metric, name))\n| where match(m, \"retain\")\n| eval v=coalesce(value, metric_value, _value)\n| bin _time span=1h\n| stats max(v) as retained_max by host, _time\n| timechart span=1h max(retained_max) by host",
  "description": "Retained messages accumulate with misconfigured publishers; growth risks disk exhaustion and slower broker startup, especially at OT edge with constrained storage.",
  "value": "Early 'HiveMQ Retained Message Store Growth' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Map the exact HiveMQ metric name from your Prometheus/SVA mapping. Alert on week-over-week growth or crossing a capacity threshold. Correlate spikes with new devices publishing retained messages on unique topics.",
  "visualization": "Line chart (retained count over time), Area chart (growth rate), Single value (current max), Table (brokers over threshold).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `ot`** — Planned work for 'HiveMQ Retained Message Store Growth' shifts `max` over `span=1h` while `host` remains in-scope for UC-14.5.18. Cross-check CMDB CHG.\n\n2. **Batch `max` on `hivemq:metrics`** — Scheduled automation elevates `_time` without crossing configured threshold. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `ot` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `hivemq:metrics`** — First 48h baseline for `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "mqtt",
    "prometheus"
  ],
  "equipmentModels": [
    "mqtt_generic",
    "prometheus_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: HiveMQ Splunk Extension (metrics to Splunk).\n• Ensure the following data sources are available: `index=ot` `sourcetype=\"hivemq:metrics\"` retained message counters.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMap the exact HiveMQ metric name from your Prometheus/SVA mapping. Alert on week-over-week growth or crossing a capacity threshold. Correlate spikes with new devices publishing retained messages on unique topics.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot sourcetype=\"hivemq:metrics\"\n| eval m=lower(coalesce(metric_name, metric, name))\n| where match(m, \"retain\")\n| eval v=coalesce(value, metric_value, _value)\n| bin _time span=1h\n| stats max(v) as retained_max by host, _time\n| timechart span=1h max(retained_max) by host\n```\n\nUnderstanding this SPL\n\n**HiveMQ Retained Message Store Growth** — Retained messages accumulate with misconfigured publishers; growth risks disk exhaustion and slower broker startup, especially at OT edge with constrained storage.\n\nDocumented **Data sources**: `index=ot` `sourcetype=\"hivemq:metrics\"` retained message counters. **App/TA** (typical add-on context): HiveMQ Splunk Extension (metrics to Splunk). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot; **sourcetype**: hivemq:metrics. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot, sourcetype=\"hivemq:metrics\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **m** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where match(m, \"retain\")` — typically the threshold or rule expression for this monitoring goal.\n• `eval` defines or adjusts **v** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by host, _time** so each row reflects one combination of those dimensions.\n• `timechart` plots the metric over time using **span=1h** buckets with a separate series **by host** — ideal for trending and alerting on this use case.\n\nStep 3 — Validate\nRun the search for a known busy window, then compare counts and clients with the HiveMQ control center or broker metrics. Reconcile top clients and topics with what the broker shows.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (retained count over time), Area chart (growth rate), Single value (current max), Table (brokers over threshold).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (HiveMQ Splunk Extension (metrics to Splunk) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=hivemq:metrics into ot. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We track retained store growth so runaway topics do not fill disk or slow the broker for everyone.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
