{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.2.27",
  "title": "EDI Acknowledgement Monitoring",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Operations"
  ],
  "splunkPillar": "Observability",
  "industry": "Manufacturing",
  "exclusions": "Covers the detection logic documented for 'EDI Acknowledgement Monitoring' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_edi_acknowledgement_monitoring` (UC-14.2.27), SOC dashboard panel \"Operations — EDI Acknowledgement Monitoring\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.2.27 positive (EDI, Acknowledgement, Monitoring): ingest synthetic events into `index=edi` sourcetype='edi:control' where `count` grouped by `interchange_id`, `doc_type`, `partner_id` satisfies `ack_latency_sec` > 3600 for 'EDI Acknowledgement Monitoring'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.2.27 negative (Acknowledgement Monitoring): ingest benign `edi` / `edi:control` events where `interchange_id`, `doc_type`, `partner_id` stays below `ack_latency_sec` > 3600, or exclude the test entity via `ops_exception_register.csv`. Variant 0 confirms zero alert rows for 'EDI Acknowledgement Monitoring'."
  },
  "dataSources": "`sourcetype=\"edi:control\"`, `sourcetype=\"as2:mdn\"` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "B2B gateway (IBM/Seeburger), VAN logs — Universal Forwarder (Splunkbase 617)",
  "spl": "index=edi sourcetype=\"edi:control\"\n| stats earliest(_time) as sent latest(ack_time) as ack by interchange_id, doc_type, partner_id\n| eval ack_latency_sec=ack-sent\n| where isnull(ack) OR ack_latency_sec>3600\n| sort -sent",
  "description": "Alerts on 'EDI Acknowledgement Monitoring' (UC-14.2.27) using `index=edi` sourcetype `edi:control` by aggregating `count` grouped by `interchange_id`, `doc_type`, `partner_id` over `the scheduled window` and alerting when `ack_latency_sec` > 3600. Keywords: EDI, Acknowledgement, Monitoring. Focuses on Acknowledgement Monitoring behaviour so operations teams can triage UC-14.2.27 without ad-hoc log review.",
  "value": "UC-14.2.27 accelerates insight into Acknowledgement Monitoring ('EDI Acknowledgement Monitoring') by tracking `activity` on `edi:control` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "SLA per trading partner; auto-retry and partner escalation on NAK codes.",
  "visualization": "Table (late/missing acks), Line chart (ack latency trend), Bar chart (by partner).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable (no CIM or Operational_Telemetry data model in this SPL).",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `edi` (UC-14.2.27)** — Planned work for 'EDI Acknowledgement Monitoring' shifts `count` over `span=1h` while `interchange_id`, `doc_type`, `partner_id` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `edi:control`** — Scheduled automation elevates `doc_type` without crossing `ack_latency_sec` > 3600 for UC-14.2.27. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `interchange_id`, `doc_type`, `partner_id`** — Health checks in `edi` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `edi:control`** — First 48h baseline for UC-14.2.27 on `interchange_id` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `interchange_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: B2B gateway (IBM/Seeburger), VAN logs.\n• Ensure the following data sources are available: `sourcetype=\"edi:control\"`, `sourcetype=\"as2:mdn\"`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nSLA per trading partner; auto-retry and partner escalation on NAK codes.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=edi sourcetype=\"edi:control\"\n| stats earliest(_time) as sent latest(ack_time) as ack by interchange_id, doc_type, partner_id\n| eval ack_latency_sec=ack-sent\n| where isnull(ack) OR ack_latency_sec>3600\n| sort -sent\n```\n\nUnderstanding this SPL\n\n**EDI Acknowledgement Monitoring** — Missing 997/999/CONTRL acks for ASNs or orders disrupt supply-chain and inventory — common in automotive / discrete manufacturing.\n\nDocumented **Data sources**: `sourcetype=\"edi:control\"`, `sourcetype=\"as2:mdn\"`. **App/TA** (typical add-on context): B2B gateway (IBM/Seeburger), VAN logs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: edi; **sourcetype**: edi:control. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=edi, sourcetype=\"edi:control\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by interchange_id, doc_type, partner_id** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **ack_latency_sec** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where isnull(ack) OR ack_latency_sec>3600` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nRun the search when the process is in a known state. Compare key fields and alarm states to the SCADA or DCS HMI and, where relevant, the engineering tool for the PLC. Confirm sourcetypes and field extractions match the vendor feed.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (late/missing acks), Line chart (ack latency trend), Bar chart (by partner).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (B2B gateway (IBM/Seeburger), VAN logs — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=edi:control into edi. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`ack_latency_sec, doc_type, interchange_id, partner_id`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch your plant control systems and field data. We help you know if something is off with this area: edi acknowledgement before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
