{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.7.3",
  "title": "Edge-to-Splunk Data Pipeline Latency",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Edge-to-Splunk Data Pipeline Latency' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_edge_to_splunk_data_pipeline_latency` (UC-14.7.3), SOC dashboard panel \"Operations — Edge-to-Splunk Data Pipeline Latency\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=ot` sourcetype='litmus:tag' that satisfy the UC-14.7.3 SPL filters for 'Edge-to-Splunk Data Pipeline Latency'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `ot` / `litmus:tag` events outside the detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-14.7.3."
  },
  "dataSources": "`index=ot` `sourcetype=\"litmus:tag\"` with `edge_timestamp` in JSON payload Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Litmus Edge (Splunk HEC connector) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=ot sourcetype=\"litmus:tag\"\n| eval edge_sec=if(edge_timestamp>1e12, edge_timestamp/1000, edge_timestamp)\n| where isnotnull(edge_timestamp) AND edge_sec>0\n| eval latency_ms=abs(_time-edge_sec)*1000\n| bin _time span=5m\n| stats perc95(latency_ms) as p95_ms, avg(latency_ms) as avg_ms by gateway_id, _time\n| where p95_ms > 5000",
  "description": "End-to-end latency distinguishes edge capture delays from Splunk indexing backlog; spikes indicate broker saturation, HEC backpressure, or clock skew.",
  "value": "Early 'Edge-to-Splunk Data Pipeline Latency' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Configure Litmus to stamp each tag event with capture time in epoch. Keep NTP synchronized on Litmus and Splunk. Alert when p95 exceeds acceptable thresholds.",
  "visualization": "Line chart (p95/p99 latency by gateway), Area chart (latency distribution), Single value (fleet p95 latency).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'Edge-to-Splunk Data Pipeline Latency'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `ot` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `ops_exception_register.csv` with entity, reason, valid_until.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Litmus Edge (Splunk HEC connector).\n• Ensure the following data sources are available: `index=ot` `sourcetype=\"litmus:tag\"` with `edge_timestamp` in JSON payload.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nConfigure Litmus to stamp each tag event with capture time in epoch. Keep NTP synchronized on Litmus and Splunk. Alert when p95 exceeds acceptable thresholds.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot sourcetype=\"litmus:tag\"\n| eval edge_sec=if(edge_timestamp>1e12, edge_timestamp/1000, edge_timestamp)\n| where isnotnull(edge_timestamp) AND edge_sec>0\n| eval latency_ms=abs(_time-edge_sec)*1000\n| bin _time span=5m\n| stats perc95(latency_ms) as p95_ms, avg(latency_ms) as avg_ms by gateway_id, _time\n| where p95_ms > 5000\n```\n\nUnderstanding this SPL\n\n**Edge-to-Splunk Data Pipeline Latency** — End-to-end latency distinguishes edge capture delays from Splunk indexing backlog; spikes indicate broker saturation, HEC backpressure, or clock skew.\n\nDocumented **Data sources**: `index=ot` `sourcetype=\"litmus:tag\"` with `edge_timestamp` in JSON payload. **App/TA** (typical add-on context): Litmus Edge (Splunk HEC connector). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot; **sourcetype**: litmus:tag. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot, sourcetype=\"litmus:tag\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **edge_sec** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where isnotnull(edge_timestamp) AND edge_sec>0` — typically the threshold or rule expression for this monitoring goal.\n• `eval` defines or adjusts **latency_ms** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by gateway_id, _time** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where p95_ms > 5000` — typically the threshold or rule expression for this monitoring goal.\n\nStep 3 — Validate\nRun the search over a window that includes a known test or production cycle, then compare with the Litmus Edge admin console for gateway health, tag paths, and pipeline status. Reconcile site and device ids.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (p95/p99 latency by gateway), Area chart (latency distribution), Single value (fleet p95 latency).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Litmus Edge (Splunk HEC connector) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=litmus:tag into ot. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, edge_sec, edge_timestamp, gateway_id`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We time how long it takes data to reach us from the industrial gateway so slow links or throttles do not hide behind averages.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
