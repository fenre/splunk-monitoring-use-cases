{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.3.28",
  "title": "USB Camera Barcode & QR Code Scanning Container",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'USB Camera Barcode & QR Code Scanning Container' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_usb_camera_barcode_qr_code_scanning_container` (UC-14.3.28), SOC dashboard panel \"Operations — USB Camera Barcode & QR Code Scanning Container\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.3.28 positive (USB, Camera, Barcode, Code, Scanning): ingest synthetic events into `index=edge-hub-logs` sourcetype='splunk_edge_hub_log' where `count` grouped by `host`, `scan_location` satisfies `scan_accuracy` < 95 for 'USB Camera Barcode & QR Code Scanning Container'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.3.28 negative (Scanning Container): ingest benign `edge-hub-logs` / `splunk_edge_hub_log` events where `host`, `scan_location` stays below `scan_accuracy` < 95, or exclude the test entity via `ops_exception_register.csv`. Variant 0 confirms zero alert rows for 'USB Camera Barcode & QR Code Scanning Container'."
  },
  "dataSources": "`index=edge-hub-logs` sourcetype=splunk_edge_hub_log barcode_scan_event, `index=edge-hub-data` scan_metadata Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Edge Hub (USB camera + custom container), v2.1+ (USB passthrough) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=edge-hub-logs sourcetype=splunk_edge_hub_log barcode_scan_event\n| regex barcode_value=\"^[0-9]{12,14}$\"\n| stats count as successful_scans, count(eval(barcode_valid=\"NO\")) as invalid_scans by host, scan_location\n| eval scan_accuracy = (successful_scans / (successful_scans + invalid_scans)) * 100\n| where scan_accuracy < 95",
  "description": "Detects 'USB Camera Barcode & QR Code Scanning Container' (UC-14.3.28) using `index=edge-hub-logs` sourcetype `splunk_edge_hub_log` by aggregating `count` grouped by `host`, `scan_location` over `the scheduled window` and alerting when `scan_accuracy` < 95. Keywords: USB, Camera, Barcode, Code, Scanning. Focuses on Scanning Container behaviour so operations teams can triage UC-14.3.28 without ad-hoc log review.",
  "value": "UC-14.3.28 accelerates insight into Scanning Container ('USB Camera Barcode & QR Code Scanning Container') by tracking `activity` on `splunk_edge_hub_log` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Connect USB camera to Edge Hub USB port (requires v2.1+ for USB device passthrough). Build custom ARM64 container using OpenCV + pyzbar/python-qrcode libraries for barcode detection. Container captures video frames, decodes barcodes/QR codes, and logs scan_id + barcode_value to edge-hub-logs. Implement local SQLite database (in container) to store scanned inventory and prevent duplicate entries. Publish scan events to MQTT (Advanced Settings → MQTT Publish) for downstream processing. Configure edge.json resource limits: memory 512MB, CPU 2 cores (video processing is CPU-intensive).",
  "visualization": "Scan success rate trend, invalid barcode timeline, inventory reconciliation report.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable (no CIM or Operational_Telemetry data model in this SPL).",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `edge-hub-logs` (UC-14.3.28)** — Planned work for 'USB Camera Barcode & QR Code Scanning Container' shifts `count` over `span=1h` while `host`, `scan_location` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `splunk_edge_hub_log`** — Scheduled automation elevates `scan_location` without crossing `scan_accuracy` < 95 for UC-14.3.28. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`, `scan_location`** — Health checks in `edge-hub-logs` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `splunk_edge_hub_log`** — First 48h baseline for UC-14.3.28 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "edge_hub",
    "mqtt"
  ],
  "equipmentModels": [
    "edge_hub_generic",
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk Edge Hub"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Edge Hub (USB camera + custom container), v2.1+ (USB passthrough).\n• Ensure the following data sources are available: `index=edge-hub-logs` sourcetype=splunk_edge_hub_log barcode_scan_event, `index=edge-hub-data` scan_metadata.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nConnect USB camera to Edge Hub USB port (requires v2.1+ for USB device passthrough). Build custom ARM64 container using OpenCV + pyzbar/python-qrcode libraries for barcode detection. Container captures video frames, decodes barcodes/QR codes, and logs scan_id + barcode_value to edge-hub-logs. Implement local SQLite database (in container) to store scanned inventory and prevent duplicate entries. Publish scan events to MQTT (Advanced Settings → MQTT Publish) for downstream processing. Configure e…\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=edge-hub-logs sourcetype=splunk_edge_hub_log barcode_scan_event\n| regex barcode_value=\"^[0-9]{12,14}$\"\n| stats count as successful_scans, count(eval(barcode_valid=\"NO\")) as invalid_scans by host, scan_location\n| eval scan_accuracy = (successful_scans / (successful_scans + invalid_scans)) * 100\n| where scan_accuracy < 95\n```\n\nUnderstanding this SPL\n\n**USB Camera Barcode & QR Code Scanning Container** — Automates material tracking and inventory verification by scanning barcodes/QR codes at the edge without manual entry.\n\nDocumented **Data sources**: `index=edge-hub-logs` sourcetype=splunk_edge_hub_log barcode_scan_event, `index=edge-hub-data` scan_metadata. **App/TA** (typical add-on context): Splunk Edge Hub (USB camera + custom container), v2.1+ (USB passthrough). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: edge-hub-logs; **sourcetype**: splunk_edge_hub_log. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=edge-hub-logs, sourcetype=splunk_edge_hub_log. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters rows matching a pattern with `regex`.\n• `stats` rolls up events into metrics; results are split **by host, scan_location** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **scan_accuracy** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where scan_accuracy < 95` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nConfirm metric or event volume in the time range, then compare trends and device status to the Splunk Edge Hub admin UI or the mobile app for the same devices. Verify metric names, hosts, and time skew.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Scan success rate trend, invalid barcode timeline, inventory reconciliation report.",
  "grandmaExplanation": "We watch what edge sensors report from the physical world. We help you know if something is off with this area: usb camera barcode & qr code scanning container before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
