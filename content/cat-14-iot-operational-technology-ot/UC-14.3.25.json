{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.3.25",
  "title": "Local Alerting & GPIO Relay Control Container",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Local Alerting & GPIO Relay Control Container' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_local_alerting_gpio_relay_control_container` (UC-14.3.25), SOC dashboard panel \"Operations — Local Alerting & GPIO Relay Control Container\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.3.25 positive (Local, Alerting, GPIO, Relay, Control): ingest synthetic events into `index=edge-hub-logs` sourcetype='splunk_edge_hub_log' where `count` grouped by `host` satisfies detection threshold for 'Local Alerting & GPIO Relay Control Container'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.3.25 negative (Control Container): ingest benign `edge-hub-logs` / `splunk_edge_hub_log` events where `host` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 12 confirms zero alert rows for 'Local Alerting & GPIO Relay Control Container'."
  },
  "dataSources": "`index=edge-hub-data` all sensor metrics, `index=edge-hub-logs` container_alert_log Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Edge Hub (custom container), gRPC SDK, GPIO control — Universal Forwarder (Splunkbase 617)",
  "spl": "index=edge-hub-logs sourcetype=splunk_edge_hub_log container_name=local_alerting alert_triggered=true\n| stats count as alerts_triggered, count(eval(relay_state=\"ENERGIZED\")) as equipment_stopped by host\n| eval safety_response_rate = (equipment_stopped / alerts_triggered) * 100",
  "description": "Identifies UC-14.3.25 — Local, Alerting, GPIO, Relay, Control — using `index=edge-hub-logs` sourcetype `splunk_edge_hub_log`, grouped by `host`. Outputs `count` each `the scheduled window`; alert when configured threshold. Primary focus: Control Container. Purpose-built for 'Local Alerting & GPIO Relay Control Container' in Operations workflows.",
  "value": "Tracking `activity` via UC-14.3.25 on `splunk_edge_hub_log` feeds improves response to 'Local Alerting & GPIO Relay Control Container' and reduces operational risk and delayed incident response.",
  "implementation": "Build custom ARM64 container with gRPC client library and GPIO library (RPi.GPIO or gpiod). Container subscribes to Edge Hub gRPC sensor stream, implements local thresholds (e.g., temperature > 90°C), and directly controls GPIO pins to energize/de-energize relays (e.g., kill power to pump, trigger siren). No cloud round-trip latency—decisions made in <100ms. Configure edge.json with resource limits (memory: 128MB, CPU: 0.5 core). Store alert events in local edge-hub-logs for compliance.",
  "visualization": "Alert frequency timeline, relay activation log, response latency histogram.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable (no CIM or Operational_Telemetry data model in this SPL).",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `edge-hub-logs` (UC-14.3.25)** — Planned work for 'Local Alerting & GPIO Relay Control Container' shifts `count` over `span=1h` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `splunk_edge_hub_log`** — Scheduled automation elevates `safety_response_rate` without crossing configured threshold for UC-14.3.25. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `edge-hub-logs` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `splunk_edge_hub_log`** — First 48h baseline for UC-14.3.25 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "edge_hub"
  ],
  "equipmentModels": [
    "edge_hub_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk Edge Hub"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Edge Hub (custom container), gRPC SDK, GPIO control.\n• Ensure the following data sources are available: `index=edge-hub-data` all sensor metrics, `index=edge-hub-logs` container_alert_log.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nBuild custom ARM64 container with gRPC client library and GPIO library (RPi.GPIO or gpiod). Container subscribes to Edge Hub gRPC sensor stream, implements local thresholds (e.g., temperature > 90°C), and directly controls GPIO pins to energize/de-energize relays (e.g., kill power to pump, trigger siren). No cloud round-trip latency—decisions made in <100ms. Configure edge.json with resource limits (memory: 128MB, CPU: 0.5 core). Store alert events in local edge-hub-logs for compliance.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=edge-hub-logs sourcetype=splunk_edge_hub_log container_name=local_alerting alert_triggered=true\n| stats count as alerts_triggered, count(eval(relay_state=\"ENERGIZED\")) as equipment_stopped by host\n| eval safety_response_rate = (equipment_stopped / alerts_triggered) * 100\n```\n\nUnderstanding this SPL\n\n**Local Alerting & GPIO Relay Control Container** — Enables immediate equipment shutdown or alarm triggering at the edge without cloud latency, critical for safety-critical systems.\n\nDocumented **Data sources**: `index=edge-hub-data` all sensor metrics, `index=edge-hub-logs` container_alert_log. **App/TA** (typical add-on context): Splunk Edge Hub (custom container), gRPC SDK, GPIO control. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: edge-hub-logs; **sourcetype**: splunk_edge_hub_log. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=edge-hub-logs, sourcetype=splunk_edge_hub_log. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by host** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **safety_response_rate** — often to normalize units, derive a ratio, or prepare for thresholds.\n\n\nStep 3 — Validate\nConfirm metric or event volume in the time range, then compare trends and device status to the Splunk Edge Hub admin UI or the mobile app for the same devices. Verify metric names, hosts, and time skew.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Alert frequency timeline, relay activation log, response latency histogram.",
  "grandmaExplanation": "We watch what edge sensors report from the physical world. We help you know if something is off with this area: local alerting & gpio relay control container before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
