{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.1.18",
  "title": "Water Leak Detection and Flood Alerts (Meraki MT)",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Water Leak Detection and Flood Alerts (Meraki MT)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_water_leak_detection_and_flood_alerts_meraki_mt` (UC-14.1.18), SOC dashboard panel \"Operations — Water Leak Detection and Flood Alerts (Meraki MT)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.1.18 positive (Water, Leak, Detection, and, Flood): ingest synthetic events into `index=meraki` sourcetype='meraki:sensorreadingshistory' where `count` grouped by `serial`, `\"network.name\"` satisfies `leak_events` > 0 for 'Water Leak Detection and Flood Alerts (Meraki MT)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.1.18 negative ((Meraki MT)): ingest benign `meraki` / `meraki:sensorreadingshistory` events where `serial`, `\"network.name\"` stays below `leak_events` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 8 confirms zero alert rows for 'Water Leak Detection and Flood Alerts (Meraki MT)'."
  },
  "dataSources": "`index=meraki sourcetype=meraki:sensorreadingshistory metric=\"water\"` from the `cisco_meraki_sensor_readings_history` input in `Splunk_TA_cisco_meraki` (Splunkbase 5580). Key fields: `serial`, `network.name`, `metric`, `water.present` (boolean), `ts`.",
  "app": "`Cisco Meraki Add-on for Splunk` (Splunkbase 5580)",
  "spl": "index=meraki sourcetype=\"meraki:sensorreadingshistory\" metric=\"water\"\n| rename \"water.present\" as water_present\n| where water_present=\"true\" OR water_present=1\n| stats count as leak_events, latest(_time) as last_detection_time by serial, \"network.name\"\n| eval last_detection=strftime(last_detection_time, \"%Y-%m-%d %H:%M:%S\")\n| where leak_events > 0\n| sort - last_detection_time",
  "description": "Detects UC-14.1.18 — Water, Leak, Detection, and, Flood — using `index=meraki` sourcetype `meraki:sensorreadingshistory`, grouped by `serial`, `\"network.name\"`. Outputs `count` each `the scheduled window`; alert when `leak_events` > 0. Primary focus: (Meraki MT). Purpose-built for 'Water Leak Detection and Flood Alerts (Meraki MT)' in Operations workflows.",
  "value": "UC-14.1.18 accelerates insight into (Meraki MT) ('Water Leak Detection and Flood Alerts (Meraki MT)') by tracking `activity` on `meraki:sensorreadingshistory` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Configure `cisco_meraki_sensor_readings_history` to poll for MT10/MT11/MT12 water sensors. Any event with `water.present=true` indicates a leak — alert immediately and dispatch facilities. Persistent positives may indicate a stuck sensor.",
  "visualization": "Leak alert dashboard; sensor location map; event timeline.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable (no CIM or Operational_Telemetry data model in this SPL).",
  "references": [
    {
      "title": "Splunkbase app 5580",
      "url": "https://splunkbase.splunk.com/app/5580",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Add-on for Cisco Meraki (Splunkbase 5631)",
      "url": "https://splunkbase.splunk.com/app/5631",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Cisco Security documentation",
      "url": "https://www.cisco.com/c/en/us/support/security/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `meraki` (UC-14.1.18)** — Planned work for 'Water Leak Detection and Flood Alerts (Meraki MT)' shifts `count` over `span=1h` while `serial`, `\"network.name\"` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `meraki:sensorreadingshistory`** — Scheduled automation elevates `\"network.name\"` without crossing `leak_events` > 0 for UC-14.1.18. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `serial`, `\"network.name\"`** — Health checks in `meraki` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `meraki:sensorreadingshistory`** — First 48h baseline for UC-14.1.18 on `serial` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `serial`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_meraki"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Cisco Meraki Add-on for Splunk` (Splunkbase 5580).\n• Ensure the following data sources are available: `index=meraki sourcetype=meraki:sensorreadingshistory metric=\"water\"` from the `cisco_meraki_sensor_readings_history` input in `Splunk_TA_cisco_meraki` (Splunkbase 5580). Key fields: `serial`, `network.name`, `metric`, `water.present` (boolean), `ts`..\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nConfigure `cisco_meraki_sensor_readings_history` to poll for MT10/MT11/MT12 water sensors. Any event with `water.present=true` indicates a leak — alert immediately and dispatch facilities. Persistent positives may indicate a stuck sensor.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=meraki sourcetype=\"meraki:sensorreadingshistory\" metric=\"water\"\n| rename \"water.present\" as water_present\n| where water_present=\"true\" OR water_present=1\n| stats count as leak_events, latest(_time) as last_detection_time by serial, \"network.name\"\n| eval last_detection=strftime(last_detection_time, \"%Y-%m-%d %H:%M:%S\")\n| where leak_events > 0\n| sort - last_detection_time\n```\n\nUnderstanding this SPL\n\n**Water Leak Detection and Flood Alerts (Meraki MT)** — Immediately detects water leaks to prevent equipment damage and business interruption.\n\nDocumented **Data sources**: `index=meraki sourcetype=meraki:sensorreadingshistory metric=\"water\"` from the `cisco_meraki_sensor_readings_history` input in `Splunk_TA_cisco_meraki` (Splunkbase 5580). Key fields: `serial`, `network.name`, `metric`, `water.present` (boolean), `ts`. **App/TA** (typical add-on context): `Cisco Meraki Add-on for Splunk` (Splunkbase 5580). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: meraki; **sourcetype**: meraki:sensorreadingshistory. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=meraki, sourcetype=\"meraki:sensorreadingshistory\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Renames fields with `rename` for clarity or joins.\n• Filters the current rows with `where water_present=\"true\" OR water_present=1` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by serial, \"network.name\"** so each row reflects one combination of those dimensions (useful for per-host, per-user, or per-entity comparisons for this use case).\n• `eval` defines or adjusts **last_detection** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where leak_events > 0` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Leak alert dashboard; sensor location map; event timeline.",
  "grandmaExplanation": "We watch how your building systems behave. We help you know if something is off with water use that might mean leaks or waste before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
