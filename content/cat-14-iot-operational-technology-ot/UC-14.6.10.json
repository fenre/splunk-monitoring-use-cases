{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.6.10",
  "title": "DNP3 Cold/Warm Restart Detection",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Fault",
    "Security",
    "Availability"
  ],
  "splunkPillar": "Security",
  "industry": "Energy and Utilities",
  "exclusions": "Covers the detection logic documented for 'DNP3 Cold/Warm Restart Detection' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_dnp3_cold_warm_restart_detection` (UC-14.6.10), SOC dashboard panel \"Operations — DNP3 Cold/Warm Restart Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.6.10 positive (DNP3, Cold, Warm, Restart, Detection): ingest synthetic events into `index=ot` sourcetype='zeek:dnp3:json' where `values` grouped by `source_h`, `destination_h` satisfies detection threshold for 'DNP3 Cold/Warm Restart Detection'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.6.10 negative (Restart Detection): ingest benign `ot` / `zeek:dnp3:json` events where `source_h`, `destination_h` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 0 confirms zero alert rows for 'DNP3 Cold/Warm Restart Detection'."
  },
  "dataSources": "`index=ot` `sourcetype=\"zeek:dnp3:json\"` (`function`, `object_type`, `source_h`, `destination_h`)",
  "app": "TA for Zeek (Splunkbase 5466), Corelight App for Splunk (Splunkbase 3884)",
  "spl": "index=ot sourcetype=\"zeek:dnp3:json\"\n| where match(_raw,\"(?i)COLD_RESTART|WARM_RESTART\") OR match(function,\"(?i)COLD_RESTART|WARM_RESTART\") OR match(object_type,\"(?i)COLD_RESTART|WARM_RESTART\")\n| stats earliest(_time) as evt_time values(function) as fn values(object_type) as ot by source_h destination_h\n| table evt_time source_h destination_h fn ot",
  "description": "Identifies 'DNP3 Cold/Warm Restart Detection' (UC-14.6.10) using `index=ot` sourcetype `zeek:dnp3:json` by aggregating `values` grouped by `source_h`, `destination_h` over `the scheduled window` and alerting when configured threshold. Keywords: DNP3, Cold, Warm, Restart, Detection. Focuses on Restart Detection behaviour so operations teams can triage UC-14.6.10 without ad-hoc log review.",
  "value": "UC-14.6.10 accelerates insight into Restart Detection ('DNP3 Cold/Warm Restart Detection') by tracking `activity` on `zeek:dnp3:json` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Capture DNP3 on links to critical RTUs via network tap; confirm field names (`function` vs `object_type`) against a sample capture. Alert any restart from non-master IPs or outside approved windows.",
  "visualization": "Timeline (restart events), Table (source, destination, function), Single value (restarts per day).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "TA for Zeek",
      "url": "https://splunkbase.splunk.com/app/5466",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Corelight App for Splunk",
      "url": "https://splunkbase.splunk.com/app/3884",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `ot` (UC-14.6.10)** — Planned work for 'DNP3 Cold/Warm Restart Detection' shifts `values` over `span=1h` while `source_h`, `destination_h` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `values` on `zeek:dnp3:json`** — Scheduled automation elevates `destination_h` without crossing configured threshold for UC-14.6.10. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `source_h`, `destination_h`** — Health checks in `ot` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `zeek:dnp3:json`** — First 48h baseline for UC-14.6.10 on `source_h` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `source_h`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: TA for Zeek (Splunkbase 5466), Corelight App for Splunk (Splunkbase 3884).\n• Ensure the following data sources are available: `index=ot` `sourcetype=\"zeek:dnp3:json\"` (`function`, `object_type`, `source_h`, `destination_h`).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nCapture DNP3 on links to critical RTUs via network tap; confirm field names (`function` vs `object_type`) against a sample capture. Alert any restart from non-master IPs or outside approved windows.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot sourcetype=\"zeek:dnp3:json\"\n| where match(_raw,\"(?i)COLD_RESTART|WARM_RESTART\") OR match(function,\"(?i)COLD_RESTART|WARM_RESTART\") OR match(object_type,\"(?i)COLD_RESTART|WARM_RESTART\")\n| stats earliest(_time) as evt_time values(function) as fn values(object_type) as ot by source_h destination_h\n| table evt_time source_h destination_h fn ot\n```\n\nUnderstanding this SPL\n\n**DNP3 Cold/Warm Restart Detection** — Restart commands to outstations reset application context and can interrupt protection; unexpected restarts may follow malware or operator error.\n\nDocumented **Data sources**: `index=ot` `sourcetype=\"zeek:dnp3:json\"` (`function`, `object_type`, `source_h`, `destination_h`). **App/TA** (typical add-on context): TA for Zeek (Splunkbase 5466), Corelight App for Splunk (Splunkbase 3884). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot; **sourcetype**: zeek:dnp3:json. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot, sourcetype=\"zeek:dnp3:json\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where match(_raw,\"(?i)COLD_RESTART|WARM_RESTART\") OR match(function,\"(?i)COLD_RESTART|WARM_RESTART\") OR match(object_type,\"…` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by source_h destination_h** so each row reflects one combination of those dimensions.\n• Pipeline stage (see **DNP3 Cold/Warm Restart Detection**): table evt_time source_h destination_h fn ot\n\nStep 3 — Validate\nRun the search for a short window and compare key fields with Zeek or Corelight protocol logs on the sensor for the same flow. Reconcile function codes, endpoints, and time order against a small known-good capture if needed.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Timeline (restart events), Table (source, destination, function), Single value (restarts per day).",
  "grandmaExplanation": "We use deep packet inspection to see cold and warm restarts in substation telecontrol so we can line up process events with changes.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
