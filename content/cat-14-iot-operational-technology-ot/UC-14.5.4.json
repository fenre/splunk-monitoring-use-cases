{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.5.4",
  "title": "OPC-UA Node Value Change Rate and Anomaly",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance",
    "Anomaly"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'OPC-UA Node Value Change Rate and Anomaly' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_opc_ua_node_value_change_rate_and_anomaly` (UC-14.5.4), SOC dashboard panel \"Operations — OPC-UA Node Value Change Rate and Anomaly\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=edge-hub-data` sourcetype='splunk_edge_hub_opcua' that satisfy the UC-14.5.4 SPL filters for 'OPC-UA Node Value Change Rate and Anomaly'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `edge-hub-data` / `splunk_edge_hub_opcua` events outside the detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-14.5.4."
  },
  "dataSources": "`index=edge-hub-data sourcetype=splunk_edge_hub_opcua` (node values) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Edge Hub (OPC-UA connector), Splunk OT Intelligence — Universal Forwarder (Splunkbase 617)",
  "spl": "index=edge-hub-data sourcetype=splunk_edge_hub_opcua\n| bin _time span=5m\n| stats count as sample_count, dc(node_id) as nodes_seen by host, _time\n| eventstats avg(sample_count) as avg_count, stdev(sample_count) as std_count by host\n| eval z = if(std_count>0, (sample_count-avg_count)/std_count, 0)\n| where abs(z) > 3\n| table host _time sample_count avg_count z",
  "description": "PLC tag value change rate and distribution indicate normal process behavior. Sudden change in rate or value distribution can signal sensor fault, process upset, or cyber event.",
  "value": "Early 'OPC-UA Node Value Change Rate and Anomaly' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Ingest OPC-UA node samples from Edge Hub. Compute per-host message rate (samples per 5 min). Baseline mean and stdev; alert when rate exceeds 3 standard deviations. Optionally run MLTK anomaly detection on critical tags.",
  "visualization": "Line chart (sample rate by host), Table (anomalous intervals), Single value (current rate vs baseline).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'OPC-UA Node Value Change Rate and Anomaly'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `edge-hub-data` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `ops_exception_register.csv` with entity, reason, valid_until.",
  "equipment": [
    "edge_hub",
    "opcua"
  ],
  "equipmentModels": [
    "edge_hub_generic",
    "opcua_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk Edge Hub",
    "Splunk OT Intelligence"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Edge Hub (OPC-UA connector), Splunk OT Intelligence.\n• Ensure the following data sources are available: `index=edge-hub-data sourcetype=splunk_edge_hub_opcua` (node values).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest OPC-UA node samples from Edge Hub. Compute per-host message rate (samples per 5 min). Baseline mean and stdev; alert when rate exceeds 3 standard deviations. Optionally run MLTK anomaly detection on critical tags.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=edge-hub-data sourcetype=splunk_edge_hub_opcua\n| bin _time span=5m\n| stats count as sample_count, dc(node_id) as nodes_seen by host, _time\n| eventstats avg(sample_count) as avg_count, stdev(sample_count) as std_count by host\n| eval z = if(std_count>0, (sample_count-avg_count)/std_count, 0)\n| where abs(z) > 3\n| table host _time sample_count avg_count z\n```\n\nUnderstanding this SPL\n\n**OPC-UA Node Value Change Rate and Anomaly** — PLC tag value change rate and distribution indicate normal process behavior. Sudden change in rate or value distribution can signal sensor fault, process upset, or cyber event.\n\nDocumented **Data sources**: `index=edge-hub-data sourcetype=splunk_edge_hub_opcua` (node values). **App/TA** (typical add-on context): Splunk Edge Hub (OPC-UA connector), Splunk OT Intelligence. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: edge-hub-data; **sourcetype**: splunk_edge_hub_opcua. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=edge-hub-data, sourcetype=splunk_edge_hub_opcua. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by host, _time** so each row reflects one combination of those dimensions.\n• `eventstats` rolls up events into metrics; results are split **by host** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **z** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where abs(z) > 3` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **OPC-UA Node Value Change Rate and Anomaly**): table host _time sample_count avg_count z\n\nStep 3 — Validate\nRun the search for a time range where you know sessions were active, then compare the same nodes and clients in an industrial client such as UA Expert. Spot-check certificate and session fields against the server.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (sample rate by host), Table (anomalous intervals), Single value (current rate vs baseline).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Edge Hub (OPC-UA connector), Splunk OT Intelligence — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=splunk_edge_hub_opcua into edge-hub-data. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, host, std_count, z`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch how fast tag values change so unusual spikes or flatlines stand out from normal production swings.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
