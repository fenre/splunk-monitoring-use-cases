{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.1.29",
  "title": "HVAC Setpoint Override Frequency and Duration",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'HVAC Setpoint Override Frequency and Duration' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_hvac_setpoint_override_frequency_and_duration` (UC-14.1.29), SOC dashboard panel \"Operations — HVAC Setpoint Override Frequency and Duration\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.1.29 positive (HVAC, Setpoint, Override, Frequency, and): ingest synthetic events into `index=building` sourcetype='bms:hvac' where `avg_duration`, `total_override_hrs`, `zones_affected` grouped by `building`, `_time` satisfies `override_count` > 5 for 'HVAC Setpoint Override Frequency and Duration'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.1.29 negative (and Duration): ingest benign `building` / `bms:hvac` events where `building`, `_time` stays below `override_count` > 5, or exclude the test entity via `ops_exception_register.csv`. Variant 14 confirms zero alert rows for 'HVAC Setpoint Override Frequency and Duration'."
  },
  "dataSources": "`sourcetype=bms:hvac`, `sourcetype=bms:bacnet` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "BMS vendor API (Honeywell EBI, Siemens Desigo, Johnson Controls Metasys), BACnet gateway — Universal Forwarder (Splunkbase 617)",
  "spl": "index=building sourcetype=\"bms:hvac\" event_type=\"setpoint_override\"\n| eval override_duration_hrs=round(override_duration_min/60, 1)\n| bin _time span=1d\n| stats count as override_count, avg(override_duration_hrs) as avg_duration, sum(override_duration_hrs) as total_override_hrs, dc(zone_name) as zones_affected by building, _time\n| where override_count > 5\n| table _time, building, override_count, zones_affected, avg_duration, total_override_hrs",
  "description": "Alerts on UC-14.1.29 — HVAC, Setpoint, Override, Frequency, and — using `index=building` sourcetype `bms:hvac`, grouped by `building`, `_time`. Outputs `avg_duration`, `total_override_hrs`, `zones_affected` each `1d`; alert when `override_count` > 5. Primary focus: and Duration. Purpose-built for 'HVAC Setpoint Override Frequency and Duration' in Operations workflows.",
  "value": "Alerting on HVAC, Setpoint, Override, Frequency, and through UC-14.1.29 reduces operational risk and delayed incident response by surfacing `avg_duration`, `total_override_hrs`, `zones_affected` early.",
  "implementation": "Capture BMS override events with zone, user, original setpoint, override value, and duration. Track daily override frequency. Alert on zones with excessive overrides.",
  "visualization": "Override count heatmap by zone and time; daily override trending; top override zones table.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable (no CIM or Operational_Telemetry data model in this SPL).",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `building` (UC-14.1.29)** — Planned work for 'HVAC Setpoint Override Frequency and Duration' shifts `avg_duration`, `total_override_hrs`, `zones_affected` over `span=1d` while `building`, `_time` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `bms:hvac`** — Scheduled automation elevates `_time` without crossing `override_count` > 5 for UC-14.1.29. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `building`, `_time`** — Health checks in `building` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `bms:hvac`** — First 48h baseline for UC-14.1.29 on `building` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `building`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: BMS vendor API (Honeywell EBI, Siemens Desigo, Johnson Controls Metasys), BACnet gateway.\n• Ensure the following data sources are available: `sourcetype=bms:hvac`, `sourcetype=bms:bacnet`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nCapture BMS override events with zone, user, original setpoint, override value, and duration. Track daily override frequency. Alert on zones with excessive overrides.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=building sourcetype=\"bms:hvac\" event_type=\"setpoint_override\"\n| eval override_duration_hrs=round(override_duration_min/60, 1)\n| bin _time span=1d\n| stats count as override_count, avg(override_duration_hrs) as avg_duration, sum(override_duration_hrs) as total_override_hrs, dc(zone_name) as zones_affected by building, _time\n| where override_count > 5\n| table _time, building, override_count, zones_affected, avg_duration, total_override_hrs\n```\n\nUnderstanding this SPL\n\n**HVAC Setpoint Override Frequency and Duration** — Frequent manual setpoint overrides by occupants or operators indicate either comfort issues or poor control tuning. Tracking override patterns helps identify problem zones and quantifies energy waste from overrides that persist beyond work hours.\n\nDocumented **Data sources**: `sourcetype=bms:hvac`, `sourcetype=bms:bacnet`. **App/TA** (typical add-on context): BMS vendor API (Honeywell EBI, Siemens Desigo, Johnson Controls Metasys), BACnet gateway. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: building; **sourcetype**: bms:hvac. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=building, sourcetype=\"bms:hvac\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **override_duration_hrs** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by building, _time** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where override_count > 5` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **HVAC Setpoint Override Frequency and Duration**): table _time, building, override_count, zones_affected, avg_duration, total_override_hrs\n\n\nStep 3 — Validate\nRun the search in a known window. Spot-check a few events against the BMS console (for example Niagara, Tridium, or Honeywell) and confirm zones, values, and alarms line up. Verify field names and that the right roles can search the index.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Override count heatmap by zone and time; daily override trending; top override zones table.",
  "grandmaExplanation": "We watch how your building systems behave. We help you know if something is off with heating, cooling, and room comfort before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
