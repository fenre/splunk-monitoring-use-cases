{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.1.21",
  "title": "Ambient Noise Level Monitoring and Trend Analysis (Meraki MT)",
  "criticality": "low",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=meraki sourcetype=meraki:sensorreadingshistory metric=\"noise\"` from the `cisco_meraki_sensor_readings_history` input in `Splunk_TA_cisco_meraki` (Splunkbase 5580). Key fields: `serial`, `network.name`, `metric`, `noise.ambient.level` (dBA), `ts`.",
  "app": "`Cisco Meraki Add-on for Splunk` (Splunkbase 5580)",
  "spl": "index=meraki sourcetype=\"meraki:sensorreadingshistory\" metric=\"noise\"\n| rename \"noise.ambient.level\" as noise_db\n| timechart span=15m avg(noise_db) as avg_noise_db, max(noise_db) as peak_noise_db by serial limit=10",
  "description": "Tracks noise levels to ensure comfortable working environment and detect anomalies.",
  "value": "Tracks noise levels to ensure comfortable working environment and detect anomalies.",
  "implementation": "Configure `cisco_meraki_sensor_readings_history` for MT15/MT20 sensors that include the ambient noise mic. Use `timechart` for trend visualisation. WHO recommends keeping office ambient noise below 55 dBA.",
  "visualization": "Noise level gauge; time-of-day heat map; location comparison chart.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable (no CIM or Operational_Telemetry data model in this SPL).",
  "references": [
    {
      "title": "Splunkbase app 5580",
      "url": "https://splunkbase.splunk.com/app/5580"
    }
  ],
  "knownFalsePositives": "Benign events during building tests, after-hours schedules, and vendor work—confirm against the BMS console before urgent response.",
  "equipment": [
    "cisco"
  ],
  "equipmentModels": [
    "cisco_meraki"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Cisco Meraki Add-on for Splunk` (Splunkbase 5580).\n• Ensure the following data sources are available: `index=meraki sourcetype=meraki:sensorreadingshistory metric=\"noise\"` from the `cisco_meraki_sensor_readings_history` input in `Splunk_TA_cisco_meraki` (Splunkbase 5580). Key fields: `serial`, `network.name`, `metric`, `noise.ambient.level` (dBA), `ts`..\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nConfigure `cisco_meraki_sensor_readings_history` for MT15/MT20 sensors that include the ambient noise mic. Use `timechart` for trend visualisation. WHO recommends keeping office ambient noise below 55 dBA.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=meraki sourcetype=\"meraki:sensorreadingshistory\" metric=\"noise\"\n| rename \"noise.ambient.level\" as noise_db\n| timechart span=15m avg(noise_db) as avg_noise_db, max(noise_db) as peak_noise_db by serial limit=10\n```\n\nUnderstanding this SPL\n\n**Ambient Noise Level Monitoring and Trend Analysis (Meraki MT)** — Tracks noise levels to ensure comfortable working environment and detect anomalies.\n\nDocumented **Data sources**: `index=meraki sourcetype=meraki:sensorreadingshistory metric=\"noise\"` from the `cisco_meraki_sensor_readings_history` input in `Splunk_TA_cisco_meraki` (Splunkbase 5580). Key fields: `serial`, `network.name`, `metric`, `noise.ambient.level` (dBA), `ts`. **App/TA** (typical add-on context): `Cisco Meraki Add-on for Splunk` (Splunkbase 5580). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: meraki; **sourcetype**: meraki:sensorreadingshistory. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=meraki, sourcetype=\"meraki:sensorreadingshistory\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Renames fields with `rename` for clarity or joins.\n• `timechart` plots the metric over time using **span=15m** buckets with a separate series **by serial limit=10** — ideal for trending and alerting on this use case.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Noise level gauge; time-of-day heat map; location comparison chart.",
  "grandmaExplanation": "We watch how your building systems behave. We help you know if something is off with this area: ambient noise level monitoring and trend analysis before harm, damage, or wasted effort piles up."
}
