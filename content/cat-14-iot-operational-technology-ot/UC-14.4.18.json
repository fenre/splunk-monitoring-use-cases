{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.4.18",
  "title": "IoT Device Provisioning Audit",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'IoT Device Provisioning Audit' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_iot_device_provisioning_audit` (UC-14.4.18), SOC dashboard panel \"Operations — IoT Device Provisioning Audit\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=audit` sourcetype='iot:provisioning' where `actor` satisfies `count` > 50 for UC-14.4.18 ('IoT Device Provisioning Audit'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `audit` / `iot:provisioning` events where `actor` stays below `count` > 50, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-14.4.18."
  },
  "dataSources": "AWS CloudTrail (`sourcetype=\"aws:cloudtrail\"`; filter `eventSource` / `eventName` for IoT control-plane APIs such as `iot.amazonaws.com`), or a custom `iot:provisioning` sourcetype from your pipeline. Collected via Splunk Add-on for Amazon Web Services (Splunkbase 1876) modular input or scripted poll into the index named in the SPL.",
  "app": "AWS IoT Fleet / Azure DPS audit — Splunk Add-on for Amazon Web Services (Splunkbase 1876)",
  "spl": "index=audit sourcetype=\"iot:provisioning\"\n| where action IN (\"RegisterThing\",\"CreateCertificate\",\"AttachPolicy\")\n| stats count by actor, device_template, src\n| where count > 50\n| sort -count",
  "description": "Unauthorized provisioning events create shadow devices and billing abuse. This use case monitors the condition described in the title using the SPL and data sources documented on this page.",
  "value": "Early 'IoT Device Provisioning Audit' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Compare provisioning rate to approved baseline. Alert on new template or IAM principal. Cross-check with HR for contractor access. For AWS, prefer CloudTrail (`sourcetype=\"aws:cloudtrail\"`) with `eventSource=\"iot.amazonaws.com\"` (and relevant `eventName` values) instead of a non-standard `cloudtrail:iot` sourcetype; keep `iot:provisioning` only if that is your normalized pipeline.",
  "visualization": "Table (provisioning events), Timeline (bursts), Bar chart (by actor).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable (no CIM or Operational_Telemetry data model in this SPL).",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 1876",
      "url": "https://splunkbase.splunk.com/app/1876",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `audit`** — Planned work for 'IoT Device Provisioning Audit' shifts `count` over `span=1h` while `actor` remains in-scope for UC-14.4.18. Cross-check CMDB CHG.\n\n2. **Batch `count` on `iot:provisioning`** — Scheduled automation elevates `device_template` without crossing `count` > 50. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `actor`** — Health checks in `audit` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `iot:provisioning`** — First 48h baseline for `actor` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `actor`, `exception_reason`, `valid_until`.",
  "equipment": [
    "aws",
    "azure"
  ],
  "equipmentModels": [
    "aws_generic",
    "azure_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: AWS IoT Fleet / Azure DPS audit.\n• Ensure the following data sources are available: AWS CloudTrail (`sourcetype=\"aws:cloudtrail\"`; filter `eventSource` / `eventName` for IoT control-plane APIs such as `iot.amazonaws.com`), or a custom `iot:provisioning` sourcetype from your pipeline..\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nCompare provisioning rate to approved baseline. Alert on new template or IAM principal. Cross-check with HR for contractor access. For AWS, prefer CloudTrail (`sourcetype=\"aws:cloudtrail\"`) with `eventSource=\"iot.amazonaws.com\"` (and relevant `eventName` values) instead of a non-standard `cloudtrail:iot` sourcetype; keep `iot:provisioning` only if that is your normalized pipeline.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=audit sourcetype=\"iot:provisioning\"\n| where action IN (\"RegisterThing\",\"CreateCertificate\",\"AttachPolicy\")\n| stats count by actor, device_template, src\n| where count > 50\n| sort -count\n```\n\nUnderstanding this SPL\n\n**IoT Device Provisioning Audit** — Unauthorized provisioning events create shadow devices and billing abuse.\n\nDocumented **Data sources**: AWS CloudTrail (`sourcetype=\"aws:cloudtrail\"`; filter `eventSource` / `eventName` for IoT control-plane APIs such as `iot.amazonaws.com`), or a custom `iot:provisioning` sourcetype from your pipeline. **App/TA** (typical add-on context): AWS IoT Fleet / Azure DPS audit. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: audit; **sourcetype**: iot:provisioning. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=audit, sourcetype=\"iot:provisioning\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where action IN (\"RegisterThing\",\"CreateCertificate\",\"AttachPolicy\")` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by actor, device_template, src** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where count > 50` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nCompare device counts, last-seen times, and health fields to the IoT platform console (for example AWS IoT, Azure IoT Hub, or Google Cloud IoT) for the same period. Verify API polling intervals and that offline rules match your operational definition.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (provisioning events), Timeline (bursts), Bar chart (by actor).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (AWS IoT Fleet / Azure DPS audit — Splunk Add-on for Amazon Web Services (Splunkbase 1876)). Enable the modular input or HEC token that writes sourcetype=iot:provisioning into audit. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`actor, device_template, src`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch your connected devices and the messages they send. We help you know if something is off with this area: iot device provisioning audit before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
