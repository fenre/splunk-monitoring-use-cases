{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.2.7",
  "title": "Modbus TCP Anomaly Detection",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Anomaly"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Modbus TCP Anomaly Detection' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_modbus_tcp_anomaly_detection` (UC-14.2.7), SOC dashboard panel \"Operations — Modbus TCP Anomaly Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=ot` sourcetype='modbus:traffic' where `_time` satisfies `count` > 100 for UC-14.2.7 ('Modbus TCP Anomaly Detection'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `ot` / `modbus:traffic` events where `_time` stays below `count` > 100, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-14.2.7."
  },
  "dataSources": "`sourcetype=\"modbus:traffic\"` or `modbus:gateway` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk OT Intelligence, Modbus TA — Universal Forwarder (Splunkbase 617)",
  "spl": "index=ot sourcetype=\"modbus:traffic\"\n| bin _time span=5m\n| stats count by _time, unit_id, function_code, src\n| eventstats median(count) as med by unit_id, function_code\n| where count > med * 5 AND count > 100",
  "description": "Unusual read/write rates or register ranges can indicate process upset or malicious manipulation. Searches `index=ot` with sourcetype `modbus:traffic` for UC-14.2.7, aggregating `median` by `_time` over `5m`. Alerts when grouped results cross `count` > 100 in the saved search. Gives Operations operators a repeatable signal for 'Modbus TCP Anomaly Detection' without manual log review.",
  "value": "Early 'Modbus TCP Anomaly Detection' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Baseline requests per 5m per unit and function code. Use MLTK or `eventstats` for median. Alert on spikes without corresponding maintenance window.",
  "visualization": "Line chart (Modbus req rate), Table (anomalous unit × function), Heatmap (time × unit).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable (no CIM or Operational_Telemetry data model in this SPL).",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `ot`** — Planned work for 'Modbus TCP Anomaly Detection' shifts `median` over `span=5m` while `_time` remains in-scope for UC-14.2.7. Cross-check CMDB CHG.\n\n2. **Batch `median` on `modbus:traffic`** — Scheduled automation elevates `unit_id` without crossing `count` > 100. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `_time`** — Health checks in `ot` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `modbus:traffic`** — First 48h baseline for `_time` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `_time`, `exception_reason`, `valid_until`.",
  "equipment": [
    "modbus"
  ],
  "equipmentModels": [
    "modbus_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk OT Intelligence"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk OT Intelligence, Modbus TA.\n• Ensure the following data sources are available: `sourcetype=\"modbus:traffic\"` or `modbus:gateway`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nBaseline requests per 5m per unit and function code. Use MLTK or `eventstats` for median. Alert on spikes without corresponding maintenance window.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot sourcetype=\"modbus:traffic\"\n| bin _time span=5m\n| stats count by _time, unit_id, function_code, src\n| eventstats median(count) as med by unit_id, function_code\n| where count > med * 5 AND count > 100\n```\n\nUnderstanding this SPL\n\n**Modbus TCP Anomaly Detection** — Unusual read/write rates or register ranges can indicate process upset or malicious manipulation.\n\nDocumented **Data sources**: `sourcetype=\"modbus:traffic\"` or `modbus:gateway`. **App/TA** (typical add-on context): Splunk OT Intelligence, Modbus TA. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot; **sourcetype**: modbus:traffic. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot, sourcetype=\"modbus:traffic\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by _time, unit_id, function_code, src** so each row reflects one combination of those dimensions.\n• `eventstats` rolls up events into metrics; results are split **by unit_id, function_code** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where count > med * 5 AND count > 100` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nRun the search when the process is in a known state. Compare key fields and alarm states to the SCADA or DCS HMI and, where relevant, the engineering tool for the PLC. Confirm sourcetypes and field extractions match the vendor feed.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (Modbus req rate), Table (anomalous unit × function), Heatmap (time × unit).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk OT Intelligence, Modbus TA — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=modbus:traffic into ot. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, function_code, src, unit_id`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch your plant control systems and field data. We help you know if something is off with read and write traffic on the plant data link before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
