{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.1.12",
  "title": "Firmware Version Compliance Across Fleet",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Firmware Version Compliance Across Fleet' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_firmware_version_compliance_across_fleet` (UC-14.1.12), SOC dashboard panel \"Operations — Firmware Version Compliance Across Fleet\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.1.12 positive (Firmware, Version, Compliance, Across, Fleet): ingest synthetic events into `index=network` sourcetype='snmp:inventory' where `count` grouped by `device_name`, `model` satisfies detection threshold for 'Firmware Version Compliance Across Fleet'. Validate lookup `approved_network_firmware.csv`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.1.12 negative (Across Fleet): ingest benign `network` / `snmp:inventory` events where `device_name`, `model` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 0 confirms zero alert rows for 'Firmware Version Compliance Across Fleet'."
  },
  "dataSources": "`sourcetype=\"snmp:inventory\"` (sysDescr, entPhysicalFirmwareRev) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "SNMP TA (ENTITY-MIB, vendor OIDs), SolarWinds/Prime export — Universal Forwarder (Splunkbase 617)",
  "spl": "index=network sourcetype=\"snmp:inventory\"\n| stats latest(firmware_version) as fw by device_name, model\n| lookup approved_network_firmware.csv model OUTPUT approved_fw\n| where fw!=approved_fw\n| table device_name, model, fw, approved_fw",
  "description": "Monitors 'Firmware Version Compliance Across Fleet' (UC-14.1.12) using `index=network` sourcetype `snmp:inventory` via lookup `approved_network_firmware.csv` by aggregating `count` grouped by `device_name`, `model` over `the scheduled window` and alerting when configured threshold. Keywords: Firmware, Version, Compliance, Across, Fleet. Focuses on Across Fleet behaviour so operations teams can triage UC-14.1.12 without ad-hoc log review.",
  "value": "UC-14.1.12 accelerates insight into Across Fleet ('Firmware Version Compliance Across Fleet') by tracking `activity` on `snmp:inventory` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Poll ENTITY-MIB / vendor firmware revision OIDs on a weekly schedule. Maintain CSV of approved builds per platform. Drive remediation tickets for non-compliant devices.",
  "visualization": "Table (non-compliant devices), Pie chart (compliance %), Bar chart (by site).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable (no CIM or Operational_Telemetry data model in this SPL).",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Cisco Security documentation",
      "url": "https://www.cisco.com/c/en/us/support/security/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `network` (UC-14.1.12)** — Planned work for 'Firmware Version Compliance Across Fleet' shifts `count` over `span=1h` while `device_name`, `model` remains in-scope. Applies lookup `approved_network_firmware.csv`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `snmp:inventory`** — Scheduled automation elevates `model` without crossing configured threshold for UC-14.1.12. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `device_name`, `model`** — Health checks in `network` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `snmp:inventory`** — First 48h baseline for UC-14.1.12 on `device_name` is unstable; join `approved_network_firmware.csv` prior to threshold.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `device_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco",
    "snmp"
  ],
  "equipmentModels": [
    "cisco_nexus",
    "snmp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: SNMP TA (ENTITY-MIB, vendor OIDs), SolarWinds/Prime export.\n• Ensure the following data sources are available: `sourcetype=\"snmp:inventory\"` (sysDescr, entPhysicalFirmwareRev).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nPoll ENTITY-MIB / vendor firmware revision OIDs on a weekly schedule. Maintain CSV of approved builds per platform. Drive remediation tickets for non-compliant devices.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=network sourcetype=\"snmp:inventory\"\n| stats latest(firmware_version) as fw by device_name, model\n| lookup approved_network_firmware.csv model OUTPUT approved_fw\n| where fw!=approved_fw\n| table device_name, model, fw, approved_fw\n```\n\nUnderstanding this SPL\n\n**Firmware Version Compliance Across Fleet** — Known-bad IOS/NX-OS/IOS-XE builds expose the network to CVEs; compliance reporting supports change windows.\n\nDocumented **Data sources**: `sourcetype=\"snmp:inventory\"` (sysDescr, entPhysicalFirmwareRev). **App/TA** (typical add-on context): SNMP TA (ENTITY-MIB, vendor OIDs), SolarWinds/Prime export. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: network; **sourcetype**: snmp:inventory. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=network, sourcetype=\"snmp:inventory\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by device_name, model** so each row reflects one combination of those dimensions.\n• Enriches events using `lookup` (lookup definition + optional OUTPUT fields).\n• Filters the current rows with `where fw!=approved_fw` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **Firmware Version Compliance Across Fleet**): table device_name, model, fw, approved_fw\n\n\nStep 3 — Validate\nRun the search in a known window. Spot-check a few events against the BMS console (for example Niagara, Tridium, or Honeywell) and confirm zones, values, and alarms line up. Verify field names and that the right roles can search the index.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (non-compliant devices), Pie chart (compliance %), Bar chart (by site).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (SNMP TA (ENTITY-MIB, vendor OIDs), SolarWinds/Prime export — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=snmp:inventory into network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`approved_fw, device_name, model`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch how your building systems behave. We help you know if something is off with room conditions your rules and equipment need before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
