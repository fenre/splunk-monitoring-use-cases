{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.1.20",
  "title": "Air Quality and CO2 Monitoring (Meraki MT)",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'Air Quality and CO2 Monitoring (Meraki MT)'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-14.1.20).",
  "evidence": "Saved search uc_14_1_20_air_quality_and_co2_monitoring_meraki_mt, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'Air Quality and CO2 Monitoring (Meraki MT)'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-14.1.20.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "`index=meraki sourcetype=meraki:sensorreadingshistory metric=\"co2\"` from the `cisco_meraki_sensor_readings_history` input in `Splunk_TA_cisco_meraki` (Splunkbase 5580). Key fields: `serial`, `network.name`, `metric`, `co2.concentration` (ppm), `ts`. For broader IAQ also consider `metric=\"tvoc\"`, `metric=\"pm25\"`, and `metric=\"indoorAirQuality\"`.",
  "app": "`Cisco Meraki Add-on for Splunk` (Splunkbase 5580)",
  "spl": "index=meraki sourcetype=\"meraki:sensorreadingshistory\" metric=\"co2\"\n| rename \"co2.concentration\" as co2_ppm\n| stats latest(co2_ppm) as current_co2_ppm, avg(co2_ppm) as avg_co2_ppm by serial, \"network.name\"\n| where current_co2_ppm > 1000\n| sort - current_co2_ppm",
  "description": "Monitors indoor air quality to ensure safe working conditions. This use case monitors the condition described in the title using the SPL and data sources documented on this page.",
  "value": "Operations and leadership rely on 'Air Quality and CO2 Monitoring (Meraki MT)' to catch regressions before they become customer-visible outages or audit findings. Early signal reduces mean time to repair, avoids duplicate manual log review, and preserves evidence for post-incident review without waiting for a user-reported ticket.",
  "implementation": "Configure `cisco_meraki_sensor_readings_history` for MT15 (IAQ) sensors. ASHRAE recommends keeping CO₂ below 1000 ppm in occupied spaces; values above 1500 ppm correlate with reduced cognitive performance. For full IAQ, combine with TVOC, PM2.5, and the composite `indoorAirQuality.score`.",
  "visualization": "CO2 level gauge per location; trend timeline; air quality status chart.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable (no CIM or Operational_Telemetry data model in this SPL).",
  "references": [
    {
      "title": "Splunkbase app 5580",
      "url": "https://splunkbase.splunk.com/app/5580",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "cisco",
    "m365"
  ],
  "equipmentModels": [
    "cisco_meraki"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Cisco Meraki Add-on for Splunk` (Splunkbase 5580).\n• Ensure the following data sources are available: `index=meraki sourcetype=meraki:sensorreadingshistory metric=\"co2\"` from the `cisco_meraki_sensor_readings_history` input in `Splunk_TA_cisco_meraki` (Splunkbase 5580). Key fields: `serial`, `network.name`, `metric`, `co2.concentration` (ppm), `ts`. For broader IAQ also consider `metric=\"tvoc\"`, `metric=\"pm25\"`, and `metric=\"indoorAirQuality\"`..\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nConfigure `cisco_meraki_sensor_readings_history` for MT15 (IAQ) sensors. ASHRAE recommends keeping CO₂ below 1000 ppm in occupied spaces; values above 1500 ppm correlate with reduced cognitive performance. For full IAQ, combine with TVOC, PM2.5, and the composite `indoorAirQuality.score`.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=meraki sourcetype=\"meraki:sensorreadingshistory\" metric=\"co2\"\n| rename \"co2.concentration\" as co2_ppm\n| stats latest(co2_ppm) as current_co2_ppm, avg(co2_ppm) as avg_co2_ppm by serial, \"network.name\"\n| where current_co2_ppm > 1000\n| sort - current_co2_ppm\n```\n\nUnderstanding this SPL\n\n**Air Quality and CO2 Monitoring (Meraki MT)** — Monitors indoor air quality to ensure safe working conditions.\n\nDocumented **Data sources**: `index=meraki sourcetype=meraki:sensorreadingshistory metric=\"co2\"` from the `cisco_meraki_sensor_readings_history` input in `Splunk_TA_cisco_meraki` (Splunkbase 5580). Key fields: `serial`, `network.name`, `metric`, `co2.concentration` (ppm), `ts`. For broader IAQ also consider `metric=\"tvoc\"`, `metric=\"pm25\"`, and `metric=\"indoorAirQuality\"`. **App/TA** (typical add-on context): `Cisco Meraki Add-on for Splunk` (Splunkbase 5580). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: meraki; **sourcetype**: meraki:sensorreadingshistory. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=meraki, sourcetype=\"meraki:sensorreadingshistory\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Renames fields with `rename` for clarity or joins.\n• `stats` rolls up events into metrics; results are split **by serial, \"network.name\"** so each row reflects one combination of those dimensions (useful for per-host, per-user, or per-entity comparisons for this use case).\n• Filters the current rows with `where current_co2_ppm > 1000` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: CO2 level gauge per location; trend timeline; air quality status chart.",
  "grandmaExplanation": "We watch how your building systems behave. We help you know if something is off with stale air, dust, and indoor comfort before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
