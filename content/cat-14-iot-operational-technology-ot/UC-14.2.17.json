{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.2.17",
  "title": "ICS Network Segmentation Violations",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'ICS Network Segmentation Violations' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_ics_network_segmentation_violations` (UC-14.2.17), SOC dashboard panel \"Operations — ICS Network Segmentation Violations\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.2.17 positive (ICS, Network, Segmentation, Violations): ingest synthetic events into `index=network` sourcetype='flow:ics' where `count` grouped by `src`, `dest`, `dest_port`, `app` satisfies detection threshold for 'ICS Network Segmentation Violations'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.2.17 negative (Segmentation Violations): ingest benign `network` / `flow:ics` events where `src`, `dest`, `dest_port`, `app` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 2 confirms zero alert rows for 'ICS Network Segmentation Violations'."
  },
  "dataSources": "`sourcetype=\"pan:traffic\"` `zone_pair` or `sourcetype=\"flow:ics\"` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Firewall, switch NetFlow, ARP tables — Universal Forwarder (Splunkbase 617)",
  "spl": "index=network sourcetype=\"flow:ics\"\n| where src_zone!=dest_zone AND allowed_pair=\"false\"\n| stats count by src, dest, dest_port, app\n| sort -count",
  "description": "Identifies UC-14.2.17 for ICS, Network, Segmentation, Violations over `index=network` sourcetype `flow:ics` with thresholds configured threshold. Groups `count` by `src`, `dest`, `dest_port`, `app` each `the scheduled window`. Centered on Segmentation Violations indicators. Supports repeatable operations triage for 'ICS Network Segmentation Violations'.",
  "value": "For 'ICS Network Segmentation Violations', UC-14.2.17 converts `activity` on `flow:ics` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Maintain `allowed_pair` lookup for zone-to-zone. Alert on any deny or unexpected allow. Quarterly review.",
  "visualization": "Sankey (zones), Table (violations), Single value (open violations).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable (no CIM or Operational_Telemetry data model in this SPL).",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `network` (UC-14.2.17)** — Planned work for 'ICS Network Segmentation Violations' shifts `count` over `span=1h` while `src`, `dest`, `dest_port`, `app` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `flow:ics`** — Scheduled automation elevates `dest` without crossing configured threshold for UC-14.2.17. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `src`, `dest`, `dest_port`, `app`** — Health checks in `network` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `flow:ics`** — First 48h baseline for UC-14.2.17 on `src` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `src`, `exception_reason`, `valid_until`.",
  "equipment": [
    "netflow"
  ],
  "equipmentModels": [
    "netflow_netflow"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Firewall, switch NetFlow, ARP tables.\n• Ensure the following data sources are available: `sourcetype=\"pan:traffic\"` `zone_pair` or `sourcetype=\"flow:ics\"`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMaintain `allowed_pair` lookup for zone-to-zone. Alert on any deny or unexpected allow. Quarterly review.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=network sourcetype=\"flow:ics\"\n| where src_zone!=dest_zone AND allowed_pair=\"false\"\n| stats count by src, dest, dest_port, app\n| sort -count\n```\n\nUnderstanding this SPL\n\n**ICS Network Segmentation Violations** — Hosts in wrong VLANs or east-west traffic across zones violates Purdue model and IEC 62443.\n\nDocumented **Data sources**: `sourcetype=\"pan:traffic\"` `zone_pair` or `sourcetype=\"flow:ics\"`. **App/TA** (typical add-on context): Firewall, switch NetFlow, ARP tables. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: network; **sourcetype**: flow:ics. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=network, sourcetype=\"flow:ics\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where src_zone!=dest_zone AND allowed_pair=\"false\"` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by src, dest, dest_port, app** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nRun the search when the process is in a known state. Compare key fields and alarm states to the SCADA or DCS HMI and, where relevant, the engineering tool for the PLC. Confirm sourcetypes and field extractions match the vendor feed.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Sankey (zones), Table (violations), Single value (open violations).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Firewall, switch NetFlow, ARP tables — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=flow:ics into network. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`app, dest, dest_port, src`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch your plant control systems and field data. We help you know if something is off with this area: ics network segmentation violations before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
