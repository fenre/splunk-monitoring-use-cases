{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.1.45",
  "title": "BACnet Controller Communication Health",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'BACnet Controller Communication Health' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_bacnet_controller_communication_health` (UC-14.1.45), SOC dashboard panel \"Operations — BACnet Controller Communication Health\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.1.45 positive (BACnet, Controller, Communication, Health): ingest synthetic events into `index=building` sourcetype='bms:bacnet' where `count` grouped by `device_id`, `device_name`, `network_id`, `location` satisfies `hours_since_seen` > 1, or `offline_events` > 3 for 'BACnet Controller Communication Health'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.1.45 negative (Communication Health): ingest benign `building` / `bms:bacnet` events where `device_id`, `device_name`, `network_id`, `location` stays below `hours_since_seen` > 1, or `offline_events` > 3, or exclude the test entity via `ops_exception_register.csv`. Variant 4 confirms zero alert rows for 'BACnet Controller Communication Health'."
  },
  "dataSources": "`sourcetype=bms:bacnet` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "BACnet network scanner, BMS supervisory controller, SNMP — Universal Forwarder (Splunkbase 617)",
  "spl": "index=building sourcetype=\"bms:bacnet\" event_type=\"device_status\"\n| stats latest(status) as current_status, latest(_time) as last_seen, count(eval(status=\"offline\")) as offline_events by device_id, device_name, network_id, location\n| eval hours_since_seen=round((now()-last_seen)/3600, 1)\n| eval health=case(\n    current_status=\"offline\" OR hours_since_seen > 1, \"OFFLINE\",\n    offline_events > 3, \"Unstable\",\n    1=1, \"Online\")\n| where health!=\"Online\"\n| table device_id, device_name, location, network_id, health, hours_since_seen, offline_events\n| sort health, -hours_since_seen",
  "description": "Monitors risk tied to BACnet, Controller, Communication, Health through UC-14.1.45 on `index=building` sourcetype `bms:bacnet`. Summarises `count` grouped by `device_id`, `device_name`, `network_id`, `location` across `the scheduled window` before applying `hours_since_seen` > 1, or `offline_events` > 3. Operators tune 'BACnet Controller Communication Health' using UC-14.1.45 runbook steps.",
  "value": "Tracking `activity` via UC-14.1.45 on `bms:bacnet` feeds improves response to 'BACnet Controller Communication Health' and reduces operational risk and delayed incident response.",
  "implementation": "Periodically scan BACnet network for device presence using BACnet Who-Is/I-Am or poll device status from supervisory controller. Track response times and offline events. Alert on any controller going offline or showing unstable communication.",
  "visualization": "Controller status map; network health dashboard; offline device list; communication stability trend.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable (no CIM or Operational_Telemetry data model in this SPL).",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `building` (UC-14.1.45)** — Planned work for 'BACnet Controller Communication Health' shifts `count` over `span=1h` while `device_id`, `device_name`, `network_id`, `location` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `bms:bacnet`** — Scheduled automation elevates `device_name` without crossing `hours_since_seen` > 1 for UC-14.1.45. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `device_id`, `device_name`, `network_id`, `location`** — Health checks in `building` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `bms:bacnet`** — First 48h baseline for UC-14.1.45 on `device_id` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `device_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "snmp"
  ],
  "equipmentModels": [
    "snmp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: BACnet network scanner, BMS supervisory controller, SNMP.\n• Ensure the following data sources are available: `sourcetype=bms:bacnet`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nPeriodically scan BACnet network for device presence using BACnet Who-Is/I-Am or poll device status from supervisory controller. Track response times and offline events. Alert on any controller going offline or showing unstable communication.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=building sourcetype=\"bms:bacnet\" event_type=\"device_status\"\n| stats latest(status) as current_status, latest(_time) as last_seen, count(eval(status=\"offline\")) as offline_events by device_id, device_name, network_id, location\n| eval hours_since_seen=round((now()-last_seen)/3600, 1)\n| eval health=case(\n    current_status=\"offline\" OR hours_since_seen > 1, \"OFFLINE\",\n    offline_events > 3, \"Unstable\",\n    1=1, \"Online\")\n| where health!=\"Online\"\n| table device_id, device_name, location, network_id, health, hours_since_seen, offline_events\n| sort health, -hours_since_seen\n```\n\nUnderstanding this SPL\n\n**BACnet Controller Communication Health** — BACnet controllers are the brains of building automation — each manages a zone, floor, or system. Communication loss means affected areas run in fallback mode with no remote visibility or control. Monitoring network health catches issues before occupants notice.\n\nDocumented **Data sources**: `sourcetype=bms:bacnet`. **App/TA** (typical add-on context): BACnet network scanner, BMS supervisory controller, SNMP. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: building; **sourcetype**: bms:bacnet. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=building, sourcetype=\"bms:bacnet\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by device_id, device_name, network_id, location** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **hours_since_seen** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **health** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where health!=\"Online\"` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **BACnet Controller Communication Health**): table device_id, device_name, location, network_id, health, hours_since_seen, offline_events\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nRun the search in a known window. Spot-check a few events against the BMS console (for example Niagara, Tridium, or Honeywell) and confirm zones, values, and alarms line up. Verify field names and that the right roles can search the index.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Controller status map; network health dashboard; offline device list; communication stability trend.",
  "grandmaExplanation": "We watch how your building systems behave. We help you know if something is off with controllers and the data links the building team relies on before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
