{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.6.2",
  "title": "S7comm Program Upload/Download Detection",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Security",
    "Change",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "industry": "Manufacturing",
  "exclusions": "Covers the detection logic documented for 'S7comm Program Upload/Download Detection' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_s7comm_program_upload_download_detection` (UC-14.6.2), SOC dashboard panel \"Operations — S7comm Program Upload/Download Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.6.2 positive (S7comm, Program, Upload, Download, Detection): ingest synthetic events into `index=ot` sourcetype='zeek:s7comm:json' where `values` grouped by `source_h`, `destination_h`, `uid` satisfies detection threshold for 'S7comm Program Upload/Download Detection'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.6.2 negative (Upload/Download Detection): ingest benign `ot` / `zeek:s7comm:json` events where `source_h`, `destination_h`, `uid` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 3 confirms zero alert rows for 'S7comm Program Upload/Download Detection'."
  },
  "dataSources": "`index=ot` `sourcetype=\"zeek:s7comm:json\"` (ICSNPP `s7comm_upload_download.log`: `function_code`, `filename`, `block_type`, `block_number`, `source_h`, `destination_h`)",
  "app": "TA for Zeek (Splunkbase 5466), Corelight App for Splunk (Splunkbase 3884)",
  "spl": "index=ot sourcetype=\"zeek:s7comm:json\"\n| eval fc_hex=replace(function_code,\"0x\",\"\")\n| eval fc=coalesce(tonumber(fc_hex,16), tonumber(function_code))\n| where fc IN (26,27) OR function_code IN (\"0x1a\",\"0x1b\",\"26\",\"27\") OR isnotnull(filename)\n| stats earliest(_time) as first_seen latest(_time) as last_seen values(filename) as filenames values(block_type) as block_types values(block_number) as block_numbers by source_h destination_h uid\n| table first_seen last_seen source_h destination_h filenames block_types block_numbers",
  "description": "Detects when Upload/Download Detection criteria fire for 'S7comm Program Upload/Download Detection' (UC-14.6.2) in `index=ot` sourcetype `zeek:s7comm:json`. Pipeline aggregates `values` by `source_h`, `destination_h`, `uid` over `the scheduled window`. Alert logic enforces configured threshold. Title terms: S7comm, Program, Upload, Download, Detection. Eliminates manual hunting for UC-14.6.2 conditions.",
  "value": "UC-14.6.2 helps teams contain S7comm, Program, Upload, Download, Detection scenarios and reduce operational risk and delayed incident response before incidents compound.",
  "implementation": "Enable ICSNPP upload/download logging on Zeek sensors at OT taps; ingest into `index=ot`. Map `function_code` 0x1a/0x1b (decimal 26/27) to engineering change workflows; require ticket IDs in SOAR for acknowledged maintenance windows.",
  "visualization": "Timeline (upload/download events), Table (filename, block, endpoints), Single value (events in last 24h).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "TA for Zeek",
      "url": "https://splunkbase.splunk.com/app/5466",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Corelight App for Splunk",
      "url": "https://splunkbase.splunk.com/app/3884",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `ot` (UC-14.6.2)** — Planned work for 'S7comm Program Upload/Download Detection' shifts `values` over `span=1h` while `source_h`, `destination_h`, `uid` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `values` on `zeek:s7comm:json`** — Scheduled automation elevates `destination_h` without crossing configured threshold for UC-14.6.2. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `source_h`, `destination_h`, `uid`** — Health checks in `ot` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `zeek:s7comm:json`** — First 48h baseline for UC-14.6.2 on `source_h` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `source_h`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: TA for Zeek (Splunkbase 5466), Corelight App for Splunk (Splunkbase 3884).\n• Ensure the following data sources are available: `index=ot` `sourcetype=\"zeek:s7comm:json\"` (ICSNPP `s7comm_upload_download.log`: `function_code`, `filename`, `block_type`, `block_number`, `source_h`, `destination_h`).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnable ICSNPP upload/download logging on Zeek sensors at OT taps; ingest into `index=ot`. Map `function_code` 0x1a/0x1b (decimal 26/27) to engineering change workflows; require ticket IDs in SOAR for acknowledged maintenance windows.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot sourcetype=\"zeek:s7comm:json\"\n| eval fc_hex=replace(function_code,\"0x\",\"\")\n| eval fc=coalesce(tonumber(fc_hex,16), tonumber(function_code))\n| where fc IN (26,27) OR function_code IN (\"0x1a\",\"0x1b\",\"26\",\"27\") OR isnotnull(filename)\n| stats earliest(_time) as first_seen latest(_time) as last_seen values(filename) as filenames values(block_type) as block_types values(block_number) as block_numbers by source_h destination_h uid\n| table first_seen last_seen source_h destination_h filenames block_types block_numbers\n```\n\nUnderstanding this SPL\n\n**S7comm Program Upload/Download Detection** — PLC program upload/download changes process logic and safety envelopes; correlating these events with change tickets prevents unauthorized logic swaps that could disrupt operations or bypass interlocks.\n\nDocumented **Data sources**: `index=ot` `sourcetype=\"zeek:s7comm:json\"` (ICSNPP `s7comm_upload_download.log`: `function_code`, `filename`, `block_type`, `block_number`, `source_h`, `destination_h`). **App/TA** (typical add-on context): TA for Zeek (Splunkbase 5466), Corelight App for Splunk (Splunkbase 3884). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot; **sourcetype**: zeek:s7comm:json. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot, sourcetype=\"zeek:s7comm:json\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **fc_hex** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **fc** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where fc IN (26,27) OR function_code IN (\"0x1a\",\"0x1b\",\"26\",\"27\") OR isnotnull(filename)` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by source_h destination_h uid** so each row reflects one combination of those dimensions.\n• Pipeline stage (see **S7comm Program Upload/Download Detection**): table first_seen last_seen source_h destination_h filenames block_types block_numbers\n\nStep 3 — Validate\nRun the search for a short window and compare key fields with Zeek or Corelight protocol logs on the sensor for the same flow. Reconcile function codes, endpoints, and time order against a small known-good capture if needed.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Timeline (upload/download events), Table (filename, block, endpoints), Single value (events in last 24h).",
  "grandmaExplanation": "We use deep packet inspection to flag program transfer sessions so we can tie uploads and downloads to approved changes.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
