{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.6.12",
  "title": "EtherNet/IP Unregistered Session Detection",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Security",
    "Fault"
  ],
  "splunkPillar": "Security",
  "industry": "Manufacturing",
  "exclusions": "Covers the detection logic documented for 'EtherNet/IP Unregistered Session Detection' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_ethernet_ip_unregistered_session_detection` (UC-14.6.12), SOC dashboard panel \"Operations — EtherNet/IP Unregistered Session Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=ot` sourcetype='zeek:enip:json' that satisfy the UC-14.6.12 SPL filters for 'EtherNet/IP Unregistered Session Detection'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `ot` / `zeek:enip:json` events outside the detection threshold, or exclude the test entity via `operational_exceptions.csv` with a valid window. Confirm zero alert rows for UC-14.6.12."
  },
  "dataSources": "`index=ot` `sourcetype=\"zeek:enip:json\"` (`enip_command`, `session_handle`, `uid`, `source_h`, `destination_h`)",
  "app": "TA for Zeek (Splunkbase 5466), Corelight App for Splunk (Splunkbase 3884)",
  "spl": "index=ot sourcetype=\"zeek:enip:json\"\n| stats earliest(enip_command) as first_cmd values(enip_command) as cmds dc(enip_command) as cmd_variety by uid source_h destination_h\n| where first_cmd!=\"Register_Session\" AND (like(enip_command,\"%SendRRData%\") OR like(enip_command,\"%Unit%\") OR mvindex(cmds,0)!=first_cmd)\n| table uid source_h destination_h first_cmd cmds",
  "description": "EtherNet/IP sessions are normally established with explicit registration; traffic that skips expected session setup may indicate tooling errors, bypass attempts, or non-compliant devices on the plant floor.",
  "value": "Early 'EtherNet/IP Unregistered Session Detection' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Ingest `enip.log` from Zeek on EtherNet/IP segments. Validate ordering with known-good PLC/HMI captures; tune exclusions for vendor-specific handshake quirks. Combine with asset roles so HMIs are not false-positive flagged incorrectly.",
  "visualization": "Table (sessions with anomalous first command), Timeline (connection uid), Bar chart (count by destination_h).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "TA for Zeek",
      "url": "https://splunkbase.splunk.com/app/5466",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Corelight App for Splunk",
      "url": "https://splunkbase.splunk.com/app/3884",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'EtherNet/IP Unregistered Session Detection'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `ot` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `operational_exceptions.csv` with entity, reason, valid_until.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: TA for Zeek (Splunkbase 5466), Corelight App for Splunk (Splunkbase 3884).\n• Ensure the following data sources are available: `index=ot` `sourcetype=\"zeek:enip:json\"` (`enip_command`, `session_handle`, `uid`, `source_h`, `destination_h`).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest `enip.log` from Zeek on EtherNet/IP segments. Validate ordering with known-good PLC/HMI captures; tune exclusions for vendor-specific handshake quirks. Combine with asset roles so HMIs are not false-positive flagged incorrectly.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot sourcetype=\"zeek:enip:json\"\n| stats earliest(enip_command) as first_cmd values(enip_command) as cmds dc(enip_command) as cmd_variety by uid source_h destination_h\n| where first_cmd!=\"Register_Session\" AND (like(enip_command,\"%SendRRData%\") OR like(enip_command,\"%Unit%\") OR mvindex(cmds,0)!=first_cmd)\n| table uid source_h destination_h first_cmd cmds\n```\n\nUnderstanding this SPL\n\n**EtherNet/IP Unregistered Session Detection** — EtherNet/IP sessions are normally established with explicit registration; traffic that skips expected session setup may indicate tooling errors, bypass attempts, or non-compliant devices on the plant floor.\n\nDocumented **Data sources**: `index=ot` `sourcetype=\"zeek:enip:json\"` (`enip_command`, `session_handle`, `uid`, `source_h`, `destination_h`). **App/TA** (typical add-on context): TA for Zeek (Splunkbase 5466), Corelight App for Splunk (Splunkbase 3884). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot; **sourcetype**: zeek:enip:json. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot, sourcetype=\"zeek:enip:json\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by uid source_h destination_h** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where first_cmd!=\"Register_Session\" AND (like(enip_command,\"%SendRRData%\") OR like(enip_command,\"%Unit%\") OR mvindex(cmds,0…` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **EtherNet/IP Unregistered Session Detection**): table uid source_h destination_h first_cmd cmds\n\nStep 3 — Validate\nRun the search for a short window and compare key fields with Zeek or Corelight protocol logs on the sensor for the same flow. Reconcile function codes, endpoints, and time order against a small known-good capture if needed.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (sessions with anomalous first command), Timeline (connection uid), Bar chart (count by destination_h).",
  "grandmaExplanation": "We use deep packet inspection to spot unregistered CIP sessions so short-lived or stray clients on the line do not go unseen.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
