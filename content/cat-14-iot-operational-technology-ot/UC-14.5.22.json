{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.5.22",
  "title": "HiveMQ License Utilization Trending",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'HiveMQ License Utilization Trending' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_hivemq_license_utilization_trending` (UC-14.5.22), SOC dashboard panel \"Operations — HiveMQ License Utilization Trending\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.5.22 positive (HiveMQ, License, Utilization, Trending): ingest synthetic events into `index=ot` sourcetype='hivemq:metrics' where `connections` grouped by `host`, `_time` satisfies `utilization_pct` > 85 for 'HiveMQ License Utilization Trending'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.5.22 negative (Utilization Trending): ingest benign `ot` / `hivemq:metrics` events where `host`, `_time` stays below `utilization_pct` > 85, or exclude the test entity via `ops_exception_register.csv`. Variant 2 confirms zero alert rows for 'HiveMQ License Utilization Trending'."
  },
  "dataSources": "`index=ot` `sourcetype=\"hivemq:metrics\"` connection count gauges Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "HiveMQ Splunk Extension (metrics), HiveMQ license reporting — Universal Forwarder (Splunkbase 617)",
  "spl": "index=ot sourcetype=\"hivemq:metrics\"\n| eval m=lower(coalesce(metric_name, metric, name))\n| eval v=coalesce(value, metric_value, _value)\n| where match(m, \"connection\") AND match(m,\"current|active|open|established\")\n| bin _time span=5m\n| stats max(v) as connections by host, _time\n| eval license_limit=10000\n| eval utilization_pct=round(100*connections/license_limit,2)\n| where utilization_pct>85",
  "description": "Alerts on UC-14.5.22 for HiveMQ, License, Utilization, Trending over `index=ot` sourcetype `hivemq:metrics` with thresholds `utilization_pct` > 85. Groups `connections` by `host`, `_time` each `5m`. Centered on Utilization Trending indicators. Supports repeatable operations triage for 'HiveMQ License Utilization Trending'.",
  "value": "For 'HiveMQ License Utilization Trending', UC-14.5.22 converts `connections` on `hivemq:metrics` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Replace the static `license_limit` with a lookup or environment-specific value. Alert at 85%/95% thresholds with different severities.",
  "visualization": "Line chart (connections vs limit), Area chart (utilization %), Gauge (current utilization), Table (hosts approaching limit).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "TA for Zeek",
      "url": "https://splunkbase.splunk.com/app/5466",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Corelight App for Splunk",
      "url": "https://splunkbase.splunk.com/app/3884",
      "retrieved": "2026-04-25"
    },
    {
      "title": "TA for Corelight",
      "url": "https://splunkbase.splunk.com/app/3885",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `ot` (UC-14.5.22)** — Planned work for 'HiveMQ License Utilization Trending' shifts `connections` over `span=5m` while `host`, `_time` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `max` on `hivemq:metrics`** — Scheduled automation elevates `_time` without crossing `utilization_pct` > 85 for UC-14.5.22. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`, `_time`** — Health checks in `ot` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `hivemq:metrics`** — First 48h baseline for UC-14.5.22 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: HiveMQ Splunk Extension (metrics), HiveMQ license reporting.\n• Ensure the following data sources are available: `index=ot` `sourcetype=\"hivemq:metrics\"` connection count gauges.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nReplace the static `license_limit` with a lookup or environment-specific value. Alert at 85%/95% thresholds with different severities.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot sourcetype=\"hivemq:metrics\"\n| eval m=lower(coalesce(metric_name, metric, name))\n| eval v=coalesce(value, metric_value, _value)\n| where match(m, \"connection\") AND match(m,\"current|active|open|established\")\n| bin _time span=5m\n| stats max(v) as connections by host, _time\n| eval license_limit=10000\n| eval utilization_pct=round(100*connections/license_limit,2)\n| where utilization_pct>85\n```\n\nUnderstanding this SPL\n\n**HiveMQ License Utilization Trending** — Connection growth toward license limits causes hard rejections during peaks; trending utilization supports procurement decisions before production brownouts.\n\nDocumented **Data sources**: `index=ot` `sourcetype=\"hivemq:metrics\"` connection count gauges. **App/TA** (typical add-on context): HiveMQ Splunk Extension (metrics), HiveMQ license reporting. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot; **sourcetype**: hivemq:metrics. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot, sourcetype=\"hivemq:metrics\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **m** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **v** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where match(m, \"connection\") AND match(m,\"current|active|open|established\")` — typically the threshold or rule expression for this monitoring goal.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by host, _time** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **license_limit** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **utilization_pct** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where utilization_pct>85` — typically the threshold or rule expression for this monitoring goal.\n\nStep 3 — Validate\nRun the search for a known busy window, then compare counts and clients with the HiveMQ control center or broker metrics. Reconcile top clients and topics with what the broker shows.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (connections vs limit), Area chart (utilization %), Gauge (current utilization), Table (hosts approaching limit).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (HiveMQ Splunk Extension (metrics), HiveMQ license reporting — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=hivemq:metrics into ot. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, host, utilization_pct`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We trend license and capacity use on the managed broker so growth or limits do not surprise us in production.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
