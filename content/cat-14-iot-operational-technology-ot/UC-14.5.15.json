{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.5.15",
  "title": "OPC-UA Write and Permission Denials",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'OPC-UA Write and Permission Denials' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_opc_ua_write_and_permission_denials` (UC-14.5.15), SOC dashboard panel \"Operations — OPC-UA Write and Permission Denials\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.5.15 positive (OPC, Write, and, Permission, Denials): ingest synthetic events into `index=ot` sourcetype='opcua_audit' where `count` grouped by `client_id`, `node_id`, `status_code`, `_time` satisfies `count` > 0 for 'OPC-UA Write and Permission Denials'. Validate filter `status_code!=\"Good\" OR permission_denied`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.5.15 negative (Permission Denials): ingest benign `ot` / `opcua_audit` events where `client_id`, `node_id`, `status_code`, `_time` stays below `count` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 0 confirms zero alert rows for 'OPC-UA Write and Permission Denials'."
  },
  "dataSources": "OPC-UA server audit log (write request, status code) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "OPC-UA server/gateway (audit or security log) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=ot sourcetype=opcua_audit action=write\n| search status_code!=\"Good\" OR permission_denied\n| bin _time span=1h\n| stats count by client_id, node_id, status_code, _time\n| where count > 0\n| table _time client_id node_id status_code count",
  "description": "Alerts on 'OPC-UA Write and Permission Denials' (UC-14.5.15) using `index=ot` sourcetype `opcua_audit` via filter `status_code!=\"Good\" OR permission_denied` by aggregating `count` grouped by `client_id`, `node_id`, `status_code`, `_time` over `1h` and alerting when `count` > 0. Keywords: OPC, Write, and, Permission, Denials. Focuses on Permission Denials behaviour so operations teams can triage UC-14.5.15 without ad-hoc log review.",
  "value": "UC-14.5.15 accelerates insight into Permission Denials ('OPC-UA Write and Permission Denials') by tracking `activity` on `opcua_audit` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Enable OPC-UA server audit or security logging for write requests. Forward to Splunk. Alert on write denials for critical nodes or high volume of denials from a single client. Dashboard writes by client and node.",
  "visualization": "Table (client, node, status, count), Timeline (write denials), Bar chart (denials by node).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `ot` (UC-14.5.15)** — Planned work for 'OPC-UA Write and Permission Denials' shifts `count` over `span=1h` while `client_id`, `node_id`, `status_code`, `_time` remains in-scope. Applies filter `status_code!=\"Good\" OR permission_denied`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `opcua_audit`** — Scheduled automation elevates `node_id` without crossing `count` > 0 for UC-14.5.15. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `client_id`, `node_id`, `status_code`, `_time`** — Health checks in `ot` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `opcua_audit`** — First 48h baseline for UC-14.5.15 on `client_id` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `client_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "opcua"
  ],
  "equipmentModels": [
    "opcua_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: OPC-UA server/gateway (audit or security log).\n• Ensure the following data sources are available: OPC-UA server audit log (write request, status code).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnable OPC-UA server audit or security logging for write requests. Forward to Splunk. Alert on write denials for critical nodes or high volume of denials from a single client. Dashboard writes by client and node.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot sourcetype=opcua_audit action=write\n| search status_code!=\"Good\" OR permission_denied\n| bin _time span=1h\n| stats count by client_id, node_id, status_code, _time\n| where count > 0\n| table _time client_id node_id status_code count\n```\n\nUnderstanding this SPL\n\n**OPC-UA Write and Permission Denials** — Unauthorized write attempts or permission denials can indicate misconfiguration, abuse, or attack. Auditing supports least-privilege and security review.\n\nDocumented **Data sources**: OPC-UA server audit log (write request, status code). **App/TA** (typical add-on context): OPC-UA server/gateway (audit or security log). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot; **sourcetype**: opcua_audit. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot, sourcetype=opcua_audit. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Applies an explicit `search` filter to narrow the current result set.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by client_id, node_id, status_code, _time** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where count > 0` — typically the threshold or rule expression for this monitoring goal.\n• Pipeline stage (see **OPC-UA Write and Permission Denials**): table _time client_id node_id status_code count\n\nStep 3 — Validate\nRun the search for a time range where you know sessions were active, then compare the same nodes and clients in an industrial client such as UA Expert. Spot-check certificate and session fields against the server.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (client, node, status, count), Timeline (write denials), Bar chart (denials by node).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (OPC-UA server/gateway (audit or security log) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=opcua_audit into ot. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, client_id, node_id, status_code`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We log denied writes and permission faults so we can tell normal operator limits from something worth investigating.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
