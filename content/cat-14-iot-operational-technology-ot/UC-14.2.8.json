{
  "$schema": "../../schemas/uc.schema.json",
  "id": "14.2.8",
  "title": "OPC-UA Session Abuse",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'OPC-UA Session Abuse' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_opc_ua_session_abuse` (UC-14.2.8), SOC dashboard panel \"Operations — OPC-UA Session Abuse\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-14.2.8 positive (OPC, Session, Abuse): ingest synthetic events into `index=ot` sourcetype='opcua:session' where `sessions`, `clients` grouped by `server_endpoint`, `_time` satisfies `sessions` > 50, or `clients` > 10 for 'OPC-UA Session Abuse'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-14.2.8 negative (Session Abuse): ingest benign `ot` / `opcua:session` events where `server_endpoint`, `_time` stays below `sessions` > 50, or `clients` > 10, or exclude the test entity via `ops_exception_register.csv`. Variant 10 confirms zero alert rows for 'OPC-UA Session Abuse'."
  },
  "dataSources": "`sourcetype=\"opcua:session\"` (server audit events) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "OPC-UA server audit logs, Edge Hub — Universal Forwarder (Splunkbase 617)",
  "spl": "index=ot sourcetype=\"opcua:session\"\n| where event_type IN (\"CreateSession\",\"ActivateSession\") AND (is_anonymous=1 OR rejected=1)\n| bin _time span=1h\n| stats dc(session_id) as sessions, dc(client_ip) as clients by server_endpoint, _time\n| where sessions > 50 OR clients > 10",
  "description": "Identifies UC-14.2.8 — OPC, Session, Abuse — using `index=ot` sourcetype `opcua:session`, grouped by `server_endpoint`, `_time`. Outputs `sessions`, `clients` each `1h`; alert when `sessions` > 50, or `clients` > 10. Primary focus: Session Abuse. Purpose-built for 'OPC-UA Session Abuse' in Operations workflows.",
  "value": "For 'OPC-UA Session Abuse', UC-14.2.8 converts `sessions`, `clients` on `opcua:session` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Ingest OPC-UA audit events. Whitelist known engineering hosts. Alert on anonymous session creation or high rejection rate.",
  "visualization": "Table (servers with suspicious sessions), Bar chart (sessions by client IP).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable (no CIM or Operational_Telemetry data model in this SPL).",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `ot` (UC-14.2.8)** — Planned work for 'OPC-UA Session Abuse' shifts `sessions`, `clients` over `span=1h` while `server_endpoint`, `_time` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `dc` on `opcua:session`** — Scheduled automation elevates `_time` without crossing `sessions` > 50 for UC-14.2.8. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `server_endpoint`, `_time`** — Health checks in `ot` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `opcua:session`** — First 48h baseline for UC-14.2.8 on `server_endpoint` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `server_endpoint`, `exception_reason`, `valid_until`.",
  "equipment": [
    "edge_hub",
    "opcua"
  ],
  "equipmentModels": [
    "edge_hub_generic",
    "opcua_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "premiumApps": [
    "Splunk Edge Hub"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: OPC-UA server audit logs, Edge Hub.\n• Ensure the following data sources are available: `sourcetype=\"opcua:session\"` (server audit events).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest OPC-UA audit events. Whitelist known engineering hosts. Alert on anonymous session creation or high rejection rate.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=ot sourcetype=\"opcua:session\"\n| where event_type IN (\"CreateSession\",\"ActivateSession\") AND (is_anonymous=1 OR rejected=1)\n| bin _time span=1h\n| stats dc(session_id) as sessions, dc(client_ip) as clients by server_endpoint, _time\n| where sessions > 50 OR clients > 10\n```\n\nUnderstanding this SPL\n\n**OPC-UA Session Abuse** — Excessive sessions or anonymous binds from new clients can indicate scanning or unauthorized access.\n\nDocumented **Data sources**: `sourcetype=\"opcua:session\"` (server audit events). **App/TA** (typical add-on context): OPC-UA server audit logs, Edge Hub. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: ot; **sourcetype**: opcua:session. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=ot, sourcetype=\"opcua:session\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where event_type IN (\"CreateSession\",\"ActivateSession\") AND (is_anonymous=1 OR rejected=1)` — typically the threshold or rule expression for this monitoring goal.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by server_endpoint, _time** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where sessions > 50 OR clients > 10` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nRun the search when the process is in a known state. Compare key fields and alarm states to the SCADA or DCS HMI and, where relevant, the engineering tool for the PLC. Confirm sourcetypes and field extractions match the vendor feed.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (servers with suspicious sessions), Bar chart (sessions by client IP).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (OPC-UA server audit logs, Edge Hub — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=opcua:session into ot. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, clients, server_endpoint, sessions`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch your plant control systems and field data. We help you know if something is off with this area: opc-ua session abuse before harm, damage, or wasted effort piles up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
