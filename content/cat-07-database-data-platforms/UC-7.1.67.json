{
  "$schema": "../../schemas/uc.schema.json",
  "id": "7.1.67",
  "title": "Cassandra Compaction Backlog and Pending Tasks",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`nodetool compactionstats` JSON/CSV ingested as `cassandra:compaction`",
  "app": "Splunk Add-on for JMX (Splunkbase 2647) for `org.apache.cassandra.metrics:type=Compaction` or scripted nodetool forwarder",
  "spl": "index=database sourcetype=\"cassandra:compaction\"\n| eval pending=tonumber(pending_tasks)\n| where pending > 100\n| bin _time span=15m\n| stats max(pending) as max_pending latest(bytes_total_in_progress) as bytes_in_progress by host, cluster_name, _time\n| where max_pending > 150",
  "description": "A monotonically growing pending compaction count means the node cannot rewrite SSTables fast enough for the write load, which soon shows up as read latency and repair failures. Early detection avoids disk-full and timeout incidents during peak traffic.",
  "value": "Keeps OLTP and analytics paths on Cassandra responsive by triggering throttling, compaction strategy reviews, or capacity adds before timeouts cascade.",
  "implementation": "Run `nodetool compactionstats` every 60–120s via scripted input; parse `pending tasks`, `id`, `keyspace`, `table`, `progress`, `total`. Alternatively enable JMX `CompactionExecutor` pending metric via TA-JMX `inputs.conf` with `sourcetype=cassandra:compaction`. Baseline per cluster class; suppress during repairs via maintenance lookup.",
  "visualization": "Line chart (max_pending), Table (keyspace, table, bytes_in_progress).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Apache Cassandra — compaction",
      "url": "https://cassandra.apache.org/doc/latest/cassandra/operating/compaction/index.html"
    },
    {
      "title": "Splunk Add-on for JMX",
      "url": "https://splunkbase.splunk.com/app/2647"
    }
  ],
  "equipment": [
    "apache",
    "cassandra"
  ],
  "equipmentModels": [
    "apache_httpd"
  ],
  "status": "draft",
  "lastReviewed": "2025-06-01",
  "splunkVersions": [
    "9.x",
    "8.x"
  ],
  "reviewer": "catalog-team"
}
