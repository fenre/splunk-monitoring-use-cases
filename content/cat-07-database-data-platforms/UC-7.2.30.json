{
  "$schema": "../../schemas/uc.schema.json",
  "id": "7.2.30",
  "title": "Cassandra Inter-Node Streaming Progress (nodetool netstats)",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Cassandra Inter-Node Streaming Progress (nodetool netstats)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_cassandra_inter_node_streaming_progress_nodetool` (UC-7.2.30), SOC dashboard panel \"Operations — Cassandra Inter-Node Streaming Progress (nodetool netstats)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-7.2.30 positive (Cassandra, Inter, Node, Streaming, Progress): ingest synthetic events into `index=database` sourcetype='cassandra:netstats' where `count` grouped by `peer`, `operation`, `host`, `cluster_name` satisfies `receiving_bytes` > 0, or `sending_bytes` > 0 for 'Cassandra Inter-Node Streaming Progress (nodetool netstats)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-7.2.30 negative ((nodetool netstats)): ingest benign `database` / `cassandra:netstats` events where `peer`, `operation`, `host`, `cluster_name` stays below `receiving_bytes` > 0, or `sending_bytes` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 12 confirms zero alert rows for 'Cassandra Inter-Node Streaming Progress (nodetool netstats)'."
  },
  "dataSources": "`nodetool netstats` stream summaries (repair, bootstrap, rebuild) with bytes sent/received and session state Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Scripted nodetool netstats collector — Universal Forwarder (Splunkbase 617)",
  "spl": "index=database sourcetype=\"cassandra:netstats\"\n| where stream_state IN (\"PREPARING\",\"STREAMING\",\"WAITING\") OR receiving_bytes > 0 OR sending_bytes > 0\n| eval stall=if(stream_state==\"WAITING\" AND receiving_bytes==0 AND sending_bytes==0,1,0)\n| where stall==1\n| stats count by peer, operation, host, cluster_name",
  "description": "Alerts on UC-7.2.30 — Cassandra, Inter, Node, Streaming, Progress — using `index=database` sourcetype `cassandra:netstats`, grouped by `peer`, `operation`, `host`, `cluster_name`. Outputs `count` each `the scheduled window`; alert when `receiving_bytes` > 0, or `sending_bytes` > 0. Primary focus: (nodetool netstats). Purpose-built for 'Cassandra Inter-Node Streaming Progress (nodetool netstats)' in Operations workflows.",
  "value": "Tracking `activity` via UC-7.2.30 on `cassandra:netstats` feeds improves response to 'Cassandra Inter-Node Streaming Progress (nodetool netstats)' and reduces operational risk and delayed incident response.",
  "implementation": "Parse netstats output into one event per stream direction. Poll every 2–5 minutes while operations are active. Suppress during approved maintenance. Correlate with nodetool compactionstats.",
  "visualization": "Sankey or table (source→peer), Timeline (bytes/sec), Single value (stuck streams).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Apache Cassandra nodetool netstats",
      "url": "https://cassandra.apache.org/doc/latest/cassandra/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `database` (UC-7.2.30)** — Planned work for 'Cassandra Inter-Node Streaming Progress (nodetool netstats)' shifts `count` over `span=1h` while `peer`, `operation`, `host`, `cluster_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `cassandra:netstats`** — Scheduled automation elevates `operation` without crossing `receiving_bytes` > 0 for UC-7.2.30. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `peer`, `operation`, `host`, `cluster_name`** — Health checks in `database` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `cassandra:netstats`** — First 48h baseline for UC-7.2.30 on `peer` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `peer`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cassandra"
  ],
  "equipmentModels": [
    "cassandra_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Scripted nodetool netstats collector — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=cassandra:netstats into database. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`cluster_name, host, operation, peer`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch Cassandra inter-node streaming progress (using each node's streaming status view) so we can keep this part of the data platform within the capacity and quality targets our teams expect.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
