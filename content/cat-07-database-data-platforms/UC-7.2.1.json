{
  "$schema": "../../schemas/uc.schema.json",
  "id": "7.2.1",
  "title": "Cluster Membership Changes",
  "criticality": "critical",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Cluster Membership Changes' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_cluster_membership_changes` (UC-7.2.1), SOC dashboard panel \"Operations — Cluster Membership Changes\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-7.2.1 positive (Cluster, Membership, Changes): ingest synthetic events into `index=database` sourcetype='mongodb:log' where `count` grouped by `entity` satisfies detection threshold for 'Cluster Membership Changes'. Validate filter `\"replSet\" (\"added\" OR \"removed\" OR \"changed state\" OR \"elect`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-7.2.1 negative (Membership Changes): ingest benign `database` / `mongodb:log` events where `entity` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 7 confirms zero alert rows for 'Cluster Membership Changes'."
  },
  "dataSources": "MongoDB replica set events, Cassandra `system.log`, Elasticsearch cluster state Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom scripted input, database event logs — Universal Forwarder (Splunkbase 617)",
  "spl": "index=database sourcetype=\"mongodb:log\"\n| search \"replSet\" (\"added\" OR \"removed\" OR \"changed state\" OR \"election\")\n| table _time, host, message\n| sort -_time",
  "description": "Alerts on UC-7.2.1 — Cluster, Membership, Changes — using `index=database` sourcetype `mongodb:log`, grouped by `entity`. Outputs `count` each `the scheduled window`; alert when configured threshold. Filter chain includes filter `\"replSet\" (\"added\" OR \"removed\" OR \"changed state\" OR \"elect`. Purpose-built for 'Cluster Membership Changes' in Operations workflows.",
  "value": "UC-7.2.1 operationalises 'Cluster Membership Changes' against `mongodb:log` feeds so operators reduce operational risk and delayed incident response without manual correlation.",
  "implementation": "Forward database logs to Splunk. Parse membership change events. Alert on unexpected node departures. For Elasticsearch, poll `_cluster/health` API and alert on node count changes.",
  "visualization": "Timeline (membership events), Single value (current node count), Table (recent cluster changes).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `database` (UC-7.2.1)** — Planned work for 'Cluster Membership Changes' shifts `count` over `span=1h` while `host` remains in-scope. Applies filter `\"replSet\" (\"added\" OR \"removed\" OR \"changed state\" OR \"elect`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `mongodb:log`** — Scheduled automation elevates `src` without crossing configured threshold for UC-7.2.1. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `database` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `mongodb:log`** — First 48h baseline for UC-7.2.1 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cassandra",
    "elasticsearch",
    "mongodb"
  ],
  "equipmentModels": [
    "elasticsearch_es",
    "mongodb_mongod"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• In operations we confirm in mongosh, MongoDB Compass, or the Atlas metrics UI so replication, elections, and cluster operations match what Splunk shows.\n• Install and configure the required add-on or app: Custom scripted input, database event logs.\n• Ensure the following data sources are available: MongoDB replica set events, Cassandra `system.log`, Elasticsearch cluster state.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nForward database logs to Splunk. Parse membership change events. Alert on unexpected node departures. For Elasticsearch, poll `_cluster/health` API and alert on node count changes.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=database sourcetype=\"mongodb:log\"\n| search \"replSet\" (\"added\" OR \"removed\" OR \"changed state\" OR \"election\")\n| table _time, host, message\n| sort -_time\n```\n\nUnderstanding this SPL\n\n**Cluster Membership Changes** — Node additions/removals affect data distribution and availability. Unexpected membership changes may indicate failures.\n\nDocumented **Data sources**: MongoDB replica set events, Cassandra `system.log`, Elasticsearch cluster state. **App/TA** (typical add-on context): Custom scripted input, database event logs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: database; **sourcetype**: mongodb:log. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=database, sourcetype=\"mongodb:log\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Applies an explicit `search` filter to narrow the current result set.\n• Pipeline stage (see **Cluster Membership Changes**): table _time, host, message\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nFor the same time range, compare Splunk results with the engine’s own tools and system views (SQL Server: SQL Server Management Studio and `sys.dm_*`; Oracle: Oracle Enterprise Manager, SQLcl, or `V$` views; MySQL: Workbench or `performance_schema` / `SHOW` output; PostgreSQL: `pg_stat_*` in psql or pgAdmin; MongoDB: mongosh or Atlas metrics; Cassandra: nodetool; Elasticsearch/OpenSearch: Kibana or REST `_cat` / `_cluster/health`; ClickHouse: `system` tables in clickhouse-client; Snowflake: Snowsight or `ACCOUNT_USAGE`; others: the managed PaaS console). Confirm event counts, field names, timestamps, and Splunk role permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Timeline (membership events), Single value (current node count), Table (recent cluster changes).",
  "grandmaExplanation": "We watch replica set and cluster membership events so we can spot unplanned node changes or elections before they become availability or data-safety issues.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
