{
  "$schema": "../../schemas/uc.schema.json",
  "id": "7.1.69",
  "title": "Cassandra Tombstone Warnings on Read Path",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Data Quality"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Cassandra Tombstone Warnings on Read Path' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_cassandra_tombstone_warnings_on_read_path` (UC-7.1.69), SOC dashboard panel \"Operations — Cassandra Tombstone Warnings on Read Path\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-7.1.69 positive (Cassandra, Tombstone, Warnings, Read, Path): ingest synthetic events into `index=database` sourcetype='cassandra:system' where `max_tombstones` grouped by `keyspace`, `table`, `host` satisfies `warn_events` >= 5, or `max_tombstones` > 10000 for 'Cassandra Tombstone Warnings on Read Path'. Validate filter `\"tombstone\" AND (\"WARN\" OR \"ERROR\" OR \"ReadCommand\")`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-7.1.69 negative (Read Path): ingest benign `database` / `cassandra:system` events where `keyspace`, `table`, `host` stays below `warn_events` >= 5, or `max_tombstones` > 10000, or exclude the test entity via `ops_exception_register.csv`. Variant 5 confirms zero alert rows for 'Cassandra Tombstone Warnings on Read Path'."
  },
  "dataSources": "Cassandra `system.log` lines for tombstone thresholds, sourcetype `cassandra:system` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Universal Forwarder on Cassandra nodes with `monitor://` on `cassandra/logs/system.log` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=database sourcetype=\"cassandra:system\"\n| search \"tombstone\" AND (\"WARN\" OR \"ERROR\" OR \"ReadCommand\")\n| rex field=_raw \"Read (?<read_type>\\w+) query.* scanned (?<scanned>\\d+) tombstones.* limit (?<limit>\\d+)\"\n| eval over_limit=if(scanned>limit,1,0)\n| stats count as warn_events max(scanned) as max_tombstones by keyspace, table, host\n| where warn_events >= 5 OR max_tombstones > 10000",
  "description": "Surfaces 'Cassandra Tombstone Warnings on Read Path' patterns (UC-7.1.69) sourced from `index=database` sourcetype `cassandra:system`. Threshold bundle: `warn_events` >= 5, or `max_tombstones` > 10000; aggregation grain: `keyspace`, `table`, `host`; metrics: `max_tombstones`. Enrichment path: filter `\"tombstone\" AND (\"WARN\" OR \"ERROR\" OR \"ReadCommand\")`.",
  "value": "UC-7.1.69 gives Operations teams earlier visibility into Read Path through `cassandra:system` feeds, lowering operational risk and delayed incident response.",
  "implementation": "Set `props.conf` `LINE_BREAKER` for multiline stack traces; extract `keyspace` and `table` via `EXTRACT-cassandra_table`. Tune `tombstone_warn_threshold` awareness—alerts should fire below catastrophic defaults. Pair with `cassandra:nodetool_cfstats` `Droppable tombstone` metrics.",
  "visualization": "Timeline (warn_events), Table (keyspace, table, max_tombstones).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Apache Cassandra — Tombstones",
      "url": "https://cassandra.apache.org/doc/latest/cassandra/developing/cql/dml.html#deletes",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `database` (UC-7.1.69)** — Planned work for 'Cassandra Tombstone Warnings on Read Path' shifts `max_tombstones` over `span=1h` while `keyspace`, `table`, `host` remains in-scope. Applies filter `\"tombstone\" AND (\"WARN\" OR \"ERROR\" OR \"ReadCommand\")`. Cross-check CMDB CHG.\n\n2. **Batch `max` on `cassandra:system`** — Scheduled automation elevates `table` without crossing `warn_events` >= 5 for UC-7.1.69. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `keyspace`, `table`, `host`** — Health checks in `database` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `cassandra:system`** — First 48h baseline for UC-7.1.69 on `keyspace` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `keyspace`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cassandra"
  ],
  "equipmentModels": [
    "cassandra_generic"
  ],
  "status": "draft",
  "lastReviewed": "2025-06-01",
  "splunkVersions": [
    "9.x",
    "8.x"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Universal Forwarder on Cassandra nodes with `monitor://` on `cassandra/logs/system.log` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=cassandra:system into database. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, keyspace, max_tombstones, over_limit`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We surface slow and blocking queries so we can fix the worst offenders first and keep applications and batch jobs within the response times we promise.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
