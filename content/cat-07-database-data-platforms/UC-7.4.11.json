{
  "$schema": "../../schemas/uc.schema.json",
  "id": "7.4.11",
  "title": "Redshift Query Queue Depth",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Redshift Query Queue Depth' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_redshift_query_queue_depth` (UC-7.4.11), SOC dashboard panel \"Operations — Redshift Query Queue Depth\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-7.4.11 positive (Redshift, Query, Queue, Depth): ingest synthetic events into `index=aws` sourcetype='aws:cloudwatch' where `queue_depth` grouped by `ClusterIdentifier`, `QueueName` satisfies `queue_depth` > 10 for 'Redshift Query Queue Depth'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-7.4.11 negative (Queue Depth): ingest benign `aws` / `aws:cloudwatch` events where `ClusterIdentifier`, `QueueName` stays below `queue_depth` > 10, or exclude the test entity via `ops_exception_register.csv`. Variant 14 confirms zero alert rows for 'Redshift Query Queue Depth'."
  },
  "dataSources": "`WLMQueueDepth`, `WLMQueriesCompletedPerSecond` Collected via Splunk Add-on for Amazon Web Services (Splunkbase 1876) modular input or scripted poll into the index named in the SPL.",
  "app": "CloudWatch, `STL_WLM_QUERY` export — Splunk Add-on for Amazon Web Services (Splunkbase 1876)",
  "spl": "index=aws sourcetype=\"aws:cloudwatch\" namespace=\"AWS/Redshift\" metric_name=\"WLMQueueDepth\"\n| timechart span=5m max(Maximum) as queue_depth by ClusterIdentifier, QueueName\n| where queue_depth > 10",
  "description": "Monitors UC-7.4.11 — Redshift, Query, Queue, Depth — using `index=aws` sourcetype `aws:cloudwatch`, grouped by `ClusterIdentifier`, `QueueName`. Outputs `queue_depth` each `5m`; alert when `queue_depth` > 10. Primary focus: Queue Depth. Purpose-built for 'Redshift Query Queue Depth' in Operations workflows.",
  "value": "Alerting on Redshift, Query, Queue, Depth through UC-7.4.11 reduces operational risk and delayed incident response by surfacing `queue_depth` early.",
  "implementation": "Map queue names to workload classes. Alert when queue_depth sustained above SLA. Tune WLM slots or concurrency scaling.",
  "visualization": "Line chart (queue depth), Table (cluster, queue, depth), Single value (max depth).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 1876",
      "url": "https://splunkbase.splunk.com/app/1876",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `aws` (UC-7.4.11)** — Planned work for 'Redshift Query Queue Depth' shifts `queue_depth` over `span=5m` while `ClusterIdentifier`, `QueueName` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `max` on `aws:cloudwatch`** — Scheduled automation elevates `QueueName` without crossing `queue_depth` > 10 for UC-7.4.11. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `ClusterIdentifier`, `QueueName`** — Health checks in `aws` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `aws:cloudwatch`** — First 48h baseline for UC-7.4.11 on `ClusterIdentifier` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `ClusterIdentifier`, `exception_reason`, `valid_until`.",
  "equipment": [
    "aws"
  ],
  "equipmentModels": [
    "aws_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• In operations we confirm in pgAdmin, psql, and `pg_stat*` views, or the managed PostgreSQL console.\n• Install and configure the required add-on or app: CloudWatch, `STL_WLM_QUERY` export.\n• Ensure the following data sources are available: `WLMQueueDepth`, `WLMQueriesCompletedPerSecond`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMap queue names to workload classes. Alert when queue_depth sustained above SLA. Tune WLM slots or concurrency scaling.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=aws sourcetype=\"aws:cloudwatch\" namespace=\"AWS/Redshift\" metric_name=\"WLMQueueDepth\"\n| timechart span=5m max(Maximum) as queue_depth by ClusterIdentifier, QueueName\n| where queue_depth > 10\n```\n\nUnderstanding this SPL\n\n**Redshift Query Queue Depth** — WLM queue length and max execution time show concurrency saturation. Growing queue depth precedes disk-based spills and timeouts.\n\nDocumented **Data sources**: `WLMQueueDepth`, `WLMQueriesCompletedPerSecond`. **App/TA** (typical add-on context): CloudWatch, `STL_WLM_QUERY` export. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: aws; **sourcetype**: aws:cloudwatch. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=aws, sourcetype=\"aws:cloudwatch\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `timechart` plots the metric over time using **span=5m** buckets with a separate series **by ClusterIdentifier, QueueName** — ideal for trending and alerting on this use case.\n• Filters the current rows with `where queue_depth > 10` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nFor the same time range, compare Splunk results with the engine’s own tools and system views (SQL Server: SQL Server Management Studio and `sys.dm_*`; Oracle: Oracle Enterprise Manager, SQLcl, or `V$` views; MySQL: Workbench or `performance_schema` / `SHOW` output; PostgreSQL: `pg_stat_*` in psql or pgAdmin; MongoDB: mongosh or Atlas metrics; Cassandra: nodetool; Elasticsearch/OpenSearch: Kibana or REST `_cat` / `_cluster/health`; ClickHouse: `system` tables in clickhouse-client; Snowflake: Snowsight or `ACCOUNT_USAGE`; others: the managed PaaS console). Confirm event counts, field names, timestamps, and Splunk role permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (queue depth), Table (cluster, queue, depth), Single value (max depth).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (CloudWatch, `STL_WLM_QUERY` export — Splunk Add-on for Amazon Web Services (Splunkbase 1876)). Enable the modular input or HEC token that writes sourcetype=aws:cloudwatch into aws. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`ClusterIdentifier, QueueName, queue_depth`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "WLM queue length and max execution time show concurrency saturation We use it to stay ahead of pain for applications and the people who depend on the data.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
