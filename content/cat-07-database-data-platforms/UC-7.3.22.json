{
  "$schema": "../../schemas/uc.schema.json",
  "id": "7.3.22",
  "title": "Redis ACL and AUTH Denial Events",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "dataSources": "Redis 6+ ACL LOG entries or `redis-server` log lines for denied commands / wrong passwords",
  "app": "Splunk Universal Forwarder on Redis nodes, or `ACL LOG` scripted poll to HEC (`redis:acl_log`)",
  "spl": "index=middleware sourcetype=\"redis:acl_log\"\n| where reason IN (\"auth\",\"deny-command\",\"key\") OR match(_raw,\"denied\")\n| bin _time span=1h\n| stats count as events dc(username) as users dc(client_info) as clients by host, _time\n| where events > 10",
  "description": "ACL denials and failed authentications surface privilege misuse, broken automation, or password-guessing against Redis—especially after enabling ACL users in production.",
  "value": "Closes the gap between open Redis instances and least-privilege enforcement by making denials visible in Splunk.",
  "implementation": "Enable ACL LOG on each primary; ship JSON lines with username, reason, client info. If only file logs exist, use sourcetype redis:log with transforms. Redact key names where sensitive.",
  "visualization": "Table (user, reason, count), Timeline (denials), Top (client_info).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Redis ACL LOG",
      "url": "https://redis.io/docs/latest/commands/acl-log/"
    }
  ],
  "equipment": [
    "hardware_bmc",
    "redis"
  ],
  "equipmentModels": [
    "hardware_bmc_edac"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases"
}
