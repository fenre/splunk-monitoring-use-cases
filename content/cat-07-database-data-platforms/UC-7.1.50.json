{
  "$schema": "../../schemas/uc.schema.json",
  "id": "7.1.50",
  "title": "MongoDB WiredTiger Dirty Cache Percentage Trending",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`db.serverStatus().wiredTiger.cache` bytes dirty vs bytes currently in cache, ingested as `mongodb:server_status`",
  "app": "Splunk Add-on for MongoDB (Splunkbase) with modular/scripted `serverStatus` input, or Splunk DB Connect polling `serverStatus` JSON",
  "spl": "index=database sourcetype=\"mongodb:server_status\"\n| eval dirty_pct=if(bytes_currently_in_the_cache>0, round(100*bytes_dirty/bytes_currently_in_the_cache,2), null())\n| bin _time span=15m\n| stats latest(dirty_pct) as dirty_pct by host, _time\n| streamstats window=6 global=f first(dirty_pct) as baseline by host\n| where dirty_pct > 25 AND dirty_pct > baseline+10",
  "description": "A rising share of the WiredTiger cache held as dirty pages means checkpoints and eviction are falling behind, which precedes IO storms and tail latency on transactional workloads. Tracking dirty percentage relative to its own baseline catches gradual regressions before absolute thresholds trip.",
  "value": "Lets capacity owners add cache, smooth write bursts, or reschedule index builds before application teams see p95 latency cliffs during peak traffic.",
  "implementation": "Install the Splunk Add-on for MongoDB and enable the `serverStatus` scripted/modular input in `inputs.conf` (interval 60–300s). Map `wiredTiger.cache.bytes dirty in the cache` and `bytes currently in the cache` to `bytes_dirty` and `bytes_currently_in_the_cache`. Normalize host/cluster tags in `props.conf` if needed. Alert when dirty_pct exceeds 25% and is 10 points above the prior 90-minute baseline for the same host.",
  "visualization": "Line chart (dirty_pct by host), Single value (worst host), Table (host, dirty_pct, baseline delta).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "MongoDB WiredTiger cache tuning",
      "url": "https://www.mongodb.com/docs/manual/administration/analyzing-mongodb-performance/#wiredtiger-cache"
    },
    {
      "title": "Splunk Add-on for MongoDB",
      "url": "https://splunkbase.splunk.com/app/3212"
    }
  ],
  "equipment": [
    "checkpoint",
    "db_connect",
    "mongodb"
  ],
  "equipmentModels": [
    "mongodb_mongod",
    "mongodb_wiredtiger"
  ],
  "status": "draft",
  "lastReviewed": "2025-06-01",
  "splunkVersions": [
    "9.x",
    "8.x"
  ],
  "reviewer": "catalog-team",
  "knownFalsePositives": "Planned step-down elections, compactions, and balanced migrations can look like incidents in logs until we compare with the MongoDB ops window or Atlas activity feed.",
  "grandmaExplanation": "We follow MongoDB operations, replication, and resource usage so we can keep queries and writes reliable as sharding and data volume grow."
}
