{
  "$schema": "../../schemas/uc.schema.json",
  "id": "7.1.65",
  "title": "ClickHouse Detached Parts Accumulation",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Data Quality"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'ClickHouse Detached Parts Accumulation' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_clickhouse_detached_parts_accumulation` (UC-7.1.65), SOC dashboard panel \"Operations — ClickHouse Detached Parts Accumulation\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=database` sourcetype='clickhouse:parts' where `database` satisfies `detached_partitions` > 3 for UC-7.1.65 ('ClickHouse Detached Parts Accumulation'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `database` / `clickhouse:parts` events where `database` stays below `detached_partitions` > 3, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-7.1.65."
  },
  "dataSources": "`system.detached_parts` or `system.parts` with `detached` flag ingested as `clickhouse:parts` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Scheduled SQL `SELECT database, table, count() cnt FROM system.detached_parts GROUP BY database, table` to HEC — Universal Forwarder (Splunkbase 617)",
  "spl": "index=database sourcetype=\"clickhouse:parts\"\n| where detached==1 OR match(_raw,\"detached\")\n| stats dc(partition) as detached_partitions sum(rows) as detached_rows latest(reason) as detach_reason by database, table, host\n| where detached_partitions > 3 OR detached_rows > 1000000",
  "description": "Detached parts often follow failed merges, hardware errors, or manual `DETACH PART` operations; unchecked growth implies data integrity work and risks query failures when ATTACH is attempted inconsistently across replicas.",
  "value": "Detached parts often follow failed merges, hardware errors, or manual `DETACH PART` operations; unchecked g.... Timely UC-7.1.65 alerting on 'ClickHouse Detached Parts Accumulation' reduces operational risk and delayed incident response before manual triage or audit channels escalate.",
  "implementation": "If using `system.detached_parts`, emit `database`, `table`, `partition_id`, `name`, `reason` per row or aggregate `count` hourly. If only `system.parts` is available, filter `active=0` and name patterns per your export. Alert on new reasons containing `broken` or sudden partition count jumps.",
  "visualization": "Table (database, table, detached_partitions, detach_reason), Line chart (detached row count).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "ClickHouse — system.detached_parts",
      "url": "https://clickhouse.com/docs/en/operations/system-tables/detached_parts",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `database`** — Planned work for 'ClickHouse Detached Parts Accumulation' shifts `dc` over `span=1h` while `database` remains in-scope for UC-7.1.65. Cross-check CMDB CHG.\n\n2. **Batch `dc` on `clickhouse:parts`** — Scheduled automation elevates `table` without crossing `detached_partitions` > 3. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `database`** — Health checks in `database` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `clickhouse:parts`** — First 48h baseline for `database` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `database`, `exception_reason`, `valid_until`.",
  "equipment": [
    "clickhouse"
  ],
  "equipmentModels": [
    "clickhouse_generic"
  ],
  "status": "draft",
  "lastReviewed": "2025-06-01",
  "splunkVersions": [
    "9.x",
    "8.x"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Scheduled SQL `SELECT database, table, count() cnt FROM system.detached_parts GROUP BY database, table` to HEC — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=clickhouse:parts into database. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`database, detached_partitions, detached_rows, host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch detached parts in ClickHouse so we can clean them up after failed moves or drops before they tie up disk and confuse capacity planning.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
