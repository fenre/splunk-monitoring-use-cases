{
  "$schema": "../../schemas/uc.schema.json",
  "id": "7.5.2",
  "title": "Elasticsearch Shard Allocation Failures",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Elasticsearch Shard Allocation Failures' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_elasticsearch_shard_allocation_failures` (UC-7.5.2), SOC dashboard panel \"Operations — Elasticsearch Shard Allocation Failures\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=database` sourcetype='elasticsearch:shard_allocation' where `index` satisfies detection threshold for UC-7.5.2 ('Elasticsearch Shard Allocation Failures'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `database` / `elasticsearch:shard_allocation` events where `index` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-7.5.2."
  },
  "dataSources": "`sourcetype=elasticsearch:shard_allocation` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom REST scripted input (`_cluster/allocation/explain`, `_cat/shards`) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=database sourcetype=\"elasticsearch:shard_allocation\"\n| where state=\"UNASSIGNED\" OR allocation_decision=\"NO\"\n| stats latest(deciders) as deciders, count by index, shard, prirep\n| sort -count",
  "description": "Unassigned or stuck relocating shards leave data unavailable or at risk; allocation explain output points to disk, routing, or settings issues before outages spread.",
  "value": "Early 'Elasticsearch Shard Allocation Failures' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Ingest `_cat/shards` with `state` filter and, for unassigned primaries, poll `POST _cluster/allocation/explain` on a schedule. Parse `allocate_explanation` and decider names. Alert when any primary shard is unassigned >5 minutes or replica unassigned count exceeds policy.",
  "visualization": "Table (index, shard, state, reason), Single value (unassigned shard count), Timeline of allocation events.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `database`** — Planned work for 'Elasticsearch Shard Allocation Failures' shifts `count` over `span=1h` while `index` remains in-scope for UC-7.5.2. Cross-check CMDB CHG.\n\n2. **Batch `count` on `elasticsearch:shard_allocation`** — Scheduled automation elevates `shard` without crossing configured threshold. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `index`** — Health checks in `database` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `elasticsearch:shard_allocation`** — First 48h baseline for `index` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `index`, `exception_reason`, `valid_until`.",
  "equipment": [
    "elasticsearch"
  ],
  "equipmentModels": [
    "elasticsearch_es"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• In operations we confirm in Kibana or OpenSearch Dashboards and the `_cat` / cluster APIs for that stack.\n• Install and configure the required add-on or app: Custom REST scripted input (`_cluster/allocation/explain`, `_cat/shards`).\n• Ensure the following data sources are available: `sourcetype=elasticsearch:shard_allocation`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest `_cat/shards` with `state` filter and, for unassigned primaries, poll `POST _cluster/allocation/explain` on a schedule. Parse `allocate_explanation` and decider names. Alert when any primary shard is unassigned >5 minutes or replica unassigned count exceeds policy.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=database sourcetype=\"elasticsearch:shard_allocation\"\n| where state=\"UNASSIGNED\" OR allocation_decision=\"NO\"\n| stats latest(deciders) as deciders, count by index, shard, prirep\n| sort -count\n```\n\nUnderstanding this SPL\n\n**Elasticsearch Shard Allocation Failures** — Unassigned or stuck relocating shards leave data unavailable or at risk; allocation explain output points to disk, routing, or settings issues before outages spread.\n\nDocumented **Data sources**: `sourcetype=elasticsearch:shard_allocation`. **App/TA** (typical add-on context): Custom REST scripted input (`_cluster/allocation/explain`, `_cat/shards`). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: database; **sourcetype**: elasticsearch:shard_allocation. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=database, sourcetype=\"elasticsearch:shard_allocation\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where state=\"UNASSIGNED\" OR allocation_decision=\"NO\"` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by index, shard, prirep** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nFor the same time range, compare Splunk results with the engine’s own tools and system views (SQL Server: SQL Server Management Studio and `sys.dm_*`; Oracle: Oracle Enterprise Manager, SQLcl, or `V$` views; MySQL: Workbench or `performance_schema` / `SHOW` output; PostgreSQL: `pg_stat_*` in psql or pgAdmin; MongoDB: mongosh or Atlas metrics; Cassandra: nodetool; Elasticsearch/OpenSearch: Kibana or REST `_cat` / `_cluster/health`; ClickHouse: `system` tables in clickhouse-client; Snowflake: Snowsight or `ACCOUNT_USAGE`; others: the managed PaaS console). Confirm event counts, field names, timestamps, and Splunk role permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (index, shard, state, reason), Single value (unassigned shard count), Timeline of allocation events.",
  "grandmaExplanation": "We follow search cluster health, indexing, and query latency so we can keep log and search services responsive when load or mapping changes.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
