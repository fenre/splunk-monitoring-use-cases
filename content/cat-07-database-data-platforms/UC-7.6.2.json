{
  "$schema": "../../schemas/uc.schema.json",
  "id": "7.6.2",
  "title": "Slow Query Volume Trending",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Slow Query Volume Trending' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_slow_query_volume_trending` (UC-7.6.2), SOC dashboard panel \"Operations — Slow Query Volume Trending\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=db` sourcetype='target_sourcetype' that satisfy the UC-7.6.2 SPL filters for 'Slow Query Volume Trending'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `db` / `target_sourcetype` events outside the detection threshold, or exclude the test entity via `operational_exceptions.csv` with a valid window. Confirm zero alert rows for UC-7.6.2."
  },
  "dataSources": "`index=db` `sourcetype=mysql:slow`, `sourcetype=postgresql:log`, `sourcetype=mssql:query`, `sourcetype=oracle:audit` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Native slow logs, Percona, `pg_stat_statements` export, SQL Server extended events — Universal Forwarder (Splunkbase 617)",
  "spl": "index=db sourcetype IN (\"mysql:slow\",\"postgresql:log\",\"mssql:query\",\"oracle:sql\")\n| eval dur_ms=coalesce(query_time_ms, duration_ms, query_duration*1000)\n| where dur_ms > 1000\n| bin _time span=1d\n| stats count as slow_queries by _time, db_name\n| timechart span=1d sum(slow_queries) as daily_slow_queries by db_name limit=12",
  "description": "Counting queries exceeding a duration threshold per day quantifies database pain for developers and DBAs. Upward trends after releases often indicate missing indexes or plan regressions before p95 latency alerts fire.",
  "value": "Early 'Slow Query Volume Trending' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Tune the millisecond threshold per environment (OLTP vs reporting). Hash or truncate SQL text for cardinality control. Exclude known batch accounts via `user` lookup. Join top patterns to `EXPLAIN` workflow or query store IDs when available. Retention on verbose logs may require summary indexing to `sourcetype=stash`.",
  "visualization": "Stacked column chart (slow queries per day by database), line chart (total slow count), table (top normalized query signatures).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'Slow Query Volume Trending'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `db` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `operational_exceptions.csv` with entity, reason, valid_until.",
  "equipment": [
    "mssql",
    "mysql",
    "oracle",
    "postgresql"
  ],
  "equipmentModels": [
    "oracle_oracle_db",
    "postgresql_pg"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• In operations we cross-check the same window in SQL Server Management Studio, Azure Data Studio, or the Azure SQL portal with `sys.dm_*` views; Oracle Enterprise Manager, SQLcl, or SQL Developer with `V$` views; psql, pgAdmin, or the managed-PostgreSQL console with `pg_stat_*` and replication views; MySQL Workbench, the managed-MySQL console, or `performance_schema` / replica status so live metrics match what Splunk shows.\n• Install and configure the required add-on or app: Native slow logs, Percona, `pg_stat_statements` export, SQL Server extended events.\n• Ensure the following data sources are available: `index=db` `sourcetype=mysql:slow`, `sourcetype=postgresql:log`, `sourcetype=mssql:query`, `sourcetype=oracle:audit`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nTune the millisecond threshold per environment (OLTP vs reporting). Hash or truncate SQL text for cardinality control. Exclude known batch accounts via `user` lookup. Join top patterns to `EXPLAIN` workflow or query store IDs when available. Retention on verbose logs may require summary indexing to `sourcetype=stash`.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=db sourcetype IN (\"mysql:slow\",\"postgresql:log\",\"mssql:query\",\"oracle:sql\")\n| eval dur_ms=coalesce(query_time_ms, duration_ms, query_duration*1000)\n| where dur_ms > 1000\n| bin _time span=1d\n| stats count as slow_queries by _time, db_name\n| timechart span=1d sum(slow_queries) as daily_slow_queries by db_name limit=12\n```\n\nUnderstanding this SPL\n\n**Slow Query Volume Trending** — Counting queries exceeding a duration threshold per day quantifies database pain for developers and DBAs. Upward trends after releases often indicate missing indexes or plan regressions before p95 latency alerts fire.\n\nDocumented **Data sources**: `index=db` `sourcetype=mysql:slow`, `sourcetype=postgresql:log`, `sourcetype=mssql:query`, `sourcetype=oracle:audit`. **App/TA** (typical add-on context): Native slow logs, Percona, `pg_stat_statements` export, SQL Server extended events. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: db.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=db. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **dur_ms** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where dur_ms > 1000` — typically the threshold or rule expression for this monitoring goal.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by _time, db_name** so each row reflects one combination of those dimensions.\n• `timechart` plots the metric over time using **span=1d** buckets with a separate series **by db_name limit=12** — ideal for trending and alerting on this use case.\n\n\nStep 3 — Validate\nFor the same time range, compare Splunk results with the engine’s own tools and system views (SQL Server: SQL Server Management Studio and `sys.dm_*`; Oracle: Oracle Enterprise Manager, SQLcl, or `V$` views; MySQL: Workbench or `performance_schema` / `SHOW` output; PostgreSQL: `pg_stat_*` in psql or pgAdmin; MongoDB: mongosh or Atlas metrics; Cassandra: nodetool; Elasticsearch/OpenSearch: Kibana or REST `_cat` / `_cluster/health`; ClickHouse: `system` tables in clickhouse-client; Snowflake: Snowsight or `ACCOUNT_USAGE`; others: the managed PaaS console). Confirm event counts, field names, timestamps, and Splunk role permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Stacked column chart (slow queries per day by database), line chart (total slow count), table (top normalized query signatures).",
  "grandmaExplanation": "We surface slow and blocking queries so we can fix the worst offenders first and keep applications and batch jobs within the response times we promise.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
