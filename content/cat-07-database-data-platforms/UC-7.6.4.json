{
  "$schema": "../../schemas/uc.schema.json",
  "id": "7.6.4",
  "title": "Database Backup Size Trending",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Database Backup Size Trending' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_database_backup_size_trending` (UC-7.6.4), SOC dashboard panel \"Operations — Database Backup Size Trending\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=db` sourcetype='target_sourcetype' where `_time` satisfies detection threshold for UC-7.6.4 ('Database Backup Size Trending'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `db` / `target_sourcetype` events where `_time` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-7.6.4."
  },
  "dataSources": "`index=db` `sourcetype=mssql:backup`, `sourcetype=mysql:backup`, `sourcetype=oracle:rman` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "RMAN, SQL Server backup history, mysqldump / Percona log parsers, cloud backup APIs — Universal Forwarder (Splunkbase 617)",
  "spl": "index=db sourcetype IN (\"mssql:backup\",\"mysql:backup\",\"oracle:rman\",\"postgresql:backup\")\n| eval size_gb=coalesce(backup_size_gb, round(backup_size_bytes/1073741824,3))\n| where backup_status IN (\"success\",\"Success\",\"completed\") OR isnull(backup_status)\n| bin _time span=1mon\n| stats max(size_gb) as backup_size_gb by _time, database_name\n| timechart span=1mon sum(backup_size_gb) as total_backup_gb by database_name limit=10",
  "description": "Monthly backup size growth forecasts storage for backup appliances and cloud object storage costs. Anomalous jumps can indicate bulk data loads, failed truncations, or ransomware preparation worth investigating.",
  "value": "Early 'Database Backup Size Trending' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Deduplicate overlapping full/diff/incremental jobs with `backup_type`. Include compression ratio if logged for better capacity forecasting. Tag cloud vs on-prem targets separately. Alert on failed backups via a companion search; this UC focuses on growth trend only.",
  "visualization": "Line chart (backup size GB over months), column chart (month-over-month growth %), table (largest databases).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `db`** — Planned work for 'Database Backup Size Trending' shifts `max` over `span=1m` while `_time` remains in-scope for UC-7.6.4. Cross-check CMDB CHG.\n\n2. **Batch `max` on `configured_sourcetype`** — Scheduled automation elevates `database_name` without crossing configured threshold. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `_time`** — Health checks in `db` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for `_time` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `_time`, `exception_reason`, `valid_until`.",
  "equipment": [
    "mssql",
    "mysql",
    "oracle",
    "postgresql"
  ],
  "equipmentModels": [
    "oracle_oracle_db",
    "postgresql_pg"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• In operations we cross-check backup reality in the right console for each engine: `msdb` and SSMS for SQL Server, RMAN and Enterprise Manager (or DBA views) for Oracle, and the postgres or managed-service view for PostgreSQL, alongside Splunk.\n• Install and configure the required add-on or app: RMAN, SQL Server backup history, mysqldump / Percona log parsers, cloud backup APIs.\n• Ensure the following data sources are available: `index=db` `sourcetype=mssql:backup`, `sourcetype=mysql:backup`, `sourcetype=oracle:rman`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nDeduplicate overlapping full/diff/incremental jobs with `backup_type`. Include compression ratio if logged for better capacity forecasting. Tag cloud vs on-prem targets separately. Alert on failed backups via a companion search; this UC focuses on growth trend only.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=db sourcetype IN (\"mssql:backup\",\"mysql:backup\",\"oracle:rman\",\"postgresql:backup\")\n| eval size_gb=coalesce(backup_size_gb, round(backup_size_bytes/1073741824,3))\n| where backup_status IN (\"success\",\"Success\",\"completed\") OR isnull(backup_status)\n| bin _time span=1mon\n| stats max(size_gb) as backup_size_gb by _time, database_name\n| timechart span=1mon sum(backup_size_gb) as total_backup_gb by database_name limit=10\n```\n\nUnderstanding this SPL\n\n**Database Backup Size Trending** — Monthly backup size growth forecasts storage for backup appliances and cloud object storage costs. Anomalous jumps can indicate bulk data loads, failed truncations, or ransomware preparation worth investigating.\n\nDocumented **Data sources**: `index=db` `sourcetype=mssql:backup`, `sourcetype=mysql:backup`, `sourcetype=oracle:rman`. **App/TA** (typical add-on context): RMAN, SQL Server backup history, mysqldump / Percona log parsers, cloud backup APIs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: db.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=db. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **size_gb** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where backup_status IN (\"success\",\"Success\",\"completed\") OR isnull(backup_status)` — typically the threshold or rule expression for this monitoring goal.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by _time, database_name** so each row reflects one combination of those dimensions.\n• `timechart` plots the metric over time using **span=1mon** buckets with a separate series **by database_name limit=10** — ideal for trending and alerting on this use case.\n\n\nStep 3 — Validate\nFor the same time range, compare Splunk results with the engine’s own tools and system views (SQL Server: SQL Server Management Studio and `sys.dm_*`; Oracle: Oracle Enterprise Manager, SQLcl, or `V$` views; MySQL: Workbench or `performance_schema` / `SHOW` output; PostgreSQL: `pg_stat_*` in psql or pgAdmin; MongoDB: mongosh or Atlas metrics; Cassandra: nodetool; Elasticsearch/OpenSearch: Kibana or REST `_cat` / `_cluster/health`; ClickHouse: `system` tables in clickhouse-client; Snowflake: Snowsight or `ACCOUNT_USAGE`; others: the managed PaaS console). Confirm event counts, field names, timestamps, and Splunk role permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (backup size GB over months), column chart (month-over-month growth %), table (largest databases).",
  "grandmaExplanation": "We check backup and snapshot success and growth so we can trust restores and long-term storage plans for regulation and for real incidents.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
