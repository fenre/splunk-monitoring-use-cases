{
  "$schema": "../../schemas/uc.schema.json",
  "id": "7.1.60",
  "title": "ClickHouse Per-Query Memory Usage and OOM Risk",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance",
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`system.query_log` memory fields (`memory_usage`, `peak_memory_usage`) as `clickhouse:query_log`",
  "app": "HEC export of finished queries from `query_log`",
  "spl": "index=database sourcetype=\"clickhouse:query_log\"\n| eval mem_mb=round(tonumber(peak_memory_usage)/1048576,1)\n| where mem_mb > 1024 OR match(_raw,\"MEMORY_LIMIT_EXCEEDED\")\n| stats max(mem_mb) as peak_mb sum(read_rows) as rows values(query_id) as qids by user, host\n| sort -peak_mb",
  "description": "Runaway `peak_memory_usage` on a few queries is the usual precursor to server-wide OOM kills under concurrent load. Tracking peak memory by `user` and `host` lets you enforce query limits and isolate bad SQL before the process restarts.",
  "value": "Stabilizes shared analytics clusters and avoids surprise restarts during month-end reporting peaks.",
  "implementation": "Confirm `peak_memory_usage` is present in your `query_log` version; add `max_memory_usage_for_user` policies in ClickHouse for prevention. In Splunk, schedule alert for peak_mb above tenant-specific limits (example 1GB). Join with LDAP/group lookup for chargeback.",
  "visualization": "Bar chart (peak_mb by user), Table (qids, rows, peak_mb).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "ClickHouse — Memory limit settings",
      "url": "https://clickhouse.com/docs/en/operations/settings/query-complexity#settings_max_memory_usage"
    }
  ],
  "equipment": [
    "clickhouse"
  ],
  "status": "draft",
  "lastReviewed": "2025-06-01",
  "splunkVersions": [
    "9.x",
    "8.x"
  ],
  "reviewer": "catalog-team",
  "knownFalsePositives": "Large merges, dictionary reloads, and Kafka consumer catch-up can temporarily stress CPU or I/O; distinguish backlog from a stuck mutation using the system tables in ClickHouse.",
  "grandmaExplanation": "We watch ClickHouse Per-Query Memory Usage and OOM Risk so we can keep this part of the data platform within the capacity and quality targets our teams expect."
}
