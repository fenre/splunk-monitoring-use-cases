{
  "$schema": "../../schemas/uc.schema.json",
  "id": "7.5.14",
  "title": "Elasticsearch ILM Policy Failures",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Elasticsearch ILM Policy Failures' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_elasticsearch_ilm_policy_failures` (UC-7.5.14), SOC dashboard panel \"Operations — Elasticsearch ILM Policy Failures\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-7.5.14 positive (Elasticsearch, ILM, Policy, Failures): ingest synthetic events into `index=database` sourcetype='elasticsearch:ilm_status' where `count` grouped by `index`, `policy` satisfies `action_time_millis` > 3600000 for 'Elasticsearch ILM Policy Failures'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-7.5.14 negative (Policy Failures): ingest benign `database` / `elasticsearch:ilm_status` events where `index`, `policy` stays below `action_time_millis` > 3600000, or exclude the test entity via `ops_exception_register.csv`. Variant 12 confirms zero alert rows for 'Elasticsearch ILM Policy Failures'."
  },
  "dataSources": "`sourcetype=elasticsearch:ilm_status` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom REST scripted input (`_ilm/explain`) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=database sourcetype=\"elasticsearch:ilm_status\"\n| where step=\"ERROR\" OR action_time_millis > 3600000\n| stats count as error_count, latest(failed_step) as failed_step, latest(step_info) as reason by index, policy\n| sort -error_count",
  "description": "Alerts on UC-7.5.14 — Elasticsearch, ILM, Policy, Failures — using `index=database` sourcetype `elasticsearch:ilm_status`, grouped by `index`, `policy`. Outputs `count` each `the scheduled window`; alert when `action_time_millis` > 3600000. Primary focus: Policy Failures. Purpose-built for 'Elasticsearch ILM Policy Failures' in Operations workflows.",
  "value": "Tracking `activity` via UC-7.5.14 on `elasticsearch:ilm_status` feeds improves response to 'Elasticsearch ILM Policy Failures' and reduces operational risk and delayed incident response.",
  "implementation": "Poll `GET */_ilm/explain` periodically and extract indices where `step` equals `ERROR`. Capture the `failed_step`, `step_info`, and `phase_time` for root cause analysis. Alert on any index stuck in ERROR. Also monitor indices that have been in the same phase longer than expected (e.g., hot phase > 30 days when policy says 7 days).",
  "visualization": "Table (indices in error with reason), Single value (error count), Bar chart (errors by policy).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `database` (UC-7.5.14)** — Planned work for 'Elasticsearch ILM Policy Failures' shifts `count` over `span=1h` while `index`, `policy` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `elasticsearch:ilm_status`** — Scheduled automation elevates `policy` without crossing `action_time_millis` > 3600000 for UC-7.5.14. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `index`, `policy`** — Health checks in `database` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `elasticsearch:ilm_status`** — First 48h baseline for UC-7.5.14 on `index` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `index`, `exception_reason`, `valid_until`.",
  "equipment": [
    "elasticsearch"
  ],
  "equipmentModels": [
    "elasticsearch_es"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• In operations we confirm in Kibana or OpenSearch Dashboards and the `_cat` / cluster APIs for that stack.\n• Install and configure the required add-on or app: Custom REST scripted input (`_ilm/explain`).\n• Ensure the following data sources are available: `sourcetype=elasticsearch:ilm_status`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nPoll `GET */_ilm/explain` periodically and extract indices where `step` equals `ERROR`. Capture the `failed_step`, `step_info`, and `phase_time` for root cause analysis. Alert on any index stuck in ERROR. Also monitor indices that have been in the same phase longer than expected (e.g., hot phase > 30 days when policy says 7 days).\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=database sourcetype=\"elasticsearch:ilm_status\"\n| where step=\"ERROR\" OR action_time_millis > 3600000\n| stats count as error_count, latest(failed_step) as failed_step, latest(step_info) as reason by index, policy\n| sort -error_count\n```\n\nUnderstanding this SPL\n\n**Elasticsearch ILM Policy Failures** — Failed ILM transitions leave indices in the wrong lifecycle phase — hot data stays on expensive storage, old data never deletes, rollover stops creating new indices. Silent failures accumulate until disk fills.\n\nDocumented **Data sources**: `sourcetype=elasticsearch:ilm_status`. **App/TA** (typical add-on context): Custom REST scripted input (`_ilm/explain`). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: database; **sourcetype**: elasticsearch:ilm_status. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=database, sourcetype=\"elasticsearch:ilm_status\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where step=\"ERROR\" OR action_time_millis > 3600000` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by index, policy** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nFor the same time range, compare Splunk results with the engine’s own tools and system views (SQL Server: SQL Server Management Studio and `sys.dm_*`; Oracle: Oracle Enterprise Manager, SQLcl, or `V$` views; MySQL: Workbench or `performance_schema` / `SHOW` output; PostgreSQL: `pg_stat_*` in psql or pgAdmin; MongoDB: mongosh or Atlas metrics; Cassandra: nodetool; Elasticsearch/OpenSearch: Kibana or REST `_cat` / `_cluster/health`; ClickHouse: `system` tables in clickhouse-client; Snowflake: Snowsight or `ACCOUNT_USAGE`; others: the managed PaaS console). Confirm event counts, field names, timestamps, and Splunk role permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (indices in error with reason), Single value (error count), Bar chart (errors by policy).",
  "grandmaExplanation": "We follow search cluster health, indexing, and query latency so we can keep log and search services responsive when load or mapping changes.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
