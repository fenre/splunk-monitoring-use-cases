{
  "$schema": "../../schemas/uc.schema.json",
  "id": "7.5.4",
  "title": "OpenSearch Search Latency",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'OpenSearch Search Latency' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_opensearch_search_latency` (UC-7.5.4), SOC dashboard panel \"Operations — OpenSearch Search Latency\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=database` sourcetype='opensearch:search_latency' where `cluster_name` satisfies `took_ms` > 500 for UC-7.5.4 ('OpenSearch Search Latency'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `database` / `opensearch:search_latency` events where `cluster_name` stays below `took_ms` > 500, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-7.5.4."
  },
  "dataSources": "`sourcetype=opensearch:search_latency`, `sourcetype=opensearch:slowlog` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom scripted input (`_nodes/stats` search, slow log), OpenSearch slow search log — Universal Forwarder (Splunkbase 617)",
  "spl": "index=database sourcetype=\"opensearch:search_latency\" OR sourcetype=\"opensearch:slowlog\"\n| eval took_ms=coalesce(took_ms, took)\n| where took_ms > 500\n| timechart span=5m perc95(took_ms) as p95_ms, perc99(took_ms) as p99_ms by cluster_name",
  "description": "P95/P99 query latency directly affects application UX; separating queue time from query time narrows tuning to thread pools vs. mappings and shards.",
  "value": "Early 'OpenSearch Search Latency' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Ingest node-level search metrics (`primaries.search.query_time_in_millis` / `query_total`) for derived latency, and/or enable slow search logging and forward with a dedicated sourcetype. Baseline p95 per cluster; alert when p95 exceeds threshold for 15+ minutes. Correlate with heap GC and segment merges.",
  "visualization": "Line chart (p95/p99 search latency), Table (slow queries from slowlog), Histogram of `took_ms`.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `database`** — Planned work for 'OpenSearch Search Latency' shifts `count` over `span=5m` while `cluster_name` remains in-scope for UC-7.5.4. Cross-check CMDB CHG.\n\n2. **Batch `count` on `opensearch:search_latency`** — Scheduled automation elevates `took_ms` without crossing `took_ms` > 500. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `cluster_name`** — Health checks in `database` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `opensearch:search_latency`** — First 48h baseline for `cluster_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `cluster_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "elasticsearch"
  ],
  "equipmentModels": [
    "elasticsearch_opensearch"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• In operations we confirm in Kibana or OpenSearch Dashboards and the `_cat` / cluster APIs for that stack.\n• Install and configure the required add-on or app: Custom scripted input (`_nodes/stats` search, slow log), OpenSearch slow search log.\n• Ensure the following data sources are available: `sourcetype=opensearch:search_latency`, `sourcetype=opensearch:slowlog`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest node-level search metrics (`primaries.search.query_time_in_millis` / `query_total`) for derived latency, and/or enable slow search logging and forward with a dedicated sourcetype. Baseline p95 per cluster; alert when p95 exceeds threshold for 15+ minutes. Correlate with heap GC and segment merges.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=database sourcetype=\"opensearch:search_latency\" OR sourcetype=\"opensearch:slowlog\"\n| eval took_ms=coalesce(took_ms, took)\n| where took_ms > 500\n| timechart span=5m perc95(took_ms) as p95_ms, perc99(took_ms) as p99_ms by cluster_name\n```\n\nUnderstanding this SPL\n\n**OpenSearch Search Latency** — P95/P99 query latency directly affects application UX; separating queue time from query time narrows tuning to thread pools vs. mappings and shards.\n\nDocumented **Data sources**: `sourcetype=opensearch:search_latency`, `sourcetype=opensearch:slowlog`. **App/TA** (typical add-on context): Custom scripted input (`_nodes/stats` search, slow log), OpenSearch slow search log. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: database; **sourcetype**: opensearch:search_latency, opensearch:slowlog. Those sourcetypes align with what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=database, sourcetype=\"opensearch:search_latency\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **took_ms** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where took_ms > 500` — typically the threshold or rule expression for this monitoring goal.\n• `timechart` plots the metric over time using **span=5m** buckets with a separate series **by cluster_name** — ideal for trending and alerting on this use case.\n\n\nStep 3 — Validate\nFor the same time range, compare Splunk results with the engine’s own tools and system views (SQL Server: SQL Server Management Studio and `sys.dm_*`; Oracle: Oracle Enterprise Manager, SQLcl, or `V$` views; MySQL: Workbench or `performance_schema` / `SHOW` output; PostgreSQL: `pg_stat_*` in psql or pgAdmin; MongoDB: mongosh or Atlas metrics; Cassandra: nodetool; Elasticsearch/OpenSearch: Kibana or REST `_cat` / `_cluster/health`; ClickHouse: `system` tables in clickhouse-client; Snowflake: Snowsight or `ACCOUNT_USAGE`; others: the managed PaaS console). Confirm event counts, field names, timestamps, and Splunk role permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (p95/p99 search latency), Table (slow queries from slowlog), Histogram of `took_ms`.",
  "grandmaExplanation": "We surface slow and blocking queries so we can fix the worst offenders first and keep applications and batch jobs within the response times we promise.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
