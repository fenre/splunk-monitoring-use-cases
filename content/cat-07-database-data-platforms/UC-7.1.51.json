{
  "$schema": "../../schemas/uc.schema.json",
  "id": "7.1.51",
  "title": "MongoDB Sharded Cluster Chunk Migration Failures",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Operations"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'MongoDB Sharded Cluster Chunk Migration Failures' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_mongodb_sharded_cluster_chunk_migration_failures` (UC-7.1.51), SOC dashboard panel \"Operations — MongoDB Sharded Cluster Chunk Migration Failures\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-7.1.51 positive (MongoDB, Sharded, Cluster, Chunk, Migration): ingest synthetic events into `index=database` sourcetype='mongodb:log' where `values` grouped by `host`, `ns`, `_time` satisfies `failures` >= 3 for 'MongoDB Sharded Cluster Chunk Migration Failures'. Validate filter `(\"FailedToStartChunkMigration\" OR \"migration failed\" OR \"mov`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-7.1.51 negative (Migration Failures): ingest benign `database` / `mongodb:log` events where `host`, `ns`, `_time` stays below `failures` >= 3, or exclude the test entity via `ops_exception_register.csv`. Variant 13 confirms zero alert rows for 'MongoDB Sharded Cluster Chunk Migration Failures'."
  },
  "dataSources": "Mongos/router logs (`moveChunk`, `MigrationFailed`, `ChunkMoveError`), sourcetype `mongodb:log` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Add-on for MongoDB log forwarding, or HEC from MongoDB Atlas / self-managed `mongod`/`mongos` log paths — Universal Forwarder (Splunkbase 617)",
  "spl": "index=database sourcetype=\"mongodb:log\"\n| search (\"FailedToStartChunkMigration\" OR \"migration failed\" OR \"moveChunk failed\" OR \"ChunkMigrationFailed\")\n| rex field=message \"ns:\\s+(?<ns>[^\\s]+)\"\n| bin _time span=5m\n| stats count as failures values(message) as samples by host, ns, _time\n| where failures >= 3",
  "description": "Monitors UC-7.1.51 — MongoDB, Sharded, Cluster, Chunk, Migration — using `index=database` sourcetype `mongodb:log`, grouped by `host`, `ns`, `_time`. Outputs `values` each `5m`; alert when `failures` >= 3. Filter chain includes filter `(\"FailedToStartChunkMigration\" OR \"migration failed\" OR \"mov`. Purpose-built for 'MongoDB Sharded Cluster Chunk Migration Failures' in Operations workflows.",
  "value": "UC-7.1.51 gives Operations teams earlier visibility into Migration Failures through `mongodb:log` feeds, lowering operational risk and delayed incident response.",
  "implementation": "Forward `mongos` and config server logs to Splunk; assign `mongodb:log` in `props.conf` with correct `TIME_FORMAT` for your MongoDB log version. Tag `host` with mongos identity. Create a correlation lookup for approved maintenance windows. Alert when three or more failure signatures occur within five minutes per namespace.",
  "visualization": "Timeline (failures over time), Table (host, ns, failures, sample message), Map (shard host roles if enriched).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "MongoDB — sharding chunk migration",
      "url": "https://www.mongodb.com/docs/manual/core/sharding-balancer-administration/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "MongoDB log messages reference",
      "url": "https://www.mongodb.com/docs/manual/reference/log-messages/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `database` (UC-7.1.51)** — Planned work for 'MongoDB Sharded Cluster Chunk Migration Failures' shifts `values` over `span=5m` while `host`, `ns`, `_time` remains in-scope. Applies filter `(\"FailedToStartChunkMigration\" OR \"migration failed\" OR \"mov`. Cross-check CMDB CHG.\n\n2. **Batch `values` on `mongodb:log`** — Scheduled automation elevates `ns` without crossing `failures` >= 3 for UC-7.1.51. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`, `ns`, `_time`** — Health checks in `database` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `mongodb:log`** — First 48h baseline for UC-7.1.51 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "mongodb"
  ],
  "equipmentModels": [
    "mongodb_mongod"
  ],
  "status": "draft",
  "lastReviewed": "2025-06-01",
  "splunkVersions": [
    "9.x",
    "8.x"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for MongoDB log forwarding, or HEC from MongoDB Atlas / self-managed `mongod`/`mongos` log paths — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=mongodb:log into database. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, failures, host, ns`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for repeated or suspicious sign-in activity on our databases so we can catch brute-force and misconfiguration before they become account takeovers.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
