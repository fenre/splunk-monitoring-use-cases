{
  "$schema": "../../schemas/uc.schema.json",
  "id": "7.4.21",
  "title": "Redis used_memory Headroom vs maxmemory",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Capacity",
    "Availability"
  ],
  "splunkPillar": "Observability",
  "dataSources": "Redis INFO fields `used_memory`, `maxmemory` (0 means no hard limit)",
  "app": "Scripted `redis-cli INFO memory` or metrics exporter",
  "spl": "index=middleware sourcetype=\"redis:info\"\n| where maxmemory > 0\n| eval mem_pct=round(used_memory/maxmemory*100,2)\n| where mem_pct > 90\n| timechart span=5m max(mem_pct) as used_pct by host, role",
  "description": "When used_memory approaches maxmemory, eviction or OOM behavior follows. Capacity monitoring uses the ratio to maxmemory as the primary early warning.",
  "value": "Prevents sudden eviction storms and write failures on memory-bound Redis clusters.",
  "implementation": "Poll INFO memory section every minute. Treat maxmemory=0 as unlimited and skip ratio alerts (use absolute used_memory trends instead). Separate primary and replica roles. Pair with UC eviction rate monitoring.",
  "visualization": "Gauge (used % of max), Line chart (used_memory), Table (hosts over threshold).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Redis memory optimization",
      "url": "https://redis.io/docs/latest/develop/use/keyspace/"
    }
  ],
  "equipment": [
    "redis"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases"
}
