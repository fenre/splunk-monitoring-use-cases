{
  "$schema": "../../schemas/uc.schema.json",
  "id": "7.4.21",
  "title": "Redis used_memory Headroom vs maxmemory",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Capacity",
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Redis used_memory Headroom vs maxmemory' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_redis_used_memory_headroom_vs_maxmemory` (UC-7.4.21), SOC dashboard panel \"Operations — Redis used_memory Headroom vs maxmemory\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-7.4.21 positive (Redis, used, memory, Headroom, maxmemory): ingest synthetic events into `index=middleware` sourcetype='redis:info' where `used_pct` grouped by `host`, `role` satisfies `maxmemory` > 0, or `mem_pct` > 90 for 'Redis used_memory Headroom vs maxmemory'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-7.4.21 negative (vs maxmemory): ingest benign `middleware` / `redis:info` events where `host`, `role` stays below `maxmemory` > 0, or `mem_pct` > 90, or exclude the test entity via `ops_exception_register.csv`. Variant 7 confirms zero alert rows for 'Redis used_memory Headroom vs maxmemory'."
  },
  "dataSources": "Redis INFO fields `used_memory`, `maxmemory` (0 means no hard limit) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Scripted `redis-cli INFO memory` or metrics exporter — Universal Forwarder (Splunkbase 617)",
  "spl": "index=middleware sourcetype=\"redis:info\"\n| where maxmemory > 0\n| eval mem_pct=round(used_memory/maxmemory*100,2)\n| where mem_pct > 90\n| timechart span=5m max(mem_pct) as used_pct by host, role",
  "description": "Detects UC-7.4.21 — Redis, used, memory, Headroom, maxmemory — using `index=middleware` sourcetype `redis:info`, grouped by `host`, `role`. Outputs `used_pct` each `5m`; alert when `maxmemory` > 0, or `mem_pct` > 90. Primary focus: vs maxmemory. Purpose-built for 'Redis used_memory Headroom vs maxmemory' in Operations workflows.",
  "value": "UC-7.4.21 operationalises 'Redis used_memory Headroom vs maxmemory' against `redis:info` feeds so operators reduce operational risk and delayed incident response without manual correlation.",
  "implementation": "Poll INFO memory section every minute. Treat maxmemory=0 as unlimited and skip ratio alerts (use absolute used_memory trends instead). Separate primary and replica roles. Pair with UC eviction rate monitoring.",
  "visualization": "Gauge (used % of max), Line chart (used_memory), Table (hosts over threshold).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Redis memory optimization",
      "url": "https://redis.io/docs/latest/develop/use/keyspace/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `middleware` (UC-7.4.21)** — Planned work for 'Redis used_memory Headroom vs maxmemory' shifts `used_pct` over `span=5m` while `host`, `role` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `max` on `redis:info`** — Scheduled automation elevates `role` without crossing `maxmemory` > 0 for UC-7.4.21. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`, `role`** — Health checks in `middleware` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `redis:info`** — First 48h baseline for UC-7.4.21 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "redis"
  ],
  "equipmentModels": [
    "redis_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Scripted `redis-cli INFO memory` or metrics exporter — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=redis:info into middleware. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, maxmemory, mem_pct, role`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We follow memory, persistence, and replication on Redis so we can catch eviction and lag before a cache or queue tier becomes the bottleneck for applications.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
