{
  "$schema": "../../schemas/uc.schema.json",
  "id": "7.1.53",
  "title": "ClickHouse Merge Rate vs Insert Rate Imbalance (Part Accumulation)",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance",
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`system.parts` / `system.parts_columns` snapshots as `clickhouse:parts`, merge activity as `clickhouse:merges`",
  "app": "Custom HEC or DB Connect export from ClickHouse system tables; optional Splunk Connect for Kafka for `system` table streams",
  "spl": "index=database sourcetype=\"clickhouse:parts\"\n| bin _time span=15m\n| stats sum(active_parts) as parts by database, table, host, _time\n| join type=inner database table host _time [\n    search index=database sourcetype=\"clickhouse:merges\"\n    | bin _time span=15m\n    | stats count as merge_events by database, table, host, _time\n  ]\n| streamstats window=4 global=f avg(parts) as parts_baseline by database, table, host\n| where parts > parts_baseline*1.5 AND merge_events < 1",
  "description": "When inserts create parts faster than background merges retire them, part count balloons toward `Too many parts` errors and queries slow dramatically. Comparing part growth to merge activity surfaces the imbalance before merges fall permanently behind.",
  "value": "Avoids emergency `OPTIMIZE` fire drills and read outages caused by wide part counts on hot tables.",
  "implementation": "Schedule a SQL job (or DB Connect query) every five minutes selecting `database`, `table`, `sum(active)` as `active_parts` from `system.parts` group by database, table. Emit to HEC with sourcetype `clickhouse:parts`. Separately log `system.merges` rows or count merge threads as `clickhouse:merges`. Set `props.conf` TIMESTAMP_FIELDS. Alert when active_parts climbs 50% above a four-window baseline while merge_events drop toward zero.",
  "visualization": "Dual-axis line (active_parts vs merge_events), Table (database, table, growth rate).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "ClickHouse — MergeTree merges",
      "url": "https://clickhouse.com/docs/en/engines/table-engines/mergetree-family/mergetree"
    },
    {
      "title": "ClickHouse — system.parts",
      "url": "https://clickhouse.com/docs/en/operations/system-tables/parts"
    }
  ],
  "equipment": [
    "clickhouse",
    "db_connect",
    "kafka"
  ],
  "status": "draft",
  "lastReviewed": "2025-06-01",
  "splunkVersions": [
    "9.x",
    "8.x"
  ],
  "reviewer": "catalog-team"
}
