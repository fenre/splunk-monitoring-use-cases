{
  "$schema": "../../schemas/uc.schema.json",
  "id": "7.1.11",
  "title": "Buffer Cache Hit Ratio",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Buffer Cache Hit Ratio' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_buffer_cache_hit_ratio` (UC-7.1.11), SOC dashboard panel \"Operations — Buffer Cache Hit Ratio\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-7.1.11 positive (Buffer, Cache, Hit, Ratio): ingest synthetic events into `index=database` sourcetype='dbconnect:perf_counters' where `hit_ratio` grouped by `instance_name` satisfies `hit_ratio` < 95 for 'Buffer Cache Hit Ratio'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-7.1.11 negative (Hit Ratio): ingest benign `database` / `dbconnect:perf_counters` events where `instance_name` stays below `hit_ratio` < 95, or exclude the test entity via `ops_exception_register.csv`. Variant 5 confirms zero alert rows for 'Buffer Cache Hit Ratio'."
  },
  "dataSources": "SQL Server performance counters, PostgreSQL `pg_stat_bgwriter` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "DB Connect, performance counters — Universal Forwarder (Splunkbase 617)",
  "spl": "index=database sourcetype=\"dbconnect:perf_counters\"\n| where counter_name=\"Buffer cache hit ratio\"\n| timechart span=15m avg(cntr_value) as hit_ratio by instance_name\n| where hit_ratio < 95",
  "description": "Identifies 'Buffer Cache Hit Ratio' patterns (UC-7.1.11) sourced from `index=database` sourcetype `dbconnect:perf_counters`. Threshold bundle: `hit_ratio` < 95; aggregation grain: `instance_name`; metrics: `hit_ratio`. Watch terms: Buffer, Cache, Hit, Ratio.",
  "value": "UC-7.1.11 gives Operations teams earlier visibility into Hit Ratio through `dbconnect:perf_counters` feeds, lowering operational risk and delayed incident response.",
  "implementation": "Poll buffer cache performance counters via DB Connect every 15 minutes. Alert when hit ratio drops below 95% for sustained periods. Correlate with memory pressure and query workload changes.",
  "visualization": "Gauge (buffer cache hit ratio), Line chart (hit ratio over time), Single value (current hit ratio %).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk — DB Connect",
      "url": "https://docs.splunk.com/Documentation/DBX/latest/DeployDBX/WhatisDBX",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `database` (UC-7.1.11)** — Planned work for 'Buffer Cache Hit Ratio' shifts `hit_ratio` over `span=15m` while `instance_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `dbconnect:perf_counters`** — Scheduled automation elevates `src` without crossing `hit_ratio` < 95 for UC-7.1.11. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `instance_name`** — Health checks in `database` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `dbconnect:perf_counters`** — First 48h baseline for UC-7.1.11 on `instance_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `instance_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "db_connect",
    "mssql",
    "postgresql"
  ],
  "equipmentModels": [
    "postgresql_pg"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• In operations we cross-check backup reality in the right console for each engine: `msdb` and SSMS for SQL Server, RMAN and Enterprise Manager (or DBA views) for Oracle, and the postgres or managed-service view for PostgreSQL, alongside Splunk.\n• Install and configure the required add-on or app: DB Connect, performance counters.\n• Ensure the following data sources are available: SQL Server performance counters, PostgreSQL `pg_stat_bgwriter`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nPoll buffer cache performance counters via DB Connect every 15 minutes. Alert when hit ratio drops below 95% for sustained periods. Correlate with memory pressure and query workload changes.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=database sourcetype=\"dbconnect:perf_counters\"\n| where counter_name=\"Buffer cache hit ratio\"\n| timechart span=15m avg(cntr_value) as hit_ratio by instance_name\n| where hit_ratio < 95\n```\n\nUnderstanding this SPL\n\n**Buffer Cache Hit Ratio** — Low buffer cache hit ratio means excessive disk I/O. Monitoring guides memory allocation decisions.\n\nDocumented **Data sources**: SQL Server performance counters, PostgreSQL `pg_stat_bgwriter`. **App/TA** (typical add-on context): DB Connect, performance counters. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: database; **sourcetype**: dbconnect:perf_counters. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=database, sourcetype=\"dbconnect:perf_counters\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where counter_name=\"Buffer cache hit ratio\"` — typically the threshold or rule expression for this monitoring goal.\n• `timechart` plots the metric over time using **span=15m** buckets with a separate series **by instance_name** — ideal for trending and alerting on this use case.\n• Filters the current rows with `where hit_ratio < 95` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nFor the same time range, compare Splunk results with the engine’s own tools and system views (SQL Server: SQL Server Management Studio and `sys.dm_*`; Oracle: Oracle Enterprise Manager, SQLcl, or `V$` views; MySQL: Workbench or `performance_schema` / `SHOW` output; PostgreSQL: `pg_stat_*` in psql or pgAdmin; MongoDB: mongosh or Atlas metrics; Cassandra: nodetool; Elasticsearch/OpenSearch: Kibana or REST `_cat` / `_cluster/health`; ClickHouse: `system` tables in clickhouse-client; Snowflake: Snowsight or `ACCOUNT_USAGE`; others: the managed PaaS console). Confirm event counts, field names, timestamps, and Splunk role permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Gauge (buffer cache hit ratio), Line chart (hit ratio over time), Single value (current hit ratio %).",
  "grandmaExplanation": "We follow buffer and cache hit rates so we can see when memory is too small for the working set and query latency starts to depend on disk more than it should.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
