{
  "$schema": "../../schemas/uc.schema.json",
  "id": "11.3.23",
  "title": "SharePoint Search Crawl Health",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'SharePoint Search Crawl Health' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_sharepoint_search_crawl_health` (UC-11.3.23), SOC dashboard panel \"Operations — SharePoint Search Crawl Health\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-11.3.23 positive (SharePoint, Search, Crawl, Health): ingest synthetic events into `index=sharepoint` sourcetype='sharepoint:search_crawl' where `unique_items` grouped by `_time`, `ContentSource`, `error_type` satisfies `crawl_errors` > 0 for 'SharePoint Search Crawl Health'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-11.3.23 negative (Crawl Health): ingest benign `sharepoint` / `sharepoint:search_crawl` events where `_time`, `ContentSource`, `error_type` stays below `crawl_errors` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 8 confirms zero alert rows for 'SharePoint Search Crawl Health'."
  },
  "dataSources": "Search Administration crawl logs, Get-SPEnterpriseSearchCrawlContentSource (on-prem), Search & Intelligence admin reports (M365) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom (SharePoint Search Admin API) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=sharepoint sourcetype=\"sharepoint:search_crawl\"\n| eval error_type=case(match(ErrorLevel, \"Error|Critical|Failure\"), \"Error\", match(ErrorLevel, \"Warning\"), \"Warning\", 1==1, \"Info\")\n| where error_type=\"Error\" OR error_type=\"Warning\"\n| bin _time span=1h\n| stats count as crawl_errors, dc(ItemId) as unique_items, latest(LastCrawlTime) as last_crawl by _time, ContentSource, error_type\n| where crawl_errors > 0\n| sort -crawl_errors\n| table _time, ContentSource, error_type, crawl_errors, unique_items, last_crawl",
  "description": "Detects UC-11.3.23 — SharePoint, Search, Crawl, Health — using `index=sharepoint` sourcetype `sharepoint:search_crawl`, grouped by `_time`, `ContentSource`, `error_type`. Outputs `unique_items` each `1h`; alert when `crawl_errors` > 0. Primary focus: Crawl Health. Purpose-built for 'SharePoint Search Crawl Health' in Operations workflows.",
  "value": "UC-11.3.23 accelerates insight into Crawl Health ('SharePoint Search Crawl Health') by tracking `unique_items` on `sharepoint:search_crawl` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "For on-prem SharePoint, query crawl logs via Search Admin API or `Get-SPEnterpriseSearchCrawlLog` and stream to Splunk. For M365, use Search & Intelligence admin center APIs or export crawl health reports. Ingest crawl start/end, error count, item count, and last successful crawl per content source. Alert on error count spike (>10 errors in 1 hour) or last successful crawl >24 hours ago for critical sources. Track crawl duration and index freshness.",
  "visualization": "Line chart (crawl errors over time), Table (content sources with errors), Single value (sources with stale index), Bar chart (errors by content source).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable; primary SPL uses raw or vendor-normalized event fields.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `sharepoint` (UC-11.3.23)** — Planned work for 'SharePoint Search Crawl Health' shifts `unique_items` over `span=1h` while `_time`, `ContentSource`, `error_type` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `dc` on `sharepoint:search_crawl`** — Scheduled automation elevates `ContentSource` without crossing `crawl_errors` > 0 for UC-11.3.23. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `_time`, `ContentSource`, `error_type`** — Health checks in `sharepoint` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `sharepoint:search_crawl`** — First 48h baseline for UC-11.3.23 on `_time` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `_time`, `exception_reason`, `valid_until`.",
  "equipment": [
    "m365",
    "sharepoint"
  ],
  "equipmentModels": [
    "m365_generic",
    "sharepoint_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Custom (SharePoint Search Admin API).\n• Ensure the following data sources are available: Search Administration crawl logs, Get-SPEnterpriseSearchCrawlContentSource (on-prem), Search & Intelligence admin reports (M365).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nFor on-prem SharePoint, query crawl logs via Search Admin API or `Get-SPEnterpriseSearchCrawlLog` and stream to Splunk. For M365, use Search & Intelligence admin center APIs or export crawl health reports. Ingest crawl start/end, error count, item count, and last successful crawl per content source. Alert on error count spike (>10 errors in 1 hour) or last successful crawl >24 hours ago for critical sources. Track crawl duration and index freshness.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=sharepoint sourcetype=\"sharepoint:search_crawl\"\n| eval error_type=case(match(ErrorLevel, \"Error|Critical|Failure\"), \"Error\", match(ErrorLevel, \"Warning\"), \"Warning\", 1==1, \"Info\")\n| where error_type=\"Error\" OR error_type=\"Warning\"\n| bin _time span=1h\n| stats count as crawl_errors, dc(ItemId) as unique_items, latest(LastCrawlTime) as last_crawl by _time, ContentSource, error_type\n| where crawl_errors > 0\n| sort -crawl_errors\n| table _time, ContentSource, error_type, crawl_errors, unique_items, last_crawl\n```\n\nUnderstanding this SPL\n\n**SharePoint Search Crawl Health** — Crawl errors, stale content, and index freshness affect findability. Failed crawls leave content undiscoverable; slow crawls delay new content visibility.\n\nDocumented **Data sources**: Search Administration crawl logs, Get-SPEnterpriseSearchCrawlContentSource (on-prem), Search & Intelligence admin reports (M365). **App/TA** (typical add-on context): Custom (SharePoint Search Admin API). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: sharepoint; **sourcetype**: sharepoint:search_crawl. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=sharepoint, sourcetype=\"sharepoint:search_crawl\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **error_type** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where error_type=\"Error\" OR error_type=\"Warning\"` — typically the threshold or rule expression for this monitoring goal.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by _time, ContentSource, error_type** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where crawl_errors > 0` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n• Pipeline stage (see **SharePoint Search Crawl Health**): table _time, ContentSource, error_type, crawl_errors, unique_items, last_crawl\n\n\nStep 3 — Validate\nCompare with SharePoint admin, storage metrics, and search service (if applicable) in the same farm or Microsoft 365 tenant for the time window.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (crawl errors over time), Table (content sources with errors), Single value (sources with stale index), Bar chart (errors by content source).",
  "grandmaExplanation": "We use this to stay ahead of email and collaboration problems, so the team is not the last to know when something drifts or breaks.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
