{
  "$schema": "../../schemas/uc.schema.json",
  "id": "11.4.4",
  "title": "SMTP Authentication and Relay Policy Violations",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'SMTP Authentication and Relay Policy Violations' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_smtp_authentication_and_relay_policy_violations` (UC-11.4.4), SOC dashboard panel \"Operations — SMTP Authentication and Relay Policy Violations\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-11.4.4 positive (SMTP, Authentication, and, Relay, Policy): ingest synthetic events into `index=mail` sourcetype='syslog' where `count` grouped by `src`, `sasl_user`, `action` satisfies `count` > 10 for 'SMTP Authentication and Relay Policy Violations'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-11.4.4 negative (Policy Violations): ingest benign `mail` / `syslog` events where `src`, `sasl_user`, `action` stays below `count` > 10, or exclude the test entity via `ops_exception_register.csv`. Variant 13 confirms zero alert rows for 'SMTP Authentication and Relay Policy Violations'."
  },
  "dataSources": "Postfix maillog, Sendmail logs, Exchange SMTP receive connector logs Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_syslog`, mail server logs — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=mail sourcetype=syslog (process=postfix OR process=sendmail) (\"authentication failed\" OR \"relay denied\" OR \"reject\")\n| rex \"user=(?<sasl_user>\\S+)\"\n| stats count by src, sasl_user, action\n| where count > 10\n| sort -count",
  "description": "Surfaces UC-11.4.4 — SMTP, Authentication, and, Relay, Policy — using `index=mail` sourcetype `syslog`, grouped by `src`, `sasl_user`, `action`. Outputs `count` each `the scheduled window`; alert when `count` > 10. Primary focus: Policy Violations. Purpose-built for 'SMTP Authentication and Relay Policy Violations' in Operations workflows.",
  "value": "UC-11.4.4 gives Operations teams earlier visibility into Policy Violations through `syslog` feeds, lowering operational risk and delayed incident response.",
  "implementation": "Forward mail server logs. Extract auth and relay outcomes. Alert on high volume of auth failures from single IP or relay denied for internal IPs (possible misconfiguration).",
  "visualization": "Table (IP, user, action, count), Timechart of failures, Map (GeoIP).",
  "cimModels": [
    "Authentication"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Authentication.Authentication by Authentication.src, Authentication.action | sort - count",
  "dataModelAcceleration": "Enable acceleration for the Authentication data model; summary range ≥ 30d for tstats in similar searches.",
  "references": [
    {
      "title": "CIM: Authentication",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Authentication",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 833",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `mail` (UC-11.4.4)** — Planned work for 'SMTP Authentication and Relay Policy Violations' shifts `count` over `span=1h` while `src`, `sasl_user`, `action` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `syslog`** — Scheduled automation elevates `sasl_user` without crossing `count` > 10 for UC-11.4.4. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `src`, `sasl_user`, `action`** — Health checks in `mail` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `syslog`** — First 48h baseline for UC-11.4.4 on `src` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `src`, `exception_reason`, `valid_until`.",
  "equipment": [
    "exchange",
    "syslog"
  ],
  "equipmentModels": [
    "exchange_generic",
    "syslog_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_syslog`, mail server logs.\n• Ensure the following data sources are available: Postfix maillog, Sendmail logs, Exchange SMTP receive connector logs.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nForward mail server logs. Extract auth and relay outcomes. Alert on high volume of auth failures from single IP or relay denied for internal IPs (possible misconfiguration).\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=mail sourcetype=syslog (process=postfix OR process=sendmail) (\"authentication failed\" OR \"relay denied\" OR \"reject\")\n| rex \"user=(?<sasl_user>\\S+)\"\n| stats count by src, sasl_user, action\n| where count > 10\n| sort -count\n```\n\nUnderstanding this SPL\n\n**SMTP Authentication and Relay Policy Violations** — Failed SMTP auth or unauthorized relay attempts may indicate credential stuffing or abuse. Monitoring supports security and ensures relay policy is enforced.\n\nDocumented **Data sources**: Postfix maillog, Sendmail logs, Exchange SMTP receive connector logs. **App/TA** (typical add-on context): `Splunk_TA_syslog`, mail server logs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: mail; **sourcetype**: syslog. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=mail, sourcetype=syslog. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Extracts fields with `rex` (regular expression).\n• `stats` rolls up events into metrics; results are split **by src, sasl_user, action** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where count > 10` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats summariesonly=t count from datamodel=Authentication.Authentication by Authentication.src, Authentication.action | sort - count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**SMTP Authentication and Relay Policy Violations** — Failed SMTP auth or unauthorized relay attempts may indicate credential stuffing or abuse. Monitoring supports security and ensures relay policy is enforced.\n\nDocumented **Data sources**: Postfix maillog, Sendmail logs, Exchange SMTP receive connector logs. **App/TA** (typical add-on context): `Splunk_TA_syslog`, mail server logs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Authentication.Authentication` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare with your MTA and mail security logs, mailq output, and DNS/MX for the same hosts and time window.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (IP, user, action, count), Timechart of failures, Map (GeoIP).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_syslog`, mail server logs — Splunk Add-on for Unix and Linux (Splunkbase 833)). Enable the modular input or HEC token that writes sourcetype=syslog into mail. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`action, sasl_user, src`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch your mail servers and queues, so delivery, auth, and encryption problems show up in time to fix them.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
