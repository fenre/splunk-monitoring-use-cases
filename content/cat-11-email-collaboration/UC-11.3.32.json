{
  "$schema": "../../schemas/uc.schema.json",
  "id": "11.3.32",
  "title": "Wire-Level VoIP Quality (MOS from RTP Stream)",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Security",
  "industry": "Telecommunications",
  "exclusions": "Covers the detection logic documented for 'Wire-Level VoIP Quality (MOS from RTP Stream)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_wire_level_voip_quality_mos_from_rtp_stream` (UC-11.3.32), SOC dashboard panel \"Operations — Wire-Level VoIP Quality (MOS from RTP Stream)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-11.3.32 positive (Wire, Level, VoIP, Quality, (MOS): ingest synthetic events into `index=target_index` sourcetype='stream:rtp' where `avg_mos`, `avg_rfactor`, `avg_loss_pct` grouped by `codec_name` satisfies `avg_mos` >= 4.0, `avg_mos` >= 3.5, or `avg_mos` >= 3.0 for 'Wire-Level VoIP Quality (MOS from RTP Stream)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-11.3.32 negative (RTP Stream)): ingest benign `target_index` / `stream:rtp` events where `codec_name` stays below `avg_mos` >= 4.0, `avg_mos` >= 3.5, or `avg_mos` >= 3.0, or exclude the test entity via `ops_exception_register.csv`. Variant 14 confirms zero alert rows for 'Wire-Level VoIP Quality (MOS from RTP Stream)'."
  },
  "dataSources": "`sourcetype=stream:rtp` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk App for Stream` (Splunkbase #1809) — Universal Forwarder (Splunkbase 617)",
  "spl": "sourcetype=\"stream:rtp\"\n| stats avg(mos_session) as avg_mos, avg(rfactor) as avg_rfactor, avg(lost) as avg_loss_pct, avg(unseq) as avg_unseq, count as streams by codec_name\n| eval quality=case(avg_mos>=4.0, \"Good\", avg_mos>=3.5, \"Acceptable\", avg_mos>=3.0, \"Poor\", 1==1, \"Unacceptable\")\n| sort avg_mos",
  "description": "Monitors UC-11.3.32 — Wire, Level, VoIP, Quality, (MOS — using `index=target_index` sourcetype `stream:rtp`, grouped by `codec_name`. Outputs `avg_mos`, `avg_rfactor`, `avg_loss_pct` each `the scheduled window`; alert when `avg_mos` >= 4.0, `avg_mos` >= 3.5, or `avg_mos` >= 3.0. Primary focus: RTP Stream). Purpose-built for 'Wire-Level VoIP Quality (MOS from RTP Stream)' in Operations workflows.",
  "value": "Alerting on Wire, Level, VoIP, Quality, (MOS through UC-11.3.32 reduces operational risk and delayed incident response by surfacing `avg_mos`, `avg_rfactor`, `avg_loss_pct` early.",
  "implementation": "Install Splunk App for Stream and configure it to capture RTP traffic on voice network segments. Enable the RTP protocol for full field extraction including `mos_session`, `rfactor`, `lost`, `unseq`, and `codec_name`. The MOS is calculated by Stream from RTP statistics (jitter, loss, codec type) per session. Deploy Stream forwarders on network taps or SPAN ports that see voice traffic. Alert when average MOS drops below 3.5 (ITU-T G.107 threshold for acceptable quality). Segment analysis by `codec_name` to identify if codec choice affects quality.",
  "visualization": "Gauge (average MOS score with thresholds: green >4.0, yellow 3.5-4.0, red <3.5), Line chart (MOS trend over 24h in 15-min buckets), Table (codec_name, avg_mos, avg_rfactor, avg_loss_pct, streams — sortable), Bar chart (stream count by quality category).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable; primary SPL uses raw or vendor-normalized event fields.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Cisco Security documentation",
      "url": "https://www.cisco.com/c/en/us/support/security/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `target_index` (UC-11.3.32)** — Planned work for 'Wire-Level VoIP Quality (MOS from RTP Stream)' shifts `avg_mos`, `avg_rfactor`, `avg_loss_pct` over `span=1h` while `codec_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `stream:rtp`** — Scheduled automation elevates `quality` without crossing `avg_mos` >= 4.0 for UC-11.3.32. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `codec_name`** — Health checks in `target_index` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `stream:rtp`** — First 48h baseline for UC-11.3.32 on `codec_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `codec_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "cisco",
    "stream"
  ],
  "equipmentModels": [
    "cisco_ucm"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk App for Stream` (Splunkbase #1809).\n• Ensure the following data sources are available: `sourcetype=stream:rtp`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nInstall Splunk App for Stream and configure it to capture RTP traffic on voice network segments. Enable the RTP protocol for full field extraction including `mos_session`, `rfactor`, `lost`, `unseq`, and `codec_name`. The MOS is calculated by Stream from RTP statistics (jitter, loss, codec type) per session. Deploy Stream forwarders on network taps or SPAN ports that see voice traffic. Alert when average MOS drops below 3.5 (ITU-T G.107 threshold for acceptable quality). Segment analysis by `cod…\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nsourcetype=\"stream:rtp\"\n| stats avg(mos_session) as avg_mos, avg(rfactor) as avg_rfactor, avg(lost) as avg_loss_pct, avg(unseq) as avg_unseq, count as streams by codec_name\n| eval quality=case(avg_mos>=4.0, \"Good\", avg_mos>=3.5, \"Acceptable\", avg_mos>=3.0, \"Poor\", 1==1, \"Unacceptable\")\n| sort avg_mos\n```\n\nUnderstanding this SPL\n\n**Wire-Level VoIP Quality (MOS from RTP Stream)** — Captures Mean Opinion Score (MOS) and R-Factor directly from RTP packets on the wire, providing platform-independent voice quality measurement. Unlike UC-11.3.1 which uses Cisco CUCM CMR data (application-level), this use case captures quality at the network level regardless of the call platform — covering third-party PBX, SIP trunking providers, and carrier interconnects.\n\nDocumented **Data sources**: `sourcetype=stream:rtp`. **App/TA** (typical add-on context): `Splunk App for Stream` (Splunkbase #1809). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **sourcetype**: stream:rtp. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: sourcetype=\"stream:rtp\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by codec_name** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **quality** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nCompare with packet capture config, CUCM, or Asterisk quality fields for the same call legs.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Gauge (average MOS score with thresholds: green >4.0, yellow 3.5-4.0, red <3.5), Line chart (MOS trend over 24h in 15-min buckets), Table (codec_name, avg_mos, avg_rfactor, avg_loss_pct, streams — sortable), Bar chart (stream count by quality category).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk App for Stream` (Splunkbase #1809) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=stream:rtp into index=main. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`avg_mos, codec_name, quality`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use this to stay ahead of email and collaboration problems, so the team is not the last to know when something drifts or breaks.",
  "prerequisiteUseCases": [],
  "telcoUseCase": "Contact Center Analytics (50 Ways #7)",
  "wave": "crawl"
}
