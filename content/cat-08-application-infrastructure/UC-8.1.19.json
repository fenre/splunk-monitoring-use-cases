{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.1.19",
  "title": "Tomcat Access Log Response Time Percentiles",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Tomcat Access Log Response Time Percentiles' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_tomcat_access_log_response_time_percentiles` (UC-8.1.19), SOC dashboard panel \"Operations — Tomcat Access Log Response Time Percentiles\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=web` sourcetype='tomcat:access' that satisfy the UC-8.1.19 SPL filters for 'Tomcat Access Log Response Time Percentiles'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `web` / `tomcat:access` events outside the detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-8.1.19."
  },
  "dataSources": "`index=web` `sourcetype=tomcat:access` (W3C or extended format with elapsed time) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Add-on for Tomcat (Splunkbase app 2911) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=web sourcetype=\"tomcat:access\"\n| eval elapsed_ms=coalesce('time-taken', time_taken, timetaken, duration_ms)\n| where isnotnull(elapsed_ms) AND elapsed_ms>0\n| timechart span=5m perc95(elapsed_ms) as p95_ms by host\n| where p95_ms > 2000",
  "description": "Tomcat access logs with `%D` or `time-taken` show end-to-end request duration. Rising P95 latency often precedes thread pool exhaustion and timeouts.",
  "value": "Teams catch Tomcat slowdowns before pools saturate and users see widespread timeouts.",
  "implementation": "Enable the AccessLogValve with elapsed time (`%D` microseconds or equivalent). Ingest with the Splunk Add-on for Tomcat. Normalize milliseconds at ingest or in SPL. Baseline P95 per application and seasonality.",
  "visualization": "Line chart (P95 elapsed ms), table (top slow hosts), single value (current P95 vs SLO).",
  "cimModels": [
    "Web"
  ],
  "cimSpl": "| tstats `summariesonly` perc95(Web.duration) as p95_ms avg(Web.duration) as avg_ms\n  from datamodel=Web.Web\n  by Web.dest Web.uri_path span=5m\n| where p95_ms > 0",
  "dataModelAcceleration": "Web: enable Data Model Acceleration for the CIM `Web` dataset once your HTTP access or proxy access logs are tagged and mapped to the model; otherwise `cimSpl` tstats may return no rows. Most teams start with the raw-log search in Step 2 and add acceleration after CIM field aliases validate.",
  "references": [
    {
      "title": "CIM: Web",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Web",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Add-on for Tomcat (Splunkbase)",
      "url": "https://splunkbase.splunk.com/app/2911",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Apache Tomcat Access Log Valve",
      "url": "https://tomcat.apache.org/tomcat-10.0-doc/config/valve.html#Access_Log_Valve",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'Tomcat Access Log Response Time Percentiles'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `web` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `ops_exception_register.csv` with entity, reason, valid_until.",
  "equipment": [
    "tomcat"
  ],
  "equipmentModels": [
    "tomcat_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Add-on for Tomcat (Splunkbase app 2911).\n• Ensure the following data sources are available: `index=web` `sourcetype=tomcat:access` (W3C or extended format with elapsed time).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnable Tomcat access logging including request duration; forward logs into Splunk with the Tomcat add-on; confirm the elapsed-time field is extracted as `time-taken` or map it to `elapsed_ms` in SPL.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=web sourcetype=\"tomcat:access\"\n| eval elapsed_ms=coalesce('time-taken', time_taken, timetaken, duration_ms)\n| where isnotnull(elapsed_ms) AND elapsed_ms>0\n| timechart span=5m perc95(elapsed_ms) as p95_ms by host\n| where p95_ms > 2000\n```\n\nUnderstanding this SPL\n\n**Tomcat Access Log Response Time Percentiles** — See the description and value fields in this use case JSON.\n\nDocumented **Data sources**: `index=web` `sourcetype=tomcat:access` (W3C or extended format with elapsed time). **App/TA**: Splunk Add-on for Tomcat (Splunkbase app 2911). Rename `index=` / `sourcetype=` to match your deployment.\n\n**Pipeline walkthrough**\n\n• Scope to the documented index and sourcetype, then apply transforms, thresholds, and `timechart`/`stats` as in the SPL above.\n\n\nStep 3 — Validate\nCompare with web server access logs on disk (Apache, NGINX, or W3C) for the same time range, or tail the same sourcetype in Search, to confirm status codes, URIs, and counts.\n\n\nStep 4 — Operationalize\nAdd to a dashboard or alert; document ownership. Suggested visuals: Line chart (P95 elapsed ms), table (top slow hosts), single value (current P95 vs SLO)..\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for Tomcat (Splunkbase app 2911) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=tomcat:access into web. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`elapsed_ms, host, p95_ms`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that teams catch Tomcat slowdowns before pools saturate and users see widespread timeouts.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
