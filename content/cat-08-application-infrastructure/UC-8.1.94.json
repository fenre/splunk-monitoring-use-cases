{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.1.94",
  "title": "WildFly / JBoss Infinispan Cache Eviction and Miss Rate",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'WildFly / JBoss Infinispan Cache Eviction and Miss Rate'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-8.1.94).",
  "evidence": "Saved search uc_8_1_94_wildfly_jboss_infinispan_cache_eviction, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'WildFly / JBoss Infinispan Cache Eviction and Miss Rate'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-8.1.94.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "WildFly server.log or JMX metrics from Infinispan cache subsystem via JMX Remote, Jolokia, or Splunk Add-on for JMX Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Add-on for JMX or custom scripted input polling WildFly management API — Universal Forwarder (Splunkbase 617)",
  "spl": "index=appserver sourcetype=\"jboss:server\" OR sourcetype=\"jmx:jboss\"\n| search cache AND (eviction OR miss OR hit)\n| rex field=_raw \"cache=(?<cache_name>[^,\\s]+)\"\n| eval hits=tonumber(coalesce(cache_hits, hits)), misses=tonumber(coalesce(cache_misses, misses)), evictions=tonumber(coalesce(cache_evictions, evictions))\n| stats latest(hits) as hits, latest(misses) as misses, latest(evictions) as evictions by host, cache_name\n| eval hit_ratio=if((hits+misses)>0, round(100*hits/(hits+misses),1), 0)\n| where hit_ratio < 80 OR evictions > 1000\n| sort -evictions",
  "description": "WildFly embeds Infinispan as its distributed cache for HTTP sessions, EJB stateful beans, Hibernate second-level cache, and JPA entity cache. High eviction rates or low hit ratios indicate that cache regions are undersized, causing repeated database round-trips, increased latency, and wasted CPU on re-computation. Monitoring these metrics prevents silent performance degradation that only surfaces under production load.",
  "value": "Detecting cache pressure early prevents cascading latency increases. A poorly tuned Infinispan cache can multiply database load by 5-10x during peak traffic, turning a cache-friendly application into one that overwhelms backend datastores.",
  "implementation": "Deploy Splunk Add-on for JMX on a Universal Forwarder co-located with the WildFly instance. Configure a JMX input targeting org.infinispan:type=Cache,name=*,component=Statistics MBeans. Alternatively, poll the WildFly management API endpoint /subsystem=infinispan/cache-container=*/local-cache=*/component=statistics via a scripted input. Set alert threshold for hit_ratio below 80% sustained over 15 minutes and evictions above 1000 per collection interval. Review cache max-entries and lifespan settings in standalone.xml or domain.xml when alerts fire.",
  "visualization": "Table (cache regions with low hit ratio), Line chart (eviction rate over time per cache region), Single value (overall cache hit ratio).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "WildFly Admin Guide — Infinispan Subsystem",
      "url": "https://docs.wildfly.org/31/Admin_Guide.html#Infinispan_Subsystem",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "jboss"
  ],
  "equipmentModels": [
    "jboss_generic"
  ],
  "status": "draft",
  "lastReviewed": "2025-06-01",
  "splunkVersions": [
    "9.x",
    "8.x"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for JMX or custom scripted input polling WildFly management API — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=jboss:server, jmx:jboss into appserver. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`cache_name, evictions, hit_ratio, host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that detecting cache pressure early prevents cascading latency increases.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
