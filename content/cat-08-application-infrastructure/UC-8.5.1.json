{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.5.1",
  "title": "Cache Hit/Miss Ratio",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Cache Hit/Miss Ratio' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_cache_hit_miss_ratio` (UC-8.5.1), SOC dashboard panel \"Operations — Cache Hit/Miss Ratio\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.5.1 positive (Cache, Hit, Miss, Ratio): ingest synthetic events into `index=cache` sourcetype='redis:info' where `cache_hit_pct` grouped by `host` satisfies `cache_hit_pct` < 90 for 'Cache Hit/Miss Ratio'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.5.1 negative (Hit/Miss Ratio): ingest benign `cache` / `redis:info` events where `host` stays below `cache_hit_pct` < 90, or exclude the test entity via `ops_exception_register.csv`. Variant 9 confirms zero alert rows for 'Cache Hit/Miss Ratio'."
  },
  "dataSources": "Redis INFO stats, Memcached stats, Varnish stats Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom scripted input (`redis-cli INFO`) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=cache sourcetype=\"redis:info\"\n| eval hit_ratio=round(keyspace_hits/(keyspace_hits+keyspace_misses)*100,2)\n| timechart span=5m avg(hit_ratio) as cache_hit_pct by host\n| where cache_hit_pct < 90",
  "description": "Identifies UC-8.5.1 — Cache, Hit, Miss, Ratio — using `index=cache` sourcetype `redis:info`, grouped by `host`. Outputs `cache_hit_pct` each `5m`; alert when `cache_hit_pct` < 90. Primary focus: Hit/Miss Ratio. Purpose-built for 'Cache Hit/Miss Ratio' in Operations workflows.",
  "value": "Operators running UC-8.5.1 on `redis:info` feeds shorten triage for Hit/Miss Ratio tied to 'Cache Hit/Miss Ratio', lowering operational risk and delayed incident response.",
  "implementation": "Run `redis-cli INFO` via scripted input every minute. Parse keyspace_hits and keyspace_misses. Calculate hit ratio. Alert when ratio drops below 90%. Correlate with application deployment events (new code may change access patterns).",
  "visualization": "Gauge (hit ratio %), Line chart (hit ratio over time), Single value (current hit ratio).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `cache` (UC-8.5.1)** — Planned work for 'Cache Hit/Miss Ratio' shifts `cache_hit_pct` over `span=5m` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `redis:info`** — Scheduled automation elevates `hit_ratio` without crossing `cache_hit_pct` < 90 for UC-8.5.1. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `cache` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `redis:info`** — First 48h baseline for UC-8.5.1 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "memcached",
    "redis",
    "varnish"
  ],
  "equipmentModels": [
    "memcached_generic",
    "redis_generic",
    "varnish_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Custom scripted input (`redis-cli INFO`).\n• Ensure the following data sources are available: Redis INFO stats, Memcached stats, Varnish stats.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nRun `redis-cli INFO` via scripted input every minute. Parse keyspace_hits and keyspace_misses. Calculate hit ratio. Alert when ratio drops below 90%. Correlate with application deployment events (new code may change access patterns).\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=cache sourcetype=\"redis:info\"\n| eval hit_ratio=round(keyspace_hits/(keyspace_hits+keyspace_misses)*100,2)\n| timechart span=5m avg(hit_ratio) as cache_hit_pct by host\n| where cache_hit_pct < 90\n```\n\nUnderstanding this SPL\n\n**Cache Hit/Miss Ratio** — Cache hit ratio directly measures cache effectiveness. Declining ratio means more backend load and higher latency.\n\nDocumented **Data sources**: Redis INFO stats, Memcached stats, Varnish stats. **App/TA** (typical add-on context): Custom scripted input (`redis-cli INFO`). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: cache; **sourcetype**: redis:info. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=cache, sourcetype=\"redis:info\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **hit_ratio** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `timechart` plots the metric over time using **span=5m** buckets with a separate series **by host** — ideal for trending and alerting on this use case.\n• Filters the current rows with `where cache_hit_pct < 90` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nCompare with `redis-cli INFO` (and slowlog if relevant) on the same instance and time window.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Gauge (hit ratio %), Line chart (hit ratio over time), Single value (current hit ratio).\n\nScripted input (generic example)\nThis use case relies on a scripted input. In the app's local/inputs.conf add a stanza such as:\n\n```ini\n[script://$SPLUNK_HOME/etc/apps/YourApp/bin/collect.sh]\ninterval = 300\nsourcetype = your_sourcetype\nindex = main\ndisabled = 0\n```\n\nThe script should print one event per line (e.g. key=value). Example minimal script (bash):\n\n```bash\n#!/usr/bin/env bash\n# Output metrics or events, one per line\necho \"metric=value timestamp=$(date +%s)\"\n```\n\nFor full details (paths, scheduling, permissions), see the Implementation guide: docs/implementation-guide.md",
  "grandmaExplanation": "We watch for signs that cache hit ratio directly measures cache effectiveness.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
