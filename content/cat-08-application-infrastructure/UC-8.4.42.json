{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.4.42",
  "title": "PHP-FPM Child Process Spawn vs Exit Rate",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Reliability",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "dataSources": "High churn suggests `pm.max_requests` recycling, crashes, or thundering herd after deploy.",
  "app": "Splunk Universal Forwarder; optional Splunk Add-on for Unix and Linux (`Splunk_TA_nix`); custom `props.conf`/`transforms.conf` for `phpfpm:*` sourcetypes",
  "spl": "index=web sourcetype=\"phpfpm:status\"\n| eval tc=tonumber(total_processes)\n| sort 0 host, pool, _time\n| streamstats window=2 global=f current=f last(tc) as prev_tc by host, pool\n| eval delta_tp=tc-prev_tc\n| timechart span=5m sum(abs(delta_tp)) as process_churn by pool",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "description": "Rapid changes in `total_processes` indicate recycling, crashes, or aggressive scaling. Comparing churn to `pm.max_requests` distinguishes healthy recycling from faults.",
  "value": "Shortens MTTR for extension crashes misread as traffic issues.",
  "grandmaExplanation": "We use this to shortens MTTR for extension crashes misread as traffic issues.",
  "implementation": "Poll status frequently enough to see step changes; compare with `pm.max_requests` setting from config lookup.",
  "visualization": "Time series (pool utilization, queue, latency percentiles), single value alerts, top charts for scripts and methods.",
  "references": [
    {
      "title": "CIM: Web",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Web"
    },
    {
      "title": "PHP-FPM configuration",
      "url": "https://www.php.net/manual/en/fpm.configuration.php#pm.max-requests"
    }
  ],
  "knownFalsePositives": "Spikes or gaps can follow approved maintenance, load tests, or short collection outages. We add a second signal and a change window before escalating.",
  "equipment": [
    "linux",
    "phpfpm"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": ""
}
