{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.4.42",
  "title": "PHP-FPM Child Process Spawn vs Exit Rate",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Reliability",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'PHP-FPM Child Process Spawn vs Exit Rate' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_php_fpm_child_process_spawn_vs_exit_rate` (UC-8.4.42), SOC dashboard panel \"Operations — PHP-FPM Child Process Spawn vs Exit Rate\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.4.42 positive (PHP, FPM, Child, Process, Spawn): ingest synthetic events into `index=web` sourcetype='phpfpm:status' where `sum` grouped by `host`, `pool` satisfies detection threshold for 'PHP-FPM Child Process Spawn vs Exit Rate'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.4.42 negative (Exit Rate): ingest benign `web` / `phpfpm:status` events where `host`, `pool` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 11 confirms zero alert rows for 'PHP-FPM Child Process Spawn vs Exit Rate'."
  },
  "dataSources": "High churn suggests `pm.max_requests` recycling, crashes, or thundering herd after deploy. Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder; optional Splunk Add-on for Unix and Linux (`Splunk_TA_nix`); custom `props.conf`/`transforms.conf` for `phpfpm:*` sourcetypes — Universal Forwarder (Splunkbase 617)",
  "spl": "index=web sourcetype=\"phpfpm:status\"\n| eval tc=tonumber(total_processes)\n| sort 0 host, pool, _time\n| streamstats window=2 global=f current=f last(tc) as prev_tc by host, pool\n| eval delta_tp=tc-prev_tc\n| timechart span=5m sum(abs(delta_tp)) as process_churn by pool",
  "description": "Alerts on UC-8.4.42 — PHP, FPM, Child, Process, Spawn — using `index=web` sourcetype `phpfpm:status`, grouped by `host`, `pool`. Outputs `sum` each `5m`; alert when configured threshold. Primary focus: Exit Rate. Purpose-built for 'PHP-FPM Child Process Spawn vs Exit Rate' in Operations workflows.",
  "value": "UC-8.4.42 helps teams contain PHP, FPM, Child, Process, Spawn scenarios and reduce operational risk and delayed incident response before incidents compound.",
  "implementation": "Poll status frequently enough to see step changes; compare with `pm.max_requests` setting from config lookup.",
  "visualization": "Time series (pool utilization, queue, latency percentiles), single value alerts, top charts for scripts and methods.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "CIM: Web",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Web",
      "retrieved": "2026-04-25"
    },
    {
      "title": "PHP-FPM configuration",
      "url": "https://www.php.net/manual/en/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `web` (UC-8.4.42)** — Planned work for 'PHP-FPM Child Process Spawn vs Exit Rate' shifts `sum` over `span=5m` while `host`, `pool` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `sum` on `phpfpm:status`** — Scheduled automation elevates `pool` without crossing configured threshold for UC-8.4.42. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`, `pool`** — Health checks in `web` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `phpfpm:status`** — First 48h baseline for UC-8.4.42 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "linux",
    "phpfpm"
  ],
  "equipmentModels": [
    "linux_generic",
    "phpfpm_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Universal Forwarder; optional Splunk Add-on for Unix and Linux (`Splunk_TA_nix`); custom `props.conf`/`transforms.conf` for `phpfpm:*` sourcetypes — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=phpfpm:status into web. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, pool`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use this to shortens MTTR for extension crashes misread as traffic issues.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
