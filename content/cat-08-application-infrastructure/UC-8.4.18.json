{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.4.18",
  "title": "Squid Cache Storage and Swap Utilization",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Squid Cache Storage and Swap Utilization' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_squid_cache_storage_and_swap_utilization` (UC-8.4.18), SOC dashboard panel \"Operations — Squid Cache Storage and Swap Utilization\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=proxy` sourcetype='squid:info' that satisfy the UC-8.4.18 SPL filters for 'Squid Cache Storage and Swap Utilization'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `proxy` / `squid:info` events outside the detection threshold, or exclude the test entity via `operational_exceptions.csv` with a valid window. Confirm zero alert rows for UC-8.4.18."
  },
  "dataSources": "Squid `store_io` metrics (`sourcetype=squid:snmp` or `squid:info`) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "SNMP polling of `cacheCurrentDiskUsage`, `cacheCurrentStoreSwapSize`, or scripted `squidclient mgr:info` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=proxy sourcetype=\"squid:info\"\n| eval swap_pct=if(max_swap_kb>0, round(100*current_swap_kb/max_swap_kb,1), null())\n| where swap_pct > 85\n| table _time, host, swap_pct, current_swap_kb, max_swap_kb",
  "description": "Disk cache exhaustion forces evictions and raises origin load; swap metrics predict hardware upgrades.",
  "value": "Avoids surprise origin floods when cache disks fill. Early detection reduces outage duration, supports audit evidence, and gives operators actionable context before users report impact.",
  "implementation": "Expose `mgr:info` or SNMP counters via scripted input; normalize KB fields at ingest.",
  "visualization": "Line chart (swap_pct), single value, forecast with `predict` optional.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "Squid Configuration Manual — Access Log",
      "url": "http://www.squid-cache.org/Doc/config/access_log/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'Squid Cache Storage and Swap Utilization'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `proxy` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `operational_exceptions.csv` with entity, reason, valid_until.",
  "equipment": [
    "snmp",
    "squid"
  ],
  "equipmentModels": [
    "snmp_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: SNMP polling of `cacheCurrentDiskUsage`, `cacheCurrentStoreSwapSize`, or scripted `squidclient mgr:info`.\n• Ensure the following data sources are available: Squid `store_io` metrics (`sourcetype=squid:snmp` or `squid:info`).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nField names must match your scripted parser; adjust `current_swap_kb` mapping.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=proxy sourcetype=\"squid:info\"\n| eval swap_pct=if(max_swap_kb>0, round(100*current_swap_kb/max_swap_kb,1), null())\n| where swap_pct > 85\n| table _time, host, swap_pct, current_swap_kb, max_swap_kb\n```\n\nUnderstanding this SPL\n\n**Squid Cache Storage and Swap Utilization** — See the description and value fields in this use case JSON.\n\nDocumented **Data sources**: Squid `store_io` metrics (`sourcetype=squid:snmp` or `squid:info`). **App/TA**: SNMP polling of `cacheCurrentDiskUsage`, `cacheCurrentStoreSwapSize`, or scripted `squidclient mgr:info`. Rename `index=` / `sourcetype=` to match your deployment.\n\n**Pipeline walkthrough**\n\n• Scope to the documented index and sourcetype, then apply transforms, thresholds, and `timechart`/`stats` as in the SPL above.\n\nStep 3 — Validate\nCompare with the API gateway or mesh admin (Kong, Apigee, AWS API Gateway, etc.) and a raw log tail for the same time range.\n\n\nStep 4 — Operationalize\nAdd to a dashboard or alert; document ownership. Suggested visuals: Line chart (swap_pct), single value, forecast with `predict` optional..\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (SNMP polling of `cacheCurrentDiskUsage`, `cacheCurrentStoreSwapSize`, or scripted `squidclient mgr:info` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=squid:info into proxy. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`max_swap_kb, swap_pct`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that avoids surprise origin floods when cache disks fill.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
