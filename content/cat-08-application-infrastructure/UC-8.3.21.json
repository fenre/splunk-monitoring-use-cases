{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.3.21",
  "title": "MSMQ Queue Depth Monitoring",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'MSMQ Queue Depth Monitoring' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_msmq_queue_depth_monitoring` (UC-8.3.21), SOC dashboard panel \"Operations — MSMQ Queue Depth Monitoring\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.3.21 positive (MSMQ, Queue, Depth, Monitoring): ingest synthetic events into `index=perfmon` sourcetype='target_sourcetype' where `AvgQueueDepth` grouped by `host` satisfies detection threshold for 'MSMQ Queue Depth Monitoring'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.3.21 negative (Depth Monitoring): ingest benign `perfmon` / `target_sourcetype` events where `host` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 0 confirms zero alert rows for 'MSMQ Queue Depth Monitoring'."
  },
  "dataSources": "`sourcetype=Perfmon:MSMQ` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_windows` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=perfmon source=\"Perfmon:MSMQ Service\" counter=\"Total Messages in all Queues\"\n| timechart span=5m avg(Value) as AvgQueueDepth by host\n| foreach * [eval <<FIELD>>=round('<<FIELD>>', 0)]",
  "description": "Alerts on 'MSMQ Queue Depth Monitoring' (UC-8.3.21) using `index=perfmon` sourcetype `configured sourcetype` by aggregating `AvgQueueDepth` grouped by `host` over `5m` and alerting when configured threshold. Keywords: MSMQ, Queue, Depth, Monitoring. Focuses on Depth Monitoring behaviour so operations teams can triage UC-8.3.21 without ad-hoc log review.",
  "value": "UC-8.3.21 accelerates insight into Depth Monitoring ('MSMQ Queue Depth Monitoring') by tracking `AvgQueueDepth` on UC-8.3.21 search results, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Configure Perfmon input for MSMQ Service counters: Total Messages in all Queues, Total Bytes in all Queues, Sessions. Also monitor individual queue counters via `MSMQ Queue` object. Alert when queue depth exceeds baseline (messages accumulating). Monitor journal queue size for message delivery confirmations. Track dead-letter queue growth for undeliverable messages.",
  "visualization": "Timechart (queue depth trend), Single value (current depth), Alert on queue growth exceeding threshold.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "Splunk_TA_windows",
      "url": "https://splunkbase.splunk.com/app/742",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `perfmon` (UC-8.3.21)** — Planned work for 'MSMQ Queue Depth Monitoring' shifts `AvgQueueDepth` over `span=5m` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `configured_sourcetype`** — Scheduled automation elevates `src` without crossing configured threshold for UC-8.3.21. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `perfmon` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `configured_sourcetype`** — First 48h baseline for UC-8.3.21 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "windows"
  ],
  "equipmentModels": [
    "windows_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_windows`.\n• Ensure the following data sources are available: `sourcetype=Perfmon:MSMQ`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nConfigure Perfmon input for MSMQ Service counters: Total Messages in all Queues, Total Bytes in all Queues, Sessions. Also monitor individual queue counters via `MSMQ Queue` object. Alert when queue depth exceeds baseline (messages accumulating). Monitor journal queue size for message delivery confirmations. Track dead-letter queue growth for undeliverable messages.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=perfmon source=\"Perfmon:MSMQ Service\" counter=\"Total Messages in all Queues\"\n| timechart span=5m avg(Value) as AvgQueueDepth by host\n| foreach * [eval <<FIELD>>=round('<<FIELD>>', 0)]\n```\n\nUnderstanding this SPL\n\n**MSMQ Queue Depth Monitoring** — Message queue buildup indicates application processing failures. Monitoring queue depth prevents message loss and detects downstream system outages.\n\nDocumented **Data sources**: `sourcetype=Perfmon:MSMQ`. **App/TA** (typical add-on context): `Splunk_TA_windows`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: perfmon.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=perfmon. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `timechart` plots the metric over time using **span=5m** buckets with a separate series **by host** — ideal for trending and alerting on this use case.\n• Iterates over multivalue fields with `foreach`.\n\n\nStep 3 — Validate\nCompare with the broker or gateway’s own UI or CLI (`kafka-consumer-groups`, RabbitMQ management, ActiveMQ console, or Traefik/Envoy access log on the node) for the same period.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Timechart (queue depth trend), Single value (current depth), Alert on queue growth exceeding threshold.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_windows` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into perfmon. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that message queue buildup indicates application processing failures.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
