{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.7.3",
  "title": "Application Error Budget Burn Rate Trending",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Application Error Budget Burn Rate Trending' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_application_error_budget_burn_rate_trending` (UC-8.7.3), SOC dashboard panel \"Operations — Application Error Budget Burn Rate Trending\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=app` sourcetype='slos:metrics' where `_time` satisfies detection threshold for UC-8.7.3 ('Application Error Budget Burn Rate Trending'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `app` / `slos:metrics` events where `_time` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-8.7.3."
  },
  "dataSources": "`index=app` SLO metrics, `sourcetype=stash` error-budget summaries, `index=middleware` synthetic or gateway SLO fields Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom SLO pipeline, Splunk Observability Cloud export, or scripted inputs from service catalogs — Universal Forwarder (Splunkbase 617)",
  "spl": "index=app sourcetype=stash source=\"*error_budget*\" OR index=middleware sourcetype=\"slos:metrics\"\n| eval remaining_pct=coalesce(error_budget_remaining_pct, slo_remaining_percent, 100 - burn_rate_pct)\n| eval sprint=strftime(_time,\"%Y-W%V\")\n| bin _time span=1d\n| stats first(remaining_pct) as budget_remaining by _time, service\n| timechart span=1d min(budget_remaining) as min_budget_remaining by service limit=10",
  "description": "Error budget remaining over a sprint or month shows whether reliability goals are sustainable. Accelerating burn triggers freeze or rollback decisions before users experience widespread outages.",
  "value": "Early 'Application Error Budget Burn Rate Trending' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Populate `remaining_pct` from your SLO tool (Datadog, Dynatrace, homemade) via HEC or scheduled pull. Define calendar alignment (monthly vs rolling 30d) consistently with product owners. Combine with release markers using `annotate` or a `releases` lookup. Alert on multi-day burn-rate thresholds per Google SRE multi-window practice if you export windowed burn fields.",
  "visualization": "Area chart (budget remaining %), line chart with release annotations, single value (days of budget left at current burn).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `app`** — Planned work for 'Application Error Budget Burn Rate Trending' shifts `min` over `span=1d` while `_time` remains in-scope for UC-8.7.3. Cross-check CMDB CHG.\n\n2. **Batch `min` on `slos:metrics`** — Scheduled automation elevates `service` without crossing configured threshold. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `_time`** — Health checks in `app` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `slos:metrics`** — First 48h baseline for `_time` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `_time`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Custom SLO pipeline, Splunk Observability Cloud export, or scripted inputs from service catalogs.\n• Ensure the following data sources are available: `index=app` SLO metrics, `sourcetype=stash` error-budget summaries, `index=middleware` synthetic or gateway SLO fields.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nPopulate `remaining_pct` from your SLO tool (Datadog, Dynatrace, homemade) via HEC or scheduled pull. Define calendar alignment (monthly vs rolling 30d) consistently with product owners. Combine with release markers using `annotate` or a `releases` lookup. Alert on multi-day burn-rate thresholds per Google SRE multi-window practice if you export windowed burn fields.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=app sourcetype=stash source=\"*error_budget*\" OR index=middleware sourcetype=\"slos:metrics\"\n| eval remaining_pct=coalesce(error_budget_remaining_pct, slo_remaining_percent, 100 - burn_rate_pct)\n| eval sprint=strftime(_time,\"%Y-W%V\")\n| bin _time span=1d\n| stats first(remaining_pct) as budget_remaining by _time, service\n| timechart span=1d min(budget_remaining) as min_budget_remaining by service limit=10\n```\n\nUnderstanding this SPL\n\n**Application Error Budget Burn Rate Trending** — Error budget remaining over a sprint or month shows whether reliability goals are sustainable. Accelerating burn triggers freeze or rollback decisions before users experience widespread outages.\n\nDocumented **Data sources**: `index=app` SLO metrics, `sourcetype=stash` error-budget summaries, `index=middleware` synthetic or gateway SLO fields. **App/TA** (typical add-on context): Custom SLO pipeline, Splunk Observability Cloud export, or scripted inputs from service catalogs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: app, middleware; **sourcetype**: stash, slos:metrics. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=app, index=middleware, sourcetype=stash. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **remaining_pct** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **sprint** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by _time, service** so each row reflects one combination of those dimensions.\n• `timechart` plots the metric over time using **span=1d** buckets with a separate series **by service limit=10** — ideal for trending and alerting on this use case.\n\n\nStep 3 — Validate\nCompare with the application or platform source of truth (logs, UI, or metrics) for the same time range, and with known change or maintenance windows.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Area chart (budget remaining %), line chart with release annotations, single value (days of budget left at current burn).",
  "grandmaExplanation": "We watch for signs that error budget remaining over a sprint or month shows whether reliability goals are sustainable.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
