{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.6.17",
  "title": "DNS Recursive Query Volume",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance",
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'DNS Recursive Query Volume' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_dns_recursive_query_volume` (UC-8.6.17), SOC dashboard panel \"Operations — DNS Recursive Query Volume\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=dns` sourcetype='bind:query' where `client_ip` satisfies `qps` > 1000 for UC-8.6.17 ('DNS Recursive Query Volume'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `dns` / `bind:query` events where `client_ip` stays below `qps` > 1000, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-8.6.17."
  },
  "dataSources": "`dns:query` recursive flag, `client` IP, `qname` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "BIND `named` logs, Infoblox DNS, CoreDNS logs — Universal Forwarder (Splunkbase 617)",
  "spl": "index=dns sourcetype=\"bind:query\" OR sourcetype=\"dns:query\"\n| where recursive=1\n| bucket _time span=1m\n| stats count as qps by client_ip, _time\n| eventstats avg(qps) as avg_q by client_ip\n| where qps > avg_q*10 AND qps > 1000",
  "description": "Sudden spike in recursive queries on internal resolvers may indicate DDoS, malware, or misconfigured application loops. Searches `index=dns` with sourcetype `bind:query` for UC-8.6.17, aggregating `avg` by `client_ip` over `1m`. Alerts when grouped results cross `qps` > 1000 in the saved search. Gives Operations operators a repeatable signal for 'DNS Recursive Query Volume' without manual log review.",
  "value": "Early 'DNS Recursive Query Volume' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Baseline QPS per client subnet. Alert on 10× baseline or absolute flood. Top `qname` for tunneling investigation.",
  "visualization": "Line chart (recursive QPS), Table (top clients), Bar chart (query types).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `dns`** — Planned work for 'DNS Recursive Query Volume' shifts `avg` over `span=1m` while `client_ip` remains in-scope for UC-8.6.17. Cross-check CMDB CHG.\n\n2. **Batch `avg` on `bind:query`** — Scheduled automation elevates `_time` without crossing `qps` > 1000. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `client_ip`** — Health checks in `dns` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `bind:query`** — First 48h baseline for `client_ip` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `client_ip`, `exception_reason`, `valid_until`.",
  "equipment": [
    "infoblox"
  ],
  "equipmentModels": [
    "infoblox_dns"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: BIND `named` logs, Infoblox DNS, CoreDNS logs.\n• Ensure the following data sources are available: `dns:query` recursive flag, `client` IP, `qname`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nBaseline QPS per client subnet. Alert on 10× baseline or absolute flood. Top `qname` for tunneling investigation.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=dns sourcetype=\"bind:query\" OR sourcetype=\"dns:query\"\n| where recursive=1\n| bucket _time span=1m\n| stats count as qps by client_ip, _time\n| eventstats avg(qps) as avg_q by client_ip\n| where qps > avg_q*10 AND qps > 1000\n```\n\nUnderstanding this SPL\n\n**DNS Recursive Query Volume** — Sudden spike in recursive queries on internal resolvers may indicate DDoS, malware, or misconfigured application loops.\n\nDocumented **Data sources**: `dns:query` recursive flag, `client` IP, `qname`. **App/TA** (typical add-on context): BIND `named` logs, Infoblox DNS, CoreDNS logs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: dns; **sourcetype**: bind:query, dns:query. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=dns, sourcetype=\"bind:query\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where recursive=1` — typically the threshold or rule expression for this monitoring goal.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by client_ip, _time** so each row reflects one combination of those dimensions.\n• `eventstats` rolls up events into metrics; results are split **by client_ip** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where qps > avg_q*10 AND qps > 1000` — typically the threshold or rule expression for this monitoring goal.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nCompare with the application or platform source of truth (logs, UI, or metrics) for the same time range, and with known change or maintenance windows.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (recursive QPS), Table (top clients), Bar chart (query types).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (BIND `named` logs, Infoblox DNS, CoreDNS logs — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=bind:query, dns:query into dns. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, client_ip, qps`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that a sudden spike in recursive queries on internal resolvers may indicate overload attacks, malware, or misconfigured application loops.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
