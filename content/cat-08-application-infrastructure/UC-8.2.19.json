{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.2.19",
  "title": "WebLogic Stuck Threads",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'WebLogic Stuck Threads' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_weblogic_stuck_threads` (UC-8.2.19), SOC dashboard panel \"Operations — WebLogic Stuck Threads\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.2.19 positive (WebLogic, Stuck, Threads): ingest synthetic events into `index=application` sourcetype='weblogic:server' where `count` grouped by `domain`, `server_name`, `thread_name` satisfies `stuck_thread_count` > 0, or `count` > 0 for 'WebLogic Stuck Threads'. Validate filter `\"BEA-000337\" OR \"STUCK\" OR stuck_thread_count>0`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.2.19 negative (Stuck Threads): ingest benign `application` / `weblogic:server` events where `domain`, `server_name`, `thread_name` stays below `stuck_thread_count` > 0, or `count` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 11 confirms zero alert rows for 'WebLogic Stuck Threads'."
  },
  "dataSources": "`weblogic:server` log, `StuckThreadCount` MBean Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "WebLogic Server logs, JMX — Universal Forwarder (Splunkbase 617)",
  "spl": "index=application sourcetype=\"weblogic:server\"\n| search \"BEA-000337\" OR \"STUCK\" OR stuck_thread_count>0\n| stats count by domain, server_name, thread_name\n| where count > 0",
  "description": "Detects UC-8.2.19 — WebLogic, Stuck, Threads — using `index=application` sourcetype `weblogic:server`, grouped by `domain`, `server_name`, `thread_name`. Outputs `count` each `the scheduled window`; alert when `stuck_thread_count` > 0, or `count` > 0. Filter chain includes filter `\"BEA-000337\" OR \"STUCK\" OR stuck_thread_count>0`. Purpose-built for 'WebLogic Stuck Threads' in Operations workflows.",
  "value": "UC-8.2.19 helps teams contain WebLogic, Stuck, Threads scenarios and reduce operational risk and delayed incident response before incidents compound.",
  "implementation": "Forward stdout/stderr and domain logs. Alert on first stuck thread. Thread dump automation on critical domains.",
  "visualization": "Table (domain, server, stuck count), Timeline (stuck events), Single value (stuck threads now).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `application` (UC-8.2.19)** — Planned work for 'WebLogic Stuck Threads' shifts `count` over `span=1h` while `domain`, `server_name`, `thread_name` remains in-scope. Applies filter `\"BEA-000337\" OR \"STUCK\" OR stuck_thread_count>0`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `weblogic:server`** — Scheduled automation elevates `server_name` without crossing `stuck_thread_count` > 0 for UC-8.2.19. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `domain`, `server_name`, `thread_name`** — Health checks in `application` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `weblogic:server`** — First 48h baseline for UC-8.2.19 on `domain` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `domain`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: WebLogic Server logs, JMX.\n• Ensure the following data sources are available: `weblogic:server` log, `StuckThreadCount` MBean.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nForward stdout/stderr and domain logs. Alert on first stuck thread. Thread dump automation on critical domains.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=application sourcetype=\"weblogic:server\"\n| search \"BEA-000337\" OR \"STUCK\" OR stuck_thread_count>0\n| stats count by domain, server_name, thread_name\n| where count > 0\n```\n\nUnderstanding this SPL\n\n**WebLogic Stuck Threads** — Stuck thread count >0 blocks request processing and triggers health check failures. Server log `BEA-000337` patterns.\n\nDocumented **Data sources**: `weblogic:server` log, `StuckThreadCount` MBean. **App/TA** (typical add-on context): WebLogic Server logs, JMX. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: application; **sourcetype**: weblogic:server. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=application, sourcetype=\"weblogic:server\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Applies an explicit `search` filter to narrow the current result set.\n• `stats` rolls up events into metrics; results are split **by domain, server_name, thread_name** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where count > 0` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nCompare with JBoss, WebLogic, or Tomcat admin consoles, or `catalina` / server logs on the host, for the same window. Confirm hostnames and fields match the vendor UI.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (domain, server, stuck count), Timeline (stuck events), Single value (stuck threads now).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (WebLogic Server logs, JMX — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=weblogic:server into application. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`domain, server_name, stuck_thread_count, thread_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that stuck thread count >0 blocks request processing and triggers health check failures.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
