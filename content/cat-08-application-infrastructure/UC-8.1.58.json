{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.1.58",
  "title": "ZooKeeper Connection Count by Client IP",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Security",
    "Operations"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'ZooKeeper Connection Count by Client IP' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_zookeeper_connection_count_by_client_ip` (UC-8.1.58), SOC dashboard panel \"Operations — ZooKeeper Connection Count by Client IP\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.1.58 positive (ZooKeeper, Connection, Count, Client): ingest synthetic events into `index=infrastructure` sourcetype='zookeeper:conf' where `sessions` grouped by `client_ip`, `host` satisfies `sessions` > 500 for 'ZooKeeper Connection Count by Client IP'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.1.58 negative (Client IP): ingest benign `infrastructure` / `zookeeper:conf` events where `client_ip`, `host` stays below `sessions` > 500, or exclude the test entity via `ops_exception_register.csv`. Variant 12 confirms zero alert rows for 'ZooKeeper Connection Count by Client IP'."
  },
  "dataSources": "`zk_num_alive_connections` from `zookeeper:mntr` plus `zookeeper:log` client connect lines if enriched Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "If client IPs are unavailable centrally, enrich via `cons` four-letter output scripted to `zookeeper:conf` custom sourcetype; otherwise track aggregate connections — Universal Forwarder (Splunkbase 617)",
  "spl": "index=infrastructure sourcetype=\"zookeeper:conf\"\n| rex field=_raw \"/(?<client_ip>\\d+\\.\\d+\\.\\d+\\.\\d+):(?<client_port>\\d+)\"\n| stats dc(client_port) as sessions by client_ip, host\n| where sessions > 500",
  "description": "Alerts on UC-8.1.58 — ZooKeeper, Connection, Count, Client — using `index=infrastructure` sourcetype `zookeeper:conf`, grouped by `client_ip`, `host`. Outputs `sessions` each `the scheduled window`; alert when `sessions` > 500. Primary focus: Client IP. Purpose-built for 'ZooKeeper Connection Count by Client IP' in Operations workflows.",
  "value": "Tracking `sessions` via UC-8.1.58 on `zookeeper:conf` feeds improves response to 'ZooKeeper Connection Count by Client IP' and reduces operational risk and delayed incident response.",
  "implementation": "Whitelist `cons` in `zoo.cfg`; schedule read-only `cons` dumps to Splunk with sourcetype `zookeeper:conf` (rename if you prefer `zookeeper:cons`). Sanitize payloads; restrict index ACLs. Alternative: alert only on `zk_num_alive_connections` spikes from `mntr` when `cons` is disallowed.",
  "visualization": "Table (client_ip, sessions), Bar chart top talkers.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "ZooKeeper — cons command",
      "url": "https://zookeeper.apache.org/doc/current/zookeeperAdmin.html#sc_cons",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `infrastructure` (UC-8.1.58)** — Planned work for 'ZooKeeper Connection Count by Client IP' shifts `sessions` over `span=1h` while `client_ip`, `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `dc` on `zookeeper:conf`** — Scheduled automation elevates `host` without crossing `sessions` > 500 for UC-8.1.58. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `client_ip`, `host`** — Health checks in `infrastructure` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `zookeeper:conf`** — First 48h baseline for UC-8.1.58 on `client_ip` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `client_ip`, `exception_reason`, `valid_until`.",
  "equipment": [
    "m365",
    "zookeeper"
  ],
  "equipmentModels": [
    "m365_generic",
    "zookeeper_generic"
  ],
  "status": "draft",
  "lastReviewed": "2025-06-01",
  "splunkVersions": [
    "9.x",
    "8.x"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (If client IPs are unavailable centrally, enrich via `cons` four-letter output scripted to `zookeeper:conf` custom sourcetype; otherwise track aggregate connections — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=zookeeper:conf into infrastructure. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`client_ip, host, sessions`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use this to protect shared ensembles from accidental denial-of-service by one application release.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
