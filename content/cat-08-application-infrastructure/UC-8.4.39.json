{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.4.39",
  "title": "PHP-FPM Pool Restart and Crash Detection",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Change",
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'PHP-FPM Pool Restart and Crash Detection' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_php_fpm_pool_restart_and_crash_detection` (UC-8.4.39), SOC dashboard panel \"Operations — PHP-FPM Pool Restart and Crash Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.4.39 positive (PHP, FPM, Pool, Restart, and): ingest synthetic events into `index=web` sourcetype='phpfpm:log' where `count` grouped by `host`, `pool`, `_raw` satisfies `count` >= 1 for 'PHP-FPM Pool Restart and Crash Detection'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.4.39 negative (Crash Detection): ingest benign `web` / `phpfpm:log` events where `host`, `pool`, `_raw` stays below `count` >= 1, or exclude the test entity via `ops_exception_register.csv`. Variant 7 confirms zero alert rows for 'PHP-FPM Pool Restart and Crash Detection'."
  },
  "dataSources": "`index=web` `sourcetype=phpfpm:log` (FPM master stderr / systemd journal forwarded), optional `phpfpm:access` for abrupt disconnect patterns Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder; optional Splunk Add-on for Unix and Linux (`Splunk_TA_nix`); custom `props.conf`/`transforms.conf` for `phpfpm:*` sourcetypes — Universal Forwarder (Splunkbase 617)",
  "spl": "index=web sourcetype=\"phpfpm:log\"\n| regex _raw=\"(?i)(SIGSEGV|SIGABRT|segmentation fault|zend_mm_heap corrupted|exiting|master process.*reload)\"\n| rex field=_raw \"\\[pool\\s+(?<pool>[^\\]]+)\\]\"\n| stats count by host, pool, _raw\n| where count >= 1",
  "description": "Detects UC-8.4.39 — PHP, FPM, Pool, Restart, and — using `index=web` sourcetype `phpfpm:log`, grouped by `host`, `pool`, `_raw`. Outputs `count` each `the scheduled window`; alert when `count` >= 1. Primary focus: Crash Detection. Purpose-built for 'PHP-FPM Pool Restart and Crash Detection' in Operations workflows.",
  "value": "UC-8.4.39 operationalises 'PHP-FPM Pool Restart and Crash Detection' against `phpfpm:log` feeds so operators reduce operational risk and delayed incident response without manual correlation.",
  "implementation": "Monitor FPM master stderr and `journalctl` via syslog; tag `phpfpm:log`. Correlate with deploy windows for graceful reload.",
  "visualization": "Time series (pool utilization, queue, latency percentiles), single value alerts, top charts for scripts and methods.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "PHP-FPM configuration",
      "url": "https://www.php.net/manual/en/install.fpm.php",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `web` (UC-8.4.39)** — Planned work for 'PHP-FPM Pool Restart and Crash Detection' shifts `count` over `span=1h` while `host`, `pool`, `_raw` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `phpfpm:log`** — Scheduled automation elevates `pool` without crossing `count` >= 1 for UC-8.4.39. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`, `pool`, `_raw`** — Health checks in `web` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `phpfpm:log`** — First 48h baseline for UC-8.4.39 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "linux",
    "phpfpm",
    "syslog"
  ],
  "equipmentModels": [
    "linux_generic",
    "phpfpm_generic",
    "syslog_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Universal Forwarder; optional Splunk Add-on for Unix and Linux (`Splunk_TA_nix`); custom `props.conf`/`transforms.conf` for `phpfpm:*` sourcetypes — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=phpfpm:log into web. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_raw, host, pool`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use this to reduce user-facing latency and 5xx rates by exposing pool saturation, slow scripts, and worker stability before front proxies fail.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
