{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.4.39",
  "title": "PHP-FPM Pool Restart and Crash Detection",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Change",
    "Fault"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=web` `sourcetype=phpfpm:log` (FPM master stderr / systemd journal forwarded), optional `phpfpm:access` for abrupt disconnect patterns",
  "app": "Splunk Universal Forwarder; optional Splunk Add-on for Unix and Linux (`Splunk_TA_nix`); custom `props.conf`/`transforms.conf` for `phpfpm:*` sourcetypes",
  "spl": "index=web sourcetype=\"phpfpm:log\"\n| regex _raw=\"(?i)(SIGSEGV|SIGABRT|segmentation fault|zend_mm_heap corrupted|exiting|master process.*reload)\"\n| rex field=_raw \"\\[pool\\s+(?<pool>[^\\]]+)\\]\"\n| stats count by host, pool, _raw\n| where count >= 1",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "description": "Graceful reloads and worker crashes leave distinct signatures in the FPM master log. Tracking signals and reload phrases helps separate planned deploys from instability and extension segfaults before pools stop accepting traffic.",
  "value": "Reduces user-facing latency and 5xx rates by exposing pool saturation, slow scripts, and worker stability before front proxies fail.",
  "grandmaExplanation": "We use this to reduce user-facing latency and 5xx rates by exposing pool saturation, slow scripts, and worker stability before front proxies fail.",
  "implementation": "Monitor FPM master stderr and `journalctl` via syslog; tag `phpfpm:log`. Correlate with deploy windows for graceful reload.",
  "visualization": "Time series (pool utilization, queue, latency percentiles), single value alerts, top charts for scripts and methods.",
  "references": [
    {
      "title": "PHP-FPM configuration",
      "url": "https://www.php.net/manual/en/install.fpm.php"
    }
  ],
  "knownFalsePositives": "Spikes or gaps can follow approved maintenance, load tests, or short collection outages. We add a second signal and a change window before escalating.",
  "equipment": [
    "linux",
    "phpfpm",
    "syslog"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": ""
}
