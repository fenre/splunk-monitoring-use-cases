{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.1.26",
  "title": "HAProxy HTTP Frontend 5xx Rate from Syslog",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'HAProxy HTTP Frontend 5xx Rate from Syslog' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_haproxy_http_frontend_5xx_rate_from_syslog` (UC-8.1.26), SOC dashboard panel \"Operations — HAProxy HTTP Frontend 5xx Rate from Syslog\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=proxy` sourcetype='haproxy:http' that satisfy the UC-8.1.26 SPL filters for 'HAProxy HTTP Frontend 5xx Rate from Syslog'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `proxy` / `haproxy:http` events outside the detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-8.1.26."
  },
  "dataSources": "`index=proxy` HAProxy HTTP log mode via syslog (`sourcetype=haproxy:http` recommended at ingest) Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "HAProxy syslog + props/transforms for HTTP log format — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=proxy sourcetype=\"haproxy:http\"\n| rex field=_raw \"\\s(?<http_status>5\\d{2})\\s+(?<resp_bytes>\\d+|-)\\s*$\"\n| where isnotnull(http_status)\n| bin _time span=5m\n| stats count as err5 by http_status\n| eventstats sum(err5) as tot\n| eval pct=round(100*err5/tot,2)\n| where pct > 2",
  "description": "HAProxy HTTP logs include terminal status codes after timing fields. Monitoring 5xx rates complements CSV stats for user-visible errors.",
  "value": "Catches application failures that do not yet mark backends DOWN in stats. Early detection reduces outage duration, supports audit evidence, and gives operators actionable context before users report impact.",
  "implementation": "Enable `option httplog`, forward syslog to Splunk, and extract status at EOL. Validate `rex` against your exact `log-format`.",
  "visualization": "Stacked bar (5xx codes), timechart of 5xx/min, top frontends if parsed.",
  "cimModels": [
    "Web"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Web.Web\n  where Web.status>=500\n  by Web.dest Web.uri_path Web.status span=5m\n| sort -count",
  "dataModelAcceleration": "Web: enable Data Model Acceleration for the CIM `Web` dataset once your HTTP access or proxy access logs are tagged and mapped to the model; otherwise `cimSpl` tstats may return no rows. Most teams start with the raw-log search in Step 2 and add acceleration after CIM field aliases validate.",
  "references": [
    {
      "title": "CIM: Web",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Web",
      "retrieved": "2026-04-25"
    },
    {
      "title": "HAProxy Management Guide — Logging",
      "url": "https://www.haproxy.com/documentation/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'HAProxy HTTP Frontend 5xx Rate from Syslog'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `proxy` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `ops_exception_register.csv` with entity, reason, valid_until.",
  "equipment": [
    "haproxy",
    "syslog"
  ],
  "equipmentModels": [
    "haproxy_generic",
    "syslog_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: HAProxy syslog + props/transforms for HTTP log format.\n• Ensure the following data sources are available: `index=proxy` HAProxy HTTP log mode via syslog (`sourcetype=haproxy:http` recommended at ingest).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nAdjust the `rex` if you customized `log-format`; test against sample events.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=proxy sourcetype=\"haproxy:http\"\n| rex field=_raw \"\\s(?<http_status>5\\d{2})\\s+(?<resp_bytes>\\d+|-)\\s*$\"\n| where isnotnull(http_status)\n| bin _time span=5m\n| stats count as err5 by http_status\n| eventstats sum(err5) as tot\n| eval pct=round(100*err5/tot,2)\n| where pct > 2\n```\n\nUnderstanding this SPL\n\n**HAProxy HTTP Frontend 5xx Rate from Syslog** — See the description and value fields in this use case JSON.\n\nDocumented **Data sources**: `index=proxy` HAProxy HTTP log mode via syslog (`sourcetype=haproxy:http` recommended at ingest). **App/TA**: HAProxy syslog + props/transforms for HTTP log format. Rename `index=` / `sourcetype=` to match your deployment.\n\n**Pipeline walkthrough**\n\n• Scope to the documented index and sourcetype, then apply transforms, thresholds, and `timechart`/`stats` as in the SPL above.\n\n\nStep 3 — Validate\nCompare with web server access logs on disk (Apache, NGINX, or W3C) for the same time range, or tail the same sourcetype in Search, to confirm status codes, URIs, and counts.\n\n\nStep 4 — Operationalize\nAdd to a dashboard or alert; document ownership. Suggested visuals: Stacked bar (5xx codes), timechart of 5xx/min, top frontends if parsed..\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (HAProxy syslog + props/transforms for HTTP log format — Splunk Add-on for Unix and Linux (Splunkbase 833)). Enable the modular input or HEC token that writes sourcetype=haproxy:http into proxy. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`http_status, pct`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that catches application failures that do not yet mark backends DOWN in stats.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
