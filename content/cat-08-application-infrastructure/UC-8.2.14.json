{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.2.14",
  "title": "JVM Garbage Collection Pause Time (STW)",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'JVM Garbage Collection Pause Time (STW)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_jvm_garbage_collection_pause_time_stw` (UC-8.2.14), SOC dashboard panel \"Operations — JVM Garbage Collection Pause Time (STW)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.2.14 positive (JVM, Garbage, Collection, Pause, Time): ingest synthetic events into `index=jvm` sourcetype='jvm:gc' where `max_pause` grouped by `host` satisfies `gc_pause_ms` > 500, or `p95_pause` > 200 for 'JVM Garbage Collection Pause Time (STW)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.2.14 negative (Time (STW)): ingest benign `jvm` / `jvm:gc` events where `host` stays below `gc_pause_ms` > 500, or `p95_pause` > 200, or exclude the test entity via `ops_exception_register.csv`. Variant 1 confirms zero alert rows for 'JVM Garbage Collection Pause Time (STW)'."
  },
  "dataSources": "`-Xlog:gc*` (Java 11+), `gc_pause_ms`, `gc_type` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "GC log parsing, `jvm:gc` sourcetype — Universal Forwarder (Splunkbase 617)",
  "spl": "index=jvm sourcetype=\"jvm:gc\"\n| where gc_pause_ms > 500\n| timechart span=5m perc95(gc_pause_ms) as p95_pause, max(gc_pause_ms) as max_pause by host\n| where p95_pause > 200",
  "description": "Detects Time (STW) activity in UC-8.2.14 ('JVM Garbage Collection Pause Time (STW)') from `index=jvm` sourcetype `jvm:gc`. Rolls up `max_pause` by `host` across `5m` and pages when `gc_pause_ms` > 500, or `p95_pause` > 200. Covers JVM, Garbage, Collection, Pause, Time. Operations operators treat UC-8.2.14 as the canonical signal.",
  "value": "Operators running UC-8.2.14 on `jvm:gc` feeds shorten triage for Time (STW) tied to 'JVM Garbage Collection Pause Time (STW)', lowering operational risk and delayed incident response.",
  "implementation": "Parse pause events only (not concurrent phases). Alert on p95 >200ms or any pause >2s. Split by pool (G1 Old Gen vs Young).",
  "visualization": "Line chart (p95/max pause), Histogram (pause distribution), Table (worst hosts).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `jvm` (UC-8.2.14)** — Planned work for 'JVM Garbage Collection Pause Time (STW)' shifts `max_pause` over `span=5m` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `max` on `jvm:gc`** — Scheduled automation elevates `src` without crossing `gc_pause_ms` > 500 for UC-8.2.14. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `jvm` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `jvm:gc`** — First 48h baseline for UC-8.2.14 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: GC log parsing, `jvm:gc` sourcetype.\n• Ensure the following data sources are available: `-Xlog:gc*` (Java 11+), `gc_pause_ms`, `gc_type`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nParse pause events only (not concurrent phases). Alert on p95 >200ms or any pause >2s. Split by pool (G1 Old Gen vs Young).\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=jvm sourcetype=\"jvm:gc\"\n| where gc_pause_ms > 500\n| timechart span=5m perc95(gc_pause_ms) as p95_pause, max(gc_pause_ms) as max_pause by host\n| where p95_pause > 200\n```\n\nUnderstanding this SPL\n\n**JVM Garbage Collection Pause Time (STW)** — Stop-the-world pause duration percentiles from unified GC logs (G1, ZGC) drive SLA breaches before heap % alerts fire.\n\nDocumented **Data sources**: `-Xlog:gc*` (Java 11+), `gc_pause_ms`, `gc_type`. **App/TA** (typical add-on context): GC log parsing, `jvm:gc` sourcetype. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: jvm; **sourcetype**: jvm:gc. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=jvm, sourcetype=\"jvm:gc\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where gc_pause_ms > 500` — typically the threshold or rule expression for this monitoring goal.\n• `timechart` plots the metric over time using **span=5m** buckets with a separate series **by host** — ideal for trending and alerting on this use case.\n• Filters the current rows with `where p95_pause > 200` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nCompare with JBoss, WebLogic, or Tomcat admin consoles, or `catalina` / server logs on the host, for the same window. Confirm hostnames and fields match the vendor UI.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (p95/max pause), Histogram (pause distribution), Table (worst hosts).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (GC log parsing, `jvm:gc` sourcetype — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=jvm:gc into jvm. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`gc_pause_ms, host, p95_pause`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that stop-the-world pause duration percentiles from unified GC logs (G1, ZGC) drive service promises breaches before heap % alerts fire.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
