{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.1.76",
  "title": "Apache Tomcat JDBC Pool Utilization (active vs idle vs max)",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Apache Tomcat JDBC Pool Utilization (active vs idle vs max)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_apache_tomcat_jdbc_pool_utilization_active_vs_id` (UC-8.1.76), SOC dashboard panel \"Operations — Apache Tomcat JDBC Pool Utilization (active vs idle vs max)\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.1.76 positive (Apache, Tomcat, JDBC, Pool, Utilization): ingest synthetic events into `index=web` sourcetype='jmx:tomcat:datasource' where `count` grouped by `entity` satisfies `max` > 0, or `pool_pct` >= 85 for 'Apache Tomcat JDBC Pool Utilization (active vs idle vs max)'. Validate filter `mbean=\"*DataSource*\" OR mbean=\"*ConnectionPool*\"`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.1.76 negative (vs max)): ingest benign `web` / `jmx:tomcat:datasource` events where `entity` stays below `max` > 0, or `pool_pct` >= 85, or exclude the test entity via `ops_exception_register.csv`. Variant 10 confirms zero alert rows for 'Apache Tomcat JDBC Pool Utilization (active vs idle vs max)'."
  },
  "dataSources": "`index=web` `sourcetype=jmx:tomcat:datasource` (or `tomcat:jmx`) with Tomcat JDBC pool MBean fields `numActive`, `numIdle`, `maxActive` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder; Splunk Add-on for JMX (`Splunk_TA_jmx`) for metrics; file inputs for `tomcat:catalina`, `tomcat:access`, `tomcat:manager` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=web (sourcetype=\"jmx:tomcat:datasource\" OR sourcetype=\"tomcat:jmx\")\n| search mbean=\"*DataSource*\" OR mbean=\"*ConnectionPool*\"\n| eval active=tonumber(numActive), idle=tonumber(numIdle), max=tonumber(maxActive)\n| eval pool_pct=if(max>0, round(100*active/max,1), null())\n| where pool_pct >= 85\n| table _time, host, mbean, active, idle, max, pool_pct",
  "description": "Monitors UC-8.1.76 — Apache, Tomcat, JDBC, Pool, Utilization — using `index=web` sourcetype `jmx:tomcat:datasource`, grouped by `entity`. Outputs `count` each `the scheduled window`; alert when `max` > 0, or `pool_pct` >= 85. Filter chain includes filter `mbean=\"*DataSource*\" OR mbean=\"*ConnectionPool*\"`. Purpose-built for 'Apache Tomcat JDBC Pool Utilization (active vs idle vs max)' in Operations workflows.",
  "value": "For 'Apache Tomcat JDBC Pool Utilization (active vs idle vs max)', UC-8.1.76 converts `activity` on `jmx:tomcat:datasource` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Enable JMX on Tomcat (`setenv.sh` CATALINA_OPTS). Map datasource MBeans in `Splunk_TA_jmx`. Create lookup from `name` to application service.",
  "visualization": "Time charts for utilization, tables for top URIs and deploy events, single-value alerts.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "Apache Tomcat documentation",
      "url": "https://tomcat.apache.org/tomcat-10.0-doc/jdbc-pool.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `web` (UC-8.1.76)** — Planned work for 'Apache Tomcat JDBC Pool Utilization (active vs idle vs max)' shifts `count` over `span=1h` while `host` remains in-scope. Applies filter `mbean=\"*DataSource*\" OR mbean=\"*ConnectionPool*\"`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `jmx:tomcat:datasource`** — Scheduled automation elevates `active` without crossing `max` > 0 for UC-8.1.76. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `web` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `jmx:tomcat:datasource`** — First 48h baseline for UC-8.1.76 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "tomcat"
  ],
  "equipmentModels": [
    "apache_httpd"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Universal Forwarder; Splunk Add-on for JMX (`Splunk_TA_jmx`) for metrics; file inputs for `tomcat:catalina`, `tomcat:access`, `tomcat:manager` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=jmx:tomcat:datasource, tomcat:jmx into web. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`max, pool_pct`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that separates database contention from application bugs and guides pool and DB scale decisions.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
