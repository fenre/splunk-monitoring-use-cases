{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.3.1",
  "title": "Consumer Lag Monitoring",
  "criticality": "critical",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Consumer Lag Monitoring' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_consumer_lag_monitoring` (UC-8.3.1), SOC dashboard panel \"Operations — Consumer Lag Monitoring\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.3.1 positive (Consumer, Lag, Monitoring): ingest synthetic events into `index=kafka` sourcetype='kafka:consumer_lag' where `consumer_lag` grouped by `consumer_group`, `topic` satisfies `consumer_lag` > 10000 for 'Consumer Lag Monitoring'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.3.1 negative (Lag Monitoring): ingest benign `kafka` / `kafka:consumer_lag` events where `consumer_group`, `topic` stays below `consumer_lag` > 10000, or exclude the test entity via `ops_exception_register.csv`. Variant 0 confirms zero alert rows for 'Consumer Lag Monitoring'."
  },
  "dataSources": "Kafka consumer group offsets (JMX, Burrow, `kafka-consumer-groups.sh`) Indexes: kafka. Sourcetypes: kafka:consumer_lag. Key fields: consumer_group, consumer_lag, topic. Validate field extraction in Search before saving the alert; compare event counts to the vendor admin console.",
  "app": "`Splunk Connect for Kafka` (Splunkbase 3862), Burrow integration, JMX",
  "spl": "index=kafka sourcetype=\"kafka:consumer_lag\"\n| timechart span=5m max(lag) as consumer_lag by consumer_group, topic\n| where consumer_lag > 10000",
  "description": "Alerts on 'Consumer Lag Monitoring' (UC-8.3.1) using `index=kafka` sourcetype `kafka:consumer_lag` by aggregating `consumer_lag` grouped by `consumer_group`, `topic` over `5m` and alerting when `consumer_lag` > 10000. Keywords: Consumer, Lag, Monitoring. Focuses on Lag Monitoring behaviour so operations teams can triage UC-8.3.1 without ad-hoc log review.",
  "value": "UC-8.3.1 accelerates insight into Lag Monitoring ('Consumer Lag Monitoring') by tracking `consumer_lag` on `kafka:consumer_lag` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Deploy Kafka consumer lag monitoring via Burrow or JMX. Poll lag per consumer group/topic/partition every minute. Alert when lag exceeds threshold (e.g., >10K messages or >5 minutes equivalent). Track lag trend for capacity planning.",
  "visualization": "Line chart (lag per consumer group), Heatmap (topic × partition lag), Single value (max lag), Table (lagging consumers).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "Splunkbase app 3862",
      "url": "https://splunkbase.splunk.com/app/3862",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `kafka` (UC-8.3.1)** — Planned work for 'Consumer Lag Monitoring' shifts `consumer_lag` over `span=5m` while `consumer_group`, `topic` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `max` on `kafka:consumer_lag`** — Scheduled automation elevates `topic` without crossing `consumer_lag` > 10000 for UC-8.3.1. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `consumer_group`, `topic`** — Health checks in `kafka` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `kafka:consumer_lag`** — First 48h baseline for UC-8.3.1 on `consumer_group` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `consumer_group`, `exception_reason`, `valid_until`.",
  "equipment": [
    "kafka"
  ],
  "equipmentModels": [
    "kafka_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk Connect for Kafka` (Splunkbase 3862), Burrow integration, JMX.\n• Ensure the following data sources are available: Kafka consumer group offsets (JMX, Burrow, `kafka-consumer-groups.sh`).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nDeploy Kafka consumer lag monitoring via Burrow or JMX. Poll lag per consumer group/topic/partition every minute. Alert when lag exceeds threshold (e.g., >10K messages or >5 minutes equivalent). Track lag trend for capacity planning.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=kafka sourcetype=\"kafka:consumer_lag\"\n| timechart span=5m max(lag) as consumer_lag by consumer_group, topic\n| where consumer_lag > 10000\n```\n\nUnderstanding this SPL\n\n**Consumer Lag Monitoring** — Growing consumer lag means messages aren't being processed fast enough, leading to data staleness and eventual message loss if retention is exceeded.\n\nDocumented **Data sources**: Kafka consumer group offsets (JMX, Burrow, `kafka-consumer-groups.sh`). **App/TA** (typical add-on context): `Splunk Connect for Kafka` (Splunkbase 3862), Burrow integration, JMX. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: kafka; **sourcetype**: kafka:consumer_lag. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=kafka, sourcetype=\"kafka:consumer_lag\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `timechart` plots the metric over time using **span=5m** buckets with a separate series **by consumer_group, topic** — ideal for trending and alerting on this use case.\n• Filters the current rows with `where consumer_lag > 10000` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nCompare with the broker or gateway’s own UI or CLI (`kafka-consumer-groups`, RabbitMQ management, ActiveMQ console, or Traefik/Envoy access log on the node) for the same period.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (lag per consumer group), Heatmap (topic × partition lag), Single value (max lag), Table (lagging consumers).",
  "grandmaExplanation": "We watch for signs that growing consumer lag means messages aren't being processed fast enough, leading to data staleness and eventual message loss if retention is exceeded.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
