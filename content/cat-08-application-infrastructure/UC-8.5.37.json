{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.5.37",
  "title": "ActiveMQ Durable Subscriber Offline Backlog Growth",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'ActiveMQ Durable Subscriber Offline Backlog Growth' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_activemq_durable_subscriber_offline_backlog_grow` (UC-8.5.37), SOC dashboard panel \"Operations — ActiveMQ Durable Subscriber Offline Backlog Growth\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=messaging` sourcetype='activemq:broker' where `dest` satisfies `inactive` > 0 for UC-8.5.37 ('ActiveMQ Durable Subscriber Offline Backlog Growth'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `messaging` / `activemq:broker` events where `dest` stays below `inactive` > 0, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-8.5.37."
  },
  "dataSources": "`index=messaging` `sourcetype=activemq:broker` topic and durable subscription statistics (`QueueSize` on the internal queue backing an offline durable, `SubscriberCount`, `InactiveDurableSubscribers`, destination names for `clientId` patterns)",
  "app": "Splunk Add-on for JMX (Splunkbase 2647) polling subscription-related MBeans",
  "spl": "index=messaging sourcetype=\"activemq:broker\" earliest=-24h\n| eval dest=coalesce(DestinationName, destination_name)\n| eval qsize=coalesce(QueueSize, queue_size)\n| eval inactive=coalesce(InactiveDurableSubscribers, inactive_durable_subscribers, 0)\n| eval durable_name=coalesce(subscription_name, client_id, durable_name)\n| where match(dest, \"(?i)dur|sub|consumer\") OR inactive>0 OR isnotnull(durable_name)\n| where qsize > 1000 OR inactive>0\n| timechart span=1h max(qsize) as durable_backlog max(inactive) as inactive_durables by dest, broker_name limit=15",
  "description": "When a durable subscriber is offline, the broker retains messages in a dedicated backlog; rapid `QueueSize` growth on those internal destinations risks exhausting store limits and slowing all clients on the broker. Tracking inactive durable counts highlights forgotten test clients and zombie subscriptions.",
  "value": "When a durable subscriber is offline, the broker retains messages in a dedicated backlog; rapid `QueueSize`.... Timely UC-8.5.37 alerting on 'ActiveMQ Durable Subscriber Offline Backlog Growth' reduces operational risk and delayed incident response before manual triage or audit channels escalate.",
  "implementation": "Tune name filters to your `clientId`/`subscriptionName` conventions. Exclude known batch consumers via lookup. Alert on week-over-week growth, not just absolute depth, for seasonal workloads.",
  "visualization": "Line chart (durable backlog), table (broker, destination, inactive count), single value (worst backlog).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "Apache ActiveMQ — Durable Subscribers",
      "url": "https://activemq.apache.org/manage-durable-subscribers",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Apache ActiveMQ — JMX",
      "url": "https://activemq.apache.org/jmx.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `messaging`** — Planned work for 'ActiveMQ Durable Subscriber Offline Backlog Growth' shifts `max` over `span=1h` while `dest` remains in-scope for UC-8.5.37. Cross-check CMDB CHG.\n\n2. **Batch `max` on `activemq:broker`** — Scheduled automation elevates `broker_name` without crossing `inactive` > 0. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `dest`** — Health checks in `messaging` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `activemq:broker`** — First 48h baseline for `dest` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `dest`, `exception_reason`, `valid_until`.",
  "equipment": [
    "activemq"
  ],
  "equipmentModels": [
    "activemq_generic"
  ],
  "status": "verified",
  "lastReviewed": "2026-06-01",
  "splunkVersions": [
    "9.x",
    "8.x"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for JMX (Splunkbase 2647) polling subscription-related MBeans). Enable the modular input or HEC token that writes sourcetype=activemq:broker into messaging. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`broker_name, dest, inactive, limit=15`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We help you prevent silent store exhaustion from abandoned durable subscriptions and quantifies catch-up work before reconnect storms after maintenance.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
