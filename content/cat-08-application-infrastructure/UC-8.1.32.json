{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.1.32",
  "title": "ActiveMQ Broker Memory Limit Pressure",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "dataSources": "JMX `org.apache.activemq:type=Broker,brokerName=*` attributes `MemoryPercentUsage`, `MemoryLimit`",
  "app": "Splunk Add-on for JMX (Splunkbase 2647) or scripted Jolokia→HEC; JMS Messaging Modular Input (Splunkbase 1317) for message ingress patterns",
  "spl": "index=messaging sourcetype=\"activemq:broker\"\n| eval mem_pct=coalesce(MemoryPercentUsage, memory_percent_usage)\n| where mem_pct > 70\n| timechart span=5m max(mem_pct) as broker_memory_pct by broker_name",
  "description": "Broker memory percent tracks usage against the broker-wide memory limit before producers are blocked or messages are spooled to temp storage.",
  "value": "Prevents broker-wide flow control and slow-consumer spirals by catching memory pressure before the store or temp limits are hit.",
  "implementation": "Poll broker MBean every 60s via JMX. Map attribute names to Splunk fields (`MemoryPercentUsage` is common on Classic brokers). Correlate with slow consumer advisories and destination memory usage. Alert at 70% sustained 10 minutes; tune per broker sizing.",
  "visualization": "Gauge (current memory %), Line chart (memory % trend), Table (brokers over threshold).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Apache ActiveMQ — JMX",
      "url": "https://activemq.apache.org/components/classic/documentation/jmx"
    },
    {
      "title": "Apache ActiveMQ — Performance",
      "url": "https://activemq.apache.org/components/classic/documentation/performance-tuning"
    }
  ],
  "equipment": [
    "activemq",
    "apache"
  ],
  "equipmentModels": [
    "apache_httpd"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "grandmaExplanation": "We watch memory pressure on the message server so it does not stall every application that depends on it."
}
