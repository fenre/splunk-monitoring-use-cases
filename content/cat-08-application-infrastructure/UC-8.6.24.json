{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.6.24",
  "title": "HAProxy Alert and Emergency Syslog Messages",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault",
    "Availability"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=proxy` syslog `sourcetype=haproxy:syslog`",
  "app": "HAProxy `log` targets with alert routing",
  "spl": "index=proxy sourcetype=\"haproxy:syslog\"\n| search \"[ALERT]\" OR \"[EMERG]\" OR \"fatal\"\n| table _time, host, _raw\n| sort -_time",
  "description": "HAProxy emits ALERT/EMERG for proxy, stick-table, and peersync failures requiring immediate operator action.",
  "value": "Captures conditions not visible in HTTP access logs.",
  "implementation": "Forward syslog with high integrity; include program name `haproxy`.",
  "visualization": "Real-time alert feed, Slack/webhook integration.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "HAProxy Management Guide — Logging",
      "url": "https://www.haproxy.com/documentation/haproxy-management-guide/latest/observability/logging/"
    }
  ],
  "equipment": [
    "haproxy",
    "syslog"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: HAProxy `log` targets with alert routing.\n• Ensure the following data sources are available: `index=proxy` syslog `sourcetype=haproxy:syslog`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nPhrase match syslog format; some distros lowercase severity.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=proxy sourcetype=\"haproxy:syslog\"\n| search \"[ALERT]\" OR \"[EMERG]\" OR \"fatal\"\n| table _time, host, _raw\n| sort -_time\n```\n\nUnderstanding this SPL\n\n**HAProxy Alert and Emergency Syslog Messages** — See the description and value fields in this use case JSON.\n\nDocumented **Data sources**: `index=proxy` syslog `sourcetype=haproxy:syslog`. **App/TA**: HAProxy `log` targets with alert routing. Rename `index=` / `sourcetype=` to match your deployment.\n\n**Pipeline walkthrough**\n\n• Scope to the documented index and sourcetype, then apply transforms, thresholds, and `timechart`/`stats` as in the SPL above.\n\nStep 3 — Validate\nConfirm events exist, field extractions match your format, and thresholds reflect production baselines.\n\nStep 4 — Operationalize\nAdd to a dashboard or alert; document ownership. Suggested visuals: Real-time alert feed, Slack/webhook integration..",
  "grandmaExplanation": "We listen for the load balancer’s own emergency shouts so infrastructure failures are not missed."
}
