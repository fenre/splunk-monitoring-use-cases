{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.3.20",
  "title": "NATS JetStream Consumer Ack Lag",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'NATS JetStream Consumer Ack Lag' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_nats_jetstream_consumer_ack_lag` (UC-8.3.20), SOC dashboard panel \"Operations — NATS JetStream Consumer Ack Lag\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.3.20 positive (NATS, JetStream, Consumer, Ack, Lag): ingest synthetic events into `index=messaging` sourcetype='nats:jetstream' where `lag` grouped by `stream_name`, `consumer_name` satisfies `num_ack_pending` > 1000, or `num_redelivered` > 100 for 'NATS JetStream Consumer Ack Lag'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.3.20 negative (Ack Lag): ingest benign `messaging` / `nats:jetstream` events where `stream_name`, `consumer_name` stays below `num_ack_pending` > 1000, or `num_redelivered` > 100, or exclude the test entity via `ops_exception_register.csv`. Variant 0 confirms zero alert rows for 'NATS JetStream Consumer Ack Lag'."
  },
  "dataSources": "`jetstream_consumer_lag`, `ack_pending` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "NATS Prometheus exporter, `nats` server varz/jsz — Universal Forwarder (Splunkbase 617)",
  "spl": "index=messaging sourcetype=\"nats:jetstream\"\n| where num_ack_pending > 1000 OR num_redelivered > 100\n| stats max(num_ack_pending) as lag by stream_name, consumer_name\n| sort -lag",
  "description": "Surfaces 'NATS JetStream Consumer Ack Lag' (UC-8.3.20) using `index=messaging` sourcetype `nats:jetstream` by aggregating `lag` grouped by `stream_name`, `consumer_name` over `the scheduled window` and alerting when `num_ack_pending` > 1000, or `num_redelivered` > 100. Keywords: NATS, JetStream, Consumer, Ack, Lag. Focuses on Ack Lag behaviour so operations teams can triage UC-8.3.20 without ad-hoc log review.",
  "value": "UC-8.3.20 accelerates insight into Ack Lag ('NATS JetStream Consumer Ack Lag') by tracking `lag` on `nats:jetstream` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Scrape `/jsz` or Prometheus metrics. Alert on rising ack_pending. Correlate with consumer pod restarts.",
  "visualization": "Line chart (ack pending), Table (stream, consumer, lag), Single value (max redelivered).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `messaging` (UC-8.3.20)** — Planned work for 'NATS JetStream Consumer Ack Lag' shifts `lag` over `span=1h` while `stream_name`, `consumer_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `max` on `nats:jetstream`** — Scheduled automation elevates `consumer_name` without crossing `num_ack_pending` > 1000 for UC-8.3.20. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `stream_name`, `consumer_name`** — Health checks in `messaging` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `nats:jetstream`** — First 48h baseline for UC-8.3.20 on `stream_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `stream_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "prometheus"
  ],
  "equipmentModels": [
    "prometheus_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: NATS Prometheus exporter, `nats` server varz/jsz.\n• Ensure the following data sources are available: `jetstream_consumer_lag`, `ack_pending`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nScrape `/jsz` or Prometheus metrics. Alert on rising ack_pending. Correlate with consumer pod restarts.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=messaging sourcetype=\"nats:jetstream\"\n| where num_ack_pending > 1000 OR num_redelivered > 100\n| stats max(num_ack_pending) as lag by stream_name, consumer_name\n| sort -lag\n```\n\nUnderstanding this SPL\n\n**NATS JetStream Consumer Ack Lag** — `NumAckPending`, `NumRedelivered`, and consumer lag for JetStream streams indicate slow consumers or poison messages.\n\nDocumented **Data sources**: `jetstream_consumer_lag`, `ack_pending`. **App/TA** (typical add-on context): NATS Prometheus exporter, `nats` server varz/jsz. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: messaging; **sourcetype**: nats:jetstream. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=messaging, sourcetype=\"nats:jetstream\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where num_ack_pending > 1000 OR num_redelivered > 100` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by stream_name, consumer_name** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nCompare with the broker or gateway’s own UI or CLI (`kafka-consumer-groups`, RabbitMQ management, ActiveMQ console, or Traefik/Envoy access log on the node) for the same period.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (ack pending), Table (stream, consumer, lag), Single value (max redelivered).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (NATS Prometheus exporter, `nats` server varz/jsz — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=nats:jetstream into messaging. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`consumer_name, num_ack_pending, num_redelivered, stream_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We follow NATS JetStream Consumer Ack Lag in plain language so we notice drift or trouble early enough to act calmly.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
