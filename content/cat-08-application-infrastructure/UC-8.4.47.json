{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.4.47",
  "title": "PHP-FPM Status Endpoint Self-Monitoring Response Time",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Operations"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'PHP-FPM Status Endpoint Self-Monitoring Response Time' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_php_fpm_status_endpoint_self_monitoring_response` (UC-8.4.47), SOC dashboard panel \"Operations — PHP-FPM Status Endpoint Self-Monitoring Response Time\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.4.47 positive (PHP, FPM, Status, Endpoint, Self): ingest synthetic events into `index=web` sourcetype='phpfpm:status' where `count` grouped by `host` satisfies detection threshold for 'PHP-FPM Status Endpoint Self-Monitoring Response Time'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.4.47 negative (Response Time): ingest benign `web` / `phpfpm:status` events where `host` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 2 confirms zero alert rows for 'PHP-FPM Status Endpoint Self-Monitoring Response Time'."
  },
  "dataSources": "If polling the status page becomes slow, the node is likely overloaded or network path degraded. Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder; optional Splunk Add-on for Unix and Linux (`Splunk_TA_nix`); custom `props.conf`/`transforms.conf` for `phpfpm:*` sourcetypes — Universal Forwarder (Splunkbase 617)",
  "spl": "index=web sourcetype=\"phpfpm:status\"\n| eval poll_duration_ms=tonumber(poll_duration_ms)\n| where isnotnull(poll_duration_ms)\n| timechart span=5m perc95(poll_duration_ms) as poller_p95_ms by host",
  "description": "Identifies UC-8.4.47 for PHP, FPM, Status, Endpoint, Self over `index=web` sourcetype `phpfpm:status` with thresholds configured threshold. Groups `count` by `host` each `5m`. Centered on Response Time indicators. Supports repeatable operations triage for 'PHP-FPM Status Endpoint Self-Monitoring Response Time'.",
  "value": "For 'PHP-FPM Status Endpoint Self-Monitoring Response Time', UC-8.4.47 converts `activity` on `phpfpm:status` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Add `poll_duration_ms` in your collector script (time curl). Alert when poller_p95_ms > 500ms.",
  "visualization": "Time series (pool utilization, queue, latency percentiles), single value alerts, top charts for scripts and methods.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "CIM: Web",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Web",
      "retrieved": "2026-04-25"
    },
    {
      "title": "PHP-FPM configuration",
      "url": "https://www.php.net/manual/en/fpm.status.php",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `web` (UC-8.4.47)** — Planned work for 'PHP-FPM Status Endpoint Self-Monitoring Response Time' shifts `count` over `span=5m` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `phpfpm:status`** — Scheduled automation elevates `poll_duration_ms` without crossing configured threshold for UC-8.4.47. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `web` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `phpfpm:status`** — First 48h baseline for UC-8.4.47 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "linux",
    "phpfpm"
  ],
  "equipmentModels": [
    "linux_generic",
    "phpfpm_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Universal Forwarder; optional Splunk Add-on for Unix and Linux (`Splunk_TA_nix`); custom `props.conf`/`transforms.conf` for `phpfpm:*` sourcetypes — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=phpfpm:status into web. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that catches observer bias where synthetic checks fail before user traffic alarms.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
