{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.4.40",
  "title": "PHP-FPM OPcache Hit Ratio from Status JSON",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "dataSources": "Low OPcache hit ratio increases CPU and latency after deploys or multi-pool cold starts.",
  "app": "Splunk Universal Forwarder; optional Splunk Add-on for Unix and Linux (`Splunk_TA_nix`); custom `props.conf`/`transforms.conf` for `phpfpm:*` sourcetypes",
  "spl": "index=web sourcetype=\"phpfpm:status\"\n| eval hits=tonumber(opcache_statistics_hits), miss=tonumber(opcache_statistics_misses)\n| eval hit_ratio=if((hits+miss)>0, round(100*hits/(hits+miss),2), null())\n| where isnotnull(hit_ratio)\n| timechart span=1h min(hit_ratio) as min_hit_ratio by host",
  "description": "OPcache hit ratio reflects how often PHP executes from shared memory versus compiling. Drops after deploys or multi-version pools signal cold caches.",
  "value": "Protects CPU and latency by catching mis-tuned `opcache.validate_timestamps` or undersized shared memory.",
  "implementation": "Requires extended status including OPcache (PHP 7+); flatten keys in HEC payload. Alert hit_ratio < 95% sustained.",
  "visualization": "Time series (pool utilization, queue, latency percentiles), single value alerts, top charts for scripts and methods.",
  "references": [
    {
      "title": "PHP-FPM configuration",
      "url": "https://www.php.net/manual/en/book.opcache.php"
    }
  ],
  "equipment": [
    "phpfpm"
  ],
  "grandmaExplanation": "We watch your PHP worker pools so busy sites do not run out of hands to answer requests.",
  "cimModels": [
    "N/A"
  ],
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "status": "draft"
}
