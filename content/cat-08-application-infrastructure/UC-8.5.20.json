{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.5.20",
  "title": "Kafka Dynamic Broker Configuration Change Detection",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Change"
  ],
  "splunkPillar": "IT Operations",
  "exclusions": "Covers the detection logic documented for 'Kafka Dynamic Broker Configuration Change Detection' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_kafka_dynamic_broker_configuration_change_detect` (UC-8.5.20), SOC dashboard panel \"Operations — Kafka Dynamic Broker Configuration Change Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.5.20 positive (Kafka, Dynamic, Broker, Configuration, Change): ingest synthetic events into `index=kafka` sourcetype='kafka:serverLog' where `count` grouped by `entity` satisfies detection threshold for 'Kafka Dynamic Broker Configuration Change Detection'. Validate filter `\"Dynamic\" AND (\"config\" OR \"configuration\") AND (\"update\" OR`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.5.20 negative (Change Detection): ingest benign `kafka` / `kafka:serverLog` events where `entity` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 0 confirms zero alert rows for 'Kafka Dynamic Broker Configuration Change Detection'."
  },
  "dataSources": "Kafka broker log `sourcetype=kafka:serverLog` (dynamic configuration updates) Indexes: kafka. Sourcetypes: kafka:serverLog. Validate field extraction in Search before saving the alert; compare event counts to the vendor admin console.",
  "app": "Splunk Add-on for Kafka (`Splunk_TA_kafka`, Splunkbase 2935)",
  "spl": "index=kafka sourcetype=\"kafka:serverLog\"\n| search \"Dynamic\" AND (\"config\" OR \"configuration\") AND (\"update\" OR \"alter\" OR \"changed\")\n| rex \"^\\[[^\\]]+\\]\\s+(?<log_level>\\w+)\"\n| where log_level IN (\"INFO\",\"WARN\",\"ERROR\")\n| table _time, host, log_level, _raw",
  "description": "Identifies 'Kafka Dynamic Broker Configuration Change Detection' (UC-8.5.20) using `index=kafka` sourcetype `kafka:serverLog` via filter `\"Dynamic\" AND (\"config\" OR \"configuration\") AND (\"update\" OR` by aggregating `count` grouped by `entity` over `the scheduled window` and alerting when configured threshold. Keywords: Kafka, Dynamic, Broker, Configuration, Change. Focuses on Change Detection behaviour so operations teams can triage UC-8.5.20 without ad-hoc log review.",
  "value": "UC-8.5.20 accelerates insight into Change Detection ('Kafka Dynamic Broker Configuration Change Detection') by tracking `activity` on `kafka:serverLog` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Tune keywords to your broker distribution (Apache vs Confluent). Feed results into a change correlation lookup (ticket ID in CMDB). Suppress maintenance windows.",
  "visualization": "Timeline (config change events), Table (host, raw line), Single value (changes per day).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "Apache Kafka — Updating Broker Config",
      "url": "https://kafka.apache.org/documentation/#brokerconfigs",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Source types for the Splunk Add-on for Kafka",
      "url": "https://docs.splunk.com/Documentation/AddOns/released/Kafka/Sourcetypes",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `kafka` (UC-8.5.20)** — Planned work for 'Kafka Dynamic Broker Configuration Change Detection' shifts `count` over `span=1h` while `host` remains in-scope. Applies filter `\"Dynamic\" AND (\"config\" OR \"configuration\") AND (\"update\" OR`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `kafka:serverLog`** — Scheduled automation elevates `src` without crossing configured threshold for UC-8.5.20. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `kafka` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `kafka:serverLog`** — First 48h baseline for UC-8.5.20 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "apache",
    "kafka"
  ],
  "equipmentModels": [
    "apache_httpd"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for Kafka (`Splunk_TA_kafka`, Splunkbase 2935)). Enable the modular input or HEC token that writes sourcetype=kafka:serverLog into kafka. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, user, action`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use this to speed incident correlation when a sudden behavior change traces to an unplanned broker property update.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
