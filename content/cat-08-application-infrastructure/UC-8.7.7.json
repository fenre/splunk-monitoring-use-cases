{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.7.7",
  "title": "IIS Client Error Status Trending for Service Review",
  "criticality": "low",
  "difficulty": "beginner",
  "monitoringType": [
    "Compliance",
    "Performance"
  ],
  "splunkPillar": "IT Operations",
  "dataSources": "`index=web` `sourcetype=ms:iis:auto` (`sc-status` 4xx)",
  "app": "Splunk Add-on for Microsoft IIS",
  "spl": "index=web sourcetype=\"ms:iis:auto\" earliest=-30d\n| where sc_status >= 400 AND sc_status < 500\n| timechart span=1d count by sc_status",
  "cimSpl": "| tstats `summariesonly` count as events\n  from datamodel=Web.Web\n  by Web.http_method Web.dest span=5m\n| sort -events",
  "cimModels": [
    "Web"
  ],
  "dataModelAcceleration": "Web: enable Data Model Acceleration for the CIM `Web` dataset once your HTTP access or proxy access logs are tagged and mapped to the model; otherwise `cimSpl` tstats may return no rows. Most teams start with the raw-log search in Step 2 and add acceleration after CIM field aliases validate.",
  "description": "4xx trends highlight broken deep links, API contract drift, and auth misconfigurations during audits of customer-facing portals.",
  "value": "Supports service owners reviewing quality alongside security teams.",
  "grandmaExplanation": "We use this to support service owners reviewing quality alongside security teams.",
  "implementation": "Exclude health-probe URIs via lookup.",
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Add-on for Microsoft IIS.\n• Ensure the following data sources are available: `index=web` `sourcetype=ms:iis:auto` (`sc-status` 4xx).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nPair with marketing campaign calendar to explain spikes.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=web sourcetype=\"ms:iis:auto\" earliest=-30d\n| where sc_status >= 400 AND sc_status < 500\n| timechart span=1d count by sc_status\n```\n\nUnderstanding this SPL\n\n**IIS Client Error Status Trending for Service Review** — See the description and value fields in this use case JSON.\n\nDocumented **Data sources**: `index=web` `sourcetype=ms:iis:auto` (`sc-status` 4xx). **App/TA**: Splunk Add-on for Microsoft IIS. Rename `index=` / `sourcetype=` to match your deployment.\n\n**Pipeline walkthrough**\n\n• Scope to the documented index and sourcetype, then apply transforms, thresholds, and `timechart`/`stats` as in the SPL above.\n\n\nStep 3 — Validate\nCompare with the application or platform source of truth (logs, UI, or metrics) for the same time range, and with known change or maintenance windows.\n\n\nStep 4 — Operationalize\nAdd to a dashboard or alert; document ownership. Suggested visuals: Stacked area (codes), executive summary panel..",
  "visualization": "Stacked area (codes), executive summary panel.",
  "references": [
    {
      "title": "CIM: Web",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Web"
    },
    {
      "title": "Splunk Add-on for Microsoft IIS (Splunkbase)",
      "url": "https://splunkbase.splunk.com/app/3185"
    }
  ],
  "knownFalsePositives": "4xx spikes from web crawlers, scanners, or after URL routing changes. We tune exclusions for known bots when needed.",
  "equipment": [
    "iis"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases"
}
