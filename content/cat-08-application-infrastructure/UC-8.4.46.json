{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.4.46",
  "title": "PHP-FPM PHP Fatal Error Rate from php error_log",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault",
    "Availability"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=web` `sourcetype=phpfpm:log` (pool stderr / `catch_workers_output` lines containing PHP fatal errors)",
  "app": "Splunk Universal Forwarder; optional Splunk Add-on for Unix and Linux (`Splunk_TA_nix`); custom `props.conf`/`transforms.conf` for `phpfpm:*` sourcetypes",
  "spl": "index=web sourcetype=\"phpfpm:log\"\n| regex _raw=\"(?i)PHP Fatal error\"\n| bin _time span=5m\n| stats count by host, pool, _time\n| where count > 10",
  "description": "PHP fatal errors stop request handling for that worker and often return 500 to users. Spikes correlate with bad deploys or missing dependencies.",
  "value": "Speeds rollback decisions with objective error rates.",
  "implementation": "Forward FPM pool logs with `catch_workers_output=yes` or aggregate `php-fpm.d/*.log` via UF. Tune threshold per environment; suppress known vendor stack traces with lookup.",
  "visualization": "Time series (pool utilization, queue, latency percentiles), single value alerts, top charts for scripts and methods.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "PHP-FPM configuration",
      "url": "https://www.php.net/manual/en/errorfunc.constants.php"
    }
  ],
  "equipment": [
    "linux",
    "phpfpm"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "grandmaExplanation": "We watch your PHP worker pools so busy sites do not run out of hands to answer requests."
}
