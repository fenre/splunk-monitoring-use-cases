{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.2.5",
  "title": "Deployment Tracking",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Configuration"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Deployment Tracking' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_deployment_tracking` (UC-8.2.5), SOC dashboard panel \"Operations — Deployment Tracking\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=deployments` sourcetype='deployment_event' where `entity` satisfies detection threshold for UC-8.2.5 ('Deployment Tracking'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `deployments` / `deployment_event` events where `entity` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-8.2.5."
  },
  "dataSources": "Deployment tool webhooks (Jenkins, GitHub Actions, ArgoCD), application version endpoints Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Webhook input, CI/CD integration — Universal Forwarder (Splunkbase 617)",
  "spl": "index=deployments sourcetype=\"deployment_event\"\n| table _time, application, version, environment, deployer, status\n| sort -_time",
  "description": "Correlating deployments with performance changes is the fastest way to identify deployment-caused regressions. Essential for change management.",
  "value": "Early 'Deployment Tracking' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Configure CI/CD pipeline to send deployment events to Splunk HEC (JSON payload with app, version, environment, deployer). Annotate timecharts with deployment markers. Correlate deployment times with error rate and latency changes.",
  "visualization": "Timeline (deployment events overlaid on performance charts), Table (recent deployments), Annotation layer on dashboards.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `deployments`** — Planned work for 'Deployment Tracking' shifts `count` over `span=1h` while `host` remains in-scope for UC-8.2.5. Cross-check CMDB CHG.\n\n2. **Batch `count` on `deployment_event`** — Scheduled automation elevates `src` without crossing configured threshold. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `deployments` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `deployment_event`** — First 48h baseline for `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "argocd",
    "github",
    "jenkins"
  ],
  "equipmentModels": [
    "argocd_generic",
    "github_generic",
    "jenkins_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Webhook input, CI/CD integration.\n• Ensure the following data sources are available: Deployment tool webhooks (Jenkins, GitHub Actions, ArgoCD), application version endpoints.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nConfigure CI/CD pipeline to send deployment events to Splunk HEC (JSON payload with app, version, environment, deployer). Annotate timecharts with deployment markers. Correlate deployment times with error rate and latency changes.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=deployments sourcetype=\"deployment_event\"\n| table _time, application, version, environment, deployer, status\n| sort -_time\n```\n\nUnderstanding this SPL\n\n**Deployment Tracking** — Correlating deployments with performance changes is the fastest way to identify deployment-caused regressions. Essential for change management.\n\nDocumented **Data sources**: Deployment tool webhooks (Jenkins, GitHub Actions, ArgoCD), application version endpoints. **App/TA** (typical add-on context): Webhook input, CI/CD integration. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: deployments; **sourcetype**: deployment_event. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=deployments, sourcetype=\"deployment_event\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Pipeline stage (see **Deployment Tracking**): table _time, application, version, environment, deployer, status\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nCompare with JBoss, WebLogic, or Tomcat admin consoles, or `catalina` / server logs on the host, for the same window. Confirm hostnames and fields match the vendor UI.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Timeline (deployment events overlaid on performance charts), Table (recent deployments), Annotation layer on dashboards.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Webhook input, CI/CD integration — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=deployment_event into deployments. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, user, action`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that correlating deployments with performance changes is the fastest way to identify deployment-caused regressions.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
