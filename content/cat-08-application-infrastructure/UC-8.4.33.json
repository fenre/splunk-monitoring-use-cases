{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.4.33",
  "title": "PHP-FPM Slow Request Log — Slowlog Timeout Analysis",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'PHP-FPM Slow Request Log — Slowlog Timeout Analysis' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_php_fpm_slow_request_log_slowlog_timeout_analysi` (UC-8.4.33), SOC dashboard panel \"Operations — PHP-FPM Slow Request Log — Slowlog Timeout Analysis\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.4.33 positive (PHP, FPM, Slow, Request, Log): ingest synthetic events into `index=web` sourcetype='phpfpm:slow' where `count` grouped by `pool`, `script_filename` satisfies detection threshold for 'PHP-FPM Slow Request Log — Slowlog Timeout Analysis'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.4.33 negative (Timeout Analysis): ingest benign `web` / `phpfpm:slow` events where `pool`, `script_filename` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 14 confirms zero alert rows for 'PHP-FPM Slow Request Log — Slowlog Timeout Analysis'."
  },
  "dataSources": "Events from `request_slowlog_timeout` trace scripts exceeding the pool slow threshold. Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder; optional Splunk Add-on for Unix and Linux (`Splunk_TA_nix`); custom `props.conf`/`transforms.conf` for `phpfpm:*` sourcetypes — Universal Forwarder (Splunkbase 617)",
  "spl": "index=web sourcetype=\"phpfpm:slow\"\n| rex field=_raw \"script_filename\\s*=\\s*(?<script_filename>\\S+)\"\n| stats count by pool, script_filename\n| sort - count\n| head 20",
  "description": "Monitors UC-8.4.33 — PHP, FPM, Slow, Request, Log — using `index=web` sourcetype `phpfpm:slow`, grouped by `pool`, `script_filename`. Outputs `count` each `the scheduled window`; alert when configured threshold. Primary focus: Timeout Analysis. Purpose-built for 'PHP-FPM Slow Request Log — Slowlog Timeout Analysis' in Operations workflows.",
  "value": "Alerting on PHP, FPM, Slow, Request, Log through UC-8.4.33 reduces operational risk and delayed incident response by surfacing `activity` early.",
  "implementation": "Enable `slowlog` and `request_slowlog_timeout` in pool config; monitor the slow log file with UF `monitor://`. Create LINE_BREAKER on pool headers `[pool www]`.",
  "visualization": "Time series (pool utilization, queue, latency percentiles), single value alerts, top charts for scripts and methods.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "PHP-FPM configuration",
      "url": "https://www.php.net/manual/en/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `web` (UC-8.4.33)** — Planned work for 'PHP-FPM Slow Request Log — Slowlog Timeout Analysis' shifts `count` over `span=1h` while `pool`, `script_filename` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `phpfpm:slow`** — Scheduled automation elevates `script_filename` without crossing configured threshold for UC-8.4.33. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `pool`, `script_filename`** — Health checks in `web` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `phpfpm:slow`** — First 48h baseline for UC-8.4.33 on `pool` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `pool`, `exception_reason`, `valid_until`.",
  "equipment": [
    "linux",
    "phpfpm"
  ],
  "equipmentModels": [
    "linux_generic",
    "phpfpm_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Universal Forwarder; optional Splunk Add-on for Unix and Linux (`Splunk_TA_nix`); custom `props.conf`/`transforms.conf` for `phpfpm:*` sourcetypes — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=phpfpm:slow into web. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`pool, script_filename`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that turns anecdotal slowness into ranked evidence for engineering backlog.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
