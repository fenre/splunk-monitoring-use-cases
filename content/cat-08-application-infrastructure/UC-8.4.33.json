{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.4.33",
  "title": "PHP-FPM Slow Request Log — Slowlog Timeout Analysis",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "dataSources": "Events from `request_slowlog_timeout` trace scripts exceeding the pool slow threshold.",
  "app": "Splunk Universal Forwarder; optional Splunk Add-on for Unix and Linux (`Splunk_TA_nix`); custom `props.conf`/`transforms.conf` for `phpfpm:*` sourcetypes",
  "spl": "index=web sourcetype=\"phpfpm:slow\"\n| rex field=_raw \"script_filename\\s*=\\s*(?<script_filename>\\S+)\"\n| stats count by pool, script_filename\n| sort - count\n| head 20",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "description": "The slow log captures stack traces for requests exceeding `request_slowlog_timeout`. Mining top scripts guides profiling, database fixes, and opcode cache tuning.",
  "value": "Turns anecdotal slowness into ranked evidence for engineering backlog.",
  "grandmaExplanation": "We watch for signs that turns anecdotal slowness into ranked evidence for engineering backlog.",
  "implementation": "Enable `slowlog` and `request_slowlog_timeout` in pool config; monitor the slow log file with UF `monitor://`. Create LINE_BREAKER on pool headers `[pool www]`.",
  "visualization": "Time series (pool utilization, queue, latency percentiles), single value alerts, top charts for scripts and methods.",
  "references": [
    {
      "title": "PHP-FPM configuration",
      "url": "https://www.php.net/manual/en/fpm.configuration.php#slowlog"
    }
  ],
  "knownFalsePositives": "Response time spikes during JVM garbage collection, connection pool exhaustion, or backend dependency degradation. Load tests, campaigns, and cold caches also move percentiles.",
  "equipment": [
    "linux",
    "phpfpm"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": ""
}
