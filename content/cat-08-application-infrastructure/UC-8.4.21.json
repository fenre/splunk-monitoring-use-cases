{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.4.21",
  "title": "HAProxy Frontend Session Rate Versus ulimit",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'HAProxy Frontend Session Rate Versus ulimit' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_haproxy_frontend_session_rate_versus_ulimit` (UC-8.4.21), SOC dashboard panel \"Operations — HAProxy Frontend Session Rate Versus ulimit\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=haproxy` sourcetype='haproxy:stats' that satisfy the UC-8.4.21 SPL filters for 'HAProxy Frontend Session Rate Versus ulimit'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `haproxy` / `haproxy:stats` events outside the detection threshold, or exclude the test entity via `operational_exceptions.csv` with a valid window. Confirm zero alert rows for UC-8.4.21."
  },
  "dataSources": "`index=haproxy` `sourcetype=haproxy:stats` (`rate`, `scur`, `slim`) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "HAProxy stats CSV scripted input — Universal Forwarder (Splunkbase 617)",
  "spl": "index=haproxy sourcetype=\"haproxy:stats\" type=frontend\n| eval sess_pct=if(slim>0, round(100*scur/slim,1), null())\n| where sess_pct > 85\n| table _time, pxname, scur, slim, sess_pct",
  "description": "`scur` approaching `slim` on a frontend signals connection limits or ulimit pressure during campaigns.",
  "value": "Informs tuning of `maxconn`, kernel limits, and cloud LB pairing. Early detection reduces outage duration, supports audit evidence, and gives operators actionable context before users report impact.",
  "implementation": "Poll stats frequently; `slim` may be 0 if unlimited—handle with `eval`.",
  "visualization": "Line chart (scur vs slim), single value alert.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "HAProxy Management Guide — Logging",
      "url": "https://www.haproxy.com/documentation/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'HAProxy Frontend Session Rate Versus ulimit'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `haproxy` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `operational_exceptions.csv` with entity, reason, valid_until.",
  "equipment": [
    "haproxy"
  ],
  "equipmentModels": [
    "haproxy_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: HAProxy stats CSV scripted input.\n• Ensure the following data sources are available: `index=haproxy` `sourcetype=haproxy:stats` (`rate`, `scur`, `slim`).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nCSV field names must match HAProxy version; confirm `slim` presence.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=haproxy sourcetype=\"haproxy:stats\" type=frontend\n| eval sess_pct=if(slim>0, round(100*scur/slim,1), null())\n| where sess_pct > 85\n| table _time, pxname, scur, slim, sess_pct\n```\n\nUnderstanding this SPL\n\n**HAProxy Frontend Session Rate Versus ulimit** — See the description and value fields in this use case JSON.\n\nDocumented **Data sources**: `index=haproxy` `sourcetype=haproxy:stats` (`rate`, `scur`, `slim`). **App/TA**: HAProxy stats CSV scripted input. Rename `index=` / `sourcetype=` to match your deployment.\n\n**Pipeline walkthrough**\n\n• Scope to the documented index and sourcetype, then apply transforms, thresholds, and `timechart`/`stats` as in the SPL above.\n\nStep 3 — Validate\nCompare with the API gateway or mesh admin (Kong, Apigee, AWS API Gateway, etc.) and a raw log tail for the same time range.\n\n\nStep 4 — Operationalize\nAdd to a dashboard or alert; document ownership. Suggested visuals: Line chart (scur vs slim), single value alert..\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (HAProxy stats CSV scripted input — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=haproxy:stats into haproxy. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`sess_pct, slim`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that informs tuning of, kernel limits, and cloud LB pairing.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
