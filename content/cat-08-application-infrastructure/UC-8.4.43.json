{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.4.43",
  "title": "PHP-FPM Unix Socket vs TCP Listen Mode Latency Comparison",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'PHP-FPM Unix Socket vs TCP Listen Mode Latency Comparison' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_php_fpm_unix_socket_vs_tcp_listen_mode_latency_c` (UC-8.4.43), SOC dashboard panel \"Operations — PHP-FPM Unix Socket vs TCP Listen Mode Latency Comparison\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.4.43 positive (PHP, FPM, Unix, Socket, TCP): ingest synthetic events into `index=web` sourcetype='phpfpm:access' where `count` grouped by `listen_mode`, `pool` satisfies detection threshold for 'PHP-FPM Unix Socket vs TCP Listen Mode Latency Comparison'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.4.43 negative (Latency Comparison): ingest benign `web` / `phpfpm:access` events where `listen_mode`, `pool` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 4 confirms zero alert rows for 'PHP-FPM Unix Socket vs TCP Listen Mode Latency Comparison'."
  },
  "dataSources": "Misconfigured TCP listen behind same-host proxy adds latency vs UDS. Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder; optional Splunk Add-on for Unix and Linux (`Splunk_TA_nix`); custom `props.conf`/`transforms.conf` for `phpfpm:*` sourcetypes — Universal Forwarder (Splunkbase 617)",
  "spl": "index=web sourcetype=\"phpfpm:access\"\n| eval listen_mode=coalesce(listen_transport, server_protocol)\n| bin _time span=15m\n| stats perc95(request_time) as p95_ms by listen_mode, pool",
  "description": "Surfaces risk tied to PHP, FPM, Unix, Socket, TCP through UC-8.4.43 on `index=web` sourcetype `phpfpm:access`. Summarises `count` grouped by `listen_mode`, `pool` across `15m` before applying configured threshold. Operators tune 'PHP-FPM Unix Socket vs TCP Listen Mode Latency Comparison' using UC-8.4.43 runbook steps.",
  "value": "Tracking `activity` via UC-8.4.43 on `phpfpm:access` feeds improves response to 'PHP-FPM Unix Socket vs TCP Listen Mode Latency Comparison' and reduces operational risk and delayed incident response.",
  "implementation": "Add custom field from pool config lookup (`listen = /run/php.sock` vs `127.0.0.1:9000`). Compare p95 request_time.",
  "visualization": "Time series (pool utilization, queue, latency percentiles), single value alerts, top charts for scripts and methods.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "CIM: Web",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Web",
      "retrieved": "2026-04-25"
    },
    {
      "title": "PHP-FPM configuration",
      "url": "https://www.php.net/manual/en/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `web` (UC-8.4.43)** — Planned work for 'PHP-FPM Unix Socket vs TCP Listen Mode Latency Comparison' shifts `count` over `span=15m` while `listen_mode`, `pool` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `phpfpm:access`** — Scheduled automation elevates `pool` without crossing configured threshold for UC-8.4.43. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `listen_mode`, `pool`** — Health checks in `web` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `phpfpm:access`** — First 48h baseline for UC-8.4.43 on `listen_mode` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `listen_mode`, `exception_reason`, `valid_until`.",
  "equipment": [
    "linux",
    "nginx",
    "phpfpm"
  ],
  "equipmentModels": [
    "nginx_open"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Universal Forwarder; optional Splunk Add-on for Unix and Linux (`Splunk_TA_nix`); custom `props.conf`/`transforms.conf` for `phpfpm:*` sourcetypes — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=phpfpm:access into web. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`listen_mode, pool`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that quantifies migration benefits when consolidating listeners.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
