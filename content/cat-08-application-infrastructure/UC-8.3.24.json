{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.3.24",
  "title": "HAProxy SSL Handshake and Client Certificate Failures",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'HAProxy SSL Handshake and Client Certificate Failures' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_haproxy_ssl_handshake_and_client_certificate_fai` (UC-8.3.24), SOC dashboard panel \"Operations — HAProxy SSL Handshake and Client Certificate Failures\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.3.24 positive (HAProxy, SSL, Handshake, and, Client): ingest synthetic events into `index=proxy` sourcetype='haproxy:http' where `count` grouped by `host` satisfies detection threshold for 'HAProxy SSL Handshake and Client Certificate Failures'. Validate filter `\"SSL\" (\"handshake failure\" OR \"verify error\" OR \"alert unkno`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.3.24 negative (Certificate Failures): ingest benign `proxy` / `haproxy:http` events where `host` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 10 confirms zero alert rows for 'HAProxy SSL Handshake and Client Certificate Failures'."
  },
  "dataSources": "`index=proxy` TLS termination logs (`sourcetype=haproxy:tcp` or `haproxy:http` with SSL fields) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "HAProxy syslog with `%sslc` / `%sslfc` log variables or custom format — Universal Forwarder (Splunkbase 617)",
  "spl": "index=proxy (sourcetype=\"haproxy:http\" OR sourcetype=\"haproxy:tcp\")\n| search \"SSL\" (\"handshake failure\" OR \"verify error\" OR \"alert unknown ca\")\n| stats count by host\n| sort -count",
  "description": "Monitors UC-8.3.24 — HAProxy, SSL, Handshake, and, Client — using `index=proxy` sourcetype `haproxy:http`, grouped by `host`. Outputs `count` each `the scheduled window`; alert when configured threshold. Filter chain includes filter `\"SSL\" (\"handshake failure\" OR \"verify error\" OR \"alert unkno`. Purpose-built for 'HAProxy SSL Handshake and Client Certificate Failures' in Operations workflows.",
  "value": "For 'HAProxy SSL Handshake and Client Certificate Failures', UC-8.3.24 converts `activity` on `haproxy:http` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Extend `log-format` with SSL variables; sample baseline noise before alerting.",
  "visualization": "Timechart (SSL errors), table (frontend), link to PKI calendar.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "CIM: Web",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Web",
      "retrieved": "2026-04-25"
    },
    {
      "title": "HAProxy Management Guide — Logging",
      "url": "https://www.haproxy.com/documentation/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `proxy` (UC-8.3.24)** — Planned work for 'HAProxy SSL Handshake and Client Certificate Failures' shifts `count` over `span=1h` while `host` remains in-scope. Applies filter `\"SSL\" (\"handshake failure\" OR \"verify error\" OR \"alert unkno`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `haproxy:http`** — Scheduled automation elevates `src` without crossing configured threshold for UC-8.3.24. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `proxy` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `haproxy:http`** — First 48h baseline for UC-8.3.24 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "haproxy",
    "syslog"
  ],
  "equipmentModels": [
    "haproxy_generic",
    "syslog_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: HAProxy syslog with `%sslc` / `%sslfc` log variables or custom format.\n• Ensure the following data sources are available: `index=proxy` TLS termination logs (`sourcetype=haproxy:tcp` or `haproxy:http` with SSL fields).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMatch phrases to your HAProxy TLS log output; many shops ship TLS termination logs only at info level.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=proxy (sourcetype=\"haproxy:http\" OR sourcetype=\"haproxy:tcp\")\n| search \"SSL\" (\"handshake failure\" OR \"verify error\" OR \"alert unknown ca\")\n| stats count by host\n| sort -count\n```\n\nUnderstanding this SPL\n\n**HAProxy SSL Handshake and Client Certificate Failures** — See the description and value fields in this use case JSON.\n\nDocumented **Data sources**: `index=proxy` TLS termination logs (`sourcetype=haproxy:tcp` or `haproxy:http` with SSL fields). **App/TA**: HAProxy syslog with `%sslc` / `%sslfc` log variables or custom format. Rename `index=` / `sourcetype=` to match your deployment.\n\n**Pipeline walkthrough**\n\n• Scope to the documented index and sourcetype, then apply transforms, thresholds, and `timechart`/`stats` as in the SPL above.\n\n\nStep 3 — Validate\nCompare with the broker or gateway’s own UI or CLI (`kafka-consumer-groups`, RabbitMQ management, ActiveMQ console, or Traefik/Envoy access log on the node) for the same period.\n\n\nStep 4 — Operationalize\nAdd to a dashboard or alert; document ownership. Suggested visuals: Timechart (SSL errors), table (frontend), link to PKI calendar..\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (HAProxy syslog with `%sslc` / `%sslfc` log variables or custom format — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=haproxy:http, haproxy:tcp into proxy. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that preserves cryptographic hygiene for zero-trust and mutual secure connections designs.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
