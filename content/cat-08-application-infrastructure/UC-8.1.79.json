{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.1.79",
  "title": "Apache Tomcat Deployment Descriptor Change Detection",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Change",
    "Security"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=web` `sourcetype=tomcat:catalina` (Catalina `catalina.out` / `localhost.log` deployment lifecycle lines)",
  "app": "Splunk Universal Forwarder; Splunk Add-on for JMX (`Splunk_TA_jmx`) for metrics; file inputs for `tomcat:catalina`, `tomcat:access`, `tomcat:manager`",
  "spl": "index=web sourcetype=\"tomcat:catalina\"\n| regex _raw=\"(?i)(Deploying web application|Deployment of web application|Starting ProtocolHandler|Stopping ProtocolHandler)\"\n| rex field=_raw \"descriptor\\[(?<descriptor>[^\\]]+)\\]\"\n| stats earliest(_time) as first_seen latest(_time) as last_seen by host, descriptor\n| where isnotnull(descriptor)",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "description": "Unplanned deploy log lines in Catalina indicate configuration drift, hot redeploy, or supply-chain risk. Correlating descriptor paths with change tickets catches shadow releases.",
  "value": "Reduces unauthorized or accidental production deployments on Java stacks.",
  "grandmaExplanation": "We use this to reduce unauthorized or accidental production deployments on Java stacks.",
  "implementation": "Monitor `$CATALINA_BASE/logs/catalina*.log` with UF; use `multiline` for Java stack traces. Alert on unexpected deploy strings outside change windows.",
  "visualization": "Time charts for utilization, tables for top URIs and deploy events, single-value alerts.",
  "references": [
    {
      "title": "Apache Tomcat documentation",
      "url": "https://tomcat.apache.org/tomcat-10.0-doc/deployer-howto.html"
    }
  ],
  "knownFalsePositives": "Spikes or gaps can follow approved maintenance, load tests, or short collection outages. We add a second signal and a change window before escalating.",
  "equipment": [
    "tomcat"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": ""
}
