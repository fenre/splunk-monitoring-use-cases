{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.1.17",
  "title": "IIS Web Server Monitoring",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'IIS Web Server Monitoring'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-8.1.17).",
  "evidence": "Saved search uc_8_1_17_iis_web_server_monitoring, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'IIS Web Server Monitoring'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-8.1.17.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "`sourcetype=ms:iis:auto` or `sourcetype=iis` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_windows`, Splunk Add-on for Microsoft IIS — Universal Forwarder (Splunkbase 617)",
  "spl": "index=web sourcetype=\"ms:iis:auto\"\n| timechart span=5m count by sc_status\n| eval error_rate = round((sc_status_500 + sc_status_502 + sc_status_503) / (sc_status_200 + sc_status_500 + sc_status_502 + sc_status_503) * 100, 2)",
  "description": "IIS access logs provide visibility into web application health — error rates, response times, and request volumes. Critical for web-facing services.",
  "value": "Operations and leadership rely on 'IIS Web Server Monitoring' to catch regressions before they become customer-visible outages or audit findings. Early signal reduces mean time to repair, avoids duplicate manual log review, and preserves evidence for post-incident review without waiting for a user-reported ticket.",
  "implementation": "Configure IIS to use W3C Extended Log Format with time-taken field. Forward IIS logs from `%SystemDrive%\\inetpub\\logs\\LogFiles`. Use the Microsoft IIS TA for field extraction. Create alerts on 5xx error rate >5%.",
  "visualization": "Line chart (requests by status code), Single value (error rate %), Table of top error URIs.",
  "cimModels": [
    "Web"
  ],
  "cimSpl": "| tstats `summariesonly` count as events\n  from datamodel=Web.Web\n  by Web.http_method Web.dest span=5m\n| sort -events",
  "dataModelAcceleration": "Web: enable Data Model Acceleration for the CIM `Web` dataset once your HTTP access or proxy access logs are tagged and mapped to the model; otherwise `cimSpl` tstats may return no rows. Most teams start with the raw-log search in Step 2 and add acceleration after CIM field aliases validate.",
  "references": [
    {
      "title": "CIM: Web",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Web",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk_TA_windows",
      "url": "https://splunkbase.splunk.com/app/742",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "iis",
    "windows"
  ],
  "equipmentModels": [
    "iis_generic",
    "windows_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_windows`, Splunk Add-on for Microsoft IIS.\n• Ensure the following data sources are available: `sourcetype=ms:iis:auto` or `sourcetype=iis`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nConfigure IIS to use W3C Extended Log Format with time-taken field. Forward IIS logs from `%SystemDrive%\\inetpub\\logs\\LogFiles`. Use the Microsoft IIS TA for field extraction. Create alerts on 5xx error rate >5%.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=web sourcetype=\"ms:iis:auto\"\n| timechart span=5m count by sc_status\n| eval error_rate = round((sc_status_500 + sc_status_502 + sc_status_503) / (sc_status_200 + sc_status_500 + sc_status_502 + sc_status_503) * 100, 2)\n```\n\nUnderstanding this SPL\n\n**IIS Web Server Monitoring** — IIS access logs provide visibility into web application health — error rates, response times, and request volumes. Critical for web-facing services.\n\nDocumented **Data sources**: `sourcetype=ms:iis:auto` or `sourcetype=iis`. **App/TA** (typical add-on context): `Splunk_TA_windows`, Splunk Add-on for Microsoft IIS. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: web; **sourcetype**: ms:iis:auto. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=web, sourcetype=\"ms:iis:auto\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `timechart` plots the metric over time using **span=5m** buckets with a separate series **by sc_status** — ideal for trending and alerting on this use case.\n• `eval` defines or adjusts **error_rate** — often to normalize units, derive a ratio, or prepare for thresholds.\n\n\n\nStep 3 — Validate\nCompare a sample of alert results to IIS W3C extended logs on the server (or a one-off export of the same file) and, when applicable, the IIS Manager site. Confirm `sc-status`, time-taken, and URI fields match what you expect.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (requests by status code), Single value (error rate %), Table of top error URIs.",
  "grandmaExplanation": "We watch for signs that iIS access logs provide visibility into web application health — error rates, response times, and request volumes.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
