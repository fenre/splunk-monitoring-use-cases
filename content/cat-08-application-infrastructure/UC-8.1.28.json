{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.1.28",
  "title": "Envoy Downstream HTTP 429 Local Rate Limit",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance",
    "Security"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Envoy Downstream HTTP 429 Local Rate Limit' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_envoy_downstream_http_429_local_rate_limit` (UC-8.1.28), SOC dashboard panel \"Operations — Envoy Downstream HTTP 429 Local Rate Limit\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=mesh` sourcetype='envoy:stats' that satisfy the UC-8.1.28 SPL filters for 'Envoy Downstream HTTP 429 Local Rate Limit'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `mesh` / `envoy:stats` events outside the detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-8.1.28."
  },
  "dataSources": "`index=mesh` `sourcetype=envoy:stats` (`http.*.downstream_rq_429` counters) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Envoy admin `/stats`, OpenTelemetry Collector, or Prometheus scraper to Splunk — Universal Forwarder (Splunkbase 617)",
  "spl": "index=mesh sourcetype=\"envoy:stats\"\n| search \"downstream_rq_429\"\n| rex \"http\\\\.(?<listener>[^.]+)\\\\.downstream_rq_429=(?<v>\\d+)\"\n| stats latest(v) as rq_429 by listener\n| where rq_429 > 0",
  "description": "429 responses from local rate limits protect upstreams but can signal abuse, mis-tuned quotas, or partner traffic spikes.",
  "value": "Balances protection with customer impact when throttling engages. Early detection reduces outage duration, supports audit evidence, and gives operators actionable context before users report impact.",
  "implementation": "Scrape Prometheus text or stats lines; ensure listener dimensions are parsed. Correlate with `envoy.access` logs if enabled.",
  "visualization": "Single value (429 rate), line chart per listener, table (cluster × route).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "Envoy — Statistics overview",
      "url": "https://www.envoyproxy.io/docs/envoy/latest/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance** — Planned change generates expected deviations for 'Envoy Downstream HTTP 429 Local Rate Limit'. Cross-check change records.\n\n2. **Batch job cycle** — Fixed-schedule workload skews `mesh` metrics.\n\n3. **Monitoring synthetic checks** — Health probes from known hosts.\n\n4. **Onboarding burst** — New entities produce atypical volume until baseline stabilizes.\n\n**Suppression mechanism:** `ops_exception_register.csv` with entity, reason, valid_until.",
  "equipment": [
    "envoy",
    "opentelemetry",
    "prometheus"
  ],
  "equipmentModels": [
    "envoy_generic",
    "opentelemetry_generic",
    "prometheus_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Envoy admin `/stats`, OpenTelemetry Collector, or Prometheus scraper to Splunk.\n• Ensure the following data sources are available: `index=mesh` `sourcetype=envoy:stats` (`http.*.downstream_rq_429` counters).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMetric naming varies by Envoy version; broaden search to `*rq_429*` if your scrape format differs.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=mesh sourcetype=\"envoy:stats\"\n| search \"downstream_rq_429\"\n| rex \"http\\\\.(?<listener>[^.]+)\\\\.downstream_rq_429=(?<v>\\d+)\"\n| stats latest(v) as rq_429 by listener\n| where rq_429 > 0\n```\n\nUnderstanding this SPL\n\n**Envoy Downstream HTTP 429 Local Rate Limit** — See the description and value fields in this use case JSON.\n\nDocumented **Data sources**: `index=mesh` `sourcetype=envoy:stats` (`http.*.downstream_rq_429` counters). **App/TA**: Envoy admin `/stats`, OpenTelemetry Collector, or Prometheus scraper to Splunk. Rename `index=` / `sourcetype=` to match your deployment.\n\n**Pipeline walkthrough**\n\n• Scope to the documented index and sourcetype, then apply transforms, thresholds, and `timechart`/`stats` as in the SPL above.\n\nStep 3 — Validate\nCompare with web server access logs on disk (Apache, NGINX, or W3C) for the same time range, or tail the same sourcetype in Search, to confirm status codes, URIs, and counts.\n\n\nStep 4 — Operationalize\nAdd to a dashboard or alert; document ownership. Suggested visuals: Single value (429 rate), line chart per listener, table (cluster × route)..\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Envoy admin `/stats`, OpenTelemetry Collector, or Prometheus scraper to Splunk — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=envoy:stats into mesh. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`listener, rq_429`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that balances protection with customer impact when throttling engages.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
