{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.1.16",
  "title": "Web Server Thread Pool Exhaustion",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Web Server Thread Pool Exhaustion' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_web_server_thread_pool_exhaustion` (UC-8.1.16), SOC dashboard panel \"Operations — Web Server Thread Pool Exhaustion\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.1.16 positive (Web, Server, Thread, Pool, Exhaustion): ingest synthetic events into `index=web` sourcetype='nginx:stub_status' where `util` grouped by `host`, `sourcetype` satisfies `util_pct` > 85, or `queue_current` > 50 for 'Web Server Thread Pool Exhaustion'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.1.16 negative (Pool Exhaustion): ingest benign `web` / `nginx:stub_status` events where `host`, `sourcetype` stays below `util_pct` > 85, or `queue_current` > 50, or exclude the test entity via `ops_exception_register.csv`. Variant 1 confirms zero alert rows for 'Web Server Thread Pool Exhaustion'."
  },
  "dataSources": "`nginx:stub_status`, `Perfmon:W3SVC_W3WP`, `apache:server_status` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`TA-nginx` stub_status, `Splunk_TA_windows` perfmon, Apache mod_status — Universal Forwarder (Splunkbase 617)",
  "spl": "index=web (sourcetype=\"nginx:stub_status\" OR sourcetype=\"apache:server_status\" OR sourcetype=\"Perfmon:W3SVC_W3WP\")\n| eval util_pct=coalesce(worker_util_pct, pct_busy, thread_pool_queue_length/max_threads*100)\n| where util_pct > 85 OR queue_current > 50\n| timechart span=5m max(util_pct) as util by host, sourcetype",
  "description": "Monitors Pool Exhaustion activity in UC-8.1.16 ('Web Server Thread Pool Exhaustion') from `index=web` sourcetype `nginx:stub_status`. Rolls up `util` by `host`, `sourcetype` across `5m` and pages when `util_pct` > 85, or `queue_current` > 50. Covers Web, Server, Thread, Pool, Exhaustion. Operations operators treat UC-8.1.16 as the canonical signal.",
  "value": "Operators running UC-8.1.16 on `nginx:stub_status` feeds shorten triage for Pool Exhaustion tied to 'Web Server Thread Pool Exhaustion', lowering operational risk and delayed incident response.",
  "implementation": "Normalize field names at ingest. Alert when util >85% for 10m or IIS request queue length sustained high. Correlate with CPU and backend latency.",
  "visualization": "Gauge (util %), Line chart (util and queue), Table (hosts over threshold).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "Splunk_TA_windows",
      "url": "https://splunkbase.splunk.com/app/742",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Web",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Web",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `web` (UC-8.1.16)** — Planned work for 'Web Server Thread Pool Exhaustion' shifts `util` over `span=5m` while `host`, `sourcetype` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `max` on `nginx:stub_status`** — Scheduled automation elevates `sourcetype` without crossing `util_pct` > 85 for UC-8.1.16. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`, `sourcetype`** — Health checks in `web` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `nginx:stub_status`** — First 48h baseline for UC-8.1.16 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "apache",
    "nginx",
    "windows"
  ],
  "equipmentModels": [
    "apache_httpd",
    "nginx_open"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `TA-nginx` stub_status, `Splunk_TA_windows` perfmon, Apache mod_status.\n• Ensure the following data sources are available: `nginx:stub_status`, `Perfmon:W3SVC_W3WP`, `apache:server_status`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nNormalize field names at ingest. Alert when util >85% for 10m or IIS request queue length sustained high. Correlate with CPU and backend latency.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=web (sourcetype=\"nginx:stub_status\" OR sourcetype=\"apache:server_status\" OR sourcetype=\"Perfmon:W3SVC_W3WP\")\n| eval util_pct=coalesce(worker_util_pct, pct_busy, thread_pool_queue_length/max_threads*100)\n| where util_pct > 85 OR queue_current > 50\n| timechart span=5m max(util_pct) as util by host, sourcetype\n```\n\nUnderstanding this SPL\n\n**Web Server Thread Pool Exhaustion** — IIS `QueueFull`, NGINX worker saturation, or Apache `BusyWorkers` at limit causes queueing. Unified thresholding across stacks.\n\nDocumented **Data sources**: `nginx:stub_status`, `Perfmon:W3SVC_W3WP`, `apache:server_status`. **App/TA** (typical add-on context): `TA-nginx` stub_status, `Splunk_TA_windows` perfmon, Apache mod_status. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: web; **sourcetype**: nginx:stub_status, apache:server_status, Perfmon:W3SVC_W3WP. Those sourcetypes align with what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=web, sourcetype=\"nginx:stub_status\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **util_pct** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where util_pct > 85 OR queue_current > 50` — typically the threshold or rule expression for this monitoring goal.\n• `timechart` plots the metric over time using **span=5m** buckets with a separate series **by host, sourcetype** — ideal for trending and alerting on this use case.\n\n\nStep 3 — Validate\nCompare with web server access logs on disk (Apache, NGINX, or W3C) for the same time range, or tail the same sourcetype in Search, to confirm status codes, URIs, and counts.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Gauge (util %), Line chart (util and queue), Table (hosts over threshold).",
  "grandmaExplanation": "We watch for signs that iIS, NGINX worker saturation, or Apache at limit causes queueing.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
