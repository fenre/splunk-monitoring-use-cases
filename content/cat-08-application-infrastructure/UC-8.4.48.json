{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.4.48",
  "title": "PHP-FPM Connection Refused Rate Correlated with Pool Saturation",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Availability",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'PHP-FPM Connection Refused Rate Correlated with Pool Saturation' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_php_fpm_connection_refused_rate_correlated_with_` (UC-8.4.48), SOC dashboard panel \"Operations — PHP-FPM Connection Refused Rate Correlated with Pool Saturation\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=web` sourcetype='nginx:error' where `host` satisfies `mc` > 0 for UC-8.4.48 ('PHP-FPM Connection Refused Rate Correlated with Pool Saturation'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `web` / `nginx:error` events where `host` stays below `mc` > 0, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-8.4.48."
  },
  "dataSources": "Upstream `connection refused` aligned with FPM saturation confirms worker exhaustion. Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder; optional Splunk Add-on for Unix and Linux (`Splunk_TA_nix`); custom `props.conf`/`transforms.conf` for `phpfpm:*` sourcetypes — Universal Forwarder (Splunkbase 617)",
  "spl": "index=web (sourcetype=\"nginx:error\" OR sourcetype=\"haproxy:http\")\n| regex _raw=\"(?i)(connect\\(\\) failed.*9000|Connection refused)\"\n| bin _time span=1m\n| stats count as refused by host, _time\n| join host _time [\n    search index=web sourcetype=\"phpfpm:status\" earliest=-2m latest=+2m\n    | bin _time span=1m\n    | stats max(active_processes) as act max(max_children) as mc by host, _time\n]\n| eval saturated=if(mc>0 AND act>=mc*0.95, 1, 0)\n| where refused > 0 AND saturated == 1",
  "description": "Upstream connection refused errors timed with FPM at `max_children` confirm worker exhaustion rather than network partitions.",
  "value": "Upstream connection refused errors timed with FPM at `max_children` confirm worker exhaustion rather than n.... Timely UC-8.4.48 alerting on 'PHP-FPM Connection Refused Rate Correlated with Pool Saturation' reduces operational risk and delayed incident response before manual triage or audit channels escalate.",
  "implementation": "Ingest nginx/haproxy error logs; time-align with `phpfpm:status` snapshots. Adjust join window for poll interval.",
  "visualization": "Time series (pool utilization, queue, latency percentiles), single value alerts, top charts for scripts and methods.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "PHP-FPM configuration",
      "url": "https://www.nginx.com/resources/wiki/start/topics/tutorials/config_pitfalls/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `web`** — Planned work for 'PHP-FPM Connection Refused Rate Correlated with Pool Saturation' shifts `max` over `span=1m` while `host` remains in-scope for UC-8.4.48. Cross-check CMDB CHG.\n\n2. **Batch `max` on `nginx:error`** — Scheduled automation elevates `_time` without crossing `mc` > 0. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `web` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `nginx:error`** — First 48h baseline for `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "haproxy",
    "linux",
    "nginx",
    "phpfpm"
  ],
  "equipmentModels": [
    "nginx_open"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Universal Forwarder; optional Splunk Add-on for Unix and Linux (`Splunk_TA_nix`); custom `props.conf`/`transforms.conf` for `phpfpm:*` sourcetypes — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=nginx:error, haproxy:http, phpfpm:status into web. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`], _time, host, mc`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that narrows blame between PHP, proxy, and network teams during incidents.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
