{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.1.31",
  "title": "RabbitMQ Queue Consumer Utilisation Degradation",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'RabbitMQ Queue Consumer Utilisation Degradation' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_rabbitmq_queue_consumer_utilisation_degradation` (UC-8.1.31), SOC dashboard panel \"Operations — RabbitMQ Queue Consumer Utilisation Degradation\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=messaging` sourcetype='rabbitmq:queue' where `entity` satisfies `consumers` > 0 for UC-8.1.31 ('RabbitMQ Queue Consumer Utilisation Degradation'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `messaging` / `rabbitmq:queue` events where `entity` stays below `consumers` > 0, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-8.1.31."
  },
  "dataSources": "RabbitMQ HTTP management API `/api/queues` fields `consumer_utilisation`, `consumers`, `messages_ready`",
  "app": "RabbitMQ management plugin + scripted/HEC poller (sourcetype `rabbitmq:queue`); AMQP Messaging Modular Input (Splunkbase 1812) for complementary message-plane visibility",
  "spl": "index=messaging sourcetype=\"rabbitmq:queue\"\n| where consumers > 0 AND isnotnull(consumer_utilisation) AND consumer_utilisation < 0.50 AND messages_ready > 100\n| table _time, vhost, name, consumers, consumer_utilisation, messages_ready",
  "description": "`consumer_utilisation` near zero with ready messages indicates consumers are connected but not keeping up (poison messages, prefetch misuse, or slow handlers), which drives queue latency.",
  "value": "`consumer_utilisation` near zero with ready messages indicates consumers are connected but not keeping up (.... Timely UC-8.1.31 alerting on 'RabbitMQ Queue Consumer Utilisation Degradation' reduces operational risk and delayed incident response before manual triage or audit channels escalate.",
  "implementation": "Ingest queue JSON from `/api/queues` at least every minute. Confirm `consumer_utilisation` is extracted as a number (0–1). Exclude federated/shovel-only queues via a lookup. Page when the condition holds for two consecutive polls on tier-1 queues.",
  "visualization": "Line chart (consumer_utilisation by queue), Table (worst queues), Bar chart (messages_ready).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "RabbitMQ — Monitoring",
      "url": "https://www.rabbitmq.com/docs/monitoring",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RabbitMQ Management HTTP API",
      "url": "https://www.rabbitmq.com/docs/management#http-api",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `messaging`** — Planned work for 'RabbitMQ Queue Consumer Utilisation Degradation' shifts `count` over `span=1h` while `host` remains in-scope for UC-8.1.31. Cross-check CMDB CHG.\n\n2. **Batch `count` on `rabbitmq:queue`** — Scheduled automation elevates `src` without crossing `consumers` > 0. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `messaging` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `rabbitmq:queue`** — First 48h baseline for `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "rabbitmq"
  ],
  "equipmentModels": [
    "rabbitmq_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (RabbitMQ management plugin + scripted/HEC poller (sourcetype `rabbitmq:queue`); AMQP Messaging Modular Input (Splunkbase 1812) for complementary message-plane visibility). Enable the modular input or HEC token that writes sourcetype=rabbitmq:queue into messaging. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`consumer_utilisation, consumers, messages_ready`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We catch ineffective consumption early — before only queue depth alarms fire — so teams can fix consumer code, scaling, or prefetch settings.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
