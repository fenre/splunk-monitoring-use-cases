{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.5.24",
  "title": "RabbitMQ Federation Link Health and Upstream Lag",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Availability",
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'RabbitMQ Federation Link Health and Upstream Lag'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-8.5.24).",
  "evidence": "Saved search uc_8_5_24_rabbitmq_federation_link_health_and_upst, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'RabbitMQ Federation Link Health and Upstream Lag'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-8.5.24.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "`index=messaging` `sourcetype=rabbitmq:metrics` (RabbitMQ Prometheus plugin; federation link gauges/counters with labels `vhost`, `upstream`), optional `sourcetype=rabbitmq:log` for federation plugin errors Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "RabbitMQ Prometheus (`rabbitmq_prometheus` plugin) scraped to Splunk HTTP Event Collector, or Observability Cloud agent; Universal Forwarder for `rabbit@*.log` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=messaging sourcetype=\"rabbitmq:metrics\" earliest=-24h\n| search metric_name=\"rabbitmq_federation_link_up\" OR metric_name=\"rabbitmq_federation_unacked_messages\"\n| eval link_up=if(metric_name=\"rabbitmq_federation_link_up\", coalesce(metric_value, value, gauge), null())\n| eval unacked_upstream=if(metric_name=\"rabbitmq_federation_unacked_messages\", coalesce(metric_value, value, gauge), null())\n| eval vhost_dim=coalesce(vhost, federation_vhost, dimension_vhost)\n| eval upstream_dim=coalesce(upstream, federation_upstream, dimension_upstream)\n| where (isnotnull(link_up) AND link_up==0) OR (isnotnull(unacked_upstream) AND unacked_upstream > 10000)\n| stats latest(link_up) as up latest(unacked_upstream) as unacked by host, vhost_dim, upstream_dim\n| where up==0 OR unacked > 10000\n| table host, vhost_dim, upstream_dim, up, unacked",
  "description": "Federation links carry traffic across clusters and regions; a link in the down state or a large `rabbitmq_federation_unacked_messages` value means upstream backlog is building and regional DR or hub-spoke routing is at risk. Correlating Prometheus federation metrics with broker logs speeds isolation of TLS, policy, or network faults.",
  "value": "Protects multi-site messaging topologies from partial outages that otherwise show up only as mysterious queue growth on downstream vhosts.",
  "implementation": "Enable `rabbitmq_prometheus` and scrape `/metrics` into Splunk; ensure label dimensions map to indexed fields (`vhost`, `upstream`). Alert on `rabbitmq_federation_link_up==0` for tier-1 links and tune unacked threshold by message size.",
  "visualization": "Table (vhost, upstream, link_up, unacked messages), geomap or topology panel by site, line chart of unacked federation backlog.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "RabbitMQ — Federation",
      "url": "https://www.rabbitmq.com/docs/federation",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RabbitMQ — Prometheus plugin",
      "url": "https://www.rabbitmq.com/docs/prometheus",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "prometheus",
    "rabbitmq"
  ],
  "equipmentModels": [
    "prometheus_generic",
    "rabbitmq_generic"
  ],
  "status": "verified",
  "lastReviewed": "2026-06-01",
  "splunkVersions": [
    "9.x",
    "8.x"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (RabbitMQ Prometheus (`rabbitmq_prometheus` plugin) scraped to Splunk HTTP Event Collector, or Observability Cloud agent; Universal Forwarder for `rabbit@*.log` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=rabbitmq:metrics into messaging. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, link_up, unacked, unacked_upstream`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use this to protect multi-site messaging topologies from partial outages that otherwise show up only as mysterious queue growth on downstream vhosts.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
