{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.1.27",
  "title": "IIS W3C Time-Taken Latency Percentiles",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`index=web` `sourcetype=ms:iis:auto` with `time-taken` field (milliseconds)",
  "app": "Splunk Add-on for Microsoft IIS (Splunkbase app 3185)",
  "spl": "index=web sourcetype=\"ms:iis:auto\"\n| eval tt=coalesce('time-taken', time_taken)\n| where isnotnull(tt) AND tt>=0\n| timechart span=5m perc95(tt) as p95_ms by s_sitename\n| where p95_ms > 3000",
  "description": "The W3C `time-taken` field is the authoritative IIS request duration. P95 by site highlights slow apps, ARR backends, or cold .NET startup.",
  "value": "Surfaces latency regressions that status-code-only dashboards miss.",
  "implementation": "Enable `time-taken` in IIS logging; use `ms:iis:auto`. Exclude static content if needed via `cs-uri-stem` filters.",
  "visualization": "Line chart (P95 per site), table (top URIs using separate drilldown search), compare to app pool recycle windows.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Add-on for Microsoft IIS (Splunkbase)",
      "url": "https://splunkbase.splunk.com/app/3185"
    }
  ],
  "equipment": [
    "iis"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Add-on for Microsoft IIS (Splunkbase app 3185).\n• Ensure the following data sources are available: `index=web` `sourcetype=ms:iis:auto` with `time-taken` field (milliseconds).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nConfirm `time-taken` is enabled in the IIS logging field list.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=web sourcetype=\"ms:iis:auto\"\n| eval tt=coalesce('time-taken', time_taken)\n| where isnotnull(tt) AND tt>=0\n| timechart span=5m perc95(tt) as p95_ms by s_sitename\n| where p95_ms > 3000\n```\n\nUnderstanding this SPL\n\n**IIS W3C Time-Taken Latency Percentiles** — See the description and value fields in this use case JSON.\n\nDocumented **Data sources**: `index=web` `sourcetype=ms:iis:auto` with `time-taken` field (milliseconds). **App/TA**: Splunk Add-on for Microsoft IIS (Splunkbase app 3185). Rename `index=` / `sourcetype=` to match your deployment.\n\n**Pipeline walkthrough**\n\n• Scope to the documented index and sourcetype, then apply transforms, thresholds, and `timechart`/`stats` as in the SPL above.\n\nStep 3 — Validate\nConfirm events exist, field extractions match your format, and thresholds reflect production baselines.\n\nStep 4 — Operationalize\nAdd to a dashboard or alert; document ownership. Suggested visuals: Line chart (P95 per site), table (top URIs using separate drilldown search), compare to app pool recycle windows..",
  "grandmaExplanation": "We track how long Windows web servers take to answer so slow releases are obvious."
}
