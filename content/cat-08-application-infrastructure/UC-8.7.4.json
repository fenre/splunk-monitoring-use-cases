{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.7.4",
  "title": "Cache Hit Ratio Trending",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Cache Hit Ratio Trending' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_cache_hit_ratio_trending` (UC-8.7.4), SOC dashboard panel \"Operations — Cache Hit Ratio Trending\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.7.4 positive (Cache, Hit, Ratio, Trending): ingest synthetic events into `index=middleware` sourcetype='redis:info' where `cache_hit_ratio_pct` grouped by `entity` satisfies detection threshold for 'Cache Hit Ratio Trending'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.7.4 negative (Ratio Trending): ingest benign `middleware` / `redis:info` events where `entity` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 2 confirms zero alert rows for 'Cache Hit Ratio Trending'."
  },
  "dataSources": "`index=middleware` `sourcetype=redis:info`, `memcached:stats`, or application-emitted cache metrics Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Add-on for Redis, Memcached, Varnish or CDN logs — Universal Forwarder (Splunkbase 617)",
  "spl": "index=middleware (sourcetype=\"redis:info\" OR sourcetype=\"memcached:stats\" OR sourcetype=\"app:cache:metrics\")\n| eval hits=coalesce(keyspace_hits, cache_hits, 0)\n| eval misses=coalesce(keyspace_misses, cache_misses, 0)\n| eval hit_ratio=if((hits+misses)>0, round(100*hits/(hits+misses),2), null())\n| timechart span=1d avg(hit_ratio) as cache_hit_ratio_pct",
  "description": "Surfaces UC-8.7.4 for Cache, Hit, Ratio, Trending over `index=middleware` sourcetype `redis:info` with thresholds configured threshold. Groups `cache_hit_ratio_pct` by `entity` each `1d`. Centered on Ratio Trending indicators. Supports repeatable operations triage for 'Cache Hit Ratio Trending'.",
  "value": "For 'Cache Hit Ratio Trending', UC-8.7.4 converts `cache_hit_ratio_pct` on `redis:info` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Poll INFO/stats on a fixed interval; compute ratio in SPL or at ingest for accuracy. Split by `instance` or `cluster` for sharded caches. Correlate drops with deployments and TTL changes. For HTTP caches, derive hits from `X-Cache` or CDN logs instead.",
  "visualization": "Line chart (hit ratio %), dual axis (hits and misses counts), single value (30-day min hit ratio).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `middleware` (UC-8.7.4)** — Planned work for 'Cache Hit Ratio Trending' shifts `cache_hit_ratio_pct` over `span=1d` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `redis:info`** — Scheduled automation elevates `hits` without crossing configured threshold for UC-8.7.4. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `middleware` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `redis:info`** — First 48h baseline for UC-8.7.4 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "memcached",
    "redis",
    "varnish"
  ],
  "equipmentModels": [
    "memcached_generic",
    "redis_generic",
    "varnish_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Add-on for Redis, Memcached, Varnish or CDN logs.\n• Ensure the following data sources are available: `index=middleware` `sourcetype=redis:info`, `memcached:stats`, or application-emitted cache metrics.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nPoll INFO/stats on a fixed interval; compute ratio in SPL or at ingest for accuracy. Split by `instance` or `cluster` for sharded caches. Correlate drops with deployments and TTL changes. For HTTP caches, derive hits from `X-Cache` or CDN logs instead.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=middleware (sourcetype=\"redis:info\" OR sourcetype=\"memcached:stats\" OR sourcetype=\"app:cache:metrics\")\n| eval hits=coalesce(keyspace_hits, cache_hits, 0)\n| eval misses=coalesce(keyspace_misses, cache_misses, 0)\n| eval hit_ratio=if((hits+misses)>0, round(100*hits/(hits+misses),2), null())\n| timechart span=1d avg(hit_ratio) as cache_hit_ratio_pct\n```\n\nUnderstanding this SPL\n\n**Cache Hit Ratio Trending** — Cache hit ratio over 30 days reveals memory sizing issues, key churn, and upstream slowdowns that force more origin fetches. Declining trends often precede latency SLO breaches.\n\nDocumented **Data sources**: `index=middleware` `sourcetype=redis:info`, `memcached:stats`, or application-emitted cache metrics. **App/TA** (typical add-on context): Splunk Add-on for Redis, Memcached, Varnish or CDN logs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: middleware; **sourcetype**: redis:info, memcached:stats, app:cache:metrics. Those sourcetypes align with what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=middleware, sourcetype=\"redis:info\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **hits** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **misses** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **hit_ratio** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `timechart` plots the metric over time using **span=1d** buckets — ideal for trending and alerting on this use case.\n\n\nStep 3 — Validate\nCompare with the application or platform source of truth (logs, UI, or metrics) for the same time range, and with known change or maintenance windows.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (hit ratio %), dual axis (hits and misses counts), single value (30-day min hit ratio).",
  "grandmaExplanation": "We watch for signs that cache hit ratio over 30 days reveals memory sizing issues, key churn, and upstream slowdowns that force more origin fetches.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
