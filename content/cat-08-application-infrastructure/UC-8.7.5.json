{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.7.5",
  "title": "Message Queue Backlog Trending",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Message Queue Backlog Trending' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_message_queue_backlog_trending` (UC-8.7.5), SOC dashboard panel \"Operations — Message Queue Backlog Trending\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=middleware` sourcetype='kafka:consumer' where `qname` satisfies detection threshold for UC-8.7.5 ('Message Queue Backlog Trending'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `middleware` / `kafka:consumer` events where `qname` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-8.7.5."
  },
  "dataSources": "`index=middleware` `sourcetype=kafka:consumer`, `rabbitmq:queue`, `activemq:queue`, `azure:eventhub:metrics` Collected via Splunk Add-on for Microsoft Azure (Splunkbase 3110) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Add-on for Kafka, RabbitMQ, ActiveMQ, AWS MSK / Azure Event Hubs TAs — Splunk Add-on for Microsoft Azure (Splunkbase 3110)",
  "spl": "index=middleware earliest=-30d (sourcetype=\"kafka:consumer\" OR sourcetype=\"rabbitmq:queue\" OR sourcetype=\"activemq:queue\")\n| eval depth=coalesce(consumer_lag, lag, queue_depth, backlog_messages)\n| eval qname=coalesce(topic_queue, queue, destination)\n| timechart span=1d max(depth) as queue_depth by qname limit=15",
  "description": "Queue depth over 7 and 30 days shows sustained consumer lag versus transient spikes. Growth trends drive consumer scaling, partition adds, or poison-message handling before disk limits or SLA misses.",
  "value": "Early 'Message Queue Backlog Trending' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Align Kafka lag with consumer group and partition; use `max` across partitions for worst-case visibility. Exclude retry/DLQ topics from primary charts or show separately. Set thresholds from peak business hours using historical baselines. For cloud queues, map metric dimensions to the same `qname` namespace.",
  "visualization": "Line chart (max depth by queue), area chart (7d vs 30d overlay using `timewrap`), table (top queues by growth rate).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 3110",
      "url": "https://splunkbase.splunk.com/app/3110",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `middleware`** — Planned work for 'Message Queue Backlog Trending' shifts `max` over `span=1d` while `qname` remains in-scope for UC-8.7.5. Cross-check CMDB CHG.\n\n2. **Batch `max` on `kafka:consumer`** — Scheduled automation elevates `limit=15` without crossing configured threshold. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `qname`** — Health checks in `middleware` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `kafka:consumer`** — First 48h baseline for `qname` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `qname`, `exception_reason`, `valid_until`.",
  "equipment": [
    "activemq",
    "aws",
    "azure",
    "kafka",
    "rabbitmq"
  ],
  "equipmentModels": [
    "activemq_generic",
    "aws_generic",
    "azure_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Add-on for Kafka, RabbitMQ, ActiveMQ, AWS MSK / Azure Event Hubs TAs.\n• Ensure the following data sources are available: `index=middleware` `sourcetype=kafka:consumer`, `rabbitmq:queue`, `activemq:queue`, `azure:eventhub:metrics`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nAlign Kafka lag with consumer group and partition; use `max` across partitions for worst-case visibility. Exclude retry/DLQ topics from primary charts or show separately. Set thresholds from peak business hours using historical baselines. For cloud queues, map metric dimensions to the same `qname` namespace.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=middleware earliest=-30d (sourcetype=\"kafka:consumer\" OR sourcetype=\"rabbitmq:queue\" OR sourcetype=\"activemq:queue\")\n| eval depth=coalesce(consumer_lag, lag, queue_depth, backlog_messages)\n| eval qname=coalesce(topic_queue, queue, destination)\n| timechart span=1d max(depth) as queue_depth by qname limit=15\n```\n\nUnderstanding this SPL\n\n**Message Queue Backlog Trending** — Queue depth over 7 and 30 days shows sustained consumer lag versus transient spikes. Growth trends drive consumer scaling, partition adds, or poison-message handling before disk limits or SLA misses.\n\nDocumented **Data sources**: `index=middleware` `sourcetype=kafka:consumer`, `rabbitmq:queue`, `activemq:queue`, `azure:eventhub:metrics`. **App/TA** (typical add-on context): Splunk Add-on for Kafka, RabbitMQ, ActiveMQ, AWS MSK / Azure Event Hubs TAs. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: middleware; **sourcetype**: kafka:consumer, rabbitmq:queue, activemq:queue. Those sourcetypes align with what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=middleware, sourcetype=\"kafka:consumer\", time bounds. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **depth** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **qname** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `timechart` plots the metric over time using **span=1d** buckets with a separate series **by qname limit=15** — ideal for trending and alerting on this use case.\n\n\nStep 3 — Validate\nCompare with the application or platform source of truth (logs, UI, or metrics) for the same time range, and with known change or maintenance windows.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (max depth by queue), area chart (7d vs 30d overlay using `timewrap`), table (top queues by growth rate).",
  "grandmaExplanation": "We watch for signs that queue depth over 7 and 30 days shows sustained consumer lag versus transient spikes.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
