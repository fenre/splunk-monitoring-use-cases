{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.2.2",
  "title": "Garbage Collection Impact",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Garbage Collection Impact' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_garbage_collection_impact` (UC-8.2.2), SOC dashboard panel \"Operations — Garbage Collection Impact\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.2.2 positive (Garbage, Collection, Impact): ingest synthetic events into `index=jvm` sourcetype='jvm:gc' where `total_pause_ms` grouped by `host` satisfies `gc_pause_ms` > 200 for 'Garbage Collection Impact'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.2.2 negative (Collection Impact): ingest benign `jvm` / `jvm:gc` events where `host` stays below `gc_pause_ms` > 200, or exclude the test entity via `ops_exception_register.csv`. Variant 6 confirms zero alert rows for 'Garbage Collection Impact'."
  },
  "dataSources": "JVM GC logs, JMX GarbageCollector MBeans Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "GC log parsing, `TA-jmx` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=jvm sourcetype=\"jvm:gc\"\n| where gc_pause_ms > 200\n| timechart span=15m count as gc_events, sum(gc_pause_ms) as total_pause_ms by host\n| eval pause_pct=round(total_pause_ms/900000*100,2)",
  "description": "Identifies Collection Impact anomalies for UC-8.2.2 with window `15m` on `index=jvm` sourcetype `jvm:gc`. Combines `total_pause_ms` by `host` and compares against `gc_pause_ms` > 200. Mapped to keywords Garbage, Collection, Impact for dashboard routing.",
  "value": "Alerting on Garbage, Collection, Impact through UC-8.2.2 reduces operational risk and delayed incident response by surfacing `total_pause_ms` early.",
  "implementation": "Enable GC logging on all JVM-based app servers (`-Xlog:gc*` for Java 11+). Forward logs via UF. Parse pause duration, type, and cause. Alert on pauses >200ms or total pause time >5% of wall clock time.",
  "visualization": "Line chart (GC pause duration), Histogram (pause distribution), Single value (total pause time per hour).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `jvm` (UC-8.2.2)** — Planned work for 'Garbage Collection Impact' shifts `total_pause_ms` over `span=15m` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `sum` on `jvm:gc`** — Scheduled automation elevates `pause_pct` without crossing `gc_pause_ms` > 200 for UC-8.2.2. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `jvm` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `jvm:gc`** — First 48h baseline for UC-8.2.2 on `host` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: GC log parsing, `TA-jmx`.\n• Ensure the following data sources are available: JVM GC logs, JMX GarbageCollector MBeans.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnable GC logging on all JVM-based app servers (`-Xlog:gc*` for Java 11+). Forward logs via UF. Parse pause duration, type, and cause. Alert on pauses >200ms or total pause time >5% of wall clock time.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=jvm sourcetype=\"jvm:gc\"\n| where gc_pause_ms > 200\n| timechart span=15m count as gc_events, sum(gc_pause_ms) as total_pause_ms by host\n| eval pause_pct=round(total_pause_ms/900000*100,2)\n```\n\nUnderstanding this SPL\n\n**Garbage Collection Impact** — Frequent or long GC pauses cause application latency spikes and request timeouts. Monitoring guides JVM tuning.\n\nDocumented **Data sources**: JVM GC logs, JMX GarbageCollector MBeans. **App/TA** (typical add-on context): GC log parsing, `TA-jmx`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: jvm; **sourcetype**: jvm:gc. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=jvm, sourcetype=\"jvm:gc\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where gc_pause_ms > 200` — typically the threshold or rule expression for this monitoring goal.\n• `timechart` plots the metric over time using **span=15m** buckets with a separate series **by host** — ideal for trending and alerting on this use case.\n• `eval` defines or adjusts **pause_pct** — often to normalize units, derive a ratio, or prepare for thresholds.\n\n\nStep 3 — Validate\nCompare with JBoss, WebLogic, or Tomcat admin consoles, or `catalina` / server logs on the host, for the same window. Confirm hostnames and fields match the vendor UI.\n\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (GC pause duration), Histogram (pause distribution), Single value (total pause time per hour).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (GC log parsing, `TA-jmx` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=jvm:gc into jvm. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`gc_pause_ms, host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that frequent or long GC pauses cause application latency spikes and request timeouts.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
