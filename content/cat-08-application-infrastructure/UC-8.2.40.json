{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.2.40",
  "title": "Microsoft IIS HTTP 500.x Sub-Status Code Breakdown",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault",
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Microsoft IIS HTTP 500.x Sub-Status Code Breakdown' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_microsoft_iis_http_500_x_sub_status_code_breakdo` (UC-8.2.40), SOC dashboard panel \"Operations — Microsoft IIS HTTP 500.x Sub-Status Code Breakdown\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.2.40 positive (Microsoft, IIS, HTTP, 500.x, Sub): ingest synthetic events into `index=web` sourcetype='ms:iis:auto' where `count` grouped by `sc_status`, `sub`, `s_sitename` satisfies `sc_status` >= 500, or `sc_status` < 600 for 'Microsoft IIS HTTP 500.x Sub-Status Code Breakdown'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.2.40 negative (Code Breakdown): ingest benign `web` / `ms:iis:auto` events where `sc_status`, `sub`, `s_sitename` stays below `sc_status` >= 500, or `sc_status` < 600, or exclude the test entity via `ops_exception_register.csv`. Variant 13 confirms zero alert rows for 'Microsoft IIS HTTP 500.x Sub-Status Code Breakdown'."
  },
  "dataSources": "`index=web` `sourcetype=ms:iis:auto` W3C extended logs with `sc-status` and `sc-substatus` fields",
  "app": "Splunk Add-on for Microsoft Windows (`Splunk_TA_windows`); Splunk Add-on for Microsoft IIS (Splunkbase 3185)",
  "spl": "index=web sourcetype=\"ms:iis:auto\"\n| where sc_status >= 500 AND sc_status < 600\n| eval sub=coalesce('sc-substatus', sc_substatus, \"0\")\n| stats count by sc_status, sub, s_sitename\n| sort - count",
  "description": "Monitors UC-8.2.40 — Microsoft, IIS, HTTP, 500.x, Sub — using `index=web` sourcetype `ms:iis:auto`, grouped by `sc_status`, `sub`, `s_sitename`. Outputs `count` each `the scheduled window`; alert when `sc_status` >= 500, or `sc_status` < 600. Primary focus: Code Breakdown. Purpose-built for 'Microsoft IIS HTTP 500.x Sub-Status Code Breakdown' in Operations workflows.",
  "value": "UC-8.2.40 gives Operations teams earlier visibility into Code Breakdown through `ms:iis:auto` feeds, lowering operational risk and delayed incident response.",
  "implementation": "Splunk Add-on for Microsoft IIS (Splunkbase 3185); ensure substatus field enabled in IIS logging. Map 500.19 to config errors, 500.0 to module crashes.",
  "visualization": "Stacked bars for status/substatus, Perfmon timecharts, top client tables.",
  "cimModels": [
    "Web"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Web.Web\n  where Web.status>=500\n  by Web.dest Web.uri_path Web.status span=5m\n| sort -count",
  "dataModelAcceleration": "Web: enable Data Model Acceleration for the CIM `Web` dataset once your HTTP access or proxy access logs are tagged and mapped to the model; otherwise `cimSpl` tstats may return no rows. Most teams start with the raw-log search in Step 2 and add acceleration after CIM field aliases validate.",
  "references": [
    {
      "title": "CIM: Web",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Web",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Microsoft IIS documentation",
      "url": "https://learn.microsoft.com/en-us/troubleshoot/developer/webapps/iis/www-administration-management/http-status-code",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `web` (UC-8.2.40)** — Planned work for 'Microsoft IIS HTTP 500.x Sub-Status Code Breakdown' shifts `count` over `span=1h` while `sc_status`, `sub`, `s_sitename` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `ms:iis:auto`** — Scheduled automation elevates `sub` without crossing `sc_status` >= 500 for UC-8.2.40. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `sc_status`, `sub`, `s_sitename`** — Health checks in `web` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `ms:iis:auto`** — First 48h baseline for UC-8.2.40 on `sc_status` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `sc_status`, `exception_reason`, `valid_until`.",
  "equipment": [
    "iis",
    "windows"
  ],
  "equipmentModels": [
    "iis_generic",
    "windows_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for Microsoft Windows (`Splunk_TA_windows`); Splunk Add-on for Microsoft IIS (Splunkbase 3185)). Enable the modular input or HEC token that writes sourcetype=ms:iis:auto into web. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`s_sitename, sc_status, sub`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that targets the right remediation playbook per failure class and shortens Sev-1 bridges.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
