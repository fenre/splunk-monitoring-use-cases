{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.3.22",
  "title": "Squid TCP_DENIED Policy Denial Spike",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "IT Operations",
  "exclusions": "Covers the detection logic documented for 'Squid TCP_DENIED Policy Denial Spike' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_squid_tcp_denied_policy_denial_spike` (UC-8.3.22), SOC dashboard panel \"Operations — Squid TCP_DENIED Policy Denial Spike\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.3.22 positive (Squid, TCP, DENIED, Policy, Denial): ingest synthetic events into `index=proxy` sourcetype='squid:access' where `count` grouped by `_time` satisfies `denied` > 100 for 'Squid TCP_DENIED Policy Denial Spike'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.3.22 negative (Denial Spike): ingest benign `proxy` / `squid:access` events where `_time` stays below `denied` > 100, or exclude the test entity via `ops_exception_register.csv`. Variant 8 confirms zero alert rows for 'Squid TCP_DENIED Policy Denial Spike'."
  },
  "dataSources": "`index=proxy` `sourcetype=squid:access` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom (Squid access.log) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=proxy sourcetype=\"squid:access\"\n| rex \"TCP_(?<cache_result>DENIED)\"\n| where isnotnull(cache_result)\n| bin _time span=15m\n| stats count as denied by _time\n| where denied > 100",
  "description": "Surfaces UC-8.3.22 — Squid, TCP, DENIED, Policy, Denial — using `index=proxy` sourcetype `squid:access`, grouped by `_time`. Outputs `count` each `15m`; alert when `denied` > 100. Primary focus: Denial Spike. Purpose-built for 'Squid TCP_DENIED Policy Denial Spike' in Operations workflows.",
  "value": "UC-8.3.22 accelerates insight into Denial Spike ('Squid TCP_DENIED Policy Denial Spike') by tracking `activity` on `squid:access` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Ensure usernames or groups appear in logs if available. Join with identity feeds for context.",
  "visualization": "Timechart (denied/min), table (top URLs), map of source subnets.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "CIM: Web",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Web",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Squid Configuration Manual — Access Log",
      "url": "http://www.squid-cache.org/Doc/config/access_log/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `proxy` (UC-8.3.22)** — Planned work for 'Squid TCP_DENIED Policy Denial Spike' shifts `count` over `span=15m` while `_time` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `squid:access`** — Scheduled automation elevates `src` without crossing `denied` > 100 for UC-8.3.22. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `_time`** — Health checks in `proxy` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `squid:access`** — First 48h baseline for UC-8.3.22 on `_time` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `_time`, `exception_reason`, `valid_until`.",
  "equipment": [
    "squid"
  ],
  "equipmentModels": [
    "squid_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Custom (Squid access.log).\n• Ensure the following data sources are available: `index=proxy` `sourcetype=squid:access`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nTune threshold to enterprise size; add URI filters to ignore health checks.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=proxy sourcetype=\"squid:access\"\n| rex \"TCP_(?<cache_result>DENIED)\"\n| where isnotnull(cache_result)\n| bin _time span=15m\n| stats count as denied by _time\n| where denied > 100\n```\n\nUnderstanding this SPL\n\n**Squid TCP_DENIED Policy Denial Spike** — See the description and value fields in this use case JSON.\n\nDocumented **Data sources**: `index=proxy` `sourcetype=squid:access`. **App/TA**: Custom (Squid access.log). Rename `index=` / `sourcetype=` to match your deployment.\n\n**Pipeline walkthrough**\n\n• Scope to the documented index and sourcetype, then apply transforms, thresholds, and `timechart`/`stats` as in the SPL above.\n\n\nStep 3 — Validate\nCompare a sample of `TCP_DENIED` lines with Squid’s `access.log` on the proxy (or a direct `grep` of the same file for that time window) to confirm counts and that parsing matches your format.\n\n\nStep 4 — Operationalize\nAdd to a dashboard or alert; document ownership. Suggested visuals: Timechart (denied/min), table (top URLs), map of source subnets..\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Custom (Squid access.log) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=squid:access into proxy. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, denied`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use this to support DLP and acceptable-use rules on our forward proxy without reading every line of the log by hand.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
