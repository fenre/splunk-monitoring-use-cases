{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.2.38",
  "title": "Microsoft IIS Application Pool Recycling Events",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Change"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Microsoft IIS Application Pool Recycling Events' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_microsoft_iis_application_pool_recycling_events` (UC-8.2.38), SOC dashboard panel \"Operations — Microsoft IIS Application Pool Recycling Events\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.2.38 positive (Microsoft, IIS, Application, Pool, Recycling): ingest synthetic events into `index=windows` sourcetype='WinEventLog:Microsoft-Windows-IIS-Configuration/Operational' where `count` grouped by `EventCode`, `host`, `Message` satisfies detection threshold for 'Microsoft IIS Application Pool Recycling Events'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.2.38 negative (Recycling Events): ingest benign `windows` / `WinEventLog:Microsoft-Windows-IIS-Configuration/Operational` events where `EventCode`, `host`, `Message` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 0 confirms zero alert rows for 'Microsoft IIS Application Pool Recycling Events'."
  },
  "dataSources": "`index=windows` IIS configuration / Application operational logs, `WinEventLog:Application` W3SVC-WP recycle messages",
  "app": "Splunk Add-on for Microsoft Windows (`Splunk_TA_windows`); Splunk Add-on for Microsoft IIS (Splunkbase 3185)",
  "spl": "index=windows sourcetype=\"WinEventLog:Microsoft-Windows-IIS-Configuration/Operational\" OR sourcetype=\"WinEventLog:Application\"\n| where EventCode IN (5074,5075,5117,5118,5119) OR match(_raw, \"(?i)application pool.*recycle\")\n| stats count by EventCode, host, Message\n| sort - count",
  "description": "Detects 'Microsoft IIS Application Pool Recycling Events' (UC-8.2.38) using `index=windows` sourcetype `WinEventLog:Microsoft-Windows-IIS-Configuration/Operational` by aggregating `count` grouped by `EventCode`, `host`, `Message` over `the scheduled window` and alerting when configured threshold. Keywords: Microsoft, IIS, Application, Pool, Recycling. Focuses on Recycling Events behaviour so operations teams can triage UC-8.2.38 without ad-hoc log review.",
  "value": "UC-8.2.38 accelerates insight into Recycling Events ('Microsoft IIS Application Pool Recycling Events') by tracking `activity` on `WinEventLog:Microsoft-Windows-IIS-Configuration/Operational` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Install Splunk Add-on for Microsoft Windows (`Splunk_TA_windows`); enable IIS operational channels. Correlate EventCode meanings with MSDN. Tag planned maintenance.",
  "visualization": "Stacked bars for status/substatus, Perfmon timecharts, top client tables.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "Microsoft IIS documentation",
      "url": "https://learn.microsoft.com/en-us/iis/get-started/introduction-to-iis/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 3185",
      "url": "https://splunkbase.splunk.com/app/3185",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Add-on for Microsoft Windows (Splunkbase 742)",
      "url": "https://splunkbase.splunk.com/app/742",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `windows` (UC-8.2.38)** — Planned work for 'Microsoft IIS Application Pool Recycling Events' shifts `count` over `span=1h` while `EventCode`, `host`, `Message` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `WinEventLog:Microsoft-Windows-IIS-Configuration/Operational`** — Scheduled automation elevates `host` without crossing configured threshold for UC-8.2.38. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `EventCode`, `host`, `Message`** — Health checks in `windows` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `WinEventLog:Microsoft-Windows-IIS-Configuration/Operational`** — First 48h baseline for UC-8.2.38 on `EventCode` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `EventCode`, `exception_reason`, `valid_until`.",
  "equipment": [
    "iis",
    "windows"
  ],
  "equipmentModels": [
    "iis_generic",
    "windows_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for Microsoft Windows (`Splunk_TA_windows`); Splunk Add-on for Microsoft IIS (Splunkbase 3185)). Enable the modular input or HEC token that writes sourcetype=WinEventLog:Microsoft-Windows-IIS-Configuration, WinEventLog:Application into windows. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`EventCode, Message, host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that cuts mean time to innocence for application teams during IIS instability windows.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
