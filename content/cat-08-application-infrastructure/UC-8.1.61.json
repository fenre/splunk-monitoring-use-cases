{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.1.61",
  "title": "ZooKeeper Quorum Role and ruok Health",
  "criticality": "critical",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`echo ruok | nc` → `imok` responses as `zookeeper:ruok`; `zk_server_state` in `zookeeper:mntr`",
  "app": "Scripted health checks from monitoring host; whitelist `ruok` in `zoo.cfg`",
  "spl": "index=infrastructure sourcetype=\"zookeeper:ruok\"\n| eval healthy=if(match(_raw,\"imok\") OR response=\"imok\",1,0)\n| bin _time span=1m\n| stats min(healthy) as ok by host, _time\n| where ok=0",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "description": "When `ruok` stops returning `imok`, the JVM may be wedged even though the process exists—dependent systems should fail over or stop sending traffic before sessions die.",
  "value": "Shortens detection path versus waiting for client session expirations across the estate.",
  "grandmaExplanation": "We use this to shortens detection path versus waiting for client session expirations across the estate.",
  "implementation": "Run checks from two vantage points to avoid false positives. Pair with `zookeeper:mntr` `zk_server_state` for leader awareness. Do not expose four-letter ports publicly.",
  "visualization": "Single value (ensemble health), Timeline (failures).",
  "references": [
    {
      "title": "ZooKeeper — ruok",
      "url": "https://zookeeper.apache.org/doc/current/zookeeperAdmin.html#sc_zkCommands"
    }
  ],
  "knownFalsePositives": "Spikes or gaps can follow approved maintenance, load tests, or short collection outages. We add a second signal and a change window before escalating.",
  "equipment": [
    "zookeeper"
  ],
  "status": "draft",
  "lastReviewed": "2025-06-01",
  "splunkVersions": [
    "9.x",
    "8.x"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": ""
}
