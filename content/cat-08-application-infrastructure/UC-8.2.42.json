{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.2.42",
  "title": "Microsoft IIS HTTP.sys Request Queue Length per Application Pool",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance",
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Microsoft IIS HTTP.sys Request Queue Length per Application Pool' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_microsoft_iis_http_sys_request_queue_length_per_` (UC-8.2.42), SOC dashboard panel \"Operations — Microsoft IIS HTTP.sys Request Queue Length per Application Pool\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.2.42 positive (Microsoft, IIS, HTTP.sys, Request, Queue): ingest synthetic events into `index=windows` sourcetype='Perfmon:IIS' where `max` grouped by `instance` satisfies `qsize` > 100 for 'Microsoft IIS HTTP.sys Request Queue Length per Application Pool'. Validate filter `object=\"HTTP Service Request Queue\" OR counter=\"CurrentQueue`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.2.42 negative (Application Pool): ingest benign `windows` / `Perfmon:IIS` events where `instance` stays below `qsize` > 100, or exclude the test entity via `ops_exception_register.csv`. Variant 9 confirms zero alert rows for 'Microsoft IIS HTTP.sys Request Queue Length per Application Pool'."
  },
  "dataSources": "`index=windows` IIS HTTP.sys Perfmon counters (`HTTP Service Request Queue\\CurrentQueueSize`) via `Splunk_TA_windows` Perfmon or `Nmon`",
  "app": "Splunk Add-on for Microsoft Windows (`Splunk_TA_windows`); Splunk Add-on for Microsoft IIS (Splunkbase 3185)",
  "spl": "index=windows sourcetype=\"Perfmon:IIS\" OR sourcetype=\"perfmon\"\n| search object=\"HTTP Service Request Queue\" OR counter=\"CurrentQueueSize\"\n| eval qsize=tonumber(Value)\n| where qsize > 100\n| timechart span=1m max(qsize) by instance",
  "description": "Identifies UC-8.2.42 — Microsoft, IIS, HTTP.sys, Request, Queue — using `index=windows` sourcetype `Perfmon:IIS`, grouped by `instance`. Outputs `max` each `1m`; alert when `qsize` > 100. Filter chain includes filter `object=\"HTTP Service Request Queue\" OR counter=\"CurrentQueue`. Purpose-built for 'Microsoft IIS HTTP.sys Request Queue Length per Application Pool' in Operations workflows.",
  "value": "Operators running UC-8.2.42 on `Perfmon:IIS` feeds shorten triage for Application Pool tied to 'Microsoft IIS HTTP.sys Request Queue Length per Application Pool', lowering operational risk and delayed incident response.",
  "implementation": "Collect Perfmon every 60s; map `instance` to site/app pool. Alert when queue sustained above baseline.",
  "visualization": "Stacked bars for status/substatus, Perfmon timecharts, top client tables.",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable: this use case is not built on a CIM data model. Rely on the search in Step 2 against your raw logs or metrics.",
  "references": [
    {
      "title": "Microsoft IIS documentation",
      "url": "https://learn.microsoft.com/en-us/windows/win32/http/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 3185",
      "url": "https://splunkbase.splunk.com/app/3185",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `windows` (UC-8.2.42)** — Planned work for 'Microsoft IIS HTTP.sys Request Queue Length per Application Pool' shifts `max` over `span=1m` while `instance` remains in-scope. Applies filter `object=\"HTTP Service Request Queue\" OR counter=\"CurrentQueue`. Cross-check CMDB CHG.\n\n2. **Batch `max` on `Perfmon:IIS`** — Scheduled automation elevates `qsize` without crossing `qsize` > 100 for UC-8.2.42. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `instance`** — Health checks in `windows` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `Perfmon:IIS`** — First 48h baseline for UC-8.2.42 on `instance` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `instance`, `exception_reason`, `valid_until`.",
  "equipment": [
    "iis",
    "windows"
  ],
  "equipmentModels": [
    "iis_generic",
    "windows_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Add-on for Microsoft Windows (`Splunk_TA_windows`); Splunk Add-on for Microsoft IIS (Splunkbase 3185)). Enable the modular input or HEC token that writes sourcetype=Perfmon:IIS, perfmon into windows. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`instance, qsize`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that explains latency that application profilers miss and speeds isolation between network, kernel, and code paths.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
