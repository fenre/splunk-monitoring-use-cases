{
  "$schema": "../../schemas/uc.schema.json",
  "id": "8.4.19",
  "title": "Traefik Per-Service Request Rate for Capacity Planning",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Traefik Per-Service Request Rate for Capacity Planning' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_traefik_per_service_request_rate_for_capacity_pl` (UC-8.4.19), SOC dashboard panel \"Operations — Traefik Per-Service Request Rate for Capacity Planning\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-8.4.19 positive (Traefik, Per, Service, Request, Rate): ingest synthetic events into `index=web` sourcetype='traefik:access' where `baseline` grouped by `ServiceName`, `_time` satisfies `rph` > 1000 for 'Traefik Per-Service Request Rate for Capacity Planning'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-8.4.19 negative (Capacity Planning): ingest benign `web` / `traefik:access` events where `ServiceName`, `_time` stays below `rph` > 1000, or exclude the test entity via `ops_exception_register.csv`. Variant 10 confirms zero alert rows for 'Traefik Per-Service Request Rate for Capacity Planning'."
  },
  "dataSources": "`index=web` `sourcetype=traefik:access` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Add On for Traefik Proxy (Splunkbase app 6733) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=web sourcetype=\"traefik:access\"\n| bin _time span=1h\n| stats count as rph by ServiceName, _time\n| eventstats avg(rph) as baseline by ServiceName\n| where rph > baseline * 1.5 AND rph > 1000",
  "description": "Monitors UC-8.4.19 — Traefik, Per, Service, Request, Rate — using `index=web` sourcetype `traefik:access`, grouped by `ServiceName`, `_time`. Outputs `baseline` each `1h`; alert when `rph` > 1000. Primary focus: Capacity Planning. Purpose-built for 'Traefik Per-Service Request Rate for Capacity Planning' in Operations workflows.",
  "value": "For 'Traefik Per-Service Request Rate for Capacity Planning', UC-8.4.19 converts `baseline` on `traefik:access` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Ensure `ServiceName` is populated; fall back to `RouterName` if null.",
  "visualization": "Line chart (RPH), baseline overlay, table (growth %).",
  "cimModels": [
    "Web"
  ],
  "cimSpl": "| tstats `summariesonly` count as events\n  from datamodel=Web.Web\n  by Web.http_method Web.dest span=1d\n| sort -events",
  "dataModelAcceleration": "Web: enable Data Model Acceleration for the CIM `Web` dataset once your HTTP access or proxy access logs are tagged and mapped to the model; otherwise `cimSpl` tstats may return no rows. Most teams start with the raw-log search in Step 2 and add acceleration after CIM field aliases validate.",
  "references": [
    {
      "title": "CIM: Web",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Web",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Add On for Traefik Proxy (Splunkbase)",
      "url": "https://splunkbase.splunk.com/app/6733",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `web` (UC-8.4.19)** — Planned work for 'Traefik Per-Service Request Rate for Capacity Planning' shifts `baseline` over `span=1h` while `ServiceName`, `_time` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `traefik:access`** — Scheduled automation elevates `_time` without crossing `rph` > 1000 for UC-8.4.19. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `ServiceName`, `_time`** — Health checks in `web` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `traefik:access`** — First 48h baseline for UC-8.4.19 on `ServiceName` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `ServiceName`, `exception_reason`, `valid_until`.",
  "equipment": [
    "traefik"
  ],
  "equipmentModels": [
    "traefik_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Add On for Traefik Proxy (Splunkbase app 6733).\n• Ensure the following data sources are available: `index=web` `sourcetype=traefik:access`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nTune multipliers for seasonal businesses.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=web sourcetype=\"traefik:access\"\n| bin _time span=1h\n| stats count as rph by ServiceName, _time\n| eventstats avg(rph) as baseline by ServiceName\n| where rph > baseline * 1.5 AND rph > 1000\n```\n\nUnderstanding this SPL\n\n**Traefik Per-Service Request Rate for Capacity Planning** — See the description and value fields in this use case JSON.\n\nDocumented **Data sources**: `index=web` `sourcetype=traefik:access`. **App/TA**: Add On for Traefik Proxy (Splunkbase app 6733). Rename `index=` / `sourcetype=` to match your deployment.\n\n**Pipeline walkthrough**\n\n• Scope to the documented index and sourcetype, then apply transforms, thresholds, and `timechart`/`stats` as in the SPL above.\n\n\nStep 3 — Validate\nCompare with the API gateway or mesh admin (Kong, Apigee, AWS API Gateway, etc.) and a raw log tail for the same time range.\n\n\nStep 4 — Operationalize\nAdd to a dashboard or alert; document ownership. Suggested visuals: Line chart (RPH), baseline overlay, table (growth %)..\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Add On for Traefik Proxy (Splunkbase app 6733) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=traefik:access into web. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`ServiceName, _time, rph`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We use this to support FinOps and ingress capacity reviews.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
