{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.71",
  "title": "/etc/shadow Modifications",
  "criticality": "critical",
  "difficulty": "beginner",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "dataSources": "`sourcetype=linux_audit` (path watch on `/etc/shadow`)",
  "app": "`Splunk_TA_nix`",
  "spl": "index=os sourcetype=linux_audit path=\"/etc/shadow\" (action=modified OR nametype=normal)\n| stats count by host, auid, exe\n| where count>0",
  "description": "Detects any audit trail row that points at a write to **/etc/shadow**, the local password-hash store, with actor fields for accountability.",
  "value": "Legitimate **shadow** changes are rare and usually tied to `passwd`, `chpasswd`, or **IAM** automation; anything else deserves an immediate, high-severity look.",
  "implementation": "Add **auditd** `-w /etc/shadow -p wa -k shadow` (key name to taste). Tighten to `| where auid>0` if you only care about non-root original logins, but keep break-glass root stories in a parallel search for regulated shops.",
  "visualization": "Alert, Table",
  "cimModels": [
    "Change"
  ],
  "cimSpl": "| tstats `summariesonly` count from datamodel=Change.All_Changes where All_Changes.action=modified AND match(All_Changes.object, \".*shadow.*\") by All_Changes.user All_Changes.dest span=1h | where count>0",
  "dataModelAcceleration": "Enable acceleration on the **Change** data model for mapped shadow edits; use the same 30–90 day lookback you allow for FIM forensics.",
  "knownFalsePositives": "Monthly forced password-rotation playbooks that are still manual; `auid=4294967295` on some kernels when login uid cannot be resolved—tune **props**; backup tools that re-copy **shadow** during DR tests—**suppress** by host group.",
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833"
    },
    {
      "title": "CIM: Change",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Change"
    }
  ],
  "equipment": [
    "linux"
  ],
  "detailedImplementation": "Prerequisites\n• Same FIM+audit story as the **passwd** use case; do **not** put raw hash contents in Splunk—only the audit metadata.\n\n**CIM** — `object` in **All_Changes** should be the file path, not a hash field.\n\n\nStep 3 — Validate\n`ausearch -f /etc/shadow` and compare to Search; for binary proof, add **KEY** to include syscall class when needed.\n\nStep 4 — Operationalize\nPage the **CISO** queue per your playbooks when **exe** is a shell in `/tmp`.\n\n",
  "grandmaExplanation": "We help you know when the secret password list on the machine is written to, which should almost never happen without a very clear, approved reason."
}
