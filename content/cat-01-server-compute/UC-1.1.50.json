{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.50",
  "title": "Transparent Hugepage Defragmentation Stalls",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Transparent Hugepage Defragmentation Stalls' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_transparent_hugepage_defragmentation_stalls` (UC-1.1.50), SOC dashboard panel \"Linux — Transparent Hugepage Defragmentation Stalls\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=os` sourcetype='syslog' that satisfy the UC-1.1.50 SPL filters for 'Transparent Hugepage Defragmentation Stalls'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `os` / `syslog` events outside the detection threshold, or exclude the test entity via `linux_capacity_exceptions.csv` with a valid window. Confirm zero alert rows for UC-1.1.50."
  },
  "dataSources": "`sourcetype=syslog` (khugepaged / THP messages) and optional debug or metrics feeds if you enable them Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix` — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=os sourcetype=syslog (\"thp_defrags\" OR \"khugepaged\" OR \"thp_collapse\")\n| stats count by host\n| where count > 5",
  "description": "Counts kernel log lines tied to transparent huge page defragmentation, which often shows up as latency blips for latency-sensitive services on the same host.",
  "value": "Knowing when a host is spending extra time in THP maintenance helps you decide whether to switch profiles (for example toward `madvise`) or to move jitter-sensitive apps.",
  "implementation": "Ingest the kernel or daemon logs that mention THP and khugepaged. Alert when more than a small number of lines appear in the window, then confirm on the host with THP settings and application guidance.",
  "visualization": "Alert, Table",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday / unattended-upgrades** — CPU and disk I/O spike during package application for 'Transparent Hugepage Defragmentation Stalls'. Correlate with apt/yum logs in same window.\n\n2. **Nightly backup or log-rotate job** — Predictable I/O burst on fixed cron schedule.\n\n3. **Synthetic monitoring probe** — Nagios/Zabbix agent generates periodic load.\n\n4. **New host onboarding** — Baseline metrics unstable first 48h on `os` `syslog`.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux",
    "syslog"
  ],
  "equipmentModels": [
    "linux_generic",
    "syslog_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Forward security-relevant **syslog** from Linux hosts, including **kern**-priority lines where THP messages appear on your distribution.\n\nStep 1 — Configure data collection\nNo extra TA feature is required beyond reliable syslog forwarding. If messages never arrive, check rsyslog/journald forwarding rules and that Splunk is not dropping **kern** facilities.\n\nStep 2 — Create the search and alert\n\n```spl\nindex=os sourcetype=syslog (\"thp_defrags\" OR \"khugepaged\" OR \"thp_collapse\")\n| stats count by host\n| where count > 5\n```\n\nTune `>5` to your log volume. Consider adding `host=prod-*` style filters for scoped rollouts.\n\n**Understanding this SPL** — Simple frequency count of THP-related substrings by host, alerting when the pattern appears more than a few times in the chosen lookback.\n\n\nStep 3 — Validate\nOn a lab host, read current policy with your distribution’s ** sysfs** or ** sysctl** view of transparent hugepage settings, reproduce a low-rate pattern if safe, and confirm matching lines in Splunk. Use `top` or `htop` while testing to see user-visible latency, not to parse THP (THP is kernel-side).\n\nStep 4 — Operationalize\nDocument policy choices (`always` vs `madvise` vs `never`) in the runbook and pair each alert with owners of databases or real-time apps on the node.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_nix` — Splunk Add-on for Unix and Linux (Splunkbase 833)). Enable the modular input or HEC token that writes sourcetype=syslog into os. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We help you know when the operating system is busy reshuffling its large memory pages, which can make some programs stutter, so you can change settings or move fussy work elsewhere.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
