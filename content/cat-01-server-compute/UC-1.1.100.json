{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.100",
  "title": "Vmstat swap-in (si) rate",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Vmstat swap-in (si) rate' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_vmstat_swap_in_si_rate` (UC-1.1.100), SOC dashboard panel \"Linux — Vmstat swap-in (si) rate\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=os` sourcetype='vmstat' that satisfy the UC-1.1.100 SPL filters for 'Vmstat swap-in (si) rate'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `os` / `vmstat` events outside the detection threshold, or exclude the test entity via `linux_capacity_exceptions.csv` with a valid window. Confirm zero alert rows for UC-1.1.100."
  },
  "dataSources": "`sourcetype=vmstat` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=os sourcetype=vmstat host=*\n| stats avg(si) as avg_swap_in by host\n| where avg_swap_in > 1000",
  "description": "In standard vmstat(8) output, `si` is kilobytes swapped in from disk per interval (memory pressure), not softirq. A sustained high `si` rate usually means the system is actively paging or thrashing swap.",
  "value": "Spotting heavy swap-in activity early helps us add memory, tune workloads, or fix leaks before latency and timeouts spread to users and services.",
  "implementation": "Enable vmstat collection in Splunk_TA_nix. Track the `si` field (swap-in) per host. Alert when the average swap-in rate exceeds your threshold; correlate with `so` (swap-out) and `vmstat` free/buff/cache fields.",
  "visualization": "Timechart, Alert",
  "cimModels": [
    "Performance"
  ],
  "cimSpl": "| tstats `summariesonly` avg(Performance.mem_used_percent) as mem_pct avg(Performance.swap_used_percent) as swap_pct\n  from datamodel=Performance where nodename=Performance.Memory\n  by Performance.host span=5m\n| where mem_pct > 90 OR swap_pct > 15",
  "dataModelAcceleration": "Performance.Memory",
  "references": [
    {
      "title": "Splunk_TA_nix",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Performance",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Performance",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday / unattended-upgrades** — CPU and disk I/O spike during package application for 'Vmstat swap-in (si) rate'. Correlate with apt/yum logs in same window.\n\n2. **Nightly backup or log-rotate job** — Predictable I/O burst on fixed cron schedule.\n\n3. **Synthetic monitoring probe** — Nagios/Zabbix agent generates periodic load.\n\n4. **New host onboarding** — Baseline metrics unstable first 48h on `os` `vmstat`.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux"
  ],
  "equipmentModels": [
    "linux_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_nix`.\n• Ensure the following data sources are available: `sourcetype=vmstat`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnable the `vmstat` scripted input on Linux. Confirm field names in Search: `si` is swap-in pages/kB per sample (per `vmstat` man page for your distro). If you need softirq rates, ingest `/proc/softirqs` or `mpstat` with a separate scripted input—do not map `si` to softirq.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=os sourcetype=vmstat host=*\n| stats avg(si) as avg_swap_in by host\n| where avg_swap_in > 1000\n```\n\nUnderstanding this SPL\n\n**Vmstat swap-in (si) rate** — In standard vmstat(8) output, `si` is kilobytes swapped in from disk per interval (memory pressure), not softirq.\n\nDocumented **Data sources**: `sourcetype=vmstat`. **App/TA** (typical add-on context): `Splunk_TA_nix`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: os; **sourcetype**: vmstat. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=os, sourcetype=vmstat. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by host** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where avg_swap_in > 1000` — tune the threshold to your environment and vmstat interval.\n\nOptional CIM / accelerated variant (related memory-pressure view via Common Information Model):\n\n```spl\n| tstats `summariesonly` avg(Performance.mem_used_percent) as mem_pct avg(Performance.swap_used_percent) as swap_pct\n  from datamodel=Performance where nodename=Performance.Memory\n  by Performance.host span=5m\n| where mem_pct > 90 OR swap_pct > 15\n```\n\nUnderstanding this CIM / accelerated SPL\n\nThis **CIM** block approximates memory pressure using **Performance.Memory** (`mem_used_percent`, `swap_used_percent`). It does not reproduce `vmstat` `si` exactly—use it when the data model is accelerated and normalized to the same hosts.\n\nEnable **acceleration** on **Performance.Memory** (and correct CIM knowledge objects) or the search may return nothing from summaries.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Timechart, Alert",
  "grandmaExplanation": "We watch Linux memory swap-in from vmstat so we can tell when the server is shuffling data to disk because it is out of RAM—before everything feels slow.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
