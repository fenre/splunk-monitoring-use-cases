{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.3.4",
  "title": "Software Update Compliance",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Security",
  "compliance": [
    {
      "regulation": "ASD E8",
      "version": "Nov 2023",
      "clause": "E8.06",
      "clauseUrl": "https://www.cyber.gov.au/resources-business-and-government/essential-cyber-security/essential-eight/essential-eight-maturity-model",
      "mode": "satisfies",
      "assurance": "partial",
      "assurance_rationale": "ASD Essential Eight E8.06 Patch operating systems — fleet-wide software update compliance is precisely what E8 maturity levels measure.",
      "controlObjective": "Evidence that ASD E8 E8.06 (Patch operating systems) is enforced — Splunk UC-1.3.4: Software Update Compliance.",
      "evidenceArtifact": "Saved search 'UC-1.3.4' running on Custom scripted input (softwareupdate -l, sw_vers), archived to the restricted audit_evidence index (default 7-year retention). Auto-drafted — SME review required.",
      "obligationRef": "asd-e8@Nov 2023#E8.06",
      "requires_sme_review": true
    },
    {
      "regulation": "APRA CPS 234",
      "version": "current",
      "clause": "15",
      "clauseUrl": "https://www.apra.gov.au/information-security",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "APRA CPS 234 15 Policy framework — update compliance evidences policy-enforcement posture under the APRA framework.",
      "controlObjective": "Evidence that APRA CPS 234 15 (Policy framework) is enforced — Splunk UC-1.3.4: Software Update Compliance.",
      "evidenceArtifact": "Saved search 'UC-1.3.4' running on Custom scripted input (softwareupdate -l, sw_vers), archived to the restricted audit_evidence index (default 7-year retention). Auto-drafted — SME review required.",
      "obligationRef": "apra-cps-234@current#15",
      "requires_sme_review": true
    }
  ],
  "dataSources": "Custom scripted input (`softwareupdate -l`, `sw_vers`)",
  "app": "Splunk Universal Forwarder, custom scripted input",
  "spl": "index=os sourcetype=macos_sw_vers host=*\n| stats latest(ProductVersion) as os_version by host\n| eval is_current = if(os_version >= \"14.3\", \"Yes\", \"No\")\n| stats count by is_current",
  "cimSpl": "N/A — macOS `ProductVersion` from inventory is not a CIM Performance field; use MDM, custom `sw_vers` data, or your own policy lookup for approved builds.",
  "description": "Unpatched macOS endpoints are vulnerable. Tracking update levels across the fleet supports vulnerability management.",
  "value": "A single view of which Macs are on an approved (or too-old) build makes patch and vulnerability work measurable instead of guessing from occasional spot checks.",
  "implementation": "Scripted input for `sw_vers` (weekly) and `softwareupdate -l` (daily). Track OS versions and pending updates. Alert when critical security updates are pending >7 days.",
  "visualization": "Table (host, OS version, pending updates), Pie chart (version distribution).",
  "cimModels": [
    "N/A"
  ],
  "dataModelAcceleration": "Not applicable — no CIM mapping for this OS-version inventory use case.",
  "knownFalsePositives": "The example cut-off (`14.3`) is illustrative; replace with your org’s minimum supported build. Deferral profiles, betas, and long-lived developer seeds can look ‘non-current’ when they are still allowed. String comparison of version numbers can mis-rank; prefer a lookup table of approved builds for production.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/"
    }
  ],
  "equipment": [
    "macos"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Universal Forwarder, custom scripted input.\n• Ensure the following data sources are available: Custom scripted input (`softwareupdate -l`, `sw_vers`).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nScripted input for `sw_vers` (weekly) and `softwareupdate -l` (daily). Track OS versions and pending updates. Alert when critical security updates are pending longer than your policy allows.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=os sourcetype=macos_sw_vers host=*\n| stats latest(ProductVersion) as os_version by host\n| eval is_current = if(os_version >= \"14.3\", \"Yes\", \"No\")\n| stats count by is_current\n```\n\nReplace the `14.3` string with a lookup or a field from your own policy. Use `| inputlookup` for approved versions per model if the fleet is mixed.\n\nUnderstanding this SPL\n\n**Software Update Compliance** — Unpatched macOS endpoints are vulnerable. Tracking update levels across the fleet supports vulnerability management.\n\n**Pipeline walkthrough**\n\n• Scopes the data: `index=os`, `sourcetype=macos_sw_vers`.\n• `stats` takes the latest `os_version` per **host**.\n• `eval` labels current vs. not against your example threshold.\n• Final `stats` gives fleet counts for reporting (not a per-host alert as written—split into a per-host form if alerting).\n\n\nStep 3 — Validate\nOn a test Mac, compare `sw_vers -productVersion` to the indexed `ProductVersion`. Verify index permissions and time zones for daily rollups.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions as required. Document your minimum supported version. Consider visualizations: Table (host, OS version, pending updates), Pie chart (version distribution). See the Implementation guide: docs/implementation-guide.md",
  "grandmaExplanation": "We track which version of the system each Mac is running so you can see which ones still need a security update before a known problem hits your whole team."
}
