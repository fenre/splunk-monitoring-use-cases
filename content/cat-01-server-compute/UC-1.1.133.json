{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.133",
  "title": "macOS CPU Thermal Pressure and Throttle Events",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance",
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'macOS CPU Thermal Pressure and Throttle Events' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_macos_cpu_thermal_pressure_and_throttle_events` (UC-1.1.133), SOC dashboard panel \"Linux — macOS CPU Thermal Pressure and Throttle Events\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=os` sourcetype='macos_thermal' where `host` satisfies `speed_limit` < 70 for UC-1.1.133 ('macOS CPU Thermal Pressure and Throttle Events'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `os` / `macos_thermal` events where `host` stays below `speed_limit` < 70, or exclude the test entity via `linux_capacity_exceptions.csv` with a valid window. Confirm zero alert rows for UC-1.1.133."
  },
  "dataSources": "`index=os` `sourcetype=macos_thermal` from `powermetrics --show-plimits` sampling or unified log export filtering `thermal` / `CPU_Speed_Limit` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder for macOS (scripted input), optional Splunk Add-on for Unix and Linux (`Splunk_TA_nix`, Splunkbase #833) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=os sourcetype=macos_thermal host=*\n| stats latest(thermal_pressure) as pressure, latest(cpu_speed_limit_pct) as speed_limit by host\n| where (isnull(pressure) OR lower(pressure)!=\"nominal\") OR speed_limit < 70",
  "description": "When macOS reports non-nominal thermal pressure or forces a low CPU speed limit, interactive and build workloads slow sharply even if average CPU utilization looks healthy. Ingesting thermal state explains user-perceived slowness tied to fan failures, dust, ambient heat, or sustained AV/backup jobs.",
  "value": "When macOS reports non-nominal thermal pressure or forces a low CPU speed limit, interactive and build work.... Timely UC-1.1.133 alerting on 'macOS CPU Thermal Pressure and Throttle Events' reduces capacity incidents, patch regressions, and service outages before manual triage or audit channels escalate.",
  "implementation": "Run a controlled `powermetrics` sample (requires admin) on an interval that matches your compliance posture—often 5–15 minutes—or parse `log show --predicate 'subsystem == \"com.apple.powermanagement\"'` / thermal-related messages into `thermal_pressure` (nominal|moderate|heavy|trapping) and `cpu_speed_limit_pct` (0–100). For fleet scripts without root, map best-effort proxies from `pmset -g therm`. Use `index=os`, `sourcetype=macos_thermal`. Alert on sustained non-nominal pressure or `cpu_speed_limit_pct` under 70% for more than two consecutive samples.",
  "visualization": "Timeline (thermal_pressure by host), Line chart (cpu_speed_limit_pct), Table (worst hosts last hour).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Apple Platform Security — macOS power and thermal management (overview)",
      "url": "https://support.apple.com/guide/security/welcome/web",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Add-on for Unix and Linux (Splunkbase 833)",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'macOS CPU Thermal Pressure and Throttle Events'. Correlate with apt/yum logs in the same `macos_thermal` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `count` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `macos_thermal`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing `speed_limit` < 70.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux",
    "macos"
  ],
  "equipmentModels": [
    "linux_generic",
    "macos_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• Decide whether elevated `powermetrics` sampling is acceptable; if not, use log-based thermal strings only.\n• Standardize enums: `thermal_pressure` lowercase nominal|moderate|heavy|trapping|unknown.\n\nStep 1 — Scripted input prints one event per interval with `cpu_speed_limit_pct` as integer percent.\n\nStep 2 — Correlate with `sourcetype=macos_top` or `cpu` from TA nix if co-deployed.\n\nStep 3 — Validate on a known thermally loaded test unit (video encode + CPU burn).\n\nStep 4 — Tune alert to ignore single-sample spikes during macOS updates.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Universal Forwarder for macOS (scripted input), optional Splunk Add-on for Unix and Linux (`Splunk_TA_nix`, Splunkbase #833) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=macos_thermal into os. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, speed_limit`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We notice when a Mac is getting too hot and has to slow itself down, so IT can fix cooling problems instead of blaming the wrong app.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
