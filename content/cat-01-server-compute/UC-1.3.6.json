{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.3.6",
  "title": "macOS Gatekeeper and XProtect Status",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'macOS Gatekeeper and XProtect Status'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-1.3.6).",
  "evidence": "Saved search uc_1_3_6_macos_gatekeeper_and_xprotect_status, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'macOS Gatekeeper and XProtect Status'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-1.3.6.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "`spctl --status`, `system_profiler SPInstallHistoryDataType` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder, custom scripted input (parsing the same `spctl` and `system_profiler` output you might template from add-on for Unix and Linux in a mixed environment) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=os sourcetype=macos_gatekeeper host=*\n| eval xprotect_age_days = now() - strptime(xprotect_date, \"%Y-%m-%d\")\n| where xprotect_age_days > 30\n| table host xprotect_ver xprotect_date xprotect_age_days",
  "description": "Verify Gatekeeper and XProtect are enabled and definitions are current. Disabled or outdated security controls increase malware risk.",
  "value": "Old malware definitions and turned-off download checks leave users exposed to the same class of file-based threats your policy assumes are blocked; this use case makes that gap visible in one place.",
  "implementation": "Create a scripted input that runs `spctl --status` (expect \"assessments enabled\" for Gatekeeper on). For XProtect, run `system_profiler SPInstallHistoryDataType` and parse XProtect/XProtect Remediator entries, or check `/Library/Apple/System/Library/CoreServices/XProtect.bundle/Contents/version.plist`. Run daily. Alert when Gatekeeper is disabled; alert when XProtect definitions are older than 30 days.",
  "visualization": "Table (host, Gatekeeper status, XProtect version), Single value (non-compliant count), Pie chart (enabled vs. disabled).",
  "cimModels": [],
  "dataModelAcceleration": "Not applicable — no CIM mapping for XProtect or Gatekeeper inventory.",
  "references": [
    {
      "title": "Splunk_TA_nix",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "macos"
  ],
  "equipmentModels": [
    "macos_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install the Universal Forwarder on the Mac; place scripts in an app or `deployment-apps` as you do for other custom inputs.\n• Data sources: `spctl --status`, and XProtect version/date from `system_profiler` or the XProtect `version.plist` (your parser must set `xprotect_date` in the format the search expects).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nCreate a scripted input that runs `spctl --status` and collects XProtect metadata. The sample search expects fields `xprotect_date`, and optionally `xprotect_ver` for the table. Run daily. Do not rely on Windows or Linux performance counters (no Perfmon, no `/proc` load) — this is macOS-only.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust the 30-day window as needed):\n\n```spl\nindex=os sourcetype=macos_gatekeeper host=*\n| eval xprotect_age_days = now() - strptime(xprotect_date, \"%Y-%m-%d\")\n| where xprotect_age_days > 30\n| table host xprotect_ver xprotect_date xprotect_age_days\n```\n\nAdd a second alert for Gatekeeper if you parse `spctl` into the same or a companion sourcetype.\n\nUnderstanding this SPL\n\n**macOS Gatekeeper and XProtect Status** — Verify Gatekeeper and XProtect are enabled and definitions are current. Disabled or outdated security controls increase malware risk.\n\n**Pipeline walkthrough**\n\n• Scopes the data: `index=os`, `sourcetype=macos_gatekeeper`.\n• `eval` turns `xprotect_date` into `xprotect_age_days`.\n• `where` flags definitions older than 30 days; `table` shows detail for remediation.\n\n\nStep 3 — Validate\nCompare `xprotect_date` in Search to `system_profiler` and Apple’s current release notes for your build. For full details, see the Implementation guide: docs/implementation-guide.md\n\nStep 4 — Operationalize\nDocument how often Apple ships definition updates in your environment and tune the 30-day threshold. Consider visualizations: Table (host, status, XProtect version), count of non-compliant hosts.",
  "grandmaExplanation": "We help you see when a Mac’s built-in file checks and malware list are out of date, so the machine is not left running with last month’s protection.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
