{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.30",
  "title": "Scheduler Latency and Run Queue Depth",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Scheduler Latency and Run Queue Depth' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_scheduler_latency_and_run_queue_depth` (UC-1.1.30), SOC dashboard panel \"Linux — Scheduler Latency and Run Queue Depth\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-1.1.30 positive (Scheduler, Latency, and, Run, Queue): ingest synthetic events into `index=os` sourcetype='vmstat' where `avg_ratio` grouped by `host` satisfies `avg_ratio` > 2 for 'Scheduler Latency and Run Queue Depth'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-1.1.30 negative (Queue Depth): ingest benign `os` / `vmstat` events where `host` stays below `avg_ratio` > 2, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 4 confirms zero alert rows for 'Scheduler Latency and Run Queue Depth'."
  },
  "dataSources": "`sourcetype=vmstat, top, custom:sched_latency` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix, custom scripted input` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=os sourcetype=vmstat host=*\n| eval runq_to_cpu=r/procs_cpu\n| stats avg(runq_to_cpu) as avg_ratio by host\n| where avg_ratio > 2",
  "description": "Alerts on risk tied to Scheduler, Latency, and, Run, Queue through UC-1.1.30 on `index=os` sourcetype `vmstat`. Summarises `avg_ratio` grouped by `host` across `the scheduled window` before applying `avg_ratio` > 2. Operators tune 'Scheduler Latency and Run Queue Depth' using UC-1.1.30 runbook steps.",
  "value": "Tracking `avg_ratio` via UC-1.1.30 on `vmstat` feeds improves response to 'Scheduler Latency and Run Queue Depth' and reduces capacity incidents, patch regressions, and service outages.",
  "implementation": "Monitor run queue (r) field from vmstat and correlate with process count. When run queue exceeds 2x CPU count, alert on scheduler saturation. Create SPL to identify top CPU-consuming processes during high latency periods.",
  "visualization": "Timechart, Multi-series Line Chart",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "CIM: Performance",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Performance",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'Scheduler Latency and Run Queue Depth'. Correlate with apt/yum logs in the same `vmstat` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `avg` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `vmstat`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing `avg_ratio` > 2.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux"
  ],
  "equipmentModels": [
    "linux_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_nix, custom scripted input`.\n• Ensure the following data sources are available: `sourcetype=vmstat, top, custom:sched_latency`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMonitor run queue (r) field from vmstat and correlate with process count. When run queue exceeds 2x CPU count, alert on scheduler saturation. Create SPL to identify top CPU-consuming processes during high latency periods.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=os sourcetype=vmstat host=*\n| eval runq_to_cpu=r/procs_cpu\n| stats avg(runq_to_cpu) as avg_ratio by host\n| where avg_ratio > 2\n```\n\nUnderstanding this SPL\n\n**Scheduler Latency and Run Queue Depth** — High run queue depth with elevated scheduling latency causes visible application performance degradation.\n\nDocumented **Data sources**: `sourcetype=vmstat, top, custom:sched_latency`. **App/TA** (typical add-on context): `Splunk_TA_nix, custom scripted input`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: os; **sourcetype**: vmstat. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=os, sourcetype=vmstat. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **runq_to_cpu** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by host** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where avg_ratio > 2` — typically the threshold or rule expression for this monitoring goal.\n\nStep 3 — Validate\nOn the host, compare with `top`, `htop`, `vmstat`, `iostat`, or `sar` as appropriate to this use case. For log-only detections, compare with the relevant file under `/var/log` (or `journalctl`) on a test host. Confirm that indexed event counts and field values line up with what you see on the system and that your role can search the right indexes and fields.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Timechart, Multi-series Line Chart",
  "grandmaExplanation": "High run queue depth with elevated scheduling latency causes visible application performance degradation — so you find out before users do when something is slowing down or breaking.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
