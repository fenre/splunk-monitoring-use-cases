{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.2.135",
  "title": "macOS Splunk Universal Forwarder Heartbeat and KV Store Queue Backlog",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Operations"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'macOS Splunk Universal Forwarder Heartbeat and KV Store Queue Backlog'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-1.2.135).",
  "evidence": "Saved search uc_1_2_135_macos_splunk_universal_forwarder_heartbe, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'macOS Splunk Universal Forwarder Heartbeat and KV Store Queue Backlog'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-1.2.135.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "Splunk internal logs forwarded from UF: `index=_internal` or customer `index=infra_int` with `source=*splunkd.log*` and `Metrics` log channel if enabled Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder on macOS, Splunk Observability optional; no extra TA required beyond UF — Universal Forwarder (Splunkbase 617)",
  "spl": "index=_internal source=*splunkd.log* host=* TermType=Forwarded\n| bin _time span=15m\n| stats dc(host) as reporting_hosts by _time\n| join type=left _time [\n    search index=_internal source=*metrics.log* group=tcpout_connections name=*\n    | bin _time span=15m\n    | stats avg(current_queue) as avg_queue by _time, host\n    | where avg_queue > 10000\n    | stats count as hosts_with_queue_pressure by _time\n]\n| fillnull value=0 hosts_with_queue_pressure",
  "description": "A silent Universal Forwarder on macOS still breaks compliance when it stops phone-home or queues explode during VPN outages. Combining presence of `splunkd` forwarded events with TCP output queue metrics surfaces dead agents before log gaps invalidate security monitoring.",
  "value": "Restores observability SLAs by detecting offline or back-pressured macOS forwarders before log loss exceeds audit windows.",
  "implementation": "Forward a minimal `_internal` slice (splunkd, metrics) from endpoints only if policy permits—many teams duplicate to a restricted `infra_int` index with transforms dropping payloads. On the search head, build a baseline count of hosts per 15m bucket; alert when a known-population host disappears or when `metrics.log` `tcpout_connections` shows `current_queue` sustained above your tier (example 10k). For pure heartbeat without internal indexes, use a custom `sourcetype=uf_heartbeat` one-liner script instead.",
  "visualization": "Area chart (reporting_hosts over time), Table (missing hosts vs CMDB), Single value (hosts_with_queue_pressure).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Docs: Forward data from Splunk Enterprise internal indexes",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Docs: Monitor internal Splunk Enterprise logs",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Troubleshooting/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "macos"
  ],
  "equipmentModels": [
    "macos_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• CMDB or saved list of expected `host` values for macOS UFs.\n• Legal/security approval for `_internal` forwarding.\n\nStep 1 — If `_internal` is blocked, deploy `sourcetype=uf_heartbeat` with a 5m cron.\n\nStep 2 — Use `inputlookup` expected_hosts and `| stats` to find missing.\n\nStep 3 — Validate by stopping splunkd on a canary.\n\nStep 4 — Route alerts to endpoint ops with VPN health correlation.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Splunk Universal Forwarder on macOS, Splunk Observability optional; no extra TA required beyond UF — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=see SPL into _internal. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`], _time, avg_queue, host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We check that the little log-forwarding program on each Mac is still talking to Splunk so we don’t think we’re protected when logs have actually stopped.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
