{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.37",
  "title": "NFS Mount Stale Handle Detection",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'NFS Mount Stale Handle Detection' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_nfs_mount_stale_handle_detection` (UC-1.1.37), SOC dashboard panel \"Linux — NFS Mount Stale Handle Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-1.1.37 positive (NFS, Mount, Stale, Handle, Detection): ingest synthetic events into `index=os` sourcetype='syslog' where `count` grouped by `host`, `nfs_server` satisfies `count` > 0 for 'NFS Mount Stale Handle Detection'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-1.1.37 negative (Handle Detection): ingest benign `os` / `syslog` events where `host`, `nfs_server` stays below `count` > 0, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 3 confirms zero alert rows for 'NFS Mount Stale Handle Detection'."
  },
  "dataSources": "`sourcetype=syslog, kernel NFS logs` Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix, custom scripted input` — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=os sourcetype=syslog \"nfs\" (\"stale\" OR \"stale NFS file handle\" OR \"Stale NFS\")\n| stats count by host, nfs_server\n| where count > 0",
  "description": "Identifies when Handle Detection criteria fire for 'NFS Mount Stale Handle Detection' (UC-1.1.37) in `index=os` sourcetype `syslog`. Pipeline aggregates `count` by `host`, `nfs_server` over `the scheduled window`. Alert logic enforces `count` > 0. Title terms: NFS, Mount, Stale, Handle, Detection. Eliminates manual hunting for UC-1.1.37 conditions.",
  "value": "UC-1.1.37 helps teams contain NFS, Mount, Stale, Handle, Detection scenarios and reduce capacity incidents, patch regressions, and service outages before incidents compound.",
  "implementation": "Monitor kernel logs and NFS client logs for stale handle errors. Create immediate alerts with escalation to storage team. Add search to identify affected processes and suggest remount or NFS server recovery.",
  "visualization": "Alert, Table",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 833",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'NFS Mount Stale Handle Detection'. Correlate with apt/yum logs in the same `syslog` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `count` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `syslog`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing `count` > 0.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux",
    "syslog"
  ],
  "equipmentModels": [
    "linux_generic",
    "syslog_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_nix, custom scripted input`.\n• Ensure the following data sources are available: `sourcetype=syslog, kernel NFS logs`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMonitor kernel logs and NFS client logs for stale handle errors. Create immediate alerts with escalation to storage team. Add search to identify affected processes and suggest remount or NFS server recovery.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=os sourcetype=syslog \"nfs\" (\"stale\" OR \"stale NFS file handle\" OR \"Stale NFS\")\n| stats count by host, nfs_server\n| where count > 0\n```\n\nUnderstanding this SPL\n\n**NFS Mount Stale Handle Detection** — Stale NFS handles cause application hangs and I/O failures that severely impact users.\n\nDocumented **Data sources**: `sourcetype=syslog, kernel NFS logs`. **App/TA** (typical add-on context): `Splunk_TA_nix, custom scripted input`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: os; **sourcetype**: syslog. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=os, sourcetype=syslog. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by host, nfs_server** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where count > 0` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nOn the host, compare with `top`, `htop`, `vmstat`, `iostat`, or `sar` as appropriate to this use case. For log-only detections, compare with the relevant file under `/var/log` (or `journalctl`) on a test host. Confirm that indexed event counts and field values line up with what you see on the system and that your role can search the right indexes and fields.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Alert, Table",
  "grandmaExplanation": "Stale NFS handles cause application hangs and I/O failures that severely impact users — so you find out before users do when something is slowing down or breaking.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
