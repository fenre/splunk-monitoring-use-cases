{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.46",
  "title": "Slab Cache Growth Monitoring",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Slab Cache Growth Monitoring' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_slab_cache_growth_monitoring` (UC-1.1.46), SOC dashboard panel \"Linux — Slab Cache Growth Monitoring\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=os` sourcetype='syslog' where `host` satisfies detection threshold for UC-1.1.46 ('Slab Cache Growth Monitoring'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `os` / `syslog` events where `host` stays below detection threshold, or exclude the test entity via `linux_capacity_exceptions.csv` with a valid window. Confirm zero alert rows for UC-1.1.46."
  },
  "dataSources": "`sourcetype=syslog` (kernel/slab messages) or a scripted `custom:slabinfo` feed from `/proc/slabinfo` for structured totals Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix` (and optional custom scripted input for slab snapshots) — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=os sourcetype=syslog \"slab\"\n| bin _time span=1d\n| stats sum(slab_size) as total_slab by host, _time\n| streamstats window=30 avg(total_slab) as baseline stdev(total_slab) as stddev by host\n| eval upper=baseline+(2*stddev)\n| where total_slab > upper",
  "description": "Detects unusual day-over-day growth in the kernel slab allocator footprint so we can catch memory pressure from slab bloat or leaks before the host runs out of usable memory.",
  "value": "Detects unusual day-over-day growth in the kernel slab allocator footprint so we can catch memory pressure.... Timely UC-1.1.46 alerting on 'Slab Cache Growth Monitoring' reduces capacity incidents, patch regressions, and service outages before manual triage or audit channels escalate.",
  "implementation": "Ingest messages that include slab sizing (or build a `custom:slabinfo` input that prints total slab size). Require numeric `slab_size` extraction. Use the daily `stats` and `streamstats` rollups for a host-specific baseline, then alert when totals exceed the upper band.",
  "visualization": "Timechart, Anomaly Chart",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'Slab Cache Growth Monitoring'. Correlate with apt/yum logs in the same `syslog` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `sum` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `syslog`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing capacity threshold.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux",
    "syslog"
  ],
  "equipmentModels": [
    "linux_generic",
    "syslog_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_nix` (and a small scripted input if you are shipping `/proc/slabinfo` as structured events).\n• Ensure the following data sources are available: as documented under **Data sources**.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nSend syslog (or a periodic scripted snapshot) that includes a numeric **slab_size** field per host, or switch the SPL to the field names your script emits. The search expects daily bins and at least a few weeks of history for `streamstats` to learn a normal band.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=os sourcetype=syslog \"slab\"\n| bin _time span=1d\n| stats sum(slab_size) as total_slab by host, _time\n| streamstats window=30 avg(total_slab) as baseline stdev(total_slab) as stddev by host\n| eval upper=baseline+(2*stddev)\n| where total_slab > upper\n```\n\nUnderstanding this SPL\n\n**Slab Cache Growth Monitoring** — Detects when total slab size on a host rises above a rolling two-sigma band, which often precedes hard-to-explain free-memory shrinkage on busy servers.\n\nThe query scopes **index=os** and a syslog-class feed, then **bins** to one day, **stats** a daily `total_slab` per host, and uses **streamstats** for a 30-observation rolling mean and spread before comparing to the eval’d upper control limit.\n\n\nStep 3 — Validate\nOn a Linux host, compare the alert time with `slabtop` and `cat /proc/slabinfo` to see which caches grew. Re-run a short time-range search and confirm the extracted `slab_size` values match what you see in `/proc/slabinfo` (or your scripted input output).\n\nStep 4 — Operationalize\nAdd the search to a dashboard, route alerts to the platform team, and document how to capture a slab snapshot in the runbook. Consider visualizations: Timechart, Anomaly Chart\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_nix` (and optional custom scripted input for slab snapshots) — Splunk Add-on for Unix and Linux (Splunkbase 833)). Enable the modular input or HEC token that writes sourcetype=syslog into os. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch whether the system’s special fast memory pools grow in an unusual way over time, so we can step in if the machine is quietly eating memory in the background.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
