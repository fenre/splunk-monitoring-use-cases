{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.49",
  "title": "Memory Cgroup Limit Enforcement",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'Memory Cgroup Limit Enforcement'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-1.1.49).",
  "evidence": "Saved search uc_1_1_49_memory_cgroup_limit_enforcement, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'Memory Cgroup Limit Enforcement'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-1.1.49.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "`sourcetype=syslog` (cgroup OOM and memory events) and/or `custom:cgroup_memory` with usage vs. limit from cgroup v1/v2 Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix` plus optional custom script for cgroup tree metrics — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=os sourcetype=syslog (\"memory.max_usage_in_bytes\" OR \"Out of memory\") \"cgroup\"\n| stats count by host, cgroup_id\n| where count > 0",
  "description": "Surfaces kernel and runtime messages that show cgroup memory pressure, including OOM-style messages tied to cgroups, so you can act before services are throttled or killed at the limit.",
  "value": "Right-sizing container memory, adjusting limits, or moving workloads is easier when you see which cgroup names hit the wall, instead of only seeing generic out-of-memory noise.",
  "implementation": "Forward `kern`/`containerd`/`kubelet`-class syslog that includes cgroup paths or IDs. Prefer structured `custom:cgroup_memory` events with `current`, `max`, and `cgroup_id` for ratio alerts; this search starts from raw syslog keywords and counts hits per host and cgroup id.",
  "visualization": "Table, Gauge",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "kubernetes",
    "linux",
    "syslog"
  ],
  "equipmentModels": [
    "kubernetes_k8s"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Ingest the OS log stream that records cgroup memory events (often forwarded as syslog) or deploy a `custom:cgroup_memory` input that samples `/sys/fs/cgroup`.\n\nStep 1 — Configure data collection\nFor syslog, ensure the forwarder is allowed to read `/var/log/kern.log` or `journal` streams with kernel priority. For structured metrics, sample usage and limit in MiB and tag `cgroup_id` from the path.\n\nStep 2 — Create the search and alert\n\n```spl\nindex=os sourcetype=syslog (\"memory.max_usage_in_bytes\" OR ((\"Out of memory\" OR \"oom\") AND \"cgroup\"))\n| stats count by host, cgroup_id\n| where count > 0\n```\n\nAdjust keywords to match your distribution’s wording. Add `rex` to extract `cgroup_id` if the field is only in `_raw`.\n\n**Understanding this SPL** — Counts qualifying messages per host and cgroup; raise when any appear in the window (tighten to `>5` in busy environments).\n\n\nStep 3 — Validate\nReproduce a benign limit in a test cgroup and compare host-side `systemd-cgtop` or `cat` of the cgroup’s `memory.current` to the log lines Splunk received.\n\nStep 4 — Operationalize\nRoute to the platform or container SRE queue with links to the namespace or workload, and add a follow-up search on usage/limit ratio if you add structured metrics.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_nix` plus optional custom script for cgroup tree metrics — Splunk Add-on for Unix and Linux (Splunkbase 833)). Enable the modular input or HEC token that writes sourcetype=syslog into os. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`cgroup_id, host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs that a container or service group is bumping into its memory cap, so you can add memory or move work before things get stopped unexpectedly.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
