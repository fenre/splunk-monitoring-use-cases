{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.48",
  "title": "NUMA Memory Imbalance Per Node",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'NUMA Memory Imbalance Per Node' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_numa_memory_imbalance_per_node` (UC-1.1.48), SOC dashboard panel \"Linux — NUMA Memory Imbalance Per Node\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-1.1.48 positive (NUMA, Memory, Imbalance, Per, Node): ingest synthetic events into `index=os` sourcetype='custom:numa_meminfo' where `avg_free`, `max_free`, `min_free` grouped by `host`, `numa_node` satisfies `imbalance_ratio` > 1.5 for 'NUMA Memory Imbalance Per Node'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-1.1.48 negative (Per Node): ingest benign `os` / `custom:numa_meminfo` events where `host`, `numa_node` stays below `imbalance_ratio` > 1.5, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 8 confirms zero alert rows for 'NUMA Memory Imbalance Per Node'."
  },
  "dataSources": "`sourcetype=custom:numa_meminfo` (parsed from `/sys/devices/system/node/node*/meminfo`) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix` with a custom scripted input for per-node meminfo — Universal Forwarder (Splunkbase 617)",
  "spl": "index=os sourcetype=custom:numa_meminfo host=*\n| stats avg(node_free) as avg_free by host, numa_node\n| stats max(avg_free) as max_free, min(avg_free) as min_free by host\n| eval imbalance_ratio=max_free/min_free\n| where imbalance_ratio > 1.5",
  "description": "Monitors UC-1.1.48 — NUMA, Memory, Imbalance, Per, Node — using `index=os` sourcetype `custom:numa_meminfo`, grouped by `host`, `numa_node`. Outputs `avg_free`, `max_free`, `min_free` each `the scheduled window`; alert when `imbalance_ratio` > 1.5. Primary focus: Per Node. Purpose-built for 'NUMA Memory Imbalance Per Node' in Linux workflows.",
  "value": "UC-1.1.48 accelerates insight into Per Node ('NUMA Memory Imbalance Per Node') by tracking `avg_free`, `max_free`, `min_free` on `custom:numa_meminfo` feeds, reducing capacity incidents, patch regressions, and service outages before spreadsheet checks hide regressions.",
  "implementation": "Emit one event per (host, numa_node) with a `node_free` field. Compare max vs min free memory across nodes; alert when the ratio passes 1.5, indicating meaningful imbalance for your hardware generation.",
  "visualization": "Gauge, Heatmap",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'NUMA Memory Imbalance Per Node'. Correlate with apt/yum logs in the same `custom:numa_meminfo` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `avg` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `custom:numa_meminfo`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing `imbalance_ratio` > 1.5.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux"
  ],
  "equipmentModels": [
    "linux_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install `Splunk_TA_nix` and a script that reads each NUMA node’s meminfo and prints structured fields, including `numa_node` and `node_free` in KB or bytes consistently.\n• Index into `os` (or your standard OS index) with sourcetype `custom:numa_meminfo`.\n\nStep 1 — Configure data collection\nRun the script every few minutes; keep units consistent. Document whether `node_free` is kB, bytes, or pages so operators do not mix comparisons.\n\nStep 2 — Create the search and alert\n\n```spl\nindex=os sourcetype=custom:numa_meminfo host=*\n| stats avg(node_free) as avg_free by host, numa_node\n| stats max(avg_free) as max_free, min(avg_free) as min_free by host\n| eval imbalance_ratio=if(min_free>0, max_free/min_free, null())\n| where imbalance_ratio > 1.5\n```\n\n**Understanding this SPL** — Averages per node, then for each host compares the fullest and emptiest node by free memory. Large ratios mean one node is much freer than another.\n\n\nStep 3 — Validate\nOn the host, use `numastat` and `/sys/devices/system/node/node*/meminfo` to compare free lines against what Splunk received for the same timestamp.\n\nStep 4 — Operationalize\nPair the alert with a short runbook: check VM placement, `numactl` policies, and large pinned allocations; consider OS tuning or workload moves.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_nix` with a custom scripted input for per-node meminfo — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=custom:numa_meminfo into os. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, imbalance_ratio, numa_node`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We catch when one side of a big server is starved of memory while the other still has room, so you can fix placement before jobs run slow in hard-to-explain ways.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
