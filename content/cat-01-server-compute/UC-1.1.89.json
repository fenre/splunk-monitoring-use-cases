{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.89",
  "title": "Syslog Flood Detection",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Anomaly"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`sourcetype=syslog`",
  "app": "`Splunk_TA_nix`",
  "spl": "index=os sourcetype=syslog host=*\n| bin _time span=5m\n| stats count by host, _time\n| where count>10000",
  "description": "Buckets **syslog** volume per **host** in **5**-**minute** intervals and flags any interval over **10**k events—tune to your daily **p99** once you have a baseline.",
  "value": "Log floods burn **ingest** license, mask **security** lines in noise, and often come from **looping** apps or **debug** left on after an incident.",
  "implementation": "Replace the static **10**k with `| eventstats median(count) as med by host | where count>20*med` for an adaptive version; add **ignore** **host** patterns for known **noisy** appliances if policy allows.",
  "visualization": "Timechart, Alert",
  "cimModels": [
    "N/A"
  ],
  "knownFalsePositives": "**SIEM** **forwarders** that batch; **k8s** **node** **syslog** during **rollouts**; too-low **10**k on a **log** **aggregator**—run this **per** **source** **type** child, not on the **agg** only, or you will never learn the true offender.",
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833"
    },
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/"
    }
  ],
  "equipment": [
    "kubernetes",
    "linux",
    "syslog"
  ],
  "equipmentModels": [
    "kubernetes_helm"
  ],
  "detailedImplementation": "Prerequisites\n• Accurate **host** field; for **K8s**, you may instead run the same SPL on **`namespace`** in a different use case.\n\n**SPL** — Replaces the invalid `where count>10000 in 5 minute window` pattern with a proper **bin** + **stats**.\n\n\nStep 3 — Validate\n`tcpdump` **not** required first—on the host, `journalctl --since` rate or **rsyslog** **impstats** if you use them; compare to the **count** in Splunk for the **bin**.\n\nStep 4 — Operationalize\nThrottle at the **forwarder** after you identify the **facility** causing the burst.\n\n",
  "grandmaExplanation": "We help you know when one machine suddenly starts writing a truly huge diary, so a broken program or attack does not fill the log system and hide other problems."
}
