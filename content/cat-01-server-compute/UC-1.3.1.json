{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.3.1",
  "title": "System Resource Monitoring",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'System Resource Monitoring'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-1.3.1).",
  "evidence": "Saved search uc_1_3_1_system_resource_monitoring, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'System Resource Monitoring'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-1.3.1.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "Custom scripted inputs (`top -l 1`, `vm_stat`, `df`) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Universal Forwarder for macOS, custom scripted inputs — Universal Forwarder (Splunkbase 617)",
  "spl": "index=os sourcetype=macos_top host=*\n| stats latest(cpu_pct) as cpu, latest(mem_pct) as memory by host\n| where cpu > 80 OR memory > 90",
  "description": "Endpoint performance visibility helps IT support triage user complaints and identify machines needing replacement or upgrades.",
  "value": "Spotting high CPU and memory on Macs before tickets pile up helps support reroute work, add RAM or replacement, and avoid users sitting through a frozen desktop during busy meetings.",
  "implementation": "Install Splunk UF on macOS endpoints. Create scripted inputs for `top -l 1 -s 0`, `vm_stat`, and `df -h`. Run every 60-300 seconds. Parse key metrics.",
  "visualization": "Table of endpoints, Gauge panels, Line chart trending.",
  "cimModels": [
    "Performance"
  ],
  "cimSpl": "| tstats `summariesonly` avg(Performance.cpu_load_percent) as cpu from datamodel=Performance where nodename=Performance.CPU by Performance.host span=5m\n| join type=outer Performance.host [ | tstats `summariesonly` avg(Performance.mem_used_percent) as memory from datamodel=Performance where nodename=Performance.Memory by Performance.host span=5m ]\n| where cpu > 80 OR memory > 90",
  "dataModelAcceleration": "Enable acceleration on the Performance data model for nodes Performance.CPU and Performance.Memory (summary range covering the alert lookback, for example 30 days).",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "macos"
  ],
  "equipmentModels": [
    "macos_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Universal Forwarder for macOS, custom scripted inputs.\n• Ensure the following data sources are available: Custom scripted inputs (`top -l 1`, `vm_stat`, `df`).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nInstall the Universal Forwarder on macOS. Create scripted inputs for `top -l 1 -s 0`, `vm_stat`, and `df -h`. Run every 60–300 seconds. Parse `cpu_pct`, `mem_pct` (or equivalent) into fields your search expects.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=os sourcetype=macos_top host=*\n| stats latest(cpu_pct) as cpu, latest(mem_pct) as memory by host\n| where cpu > 80 OR memory > 90\n```\n\nUnderstanding this SPL\n\n**System Resource Monitoring** — Endpoint performance visibility helps IT support triage user complaints and identify machines needing replacement or upgrades.\n\nDocumented **Data sources**: Custom scripted inputs (`top -l 1`, `vm_stat`, `df`). The SPL targets **index** `os` and your macOS process/resource sourcetype; rename if your deployment differs. If you normalize the same host metrics through the add-on for Unix and Linux, you can also express this use case with the Performance data model and `tstats` (see the **cimSpl** field) once mapping is in place.\n\n**Pipeline walkthrough**\n\n• Scopes the data: `index=os`, `sourcetype=macos_top`. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` keeps the latest `cpu` and `memory` per **host**.\n• `where` applies the high-utilization condition for alerting.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with Activity Monitor for CPU and memory on a test Mac. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table of endpoints, Gauge panels, Line chart trending.\n\nScripted input (generic example)\nIn the app’s `local/inputs.conf` add a stanza such as:\n\n```ini\n[script://$SPLUNK_HOME/etc/apps/YourApp/bin/macos_top.sh]\ninterval = 60\nsourcetype = macos_top\nindex = os\ndisabled = 0\n```\n\nThe script should emit one line per snapshot with parseable key=value fields. For full details (paths, scheduling, permissions), see the Implementation guide: docs/implementation-guide.md",
  "grandmaExplanation": "We watch each Mac’s processor and memory use so we can see which ones are under heavy load before people complain that their computer feels slow or frozen.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
