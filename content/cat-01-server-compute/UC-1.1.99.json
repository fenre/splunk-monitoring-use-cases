{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.99",
  "title": "Kernel Lock Contention Detection",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Kernel Lock Contention Detection' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_kernel_lock_contention_detection` (UC-1.1.99), SOC dashboard panel \"Linux — Kernel Lock Contention Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-1.1.99 positive (Kernel, Lock, Contention, Detection): ingest synthetic events into `index=os` sourcetype='custom:lock_stats' where `avg_contention` grouped by `host`, `lock_name` satisfies detection threshold for 'Kernel Lock Contention Detection'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-1.1.99 negative (Contention Detection): ingest benign `os` / `custom:lock_stats` events where `host`, `lock_name` stays below detection threshold, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 10 confirms zero alert rows for 'Kernel Lock Contention Detection'."
  },
  "dataSources": "`sourcetype=custom:lock_stats, /proc/lock_stat` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix, custom scripted input` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=os sourcetype=custom:lock_stats host=*\n| stats avg(contentions) as avg_contention by host, lock_name\n| where avg_contention > threshold",
  "description": "Identifies UC-1.1.99 — Kernel, Lock, Contention, Detection — using `index=os` sourcetype `custom:lock_stats`, grouped by `host`, `lock_name`. Outputs `avg_contention` each `the scheduled window`; alert when configured threshold. Primary focus: Contention Detection. Purpose-built for 'Kernel Lock Contention Detection' in Linux workflows.",
  "value": "For 'Kernel Lock Contention Detection', UC-1.1.99 converts `avg_contention` on `custom:lock_stats` feeds into actionable rows and reduces capacity incidents, patch regressions, and service outages.",
  "implementation": "Enable kernel lock statistics via /proc/lock_stat or perf tools. Monitor lock contention per lock. Create alerts for high-contention locks with recommendations for kernel/application tuning.",
  "visualization": "Table, Bar Chart",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'Kernel Lock Contention Detection'. Correlate with apt/yum logs in the same `custom:lock_stats` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `avg` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `custom:lock_stats`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing capacity threshold.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux"
  ],
  "equipmentModels": [
    "linux_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_nix, custom scripted input`.\n• Ensure the following data sources are available: `sourcetype=custom:lock_stats, /proc/lock_stat`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnable kernel lock statistics via /proc/lock_stat or perf tools. Monitor lock contention per lock. Create alerts for high-contention locks with recommendations for kernel/application tuning.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=os sourcetype=custom:lock_stats host=*\n| stats avg(contentions) as avg_contention by host, lock_name\n| where avg_contention > threshold\n```\n\nUnderstanding this SPL\n\n**Kernel Lock Contention Detection** — Kernel lock contention degrades multi-core scalability and application throughput.\n\nDocumented **Data sources**: `sourcetype=custom:lock_stats, /proc/lock_stat`. **App/TA** (typical add-on context): `Splunk_TA_nix, custom scripted input`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: os; **sourcetype**: custom:lock_stats. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=os, sourcetype=custom:lock_stats. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by host, lock_name** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where avg_contention > threshold` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table, Bar Chart",
  "grandmaExplanation": "We look for hot kernel locks so we can tell when applications are tripping over each other inside the operating system, not just in user code.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
