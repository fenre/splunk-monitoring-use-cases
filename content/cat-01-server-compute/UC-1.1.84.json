{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.84",
  "title": "Runaway Process Detection (CPU Hog)",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`sourcetype=top` (Splunk_TA_nix per-process CPU fields)",
  "app": "`Splunk_TA_nix`",
  "spl": "index=os sourcetype=top host=*\n| stats avg(cpu_pct) as avg_cpu by host, process\n| where avg_cpu>80",
  "description": "Averages per-process **cpu_pct** from `top` samples in the search window and flags **(host,process)** pairs above an **80**% average—tune to your workload class.",
  "value": "A single hot process on a shared host can starve others even when **vmstat** still looks “OK”; `top` granularity is the fastest path to the binary name for support teams.",
  "implementation": "Field may be **pctCPU** or **pcpu** depending on TA version—normalize in **props**. Add `| where count>10` on underlying events to require enough samples before paging.",
  "visualization": "Table, Timechart",
  "cimModels": [
    "Endpoint.Processes"
  ],
  "cimSpl": "| tstats `summariesonly` avg(Processes.cpu_load_percent) as avg_cpu from datamodel=Endpoint.Processes by Processes.dest Processes.process_name span=5m | where avg_cpu>80",
  "dataModelAcceleration": "Enable **Endpoint.Processes** acceleration after `top`/`ps` is mapped with **cpu_load_percent** per process (CIM field names may differ—align `Fieldsummary` first).",
  "knownFalsePositives": "**gzip** during backup hour; **java** GC spikes; **containers** where **process** names collapse to `java`—add **container** id from **cgroup** in a follow-on use case.",
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833"
    },
    {
      "title": "CIM: Endpoint",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Endpoint"
    }
  ],
  "equipment": [
    "linux"
  ],
  "detailedImplementation": "Prerequisites\n• `top` script enabled; confirm **cpu_pct** exists with a short **Fieldsummary**.\n\n**CIM** — If `Processes.cpu_load_percent` does not exist in your build, adjust `cimSpl` to the field your **CIM** engineer published for per-process CPU.\n\n\nStep 3 — Validate\nRun `top` or `htop` on the host and **pidstat** for one-minute **CPU** proof; compare the **process** string to Splunk (watch for **truncation**).\n\nStep 4 — Operationalize\nUse the same panel next to the **CPU utilization trending** host-level UC for context.\n\n",
  "grandmaExplanation": "We help you spot one program that is eating most of the processor for a while, so you can stop it or fix it before everything else on the machine feels slow."
}
