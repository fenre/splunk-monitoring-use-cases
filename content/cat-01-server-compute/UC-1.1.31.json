{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.31",
  "title": "Hugepage Allocation and Usage",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Hugepage Allocation and Usage' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_hugepage_allocation_and_usage` (UC-1.1.31), SOC dashboard panel \"Linux — Hugepage Allocation and Usage\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-1.1.31 positive (Hugepage, Allocation, and, Usage): ingest synthetic events into `index=os` sourcetype='custom:hugepages' where `total`, `free` grouped by `host` satisfies `usage_pct` > 90 for 'Hugepage Allocation and Usage'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-1.1.31 negative (and Usage): ingest benign `os` / `custom:hugepages` events where `host` stays below `usage_pct` > 90, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 15 confirms zero alert rows for 'Hugepage Allocation and Usage'."
  },
  "dataSources": "`sourcetype=custom:hugepages, /proc/meminfo` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix, custom scripted input` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=os sourcetype=custom:hugepages host=*\n| stats avg(HugePages_Total) as total, avg(HugePages_Free) as free by host\n| eval usage_pct=(total-free)/total*100\n| where usage_pct > 90",
  "description": "Detects UC-1.1.31 — Hugepage, Allocation, and, Usage — using `index=os` sourcetype `custom:hugepages`, grouped by `host`. Outputs `total`, `free` each `the scheduled window`; alert when `usage_pct` > 90. Primary focus: and Usage. Purpose-built for 'Hugepage Allocation and Usage' in Linux workflows.",
  "value": "UC-1.1.31 operationalises 'Hugepage Allocation and Usage' against `custom:hugepages` feeds so operators reduce capacity incidents, patch regressions, and service outages without manual correlation.",
  "implementation": "Create a scripted input parsing /proc/meminfo for hugepage metrics. Track HugePages_Total, HugePages_Free, HugePages_Rsvd, and HugePages_Surp. Alert when free hugepages fall below 10% or when failed allocations occur.",
  "visualization": "Gauge, Single Value",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'Hugepage Allocation and Usage'. Correlate with apt/yum logs in the same `custom:hugepages` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `avg` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `custom:hugepages`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing `usage_pct` > 90.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux"
  ],
  "equipmentModels": [
    "linux_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_nix, custom scripted input`.\n• Ensure the following data sources are available: `sourcetype=custom:hugepages, /proc/meminfo`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nCreate a scripted input parsing /proc/meminfo for hugepage metrics. Track HugePages_Total, HugePages_Free, HugePages_Rsvd, and HugePages_Surp. Alert when free hugepages fall below 10% or when failed allocations occur.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=os sourcetype=custom:hugepages host=*\n| stats avg(HugePages_Total) as total, avg(HugePages_Free) as free by host\n| eval usage_pct=(total-free)/total*100\n| where usage_pct > 90\n```\n\nUnderstanding this SPL\n\n**Hugepage Allocation and Usage** — Hugepage contention or allocation failures impact database and large memory workload performance.\n\nDocumented **Data sources**: `sourcetype=custom:hugepages, /proc/meminfo`. **App/TA** (typical add-on context): `Splunk_TA_nix, custom scripted input`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: os; **sourcetype**: custom:hugepages. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=os, sourcetype=custom:hugepages. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by host** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **usage_pct** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where usage_pct > 90` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nOn the host, compare with `top`, `htop`, `vmstat`, `iostat`, or `sar` as appropriate to this use case. For log-only detections, compare with the relevant file under `/var/log` (or `journalctl`) on a test host. Confirm that indexed event counts and field values line up with what you see on the system and that your role can search the right indexes and fields.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Gauge, Single Value\n\nScripted input (generic example)\nThis use case relies on a scripted input. In the app's local/inputs.conf add a stanza such as:\n\n```ini\n[script://$SPLUNK_HOME/etc/apps/YourApp/bin/collect.sh]\ninterval = 300\nsourcetype = your_sourcetype\nindex = main\ndisabled = 0\n```\n\nThe script should print one event per line (e.g. key=value). Example minimal script (bash):\n\n```bash\n#!/usr/bin/env bash\n# Output metrics or events, one per line\necho \"metric=value timestamp=$(date +%s)\"\n```\n\nFor full details (paths, scheduling, permissions), see the Implementation guide: docs/implementation-guide.md",
  "grandmaExplanation": "Hugepage contention or allocation failures impact database and large memory workload performance — so you find out before users do when something is slowing down or breaking.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
