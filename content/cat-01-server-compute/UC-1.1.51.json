{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.51",
  "title": "TCP Retransmission Rate Elevation",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'TCP Retransmission Rate Elevation' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_tcp_retransmission_rate_elevation` (UC-1.1.51), SOC dashboard panel \"Linux — TCP Retransmission Rate Elevation\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-1.1.51 positive (TCP, Retransmission, Rate, Elevation): ingest synthetic events into `index=os` sourcetype='netstat' where `retrans`, `baseline`, `stddev` grouped by `host`, `_time` satisfies detection threshold for 'TCP Retransmission Rate Elevation'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-1.1.51 negative (Rate Elevation): ingest benign `os` / `netstat` events where `host`, `_time` stays below detection threshold, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 7 confirms zero alert rows for 'TCP Retransmission Rate Elevation'."
  },
  "dataSources": "`sourcetype=netstat` (or a structured `custom:tcp_retrans` input from `/proc/net/snmp`) with `retransSegs` per interval Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix` (netstat) and/or a scripted companion for exact SNMP-style counters — Universal Forwarder (Splunkbase 617)",
  "spl": "index=os sourcetype=netstat host=*\n| bin _time span=5m\n| stats sum(retransSegs) as retrans by host, _time\n| streamstats window=100 avg(retrans) as baseline stdev(retrans) as stddev by host\n| eval upper=baseline+(2*stddev)\n| where retrans > upper",
  "description": "Identifies UC-1.1.51 — TCP, Retransmission, Rate, Elevation — using `index=os` sourcetype `netstat`, grouped by `host`, `_time`. Outputs `retrans`, `baseline`, `stddev` each `5m`; alert when configured threshold. Primary focus: Rate Elevation. Purpose-built for 'TCP Retransmission Rate Elevation' in Linux workflows.",
  "value": "UC-1.1.51 operationalises 'TCP Retransmission Rate Elevation' against `netstat` feeds so operators reduce capacity incidents, patch regressions, and service outages without manual correlation.",
  "implementation": "Ingest retransmission counters (here `retransSegs` from a netstat-class feed) at a steady interval, bucket to five minutes, and compare each bucket to a rolling `streamstats` band so occasional spikes in healthy churn do not false-positive constantly.",
  "visualization": "Timechart, Anomaly Chart",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'TCP Retransmission Rate Elevation'. Correlate with apt/yum logs in the same `netstat` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `sum` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `netstat`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing capacity threshold.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux",
    "snmp"
  ],
  "equipmentModels": [
    "snmp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install `Splunk_TA_nix` and enable a netstat-style input, or build a `custom:tcp_retrans` script that prints cumulative `retransSegs` from `/proc/net/snmp`.\n\nStep 1 — Configure data collection\nIf you use **counters**, either emit deltas in the script or precompute delta `retransSegs` per interval so the **sum** in Splunk is meaningful. Align collection interval with the `bin` span for stable math.\n\nStep 2 — Create the search and alert\n\n```spl\nindex=os sourcetype=netstat host=*\n| bin _time span=5m\n| stats sum(retransSegs) as retrans by host, _time\n| streamstats window=100 avg(retrans) as baseline stdev(retrans) as stddev by host\n| eval upper=baseline+(2*stddev)\n| where retrans > upper\n```\n\n**Understanding this SPL** — Anomaly over time per host using `streamstats`; widen `window` on chatty sites so baselines are meaningful.\n\n\nStep 3 — Validate\nOn the host, compare with `nstat -z` or `ss -i` and packet captures if needed; also check switch error counters on the same period.\n\nStep 4 — Operationalize\nRoute to network and app owners together—pure host tuning rarely fixes a fabric issue.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_nix` (netstat) and/or a scripted companion for exact SNMP-style counters — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=netstat into os. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch for signs the computer is resending a lot of network data it already tried to send, which usually means a rough connection or a sick link somewhere along the way.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
