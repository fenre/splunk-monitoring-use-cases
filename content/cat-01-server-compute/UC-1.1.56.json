{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.56",
  "title": "Firewall Rule Hit Tracking (iptables/nftables)",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'Firewall Rule Hit Tracking (iptables/nftables)'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-1.1.56).",
  "evidence": "Saved search uc_1_1_56_firewall_rule_hit_tracking_iptables_nfta, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'Firewall Rule Hit Tracking (iptables/nftables)'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-1.1.56.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "`sourcetype=syslog` (ufw, iptables LOG targets, or nft `log` traces forwarded to a central log) Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix` — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=os sourcetype=syslog \"ufw\" (\"DENY\" OR \"REJECT\" OR \"DROP\")\n| stats count by host, src, dst_port, protocol\n| where count > 100",
  "description": "Builds a top-list of kernel-style firewall block lines that mention UFW or explicit DENY/REJECT/DROP actions, to spot surges in blocked flows by source, port, and protocol.",
  "value": "A sudden climb in denials for a single port or address range is often a scan, a mis-pointed client, or a policy bug—metrics here shorten triage for both security and app teams.",
  "implementation": "Make sure your logging rules prefix messages so the search can see them. Lower `>100` in quiet segments or add time bucketing in a follow-on alert. Extract `src`, `dst_port`, and `protocol` with your props; if missing, add `REX` temporarily in the base search.",
  "visualization": "Table, Bar Chart",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "linux",
    "m365",
    "syslog"
  ],
  "equipmentModels": [
    "linux_generic",
    "m365_generic",
    "syslog_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• On each host, enable **iptables** or **nftables** `LOG` for deny actions you care about, and forward **kern**-priority syslog to the Splunk input already managed by the TA.\n\nStep 1 — Configure data collection\nMap vendor-specific strings; UFW labels lines with `[UFW` while raw iptables might only show `IN=... OUT=...`. Use **props** to normalize to `action`, `src`, and `dst_port`.\n\nStep 2 — Create the search and alert\nThe SPL groups high-volume denials. Start with a **report**; promote to an **alert** only after the volume threshold matches your noise floor.\n\n**Understanding this SPL** — A straight count by tuple; adjust OR clauses if you standardize on `action=deny` instead of raw substrings.\n\n\nStep 3 — Validate\nOn host, `iptables -L -n` / `nft list ruleset` to confirm the rule you think is logging; use `tcpdump` or `conntrack` if you also need to prove the packet really reached the policer.\n\nStep 4 — Operationalize\nFeed top sources into a threat or CMDB workflow; for benign partners, add CIDRs to an allow list rather than silencing the whole use case.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_nix` — Splunk Add-on for Unix and Linux (Splunkbase 833)). Enable the modular input or HEC token that writes sourcetype=syslog into os. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`dst_port, host, protocol, src`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We help you see when a firewall is saying “no” a lot more than usual, which can show an attack, a wrong setting, or a client pointed at the wrong place.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
