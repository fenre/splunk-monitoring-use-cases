{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.113",
  "title": "SETUID Audit and Tracking",
  "criticality": "critical",
  "difficulty": "beginner",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'SETUID Audit and Tracking' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_setuid_audit_and_tracking` (UC-1.1.113), SOC dashboard panel \"Linux — SETUID Audit and Tracking\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-1.1.113 positive (SETUID, Audit, and, Tracking): ingest synthetic events into `index=os` sourcetype='linux_audit' where `count` grouped by `host`, `exe`, `user` satisfies detection threshold for 'SETUID Audit and Tracking'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-1.1.113 negative (and Tracking): ingest benign `os` / `linux_audit` events where `host`, `exe`, `user` stays below detection threshold, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 2 confirms zero alert rows for 'SETUID Audit and Tracking'."
  },
  "dataSources": "`sourcetype=linux_audit` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix, custom scripted input` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=os sourcetype=linux_audit type=EXECVE exe=\"*\" AND suid\n| stats count by host, exe, user\n| where count > threshold",
  "description": "Monitors UC-1.1.113 for SETUID, Audit, and, Tracking over `index=os` sourcetype `linux_audit` with thresholds configured threshold. Groups `count` by `host`, `exe`, `user` each `the scheduled window`. Centered on and Tracking indicators. Supports repeatable linux triage for 'SETUID Audit and Tracking'.",
  "value": "For 'SETUID Audit and Tracking', UC-1.1.113 converts `activity` on `linux_audit` feeds into actionable rows and reduces capacity incidents, patch regressions, and service outages.",
  "implementation": "Configure auditctl rules monitoring EXECVE syscalls with setuid. Create alerts on SETUID binary execution by unexpected users. Baseline expected SETUID usage per host.",
  "visualization": "Table, Alert",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'SETUID Audit and Tracking'. Correlate with apt/yum logs in the same `linux_audit` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `count` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `linux_audit`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing capacity threshold.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux"
  ],
  "equipmentModels": [
    "linux_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_nix, custom scripted input`.\n• Ensure the following data sources are available: `sourcetype=linux_audit`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nConfigure auditctl rules monitoring EXECVE syscalls with setuid. Create alerts on SETUID binary execution by unexpected users. Baseline expected SETUID usage per host.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=os sourcetype=linux_audit type=EXECVE exe=\"*\" AND suid\n| stats count by host, exe, user\n| where count > threshold\n```\n\nUnderstanding this SPL\n\n**SETUID Audit and Tracking** — SETUID binary execution enables privilege escalation requiring tracking for security monitoring.\n\nDocumented **Data sources**: `sourcetype=linux_audit`. **App/TA** (typical add-on context): `Splunk_TA_nix, custom scripted input`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: os; **sourcetype**: linux_audit. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=os, sourcetype=linux_audit. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by host, exe, user** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where count > threshold` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table, Alert",
  "grandmaExplanation": "We track when setuid programs run from Linux audit data so only the binaries and users we expect can elevate privileges.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
