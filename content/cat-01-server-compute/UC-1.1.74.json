{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.74",
  "title": "Login from Unusual Source IPs",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "dataSources": "`sourcetype=linux_secure` (accepted SSH or other login success lines with **src**)",
  "app": "`Splunk_TA_nix`",
  "spl": "index=os sourcetype=linux_secure (\"Accepted publickey\" OR \"Accepted password\")\n| bin _time span=24h\n| stats dc(src) as unique_ips by host, user, _time\n| eventstats avg(unique_ips) as baseline_ips by user, _time\n| where isnotnull(baseline_ips) AND unique_ips > baseline_ips + 3",
  "description": "Within each **24h** **bucket**, compares the distinct **src** count for a **(host,user)** pair to that user’s same-day **average** distinct **src** from **eventstats**, flagging abnormally many client IPs in that window (requires a few days of history to behave).",
  "value": "Noisy in cloud NAT environments unless you add **CIDR** allowlists, but in fixed-VPC server estates it is a light-weight “more sources than this account usually has” check before full UEBA tools.",
  "implementation": "For pure burst detection, prefer a **7d**-long **inputlookup** baseline per **user**; the sample uses **eventstats** in-window as a day-one stand-in. Add `| where count>5` on underlying events if each row is a single line.",
  "visualization": "Table, Scatter Plot",
  "cimModels": [
    "Authentication"
  ],
  "cimSpl": "| tstats `summariesonly` dc(Authentication.src) as nsrc from datamodel=Authentication.Authentication where Authentication.action=success by Authentication.user Authentication.dest span=24h | where nsrc>5",
  "dataModelAcceleration": "Enable **Authentication** acceleration; map `linux_secure` success lines to **action=success** and ensure **src** and **user** are non-null in CIM.",
  "knownFalsePositives": "Any corporate **NAT** that rotates exit IPs; **CI** users; **SRE** jump hosts that legitimately have dozens of home IPs a day; **zero**-history first day of deployment (no baseline) — the SPL guards with `isnotnull(baseline_ips)` but still think about warm-up time.",
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833"
    },
    {
      "title": "CIM: Authentication",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Authentication"
    }
  ],
  "equipment": [
    "linux"
  ],
  "detailedImplementation": "Prerequisites\n• Successful logins with a parseable `src` (often from **OpenSSH** “Accepted” lines in **auth** / **secure** logs).\n\n**SPL** — If you prefer machine learning, swap to **anomalydetection** on **dc(src)**. If **eventstats** is too fragile, pre-build **| inputlookup user_ssh_src_baseline.csv**.\n\n**CIM** — The `cimSpl` is a different but related story (raw distinct sources per day); re-tune to mirror your org’s UEBA choice.\n\n\nStep 3 — Validate\n`last` / `wtmp` and **auth** on the host for a known **user** and compare the **IP** set to the Search output for the day.\n\nStep 4 — Operationalize\nCorrelate with **MFA** enrollments; if a user is meant to travel, use **Okta/Entra** travel signals instead of raw IP alone on laptops.\n\n",
  "grandmaExplanation": "We help you notice when someone signs in from more different places than they usually do in a day, which can be travel—or someone else using their name."
}
