{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.42",
  "title": "SSD Wear Leveling and Health",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'SSD Wear Leveling and Health' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_ssd_wear_leveling_and_health` (UC-1.1.42), SOC dashboard panel \"Linux — SSD Wear Leveling and Health\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-1.1.42 positive (SSD, Wear, Leveling, and, Health): ingest synthetic events into `index=os` sourcetype='custom:nvme' where `count` grouped by `host`, `device` satisfies `wear_pct` > 80, or `spare` < 5 for 'SSD Wear Leveling and Health'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-1.1.42 negative (and Health): ingest benign `os` / `custom:nvme` events where `host`, `device` stays below `wear_pct` > 80, or `spare` < 5, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 13 confirms zero alert rows for 'SSD Wear Leveling and Health'."
  },
  "dataSources": "`sourcetype=custom:nvme, nvme-cli output` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix, custom scripted input` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=os sourcetype=custom:nvme host=*\n| stats latest(percentage_used) as wear_pct, latest(available_spare) as spare by host, device\n| where wear_pct > 80 OR spare < 5",
  "description": "Detects UC-1.1.42 — SSD, Wear, Leveling, and, Health — using `index=os` sourcetype `custom:nvme`, grouped by `host`, `device`. Outputs `count` each `the scheduled window`; alert when `wear_pct` > 80, or `spare` < 5. Primary focus: and Health. Purpose-built for 'SSD Wear Leveling and Health' in Linux workflows.",
  "value": "UC-1.1.42 gives Linux teams earlier visibility into and Health through `custom:nvme` feeds, lowering capacity incidents, patch regressions, and service outages.",
  "implementation": "Create a scripted input running 'nvme smart-log' for NVMe drives. Track percentage_used, available_spare, and media_errors. Alert when wear exceeds 80% or spare drops below 5%.",
  "visualization": "Gauge, Single Value",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'SSD Wear Leveling and Health'. Correlate with apt/yum logs in the same `custom:nvme` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `count` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `custom:nvme`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing `wear_pct` > 80.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux"
  ],
  "equipmentModels": [
    "linux_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_nix, custom scripted input`.\n• Ensure the following data sources are available: `sourcetype=custom:nvme, nvme-cli output`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nCreate a scripted input running 'nvme smart-log' for NVMe drives. Track percentage_used, available_spare, and media_errors. Alert when wear exceeds 80% or spare drops below 5%.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=os sourcetype=custom:nvme host=*\n| stats latest(percentage_used) as wear_pct, latest(available_spare) as spare by host, device\n| where wear_pct > 80 OR spare < 5\n```\n\nUnderstanding this SPL\n\n**SSD Wear Leveling and Health** — SSD wear metrics indicate remaining lifespan, enabling proactive replacement planning.\n\nDocumented **Data sources**: `sourcetype=custom:nvme, nvme-cli output`. **App/TA** (typical add-on context): `Splunk_TA_nix, custom scripted input`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: os; **sourcetype**: custom:nvme. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=os, sourcetype=custom:nvme. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by host, device** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where wear_pct > 80 OR spare < 5` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nOn the host, compare with `top`, `htop`, `vmstat`, `iostat`, or `sar` as appropriate to this use case. For log-only detections, compare with the relevant file under `/var/log` (or `journalctl`) on a test host. Confirm that indexed event counts and field values line up with what you see on the system and that your role can search the right indexes and fields.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Gauge, Single Value\n\nScripted input (generic example)\nThis use case relies on a scripted input. In the app's local/inputs.conf add a stanza such as:\n\n```ini\n[script://$SPLUNK_HOME/etc/apps/YourApp/bin/collect.sh]\ninterval = 300\nsourcetype = your_sourcetype\nindex = main\ndisabled = 0\n```\n\nThe script should print one event per line (e.g. key=value). Example minimal script (bash):\n\n```bash\n#!/usr/bin/env bash\n# Output metrics or events, one per line\necho \"metric=value timestamp=$(date +%s)\"\n```\n\nFor full details (paths, scheduling, permissions), see the Implementation guide: docs/implementation-guide.md",
  "grandmaExplanation": "SSD wear metrics indicate remaining lifespan, enabling proactive replacement planning — so you find out before users do when something is slowing down or breaking.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
