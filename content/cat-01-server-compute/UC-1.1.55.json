{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.55",
  "title": "DNS Resolution Failure Rate",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'DNS Resolution Failure Rate' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_dns_resolution_failure_rate` (UC-1.1.55), SOC dashboard panel \"Linux — DNS Resolution Failure Rate\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=os` sourcetype='syslog' where `host` satisfies `failures` > 10 for UC-1.1.55 ('DNS Resolution Failure Rate'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `os` / `syslog` events where `host` stays below `failures` > 10, or exclude the test entity via `linux_capacity_exceptions.csv` with a valid window. Confirm zero alert rows for UC-1.1.55."
  },
  "dataSources": "`sourcetype=syslog` (systemd-resolved, unbound, BIND, or CoreDNS logs forwarded as OS logs) Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix` — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=os sourcetype=syslog \"systemd-resolved\" (\"SERVFAIL\" OR \"NXDOMAIN\" OR \"TIMEOUT\")\n| stats count as failures by host, query_name\n| where failures > 10",
  "description": "Aggregates resolver log lines that show hard failures (SERVFAIL, TIMEOUT) or explicit NXDOMAIN responses for monitored strings, grouped by host and query name so you can see who is failing and for what name.",
  "value": "Aggregates resolver log lines that show hard failures (SERVFAIL, TIMEOUT) or explicit NXDOMAIN responses fo.... Timely UC-1.1.55 alerting on 'DNS Resolution Failure Rate' reduces capacity incidents, patch regressions, and service outages before manual triage or audit channels escalate.",
  "implementation": "Forward resolver logs into the OS index. Tune the keyword set to your resolver (not every stack logs `systemd-resolved`). Treat `NXDOMAIN` carefully: include it only when it is unexpected for your app, or split it to a separate alert with a lookup of known-pruned names.",
  "visualization": "Table, Timechart",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'DNS Resolution Failure Rate'. Correlate with apt/yum logs in the same `syslog` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `count` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `syslog`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing `failures` > 10.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux",
    "syslog"
  ],
  "equipmentModels": [
    "linux_generic",
    "syslog_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Enable forwarding of resolver logs. **systemd-resolved** often lives under the journal: export via **journald** or **rsyslog** to the same `syslog` sourcetype the TA already parses.\n\nStep 1 — Configure data collection\nAdd **host**-level filters in inputs.conf to avoid double-ingesting unrelated facilities; tag `query_name` with **SEDCMD** or transforms if the resolver only prints inside `_raw`.\n\nStep 2 — Create the search and alert\n\n```spl\nindex=os sourcetype=syslog (\"systemd-resolved\" OR \"unbound\" OR \"named\")\n| search \"SERVFAIL\" OR \"TIMEOUT\" OR (query_level=\"error\" AND \"NXDOMAIN\")\n| stats count as failures by host, query_name\n| where failures > 10\n```\n\nSimplify the middle line to the sample in the `spl` field if you only have **systemd-resolved** today.\n\n**Understanding this SPL** — Counts problem strings per **host** and **query_name**; raise threshold on busy resolvers or require `>10` per hour with `earliest` bounds.\n\n\nStep 3 — Validate\nFrom the host, run `resolvectl status` or your resolver’s test CLI; compare to `_raw` lines in Splunk at the same time. For cross-checks, use `dig` or `getent` from an admin shell during a controlled test, not in place of long-term collection.\n\nStep 4 — Operationalize\nSend to both platform DNS and the application team when `query_name` shows an app-specific suffix pattern.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_nix` — Splunk Add-on for Unix and Linux (Splunkbase 833)). Enable the modular input or HEC token that writes sourcetype=syslog into os. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`failures, host, query_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch the machine’s DNS resolution diary for a jump in “couldn’t look that up” lines, so teams can fix the phone book before apps start failing in big numbers.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
