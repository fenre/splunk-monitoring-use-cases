{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.62",
  "title": "Network Bandwidth Utilization by Interface (Linux)",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Network Bandwidth Utilization by Interface (Linux)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_network_bandwidth_utilization_by_interface_linux` (UC-1.1.62), SOC dashboard panel \"Linux — Network Bandwidth Utilization by Interface (Linux)\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=os` sourcetype='interfaces' where `host` satisfies `mbps` > 500 for UC-1.1.62 ('Network Bandwidth Utilization by Interface (Linux)'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `os` / `interfaces` events where `host` stays below `mbps` > 500, or exclude the test entity via `linux_capacity_exceptions.csv` with a valid window. Confirm zero alert rows for UC-1.1.62."
  },
  "dataSources": "`sourcetype=interfaces` (Splunk_TA_nix; cumulative `bytes_in` / `bytes_out` per interface) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=os sourcetype=interfaces host=*\n| bin _time span=5m\n| stats first(bytes_in) as s_in last(bytes_in) as e_in first(bytes_out) as s_out last(bytes_out) as e_out by host, interface, _time\n| eval dbytes_in=e_in-s_in\n| eval dbytes_out=e_out-s_out\n| eval mbps=((dbytes_in+dbytes_out)*8)/300/1000000\n| where mbps>500",
  "description": "Builds five-minute byte deltas on each interface from cumulative counters, converts the combined send+receive volume into megabits per second, and flags intervals above a high baseline (500 Mbps here—tune to your link speeds).",
  "value": "Builds five-minute byte deltas on each interface from cumulative counters, converts the combined send+recei.... Timely UC-1.1.62 alerting on 'Network Bandwidth Utilization by Interface (Linux)' reduces capacity incidents, patch regressions, and service outages before manual triage or audit channels escalate.",
  "implementation": "Keep the `interfaces` input on a short interval (≤60s) so first/last inside each five-minute bin reflects real use. Set `>500` from your p95 capacity (for example, alert when sustained above 70% of a 1 Gbps link by using `>700`).",
  "visualization": "Timechart, Heatmap",
  "cimModels": [
    "Network_Traffic"
  ],
  "cimSpl": "| tstats `summariesonly` sum(All_Traffic.bytes_in) as bi sum(All_Traffic.bytes_out) as bo\n  from datamodel=Network_Traffic.All_Traffic\n  by All_Traffic.dvc All_Traffic.src_interface span=5m\n| eval mbps=((bi+bo)*8)/300/1000000\n| where mbps>500",
  "dataModelAcceleration": "Enable acceleration for the **Network_Traffic** data model (All_Traffic dataset) and map **interfaces** into CIM with the Splunk CIM add-on so `tstats` returns rows.",
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Network_Traffic",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Network_Traffic",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'Network Bandwidth Utilization by Interface (Linux)'. Correlate with apt/yum logs in the same `interfaces` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `count` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `interfaces`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing `mbps` > 500.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux"
  ],
  "equipmentModels": [
    "linux_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• `Splunk_TA_nix` with the **interfaces** scripted input pointed at the **os** index.\n• Optional: CIM add-on to tag the sourcetype so the **tstats** block works.\n\nStep 1 — Configure data collection\nIf samples are sparser than the five-minute **bin**, narrow the bin or switch to a `timechart` of **per_second* functions on summarized metrics.\n\nStep 2 — Create the search and alert\nTuning recipe: set **mbps** threshold to `0.7 * link_gbps * 1000` for 70% util alerts once you add a `lookup` for link speed per `host`+`interface`.\n\n**Optional CIM / accelerated form** — The `cimSpl` mirrors the same **mbps** idea over **All_Traffic**; rename **All_Traffic.dvc** / **src_interface** in your build if your CIM mapping uses different interface field names (many shops alias **src_interface** to **interface**).\n\n\nStep 3 — Validate\n`ip -s link` for errors, `sar -n DEV` (if `sar` exists) for sanity on throughput, and compare peak **mbps** in Splunk to what you expect from application charts.\n\nStep 4 — Operationalize\nPair panels with the **Dropped Packets** use case in this library for the same **interface** field.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_nix` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=interfaces into os. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, host, interface, mbps`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch how much data each network card is moving in each few-minute slice, and we raise a hand when that slice is so busy it’s probably time to add speed or move work.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
