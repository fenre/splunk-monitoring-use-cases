{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.2.133",
  "title": "Windows WMI Repository Health",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Windows WMI Repository Health' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_windows_wmi_repository_health` (UC-1.2.133), SOC dashboard panel \"Linux — Windows WMI Repository Health\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-1.2.133 positive (Windows, WMI, Repository, Health): ingest synthetic events into `index=os` sourcetype='wmi_verify' where `count` grouped by `entity` satisfies detection threshold for 'Windows WMI Repository Health'. Validate filter `\"inconsistent\" OR \"corrupt\" OR \"failed\"`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-1.2.133 negative (Repository Health): ingest benign `os` / `wmi_verify` events where `entity` stays below detection threshold, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 2 confirms zero alert rows for 'Windows WMI Repository Health'."
  },
  "dataSources": "`winmgmt /verifyrepository` output Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_windows` (scripted input) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=os sourcetype=wmi_verify host=*\n| search \"inconsistent\" OR \"corrupt\" OR \"failed\"\n| table _time host _raw",
  "description": "Identifies UC-1.2.133 for Windows, WMI, Repository, Health over `index=os` sourcetype `wmi_verify` with thresholds configured threshold. Groups `count` by `entity` each `the scheduled window`. Differentiated by filter `\"inconsistent\" OR \"corrupt\" OR \"failed\"`. Supports repeatable linux triage for 'Windows WMI Repository Health'.",
  "value": "For 'Windows WMI Repository Health', UC-1.2.133 converts `activity` on `wmi_verify` feeds into actionable rows and reduces capacity incidents, patch regressions, and service outages.",
  "implementation": "Create a scripted input that runs `winmgmt /verifyrepository` and captures output. Parse for \"repository is consistent\" (success) vs \"inconsistent\" or \"corrupt\". Run daily or weekly. Alert immediately on inconsistent. Remediation: `winmgmt /resetrepository` (requires reboot). WMI issues often cause perfmon and other agent inputs to fail.",
  "visualization": "Table (host, WMI status), Single value (hosts with WMI issues), Alert.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk_TA_windows",
      "url": "https://splunkbase.splunk.com/app/742",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'Windows WMI Repository Health'. Correlate with apt/yum logs in the same `wmi_verify` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `count` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `wmi_verify`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing capacity threshold.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "windows"
  ],
  "equipmentModels": [
    "windows_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_windows` (scripted input).\n• Ensure the following data sources are available: `winmgmt /verifyrepository` output.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nCreate a scripted input that runs `winmgmt /verifyrepository` and captures output. Parse for \"repository is consistent\" (success) vs \"inconsistent\" or \"corrupt\". Run daily or weekly. Alert immediately on inconsistent. Remediation: `winmgmt /resetrepository` (requires reboot). WMI issues often cause perfmon and other agent inputs to fail.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=os sourcetype=wmi_verify host=*\n| search \"inconsistent\" OR \"corrupt\" OR \"failed\"\n| table _time host _raw\n```\n\nUnderstanding this SPL\n\n**Windows WMI Repository Health** — WMI corruption breaks many monitoring agents, SCCM, and management tools. Detecting broken WMI enables early remediation before dependent systems fail.\n\nDocumented **Data sources**: `winmgmt /verifyrepository` output. **App/TA** (typical add-on context): `Splunk_TA_windows` (scripted input). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: os; **sourcetype**: wmi_verify. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=os, sourcetype=wmi_verify. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Applies an explicit `search` filter to narrow the current result set.\n• Pipeline stage (see **Windows WMI Repository Health**): table _time host _raw\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (host, WMI status), Single value (hosts with WMI issues), Alert.\n\nScripted input (generic example)\nThis use case relies on a scripted input. In the app's local/inputs.conf add a stanza such as:\n\n```ini\n[script://$SPLUNK_HOME/etc/apps/YourApp/bin/collect.sh]\ninterval = 300\nsourcetype = your_sourcetype\nindex = main\ndisabled = 0\n```\n\nThe script should print one event per line (e.g. key=value). Example minimal script (bash):\n\n```bash\n#!/usr/bin/env bash\n# Output metrics or events, one per line\necho \"metric=value timestamp=$(date +%s)\"\n```\n\nFor full details (paths, scheduling, permissions), see the Implementation guide: docs/implementation-guide.md",
  "grandmaExplanation": "WMI corruption breaks many monitoring agents, SCCM, and management tools — so you find out before users do when something is slowing down or breaking.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
