{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.4.8",
  "title": "PCIe Link Width and Speed Degradation",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'PCIe Link Width and Speed Degradation' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_pcie_link_width_and_speed_degradation` (UC-1.4.8), SOC dashboard panel \"Linux — PCIe Link Width and Speed Degradation\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "compliance": [
    {
      "regulation": "pci-dss",
      "version": "v4.0",
      "clause": "6.3.3",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Auto-generated from legacy markdown 'Regulations' line during the per-UC migration. Requires SME review to fill in version, clause, mode, and assurance for PCI DSS.",
      "controlObjective": "Evidence that PCI DSS unknown is enforced — Splunk UC-1.4.8: PCIe Link Width and Speed Degradation.",
      "evidenceArtifact": "Saved search 'UC-1.4.8' running on lspci -vv, /sys/bus/pci/devices/*/current_link_width_speed, archived to the restricted audit_evidence index (default 7-year retention). Auto-drafted — SME review required.",
      "obligationRef": "pci-dss@v4.0#6.3.3",
      "requires_sme_review": true,
      "provenance": "maintainer"
    }
  ],
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=hardware` sourcetype='pcie_link' that satisfy the UC-1.4.8 SPL filters for 'PCIe Link Width and Speed Degradation'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `hardware` / `pcie_link` events outside the detection threshold, or exclude the test entity via `linux_capacity_exceptions.csv` with a valid window. Confirm zero alert rows for UC-1.4.8."
  },
  "dataSources": "`lspci -vv`, `/sys/bus/pci/devices/*/current_link_width_speed` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom scripted input (`lspci -vv` and sysfs on Linux; equivalent vendor PCI inventory on other OSes if used) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=hardware sourcetype=pcie_link host=*\n| stats latest(link_width) as width, latest(link_speed) as speed by host, slot\n| lookup pcie_expected host slot OUTPUT expected_width expected_speed\n| where width < expected_width OR speed < expected_speed\n| table host slot width speed expected_width expected_speed",
  "description": "PCIe links that downgrade (e.g. x16→x8) indicate slot or cable issues. Affects GPU, NVMe, and HBA performance and can precede full failure.",
  "value": "A slot that trained narrower or slower than designed hurts throughput for GPUs, NVMe, and HBAs, and is often a sign of a bad connection long before a device disappears entirely.",
  "implementation": "Parse `lspci -vv` for \"LnkCap\" and \"LnkSta\" or read sysfs. Run daily. Maintain lookup of expected width/speed per host and slot. Alert on downgrade.",
  "visualization": "Table (host, slot, current vs. expected), Bar chart of link widths.",
  "cimModels": [],
  "dataModelAcceleration": "Not applicable — no CIM data model for PCIe link width.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday / unattended-upgrades** — CPU and disk I/O spike during package application for 'PCIe Link Width and Speed Degradation'. Correlate with apt/yum logs in same window.\n\n2. **Nightly backup or log-rotate job** — Predictable I/O burst on fixed cron schedule.\n\n3. **Synthetic monitoring probe** — Nagios/Zabbix agent generates periodic load.\n\n4. **New host onboarding** — Baseline metrics unstable first 48h on `hardware` `pcie_link`.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "hardware_bmc"
  ],
  "equipmentModels": [
    "hardware_bmc_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Custom scripted input (`lspci -vv` and sysfs on Linux; vendor inventory scripts elsewhere).\n• Ensure the following data sources are available: `lspci -vv`, `/sys/bus/pci/devices/*/current_link_width_speed` (Linux typical).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nOn Linux, parse `lspci -vv` for LnkCap/LnkSta and/or read sysfs for current link width and speed per device. On non-Linux hosts, use the vendor’s PCI reporting tool to emit the same `link_width` and `link_speed` fields. Populate the `pcie_expected` lookup with per-host, per-slot expectations.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust as needed):\n\n```spl\nindex=hardware sourcetype=pcie_link host=*\n| stats latest(link_width) as width, latest(link_speed) as speed by host, slot\n| lookup pcie_expected host slot OUTPUT expected_width expected_speed\n| where width < expected_width OR speed < expected_speed\n| table host slot width speed expected_width expected_speed\n```\n\nUnderstanding this SPL\n\n**PCIe Link Width and Speed Degradation** — PCIe links that downgrade (e.g. x16→x8) indicate slot or cable issues. Affects GPU, NVMe, and HBA performance and can precede full failure.\n\n**Pipeline walkthrough**\n\n• Scopes the data: `index=hardware`, `sourcetype=pcie_link`.\n• `stats` and `lookup` compare current width/speed to `expected_*` from policy.\n\n\nStep 3 — Validate\nOn a test host, compare `lspci -vv` to indexed fields. For full details, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Custom scripted input (`lspci -vv` and sysfs on Linux; equivalent vendor PCI inventory on other OSes if used) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=pcie_link into hardware. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, slot`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We compare how wide and fast the plug-in cards’ data lanes are running to what they should be, so a loose slot or bad cable does not quietly slow down a whole server.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
