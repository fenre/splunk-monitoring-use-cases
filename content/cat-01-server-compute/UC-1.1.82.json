{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.82",
  "title": "D-State (Uninterruptible Sleep) Process Detection",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'D-State (Uninterruptible Sleep) Process Detection' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_d_state_uninterruptible_sleep_process_detection` (UC-1.1.82), SOC dashboard panel \"Linux — D-State (Uninterruptible Sleep) Process Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-1.1.82 positive (State, (Uninterruptible, Sleep), Process, Detection): ingest synthetic events into `index=os` sourcetype='ps' where `count` grouped by `host` satisfies `dstate_count` > 0 for 'D-State (Uninterruptible Sleep) Process Detection'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-1.1.82 negative (Process Detection): ingest benign `os` / `ps` events where `host` stays below `dstate_count` > 0, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 10 confirms zero alert rows for 'D-State (Uninterruptible Sleep) Process Detection'."
  },
  "dataSources": "`sourcetype=ps` (Splunk_TA_nix process snapshot with a `state` / `S` field) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=os sourcetype=ps host=* (S=\"D\" OR state=\"D\")\n| stats count as dstate_count by host\n| where dstate_count>0",
  "description": "Monitors UC-1.1.82 — State, (Uninterruptible, Sleep), Process, Detection — using `index=os` sourcetype `ps`, grouped by `host`. Outputs `count` each `the scheduled window`; alert when `dstate_count` > 0. Primary focus: Process Detection. Purpose-built for 'D-State (Uninterruptible Sleep) Process Detection' in Linux workflows.",
  "value": "For 'D-State (Uninterruptible Sleep) Process Detection', UC-1.1.82 converts `activity` on `ps` feeds into actionable rows and reduces capacity incidents, patch regressions, and service outages.",
  "implementation": "Use the `S` or `state` field your **ps** sourcetype actually ships; add `| where dstate_count>5` if a single **D** from a **df** snapshot is normal on your **NFS** clients.",
  "visualization": "Alert, Table",
  "cimModels": [
    "Endpoint.Processes",
    "Endpoint"
  ],
  "cimSpl": "| tstats `summariesonly` count from datamodel=Endpoint.Processes where Processes.process_state=\"D\" by Processes.dest Processes.process_name span=5m | where count>0",
  "dataModelAcceleration": "After `ps` is CIM-mapped into **Endpoint.Processes** with **process_state**, enable Endpoint model acceleration.",
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Endpoint",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Endpoint",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'D-State (Uninterruptible Sleep) Process Detection'. Correlate with apt/yum logs in the same `ps` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `count` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `ps`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing `dstate_count` > 0.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux"
  ],
  "equipmentModels": [
    "linux_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• **ps** script on a **5**-**15** minute cadence; faster if you need near-real-time **D** detection for trading stacks.\n\n**CIM** — **process_state** naming may differ (`D` vs `uninterruptible`); align `cimSpl` after you see **Fieldsummary** on `Processes`.\n\n\nStep 3 — Validate\n`ps -o pid,stat,wchan,cmd` (or `top` then **H** for threads) on the host; use `echo w > /proc/sysrq-trigger` only with an operator and a runbook, not casually.\n\nStep 4 — Operationalize\nOpen a **storage** bridge with **dmesg** **I/O** errors the same minute.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_nix` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=ps into os. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`dstate_count, host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We help you know when programs are stuck waiting on the disk or network in a way they can’t be interrupted, which is a strong hint to look at storage before blaming the app.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
