{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.52",
  "title": "Connection Tracking Table Exhaustion",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Connection Tracking Table Exhaustion' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_connection_tracking_table_exhaustion` (UC-1.1.52), SOC dashboard panel \"Linux — Connection Tracking Table Exhaustion\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=os` sourcetype='custom:conntrack' that satisfy the UC-1.1.52 SPL filters for 'Connection Tracking Table Exhaustion'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `os` / `custom:conntrack` events outside the detection threshold, or exclude the test entity via `linux_capacity_exceptions.csv` with a valid window. Confirm zero alert rows for UC-1.1.52."
  },
  "dataSources": "`sourcetype=custom:conntrack` (samples of `nf_conntrack_count` vs `nf_conntrack_max`) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix` with a custom scripted input for conntrack counters — Universal Forwarder (Splunkbase 617)",
  "spl": "index=os sourcetype=custom:conntrack host=*\n| stats latest(current_count) as current, latest(max_size) as maximum by host\n| eval usage_pct=(current/maximum)*100\n| where usage_pct > 80",
  "description": "Detects when the netfilter connection tracking table on a host is most of the way to its hard maximum, a condition that will soon block or drop new flow setups.",
  "value": "Avoiding a full conntrack table protects stateful firewalls, NAT, and any service that cannot accept “cannot allocate new connection” style failures during busy periods.",
  "implementation": "Sample `current_count` and `max_size` (from `/proc/sys/net/netfilter/nf_conntrack_max` or equivalent) into Splunk, compute utilization, and page before you reach the drop regime above roughly ninety percent in most shops.",
  "visualization": "Gauge, Alert",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday / unattended-upgrades** — CPU and disk I/O spike during package application for 'Connection Tracking Table Exhaustion'. Correlate with apt/yum logs in same window.\n\n2. **Nightly backup or log-rotate job** — Predictable I/O burst on fixed cron schedule.\n\n3. **Synthetic monitoring probe** — Nagios/Zabbix agent generates periodic load.\n\n4. **New host onboarding** — Baseline metrics unstable first 48h on `os` `custom:conntrack`.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux"
  ],
  "equipmentModels": [
    "linux_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Deploy a small script (often paired with the TA) that reads `nf_conntrack_count` and the configured max, then posts **key=value** lines to Splunk on a 60 second cadence.\n\nStep 1 — Configure data collection\nKeep units consistent: both counts must be in the same integer space. Re-read max after sysctls or boot.\n\nStep 2 — Create the search and alert\nThe SPL in the `spl` field is self-contained; set separate alerts at 80% (warn) and 95% (page) if you prefer two saved searches.\n\n\nStep 3 — Validate\nOn host, compare `cat /proc/sys/net/netfilter/nf_conntrack_count` to the latest event; ensure `ss -s` or `conntrack -S` (where available) does not show divergent health.\n\nStep 4 — Operationalize\nRunbook: raise `nf_conntrack_max` with capacity review, reduce wasteful long-lived connections, or shard workloads.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_nix` with a custom scripted input for conntrack counters — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=custom:conntrack into os. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, usage_pct`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We help you know when a safety table that tracks open network connections is almost full, so new connections can still get through when things get busy.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
