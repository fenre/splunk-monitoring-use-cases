{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.80",
  "title": "Systemd Unit Failures",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "dataSources": "`sourcetype=syslog` (systemd journal forwarded or rsyslog facility lines)",
  "app": "`Splunk_TA_nix`",
  "spl": "index=os sourcetype=syslog \"systemd\" (\"Failed\" OR \"ERROR\" OR \"not-found\")\n| stats count by host, unit\n| where count>0",
  "description": "Counts **systemd** log lines that mention failure, error, or missing unit files, grouped by **host** and **unit** field (when extracted) or by **host** alone if **unit** is still in `_raw`.",
  "value": "Service **active** state in **systemd** is the source of truth for many apps; log lines here are a quick backstop when you have not yet exported full **systemd** JSON to Splunk.",
  "implementation": "Add `REX` for `unit=` or the **User=** / **Subject:** lines your OS prints. Remove `not-found` if **first boot** noise is too high in your cloud image factory.",
  "visualization": "Alert, Table",
  "cimModels": [
    "N/A"
  ],
  "knownFalsePositives": "Short spikes during **package** upgrades; units that **restart** often and log `Failed` even when **systemd** eventually recovers—tighten with `| where count>5` in 15**m**; multi-line events split wrong in **props**.",
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833"
    },
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/"
    }
  ],
  "equipment": [
    "linux",
    "syslog"
  ],
  "detailedImplementation": "Prerequisites\n• Forward **journal** for **systemd** units of interest, or use the **UF** `journald` input where available on your OS version.\n\n**CIM** — Not applicable; these are **Availability** events, not **Authentication**.\n\n\nStep 3 — Validate\n`systemctl status unit` and `journalctl -u unit --since` on the host for the same minute you see in Splunk. Use `systemd-analyze blame` only for slow boot, not every **Failed** line.\n\nStep 4 — Operationalize\nPair with the **D-state** and **memory** use cases when a unit failure is just a symptom of resource exhaustion.\n\n",
  "grandmaExplanation": "We help you know when the built-in service manager keeps saying it could not start or find an important service, so you can fix the app or the machine before users feel it."
}
