{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.4.10",
  "title": "Disk Controller and HBA Health",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Disk Controller and HBA Health' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_disk_controller_and_hba_health` (UC-1.4.10), SOC dashboard panel \"Linux — Disk Controller and HBA Health\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=hardware` sourcetype='raid_controller' that satisfy the UC-1.4.10 SPL filters for 'Disk Controller and HBA Health'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `hardware` / `raid_controller` events outside the detection threshold, or exclude the test entity via `linux_capacity_exceptions.csv` with a valid window. Confirm zero alert rows for UC-1.4.10."
  },
  "dataSources": "Vendor CLI output (e.g. `MegaCli64 -AdpAllInfo -aAll`), `/proc/scsi/` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "Custom scripted input (MegaRAID, perccli, hpssacli) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=hardware sourcetype=raid_controller host=*\n| stats latest(controller_status) as status, latest(degraded_virtual_drives) as degraded by host, controller_id\n| where status != \"Optimal\" OR degraded > 0\n| table host controller_id status degraded",
  "description": "RAID/HBA controller errors and degraded state often precede array failure. Early visibility enables planned maintenance and avoids data loss.",
  "value": "The controller is the traffic cop for the disks; if it reports a bad state or degraded virtual drives, you want a ticket while data is still recoverable, not when volumes go offline together.",
  "implementation": "Run vendor CLI (MegaCli, perccli, hpssacli) via scripted input every 15 minutes. Parse controller and virtual drive state. Alert when status is not Optimal or any array is degraded.",
  "visualization": "Status panel (Optimal/Degraded/Failed), Table of degraded arrays.",
  "cimModels": [],
  "dataModelAcceleration": "Not applicable — no CIM data model for RAID controller health.",
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday / unattended-upgrades** — CPU and disk I/O spike during package application for 'Disk Controller and HBA Health'. Correlate with apt/yum logs in same window.\n\n2. **Nightly backup or log-rotate job** — Predictable I/O burst on fixed cron schedule.\n\n3. **Synthetic monitoring probe** — Nagios/Zabbix agent generates periodic load.\n\n4. **New host onboarding** — Baseline metrics unstable first 48h on `hardware` `raid_controller`.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "hardware_bmc"
  ],
  "equipmentModels": [
    "hardware_bmc_megacli",
    "hardware_bmc_perccli",
    "hardware_bmc_ssacli"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Custom scripted input (MegaRAID, perccli, hpssacli).\n• Ensure the following data sources are available: Vendor CLI output (e.g. `MegaCli64 -AdpAllInfo -aAll`), `/proc/scsi/` on Linux for context if needed.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nOn Linux, install the vendor’s CLI in the same image as the forwarder or call it over SSH. Run every 15 minutes and emit `controller_status` and `degraded_virtual_drives` (or your naming) per `controller_id` and `host`.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust healthy strings to your vendor):\n\n```spl\nindex=hardware sourcetype=raid_controller host=*\n| stats latest(controller_status) as status, latest(degraded_virtual_drives) as degraded by host, controller_id\n| where status != \"Optimal\" OR degraded > 0\n| table host controller_id status degraded\n```\n\nUnderstanding this SPL\n\n**Disk Controller and HBA Health** — RAID/HBA controller errors and degraded state often precede array failure. Early visibility enables planned maintenance and avoids data loss.\n\n**Pipeline walkthrough**\n\n• Scopes the data: `index=hardware`, `sourcetype=raid_controller`.\n• `stats` takes latest controller and degraded counts per `host` and `controller_id`.\n• `where` flags not-`Optimal` or any `degraded` &gt; 0 in the sample.\n\n\nStep 3 — Validate\nOn a test host, run the vendor CLI and compare to indexed `status` and `degraded`. For full details, see the Implementation guide: docs/implementation-guide.md",
  "grandmaExplanation": "We look at the card that runs your disk storage so you know when it is unhappy or a disk group is in trouble before a full storage failure.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
