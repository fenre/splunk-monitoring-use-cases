{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.95",
  "title": "TCP Connection Establishment Rate",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'TCP Connection Establishment Rate' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_tcp_connection_establishment_rate` (UC-1.1.95), SOC dashboard panel \"Linux — TCP Connection Establishment Rate\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-1.1.95 positive (TCP, Connection, Establishment, Rate): ingest synthetic events into `index=os` sourcetype='custom:netstat_stats' where `avg_active`, `baseline`, `stddev` grouped by `host` satisfies detection threshold for 'TCP Connection Establishment Rate'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-1.1.95 negative (Establishment Rate): ingest benign `os` / `custom:netstat_stats` events where `host` stays below detection threshold, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 7 confirms zero alert rows for 'TCP Connection Establishment Rate'."
  },
  "dataSources": "`sourcetype=custom:netstat_stats, /proc/net/snmp` Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix, custom scripted input` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=os sourcetype=custom:netstat_stats host=*\n| stats avg(TcpActiveOpens) as avg_active by host\n| streamstats avg(avg_active) as baseline, stdev(avg_active) as stddev\n| where avg_active > baseline + 3*stddev",
  "description": "Surfaces UC-1.1.95 — TCP, Connection, Establishment, Rate — using `index=os` sourcetype `custom:netstat_stats`, grouped by `host`. Outputs `avg_active`, `baseline`, `stddev` each `the scheduled window`; alert when configured threshold. Primary focus: Establishment Rate. Purpose-built for 'TCP Connection Establishment Rate' in Linux workflows.",
  "value": "UC-1.1.95 operationalises 'TCP Connection Establishment Rate' against `custom:netstat_stats` feeds so operators reduce capacity incidents, patch regressions, and service outages without manual correlation.",
  "implementation": "Monitor TcpActiveOpens from /proc/net/snmp. Track baseline and detect anomalies. Create alerts for sustained elevation in connection rate indicating potential DDoS or application issues.",
  "visualization": "Timechart, Anomaly Chart",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'TCP Connection Establishment Rate'. Correlate with apt/yum logs in the same `custom:netstat_stats` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `avg` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `custom:netstat_stats`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing capacity threshold.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux",
    "snmp"
  ],
  "equipmentModels": [
    "snmp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_nix, custom scripted input`.\n• Ensure the following data sources are available: `sourcetype=custom:netstat_stats, /proc/net/snmp`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMonitor TcpActiveOpens from /proc/net/snmp. Track baseline and detect anomalies. Create alerts for sustained elevation in connection rate indicating potential DDoS or application issues.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=os sourcetype=custom:netstat_stats host=*\n| stats avg(TcpActiveOpens) as avg_active by host\n| streamstats avg(avg_active) as baseline, stdev(avg_active) as stddev\n| where avg_active > baseline + 3*stddev\n```\n\nUnderstanding this SPL\n\n**TCP Connection Establishment Rate** — High connection establishment rate may indicate application behavior changes or DDoS attack preparation.\n\nDocumented **Data sources**: `sourcetype=custom:netstat_stats, /proc/net/snmp`. **App/TA** (typical add-on context): `Splunk_TA_nix, custom scripted input`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: os; **sourcetype**: custom:netstat_stats. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=os, sourcetype=custom:netstat_stats. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by host** so each row reflects one combination of those dimensions.\n• `streamstats` aggregates the pipeline (counts, distinct values, sums, percentiles, etc.) into fewer rows.\n• Filters the current rows with `where avg_active > baseline + 3*stddev` — typically the threshold or rule expression for this monitoring goal.\n\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Timechart, Anomaly Chart",
  "grandmaExplanation": "We track how fast the system opens new TCP connections so we notice traffic spikes that might mean a misbehaving app or an early-stage flood.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
