{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.58",
  "title": "Network Bond Failover Events (Linux)",
  "criticality": "critical",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Network Bond Failover Events (Linux)' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_network_bond_failover_events_linux` (UC-1.1.58), SOC dashboard panel \"Linux — Network Bond Failover Events (Linux)\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-1.1.58 positive (Network, Bond, Failover, Events, (Linux)): ingest synthetic events into `index=os` sourcetype='syslog' where `count` grouped by `host` satisfies detection threshold for 'Network Bond Failover Events (Linux)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-1.1.58 negative (Events (Linux)): ingest benign `os` / `syslog` events where `host` stays below detection threshold, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 2 confirms zero alert rows for 'Network Bond Failover Events (Linux)'."
  },
  "dataSources": "`sourcetype=syslog` (kernel bonding driver messages) Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix` — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=os sourcetype=syslog \"bonding:\" (\"slave\" OR \"primary\") (\"failed\" OR \"recovering\" OR \"detected\")\n| timechart count by host",
  "description": "Monitors UC-1.1.58 for Network, Bond, Failover, Events, (Linux) over `index=os` sourcetype `syslog` with thresholds configured threshold. Groups `count` by `host` each `the scheduled window`. Centered on Events (Linux) indicators. Supports repeatable linux triage for 'Network Bond Failover Events (Linux)'.",
  "value": "For 'Network Bond Failover Events (Linux)', UC-1.1.58 converts `activity` on `syslog` feeds into actionable rows and reduces capacity incidents, patch regressions, and service outages.",
  "implementation": "Kernel bonding logs to **syslog** by default on many distros. Keep `bonding` module logging at a useful level; for Dashboard Studio, convert the timechart to a thresholded alert on spikes vs baseline, or add `| where count>10` in a tstats of the underlying search in a subsearch pattern—here we keep a simple explorer **timechart** you can save as a report and clone into an alert with `anomalydetection` if you standardize on that in your org.",
  "visualization": "Alert, Timechart",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'Network Bond Failover Events (Linux)'. Correlate with apt/yum logs in the same `syslog` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `count` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `syslog`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing capacity threshold.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux",
    "syslog"
  ],
  "equipmentModels": [
    "linux_generic",
    "syslog_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Forward **kern**-priority and driver messages to the same `syslog` path your OS index already uses.\n\nStep 1 — Configure data collection\nIf nothing arrives, on-box run `dmesg -w` and confirm bonding strings while unplugging a lab cable; add **rsyslog** `imklog` or **journald** forwarding for those facilities.\n\nStep 2 — Create the report or alert\nThe SPL ends at **timechart**; to alert, consider wrapping a **per-host** `bucket` in five-minute bins and `where count>threshold`, or an **anomalydetection** command on the series.\n\n**Understanding this SPL** — String match on the bonding log lines you care about; add `bond0`-style interface filters if you multi-bond a host and only want one of them.\n\n\nStep 3 — Validate\nOn host, `cat /proc/net/bonding/bond0` (interface name as deployed) to see live `MII Status`, then correlate timestamps with Splunk. Use `ethtool` on each slave NIC for port-level proof.\n\nStep 4 — Operationalize\nPage network operations first, then system owners; add switch port to incident tickets while links are down.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_nix` — Splunk Add-on for Unix and Linux (Splunkbase 833)). Enable the modular input or HEC token that writes sourcetype=syslog into os. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We help you know when a server’s paired network lines stop taking turns nicely, so someone can check cables and switches before the last good line goes bad too.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
