{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.2.106",
  "title": "Local Administrator Group Membership Changes",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Local Administrator Group Membership Changes' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_local_administrator_group_membership_changes` (UC-1.2.106), SOC dashboard panel \"Linux — Local Administrator Group Membership Changes\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-1.2.106 positive (Local, Administrator, Group, Membership, Changes): ingest synthetic events into `index=wineventlog` sourcetype='target_sourcetype' EventCode=4732 where `count` grouped by `entity` satisfies detection threshold for 'Local Administrator Group Membership Changes'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-1.2.106 negative (Membership Changes): ingest benign `wineventlog` / `target_sourcetype` events where `entity` stays below detection threshold, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 0 confirms zero alert rows for 'Local Administrator Group Membership Changes'."
  },
  "dataSources": "`sourcetype=WinEventLog:Security` (EventCode 4732, 4733) Collected via Splunk Add-on for Microsoft Windows (Splunkbase 742) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_windows` — Splunk Add-on for Microsoft Windows (Splunkbase 742)",
  "spl": "index=wineventlog EventCode IN (4732, 4733) TargetUserName=\"Administrators\"\n| eval Action=case(EventCode=4732,\"Member_Added\", EventCode=4733,\"Member_Removed\", 1=1,\"Other\")\n| table _time, host, Action, MemberName, MemberSid, SubjectUserName, SubjectDomainName\n| sort -_time",
  "description": "Surfaces 'Local Administrator Group Membership Changes' (UC-1.2.106) using `index=wineventlog` sourcetype `configured sourcetype` by aggregating `count` grouped by `entity` over `the scheduled window` and alerting when configured threshold. Keywords: Local, Administrator, Group, Membership, Changes. Focuses on Membership Changes behaviour so linux teams can triage UC-1.2.106 without ad-hoc log review.",
  "value": "UC-1.2.106 accelerates insight into Membership Changes ('Local Administrator Group Membership Changes') by tracking `activity` on UC-1.2.106 search results, reducing capacity incidents, patch regressions, and service outages before spreadsheet checks hide regressions.",
  "implementation": "Enable Audit Security Group Management. Track additions (4732) and removals (4733) from the local Administrators group. Alert on any additions, especially by non-domain-admin accounts. Monitor for patterns: add user → perform action → remove user (cleanup). Correlate with LAPS password rotations and PAM solutions. On servers, local admin changes should be extremely rare.",
  "visualization": "Table (membership changes), Alert on all additions, Trend chart.",
  "cimModels": [
    "Authentication",
    "Change"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Change.All_Changes\n  by All_Changes.user All_Changes.dest span=1h\n| where count > 0",
  "dataModelAcceleration": "Enable acceleration for Change/Authentication for 4728/4732/4756 and related group membership (when CIM-tagged from Security).",
  "references": [
    {
      "title": "Enable Audit Security Group Management. Track additions",
      "url": "https://splunkbase.splunk.com/app/4732",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk_TA_windows",
      "url": "https://splunkbase.splunk.com/app/742",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Authentication",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Authentication",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `wineventlog`** — Package managers spike `host` metrics for 'Local Administrator Group Membership Changes'. Correlate with apt/yum logs in the same `cpu` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `count` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `cpu`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing capacity threshold.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "windows"
  ],
  "equipmentModels": [
    "windows_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_windows`.\n• Ensure the following data sources are available: `sourcetype=WinEventLog:Security` (EventCode 4732, 4733).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnable Audit Security Group Management. Track additions (4732) and removals (4733) from the local Administrators group. Alert on any additions, especially by non-domain-admin accounts. Monitor for patterns: add user → perform action → remove user (cleanup). Correlate with LAPS password rotations and PAM solutions. On servers, local admin changes should be extremely rare.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=wineventlog EventCode IN (4732, 4733) TargetUserName=\"Administrators\"\n| eval Action=case(EventCode=4732,\"Member_Added\", EventCode=4733,\"Member_Removed\", 1=1,\"Other\")\n| table _time, host, Action, MemberName, MemberSid, SubjectUserName, SubjectDomainName\n| sort -_time\n```\n\nUnderstanding this SPL\n\n**Local Administrator Group Membership Changes** — Local admin privileges enable credential theft, persistence, and lateral movement. Monitoring local admin group changes detects privilege escalation attacks.\n\nDocumented **Data sources**: `sourcetype=WinEventLog:Security` (EventCode 4732, 4733). **App/TA** (typical add-on context): `Splunk_TA_windows`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: wineventlog.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=wineventlog. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **Action** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Pipeline stage (see **Local Administrator Group Membership Changes**): table _time, host, Action, MemberName, MemberSid, SubjectUserName, SubjectDomainName\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` count\n  from datamodel=Authentication.Authentication\n  where Authentication.action=success\n  by Authentication.user Authentication.src Authentication.dest span=1h\n| search Authentication.user=*admin* OR Authentication.user=root\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Local Administrator Group Membership Changes** — Local admin privileges enable credential theft, persistence, and lateral movement. Monitoring local admin group changes detects privilege escalation attacks.\n\nDocumented **Data sources**: `sourcetype=WinEventLog:Security` (EventCode 4732, 4733). **App/TA** (typical add-on context): `Splunk_TA_windows`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Authentication.Authentication` — enable acceleration for that model.\n• Applies an explicit `search` filter to narrow the current result set.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (membership changes), Alert on all additions, Trend chart.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_windows` — Splunk Add-on for Microsoft Windows (Splunkbase 742)). Enable the modular input or HEC token that writes sourcetype=see SPL into wineventlog. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`Action`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We catch changes to the local administrators group on Windows servers, because that is still a quick way for anyone with a foothold to hand themselves full control on the box.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
