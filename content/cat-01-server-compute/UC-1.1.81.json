{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.81",
  "title": "Systemd Timer Missed Triggers",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Systemd Timer Missed Triggers' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_systemd_timer_missed_triggers` (UC-1.1.81), SOC dashboard panel \"Linux — Systemd Timer Missed Triggers\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-1.1.81 positive (Systemd, Timer, Missed, Triggers): ingest synthetic events into `index=os` sourcetype='syslog' where `count` grouped by `host`, `timer_unit` satisfies `count` > 0 for 'Systemd Timer Missed Triggers'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-1.1.81 negative (Missed Triggers): ingest benign `os` / `syslog` events where `host`, `timer_unit` stays below `count` > 0, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 0 confirms zero alert rows for 'Systemd Timer Missed Triggers'."
  },
  "dataSources": "`sourcetype=syslog` Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix` — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=os sourcetype=syslog \"systemd\" \"timer\" (\"cannot run\" OR \"Skipping\")\n| stats count by host, timer_unit\n| where count>0",
  "description": "Detects 'Systemd Timer Missed Triggers' (UC-1.1.81) using `index=os` sourcetype `syslog` by aggregating `count` grouped by `host`, `timer_unit` over `the scheduled window` and alerting when `count` > 0. Keywords: Systemd, Timer, Missed, Triggers. Focuses on Missed Triggers behaviour so linux teams can triage UC-1.1.81 without ad-hoc log review.",
  "value": "UC-1.1.81 accelerates insight into Missed Triggers ('Systemd Timer Missed Triggers') by tracking `activity` on `syslog` feeds, reducing capacity incidents, patch regressions, and service outages before spreadsheet checks hide regressions.",
  "implementation": "Some distros log these at **notice** not **err**—verify your **syslog** facility filter is not dropping them. Add `| where count>1` to avoid one-off clock skew noise.",
  "visualization": "Alert, Table",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'Systemd Timer Missed Triggers'. Correlate with apt/yum logs in the same `syslog` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `count` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `syslog`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing `count` > 0.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux",
    "syslog"
  ],
  "equipmentModels": [
    "linux_generic",
    "syslog_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Timers defined in `/etc/systemd/system` or **vendor** trees, with **Persistent=true** where you need catch-up after downtime.\n\n\nStep 3 — Validate\n`systemctl list-timers --all` and `journalctl -u mytimer.timer` on the host; compare **NEXT** / **LEFT** with the Splunk timestamp.\n\nStep 4 — Operationalize\nWhen a timer is **missed**, check **OnCalendar=** vs actual host load first, not only the app.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_nix` — Splunk Add-on for Unix and Linux (Splunkbase 833)). Enable the modular input or HEC token that writes sourcetype=syslog into os. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, timer_unit`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We help you know when a scheduled “do this later” task on the machine did not run when it should, which can quietly break backups or clean-up jobs.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
