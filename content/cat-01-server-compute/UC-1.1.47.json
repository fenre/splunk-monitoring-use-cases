{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.47",
  "title": "Page Cache Pressure and Reclaim Activity",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'Page Cache Pressure and Reclaim Activity'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-1.1.47).",
  "evidence": "Saved search uc_1_1_47_page_cache_pressure_and_reclaim_activity, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'Page Cache Pressure and Reclaim Activity'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-1.1.47.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "`sourcetype=custom:meminfo_delta` (or vmstat-class feed) with `pgscan_direct` and `pgsteal_direct` from `/proc/vmstat` deltas Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix` and a small scripted input for `/proc/vmstat` deltas — Universal Forwarder (Splunkbase 617)",
  "spl": "index=os sourcetype=custom:meminfo_delta host=*\n| stats avg(pgscan_direct) as scan_avg, avg(pgsteal_direct) as steal_avg by host\n| eval steal_ratio=if(scan_avg>0, steal_avg/scan_avg, null())\n| where scan_avg>0 AND steal_ratio > 0.7",
  "description": "Flags hosts where the kernel is spending a high share of direct reclaim scans on successful steals, a sign the system is under real memory pressure and stealing pages from the page cache aggressively.",
  "value": "Spotting reclaim-heavy periods early helps you add RAM, tune workloads, or fix leaks before latency spreads to databases, batch jobs, and user-facing services on the same box.",
  "implementation": "Ship time-delta’d `/proc/vmstat` metrics as `pgscan_direct` and `pgsteal_direct`. Alert when the ratio of steals to scans stays above 0.7 for the analysis window, indicating the host is doing heavy direct reclaim.",
  "visualization": "Timechart, Single Value",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "linux"
  ],
  "equipmentModels": [
    "linux_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install `Splunk_TA_nix` and add a scripted input (or an extension) that records `/proc/vmstat` on an interval and emits **delta** values for `pgscan_direct` and `pgsteal_direct` or absolute counters your SPL differences correctly.\n• Ensure the fields used in the search exist in the chosen sourcetype.\n• See docs/implementation-guide.md for inputs.conf layout.\n\nStep 1 — Configure data collection\nParse `/proc/vmstat` on a fixed schedule; compute per-interval deltas in the script or in SPL before this alert. Point events at `index=os` and your agreed sourcetype (here `custom:meminfo_delta`).\n\nStep 2 — Create the search and alert\n\n```spl\nindex=os sourcetype=custom:meminfo_delta host=*\n| stats avg(pgscan_direct) as scan_avg, avg(pgsteal_direct) as steal_avg by host\n| eval steal_ratio=if(scan_avg>0, steal_avg/scan_avg, null())\n| where steal_ratio > 0.7\n```\n\n**Understanding this SPL** — Averages the delta rates per host, forms `steal_ratio`, and flags hosts where a large share of direct scans result in a steal, which is typical of memory pressure. Add `scan_avg>0` in the `where` if you want to drop idle hosts.\n\n\nStep 3 — Validate\nOn the host, run `vmstat 1` and watch `si`/`so` and overall memory behavior; use `cat /proc/vmstat | egrep 'pgsteal|pgscan'` to sanity-check the same counters your script ships.\n\nStep 4 — Operationalize\nChart steal ratio on a host dashboard, tie alerts to the owning app team, and document when to add RAM versus tune application cache settings.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_nix` and a small scripted input for `/proc/vmstat` deltas — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=custom:meminfo_delta into os. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, scan_avg, steal_ratio`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We help you know when the server is so tight on memory that it keeps reclaiming file cache, which is often when things start feeling slow for everyone on that machine.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
