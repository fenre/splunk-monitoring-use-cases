{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.77",
  "title": "Unauthorized Cron Job Additions",
  "criticality": "critical",
  "difficulty": "beginner",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Unauthorized Cron Job Additions' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_unauthorized_cron_job_additions` (UC-1.1.77), SOC dashboard panel \"Linux — Unauthorized Cron Job Additions\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-1.1.77 positive (Unauthorized, Cron, Job, Additions): ingest synthetic events into `index=os` sourcetype='linux_audit' where `count` grouped by `host`, `auid`, `file_name` satisfies `count` > 0 for 'Unauthorized Cron Job Additions'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-1.1.77 negative (Job Additions): ingest benign `os` / `linux_audit` events where `host`, `auid`, `file_name` stays below `count` > 0, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 7 confirms zero alert rows for 'Unauthorized Cron Job Additions'."
  },
  "dataSources": "`sourcetype=linux_audit` (watch `/var/spool/cron/*` and `/etc/cron.*`) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix` — Universal Forwarder (Splunkbase 617)",
  "spl": "index=os sourcetype=linux_audit path~=\"/var/spool/cron\" OR path~=\"/etc/cron\"\n| where match(action, \"(modified|created)\") OR nametype=create\n| stats count by host, auid, file_name\n| where count>0",
  "description": "Surfaces UC-1.1.77 — Unauthorized, Cron, Job, Additions — using `index=os` sourcetype `linux_audit`, grouped by `host`, `auid`, `file_name`. Outputs `count` each `the scheduled window`; alert when `count` > 0. Primary focus: Job Additions. Purpose-built for 'Unauthorized Cron Job Additions' in Linux workflows.",
  "value": "UC-1.1.77 operationalises 'Unauthorized Cron Job Additions' against `linux_audit` feeds so operators reduce capacity incidents, patch regressions, and service outages without manual correlation.",
  "implementation": "Paths differ per distro (`/var/spool/cron/crontabs` vs `cron/tabs`). Broaden the `path~` until your coverage matches your **CMDB**; add `| lookup` for package-managed cron files in `/etc/cron.d` once you have an inventory.",
  "visualization": "Alert, Table",
  "cimModels": [
    "Change"
  ],
  "cimSpl": "| tstats `summariesonly` count from datamodel=Change.All_Changes where All_Changes.action IN (\"created\", \"modified\") AND match(All_Changes.object, \".*cron.*\") by All_Changes.user All_Changes.dest span=1d | where count>0",
  "dataModelAcceleration": "Enable the **Change** data model for mapped FIM+audit file events under cron paths.",
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Change",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Change",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'Unauthorized Cron Job Additions'. Correlate with apt/yum logs in the same `linux_audit` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `count` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `linux_audit`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing `count` > 0.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux"
  ],
  "equipmentModels": [
    "linux_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• `auditd` with watches that cover all **cron** drop directories you use, including per-user `crontab -e` locations.\n\n**SPL** — `match` on `action` assumes your **KEY** sets **action=**; if you only have **type=PATH**, rephrase the filter.\n\n\nStep 3 — Validate\n`ls -l` the path on host and `crontab -l -u` for the user. Compare the **auid** to **sudoreplay** or **btmp** for interactive proof when needed (never on production without a change).\n\nStep 4 — Operationalize\nAny **.sh** in **cron** that lives under `/tmp` is almost always a red flag in regulated environments.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_nix` — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=linux_audit into os. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`auid, file_name, host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We help you know when the hidden clock-driven task list is edited, which is a classic place for a bad actor to leave something running in the background.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
