{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.104",
  "title": "Thermal Throttling Detection",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Thermal Throttling Detection' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_thermal_throttling_detection` (UC-1.1.104), SOC dashboard panel \"Linux — Thermal Throttling Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-1.1.104 positive (Thermal, Throttling, Detection): ingest synthetic events into `index=os` sourcetype='syslog' where `count` grouped by `host` satisfies `count` > 0 for 'Thermal Throttling Detection'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-1.1.104 negative (Throttling Detection): ingest benign `os` / `syslog` events where `host` stays below `count` > 0, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 8 confirms zero alert rows for 'Thermal Throttling Detection'."
  },
  "dataSources": "`sourcetype=syslog, kernel thermal logs` Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix, custom scripted input` — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=os sourcetype=syslog (\"thermal throttling\" OR \"cpu clock throttled\" OR \"cpu thermal\" OR \"THERMAL_EVENT\")\n| stats count by host\n| where count > 0",
  "description": "Surfaces UC-1.1.104 — Thermal, Throttling, Detection — using `index=os` sourcetype `syslog`, grouped by `host`. Outputs `count` each `the scheduled window`; alert when `count` > 0. Primary focus: Throttling Detection. Purpose-built for 'Thermal Throttling Detection' in Linux workflows.",
  "value": "UC-1.1.104 accelerates insight into Throttling Detection ('Thermal Throttling Detection') by tracking `activity` on `syslog` feeds, reducing capacity incidents, patch regressions, and service outages before spreadsheet checks hide regressions.",
  "implementation": "Monitor kernel thermal throttling messages. Create alerts on any throttling events. Include thermal zone temperatures and recommendations for cooling investigation.",
  "visualization": "Alert, Table",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 833",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'Thermal Throttling Detection'. Correlate with apt/yum logs in the same `syslog` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `count` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `syslog`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing `count` > 0.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux",
    "syslog"
  ],
  "equipmentModels": [
    "linux_generic",
    "syslog_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_nix, custom scripted input`.\n• Ensure the following data sources are available: `sourcetype=syslog, kernel thermal logs`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMonitor kernel thermal throttling messages. Create alerts on any throttling events. Include thermal zone temperatures and recommendations for cooling investigation.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=os sourcetype=syslog (\"thermal throttling\" OR \"cpu clock throttled\" OR \"cpu thermal\" OR \"THERMAL_EVENT\")\n| stats count by host\n| where count > 0\n```\n\nUnderstanding this SPL\n\n**Thermal Throttling Detection** — Thermal throttling reduces CPU performance to prevent overheating, indicating cooling system issues.\n\nDocumented **Data sources**: `sourcetype=syslog, kernel thermal logs`. **App/TA** (typical add-on context): `Splunk_TA_nix, custom scripted input`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: os; **sourcetype**: syslog. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=os, sourcetype=syslog. Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by host** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where count > 0` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Alert, Table",
  "grandmaExplanation": "We alert when the kernel says the CPU had to slow down for heat so we can fix fans, airflow, or paste before performance tanks.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
