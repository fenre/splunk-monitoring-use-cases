{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.86",
  "title": "Fork Bomb Detection",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Fork Bomb Detection' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_fork_bomb_detection` (UC-1.1.86), SOC dashboard panel \"Linux — Fork Bomb Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-1.1.86 positive (Fork, Bomb, Detection): ingest synthetic events into `index=os` sourcetype='custom:process_count' where `avg_procs`, `stddev` grouped by `host` satisfies `stddev` > 0 for 'Fork Bomb Detection'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-1.1.86 negative (Bomb Detection): ingest benign `os` / `custom:process_count` events where `host` stays below `stddev` > 0, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 0 confirms zero alert rows for 'Fork Bomb Detection'."
  },
  "dataSources": "`sourcetype=custom:process_count` (sample of `/proc/loadavg` + process count or `ps` count) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix` with a custom metric — Universal Forwarder (Splunkbase 617)",
  "spl": "index=os sourcetype=custom:process_count host=*\n| stats latest(process_count) as process_count, avg(process_count) as avg_procs, stdev(process_count) as stddev by host\n| where process_count>(avg_procs+4*stddev) AND stddev>0",
  "description": "Detects 'Fork Bomb Detection' (UC-1.1.86) using `index=os` sourcetype `custom:process_count` by aggregating `avg_procs`, `stddev` grouped by `host` over `the scheduled window` and alerting when `stddev` > 0. Keywords: Fork, Bomb, Detection. Focuses on Bomb Detection behaviour so linux teams can triage UC-1.1.86 without ad-hoc log review.",
  "value": "UC-1.1.86 accelerates insight into Bomb Detection ('Fork Bomb Detection') by tracking `avg_procs`, `stddev` on `custom:process_count` feeds, reducing capacity incidents, patch regressions, and service outages before spreadsheet checks hide regressions.",
  "implementation": "Emit `process_count` from `ps -e | wc -l` or `/proc` stat readers. Widen the **stats** window or use `streamstats` over **7d** if your sample rate is sparse.",
  "visualization": "Alert, Anomaly Chart",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'Fork Bomb Detection'. Correlate with apt/yum logs in the same `custom:process_count` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `avg` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `custom:process_count`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing `stddev` > 0.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux"
  ],
  "equipmentModels": [
    "linux_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• A script that can read **process** count as a non-root user in your security model (often yes on **proc**).\n\n**Fix note** — Earlier templates referenced `process_count` after `stats` without carrying the field forward; this revision **latest(process_count)** keeps the field for the threshold line.\n\n\nStep 3 — Validate\n`ps -e | wc -l` and `uptime` on the host in the same minute; use `pstree` only for **visual** proof, not as the primary metric.\n\nStep 4 — Operationalize\nIf this fires, also check **nproc** **ulimit** and **systemd** **TasksMax** for the service **unit**.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_nix` with a custom metric — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=custom:process_count into os. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, stddev`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We help you know when a machine suddenly has far more running programs than it usually does, which can be a runaway script that keeps starting children until the system chokes.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
