{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.57",
  "title": "ARP Table Overflow Detection",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'ARP Table Overflow Detection'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-1.1.57).",
  "evidence": "Saved search uc_1_1_57_arp_table_overflow_detection, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'ARP Table Overflow Detection'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-1.1.57.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "`sourcetype=custom:arp` (one row per poll with `arp_entry_count` and optional `gc_thresh` for dynamic limits) Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix` with a custom scripted input counting `/proc/net/arp` entries — Universal Forwarder (Splunkbase 617)",
  "spl": "index=os sourcetype=custom:arp host=*\n| stats latest(arp_entry_count) as arp_entry_count, latest(max_entries) as max_entries by host\n| eval max_entries=coalesce(max_entries, 1024)\n| where arp_entry_count > (max_entries * 0.8)",
  "description": "Compares the live ARP cache size to a configured or sampled maximum, alerting when the table is most of the way to exhaustion—whether from scans, virtual churn, or mis-set limits.",
  "value": "A full ARP table stops new on-net conversations cold; this control buys time to raise `gc_thresh*`, find spoofing, or break L2 storms before a host goes dark.",
  "implementation": "Count non-header lines in `/proc/net/arp` (or your script’s equivalent) into `arp_entry_count`. Set `max_entries` from a maintained lookup or from `/proc/sys/net/ipv4/neigh/default/gc_thresh3` on hosts where that maps to your policy; the sample SPL used a static `max_entries=1024` before—prefer dynamic `max_entries` to avoid one-size-fits-all false work.",
  "visualization": "Gauge, Alert",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "linux"
  ],
  "equipmentModels": [
    "linux_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Provide both **arp_entry_count** and a realistic **max_entries** per host class (lookup CSV or field from the same script that reads `gc_thresh` values).\n\nStep 1 — Configure data collection\nRun every minute on hypervisors and routers where ARP pressure first appears. Document IPv4 vs IPv6 if you split families.\n\nStep 2 — Create the search and alert\nThe SPL in the JSON body expects both fields. If you still use a static max, set `| eval max_entries=coalesce(max_entries,1024)` after **stats**.\n\n**Understanding this SPL** — Simple utilization check with guard `max_entries>0` to avoid divide-by-zero artifacts.\n\n\nStep 3 — Validate\nCompare `arp_entry_count` to `wc -l /proc/net/arp` minus the header, and to `ip neigh show | wc` for humans on the same second.\n\nStep 4 — Operationalize\nWhen paging, also capture `dmesg` for **neighbour table overflow** lines and work with the network team on VLAN design or storm control.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_nix` with a custom scripted input counting `/proc/net/arp` entries — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=custom:arp into os. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We watch how full the local address list is, because when that list is full, the server can’t place new devices on the map and traffic stops making sense.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
