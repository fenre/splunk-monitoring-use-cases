{
  "$schema": "../../schemas/uc.schema.json",
  "id": "1.1.67",
  "title": "AppArmor Profile Violation Detection",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'AppArmor Profile Violation Detection' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `linux_capacity_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `linux_apparmor_profile_violation_detection` (UC-1.1.67), SOC dashboard panel \"Linux — AppArmor Profile Violation Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:linux_controls.",
  "controlTest": {
    "positiveScenario": "UC-1.1.67 positive (AppArmor, Profile, Violation, Detection): ingest synthetic events into `index=os` sourcetype='syslog' where `count` grouped by `host`, `profile`, `operation` satisfies `count` > 5 for 'AppArmor Profile Violation Detection'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-1.1.67 negative (Violation Detection): ingest benign `os` / `syslog` events where `host`, `profile`, `operation` stays below `count` > 5, or exclude the test entity via `linux_capacity_exceptions.csv`. Variant 15 confirms zero alert rows for 'AppArmor Profile Violation Detection'."
  },
  "dataSources": "`sourcetype=syslog` (AppArmor **DENIED** / **ALLOW** audit stream) Collected via Splunk Add-on for Unix and Linux (Splunkbase 833) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_nix` — Splunk Add-on for Unix and Linux (Splunkbase 833)",
  "spl": "index=os sourcetype=syslog \"apparmor\" (\"DENIED\" OR \"DENY\" OR (\"ALLOWED\" AND \"enforce\"))\n| stats count by host, profile, operation\n| where count>5",
  "description": "Identifies UC-1.1.67 — AppArmor, Profile, Violation, Detection — using `index=os` sourcetype `syslog`, grouped by `host`, `profile`, `operation`. Outputs `count` each `the scheduled window`; alert when `count` > 5. Primary focus: Violation Detection. Purpose-built for 'AppArmor Profile Violation Detection' in Linux workflows.",
  "value": "UC-1.1.67 operationalises 'AppArmor Profile Violation Detection' against `syslog` feeds so operators reduce capacity incidents, patch regressions, and service outages without manual correlation.",
  "implementation": "The exact `mode=enforce` string varies—adjust to your `dmesg` or **journal** phrasing. Prefer extractions of **profile** and **operation**; if your logs only have `_raw`, add two `REX` lines in **props** before you alert.",
  "visualization": "Table, Alert",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Add-on for Unix and Linux",
      "url": "https://splunkbase.splunk.com/app/833",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Patch Tuesday on `os`** — Package managers spike `host` metrics for 'AppArmor Profile Violation Detection'. Correlate with apt/yum logs in the same `syslog` window.\n\n2. **Nightly backup I/O on `host`** — Predictable `count` burst on fixed cron; compare `_time` minute before paging.\n\n3. **Synthetic probe on `syslog`** — Nagios/Zabbix polls `host` and mimics sustained load without crossing `count` > 5.\n\n4. **New host onboarding** — `host` baseline unstable first 48h after UF deploy.\n\n**Suppression mechanism:** `linux_capacity_exceptions.csv` keyed on `host`, `valid_until`.",
  "equipment": [
    "linux",
    "syslog"
  ],
  "equipmentModels": [
    "linux_generic",
    "syslog_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• **AppArmor** with logging to `kern` **syslog** or **journald** that Splunk can read. Enable forwarding for **audit**-style **AppArmor** events.\n\nStep 1 — Configure data collection\nHarden the **search** string for your version of AppArmor: some builds say “audit” in every line, others are terse.\n\n**CIM** — Not modelled; keep **N/A** unless you add an internal **security**-style CIM extension.\n\n\nStep 3 — Validate\n`aa-status` and `dmesg | grep -i apparmor` (or `journalctl -k` as appropriate) on the host, then the same line in **Search** in Splunk.\n\nStep 4 — Operationalize\nTie the **profile** field to a CMDB owner for apps; route generic **unknown** profile buckets to a platform queue.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_nix` — Splunk Add-on for Unix and Linux (Splunkbase 833)). Enable the modular input or HEC token that writes sourcetype=syslog into os. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, operation, profile`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We help you see when a safety profile blocks an action more than a few times, which is a good time to see if a program changed or if someone is poking at things they shouldn’t.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
