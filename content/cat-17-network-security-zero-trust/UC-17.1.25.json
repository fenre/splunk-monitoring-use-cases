{
  "$schema": "../../schemas/uc.schema.json",
  "id": "17.1.25",
  "title": "SonicWall High-Volume Firewall Denies from Single Source (Reconnaissance)",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'SonicWall High-Volume Firewall Denies from Single Source (Reconnaissance)' only. Does not replace full NAC policy design or firewall rule authoring or regulatory attestation. Entities listed in `network_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `network_sonicwall_high_volume_firewall_denies_from_singl` (UC-17.1.25), SOC dashboard panel \"Network — SonicWall High-Volume Firewall Denies from Single Source (Reconnaissance)\", and weekly export to `index=audit_evidence` sourcetype=csv:network_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=netfw` sourcetype='dell:sonicwall' where `src_ip` satisfies `count` > 75 for UC-17.1.25 ('SonicWall High-Volume Firewall Denies from Single Source (Reconnaissance)'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `netfw` / `dell:sonicwall` events where `src_ip` stays below `count` > 75, or exclude the test entity via `network_exception_register.csv` with a valid window. Confirm zero alert rows for UC-17.1.25."
  },
  "dataSources": "SonicWall syslog via `sourcetype=dell:sonicwall` or `sourcetype=dell:sonicwall:firewall`",
  "app": "Dell SonicWall Add-on for Splunk (Splunkbase 6203)",
  "spl": "index=netfw (sourcetype=\"dell:sonicwall\" OR sourcetype=\"dell:sonicwall:firewall\") earliest=-1h\n| eval act=lower(coalesce(action, fw_action, act))\n| eval src_ip=coalesce(src_ip, src, c_src)\n| eval dest_ip=coalesce(dest_ip, dst, c_dst)\n| eval dest_port=coalesce(dest_port, dst_port, dpt, service)\n| where match(act,\"(?i)deny|drop|block|reject\")\n| stats count by src_ip, dest_ip, dest_port\n| where count > 75\n| sort -count\n| head 25",
  "description": "Concentrated denied flows from one source toward many destinations or ports typically indicate Internet-wide scanning, brute forcing, or a compromised internal host attempting lateral movement through the firewall.",
  "value": "Concentrated denied flows from one source toward many destinations or ports typically indicate Internet-wid.... Timely UC-17.1.25 alerting on 'SonicWall High-Volume Firewall Denies from Single Source (Reconnaissance)' reduces unauthorized access, segmentation drift, and NAC policy failures before manual triage or audit channels escalate.",
  "implementation": "Normalise `action` fields using the TA’s CIM aliases. Adjust the deny count threshold for your enterprise’s NAT and scanner noise profile. Enrich top sources with ASN and threat intelligence lookups; auto-submit to SOAR for containment on critical assets.",
  "visualization": "Table (top sources), Bar chart (denies by destination port), Map (src_ip geolocation).",
  "cimModels": [
    "Network_Traffic"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Network_Traffic.All_Traffic\n  by All_Traffic.user, All_Traffic.action, All_Traffic.src, All_Traffic.dest span=1h\n| sort - count",
  "dataModelAcceleration": "Enable for Network_Traffic (All_Traffic).",
  "references": [
    {
      "title": "Splunk Connect for Syslog — Dell SonicWall",
      "url": "https://splunk.github.io/splunk-connect-for-syslog/main/sources/vendor/Dell/sonicwall/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase — Dell SonicWall Add-on for Splunk",
      "url": "https://splunkbase.splunk.com/app/6203",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Vulnerability scanner signature matches** — Qualys/Tenable traffic triggers IPS/threat signatures for 'SonicWall High-Volume Firewall Denies from Single Source (Reconnaissance)'. Compare `src` against `scanner_ips` lookup.\n\n2. **Policy push during change window** — New deny rule generates expected blocks. Match firewall commit timestamp in change calendar.\n\n3. **Encrypted traffic inspection gap** — TLS-heavy apps bypass deep inspection; volume drop is operational, not a sensor failure.\n\n4. **Red-team C2 simulation** — Approved exercise from `pen_test_hosts.csv`.\n\n**Suppression mechanism:** `network_exception_register.csv` keyed on `src`, `signature_id`, `valid_until`.",
  "securityDomain": "network",
  "equipment": [
    "m365",
    "sonicwall",
    "syslog"
  ],
  "equipmentModels": [
    "m365_generic",
    "sonicwall_generic",
    "syslog_generic"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• SonicWall syslog to Splunk. Data in `index=firewall` with `sourcetype=sonicwall`. Key fields: `msg_id` (264=Connection Dropped, 36=Connection Opened then Dropped, 173=Fragmented Packet Dropped), `src`, `srcPort`, `dst`, `dstPort`, `proto`, `fw_action` (drop/deny/block).\n• Reconnaissance signatures: a single source IP sending connections to many destination IPs (host scan), many destination ports (port scan), or both (compound scan). The volume and pattern distinguish legitimate traffic (CDN, DNS) from malicious scanning.\n• Create a `trusted_scanners.csv` lookup: `src_ip,scanner_name,owner,authorized` to whitelist known vulnerability scanners and monitoring tools.\n\nStep 1 — Configure data collection\nVerify deny events:\n```spl\nindex=firewall sourcetype=sonicwall fw_action IN (\"drop\",\"deny\",\"block\") earliest=-1h\n| stats count\n```\n\nStep 2 — Create the search and alert\n\nPrimary search — Reconnaissance detection:\n```spl\nindex=firewall sourcetype=sonicwall fw_action IN (\"drop\",\"deny\",\"block\") earliest=-1h\n| stats count dc(dst) as unique_targets dc(dstPort) as unique_ports by src\n| lookup trusted_scanners.csv src_ip as src OUTPUT authorized\n| where isnull(authorized)\n| eval scan_type=case(\n    unique_targets > 20 AND unique_ports > 20, \"Compound Scan\",\n    unique_targets > 20, \"Host Scan\",\n    unique_ports > 20, \"Port Scan\",\n    count > 500, \"High-Volume Deny\",\n    1==1, \"Low\"\n  )\n| where scan_type!=\"Low\"\n| sort - count\n| iplocation src\n| table src, Country, count, unique_targets, unique_ports, scan_type\n```\n\nUnderstanding this SPL: Classifies high-volume deny sources into scan types. A host scan (many targets, few ports) targets a specific service across the network — often looking for exposed SSH, RDP, or SMB. A port scan (one target, many ports) enumerates services on a single host — precursor to targeted exploitation. A compound scan is the most aggressive. After excluding authorized scanners, all flagged IPs warrant investigation.\n\nPort scan targeting analysis:\n```spl\nindex=firewall sourcetype=sonicwall fw_action IN (\"drop\",\"deny\",\"block\") earliest=-1h\n| stats count by src, dstPort\n| eventstats sum(count) as total_from_src by src\n| where total_from_src > 200\n| sort src, - count\n| head 50\n```\n\nStep 3 — Validate\n(a) Run `nmap -sS [target]` from an external test host. Verify the deny events and scan classification.\n(b) Verify trusted scanners are excluded.\n\nStep 4 — Operationalize\nDashboard: Row 1 — Active scanners (last hour). Row 2 — Scan type breakdown. Row 3 — Top denied ports.\nAlert: Immediate on Compound Scan. Informational on Host/Port Scan.\n\nRunbook:\n1. **External host scan** — Block source IP at the border. Report to abuse contact.\n2. **Internal host scan** — Potential compromised host. Isolate and investigate.\n3. **High-volume deny from known IP** — May be a misconfigured application. Check with app owner.\n\nStep 5 — Troubleshooting\n• **Too many false positives** — Raise thresholds. CDN/DNS IPs hit many destinations legitimately.\n• **No deny events** — SonicWall may have an implicit allow rule. Check firewall rules.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Dell SonicWall Add-on for Splunk (Splunkbase 6203)). Enable the modular input or HEC token that writes sourcetype=dell:sonicwall, dell:sonicwall:firewall into netfw. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`dest_ip, dest_port, src_ip`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "When someone tries to knock on a lot of doors that are locked, it usually means they are looking for one that is open. We count how many locked doors each visitor tries, and if the number is unusually high, we flag them as suspicious and investigate.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
