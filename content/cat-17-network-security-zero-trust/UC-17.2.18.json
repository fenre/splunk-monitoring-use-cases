{
  "$schema": "../../schemas/uc.schema.json",
  "id": "17.2.18",
  "title": "Site-to-Site Tunnel Flapping",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Availability"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Site-to-Site Tunnel Flapping' only. Does not replace full NAC policy design or firewall rule authoring or regulatory attestation. Entities listed in `network_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `network_site_to_site_tunnel_flapping` (UC-17.2.18), SOC dashboard panel \"Network — Site-to-Site Tunnel Flapping\", and weekly export to `index=audit_evidence` sourcetype=csv:network_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=vpn` sourcetype='cisco:asa' where `peer` satisfies `count` > 10 for UC-17.2.18 ('Site-to-Site Tunnel Flapping'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `vpn` / `cisco:asa` events where `peer` stays below `count` > 10, or exclude the test entity via `network_exception_register.csv` with a valid window. Confirm zero alert rows for UC-17.2.18."
  },
  "dataSources": "VPN syslog tunnel events Collected via Splunk Add-on for Cisco Security (Splunkbase 1352) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_cisco-asa`, `Splunk_TA_paloalto`, `TA-fortinet_fortigate`, `Splunk_TA_juniper` — Splunk Add-on for Cisco Security (Splunkbase 1352)",
  "spl": "index=vpn sourcetype=\"cisco:asa\" earliest=-24h\n| search \"Tunnel is UP\" OR \"Tunnel is DOWN\" OR \"IPSEC.*DOWN\"\n| eval peer=coalesce(peer_ip, tunnel_group)\n| stats count by peer\n| where count>10\n| sort -count",
  "description": "Counts IKE/IPsec up/down events per peer for unstable WAN or crypto issues. This use case monitors the condition described in the title using the SPL and data sources documented on this page.",
  "value": "Counts IKE/IPsec up/down events per peer for unstable WAN or crypto issues. Timely UC-17.2.18 alerting on 'Site-to-Site Tunnel Flapping' reduces unauthorized access, segmentation drift, and NAC policy failures before manual triage or audit channels escalate.",
  "implementation": "Vendor message strings vary — maintain `rex` extractions in props. Alert when transitions >N per hour per peer.",
  "visualization": "Line chart (transitions over time), Table (worst peers), Single value (flapping peers).",
  "cimModels": [],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Network_Traffic.All_Traffic\n  by All_Traffic.user, All_Traffic.action, All_Traffic.src, All_Traffic.dest span=1h\n| sort - count",
  "dataModelAcceleration": "Enable for Network_Traffic (All_Traffic).",
  "references": [
    {
      "title": "Splunk_TA_paloalto",
      "url": "https://splunkbase.splunk.com/app/2757",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 1352",
      "url": "https://splunkbase.splunk.com/app/1352",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Cisco Security documentation",
      "url": "https://www.cisco.com/c/en/us/support/security/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Palo Alto Networks documentation",
      "url": "https://docs.paloaltonetworks.com/",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Remote workforce login storm** — Morning VPN/ZTNA session spike for 'Site-to-Site Tunnel Flapping'. Compare with historical same-hour baseline.\n\n2. **IdP or SAML metadata refresh** — Brief auth failures during federation cert rotation.\n\n3. **Split-tunnel policy change** — New route advertisements shift traffic patterns.\n\n4. **Synthetic ZTNA health checks** — Monitoring probes from known appliance IPs.\n\n**Suppression mechanism:** `network_exception_register.csv` keyed on `user`, `src`, `valid_until`.",
  "mitreAttack": [
    "T1133"
  ],
  "equipment": [
    "cisco",
    "fortinet",
    "paloalto",
    "syslog"
  ],
  "equipmentModels": [
    "cisco_asa",
    "fortinet_fortigate",
    "paloalto_pan_firewall"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Splunk Add-on for Cisco ASA (`sourcetype=cisco:asa`). Data in `index=vpn`.\n• Key fields: `peer_ip`, `tunnel_group`, `action/event` (tunnel up/down). Cisco ASA message IDs: 602303 (IPSec SA created), 602304 (IPSec SA deleted), 402119 (IPSEC SA lifetime expired), 752004 (tunnel up), 752005 (tunnel down).\n• Site-to-site tunnel flapping disrupts branch connectivity, application availability, and real-time protocols (VoIP, video).\n• Create a `site_tunnels.csv` lookup: `peer_ip,site_name,circuit_id,isp,criticality`.\n\nStep 1 — Configure data collection\nVerify tunnel state events:\n```spl\nindex=vpn sourcetype=\"cisco:asa\" earliest=-24h\n| search \"Tunnel is UP\" OR \"Tunnel is DOWN\" OR \"IPSEC.*DOWN\" OR \"IPSEC.*UP\"\n| stats count by host, peer_ip\n```\n\nStep 2 — Create the search and alert\n\nPrimary search — Tunnel flap detection:\n```spl\nindex=vpn sourcetype=\"cisco:asa\" earliest=-24h\n| search \"Tunnel is UP\" OR \"Tunnel is DOWN\" OR \"IPSEC.*DOWN\" OR \"IPSEC.*UP\"\n| eval state=if(match(_raw, \"UP\"), \"UP\", \"DOWN\")\n| eval peer=coalesce(peer_ip, tunnel_group)\n| sort peer, _time\n| streamstats current=f last(state) as prev_state by peer\n| where state!=prev_state\n| stats count as state_changes earliest(_time) as first latest(_time) as last by peer, host\n| eval flap_rate=if((last-first)>0, round(state_changes/((last-first)/3600), 1), state_changes)\n| lookup site_tunnels.csv peer_ip as peer OUTPUT site_name circuit_id criticality\n| where state_changes > 4\n| sort - flap_rate\n```\n\nUnderstanding this SPL: Counts state transitions (UP->DOWN or DOWN->UP) per tunnel peer. A stable tunnel has 0 transitions. More than 4 transitions in 24 hours indicates instability. The `flap_rate` (transitions per hour) distinguishes between occasional blips and persistent instability. Critical site tunnels need immediate attention.\n\nStep 3 — Validate\n(a) On ASA: `show crypto ipsec sa | grep peer` shows current tunnel state.\n\nStep 4 — Operationalize\nDashboard: Row 1 — \"Flapping tunnels\", \"Stable tunnels\". Row 2 — Table: peer, site, state changes, flap rate, criticality.\nAlert: Immediate when a critical-site tunnel flaps more than 2 times in 1 hour.\n\nRunbook:\n1. **Frequent flapping** — Check the circuit: run traceroute to the peer, contact the ISP, check link utilization.\n2. **Tunnel down and not recovering** — IKE negotiation may be failing. Check `show crypto isakmp sa` for error states. Common cause: pre-shared key mismatch after change.\n3. **Seasonal pattern** — ISP maintenance windows. Schedule monitoring suppression.\n\nStep 5 — Troubleshooting\n• **Tunnel events not being logged** — Enable `logging class vpnc level 6` on ASA.\n• **`peer_ip` not parsed** — Use `rex` to extract from the syslog message: `| rex \"peer=(?<peer_ip>\\d+\\.\\d+\\.\\d+\\.\\d+)\"`.",
  "grandmaExplanation": "We watch the connections between our offices, and when one keeps going up and down rapidly like a flickering light, we investigate the network path to fix it before it stops working completely.",
  "hardware": "Firewalls, routers",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
