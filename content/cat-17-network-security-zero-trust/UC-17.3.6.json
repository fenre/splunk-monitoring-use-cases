{
  "$schema": "../../schemas/uc.schema.json",
  "id": "17.3.6",
  "title": "Policy Drift Detection",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Configuration"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Policy Drift Detection' only. Does not replace full NAC policy design or firewall rule authoring or regulatory attestation. Entities listed in `network_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `network_policy_drift_detection` (UC-17.3.6), SOC dashboard panel \"Network — Policy Drift Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:network_controls.",
  "controlTest": {
    "positiveScenario": "UC-17.3.6 positive (Policy, Drift, Detection): ingest synthetic events into `index=zt` sourcetype='zt:admin_audit' where `count` grouped by `entity` satisfies detection threshold for 'Policy Drift Detection'. Validate filter `action IN (\"policy_modified\",\"rule_added\",\"rule_deleted\",\"ru`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-17.3.6 negative (Drift Detection): ingest benign `zt` / `zt:admin_audit` events where `entity` stays below detection threshold, or exclude the test entity via `network_exception_register.csv`. Variant 8 confirms zero alert rows for 'Policy Drift Detection'."
  },
  "dataSources": "ZT policy audit logs, configuration snapshots Indexes: zt. Sourcetypes: zt:admin_audit. Validate field extraction in Search before saving the alert; compare event counts to the vendor admin console.",
  "app": "`Splunk_TA_zscaler`, Netskope Add-on for Splunk (Splunkbase 3808), `Splunk_TA_paloalto` (Prisma Access), `TA-fortinet_fortigate` (FortiSASE), Check Point App for Splunk (Splunkbase 4293)",
  "spl": "index=zt sourcetype=\"zt:admin_audit\"\n| search action IN (\"policy_modified\",\"rule_added\",\"rule_deleted\",\"rule_disabled\")\n| table _time, admin, action, policy_name, details\n| sort -_time",
  "description": "Identifies UC-17.3.6 — Policy, Drift, Detection — using `index=zt` sourcetype `zt:admin_audit`, grouped by `entity`. Outputs `count` each `the scheduled window`; alert when configured threshold. Filter chain includes filter `action IN (\"policy_modified\",\"rule_added\",\"rule_deleted\",\"ru`. Purpose-built for 'Policy Drift Detection' in Network workflows.",
  "value": "UC-17.3.6 accelerates insight into Drift Detection ('Policy Drift Detection') by tracking `activity` on `zt:admin_audit` feeds, reducing unauthorized access, segmentation drift, and NAC policy failures before spreadsheet checks hide regressions.",
  "implementation": "Track all ZT policy changes via audit logs. Compare current configuration against approved baseline. Alert on unauthorized modifications. Require change management approval for policy changes. Report on policy change frequency.",
  "visualization": "Table (policy changes), Timeline (modification events), Bar chart (changes by admin), Single value (changes this week).",
  "cimModels": [
    "Change",
    "Authentication"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Authentication.Authentication\n  by Authentication.user, Authentication.action, Authentication.src span=1h\n| sort - count",
  "dataModelAcceleration": "Enable for Authentication (Authentication).",
  "references": [
    {
      "title": "Netskope Add-on for Splunk",
      "url": "https://splunkbase.splunk.com/app/3808",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Check Point App for Splunk",
      "url": "https://splunkbase.splunk.com/app/4293",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk_TA_paloalto",
      "url": "https://splunkbase.splunk.com/app/2757",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Authentication",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Authentication",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `zt` (UC-17.3.6)** — Planned work for 'Policy Drift Detection' shifts `count` over `span=1h` while `host` remains in-scope. Applies filter `action IN (\"policy_modified\",\"rule_added\",\"rule_deleted\",\"ru`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `zt:admin_audit`** — Scheduled automation elevates `src` without crossing configured threshold for UC-17.3.6. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `zt` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `zt:admin_audit`** — First 48h baseline for UC-17.3.6 on `host` is unstable; window via `network_exception_register.csv`.\n\n**Suppression mechanism:** `network_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1562.004",
    "T1133"
  ],
  "equipment": [
    "checkpoint",
    "fortinet",
    "netskope",
    "nutanix",
    "paloalto",
    "zscaler"
  ],
  "equipmentModels": [
    "fortinet_fortigate",
    "paloalto_pan_firewall",
    "paloalto_prisma"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Zero Trust policy platform admin/audit logs. Microsoft Entra: `sourcetype=azure:aad:audit`. Zscaler: `sourcetype=zscaler:zia:admin_audit`. Palo Alto Prisma: `sourcetype=pan:system` with config change events. Key fields: `actor`, `target`, `operationType` (Add/Update/Delete), `modifiedProperties`, `timestamp`.\n• Policy drift = the gap between the intended Zero Trust policy design and the actual deployed configuration. It happens when: admins make ad-hoc exceptions, temporary rules become permanent, or automated tools modify policies without review.\n• Create a `zt_policy_baseline.csv` lookup: `policy_name,baseline_version,baseline_date,approved_by` — a snapshot of the approved policy set. This baseline is updated only through the change management process.\n\nStep 1 — Configure data collection\nVerify audit event flow:\n```spl\nindex=azure sourcetype=\"azure:aad:audit\" earliest=-7d\n| where match(operationType, \"(?i)update|add|delete\") AND match(category, \"(?i)policy\")\n| stats count by operationType, activityDisplayName\n```\n\nStep 2 — Create the search and alert\n\nPrimary search — Policy change detection:\n```spl\nindex=azure sourcetype=\"azure:aad:audit\" earliest=-7d\n| where match(category, \"(?i)policy\")\n| eval change_type=coalesce(operationType, \"unknown\")\n| eval change_hour=strftime(_time, \"%H\"), change_dow=strftime(_time, \"%A\")\n| eval out_of_hours=if((change_hour<\"08\" OR change_hour>\"18\") OR match(change_dow, \"Saturday|Sunday\"), \"YES\", \"no\")\n| table _time, initiatedBy.user.userPrincipalName, change_type, activityDisplayName, targetResources{}.displayName, out_of_hours\n| sort - _time\n```\n\nUnderstanding this SPL: Lists every policy modification in Entra ID, flagging out-of-hours changes. Policy drift often starts with a well-intentioned exception ('just for this one user') that is never reviewed or removed. Over time, dozens of exceptions accumulate and the policy no longer matches the design. The out-of-hours flag catches emergency changes that may bypass the change process.\n\nDrift quantification:\n```spl\nindex=azure sourcetype=\"azure:aad:audit\" earliest=-30d\n| where match(category, \"(?i)policy\")\n| stats count by initiatedBy.user.userPrincipalName, operationType\n| sort - count\n```\n\nStep 3 — Validate\n(a) Make a test policy change in Entra ID. Verify the audit event appears.\n(b) Compare current policy settings with the baseline CSV.\n\nStep 4 — Operationalize\nDashboard: Row 1 — Policy changes per day. Row 2 — Out-of-hours changes. Row 3 — Changes by admin.\nAlert: Immediate on any CA policy deletion. Daily digest for all changes.\n\nRunbook:\n1. **Policy deleted** — Critical. Verify with admin. May leave users unprotected.\n2. **High change frequency** — May indicate testing in production. Enforce staging.\n3. **Out-of-hours change** — Verify with admin. If emergency, document the justification.\n\nStep 5 — Troubleshooting\n• **No policy audit events** — Entra ID Diagnostic Settings must include AuditLogs category.\n• **`modifiedProperties` empty** — Some changes don't include before/after values in audit logs.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_zscaler`, Netskope Add-on for Splunk (Splunkbase 3808), `Splunk_TA_paloalto` (Prisma Access), `TA-fortinet_fortigate` (FortiSASE), Check Point App for Splunk (Splunkbase 4293)). Enable the modular input or HEC token that writes sourcetype=zt:admin_audit into zt. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`host, user, action`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Our Zero Trust security rules should only change through a proper review process. We track every single change — who made it, when, and what they changed — so we can catch unauthorized modifications before they create security gaps.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
