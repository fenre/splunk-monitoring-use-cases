{
  "$schema": "../../schemas/uc.schema.json",
  "id": "17.5.15",
  "title": "Forcepoint Policy Sync Status Between Cloud SSE and On-Prem Web (`forcepoint:one`)",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Configuration",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "dataSources": "Forcepoint cloud SSE (`forcepoint:one`), on-prem Web Security (`websense:cg:kv`), Email (`forcepoint:email:kv`), DLP (`forcepoint:dlp`)",
  "app": "Forcepoint Insights SIEM App (Splunkbase 8053); Forcepoint Web Security TA (`websense:cg:kv`); Email Security KV (`forcepoint:email:kv`)",
  "spl": "index=sase sourcetype=\"forcepoint:one\" OR index=proxy sourcetype=\"websense:cg:kv\" earliest=-24h\n| eval plane=if(sourcetype==\"forcepoint:one\",\"cloud\",\"onprem\")\n| where match(_raw,\"(?i)sync|publish|revision\")\n| stats latest(_time) as t by plane, policy_name\n| eventstats max(t) as newest\n| eval lag_sec=newest-t\n| where lag_sec>3600",
  "description": "Split-brain policies confuse users and create audit gaps between legacy web proxies and ONE.",
  "value": "Ensures unified enforcement story across hybrid deployments.",
  "implementation": "Install the Insights SIEM app, configure HEC inputs per product line, and align indexes (`proxy`, `email`, `dlp`, `sase`). Normalize usernames to enterprise SAMAccountName.",
  "visualization": "Table (plane, policy, lag), alert.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunkbase — Forcepoint Insights SIEM App",
      "url": "https://splunkbase.splunk.com/app/8053"
    },
    {
      "title": "Splunk Add-on for Forcepoint Web Security",
      "url": "https://splunkbase.splunk.com/app/3245"
    }
  ],
  "securityDomain": "network",
  "equipment": [
    "forcepoint"
  ],
  "status": "verified",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "grandmaExplanation": "We pull together Forcepoint security logs so risky web, email, and data-loss patterns are visible in one place."
}
