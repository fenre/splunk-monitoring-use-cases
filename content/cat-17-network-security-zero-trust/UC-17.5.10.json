{
  "$schema": "../../schemas/uc.schema.json",
  "id": "17.5.10",
  "title": "Forcepoint Advanced Malware Detection File Analysis Outcomes (`forcepoint:one`)",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Risk"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Forcepoint Advanced Malware Detection File Analysis Outcomes (`forcepoint:one`)' only. Does not replace full NAC policy design or firewall rule authoring or regulatory attestation. Entities listed in `network_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `network_forcepoint_advanced_malware_detection_file_analy` (UC-17.5.10), SOC dashboard panel \"Network — Forcepoint Advanced Malware Detection File Analysis Outcomes (`forcepoint:one`)\", and weekly export to `index=audit_evidence` sourcetype=csv:network_controls.",
  "controlTest": {
    "positiveScenario": "UC-17.5.10 positive (Forcepoint, Advanced, Malware, Detection, File): ingest synthetic events into `index=sase` sourcetype='forcepoint:one' where `count` grouped by `verdict`, `file_type`, `sha256` satisfies -7d for 'Forcepoint Advanced Malware Detection File Analysis Outcomes (`forcepoint:one`)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-17.5.10 negative (Outcomes (`forcepoint:one`)): ingest benign `sase` / `forcepoint:one` events where `verdict`, `file_type`, `sha256` stays below -7d, or exclude the test entity via `network_exception_register.csv`. Variant 15 confirms zero alert rows for 'Forcepoint Advanced Malware Detection File Analysis Outcomes (`forcepoint:one`)'."
  },
  "dataSources": "Forcepoint cloud SSE (`forcepoint:one`), on-prem Web Security (`websense:cg:kv`), Email (`forcepoint:email:kv`), DLP (`forcepoint:dlp`)",
  "app": "Forcepoint Insights SIEM App (Splunkbase 8053); Forcepoint Web Security TA (`websense:cg:kv`); Email Security KV (`forcepoint:email:kv`)",
  "spl": "index=sase sourcetype=\"forcepoint:one\" earliest=-7d\n| where match(lower(event_type),\"(?i)malware|sandbox|amd\")\n| stats count by verdict, file_type, sha256\n| sort -count",
  "description": "Monitors UC-17.5.10 — Forcepoint, Advanced, Malware, Detection, File — using `index=sase` sourcetype `forcepoint:one`, grouped by `verdict`, `file_type`, `sha256`. Outputs `count` each `the scheduled window`; alert when -7d. Primary focus: Outcomes (`forcepoint:one`). Purpose-built for 'Forcepoint Advanced Malware Detection File Analysis Outcomes (`forcepoint:one`)' in Network workflows.",
  "value": "UC-17.5.10 operationalises 'Forcepoint Advanced Malware Detection File Analysis Outcomes (`forcepoint:one`)' against `forcepoint:one` feeds so operators reduce unauthorized access, segmentation drift, and NAC policy failures without manual correlation.",
  "implementation": "Install the Insights SIEM app, configure HEC inputs per product line, and align indexes (`proxy`, `email`, `dlp`, `sase`). Normalize usernames to enterprise SAMAccountName.",
  "visualization": "Pie (verdict), table (hash).",
  "cimModels": [],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Email.All_Email\n  by Email.user, Email.action, Email.src, Email.dest span=1h\n| sort - count",
  "dataModelAcceleration": "Enable for Email (Email).",
  "references": [
    {
      "title": "Splunkbase — Forcepoint Insights SIEM App",
      "url": "https://splunkbase.splunk.com/app/8053",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Add-on for Forcepoint Web Security",
      "url": "https://splunkbase.splunk.com/app/3245",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `sase` (UC-17.5.10)** — Planned work for 'Forcepoint Advanced Malware Detection File Analysis Outcomes (`forcepoint:one`)' shifts `count` over `span=1h` while `verdict`, `file_type`, `sha256` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `forcepoint:one`** — Scheduled automation elevates `file_type` without crossing configured threshold for UC-17.5.10. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `verdict`, `file_type`, `sha256`** — Health checks in `sase` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `forcepoint:one`** — First 48h baseline for UC-17.5.10 on `verdict` is unstable; window via `network_exception_register.csv`.\n\n**Suppression mechanism:** `network_exception_register.csv` keyed on `verdict`, `exception_reason`, `valid_until`.",
  "securityDomain": "network",
  "equipment": [
    "forcepoint"
  ],
  "equipmentModels": [
    "forcepoint_generic"
  ],
  "status": "verified",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "Prerequisites\n• Forcepoint Insights SIEM App (Splunkbase 8053) or direct integration from Forcepoint ONE SSE.\n• Data in `index=sase` with `sourcetype=forcepoint:one`. Key fields: `event_type` (malware/sandbox), `file_name`, `file_hash` (SHA256), `file_type`, `verdict` (clean/suspicious/malicious), `analysis_type` (static/dynamic/sandbox), `user`, `url`, `action` (allow/block/quarantine).\n• Forcepoint ONE SSE with Advanced Malware Detection (AMD) module — provides cloud-based sandboxing for unknown files. Files not matched by signature-based detection are submitted for behavioral analysis.\n\nStep 1 — Configure data collection\nVerify AMD events:\n```spl\nindex=sase sourcetype=\"forcepoint:one\" earliest=-7d\n| where match(lower(event_type), \"malware|sandbox\")\n| stats count by verdict, analysis_type, action\n```\n\nStep 2 — Create the search and alert\n\nPrimary search — File analysis outcomes:\n```spl\nindex=sase sourcetype=\"forcepoint:one\" earliest=-7d\n| where match(lower(event_type), \"malware|sandbox\")\n| stats count as analyses dc(file_hash) as unique_files dc(user) as affected_users by verdict, file_type, analysis_type\n| eval detection_detail=verdict.\" (\".analysis_type.\")\"\n| sort - analyses\n```\n\nUnderstanding this SPL: Groups file analysis results by verdict and detection method. 'Malicious' verdicts from dynamic/sandbox analysis represent zero-day threats that evaded signature detection — these are the highest-value detections. 'Suspicious' verdicts may need manual review. The ratio of malicious to total analyzed files indicates the environment's threat exposure level. High unique file counts with the same hash across multiple users suggest a targeted campaign.\n\nMalicious file detail:\n```spl\nindex=sase sourcetype=\"forcepoint:one\" earliest=-7d\n| where match(lower(event_type), \"malware|sandbox\") AND verdict=\"malicious\"\n| stats count dc(user) as affected_users values(user) as users values(url) as source_urls by file_name, file_hash, file_type\n| sort - affected_users\n```\n\nStep 3 — Validate\n(a) In Forcepoint ONE portal: Security > Malware Detection > view analysis history. Compare verdicts.\n(b) Submit the EICAR test file through the proxy. Verify detection.\n(c) Cross-reference malicious file hashes with VirusTotal for independent validation.\n\nStep 4 — Operationalize\nDashboard (\"Forcepoint — Malware Detection\"):\n• Row 1 — Single-value: \"Malicious files (7d)\", \"Unique threats\", \"Users affected\".\n• Row 2 — Pie chart: verdict distribution.\n• Row 3 — Table: malicious files with hash, type, users, source URLs.\n\nAlert: Immediate alert for any 'malicious' verdict. Daily digest for 'suspicious'.\n\nRunbook:\n1. **Malicious file detected** — Verify block action was taken. Cross-reference the file hash with EDR to check if any endpoint executed it.\n2. **Same hash hitting multiple users** — Targeted campaign. Block the source URL/domain. Alert all affected users.\n3. **High volume of suspicious verdicts** — May indicate aggressive sandboxing thresholds. Review and tune the AMD sensitivity.\n\nStep 5 — Troubleshooting\n• **No AMD events** — AMD module may not be licensed. Check Forcepoint ONE > Settings.\n• **All verdicts are 'clean'** — Malware may be evading sandbox detection. Consider supplementing with EDR.\n• **File analysis taking too long** — Sandbox queue may be backed up. Check AMD service health.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Forcepoint Insights SIEM App (Splunkbase 8053); Forcepoint Web Security TA (`websense:cg:kv`); Email Security KV (`forcepoint:email:kv`)). Enable the modular input or HEC token that writes sourcetype=forcepoint:one into sase. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`file_type, sha256, verdict`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We check the results when our system tests suspicious files in a safe sandbox, seeing which ones turned out to be harmful and which were safe, so we can catch new threats that traditional virus scanners miss.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
