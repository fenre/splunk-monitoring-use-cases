{
  "$schema": "../../schemas/uc.schema.json",
  "id": "17.3.3",
  "title": "Micro-Segmentation Audit",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Security",
    "Configuration"
  ],
  "splunkPillar": "Security",
  "compliance": [
    {
      "regulation": "nerc-cip",
      "version": "current",
      "clause": "CIP-005-7 R1",
      "clauseUrl": "https://www.nerc.com/pa/Stand/Pages/CIPStandards.aspx",
      "mode": "satisfies",
      "assurance": "partial",
      "assurance_rationale": "NERC CIP-005-7 R1 Electronic security perimeter — micro-segmentation audits validate the Electronic Security Perimeter is continuously enforced for BES Cyber Systems.",
      "controlObjective": "Evidence that NERC CIP CIP-005-7 R1 (Electronic security perimeter) is enforced — Splunk UC-17.3.3: Micro-Segmentation Audit.",
      "evidenceArtifact": "Saved search 'UC-17.3.3' running on Micro-segmentation policy logs (allow/deny events), archived to the restricted audit_evidence index (default 7-year retention). Auto-drafted — SME review required.",
      "obligationRef": "nerc-cip@current#CIP-005-7 R1",
      "requires_sme_review": true
    },
    {
      "regulation": "uk-cyber-essentials",
      "version": "Montpellier (2025)",
      "clause": "CE.BF.1",
      "clauseUrl": "https://www.ncsc.gov.uk/cyberessentials/overview",
      "mode": "satisfies",
      "assurance": "partial",
      "assurance_rationale": "UK Cyber Essentials Montpellier CE.BF.1 Boundary firewalls — micro-segmentation is an extension of the boundary-firewall requirement updated in Montpellier.",
      "controlObjective": "Evidence that Cyber Essentials CE.BF.1 (Boundary firewalls) is enforced — Splunk UC-17.3.3: Micro-Segmentation Audit.",
      "evidenceArtifact": "Saved search 'UC-17.3.3' running on Micro-segmentation policy logs (allow/deny events), archived to the restricted audit_evidence index (default 7-year retention). Auto-drafted — SME review required.",
      "obligationRef": "uk-cyber-essentials@Montpellier (2025)#CE.BF.1",
      "requires_sme_review": true
    },
    {
      "regulation": "iec-62443",
      "version": "2013-ongoing",
      "clause": "FR 6.2",
      "clauseUrl": "https://www.isa.org/standards-and-publications/isa-standards/isa-iec-62443-series-of-standards",
      "mode": "satisfies",
      "assurance": "partial",
      "assurance_rationale": "IEC 62443 FR 6.2 Continuous monitoring — micro-segmentation audits feed the continuous-monitoring Foundational Requirement for control-system zones.",
      "controlObjective": "Evidence that IEC 62443 FR 6.2 (Continuous monitoring) is enforced — Splunk UC-17.3.3: Micro-Segmentation Audit.",
      "evidenceArtifact": "Saved search 'UC-17.3.3' running on Micro-segmentation policy logs (allow/deny events), archived to the restricted audit_evidence index (default 7-year retention). Auto-drafted — SME review required.",
      "obligationRef": "iec-62443@2013-ongoing#FR 6.2",
      "requires_sme_review": true
    }
  ],
  "dataSources": "Micro-segmentation policy logs (allow/deny events)",
  "app": "VMware NSX Add-on, Illumio syslog/HEC, Cisco Secure Workload TA, Akamai Guardicore Add-on for Splunk (Splunkbase 7426)",
  "spl": "index=zt sourcetype=\"microseg:policy\"\n| where action=\"deny\"\n| stats count by src_workload, dest_workload, dest_port, policy_name\n| sort -count",
  "description": "Micro-segmentation limits lateral movement. Audit logs validate policy enforcement and detect bypasses.",
  "value": "Security architects audit microsegmentation policy enforcement against the approved workload communication matrix, identifying allowed traffic that violates the design, default-rule leaks, and segments not yet covered by specific policies.",
  "implementation": "Ingest micro-segmentation policy enforcement logs. Track allowed and denied traffic between workloads. Alert on unexpected denials (may indicate misconfiguration) and unexpected allows (policy gaps). Report on segmentation coverage.",
  "visualization": "Heatmap (workload × workload traffic), Table (policy violations), Sankey diagram (traffic flows).",
  "cimModels": [],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Network_Traffic.All_Traffic\n  by All_Traffic.user, All_Traffic.action, All_Traffic.src, All_Traffic.dest span=1h\n| sort - count",
  "dataModelAcceleration": "Enable for Network_Traffic (All_Traffic).",
  "references": [
    {
      "title": "Akamai Guardicore Add-on for Splunk",
      "url": "https://splunkbase.splunk.com/app/7426"
    },
    {
      "title": "CIM: Authentication",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Authentication"
    }
  ],
  "knownFalsePositives": "Microseg blocks from new app deployments, missing exception rules, or planned segmentation rollouts.",
  "mitreAttack": [
    "T1021",
    "T1570"
  ],
  "equipment": [
    "cisco",
    "guardicore",
    "nsx",
    "syslog",
    "vmware"
  ],
  "detailedImplementation": "Prerequisites\n• Microsegmentation platform (Akamai Guardicore, Illumio, VMware NSX) logs in `index=microseg` or `index=nac`. Key fields vary by vendor: Guardicore: `sourcetype=guardicore:syslog` with `src_label`, `dst_label`, `policy_name`, `action` (allow/block). Illumio: `sourcetype=illumio:traffic` with `src_ip`, `dst_ip`, `service`, `decision`. NSX: `sourcetype=vmware:nsx:firewall` with `action`, `rule_id`, `src`, `dst`.\n• Microsegmentation creates software-defined boundaries between workloads. The audit verifies: all traffic is subject to a policy (no 'default allow' leaks), policies match the intended design (application communication matrix), and no unauthorized lateral movement occurs.\n• Create a `segment_policy_design.csv` lookup: `src_segment,dst_segment,allowed_ports,business_justification` — the approved communication matrix.\n\nStep 1 — Configure data collection\nVerify segment policy events:\n```spl\nindex=microseg earliest=-24h\n| stats count by sourcetype, action\n```\nExpected: allow and block events. If only allow: the default policy may be 'allow all' (not yet enforcing).\n\nStep 2 — Create the search and alert\n\nPrimary search — Policy audit against design:\n```spl\nindex=microseg earliest=-24h\n| stats count by src_label, dst_label, dstPort, action\n| lookup segment_policy_design.csv src_segment as src_label dst_segment as dst_label OUTPUT allowed_ports\n| eval expected=if(match(allowed_ports, dstPort) OR isnull(allowed_ports), \"Expected\", \"Unexpected\")\n| where action=\"allow\" AND expected=\"Unexpected\"\n| sort - count\n```\n\nUnderstanding this SPL: Identifies allowed traffic that is NOT in the approved communication matrix. This is the most important microsegmentation audit — it finds policy gaps where workloads can communicate in ways not intended by the design. Common causes: overly broad policies, default rules, or forgotten legacy rules.\n\nDefault rule hits:\n```spl\nindex=microseg earliest=-24h\n| where match(policy_name, \"(?i)default\") AND action=\"allow\"\n| stats count dc(src_label) as src_segments dc(dst_label) as dst_segments by policy_name\n```\n\nStep 3 — Validate\n(a) Pick a workload pair from the communication matrix. Verify the policy is enforced.\n(b) Attempt traffic outside the matrix. Verify it is blocked.\n\nStep 4 — Operationalize\nDashboard: Row 1 — Policy compliance rate. Row 2 — Unexpected allowed flows. Row 3 — Default rule hits.\nReport: Monthly microsegmentation audit for compliance.\n\nRunbook:\n1. **Unexpected allow** — Review the policy rule. Tighten or create a specific rule.\n2. **High default rule traffic** — Segments not yet covered by specific policies. Prioritize.\n3. **New src/dst pair** — New application or service. Add to communication matrix if approved.\n\nStep 5 — Troubleshooting\n• **No block events** — Microsegmentation may be in 'monitor' mode. Switch to 'enforce' after validation.\n• **`src_label` empty** — Agent may not be labeling workloads. Check agent configuration.",
  "grandmaExplanation": "We divide our internal network into tiny compartments so even if an attacker gets in, they cannot move around freely. We regularly check that these walls are actually working by comparing what traffic we see against what traffic we expect to see."
}
