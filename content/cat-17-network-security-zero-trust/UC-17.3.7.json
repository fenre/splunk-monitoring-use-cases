{
  "$schema": "../../schemas/uc.schema.json",
  "id": "17.3.7",
  "title": "Device Certificate Expiration and Renewal",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Availability"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Device Certificate Expiration and Renewal' only. Does not replace full NAC policy design or firewall rule authoring or regulatory attestation. Entities listed in `network_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `network_device_certificate_expiration_and_renewal` (UC-17.3.7), SOC dashboard panel \"Network — Device Certificate Expiration and Renewal\", and weekly export to `index=audit_evidence` sourcetype=csv:network_controls.",
  "controlTest": {
    "positiveScenario": "UC-17.3.7 positive (Device, Certificate, Expiration, and, Renewal): ingest synthetic events into `index=zt` sourcetype='device:cert' where `count` grouped by `entity` satisfies `days_left` < 30 for 'Device Certificate Expiration and Renewal'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-17.3.7 negative (and Renewal): ingest benign `zt` / `device:cert` events where `entity` stays below `days_left` < 30, or exclude the test entity via `network_exception_register.csv`. Variant 13 confirms zero alert rows for 'Device Certificate Expiration and Renewal'."
  },
  "dataSources": "Certificate validity, renewal requests, enrollment events Indexes: zt. Sourcetypes: device:cert. Key fields: days_left. Validate field extraction in Search before saving the alert; compare event counts to the vendor admin console.",
  "app": "PKI/certificate inventory, `Splunk_TA_zscaler`, Netskope Add-on for Splunk (Splunkbase 3808), `Splunk_TA_paloalto` (Prisma Access)",
  "spl": "index=zt sourcetype=\"device:cert\"\n| eval days_left=floor((expiry_time-now())/86400)\n| where days_left < 30 OR renewal_status=\"failed\"\n| table device_id, cn, expiry_time, days_left, renewal_status\n| sort days_left",
  "description": "Detects UC-17.3.7 — Device, Certificate, Expiration, and, Renewal — using `index=zt` sourcetype `device:cert`, grouped by `entity`. Outputs `count` each `the scheduled window`; alert when `days_left` < 30. Primary focus: and Renewal. Purpose-built for 'Device Certificate Expiration and Renewal' in Network workflows.",
  "value": "UC-17.3.7 gives Network teams earlier visibility into and Renewal through `device:cert` feeds, lowering unauthorized access, segmentation drift, and NAC policy failures.",
  "implementation": "Ingest device certificate inventory and renewal events. Alert when cert expires in <30 days or renewal fails. Report on cert distribution and renewal success rate. Automate renewal where possible.",
  "visualization": "Table (certs expiring soon), Single value (failed renewals), Bar chart (expiry by month).",
  "cimModels": [],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Authentication.Authentication\n  by Authentication.user, Authentication.action, Authentication.src span=1h\n| sort - count",
  "dataModelAcceleration": "Enable for Authentication (Authentication).",
  "references": [
    {
      "title": "Netskope Add-on for Splunk",
      "url": "https://splunkbase.splunk.com/app/3808",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk_TA_paloalto",
      "url": "https://splunkbase.splunk.com/app/2757",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `zt` (UC-17.3.7)** — Planned work for 'Device Certificate Expiration and Renewal' shifts `count` over `span=1h` while `host` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `device:cert`** — Scheduled automation elevates `days_left` without crossing `days_left` < 30 for UC-17.3.7. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `host`** — Health checks in `zt` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `device:cert`** — First 48h baseline for UC-17.3.7 on `host` is unstable; window via `network_exception_register.csv`.\n\n**Suppression mechanism:** `network_exception_register.csv` keyed on `host`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1573.002"
  ],
  "equipment": [
    "netskope",
    "nutanix",
    "paloalto",
    "zscaler"
  ],
  "equipmentModels": [
    "paloalto_pan_firewall",
    "paloalto_prisma"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Certificate lifecycle data from PKI/MDM/ZTNA platforms. Microsoft Intune/SCEP: `sourcetype=ms:intune:certificates`. Cisco ISE pxGrid: certificate status events. Zscaler ZPA: client certificate events. Key fields: `serialNumber`, `subjectName`, `issuerName`, `notBefore`, `notAfter`, `status` (valid/expired/revoked), `renewalStatus`.\n• Device certificates authenticate devices in Zero Trust (mTLS, EAP-TLS, ZTNA client). Expired certificates = devices lose access. Mass expiry = service outage.\n• Create a `cert_renewal_sla.csv` lookup: `cert_template,auto_renewal,renewal_days_before_expiry,escalation_contact`.\n\nStep 1 — Configure data collection\nVerify certificate data:\n```spl\nindex=certificates earliest=-30d\n| where isnotnull(notAfter)\n| stats count by status\n```\n\nStep 2 — Create the search and alert\n\nPrimary search — Certificate expiration forecast:\n```spl\nindex=certificates\n| dedup serialNumber sortby -_time\n| eval expiry_epoch=strptime(notAfter, \"%Y-%m-%dT%H:%M:%S\")\n| eval days_to_expiry=round((expiry_epoch - now()) / 86400, 0)\n| eval urgency=case(\n    days_to_expiry < 0, \"EXPIRED\",\n    days_to_expiry <= 7, \"Critical (<=7d)\",\n    days_to_expiry <= 30, \"Warning (<=30d)\",\n    days_to_expiry <= 90, \"Plan (<=90d)\",\n    1==1, \"OK\"\n  )\n| stats count by urgency\n| sort urgency\n```\n\nUnderstanding this SPL: Categorizes all device certificates by days to expiry. The 'EXPIRED' and 'Critical' buckets require immediate action. 'Warning' is the ideal renewal window. A large 'Warning' bucket means the auto-renewal process may be failing — devices that should have renewed have not. Mass certificate expiry often happens when the issuing CA certificate expires first, invalidating all child certificates.\n\nRenewal failure tracking:\n```spl\nindex=certificates earliest=-30d\n| where renewalStatus=\"Failed\"\n| stats count by subjectName, issuerName\n| sort - count\n```\n\nStep 3 — Validate\n(a) Check PKI/CA console for certificate expiry list. Compare with Splunk.\n(b) Verify a test certificate's `notAfter` date matches Splunk.\n\nStep 4 — Operationalize\nDashboard: Row 1 — Expiry forecast (urgency distribution). Row 2 — Renewal failures. Row 3 — Certificates expiring this week.\nAlert: Daily report of certificates expiring within 30 days. Immediate for certificates expiring within 7 days.\n\nRunbook:\n1. **Mass expiry** — Check issuing CA certificate expiry. If CA cert expired, all child certs fail.\n2. **Renewal failures** — Check SCEP/NDES infrastructure. The enrollment server may be down.\n3. **Specific device expired** — Push manual renewal via MDM.\n\nStep 5 — Troubleshooting\n• **No certificate data** — PKI/MDM may not export certificate lifecycle events. Configure Intune diagnostic logs or ADCS event forwarding.\n• **`notAfter` format varies** — Normalize the date format in props.conf or at search time.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (PKI/certificate inventory, `Splunk_TA_zscaler`, Netskope Add-on for Splunk (Splunkbase 3808), `Splunk_TA_paloalto` (Prisma Access)). Enable the modular input or HEC token that writes sourcetype=device:cert into zt. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`days_left`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Every device has a digital ID card with an expiration date. We watch all these expiration dates and send warnings when cards are about to expire, because if too many expire at once, those devices suddenly lose access to everything.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
