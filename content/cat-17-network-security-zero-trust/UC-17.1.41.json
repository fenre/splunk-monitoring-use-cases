{
  "$schema": "../../schemas/uc.schema.json",
  "id": "17.1.41",
  "title": "Threat-Centric NAC (TC-NAC) — STIX/TAXII Feed and Auto-Quarantine Effectiveness",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Security",
    "Operations"
  ],
  "splunkPillar": "Security",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'Threat-Centric NAC (TC-NAC) — STIX/TAXII Feed and Auto-Quarantine Effectiveness'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-17.1.41).",
  "evidence": "Saved search uc_17_1_41_threat_centric_nac_tc_nac_stix_taxii_fee, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "compliance": [
    {
      "regulation": "pci-dss",
      "version": "v4.0",
      "clause": "11.3",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "PCI 11.3 requires vulnerability-management identification and remediation. TC-NAC ties vuln-scanner output to network-access enforcement; pipeline-health monitoring evidences the vuln-to-enforcement link is operational.",
      "controlObjective": "Operational integrity of vuln-to-enforcement pipeline.",
      "evidenceArtifact": "UC-17.1.41 alert log + monthly TC-NAC operational report.",
      "obligationRef": "pci-dss@v4.0#11.3",
      "provenance": "maintainer"
    },
    {
      "regulation": "nis2",
      "version": "Directive (EU) 2022/2555",
      "clause": "Art.21(2)(e)",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Art.21(2)(e) covers vulnerability handling and disclosure. TC-NAC operational health evidences the enforcement arm of the vuln-handling process.",
      "controlObjective": "Operational vuln-to-enforcement integration.",
      "evidenceArtifact": "UC-17.1.41 alert log.",
      "obligationRef": "nis2@Directive (EU) 2022/2555#Art.21(2)(e)",
      "provenance": "maintainer",
      "clauseUrl": "https://eur-lex.europa.eu/eli/dir/2022/2555/oj"
    },
    {
      "regulation": "nist-800-53",
      "version": "Rev. 5",
      "clause": "RA-5",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "RA-5 (Vulnerability Monitoring and Scanning) requires the system scan for vulns and remediate. TC-NAC's role bridges scanning and enforcement; pipeline monitoring evidences the bridge is operational.",
      "controlObjective": "Operational integrity of scan-to-enforce bridge.",
      "evidenceArtifact": "UC-17.1.41 alert log.",
      "obligationRef": "nist-800-53@Rev. 5#RA-5",
      "provenance": "maintainer"
    },
    {
      "regulation": "iso-27001",
      "version": "2022",
      "clause": "A.8.8",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "A.8.8 (Management of technical vulnerabilities) — TC-NAC is the operational link.",
      "controlObjective": "Operational vuln management bridge.",
      "evidenceArtifact": "UC-17.1.41 alert log.",
      "obligationRef": "iso-27001@2022#A.8.8",
      "provenance": "maintainer"
    }
  ],
  "controlTest": {
    "positiveScenario": "Disable a TC-NAC adapter (e.g. stop the Qualys API integration). ISE emits 83104 (adapter connection lost). UC-17.1.41 fires showing that vendor with adapter_drops > 0.",
    "negativeScenario": "Re-enable the adapter and confirm ISE emits a new threat sync without errors. Alert clears."
  },
  "dataSources": "ISE syslog `index=nac sourcetype=cisco:ise:syslog` *Threat-Centric NAC* (TC-NAC) category — codes `83100` (TC-NAC threat detected), `83101` (TC-NAC quarantine action taken), `83102` (TC-NAC vulnerability score updated), `83103` (TC-NAC feed import failed), `83104` (TC-NAC adapter connection lost). Vendor-side feeds (Qualys VM, Tenable Nessus, Rapid7 InsightVM, Cisco AMP for Endpoints) feed ISE via the TC-NAC adapters. ISE OpenAPI `/api/v1/threatlist/source` and `/api/v1/threatlist/threat` polled every 30 min for the threat-list catalogue and recent threat detections, written to `index=nac sourcetype=cisco:ise:tcnac`. Fields: `endpoint_mac`, `threat_type`, `vendor`, `score`, `quarantine_state`. CIM mapping: `Alerts.severity`, `Alerts.signature` after a `props.conf` mapping.",
  "app": "`Splunk Add-on for Cisco Identity Services` ([Splunkbase 1915](https://splunkbase.splunk.com/app/1915)) for syslog. Custom OpenAPI poller for the TC-NAC catalogue feed.",
  "spl": "(index=nac sourcetype=\"cisco:ise:syslog\" MessageCode IN (83100, 83101, 83102, 83103, 83104))\n  OR (index=nac sourcetype=\"cisco:ise:tcnac\")\n| stats count latest(_time) as last_event, count(eval(MessageCode==\"83100\")) as detections, count(eval(MessageCode==\"83101\")) as quarantines, count(eval(MessageCode==\"83103\")) as feed_failures, count(eval(MessageCode==\"83104\")) as adapter_drops by vendor\n| eval coverage_ratio = round((quarantines / detections) * 100, 1)\n| where feed_failures > 0 OR adapter_drops > 0 OR (detections > 5 AND coverage_ratio < 90)",
  "description": "Detects TC-NAC adapter connection failures, threat-list import failures, and gaps in the detection-to-quarantine pipeline (where threats are detected by a vendor feed but ISE failed to apply the corresponding quarantine action).",
  "value": "TC-NAC is the link between the vulnerability scanner / EDR fabric and ISE network enforcement. When an adapter fails or the feed import errors, ISE's authorization decisions revert to the non-threat-aware baseline — vulnerable endpoints continue to receive full network access. This UC catches the silent integration break.",
  "implementation": "Confirm the *Threat-Centric NAC* category is at INFO. Deploy the OpenAPI poller every 30 min. Schedule the search every 15 min, search window `-1h@h to @h`, trigger on count > 0, throttle suppression `vendor` for 30 min.",
  "visualization": "(1) Single value: TC-NAC adapters healthy / total. (2) Sankey: vendor → threat_type → quarantine_state. (3) Top-N: endpoints with highest threat scores, last 7d. (4) Trend: detection-to-quarantine ratio over 30d.",
  "cimModels": [
    "Alerts"
  ],
  "dataModelAcceleration": "Optional. Alerts DM acceleration helps cross-correlation panels.",
  "references": [
    {
      "title": "Cisco ISE 3.4 — Threat-Centric NAC",
      "url": "https://www.cisco.com/c/en/us/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Cisco TC-NAC Adapter Catalog",
      "url": "https://www.cisco.com/c/en/us/products/security/identity-services-engine/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "mitreAttack": [
    "T1190",
    "T1059"
  ],
  "equipment": [
    "cisco",
    "qualys",
    "syslog",
    "tenable"
  ],
  "equipmentModels": [
    "cisco_ise"
  ],
  "status": "verified",
  "lastReviewed": "2026-05-06",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "detailedImplementation": "Prerequisites\n• ISE TC-NAC license (Premier tier).\n• At least one TC-NAC adapter configured (Qualys/Tenable/Rapid7/Cisco AMP).\n• `tcnac_adapters.csv` lookup with `vendor`, `is_critical`.\n\nStep 1 — Configure data collection\nEnable *Threat-Centric NAC* category at INFO. Deploy OpenAPI poller for `/api/v1/threatlist/source` and `/api/v1/threatlist/threat` every 30 min.\n\nStep 2 — Create the search\nSearch in `spl` field. Schedule every 15 min.\n\nStep 3 — Validate\nProvoke as in controlTest. Confirm alert.\n\nStep 4 — Operationalize\nDashboard \"ISE — TC-NAC Pipeline Health\":\n• Per-vendor adapter status.\n• Detection-to-quarantine ratio trend.\n• Top-N endpoints by threat score.\n\nAlerting: PagerDuty medium on adapter drop. Slack on detection-to-quarantine ratio drop.\n\nStep 5 — Troubleshooting\n• **Adapter drops every hour** — vendor API rate limit. Tune polling interval.\n• **Detection-to-quarantine ratio = 0** — Authorization policy not consuming TC-NAC threat score; review the auth policy conditions.\n• **Feed-import failures** — vendor TLS cert change. Re-import vendor cert into ISE Trusted Certs.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk Add-on for Cisco Identity Services` ([Splunkbase 1915](https://splunkbase.splunk.com/app/1915)) for syslog. Custom OpenAPI poller for the TC-NAC catalogue feed.). Enable the modular input or HEC token that writes sourcetype=cisco:ise:syslog, cisco:ise:tcnac into nac. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`adapter_drops, coverage_ratio, detections, feed_failures`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "When a vulnerability scanner finds a sick computer, ISE should quarantine it. We check that the message gets through from the scanner to ISE — and ring the bell if the connection is broken or the quarantine never happens.",
  "prerequisiteUseCases": [],
  "wave": "run"
}
