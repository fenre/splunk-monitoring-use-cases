{
  "$schema": "../../schemas/uc.schema.json",
  "id": "17.3.43",
  "title": "FortiSASE Admin Configuration Audit (Fortinet)",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Security",
    "Configuration",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'FortiSASE Admin Configuration Audit (Fortinet)' only. Does not replace full NAC policy design or firewall rule authoring or regulatory attestation. Entities listed in `network_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `network_fortisase_admin_configuration_audit_fortinet` (UC-17.3.43), SOC dashboard panel \"Network — FortiSASE Admin Configuration Audit (Fortinet)\", and weekly export to `index=audit_evidence` sourcetype=csv:network_controls.",
  "controlTest": {
    "positiveScenario": "UC-17.3.43 positive (FortiSASE, Admin, Configuration, Audit, (Fortinet)): ingest synthetic events into `index=sase` sourcetype='fgt_event' where `values` grouped by `user`, `ui` satisfies -30d for 'FortiSASE Admin Configuration Audit (Fortinet)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-17.3.43 negative (Audit (Fortinet)): ingest benign `sase` / `fgt_event` events where `user`, `ui` stays below -30d, or exclude the test entity via `network_exception_register.csv`. Variant 9 confirms zero alert rows for 'FortiSASE Admin Configuration Audit (Fortinet)'."
  },
  "dataSources": "`sourcetype=fgt_event` subtype=system, logid related to admin/config Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`TA-fortinet_fortigate` (FortiSASE admin audit) — Universal Forwarder (Splunkbase 617)",
  "spl": "index=sase sourcetype=\"fgt_event\" subtype=\"system\" earliest=-30d\n| where match(lower(msg),\"(?i)config|policy|rule|admin.*login|object.*modified\")\n| stats count earliest(_time) as first latest(_time) as last values(msg) as actions by user, ui\n| sort -last",
  "description": "Identifies UC-17.3.43 — FortiSASE, Admin, Configuration, Audit, (Fortinet) — using `index=sase` sourcetype `fgt_event`, grouped by `user`, `ui`. Outputs `values` each `the scheduled window`; alert when -30d. Primary focus: Audit (Fortinet). Purpose-built for 'FortiSASE Admin Configuration Audit (Fortinet)' in Network workflows.",
  "value": "Operators running UC-17.3.43 on `fgt_event` feeds shorten triage for Audit (Fortinet) tied to 'FortiSASE Admin Configuration Audit (Fortinet)', lowering unauthorized access, segmentation drift, and NAC policy failures.",
  "implementation": "Forward FortiSASE management audit events. Alert on after-hours changes or bulk policy modifications. Correlate with ITSM change tickets.",
  "visualization": "Table (admin changes), Timeline (configuration events), Bar chart (changes by admin).",
  "cimModels": [
    "Change",
    "Authentication"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Authentication.Authentication\n  by Authentication.user, Authentication.action, Authentication.src span=1h\n| sort - count",
  "dataModelAcceleration": "Enable for Authentication (Authentication).",
  "references": [
    {
      "title": "CIM: Change",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Change",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Authentication",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Authentication",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `sase` (UC-17.3.43)** — Planned work for 'FortiSASE Admin Configuration Audit (Fortinet)' shifts `values` over `span=1h` while `user`, `ui` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `values` on `fgt_event`** — Scheduled automation elevates `ui` without crossing configured threshold for UC-17.3.43. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `user`, `ui`** — Health checks in `sase` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `fgt_event`** — First 48h baseline for UC-17.3.43 on `user` is unstable; window via `network_exception_register.csv`.\n\n**Suppression mechanism:** `network_exception_register.csv` keyed on `user`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1562.004",
    "T1133"
  ],
  "equipment": [
    "fortinet",
    "m365"
  ],
  "equipmentModels": [
    "fortinet_fortigate"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• FortiSASE admin audit events in `index=fortisase` with `sourcetype=fortisase:event` or `sourcetype=fortisase:audit`. Key fields: `user` (admin), `action` (login/logout/add/edit/delete), `msg` (description), `cfgpath` (configuration path changed), `cfgobj` (object name), `devname`.\n• FortiSASE configuration changes can have immediate impact: modifying a security profile, changing ZTNA rules, or altering SD-WAN policies affect all connected users and sites.\n\nStep 1 — Configure data collection\nVerify admin audit events:\n```spl\nindex=fortisase sourcetype IN (\"fortisase:event\",\"fortisase:audit\") earliest=-30d\n| where isnotnull(cfgpath) OR match(action, \"(?i)add|edit|delete\")\n| stats count by action, cfgpath\n```\n\nStep 2 — Create the search and alert\n\nPrimary search — Configuration change audit:\n```spl\nindex=fortisase sourcetype IN (\"fortisase:event\",\"fortisase:audit\") earliest=-7d\n| where isnotnull(cfgpath) OR match(action, \"(?i)add|edit|delete\")\n| eval change_hour=strftime(_time, \"%H\")\n| eval out_of_hours=if(change_hour<\"08\" OR change_hour>\"18\", \"YES\", \"no\")\n| table _time, user, action, cfgpath, cfgobj, msg, out_of_hours\n| sort - _time\n```\n\nUnderstanding this SPL: Lists all FortiSASE configuration changes with timing. Changes to `firewall/policy`, `webfilter/profile`, `ips/sensor`, or `ztna/rule` are high-impact. Out-of-hours changes are flagged. A deleted security profile may leave policies without protection.\n\nHigh-impact changes:\n```spl\nindex=fortisase sourcetype IN (\"fortisase:event\",\"fortisase:audit\") earliest=-7d\n| where match(cfgpath, \"(?i)policy|profile|sensor|ztna|security\") OR action=\"delete\"\n| stats count by user, action, cfgpath\n| sort - count\n```\n\nStep 3 — Validate\n(a) Make a test config change. Verify audit event.\n(b) In FortiSASE: Log & Report > Event Log > System Events.\n\nStep 4 — Operationalize\nDashboard: Row 1 — Changes per day. Row 2 — High-impact changes. Row 3 — Out-of-hours.\nAlert: Immediate on security profile deletion.\n\nRunbook:\n1. **Profile deleted** — May break dependent policies. Verify and restore.\n2. **Out-of-hours change** — Verify with admin. Document justification.\n3. **Many changes by one admin** — Could be automation or a bulk mistake.\n\nStep 5 — Troubleshooting\n• **No audit events** — Event logging may be filtered. Check FortiSASE log settings.\n• **`cfgpath` empty** — Login/logout events don't include config paths. Filter for config changes only.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`TA-fortinet_fortigate` (FortiSASE admin audit) — Universal Forwarder (Splunkbase 617)). Enable the modular input or HEC token that writes sourcetype=fgt_event into sase. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`ui, user`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Every time an administrator changes our FortiSASE security settings, we record it. We especially watch for deletions of security rules and changes made at unusual hours, because a single wrong change to our cloud security system can affect every employee at once.",
  "hardware": "FortiSASE (cloud management plane)",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
