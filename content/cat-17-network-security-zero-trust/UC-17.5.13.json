{
  "$schema": "../../schemas/uc.schema.json",
  "id": "17.5.13",
  "title": "Forcepoint Agent Health and Enrollment Coverage (`forcepoint:one`)",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Configuration"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Forcepoint Agent Health and Enrollment Coverage (`forcepoint:one`)' only. Does not replace full NAC policy design or firewall rule authoring or regulatory attestation. Entities listed in `network_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `network_forcepoint_agent_health_and_enrollment_coverage_` (UC-17.5.13), SOC dashboard panel \"Network — Forcepoint Agent Health and Enrollment Coverage (`forcepoint:one`)\", and weekly export to `index=audit_evidence` sourcetype=csv:network_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=sase` sourcetype='forcepoint:one' where `hostname` satisfies detection threshold for UC-17.5.13 ('Forcepoint Agent Health and Enrollment Coverage (`forcepoint:one`)'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `sase` / `forcepoint:one` events where `hostname` stays below detection threshold, or exclude the test entity via `network_exception_register.csv` with a valid window. Confirm zero alert rows for UC-17.5.13."
  },
  "dataSources": "Forcepoint cloud SSE (`forcepoint:one`), on-prem Web Security (`websense:cg:kv`), Email (`forcepoint:email:kv`), DLP (`forcepoint:dlp`)",
  "app": "Forcepoint Insights SIEM App (Splunkbase 8053); Forcepoint Web Security TA (`websense:cg:kv`); Email Security KV (`forcepoint:email:kv`)",
  "spl": "index=sase sourcetype=\"forcepoint:one\" earliest=-4h\n| where match(lower(event_type),\"(?i)agent|endpoint\")\n| stats latest(status) as st latest(_time) as seen by hostname, agent_version\n| where st!=\"healthy\" OR seen < relative_time(now(),\"-2h\")\n| sort seen",
  "description": "Agents off-patch or offline bypass web and DLP controls at the endpoint. This use case monitors the condition described in the title using the SPL and data sources documented on this page.",
  "value": "Agents off-patch or offline bypass web and DLP controls at the endpoint. Timely UC-17.5.13 alerting on 'Forcepoint Agent Health and Enrollment Coverage (`forcepoint:one`)' reduces unauthorized access, segmentation drift, and NAC policy failures before manual triage or audit channels escalate.",
  "implementation": "Install the Insights SIEM app, configure HEC inputs per product line, and align indexes (`proxy`, `email`, `dlp`, `sase`). Normalize usernames to enterprise SAMAccountName.",
  "visualization": "Single value (coverage %), table.",
  "cimModels": [],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Email.All_Email\n  by Email.user, Email.action, Email.src, Email.dest span=1h\n| sort - count",
  "dataModelAcceleration": "Enable for Email (Email).",
  "references": [
    {
      "title": "Splunkbase — Forcepoint Insights SIEM App",
      "url": "https://splunkbase.splunk.com/app/8053",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Add-on for Forcepoint Web Security",
      "url": "https://splunkbase.splunk.com/app/3245",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `sase`** — Planned work for 'Forcepoint Agent Health and Enrollment Coverage (`forcepoint:one`)' shifts `count` over `span=1h` while `hostname` remains in-scope for UC-17.5.13. Cross-check CMDB CHG.\n\n2. **Batch `count` on `forcepoint:one`** — Scheduled automation elevates `agent_version` without crossing configured threshold. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `hostname`** — Health checks in `sase` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `forcepoint:one`** — First 48h baseline for `hostname` is unstable; window via `network_exception_register.csv`.\n\n**Suppression mechanism:** `network_exception_register.csv` keyed on `hostname`, `exception_reason`, `valid_until`.",
  "securityDomain": "network",
  "equipment": [
    "forcepoint"
  ],
  "equipmentModels": [
    "forcepoint_generic"
  ],
  "status": "verified",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "Prerequisites\n• Forcepoint Insights SIEM App (Splunkbase 8053) or direct integration from Forcepoint ONE SSE.\n• Data in `index=sase` with `sourcetype=forcepoint:one`. Key fields: `event_type` (agent/endpoint), `hostname`, `agent_version`, `agent_status` (active/inactive/disconnected), `os_type`, `os_version`, `user`, `last_checkin`, `enrollment_date`.\n• Forcepoint ONE SSE endpoint agent deployed to managed devices for traffic steering and device posture assessment.\n• Create a `fp_expected_endpoints.csv` lookup: `hostname,user,department,expected` from HR/CMDB systems.\n\nStep 1 — Configure data collection\nVerify agent health events:\n```spl\nindex=sase sourcetype=\"forcepoint:one\" earliest=-4h\n| where match(lower(event_type), \"agent|endpoint\")\n| stats dc(hostname) as endpoints count by agent_status\n```\n\nStep 2 — Create the search and alert\n\nPrimary search — Agent enrollment coverage:\n```spl\n| inputlookup fp_expected_endpoints.csv where expected=\"yes\"\n| join type=left hostname [\n    search index=sase sourcetype=\"forcepoint:one\" earliest=-4h\n    | where match(lower(event_type), \"agent|endpoint\")\n    | stats latest(agent_status) as status latest(agent_version) as version latest(last_checkin) as checkin by hostname\n]\n| eval enrolled=if(isnotnull(status) AND status=\"active\", \"YES\", \"NO\")\n| stats count(eval(enrolled=\"YES\")) as enrolled count(eval(enrolled=\"NO\")) as missing by department\n| eval coverage_pct=round(enrolled/(enrolled+missing)*100, 1)\n| sort coverage_pct\n```\n\nUnderstanding this SPL: Compares expected endpoints from HR/CMDB against actual agent enrollment. Gaps mean devices are accessing corporate resources without going through the SSE — bypassing web filtering, DLP, and CASB controls. Coverage below 90% significantly reduces the value of SSE-based security controls.\n\nStale agents (enrolled but not checking in):\n```spl\nindex=sase sourcetype=\"forcepoint:one\" earliest=-7d\n| where match(lower(event_type), \"agent|endpoint\")\n| stats latest(_time) as last_seen latest(agent_status) as status by hostname, user\n| eval days_since=round((now() - last_seen)/86400, 0)\n| where days_since > 3 AND status!=\"active\"\n| sort - days_since\n```\n\nStep 3 — Validate\n(a) In Forcepoint ONE portal: Endpoints > view enrolled devices. Compare with Splunk.\n(b) Cross-reference gap devices with AD/CMDB to confirm they are active and should be enrolled.\n\nStep 4 — Operationalize\nDashboard (\"Forcepoint — Agent Coverage\"):\n• Row 1 — Single-value: \"Enrollment rate (%)\", \"Missing endpoints\", \"Stale agents\".\n• Row 2 — Bar chart: coverage by department.\n• Row 3 — Table: unenrolled/stale endpoints.\n\nAlert: Weekly coverage report. Immediate alert if coverage drops below 85%.\n\nRunbook:\n1. **Missing endpoints** — Deploy the Forcepoint agent via MDM (Intune, Jamf) or GPO.\n2. **Stale agents** — The device may be offline, decommissioned, or the agent crashed. Check device status.\n3. **Coverage improving slowly** — Automate agent deployment in the device onboarding process.\n\nStep 5 — Troubleshooting\n• **Agent count in Splunk lower than portal** — TA poll interval may not have captured recent enrollments.\n• **`fp_expected_endpoints.csv` stale** — Automate the lookup from CMDB/AD exports.\n• **Agent enrolled but not active** — The agent may be installed but not connected. Check network/firewall rules allowing agent-to-cloud communication.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (Forcepoint Insights SIEM App (Splunkbase 8053); Forcepoint Web Security TA (`websense:cg:kv`); Email Security KV (`forcepoint:email:kv`)). Enable the modular input or HEC token that writes sourcetype=forcepoint:one into sase. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`agent_version, hostname`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We make sure every company laptop and phone has the security software installed that routes internet traffic through our protective system, because devices without it are unprotected.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
