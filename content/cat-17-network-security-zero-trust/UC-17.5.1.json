{
  "$schema": "../../schemas/uc.schema.json",
  "id": "17.5.1",
  "title": "Forcepoint Web Security URL Category Block Trending (`websense:cg:kv`)",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Forcepoint Web Security URL Category Block Trending (`websense:cg:kv`)' only. Does not replace full NAC policy design or firewall rule authoring or regulatory attestation. Entities listed in `network_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `network_forcepoint_web_security_url_category_block_trend` (UC-17.5.1), SOC dashboard panel \"Network — Forcepoint Web Security URL Category Block Trending (`websense:cg:kv`)\", and weekly export to `index=audit_evidence` sourcetype=csv:network_controls.",
  "controlTest": {
    "positiveScenario": "UC-17.5.1 positive (Forcepoint, Web, Security, URL, Category): ingest synthetic events into `index=proxy` sourcetype='websense:cg:kv' where `count` grouped by `category` satisfies -30d for 'Forcepoint Web Security URL Category Block Trending (`websense:cg:kv`)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-17.5.1 negative (Trending (`websense:cg:kv`)): ingest benign `proxy` / `websense:cg:kv` events where `category` stays below -30d, or exclude the test entity via `network_exception_register.csv`. Variant 12 confirms zero alert rows for 'Forcepoint Web Security URL Category Block Trending (`websense:cg:kv`)'."
  },
  "dataSources": "Forcepoint cloud SSE (`forcepoint:one`), on-prem Web Security (`websense:cg:kv`), Email (`forcepoint:email:kv`), DLP (`forcepoint:dlp`)",
  "app": "Forcepoint Insights SIEM App (Splunkbase 8053); Forcepoint Web Security TA (`websense:cg:kv`); Email Security KV (`forcepoint:email:kv`)",
  "spl": "index=proxy sourcetype=\"websense:cg:kv\" earliest=-30d@d\n| where match(lower(action),\"(?i)block|deny\")\n| timechart span=1d count by category",
  "description": "Detects UC-17.5.1 — Forcepoint, Web, Security, URL, Category — using `index=proxy` sourcetype `websense:cg:kv`, grouped by `category`. Outputs `count` each `1d`; alert when -30d. Primary focus: Trending (`websense:cg:kv`). Purpose-built for 'Forcepoint Web Security URL Category Block Trending (`websense:cg:kv`)' in Network workflows.",
  "value": "Tracking `activity` via UC-17.5.1 on `websense:cg:kv` feeds improves response to 'Forcepoint Web Security URL Category Block Trending (`websense:cg:kv`)' and reduces unauthorized access, segmentation drift, and NAC policy failures.",
  "implementation": "Install the Insights SIEM app, configure HEC inputs per product line, and align indexes (`proxy`, `email`, `dlp`, `sase`). Normalize usernames to enterprise SAMAccountName.",
  "visualization": "Stacked area (categories), table (top users).",
  "cimModels": [
    "Web",
    "Email"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Email.All_Email\n  by Email.user, Email.action, Email.src, Email.dest span=1h\n| sort - count",
  "dataModelAcceleration": "Enable for Email (Email).",
  "references": [
    {
      "title": "Splunkbase — Forcepoint Insights SIEM App",
      "url": "https://splunkbase.splunk.com/app/8053",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Add-on for Forcepoint Web Security",
      "url": "https://splunkbase.splunk.com/app/3245",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Vulnerability scanner signature matches** — Qualys/Tenable traffic triggers IPS/threat signatures for 'Forcepoint Web Security URL Category Block Trending (`websense:cg:kv`)'. Compare `src` against `scanner_ips` lookup.\n\n2. **Policy push during change window** — New deny rule generates expected blocks. Match firewall commit timestamp in change calendar.\n\n3. **Encrypted traffic inspection gap** — TLS-heavy apps bypass deep inspection; volume drop is operational, not a sensor failure.\n\n4. **Red-team C2 simulation** — Approved exercise from `pen_test_hosts.csv`.\n\n**Suppression mechanism:** `network_exception_register.csv` keyed on `src`, `signature_id`, `valid_until`.",
  "securityDomain": "network",
  "equipment": [
    "forcepoint"
  ],
  "equipmentModels": [
    "forcepoint_generic"
  ],
  "status": "verified",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "Prerequisites\n• Splunk Add-on for Forcepoint Web Security (Splunkbase, built by Splunk) installed on the Search Head and Heavy Forwarder. The TA provides field extractions for the `websense:cg:kv` sourcetype.\n• Forcepoint Web Security (on-prem Content Gateway or cloud SWG) configured to send syslog to the Heavy Forwarder. The Content Gateway exports key-value formatted logs via syslog (UDP/TCP 514) or file export.\n• Data lands in `index=proxy` with `sourcetype=websense:cg:kv`. Key fields from the TA: `action` (Permitted/Blocked), `category` (URL category name), `user` (authenticated username), `url`, `src_ip`, `dest_ip`, `http_method`, `bytes_in`, `bytes_out`, `disposition` (numeric action code), `http_response` (status code).\n• The TA maps fields to CIM Web data model: `action`, `category`, `url`, `src`, `dest`, `http_method`, `bytes_in`, `bytes_out`, `user`, `status`.\n• Create a `fp_category_policy.csv` lookup: `category,risk_level,policy_owner,expected_action` for enrichment.\n\nStep 1 — Configure data collection\nOn the Forcepoint Content Gateway: Administration > Log Settings > SIEM Integration > configure syslog destination (Heavy Forwarder IP, port 514, KV format). Enable all log fields.\n\nOn the Heavy Forwarder, add to `inputs.conf`:\n```\n[udp://514]\nsourcetype = websense:cg:kv\nindex = proxy\n```\n\nVerify data arrival:\n```spl\nindex=proxy sourcetype=\"websense:cg:kv\" earliest=-15m\n| stats count by action, category\n```\nYou should see Permitted and Blocked actions across various URL categories (Business, Social Networking, Malware, etc.). If empty: check syslog connectivity, verify the Content Gateway is sending logs, and confirm the HF port is open.\n\nStep 2 — Create the search and alert\n\nPrimary search — URL category block trending:\n```spl\nindex=proxy sourcetype=\"websense:cg:kv\" earliest=-30d\n| where match(lower(action), \"block|deny\")\n| timechart span=1d count by category limit=15 useother=t\n```\n\nUnderstanding this SPL: Tracks daily block volume per URL category over 30 days. Gradual increases in a specific category (e.g., 'Malware' or 'Phishing') indicate an emerging campaign targeting your users. Sudden spikes in 'Uncategorized' suggest users are finding new evasion sites faster than the category database updates. A decline in blocks for a category you expect to block (e.g., 'Gambling') may mean the policy was relaxed or users switched to encrypted channels not inspected.\n\nTop blocked users:\n```spl\nindex=proxy sourcetype=\"websense:cg:kv\" earliest=-7d\n| where match(lower(action), \"block|deny\")\n| stats count as blocks dc(url) as unique_urls by user, category\n| sort - blocks\n| head 25\n```\n\nStep 3 — Validate\n(a) In Forcepoint Security Manager: Reporting > Web Security > Blocked Requests by Category. Compare the top categories and volumes with Splunk.\n(b) Test: browse to a known blocked category site (e.g., a test malware URL from EICAR). Verify the block event appears in Splunk with the correct `category` and `action=Blocked`.\n(c) Verify CIM mapping: `| datamodel Web search | head 5` should return events with populated `action`, `url`, `category` fields.\n\nStep 4 — Operationalize\nDashboard (\"Forcepoint — Web Security Blocks\"):\n• Row 1 — Single-value tiles: \"Total blocks (24h)\", \"Top blocked category\", \"Users generating blocks\".\n• Row 2 — Timechart: blocks by category over 30 days.\n• Row 3 — Table: top blocked users, categories, block count, unique URLs.\n\nAlert: Trigger when 'Malware' or 'Phishing' category blocks increase 3x over the 7-day average in any 4h window.\n\nRunbook:\n1. **Malware/Phishing spike** — An active campaign is targeting users. Cross-reference blocked URLs with threat intelligence. Alert the SOC and send a user awareness notice.\n2. **'Uncategorized' surge** — New evasion sites. Submit the URLs to Forcepoint for categorization. Consider tightening the 'Uncategorized' policy to 'Block' or 'Warn'.\n3. **Single user generating excessive blocks** — Compromised endpoint or intentional policy evasion. Investigate the user's device for malware.\n\nStep 5 — Troubleshooting\n• **No block events** — The blocking policy may be in 'log only' mode. Check Forcepoint Security Manager > Policy > verify action is 'Block' not 'Log Only'.\n• **`category` field is empty** — The Content Gateway is not including category information in the syslog. Check the log format configuration — ensure 'URL Category' is in the exported fields.\n• **Block counts in Splunk lower than Forcepoint reports** — Syslog may be dropping packets (UDP). Switch to TCP syslog or use the file export method.",
  "grandmaExplanation": "We track which types of websites our web filter blocks most often, like harmful or inappropriate sites, so we can see if new threats are increasing and whether anyone is repeatedly trying to visit blocked places.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
