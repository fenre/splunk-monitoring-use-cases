{
  "$schema": "../../schemas/uc.schema.json",
  "id": "17.5.12",
  "title": "Forcepoint Administrator Configuration Change Audit (`forcepoint:one`)",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Audit",
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'Forcepoint Administrator Configuration Change Audit (`forcepoint:one`)'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-17.5.12).",
  "evidence": "Saved search uc_17_5_12_forcepoint_administrator_configuration_c, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'Forcepoint Administrator Configuration Change Audit (`forcepoint:one`)'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-17.5.12.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "Forcepoint cloud SSE (`forcepoint:one`), on-prem Web Security (`websense:cg:kv`), Email (`forcepoint:email:kv`), DLP (`forcepoint:dlp`)",
  "app": "Forcepoint Insights SIEM App (Splunkbase 8053); Forcepoint Web Security TA (`websense:cg:kv`); Email Security KV (`forcepoint:email:kv`)",
  "spl": "index=sase sourcetype=\"forcepoint:one\" earliest=-24h\n| where match(lower(event_type),\"(?i)admin|config|policy.publish\")\n| stats count by admin_user, object, action\n| sort -count",
  "description": "SSE consoles are high-value targets; immutable logs of admin edits support insider investigations.",
  "value": "Security governance teams audit all administrator configuration changes to detect unauthorized policy relaxations, track who disabled protections and when, and verify changes originate from expected admin workstations.",
  "implementation": "Install the Insights SIEM app, configure HEC inputs per product line, and align indexes (`proxy`, `email`, `dlp`, `sase`). Normalize usernames to enterprise SAMAccountName.",
  "visualization": "Timeline, table.",
  "cimModels": [],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Email.All_Email\n  by Email.user, Email.action, Email.src, Email.dest span=1h\n| sort - count",
  "dataModelAcceleration": "Enable for Email (Email).",
  "references": [
    {
      "title": "Splunkbase — Forcepoint Insights SIEM App",
      "url": "https://splunkbase.splunk.com/app/8053",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Add-on for Forcepoint Web Security",
      "url": "https://splunkbase.splunk.com/app/3245",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "securityDomain": "network",
  "equipment": [
    "forcepoint"
  ],
  "equipmentModels": [
    "forcepoint_generic"
  ],
  "status": "verified",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "catalog-team",
  "detailedImplementation": "Prerequisites\n• Forcepoint Insights SIEM App (Splunkbase 8053) or direct integration from Forcepoint ONE SSE.\n• Data in `index=sase` with `sourcetype=forcepoint:one`. Key fields: `event_type` (admin/config), `admin_user`, `action` (create/modify/delete), `config_section` (policy/user/application/network), `old_value`, `new_value`, `src_ip`, `timestamp`.\n• Forcepoint ONE SSE admin audit logging enabled (default).\n• Create a `fp_admins.csv` lookup: `admin_user,role,team,mfa_required` for admin context.\n\nStep 1 — Configure data collection\nVerify admin audit events:\n```spl\nindex=sase sourcetype=\"forcepoint:one\" earliest=-24h\n| where match(lower(event_type), \"admin|config\")\n| stats count by admin_user, action, config_section\n```\n\nStep 2 — Create the search and alert\n\nPrimary search — Configuration changes with risk assessment:\n```spl\nindex=sase sourcetype=\"forcepoint:one\" earliest=-24h\n| where match(lower(event_type), \"admin|config\")\n| eval risk=case(\n    action=\"delete\" AND config_section=\"policy\", \"HIGH\",\n    match(lower(new_value), \"disable|off|allow.*all|bypass\"), \"HIGH\",\n    action=\"modify\" AND config_section=\"policy\", \"MEDIUM\",\n    1==1, \"LOW\"\n)\n| lookup fp_admins.csv admin_user OUTPUT role team\n| stats count values(action) as actions values(config_section) as sections by admin_user, risk, src_ip, team\n| sort - risk, - count\n```\n\nUnderstanding this SPL: Classifies admin changes by risk. Policy deletions and changes that disable protections (disable SSL inspection, bypass DLP, allow-all rules) are high-risk. These changes could be intentional administration, or they could indicate a compromised admin account being used to weaken defenses before an attack.\n\nStep 3 — Validate\n(a) In Forcepoint ONE portal: Administration > Audit Log. Compare with Splunk.\n(b) Make a test configuration change and verify it appears in Splunk.\n\nStep 4 — Operationalize\nDashboard (\"Forcepoint — Admin Audit\"):\n• Row 1 — Single-value: \"Config changes (24h)\", \"High-risk changes\", \"Active admins\".\n• Row 2 — Table: changes with admin, action, section, risk, source IP.\n\nAlert: Immediate alert for any HIGH-risk change. Daily digest of all changes.\n\nRunbook:\n1. **Policy deleted** — Verify with the admin. Restore from backup if unintentional.\n2. **Protection disabled** — Investigate immediately. Confirm the admin account is not compromised.\n3. **Changes from unexpected IP** — The admin may be working remotely, or the account is compromised. Verify with the admin directly.\n\nStep 5 — Troubleshooting\n• **No admin events** — Audit logging may be disabled. Check Forcepoint ONE > Settings > Logging.\n• **`src_ip` not captured** — Some admin events may not include the source IP. This is a limitation of the platform.\n• **Automated changes flooding the log** — Filter out known automation accounts.",
  "grandmaExplanation": "We keep a record of every change an administrator makes to our security settings, so we can tell if someone weakened our defenses on purpose or by accident.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
