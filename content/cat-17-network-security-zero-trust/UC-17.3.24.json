{
  "$schema": "../../schemas/uc.schema.json",
  "id": "17.3.24",
  "title": "Conditional Access Policy Enforcement (Entra ID)",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Conditional Access Policy Enforcement (Entra ID)' only. Does not replace full NAC policy design or firewall rule authoring or regulatory attestation. Entities listed in `network_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `network_conditional_access_policy_enforcement_entra_id` (UC-17.3.24), SOC dashboard panel \"Network — Conditional Access Policy Enforcement (Entra ID)\", and weekly export to `index=audit_evidence` sourcetype=csv:network_controls.",
  "controlTest": {
    "positiveScenario": "UC-17.3.24 positive (Conditional, Access, Policy, Enforcement, (Entra): ingest synthetic events into `index=identity` sourcetype='azure:signin' where `count` grouped by `conditional_access_policy_name`, `conditional_access_status` satisfies -7d for 'Conditional Access Policy Enforcement (Entra ID)'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-17.3.24 negative ((Entra ID)): ingest benign `identity` / `azure:signin` events where `conditional_access_policy_name`, `conditional_access_status` stays below -7d, or exclude the test entity via `network_exception_register.csv`. Variant 5 confirms zero alert rows for 'Conditional Access Policy Enforcement (Entra ID)'."
  },
  "dataSources": "`sourcetype=azure:signin` with `conditional_access_status` Collected via Splunk Add-on for Microsoft Azure (Splunkbase 3110) modular input or scripted poll into the index named in the SPL.",
  "app": "Azure / Entra TA — Splunk Add-on for Microsoft Azure (Splunkbase 3110)",
  "spl": "index=identity sourcetype=\"azure:signin\" earliest=-7d\n| where isnotnull(conditional_access_policy_name)\n| stats count by conditional_access_policy_name, conditional_access_status\n| sort -count",
  "description": "Identifies 'Conditional Access Policy Enforcement (Entra ID)' patterns (UC-17.3.24) sourced from `index=identity` sourcetype `azure:signin`. Threshold bundle: -7d; aggregation grain: `conditional_access_policy_name`, `conditional_access_status`; metrics: `count`. Watch terms: Conditional, Access, Policy, Enforcement, (Entra.",
  "value": "UC-17.3.24 gives Network teams earlier visibility into (Entra ID) through `azure:signin` feeds, lowering unauthorized access, segmentation drift, and NAC policy failures.",
  "implementation": "Include `failureReason` for blocks. Alert when block rate for a policy jumps without change ticket.",
  "visualization": "Stacked bar (policy × status), Table (top blocks), Line chart (blocks / day per policy).",
  "cimModels": [
    "Authentication"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Authentication.Authentication\n  by Authentication.user, Authentication.action, Authentication.src span=1h\n| sort - count",
  "dataModelAcceleration": "Enable for Authentication (Authentication).",
  "references": [
    {
      "title": "CIM: Authentication",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Authentication",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 3110",
      "url": "https://splunkbase.splunk.com/app/3110",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `identity` (UC-17.3.24)** — Planned work for 'Conditional Access Policy Enforcement (Entra ID)' shifts `count` over `span=1h` while `conditional_access_policy_name`, `conditional_access_status` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `azure:signin`** — Scheduled automation elevates `conditional_access_status` without crossing configured threshold for UC-17.3.24. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `conditional_access_policy_name`, `conditional_access_status`** — Health checks in `identity` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `azure:signin`** — First 48h baseline for UC-17.3.24 on `conditional_access_policy_name` is unstable; window via `network_exception_register.csv`.\n\n**Suppression mechanism:** `network_exception_register.csv` keyed on `conditional_access_policy_name`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1021"
  ],
  "equipment": [
    "azure",
    "m365"
  ],
  "equipmentModels": [
    "azure_generic",
    "m365_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Microsoft Entra ID sign-in and audit logs in `index=azure`. `sourcetype=azure:aad:signin` for CA enforcement during sign-in, `sourcetype=azure:aad:audit` for CA policy changes. Key fields: `properties.conditionalAccessPolicies{}` (array with id, displayName, result, enforcedGrantControls, enforcedSessionControls), `properties.status.errorCode`, `properties.status.failureReason`.\n• This UC focuses specifically on Entra ID CA policy enforcement depth: not just whether a policy was applied, but which grant controls (MFA, compliant device, approved app) and session controls (sign-in frequency, persistent browser) were enforced.\n\nStep 1 — Configure data collection\nVerify CA enforcement detail:\n```spl\nindex=azure sourcetype=\"azure:aad:signin\" earliest=-24h\n| spath output=ca_policies path=properties.conditionalAccessPolicies{}\n| mvexpand ca_policies\n| spath input=ca_policies output=grant_controls path=enforcedGrantControls{}\n| stats count by grant_controls\n```\n\nStep 2 — Create the search and alert\n\nPrimary search — Grant control enforcement distribution:\n```spl\nindex=azure sourcetype=\"azure:aad:signin\" earliest=-7d\n| spath output=ca_policies path=properties.conditionalAccessPolicies{}\n| mvexpand ca_policies\n| spath input=ca_policies output=policy_name path=displayName\n| spath input=ca_policies output=policy_result path=result\n| spath input=ca_policies output=grant_controls path=enforcedGrantControls{}\n| spath input=ca_policies output=session_controls path=enforcedSessionControls{}\n| where policy_result=\"success\"\n| stats count dc(properties.userPrincipalName) as users by policy_name, grant_controls, session_controls\n| sort - count\n```\n\nUnderstanding this SPL: Shows which specific controls each CA policy enforces. If a policy named 'Require MFA for all users' shows `grant_controls=mfa` with 10,000 sign-ins, MFA is being enforced. If the same policy has `grant_controls=null`, MFA may not be configured correctly. Session controls like `signInFrequency` ensure tokens expire regularly.\n\nReport-only vs enforced policies:\n```spl\nindex=azure sourcetype=\"azure:aad:signin\" earliest=-24h\n| spath output=ca_policies path=properties.conditionalAccessPolicies{}\n| mvexpand ca_policies\n| spath input=ca_policies output=policy_name path=displayName\n| spath input=ca_policies output=policy_result path=result\n| stats count by policy_name, policy_result\n| eval mode=if(policy_result=\"reportOnlySuccess\" OR policy_result=\"reportOnlyFailure\", \"Report-Only\", \"Enforced\")\n| stats sum(count) as total by policy_name, mode\n| sort policy_name, mode\n```\n\nStep 3 — Validate\n(a) In Entra ID: Sign-in Logs > Conditional Access tab. Verify grant controls match Splunk.\n(b) Create a report-only CA policy and verify it appears as 'reportOnly' in Splunk.\n\nStep 4 — Operationalize\nDashboard: Row 1 — Grant control distribution. Row 2 — Report-only vs Enforced policies. Row 3 — Policies with no grant controls.\nAlert: When a policy enforces no grant controls (effectively a pass-through).\n\nRunbook:\n1. **Policy with no controls** — Misconfiguration. Review the CA policy conditions and grant section.\n2. **Report-only for > 30 days** — Should be promoted to enforced after validation.\n3. **Session controls missing** — Add sign-in frequency for sensitive apps.\n\nStep 5 — Troubleshooting\n• **`enforcedGrantControls` empty** — The policy may use only session controls (no grant). Check policy config.\n• **No report-only data** — Report-only mode requires Entra ID P1 license.",
  "grandmaExplanation": "We do not just check whether our login rules are turned on — we check exactly what each rule enforces. Some rules might say 'require MFA' but actually let people in without it. We find these gaps by looking at what the rules actually do, not just what they are called.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
