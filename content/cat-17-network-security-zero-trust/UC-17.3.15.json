{
  "$schema": "../../schemas/uc.schema.json",
  "id": "17.3.15",
  "title": "SASE Tunnel Health",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Availability"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'SASE Tunnel Health' only. Does not replace full NAC policy design or firewall rule authoring or regulatory attestation. Entities listed in `network_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `network_sase_tunnel_health` (UC-17.3.15), SOC dashboard panel \"Network — SASE Tunnel Health\", and weekly export to `index=audit_evidence` sourcetype=csv:network_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=sase` sourcetype='sase:tunnel' where `tunnel_id` satisfies `packet_loss_pct` < 2 for UC-17.3.15 ('SASE Tunnel Health'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `sase` / `sase:tunnel` events where `tunnel_id` stays below `packet_loss_pct` < 2, or exclude the test entity via `network_exception_register.csv` with a valid window. Confirm zero alert rows for UC-17.3.15."
  },
  "dataSources": "`sourcetype=sase:tunnel`, SD-WAN to SASE Indexes: sase. Sourcetypes: sase:tunnel. Key fields: healthy, latency_ms, packet_loss_pct, site, tunnel_id. Validate field extraction in Search before saving the alert; compare event counts to the vendor admin console.",
  "app": "`Splunk_TA_zscaler`, `Splunk_TA_paloalto` (Prisma Access), Cato Networks Events App (Splunkbase 8037), `TA-fortinet_fortigate` (FortiSASE), Netskope Add-on for Splunk (Splunkbase 3808)",
  "spl": "index=sase sourcetype=\"sase:tunnel\" earliest=-24h\n| eval healthy=if(match(lower(state),\"(?i)up|active\") AND packet_loss_pct < 2 AND latency_ms < 200,1,0)\n| where healthy=0\n| stats latest(latency_ms) as latency_ms latest(packet_loss_pct) as loss by tunnel_id, site\n| sort loss",
  "description": "Monitors IPSec/GRE/SSL tunnels from branch to SASE PoPs — packet loss, latency, and down events. Searches `index=sase` with sourcetype `sase:tunnel` for UC-17.3.15, aggregating `count` by `tunnel_id` over `the scheduled window`. Alerts when grouped results cross `packet_loss_pct` < 2 in the saved search. Gives Network operators a repeatable signal for 'SASE Tunnel Health' without manual log review.",
  "value": "Network operations teams monitor SASE tunnel health across all sites and users, detecting down tunnels, degraded connections with high latency or packet loss, and flapping tunnels that silently degrade user experience without triggering traditional outage alerts.",
  "implementation": "Field names vary (Zscaler GRE, Prisma IPSec). Use unified summary index if multi-vendor.",
  "visualization": "Table (unhealthy tunnels), Geo map (site), Line chart (loss trend).",
  "cimModels": [],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Authentication.Authentication\n  by Authentication.user, Authentication.action, Authentication.src span=1h\n| sort - count",
  "dataModelAcceleration": "Enable for Authentication (Authentication).",
  "references": [
    {
      "title": "Cato Networks Events App",
      "url": "https://splunkbase.splunk.com/app/8037",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Netskope Add-on for Splunk",
      "url": "https://splunkbase.splunk.com/app/3808",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk_TA_paloalto",
      "url": "https://splunkbase.splunk.com/app/2757",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `sase`** — Planned work for 'SASE Tunnel Health' shifts `count` over `span=1h` while `tunnel_id` remains in-scope for UC-17.3.15. Cross-check CMDB CHG.\n\n2. **Batch `count` on `sase:tunnel`** — Scheduled automation elevates `site` without crossing `packet_loss_pct` < 2. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `tunnel_id`** — Health checks in `sase` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `sase:tunnel`** — First 48h baseline for `tunnel_id` is unstable; window via `network_exception_register.csv`.\n\n**Suppression mechanism:** `network_exception_register.csv` keyed on `tunnel_id`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1133"
  ],
  "equipment": [
    "fortinet",
    "netskope",
    "nutanix",
    "paloalto",
    "zscaler"
  ],
  "equipmentModels": [
    "fortinet_fortigate",
    "paloalto_pan_firewall",
    "paloalto_prisma"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• SASE platform tunnel/connectivity logs. Zscaler: `sourcetype=zscaler:zia:tunnel` or `sourcetype=zscaler:zpa:connector`. Cato Networks: `sourcetype=cato:sdwan:events`. Palo Alto Prisma Access: `sourcetype=pan:globalprotect` for agent tunnels. Key fields vary: `tunnel_status` (up/down), `tunnel_type` (GRE/IPSec/DTLS), `site_name`, `bandwidth_mbps`, `latency_ms`, `packet_loss_pct`.\n• SASE tunnels connect users (agent tunnel) and sites (IPSec/GRE) to the SASE cloud. Tunnel health directly affects user experience — a down tunnel means no access to cloud-delivered security services.\n• Create a `sase_tunnels.csv` lookup: `tunnel_id,site_name,tunnel_type,expected_bandwidth_mbps,sla_latency_ms`.\n\nStep 1 — Configure data collection\nVerify tunnel events:\n```spl\nindex=sase earliest=-24h\n| where isnotnull(tunnel_status)\n| stats count by tunnel_status, tunnel_type\n```\n\nStep 2 — Create the search and alert\n\nPrimary search — Tunnel health overview:\n```spl\nindex=sase earliest=-24h\n| dedup tunnel_id sortby -_time\n| lookup sase_tunnels.csv tunnel_id OUTPUT site_name expected_bandwidth_mbps sla_latency_ms\n| eval health=case(\n    tunnel_status=\"down\", \"DOWN\",\n    tonumber(latency_ms) > tonumber(sla_latency_ms)*2, \"DEGRADED\",\n    tonumber(packet_loss_pct) > 2, \"DEGRADED\",\n    1==1, \"Healthy\"\n  )\n| stats count by health, site_name, tunnel_type\n| sort health\n```\n\nUnderstanding this SPL: Classifies each tunnel as Healthy, Degraded (high latency or packet loss), or Down. Degraded tunnels are worse than down tunnels in some ways because users experience slow and unreliable access but cannot easily tell why. Down tunnels trigger immediate failover (if configured); degraded tunnels just silently suffer.\n\nTunnel flapping:\n```spl\nindex=sase earliest=-24h\n| where tunnel_status=\"down\" OR tunnel_status=\"up\"\n| stats count by tunnel_id, site_name\n| where count > 10\n| sort - count\n```\n\nStep 3 — Validate\n(a) Check SASE admin console for tunnel status. Compare with Splunk.\n(b) Disconnect a test tunnel and verify the down event.\n\nStep 4 — Operationalize\nDashboard: Row 1 — Tunnel status map. Row 2 — Degraded tunnels. Row 3 — Flapping tunnels.\nAlert: Immediate on any site tunnel down. Warning on degraded (>SLA latency).\n\nRunbook:\n1. **Tunnel down** — Check ISP link at the site. Failover to backup tunnel if available.\n2. **Degraded (high latency)** — ISP quality issue. Run traceroute from the site.\n3. **Flapping** — Unstable ISP link or SASE PoP issue. Contact SASE vendor support.\n\nStep 5 — Troubleshooting\n• **No tunnel events** — SASE platform may not export tunnel status logs. Check vendor documentation.\n• **`latency_ms` always 0** — Platform may not measure tunnel latency. Use external monitoring (ThousandEyes).",
  "grandmaExplanation": "All our internet security goes through cloud tunnels. If a tunnel breaks, people lose access to everything. We monitor every tunnel for problems — not just whether it is up or down, but also whether it is slow or unstable — because a limping tunnel is sometimes worse than one that is completely down.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
