{
  "$schema": "../../schemas/uc.schema.json",
  "id": "17.2.17",
  "title": "VPN Client Version Compliance",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'VPN Client Version Compliance' only. Does not replace full NAC policy design or firewall rule authoring or regulatory attestation. Entities listed in `network_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `network_vpn_client_version_compliance` (UC-17.2.17), SOC dashboard panel \"Network — VPN Client Version Compliance\", and weekly export to `index=audit_evidence` sourcetype=csv:network_controls.",
  "controlTest": {
    "positiveScenario": "UC-17.2.17 positive (VPN, Client, Version, Compliance): ingest synthetic events into `index=vpn` sourcetype='cisco:asa' where `count` grouped by `user`, `client_version`, `platform` satisfies -24h for 'VPN Client Version Compliance'. Validate lookup `vpn_min_client.csv`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-17.2.17 negative (Version Compliance): ingest benign `vpn` / `cisco:asa` events where `user`, `client_version`, `platform` stays below -24h, or exclude the test entity via `network_exception_register.csv`. Variant 4 confirms zero alert rows for 'VPN Client Version Compliance'."
  },
  "dataSources": "VPN session logs with `client_version` Collected via Splunk Add-on for Cisco Security (Splunkbase 1352) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_cisco-asa`, `Splunk_TA_paloalto` (GlobalProtect) — Splunk Add-on for Cisco Security (Splunkbase 1352)",
  "spl": "index=vpn (sourcetype=\"cisco:asa\" OR sourcetype=\"pan:globalprotect\") action=\"session_connect\" earliest=-24h\n| eval major_minor=replace(client_version,\"^(\\d+\\.\\d+).*\",\"\\1\")\n| lookup vpn_min_client.csv platform OUTPUT min_version\n| eval compliant=if(major_minor>=min_version,1,0)\n| where compliant=0\n| stats count by user, client_version, platform\n| sort -count",
  "description": "Identifies risk tied to VPN, Client, Version, Compliance through UC-17.2.17 on `index=vpn` sourcetype `cisco:asa`. Summarises `count` grouped by `user`, `client_version`, `platform` across `the scheduled window` before applying -24h. Operators tune 'VPN Client Version Compliance' using UC-17.2.17 runbook steps.",
  "value": "Tracking `activity` via UC-17.2.17 on `cisco:asa` feeds improves response to 'VPN Client Version Compliance' and reduces unauthorized access, segmentation drift, and NAC policy failures.",
  "implementation": "Use `ver` normalisation or `version` field if numeric. Block or warn via posture integration.",
  "visualization": "Pie chart (compliant vs not), Table (outdated clients), Bar chart (by version).",
  "cimModels": [],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Network_Traffic.All_Traffic\n  by All_Traffic.user, All_Traffic.action, All_Traffic.src, All_Traffic.dest span=1h\n| sort - count",
  "dataModelAcceleration": "Enable for Network_Traffic (All_Traffic).",
  "references": [
    {
      "title": "Splunk_TA_paloalto",
      "url": "https://splunkbase.splunk.com/app/2757",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Authentication",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Authentication",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Vulnerability scanner signature matches** — Qualys/Tenable traffic triggers IPS/threat signatures for 'VPN Client Version Compliance'. Compare `src` against `scanner_ips` lookup.\n\n2. **Policy push during change window** — New deny rule generates expected blocks. Match firewall commit timestamp in change calendar.\n\n3. **Encrypted traffic inspection gap** — TLS-heavy apps bypass deep inspection; volume drop is operational, not a sensor failure.\n\n4. **Red-team C2 simulation** — Approved exercise from `pen_test_hosts.csv`.\n\n**Suppression mechanism:** `network_exception_register.csv` keyed on `src`, `signature_id`, `valid_until`.",
  "mitreAttack": [
    "T1133",
    "T1078"
  ],
  "equipment": [
    "cisco",
    "paloalto"
  ],
  "equipmentModels": [
    "cisco_asa",
    "paloalto_globalprotect",
    "paloalto_pan_firewall"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Splunk Add-on for Cisco ASA (`sourcetype=cisco:asa`) or Palo Alto Networks Add-on (`sourcetype=pan:globalprotect`). Data in `index=vpn`.\n• Key fields: `user`, `client_version` (AnyConnect version, GlobalProtect version), `os_type`, `action=session_connect`.\n• On Cisco ASA, AnyConnect version appears in session connect events (message ID 722022/722032).\n• Create a `vpn_target_version.csv` lookup: `os_type,target_version,min_version,eol_version`.\n\nStep 1 — Configure data collection\nVerify client version data:\n```spl\nindex=vpn (sourcetype=\"cisco:asa\" OR sourcetype=\"pan:globalprotect\") action=\"session_connect\" earliest=-24h\n| stats dc(user) as users by client_version\n| sort client_version\n```\n\nStep 2 — Create the search and alert\n\nPrimary search — Version compliance:\n```spl\nindex=vpn (sourcetype=\"cisco:asa\" OR sourcetype=\"pan:globalprotect\") action=\"session_connect\" earliest=-7d\n| eval major_minor=replace(client_version, \"^(\\d+\\.\\d+).*\", \"\\1\")\n| stats dc(user) as users latest(_time) as last_seen by client_version, major_minor\n| lookup vpn_target_version.csv OUTPUT target_version min_version eol_version\n| eval compliance=case(client_version=target_version, \"CURRENT\", client_version<min_version, \"OUTDATED\", client_version=eol_version, \"EOL\", 1==1, \"ACCEPTABLE\")\n| stats sum(users) as total_users by compliance\n```\n\nUnderstanding this SPL: Categorizes VPN client versions against the target, minimum, and end-of-life thresholds. EOL clients may have known vulnerabilities. The compliance rate shows upgrade campaign effectiveness. Splitting by `major_minor` helps identify which release branch is behind.\n\nStep 3 — Validate\n(a) Check your own AnyConnect version (Help > About) and compare with Splunk.\n\nStep 4 — Operationalize\nDashboard: Pie chart of compliance breakdown. Table of EOL/outdated users.\nAlert: Weekly compliance report. Immediate for EOL clients connecting.\n\nRunbook:\n1. **EOL clients** — Push auto-update via AnyConnect headend update or SCCM deployment.\n2. **Compliance rate declining** — Auto-update may be failing. Check the ASA AnyConnect image configuration.\n\nStep 5 — Troubleshooting\n• **`client_version` null** — Not all ASA log messages include the version. Look specifically at message ID 722032.\n• **Version format varies** — Different platforms (Windows, macOS, Linux, mobile) may use different version strings.",
  "grandmaExplanation": "We check that everyone's VPN software on their laptop is up to date, because old versions might have security holes that attackers could exploit.",
  "hardware": "ASA, GP portal",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
