{
  "$schema": "../../schemas/uc.schema.json",
  "id": "17.3.21",
  "title": "SWG Bypass Attempt Detection",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'SWG Bypass Attempt Detection' only. Does not replace full NAC policy design or firewall rule authoring or regulatory attestation. Entities listed in `network_exception_register.csv` during approved tests are excluded.",
  "evidence": "Saved search `network_swg_bypass_attempt_detection` (UC-17.3.21), SOC dashboard panel \"Network — SWG Bypass Attempt Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:network_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=proxy` sourcetype='zscaler:web' that satisfy the UC-17.3.21 SPL filters for 'SWG Bypass Attempt Detection'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `proxy` / `zscaler:web` events outside the detection threshold, or exclude the test entity via `network_exception_register.csv` with a valid window. Confirm zero alert rows for UC-17.3.21."
  },
  "dataSources": "`zscaler:web`, endpoint proxy logs Collected via Universal Forwarder (Splunkbase 617) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_zscaler`, Netskope Add-on for Splunk — Universal Forwarder (Splunkbase 617)",
  "spl": "index=proxy sourcetype=\"zscaler:web\" earliest=-24h\n| where match(lower(reason),\"(?i)bypass|tunnel|direct|pac\") OR match(lower(url),\"(?i)proxy\\.pac\")\n| stats count by user, src, reason\n| sort -count",
  "description": "Detects attempts to reach direct IPs, misuse PAC files, or tunnel out of SWG inspection.",
  "value": "Security teams detect SWG bypass attempts by comparing active users against SWG-proxied traffic and monitoring for direct-to-internet connections from managed endpoints, ensuring all web traffic passes through cloud security inspection.",
  "implementation": "Correlate with firewall deny for non-standard ports. Tune for false positives from dev tools.",
  "visualization": "Table (bypass attempts), Bar chart (by user), Timeline.",
  "cimModels": [
    "Web",
    "Authentication"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Authentication.Authentication\n  by Authentication.user, Authentication.action, Authentication.src span=1h\n| sort - count",
  "dataModelAcceleration": "Enable for Authentication (Authentication).",
  "references": [
    {
      "title": "CIM: Network_Traffic",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Network_Traffic",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Vulnerability scanner signature matches** — Qualys/Tenable traffic triggers IPS/threat signatures for 'SWG Bypass Attempt Detection'. Compare `src` against `scanner_ips` lookup.\n\n2. **Policy push during change window** — New deny rule generates expected blocks. Match firewall commit timestamp in change calendar.\n\n3. **Encrypted traffic inspection gap** — TLS-heavy apps bypass deep inspection; volume drop is operational, not a sensor failure.\n\n4. **Red-team C2 simulation** — Approved exercise from `pen_test_hosts.csv`.\n\n**Suppression mechanism:** `network_exception_register.csv` keyed on `src`, `signature_id`, `valid_until`.",
  "mitreAttack": [
    "T1572",
    "T1090",
    "T1048",
    "T1573"
  ],
  "equipment": [
    "netskope",
    "zscaler"
  ],
  "equipmentModels": [
    "netskope_generic",
    "zscaler_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Secure Web Gateway (SWG) logs from Zscaler ZIA, Netskope, or cloud proxy. Data in `index=zscaler` or `index=proxy` with `sourcetype=zscaler:zia:web`. Also: endpoint DNS logs or firewall logs to detect direct-to-internet connections. Key fields: `action`, `url`, `user`, `department`, `tunnelType` (ZscalerClientConnector/PAC/GRE), `proxyAction`.\n• SWG bypass = any traffic that reaches the internet without going through the SWG. Methods: disabling the ZTNA/SWG client, using a personal hotspot, connecting via a non-inspected path, or using DNS-over-HTTPS to bypass DNS filtering.\n\nStep 1 — Configure data collection\nVerify SWG coverage:\n```spl\nindex=zscaler sourcetype=\"zscaler:zia:web\" earliest=-24h\n| stats dc(user) as users_through_swg\n```\nCompare with total active users from AD/IdP.\n\nStep 2 — Create the search and alert\n\nPrimary search — Bypass detection via missing SWG traffic:\n```spl\n| inputlookup active_users.csv\n| lookup swg_seen_users.csv user OUTPUT last_seen\n| eval hours_since_seen=round((now() - strptime(last_seen, \"%Y-%m-%dT%H:%M:%S\")) / 3600, 0)\n| where hours_since_seen > 24 OR isnull(last_seen)\n| table user, department, hours_since_seen\n| sort - hours_since_seen\n```\n\nUnderstanding this SPL: Compares the list of active users (from AD) with users seen in SWG logs. Users not seen through the SWG in 24+ hours either have a disabled SWG client, are bypassing the proxy, or are not working. Cross-referencing with VPN/ZTNA login times helps distinguish.\n\nDirect-to-internet detection (from firewall):\n```spl\nindex=firewall earliest=-24h\n| where dest_port IN (80, 443) AND NOT match(dest_ip, \"^(10\\.|172\\.(1[6-9]|2[0-9]|3[0-1])\\.|192\\.168\\.)\")\n| lookup swg_proxy_ips.csv dest_ip OUTPUT is_swg_ip\n| where isnull(is_swg_ip)\n| stats count dc(dest_ip) as unique_destinations by src_ip\n| where count > 10\n| sort - count\n```\n\nStep 3 — Validate\n(a) Disconnect the SWG client on a test device. Verify the user disappears from SWG logs.\n(b) Make a direct-to-internet connection. Verify the firewall detects it.\n\nStep 4 — Operationalize\nDashboard: Row 1 — SWG coverage % of active users. Row 2 — Users missing from SWG. Row 3 — Direct-to-internet detections.\nAlert: When SWG coverage drops below 95%. Immediate on direct-to-internet for managed devices.\n\nRunbook:\n1. **User missing from SWG** — Check if SWG client is installed and running. Push via MDM.\n2. **Direct-to-internet** — Block at firewall. Require all HTTP/S through SWG.\n3. **Coverage declining** — May indicate users disabling the client. Enforce via MDM policy.\n\nStep 5 — Troubleshooting\n• **Cannot detect bypasses** — Firewall must log outbound HTTP/S connections. If cloud-only, use DNS monitoring.\n• **`active_users.csv` stale** — Schedule a daily export from AD/IdP.",
  "grandmaExplanation": "All our internet traffic is supposed to go through a security filter in the cloud. We watch for employees who find ways around this filter — like turning off their security software or using a personal hotspot — because unfiltered internet access is how malware and data theft happen.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
