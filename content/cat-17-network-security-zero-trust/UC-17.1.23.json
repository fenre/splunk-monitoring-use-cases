{
  "$schema": "../../schemas/uc.schema.json",
  "id": "17.1.23",
  "title": "SonicWall NGFW IPS and Gateway Anti-Virus Alerts by Signature",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "dataSources": "SonicWall firewall syslog (CIM Network Traffic / IDS), `sourcetype=dell:sonicwall` or `sourcetype=dell:sonicwall:firewall`",
  "app": "Dell SonicWall Add-on for Splunk (Splunkbase 6203)",
  "spl": "index=netfw (sourcetype=\"dell:sonicwall\" OR sourcetype=\"dell:sonicwall:firewall\") earliest=-24h\n| eval sig=coalesce(signature, msg, cat, threat_name)\n| eval src_ip=coalesce(src_ip, src, c_src)\n| eval dest_ip=coalesce(dest_ip, dst, c_dst)\n| where match(lower(sig),\"(?i)intrusion|ips|gateway anti|virus|sid\") OR match(_raw,\"(?i)priority.*(high|critical)\")\n| stats count by src_ip, dest_ip, sig\n| where count>=5\n| sort -count\n| head 40",
  "description": "SonicWall deep packet inspection generates high-fidelity IPS and gateway anti-malware events. Clustering by signature and endpoint pair reveals active exploitation attempts, worm propagation, or policy gaps where expected traffic is misclassified.",
  "value": "Prioritises SOC review toward the noisiest defensive actions at the perimeter without waiting for endpoint telemetry lag.",
  "implementation": "Forward SonicWall syslog to Splunk via SC4S or a forwarder; set `linebreaking` per vendor guidance. Install Splunkbase app 6203 for field extractions. Validate whether your deployment uses `dell:sonicwall` or `dell:sonicwall:firewall`. Create lookups for pen-test windows and vulnerability-scanner subnets.",
  "visualization": "Table (signature, sources, destinations), Heatmap (time × signature), MITRE tagging overlay in ES if available.",
  "references": [
    {
      "title": "Splunk Connect for Syslog — Dell SonicWall",
      "url": "https://splunk.github.io/splunk-connect-for-syslog/main/sources/vendor/Dell/sonicwall/"
    },
    {
      "title": "Splunkbase — Dell SonicWall Add-on for Splunk",
      "url": "https://splunkbase.splunk.com/app/6203"
    }
  ],
  "securityDomain": "threat",
  "equipment": [
    "sonicwall",
    "syslog"
  ],
  "status": "draft",
  "lastReviewed": "2026-04-24",
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "reviewer": "@splunk-monitoring-use-cases",
  "grandmaExplanation": "We use network security tools to watch who accesses what, help prevent data leaks, enforce network isolation, and secure cloud and remote access.",
  "cimModels": [
    "Authentication"
  ],
  "dataModelAcceleration": "Enable for Authentication (Authentication).",
  "knownFalsePositives": "Anomalies from new SaaS app rollouts, browser updates, or vendor URL category changes.",
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Authentication.Authentication\n  by Authentication.user, Authentication.action, Authentication.src span=1h\n| sort - count"
}
