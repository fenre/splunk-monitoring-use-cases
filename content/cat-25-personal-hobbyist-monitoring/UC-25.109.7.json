{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.109.7",
  "title": "Roborock Session Failure Rate",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault",
    "Quality"
  ],
  "splunkPillar": "Platform",
  "dataSources": "Roborock sessions (`roborock:session`), Tidy schedules (`tidy:schedule`), cleaning checklists (`cleaning:checklist`), and vacuum runtime logs (`vacuum:runtime`).",
  "app": "Roborock vacuum session exports, Tidy scheduling logs, cleaning checklists, and runtime telemetry from robot vacuums ingested into Splunk HEC.",
  "spl": "index=personal sourcetype=roborock:session\n| eval status_value=lower(coalesce(session_status,\"unknown\"))\n| eval failed=if(match(status_value,\"error|fail|alert|overdue|late|stale|breach\"),1,0)\n| stats count as events, sum(failed) as failed_events by room_name\n| eval failure_pct=round(100*failed_events/events,1)\n| sort - failure_pct - failed_events",
  "description": "Measures how often Roborock session records land in an unhealthy or exception state for each room.",
  "value": "A plain failure-rate view highlights noisy devices, flaky automations, and habits that repeatedly slip.",
  "implementation": "Normalize session_status values into good or bad states and review the failure percentage per room.",
  "visualization": "Bar chart of failure percentage by entity.",
  "cimModels": [],
  "references": [
    {
      "title": "Roborock Developer",
      "url": "https://www.roborock.com/"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "grandmaExplanation": "It maps which parts of your Roborock session data keep ending up in a bad state instead of working cleanly."
}
