{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.26.11",
  "title": "Species Gap After Feeder Refill",
  "criticality": "low",
  "difficulty": "beginner",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Species Gap After Feeder Refill' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_species_gap_after_feeder_refill` (UC-25.26.11), SOC dashboard panel \"Personal — Species Gap After Feeder Refill\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=personal` sourcetype='birdnet:detection' that satisfy the UC-25.26.11 SPL filters for 'Species Gap After Feeder Refill'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `personal` / `birdnet:detection` events outside the detection threshold, or exclude the test entity via `personal_monitoring_notes.csv` with a valid window. Confirm zero alert rows for UC-25.26.11."
  },
  "dataSources": "Bird acoustic detections (`birdnet:detection`), wildlife-camera visits (`wildlifecam:visit`), nest-box state (`nestbox:event`), frog-call activity (`frogcall:detection`), and pollinator counts (`pollinator:count`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Wildlife telemetry from BirdNET, trail and feeder cameras, nest-box sensors, frog-call detectors, and pollinator counters streamed to Splunk HEC via MQTT, APIs, and scripted imports. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=birdnet:detection\n| stats latest(hours_since_feeder_refill) as hours_since_feeder_refill dc(species) as species_count by feeder\n| sort hours_since_feeder_refill",
  "description": "Compares species diversity at each feeder against how long it has been since that feeder was refilled.",
  "value": "This makes it easier to see whether feeder freshness changes which birds bother to visit your yard.",
  "implementation": "Attach feeder-refill timing to BirdNET detections from microphones placed near each feeder.",
  "visualization": "Table of species count and time since refill by feeder.",
  "cimModels": [],
  "references": [
    {
      "title": "BirdNET - acoustic bird identification",
      "url": "https://birdnet.cornell.edu/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "iNaturalist - biodiversity observations",
      "url": "https://www.inaturalist.org/pages/api+reference",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry** — Vendor connector stops polling for 'Species Gap After Feeder Refill'; stale rows remain in `index=personal`. Check `_internal` HEC 401s and renew token in passwords.conf.\n\n2. **Rest day or planned break** — Zero activities is expected, not ingestion failure. Vendor app shows no sessions for the calendar day.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `birdnet:detection`. Dedup with `stats latest(*) by external_id`.\n\n4. **Timezone bucket misalignment** — Weekly bins on UTC vs local schedule skew thresholds. Document offset in SPL `bin _time` or connector config.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Wildlife & Biodiversity — typical feeds: BirdNET detections (`birdnet:detection`), trail-cam detections (`trailcam:detection`), moth-trap captures (`mothtrap:capture`), bat-detector passes (`batdetector:pass`), pollinator counts (`pollinator.\n• **Sourcetypes in this search**: `birdnet:detection`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=birdnet:detection`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Backyard wildlife monitoring — BirdNET acoustic bird ID, trail cameras with object detection, camera moth traps, ultrasonic bat detectors, and pollinator counts — via MQTT and scripted inputs..\n\n**Author notes:** Attach feeder-refill timing to BirdNET detections from microphones placed near each feeder.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"birdnet:detection\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=birdnet:detection earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Species Gap After Feeder Refill** over `personal` data (primarily `sourcetype=birdnet:detection`) across the full search range. The `latest, dc` aggregation groups by `feeder`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `feeder`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=birdnet:detection\n| stats latest(hours_since_feeder_refill) as hours_since_feeder_refill dc(species) as species_count by feeder\n| sort hours_since_feeder_refill\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=birdnet:detection earliest=-7d\n| head 50\n| table feeder\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=birdnet:detection earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.26.11: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Species Count And Time Since Refill By Feeder (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `birdnet:detection` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=birdnet:detection | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It tracks whether birds visit a feeder differently right after it has been topped up.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
