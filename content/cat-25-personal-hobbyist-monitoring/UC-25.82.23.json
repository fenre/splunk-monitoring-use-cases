{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.82.23",
  "title": "Utility Rate Plan Statement vs Meter Variance",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Audit",
    "Data Quality"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Utility Rate Plan Statement vs Meter Variance' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_utility_rate_plan_statement_vs_meter_variance` (UC-25.82.23), SOC dashboard panel \"Personal — Utility Rate Plan Statement vs Meter Variance\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.82.23 positive (Utility, Rate, Plan, Statement, Meter): ingest synthetic events into `index=personal` sourcetype='utility:rate' where `avg_variance_pct`, `worst_variance_pct` grouped by `provider` satisfies detection threshold for 'Utility Rate Plan Statement vs Meter Variance'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.82.23 negative (Meter Variance): ingest benign `personal` / `utility:rate` events where `provider` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 6 confirms zero alert rows for 'Utility Rate Plan Statement vs Meter Variance'."
  },
  "dataSources": "Utility bills (`utility:bill`), utility meter readings (`utility:meter`), utility rate changes (`utility:rate`), household accounts (`household:account`), utility payments (`utility:payment`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Electric, gas, and water bills, smart-meter readings, household-account records, and utility rate-plan changes streamed to Splunk HEC via portals, CSV exports, and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=utility:rate\n| where statement_vs_meter_pct=*\n| stats avg(statement_vs_meter_pct) as avg_variance_pct max(statement_vs_meter_pct) as worst_variance_pct by provider\n| sort - worst_variance_pct",
  "description": "Alerts on Meter Variance anomalies for UC-25.82.23 with window `the scheduled window` on `index=personal` sourcetype `utility:rate`. Combines `avg_variance_pct`, `worst_variance_pct` by `provider` and compares against configured threshold. Mapped to keywords Utility, Rate, Plan, Statement, Meter for dashboard routing.",
  "value": "Alerting on Utility, Rate, Plan, Statement, Meter through UC-25.82.23 reduces missed training goals, stale hobby data, and connector failures by surfacing `avg_variance_pct`, `worst_variance_pct` early.",
  "implementation": "Write a precomputed `statement_vs_meter_pct` field during bill reconciliation or ingest processing.",
  "visualization": "Table of provider values by average and worst variance percent.",
  "cimModels": [],
  "references": [
    {
      "title": "U.S. Energy Information Administration",
      "url": "https://www.eia.gov/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "EPA WaterSense",
      "url": "https://www.epa.gov/watersense",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Home Assistant Utility Meter",
      "url": "https://www.home-assistant.io/integrations/utility_meter/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.82.23)** — Connector for 'Utility Rate Plan Statement vs Meter Variance' stops polling `utility:rate`; stale `avg_variance_pct`, `worst_variance_pct` on `provider` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `avg_variance_pct`, `worst_variance_pct` over `1w` is expected for UC-25.82.23, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `utility:rate`. Dedup with `stats latest(*) by provider`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Utility Rate Plan Statement vs Meter Variance'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Utility Bills & Household Accounts — typical feeds: Utility bills (`utility:bill`), utility meter readings (`utility:meter`), utility rate changes (`utility:rate`), household accounts (`household:account`), utility payments (`utility:payment`)..\n• **Sourcetypes in this search**: `utility:rate`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=utility:rate`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Electric, gas, and water bills, smart-meter readings, household-account records, and utility rate-plan changes streamed to Splunk HEC via portals, CSV exports, and scripted inputs..\n\n**Author notes:** Write a precomputed `statement_vs_meter_pct` field during bill reconciliation or ingest processing.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"utility:rate\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=utility:rate earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Utility Rate Plan Statement vs Meter Variance** over `personal` data (primarily `sourcetype=utility:rate`) across the full search range. The `avg, max` aggregation groups by `provider`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `provider`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=utility:rate\n| where statement_vs_meter_pct=*\n| stats avg(statement_vs_meter_pct) as avg_variance_pct max(statement_vs_meter_pct) as worst_variance_pct by provider\n| sort - worst_variance_pct\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=utility:rate earliest=-7d\n| head 50\n| table provider\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=utility:rate earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.82.23: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Provider Values By Average And Worst Variance Percent (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `utility:rate` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=utility:rate | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It blends when the official statement for utility rate plan does not match the usage you expected.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
