{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.18.14",
  "title": "Gate and Fence Breach Sequence Correlation",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Physical Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers personal hobby monitoring for 'Gate and Fence Breach Sequence Correlation' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_gate_and_fence_breach_sequence_correlation` (UC-25.18.14), SOC dashboard panel \"Personal — Gate and Fence Breach Sequence Correlation\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.18.14 positive (Gate, and, Fence, Breach, Sequence): ingest synthetic events into `index=personal` sourcetype='gate:event' where `breach_sequences` grouped by `zone` satisfies `prev_time` <= 120 for 'Gate and Fence Breach Sequence Correlation'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.18.14 negative (Sequence Correlation): ingest benign `personal` / `gate:event` events where `zone` stays below `prev_time` <= 120, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 3 confirms zero alert rows for 'Gate and Fence Breach Sequence Correlation'."
  },
  "dataSources": "Object detections (`frigate:event`), camera health (`camera:health`), alarm state changes (`alarm:event`), smart-lock access (`lock:access`), plate and gate events (`gate:event`), and doorbell delivery telemetry (`doorbell:event`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Home-security telemetry from Frigate and Blue Iris detections, camera health feeds, smart locks, alarm panels, doorbells, and gate sensors streamed to Splunk HEC via MQTT and Home Assistant integrations. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=gate:event\n| sort 0 _time\n| streamstats window=1 current=f last(sensor) as prev_sensor last(_time) as prev_time by zone\n| eval correlated=if(prev_sensor=\"fence\" AND sensor=\"gate\" AND _time-prev_time<=120,1,0)\n| stats sum(correlated) as breach_sequences by zone",
  "description": "Identifies when Sequence Correlation criteria fire for 'Gate and Fence Breach Sequence Correlation' (UC-25.18.14) in `index=personal` sourcetype `gate:event`. Pipeline aggregates `breach_sequences` by `zone` over `the scheduled window`. Alert logic enforces `prev_time` <= 120. Title terms: Gate, and, Fence, Breach, Sequence. Eliminates manual hunting for UC-25.18.14 conditions.",
  "value": "UC-25.18.14 helps teams contain Gate, and, Fence, Breach, Sequence scenarios and reduce missed training goals, stale hobby data, and connector failures before incidents compound.",
  "implementation": "Normalize gate magnetic contacts and fence sensors to a shared zone name, then look for short event chains.",
  "visualization": "Table of likely breach sequences by perimeter zone.",
  "cimModels": [],
  "references": [
    {
      "title": "Frigate NVR - documentation",
      "url": "https://docs.frigate.video/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Home Assistant - lock integration",
      "url": "https://www.home-assistant.io/integrations/lock/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.18.14)** — Connector for 'Gate and Fence Breach Sequence Correlation' stops polling `gate:event`; stale `breach_sequences` on `zone` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `breach_sequences` over `1w` is expected for UC-25.18.14, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `gate:event`. Dedup with `stats latest(*) by zone`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `prev_time` <= 120 for 'Gate and Fence Breach Sequence Correlation'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Home Security & Surveillance — typical feeds: NVR object-detection (`frigate:event`), camera health (`camera:health`), alarm arm/disarm and life-safety (`alarm:event`), lock access (`lock:access`), plate recognition (`plate:detection`), doorbell .\n• **Sourcetypes in this search**: `gate:event`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=gate:event`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Frigate / Blue Iris NVR object-detection events, camera health, alarm and smart-lock logs, and licence-plate recognition via MQTT and Splunk HEC..\n\n**Author notes:** Normalize gate magnetic contacts and fence sensors to a shared zone name, then look for short event chains.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"gate:event\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=gate:event earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Gate and Fence Breach Sequence Correlation** over `personal` data (primarily `sourcetype=gate:event`) across the full search range. `eventstats` / `streamstats` compute rolling baselines before filtering outliers; window size controls sensitivity (smaller window = noisier alerts).\n\n**Fields this search depends on:** `correlated`, `prev_time`, `zone`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=gate:event\n| sort 0 _time\n| streamstats window=1 current=f last(sensor) as prev_sensor last(_time) as prev_time by zone\n| eval correlated=if(prev_sensor=\"fence\" AND sensor=\"gate\" AND _time-prev_time<=120,1,0)\n| stats sum(correlated) as breach_sequences by zone\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=gate:event earliest=-7d\n| head 50\n| table correlated, prev_time, zone\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=gate:event earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `prev_time` before alerting:\n\n```spl\nindex=personal sourcetype=gate:event earliest=-30d\n| stats perc50(prev_time) AS p50 perc90(prev_time) AS p90 perc99(prev_time) AS p99 max(prev_time) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.18.14: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Likely Breach Sequences By Perimeter Zone (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `gate:event` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=gate:event | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Everything is an outlier:** Baseline window is too short or `stdev=0` — require minimum sample count (`where count>=5`) before computing z-scores.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It watches when two nearby security sensors trip in a pattern that looks more like a real entry than random movement.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
