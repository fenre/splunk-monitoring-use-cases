{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.24.24",
  "title": "Goat Feed Station Queue Time",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Goat Feed Station Queue Time' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_goat_feed_station_queue_time` (UC-25.24.24), SOC dashboard panel \"Personal — Goat Feed Station Queue Time\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.24.24 positive (Goat, Feed, Station, Queue, Time): ingest synthetic events into `index=personal` sourcetype='goatcare:event' where `avg_queue_sec` grouped by `station_id` satisfies detection threshold for 'Goat Feed Station Queue Time'. Validate filter `event_type=\"feed_queue\" queue_sec=*`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.24.24 negative (Queue Time): ingest benign `personal` / `goatcare:event` events where `station_id` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 0 confirms zero alert rows for 'Goat Feed Station Queue Time'."
  },
  "dataSources": "Hive metrics (`flowhive:reading`), egg counter events (`eggcounter:event`), goat RFID visits (`goattag:scan`), goat watering and feed logs (`goatcare:event`), and compost readings (`compost:reading`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Smallholding and backyard telemetry from Flow Hive scales, chicken coop counters, RFID goat tags, compost thermometers, and camera-driven chore logs sent to Splunk HEC via MQTT and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=goatcare:event\n| search event_type=\"feed_queue\" queue_sec=*\n| stats avg(queue_sec) as avg_queue_sec perc90(queue_sec) as p90_queue_sec count as samples by station_id\n| sort 0 - avg_queue_sec",
  "description": "Identifies 'Goat Feed Station Queue Time' (UC-25.24.24) using `index=personal` sourcetype `goatcare:event` via filter `event_type=\"feed_queue\" queue_sec=*` by aggregating `avg_queue_sec` grouped by `station_id` over `the scheduled window` and alerting when configured threshold. Keywords: Goat, Feed, Station, Queue, Time. Focuses on Queue Time behaviour so personal teams can triage UC-25.24.24 without ad-hoc log review.",
  "value": "UC-25.24.24 accelerates insight into Queue Time ('Goat Feed Station Queue Time') by tracking `avg_queue_sec` on `goatcare:event` feeds, reducing missed training goals, stale hobby data, and connector failures before spreadsheet checks hide regressions.",
  "implementation": "Send the relevant duration field for goat feed-station waiting time to `index=personal` and review both average and p90 latency instead of relying on single examples.",
  "visualization": "Table of average and p90 goat feed-station waiting time.",
  "cimModels": [],
  "references": [
    {
      "title": "Flow Hive",
      "url": "https://www.honeyflow.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Frigate",
      "url": "https://frigate.video/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Allflex Livestock Intelligence",
      "url": "https://www.allflex.global/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "REOTEMP compost thermometers",
      "url": "https://reotemp.com/products/compost-thermometers/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.24.24)** — Connector for 'Goat Feed Station Queue Time' stops polling `goatcare:event`; stale `avg_queue_sec` on `station_id` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `avg_queue_sec` over `1w` is expected for UC-25.24.24, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `goatcare:event`. Dedup with `stats latest(*) by station_id`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Goat Feed Station Queue Time'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Homestead, Bees & Livestock — typical feeds: Beehive telemetry (`beehive:reading`), coop door (`chickencoop:door`), egg counts (`egg:count`), livestock tags (`livestock:tag`), feed level (`feed:level`), pasture sensors (`pasture:sensor`), trail .\n• **Sourcetypes in this search**: `goatcare:event`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=goatcare:event`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Homestead sensors — connected beehive scale/temperature/sound, automatic chicken-coop door, egg counter, livestock GPS/health tags, feed-silo level, and pasture soil sensors — via MQTT and LoRaWAN gateways..\n\n**Author notes:** Send the relevant duration field for goat feed-station waiting time to `index=personal` and review both average and p90 latency instead of relying on single examples.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"goatcare:event\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=goatcare:event earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Goat Feed Station Queue Time** over `personal` data (primarily `sourcetype=goatcare:event`) across the full search range. The `avg, perc90` aggregation groups by `station_id`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `station_id`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=goatcare:event\n| search event_type=\"feed_queue\" queue_sec=*\n| stats avg(queue_sec) as avg_queue_sec perc90(queue_sec) as p90_queue_sec count as samples by station_id\n| sort 0 - avg_queue_sec\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=goatcare:event earliest=-7d\n| head 50\n| table station_id\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=goatcare:event earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.24.24: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Average And P90 Goat Feed-Station Waiting Time (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `goatcare:event` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=goatcare:event | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It reveals how long goat feed-station waiting time usually takes, so slowdowns are obvious instead of anecdotal.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
