{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.20.17",
  "title": "First-Bloom and Phenology Shift Calendar",
  "criticality": "low",
  "difficulty": "beginner",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'First-Bloom and Phenology Shift Calendar' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_first_bloom_and_phenology_shift_calendar` (UC-25.20.17), SOC dashboard panel \"Personal — First-Bloom and Phenology Shift Calendar\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.20.17 positive (First, Bloom, and, Phenology, Shift): ingest synthetic events into `index=personal` sourcetype='phenology:observation' where `count` grouped by `species`, `year` satisfies detection threshold for 'First-Bloom and Phenology Shift Calendar'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.20.17 negative (Shift Calendar): ingest benign `personal` / `phenology:observation` events where `species`, `year` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 12 confirms zero alert rows for 'First-Bloom and Phenology Shift Calendar'."
  },
  "dataSources": "Seismograph events (`seismograph:event`), seismic noise (`seismograph:noise`), air quality (`airquality:reading`), radiation counts (`radiation:count`), lightning strikes (`lightning:strike`), phenology observations (`phenology:observation`), and weather-station rainfall (`weatherstation:reading`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Citizen-science telemetry from seismographs, weather stations, air-quality monitors, Geiger counters, lightning feeds, and phenology journals streamed to Splunk HEC via MQTT, APIs, and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=phenology:observation event=\"first_bloom\"\n| eval year=strftime(_time,\"%Y\")\n| stats earliest(_time) as first_seen by species year\n| eval day_of_year=strftime(first_seen,\"%j\")\n| sort species year",
  "description": "Identifies UC-25.20.17 — First, Bloom, and, Phenology, Shift — using `index=personal` sourcetype `phenology:observation`, grouped by `species`, `year`. Outputs `count` each `the scheduled window`; alert when configured threshold. Primary focus: Shift Calendar. Purpose-built for 'First-Bloom and Phenology Shift Calendar' in Personal workflows.",
  "value": "Tracking `activity` via UC-25.20.17 on `phenology:observation` feeds improves response to 'First-Bloom and Phenology Shift Calendar' and reduces missed training goals, stale hobby data, and connector failures.",
  "implementation": "Log bloom-start observations with species names and keep the same naming conventions year after year.",
  "visualization": "Line chart of first-bloom day by species and year.",
  "cimModels": [],
  "references": [
    {
      "title": "Raspberry Shake - citizen seismograph",
      "url": "https://raspberryshake.org/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Safecast - open radiation data",
      "url": "https://api.safecast.org/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.20.17)** — Connector for 'First-Bloom and Phenology Shift Calendar' stops polling `phenology:observation`; stale `count` on `species`, `year` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `count` over `1w` is expected for UC-25.20.17, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `phenology:observation`. Dedup with `stats latest(*) by species`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'First-Bloom and Phenology Shift Calendar'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Citizen Science & Backyard Sensing — typical feeds: Seismograph (`seismo:reading`), Geiger counter (`geiger:cpm`), lightning strikes (`lightning:strike`), radon (`radon:reading`), all-sky meteor camera (`allsky:capture`), river-level gauge (`riverlevel.\n• **Sourcetypes in this search**: `phenology:observation`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=phenology:observation`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** DIY citizen-science sensors — Raspberry Shake seismograph, Geiger counter, AS3935/Blitzortung lightning detector, radon monitor, all-sky meteor camera, river-level gauge, and magnetometer — via MQTT and scripted inputs to Splunk HEC..\n\n**Author notes:** Log bloom-start observations with species names and keep the same naming conventions year after year.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"phenology:observation\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=phenology:observation earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **First-Bloom and Phenology Shift Calendar** over `personal` data (primarily `sourcetype=phenology:observation`) across the full search range. The `earliest` aggregation groups by `species`, `year`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `species`, `year`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=phenology:observation event=\"first_bloom\"\n| eval year=strftime(_time,\"%Y\")\n| stats earliest(_time) as first_seen by species year\n| eval day_of_year=strftime(first_seen,\"%j\")\n| sort species year\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=phenology:observation earliest=-7d\n| head 50\n| table species, year\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=phenology:observation earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.20.17: Personal evidence` with the following panel layout:\n\n**Panel 1 — Line Chart Of First-Bloom Day By Species And Year (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `phenology:observation` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=phenology:observation | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It surfaces when each plant first flowers each year, which helps you see if the seasons are shifting over time.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
