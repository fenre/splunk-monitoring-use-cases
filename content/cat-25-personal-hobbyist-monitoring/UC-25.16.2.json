{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.16.2",
  "title": "Fermentation Temperature Stability Alert",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Anomaly"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Fermentation Temperature Stability Alert' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_fermentation_temperature_stability_alert` (UC-25.16.2), SOC dashboard panel \"Personal — Fermentation Temperature Stability Alert\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.16.2 positive (Fermentation, Temperature, Stability, Alert): ingest synthetic events into `index=personal` sourcetype='brew:fermentation' where `sum` grouped by `entity` satisfies detection threshold for 'Fermentation Temperature Stability Alert'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.16.2 negative (Stability Alert): ingest benign `personal` / `brew:fermentation` events where `entity` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 0 confirms zero alert rows for 'Fermentation Temperature Stability Alert'."
  },
  "dataSources": "Fermentation gravity/temperature (`brew:fermentation`), kegerator pours (`kegerator:pour`), wine-cellar climate (`winecellar:reading`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Fermentation sensors (Tilt/iSpindel hydrometer, temperature), kegerator flow meters and scales, and wine-cellar climate sensors via MQTT and Splunk HEC. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=brew:fermentation\n| eval in_band=if(temp_c>=target_c-1 AND temp_c<=target_c+1,1,0)\n| stats latest(temp_c) as temp, latest(target_c) as target, sum(eval(in_band=0)) as out_of_band\n| eval drift=round(temp-target,1)",
  "description": "Alerts on 'Fermentation Temperature Stability Alert' (UC-25.16.2) using `index=personal` sourcetype `brew:fermentation` by aggregating `sum` grouped by `entity` over `the scheduled window` and alerting when configured threshold. Keywords: Fermentation, Temperature, Stability, Alert. Focuses on Stability Alert behaviour so personal teams can triage UC-25.16.2 without ad-hoc log review.",
  "value": "UC-25.16.2 accelerates insight into Stability Alert ('Fermentation Temperature Stability Alert') by tracking `activity` on `brew:fermentation` feeds, reducing missed training goals, stale hobby data, and connector failures before spreadsheet checks hide regressions.",
  "implementation": "Ingest fermentation-chamber temperature; alert whenever the reading leaves the target band around the set point.",
  "visualization": "Time chart of fermentation temperature with a target band overlay.",
  "cimModels": [],
  "references": [
    {
      "title": "iSpindel — DIY digital hydrometer",
      "url": "https://www.ispindel.de/docs/README_en.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "ESPHome — sensor components",
      "url": "https://esphome.io/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.16.2)** — Connector for 'Fermentation Temperature Stability Alert' stops polling `brew:fermentation`; stale `sum` on `external_id` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `sum` over `1w` is expected for UC-25.16.2, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `brew:fermentation`. Dedup with `stats latest(*) by external_id`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Fermentation Temperature Stability Alert'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Homebrewing, Beer & Wine — typical feeds: Fermentation gravity/temperature (`brew:fermentation`), kegerator pours (`kegerator:pour`), wine-cellar climate (`winecellar:reading`)..\n• **Sourcetypes in this search**: `brew:fermentation`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=brew:fermentation`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Fermentation sensors (Tilt/iSpindel hydrometer, temperature), kegerator flow meters and scales, and wine-cellar climate sensors via MQTT and Splunk HEC..\n\n**Author notes:** Ingest fermentation-chamber temperature; alert whenever the reading leaves the target band around the set point.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"brew:fermentation\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=brew:fermentation earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Fermentation Temperature Stability Alert** over `personal` data (primarily `sourcetype=brew:fermentation`) across the full search range. The `latest, sum` aggregation groups by the time bucket; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `in_band`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=brew:fermentation\n| eval in_band=if(temp_c>=target_c-1 AND temp_c<=target_c+1,1,0)\n| stats latest(temp_c) as temp, latest(target_c) as target, sum(eval(in_band=0)) as out_of_band\n| eval drift=round(temp-target,1)\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=brew:fermentation earliest=-7d\n| head 50\n| table in_band\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=brew:fermentation earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.16.2: Personal evidence` with the following panel layout:\n\n**Panel 1 — Time Chart Of Fermentation Temperature With A Target Band Overlay (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `brew:fermentation` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=brew:fermentation | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It warns you if your fermenting beer gets too warm or too cold, which can spoil the flavour.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
