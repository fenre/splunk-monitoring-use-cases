{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.41.29",
  "title": "Carve Session Duration by Surface Type",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "dataSources": "Rides (`ride:session`), charge sessions (`ride:charge`), ride exports (`ride:export`), surf forecasts (`surf:forecast`), ride incidents (`ride:incident`), segment efforts (`fitness:segment`).",
  "app": "Micro-mobility and action-sports telemetry — Onewheel ride exports, legacy Boosted board logs, Surfline forecast snapshots, and Strava segment efforts — streamed to Splunk HEC via vendor exports and scripted inputs.",
  "spl": "index=personal sourcetype=ride:session surface_type=* session_duration_min=*\n| stats avg(session_duration_min) as avg_session_duration_min, count as events by surface_type\n| sort - avg_session_duration_min",
  "description": "Shows average session duration minutes by surface type for carve sessions.",
  "value": "Average session duration minutes by surface type helps compare carve sessions across the factors that matter most.",
  "implementation": "Capture `session_duration_min` on `ride:session` events and normalize `surface_type` so Splunk can summarize performance by surface type.",
  "visualization": "Bar chart of average session duration minutes by surface type.",
  "cimModels": [],
  "references": [
    {
      "title": "Onewheel — boards",
      "url": "https://onewheel.com/"
    },
    {
      "title": "Boosted — electric skateboards",
      "url": "https://boostedusa.com/"
    },
    {
      "title": "Surfline — surf reports",
      "url": "https://www.surfline.com/"
    },
    {
      "title": "Strava — segments",
      "url": "https://www.strava.com/"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Micro-Mobility & Action Sports — typical feeds: E-bike rides (`ebike:ride`), scooter trips (`scooter:trip`), skate sessions (`skate:session`), surf sessions (`surf:session`), snow runs (`snow:run`)..\n• **Sourcetypes in this search**: `ride:session`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=ride:session`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Micro-mobility and action-sport trackers — e-bike / e-scooter apps (VanMoof, Bosch eBike Flow), and skate / surf / snow session trackers (Trace, Slopes, Xensr) with IMU sensors — streamed to Splunk HEC via APIs and scripted inputs..\n\n**Author notes:** Capture `session_duration_min` on `ride:session` events and normalize `surface_type` so Splunk can summarize performance by surface type.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"ride:session\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=ride:session earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Carve Session Duration by Surface Type** over `personal` data (primarily `sourcetype=ride:session`) across the full search range. The `avg` aggregation groups by `surface_type`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `surface_type`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=ride:session surface_type=* session_duration_min=*\n| stats avg(session_duration_min) as avg_session_duration_min, count as events by surface_type\n| sort - avg_session_duration_min\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=ride:session earliest=-7d\n| head 50\n| table surface_type\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=ride:session earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.41.29: Personal evidence` with the following panel layout:\n\n**Panel 1 — Bar Chart Of Average Session Duration Minutes By Surface Type (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `ride:session` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=ride:session | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It frames the average session duration minutes for each surface type so you can see what works best for carve sessions.",
  "prerequisiteUseCases": [],
  "wave": "walk"
}
