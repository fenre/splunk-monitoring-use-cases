{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.4.211",
  "title": "Reolink Person Detection Confidence Drift",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Physical Security",
    "Availability"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Reolink Person Detection Confidence Drift' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_reolink_person_detection_confidence_drift` (UC-25.4.211), SOC dashboard panel \"Personal — Reolink Person Detection Confidence Drift\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.4.211 positive (Reolink, Person, Detection, Confidence, Drift): ingest synthetic events into `index=personal` sourcetype='reolink:event' where `confidence_pct`, `baseline_confidence_pct` grouped by `camera`, `_time` satisfies `baseline_confidence_pct` > 0 for 'Reolink Person Detection Confidence Drift'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.4.211 negative (Confidence Drift): ingest benign `personal` / `reolink:event` events where `camera`, `_time` stays below `baseline_confidence_pct` > 0, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 12 confirms zero alert rows for 'Reolink Person Detection Confidence Drift'."
  },
  "dataSources": "Home security telemetry (`reolink:event`, `unifiprotect:event`, `ring:event`, `nestprotect:event`, `frigate:event`, `camera:health`, `alarm:event`, `doorbell:event`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Reolink, UniFi Protect, Ring, Nest Protect, Frigate, and Home Assistant security integrations streamed to Splunk HEC via MQTT, webhooks, and vendor APIs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=reolink:event event=person_detection\n| bin _time span=1w\n| stats avg(confidence_pct) as confidence_pct by camera _time\n| streamstats current=f avg(confidence_pct) as baseline_confidence_pct by camera\n| where baseline_confidence_pct>0 AND confidence_pct<baseline_confidence_pct*0.8\n| sort confidence_pct",
  "description": "Alerts on UC-25.4.211 — Reolink, Person, Detection, Confidence, Drift — using `index=personal` sourcetype `reolink:event`, grouped by `camera`, `_time`. Outputs `confidence_pct`, `baseline_confidence_pct` each `1w`; alert when `baseline_confidence_pct` > 0. Primary focus: Confidence Drift. Purpose-built for 'Reolink Person Detection Confidence Drift' in Personal workflows.",
  "value": "Tracking `confidence_pct`, `baseline_confidence_pct` via UC-25.4.211 on `reolink:event` feeds improves response to 'Reolink Person Detection Confidence Drift' and reduces missed training goals, stale hobby data, and connector failures.",
  "implementation": "Ingest Reolink detection events data through its real API, export, MQTT bridge, or open-source connector into `index=personal`; preserve the fields referenced in the SPL and schedule the search on a hourly cadence.",
  "visualization": "Weekly confidence trend by camera.",
  "cimModels": [],
  "references": [
    {
      "title": "Reolink Home Assistant integration",
      "url": "https://www.home-assistant.io/integrations/reolink/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "UniFi Protect integration",
      "url": "https://www.home-assistant.io/integrations/unifiprotect/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Ring integration",
      "url": "https://www.home-assistant.io/integrations/ring/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Nest integration",
      "url": "https://www.home-assistant.io/integrations/nest/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Frigate docs",
      "url": "https://docs.frigate.video/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.4.211)** — Connector for 'Reolink Person Detection Confidence Drift' stops polling `reolink:event`; stale `confidence_pct`, `baseline_confidence_pct` on `camera`, `_time` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `confidence_pct`, `baseline_confidence_pct` over `1w` is expected for UC-25.4.211, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `reolink:event`. Dedup with `stats latest(*) by camera`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `baseline_confidence_pct` > 0 for 'Reolink Person Detection Confidence Drift'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Home Security & Surveillance — typical feeds: NVR object-detection (`frigate:event`), camera health (`camera:health`), alarm arm/disarm and life-safety (`alarm:event`), lock access (`lock:access`), plate recognition (`plate:detection`), doorbell .\n• **Sourcetypes in this search**: `reolink:event`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=reolink:event`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Frigate / Blue Iris NVR object-detection events, camera health, alarm and smart-lock logs, and licence-plate recognition via MQTT and Splunk HEC..\n\n**Author notes:** Ingest Reolink detection events data through its real API, export, MQTT bridge, or open-source connector into `index=personal`; preserve the fields referenced in the SPL and schedule the search on a hourly cadence.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"reolink:event\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=reolink:event earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Reolink Person Detection Confidence Drift** over `personal` data (primarily `sourcetype=reolink:event`) across the full search range. `eventstats` / `streamstats` compute rolling baselines before filtering outliers; window size controls sensitivity (smaller window = noisier alerts).\n\n**Fields this search depends on:** `_time`, `baseline_confidence_pct`, `camera`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=reolink:event event=person_detection\n| bin _time span=1w\n| stats avg(confidence_pct) as confidence_pct by camera _time\n| streamstats current=f avg(confidence_pct) as baseline_confidence_pct by camera\n| where baseline_confidence_pct>0 AND confidence_pct<baseline_confidence_pct*0.8\n| sort confidence_pct\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=reolink:event earliest=-7d\n| head 50\n| table _time, baseline_confidence_pct, camera\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=reolink:event earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `baseline_confidence_pct` before alerting:\n\n```spl\nindex=personal sourcetype=reolink:event earliest=-30d\n| stats perc50(baseline_confidence_pct) AS p50 perc90(baseline_confidence_pct) AS p90 perc99(baseline_confidence_pct) AS p99 max(baseline_confidence_pct) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.4.211: Personal evidence` with the following panel layout:\n\n**Panel 1 — Weekly Confidence Trend By Camera (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `reolink:event` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=reolink:event | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Everything is an outlier:** Baseline window is too short or `stdev=0` — require minimum sample count (`where count>=5`) before computing z-scores.\n\n**Bucket misalignment:** `span=1w` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1w` buckets.",
  "grandmaExplanation": "It flags weekly confidence drift in person detections so you can spot camera views that degraded due to placement, weather, or firmware before it becomes annoying.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
