{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.12.36",
  "title": "Fidelity Cash Drag Above Target",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Cost",
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Fidelity Cash Drag Above Target' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_fidelity_cash_drag_above_target` (UC-25.12.36), SOC dashboard panel \"Personal — Fidelity Cash Drag Above Target\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.12.36 positive (Fidelity, Cash, Drag, Above, Target): ingest synthetic events into `index=personal` sourcetype='fidelity:balance' where `count` grouped by `entity` satisfies `cash_usd` > 10000 for 'Fidelity Cash Drag Above Target'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.12.36 negative (Above Target): ingest benign `personal` / `fidelity:balance` events where `entity` stays below `cash_usd` > 10000, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 4 confirms zero alert rows for 'Fidelity Cash Drag Above Target'."
  },
  "dataSources": "Crypto prices/holdings (`crypto:price`, `wallet:balance`), exchange transactions (`coinbase:txn`), budget/transaction exports (`firefly:txn`, `ynab:txn`), recurring-subscription and bill feeds. Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Crypto exchange/wallet APIs (Coinbase, on-chain), budgeting exports (YNAB/Firefly III), and subscription trackers via HEC scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=fidelity:balance cash_usd>10000\n| table account_name cash_usd balance_usd",
  "description": "Alerts on risk tied to Fidelity, Cash, Drag, Above, Target through UC-25.12.36 on `index=personal` sourcetype `fidelity:balance`. Summarises `count` grouped by `entity` across `the scheduled window` before applying `cash_usd` > 10000. Operators tune 'Fidelity Cash Drag Above Target' using UC-25.12.36 runbook steps.",
  "value": "Tracking `activity` via UC-25.12.36 on `fidelity:balance` feeds improves response to 'Fidelity Cash Drag Above Target' and reduces missed training goals, stale hobby data, and connector failures.",
  "implementation": "Ingest the relevant personal finance & crypto export into `index=personal` and schedule this search weekly.",
  "visualization": "Table or chart of key fields.",
  "cimModels": [],
  "references": [
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk ES Content Management",
      "url": "https://docs.splunk.com/Documentation/ES/latest/Admin/ContentManagement",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.12.36)** — Connector for 'Fidelity Cash Drag Above Target' stops polling `fidelity:balance`; stale `count` on `external_id` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `count` over `1w` is expected for UC-25.12.36, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `fidelity:balance`. Dedup with `stats latest(*) by external_id`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `cash_usd` > 10000 for 'Fidelity Cash Drag Above Target'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "exchange"
  ],
  "equipmentModels": [
    "exchange_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Personal Finance & Crypto — typical feeds: Crypto prices/holdings (`crypto:price`, `wallet:balance`), exchange transactions (`coinbase:txn`), budget/transaction exports (`firefly:txn`, `ynab:txn`), recurring-subscription and bill feeds..\n• **Sourcetypes in this search**: `fidelity:balance`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=fidelity:balance`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Crypto exchange/wallet APIs (Coinbase, on-chain), budgeting exports (YNAB/Firefly III), and subscription trackers via HEC scripted inputs..\n\n**Author notes:** Ingest the relevant personal finance & crypto export into `index=personal` and schedule this search weekly.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"fidelity:balance\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=fidelity:balance earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Fidelity Cash Drag Above Target** over `personal` data (primarily `sourcetype=fidelity:balance`) across the full search range. It applies numeric thresholds on `cash_usd` (> 10000) after aggregation — tune these constants to your household baseline.\n\n**Fields this search depends on:** `cash_usd`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=fidelity:balance cash_usd>10000\n| table account_name cash_usd balance_usd\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=fidelity:balance earliest=-7d\n| head 50\n| table cash_usd\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=fidelity:balance earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `cash_usd` before alerting:\n\n```spl\nindex=personal sourcetype=fidelity:balance earliest=-30d\n| stats perc50(cash_usd) AS p50 perc90(cash_usd) AS p90 perc99(cash_usd) AS p99 max(cash_usd) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.12.36: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Or Chart Of Key Fields (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `fidelity:balance` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=fidelity:balance | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It notices when fidelity cash drag above target matters for your routine.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
