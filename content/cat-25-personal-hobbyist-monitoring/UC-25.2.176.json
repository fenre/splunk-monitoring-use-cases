{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.2.176",
  "title": "Cold & Flu Recovery Curve",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Cold & Flu Recovery Curve' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_cold_flu_recovery_curve` (UC-25.2.176), SOC dashboard panel \"Personal — Cold & Flu Recovery Curve\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.2.176 positive (Cold, Flu, Recovery, Curve): ingest synthetic events into `index=personal` sourcetype='symptom:log' where `onset`, `recovered`, `peak` grouped by `episode` satisfies detection threshold for 'Cold & Flu Recovery Curve'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.2.176 negative (Recovery Curve): ingest benign `personal` / `symptom:log` events where `episode` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 7 confirms zero alert rows for 'Cold & Flu Recovery Curve'."
  },
  "dataSources": "Posture readings (`posture:reading`), symptom logs (`symptom:log`), bathroom visits (`bathroom:visit`), sneeze events (`sneeze:event`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Everyday body-signal logs — posture sensors, symptom and allergy journals, hydration-linked bathroom logs, and quirky personal counters — sent to Splunk HEC via wearables, MQTT, and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=symptom:log episode=*\n| stats min(_time) as onset, max(_time) as recovered, max(severity) as peak by episode\n| eval duration_days=round((recovered-onset)/86400,1)\n| sort - onset",
  "description": "Alerts on UC-25.2.176 — Cold, Flu, Recovery, Curve — using `index=personal` sourcetype `symptom:log`, grouped by `episode`. Outputs `onset`, `recovered`, `peak` each `the scheduled window`; alert when configured threshold. Primary focus: Recovery Curve. Purpose-built for 'Cold & Flu Recovery Curve' in Personal workflows.",
  "value": "UC-25.2.176 operationalises 'Cold & Flu Recovery Curve' against `symptom:log` feeds so operators reduce missed training goals, stale hobby data, and connector failures without manual correlation.",
  "implementation": "Group symptom episodes in `index=personal`; review duration and peak severity per illness.",
  "visualization": "Table of illness episodes with duration and peak severity.",
  "cimModels": [],
  "references": [
    {
      "title": "NHS — common cold",
      "url": "https://www.nhs.uk/conditions/common-cold/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.2.176)** — Connector for 'Cold & Flu Recovery Curve' stops polling `symptom:log`; stale `onset`, `recovered`, `peak` on `episode` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `onset`, `recovered`, `peak` over `1w` is expected for UC-25.2.176, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `symptom:log`. Dedup with `stats latest(*) by episode`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Cold & Flu Recovery Curve'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Body Signals & Personal Oddities — typical feeds: Posture readings (`posture:reading`), symptom logs (`symptom:log`), bathroom visits (`bathroom:visit`), sneeze events (`sneeze:event`)..\n• **Sourcetypes in this search**: `symptom:log`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=symptom:log`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Everyday body-signal logs — posture sensors, symptom and allergy journals, hydration-linked bathroom logs, and quirky personal counters — sent to Splunk HEC via wearables, MQTT, and scripted inputs..\n\n**Author notes:** Group symptom episodes in `index=personal`; review duration and peak severity per illness.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"symptom:log\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=symptom:log earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Cold & Flu Recovery Curve** over `personal` data (primarily `sourcetype=symptom:log`) across the full search range. The `min, max` aggregation groups by `episode`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `episode`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=symptom:log episode=*\n| stats min(_time) as onset, max(_time) as recovered, max(severity) as peak by episode\n| eval duration_days=round((recovered-onset)/86400,1)\n| sort - onset\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=symptom:log earliest=-7d\n| head 50\n| table episode\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=symptom:log earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.2.176: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Illness Episodes With Duration And Peak Severity (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `symptom:log` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=symptom:log | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It records how long each cold lasts and how bad it got, so you know what is normal for you.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
