{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.56.23",
  "title": "Church Attendance Trend by Month",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Church Attendance Trend by Month' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_church_attendance_trend_by_month` (UC-25.56.23), SOC dashboard panel \"Personal — Church Attendance Trend by Month\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=personal` sourcetype='faith:attendance' that satisfy the UC-25.56.23 SPL filters for 'Church Attendance Trend by Month'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `personal` / `faith:attendance` events outside the detection threshold, or exclude the test entity via `personal_monitoring_notes.csv` with a valid window. Confirm zero alert rows for UC-25.56.23."
  },
  "dataSources": "Prayer sessions (`faith:rosary`), retreat events (`faith:retreat`), attendance logs (`faith:attendance`), fasting journals (`faith:fast`), practice reviews (`faith:ops`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Spiritual-practice telemetry — rosary counters, meditation-retreat schedules, church-attendance logs, and fasting journals — streamed to Splunk HEC via exports and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=faith:attendance service_type=* attended=*\n| bin _time span=1mon\n| stats sum(eval(if(attended=\"yes\",1,0))) as metric by _time service_type\n| sort 0 _time service_type",
  "description": "Trends monthly attended services by service type for church attendance. This use case monitors the condition described in the title using the SPL and data sources documented on this page.",
  "value": "Monthly trend lines show whether church attendance are improving, stalling, or becoming unbalanced over time.",
  "implementation": "Normalize `service_type` on `faith:attendance` events and store the fields needed to calculate monthly attended services.",
  "visualization": "Line chart of monthly attended services by service type.",
  "cimModels": [],
  "references": [
    {
      "title": "Hallow",
      "url": "https://hallow.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Church Center",
      "url": "https://www.churchcenter.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "YouVersion",
      "url": "https://www.youversion.com/the-bible-app/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry** — Vendor connector stops polling for 'Church Attendance Trend by Month'; stale rows remain in `index=personal`. Check `_internal` HEC 401s and renew token in passwords.conf.\n\n2. **Rest day or planned break** — Zero activities is expected, not ingestion failure. Vendor app shows no sessions for the calendar day.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `faith:attendance`. Dedup with `stats latest(*) by external_id`.\n\n4. **Timezone bucket misalignment** — Weekly bins on UTC vs local schedule skew thresholds. Document offset in SPL `bin _time` or connector config.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Spiritual Practice & Mindfulness — typical feeds: Prayer sessions (`prayer:session`), scripture readings (`scripture:reading`), fasting logs (`fasting:log`), gratitude entries (`gratitude:entry`), retreats (`retreat:event`)..\n• **Sourcetypes in this search**: `faith:attendance`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=faith:attendance`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Spiritual-practice and mindfulness logs — prayer / meditation timers, scripture-reading plans (YouVersion), fasting and gratitude journals, and retreat calendars — streamed to Splunk HEC via APIs and scripted inputs..\n\n**Author notes:** Normalize `service_type` on `faith:attendance` events and store the fields needed to calculate monthly attended services.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"faith:attendance\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=faith:attendance earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Church Attendance Trend by Month** over `personal` data (primarily `sourcetype=faith:attendance`) across the full search range. The `sum` aggregation groups by `_time`, `service_type`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `_time`, `service_type`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=faith:attendance service_type=* attended=*\n| bin _time span=1mon\n| stats sum(eval(if(attended=\"yes\",1,0))) as metric by _time service_type\n| sort 0 _time service_type\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=faith:attendance earliest=-7d\n| head 50\n| table _time, service_type\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=faith:attendance earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.56.23: Personal evidence` with the following panel layout:\n\n**Panel 1 — Line Chart Of Monthly Attended Services By Service Type (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `faith:attendance` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=faith:attendance | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=1m` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1m` buckets.",
  "grandmaExplanation": "It totals how attended services changes month by month for each service type so you can see whether church attendance are heading the right way.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
