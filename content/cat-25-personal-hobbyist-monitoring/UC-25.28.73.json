{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.28.73",
  "title": "Kaya Session Volume Personal Best Detection",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics",
    "Performance"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Kaya Session Volume Personal Best Detection' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_kaya_session_volume_personal_best_detection` (UC-25.28.73), SOC dashboard panel \"Personal — Kaya Session Volume Personal Best Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.28.73 positive (Kaya, Session, Volume, Personal, Best): ingest synthetic events into `index=personal` sourcetype='kaya:session' where `prior_best` grouped by `entity` satisfies detection threshold for 'Kaya Session Volume Personal Best Detection'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.28.73 negative (Best Detection): ingest benign `personal` / `kaya:session` events where `entity` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 12 confirms zero alert rows for 'Kaya Session Volume Personal Best Detection'."
  },
  "dataSources": "Kaya sessions (`kaya:session`), MoonBoard sessions (`moonboard:session`), Tension Board sessions (`tensionboard:session`), 8a.nu sends (`eighta:send`), hangboard protocols (`hangboard:protocol`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Kaya app sessions, MoonBoard and Tension Board logs, 8a.nu send history, and hangboard protocol trackers streamed to Splunk HEC via APIs, CSV exports, and mobile forms. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=kaya:session\n| sort 0 _time\n| streamstats max(problems_completed) as prior_best current=f\n| where isnotnull(prior_best) AND problems_completed>prior_best\n| table _time gym_area problems_completed prior_best",
  "description": "Alerts on UC-25.28.73 — Kaya, Session, Volume, Personal, Best — using `index=personal` sourcetype `kaya:session`, grouped by `entity`. Outputs `prior_best` each `the scheduled window`; alert when configured threshold. Primary focus: Best Detection. Purpose-built for 'Kaya Session Volume Personal Best Detection' in Personal workflows.",
  "value": "Tracking `prior_best` via UC-25.28.73 on `kaya:session` feeds improves response to 'Kaya Session Volume Personal Best Detection' and reduces missed training goals, stale hobby data, and connector failures.",
  "implementation": "Keep the underlying metric normalized across sessions so the best-detection logic compares like events with like events.",
  "visualization": "Table of personal-best events with the previous best beside the new value.",
  "cimModels": [],
  "references": [
    {
      "title": "KAYA",
      "url": "https://kayaclimb.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "MoonBoard",
      "url": "https://www.moonboard.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "8a.nu",
      "url": "https://www.8a.nu/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Tension Climbing",
      "url": "https://tensionclimbing.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.28.73)** — Connector for 'Kaya Session Volume Personal Best Detection' stops polling `kaya:session`; stale `prior_best` on `external_id` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `prior_best` over `1w` is expected for UC-25.28.73, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `kaya:session`. Dedup with `stats latest(*) by external_id`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Kaya Session Volume Personal Best Detection'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Climbing & Bouldering — typical feeds: Kaya sessions (`kaya:session`), MoonBoard sessions (`moonboard:session`), Tension Board sessions (`tensionboard:session`), 8a.nu sends (`eighta:send`), hangboard protocols (`hangboard:protocol`)..\n• **Sourcetypes in this search**: `kaya:session`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=kaya:session`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Kaya app sessions, MoonBoard and Tension Board logs, 8a.nu send history, and hangboard protocol trackers streamed to Splunk HEC via APIs, CSV exports, and mobile forms..\n\n**Author notes:** Keep the underlying metric normalized across sessions so the best-detection logic compares like events with like events.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"kaya:session\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=kaya:session earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Kaya Session Volume Personal Best Detection** over `personal` data (primarily `sourcetype=kaya:session`) across the full search range. `eventstats` / `streamstats` compute rolling baselines before filtering outliers; window size controls sensitivity (smaller window = noisier alerts).\n\n```spl\nindex=personal sourcetype=kaya:session\n| sort 0 _time\n| streamstats max(problems_completed) as prior_best current=f\n| where isnotnull(prior_best) AND problems_completed>prior_best\n| table _time gym_area problems_completed prior_best\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=kaya:session earliest=-7d\n| head 50\n| table _time, sourcetype, host\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=kaya:session earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.28.73: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Personal-Best Events With The Previous Best Beside The New Value (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `kaya:session` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=kaya:session | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Everything is an outlier:** Baseline window is too short or `stdev=0` — require minimum sample count (`where count>=5`) before computing z-scores.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It warns the moments when kaya session volume reached a new best for you.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
