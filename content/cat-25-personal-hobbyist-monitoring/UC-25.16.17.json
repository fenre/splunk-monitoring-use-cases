{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.16.17",
  "title": "Mash-Step Temperature Hold Compliance",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Quality"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Mash-Step Temperature Hold Compliance' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_mash_step_temperature_hold_compliance` (UC-25.16.17), SOC dashboard panel \"Personal — Mash-Step Temperature Hold Compliance\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.16.17 positive (Mash, Step, Temperature, Hold, Compliance): ingest synthetic events into `index=personal` sourcetype='brew:mash' where `avg_delta_c`, `max_delta_c` grouped by `batch`, `step_name` satisfies detection threshold for 'Mash-Step Temperature Hold Compliance'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.16.17 negative (Hold Compliance): ingest benign `personal` / `brew:mash` events where `batch`, `step_name` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 4 confirms zero alert rows for 'Mash-Step Temperature Hold Compliance'."
  },
  "dataSources": "Fermentation telemetry (`brew:fermentation`), brew-day records (`brew:batch`), mash probes (`brew:mash`), keg pressure (`kegerator:pressure`), tap pours (`kegerator:pour`), brew-fridge duty logs (`brewfridge:compressor`), cellar climate (`winecellar:reading`), and line-cleaning events (`tapline:clean`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Homebrewing telemetry from Tilt/iSpindel fermentation sensors, Brewfather batch exports, ESPHome mash and fridge probes, keg pressure sensors, tap flow meters, and cellar climate sensors streamed to Splunk HEC via MQTT and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=brew:mash\n| eval abs_delta=abs(temp_delta_c)\n| stats avg(temp_delta_c) as avg_delta_c max(abs_delta) as max_delta_c by batch step_name\n| sort batch step_name",
  "description": "Monitors risk tied to Mash, Step, Temperature, Hold, Compliance through UC-25.16.17 on `index=personal` sourcetype `brew:mash`. Summarises `avg_delta_c`, `max_delta_c` grouped by `batch`, `step_name` across `the scheduled window` before applying configured threshold. Operators tune 'Mash-Step Temperature Hold Compliance' using UC-25.16.17 runbook steps.",
  "value": "Tracking `avg_delta_c`, `max_delta_c` via UC-25.16.17 on `brew:mash` feeds improves response to 'Mash-Step Temperature Hold Compliance' and reduces missed training goals, stale hobby data, and connector failures.",
  "implementation": "Send target and actual mash temperatures from your controller or recipe app during each step of the mash schedule.",
  "visualization": "Table of average and worst temperature drift by mash step.",
  "cimModels": [],
  "references": [
    {
      "title": "iSpindel - DIY digital hydrometer",
      "url": "https://www.ispindel.de/docs/README_en.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Brewfather - API",
      "url": "https://api.brewfather.app/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.16.17)** — Connector for 'Mash-Step Temperature Hold Compliance' stops polling `brew:mash`; stale `avg_delta_c`, `max_delta_c` on `batch`, `step_name` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `avg_delta_c`, `max_delta_c` over `1w` is expected for UC-25.16.17, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `brew:mash`. Dedup with `stats latest(*) by batch`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Mash-Step Temperature Hold Compliance'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Homebrewing, Beer & Wine — typical feeds: Fermentation gravity/temperature (`brew:fermentation`), kegerator pours (`kegerator:pour`), wine-cellar climate (`winecellar:reading`)..\n• **Sourcetypes in this search**: `brew:mash`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=brew:mash`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Fermentation sensors (Tilt/iSpindel hydrometer, temperature), kegerator flow meters and scales, and wine-cellar climate sensors via MQTT and Splunk HEC..\n\n**Author notes:** Send target and actual mash temperatures from your controller or recipe app during each step of the mash schedule.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"brew:mash\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=brew:mash earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Mash-Step Temperature Hold Compliance** over `personal` data (primarily `sourcetype=brew:mash`) across the full search range. The `avg, max` aggregation groups by `batch`, `step_name`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `batch`, `step_name`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=brew:mash\n| eval abs_delta=abs(temp_delta_c)\n| stats avg(temp_delta_c) as avg_delta_c max(abs_delta) as max_delta_c by batch step_name\n| sort batch step_name\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=brew:mash earliest=-7d\n| head 50\n| table batch, step_name\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=brew:mash earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.16.17: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Average And Worst Temperature Drift By Mash Step (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `brew:mash` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=brew:mash | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It compares whether each mash step stayed near the planned temperature, which helps explain why one batch differs from another.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
