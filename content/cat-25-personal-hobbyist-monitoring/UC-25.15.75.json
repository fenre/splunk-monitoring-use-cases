{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.15.75",
  "title": "Kegerator Temperature and CO2 Health",
  "criticality": "low",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Kegerator Temperature and CO2 Health' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_kegerator_temperature_and_co2_health` (UC-25.15.75), SOC dashboard panel \"Personal — Kegerator Temperature and CO2 Health\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.15.75 positive (Kegerator, Temperature, and, CO2, Health): ingest synthetic events into `index=personal` sourcetype='kegerator:pour' where `count` grouped by `entity` satisfies `temp` >= 2, `temp` <= 5, `co2` >= 8, or `co2` <= 14 for 'Kegerator Temperature and CO2 Health'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.15.75 negative (CO2 Health): ingest benign `personal` / `kegerator:pour` events where `entity` stays below `temp` >= 2, `temp` <= 5, `co2` >= 8, or `co2` <= 14, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 10 confirms zero alert rows for 'Kegerator Temperature and CO2 Health'."
  },
  "dataSources": "Fermentation gravity/temperature (`brew:fermentation`), kegerator pours (`kegerator:pour`), wine-cellar climate (`winecellar:reading`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Fermentation sensors (Tilt/iSpindel hydrometer, temperature), kegerator flow meters and scales, and wine-cellar climate sensors via MQTT and Splunk HEC. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=kegerator:pour\n| stats latest(fridge_temp_c) as temp, latest(co2_psi) as co2\n| eval temp_ok=if(temp>=2 AND temp<=5,\"good\",\"check\"), co2_ok=if(co2>=8 AND co2<=14,\"good\",\"check\")",
  "description": "Monitors UC-25.15.75 — Kegerator, Temperature, and, CO2, Health — using `index=personal` sourcetype `kegerator:pour`, grouped by `entity`. Outputs `count` each `the scheduled window`; alert when `temp` >= 2, `temp` <= 5, `co2` >= 8, or `co2` <= 14. Primary focus: CO2 Health. Purpose-built for 'Kegerator Temperature and CO2 Health' in Personal workflows.",
  "value": "For 'Kegerator Temperature and CO2 Health', UC-25.15.75 converts `activity` on `kegerator:pour` feeds into actionable rows and reduces missed training goals, stale hobby data, and connector failures.",
  "implementation": "Ingest fridge temperature and regulator pressure; surface simple good-or-check states for each.",
  "visualization": "Single-value panels for serving temperature and CO2 pressure.",
  "cimModels": [],
  "references": [
    {
      "title": "ESPHome — sensor components",
      "url": "https://esphome.io/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.15.75)** — Connector for 'Kegerator Temperature and CO2 Health' stops polling `kegerator:pour`; stale `count` on `external_id` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `count` over `1w` is expected for UC-25.15.75, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `kegerator:pour`. Dedup with `stats latest(*) by external_id`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `temp` >= 2 for 'Kegerator Temperature and CO2 Health'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Homebrewing, Beer & Wine — typical feeds: Fermentation gravity/temperature (`brew:fermentation`), kegerator pours (`kegerator:pour`), wine-cellar climate (`winecellar:reading`)..\n• **Sourcetypes in this search**: `kegerator:pour`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=kegerator:pour`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Fermentation sensors (Tilt/iSpindel hydrometer, temperature), kegerator flow meters and scales, and wine-cellar climate sensors via MQTT and Splunk HEC..\n\n**Author notes:** Ingest fridge temperature and regulator pressure; surface simple good-or-check states for each.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"kegerator:pour\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=kegerator:pour earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Kegerator Temperature and CO2 Health** over `personal` data (primarily `sourcetype=kegerator:pour`) across the full search range. It applies numeric thresholds on `temp` (>= 2) after latest — tune these constants to your household baseline.\n\n**Fields this search depends on:** `co2`, `temp`, `temp_ok`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=kegerator:pour\n| stats latest(fridge_temp_c) as temp, latest(co2_psi) as co2\n| eval temp_ok=if(temp>=2 AND temp<=5,\"good\",\"check\"), co2_ok=if(co2>=8 AND co2<=14,\"good\",\"check\")\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=kegerator:pour earliest=-7d\n| head 50\n| table co2, temp, temp_ok\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=kegerator:pour earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `temp` before alerting:\n\n```spl\nindex=personal sourcetype=kegerator:pour earliest=-30d\n| stats perc50(temp) AS p50 perc90(temp) AS p90 perc99(temp) AS p99 max(temp) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.15.75: Personal evidence` with the following panel layout:\n\n**Panel 1 — Single-Value Panels For Serving Temperature And Co2 Pressure (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `kegerator:pour` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=kegerator:pour | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It checks that your beer fridge is cold enough and the gas pressure is right, so every pint tastes good.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
