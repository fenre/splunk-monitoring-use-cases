{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.44.31",
  "title": "Cross-App Spot Note Backlog",
  "criticality": "low",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Cross-App Spot Note Backlog' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_cross_app_spot_note_backlog` (UC-25.44.31), SOC dashboard panel \"Personal — Cross-App Spot Note Backlog\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.44.31 positive (Cross, App, Spot, Note, Backlog): ingest synthetic events into `index=personal` sourcetype='outdoor:queue' where `sum` grouped by `source_app` satisfies `backlog` > 0 for 'Cross-App Spot Note Backlog'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.44.31 negative (Note Backlog): ingest benign `personal` / `outdoor:queue` events where `source_app` stays below `backlog` > 0, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 0 confirms zero alert rows for 'Cross-App Spot Note Backlog'."
  },
  "dataSources": "Catch trips (`outdoor:trip`), hunt map activity (`hunt:map`), foraging observations (`forage:observation`), smoker sessions (`cook:smoker`), harvest queues (`outdoor:queue`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Fishing, hunting, foraging, and outdoor-cooking telemetry — Fishbrain catch logs, onX Hunt map exports, iNaturalist observations, and ThermoWorks smoker probes — streamed to Splunk HEC via APIs, exports, and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=outdoor:queue source_app=* note_state=*\n| stats sum(eval(if(note_state!=\"linked\",1,0))) as backlog, count as total by source_app\n| where backlog>0\n| sort - backlog",
  "description": "Identifies 'Cross-App Spot Note Backlog' (UC-25.44.31) using `index=personal` sourcetype `outdoor:queue` by aggregating `sum` grouped by `source_app` over `the scheduled window` and alerting when `backlog` > 0. Keywords: Cross, App, Spot, Note, Backlog. Focuses on Note Backlog behaviour so personal teams can triage UC-25.44.31 without ad-hoc log review.",
  "value": "UC-25.44.31 accelerates insight into Note Backlog ('Cross-App Spot Note Backlog') by tracking `activity` on `outdoor:queue` feeds, reducing missed training goals, stale hobby data, and connector failures before spreadsheet checks hide regressions.",
  "implementation": "Mark each `outdoor:queue` record with a pending-state flag and summarize backlog totals by `source_app`.",
  "visualization": "Table of source app with pending spot notes counts.",
  "cimModels": [],
  "references": [
    {
      "title": "Fishbrain — fishing app",
      "url": "https://fishbrain.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "onX Hunt — maps",
      "url": "https://www.onxmaps.com/hunt/app",
      "retrieved": "2026-04-25"
    },
    {
      "title": "iNaturalist — observations",
      "url": "https://www.inaturalist.org/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "ThermoWorks Signals — smoker telemetry",
      "url": "https://www.thermoworks.com/signals/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.44.31)** — Connector for 'Cross-App Spot Note Backlog' stops polling `outdoor:queue`; stale `sum` on `source_app` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `sum` over `1w` is expected for UC-25.44.31, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `outdoor:queue`. Dedup with `stats latest(*) by source_app`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `backlog` > 0 for 'Cross-App Spot Note Backlog'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Fishing, Hunting & Foraging — typical feeds: Fishing catches (`fishing:catch`), fishfinder sonar (`fishfinder:reading`), foraging finds (`foraging:find`), game-cam triggers (`gamecam:trigger`)..\n• **Sourcetypes in this search**: `outdoor:queue`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=outdoor:queue`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Outdoor logs — fishing apps (Fishbrain), sonar / fishfinder exports, game/trail cameras, and foraging journals — streamed to Splunk HEC via APIs and scripted inputs..\n\n**Author notes:** Mark each `outdoor:queue` record with a pending-state flag and summarize backlog totals by `source_app`.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"outdoor:queue\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=outdoor:queue earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Cross-App Spot Note Backlog** over `personal` data (primarily `sourcetype=outdoor:queue`) across the full search range. It applies numeric thresholds on `backlog` (> 0) after sum — tune these constants to your household baseline.\n\n**Fields this search depends on:** `backlog`, `source_app`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=outdoor:queue source_app=* note_state=*\n| stats sum(eval(if(note_state!=\"linked\",1,0))) as backlog, count as total by source_app\n| where backlog>0\n| sort - backlog\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=outdoor:queue earliest=-7d\n| head 50\n| table backlog, source_app\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=outdoor:queue earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `backlog` before alerting:\n\n```spl\nindex=personal sourcetype=outdoor:queue earliest=-30d\n| stats perc50(backlog) AS p50 perc90(backlog) AS p90 perc99(backlog) AS p99 max(backlog) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.44.31: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Source App With Pending Spot Notes Counts (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `outdoor:queue` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=outdoor:queue | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It frames where unfinished spot notes are piling up so you can clear the biggest queues in outdoor research logs.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
