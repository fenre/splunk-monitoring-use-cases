{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.79.27",
  "title": "Client Pipeline Value Stage Concentration",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics",
    "Risk"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Client Pipeline Value Stage Concentration' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_client_pipeline_value_stage_concentration` (UC-25.79.27), SOC dashboard panel \"Personal — Client Pipeline Value Stage Concentration\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.79.27 positive (Client, Pipeline, Value, Stage, Concentration): ingest synthetic events into `index=personal` sourcetype='freelance:lead' where `amount`, `total_amount` grouped by `stage` satisfies detection threshold for 'Client Pipeline Value Stage Concentration'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.79.27 negative (Stage Concentration): ingest benign `personal` / `freelance:lead` events where `stage` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 2 confirms zero alert rows for 'Client Pipeline Value Stage Concentration'."
  },
  "dataSources": "Invoices (`freelance:invoice`), billed hours (`freelance:hours`), tax estimates (`freelance:tax`), client pipeline records (`freelance:lead`), project health (`freelance:project`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Freelance invoices, billed-hours exports, quarterly tax estimates, and client-pipeline updates streamed to Splunk HEC via accounting APIs, spreadsheets, and CRM forms. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=freelance:lead\n| stats sum(pipeline_value_usd) as amount count as records by stage\n| eventstats sum(amount) as total_amount\n| eval pct_of_total=round(100*amount/total_amount,1)\n| sort - pct_of_total",
  "description": "Identifies UC-25.79.27 for Client, Pipeline, Value, Stage, Concentration over `index=personal` sourcetype `freelance:lead` with thresholds configured threshold. Groups `amount`, `total_amount` by `stage` each `the scheduled window`. Centered on Stage Concentration indicators. Supports repeatable personal triage for 'Client Pipeline Value Stage Concentration'.",
  "value": "For 'Client Pipeline Value Stage Concentration', UC-25.79.27 converts `amount`, `total_amount` on `freelance:lead` feeds into actionable rows and reduces missed training goals, stale hobby data, and connector failures.",
  "implementation": "Use stable group labels such as client segment, quarter, or service line for consistent trending.",
  "visualization": "Treemap or bar chart of amount share by stage.",
  "cimModels": [],
  "references": [
    {
      "title": "IRS - Estimated taxes",
      "url": "https://www.irs.gov/businesses/small-businesses-self-employed/estimated-taxes",
      "retrieved": "2026-04-25"
    },
    {
      "title": "FreshBooks - freelancer invoicing",
      "url": "https://www.freshbooks.com/hub/invoicing/how-to-invoice-as-a-freelancer",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Upwork - freelance proposals",
      "url": "https://www.upwork.com/resources/how-to-write-a-proposal",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.79.27)** — Connector for 'Client Pipeline Value Stage Concentration' stops polling `freelance:lead`; stale `amount`, `total_amount` on `stage` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `amount`, `total_amount` over `1w` is expected for UC-25.79.27, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `freelance:lead`. Dedup with `stats latest(*) by stage`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Client Pipeline Value Stage Concentration'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "m365"
  ],
  "equipmentModels": [
    "m365_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Side Hustle & Freelance — typical feeds: Invoices (`freelance:invoice`), billed hours (`freelance:hours`), tax estimates (`freelance:tax`), client pipeline records (`freelance:lead`), project health (`freelance:project`)..\n• **Sourcetypes in this search**: `freelance:lead`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=freelance:lead`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Freelance invoices, billed-hours exports, quarterly tax estimates, and client-pipeline updates streamed to Splunk HEC via accounting APIs, spreadsheets, and CRM forms..\n\n**Author notes:** Use stable group labels such as client segment, quarter, or service line for consistent trending.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"freelance:lead\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=freelance:lead earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Client Pipeline Value Stage Concentration** over `personal` data (primarily `sourcetype=freelance:lead`) across the full search range. `eventstats` / `streamstats` compute rolling baselines before filtering outliers; window size controls sensitivity (smaller window = noisier alerts).\n\n**Fields this search depends on:** `stage`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=freelance:lead\n| stats sum(pipeline_value_usd) as amount count as records by stage\n| eventstats sum(amount) as total_amount\n| eval pct_of_total=round(100*amount/total_amount,1)\n| sort - pct_of_total\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=freelance:lead earliest=-7d\n| head 50\n| table stage\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=freelance:lead earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.79.27: Personal evidence` with the following panel layout:\n\n**Panel 1 — Treemap Or Bar Chart Of Amount Share By Stage (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `freelance:lead` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=freelance:lead | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Everything is an outlier:** Baseline window is too short or `stdev=0` — require minimum sample count (`where count>=5`) before computing z-scores.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It produces whether too much of client pipeline value depends on one stage bucket.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
