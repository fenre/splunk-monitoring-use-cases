{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.13.202",
  "title": "Roam Daily Notes Graph Growth by Month",
  "criticality": "low",
  "difficulty": "beginner",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Roam Daily Notes Graph Growth by Month' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_roam_daily_notes_graph_growth_by_month` (UC-25.13.202), SOC dashboard panel \"Personal — Roam Daily Notes Graph Growth by Month\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.13.202 positive (Roam, Daily, Notes, Graph, Growth): ingest synthetic events into `index=personal` sourcetype='roam:daily' where `month_end_value` grouped by `_time` satisfies detection threshold for 'Roam Daily Notes Graph Growth by Month'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.13.202 negative (by Month): ingest benign `personal` / `roam:daily` events where `_time` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 9 confirms zero alert rows for 'Roam Daily Notes Graph Growth by Month'."
  },
  "dataSources": "Obsidian graph stats (`obsidian:stats`), Roam daily notes (`roam:daily`), Logseq tasks (`logseq:task`), Zettelkasten notes (`zettel:note`), PKM review summaries (`pkm:review`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Obsidian graph stats, Roam daily notes, Logseq task exports, and Zettelkasten counts streamed to Splunk HEC via plugins, CSV exports, and scripted vault scans. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=roam:daily\n| bin _time span=1mon\n| stats max(daily_note_words) as month_end_value by _time\n| sort - _time",
  "description": "Monitors UC-25.13.202 — Roam, Daily, Notes, Graph, Growth — using `index=personal` sourcetype `roam:daily`, grouped by `_time`. Outputs `month_end_value` each `1m`; alert when configured threshold. Primary focus: by Month. Purpose-built for 'Roam Daily Notes Graph Growth by Month' in Personal workflows.",
  "value": "Operators running UC-25.13.202 on `roam:daily` feeds shorten triage for by Month tied to 'Roam Daily Notes Graph Growth by Month', lowering missed training goals, stale hobby data, and connector failures.",
  "implementation": "Use a stable month-end summary event if the source emits many small updates each day.",
  "visualization": "Line chart of month-end daily note words.",
  "cimModels": [],
  "references": [
    {
      "title": "Obsidian",
      "url": "https://obsidian.md/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Roam Research",
      "url": "https://roamresearch.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Logseq",
      "url": "https://logseq.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Zettelkasten Method",
      "url": "https://zettelkasten.de/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.13.202)** — Connector for 'Roam Daily Notes Graph Growth by Month' stops polling `roam:daily`; stale `month_end_value` on `_time` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `month_end_value` over `1m` is expected for UC-25.13.202, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `roam:daily`. Dedup with `stats latest(*) by _time`.\n\n4. **Timezone bucket misalignment** — `1m` bins on UTC vs local schedule skew goal threshold for 'Roam Daily Notes Graph Growth by Month'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "hashicorp"
  ],
  "equipmentModels": [
    "hashicorp_vault"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Personal Knowledge & PKM — typical feeds: Obsidian graph stats (`obsidian:stats`), Roam daily notes (`roam:daily`), Logseq tasks (`logseq:task`), Zettelkasten notes (`zettel:note`), PKM review summaries (`pkm:review`)..\n• **Sourcetypes in this search**: `roam:daily`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=roam:daily`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Obsidian graph stats, Roam daily notes, Logseq task exports, and Zettelkasten counts streamed to Splunk HEC via plugins, CSV exports, and scripted vault scans..\n\n**Author notes:** Use a stable month-end summary event if the source emits many small updates each day.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"roam:daily\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=roam:daily earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Roam Daily Notes Graph Growth by Month** over `personal` data (primarily `sourcetype=roam:daily`) across the full search range. The `max` aggregation groups by `_time`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `_time`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=roam:daily\n| bin _time span=1mon\n| stats max(daily_note_words) as month_end_value by _time\n| sort - _time\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=roam:daily earliest=-7d\n| head 50\n| table _time\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=roam:daily earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.13.202: Personal evidence` with the following panel layout:\n\n**Panel 1 — Line Chart Of Month-End Daily Note Words (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `roam:daily` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=roam:daily | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=1m` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1m` buckets.",
  "grandmaExplanation": "It checks how your roam daily notes system is growing over the long run.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
