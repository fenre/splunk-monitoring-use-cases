{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.19.12",
  "title": "Toilet Refill-Cycle Leak Fingerprint",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Anomaly"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Toilet Refill-Cycle Leak Fingerprint' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_toilet_refill_cycle_leak_fingerprint` (UC-25.19.12), SOC dashboard panel \"Personal — Toilet Refill-Cycle Leak Fingerprint\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=personal` sourcetype='watermeter:flow' where `bathroom` satisfies `refill_events` > 3 for UC-25.19.12 ('Toilet Refill-Cycle Leak Fingerprint'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `personal` / `watermeter:flow` events where `bathroom` stays below `refill_events` > 3, or exclude the test entity via `personal_monitoring_notes.csv` with a valid window. Confirm zero alert rows for UC-25.19.12."
  },
  "dataSources": "Whole-home water flow (`watermeter:flow`), water pressure (`waterpressure:reading`), irrigation control (`irrigation:zone`), water-heater events (`waterheater:event`), greywater tanks (`greywater:tank`), and utility sensors for pumps and softeners (`sumppump:event`, `wellpump:event`, `watersoftener:status`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Water and plumbing telemetry from smart water meters, pressure sensors, irrigation controllers, sump and well pumps, greywater tanks, and water-heater sensors streamed to Splunk HEC via MQTT and vendor APIs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=watermeter:flow fixture=\"toilet\"\n| bin _time span=30m\n| stats count as refill_events avg(flow_liters) as avg_cycle_liters by bathroom _time\n| where refill_events>3",
  "description": "Looks for unusually frequent refill cycles from the same toilet, a classic sign of a flapper or valve leak. Searches `index=personal` with sourcetype `watermeter:flow` for UC-25.19.12, aggregating `avg` by `bathroom` over `30m`. Alerts when grouped results cross `refill_events` > 3 in the saved search. Gives Personal operators a repeatable signal for 'Toilet Refill-Cycle Leak Fingerprint' without manual log review.",
  "value": "Looks for unusually frequent refill cycles from the same toilet, a classic sign of a flapper or valve leak. Timely UC-25.19.12 alerting on 'Toilet Refill-Cycle Leak Fingerprint' reduces missed training goals, stale hobby data, and connector failures before manual triage or audit channels escalate.",
  "implementation": "Tag fixture-level water events from an instrumented toilet or a smart meter classifier and review short repeat cycles.",
  "visualization": "Timeline of refill-event counts by bathroom.",
  "cimModels": [],
  "references": [
    {
      "title": "Flume - personal API",
      "url": "https://help.flumewater.com/en/articles/3457857-flume-personal-api",
      "retrieved": "2026-04-25"
    },
    {
      "title": "OpenSprinkler - API",
      "url": "https://openthings.freshdesk.com/support/solutions/articles/5000821298-opensprinkler-api",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry** — Connector for 'Toilet Refill-Cycle Leak Fingerprint' stops polling `watermeter:flow`; stale `bathroom` rows remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `avg` over `30m` is expected, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `watermeter:flow`. Dedup with `stats latest(*) by bathroom`.\n\n4. **Timezone bucket misalignment** — `30m` bins on UTC vs local schedule skew `refill_events` > 3. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Water, Plumbing & Utilities — typical feeds: Whole-home water flow (`watermeter:flow`), sump pump (`sumppump:event`), well pump (`wellpump:event`), pool/spa chemistry (`pool:chemistry`), water softener (`watersoftener:status`), irrigation zones .\n• **Sourcetypes in this search**: `watermeter:flow`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=watermeter:flow`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Smart water meters (Flume/Flo/HomeWizard), sump- and well-pump sensors, pool/spa controllers, water-softener and irrigation controllers via vendor APIs, MQTT, and HEC..\n\n**Author notes:** Tag fixture-level water events from an instrumented toilet or a smart meter classifier and review short repeat cycles.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"watermeter:flow\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=watermeter:flow earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Toilet Refill-Cycle Leak Fingerprint** over `personal` data (primarily `sourcetype=watermeter:flow`) across the full search range. It applies numeric thresholds on `refill_events` (> 3) after avg — tune these constants to your household baseline.\n\n**Fields this search depends on:** `_time`, `bathroom`, `refill_events`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=watermeter:flow fixture=\"toilet\"\n| bin _time span=30m\n| stats count as refill_events avg(flow_liters) as avg_cycle_liters by bathroom _time\n| where refill_events>3\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=watermeter:flow earliest=-7d\n| head 50\n| table _time, bathroom, refill_events\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=watermeter:flow earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `refill_events` before alerting:\n\n```spl\nindex=personal sourcetype=watermeter:flow earliest=-30d\n| stats perc50(refill_events) AS p50 perc90(refill_events) AS p90 perc99(refill_events) AS p99 max(refill_events) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.19.12: Personal evidence` with the following panel layout:\n\n**Panel 1 — Timeline Of Refill-Event Counts By Bathroom (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `watermeter:flow` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=watermeter:flow | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=30m` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `30m` buckets.",
  "grandmaExplanation": "It tracks when the same toilet keeps topping itself up over and over, which usually means water is leaking away.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
