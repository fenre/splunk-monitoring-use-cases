{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.82.11",
  "title": "Gas Bill Tracking Payment Overdue Alert",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Risk",
    "Business"
  ],
  "splunkPillar": "Platform",
  "dataSources": "Utility bills (`utility:bill`), utility meter readings (`utility:meter`), utility rate changes (`utility:rate`), household accounts (`household:account`), utility payments (`utility:payment`).",
  "app": "Electric, gas, and water bills, smart-meter readings, household-account records, and utility rate-plan changes streamed to Splunk HEC via portals, CSV exports, and scripted inputs.",
  "spl": "index=personal sourcetype=utility:bill bill_type=\"gas\"\n| eval days_overdue=round((now()-due_epoch)/86400,1)\n| where payment_status!=\"paid\" AND days_overdue>0\n| table _time provider bill_amount days_overdue\n| sort - days_overdue",
  "description": "Lists unpaid gas bill tracking items that are already past due.",
  "value": "Late utility payments are one of the easiest household problems to prevent with a small exception queue.",
  "implementation": "Map provider payment state into a simple `payment_status` field and populate `due_epoch` from the statement.",
  "visualization": "Table of overdue utility payments by provider and overdue age.",
  "cimModels": [],
  "references": [
    {
      "title": "U.S. Energy Information Administration",
      "url": "https://www.eia.gov/"
    },
    {
      "title": "EPA WaterSense",
      "url": "https://www.epa.gov/watersense"
    },
    {
      "title": "Home Assistant Utility Meter",
      "url": "https://www.home-assistant.io/integrations/utility_meter/"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Utility Bills & Household Accounts — typical feeds: Utility bills (`utility:bill`), utility meter readings (`utility:meter`), utility rate changes (`utility:rate`), household accounts (`household:account`), utility payments (`utility:payment`)..\n• **Sourcetypes in this search**: `utility:bill`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=utility:bill`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Electric, gas, and water bills, smart-meter readings, household-account records, and utility rate-plan changes streamed to Splunk HEC via portals, CSV exports, and scripted inputs..\n\n**Author notes:** Map provider payment state into a simple `payment_status` field and populate `due_epoch` from the statement.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"utility:bill\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=utility:bill earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Gas Bill Tracking Payment Overdue Alert** over `personal` data (primarily `sourcetype=utility:bill`) across the full search range. It applies numeric thresholds on `days_overdue` (> 0) after aggregation — tune these constants to your household baseline.\n\n**Fields this search depends on:** `days_overdue`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=utility:bill bill_type=\"gas\"\n| eval days_overdue=round((now()-due_epoch)/86400,1)\n| where payment_status!=\"paid\" AND days_overdue>0\n| table _time provider bill_amount days_overdue\n| sort - days_overdue\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=utility:bill earliest=-7d\n| head 50\n| table days_overdue\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=utility:bill earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `days_overdue` before alerting:\n\n```spl\nindex=personal sourcetype=utility:bill earliest=-30d\n| stats perc50(days_overdue) AS p50 perc90(days_overdue) AS p90 perc99(days_overdue) AS p99 max(days_overdue) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.82.11: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Overdue Utility Payments By Provider And Overdue Age (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `utility:bill` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=utility:bill | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It checks which gas bill tracking bills are already late and should be handled first.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
