{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.27.9",
  "title": "Tank Equipment Uptime and Pump Health",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Tank Equipment Uptime and Pump Health' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_tank_equipment_uptime_and_pump_health` (UC-25.27.9), SOC dashboard panel \"Personal — Tank Equipment Uptime and Pump Health\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.27.9 positive (Tank, Equipment, Uptime, and, Pump): ingest synthetic events into `index=personal` sourcetype='reeftank:param' where `count` grouped by `entity` satisfies `return_pump` < 5 for 'Tank Equipment Uptime and Pump Health'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.27.9 negative (Pump Health): ingest benign `personal` / `reeftank:param` events where `entity` stays below `return_pump` < 5, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 3 confirms zero alert rows for 'Tank Equipment Uptime and Pump Health'."
  },
  "dataSources": "Reef-tank parameters (`reeftank:param`), vivarium climate (`vivarium:climate`), fish feeder (`fishfeeder:event`), auto-doser (`doser:event`), auto-top-off (`ato:event`), pond sensors (`pondsensor:reading`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Aquarium and vivarium controllers — reef-tank parameter monitors, auto-dosers, auto-top-off, reptile terrarium climate/UVB, and pond sensors — streamed to Splunk HEC via MQTT and controller APIs (e.g. Apex, GHL, ESPHome). — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=reeftank:param\n| stats latest(return_pump_w) as return_pump, latest(skimmer_w) as skimmer, latest(_time) as seen\n| eval pump_off=if(return_pump<5,1,0), stale=if((now()-seen)>600,1,0)",
  "description": "Detects when Pump Health criteria fire for 'Tank Equipment Uptime and Pump Health' (UC-25.27.9) in `index=personal` sourcetype `reeftank:param`. Pipeline aggregates `count` by `entity` over `the scheduled window`. Alert logic enforces `return_pump` < 5. Title terms: Tank, Equipment, Uptime, and, Pump. Eliminates manual hunting for UC-25.27.9 conditions.",
  "value": "UC-25.27.9 helps teams contain Tank, Equipment, Uptime, and, Pump scenarios and reduce missed training goals, stale hobby data, and connector failures before incidents compound.",
  "implementation": "Ingest equipment power draw into `index=personal`; alert when the return pump stops or telemetry goes stale.",
  "visualization": "Status panel of pump and skimmer power with a health flag.",
  "cimModels": [],
  "references": [
    {
      "title": "Neptune Apex — aquarium controller",
      "url": "https://www.neptunesystems.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.27.9)** — Connector for 'Tank Equipment Uptime and Pump Health' stops polling `reeftank:param`; stale `count` on `external_id` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `count` over `1w` is expected for UC-25.27.9, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `reeftank:param`. Dedup with `stats latest(*) by external_id`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `return_pump` < 5 for 'Tank Equipment Uptime and Pump Health'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Aquariums, Reptiles & Vivariums — typical feeds: Reef-tank parameters (`reeftank:param`), vivarium climate (`vivarium:climate`), fish feeder (`fishfeeder:event`), auto-doser (`doser:event`), auto-top-off (`ato:event`), pond sensors (`pondsensor:read.\n• **Sourcetypes in this search**: `reeftank:param`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=reeftank:param`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Aquarium and vivarium controllers — reef-tank parameter monitors, auto-dosers, auto-top-off, reptile terrarium climate/UVB, and pond sensors — via MQTT and controller APIs (Apex, GHL, ESPHome)..\n\n**Author notes:** Ingest equipment power draw into `index=personal`; alert when the return pump stops or telemetry goes stale.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"reeftank:param\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=reeftank:param earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Tank Equipment Uptime and Pump Health** over `personal` data (primarily `sourcetype=reeftank:param`) across the full search range. It applies numeric thresholds on `return_pump` (< 5) after latest — tune these constants to your household baseline.\n\n**Fields this search depends on:** `pump_off`, `return_pump`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=reeftank:param\n| stats latest(return_pump_w) as return_pump, latest(skimmer_w) as skimmer, latest(_time) as seen\n| eval pump_off=if(return_pump<5,1,0), stale=if((now()-seen)>600,1,0)\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=reeftank:param earliest=-7d\n| head 50\n| table pump_off, return_pump\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=reeftank:param earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `return_pump` before alerting:\n\n```spl\nindex=personal sourcetype=reeftank:param earliest=-30d\n| stats perc50(return_pump) AS p50 perc90(return_pump) AS p90 perc99(return_pump) AS p99 max(return_pump) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.27.9: Personal evidence` with the following panel layout:\n\n**Panel 1 — Status Panel Of Pump And Skimmer Power With A Health Flag (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `reeftank:param` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=reeftank:param | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It checks the tank's pumps are actually running, so a stopped pump is caught before the fish are harmed.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
