{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.10.143",
  "title": "Nest-Box Occupancy Timeline",
  "criticality": "low",
  "difficulty": "beginner",
  "monitoringType": [
    "Inventory"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Nest-Box Occupancy Timeline' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_nest_box_occupancy_timeline` (UC-25.10.143), SOC dashboard panel \"Personal — Nest-Box Occupancy Timeline\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.10.143 positive (Nest, Box, Occupancy, Timeline): ingest synthetic events into `index=personal` sourcetype='nestbox:event' where `count` grouped by `box`, `_time` satisfies detection threshold for 'Nest-Box Occupancy Timeline'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.10.143 negative (Occupancy Timeline): ingest benign `personal` / `nestbox:event` events where `box`, `_time` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 2 confirms zero alert rows for 'Nest-Box Occupancy Timeline'."
  },
  "dataSources": "Bird acoustic detections (`birdnet:detection`), wildlife-camera visits (`wildlifecam:visit`), nest-box state (`nestbox:event`), frog-call activity (`frogcall:detection`), and pollinator counts (`pollinator:count`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Wildlife telemetry from BirdNET, trail and feeder cameras, nest-box sensors, frog-call detectors, and pollinator counters streamed to Splunk HEC via MQTT, APIs, and scripted imports. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=nestbox:event\n| bin _time span=1d\n| stats latest(status) as status by box _time\n| sort box _time",
  "description": "Monitors UC-25.10.143 for Nest, Box, Occupancy, Timeline over `index=personal` sourcetype `nestbox:event` with thresholds configured threshold. Groups `count` by `box`, `_time` each `1d`. Centered on Occupancy Timeline indicators. Supports repeatable personal triage for 'Nest-Box Occupancy Timeline'.",
  "value": "For 'Nest-Box Occupancy Timeline', UC-25.10.143 converts `activity` on `nestbox:event` feeds into actionable rows and reduces missed training goals, stale hobby data, and connector failures.",
  "implementation": "Log box state transitions such as empty, inspecting, nesting, active, and fledged from sensors or manual observations.",
  "visualization": "Heat map of daily nest-box status by box.",
  "cimModels": [],
  "references": [
    {
      "title": "BirdNET - acoustic bird identification",
      "url": "https://birdnet.cornell.edu/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "iNaturalist - biodiversity observations",
      "url": "https://www.inaturalist.org/pages/api+reference",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.10.143)** — Connector for 'Nest-Box Occupancy Timeline' stops polling `nestbox:event`; stale `count` on `box`, `_time` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `count` over `1d` is expected for UC-25.10.143, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `nestbox:event`. Dedup with `stats latest(*) by box`.\n\n4. **Timezone bucket misalignment** — `1d` bins on UTC vs local schedule skew goal threshold for 'Nest-Box Occupancy Timeline'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Wildlife & Biodiversity — typical feeds: BirdNET detections (`birdnet:detection`), trail-cam detections (`trailcam:detection`), moth-trap captures (`mothtrap:capture`), bat-detector passes (`batdetector:pass`), pollinator counts (`pollinator.\n• **Sourcetypes in this search**: `nestbox:event`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=nestbox:event`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Backyard wildlife monitoring — BirdNET acoustic bird ID, trail cameras with object detection, camera moth traps, ultrasonic bat detectors, and pollinator counts — via MQTT and scripted inputs..\n\n**Author notes:** Log box state transitions such as empty, inspecting, nesting, active, and fledged from sensors or manual observations.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"nestbox:event\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=nestbox:event earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Nest-Box Occupancy Timeline** over `personal` data (primarily `sourcetype=nestbox:event`) across the full search range. The `latest` aggregation groups by `box`, `_time`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `_time`, `box`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=nestbox:event\n| bin _time span=1d\n| stats latest(status) as status by box _time\n| sort box _time\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=nestbox:event earliest=-7d\n| head 50\n| table _time, box\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=nestbox:event earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.10.143: Personal evidence` with the following panel layout:\n\n**Panel 1 — Heat Map Of Daily Nest-Box Status By Box (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `nestbox:event` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=nestbox:event | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=1d` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1d` buckets.",
  "grandmaExplanation": "It spots which nest boxes are empty, active, or finished as the season progresses.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
