{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.47.31",
  "title": "Goodreads Buddy-Read Completion Rate",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Goodreads Buddy-Read Completion Rate' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_goodreads_buddy_read_completion_rate` (UC-25.47.31), SOC dashboard panel \"Personal — Goodreads Buddy-Read Completion Rate\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=personal` sourcetype='reading:book' that satisfy the UC-25.47.31 SPL filters for 'Goodreads Buddy-Read Completion Rate'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `personal` / `reading:book` events outside the detection threshold, or exclude the test entity via `personal_monitoring_notes.csv` with a valid window. Confirm zero alert rows for UC-25.47.31."
  },
  "dataSources": "Highlights (`reading:highlight`), book activity (`reading:book`), reviews (`reading:review`), paper libraries (`research:paper`), sync states (`reading:sync`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Reading and second-brain telemetry — Readwise review exports, Kindle highlights, Goodreads shelves, and Zotero research libraries — streamed to Splunk HEC via APIs, exports, and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=reading:book buddy_name=* completed=*\n| stats count as total, sum(eval(if(completed=\"yes\",1,0))) as matched by buddy_name\n| eval a_completed_buddy_read_pct=round(100*matched/total,1)\n| sort - a_completed_buddy_read_pct",
  "description": "Measures the percentage of buddy-read attempts that end with a completed buddy read by buddy.",
  "value": "Completion and exception percentages make it easier to spot which buddy are helping or hurting buddy-read attempts.",
  "implementation": "Store a normalized flag for `a completed buddy read` on `reading:book` events and summarize the hit rate by `buddy_name`.",
  "visualization": "Table of buddy with percentage of buddy-read attempts ending with a completed buddy read.",
  "cimModels": [],
  "references": [
    {
      "title": "Readwise — review workflow",
      "url": "https://readwise.io/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Amazon Kindle",
      "url": "https://www.amazon.com/kindle-dbs/fd/kcp",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Goodreads",
      "url": "https://www.goodreads.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Zotero",
      "url": "https://www.zotero.org/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry** — Vendor connector stops polling for 'Goodreads Buddy-Read Completion Rate'; stale rows remain in `index=personal`. Check `_internal` HEC 401s and renew token in passwords.conf.\n\n2. **Rest day or planned break** — Zero activities is expected, not ingestion failure. Vendor app shows no sessions for the calendar day.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `reading:book`. Dedup with `stats latest(*) by external_id`.\n\n4. **Timezone bucket misalignment** — Weekly bins on UTC vs local schedule skew thresholds. Document offset in SPL `bin _time` or connector config.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Reading & Second Brain — typical feeds: Reading progress (`reading:progress`), highlights (`highlight:capture`), note links (`note:link`), books finished (`book:finished`)..\n• **Sourcetypes in this search**: `reading:book`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=reading:book`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Reading and knowledge-management telemetry — Goodreads / StoryGraph libraries, Kindle highlights (Readwise), and note-graph exports (Obsidian, Notion) — streamed to Splunk HEC via APIs and scripted inputs..\n\n**Author notes:** Store a normalized flag for `a completed buddy read` on `reading:book` events and summarize the hit rate by `buddy_name`.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"reading:book\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=reading:book earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Goodreads Buddy-Read Completion Rate** over `personal` data (primarily `sourcetype=reading:book`) across the full search range. The `sum` aggregation groups by `buddy_name`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `buddy_name`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=reading:book buddy_name=* completed=*\n| stats count as total, sum(eval(if(completed=\"yes\",1,0))) as matched by buddy_name\n| eval a_completed_buddy_read_pct=round(100*matched/total,1)\n| sort - a_completed_buddy_read_pct\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=reading:book earliest=-7d\n| head 50\n| table buddy_name\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=reading:book earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.47.31: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Buddy With Percentage Of Buddy-Read Attempts Ending With A Completed Buddy Read (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `reading:book` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=reading:book | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It gathers how often a completed buddy read happens for each buddy so you can see where buddy-read attempts go smoothly or get stuck.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
