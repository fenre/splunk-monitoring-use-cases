{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.42.5",
  "title": "Flight-Sim Landing Smoothness Score",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Quality"
  ],
  "splunkPillar": "Platform",
  "dataSources": "Pilot logbook (`flightlog:entry`), avionics telemetry (`avionics:telemetry`), flight-sim sessions (`flightsim:session`), pre-flight checklists (`preflight:check`).",
  "app": "Pilot and flight-sim telemetry — electronic logbooks (ForeFlight, LogTen Pro), Stratux / GDL90 avionics receivers, and Microsoft Flight Simulator / X-Plane SimConnect sessions — streamed to Splunk HEC via scripted inputs.",
  "spl": "index=personal sourcetype=flightsim:session event=landing\n| eval grade=case(touchdown_fpm>-100,\"butter\",touchdown_fpm>-300,\"good\",touchdown_fpm>-600,\"firm\",1=1,\"hard\")\n| stats count as landings by grade\n| sort grade",
  "description": "Grades your flight-sim landings by touchdown rate, turning greaser hunting into a tracked skill.",
  "value": "A landing-quality breakdown makes sim practice measurable and genuinely transfers to smoother real approaches.",
  "implementation": "Ingest SimConnect landing events into `index=personal`; grade landings by descent rate.",
  "visualization": "Bar chart of flight-sim landings by smoothness grade.",
  "cimModels": [],
  "references": [
    {
      "title": "MSFS — SimConnect SDK",
      "url": "https://docs.flightsimulator.com/"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "equipment": [
    "hardware_bmc"
  ],
  "equipmentModels": [
    "hardware_bmc_ilo"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Aviation & Flight Simulation — typical feeds: Pilot logbook (`flightlog:entry`), avionics telemetry (`avionics:telemetry`), flight-sim sessions (`flightsim:session`), pre-flight checklists (`preflight:check`)..\n• **Sourcetypes in this search**: `flightsim:session`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=flightsim:session`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Pilot and flight-sim telemetry — electronic logbooks (ForeFlight, LogTen Pro), Stratux / GDL90 avionics receivers, and Microsoft Flight Simulator / X-Plane SimConnect sessions — streamed to Splunk HEC via scripted inputs..\n\n**Author notes:** Ingest SimConnect landing events into `index=personal`; grade landings by descent rate.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"flightsim:session\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=flightsim:session earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Flight-Sim Landing Smoothness Score** over `personal` data (primarily `sourcetype=flightsim:session`) across the full search range. It applies numeric thresholds on `touchdown_fpm` (> -100) after aggregation — tune these constants to your household baseline.\n\n**Fields this search depends on:** `grade`, `touchdown_fpm`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=flightsim:session event=landing\n| eval grade=case(touchdown_fpm>-100,\"butter\",touchdown_fpm>-300,\"good\",touchdown_fpm>-600,\"firm\",1=1,\"hard\")\n| stats count as landings by grade\n| sort grade\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=flightsim:session earliest=-7d\n| head 50\n| table grade, touchdown_fpm\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=flightsim:session earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `touchdown_fpm` before alerting:\n\n```spl\nindex=personal sourcetype=flightsim:session earliest=-30d\n| stats perc50(touchdown_fpm) AS p50 perc90(touchdown_fpm) AS p90 perc99(touchdown_fpm) AS p99 max(touchdown_fpm) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.42.5: Personal evidence` with the following panel layout:\n\n**Panel 1 — Bar Chart Of Flight-Sim Landings By Smoothness Grade (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `flightsim:session` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=flightsim:session | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It grades how smoothly you land in the flight simulator, turning practice into a fun skill to improve.",
  "prerequisiteUseCases": [],
  "wave": "walk"
}
