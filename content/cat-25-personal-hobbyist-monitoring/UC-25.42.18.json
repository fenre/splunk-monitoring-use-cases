{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.42.18",
  "title": "Altitude Deviation Hotspots",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics",
    "Quality"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Altitude Deviation Hotspots' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_altitude_deviation_hotspots` (UC-25.42.18), SOC dashboard panel \"Personal — Altitude Deviation Hotspots\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=personal` sourcetype='avionics:telemetry' where `flight_phase` satisfies detection threshold for UC-25.42.18 ('Altitude Deviation Hotspots'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `personal` / `avionics:telemetry` events where `flight_phase` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv` with a valid window. Confirm zero alert rows for UC-25.42.18."
  },
  "dataSources": "Pilot logbook (`flightlog:entry`), avionics telemetry (`avionics:telemetry`), flight-sim sessions (`flightsim:session`), pre-flight checklists (`preflight:check`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Pilot and flight-sim telemetry — electronic logbooks (ForeFlight, LogTen Pro), Stratux / GDL90 avionics receivers, and Microsoft Flight Simulator / X-Plane SimConnect sessions — streamed to Splunk HEC via scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=avionics:telemetry altitude_error_ft=*\n| stats avg(abs(altitude_error_ft)) as avg_error, max(abs(altitude_error_ft)) as max_error by flight_phase\n| sort - avg_error",
  "description": "Shows the flight phases where altitude control is least consistent. This use case monitors the condition described in the title using the SPL and data sources documented on this page.",
  "value": "Shows the flight phases where altitude control is least consistent. Timely UC-25.42.18 alerting on 'Altitude Deviation Hotspots' reduces missed training goals, stale hobby data, and connector failures before manual triage or audit channels escalate.",
  "implementation": "Ingest avionics telemetry with altitude-error values; summarize average and max deviation by flight phase.",
  "visualization": "Flight-phase table of average and maximum altitude deviation.",
  "cimModels": [],
  "references": [
    {
      "title": "ForeFlight — pilot app",
      "url": "https://foreflight.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "FAA — Airman Certification",
      "url": "https://www.faa.gov/pilots/training/airman_education",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry** — Connector for 'Altitude Deviation Hotspots' stops polling `avionics:telemetry`; stale `flight_phase` rows remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `avg` over `1w` is expected, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `avionics:telemetry`. Dedup with `stats latest(*) by flight_phase`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "hardware_bmc"
  ],
  "equipmentModels": [
    "hardware_bmc_ilo"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Aviation & Flight Simulation — typical feeds: Pilot logbook (`flightlog:entry`), avionics telemetry (`avionics:telemetry`), flight-sim sessions (`flightsim:session`), pre-flight checklists (`preflight:check`)..\n• **Sourcetypes in this search**: `avionics:telemetry`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=avionics:telemetry`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Pilot and flight-sim telemetry — electronic logbooks (ForeFlight, LogTen Pro), Stratux / GDL90 avionics receivers, and Microsoft Flight Simulator / X-Plane SimConnect sessions — streamed to Splunk HEC via scripted inputs..\n\n**Author notes:** Ingest avionics telemetry with altitude-error values; summarize average and max deviation by flight phase.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"avionics:telemetry\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=avionics:telemetry earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Altitude Deviation Hotspots** over `personal` data (primarily `sourcetype=avionics:telemetry`) across the full search range. The `avg, max` aggregation groups by `flight_phase`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `flight_phase`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=avionics:telemetry altitude_error_ft=*\n| stats avg(abs(altitude_error_ft)) as avg_error, max(abs(altitude_error_ft)) as max_error by flight_phase\n| sort - avg_error\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=avionics:telemetry earliest=-7d\n| head 50\n| table flight_phase\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=avionics:telemetry earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.42.18: Personal evidence` with the following panel layout:\n\n**Panel 1 — Flight-Phase Table Of Average And Maximum Altitude Deviation (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `avionics:telemetry` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=avionics:telemetry | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It measures which parts of a flight are most likely to see altitude wandering, helping you target practice more precisely.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
