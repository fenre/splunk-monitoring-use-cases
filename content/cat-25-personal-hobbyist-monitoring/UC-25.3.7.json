{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.3.7",
  "title": "Charging Session Failure Detection",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers personal hobby monitoring for 'Charging Session Failure Detection' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_charging_session_failure_detection` (UC-25.3.7), SOC dashboard panel \"Personal — Charging Session Failure Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.3.7 positive (Charging, Session, Failure, Detection): ingest synthetic events into `index=personal` sourcetype='evcharger:session' where `sum` grouped by `charger_id` satisfies `failed` > 0 for 'Charging Session Failure Detection'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.3.7 negative (Failure Detection): ingest benign `personal` / `evcharger:session` events where `charger_id` stays below `failed` > 0, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 15 confirms zero alert rows for 'Charging Session Failure Detection'."
  },
  "dataSources": "Tesla vehicle/charge state (`tesla:vehicle`, `tesla:charge`), TeslaMate MQTT, OBD-II PIDs (`obd:pid`), Smartcar API (`smartcar:vehicle`), charger sessions (`evcharger:session`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Tesla Fleet API / TeslaMate, Teslafi, and generic OBD-II / Smartcar / manufacturer APIs via HEC scripted inputs and MQTT bridges. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=evcharger:session\n| stats count as sessions, sum(eval(status=\"faulted\" OR status=\"interrupted\")) as failed by charger_id\n| eval fail_rate=round(100*failed/sessions,1)\n| where failed>0\n| sort - fail_rate",
  "description": "Monitors UC-25.3.7 — Charging, Session, Failure, Detection — using `index=personal` sourcetype `evcharger:session`, grouped by `charger_id`. Outputs `sum` each `the scheduled window`; alert when `failed` > 0. Primary focus: Failure Detection. Purpose-built for 'Charging Session Failure Detection' in Personal workflows.",
  "value": "UC-25.3.7 operationalises 'Charging Session Failure Detection' against `evcharger:session` feeds so operators reduce missed training goals, stale hobby data, and connector failures without manual correlation.",
  "implementation": "Ingest charger session records; schedule daily and alert when a charger's failure rate climbs.",
  "visualization": "Table of chargers by failure rate with a trend.",
  "cimModels": [],
  "references": [
    {
      "title": "Open Charge Point Protocol (OCPP)",
      "url": "https://openchargealliance.org/protocols/open-charge-point-protocol/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.3.7)** — Connector for 'Charging Session Failure Detection' stops polling `evcharger:session`; stale `sum` on `charger_id` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `sum` over `1w` is expected for UC-25.3.7, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `evcharger:session`. Dedup with `stats latest(*) by charger_id`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `failed` > 0 for 'Charging Session Failure Detection'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Connected Cars & EVs — typical feeds: Tesla vehicle/charge state (`tesla:vehicle`, `tesla:charge`), TeslaMate MQTT/Postgres, OBD-II PIDs (`obd:pid`), Smartcar API (`smartcar:vehicle`), EV charger sessions (`evcharger:session`)..\n• **Sourcetypes in this search**: `evcharger:session`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=evcharger:session`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Tesla Fleet API / TeslaMate, Teslafi, and generic OBD-II / Smartcar / manufacturer APIs via HEC scripted inputs and MQTT..\n\n**Author notes:** Ingest charger session records; schedule daily and alert when a charger's failure rate climbs.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"evcharger:session\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=evcharger:session earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Charging Session Failure Detection** over `personal` data (primarily `sourcetype=evcharger:session`) across the full search range. It applies numeric thresholds on `failed` (> 0) after sum — tune these constants to your household baseline.\n\n**Fields this search depends on:** `charger_id`, `failed`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=evcharger:session\n| stats count as sessions, sum(eval(status=\"faulted\" OR status=\"interrupted\")) as failed by charger_id\n| eval fail_rate=round(100*failed/sessions,1)\n| where failed>0\n| sort - fail_rate\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=evcharger:session earliest=-7d\n| head 50\n| table charger_id, failed\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=evcharger:session earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `failed` before alerting:\n\n```spl\nindex=personal sourcetype=evcharger:session earliest=-30d\n| stats perc50(failed) AS p50 perc90(failed) AS p90 perc99(failed) AS p99 max(failed) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.3.7: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Chargers By Failure Rate With A Trend (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `evcharger:session` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=evcharger:session | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It spots when your car charger keeps stopping early, so you are not left with an empty battery in the morning.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
