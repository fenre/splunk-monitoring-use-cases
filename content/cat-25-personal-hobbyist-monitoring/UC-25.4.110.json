{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.4.110",
  "title": "Sensor Check-In Gap Detection",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability",
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers personal hobby monitoring for 'Sensor Check-In Gap Detection' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_sensor_check_in_gap_detection` (UC-25.4.110), SOC dashboard panel \"Personal — Sensor Check-In Gap Detection\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.4.110 positive (Sensor, Check, Gap, Detection): ingest synthetic events into `index=personal` sourcetype='zigbee2mqtt:device' where `last_seen` grouped by `device` satisfies `minutes_since` > 120 for 'Sensor Check-In Gap Detection'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.4.110 negative (Gap Detection): ingest benign `personal` / `zigbee2mqtt:device` events where `device` stays below `minutes_since` > 120, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 6 confirms zero alert rows for 'Sensor Check-In Gap Detection'."
  },
  "dataSources": "Zigbee2MQTT device reports (`zigbee2mqtt:device`), Shelly/Tasmota telemetry (`shelly:status`, `tasmota:sensor`), Hue bridge events (`hue:event`), Nest/Ecobee thermostat (`ecobee:thermostat`), door/leak/motion sensors. Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Philips Hue, smart plugs (Tasmota/Shelly/TP-Link Kasa), thermostats (Nest/Ecobee), locks, and Zigbee/Z-Wave sensors via MQTT / Zigbee2MQTT and HEC. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=zigbee2mqtt:device\n| stats max(_time) as last_seen by device\n| eval minutes_since=round((now()-last_seen)/60,1)\n| where minutes_since>120\n| sort - minutes_since",
  "description": "Alerts on Gap Detection anomalies for UC-25.4.110 with window `the scheduled window` on `index=personal` sourcetype `zigbee2mqtt:device`. Combines `last_seen` by `device` and compares against `minutes_since` > 120. Mapped to keywords Sensor, Check, Gap, Detection for dashboard routing.",
  "value": "Alerting on Sensor, Check, Gap, Detection through UC-25.4.110 reduces missed training goals, stale hobby data, and connector failures by surfacing `last_seen` early.",
  "implementation": "Forward regular Zigbee2MQTT device reports; alert when a device stays silent beyond its normal reporting interval.",
  "visualization": "Table of devices by minutes since last seen.",
  "cimModels": [],
  "references": [
    {
      "title": "Zigbee2MQTT - Documentation",
      "url": "https://www.zigbee2mqtt.io/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.4.110)** — Connector for 'Sensor Check-In Gap Detection' stops polling `zigbee2mqtt:device`; stale `last_seen` on `device` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `last_seen` over `1w` is expected for UC-25.4.110, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `zigbee2mqtt:device`. Dedup with `stats latest(*) by device`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `minutes_since` > 120 for 'Sensor Check-In Gap Detection'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "f5",
    "mqtt"
  ],
  "equipmentModels": [
    "f5_asm"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Smart Home Devices & Sensors — typical feeds: Zigbee2MQTT device reports (`zigbee2mqtt:device`), Shelly/Tasmota telemetry (`shelly:status`, `tasmota:sensor`), Hue bridge events (`hue:event`), Nest/Ecobee thermostat (`ecobee:thermostat`), door/lea.\n• **Sourcetypes in this search**: `zigbee2mqtt:device`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=zigbee2mqtt:device`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Philips Hue, smart plugs (Tasmota/Shelly/TP-Link Kasa), thermostats (Nest/Ecobee), locks, and Zigbee/Z-Wave sensors via MQTT / Zigbee2MQTT and HEC..\n\n**Author notes:** Forward regular Zigbee2MQTT device reports; alert when a device stays silent beyond its normal reporting interval.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"zigbee2mqtt:device\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=zigbee2mqtt:device earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Sensor Check-In Gap Detection** over `personal` data (primarily `sourcetype=zigbee2mqtt:device`) across the full search range. It applies numeric thresholds on `minutes_since` (> 120) after max — tune these constants to your household baseline.\n\n**Fields this search depends on:** `device`, `minutes_since`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=zigbee2mqtt:device\n| stats max(_time) as last_seen by device\n| eval minutes_since=round((now()-last_seen)/60,1)\n| where minutes_since>120\n| sort - minutes_since\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=zigbee2mqtt:device earliest=-7d\n| head 50\n| table device, minutes_since\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=zigbee2mqtt:device earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `minutes_since` before alerting:\n\n```spl\nindex=personal sourcetype=zigbee2mqtt:device earliest=-30d\n| stats perc50(minutes_since) AS p50 perc90(minutes_since) AS p90 perc99(minutes_since) AS p99 max(minutes_since) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.4.110: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Devices By Minutes Since Last Seen (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `zigbee2mqtt:device` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=zigbee2mqtt:device | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It watches which smart sensors have gone quiet long enough that they may be dead, asleep, or out of range.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
