{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.31.29",
  "title": "Session Note Export Backlog",
  "criticality": "low",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Session Note Export Backlog' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_session_note_export_backlog` (UC-25.31.29), SOC dashboard panel \"Personal — Session Note Export Backlog\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.31.29 positive (Session, Note, Export, Backlog): ingest synthetic events into `index=personal` sourcetype='therapy:homework' where `count` grouped by `provider_name` satisfies detection threshold for 'Session Note Export Backlog'. Validate filter `event_type=\"session_note_export\"`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.31.29 negative (Export Backlog): ingest benign `personal` / `therapy:homework` events where `provider_name` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 8 confirms zero alert rows for 'Session Note Export Backlog'."
  },
  "dataSources": "Mood entries (`daylio:entry`), CBT thought records (`cbt:record`), breathwork sessions (`breathwork:session`), and therapy homework items (`therapy:homework`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Wellbeing journaling telemetry from Daylio exports, CBT thought records, breathwork apps, and therapy homework trackers sent to Splunk HEC via APIs and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=therapy:homework\n| search event_type=\"session_note_export\"\n| stats latest(_time) as last_seen count as events by provider_name\n| eval days_since=round((now()-last_seen)/86400,1)\n| sort - days_since",
  "description": "Detects UC-25.31.29 — Session, Note, Export, Backlog — using `index=personal` sourcetype `therapy:homework`, grouped by `provider_name`. Outputs `count` each `the scheduled window`; alert when configured threshold. Filter chain includes filter `event_type=\"session_note_export\"`. Purpose-built for 'Session Note Export Backlog' in Personal workflows.",
  "value": "UC-25.31.29 accelerates insight into Export Backlog ('Session Note Export Backlog') by tracking `activity` on `therapy:homework` feeds, reducing missed training goals, stale hobby data, and connector failures before spreadsheet checks hide regressions.",
  "implementation": "Send session note exports events to `index=personal` and alert when the gap since the last event grows beyond the expected cadence.",
  "visualization": "Table of providerss with time since last event.",
  "cimModels": [],
  "references": [
    {
      "title": "Daylio",
      "url": "https://daylio.net/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Psychology Tools thought record",
      "url": "https://www.psychologytools.com/resource/thought-record/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Breathwrk",
      "url": "https://www.breathwrk.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "SimplePractice",
      "url": "https://www.simplepractice.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.31.29)** — Connector for 'Session Note Export Backlog' stops polling `therapy:homework`; stale `count` on `provider_name` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `count` over `1w` is expected for UC-25.31.29, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `therapy:homework`. Dedup with `stats latest(*) by provider_name`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Session Note Export Backlog'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Mind, Mood & Journaling — typical feeds: Mood entries (`mood:entry`), meditation sessions (`meditation:session`), journal entries (`journal:entry`), gratitude logs (`gratitude:log`)..\n• **Sourcetypes in this search**: `therapy:homework`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=therapy:homework`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Mood, mindfulness and journaling apps — Daylio / How We Feel mood exports, Headspace / Insight Timer meditation logs, journaling and gratitude entries — sent to Splunk HEC via scripted inputs..\n\n**Author notes:** Send session note exports events to `index=personal` and alert when the gap since the last event grows beyond the expected cadence.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"therapy:homework\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=therapy:homework earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Session Note Export Backlog** over `personal` data (primarily `sourcetype=therapy:homework`) across the full search range. The `latest` aggregation groups by `provider_name`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `provider_name`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=therapy:homework\n| search event_type=\"session_note_export\"\n| stats latest(_time) as last_seen count as events by provider_name\n| eval days_since=round((now()-last_seen)/86400,1)\n| sort - days_since\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=therapy:homework earliest=-7d\n| head 50\n| table provider_name\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=therapy:homework earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.31.29: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Providerss With Time Since Last Event (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `therapy:homework` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=therapy:homework | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It celebrates which providerss have gone quiet longer than they should for session note exports.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
