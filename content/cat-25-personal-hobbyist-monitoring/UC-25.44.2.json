{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.44.2",
  "title": "Bite-Time vs Conditions Correlation",
  "criticality": "low",
  "difficulty": "advanced",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "dataSources": "Fishing catches (`fishing:catch`), fishfinder sonar (`fishfinder:reading`), foraging finds (`foraging:find`), game-cam triggers (`gamecam:trigger`).",
  "app": "Outdoor logs — fishing apps (Fishbrain), sonar / fishfinder exports, game/trail cameras, and foraging journals — streamed to Splunk HEC via APIs and scripted inputs.",
  "spl": "index=personal (sourcetype=fishing:catch OR sourcetype=ecowitt:obs)\n| bin _time span=1h\n| stats sum(eval(if(sourcetype==\"fishing:catch\",1,0))) as catches, avg(pressure_hpa) as pressure by _time\n| eval falling=if(pressure<1010,\"low/falling\",\"high\")\n| stats avg(catches) as avg_catches by falling",
  "description": "Correlates your catch rate with barometric pressure and time of day to learn when the fish really bite.",
  "value": "The old wisdom that fish bite on a falling barometer becomes testable against your own logged results.",
  "implementation": "Join catches with weather in `index=personal`; compare catch rate by pressure trend.",
  "visualization": "Bar chart of catch rate by barometric condition.",
  "cimModels": [],
  "references": [
    {
      "title": "Fishbrain — fishing app",
      "url": "https://fishbrain.com/"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Fishing, Hunting & Foraging — typical feeds: Fishing catches (`fishing:catch`), fishfinder sonar (`fishfinder:reading`), foraging finds (`foraging:find`), game-cam triggers (`gamecam:trigger`)..\n• **Sourcetypes in this search**: `fishing:catch`, `ecowitt:obs`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=fishing:catch`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Outdoor logs — fishing apps (Fishbrain), sonar / fishfinder exports, game/trail cameras, and foraging journals — streamed to Splunk HEC via APIs and scripted inputs..\n\n**Author notes:** Join catches with weather in `index=personal`; compare catch rate by pressure trend.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"fishing:catch\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=fishing:catch earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Bite-Time vs Conditions Correlation** over `personal` data (primarily `sourcetype=fishing:catch`) across the full search range. It applies numeric thresholds on `pressure` (< 1010) after sum, avg — tune these constants to your household baseline.\n\n**Fields this search depends on:** `_time`, `falling`, `pressure`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal (sourcetype=fishing:catch OR sourcetype=ecowitt:obs)\n| bin _time span=1h\n| stats sum(eval(if(sourcetype==\"fishing:catch\",1,0))) as catches, avg(pressure_hpa) as pressure by _time\n| eval falling=if(pressure<1010,\"low/falling\",\"high\")\n| stats avg(catches) as avg_catches by falling\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=fishing:catch earliest=-7d\n| head 50\n| table _time, falling, pressure\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=fishing:catch earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `pressure` before alerting:\n\n```spl\nindex=personal sourcetype=fishing:catch earliest=-30d\n| stats perc50(pressure) AS p50 perc90(pressure) AS p90 perc99(pressure) AS p99 max(pressure) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.44.2: Personal evidence` with the following panel layout:\n\n**Panel 1 — Bar Chart Of Catch Rate By Barometric Condition (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `fishing:catch` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=fishing:catch | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=1h` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1h` buckets.",
  "grandmaExplanation": "It works out when the fish actually bite for you, based on weather and time of day.",
  "prerequisiteUseCases": [],
  "wave": "run"
}
