{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.98.4",
  "title": "GOAT Listing Stale Inventory",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Operations",
    "Cost"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'GOAT Listing Stale Inventory' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_goat_listing_stale_inventory` (UC-25.98.4), SOC dashboard panel \"Personal — GOAT Listing Stale Inventory\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.98.4 positive (GOAT, Listing, Stale, Inventory): ingest synthetic events into `index=personal` sourcetype='goat:listing' where `avg_price` grouped by `sku` satisfies `days_listed` > 45 for 'GOAT Listing Stale Inventory'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.98.4 negative (Stale Inventory): ingest benign `personal` / `goat:listing` events where `sku` stays below `days_listed` > 45, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 15 confirms zero alert rows for 'GOAT Listing Stale Inventory'."
  },
  "dataSources": "StockX market prices (`stockx:price`), GOAT listings (`goat:listing`), Alias sales (`alias:sale`), sneaker rotation logs (`sneaker:rotation`), and deadstock age checks (`deadstock:age`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "StockX price alerts, GOAT listing exports, Alias seller settlement logs, sneaker rotation wear trackers, and deadstock age audits ingested into Splunk HEC. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=goat:listing listing_status=\"active\" days_listed=*\n| where days_listed>45\n| stats count as stale_listings, avg(list_price_usd) as avg_price by sku\n| sort - stale_listings\n| head 15",
  "description": "Detects UC-25.98.4 — GOAT, Listing, Stale, Inventory — using `index=personal` sourcetype `goat:listing`, grouped by `sku`. Outputs `avg_price` each `the scheduled window`; alert when `days_listed` > 45. Primary focus: Stale Inventory. Purpose-built for 'GOAT Listing Stale Inventory' in Personal workflows.",
  "value": "UC-25.98.4 operationalises 'GOAT Listing Stale Inventory' against `goat:listing` feeds so operators reduce missed training goals, stale hobby data, and connector failures without manual correlation.",
  "implementation": "Export GOAT active listings with `days_listed` and review stale SKUs biweekly for repricing.",
  "visualization": "Table of stale listings by SKU with average list price.",
  "cimModels": [],
  "references": [
    {
      "title": "StockX Developer Portal",
      "url": "https://stockx.com/about/api",
      "retrieved": "2026-04-25"
    },
    {
      "title": "GOAT",
      "url": "https://www.goat.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.98.4)** — Connector for 'GOAT Listing Stale Inventory' stops polling `goat:listing`; stale `avg_price` on `sku` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `avg_price` over `1w` is expected for UC-25.98.4, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `goat:listing`. Dedup with `stats latest(*) by sku`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `days_listed` > 45 for 'GOAT Listing Stale Inventory'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Sneakers & Resale Collecting — typical feeds: StockX market prices (`stockx:price`), GOAT listings (`goat:listing`), Alias sales (`alias:sale`), sneaker rotation logs (`sneaker:rotation`), and deadstock age checks (`deadstock:age`)..\n• **Sourcetypes in this search**: `goat:listing`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=goat:listing`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** StockX price alerts, GOAT listing exports, Alias seller settlement logs, sneaker rotation wear trackers, and deadstock age audits ingested into Splunk HEC..\n\n**Author notes:** Export GOAT active listings with `days_listed` and review stale SKUs biweekly for repricing.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"goat:listing\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=goat:listing earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **GOAT Listing Stale Inventory** over `personal` data (primarily `sourcetype=goat:listing`) across the full search range. It applies numeric thresholds on `days_listed` (> 45) after avg — tune these constants to your household baseline.\n\n**Fields this search depends on:** `days_listed`, `sku`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=goat:listing listing_status=\"active\" days_listed=*\n| where days_listed>45\n| stats count as stale_listings, avg(list_price_usd) as avg_price by sku\n| sort - stale_listings\n| head 15\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=goat:listing earliest=-7d\n| head 50\n| table days_listed, sku\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=goat:listing earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `days_listed` before alerting:\n\n```spl\nindex=personal sourcetype=goat:listing earliest=-30d\n| stats perc50(days_listed) AS p50 perc90(days_listed) AS p90 perc99(days_listed) AS p99 max(days_listed) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.98.4: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Stale Listings By Sku With Average List Price (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `goat:listing` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=goat:listing | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It spots sneakers listed on GOAT so long they probably need a price cut.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
