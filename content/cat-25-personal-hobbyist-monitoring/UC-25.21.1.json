{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.21.1",
  "title": "WSPR Propagation Reach and DX Records",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "dataSources": "APRS packets (`aprs:packet`), WSPR spots (`wspr:spot`), satellite passes (`satpass:prediction`), NOAA APT captures (`noaa:apt`), meteor-scatter pings (`meteorscatter:ping`), SSTV images (`sstv:image`).",
  "app": "Software-defined radio and ham-radio stations — APRS igate, WSPR beacon, NOAA weather-satellite (APT) receiver, meteor-scatter monitor, and SSTV decoder — logging to Splunk HEC via scripted inputs.",
  "spl": "index=personal sourcetype=wspr:spot\n| stats max(distance_km) as best_dx, dc(reporter_grid) as stations, count as spots by band\n| sort - best_dx",
  "description": "Summarises how far your low-power WSPR beacon reached on each band, capturing your best long-distance contacts.",
  "value": "WSPR turns a tiny signal into a global propagation experiment; a per-band reach record shows which band is open and how far it carries.",
  "implementation": "Ingest your WSPR spots into `index=personal`; schedule daily and review best distance and reporting stations per band.",
  "visualization": "Bar chart of best distance reached by band.",
  "cimModels": [],
  "references": [
    {
      "title": "WSPRnet — weak-signal propagation reporter",
      "url": "https://www.wsprnet.org/"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "grandmaExplanation": "It shows how far your tiny radio beacon was heard on each band, which is a fun way to study how radio waves travel."
}
