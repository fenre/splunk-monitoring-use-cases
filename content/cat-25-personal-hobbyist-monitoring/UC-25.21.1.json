{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.21.1",
  "title": "WSPR Propagation Reach and DX Records",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'WSPR Propagation Reach and DX Records' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_wspr_propagation_reach_and_dx_records` (UC-25.21.1), SOC dashboard panel \"Personal — WSPR Propagation Reach and DX Records\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=personal` sourcetype='wspr:spot' where `band` satisfies detection threshold for UC-25.21.1 ('WSPR Propagation Reach and DX Records'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `personal` / `wspr:spot` events where `band` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv` with a valid window. Confirm zero alert rows for UC-25.21.1."
  },
  "dataSources": "APRS packets (`aprs:packet`), WSPR spots (`wspr:spot`), satellite passes (`satpass:prediction`), NOAA APT captures (`noaa:apt`), meteor-scatter pings (`meteorscatter:ping`), SSTV images (`sstv:image`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Software-defined radio and ham-radio stations — APRS igate, WSPR beacon, NOAA weather-satellite (APT) receiver, meteor-scatter monitor, and SSTV decoder — logging to Splunk HEC via scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=wspr:spot\n| stats max(distance_km) as best_dx, dc(reporter_grid) as stations, count as spots by band\n| sort - best_dx",
  "description": "Summarises how far your low-power WSPR beacon reached on each band, capturing your best long-distance contacts. Searches `index=personal` with sourcetype `wspr:spot` for UC-25.21.1, aggregating `max` by `band` over `the scheduled window`. Alerts when grouped results cross configured threshold in the saved search. Gives Personal operators a repeatable signal for 'WSPR Propagation Reach and DX Records' without manual log review.",
  "value": "Summarises how far your low-power WSPR beacon reached on each band, capturing your best long-distance contacts. Timely UC-25.21.1 alerting on 'WSPR Propagation Reach and DX Records' reduces missed training goals, stale hobby data, and connector failures before manual triage or audit channels escalate.",
  "implementation": "Ingest your WSPR spots into `index=personal`; schedule daily and review best distance and reporting stations per band.",
  "visualization": "Bar chart of best distance reached by band.",
  "cimModels": [],
  "references": [
    {
      "title": "WSPRnet — weak-signal propagation reporter",
      "url": "https://www.wsprnet.org/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry** — Connector for 'WSPR Propagation Reach and DX Records' stops polling `wspr:spot`; stale `band` rows remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `max` over `1w` is expected, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `wspr:spot`. Dedup with `stats latest(*) by band`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Radio, SDR & Space — typical feeds: APRS packets (`aprs:packet`), WSPR spots (`wspr:spot`), satellite passes (`satpass:prediction`), NOAA APT captures (`noaa:apt`), meteor-scatter pings (`meteorscatter:ping`), SSTV images (`sstv:image`).\n• **Sourcetypes in this search**: `wspr:spot`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=wspr:spot`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Software-defined radio and ham stations — APRS igate, WSPR beacon, NOAA weather-satellite (APT) receiver, meteor-scatter monitor, and SSTV decoder — logging to Splunk HEC via scripted inputs..\n\n**Author notes:** Ingest your WSPR spots into `index=personal`; schedule daily and review best distance and reporting stations per band.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"wspr:spot\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=wspr:spot earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **WSPR Propagation Reach and DX Records** over `personal` data (primarily `sourcetype=wspr:spot`) across the full search range. The `max, dc` aggregation groups by `band`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `band`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=wspr:spot\n| stats max(distance_km) as best_dx, dc(reporter_grid) as stations, count as spots by band\n| sort - best_dx\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=wspr:spot earliest=-7d\n| head 50\n| table band\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=wspr:spot earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.21.1: Personal evidence` with the following panel layout:\n\n**Panel 1 — Bar Chart Of Best Distance Reached By Band (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `wspr:spot` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=wspr:spot | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It shows how far your tiny radio beacon was heard on each band, which is a fun way to study how radio waves travel.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
