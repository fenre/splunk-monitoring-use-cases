{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.11.5",
  "title": "Bird Feeder Visitor Species Log",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics",
    "Inventory"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Bird Feeder Visitor Species Log' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_bird_feeder_visitor_species_log` (UC-25.11.5), SOC dashboard panel \"Personal — Bird Feeder Visitor Species Log\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=personal` sourcetype='birdfeeder:detection' where `_time` satisfies `confidence` > 0.7 for UC-25.11.5 ('Bird Feeder Visitor Species Log'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `personal` / `birdfeeder:detection` events where `_time` stays below `confidence` > 0.7, or exclude the test entity via `personal_monitoring_notes.csv` with a valid window. Confirm zero alert rows for UC-25.11.5."
  },
  "dataSources": "Pet tracker location/activity (`tractive:position`, `whistle:activity`), smart feeder events (`petfeeder:event`), litter-box usage (`litterrobot:cycle`), bird-feeder detections. Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Pet GPS/activity trackers (Tractive/Fi/Whistle), smart feeders and litter boxes, bird feeders/cams, and plant sensors via vendor APIs and MQTT to HEC. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=birdfeeder:detection confidence>0.7\n| bin _time span=1d\n| stats count as visits, dc(species) as species_seen by _time\n| stats sum(visits) as total_visits, max(species_seen) as daily_species by species\n| sort - total_visits",
  "description": "Turns your smart bird feeder's camera detections into a running log of which species visited and how often, a delightful backyard biodiversity record.",
  "value": "Turns your smart bird feeder's camera detections into a running log of which species visited and how often,.... Timely UC-25.11.5 alerting on 'Bird Feeder Visitor Species Log' reduces missed training goals, stale hobby data, and connector failures before manual triage or audit channels escalate.",
  "implementation": "Forward bird-camera detections with species and confidence; schedule daily and build a species leaderboard.",
  "visualization": "Bar chart of visits by species over time.",
  "cimModels": [],
  "references": [
    {
      "title": "Bird feeder AI cameras",
      "url": "https://www.home-assistant.io/integrations/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry** — Connector for 'Bird Feeder Visitor Species Log' stops polling `birdfeeder:detection`; stale `_time` rows remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `dc` over `1d` is expected, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `birdfeeder:detection`. Dedup with `stats latest(*) by _time`.\n\n4. **Timezone bucket misalignment** — `1d` bins on UTC vs local schedule skew `confidence` > 0.7. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Pets & Home Life — typical feeds: Pet tracker location/activity (`tractive:position`, `whistle:activity`), smart feeder events (`petfeeder:event`), litter-box usage (`litterrobot:cycle`), bird-feeder cam detections, plant/soil sensors.\n• **Sourcetypes in this search**: `birdfeeder:detection`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=birdfeeder:detection`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Pet GPS/activity trackers (Tractive/Fi/Whistle), smart feeders and litter boxes, bird feeders/cams, and plant sensors via vendor APIs and MQTT to HEC..\n\n**Author notes:** Forward bird-camera detections with species and confidence; schedule daily and build a species leaderboard.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"birdfeeder:detection\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=birdfeeder:detection earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Bird Feeder Visitor Species Log** over `personal` data (primarily `sourcetype=birdfeeder:detection`) across the full search range. It applies numeric thresholds on `confidence` (> 0.7) after dc, sum, max — tune these constants to your household baseline.\n\n**Fields this search depends on:** `_time`, `confidence`, `species`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=birdfeeder:detection confidence>0.7\n| bin _time span=1d\n| stats count as visits, dc(species) as species_seen by _time\n| stats sum(visits) as total_visits, max(species_seen) as daily_species by species\n| sort - total_visits\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=birdfeeder:detection earliest=-7d\n| head 50\n| table _time, confidence, species\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=birdfeeder:detection earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `confidence` before alerting:\n\n```spl\nindex=personal sourcetype=birdfeeder:detection earliest=-30d\n| stats perc50(confidence) AS p50 perc90(confidence) AS p90 perc99(confidence) AS p99 max(confidence) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.11.5: Personal evidence` with the following panel layout:\n\n**Panel 1 — Bar Chart Of Visits By Species Over Time (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `birdfeeder:detection` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=birdfeeder:detection | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=1d` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1d` buckets.",
  "grandmaExplanation": "It keeps a fun log of which birds visited your feeder and how often, like a backyard nature diary.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
