{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.9.31",
  "title": "Cloudflare Tunnel Hostname Demand Inventory",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Availability",
    "Security"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Cloudflare Tunnel Hostname Demand Inventory' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_cloudflare_tunnel_hostname_demand_inventory` (UC-25.9.31), SOC dashboard panel \"Personal — Cloudflare Tunnel Hostname Demand Inventory\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=personal` sourcetype='cloudflared:connector' that satisfy the UC-25.9.31 SPL filters for 'Cloudflare Tunnel Hostname Demand Inventory'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `personal` / `cloudflared:connector` events outside the detection threshold, or exclude the test entity via `personal_monitoring_notes.csv` with a valid window. Confirm zero alert rows for UC-25.9.31."
  },
  "dataSources": "Personal network telemetry (`openwrt:syslog`, `unifi:event`, `pihole:query`, `adguard:query`, `tailscale:netmap`, `cloudflared:connector`, `doh:query`, `speedtest:result`, `uptime:probe`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "OpenWrt, UniFi, Pi-hole, AdGuard Home, Tailscale, Cloudflare Tunnel, DNS over HTTPS resolvers, and scheduled probes via syslog, APIs, and Splunk HEC. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=cloudflared:connector\n| stats count as requests, values(connector) as connectors by hostname\n| sort - requests",
  "description": "Uses Cloudflare Tunnel request data telemetry to monitor which hostnames are receiving the most tunneled demand in index=personal.",
  "value": "Splunk turns the feed into an operational check for public names that deserve priority when debugging or scaling, so the signal does not stay buried inside separate consumer apps or exports.",
  "implementation": "Ingest Cloudflare Tunnel request data data through its real API, export, MQTT bridge, or open-source connector into `index=personal`; preserve the fields referenced in the SPL and schedule the search on a hourly cadence.",
  "visualization": "Hostname inventory sorted by request volume.",
  "cimModels": [],
  "references": [
    {
      "title": "OpenWrt docs",
      "url": "https://openwrt.org/docs/start",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Tailscale docs",
      "url": "https://tailscale.com/kb",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Cloudflare Tunnel docs",
      "url": "https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Pi-hole docs",
      "url": "https://docs.pi-hole.net/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "AdGuard Home overview",
      "url": "https://adguard.com/kb/adguard-home/overview/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Ookla Speedtest CLI",
      "url": "https://www.speedtest.net/apps/cli",
      "retrieved": "2026-04-25"
    },
    {
      "title": "UniFi Network docs",
      "url": "https://help.ui.com/hc/en-us/categories/6583256751383",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry** — Vendor connector stops polling for 'Cloudflare Tunnel Hostname Demand Inventory'; stale rows remain in `index=personal`. Check `_internal` HEC 401s and renew token in passwords.conf.\n\n2. **Rest day or planned break** — Zero activities is expected, not ingestion failure. Vendor app shows no sessions for the calendar day.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `cloudflared:connector`. Dedup with `stats latest(*) by external_id`.\n\n4. **Timezone bucket misalignment** — Weekly bins on UTC vs local schedule skew thresholds. Document offset in SPL `bin _time` or connector config.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "cloudflare",
    "mqtt",
    "syslog"
  ],
  "equipmentModels": [
    "cloudflare_generic",
    "mqtt_generic",
    "syslog_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Home Network & Connectivity — typical feeds: UniFi controller events (`unifi:event`), Pi-hole query log (`pihole:query`), AdGuard Home (`adguard:query`), speedtest-cli results (`speedtest:result`), router/firewall syslog, ISP uptime probes..\n• **Sourcetypes in this search**: `cloudflared:connector`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=cloudflared:connector`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** UniFi / router syslog, Pi-hole/AdGuard, and scheduled speedtests via HEC scripted inputs and the Universal Forwarder..\n\n**Author notes:** Ingest Cloudflare Tunnel request data data through its real API, export, MQTT bridge, or open-source connector into `index=personal`; preserve the fields referenced in the SPL and schedule the search on a hourly cadence.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"cloudflared:connector\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=cloudflared:connector earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Cloudflare Tunnel Hostname Demand Inventory** over `personal` data (primarily `sourcetype=cloudflared:connector`) across the full search range. The `values` aggregation groups by `hostname`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `hostname`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=cloudflared:connector\n| stats count as requests, values(connector) as connectors by hostname\n| sort - requests\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=cloudflared:connector earliest=-7d\n| head 50\n| table hostname\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=cloudflared:connector earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.9.31: Personal evidence` with the following panel layout:\n\n**Panel 1 — Hostname Inventory Sorted By Request Volume (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `cloudflared:connector` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=cloudflared:connector | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It gathers which hostnames are receiving the most tunneled demand so you can spot public names that deserve priority when debugging or scaling before it becomes annoying.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
