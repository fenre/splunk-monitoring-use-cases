{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.55.29",
  "title": "Obituary Retrieval Backlog",
  "criticality": "low",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Obituary Retrieval Backlog' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_obituary_retrieval_backlog` (UC-25.55.29), SOC dashboard panel \"Personal — Obituary Retrieval Backlog\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.55.29 positive (Obituary, Retrieval, Backlog): ingest synthetic events into `index=personal` sourcetype='genealogy:ops' where `sum` grouped by `newspaper_title` satisfies `backlog` > 0 for 'Obituary Retrieval Backlog'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.55.29 negative (Retrieval Backlog): ingest benign `personal` / `genealogy:ops` events where `newspaper_title` stays below `backlog` > 0, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 8 confirms zero alert rows for 'Obituary Retrieval Backlog'."
  },
  "dataSources": "Hints (`genealogy:hint`), cemetery visits (`genealogy:cemetery`), family-photo work (`genealogy:photo`), research ops (`genealogy:ops`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Genealogy and family-history telemetry — Ancestry hints, cemetery visit logs, and photo-restoration queues — streamed to Splunk HEC via exports and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=genealogy:ops newspaper_title=* retrieval_state=*\n| stats sum(eval(if(retrieval_state!=\"done\",1,0))) as backlog, count as total by newspaper_title\n| where backlog>0\n| sort - backlog",
  "description": "Monitors UC-25.55.29 — Obituary, Retrieval, Backlog — using `index=personal` sourcetype `genealogy:ops`, grouped by `newspaper_title`. Outputs `sum` each `the scheduled window`; alert when `backlog` > 0. Primary focus: Retrieval Backlog. Purpose-built for 'Obituary Retrieval Backlog' in Personal workflows.",
  "value": "UC-25.55.29 accelerates insight into Retrieval Backlog ('Obituary Retrieval Backlog') by tracking `activity` on `genealogy:ops` feeds, reducing missed training goals, stale hobby data, and connector failures before spreadsheet checks hide regressions.",
  "implementation": "Mark each `genealogy:ops` record with a pending-state flag and summarize backlog totals by `newspaper_title`.",
  "visualization": "Table of newspaper with pending obituary requests counts.",
  "cimModels": [],
  "references": [
    {
      "title": "Ancestry",
      "url": "https://www.ancestry.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Find a Grave",
      "url": "https://www.findagrave.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "MyHeritage Photo Enhancer",
      "url": "https://www.myheritage.com/photo-enhancer",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.55.29)** — Connector for 'Obituary Retrieval Backlog' stops polling `genealogy:ops`; stale `sum` on `newspaper_title` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `sum` over `1w` is expected for UC-25.55.29, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `genealogy:ops`. Dedup with `stats latest(*) by newspaper_title`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `backlog` > 0 for 'Obituary Retrieval Backlog'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "asterisk"
  ],
  "equipmentModels": [
    "asterisk_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Genealogy & Family History — typical feeds: Tree records (`genealogy:record`), DNA matches (`dna:match`), ancestor events (`ancestor:event`), archive documents (`archive:document`), tree nodes (`familytree:node`)..\n• **Sourcetypes in this search**: `genealogy:ops`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=genealogy:ops`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Genealogy and family-history research — tree platforms (Ancestry, FamilySearch), DNA match exports (23andMe, MyHeritage), and document / archive catalogues — streamed to Splunk HEC via GEDCOM and CSV exports..\n\n**Author notes:** Mark each `genealogy:ops` record with a pending-state flag and summarize backlog totals by `newspaper_title`.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"genealogy:ops\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=genealogy:ops earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Obituary Retrieval Backlog** over `personal` data (primarily `sourcetype=genealogy:ops`) across the full search range. It applies numeric thresholds on `backlog` (> 0) after sum — tune these constants to your household baseline.\n\n**Fields this search depends on:** `backlog`, `newspaper_title`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=genealogy:ops newspaper_title=* retrieval_state=*\n| stats sum(eval(if(retrieval_state!=\"done\",1,0))) as backlog, count as total by newspaper_title\n| where backlog>0\n| sort - backlog\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=genealogy:ops earliest=-7d\n| head 50\n| table backlog, newspaper_title\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=genealogy:ops earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `backlog` before alerting:\n\n```spl\nindex=personal sourcetype=genealogy:ops earliest=-30d\n| stats perc50(backlog) AS p50 perc90(backlog) AS p90 perc99(backlog) AS p99 max(backlog) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.55.29: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Newspaper With Pending Obituary Requests Counts (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `genealogy:ops` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=genealogy:ops | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It blends where unfinished obituary requests are piling up so you can clear the biggest queues in document retricheck.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
