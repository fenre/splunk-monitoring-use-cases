{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.39.27",
  "title": "Birthday Budget Overrun by Recipient Group",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Cost",
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Birthday Budget Overrun by Recipient Group' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_birthday_budget_overrun_by_recipient_group` (UC-25.39.27), SOC dashboard panel \"Personal — Birthday Budget Overrun by Recipient Group\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.39.27 positive (Birthday, Budget, Overrun, Recipient, Group): ingest synthetic events into `index=personal` sourcetype='birthday:spend' where `overrun_amount` grouped by `recipient_group` satisfies `overrun_amount` > 0 for 'Birthday Budget Overrun by Recipient Group'. Validate filter `overrun_amount>0`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.39.27 negative (Recipient Group): ingest benign `personal` / `birthday:spend` events where `recipient_group` stays below `overrun_amount` > 0, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 12 confirms zero alert rows for 'Birthday Budget Overrun by Recipient Group'."
  },
  "dataSources": "Decoration timers (`holidaytimer:event`), birthday spending (`birthday:spend`), and fireworks noise events (`fireworks:noise`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Seasonal household telemetry from decoration timers, birthday spending trackers, and fireworks noise sensors sent to Splunk HEC via Home Assistant, budget exports, and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=birthday:spend\n| search overrun_amount>0\n| stats sum(overrun_amount) as overrun_amount count as recipients by recipient_group\n| sort 0 - overrun_amount",
  "description": "Detects UC-25.39.27 — Birthday, Budget, Overrun, Recipient, Group — using `index=personal` sourcetype `birthday:spend`, grouped by `recipient_group`. Outputs `overrun_amount` each `the scheduled window`; alert when `overrun_amount` > 0. Filter chain includes filter `overrun_amount>0`. Purpose-built for 'Birthday Budget Overrun by Recipient Group' in Personal workflows.",
  "value": "Tracking `overrun_amount` via UC-25.39.27 on `birthday:spend` feeds improves response to 'Birthday Budget Overrun by Recipient Group' and reduces missed training goals, stale hobby data, and connector failures.",
  "implementation": "Send birthday budget overrun events to `index=personal` and keep `recipient_group` populated so the leaderboard stays actionable.",
  "visualization": "Bar chart of birthday budget overrun by recipient groups.",
  "cimModels": [],
  "references": [
    {
      "title": "Home Assistant",
      "url": "https://www.home-assistant.io/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "YNAB",
      "url": "https://www.ynab.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "ESPHome",
      "url": "https://esphome.io/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.39.27)** — Connector for 'Birthday Budget Overrun by Recipient Group' stops polling `birthday:spend`; stale `overrun_amount` on `recipient_group` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `overrun_amount` over `1w` is expected for UC-25.39.27, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `birthday:spend`. Dedup with `stats latest(*) by recipient_group`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `overrun_amount` > 0 for 'Birthday Budget Overrun by Recipient Group'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Seasonal & Silly — typical feeds: Festive lights (`festive:lights`), candy counter (`candy:counter`), countdown events (`countdown:event`), Santa tracker (`santa:tracker`)..\n• **Sourcetypes in this search**: `birthday:spend`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=birthday:spend`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Seasonal novelty feeds — smart Christmas-light power meters, a Halloween candy counter, advent/holiday countdowns, and festive trackers — streamed to Splunk HEC via MQTT and smart plugs..\n\n**Author notes:** Send birthday budget overrun events to `index=personal` and keep `recipient_group` populated so the leaderboard stays actionable.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"birthday:spend\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=birthday:spend earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Birthday Budget Overrun by Recipient Group** over `personal` data (primarily `sourcetype=birthday:spend`) across the full search range. It applies numeric thresholds on `overrun_amount` (> 0) after sum — tune these constants to your household baseline.\n\n**Fields this search depends on:** `overrun_amount`, `recipient_group`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=birthday:spend\n| search overrun_amount>0\n| stats sum(overrun_amount) as overrun_amount count as recipients by recipient_group\n| sort 0 - overrun_amount\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=birthday:spend earliest=-7d\n| head 50\n| table overrun_amount, recipient_group\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=birthday:spend earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `overrun_amount` before alerting:\n\n```spl\nindex=personal sourcetype=birthday:spend earliest=-30d\n| stats perc50(overrun_amount) AS p50 perc90(overrun_amount) AS p90 perc99(overrun_amount) AS p99 max(overrun_amount) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.39.27: Personal evidence` with the following panel layout:\n\n**Panel 1 — Bar Chart Of Birthday Budget Overrun By Recipient Groups (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `birthday:spend` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=birthday:spend | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It celebrates which recipient groupss stand out most for birthday budget overrun.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
