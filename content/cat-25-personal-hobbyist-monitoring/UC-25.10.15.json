{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.10.15",
  "title": "Leaf Wetness Fungus Risk Watch",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Safety",
    "Quality"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers personal hobby monitoring for 'Leaf Wetness Fungus Risk Watch' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_leaf_wetness_fungus_risk_watch` (UC-25.10.15), SOC dashboard panel \"Personal — Leaf Wetness Fungus Risk Watch\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.10.15 positive (Leaf, Wetness, Fungus, Risk, Watch): ingest synthetic events into `index=personal` sourcetype='plant:sensor' where `wetness`, `humidity` grouped by `zone` satisfies `wetness` > 80, or `humidity` > 85 for 'Leaf Wetness Fungus Risk Watch'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.10.15 negative (Risk Watch): ingest benign `personal` / `plant:sensor` events where `zone` stays below `wetness` > 80, or `humidity` > 85, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 3 confirms zero alert rows for 'Leaf Wetness Fungus Risk Watch'."
  },
  "dataSources": "Weather-station observations (`ecowitt:obs`, `weatherflow:obs`), air-quality readings (`purpleair:aqi`, `airgradient:sensor`), soil-moisture and greenhouse sensors (`plant:sensor`), aquarium controllers (`aquarium:sensor`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Personal weather stations (Ecowitt/Ambient/Davis/WeatherFlow), air-quality sensors (PurpleAir/AirGradient), and aquarium/greenhouse/soil sensors via HEC and MQTT. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=plant:sensor\n| stats avg(leaf_wetness_pct) as wetness, avg(humidity_pct) as humidity by zone\n| where wetness>80 AND humidity>85\n| sort - wetness",
  "description": "Surfaces when Risk Watch criteria fire for 'Leaf Wetness Fungus Risk Watch' (UC-25.10.15) in `index=personal` sourcetype `plant:sensor`. Pipeline aggregates `wetness`, `humidity` by `zone` over `the scheduled window`. Alert logic enforces `wetness` > 80, or `humidity` > 85. Title terms: Leaf, Wetness, Fungus, Risk, Watch. Eliminates manual hunting for UC-25.10.15 conditions.",
  "value": "UC-25.10.15 helps teams contain Leaf, Wetness, Fungus, Risk, Watch scenarios and reduce missed training goals, stale hobby data, and connector failures before incidents compound.",
  "implementation": "Forward leaf-wetness and humidity signals where available; review hot spots daily during high-risk seasons.",
  "visualization": "Zone table of leaf wetness and humidity with risk status.",
  "cimModels": [],
  "references": [
    {
      "title": "Home Assistant - Plant integration",
      "url": "https://www.home-assistant.io/integrations/plant/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.10.15)** — Connector for 'Leaf Wetness Fungus Risk Watch' stops polling `plant:sensor`; stale `wetness`, `humidity` on `zone` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `wetness`, `humidity` over `1w` is expected for UC-25.10.15, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `plant:sensor`. Dedup with `stats latest(*) by zone`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `wetness` > 80 for 'Leaf Wetness Fungus Risk Watch'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Weather, Environment & Garden — typical feeds: Weather-station observations (`ecowitt:obs`, `weatherflow:obs`), air-quality readings (`purpleair:aqi`, `airgradient:sensor`), soil-moisture/greenhouse sensors, aquarium controllers (`aquarium:sensor`.\n• **Sourcetypes in this search**: `plant:sensor`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=plant:sensor`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Personal weather stations (Ecowitt/Ambient/Davis/WeatherFlow), air-quality sensors (PurpleAir/AirGradient), and aquarium/greenhouse/soil sensors via HEC and MQTT..\n\n**Author notes:** Forward leaf-wetness and humidity signals where available; review hot spots daily during high-risk seasons.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"plant:sensor\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=plant:sensor earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Leaf Wetness Fungus Risk Watch** over `personal` data (primarily `sourcetype=plant:sensor`) across the full search range. It applies numeric thresholds on `wetness` (> 80) after avg — tune these constants to your household baseline.\n\n**Fields this search depends on:** `humidity`, `wetness`, `zone`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=plant:sensor\n| stats avg(leaf_wetness_pct) as wetness, avg(humidity_pct) as humidity by zone\n| where wetness>80 AND humidity>85\n| sort - wetness\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=plant:sensor earliest=-7d\n| head 50\n| table humidity, wetness, zone\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=plant:sensor earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `wetness` before alerting:\n\n```spl\nindex=personal sourcetype=plant:sensor earliest=-30d\n| stats perc50(wetness) AS p50 perc90(wetness) AS p90 perc99(wetness) AS p99 max(wetness) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.10.15: Personal evidence` with the following panel layout:\n\n**Panel 1 — Zone Table Of Leaf Wetness And Humidity With Risk Status (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `plant:sensor` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=plant:sensor | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It surfaces which parts of the garden are staying wet and humid enough that fungus problems could start growing.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
