{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.2.1",
  "title": "Resting Heart Rate Anomaly Detection",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Anomaly",
    "Safety"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Does not replace enterprise SIEM correlation for unrelated threat classes. Does not constitute legal, regulatory, or executive attestation for 'Resting Heart Rate Anomaly Detection'. Does not cover entities explicitly scoped out in operational_exceptions.csv. Pair with sibling UCs in the same subcategory when the SPL surface alone cannot prove control effectiveness (see prerequisiteUseCases on UC-25.2.1).",
  "evidence": "Saved search uc_25_2_1_resting_heart_rate_anomaly_detection, dashboard panel tied to this UC, weekly CSV export archived to index=evidence with change-ticket reference, and git-tracked lookup baselines referenced in the search.",
  "controlTest": {
    "positiveScenario": "On a lab host or staging index, ingest sample events that satisfy all SPL filters for 'Resting Heart Rate Anomaly Detection'. Run the saved search manually and confirm at least one result row with populated severity or metric fields. Verify the alert action fires within one schedule interval and the output matches the dashboard table projection for UC-25.2.1.",
    "negativeScenario": "Ingest a control batch where threshold fields remain within normal bounds (for example compliant configuration values, timestamps outside the monitoring window, or entities listed in operational_exceptions.csv). Confirm the saved search returns zero rows, no notable is created, and scheduled export volume to the evidence index stays flat."
  },
  "dataSources": "Apple Health metrics (`apple:health`), Oura Ring readiness/sleep (`oura:daily`), Whoop recovery/strain (`whoop:cycle`), Withings measurements (`withings:measure`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Apple Health / Apple Watch exports (Health Auto Export or Shortcuts) via HEC; Oura, Whoop, Withings, and Dexcom APIs via scripted REST inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=apple:health metric=resting_heart_rate\n| timechart span=1d avg(value) as rhr\n| eventstats avg(rhr) as base, stdev(rhr) as sd\n| eval elevated=if(rhr>base+2*sd,1,0)\n| where elevated=1",
  "description": "Baselines your daily resting heart rate and flags days when it jumps well above normal, an early sign of illness, poor recovery, or overtraining.",
  "value": "A sudden resting-heart-rate rise often precedes a cold or burnout by a day or two; catching it lets you rest before you get sick.",
  "implementation": "Export Apple Watch resting heart rate into `index=personal`; schedule daily and alert when today's value exceeds your personal baseline.",
  "visualization": "Line chart of daily resting heart rate with an anomaly band.",
  "cimModels": [],
  "references": [
    {
      "title": "Apple — HealthKit documentation",
      "url": "https://developer.apple.com/documentation/healthkit",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Scheduled maintenance windows** — Planned change activity in the CMDB or change calendar produces expected deviations that match this detection pattern during approved maintenance. Cross-check ServiceNow change records for `state=implement` and the affected CI list.\n\n2. **Backup and batch job cycles** — Nightly ETL, backup agents, or log rotation jobs generate bursts of similar events that resemble alert conditions but occur on a fixed schedule. Compare `_time` against the documented batch window before paging.\n\n3. **Onboarding and provisioning bursts** — New users, devices, or services provisioned in bulk can temporarily skew counts until baselines stabilize. Filter events tagged `onboarding=true` in the identity or asset lookup.\n\n4. **Monitoring and health-check traffic** — Synthetic transactions or internal scanner accounts generate probe traffic designed to exercise the control. Exclude service accounts listed in `monitoring_exceptions.csv`.\n\n**Suppression mechanism:** Maintain a KV Store lookup `operational_exceptions.csv` with columns `entity`, `exception_reason`, `valid_from`, `valid_until`, and `approved_by`. Join the saved search to this lookup and filter `where isnull(exception_reason) OR now() > valid_until` so approved windows suppress alerts without muting the search globally.",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Apple Health — Health Auto Export app or Shortcuts automation → HEC.\n• **Subcategory context**: Health, Sleep & Wearables — typical feeds: Apple Health metrics (`apple:health`), Oura Ring daily readiness/sleep (`oura:daily`), Whoop recovery/strain (`whoop:cycle`), Withings body/blood-pressure (`withings:measure`), Dexcom CGM glucose (`de.\n• **Sourcetypes in this search**: `apple:health`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Apple Health** events into `index=personal` with `sourcetype=apple:health`.\n\n**Collection method:** Health Auto Export app or Shortcuts automation → HEC.\n\n**Reference endpoint / path:** `iOS shortcut POST JSON batch nightly`.\n\n**Suggested poll interval:** 86400s.\n\n**Key fields to extract:** `metric, value, unit, sourceName, startDate`.\n\n**Typical connector stack:** Apple Health / Apple Watch exports (Health Auto Export, Shortcuts) via HEC; Oura, Whoop, Withings, Dexcom APIs via scripted REST inputs..\n\n**Author notes:** Export Apple Watch resting heart rate into `index=personal`; schedule daily and alert when today's value exceeds your personal baseline.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"apple:health\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=apple:health earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Resting Heart Rate Anomaly Detection** over `personal` data (primarily `sourcetype=apple:health`) across the full search range. `eventstats` / `streamstats` compute rolling baselines before filtering outliers; window size controls sensitivity (smaller window = noisier alerts).\n\n**Fields this search depends on:** `elevated`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=apple:health metric=resting_heart_rate\n| timechart span=1d avg(value) as rhr\n| eventstats avg(rhr) as base, stdev(rhr) as sd\n| eval elevated=if(rhr>base+2*sd,1,0)\n| where elevated=1\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Apple Health app → Browse → metric category. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=apple:health earliest=-7d\n| head 50\n| table elevated\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=apple:health earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.2.1: Personal evidence` with the following panel layout:\n\n**Panel 1 — Line Chart Of Daily Resting Heart Rate With An Anomaly Band (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Apple Health — no Apple Health events / missing fields:** If `apple:health` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Apple Health connector script is still running (`systemctl status` or cron log).\n\n**Apple Health — NULL values in extracted fields:** The Apple Health JSON export shape drifted — compare a raw event (`index=personal sourcetype=apple:health | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Apple Health — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Apple Health — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Apple Health APIs often cap requests per 15 minutes on free tiers.\n\n**Everything is an outlier:** Baseline window is too short or `stdev=0` — require minimum sample count (`where count>=5`) before computing z-scores.\n\n**Bucket misalignment:** `span=1d` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1d` buckets.",
  "grandmaExplanation": "It learns your normal resting heartbeat and warns you on days it is unusually high, which can mean you are getting run down.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
