{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.104.3",
  "title": "Derm Appointment Interval Breach",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Operations",
    "Compliance"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Derm Appointment Interval Breach' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_derm_appointment_interval_breach` (UC-25.104.3), SOC dashboard panel \"Personal — Derm Appointment Interval Breach\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.104.3 positive (Derm, Appointment, Interval, Breach): ingest synthetic events into `index=personal` sourcetype='derm:appointment' where `last_visit` grouped by `patient_id`, `concern_type` satisfies `days_since` > 365 for 'Derm Appointment Interval Breach'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.104.3 negative (Interval Breach): ingest benign `personal` / `derm:appointment` events where `patient_id`, `concern_type` stays below `days_since` > 365, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 3 confirms zero alert rows for 'Derm Appointment Interval Breach'."
  },
  "dataSources": "Curology prescriptions (`curology:prescription`), skin diaries (`skin:diary`), dermatology appointments (`derm:appointment`), and UV exposure logs (`uv:exposure`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Curology prescription shipments, daily skin diary entries, dermatology appointment exports, and UV exposure tracker logs ingested into Splunk HEC. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=derm:appointment patient_id=* appt_date=*\n| stats max(appt_date) as last_visit by patient_id concern_type\n| eval days_since=round((now()-strptime(last_visit,\"%Y-%m-%d\"))/86400,0)\n| where days_since>365\n| sort - days_since",
  "description": "Alerts on when Interval Breach criteria fire for 'Derm Appointment Interval Breach' (UC-25.104.3) in `index=personal` sourcetype `derm:appointment`. Pipeline aggregates `last_visit` by `patient_id`, `concern_type` over `the scheduled window`. Alert logic enforces `days_since` > 365. Title terms: Derm, Appointment, Interval, Breach. Eliminates manual hunting for UC-25.104.3 conditions.",
  "value": "UC-25.104.3 helps teams contain Derm, Appointment, Interval, Breach scenarios and reduce missed training goals, stale hobby data, and connector failures before incidents compound.",
  "implementation": "Emit one `derm:appointment` event per visit with ISO `appt_date` and review overdue concerns quarterly.",
  "visualization": "Table of concerns by days since last derm visit.",
  "cimModels": [],
  "references": [
    {
      "title": "Curology",
      "url": "https://curology.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.104.3)** — Connector for 'Derm Appointment Interval Breach' stops polling `derm:appointment`; stale `last_visit` on `patient_id`, `concern_type` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `last_visit` over `1w` is expected for UC-25.104.3, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `derm:appointment`. Dedup with `stats latest(*) by patient_id`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `days_since` > 365 for 'Derm Appointment Interval Breach'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Skincare & Dermatology — typical feeds: Curology prescriptions (`curology:prescription`), skin diaries (`skin:diary`), dermatology appointments (`derm:appointment`), and UV exposure logs (`uv:exposure`)..\n• **Sourcetypes in this search**: `derm:appointment`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=derm:appointment`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Curology prescription shipments, daily skin diary entries, dermatology appointment exports, and UV exposure tracker logs ingested into Splunk HEC..\n\n**Author notes:** Emit one `derm:appointment` event per visit with ISO `appt_date` and review overdue concerns quarterly.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"derm:appointment\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=derm:appointment earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Derm Appointment Interval Breach** over `personal` data (primarily `sourcetype=derm:appointment`) across the full search range. The `max` aggregation groups by `patient_id`, `concern_type`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `concern_type`, `days_since`, `last_visit`, `patient_id`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=derm:appointment patient_id=* appt_date=*\n| stats max(appt_date) as last_visit by patient_id concern_type\n| eval days_since=round((now()-strptime(last_visit,\"%Y-%m-%d\"))/86400,0)\n| where days_since>365\n| sort - days_since\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=derm:appointment earliest=-7d\n| head 50\n| table concern_type, days_since, last_visit, patient_id\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=derm:appointment earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `days_since` before alerting:\n\n```spl\nindex=personal sourcetype=derm:appointment earliest=-30d\n| stats perc50(days_since) AS p50 perc90(days_since) AS p90 perc99(days_since) AS p99 max(days_since) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.104.3: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Concerns By Days Since Last Derm Visit (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `derm:appointment` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=derm:appointment | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It counts skin issues you have not had a dermatologist look at in over a year.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
