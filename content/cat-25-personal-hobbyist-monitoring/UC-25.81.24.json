{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.81.24",
  "title": "Social Club Attendance Participation Gap Alert",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability",
    "Business"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Social Club Attendance Participation Gap Alert' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_social_club_attendance_participation_gap_alert` (UC-25.81.24), SOC dashboard panel \"Personal — Social Club Attendance Participation Gap Alert\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.81.24 positive (Social, Club, Attendance, Participation, Gap): ingest synthetic events into `index=personal` sourcetype='club:attendance' where `last_seen` grouped by `member` satisfies `days_since` > 21 for 'Social Club Attendance Participation Gap Alert'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.81.24 negative (Gap Alert): ingest benign `personal` / `club:attendance` events where `member` stays below `days_since` > 21, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 8 confirms zero alert rows for 'Social Club Attendance Participation Gap Alert'."
  },
  "dataSources": "Club attendance (`club:attendance`), running-club sessions (`runningclub:session`), maker-space visits (`makerspace:visit`), book-club meetings (`bookclub:meeting`), community events (`community:event`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Book-club attendance, running-club miles, maker-space visits, and general social-club participation logs streamed to Splunk HEC via forms, check-ins, and calendar exports. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=club:attendance\n| stats max(_time) as last_seen by member\n| eval days_since=round((now()-last_seen)/86400,1)\n| where days_since>21\n| sort - days_since",
  "description": "Detects UC-25.81.24 — Social, Club, Attendance, Participation, Gap — using `index=personal` sourcetype `club:attendance`, grouped by `member`. Outputs `last_seen` each `the scheduled window`; alert when `days_since` > 21. Primary focus: Gap Alert. Purpose-built for 'Social Club Attendance Participation Gap Alert' in Personal workflows.",
  "value": "UC-25.81.24 accelerates insight into Gap Alert ('Social Club Attendance Participation Gap Alert') by tracking `last_seen` on `club:attendance` feeds, reducing missed training goals, stale hobby data, and connector failures before spreadsheet checks hide regressions.",
  "implementation": "Adjust the gap threshold to the normal cadence of the club or activity.",
  "visualization": "Table of member values by days since last participation.",
  "cimModels": [],
  "references": [
    {
      "title": "Meetup",
      "url": "https://www.meetup.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "StoryGraph - book clubs",
      "url": "https://thestorygraph.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Makerspace.com",
      "url": "https://makerspace.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.81.24)** — Connector for 'Social Club Attendance Participation Gap Alert' stops polling `club:attendance`; stale `last_seen` on `member` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `last_seen` over `1w` is expected for UC-25.81.24, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `club:attendance`. Dedup with `stats latest(*) by member`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `days_since` > 21 for 'Social Club Attendance Participation Gap Alert'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "dell_emc",
    "oci"
  ],
  "equipmentModels": [
    "dell_emc_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Social Clubs & Community Groups — typical feeds: Club attendance (`club:attendance`), running-club sessions (`runningclub:session`), maker-space visits (`makerspace:visit`), book-club meetings (`bookclub:meeting`), community events (`community:event.\n• **Sourcetypes in this search**: `club:attendance`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=club:attendance`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Book-club attendance, running-club miles, maker-space visits, and general social-club participation logs streamed to Splunk HEC via forms, check-ins, and calendar exports..\n\n**Author notes:** Adjust the gap threshold to the normal cadence of the club or activity.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"club:attendance\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=club:attendance earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Social Club Attendance Participation Gap Alert** over `personal` data (primarily `sourcetype=club:attendance`) across the full search range. It applies numeric thresholds on `days_since` (> 21) after max — tune these constants to your household baseline.\n\n**Fields this search depends on:** `days_since`, `member`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=club:attendance\n| stats max(_time) as last_seen by member\n| eval days_since=round((now()-last_seen)/86400,1)\n| where days_since>21\n| sort - days_since\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=club:attendance earliest=-7d\n| head 50\n| table days_since, member\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=club:attendance earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `days_since` before alerting:\n\n```spl\nindex=personal sourcetype=club:attendance earliest=-30d\n| stats perc50(days_since) AS p50 perc90(days_since) AS p90 perc99(days_since) AS p99 max(days_since) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.81.24: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Member Values By Days Since Last Participation (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `club:attendance` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=club:attendance | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It gathers who has drifted away from social club attendance long enough to merit a nudge.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
