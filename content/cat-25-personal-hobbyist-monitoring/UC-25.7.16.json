{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.7.16",
  "title": "New Music Discovery Ratio",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics",
    "Business"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'New Music Discovery Ratio' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_new_music_discovery_ratio` (UC-25.7.16), SOC dashboard panel \"Personal — New Music Discovery Ratio\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.7.16 positive (New, Music, Discovery, Ratio): ingest synthetic events into `index=personal` sourcetype='spotify:play' where `sum` grouped by `_time` satisfies `plays` > 0 for 'New Music Discovery Ratio'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.7.16 negative (Discovery Ratio): ingest benign `personal` / `spotify:play` events where `_time` stays below `plays` > 0, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 13 confirms zero alert rows for 'New Music Discovery Ratio'."
  },
  "dataSources": "Tautulli/Plex playback (`tautulli:play`), Jellyfin activity (`jellyfin:activity`), Spotify recently-played (`spotify:play`), Steam player and achievements (`steam:player`), Sonarr events (`sonarr:event`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Plex / Jellyfin (Tautulli), Sonos, Spotify, Steam, Xbox/PSN, and the *arr stack (Sonarr/Radarr) APIs and webhooks via HEC scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=spotify:play\n| bin _time span=1w\n| stats count as plays, sum(eval(first_time_artist=\"true\")) as new_artist_plays by _time\n| eval discovery_pct=round(100*new_artist_plays/plays,1)\n| where plays>0\n| sort - _time",
  "description": "Monitors UC-25.7.16 — New, Music, Discovery, Ratio — using `index=personal` sourcetype `spotify:play`, grouped by `_time`. Outputs `sum` each `1w`; alert when `plays` > 0. Primary focus: Discovery Ratio. Purpose-built for 'New Music Discovery Ratio' in Personal workflows.",
  "value": "UC-25.7.16 gives Personal teams earlier visibility into Discovery Ratio through `spotify:play` feeds, lowering missed training goals, stale hobby data, and connector failures.",
  "implementation": "Forward a first-time-artist marker from your playback export or enrichment job; review weekly discovery percentage over time.",
  "visualization": "Weekly line chart of new-artist play percentage.",
  "cimModels": [],
  "references": [
    {
      "title": "Spotify Web API",
      "url": "https://developer.spotify.com/documentation/web-api",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.7.16)** — Connector for 'New Music Discovery Ratio' stops polling `spotify:play`; stale `sum` on `_time` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `sum` over `1w` is expected for UC-25.7.16, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `spotify:play`. Dedup with `stats latest(*) by _time`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `plays` > 0 for 'New Music Discovery Ratio'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Spotify — Spotify Web API recently-played → HEC.\n• **Subcategory context**: Media, Gaming & Entertainment — typical feeds: Tautulli/Plex playback (`tautulli:play`), Jellyfin activity (`jellyfin:activity`), Spotify recently-played (`spotify:play`), Steam player/achievements (`steam:player`), Sonarr/Radarr events (`sonarr:e.\n• **Sourcetypes in this search**: `spotify:play`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Spotify** events into `index=personal` with `sourcetype=spotify:play`.\n\n**Collection method:** Spotify Web API recently-played → HEC.\n\n**Reference endpoint / path:** `GET /v1/me/player/recently-played`.\n\n**Suggested poll interval:** 900s.\n\n**Key fields to extract:** `track.name, artist, played_at, duration_ms`.\n\n**Typical connector stack:** Plex / Jellyfin (Tautulli), Sonos, Spotify, Steam, Xbox/PSN, and *arr stack (Sonarr/Radarr) APIs and webhooks via HEC..\n\n**Author notes:** Forward a first-time-artist marker from your playback export or enrichment job; review weekly discovery percentage over time.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"spotify:play\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=spotify:play earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **New Music Discovery Ratio** over `personal` data (primarily `sourcetype=spotify:play`) across the full search range. It applies numeric thresholds on `plays` (> 0) after sum — tune these constants to your household baseline.\n\n**Fields this search depends on:** `_time`, `plays`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=spotify:play\n| bin _time span=1w\n| stats count as plays, sum(eval(first_time_artist=\"true\")) as new_artist_plays by _time\n| eval discovery_pct=round(100*new_artist_plays/plays,1)\n| where plays>0\n| sort - _time\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Spotify app → Recently played. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=spotify:play earliest=-7d\n| head 50\n| table _time, plays\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=spotify:play earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `plays` before alerting:\n\n```spl\nindex=personal sourcetype=spotify:play earliest=-30d\n| stats perc50(plays) AS p50 perc90(plays) AS p90 perc99(plays) AS p99 max(plays) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.7.16: Personal evidence` with the following panel layout:\n\n**Panel 1 — Weekly Line Chart Of New-Artist Play Percentage (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Spotify — no Spotify events / missing fields:** If `spotify:play` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Spotify connector script is still running (`systemctl status` or cron log).\n\n**Spotify — NULL values in extracted fields:** The Spotify JSON export shape drifted — compare a raw event (`index=personal sourcetype=spotify:play | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Spotify — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Spotify — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Spotify APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=1w` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1w` buckets.",
  "grandmaExplanation": "It surfaces how much of your listening was truly new music instead of the same familiar artists again.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
