{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.51.55",
  "title": "GoodRx Price Lookup Failure Rate",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault",
    "Quality"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'GoodRx Price Lookup Failure Rate' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_goodrx_price_lookup_failure_rate` (UC-25.51.55), SOC dashboard panel \"Personal — GoodRx Price Lookup Failure Rate\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.51.55 positive (GoodRx, Price, Lookup, Failure, Rate): ingest synthetic events into `index=personal` sourcetype='goodrx:price' where `failed_events` grouped by `drug_name` satisfies detection threshold for 'GoodRx Price Lookup Failure Rate'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.51.55 negative (Failure Rate): ingest benign `personal` / `goodrx:price` events where `drug_name` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 8 confirms zero alert rows for 'GoodRx Price Lookup Failure Rate'."
  },
  "dataSources": "GoodRx prices (`goodrx:price`), CVS refills (`cvs:refill`), pharmacy pickups (`pharmacy:pickup`), and prescription adherence logs (`prescription:adherence`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "GoodRx price lookups, CVS refill confirmations, pharmacy pickup logs, and prescription adherence trackers streamed into Splunk HEC. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=goodrx:price\n| eval status_value=lower(coalesce(lookup_status,\"unknown\"))\n| eval failed=if(match(status_value,\"error|fail|alert|overdue|late|stale|breach\"),1,0)\n| stats count as events, sum(failed) as failed_events by drug_name\n| eval failure_pct=round(100*failed_events/events,1)\n| sort - failure_pct - failed_events",
  "description": "Identifies UC-25.51.55 — GoodRx, Price, Lookup, Failure, Rate — using `index=personal` sourcetype `goodrx:price`, grouped by `drug_name`. Outputs `failed_events` each `the scheduled window`; alert when configured threshold. Primary focus: Failure Rate. Purpose-built for 'GoodRx Price Lookup Failure Rate' in Personal workflows.",
  "value": "UC-25.51.55 accelerates insight into Failure Rate ('GoodRx Price Lookup Failure Rate') by tracking `failed_events` on `goodrx:price` feeds, reducing missed training goals, stale hobby data, and connector failures before spreadsheet checks hide regressions.",
  "implementation": "Normalize lookup_status values into good or bad states and review the failure percentage per drug.",
  "visualization": "Bar chart of failure percentage by entity.",
  "cimModels": [],
  "references": [
    {
      "title": "GoodRx API",
      "url": "https://www.goodrx.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CVS Pharmacy",
      "url": "https://www.cvs.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.51.55)** — Connector for 'GoodRx Price Lookup Failure Rate' stops polling `goodrx:price`; stale `failed_events` on `drug_name` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `failed_events` over `1w` is expected for UC-25.51.55, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `goodrx:price`. Dedup with `stats latest(*) by drug_name`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'GoodRx Price Lookup Failure Rate'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "snmp"
  ],
  "equipmentModels": [
    "snmp_ups"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Pharmacy & Prescriptions — typical feeds: GoodRx prices (`goodrx:price`), CVS refills (`cvs:refill`), pharmacy pickups (`pharmacy:pickup`), and prescription adherence logs (`prescription:adherence`)..\n• **Sourcetypes in this search**: `goodrx:price`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=goodrx:price`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** GoodRx price lookups, CVS refill confirmations, pharmacy pickup logs, and prescription adherence trackers streamed into Splunk HEC..\n\n**Author notes:** Normalize lookup_status values into good or bad states and review the failure percentage per drug.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"goodrx:price\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=goodrx:price earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **GoodRx Price Lookup Failure Rate** over `personal` data (primarily `sourcetype=goodrx:price`) across the full search range. The `sum` aggregation groups by `drug_name`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `drug_name`, `failed`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=goodrx:price\n| eval status_value=lower(coalesce(lookup_status,\"unknown\"))\n| eval failed=if(match(status_value,\"error|fail|alert|overdue|late|stale|breach\"),1,0)\n| stats count as events, sum(failed) as failed_events by drug_name\n| eval failure_pct=round(100*failed_events/events,1)\n| sort - failure_pct - failed_events\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=goodrx:price earliest=-7d\n| head 50\n| table drug_name, failed\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=goodrx:price earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.51.55: Personal evidence` with the following panel layout:\n\n**Panel 1 — Bar Chart Of Failure Percentage By Entity (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `goodrx:price` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=goodrx:price | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It maps which parts of your GoodRx price reference list data keep ending up in a bad state instead of working cleanly.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
