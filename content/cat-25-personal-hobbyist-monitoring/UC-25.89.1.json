{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.89.1",
  "title": "Nanny Hours vs Contracted Weekly Cap",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Cost",
    "Operations"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Nanny Hours vs Contracted Weekly Cap' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_nanny_hours_vs_contracted_weekly_cap` (UC-25.89.1), SOC dashboard panel \"Personal — Nanny Hours vs Contracted Weekly Cap\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.89.1 positive (Nanny, Hours, Contracted, Weekly, Cap): ingest synthetic events into `index=personal` sourcetype='nanny:timesheet' where `weekly_hours` grouped by `_time`, `caregiver_name` satisfies `weekly_hours` > 40, or `over_cap` > 0 for 'Nanny Hours vs Contracted Weekly Cap'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.89.1 negative (Weekly Cap): ingest benign `personal` / `nanny:timesheet` events where `_time`, `caregiver_name` stays below `weekly_hours` > 40, or `over_cap` > 0, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 11 confirms zero alert rows for 'Nanny Hours vs Contracted Weekly Cap'."
  },
  "dataSources": "brightwheel check-ins (`brightwheel:checkin`), Procare attendance (`procare:attendance`), Huckleberry sleep logs (`huckleberry:sleep`), daycare incidents (`daycare:incident`), and nanny timesheets (`nanny:timesheet`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "brightwheel check-in exports, Procare attendance logs, Huckleberry sleep tracking, daycare incident reports, and nanny timesheet entries streamed into Splunk HEC. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=nanny:timesheet hours_worked=*\n| bin _time span=1w\n| stats sum(hours_worked) as weekly_hours by _time caregiver_name\n| eval over_cap=if(weekly_hours>40,weekly_hours-40,0)\n| where over_cap>0\n| sort - over_cap",
  "description": "Alerts on UC-25.89.1 — Nanny, Hours, Contracted, Weekly, Cap — using `index=personal` sourcetype `nanny:timesheet`, grouped by `_time`, `caregiver_name`. Outputs `weekly_hours` each `1w`; alert when `weekly_hours` > 40, or `over_cap` > 0. Primary focus: Weekly Cap. Purpose-built for 'Nanny Hours vs Contracted Weekly Cap' in Personal workflows.",
  "value": "UC-25.89.1 helps teams contain Nanny, Hours, Contracted, Weekly, Cap scenarios and reduce missed training goals, stale hobby data, and connector failures before incidents compound.",
  "implementation": "Export nanny timesheets with `hours_worked` and `caregiver_name`, then alert when weekly totals exceed your contract.",
  "visualization": "Table of weeks and caregivers with overtime hours.",
  "cimModels": [],
  "references": [
    {
      "title": "brightwheel",
      "url": "https://mybrightwheel.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Procare Solutions",
      "url": "https://www.procaresoftware.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Huckleberry",
      "url": "https://huckleberrycare.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.89.1)** — Connector for 'Nanny Hours vs Contracted Weekly Cap' stops polling `nanny:timesheet`; stale `weekly_hours` on `_time`, `caregiver_name` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `weekly_hours` over `1w` is expected for UC-25.89.1, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `nanny:timesheet`. Dedup with `stats latest(*) by _time`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `weekly_hours` > 40 for 'Nanny Hours vs Contracted Weekly Cap'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Childcare & Early Years — typical feeds: brightwheel check-ins (`brightwheel:checkin`), Procare attendance (`procare:attendance`), Huckleberry sleep logs (`huckleberry:sleep`), daycare incidents (`daycare:incident`), and nanny timesheets (`n.\n• **Sourcetypes in this search**: `nanny:timesheet`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=nanny:timesheet`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** brightwheel check-in exports, Procare attendance logs, Huckleberry sleep tracking, daycare incident reports, and nanny timesheet entries streamed into Splunk HEC..\n\n**Author notes:** Export nanny timesheets with `hours_worked` and `caregiver_name`, then alert when weekly totals exceed your contract.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"nanny:timesheet\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=nanny:timesheet earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Nanny Hours vs Contracted Weekly Cap** over `personal` data (primarily `sourcetype=nanny:timesheet`) across the full search range. It applies numeric thresholds on `weekly_hours` (> 40) after sum — tune these constants to your household baseline.\n\n**Fields this search depends on:** `_time`, `caregiver_name`, `over_cap`, `weekly_hours`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=nanny:timesheet hours_worked=*\n| bin _time span=1w\n| stats sum(hours_worked) as weekly_hours by _time caregiver_name\n| eval over_cap=if(weekly_hours>40,weekly_hours-40,0)\n| where over_cap>0\n| sort - over_cap\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=nanny:timesheet earliest=-7d\n| head 50\n| table _time, caregiver_name, over_cap, weekly_hours\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=nanny:timesheet earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `weekly_hours` before alerting:\n\n```spl\nindex=personal sourcetype=nanny:timesheet earliest=-30d\n| stats perc50(weekly_hours) AS p50 perc90(weekly_hours) AS p90 perc99(weekly_hours) AS p99 max(weekly_hours) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.89.1: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Weeks And Caregivers With Overtime Hours (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `nanny:timesheet` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=nanny:timesheet | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=1w` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1w` buckets.",
  "grandmaExplanation": "It tracks when nanny hours went over the weekly limit you planned for.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
