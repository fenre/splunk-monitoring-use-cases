{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.4.270",
  "title": "Dryer Lint Reminder Overrun",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Safety",
    "Operations"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Dryer Lint Reminder Overrun' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_dryer_lint_reminder_overrun` (UC-25.4.270), SOC dashboard panel \"Personal — Dryer Lint Reminder Overrun\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.4.270 positive (Dryer, Lint, Reminder, Overrun): ingest synthetic events into `index=personal` sourcetype='thinq:dryer' where `cycles_since_clean` grouped by `machine_name` satisfies `cycles_since_clean` > 10 for 'Dryer Lint Reminder Overrun'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.4.270 negative (Reminder Overrun): ingest benign `personal` / `thinq:dryer` events where `machine_name` stays below `cycles_since_clean` > 10, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 5 confirms zero alert rows for 'Dryer Lint Reminder Overrun'."
  },
  "dataSources": "LG ThinQ washer cycles (`thinq:washer`), LG ThinQ dryer cycles (`thinq:dryer`), Home Connect dishwasher runs (`homeconnect:dishwasher`), and ecobee HVAC runtime (`ecobee:thermostat`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "LG ThinQ washer and dryer telemetry, Home Connect dishwasher data, and ecobee HVAC runtime exported into Splunk. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=thinq:dryer cycle_count_since_lint_clean=*\n| stats max(cycle_count_since_lint_clean) as cycles_since_clean by machine_name\n| where cycles_since_clean>10\n| sort - cycles_since_clean",
  "description": "Alerts on 'Dryer Lint Reminder Overrun' patterns (UC-25.4.270) sourced from `index=personal` sourcetype `thinq:dryer`. Threshold bundle: `cycles_since_clean` > 10; aggregation grain: `machine_name`; metrics: `cycles_since_clean`. Watch terms: Dryer, Lint, Reminder, Overrun.",
  "value": "UC-25.4.270 gives Personal teams earlier visibility into Reminder Overrun through `thinq:dryer` feeds, lowering missed training goals, stale hobby data, and connector failures.",
  "implementation": "Increment a `cycle_count_since_lint_clean` counter on each completed cycle and reset it when cleaning is recorded.",
  "visualization": "Single-value or table of dryers by cycles since lint clean.",
  "cimModels": [],
  "references": [
    {
      "title": "LG ThinQ",
      "url": "https://www.lg.com/us/lg-thinq",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Home Connect API",
      "url": "https://api-docs.home-connect.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "ecobee Developer API",
      "url": "https://www.ecobee.com/home/developer/api/introduction/index.shtml",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.4.270)** — Connector for 'Dryer Lint Reminder Overrun' stops polling `thinq:dryer`; stale `cycles_since_clean` on `machine_name` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `cycles_since_clean` over `1w` is expected for UC-25.4.270, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `thinq:dryer`. Dedup with `stats latest(*) by machine_name`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `cycles_since_clean` > 10 for 'Dryer Lint Reminder Overrun'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Laundry & Major Appliances — typical feeds: LG ThinQ washer cycles (`thinq:washer`), LG ThinQ dryer cycles (`thinq:dryer`), Home Connect dishwasher runs (`homeconnect:dishwasher`), and ecobee HVAC runtime (`ecobee:thermostat`)..\n• **Sourcetypes in this search**: `thinq:dryer`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=thinq:dryer`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** LG ThinQ washer and dryer telemetry, Home Connect dishwasher data, and ecobee HVAC runtime exported into Splunk..\n\n**Author notes:** Increment a `cycle_count_since_lint_clean` counter on each completed cycle and reset it when cleaning is recorded.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"thinq:dryer\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=thinq:dryer earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Dryer Lint Reminder Overrun** over `personal` data (primarily `sourcetype=thinq:dryer`) across the full search range. It applies numeric thresholds on `cycles_since_clean` (> 10) after max — tune these constants to your household baseline.\n\n**Fields this search depends on:** `cycles_since_clean`, `machine_name`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=thinq:dryer cycle_count_since_lint_clean=*\n| stats max(cycle_count_since_lint_clean) as cycles_since_clean by machine_name\n| where cycles_since_clean>10\n| sort - cycles_since_clean\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=thinq:dryer earliest=-7d\n| head 50\n| table cycles_since_clean, machine_name\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=thinq:dryer earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `cycles_since_clean` before alerting:\n\n```spl\nindex=personal sourcetype=thinq:dryer earliest=-30d\n| stats perc50(cycles_since_clean) AS p50 perc90(cycles_since_clean) AS p90 perc99(cycles_since_clean) AS p99 max(cycles_since_clean) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.4.270: Personal evidence` with the following panel layout:\n\n**Panel 1 — Single-Value Or Table Of Dryers By Cycles Since Lint Clean (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `thinq:dryer` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=thinq:dryer | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It watches which dryer has gone too many loads without a proper lint clean-out.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
