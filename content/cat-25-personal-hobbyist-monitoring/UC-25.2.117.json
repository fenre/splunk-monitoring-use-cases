{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.2.117",
  "title": "Body Fat Scale Hydration vs Weight Swing",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Body Fat Scale Hydration vs Weight Swing' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_body_fat_scale_hydration_vs_weight_swing` (UC-25.2.117), SOC dashboard panel \"Personal — Body Fat Scale Hydration vs Weight Swing\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.2.117 positive (Body, Fat, Scale, Hydration, Weight): ingest synthetic events into `index=personal` sourcetype='bodycomp:reading' where `avg_weight_delta_kg`, `avg_hydration_pct` grouped by `day_of_week` satisfies detection threshold for 'Body Fat Scale Hydration vs Weight Swing'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.2.117 negative (Weight Swing): ingest benign `personal` / `bodycomp:reading` events where `day_of_week` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 15 confirms zero alert rows for 'Body Fat Scale Hydration vs Weight Swing'."
  },
  "dataSources": "Oura stress and recovery (`oura:stress`), CPAP therapy sessions (`cpap:session`), oral thermometer readings (`thermometer:reading`), and body-composition scale snapshots (`bodycomp:reading`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Personal health telemetry from Oura, OSCAR CPAP exports, oral thermometer readings, and smart body-composition scales sent to Splunk HEC via APIs and CSV exports. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=bodycomp:reading\n| stats avg(weight_delta_kg) as avg_weight_delta_kg avg(hydration_pct) as avg_hydration_pct by day_of_week\n| sort 0 - avg_weight_delta_kg",
  "description": "Monitors UC-25.2.117 — Body, Fat, Scale, Hydration, Weight — using `index=personal` sourcetype `bodycomp:reading`, grouped by `day_of_week`. Outputs `avg_weight_delta_kg`, `avg_hydration_pct` each `the scheduled window`; alert when configured threshold. Primary focus: Weight Swing. Purpose-built for 'Body Fat Scale Hydration vs Weight Swing' in Personal workflows.",
  "value": "UC-25.2.117 operationalises 'Body Fat Scale Hydration vs Weight Swing' against `bodycomp:reading` feeds so operators reduce missed training goals, stale hobby data, and connector failures without manual correlation.",
  "implementation": "Send weight swing with hydration context events to `index=personal` and keep `day_of_week` populated so the leaderboard stays actionable.",
  "visualization": "Bar chart of weight swing with hydration context by days of week.",
  "cimModels": [],
  "references": [
    {
      "title": "Oura",
      "url": "https://oura.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "OSCAR CPAP analysis",
      "url": "https://www.sleepfiles.com/OSCAR/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Kinsa",
      "url": "https://home.kinsahealth.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Withings scales",
      "url": "https://www.withings.com/us/en/scales",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.2.117)** — Connector for 'Body Fat Scale Hydration vs Weight Swing' stops polling `bodycomp:reading`; stale `avg_weight_delta_kg`, `avg_hydration_pct` on `day_of_week` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `avg_weight_delta_kg`, `avg_hydration_pct` over `1w` is expected for UC-25.2.117, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `bodycomp:reading`. Dedup with `stats latest(*) by day_of_week`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Body Fat Scale Hydration vs Weight Swing'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Advanced Health & Biohacking — typical feeds: HRV (`hrv:reading`), blood oxygen (`spo2:reading`), lab panels (`bloodpanel:result`), supplements (`supplement:intake`), continuous glucose (`cgm:glucose`), sleep-apnea events (`apnea:event`), body co.\n• **Sourcetypes in this search**: `bodycomp:reading`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=bodycomp:reading`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Biohacking data — HRV straps, pulse oximeters, blood-pressure cuffs, continuous glucose monitors, body-composition scales, lab-result exports, and supplement logs — ingested to Splunk HEC via APIs and scripted inputs..\n\n**Author notes:** Send weight swing with hydration context events to `index=personal` and keep `day_of_week` populated so the leaderboard stays actionable.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"bodycomp:reading\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=bodycomp:reading earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Body Fat Scale Hydration vs Weight Swing** over `personal` data (primarily `sourcetype=bodycomp:reading`) across the full search range. The `avg` aggregation groups by `day_of_week`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `day_of_week`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=bodycomp:reading\n| stats avg(weight_delta_kg) as avg_weight_delta_kg avg(hydration_pct) as avg_hydration_pct by day_of_week\n| sort 0 - avg_weight_delta_kg\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=bodycomp:reading earliest=-7d\n| head 50\n| table day_of_week\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=bodycomp:reading earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.2.117: Personal evidence` with the following panel layout:\n\n**Panel 1 — Bar Chart Of Weight Swing With Hydration Context By Days Of Week (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `bodycomp:reading` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=bodycomp:reading | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It reveals which days of weeks stand out most for weight swing with hydration context.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
