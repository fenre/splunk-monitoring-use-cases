{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.2.16",
  "title": "Dexcom Dawn Phenomenon Trend",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Analytics",
    "Anomaly"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Dexcom Dawn Phenomenon Trend' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_dexcom_dawn_phenomenon_trend` (UC-25.2.16), SOC dashboard panel \"Personal — Dexcom Dawn Phenomenon Trend\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.2.16 positive (Dexcom, Dawn, Phenomenon, Trend): ingest synthetic events into `index=personal` sourcetype='dexcom:egv' where `count` grouped by `user`, `_time` satisfies `hour` >= 4, `hour` <= 8, or `dawn_rise` > 30 for 'Dexcom Dawn Phenomenon Trend'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.2.16 negative (Phenomenon Trend): ingest benign `personal` / `dexcom:egv` events where `user`, `_time` stays below `hour` >= 4, `hour` <= 8, or `dawn_rise` > 30, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 12 confirms zero alert rows for 'Dexcom Dawn Phenomenon Trend'."
  },
  "dataSources": "Apple Health metrics (`apple:health`), Oura Ring daily readiness/sleep (`oura:daily`), WHOOP recovery/strain (`whoop:cycle`), Withings body and blood-pressure (`withings:measure`), Dexcom CGM glucose (`dexcom:egv`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Apple Health / Apple Watch exports (Health Auto Export, Shortcuts) via HEC; Oura, WHOOP, Withings, and Dexcom APIs via scripted REST inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=dexcom:egv\n| eval hour=strftime(_time,\"%H\")\n| where hour>=4 AND hour<=8\n| bin _time span=1d\n| stats earliest(glucose_mgdl) as start_glucose, latest(glucose_mgdl) as end_glucose by user _time\n| eval dawn_rise=end_glucose-start_glucose\n| where dawn_rise>30\n| sort - dawn_rise",
  "description": "Detects UC-25.2.16 — Dexcom, Dawn, Phenomenon, Trend — using `index=personal` sourcetype `dexcom:egv`, grouped by `user`, `_time`. Outputs `count` each `1d`; alert when `hour` >= 4, `hour` <= 8, or `dawn_rise` > 30. Primary focus: Phenomenon Trend. Purpose-built for 'Dexcom Dawn Phenomenon Trend' in Personal workflows.",
  "value": "Tracking `activity` via UC-25.2.16 on `dexcom:egv` feeds improves response to 'Dexcom Dawn Phenomenon Trend' and reduces missed training goals, stale hobby data, and connector failures.",
  "implementation": "Ingest Dexcom glucose values at their native cadence; schedule daily and review mornings with a strong pre-breakfast rise.",
  "visualization": "Daily bar chart of dawn-rise glucose change.",
  "cimModels": [],
  "references": [
    {
      "title": "Dexcom Developer portal",
      "url": "https://developer.dexcom.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.2.16)** — Connector for 'Dexcom Dawn Phenomenon Trend' stops polling `dexcom:egv`; stale `count` on `user`, `_time` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `count` over `1d` is expected for UC-25.2.16, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `dexcom:egv`. Dedup with `stats latest(*) by user`.\n\n4. **Timezone bucket misalignment** — `1d` bins on UTC vs local schedule skew `hour` >= 4 for 'Dexcom Dawn Phenomenon Trend'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Health, Sleep & Wearables — typical feeds: Apple Health metrics (`apple:health`), Oura Ring daily readiness/sleep (`oura:daily`), Whoop recovery/strain (`whoop:cycle`), Withings body/blood-pressure (`withings:measure`), Dexcom CGM glucose (`de.\n• **Sourcetypes in this search**: `dexcom:egv`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=dexcom:egv`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Apple Health / Apple Watch exports (Health Auto Export, Shortcuts) via HEC; Oura, Whoop, Withings, Dexcom APIs via scripted REST inputs..\n\n**Author notes:** Ingest Dexcom glucose values at their native cadence; schedule daily and review mornings with a strong pre-breakfast rise.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"dexcom:egv\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=dexcom:egv earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Dexcom Dawn Phenomenon Trend** over `personal` data (primarily `sourcetype=dexcom:egv`) across the full search range. It applies numeric thresholds on `hour` (>= 4) after earliest, latest — tune these constants to your household baseline.\n\n**Fields this search depends on:** `_time`, `dawn_rise`, `hour`, `user`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=dexcom:egv\n| eval hour=strftime(_time,\"%H\")\n| where hour>=4 AND hour<=8\n| bin _time span=1d\n| stats earliest(glucose_mgdl) as start_glucose, latest(glucose_mgdl) as end_glucose by user _time\n| eval dawn_rise=end_glucose-start_glucose\n| where dawn_rise>30\n| sort - dawn_rise\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=dexcom:egv earliest=-7d\n| head 50\n| table _time, dawn_rise, hour, user\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=dexcom:egv earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `hour` before alerting:\n\n```spl\nindex=personal sourcetype=dexcom:egv earliest=-30d\n| stats perc50(hour) AS p50 perc90(hour) AS p90 perc99(hour) AS p99 max(hour) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.2.16: Personal evidence` with the following panel layout:\n\n**Panel 1 — Daily Bar Chart Of Dawn-Rise Glucose Change (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `dexcom:egv` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=dexcom:egv | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=1d` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1d` buckets.",
  "grandmaExplanation": "It logs when your blood sugar keeps climbing in the early morning even before breakfast.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
