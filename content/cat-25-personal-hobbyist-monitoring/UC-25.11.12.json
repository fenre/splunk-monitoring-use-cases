{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.11.12",
  "title": "Litter Box Visit Frequency Spike",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics",
    "Safety"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers personal hobby monitoring for 'Litter Box Visit Frequency Spike' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_litter_box_visit_frequency_spike` (UC-25.11.12), SOC dashboard panel \"Personal — Litter Box Visit Frequency Spike\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.11.12 positive (Litter, Box, Visit, Frequency, Spike): ingest synthetic events into `index=personal` sourcetype='litterrobot:cycle' where `baseline` grouped by `_time` satisfies `baseline` > 0 for 'Litter Box Visit Frequency Spike'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.11.12 negative (Frequency Spike): ingest benign `personal` / `litterrobot:cycle` events where `_time` stays below `baseline` > 0, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 4 confirms zero alert rows for 'Litter Box Visit Frequency Spike'."
  },
  "dataSources": "Pet tracker location/activity (`tractive:position`, `whistle:activity`), smart feeder events (`petfeeder:event`), litter-box usage (`litterrobot:cycle`), bird-feeder detections (`birdfeeder:detection`), doorbell/package events (`doorbell:event`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Pet GPS/activity trackers (Tractive/Fi/Whistle), smart feeders and litter boxes, bird feeders/cams, and home-life sensors via vendor APIs and MQTT to HEC. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=litterrobot:cycle\n| bin _time span=1d\n| stats count as visits by _time\n| streamstats window=7 current=f avg(visits) as baseline\n| where baseline>0 AND visits>baseline*1.5\n| sort - visits",
  "description": "Monitors risk tied to Litter, Box, Visit, Frequency, Spike through UC-25.11.12 on `index=personal` sourcetype `litterrobot:cycle`. Summarises `baseline` grouped by `_time` across `1d` before applying `baseline` > 0. Operators tune 'Litter Box Visit Frequency Spike' using UC-25.11.12 runbook steps.",
  "value": "Tracking `baseline` via UC-25.11.12 on `litterrobot:cycle` feeds improves response to 'Litter Box Visit Frequency Spike' and reduces missed training goals, stale hobby data, and connector failures.",
  "implementation": "Forward litter-box cycle events; compare each day to the recent rolling average and alert on significant increases.",
  "visualization": "Daily chart of litter-box visits versus rolling baseline.",
  "cimModels": [],
  "references": [
    {
      "title": "Litter-Robot",
      "url": "https://www.litter-robot.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.11.12)** — Connector for 'Litter Box Visit Frequency Spike' stops polling `litterrobot:cycle`; stale `baseline` on `_time` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `baseline` over `1d` is expected for UC-25.11.12, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `litterrobot:cycle`. Dedup with `stats latest(*) by _time`.\n\n4. **Timezone bucket misalignment** — `1d` bins on UTC vs local schedule skew `baseline` > 0 for 'Litter Box Visit Frequency Spike'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Pets & Home Life — typical feeds: Pet tracker location/activity (`tractive:position`, `whistle:activity`), smart feeder events (`petfeeder:event`), litter-box usage (`litterrobot:cycle`), bird-feeder cam detections, plant/soil sensors.\n• **Sourcetypes in this search**: `litterrobot:cycle`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=litterrobot:cycle`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Pet GPS/activity trackers (Tractive/Fi/Whistle), smart feeders and litter boxes, bird feeders/cams, and plant sensors via vendor APIs and MQTT to HEC..\n\n**Author notes:** Forward litter-box cycle events; compare each day to the recent rolling average and alert on significant increases.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"litterrobot:cycle\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=litterrobot:cycle earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Litter Box Visit Frequency Spike** over `personal` data (primarily `sourcetype=litterrobot:cycle`) across the full search range. `eventstats` / `streamstats` compute rolling baselines before filtering outliers; window size controls sensitivity (smaller window = noisier alerts).\n\n**Fields this search depends on:** `_time`, `baseline`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=litterrobot:cycle\n| bin _time span=1d\n| stats count as visits by _time\n| streamstats window=7 current=f avg(visits) as baseline\n| where baseline>0 AND visits>baseline*1.5\n| sort - visits\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=litterrobot:cycle earliest=-7d\n| head 50\n| table _time, baseline\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=litterrobot:cycle earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `baseline` before alerting:\n\n```spl\nindex=personal sourcetype=litterrobot:cycle earliest=-30d\n| stats perc50(baseline) AS p50 perc90(baseline) AS p90 perc99(baseline) AS p99 max(baseline) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.11.12: Personal evidence` with the following panel layout:\n\n**Panel 1 — Daily Chart Of Litter-Box Visits Versus Rolling Baseline (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `litterrobot:cycle` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=litterrobot:cycle | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Everything is an outlier:** Baseline window is too short or `stdev=0` — require minimum sample count (`where count>=5`) before computing z-scores.\n\n**Bucket misalignment:** `span=1d` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1d` buckets.",
  "grandmaExplanation": "It logs when the cat is using the litter box much more often than usual.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
