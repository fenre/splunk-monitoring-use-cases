{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.41.6",
  "title": "Action-Sport Crash and Big-Impact Log",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Safety",
    "Anomaly"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Action-Sport Crash and Big-Impact Log' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_action_sport_crash_and_big_impact_log` (UC-25.41.6), SOC dashboard panel \"Personal — Action-Sport Crash and Big-Impact Log\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.41.6 positive (Action, Sport, Crash, and, Big): ingest synthetic events into `index=personal` sourcetype='skate:session' where `worst` grouped by `sport` satisfies `impact_g` > 6 for 'Action-Sport Crash and Big-Impact Log'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.41.6 negative (Impact Log): ingest benign `personal` / `skate:session` events where `sport` stays below `impact_g` > 6, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 11 confirms zero alert rows for 'Action-Sport Crash and Big-Impact Log'."
  },
  "dataSources": "E-bike rides (`ebike:ride`), scooter trips (`scooter:trip`), skate sessions (`skate:session`), surf sessions (`surf:session`), snow runs (`snow:run`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Micro-mobility and action-sport trackers — e-bike / e-scooter apps (VanMoof, Bosch eBike Flow), and skate / surf / snow session trackers (Trace, Slopes, Xensr) with IMU sensors — streamed to Splunk HEC via APIs and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal (sourcetype=skate:session OR sourcetype=snow:run OR sourcetype=surf:session)\n| where impact_g>6\n| stats count as hard_impacts, max(impact_g) as worst by sport\n| sort - worst",
  "description": "Monitors UC-25.41.6 — Action, Sport, Crash, and, Big — using `index=personal` sourcetype `skate:session`, grouped by `sport`. Outputs `worst` each `the scheduled window`; alert when `impact_g` > 6. Primary focus: Impact Log. Purpose-built for 'Action-Sport Crash and Big-Impact Log' in Personal workflows.",
  "value": "UC-25.41.6 helps teams contain Action, Sport, Crash, and, Big scenarios and reduce missed training goals, stale hobby data, and connector failures before incidents compound.",
  "implementation": "Ingest impact data from IMU sensors into `index=personal`; count and rank hard impacts per sport.",
  "visualization": "Bar chart of hard impacts by sport with the worst reading.",
  "cimModels": [],
  "references": [
    {
      "title": "Home Assistant — sensors",
      "url": "https://www.home-assistant.io/integrations/sensor/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.41.6)** — Connector for 'Action-Sport Crash and Big-Impact Log' stops polling `skate:session`; stale `worst` on `sport` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `worst` over `1w` is expected for UC-25.41.6, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `skate:session`. Dedup with `stats latest(*) by sport`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `impact_g` > 6 for 'Action-Sport Crash and Big-Impact Log'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "servicenow"
  ],
  "equipmentModels": [
    "servicenow_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Micro-Mobility & Action Sports — typical feeds: E-bike rides (`ebike:ride`), scooter trips (`scooter:trip`), skate sessions (`skate:session`), surf sessions (`surf:session`), snow runs (`snow:run`)..\n• **Sourcetypes in this search**: `skate:session`, `snow:run`, `surf:session`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=skate:session`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Micro-mobility and action-sport trackers — e-bike / e-scooter apps (VanMoof, Bosch eBike Flow), and skate / surf / snow session trackers (Trace, Slopes, Xensr) with IMU sensors — streamed to Splunk HEC via APIs and scripted inputs..\n\n**Author notes:** Ingest impact data from IMU sensors into `index=personal`; count and rank hard impacts per sport.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"skate:session\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=skate:session earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Action-Sport Crash and Big-Impact Log** over `personal` data (primarily `sourcetype=skate:session`) across the full search range. It applies numeric thresholds on `impact_g` (> 6) after max — tune these constants to your household baseline.\n\n**Fields this search depends on:** `impact_g`, `sport`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal (sourcetype=skate:session OR sourcetype=snow:run OR sourcetype=surf:session)\n| where impact_g>6\n| stats count as hard_impacts, max(impact_g) as worst by sport\n| sort - worst\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=skate:session earliest=-7d\n| head 50\n| table impact_g, sport\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=skate:session earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `impact_g` before alerting:\n\n```spl\nindex=personal sourcetype=skate:session earliest=-30d\n| stats perc50(impact_g) AS p50 perc90(impact_g) AS p90 perc99(impact_g) AS p99 max(impact_g) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.41.6: Personal evidence` with the following panel layout:\n\n**Panel 1 — Bar Chart Of Hard Impacts By Sport With The Worst Reading (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `skate:session` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=skate:session | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It flags your hardest falls across board and snow sports, a simple safety log for a rough day.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
