{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.20.7",
  "title": "River or Creek Level Flood Watch",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Risk",
    "Anomaly"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'River or Creek Level Flood Watch' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_river_or_creek_level_flood_watch` (UC-25.20.7), SOC dashboard panel \"Personal — River or Creek Level Flood Watch\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.20.7 positive (River, Creek, Level, Flood, Watch): ingest synthetic events into `index=personal` sourcetype='riverlevel:reading' where `fastest_rise` grouped by `entity` satisfies `level` > 200, or `fastest_rise` > 15 for 'River or Creek Level Flood Watch'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.20.7 negative (Flood Watch): ingest benign `personal` / `riverlevel:reading` events where `entity` stays below `level` > 200, or `fastest_rise` > 15, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 2 confirms zero alert rows for 'River or Creek Level Flood Watch'."
  },
  "dataSources": "Seismograph (`seismo:reading`), Geiger counter (`geiger:cpm`), lightning strikes (`lightning:strike`), radon (`radon:reading`), all-sky meteor camera (`allsky:capture`), river-level gauge (`riverlevel:reading`), magnetometer (`magnetometer:reading`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "DIY citizen-science sensors — Raspberry Shake seismograph, DIY Geiger counter, AS3935/Blitzortung lightning detector, radon monitor, all-sky meteor camera, river-level gauge, and magnetometer — streamed to Splunk HEC via MQTT and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=riverlevel:reading\n| streamstats current=f last(level_cm) as prev\n| eval rise_rate=level_cm-prev\n| stats latest(level_cm) as level, max(rise_rate) as fastest_rise\n| eval flood_risk=if(level>200 OR fastest_rise>15,1,0)",
  "description": "Detects UC-25.20.7 for River, Creek, Level, Flood, Watch over `index=personal` sourcetype `riverlevel:reading` with thresholds `level` > 200, or `fastest_rise` > 15. Groups `fastest_rise` by `entity` each `the scheduled window`. Centered on Flood Watch indicators. Supports repeatable personal triage for 'River or Creek Level Flood Watch'.",
  "value": "For 'River or Creek Level Flood Watch', UC-25.20.7 converts `fastest_rise` on `riverlevel:reading` feeds into actionable rows and reduces missed training goals, stale hobby data, and connector failures.",
  "implementation": "Ingest an ultrasonic level sensor over the water into `index=personal`; alert on a high level or a rapid rise.",
  "visualization": "Time chart of water level with rise-rate and flood threshold.",
  "cimModels": [],
  "references": [
    {
      "title": "USGS — water data for the nation",
      "url": "https://waterdata.usgs.gov/nwis",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.20.7)** — Connector for 'River or Creek Level Flood Watch' stops polling `riverlevel:reading`; stale `fastest_rise` on `external_id` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `fastest_rise` over `1w` is expected for UC-25.20.7, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `riverlevel:reading`. Dedup with `stats latest(*) by external_id`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `level` > 200 for 'River or Creek Level Flood Watch'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Citizen Science & Backyard Sensing — typical feeds: Seismograph (`seismo:reading`), Geiger counter (`geiger:cpm`), lightning strikes (`lightning:strike`), radon (`radon:reading`), all-sky meteor camera (`allsky:capture`), river-level gauge (`riverlevel.\n• **Sourcetypes in this search**: `riverlevel:reading`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=riverlevel:reading`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** DIY citizen-science sensors — Raspberry Shake seismograph, Geiger counter, AS3935/Blitzortung lightning detector, radon monitor, all-sky meteor camera, river-level gauge, and magnetometer — via MQTT and scripted inputs to Splunk HEC..\n\n**Author notes:** Ingest an ultrasonic level sensor over the water into `index=personal`; alert on a high level or a rapid rise.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"riverlevel:reading\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=riverlevel:reading earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **River or Creek Level Flood Watch** over `personal` data (primarily `sourcetype=riverlevel:reading`) across the full search range. `eventstats` / `streamstats` compute rolling baselines before filtering outliers; window size controls sensitivity (smaller window = noisier alerts).\n\n**Fields this search depends on:** `fastest_rise`, `flood_risk`, `level`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=riverlevel:reading\n| streamstats current=f last(level_cm) as prev\n| eval rise_rate=level_cm-prev\n| stats latest(level_cm) as level, max(rise_rate) as fastest_rise\n| eval flood_risk=if(level>200 OR fastest_rise>15,1,0)\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=riverlevel:reading earliest=-7d\n| head 50\n| table fastest_rise, flood_risk, level\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=riverlevel:reading earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `level` before alerting:\n\n```spl\nindex=personal sourcetype=riverlevel:reading earliest=-30d\n| stats perc50(level) AS p50 perc90(level) AS p90 perc99(level) AS p99 max(level) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.20.7: Personal evidence` with the following panel layout:\n\n**Panel 1 — Time Chart Of Water Level With Rise-Rate And Flood Threshold (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `riverlevel:reading` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=riverlevel:reading | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Everything is an outlier:** Baseline window is too short or `stdev=0` — require minimum sample count (`where count>=5`) before computing z-scores.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It watches a nearby stream's level and how quickly it is rising, giving early warning of possible flooding.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
