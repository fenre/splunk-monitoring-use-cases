{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.18.5",
  "title": "Alarm Arm and Disarm Audit",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Audit"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers personal hobby monitoring for 'Alarm Arm and Disarm Audit' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_alarm_arm_and_disarm_audit` (UC-25.18.5), SOC dashboard panel \"Personal — Alarm Arm and Disarm Audit\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.18.5 positive (Alarm, Arm, and, Disarm, Audit): ingest synthetic events into `index=personal` sourcetype='alarm:event' where `values` grouped by `user`, `_time` satisfies detection threshold for 'Alarm Arm and Disarm Audit'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.18.5 negative (Disarm Audit): ingest benign `personal` / `alarm:event` events where `user`, `_time` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 1 confirms zero alert rows for 'Alarm Arm and Disarm Audit'."
  },
  "dataSources": "NVR object-detection (`frigate:event`), camera health (`camera:health`), alarm arm/disarm and life-safety (`alarm:event`), lock access (`lock:access`), plate recognition (`plate:detection`), doorbell (`doorbell:event`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Frigate / Blue Iris NVR object-detection events, camera health, alarm and smart-lock logs, and licence-plate recognition via MQTT and Splunk HEC. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=alarm:event (action=arm OR action=disarm)\n| stats count as events, values(action) as actions by user _time\n| sort - _time\n| head 50",
  "description": "Identifies Disarm Audit activity in UC-25.18.5 ('Alarm Arm and Disarm Audit') from `index=personal` sourcetype `alarm:event`. Rolls up `values` by `user`, `_time` across `the scheduled window` and pages when configured threshold. Covers Alarm, Arm, and, Disarm, Audit. Personal operators treat UC-25.18.5 as the canonical signal.",
  "value": "Operators running UC-25.18.5 on `alarm:event` feeds shorten triage for Disarm Audit tied to 'Alarm Arm and Disarm Audit', lowering missed training goals, stale hobby data, and connector failures.",
  "implementation": "Ingest alarm-panel events to `index=personal`; review the recent arm and disarm history by user.",
  "visualization": "Table of recent arm and disarm actions by user and time.",
  "cimModels": [],
  "references": [
    {
      "title": "Home Assistant — alarm control panel",
      "url": "https://www.home-assistant.io/integrations/alarm_control_panel/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.18.5)** — Connector for 'Alarm Arm and Disarm Audit' stops polling `alarm:event`; stale `values` on `user`, `_time` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `values` over `1w` is expected for UC-25.18.5, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `alarm:event`. Dedup with `stats latest(*) by user`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Alarm Arm and Disarm Audit'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "cctv",
    "mqtt"
  ],
  "equipmentModels": [
    "cctv_generic",
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Home Security & Surveillance — typical feeds: NVR object-detection (`frigate:event`), camera health (`camera:health`), alarm arm/disarm and life-safety (`alarm:event`), lock access (`lock:access`), plate recognition (`plate:detection`), doorbell .\n• **Sourcetypes in this search**: `alarm:event`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=alarm:event`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Frigate / Blue Iris NVR object-detection events, camera health, alarm and smart-lock logs, and licence-plate recognition via MQTT and Splunk HEC..\n\n**Author notes:** Ingest alarm-panel events to `index=personal`; review the recent arm and disarm history by user.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"alarm:event\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=alarm:event earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Alarm Arm and Disarm Audit** over `personal` data (primarily `sourcetype=alarm:event`) across the full search range. The `values` aggregation groups by `user`, `_time`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `_time`, `user`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=alarm:event (action=arm OR action=disarm)\n| stats count as events, values(action) as actions by user _time\n| sort - _time\n| head 50\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=alarm:event earliest=-7d\n| head 50\n| table _time, user\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=alarm:event earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.18.5: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Recent Arm And Disarm Actions By User And Time (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `alarm:event` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=alarm:event | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It keeps a record of who turned the burglar alarm on or off and when, so you can always check.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
