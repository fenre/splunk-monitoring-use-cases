{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.44.26",
  "title": "Smoker Stall Duration by Meat Cut",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Smoker Stall Duration by Meat Cut' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_smoker_stall_duration_by_meat_cut` (UC-25.44.26), SOC dashboard panel \"Personal — Smoker Stall Duration by Meat Cut\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.44.26 positive (Smoker, Stall, Duration, Meat, Cut): ingest synthetic events into `index=personal` sourcetype='cook:smoker' where `avg_stall_duration_min` grouped by `meat_cut` satisfies detection threshold for 'Smoker Stall Duration by Meat Cut'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.44.26 negative (Meat Cut): ingest benign `personal` / `cook:smoker` events where `meat_cut` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 2 confirms zero alert rows for 'Smoker Stall Duration by Meat Cut'."
  },
  "dataSources": "Catch trips (`outdoor:trip`), hunt map activity (`hunt:map`), foraging observations (`forage:observation`), smoker sessions (`cook:smoker`), harvest queues (`outdoor:queue`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Fishing, hunting, foraging, and outdoor-cooking telemetry — Fishbrain catch logs, onX Hunt map exports, iNaturalist observations, and ThermoWorks smoker probes — streamed to Splunk HEC via APIs, exports, and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=cook:smoker meat_cut=* stall_duration_min=*\n| stats avg(stall_duration_min) as avg_stall_duration_min, count as events by meat_cut\n| sort - avg_stall_duration_min",
  "description": "Detects UC-25.44.26 for Smoker, Stall, Duration, Meat, Cut over `index=personal` sourcetype `cook:smoker` with thresholds configured threshold. Groups `avg_stall_duration_min` by `meat_cut` each `the scheduled window`. Centered on Meat Cut indicators. Supports repeatable personal triage for 'Smoker Stall Duration by Meat Cut'.",
  "value": "For 'Smoker Stall Duration by Meat Cut', UC-25.44.26 converts `avg_stall_duration_min` on `cook:smoker` feeds into actionable rows and reduces missed training goals, stale hobby data, and connector failures.",
  "implementation": "Capture `stall_duration_min` on `cook:smoker` events and normalize `meat_cut` so Splunk can summarize performance by meat cut.",
  "visualization": "Bar chart of average stall duration minutes by meat cut.",
  "cimModels": [],
  "references": [
    {
      "title": "Fishbrain — fishing app",
      "url": "https://fishbrain.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "onX Hunt — maps",
      "url": "https://www.onxmaps.com/hunt/app",
      "retrieved": "2026-04-25"
    },
    {
      "title": "iNaturalist — observations",
      "url": "https://www.inaturalist.org/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "ThermoWorks Signals — smoker telemetry",
      "url": "https://www.thermoworks.com/signals/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.44.26)** — Connector for 'Smoker Stall Duration by Meat Cut' stops polling `cook:smoker`; stale `avg_stall_duration_min` on `meat_cut` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `avg_stall_duration_min` over `1w` is expected for UC-25.44.26, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `cook:smoker`. Dedup with `stats latest(*) by meat_cut`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Smoker Stall Duration by Meat Cut'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Fishing, Hunting & Foraging — typical feeds: Fishing catches (`fishing:catch`), fishfinder sonar (`fishfinder:reading`), foraging finds (`foraging:find`), game-cam triggers (`gamecam:trigger`)..\n• **Sourcetypes in this search**: `cook:smoker`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=cook:smoker`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Outdoor logs — fishing apps (Fishbrain), sonar / fishfinder exports, game/trail cameras, and foraging journals — streamed to Splunk HEC via APIs and scripted inputs..\n\n**Author notes:** Capture `stall_duration_min` on `cook:smoker` events and normalize `meat_cut` so Splunk can summarize performance by meat cut.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"cook:smoker\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=cook:smoker earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Smoker Stall Duration by Meat Cut** over `personal` data (primarily `sourcetype=cook:smoker`) across the full search range. The `avg` aggregation groups by `meat_cut`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `meat_cut`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=cook:smoker meat_cut=* stall_duration_min=*\n| stats avg(stall_duration_min) as avg_stall_duration_min, count as events by meat_cut\n| sort - avg_stall_duration_min\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=cook:smoker earliest=-7d\n| head 50\n| table meat_cut\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=cook:smoker earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.44.26: Personal evidence` with the following panel layout:\n\n**Panel 1 — Bar Chart Of Average Stall Duration Minutes By Meat Cut (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `cook:smoker` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=cook:smoker | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It celebrates the average stall duration minutes for each meat cut so you can see what works best for smoker cooks.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
