{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.37.32",
  "title": "Air Handler Runtime Before Filter Threshold",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Air Handler Runtime Before Filter Threshold' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_air_handler_runtime_before_filter_threshold` (UC-25.37.32), SOC dashboard panel \"Personal — Air Handler Runtime Before Filter Threshold\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.37.32 positive (Air, Handler, Runtime, Before, Filter): ingest synthetic events into `index=personal` sourcetype='hvacfilter:reading' where `avg_runtime_to_threshold_hr` grouped by `_time`, `unit_name` satisfies detection threshold for 'Air Handler Runtime Before Filter Threshold'. Validate filter `runtime_to_threshold_hr=*`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.37.32 negative (Filter Threshold): ingest benign `personal` / `hvacfilter:reading` events where `_time`, `unit_name` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 14 confirms zero alert rows for 'Air Handler Runtime Before Filter Threshold'."
  },
  "dataSources": "Radon readings (`radon:reading`), sump humidity events (`sump:humidity`), HVAC filter telemetry (`hvacfilter:reading`), and roof leak events (`roofleak:event`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Home health telemetry from radon monitors, sump humidity probes, HVAC filter tracking, and roof leak sensors sent to Splunk HEC via MQTT and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=hvacfilter:reading\n| search runtime_to_threshold_hr=*\n| bin _time span=1w\n| stats avg(runtime_to_threshold_hr) as avg_runtime_to_threshold_hr by _time unit_name\n| sort - _time",
  "description": "Monitors UC-25.37.32 — Air, Handler, Runtime, Before, Filter — using `index=personal` sourcetype `hvacfilter:reading`, grouped by `_time`, `unit_name`. Outputs `avg_runtime_to_threshold_hr` each `1w`; alert when configured threshold. Filter chain includes filter `runtime_to_threshold_hr=*`. Purpose-built for 'Air Handler Runtime Before Filter Threshold' in Personal workflows.",
  "value": "Alerting on Air, Handler, Runtime, Before, Filter through UC-25.37.32 reduces missed training goals, stale hobby data, and connector failures by surfacing `avg_runtime_to_threshold_hr` early.",
  "implementation": "Send air handler runtime before filter threshold events to `index=personal` with the fields used in the breakdown and review the resulting trend at 1w granularity.",
  "visualization": "Stacked time chart of air handler runtime before filter threshold by unit.",
  "cimModels": [],
  "references": [
    {
      "title": "Airthings",
      "url": "https://www.airthings.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "ESPHome",
      "url": "https://esphome.io/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Filtrete",
      "url": "https://www.filtrete.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Govee water detectors",
      "url": "https://us.govee.com/products/govee-water-detectors",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.37.32)** — Connector for 'Air Handler Runtime Before Filter Threshold' stops polling `hvacfilter:reading`; stale `avg_runtime_to_threshold_hr` on `_time`, `unit_name` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `avg_runtime_to_threshold_hr` over `1w` is expected for UC-25.37.32, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `hvacfilter:reading`. Dedup with `stats latest(*) by _time`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Air Handler Runtime Before Filter Threshold'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Building Health & Structural — typical feeds: Mould risk (`mould:risk`), crack meters (`crackmeter:reading`), HVAC filter life (`hvacfilter:life`), structural tilt (`structural:tilt`)..\n• **Sourcetypes in this search**: `hvacfilter:reading`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=hvacfilter:reading`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Building-health sensors — indoor humidity / mould-risk monitors, crack and tilt meters, HVAC filter-life trackers, and damp sensors — streamed to Splunk HEC via MQTT and ESPHome..\n\n**Author notes:** Send air handler runtime before filter threshold events to `index=personal` with the fields used in the breakdown and review the resulting trend at 1w granularity.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"hvacfilter:reading\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=hvacfilter:reading earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Air Handler Runtime Before Filter Threshold** over `personal` data (primarily `sourcetype=hvacfilter:reading`) across the full search range. The `avg` aggregation groups by `_time`, `unit_name`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `_time`, `unit_name`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=hvacfilter:reading\n| search runtime_to_threshold_hr=*\n| bin _time span=1w\n| stats avg(runtime_to_threshold_hr) as avg_runtime_to_threshold_hr by _time unit_name\n| sort - _time\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=hvacfilter:reading earliest=-7d\n| head 50\n| table _time, unit_name\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=hvacfilter:reading earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.37.32: Personal evidence` with the following panel layout:\n\n**Panel 1 — Stacked Time Chart Of Air Handler Runtime Before Filter Threshold By Unit (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `hvacfilter:reading` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=hvacfilter:reading | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=1w` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1w` buckets.",
  "grandmaExplanation": "It frames how air handler runtime before filter threshold changes over time so the busy spells and quiet spells stand out.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
