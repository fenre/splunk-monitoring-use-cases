{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.53.32",
  "title": "Vendor Insurance Certificate Gap",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Vendor Insurance Certificate Gap' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_vendor_insurance_certificate_gap` (UC-25.53.32), SOC dashboard panel \"Personal — Vendor Insurance Certificate Gap\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.53.32 positive (Vendor, Insurance, Certificate, Gap): ingest synthetic events into `index=personal` sourcetype='event:vendor' where `sum` grouped by `vendor_type` satisfies `backlog` > 0 for 'Vendor Insurance Certificate Gap'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.53.32 negative (Certificate Gap): ingest benign `personal` / `event:vendor` events where `vendor_type` stays below `backlog` > 0, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 14 confirms zero alert rows for 'Vendor Insurance Certificate Gap'."
  },
  "dataSources": "Planning tasks (`event:checklist`), seating revisions (`event:seating`), vendor contracts (`event:vendor`), deposits (`event:deposit`), planning syncs (`event:sync`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Wedding and event-planning telemetry — The Knot checklist exports, seating-chart revisions, vendor deposit schedules, and RSVP operations — streamed to Splunk HEC via exports and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=event:vendor vendor_type=* insurance_state=*\n| stats sum(eval(if(insurance_state!=\"received\",1,0))) as backlog, count as total by vendor_type\n| where backlog>0\n| sort - backlog",
  "description": "Identifies UC-25.53.32 — Vendor, Insurance, Certificate, Gap — using `index=personal` sourcetype `event:vendor`, grouped by `vendor_type`. Outputs `sum` each `the scheduled window`; alert when `backlog` > 0. Primary focus: Certificate Gap. Purpose-built for 'Vendor Insurance Certificate Gap' in Personal workflows.",
  "value": "Alerting on Vendor, Insurance, Certificate, Gap through UC-25.53.32 reduces missed training goals, stale hobby data, and connector failures by surfacing `activity` early.",
  "implementation": "Mark each `event:vendor` record with a pending-state flag and summarize backlog totals by `vendor_type`.",
  "visualization": "Table of vendor type with pending insurance certificates counts.",
  "cimModels": [],
  "references": [
    {
      "title": "The Knot",
      "url": "https://www.theknot.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "AllSeated",
      "url": "https://www.allseated.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Zola",
      "url": "https://www.zola.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "HoneyBook",
      "url": "https://www.honeybook.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.53.32)** — Connector for 'Vendor Insurance Certificate Gap' stops polling `event:vendor`; stale `sum` on `vendor_type` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `sum` over `1w` is expected for UC-25.53.32, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `event:vendor`. Dedup with `stats latest(*) by vendor_type`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `backlog` > 0 for 'Vendor Insurance Certificate Gap'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Wedding & Event Planning — typical feeds: Planning tasks (`event:task`), budget lines (`event:budget`), RSVPs (`event:rsvp`), vendor bookings (`event:vendor`), countdown milestones (`event:countdown`)..\n• **Sourcetypes in this search**: `event:vendor`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=event:vendor`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Wedding and event planning — task and budget spreadsheets, RSVP platforms (Zola, The Knot), vendor contracts, and seating tools — streamed to Splunk HEC via CSV exports and scripted inputs..\n\n**Author notes:** Mark each `event:vendor` record with a pending-state flag and summarize backlog totals by `vendor_type`.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"event:vendor\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=event:vendor earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Vendor Insurance Certificate Gap** over `personal` data (primarily `sourcetype=event:vendor`) across the full search range. It applies numeric thresholds on `backlog` (> 0) after sum — tune these constants to your household baseline.\n\n**Fields this search depends on:** `backlog`, `vendor_type`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=event:vendor vendor_type=* insurance_state=*\n| stats sum(eval(if(insurance_state!=\"received\",1,0))) as backlog, count as total by vendor_type\n| where backlog>0\n| sort - backlog\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=event:vendor earliest=-7d\n| head 50\n| table backlog, vendor_type\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=event:vendor earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `backlog` before alerting:\n\n```spl\nindex=personal sourcetype=event:vendor earliest=-30d\n| stats perc50(backlog) AS p50 perc90(backlog) AS p90 perc99(backlog) AS p99 max(backlog) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.53.32: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Vendor Type With Pending Insurance Certificates Counts (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `event:vendor` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=event:vendor | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It frames where unfinished insurance certificates are piling up so you can clear the biggest queues in vendor compliance.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
