{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.78.53",
  "title": "Electric Bill Tracking Seasonal Usage Baseline",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Electric Bill Tracking Seasonal Usage Baseline' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_electric_bill_tracking_seasonal_usage_baseline` (UC-25.78.53), SOC dashboard panel \"Personal — Electric Bill Tracking Seasonal Usage Baseline\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.78.53 positive (Electric, Bill, Tracking, Seasonal, Usage): ingest synthetic events into `index=personal` sourcetype='utility:bill' where `avg_usage`, `overall_usage` grouped by `season` satisfies detection threshold for 'Electric Bill Tracking Seasonal Usage Baseline'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.78.53 negative (Usage Baseline): ingest benign `personal` / `utility:bill` events where `season` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 6 confirms zero alert rows for 'Electric Bill Tracking Seasonal Usage Baseline'."
  },
  "dataSources": "Utility bills (`utility:bill`), utility meter readings (`utility:meter`), utility rate changes (`utility:rate`), household accounts (`household:account`), utility payments (`utility:payment`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Electric, gas, and water bills, smart-meter readings, household-account records, and utility rate-plan changes streamed to Splunk HEC via portals, CSV exports, and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=utility:bill bill_type=\"electric\"\n| stats avg(usage_kwh) as avg_usage by season\n| eventstats avg(avg_usage) as overall_usage\n| eval delta=round(avg_usage-overall_usage,2)\n| sort - delta",
  "description": "Detects Usage Baseline anomalies for UC-25.78.53 with window `the scheduled window` on `index=personal` sourcetype `utility:bill`. Combines `avg_usage`, `overall_usage` by `season` and compares against configured threshold. Mapped to keywords Electric, Bill, Tracking, Seasonal, Usage for dashboard routing.",
  "value": "Alerting on Electric, Bill, Tracking, Seasonal, Usage through UC-25.78.53 reduces missed training goals, stale hobby data, and connector failures by surfacing `avg_usage`, `overall_usage` early.",
  "implementation": "Populate a simple season field during ingest so the chart stays readable.",
  "visualization": "Bar chart of seasonal average usage versus overall average.",
  "cimModels": [],
  "references": [
    {
      "title": "U.S. Energy Information Administration",
      "url": "https://www.eia.gov/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "EPA WaterSense",
      "url": "https://www.epa.gov/watersense",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Home Assistant Utility Meter",
      "url": "https://www.home-assistant.io/integrations/utility_meter/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.78.53)** — Connector for 'Electric Bill Tracking Seasonal Usage Baseline' stops polling `utility:bill`; stale `avg_usage`, `overall_usage` on `season` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `avg_usage`, `overall_usage` over `1w` is expected for UC-25.78.53, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `utility:bill`. Dedup with `stats latest(*) by season`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Electric Bill Tracking Seasonal Usage Baseline'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Utility Bills & Household Accounts — typical feeds: Utility bills (`utility:bill`), utility meter readings (`utility:meter`), utility rate changes (`utility:rate`), household accounts (`household:account`), utility payments (`utility:payment`)..\n• **Sourcetypes in this search**: `utility:bill`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=utility:bill`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Electric, gas, and water bills, smart-meter readings, household-account records, and utility rate-plan changes streamed to Splunk HEC via portals, CSV exports, and scripted inputs..\n\n**Author notes:** Populate a simple season field during ingest so the chart stays readable.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"utility:bill\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=utility:bill earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Electric Bill Tracking Seasonal Usage Baseline** over `personal` data (primarily `sourcetype=utility:bill`) across the full search range. `eventstats` / `streamstats` compute rolling baselines before filtering outliers; window size controls sensitivity (smaller window = noisier alerts).\n\n**Fields this search depends on:** `season`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=utility:bill bill_type=\"electric\"\n| stats avg(usage_kwh) as avg_usage by season\n| eventstats avg(avg_usage) as overall_usage\n| eval delta=round(avg_usage-overall_usage,2)\n| sort - delta\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=utility:bill earliest=-7d\n| head 50\n| table season\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=utility:bill earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.78.53: Personal evidence` with the following panel layout:\n\n**Panel 1 — Bar Chart Of Seasonal Average Usage Versus Overall Average (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `utility:bill` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=utility:bill | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Everything is an outlier:** Baseline window is too short or `stdev=0` — require minimum sample count (`where count>=5`) before computing z-scores.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It warns whether one season regularly makes electric bill tracking much heavier or lighter than normal.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
