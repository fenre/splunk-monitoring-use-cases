{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.74.31",
  "title": "Running Races & Endurance Weekend vs Weekday Split",
  "criticality": "low",
  "difficulty": "beginner",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "dataSources": "Race results (`raceresult:event`), TrainingPeaks workouts (`trainingpeaks:workout`), marathon splits (`race:split`), bib pickup and race logistics (`race:bib`).",
  "app": "Race result APIs, TrainingPeaks workouts and TSS, marathon split exports, and bib-collection checklists streamed to Splunk HEC via scripted REST inputs, CSV imports, and mobile forms.",
  "spl": "index=personal sourcetype=raceresult:event\n| eval day_type=if(in(strftime(_time,\"%u\"),\"6\",\"7\"),\"weekend\",\"weekday\")\n| stats count as events, avg(duration_min) as avg_duration by day_type",
  "description": "Separates running races activity between weekdays and weekends when duration is present.",
  "value": "Weekend/weekday splits quickly show whether a life domain is work-driven, leisure-driven, or bleeding into the wrong days.",
  "implementation": "Populate optional `duration_min` on `raceresult:event` events if available; the search still works on counts alone when duration is missing.",
  "visualization": "Side-by-side bar chart of weekday vs weekend event counts.",
  "cimModels": [],
  "references": [
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/WhatsInThisManual"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "grandmaExplanation": "It works out whether your running races activity mostly happens on weekdays or weekends so you can see if the habit fits the life you actually live."
}
