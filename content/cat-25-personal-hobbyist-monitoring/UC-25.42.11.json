{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.42.11",
  "title": "Night-Currency Window Tracker",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Compliance",
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Night-Currency Window Tracker' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_night_currency_window_tracker` (UC-25.42.11), SOC dashboard panel \"Personal — Night-Currency Window Tracker\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.42.11 positive (Night, Currency, Window, Tracker): ingest synthetic events into `index=personal` sourcetype='flightlog:entry' where `sum` grouped by `entity` satisfies `days_ago` <= 90, or `recent_night_landings` >= 3 for 'Night-Currency Window Tracker'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.42.11 negative (Window Tracker): ingest benign `personal` / `flightlog:entry` events where `entity` stays below `days_ago` <= 90, or `recent_night_landings` >= 3, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 2 confirms zero alert rows for 'Night-Currency Window Tracker'."
  },
  "dataSources": "Pilot logbook (`flightlog:entry`), avionics telemetry (`avionics:telemetry`), flight-sim sessions (`flightsim:session`), pre-flight checklists (`preflight:check`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Pilot and flight-sim telemetry — electronic logbooks (ForeFlight, LogTen Pro), Stratux / GDL90 avionics receivers, and Microsoft Flight Simulator / X-Plane SimConnect sessions — streamed to Splunk HEC via scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=flightlog:entry\n| eval days_ago=round((now()-_time)/86400,0)\n| stats sum(eval(if(days_ago<=90,night_landings,0))) as recent_night_landings\n| eval current=if(recent_night_landings>=3,\"current\",\"NOT current\")",
  "description": "Surfaces UC-25.42.11 for Night, Currency, Window, Tracker over `index=personal` sourcetype `flightlog:entry` with thresholds `days_ago` <= 90, or `recent_night_landings` >= 3. Groups `sum` by `entity` each `the scheduled window`. Centered on Window Tracker indicators. Supports repeatable personal triage for 'Night-Currency Window Tracker'.",
  "value": "For 'Night-Currency Window Tracker', UC-25.42.11 converts `activity` on `flightlog:entry` feeds into actionable rows and reduces missed training goals, stale hobby data, and connector failures.",
  "implementation": "Ingest flight-log entries with night-landing counts; calculate rolling night-currency status.",
  "visualization": "Single-value tile of recent night landings and current/not-current status.",
  "cimModels": [],
  "references": [
    {
      "title": "ForeFlight — pilot app",
      "url": "https://foreflight.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "FAA — Airman Certification",
      "url": "https://www.faa.gov/pilots/training/airman_education",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.42.11)** — Connector for 'Night-Currency Window Tracker' stops polling `flightlog:entry`; stale `sum` on `external_id` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `sum` over `1w` is expected for UC-25.42.11, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `flightlog:entry`. Dedup with `stats latest(*) by external_id`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `days_ago` <= 90 for 'Night-Currency Window Tracker'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "hardware_bmc"
  ],
  "equipmentModels": [
    "hardware_bmc_ilo"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Aviation & Flight Simulation — typical feeds: Pilot logbook (`flightlog:entry`), avionics telemetry (`avionics:telemetry`), flight-sim sessions (`flightsim:session`), pre-flight checklists (`preflight:check`)..\n• **Sourcetypes in this search**: `flightlog:entry`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=flightlog:entry`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Pilot and flight-sim telemetry — electronic logbooks (ForeFlight, LogTen Pro), Stratux / GDL90 avionics receivers, and Microsoft Flight Simulator / X-Plane SimConnect sessions — streamed to Splunk HEC via scripted inputs..\n\n**Author notes:** Ingest flight-log entries with night-landing counts; calculate rolling night-currency status.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"flightlog:entry\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=flightlog:entry earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Night-Currency Window Tracker** over `personal` data (primarily `sourcetype=flightlog:entry`) across the full search range. It applies numeric thresholds on `days_ago` (<= 90) after sum — tune these constants to your household baseline.\n\n**Fields this search depends on:** `current`, `days_ago`, `recent_night_landings`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=flightlog:entry\n| eval days_ago=round((now()-_time)/86400,0)\n| stats sum(eval(if(days_ago<=90,night_landings,0))) as recent_night_landings\n| eval current=if(recent_night_landings>=3,\"current\",\"NOT current\")\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=flightlog:entry earliest=-7d\n| head 50\n| table current, days_ago, recent_night_landings\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=flightlog:entry earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `days_ago` before alerting:\n\n```spl\nindex=personal sourcetype=flightlog:entry earliest=-30d\n| stats perc50(days_ago) AS p50 perc90(days_ago) AS p90 perc99(days_ago) AS p99 max(days_ago) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.42.11: Personal evidence` with the following panel layout:\n\n**Panel 1 — Single-Value Tile Of Recent Night Landings And Current/Not-Current Status (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `flightlog:entry` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=flightlog:entry | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It watches whether your recent night flying practice is still fresh enough to stay comfortable and current.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
