{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.17.2",
  "title": "Print Failure and Reason Breakdown",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Print Failure and Reason Breakdown' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_print_failure_and_reason_breakdown` (UC-25.17.2), SOC dashboard panel \"Personal — Print Failure and Reason Breakdown\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.17.2 positive (Print, Failure, and, Reason, Breakdown): ingest synthetic events into `index=personal` sourcetype='octoprint:job' where `count` grouped by `reason`, `printer` satisfies detection threshold for 'Print Failure and Reason Breakdown'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.17.2 negative (Reason Breakdown): ingest benign `personal` / `octoprint:job` events where `reason`, `printer` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 0 confirms zero alert rows for 'Print Failure and Reason Breakdown'."
  },
  "dataSources": "Print jobs (`octoprint:job`), printer firmware telemetry (`printer:telemetry`), filament spools (`filament:spool`), CNC/laser jobs (`cnc:job`), workshop air (`workshop:air`), cordless tool batteries (`tool:battery`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "OctoPrint / Klipper (Moonraker) print servers, printer firmware telemetry, filament spool tracking, CNC/laser job logs, and workshop air/tool sensors via HEC and MQTT. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=octoprint:job result=failed\n| stats count as failures by reason printer\n| sort - failures",
  "description": "Identifies 'Print Failure and Reason Breakdown' (UC-25.17.2) using `index=personal` sourcetype `octoprint:job` by aggregating `count` grouped by `reason`, `printer` over `the scheduled window` and alerting when configured threshold. Keywords: Print, Failure, and, Reason, Breakdown. Focuses on Reason Breakdown behaviour so personal teams can triage UC-25.17.2 without ad-hoc log review.",
  "value": "UC-25.17.2 accelerates insight into Reason Breakdown ('Print Failure and Reason Breakdown') by tracking `activity` on `octoprint:job` feeds, reducing missed training goals, stale hobby data, and connector failures before spreadsheet checks hide regressions.",
  "implementation": "Tag failed jobs with a reason; schedule weekly and review the leading failure causes per printer.",
  "visualization": "Pie chart of print failures by reason.",
  "cimModels": [],
  "references": [
    {
      "title": "OctoPrint — REST API",
      "url": "https://docs.octoprint.org/en/master/api/index.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.17.2)** — Connector for 'Print Failure and Reason Breakdown' stops polling `octoprint:job`; stale `count` on `reason`, `printer` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `count` over `1w` is expected for UC-25.17.2, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `octoprint:job`. Dedup with `stats latest(*) by reason`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Print Failure and Reason Breakdown'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Making, 3D Printing & Workshop — typical feeds: Print jobs (`octoprint:job`), printer firmware telemetry (`printer:telemetry`), filament spools (`filament:spool`), CNC/laser jobs (`cnc:job`), workshop air (`workshop:air`), cordless tool batteries (.\n• **Sourcetypes in this search**: `octoprint:job`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=octoprint:job`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** OctoPrint / Klipper (Moonraker) print servers, printer firmware telemetry, filament spool tracking, CNC/laser job logs, and workshop air/tool sensors via HEC and MQTT..\n\n**Author notes:** Tag failed jobs with a reason; schedule weekly and review the leading failure causes per printer.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"octoprint:job\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=octoprint:job earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Print Failure and Reason Breakdown** over `personal` data (primarily `sourcetype=octoprint:job`) across the full search range. The `aggregation` aggregation groups by `reason`, `printer`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `printer`, `reason`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=octoprint:job result=failed\n| stats count as failures by reason printer\n| sort - failures\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=octoprint:job earliest=-7d\n| head 50\n| table printer, reason\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=octoprint:job earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.17.2: Personal evidence` with the following panel layout:\n\n**Panel 1 — Pie Chart Of Print Failures By Reason (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `octoprint:job` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=octoprint:job | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It sorts your failed prints by what went wrong, so you can fix the most common problem first.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
