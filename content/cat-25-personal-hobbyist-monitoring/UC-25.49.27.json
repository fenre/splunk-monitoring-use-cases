{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.49.27",
  "title": "Outfit Photo Logging Freshness",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Outfit Photo Logging Freshness' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_outfit_photo_logging_freshness` (UC-25.49.27), SOC dashboard panel \"Personal — Outfit Photo Logging Freshness\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.49.27 positive (Outfit, Photo, Logging, Freshness): ingest synthetic events into `index=personal` sourcetype='style:wardrobe' where `avg_lag_hours`, `worst_lag_hours` grouped by `source_name` satisfies detection threshold for 'Outfit Photo Logging Freshness'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.49.27 negative (Logging Freshness): ingest benign `personal` / `style:wardrobe` events where `source_name` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 4 confirms zero alert rows for 'Outfit Photo Logging Freshness'."
  },
  "dataSources": "Closet activity (`style:wardrobe`), cleaning tasks (`style:cleaning`), shoe wears (`style:shoes`), beauty inventory (`beauty:product`), audits (`style:audit`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Wardrobe and beauty telemetry — Stylebook closet exports, dry-cleaning reminders, shoe-rotation logs, and makeup inventory audits — streamed to Splunk HEC via exports and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=style:wardrobe source_name=* worn_epoch=* photo_logged_epoch=*\n| eval lag_hours=round((photo_logged_epoch-worn_epoch)/3600,1)\n| stats avg(lag_hours) as avg_lag_hours, max(lag_hours) as worst_lag_hours by source_name\n| sort - avg_lag_hours",
  "description": "Alerts on risk tied to Outfit, Photo, Logging, Freshness through UC-25.49.27 on `index=personal` sourcetype `style:wardrobe`. Summarises `avg_lag_hours`, `worst_lag_hours` grouped by `source_name` across `the scheduled window` before applying configured threshold. Operators tune 'Outfit Photo Logging Freshness' using UC-25.49.27 runbook steps.",
  "value": "Tracking `avg_lag_hours`, `worst_lag_hours` via UC-25.49.27 on `style:wardrobe` feeds improves response to 'Outfit Photo Logging Freshness' and reduces missed training goals, stale hobby data, and connector failures.",
  "implementation": "Capture the timestamps needed to compute `outfit photo lag` on `style:wardrobe` events and aggregate them by `source_name`.",
  "visualization": "Table of capture source with average and worst outfit photo lag.",
  "cimModels": [],
  "references": [
    {
      "title": "Stylebook",
      "url": "https://stylebookapp.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Google Calendar",
      "url": "https://calendar.google.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Notion",
      "url": "https://www.notion.so/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.49.27)** — Connector for 'Outfit Photo Logging Freshness' stops polling `style:wardrobe`; stale `avg_lag_hours`, `worst_lag_hours` on `source_name` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `avg_lag_hours`, `worst_lag_hours` over `1w` is expected for UC-25.49.27, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `style:wardrobe`. Dedup with `stats latest(*) by source_name`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Outfit Photo Logging Freshness'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Wardrobe, Fashion & Beauty — typical feeds: Wardrobe items (`wardrobe:item`), outfit wears (`outfit:wear`), skincare routines (`skincare:routine`), laundry cycles (`laundry:cycle`)..\n• **Sourcetypes in this search**: `style:wardrobe`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=style:wardrobe`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Wardrobe and grooming logs — closet apps (Whering, Stylebook), cost-per-wear trackers, skincare-routine logs, and smart-laundry cycles — streamed to Splunk HEC via APIs and scripted inputs..\n\n**Author notes:** Capture the timestamps needed to compute `outfit photo lag` on `style:wardrobe` events and aggregate them by `source_name`.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"style:wardrobe\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=style:wardrobe earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Outfit Photo Logging Freshness** over `personal` data (primarily `sourcetype=style:wardrobe`) across the full search range. The `avg, max` aggregation groups by `source_name`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `source_name`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=style:wardrobe source_name=* worn_epoch=* photo_logged_epoch=*\n| eval lag_hours=round((photo_logged_epoch-worn_epoch)/3600,1)\n| stats avg(lag_hours) as avg_lag_hours, max(lag_hours) as worst_lag_hours by source_name\n| sort - avg_lag_hours\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=style:wardrobe earliest=-7d\n| head 50\n| table source_name\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=style:wardrobe earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.49.27: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Capture Source With Average And Worst Outfit Photo Lag (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `style:wardrobe` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=style:wardrobe | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It reveals how long things take for each capture source so you can see where outfit photo logging slows down.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
