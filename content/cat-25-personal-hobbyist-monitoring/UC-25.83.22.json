{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.83.22",
  "title": "Pendant Alert Test Reliability Low-Level Alert",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Safety",
    "Availability"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Pendant Alert Test Reliability Low-Level Alert' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_pendant_alert_test_reliability_low_level_alert` (UC-25.83.22), SOC dashboard panel \"Personal — Pendant Alert Test Reliability Low-Level Alert\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.83.22 positive (Pendant, Alert, Test, Reliability, Low): ingest synthetic events into `index=personal` sourcetype='pendant:test' where `count` grouped by `pendant_id` satisfies `metric_value` < 70.0 for 'Pendant Alert Test Reliability Low-Level Alert'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.83.22 negative (Level Alert): ingest benign `personal` / `pendant:test` events where `pendant_id` stays below `metric_value` < 70.0, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 1 confirms zero alert rows for 'Pendant Alert Test Reliability Low-Level Alert'."
  },
  "dataSources": "Mobility-aid status (`mobilityaid:status`), wheelchair battery telemetry (`wheelchair:battery`), stairlift service records (`stairlift:service`), hearing-aid battery records (`hearingaid:battery`), pendant-alert tests (`pendant:test`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Wheelchair battery telemetry, stairlift service records, hearing-aid battery reminders, and pendant-alert test logs streamed to Splunk HEC via apps, service portals, and caregiver forms. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=pendant:test\n| stats latest(signal_strength_pct) as metric_value by pendant_id\n| where metric_value<70.0\n| sort metric_value",
  "description": "Alerts on Level Alert activity in UC-25.83.22 ('Pendant Alert Test Reliability Low-Level Alert') from `index=personal` sourcetype `pendant:test`. Rolls up `count` by `pendant_id` across `the scheduled window` and pages when `metric_value` < 70.0. Covers Pendant, Alert, Test, Reliability, Low. Personal operators treat UC-25.83.22 as the canonical signal.",
  "value": "Operators running UC-25.83.22 on `pendant:test` feeds shorten triage for Level Alert tied to 'Pendant Alert Test Reliability Low-Level Alert', lowering missed training goals, stale hobby data, and connector failures.",
  "implementation": "Tune the threshold to the minimum safe operating level for each device family.",
  "visualization": "Table of devices with current signal strength percentage.",
  "cimModels": [],
  "references": [
    {
      "title": "NIDCD - Hearing aids",
      "url": "https://www.nidcd.nih.gov/health/hearing-aids",
      "retrieved": "2026-04-25"
    },
    {
      "title": "MedlinePlus - Wheelchairs",
      "url": "https://medlineplus.gov/wheelchairsandmobilityaids.html",
      "retrieved": "2026-04-25"
    },
    {
      "title": "ADA National Network",
      "url": "https://adata.org/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.83.22)** — Connector for 'Pendant Alert Test Reliability Low-Level Alert' stops polling `pendant:test`; stale `count` on `pendant_id` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `count` over `1w` is expected for UC-25.83.22, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `pendant:test`. Dedup with `stats latest(*) by pendant_id`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `metric_value` < 70.0 for 'Pendant Alert Test Reliability Low-Level Alert'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Mobility & Accessibility Aids — typical feeds: Mobility-aid status (`mobilityaid:status`), wheelchair battery telemetry (`wheelchair:battery`), stairlift service records (`stairlift:service`), hearing-aid battery records (`hearingaid:battery`), pe.\n• **Sourcetypes in this search**: `pendant:test`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=pendant:test`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Wheelchair battery telemetry, stairlift service records, hearing-aid battery reminders, and pendant-alert test logs streamed to Splunk HEC via apps, service portals, and caregiver forms..\n\n**Author notes:** Tune the threshold to the minimum safe operating level for each device family.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"pendant:test\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=pendant:test earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Pendant Alert Test Reliability Low-Level Alert** over `personal` data (primarily `sourcetype=pendant:test`) across the full search range. It applies numeric thresholds on `metric_value` (< 70.0) after latest — tune these constants to your household baseline.\n\n**Fields this search depends on:** `metric_value`, `pendant_id`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=pendant:test\n| stats latest(signal_strength_pct) as metric_value by pendant_id\n| where metric_value<70.0\n| sort metric_value\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=pendant:test earliest=-7d\n| head 50\n| table metric_value, pendant_id\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=pendant:test earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `metric_value` before alerting:\n\n```spl\nindex=personal sourcetype=pendant:test earliest=-30d\n| stats perc50(metric_value) AS p50 perc90(metric_value) AS p90 perc99(metric_value) AS p99 max(metric_value) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.83.22: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Devices With Current Signal Strength Percentage (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `pendant:test` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=pendant:test | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It nudges which pendant alert test reliability devices are getting low enough to need attention soon.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
