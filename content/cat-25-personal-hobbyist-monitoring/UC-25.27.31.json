{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.27.31",
  "title": "Thermostat Probe Disagreement by Enclosure",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics",
    "Risk"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Thermostat Probe Disagreement by Enclosure' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_thermostat_probe_disagreement_by_enclosure` (UC-25.27.31), SOC dashboard panel \"Personal — Thermostat Probe Disagreement by Enclosure\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.27.31 positive (Thermostat, Probe, Disagreement, Enclosure): ingest synthetic events into `index=personal` sourcetype='reptilethermostat:event' where `avg_probe_delta_c`, `max_probe_delta_c` grouped by `enclosure_name` satisfies detection threshold for 'Thermostat Probe Disagreement by Enclosure'. Validate filter `probe_delta_c=*`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.27.31 negative (by Enclosure): ingest benign `personal` / `reptilethermostat:event` events where `enclosure_name` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 0 confirms zero alert rows for 'Thermostat Probe Disagreement by Enclosure'."
  },
  "dataSources": "Apex controller readings (`apex:reading`), Seneye measurements (`seneye:reading`), reptile thermostat events (`reptilethermostat:event`), and misting-system cycles (`mister:cycle`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Aquarium and terrarium telemetry from Neptune Apex, Seneye, reptile thermostats, and misting systems sent to Splunk HEC via controller APIs, MQTT, and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=reptilethermostat:event\n| search probe_delta_c=*\n| stats avg(probe_delta_c) as avg_probe_delta_c max(probe_delta_c) as max_probe_delta_c by enclosure_name\n| sort 0 - avg_probe_delta_c",
  "description": "Detects 'Thermostat Probe Disagreement by Enclosure' (UC-25.27.31) using `index=personal` sourcetype `reptilethermostat:event` via filter `probe_delta_c=*` by aggregating `avg_probe_delta_c`, `max_probe_delta_c` grouped by `enclosure_name` over `the scheduled window` and alerting when configured threshold. Keywords: Thermostat, Probe, Disagreement, Enclosure. Focuses on by Enclosure behaviour so personal teams can triage UC-25.27.31 without ad-hoc log review.",
  "value": "UC-25.27.31 accelerates insight into by Enclosure ('Thermostat Probe Disagreement by Enclosure') by tracking `avg_probe_delta_c`, `max_probe_delta_c` on `reptilethermostat:event` feeds, reducing missed training goals, stale hobby data, and connector failures before spreadsheet checks hide regressions.",
  "implementation": "Send probe disagreement events to `index=personal` and keep `enclosure_name` populated so the leaderboard stays actionable.",
  "visualization": "Bar chart of probe disagreement by enclosures.",
  "cimModels": [],
  "references": [
    {
      "title": "Neptune Apex",
      "url": "https://www.neptunesystems.com/apex/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Seneye",
      "url": "https://seneye.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Herpstat",
      "url": "https://spyderrobotics.com/index.php?main_page=index&cPath=1",
      "retrieved": "2026-04-25"
    },
    {
      "title": "MistKing",
      "url": "https://mistking.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.27.31)** — Connector for 'Thermostat Probe Disagreement by Enclosure' stops polling `reptilethermostat:event`; stale `avg_probe_delta_c`, `max_probe_delta_c` on `enclosure_name` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `avg_probe_delta_c`, `max_probe_delta_c` over `1w` is expected for UC-25.27.31, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `reptilethermostat:event`. Dedup with `stats latest(*) by enclosure_name`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Thermostat Probe Disagreement by Enclosure'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Aquariums, Reptiles & Vivariums — typical feeds: Reef-tank parameters (`reeftank:param`), vivarium climate (`vivarium:climate`), fish feeder (`fishfeeder:event`), auto-doser (`doser:event`), auto-top-off (`ato:event`), pond sensors (`pondsensor:read.\n• **Sourcetypes in this search**: `reptilethermostat:event`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=reptilethermostat:event`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Aquarium and vivarium controllers — reef-tank parameter monitors, auto-dosers, auto-top-off, reptile terrarium climate/UVB, and pond sensors — via MQTT and controller APIs (Apex, GHL, ESPHome)..\n\n**Author notes:** Send probe disagreement events to `index=personal` and keep `enclosure_name` populated so the leaderboard stays actionable.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"reptilethermostat:event\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=reptilethermostat:event earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Thermostat Probe Disagreement by Enclosure** over `personal` data (primarily `sourcetype=reptilethermostat:event`) across the full search range. The `avg, max` aggregation groups by `enclosure_name`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `enclosure_name`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=reptilethermostat:event\n| search probe_delta_c=*\n| stats avg(probe_delta_c) as avg_probe_delta_c max(probe_delta_c) as max_probe_delta_c by enclosure_name\n| sort 0 - avg_probe_delta_c\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=reptilethermostat:event earliest=-7d\n| head 50\n| table enclosure_name\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=reptilethermostat:event earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.27.31: Personal evidence` with the following panel layout:\n\n**Panel 1 — Bar Chart Of Probe Disagreement By Enclosures (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `reptilethermostat:event` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=reptilethermostat:event | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It gathers which enclosuress stand out most for probe disagreement.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
