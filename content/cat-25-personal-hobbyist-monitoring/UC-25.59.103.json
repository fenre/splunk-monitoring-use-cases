{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.59.103",
  "title": "Habitica Party Check-In Reliability",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Habitica Party Check-In Reliability' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_habitica_party_check_in_reliability` (UC-25.59.103), SOC dashboard panel \"Personal — Habitica Party Check-In Reliability\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.59.103 positive (Habitica, Party, Check, Reliability): ingest synthetic events into `index=personal` sourcetype='lifeos:habit' where `sum` grouped by `party_name` satisfies detection threshold for 'Habitica Party Check-In Reliability'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.59.103 negative (In Reliability): ingest benign `personal` / `lifeos:habit` events where `party_name` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 8 confirms zero alert rows for 'Habitica Party Check-In Reliability'."
  },
  "dataSources": "Habit activity (`lifeos:habit`), streaks (`lifeos:streak`), Beeminder goals (`lifeos:beeminder`), OKRs (`lifeos:okr`), planning syncs (`lifeos:ops`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Personal life-OS telemetry — Habitica quest logs, Streaks app habit chains, Beeminder goal data, and personal OKR reviews — streamed to Splunk HEC via APIs, exports, and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=lifeos:habit party_name=* checkin_done=*\n| stats count as total, sum(eval(if(checkin_done=\"yes\",1,0))) as matched by party_name\n| eval a_completed_party_check_in_pct=round(100*matched/total,1)\n| sort - a_completed_party_check_in_pct",
  "description": "Alerts on UC-25.59.103 — Habitica, Party, Check, Reliability — using `index=personal` sourcetype `lifeos:habit`, grouped by `party_name`. Outputs `sum` each `the scheduled window`; alert when configured threshold. Primary focus: In Reliability. Purpose-built for 'Habitica Party Check-In Reliability' in Personal workflows.",
  "value": "UC-25.59.103 accelerates insight into In Reliability ('Habitica Party Check-In Reliability') by tracking `activity` on `lifeos:habit` feeds, reducing missed training goals, stale hobby data, and connector failures before spreadsheet checks hide regressions.",
  "implementation": "Store a normalized flag for `a completed party check-in` on `lifeos:habit` events and summarize the hit rate by `party_name`.",
  "visualization": "Table of party with percentage of party coordination ending with a completed party check-in.",
  "cimModels": [],
  "references": [
    {
      "title": "Habitica",
      "url": "https://habitica.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Streaks",
      "url": "https://streaksapp.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Beeminder",
      "url": "https://www.beeminder.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Notion",
      "url": "https://www.notion.so/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.59.103)** — Connector for 'Habitica Party Check-In Reliability' stops polling `lifeos:habit`; stale `sum` on `party_name` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `sum` over `1w` is expected for UC-25.59.103, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `lifeos:habit`. Dedup with `stats latest(*) by party_name`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Habitica Party Check-In Reliability'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Personal Life-OS & Gamification — typical feeds: Life score (`lifeos:score`), quests / tasks (`quest:task`), habit streaks (`streak:status`), life achievements (`achievement:life`), household leaderboard (`household:leaderboard`), plus every other `.\n• **Sourcetypes in this search**: `lifeos:habit`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=lifeos:habit`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** The parody-the-enterprise meta layer — a Personal NOC (house-health wall), Personal SOC (entry / impossible-travel), Personal SRE (SLOs on your own habits), and a gamification layer (XP, achievements, streaks, household leaderboards) — all computed in Splunk over `index=personal`..\n\n**Author notes:** Store a normalized flag for `a completed party check-in` on `lifeos:habit` events and summarize the hit rate by `party_name`.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"lifeos:habit\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=lifeos:habit earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Habitica Party Check-In Reliability** over `personal` data (primarily `sourcetype=lifeos:habit`) across the full search range. The `sum` aggregation groups by `party_name`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `party_name`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=lifeos:habit party_name=* checkin_done=*\n| stats count as total, sum(eval(if(checkin_done=\"yes\",1,0))) as matched by party_name\n| eval a_completed_party_check_in_pct=round(100*matched/total,1)\n| sort - a_completed_party_check_in_pct\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=lifeos:habit earliest=-7d\n| head 50\n| table party_name\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=lifeos:habit earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.59.103: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Party With Percentage Of Party Coordination Ending With A Completed Party Check-In (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `lifeos:habit` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=lifeos:habit | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It gathers how often a completed party check-in happens for each party so you can see where party coordination go smoothly or get stuck.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
