{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.10.85",
  "title": "Microseism Trend vs Storm Surf",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Microseism Trend vs Storm Surf' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_microseism_trend_vs_storm_surf` (UC-25.10.85), SOC dashboard panel \"Personal — Microseism Trend vs Storm Surf\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.10.85 positive (Microseism, Trend, Storm, Surf): ingest synthetic events into `index=personal` sourcetype='seismograph:noise' where `microseism_db`, `wind_kph` grouped by `entity` satisfies detection threshold for 'Microseism Trend vs Storm Surf'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.10.85 negative (Storm Surf): ingest benign `personal` / `seismograph:noise` events where `entity` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 13 confirms zero alert rows for 'Microseism Trend vs Storm Surf'."
  },
  "dataSources": "Seismograph events (`seismograph:event`), seismic noise (`seismograph:noise`), air quality (`airquality:reading`), radiation counts (`radiation:count`), lightning strikes (`lightning:strike`), phenology observations (`phenology:observation`), and weather-station rainfall (`weatherstation:reading`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Citizen-science telemetry from seismographs, weather stations, air-quality monitors, Geiger counters, lightning feeds, and phenology journals streamed to Splunk HEC via MQTT, APIs, and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=seismograph:noise\n| timechart span=1d avg(microseism_db) as microseism_db avg(wind_kph) as wind_kph",
  "description": "Monitors UC-25.10.85 — Microseism, Trend, Storm, Surf — using `index=personal` sourcetype `seismograph:noise`, grouped by `entity`. Outputs `microseism_db`, `wind_kph` each `1d`; alert when configured threshold. Primary focus: Storm Surf. Purpose-built for 'Microseism Trend vs Storm Surf' in Personal workflows.",
  "value": "UC-25.10.85 gives Personal teams earlier visibility into Storm Surf through `seismograph:noise` feeds, lowering missed training goals, stale hobby data, and connector failures.",
  "implementation": "Capture daily noise-floor summaries and enrich them with local wind or swell context from your weather feed.",
  "visualization": "Dual-axis line chart of microseism level and wind speed.",
  "cimModels": [],
  "references": [
    {
      "title": "Raspberry Shake - citizen seismograph",
      "url": "https://raspberryshake.org/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Safecast - open radiation data",
      "url": "https://api.safecast.org/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.10.85)** — Connector for 'Microseism Trend vs Storm Surf' stops polling `seismograph:noise`; stale `microseism_db`, `wind_kph` on `external_id` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `microseism_db`, `wind_kph` over `1d` is expected for UC-25.10.85, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `seismograph:noise`. Dedup with `stats latest(*) by external_id`.\n\n4. **Timezone bucket misalignment** — `1d` bins on UTC vs local schedule skew goal threshold for 'Microseism Trend vs Storm Surf'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Citizen Science & Backyard Sensing — typical feeds: Seismograph (`seismo:reading`), Geiger counter (`geiger:cpm`), lightning strikes (`lightning:strike`), radon (`radon:reading`), all-sky meteor camera (`allsky:capture`), river-level gauge (`riverlevel.\n• **Sourcetypes in this search**: `seismograph:noise`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=seismograph:noise`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** DIY citizen-science sensors — Raspberry Shake seismograph, Geiger counter, AS3935/Blitzortung lightning detector, radon monitor, all-sky meteor camera, river-level gauge, and magnetometer — via MQTT and scripted inputs to Splunk HEC..\n\n**Author notes:** Capture daily noise-floor summaries and enrich them with local wind or swell context from your weather feed.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"seismograph:noise\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=seismograph:noise earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Microseism Trend vs Storm Surf** over `personal` data (primarily `sourcetype=seismograph:noise`) across the full search range. The `avg` aggregation groups by the time bucket; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n```spl\nindex=personal sourcetype=seismograph:noise\n| timechart span=1d avg(microseism_db) as microseism_db avg(wind_kph) as wind_kph\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=seismograph:noise earliest=-7d\n| head 50\n| table _time, sourcetype, host\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=seismograph:noise earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.10.85: Personal evidence` with the following panel layout:\n\n**Panel 1 — Dual-Axis Line Chart Of Microseism Level And Wind Speed (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `seismograph:noise` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=seismograph:noise | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=1d` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1d` buckets.",
  "grandmaExplanation": "It warns how the steady background earth rumble rises and falls with stormy conditions far away.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
