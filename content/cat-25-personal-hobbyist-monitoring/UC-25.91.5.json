{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.91.5",
  "title": "Slopes Ski Session Daily Event Volume",
  "criticality": "low",
  "difficulty": "beginner",
  "monitoringType": [
    "Analytics",
    "Operations"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Slopes Ski Session Daily Event Volume' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_slopes_ski_session_daily_event_volume` (UC-25.91.5), SOC dashboard panel \"Personal — Slopes Ski Session Daily Event Volume\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.91.5 positive (Slopes, Ski, Session, Daily, Event): ingest synthetic events into `index=personal` sourcetype='slopes:session' where `distinct_entities` grouped by `_time` satisfies detection threshold for 'Slopes Ski Session Daily Event Volume'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.91.5 negative (Event Volume): ingest benign `personal` / `slopes:session` events where `_time` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 7 confirms zero alert rows for 'Slopes Ski Session Daily Event Volume'."
  },
  "dataSources": "Slopes ski sessions (`slopes:session`), Ski Tracks runs (`skitracks:run`), OnTheSnow resort reports (`onthesnow:report`), snow safety checks (`snow:safety`), and Ikon Pass lift scans (`ikon:pass`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Slopes ski sessions, Ski Tracks GPS runs, OnTheSnow resort reports, snow safety checklist logs, and Ikon Pass scan exports forwarded to Splunk HEC. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=slopes:session\n| bin _time span=1d\n| stats count as events, dc(resort_name) as distinct_entities by _time\n| sort - _time",
  "description": "Monitors UC-25.91.5 — Slopes, Ski, Session, Daily, Event — using `index=personal` sourcetype `slopes:session`, grouped by `_time`. Outputs `distinct_entities` each `1d`; alert when configured threshold. Primary focus: Event Volume. Purpose-built for 'Slopes Ski Session Daily Event Volume' in Personal workflows.",
  "value": "UC-25.91.5 operationalises 'Slopes Ski Session Daily Event Volume' against `slopes:session` feeds so operators reduce missed training goals, stale hobby data, and connector failures without manual correlation.",
  "implementation": "Ingest Slopes ski session records into `index=personal`; schedule daily and compare count and distinct resort values against your normal baseline.",
  "visualization": "Column chart of daily event count with a line for distinct entities.",
  "cimModels": [],
  "references": [
    {
      "title": "Slopes",
      "url": "https://getslopes.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Ski Tracks",
      "url": "https://www.corecoders.com/skitracks/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "OnTheSnow",
      "url": "https://www.onthesnow.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Ikon Pass",
      "url": "https://www.ikonpass.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.91.5)** — Connector for 'Slopes Ski Session Daily Event Volume' stops polling `slopes:session`; stale `distinct_entities` on `_time` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `distinct_entities` over `1d` is expected for UC-25.91.5, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `slopes:session`. Dedup with `stats latest(*) by _time`.\n\n4. **Timezone bucket misalignment** — `1d` bins on UTC vs local schedule skew goal threshold for 'Slopes Ski Session Daily Event Volume'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "servicenow"
  ],
  "equipmentModels": [
    "servicenow_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Snow Sports & Winter Recreation — typical feeds: Slopes ski sessions (`slopes:session`), Ski Tracks runs (`skitracks:run`), OnTheSnow resort reports (`onthesnow:report`), snow safety checks (`snow:safety`), and Ikon Pass lift scans (`ikon:pass`)..\n• **Sourcetypes in this search**: `slopes:session`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=slopes:session`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Slopes ski sessions, Ski Tracks GPS runs, OnTheSnow resort reports, snow safety checklist logs, and Ikon Pass scan exports forwarded to Splunk HEC..\n\n**Author notes:** Ingest Slopes ski session records into `index=personal`; schedule daily and compare count and distinct resort values against your normal baseline.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"slopes:session\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=slopes:session earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Slopes Ski Session Daily Event Volume** over `personal` data (primarily `sourcetype=slopes:session`) across the full search range. The `dc` aggregation groups by `_time`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `_time`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=slopes:session\n| bin _time span=1d\n| stats count as events, dc(resort_name) as distinct_entities by _time\n| sort - _time\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=slopes:session earliest=-7d\n| head 50\n| table _time\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=slopes:session earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.91.5: Personal evidence` with the following panel layout:\n\n**Panel 1 — Column Chart Of Daily Event Count With A Line For Distinct Entities (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `slopes:session` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=slopes:session | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=1d` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1d` buckets.",
  "grandmaExplanation": "It warns how much Slopes ski session activity showed up each day so you can see if the feed went quiet or suddenly got busy.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
