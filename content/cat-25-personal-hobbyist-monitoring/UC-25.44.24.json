{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.44.24",
  "title": "iNaturalist Observation Confidence by Species",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'iNaturalist Observation Confidence by Species' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_inaturalist_observation_confidence_by_species` (UC-25.44.24), SOC dashboard panel \"Personal — iNaturalist Observation Confidence by Species\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.44.24 positive (iNaturalist, Observation, Confidence, Species): ingest synthetic events into `index=personal` sourcetype='forage:observation' where `sum` grouped by `species` satisfies detection threshold for 'iNaturalist Observation Confidence by Species'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.44.24 negative (by Species): ingest benign `personal` / `forage:observation` events where `species` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 3 confirms zero alert rows for 'iNaturalist Observation Confidence by Species'."
  },
  "dataSources": "Catch trips (`outdoor:trip`), hunt map activity (`hunt:map`), foraging observations (`forage:observation`), smoker sessions (`cook:smoker`), harvest queues (`outdoor:queue`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Fishing, hunting, foraging, and outdoor-cooking telemetry — Fishbrain catch logs, onX Hunt map exports, iNaturalist observations, and ThermoWorks smoker probes — streamed to Splunk HEC via APIs, exports, and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=forage:observation species=* community_id_agrees=*\n| stats count as total, sum(eval(if(community_id_agrees=\"yes\",1,0))) as matched by species\n| eval community_agreement_pct=round(100*matched/total,1)\n| sort - community_agreement_pct",
  "description": "Identifies when by Species criteria fire for 'iNaturalist Observation Confidence by Species' (UC-25.44.24) in `index=personal` sourcetype `forage:observation`. Pipeline aggregates `sum` by `species` over `the scheduled window`. Alert logic enforces configured threshold. Title terms: iNaturalist, Observation, Confidence, Species. Eliminates manual hunting for UC-25.44.24 conditions.",
  "value": "UC-25.44.24 helps teams contain iNaturalist, Observation, Confidence, Species scenarios and reduce missed training goals, stale hobby data, and connector failures before incidents compound.",
  "implementation": "Store a normalized flag for `community agreement` on `forage:observation` events and summarize the hit rate by `species`.",
  "visualization": "Table of species with percentage of foraging observations ending with community agreement.",
  "cimModels": [],
  "references": [
    {
      "title": "Fishbrain — fishing app",
      "url": "https://fishbrain.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "onX Hunt — maps",
      "url": "https://www.onxmaps.com/hunt/app",
      "retrieved": "2026-04-25"
    },
    {
      "title": "iNaturalist — observations",
      "url": "https://www.inaturalist.org/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "ThermoWorks Signals — smoker telemetry",
      "url": "https://www.thermoworks.com/signals/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.44.24)** — Connector for 'iNaturalist Observation Confidence by Species' stops polling `forage:observation`; stale `sum` on `species` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `sum` over `1w` is expected for UC-25.44.24, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `forage:observation`. Dedup with `stats latest(*) by species`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'iNaturalist Observation Confidence by Species'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "dell_emc"
  ],
  "equipmentModels": [
    "dell_emc_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Fishing, Hunting & Foraging — typical feeds: Fishing catches (`fishing:catch`), fishfinder sonar (`fishfinder:reading`), foraging finds (`foraging:find`), game-cam triggers (`gamecam:trigger`)..\n• **Sourcetypes in this search**: `forage:observation`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=forage:observation`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Outdoor logs — fishing apps (Fishbrain), sonar / fishfinder exports, game/trail cameras, and foraging journals — streamed to Splunk HEC via APIs and scripted inputs..\n\n**Author notes:** Store a normalized flag for `community agreement` on `forage:observation` events and summarize the hit rate by `species`.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"forage:observation\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=forage:observation earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **iNaturalist Observation Confidence by Species** over `personal` data (primarily `sourcetype=forage:observation`) across the full search range. The `sum` aggregation groups by `species`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `species`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=forage:observation species=* community_id_agrees=*\n| stats count as total, sum(eval(if(community_id_agrees=\"yes\",1,0))) as matched by species\n| eval community_agreement_pct=round(100*matched/total,1)\n| sort - community_agreement_pct\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=forage:observation earliest=-7d\n| head 50\n| table species\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=forage:observation earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.44.24: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Species With Percentage Of Foraging Observations Ending With Community Agreement (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `forage:observation` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=forage:observation | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It reveals how often community agreement happens for each species so you can see where foraging observations go smoothly or get stuck.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
