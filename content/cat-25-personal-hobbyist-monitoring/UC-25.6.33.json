{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.6.33",
  "title": "Self-Powered Day by Month",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Cost",
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "dataSources": "Home energy telemetry (`powerwall:aggregate`, `enphase:production`, `solaredge:power`, `sense:device`, `emporia:circuit`, `dsmr:telegram`, `utilityrate:tariff`).",
  "app": "Tesla Powerwall, Enphase IQ, SolarEdge, Sense, Emporia Vue, DSMR / P1 smart meters, and utility tariff exports collected via vendor APIs, Home Assistant, MQTT, and Splunk HEC.",
  "spl": "index=personal sourcetype=powerwall:aggregate\n| bin _time span=1d\n| stats sum(eval(if(grid_kw>0,grid_kw,0))) as import_kw by _time\n| eval self_powered_day=if(import_kw<15,1,0)\n| where self_powered_day=1\n| sort - _time",
  "description": "Uses Powerwall aggregate telemetry telemetry to monitor which days stayed almost entirely self-powered in index=personal.",
  "value": "Splunk turns the feed into an operational check for months that are quietly improving energy independence, so the signal does not stay buried inside separate consumer apps or exports.",
  "implementation": "Ingest Powerwall aggregate telemetry data through its real API, export, MQTT bridge, or open-source connector into `index=personal`; preserve the fields referenced in the SPL and schedule the search on a daily cadence.",
  "visualization": "Daily table of near self-powered days.",
  "cimModels": [],
  "references": [
    {
      "title": "Tesla Powerwall",
      "url": "https://www.tesla.com/support/energy/powerwall"
    },
    {
      "title": "Enphase API",
      "url": "https://developer-v4.enphase.com/"
    },
    {
      "title": "SolarEdge Monitoring API",
      "url": "https://developers.solaredge.com/docs/monitoring-api/"
    },
    {
      "title": "Home Assistant Sense integration",
      "url": "https://www.home-assistant.io/integrations/sense/"
    },
    {
      "title": "Emporia Vue integration",
      "url": "https://github.com/magico13/ha-emporia-vue"
    },
    {
      "title": "DSMR Reader docs",
      "url": "https://dsmr-reader.readthedocs.io/"
    },
    {
      "title": "Home Assistant utility meter",
      "url": "https://www.home-assistant.io/integrations/utility_meter/"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "equipment": [
    "mqtt"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Home Energy & Solar — typical feeds: SolarEdge/Enphase production (`solaredge:power`, `enphase:production`), Tesla Powerwall (`powerwall:aggregate`), smart-meter P1/DSMR (`dsmr:telegram`), Emporia Vue circuits (`emporia:circuit`), Sense .\n• **Sourcetypes in this search**: `powerwall:aggregate`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=powerwall:aggregate`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Solar inverters (SolarEdge/Enphase/Fronius/Growatt), Tesla Powerwall, smart meters (P1/DSMR, Emporia Vue, Sense) via vendor APIs and MQTT to HEC..\n\n**Author notes:** Ingest Powerwall aggregate telemetry data through its real API, export, MQTT bridge, or open-source connector into `index=personal`; preserve the fields referenced in the SPL and schedule the search on a daily cadence.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"powerwall:aggregate\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=powerwall:aggregate earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Self-Powered Day by Month** over `personal` data (primarily `sourcetype=powerwall:aggregate`) across the full search range. It applies numeric thresholds on `grid_kw` (> 0) after sum — tune these constants to your household baseline.\n\n**Fields this search depends on:** `_time`, `grid_kw`, `import_kw`, `self_powered_day`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=powerwall:aggregate\n| bin _time span=1d\n| stats sum(eval(if(grid_kw>0,grid_kw,0))) as import_kw by _time\n| eval self_powered_day=if(import_kw<15,1,0)\n| where self_powered_day=1\n| sort - _time\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=powerwall:aggregate earliest=-7d\n| head 50\n| table _time, grid_kw, import_kw, self_powered_day\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=powerwall:aggregate earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `grid_kw` before alerting:\n\n```spl\nindex=personal sourcetype=powerwall:aggregate earliest=-30d\n| stats perc50(grid_kw) AS p50 perc90(grid_kw) AS p90 perc99(grid_kw) AS p99 max(grid_kw) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.6.33: Personal evidence` with the following panel layout:\n\n**Panel 1 — Daily Table Of Near Self-Powered Days (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `powerwall:aggregate` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=powerwall:aggregate | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=1d` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1d` buckets.",
  "grandmaExplanation": "It produces which days stayed almost entirely self-powered so you can spot months that are quietly improving energy independence before it becomes annoying.",
  "prerequisiteUseCases": [],
  "wave": "run"
}
