{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.22.9",
  "title": "Clear-Night Utilisation Rate",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Platform",
  "dataSources": "Observatory environment (`observatory:env`), sky-quality meter (`skyquality:reading`), imaging sessions (`imaging:session`), telescope mount (`mount:status`), cloud sensor (`allsky:cloud`).",
  "app": "Backyard observatory automation — roll-off-roof/dome controller, sky-quality meter, cloud sensor, telescope mount, and astrophotography capture software (N.I.N.A./Ekos) — logging to Splunk HEC via MQTT and scripted inputs.",
  "spl": "index=personal sourcetype=allsky:cloud\n| bin _time span=1d\n| stats sum(eval(if(sky_temp_c<-20,1,0))) as clear_readings, count as readings by _time\n| eval clear_pct=round(100*clear_readings/readings,1)\n| stats avg(clear_pct) as avg_clear",
  "description": "Measures what fraction of night hours were actually clear over time, a sobering but useful reality check for the hobby.",
  "value": "Knowing your true clear-sky percentage sets realistic expectations and helps justify remote or travel imaging plans.",
  "implementation": "Ingest cloud-sensor readings; schedule monthly and compute the share of clear night hours.",
  "visualization": "Single-value clear-sky percentage with a monthly trend.",
  "cimModels": [],
  "references": [
    {
      "title": "KStars/Ekos — observatory control",
      "url": "https://kstars.kde.org/"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "equipment": [
    "mqtt"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Backyard Astronomy & Observatory — typical feeds: Observatory environment (`observatory:env`), sky-quality meter (`skyquality:reading`), imaging sessions (`imaging:session`), telescope mount (`mount:status`), cloud sensor (`allsky:cloud`)..\n• **Sourcetypes in this search**: `allsky:cloud`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=allsky:cloud`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Backyard observatory automation — roll-off-roof/dome controller, sky-quality meter, cloud sensor, telescope mount, and astrophotography software (N.I.N.A./Ekos) — via MQTT and scripted inputs..\n\n**Author notes:** Ingest cloud-sensor readings; schedule monthly and compute the share of clear night hours.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"allsky:cloud\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=allsky:cloud earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Clear-Night Utilisation Rate** over `personal` data (primarily `sourcetype=allsky:cloud`) across the full search range. It applies numeric thresholds on `sky_temp_c` (< -20) after sum, avg — tune these constants to your household baseline.\n\n**Fields this search depends on:** `_time`, `sky_temp_c`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=allsky:cloud\n| bin _time span=1d\n| stats sum(eval(if(sky_temp_c<-20,1,0))) as clear_readings, count as readings by _time\n| eval clear_pct=round(100*clear_readings/readings,1)\n| stats avg(clear_pct) as avg_clear\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=allsky:cloud earliest=-7d\n| head 50\n| table _time, sky_temp_c\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=allsky:cloud earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `sky_temp_c` before alerting:\n\n```spl\nindex=personal sourcetype=allsky:cloud earliest=-30d\n| stats perc50(sky_temp_c) AS p50 perc90(sky_temp_c) AS p90 perc99(sky_temp_c) AS p99 max(sky_temp_c) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.22.9: Personal evidence` with the following panel layout:\n\n**Panel 1 — Single-Value Clear-Sky Percentage With A Monthly Trend (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `allsky:cloud` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=allsky:cloud | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=1d` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1d` buckets.",
  "grandmaExplanation": "It works out how often your nights are actually clear enough for stargazing, which is often fewer than you would hope.",
  "prerequisiteUseCases": [],
  "wave": "walk"
}
