{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.10.96",
  "title": "Raspberry Shake Event Detection vs Local Noise",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Physical",
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Raspberry Shake Event Detection vs Local Noise' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_raspberry_shake_event_detection_vs_local_noise` (UC-25.10.96), SOC dashboard panel \"Personal — Raspberry Shake Event Detection vs Local Noise\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.10.96 positive (Raspberry, Shake, Event, Detection, Local): ingest synthetic events into `index=personal` sourcetype='seismo:reading' where `noise_floor` grouped by `station`, `_time` satisfies `detected_events` > 0 for 'Raspberry Shake Event Detection vs Local Noise'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.10.96 negative (Local Noise): ingest benign `personal` / `seismo:reading` events where `station`, `_time` stays below `detected_events` > 0, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 4 confirms zero alert rows for 'Raspberry Shake Event Detection vs Local Noise'."
  },
  "dataSources": "Citizen-science telemetry (`seismo:reading`, `geiger:cpm`, `lightning:strike`, `airgradient:sensor`, `radon:reading`, `allsky:capture`, `magnetometer:reading`, `weatherstation:reading`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Raspberry Shake, Safecast, Blitzortung-compatible lightning sensors, AirGradient CO2 sensors, radon monitors, allsky cameras, and magnetometers streamed to Splunk HEC. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=seismo:reading\n| bin _time span=1d\n| stats avg(noise_floor) as noise_floor, count(eval(event_detected=1)) as detected_events by station _time\n| where detected_events>0\n| sort - detected_events",
  "description": "Identifies risk tied to Raspberry, Shake, Event, Detection, Local through UC-25.10.96 on `index=personal` sourcetype `seismo:reading`. Summarises `noise_floor` grouped by `station`, `_time` across `1d` before applying `detected_events` > 0. Operators tune 'Raspberry Shake Event Detection vs Local Noise' using UC-25.10.96 runbook steps.",
  "value": "Tracking `noise_floor` via UC-25.10.96 on `seismo:reading` feeds improves response to 'Raspberry Shake Event Detection vs Local Noise' and reduces missed training goals, stale hobby data, and connector failures.",
  "implementation": "Ingest Raspberry Shake telemetry data through its real API, export, MQTT bridge, or open-source connector into `index=personal`; preserve the fields referenced in the SPL and schedule the search on a hourly cadence.",
  "visualization": "Daily station table of noise floor and detected events.",
  "cimModels": [],
  "references": [
    {
      "title": "Raspberry Shake",
      "url": "https://raspberryshake.org/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Safecast",
      "url": "https://safecast.org/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Blitzortung",
      "url": "https://www.blitzortung.org/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "AirGradient documentation",
      "url": "https://www.airgradient.com/documentation/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Allsky",
      "url": "https://github.com/AllSkyCamera/allsky",
      "retrieved": "2026-04-25"
    },
    {
      "title": "RadonEye",
      "url": "https://radonftlab.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.10.96)** — Connector for 'Raspberry Shake Event Detection vs Local Noise' stops polling `seismo:reading`; stale `noise_floor` on `station`, `_time` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `noise_floor` over `1d` is expected for UC-25.10.96, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `seismo:reading`. Dedup with `stats latest(*) by station`.\n\n4. **Timezone bucket misalignment** — `1d` bins on UTC vs local schedule skew `detected_events` > 0 for 'Raspberry Shake Event Detection vs Local Noise'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Citizen Science & Backyard Sensing — typical feeds: Seismograph (`seismo:reading`), Geiger counter (`geiger:cpm`), lightning strikes (`lightning:strike`), radon (`radon:reading`), all-sky meteor camera (`allsky:capture`), river-level gauge (`riverlevel.\n• **Sourcetypes in this search**: `seismo:reading`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=seismo:reading`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** DIY citizen-science sensors — Raspberry Shake seismograph, Geiger counter, AS3935/Blitzortung lightning detector, radon monitor, all-sky meteor camera, river-level gauge, and magnetometer — via MQTT and scripted inputs to Splunk HEC..\n\n**Author notes:** Ingest Raspberry Shake telemetry data through its real API, export, MQTT bridge, or open-source connector into `index=personal`; preserve the fields referenced in the SPL and schedule the search on a hourly cadence.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"seismo:reading\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=seismo:reading earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Raspberry Shake Event Detection vs Local Noise** over `personal` data (primarily `sourcetype=seismo:reading`) across the full search range. It applies numeric thresholds on `detected_events` (> 0) after avg, count — tune these constants to your household baseline.\n\n**Fields this search depends on:** `_time`, `detected_events`, `station`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=seismo:reading\n| bin _time span=1d\n| stats avg(noise_floor) as noise_floor, count(eval(event_detected=1)) as detected_events by station _time\n| where detected_events>0\n| sort - detected_events\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=seismo:reading earliest=-7d\n| head 50\n| table _time, detected_events, station\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=seismo:reading earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `detected_events` before alerting:\n\n```spl\nindex=personal sourcetype=seismo:reading earliest=-30d\n| stats perc50(detected_events) AS p50 perc90(detected_events) AS p90 perc99(detected_events) AS p99 max(detected_events) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.10.96: Personal evidence` with the following panel layout:\n\n**Panel 1 — Daily Station Table Of Noise Floor And Detected Events (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `seismo:reading` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=seismo:reading | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=1d` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1d` buckets.",
  "grandmaExplanation": "It lists detected events alongside the local seismic noise floor so you can spot whether the sensor is seeing interesting events or just living in a loud spot before it becomes annoying.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
