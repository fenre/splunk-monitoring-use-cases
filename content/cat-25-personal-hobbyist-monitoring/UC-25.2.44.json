{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.2.44",
  "title": "Fitbit Sleep Log Daily Event Volume",
  "criticality": "low",
  "difficulty": "beginner",
  "monitoringType": [
    "Analytics",
    "Operations"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Fitbit Sleep Log Daily Event Volume' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_fitbit_sleep_log_daily_event_volume` (UC-25.2.44), SOC dashboard panel \"Personal — Fitbit Sleep Log Daily Event Volume\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=personal` sourcetype='fitbit:sleep' that satisfy the UC-25.2.44 SPL filters for 'Fitbit Sleep Log Daily Event Volume'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `personal` / `fitbit:sleep` events outside the detection threshold, or exclude the test entity via `personal_monitoring_notes.csv` with a valid window. Confirm zero alert rows for UC-25.2.44."
  },
  "dataSources": "Apple Health metrics (`apple:health`), Oura Ring daily readiness/sleep (`oura:daily`), Whoop recovery/strain (`whoop:cycle`), Withings body/blood-pressure (`withings:measure`), Dexcom CGM glucose (`dexcom:egv`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Apple Health / Apple Watch exports (Health Auto Export, Shortcuts) via HEC; Oura, Whoop, Withings, Dexcom APIs via scripted REST inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=fitbit:sleep\n| bin _time span=1d\n| stats count as events, dc(sleep_stage) as distinct_entities by _time\n| sort - _time",
  "description": "Counts how many Fitbit sleep log events land each day and how many distinct sleep stage values are represented.",
  "value": "Daily volume is the fastest way to spot a dead feed, a sudden burst, or a workflow that quietly changed.",
  "implementation": "Ingest Fitbit sleep log records into `index=personal`; schedule daily and compare count and distinct sleep stage values against your normal baseline.",
  "visualization": "Column chart of daily event count with a line for distinct entities.",
  "cimModels": [],
  "references": [
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Common Information Model",
      "url": "https://docs.splunk.com/Documentation/CIM",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry** — Vendor connector stops polling for 'Fitbit Sleep Log Daily Event Volume'; stale rows remain in `index=personal`. Check `_internal` HEC 401s and renew token in passwords.conf.\n\n2. **Rest day or planned break** — Zero activities is expected, not ingestion failure. Vendor app shows no sessions for the calendar day.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `fitbit:sleep`. Dedup with `stats latest(*) by external_id`.\n\n4. **Timezone bucket misalignment** — Weekly bins on UTC vs local schedule skew thresholds. Document offset in SPL `bin _time` or connector config.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Fitbit Web API — OAuth2 REST poll → HEC.\n• **Subcategory context**: Health, Sleep & Wearables — typical feeds: Apple Health metrics (`apple:health`), Oura Ring daily readiness/sleep (`oura:daily`), Whoop recovery/strain (`whoop:cycle`), Withings body/blood-pressure (`withings:measure`), Dexcom CGM glucose (`de.\n• **Sourcetypes in this search**: `fitbit:sleep`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Fitbit Web API** events into `index=personal` with `sourcetype=fitbit:sleep`.\n\n**Collection method:** OAuth2 REST poll → HEC.\n\n**Reference endpoint / path:** `GET /1/user/-/activities/list.json`.\n\n**Suggested poll interval:** 3600s.\n\n**Key fields to extract:** `activityName, distance, duration, startTime`.\n\n**Typical connector stack:** Apple Health / Apple Watch exports (Health Auto Export, Shortcuts) via HEC; Oura, Whoop, Withings, Dexcom APIs via scripted REST inputs..\n\n**Author notes:** Ingest Fitbit sleep log records into `index=personal`; schedule daily and compare count and distinct sleep stage values against your normal baseline.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"fitbit:sleep\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=fitbit:sleep earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Fitbit Sleep Log Daily Event Volume** over `personal` data (primarily `sourcetype=fitbit:sleep`) across the full search range. The `dc` aggregation groups by `_time`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `_time`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=fitbit:sleep\n| bin _time span=1d\n| stats count as events, dc(sleep_stage) as distinct_entities by _time\n| sort - _time\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Fitbit app → Exercise history. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=fitbit:sleep earliest=-7d\n| head 50\n| table _time\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=fitbit:sleep earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.2.44: Personal evidence` with the following panel layout:\n\n**Panel 1 — Column Chart Of Daily Event Count With A Line For Distinct Entities (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Fitbit Web API — no Fitbit Web API events / missing fields:** If `fitbit:sleep` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Fitbit Web API connector script is still running (`systemctl status` or cron log).\n\n**Fitbit Web API — NULL values in extracted fields:** The Fitbit Web API JSON export shape drifted — compare a raw event (`index=personal sourcetype=fitbit:sleep | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Fitbit Web API — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Fitbit Web API — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Fitbit Web API APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=1d` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1d` buckets.",
  "grandmaExplanation": "It catches how much Fitbit sleep log activity showed up each day so you can see if the feed went quiet or suddenly got busy.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
