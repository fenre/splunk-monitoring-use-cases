{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.21.55",
  "title": "Kerchunk Stream Status Sync Gap Watch",
  "criticality": "low",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability",
    "Data Quality"
  ],
  "splunkPillar": "Platform",
  "dataSources": "APRS packets (`aprs:packet`), WSPR spots (`wspr:spot`), satellite passes (`satpass:prediction`), NOAA APT captures (`noaa:apt`), meteor-scatter pings (`meteorscatter:ping`), SSTV images (`sstv:image`).",
  "app": "Software-defined radio and ham stations — APRS igate, WSPR beacon, NOAA weather-satellite (APT) receiver, meteor-scatter monitor, and SSTV decoder — logging to Splunk HEC via scripted inputs.",
  "spl": "index=personal sourcetype=kerchunk:stream\n| stats max(_time) as last_seen by stream_name\n| eval hours_since=round((now()-last_seen)/3600,1)\n| where hours_since>1\n| sort - hours_since",
  "description": "Detects when Kerchunk stream status data has stopped arriving for a given stream.",
  "value": "Personal telemetry is only useful when it is current; sync-gap alerts catch failed exports and broken credentials early.",
  "implementation": "Track the latest event time per stream and alert when the feed is older than 1 hours.",
  "visualization": "Table of entities by hours since last event.",
  "cimModels": [],
  "references": [
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "grandmaExplanation": "It charts when one part of your Kerchunk stream status feed has stopped checking in and probably needs fixing."
}
