{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.10.33",
  "title": "Aquarium Temperature Drift During Heat Wave",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Physical",
    "Anomaly"
  ],
  "splunkPillar": "Platform",
  "dataSources": "Weather and garden telemetry (`weatherflow:obs`, `ecowitt:obs`, `purpleair:aqi`, `airgradient:sensor`, `plant:sensor`, `aquarium:sensor`, `ambee:pollen`).",
  "app": "WeatherFlow Tempest, Ecowitt, PurpleAir, AirGradient, ESPHome plant sensors, aquarium controllers, and pollen feeds streamed to Splunk HEC via MQTT and scripted inputs.",
  "spl": "index=personal sourcetype=aquarium:sensor\n| stats latest(water_temp_c) as water_temp_c, max(room_temp_c) as room_temp_c by tank\n| where room_temp_c>28 AND (water_temp_c<23 OR water_temp_c>27)\n| sort - room_temp_c",
  "description": "Uses Aquarium climate telemetry telemetry to monitor tank temperature control during hot weather in index=personal.",
  "value": "Splunk turns the feed into an operational check for heat-wave days that stress fish or cooling gear, so the signal does not stay buried inside separate consumer apps or exports.",
  "implementation": "Ingest Aquarium climate telemetry data through its real API, export, MQTT bridge, or open-source connector into `index=personal`; preserve the fields referenced in the SPL and schedule the search on a daily cadence.",
  "visualization": "Tank table of water and room temperature.",
  "cimModels": [],
  "references": [
    {
      "title": "Tempest API",
      "url": "https://weatherflow.github.io/Tempest/api/"
    },
    {
      "title": "Ecowitt support",
      "url": "https://www.ecowitt.com/support.html"
    },
    {
      "title": "PurpleAir API",
      "url": "https://api2.purpleair.com/"
    },
    {
      "title": "AirGradient documentation",
      "url": "https://www.airgradient.com/documentation/"
    },
    {
      "title": "ESPHome",
      "url": "https://esphome.io/"
    },
    {
      "title": "Ambee pollen API",
      "url": "https://www.getambee.com/api/pollen"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "equipment": [
    "mqtt"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Weather, Environment & Garden — typical feeds: Weather-station observations (`ecowitt:obs`, `weatherflow:obs`), air-quality readings (`purpleair:aqi`, `airgradient:sensor`), soil-moisture/greenhouse sensors, aquarium controllers (`aquarium:sensor`.\n• **Sourcetypes in this search**: `aquarium:sensor`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=aquarium:sensor`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Personal weather stations (Ecowitt/Ambient/Davis/WeatherFlow), air-quality sensors (PurpleAir/AirGradient), and aquarium/greenhouse/soil sensors via HEC and MQTT..\n\n**Author notes:** Ingest Aquarium climate telemetry data through its real API, export, MQTT bridge, or open-source connector into `index=personal`; preserve the fields referenced in the SPL and schedule the search on a daily cadence.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"aquarium:sensor\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=aquarium:sensor earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Aquarium Temperature Drift During Heat Wave** over `personal` data (primarily `sourcetype=aquarium:sensor`) across the full search range. It applies numeric thresholds on `room_temp_c` (> 28) after latest, max — tune these constants to your household baseline.\n\n**Fields this search depends on:** `room_temp_c`, `tank`, `water_temp_c`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=aquarium:sensor\n| stats latest(water_temp_c) as water_temp_c, max(room_temp_c) as room_temp_c by tank\n| where room_temp_c>28 AND (water_temp_c<23 OR water_temp_c>27)\n| sort - room_temp_c\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=aquarium:sensor earliest=-7d\n| head 50\n| table room_temp_c, tank, water_temp_c\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=aquarium:sensor earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `room_temp_c` before alerting:\n\n```spl\nindex=personal sourcetype=aquarium:sensor earliest=-30d\n| stats perc50(room_temp_c) AS p50 perc90(room_temp_c) AS p90 perc99(room_temp_c) AS p99 max(room_temp_c) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.10.33: Personal evidence` with the following panel layout:\n\n**Panel 1 — Tank Table Of Water And Room Temperature (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `aquarium:sensor` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=aquarium:sensor | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It notices tank temperature control during hot weather so you can spot heat-wave days that stress fish or cooling gear before it becomes annoying.",
  "prerequisiteUseCases": [],
  "wave": "walk"
}
