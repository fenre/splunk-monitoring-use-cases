{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.5.1",
  "title": "Zigbee Sensor Low-Battery Roundup",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability",
    "Inventory"
  ],
  "splunkPillar": "Observability",
  "dataSources": "Zigbee2MQTT device reports (`zigbee2mqtt:device`), Shelly/Tasmota telemetry (`shelly:status`, `tasmota:sensor`), Hue events (`hue:event`), thermostats (`ecobee:thermostat`).",
  "app": "Philips Hue, smart plugs (Tasmota/Shelly/TP-Link Kasa), thermostats (Nest/Ecobee), locks, and Zigbee/Z-Wave sensors via MQTT / Zigbee2MQTT bridged to HEC.",
  "spl": "index=personal sourcetype=zigbee2mqtt:device\n| stats latest(battery) as battery, max(_time) as last_seen by friendly_name\n| where battery<20\n| eval battery=battery.\"%\"\n| sort battery",
  "description": "Rounds up every Zigbee sensor whose battery is running low so you can replace them all in one trip instead of one dead sensor at a time.",
  "value": "Scattered battery warnings are easy to ignore until a critical door or leak sensor dies; a single roundup makes battery care a five-minute chore.",
  "implementation": "Bridge Zigbee2MQTT device reports to HEC; schedule daily and alert when any battery drops below 20 percent.",
  "visualization": "Table of sensors by battery percentage, sorted lowest first.",
  "cimModels": [],
  "references": [
    {
      "title": "Zigbee2MQTT — Documentation",
      "url": "https://www.zigbee2mqtt.io/"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "equipment": [
    "f5",
    "mqtt"
  ],
  "equipmentModels": [
    "f5_asm"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Smart Home Devices & Sensors — typical feeds: Zigbee2MQTT device reports (`zigbee2mqtt:device`), Shelly/Tasmota telemetry (`shelly:status`, `tasmota:sensor`), Hue bridge events (`hue:event`), Nest/Ecobee thermostat (`ecobee:thermostat`), door/lea.\n• **Sourcetypes in this search**: `zigbee2mqtt:device`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=zigbee2mqtt:device`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Philips Hue, smart plugs (Tasmota/Shelly/TP-Link Kasa), thermostats (Nest/Ecobee), locks, and Zigbee/Z-Wave sensors via MQTT / Zigbee2MQTT and HEC..\n\n**Author notes:** Bridge Zigbee2MQTT device reports to HEC; schedule daily and alert when any battery drops below 20 percent.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"zigbee2mqtt:device\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=zigbee2mqtt:device earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Zigbee Sensor Low-Battery Roundup** over `personal` data (primarily `sourcetype=zigbee2mqtt:device`) across the full search range. It applies numeric thresholds on `battery` (< 20) after latest, max — tune these constants to your household baseline.\n\n**Fields this search depends on:** `battery`, `friendly_name`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=zigbee2mqtt:device\n| stats latest(battery) as battery, max(_time) as last_seen by friendly_name\n| where battery<20\n| eval battery=battery.\"%\"\n| sort battery\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=zigbee2mqtt:device earliest=-7d\n| head 50\n| table battery, friendly_name\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=zigbee2mqtt:device earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `battery` before alerting:\n\n```spl\nindex=personal sourcetype=zigbee2mqtt:device earliest=-30d\n| stats perc50(battery) AS p50 perc90(battery) AS p90 perc99(battery) AS p99 max(battery) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.5.1: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Sensors By Battery Percentage (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n**Panel 2 — Sorted Lowest First (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `zigbee2mqtt:device` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=zigbee2mqtt:device | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It lists every wireless sensor with a low battery, so you can change them all at once before any stop working.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
