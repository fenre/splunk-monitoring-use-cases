{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.54.30",
  "title": "Punch List Density by Room",
  "criticality": "low",
  "difficulty": "beginner",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Punch List Density by Room' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_punch_list_density_by_room` (UC-25.54.30), SOC dashboard panel \"Personal — Punch List Density by Room\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=personal` sourcetype='reno:punchlist' that satisfy the UC-25.54.30 SPL filters for 'Punch List Density by Room'. Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `personal` / `reno:punchlist` events outside the detection threshold, or exclude the test entity via `personal_monitoring_notes.csv` with a valid window. Confirm zero alert rows for UC-25.54.30."
  },
  "dataSources": "Schedules (`reno:gantt`), punch lists (`reno:punchlist`), photo logs (`reno:photo`), permits (`reno:permit`), project controls (`reno:ops`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Home-renovation project telemetry — Gantt plans, punch-list updates, inspection photo logs, and permit expiry trackers — streamed to Splunk HEC via builder-tool exports and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=reno:punchlist room=*\n| stats count as punch_list_items by room\n| sort - punch_list_items",
  "description": "Ranks room by punch-list items volume for room-level work. Runs scheduled SPL against `index=personal` to surface **Punch List Density by Room** so you can spot drift before it becomes a habit.",
  "value": "Volume rankings show which room are producing the most activity, demand, or friction inside room-level work.",
  "implementation": "Capture each punch-list items as a `reno:punchlist` event and normalize `room` so the totals are comparable.",
  "visualization": "Bar chart of punch-list items volume by room.",
  "cimModels": [],
  "references": [
    {
      "title": "Buildertrend",
      "url": "https://buildertrend.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Trello",
      "url": "https://trello.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Notion",
      "url": "https://www.notion.so/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry** — Vendor connector stops polling for 'Punch List Density by Room'; stale rows remain in `index=personal`. Check `_internal` HEC 401s and renew token in passwords.conf.\n\n2. **Rest day or planned break** — Zero activities is expected, not ingestion failure. Vendor app shows no sessions for the calendar day.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `reno:punchlist`. Dedup with `stats latest(*) by external_id`.\n\n4. **Timezone bucket misalignment** — Weekly bins on UTC vs local schedule skew thresholds. Document offset in SPL `bin _time` or connector config.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Home Renovation & Project Management — typical feeds: Renovation tasks (`reno:task`), budget and receipts (`reno:budget`), material orders (`reno:material`), inspections (`reno:inspection`), contractor logs (`reno:contractor`)..\n• **Sourcetypes in this search**: `reno:punchlist`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=reno:punchlist`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Home-renovation project management — task boards (Trello, Notion), budget and receipt trackers, material orders, and inspection / permit logs — streamed to Splunk HEC via APIs and scripted inputs..\n\n**Author notes:** Capture each punch-list items as a `reno:punchlist` event and normalize `room` so the totals are comparable.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"reno:punchlist\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=reno:punchlist earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Punch List Density by Room** over `personal` data (primarily `sourcetype=reno:punchlist`) across the full search range. The `aggregation` aggregation groups by `room`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `room`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=reno:punchlist room=*\n| stats count as punch_list_items by room\n| sort - punch_list_items\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=reno:punchlist earliest=-7d\n| head 50\n| table room\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=reno:punchlist earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.54.30: Personal evidence` with the following panel layout:\n\n**Panel 1 — Bar Chart Of Punch-List Items Volume By Room (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `reno:punchlist` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=reno:punchlist | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It nudges which room show up the most in room-level work so you know where the busiest areas are.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
