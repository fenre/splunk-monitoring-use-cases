{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.43.3",
  "title": "House-Battery State-of-Charge Watch",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity",
    "Availability"
  ],
  "splunkPillar": "Platform",
  "dataSources": "NMEA/SignalK data (`nmea:reading`), bilge pump (`bilge:event`), mooring/anchor watch (`mooring:status`), marine weather (`marineweather:forecast`).",
  "app": "Marine electronics — NMEA 2000 / SignalK boat networks, bilge, battery and shore-power sensors, anchor-watch GPS, and marine weather feeds — streamed to Splunk HEC via SignalK and scripted inputs.",
  "spl": "index=personal sourcetype=nmea:reading measurement=battery\n| timechart span=15m min(soc_pct) as soc by bank\n| eval low=if(soc<50,1,0)",
  "description": "Trends the house-battery charge on board and warns before it falls too low to run the fridge, lights, and pumps.",
  "value": "Flat house batteries at anchor mean warm food and dead instruments; an early warning lets you run the engine in time.",
  "implementation": "Ingest battery telemetry into `index=personal`; alert on low state of charge per bank.",
  "visualization": "Line chart of battery state of charge by bank.",
  "cimModels": [],
  "references": [
    {
      "title": "SignalK — open marine data",
      "url": "https://signalk.org/"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Boating & Marine — typical feeds: NMEA/SignalK data (`nmea:reading`), bilge pump (`bilge:event`), mooring/anchor watch (`mooring:status`), marine weather (`marineweather:forecast`)..\n• **Sourcetypes in this search**: `nmea:reading`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=nmea:reading`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Marine electronics — NMEA 2000 / SignalK boat networks, bilge, battery and shore-power sensors, anchor-watch GPS, and marine weather feeds — streamed to Splunk HEC via SignalK and scripted inputs..\n\n**Author notes:** Ingest battery telemetry into `index=personal`; alert on low state of charge per bank.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"nmea:reading\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=nmea:reading earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **House-Battery State-of-Charge Watch** over `personal` data (primarily `sourcetype=nmea:reading`) across the full search range. It applies numeric thresholds on `soc` (< 50) after min — tune these constants to your household baseline.\n\n**Fields this search depends on:** `bank`, `low`, `soc`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=nmea:reading measurement=battery\n| timechart span=15m min(soc_pct) as soc by bank\n| eval low=if(soc<50,1,0)\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=nmea:reading earliest=-7d\n| head 50\n| table bank, low, soc\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=nmea:reading earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `soc` before alerting:\n\n```spl\nindex=personal sourcetype=nmea:reading earliest=-30d\n| stats perc50(soc) AS p50 perc90(soc) AS p90 perc99(soc) AS p99 max(soc) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.43.3: Personal evidence` with the following panel layout:\n\n**Panel 1 — Line Chart Of Battery State Of Charge By Bank (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `nmea:reading` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=nmea:reading | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=15m` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `15m` buckets.",
  "grandmaExplanation": "It tracks the boat's batteries and warns before they run too low to power the fridge and lights.",
  "prerequisiteUseCases": [],
  "wave": "walk"
}
