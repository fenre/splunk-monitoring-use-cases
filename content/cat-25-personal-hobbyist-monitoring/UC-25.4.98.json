{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.4.98",
  "title": "Homeassistant Feed High Value Outliers",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Anomaly",
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Homeassistant Feed High Value Outliers' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_homeassistant_feed_high_value_outliers` (UC-25.4.98), SOC dashboard panel \"Personal — Homeassistant Feed High Value Outliers\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.4.98 positive (Homeassistant, Feed, High, Value, Outliers): ingest synthetic events into `index=personal` sourcetype='homeassistant:metric0' where `count` grouped by `entity` satisfies detection threshold for 'Homeassistant Feed High Value Outliers'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.4.98 negative (Value Outliers): ingest benign `personal` / `homeassistant:metric0` events where `entity` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 1 confirms zero alert rows for 'Homeassistant Feed High Value Outliers'."
  },
  "dataSources": "Home Assistant state/logbook events (`homeassistant:event`), Homey flow/insights (`homey:flow`), SmartThings events (`smartthings:event`), Hubitat/openHAB logs, Node-RED flow events (`nodered:event`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Home Assistant / Homey / SmartThings / Hubitat / openHAB event streams via MQTT, webhooks, and REST — forwarded to Splunk HEC. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=homeassistant:metric0 value=*\n| eventstats perc90(value) as p90, perc10(value) as p10\n| where value>=p90 OR value<=p10\n| table _time source_id value p90 p10",
  "description": "Monitors Value Outliers activity in UC-25.4.98 ('Homeassistant Feed High Value Outliers') from `index=personal` sourcetype `homeassistant:metric0`. Rolls up `count` by `entity` across `the scheduled window` and pages when configured threshold. Covers Homeassistant, Feed, High, Value, Outliers. Personal operators treat UC-25.4.98 as the canonical signal.",
  "value": "Operators running UC-25.4.98 on `homeassistant:metric0` feeds shorten triage for Value Outliers tied to 'Homeassistant Feed High Value Outliers', lowering missed training goals, stale hobby data, and connector failures.",
  "implementation": "Compute percentile bands on `value` and review extreme homeassistant records.",
  "visualization": "Scatter plot of value with percentile bands.",
  "cimModels": [],
  "references": [
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Home Assistant REST API",
      "url": "https://developers.home-assistant.io/docs/api/rest/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.4.98)** — Connector for 'Homeassistant Feed High Value Outliers' stops polling `homeassistant:metric0`; stale `count` on `external_id` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `count` over `1w` is expected for UC-25.4.98, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `homeassistant:metric0`. Dedup with `stats latest(*) by external_id`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Homeassistant Feed High Value Outliers'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Home Assistant — HA Splunk integration, webhook, or MQTT → HEC.\n• **Subcategory context**: Smart Home Platforms & Automation — typical feeds: Home Assistant state/logbook events (`homeassistant:event`), Homey flow/insights (`homey:flow`), SmartThings events (`smartthings:event`), Hubitat/openHAB logs, Node-RED flow events (`nodered:event`)..\n• **Sourcetypes in this search**: `homeassistant:metric0`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Home Assistant** events into `index=personal` with `sourcetype=homeassistant:metric0`.\n\n**Collection method:** HA Splunk integration, webhook, or MQTT → HEC.\n\n**Reference endpoint / path:** `POST /api/events/state_changed or logbook stream`.\n\n**Suggested poll interval:** real-time (event-driven).\n\n**Key fields to extract:** `entity_id, state, attributes, domain`.\n\n**Typical connector stack:** Home Assistant / Homey / SmartThings / Hubitat / openHAB event streams via MQTT, webhooks, and REST — forwarded to Splunk HEC..\n\n**Author notes:** Compute percentile bands on `value` and review extreme homeassistant records.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"homeassistant:metric0\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=homeassistant:metric0 earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Homeassistant Feed High Value Outliers** over `personal` data (primarily `sourcetype=homeassistant:metric0`) across the full search range. `eventstats` / `streamstats` compute rolling baselines before filtering outliers; window size controls sensitivity (smaller window = noisier alerts).\n\n```spl\nindex=personal sourcetype=homeassistant:metric0 value=*\n| eventstats perc90(value) as p90, perc10(value) as p10\n| where value>=p90 OR value<=p10\n| table _time source_id value p90 p10\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Home Assistant → Developer Tools → States. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=homeassistant:metric0 earliest=-7d\n| head 50\n| table _time, sourcetype, host\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=homeassistant:metric0 earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.4.98: Personal evidence` with the following panel layout:\n\n**Panel 1 — Scatter Plot Of Value With Percentile Bands (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Home Assistant — no Home Assistant events / missing fields:** If `homeassistant:metric0` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Home Assistant connector script is still running (`systemctl status` or cron log).\n\n**Home Assistant — NULL values in extracted fields:** The Home Assistant JSON export shape drifted — compare a raw event (`index=personal sourcetype=homeassistant:metric0 | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Home Assistant — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Home Assistant — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Home Assistant APIs often cap requests per 15 minutes on free tiers.\n\n**Everything is an outlier:** Baseline window is too short or `stdev=0` — require minimum sample count (`where count>=5`) before computing z-scores.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It will reveal the unusually high or low value moments in your homeassistant history.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
