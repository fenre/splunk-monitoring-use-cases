{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.27.5",
  "title": "Fish-Feeder Confirmation and Overfeeding Guard",
  "criticality": "low",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Fish-Feeder Confirmation and Overfeeding Guard' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_fish_feeder_confirmation_and_overfeeding_guard` (UC-25.27.5), SOC dashboard panel \"Personal — Fish-Feeder Confirmation and Overfeeding Guard\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.27.5 positive (Fish, Feeder, Confirmation, and, Overfeeding): ingest synthetic events into `index=personal` sourcetype='fishfeeder:event' where `count` grouped by `_time` satisfies `feeds` > 3 for 'Fish-Feeder Confirmation and Overfeeding Guard'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.27.5 negative (Overfeeding Guard): ingest benign `personal` / `fishfeeder:event` events where `_time` stays below `feeds` > 3, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 6 confirms zero alert rows for 'Fish-Feeder Confirmation and Overfeeding Guard'."
  },
  "dataSources": "Reef-tank parameters (`reeftank:param`), vivarium climate (`vivarium:climate`), fish feeder (`fishfeeder:event`), auto-doser (`doser:event`), auto-top-off (`ato:event`), pond sensors (`pondsensor:reading`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Aquarium and vivarium controllers — reef-tank parameter monitors, auto-dosers, auto-top-off, reptile terrarium climate/UVB, and pond sensors — streamed to Splunk HEC via MQTT and controller APIs (e.g. Apex, GHL, ESPHome). — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=fishfeeder:event\n| bin _time span=1d\n| stats count as feeds by _time\n| eval status=case(feeds=0,\"missed\",feeds>3,\"overfed\",1=1,\"ok\")\n| sort - _time",
  "description": "Identifies Overfeeding Guard anomalies for UC-25.27.5 with window `1d` on `index=personal` sourcetype `fishfeeder:event`. Combines `count` by `_time` and compares against `feeds` > 3. Mapped to keywords Fish, Feeder, Confirmation, and, Overfeeding for dashboard routing.",
  "value": "Alerting on Fish, Feeder, Confirmation, and, Overfeeding through UC-25.27.5 reduces missed training goals, stale hobby data, and connector failures by surfacing `activity` early.",
  "implementation": "Ingest feeder events into `index=personal`; schedule daily and flag missed or excessive feeds.",
  "visualization": "Column chart of daily feeds with a normal range.",
  "cimModels": [],
  "references": [
    {
      "title": "Arduino — project hub",
      "url": "https://projecthub.arduino.cc/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.27.5)** — Connector for 'Fish-Feeder Confirmation and Overfeeding Guard' stops polling `fishfeeder:event`; stale `count` on `_time` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `count` over `1d` is expected for UC-25.27.5, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `fishfeeder:event`. Dedup with `stats latest(*) by _time`.\n\n4. **Timezone bucket misalignment** — `1d` bins on UTC vs local schedule skew `feeds` > 3 for 'Fish-Feeder Confirmation and Overfeeding Guard'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Aquariums, Reptiles & Vivariums — typical feeds: Reef-tank parameters (`reeftank:param`), vivarium climate (`vivarium:climate`), fish feeder (`fishfeeder:event`), auto-doser (`doser:event`), auto-top-off (`ato:event`), pond sensors (`pondsensor:read.\n• **Sourcetypes in this search**: `fishfeeder:event`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=fishfeeder:event`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Aquarium and vivarium controllers — reef-tank parameter monitors, auto-dosers, auto-top-off, reptile terrarium climate/UVB, and pond sensors — via MQTT and controller APIs (Apex, GHL, ESPHome)..\n\n**Author notes:** Ingest feeder events into `index=personal`; schedule daily and flag missed or excessive feeds.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"fishfeeder:event\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=fishfeeder:event earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Fish-Feeder Confirmation and Overfeeding Guard** over `personal` data (primarily `sourcetype=fishfeeder:event`) across the full search range. It applies numeric thresholds on `feeds` (> 3) after aggregation — tune these constants to your household baseline.\n\n**Fields this search depends on:** `_time`, `feeds`, `status`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=fishfeeder:event\n| bin _time span=1d\n| stats count as feeds by _time\n| eval status=case(feeds=0,\"missed\",feeds>3,\"overfed\",1=1,\"ok\")\n| sort - _time\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=fishfeeder:event earliest=-7d\n| head 50\n| table _time, feeds, status\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=fishfeeder:event earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `feeds` before alerting:\n\n```spl\nindex=personal sourcetype=fishfeeder:event earliest=-30d\n| stats perc50(feeds) AS p50 perc90(feeds) AS p90 perc99(feeds) AS p99 max(feeds) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.27.5: Personal evidence` with the following panel layout:\n\n**Panel 1 — Column Chart Of Daily Feeds With A Normal Range (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `fishfeeder:event` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=fishfeeder:event | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=1d` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1d` buckets.",
  "grandmaExplanation": "It checks the automatic fish feeder ran the right number of times, so the fish are neither missed nor overfed.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
