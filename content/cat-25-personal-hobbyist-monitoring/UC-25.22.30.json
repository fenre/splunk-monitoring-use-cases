{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.22.30",
  "title": "Filter Wheel Position Mismatch Alert",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Reliability"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Filter Wheel Position Mismatch Alert' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_filter_wheel_position_mismatch_alert` (UC-25.22.30), SOC dashboard panel \"Personal — Filter Wheel Position Mismatch Alert\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.22.30 positive (Filter, Wheel, Position, Mismatch, Alert): ingest synthetic events into `index=personal` sourcetype='filterwheel:event' where `count` grouped by `wheel_name` satisfies detection threshold for 'Filter Wheel Position Mismatch Alert'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.22.30 negative (Mismatch Alert): ingest benign `personal` / `filterwheel:event` events where `wheel_name` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 3 confirms zero alert rows for 'Filter Wheel Position Mismatch Alert'."
  },
  "dataSources": "NINA sequences (`nina:sequence`), NINA autofocus runs (`nina:autofocus`), Ekos scheduler and device events (`ekos:scheduler`, `ekos:device`), PHD2 guiding telemetry (`phd2:guide`), plate solves (`platesolve:run`), filter wheel events (`filterwheel:event`), and flat frames (`flatlib:frame`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Astrophotography session telemetry from N.I.N.A., KStars/Ekos, PHD2, plate-solving tools, ASCOM gear, and filter-wheel controllers sent to Splunk HEC via logs and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=filterwheel:event\n| stats count(eval(expected_slot!=actual_slot)) as mismatches count as moves by wheel_name\n| sort 0 - mismatches",
  "description": "Surfaces when Mismatch Alert criteria fire for 'Filter Wheel Position Mismatch Alert' (UC-25.22.30) in `index=personal` sourcetype `filterwheel:event`. Pipeline aggregates `count` by `wheel_name` over `the scheduled window`. Alert logic enforces configured threshold. Title terms: Filter, Wheel, Position, Mismatch, Alert. Eliminates manual hunting for UC-25.22.30 conditions.",
  "value": "UC-25.22.30 helps teams contain Filter, Wheel, Position, Mismatch, Alert scenarios and reduce missed training goals, stale hobby data, and connector failures before incidents compound.",
  "implementation": "Send filter-wheel mismatches events to `index=personal` and keep `wheel_name` populated so the leaderboard stays actionable.",
  "visualization": "Bar chart of filter-wheel mismatches by filter wheels.",
  "cimModels": [],
  "references": [
    {
      "title": "N.I.N.A.",
      "url": "https://nighttime-imaging.eu/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "KStars / Ekos",
      "url": "https://edu.kde.org/kstars/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "PHD2 Guiding",
      "url": "https://openphdguiding.org/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Astrometry.net",
      "url": "https://astrometry.net/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "ASCOM",
      "url": "https://ascom-standards.org/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.22.30)** — Connector for 'Filter Wheel Position Mismatch Alert' stops polling `filterwheel:event`; stale `count` on `wheel_name` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `count` over `1w` is expected for UC-25.22.30, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `filterwheel:event`. Dedup with `stats latest(*) by wheel_name`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Filter Wheel Position Mismatch Alert'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Backyard Astronomy & Observatory — typical feeds: Observatory environment (`observatory:env`), sky-quality meter (`skyquality:reading`), imaging sessions (`imaging:session`), telescope mount (`mount:status`), cloud sensor (`allsky:cloud`)..\n• **Sourcetypes in this search**: `filterwheel:event`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=filterwheel:event`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Backyard observatory automation — roll-off-roof/dome controller, sky-quality meter, cloud sensor, telescope mount, and astrophotography software (N.I.N.A./Ekos) — via MQTT and scripted inputs..\n\n**Author notes:** Send filter-wheel mismatches events to `index=personal` and keep `wheel_name` populated so the leaderboard stays actionable.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"filterwheel:event\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=filterwheel:event earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Filter Wheel Position Mismatch Alert** over `personal` data (primarily `sourcetype=filterwheel:event`) across the full search range. The `count` aggregation groups by `wheel_name`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `wheel_name`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=filterwheel:event\n| stats count(eval(expected_slot!=actual_slot)) as mismatches count as moves by wheel_name\n| sort 0 - mismatches\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=filterwheel:event earliest=-7d\n| head 50\n| table wheel_name\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=filterwheel:event earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.22.30: Personal evidence` with the following panel layout:\n\n**Panel 1 — Bar Chart Of Filter-Wheel Mismatches By Filter Wheels (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `filterwheel:event` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=filterwheel:event | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It frames which filter wheelss stand out most for filter-wheel mismatches.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
