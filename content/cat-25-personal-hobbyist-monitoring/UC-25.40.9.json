{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.40.9",
  "title": "Personal Digital-Twin Data Freshness",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Data Quality"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Personal Digital-Twin Data Freshness' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_personal_digital_twin_data_freshness` (UC-25.40.9), SOC dashboard panel \"Personal — Personal Digital-Twin Data Freshness\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.40.9 positive (Personal, Digital, Twin, Data, Freshness): ingest synthetic events into `index=personal` sourcetype='target_sourcetype' where `count` grouped by `sourcetype` satisfies `hours_silent` > 48 for 'Personal Digital-Twin Data Freshness'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.40.9 negative (Data Freshness): ingest benign `personal` / `target_sourcetype` events where `sourcetype` stays below `hours_silent` > 48, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 3 confirms zero alert rows for 'Personal Digital-Twin Data Freshness'."
  },
  "dataSources": "Daily life scores (`lifescore:daily`), personal SLO status (`personalslo:status`), cross-signal correlations (`correlation:pair`), plus every other `index=personal` sourcetype as input. Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "The meta layer — correlating every `index=personal` feed (sleep, activity, mood, weather, spend, productivity, home telemetry) into daily life scores, personal SLOs, and cross-signal anomaly detection, all computed in Splunk. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal\n| stats latest(_time) as last_seen by sourcetype\n| eval hours_silent=round((now()-last_seen)/3600,1), stale=if(hours_silent>48,1,0)\n| where stale=1\n| sort - hours_silent",
  "description": "Detects when Data Freshness criteria fire for 'Personal Digital-Twin Data Freshness' (UC-25.40.9) in `index=personal` sourcetype `configured sourcetype`. Pipeline aggregates `count` by `sourcetype` over `the scheduled window`. Alert logic enforces `hours_silent` > 48. Title terms: Personal, Digital, Twin, Data, Freshness. Eliminates manual hunting for UC-25.40.9 conditions.",
  "value": "UC-25.40.9 helps teams contain Personal, Digital, Twin, Data, Freshness scenarios and reduce missed training goals, stale hobby data, and connector failures before incidents compound.",
  "implementation": "Track last-seen per sourcetype across `index=personal`; alert on any feed that has gone quiet.",
  "visualization": "Table of personal feeds with time since last data.",
  "cimModels": [],
  "references": [
    {
      "title": "Google — SRE book (SLOs)",
      "url": "https://sre.google/sre-book/service-level-objectives/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.40.9)** — Connector for 'Personal Digital-Twin Data Freshness' stops polling `configured sourcetype`; stale `count` on `sourcetype` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `count` over `1w` is expected for UC-25.40.9, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `configured sourcetype`. Dedup with `stats latest(*) by sourcetype`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `hours_silent` > 48 for 'Personal Digital-Twin Data Freshness'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Cross-Stream ML & the Personal Digital Twin — typical feeds: Daily life scores (`lifescore:daily`), personal SLO status (`personalslo:status`), cross-signal correlations (`correlation:pair`), plus every other `index=personal` sourcetype as input..\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=your:export`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** The meta layer — correlating every `index=personal` feed (sleep, activity, mood, weather, spend, productivity, home telemetry) into daily life scores, personal SLOs, and cross-signal anomaly detection, all computed in Splunk..\n\n**Author notes:** Track last-seen per sourcetype across `index=personal`; alert on any feed that has gone quiet.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"your:export\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=your:export earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Personal Digital-Twin Data Freshness** over `personal` data (primarily `sourcetype=your_sourcetype`) across the full search range. It applies numeric thresholds on `hours_silent` (> 48) after latest — tune these constants to your household baseline.\n\n**Fields this search depends on:** `hours_silent`, `sourcetype`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal\n| stats latest(_time) as last_seen by sourcetype\n| eval hours_silent=round((now()-last_seen)/3600,1), stale=if(hours_silent>48,1,0)\n| where stale=1\n| sort - hours_silent\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=your:export earliest=-7d\n| head 50\n| table hours_silent, sourcetype\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=your:export earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `hours_silent` before alerting:\n\n```spl\nindex=personal sourcetype=your:export earliest=-30d\n| stats perc50(hours_silent) AS p50 perc90(hours_silent) AS p90 perc99(hours_silent) AS p99 max(hours_silent) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.40.9: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Personal Feeds With Time Since Last Data (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `your:export` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=your:export | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It checks every source of your life data is still updating, so nothing quietly stops feeding in.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
