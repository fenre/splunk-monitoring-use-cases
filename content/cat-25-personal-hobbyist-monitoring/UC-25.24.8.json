{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.24.8",
  "title": "Pasture Soil-Moisture and Grazing Readiness",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "dataSources": "Beehive telemetry (`beehive:reading`), coop door (`chickencoop:door`), egg counts (`egg:count`), livestock tags (`livestock:tag`), feed level (`feed:level`), pasture sensors (`pasture:sensor`).",
  "app": "Homestead sensors — connected beehive scale/temperature/sound, automatic chicken-coop door, egg counter, livestock GPS/health tags, feed-silo level, and pasture soil sensors — streamed to Splunk HEC via MQTT and LoRaWAN gateways.",
  "spl": "index=personal sourcetype=pasture:sensor\n| stats avg(soil_moisture_pct) as moisture, avg(soil_temp_c) as temp by paddock\n| eval growing=if(moisture>25 AND temp>8,\"growing\",\"resting\")\n| sort - moisture",
  "description": "Tracks soil moisture and temperature across paddocks to judge which are growing grass and ready to graze.",
  "value": "Rotational grazing depends on grass recovery; measuring paddock conditions helps move stock at the right time for healthy pasture.",
  "implementation": "Ingest pasture soil sensors into `index=personal`; review moisture and temperature by paddock.",
  "visualization": "Bar chart of soil moisture by paddock with a growing indicator.",
  "cimModels": [],
  "references": [
    {
      "title": "The Things Network — LoRaWAN",
      "url": "https://www.thethingsnetwork.org/"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector"
    }
  ],
  "equipment": [
    "hardware_bmc",
    "mqtt"
  ],
  "equipmentModels": [
    "hardware_bmc_ilo"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Homestead, Bees & Livestock — typical feeds: Beehive telemetry (`beehive:reading`), coop door (`chickencoop:door`), egg counts (`egg:count`), livestock tags (`livestock:tag`), feed level (`feed:level`), pasture sensors (`pasture:sensor`), trail .\n• **Sourcetypes in this search**: `pasture:sensor`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=pasture:sensor`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Homestead sensors — connected beehive scale/temperature/sound, automatic chicken-coop door, egg counter, livestock GPS/health tags, feed-silo level, and pasture soil sensors — via MQTT and LoRaWAN gateways..\n\n**Author notes:** Ingest pasture soil sensors into `index=personal`; review moisture and temperature by paddock.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"pasture:sensor\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=pasture:sensor earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Pasture Soil-Moisture and Grazing Readiness** over `personal` data (primarily `sourcetype=pasture:sensor`) across the full search range. It applies numeric thresholds on `moisture` (> 25) after avg — tune these constants to your household baseline.\n\n**Fields this search depends on:** `growing`, `moisture`, `paddock`, `temp`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=pasture:sensor\n| stats avg(soil_moisture_pct) as moisture, avg(soil_temp_c) as temp by paddock\n| eval growing=if(moisture>25 AND temp>8,\"growing\",\"resting\")\n| sort - moisture\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=pasture:sensor earliest=-7d\n| head 50\n| table growing, moisture, paddock, temp\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=pasture:sensor earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `moisture` before alerting:\n\n```spl\nindex=personal sourcetype=pasture:sensor earliest=-30d\n| stats perc50(moisture) AS p50 perc90(moisture) AS p90 perc99(moisture) AS p99 max(moisture) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.24.8: Personal evidence` with the following panel layout:\n\n**Panel 1 — Bar Chart Of Soil Moisture By Paddock With A Growing Indicator (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `pasture:sensor` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=pasture:sensor | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It checks how moist and warm the soil is in each field, so you know which are ready for animals to graze.",
  "prerequisiteUseCases": [],
  "wave": "walk"
}
