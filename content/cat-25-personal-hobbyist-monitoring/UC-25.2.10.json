{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.2.10",
  "title": "Wearable Battery and Sync Health",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Data Quality"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Wearable Battery and Sync Health' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_wearable_battery_and_sync_health` (UC-25.2.10), SOC dashboard panel \"Personal — Wearable Battery and Sync Health\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.2.10 positive (Wearable, Battery, and, Sync, Health): ingest synthetic events into `index=personal` sourcetype='oura:daily' where `last_seen` grouped by `device` satisfies `hours_since` > 30, or `battery` < 15 for 'Wearable Battery and Sync Health'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.2.10 negative (Sync Health): ingest benign `personal` / `oura:daily` events where `device` stays below `hours_since` > 30, or `battery` < 15, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 0 confirms zero alert rows for 'Wearable Battery and Sync Health'."
  },
  "dataSources": "Apple Health metrics (`apple:health`), Oura Ring readiness/sleep (`oura:daily`), Whoop recovery/strain (`whoop:cycle`), Withings measurements (`withings:measure`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Apple Health / Apple Watch exports (Health Auto Export or Shortcuts) via HEC; Oura, Whoop, Withings, and Dexcom APIs via scripted REST inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=oura:daily\n| stats max(_time) as last_seen, latest(battery_pct) as battery by device\n| eval hours_since=round((now()-last_seen)/3600,1)\n| where hours_since>30 OR battery<15\n| sort - hours_since",
  "description": "Surfaces 'Wearable Battery and Sync Health' (UC-25.2.10) using `index=personal` sourcetype `oura:daily` by aggregating `last_seen` grouped by `device` over `the scheduled window` and alerting when `hours_since` > 30, or `battery` < 15. Keywords: Wearable, Battery, and, Sync, Health. Focuses on Sync Health behaviour so personal teams can triage UC-25.2.10 without ad-hoc log review.",
  "value": "UC-25.2.10 accelerates insight into Sync Health ('Wearable Battery and Sync Health') by tracking `last_seen` on `oura:daily` feeds, reducing missed training goals, stale hobby data, and connector failures before spreadsheet checks hide regressions.",
  "implementation": "Ingest device battery and last-sync fields; schedule twice daily and alert on low battery or a sync gap before bedtime.",
  "visualization": "Table of devices by battery level and hours since last sync.",
  "cimModels": [],
  "references": [
    {
      "title": "Oura — API v2 documentation",
      "url": "https://cloud.ouraring.com/v2/docs",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.2.10)** — Connector for 'Wearable Battery and Sync Health' stops polling `oura:daily`; stale `last_seen` on `device` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `last_seen` over `1w` is expected for UC-25.2.10, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `oura:daily`. Dedup with `stats latest(*) by device`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `hours_since` > 30 for 'Wearable Battery and Sync Health'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Oura Ring — Oura Cloud API v2 → HEC scripted input.\n• **Subcategory context**: Health, Sleep & Wearables — typical feeds: Apple Health metrics (`apple:health`), Oura Ring daily readiness/sleep (`oura:daily`), Whoop recovery/strain (`whoop:cycle`), Withings body/blood-pressure (`withings:measure`), Dexcom CGM glucose (`de.\n• **Sourcetypes in this search**: `oura:daily`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Oura Ring** events into `index=personal` with `sourcetype=oura:daily`.\n\n**Collection method:** Oura Cloud API v2 → HEC scripted input.\n\n**Reference endpoint / path:** `GET /v2/usercollection/daily_sleep`.\n\n**Suggested poll interval:** 86400s (daily sync).\n\n**Key fields to extract:** `score, contributors, heart_rate, hrv`.\n\n**Typical connector stack:** Apple Health / Apple Watch exports (Health Auto Export, Shortcuts) via HEC; Oura, Whoop, Withings, Dexcom APIs via scripted REST inputs..\n\n**Author notes:** Ingest device battery and last-sync fields; schedule twice daily and alert on low battery or a sync gap before bedtime.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"oura:daily\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=oura:daily earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Wearable Battery and Sync Health** over `personal` data (primarily `sourcetype=oura:daily`) across the full search range. It applies numeric thresholds on `hours_since` (> 30) after max, latest — tune these constants to your household baseline.\n\n**Fields this search depends on:** `battery`, `device`, `hours_since`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=oura:daily\n| stats max(_time) as last_seen, latest(battery_pct) as battery by device\n| eval hours_since=round((now()-last_seen)/3600,1)\n| where hours_since>30 OR battery<15\n| sort - hours_since\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Oura app → Readiness / Sleep tabs. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=oura:daily earliest=-7d\n| head 50\n| table battery, device, hours_since\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=oura:daily earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `hours_since` before alerting:\n\n```spl\nindex=personal sourcetype=oura:daily earliest=-30d\n| stats perc50(hours_since) AS p50 perc90(hours_since) AS p90 perc99(hours_since) AS p99 max(hours_since) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.2.10: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Devices By Battery Level And Hours Since Last Sync (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Oura Ring — no Oura Ring events / missing fields:** If `oura:daily` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Oura Ring connector script is still running (`systemctl status` or cron log).\n\n**Oura Ring — NULL values in extracted fields:** The Oura Ring JSON export shape drifted — compare a raw event (`index=personal sourcetype=oura:daily | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Oura Ring — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Oura Ring — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Oura Ring APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It reminds you to charge your health ring or watch before the battery dies and you miss a night of data.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
