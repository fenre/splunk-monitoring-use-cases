{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.2.12",
  "title": "Sleep Debt Accumulation Watch",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics",
    "Safety"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Sleep Debt Accumulation Watch' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_sleep_debt_accumulation_watch` (UC-25.2.12), SOC dashboard panel \"Personal — Sleep Debt Accumulation Watch\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=personal` sourcetype='oura:daily' where `user` satisfies `sleep_debt` > 0 for UC-25.2.12 ('Sleep Debt Accumulation Watch'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `personal` / `oura:daily` events where `user` stays below `sleep_debt` > 0, or exclude the test entity via `personal_monitoring_notes.csv` with a valid window. Confirm zero alert rows for UC-25.2.12."
  },
  "dataSources": "Apple Health metrics (`apple:health`), Oura Ring daily readiness/sleep (`oura:daily`), WHOOP recovery/strain (`whoop:cycle`), Withings body and blood-pressure (`withings:measure`), Dexcom CGM glucose (`dexcom:egv`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Apple Health / Apple Watch exports (Health Auto Export, Shortcuts) via HEC; Oura, WHOOP, Withings, and Dexcom APIs via scripted REST inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=oura:daily\n| eval sleep_debt=target_sleep_hours-sleep_hours\n| where sleep_debt>0\n| sort 0 user _time\n| streamstats window=7 sum(sleep_debt) as debt_7d by user\n| where debt_7d>5\n| sort - _time",
  "description": "Adds up your past week's sleep shortfall and flags when the debt has become large enough that caffeine and optimism are no longer hiding it.",
  "value": "Adds up your past week's sleep shortfall and flags when the debt has become large enough that caffeine and.... Timely UC-25.2.12 alerting on 'Sleep Debt Accumulation Watch' reduces missed training goals, stale hobby data, and connector failures before manual triage or audit channels escalate.",
  "implementation": "Forward Oura daily totals with target sleep hours; schedule each morning and alert when rolling sleep debt gets too high.",
  "visualization": "Daily line chart of seven-day sleep debt.",
  "cimModels": [],
  "references": [
    {
      "title": "Oura API v2 documentation",
      "url": "https://cloud.ouraring.com/v2/docs",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry** — Connector for 'Sleep Debt Accumulation Watch' stops polling `oura:daily`; stale `user` rows remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `sum` over `1w` is expected, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `oura:daily`. Dedup with `stats latest(*) by user`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew `sleep_debt` > 0. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Oura Ring — Oura Cloud API v2 → HEC scripted input.\n• **Subcategory context**: Health, Sleep & Wearables — typical feeds: Apple Health metrics (`apple:health`), Oura Ring daily readiness/sleep (`oura:daily`), Whoop recovery/strain (`whoop:cycle`), Withings body/blood-pressure (`withings:measure`), Dexcom CGM glucose (`de.\n• **Sourcetypes in this search**: `oura:daily`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Oura Ring** events into `index=personal` with `sourcetype=oura:daily`.\n\n**Collection method:** Oura Cloud API v2 → HEC scripted input.\n\n**Reference endpoint / path:** `GET /v2/usercollection/daily_sleep`.\n\n**Suggested poll interval:** 86400s (daily sync).\n\n**Key fields to extract:** `score, contributors, heart_rate, hrv`.\n\n**Typical connector stack:** Apple Health / Apple Watch exports (Health Auto Export, Shortcuts) via HEC; Oura, Whoop, Withings, Dexcom APIs via scripted REST inputs..\n\n**Author notes:** Forward Oura daily totals with target sleep hours; schedule each morning and alert when rolling sleep debt gets too high.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"oura:daily\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=oura:daily earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Sleep Debt Accumulation Watch** over `personal` data (primarily `sourcetype=oura:daily`) across the full search range. `eventstats` / `streamstats` compute rolling baselines before filtering outliers; window size controls sensitivity (smaller window = noisier alerts).\n\n**Fields this search depends on:** `debt_7d`, `sleep_debt`, `user`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=oura:daily\n| eval sleep_debt=target_sleep_hours-sleep_hours\n| where sleep_debt>0\n| sort 0 user _time\n| streamstats window=7 sum(sleep_debt) as debt_7d by user\n| where debt_7d>5\n| sort - _time\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Oura app → Readiness / Sleep tabs. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=oura:daily earliest=-7d\n| head 50\n| table debt_7d, sleep_debt, user\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=oura:daily earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `sleep_debt` before alerting:\n\n```spl\nindex=personal sourcetype=oura:daily earliest=-30d\n| stats perc50(sleep_debt) AS p50 perc90(sleep_debt) AS p90 perc99(sleep_debt) AS p99 max(sleep_debt) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.2.12: Personal evidence` with the following panel layout:\n\n**Panel 1 — Daily Line Chart Of Seven-Day Sleep Debt (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Oura Ring — no Oura Ring events / missing fields:** If `oura:daily` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Oura Ring connector script is still running (`systemctl status` or cron log).\n\n**Oura Ring — NULL values in extracted fields:** The Oura Ring JSON export shape drifted — compare a raw event (`index=personal sourcetype=oura:daily | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Oura Ring — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Oura Ring — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Oura Ring APIs often cap requests per 15 minutes on free tiers.\n\n**Everything is an outlier:** Baseline window is too short or `stdev=0` — require minimum sample count (`where count>=5`) before computing z-scores.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It watches when too many slightly short nights pile up into a bigger sleep debt that your body can feel.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
