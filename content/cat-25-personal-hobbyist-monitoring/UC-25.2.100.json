{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.2.100",
  "title": "Sleep-Apnea Event Frequency",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Anomaly"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Sleep-Apnea Event Frequency' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_sleep_apnea_event_frequency` (UC-25.2.100), SOC dashboard panel \"Personal — Sleep-Apnea Event Frequency\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.2.100 positive (Sleep, Apnea, Event, Frequency): ingest synthetic events into `index=personal` sourcetype='apnea:event' where `hours` grouped by `_time` satisfies `ahi` > 15 for 'Sleep-Apnea Event Frequency'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.2.100 negative (Event Frequency): ingest benign `personal` / `apnea:event` events where `_time` stays below `ahi` > 15, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 11 confirms zero alert rows for 'Sleep-Apnea Event Frequency'."
  },
  "dataSources": "HRV (`hrv:reading`), blood oxygen (`spo2:reading`), lab panels (`bloodpanel:result`), supplements (`supplement:intake`), continuous glucose (`cgm:glucose`), sleep-apnea events (`apnea:event`), body composition (`bodycomp:measure`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Biohacking and advanced-health data — HRV chest straps, pulse oximeters, blood-pressure cuffs, continuous glucose monitors, body-composition scales, lab-result exports, and supplement logs — ingested to Splunk HEC via APIs and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=apnea:event\n| bin _time span=1d\n| stats count as events, sum(sleep_hours) as hours by _time\n| eval ahi=round(events/hours,1), severe=if(ahi>15,1,0)\n| sort - _time",
  "description": "Detects UC-25.2.100 — Sleep, Apnea, Event, Frequency — using `index=personal` sourcetype `apnea:event`, grouped by `_time`. Outputs `hours` each `1d`; alert when `ahi` > 15. Primary focus: Event Frequency. Purpose-built for 'Sleep-Apnea Event Frequency' in Personal workflows.",
  "value": "UC-25.2.100 helps teams contain Sleep, Apnea, Event, Frequency scenarios and reduce missed training goals, stale hobby data, and connector failures before incidents compound.",
  "implementation": "Ingest apnea-event data into `index=personal`; schedule each morning and trend the events-per-hour index.",
  "visualization": "Line chart of nightly apnea index with a severity threshold.",
  "cimModels": [],
  "references": [
    {
      "title": "Quantified Self — self-tracking community",
      "url": "https://quantifiedself.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.2.100)** — Connector for 'Sleep-Apnea Event Frequency' stops polling `apnea:event`; stale `hours` on `_time` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `hours` over `1d` is expected for UC-25.2.100, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `apnea:event`. Dedup with `stats latest(*) by _time`.\n\n4. **Timezone bucket misalignment** — `1d` bins on UTC vs local schedule skew `ahi` > 15 for 'Sleep-Apnea Event Frequency'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Advanced Health & Biohacking — typical feeds: HRV (`hrv:reading`), blood oxygen (`spo2:reading`), lab panels (`bloodpanel:result`), supplements (`supplement:intake`), continuous glucose (`cgm:glucose`), sleep-apnea events (`apnea:event`), body co.\n• **Sourcetypes in this search**: `apnea:event`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=apnea:event`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Biohacking data — HRV straps, pulse oximeters, blood-pressure cuffs, continuous glucose monitors, body-composition scales, lab-result exports, and supplement logs — ingested to Splunk HEC via APIs and scripted inputs..\n\n**Author notes:** Ingest apnea-event data into `index=personal`; schedule each morning and trend the events-per-hour index.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"apnea:event\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=apnea:event earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Sleep-Apnea Event Frequency** over `personal` data (primarily `sourcetype=apnea:event`) across the full search range. It applies numeric thresholds on `ahi` (> 15) after sum — tune these constants to your household baseline.\n\n**Fields this search depends on:** `_time`, `ahi`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=apnea:event\n| bin _time span=1d\n| stats count as events, sum(sleep_hours) as hours by _time\n| eval ahi=round(events/hours,1), severe=if(ahi>15,1,0)\n| sort - _time\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=apnea:event earliest=-7d\n| head 50\n| table _time, ahi\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=apnea:event earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `ahi` before alerting:\n\n```spl\nindex=personal sourcetype=apnea:event earliest=-30d\n| stats perc50(ahi) AS p50 perc90(ahi) AS p90 perc99(ahi) AS p99 max(ahi) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.2.100: Personal evidence` with the following panel layout:\n\n**Panel 1 — Line Chart Of Nightly Apnea Index With A Severity Threshold (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `apnea:event` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=apnea:event | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=1d` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1d` buckets.",
  "grandmaExplanation": "It estimates how often your breathing was interrupted per hour of sleep, a number sleep doctors care about.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
