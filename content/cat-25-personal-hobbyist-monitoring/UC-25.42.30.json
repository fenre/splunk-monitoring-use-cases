{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.42.30",
  "title": "Checkride Mock-Oral Score Trend",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Checkride Mock-Oral Score Trend' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_checkride_mock_oral_score_trend` (UC-25.42.30), SOC dashboard panel \"Personal — Checkride Mock-Oral Score Trend\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.42.30 positive (Checkride, Mock, Oral, Score, Trend): ingest synthetic events into `index=personal` sourcetype='checkride:prep' where `metric` grouped by `_time`, `subject` satisfies detection threshold for 'Checkride Mock-Oral Score Trend'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.42.30 negative (Score Trend): ingest benign `personal` / `checkride:prep` events where `_time`, `subject` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 7 confirms zero alert rows for 'Checkride Mock-Oral Score Trend'."
  },
  "dataSources": "Flight logs (`flight:log`), flight-plan snapshots (`simbrief:plan`), network sessions (`vatsim:session`), prep tasks (`checkride:prep`), export syncs (`flight:export`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Aviation and flight-sim telemetry — ForeFlight exports, SimBrief flight plans, VATSIM session logs, and checkride-prep trackers — streamed to Splunk HEC via exports and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=checkride:prep subject=* mock_oral_score=*\n| bin _time span=1mon\n| stats avg(mock_oral_score) as metric by _time subject\n| sort 0 _time subject",
  "description": "Alerts on UC-25.42.30 — Checkride, Mock, Oral, Score, Trend — using `index=personal` sourcetype `checkride:prep`, grouped by `_time`, `subject`. Outputs `metric` each `1m`; alert when configured threshold. Primary focus: Score Trend. Purpose-built for 'Checkride Mock-Oral Score Trend' in Personal workflows.",
  "value": "UC-25.42.30 operationalises 'Checkride Mock-Oral Score Trend' against `checkride:prep` feeds so operators reduce missed training goals, stale hobby data, and connector failures without manual correlation.",
  "implementation": "Normalize `subject` on `checkride:prep` events and store the fields needed to calculate monthly mock-oral score.",
  "visualization": "Line chart of monthly mock-oral score by checkride subject.",
  "cimModels": [],
  "references": [
    {
      "title": "ForeFlight — flight bag",
      "url": "https://foreflight.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "SimBrief — flight planning",
      "url": "https://www.simbrief.com/home/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "VATSIM — virtual ATC",
      "url": "https://vatsim.net/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "FAA — Airman Certification Standards",
      "url": "https://www.faa.gov/training_testing/testing/acs",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.42.30)** — Connector for 'Checkride Mock-Oral Score Trend' stops polling `checkride:prep`; stale `metric` on `_time`, `subject` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `metric` over `1m` is expected for UC-25.42.30, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `checkride:prep`. Dedup with `stats latest(*) by _time`.\n\n4. **Timezone bucket misalignment** — `1m` bins on UTC vs local schedule skew goal threshold for 'Checkride Mock-Oral Score Trend'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Aviation & Flight Simulation — typical feeds: Pilot logbook (`flightlog:entry`), avionics telemetry (`avionics:telemetry`), flight-sim sessions (`flightsim:session`), pre-flight checklists (`preflight:check`)..\n• **Sourcetypes in this search**: `checkride:prep`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=checkride:prep`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Pilot and flight-sim telemetry — electronic logbooks (ForeFlight, LogTen Pro), Stratux / GDL90 avionics receivers, and Microsoft Flight Simulator / X-Plane SimConnect sessions — streamed to Splunk HEC via scripted inputs..\n\n**Author notes:** Normalize `subject` on `checkride:prep` events and store the fields needed to calculate monthly mock-oral score.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"checkride:prep\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=checkride:prep earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Checkride Mock-Oral Score Trend** over `personal` data (primarily `sourcetype=checkride:prep`) across the full search range. The `avg` aggregation groups by `_time`, `subject`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `_time`, `subject`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=checkride:prep subject=* mock_oral_score=*\n| bin _time span=1mon\n| stats avg(mock_oral_score) as metric by _time subject\n| sort 0 _time subject\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=checkride:prep earliest=-7d\n| head 50\n| table _time, subject\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=checkride:prep earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.42.30: Personal evidence` with the following panel layout:\n\n**Panel 1 — Line Chart Of Monthly Mock-Oral Score By Checkride Subject (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `checkride:prep` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=checkride:prep | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=1m` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1m` buckets.",
  "grandmaExplanation": "It frames how mock-oral score changes month by month for each checkride subject so you can see whether checkride study blocks are heading the right way.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
