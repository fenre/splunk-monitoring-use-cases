{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.99.28",
  "title": "Race Watch Party Status Mix",
  "criticality": "low",
  "difficulty": "beginner",
  "monitoringType": [
    "Analytics",
    "Audit"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Race Watch Party Status Mix' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_race_watch_party_status_mix` (UC-25.99.28), SOC dashboard panel \"Personal — Race Watch Party Status Mix\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.99.28 positive (Race, Watch, Party, Status, Mix): ingest synthetic events into `index=personal` sourcetype='race:watchparty' where `count` grouped by `status_value` satisfies detection threshold for 'Race Watch Party Status Mix'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.99.28 negative (Status Mix): ingest benign `personal` / `race:watchparty` events where `status_value` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 1 confirms zero alert rows for 'Race Watch Party Status Mix'."
  },
  "dataSources": "FastF1 session data (`fastf1:session`), Ergast race results (`ergast:result`), F1 Fantasy teams (`f1fantasy:team`), race watch parties (`race:watchparty`), and F1 telemetry laps (`f1telemetry:lap`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "FastF1 session exports, Ergast race results, F1 Fantasy team snapshots, race watch-party logs, and F1 telemetry lap dumps forwarded to Splunk HEC. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=race:watchparty\n| eval status_value=lower(coalesce(party_status,\"unknown\"))\n| stats count as events by status_value\n| sort - events",
  "description": "Monitors Status Mix activity in UC-25.99.28 ('Race Watch Party Status Mix') from `index=personal` sourcetype `race:watchparty`. Rolls up `count` by `status_value` across `the scheduled window` and pages when configured threshold. Covers Race, Watch, Party, Status, Mix. Personal operators treat UC-25.99.28 as the canonical signal.",
  "value": "Operators running UC-25.99.28 on `race:watchparty` feeds shorten triage for Status Mix tied to 'Race Watch Party Status Mix', lowering missed training goals, stale hobby data, and connector failures.",
  "implementation": "Map the source's `party_status` values into a small set of clear labels and chart the resulting counts.",
  "visualization": "Donut or bar chart of event count by status.",
  "cimModels": [],
  "references": [
    {
      "title": "FastF1 Python API",
      "url": "https://docs.fastf1.dev/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Ergast F1 API",
      "url": "http://ergast.com/mrd/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "F1 Fantasy",
      "url": "https://fantasy.formula1.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.99.28)** — Connector for 'Race Watch Party Status Mix' stops polling `race:watchparty`; stale `count` on `status_value` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `count` over `1w` is expected for UC-25.99.28, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `race:watchparty`. Dedup with `stats latest(*) by status_value`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Race Watch Party Status Mix'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Formula 1 & Motorsport Fandom — typical feeds: FastF1 session data (`fastf1:session`), Ergast race results (`ergast:result`), F1 Fantasy teams (`f1fantasy:team`), race watch parties (`race:watchparty`), and F1 telemetry laps (`f1telemetry:lap`)..\n• **Sourcetypes in this search**: `race:watchparty`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=race:watchparty`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** FastF1 session exports, Ergast race results, F1 Fantasy team snapshots, race watch-party logs, and F1 telemetry lap dumps forwarded to Splunk HEC..\n\n**Author notes:** Map the source's `party_status` values into a small set of clear labels and chart the resulting counts.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"race:watchparty\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=race:watchparty earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Race Watch Party Status Mix** over `personal` data (primarily `sourcetype=race:watchparty`) across the full search range. The `aggregation` aggregation groups by `status_value`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `status_value`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=race:watchparty\n| eval status_value=lower(coalesce(party_status,\"unknown\"))\n| stats count as events by status_value\n| sort - events\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=race:watchparty earliest=-7d\n| head 50\n| table status_value\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=race:watchparty earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.99.28: Personal evidence` with the following panel layout:\n\n**Panel 1 — Donut Or Bar Chart Of Event Count By Status (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `race:watchparty` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=race:watchparty | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It builds the overall mood of your race watch party feed by counting how many events are healthy, warning, or bad.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
