{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.28.13",
  "title": "Tennis Serve Placement Error Map",
  "criticality": "low",
  "difficulty": "beginner",
  "monitoringType": [
    "Quality"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Tennis Serve Placement Error Map' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_tennis_serve_placement_error_map` (UC-25.28.13), SOC dashboard panel \"Personal — Tennis Serve Placement Error Map\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.28.13 positive (Tennis, Serve, Placement, Error, Map): ingest synthetic events into `index=personal` sourcetype='serve:attempt' where `sum` grouped by `zone` satisfies detection threshold for 'Tennis Serve Placement Error Map'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.28.13 negative (Error Map): ingest benign `personal` / `serve:attempt` events where `zone` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 8 confirms zero alert rows for 'Tennis Serve Placement Error Map'."
  },
  "dataSources": "Bar-velocity reps (`vbt:rep`), sprint reps (`sprint:rep`), serve attempts (`serve:attempt`), pickleball rallies (`pickleball:rally`), swim sets (`swim:set`), archery ends (`archery:end`), balance trials (`balance:trial`), and batting reps (`batting:rep`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Sports and skill telemetry from VBT devices, sprint timers, serve trackers, swim logs, archery apps, balance trainers, and batting sensors streamed to Splunk HEC via APIs and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=serve:attempt\n| stats count as attempts sum(eval(if(in=1,1,0))) as serves_in by zone\n| eval in_pct=round(100*serves_in/attempts,1)\n| sort zone",
  "description": "Identifies UC-25.28.13 — Tennis, Serve, Placement, Error, Map — using `index=personal` sourcetype `serve:attempt`, grouped by `zone`. Outputs `sum` each `the scheduled window`; alert when configured threshold. Primary focus: Error Map. Purpose-built for 'Tennis Serve Placement Error Map' in Personal workflows.",
  "value": "UC-25.28.13 accelerates insight into Error Map ('Tennis Serve Placement Error Map') by tracking `activity` on `serve:attempt` feeds, reducing missed training goals, stale hobby data, and connector failures before spreadsheet checks hide regressions.",
  "implementation": "Send serve outcomes from a practice app or court camera model with standardized service-box zones.",
  "visualization": "Court heat map of serve accuracy by zone.",
  "cimModels": [],
  "references": [
    {
      "title": "TrainingPeaks - training analytics",
      "url": "https://www.trainingpeaks.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "GoldenCheetah - open cycling analytics",
      "url": "https://www.goldencheetah.org/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.28.13)** — Connector for 'Tennis Serve Placement Error Map' stops polling `serve:attempt`; stale `sum` on `zone` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `sum` over `1w` is expected for UC-25.28.13, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `serve:attempt`. Dedup with `stats latest(*) by zone`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Tennis Serve Placement Error Map'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Sports, Skills & Training Telemetry — typical feeds: Bar-velocity reps (`vbt:rep`), running gait (`gait:metric`), hangboard sessions (`hangboard:session`), golf-swing metrics (`golfswing:metric`), power-meter intervals (`powermeter:interval`), basketbal.\n• **Sourcetypes in this search**: `serve:attempt`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=serve:attempt`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Sports and skill sensors — velocity-based-training bar trackers, running-form IMUs, climbing hangboard load cells, golf launch monitors, cycling power meters, and computer-vision shot trackers — logged to Splunk HEC via APIs and scripted inputs..\n\n**Author notes:** Send serve outcomes from a practice app or court camera model with standardized service-box zones.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"serve:attempt\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=serve:attempt earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Tennis Serve Placement Error Map** over `personal` data (primarily `sourcetype=serve:attempt`) across the full search range. The `sum` aggregation groups by `zone`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `zone`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=serve:attempt\n| stats count as attempts sum(eval(if(in=1,1,0))) as serves_in by zone\n| eval in_pct=round(100*serves_in/attempts,1)\n| sort zone\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=serve:attempt earliest=-7d\n| head 50\n| table zone\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=serve:attempt earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.28.13: Personal evidence` with the following panel layout:\n\n**Panel 1 — Court Heat Map Of Serve Accuracy By Zone (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `serve:attempt` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=serve:attempt | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It warns which parts of the service box you hit reliably and which spots still cause trouble.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
