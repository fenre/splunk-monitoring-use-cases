{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.2.19",
  "title": "Eight Sleep Temperature Intervention Spike",
  "criticality": "medium",
  "difficulty": "advanced",
  "monitoringType": [
    "Analytics",
    "Risk"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Eight Sleep Temperature Intervention Spike' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_eight_sleep_temperature_intervention_spike` (UC-25.2.19), SOC dashboard panel \"Personal — Eight Sleep Temperature Intervention Spike\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=personal` sourcetype='eightsleep:sleep' where `sleeper` satisfies `temp_adjustments` > 6 for UC-25.2.19 ('Eight Sleep Temperature Intervention Spike'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `personal` / `eightsleep:sleep` events where `sleeper` stays below `temp_adjustments` > 6, or exclude the test entity via `personal_monitoring_notes.csv` with a valid window. Confirm zero alert rows for UC-25.2.19."
  },
  "dataSources": "Recovery and biometrics (`apple:health`, `oura:daily`, `whoop:cycle`, `dexcom:egv`, `levels:glucose`, `libre:glucose`, `eightsleep:sleep`, `withings:measure`, `garmin:bodybattery`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Apple Health / Apple Watch exports, Oura, WHOOP, Dexcom, Levels, LibreView, Eight Sleep, Withings, and Garmin Body Battery feeds via vendor APIs, exports, Home Assistant bridges, and Splunk HEC. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=eightsleep:sleep\n| bin _time span=1d\n| stats sum(temp_adjustments) as temp_adjustments, avg(room_temp_c) as room_temp_c by sleeper _time\n| where temp_adjustments>6\n| sort - temp_adjustments",
  "description": "Uses Eight Sleep exports telemetry to monitor how often the bed had to correct temperature through the night in index=personal.",
  "value": "Uses Eight Sleep exports telemetry to monitor how often the bed had to correct temperature through the nigh.... Timely UC-25.2.19 alerting on 'Eight Sleep Temperature Intervention Spike' reduces missed training goals, stale hobby data, and connector failures before manual triage or audit channels escalate.",
  "implementation": "Ingest Eight Sleep exports data through its real API, export, MQTT bridge, or open-source connector into `index=personal`; preserve the fields referenced in the SPL and schedule the search on a daily cadence.",
  "visualization": "Daily chart of temperature adjustments and room temperature.",
  "cimModels": [],
  "references": [
    {
      "title": "Apple HealthKit",
      "url": "https://developer.apple.com/documentation/healthkit",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Oura API v2",
      "url": "https://cloud.ouraring.com/v2/docs",
      "retrieved": "2026-04-25"
    },
    {
      "title": "WHOOP developer platform",
      "url": "https://developer.whoop.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Dexcom developer portal",
      "url": "https://developer.dexcom.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Eight Sleep",
      "url": "https://www.eightsleep.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "LibreView",
      "url": "https://www.libreview.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Levels",
      "url": "https://www.levelshealth.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry** — Connector for 'Eight Sleep Temperature Intervention Spike' stops polling `eightsleep:sleep`; stale `sleeper` rows remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `sum` over `1d` is expected, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `eightsleep:sleep`. Dedup with `stats latest(*) by sleeper`.\n\n4. **Timezone bucket misalignment** — `1d` bins on UTC vs local schedule skew `temp_adjustments` > 6. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "mqtt"
  ],
  "equipmentModels": [
    "mqtt_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Health, Sleep & Wearables — typical feeds: Apple Health metrics (`apple:health`), Oura Ring daily readiness/sleep (`oura:daily`), Whoop recovery/strain (`whoop:cycle`), Withings body/blood-pressure (`withings:measure`), Dexcom CGM glucose (`de.\n• **Sourcetypes in this search**: `eightsleep:sleep`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=eightsleep:sleep`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Apple Health / Apple Watch exports (Health Auto Export, Shortcuts) via HEC; Oura, Whoop, Withings, Dexcom APIs via scripted REST inputs..\n\n**Author notes:** Ingest Eight Sleep exports data through its real API, export, MQTT bridge, or open-source connector into `index=personal`; preserve the fields referenced in the SPL and schedule the search on a daily cadence.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"eightsleep:sleep\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=eightsleep:sleep earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Eight Sleep Temperature Intervention Spike** over `personal` data (primarily `sourcetype=eightsleep:sleep`) across the full search range. It applies numeric thresholds on `temp_adjustments` (> 6) after sum, avg — tune these constants to your household baseline.\n\n**Fields this search depends on:** `_time`, `sleeper`, `temp_adjustments`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=eightsleep:sleep\n| bin _time span=1d\n| stats sum(temp_adjustments) as temp_adjustments, avg(room_temp_c) as room_temp_c by sleeper _time\n| where temp_adjustments>6\n| sort - temp_adjustments\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=eightsleep:sleep earliest=-7d\n| head 50\n| table _time, sleeper, temp_adjustments\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=eightsleep:sleep earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n**(d) Threshold calibration:** Profile `temp_adjustments` before alerting:\n\n```spl\nindex=personal sourcetype=eightsleep:sleep earliest=-30d\n| stats perc50(temp_adjustments) AS p50 perc90(temp_adjustments) AS p90 perc99(temp_adjustments) AS p99 max(temp_adjustments) AS hi\n```\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.2.19: Personal evidence` with the following panel layout:\n\n**Panel 1 — Daily Chart Of Temperature Adjustments And Room Temperature (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `eightsleep:sleep` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=eightsleep:sleep | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Bucket misalignment:** `span=1d` buckets on UTC by default — if your routine is local-time based, add `| eval _time=_time + (local_offset)` or use `@d` / `@w` snap.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `1d` buckets.",
  "grandmaExplanation": "It compares how often the bed had to correct temperature through the night so you can spot sleep sessions fighting room conditions or recovery stress before it becomes annoying.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
