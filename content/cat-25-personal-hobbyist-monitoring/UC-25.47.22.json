{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.47.22",
  "title": "Goodreads Review Completion Lag",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability",
    "Analytics"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'Goodreads Review Completion Lag' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_goodreads_review_completion_lag` (UC-25.47.22), SOC dashboard panel \"Personal — Goodreads Review Completion Lag\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.47.22 positive (Goodreads, Review, Completion, Lag): ingest synthetic events into `index=personal` sourcetype='reading:review' where `avg_lag_days`, `worst_lag_days` grouped by `shelf` satisfies detection threshold for 'Goodreads Review Completion Lag'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.47.22 negative (Completion Lag): ingest benign `personal` / `reading:review` events where `shelf` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 14 confirms zero alert rows for 'Goodreads Review Completion Lag'."
  },
  "dataSources": "Highlights (`reading:highlight`), book activity (`reading:book`), reviews (`reading:review`), paper libraries (`research:paper`), sync states (`reading:sync`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Reading and second-brain telemetry — Readwise review exports, Kindle highlights, Goodreads shelves, and Zotero research libraries — streamed to Splunk HEC via APIs, exports, and scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=reading:review shelf=* finished_epoch=* review_epoch=*\n| eval lag_days=round((review_epoch-finished_epoch)/86400,1)\n| stats avg(lag_days) as avg_lag_days, max(lag_days) as worst_lag_days by shelf\n| sort - avg_lag_days",
  "description": "Monitors UC-25.47.22 — Goodreads, Review, Completion, Lag — using `index=personal` sourcetype `reading:review`, grouped by `shelf`. Outputs `avg_lag_days`, `worst_lag_days` each `the scheduled window`; alert when configured threshold. Primary focus: Completion Lag. Purpose-built for 'Goodreads Review Completion Lag' in Personal workflows.",
  "value": "Alerting on Goodreads, Review, Completion, Lag through UC-25.47.22 reduces missed training goals, stale hobby data, and connector failures by surfacing `avg_lag_days`, `worst_lag_days` early.",
  "implementation": "Capture the timestamps needed to compute `review completion lag` on `reading:review` events and aggregate them by `shelf`.",
  "visualization": "Table of shelf with average and worst review completion lag.",
  "cimModels": [],
  "references": [
    {
      "title": "Readwise — review workflow",
      "url": "https://readwise.io/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Amazon Kindle",
      "url": "https://www.amazon.com/kindle-dbs/fd/kcp",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Goodreads",
      "url": "https://www.goodreads.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Zotero",
      "url": "https://www.zotero.org/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.47.22)** — Connector for 'Goodreads Review Completion Lag' stops polling `reading:review`; stale `avg_lag_days`, `worst_lag_days` on `shelf` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `avg_lag_days`, `worst_lag_days` over `1w` is expected for UC-25.47.22, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `reading:review`. Dedup with `stats latest(*) by shelf`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'Goodreads Review Completion Lag'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Reading & Second Brain — typical feeds: Reading progress (`reading:progress`), highlights (`highlight:capture`), note links (`note:link`), books finished (`book:finished`)..\n• **Sourcetypes in this search**: `reading:review`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=reading:review`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Reading and knowledge-management telemetry — Goodreads / StoryGraph libraries, Kindle highlights (Readwise), and note-graph exports (Obsidian, Notion) — streamed to Splunk HEC via APIs and scripted inputs..\n\n**Author notes:** Capture the timestamps needed to compute `review completion lag` on `reading:review` events and aggregate them by `shelf`.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"reading:review\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=reading:review earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **Goodreads Review Completion Lag** over `personal` data (primarily `sourcetype=reading:review`) across the full search range. The `avg, max` aggregation groups by `shelf`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `shelf`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=reading:review shelf=* finished_epoch=* review_epoch=*\n| eval lag_days=round((review_epoch-finished_epoch)/86400,1)\n| stats avg(lag_days) as avg_lag_days, max(lag_days) as worst_lag_days by shelf\n| sort - avg_lag_days\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=reading:review earliest=-7d\n| head 50\n| table shelf\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=reading:review earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.47.22: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Shelf With Average And Worst Review Completion Lag (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `reading:review` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=reading:review | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It records how long things take for each shelf so you can see where Goodreads reviews slows down.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
