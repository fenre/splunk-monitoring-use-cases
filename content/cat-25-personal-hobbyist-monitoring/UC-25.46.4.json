{
  "$schema": "../../schemas/uc.schema.json",
  "id": "25.46.4",
  "title": "NPC Roster and Recurring Characters",
  "criticality": "low",
  "difficulty": "intermediate",
  "monitoringType": [
    "Inventory"
  ],
  "splunkPillar": "Platform",
  "exclusions": "Covers personal hobby monitoring for 'NPC Roster and Recurring Characters' only. Does not replace enterprise SIEM, ITSM, or compliance attestation. Not intended for production security operations — exclude shared/demo HEC tokens during connector testing.",
  "evidence": "Saved search `personal_npc_roster_and_recurring_characters` (UC-25.46.4), SOC dashboard panel \"Personal — NPC Roster and Recurring Characters\", and weekly export to `index=audit_evidence` sourcetype=csv:personal_metrics.",
  "controlTest": {
    "positiveScenario": "UC-25.46.4 positive (NPC, Roster, and, Recurring, Characters): ingest synthetic events into `index=personal` sourcetype='npc:ledger' where `count` grouped by `npc` satisfies detection threshold for 'NPC Roster and Recurring Characters'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-25.46.4 negative (Recurring Characters): ingest benign `personal` / `npc:ledger` events where `npc` stays below detection threshold, or exclude the test entity via `personal_monitoring_notes.csv`. Variant 6 confirms zero alert rows for 'NPC Roster and Recurring Characters'."
  },
  "dataSources": "Campaign sessions (`campaign:session`), NPC ledger (`npc:ledger`), loot drops (`loot:drop`), XP awards (`xp:award`). Ingested via Universal Forwarder or Splunk HEC (Splunkbase 617) scripted input or token.",
  "app": "Tabletop RPG telemetry — virtual-tabletop session logs (Roll20, Foundry VTT), campaign trackers (World Anvil), and XP / loot / NPC ledgers — streamed to Splunk HEC via scripted inputs. — Universal Forwarder or Splunk HEC (Splunkbase 617)",
  "spl": "index=personal sourcetype=npc:ledger\n| stats count as appearances, latest(status) as status, latest(_time) as last_seen by npc\n| where status=\"alive\"\n| sort - appearances",
  "description": "Identifies Recurring Characters anomalies for UC-25.46.4 with window `the scheduled window` on `index=personal` sourcetype `npc:ledger`. Combines `count` by `npc` and compares against configured threshold. Mapped to keywords NPC, Roster, and, Recurring, Characters for dashboard routing.",
  "value": "Alerting on NPC, Roster, and, Recurring, Characters through UC-25.46.4 reduces missed training goals, stale hobby data, and connector failures by surfacing `activity` early.",
  "implementation": "Ingest NPC ledger entries into `index=personal`; track appearances and status per NPC.",
  "visualization": "Table of NPCs by appearances and status.",
  "cimModels": [],
  "references": [
    {
      "title": "World Anvil — worldbuilding",
      "url": "https://www.worldanvil.com/",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **OAuth or API token expiry (UC-25.46.4)** — Connector for 'NPC Roster and Recurring Characters' stops polling `npc:ledger`; stale `count` on `npc` remain in `index=personal`. Check `_internal` HEC 401s.\n\n2. **Rest day or planned break** — Zero `count` over `1w` is expected for UC-25.46.4, not ingestion failure.\n\n3. **Multi-device double-count** — Same activity synced from watch and phone into `npc:ledger`. Dedup with `stats latest(*) by npc`.\n\n4. **Timezone bucket misalignment** — `1w` bins on UTC vs local schedule skew goal threshold for 'NPC Roster and Recurring Characters'. Document offset in SPL `bin _time`.\n\n**Suppression mechanism:** Personal preference notes in `personal_monitoring_notes.csv` (not enterprise CMDB/ServiceNow registers).",
  "equipment": [
    "splunk"
  ],
  "equipmentModels": [
    "splunk_platform"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "## Prerequisites\n\n• **Splunk**: Splunk Enterprise ≥9.0 or Splunk Cloud — a single-instance home lab is fine. Create `index=personal` with 90–365 day retention (`indexes.conf`).\n• **HEC token**: Settings → Data Inputs → HTTP Event Collector → New Token → index `personal`. Store the token in your connector's environment (never commit it to git).\n• **Upstream product**: Personal HEC index — HTTP Event Collector token on index=personal.\n• **Subcategory context**: Tabletop RPG Campaigns — typical feeds: Campaign sessions (`campaign:session`), NPC ledger (`npc:ledger`), loot drops (`loot:drop`), XP awards (`xp:award`)..\n• **Sourcetypes in this search**: `npc:ledger`.\n• **Network**: The connector host must reach both the vendor API/MQTT broker and your Splunk HEC port (8088/tcp).\n\n## Step 1 — Configure data collection\n\nRoute **Personal HEC index** events into `index=personal` with `sourcetype=npc:ledger`.\n\n**Collection method:** HTTP Event Collector token on index=personal.\n\n**Reference endpoint / path:** `POST https://<splunk>:8088/services/collector/event`.\n\n**Suggested poll interval:** depends on upstream connector.\n\n**Key fields to extract:** `sourcetype-specific — see search filters`.\n\n**Typical connector stack:** Tabletop RPG telemetry — virtual-tabletop session logs (Roll20, Foundry VTT), campaign trackers (World Anvil), and XP / loot / NPC ledgers — streamed to Splunk HEC via scripted inputs..\n\n**Author notes:** Ingest NPC ledger entries into `index=personal`; track appearances and status per NPC.\n\n\nExample HEC payload shape:\n\n```json\n{\"index\": \"personal\", \"sourcetype\": \"npc:ledger\", \"event\": { ... vendor fields ... }}\n```\n\nVerify ingestion with:\n\n```spl\nindex=personal sourcetype=npc:ledger earliest=-24h | head 20\n```\n\n## Step 2 — Create the search and alert\n\nThis search implements **NPC Roster and Recurring Characters** over `personal` data (primarily `sourcetype=npc:ledger`) across the full search range. The `latest` aggregation groups by `npc`; change the `by` clause if you want per-device, per-room, or per-user breakdowns instead.\n\n**Fields this search depends on:** `npc`. Confirm each is populated before scheduling alerts.\n\n```spl\nindex=personal sourcetype=npc:ledger\n| stats count as appearances, latest(status) as status, latest(_time) as last_seen by npc\n| where status=\"alive\"\n| sort - appearances\n```\n\n## Step 3 — Validate\n\n**(a) Compare to vendor UI:** Splunk Search → index=personal | stats count by sourcetype. Totals for the same day/week should match within connector lag (usually one poll interval).\n\n**(b) Field presence:**\n\n```spl\nindex=personal sourcetype=npc:ledger earliest=-7d\n| head 50\n| table npc\n```\n\nEvery column the detection uses should be non-null on recent rows.\n\n**(c) Volume sanity:**\n\n```spl\nindex=personal sourcetype=npc:ledger earliest=-30d\n| timechart span=1d count\n```\n\nFlatlines usually mean a broken token, expired OAuth refresh, or HEC URL change.\n\n## Step 4 — Operationalize\n\nCreate a companion dashboard titled `UC-25.46.4: Personal evidence` with the following panel layout:\n\n**Panel 1 — Table Of Npcs By Appearances And Status (the key fields):** Configure according to standard Splunk visualization patterns for this panel type.\n\n\nTime picker defaults: set to last 30 days with named presets for 24h, 7d, 30d, 90d, and 1y — the auditor needs all five windows without retyping. Enable scheduled PDF export for the full dashboard on the regulation's reporting cadence.\n\n## Step 5 — Troubleshooting\n\n**Personal HEC index — no Personal HEC index events / missing fields:** If `npc:ledger` stops updating, re-check HEC token, index `personal`, and firewall to port 8088. Search `index=_internal sourcetype=splunkd group=http_event_collector` for rejection reasons. Confirm the Personal HEC index connector script is still running (`systemctl status` or cron log).\n\n**Personal HEC index — NULL values in extracted fields:** The Personal HEC index JSON export shape drifted — compare a raw event (`index=personal sourcetype=npc:ledger | head 1 | spath`) against the field names in Step 2. Re-map in the connector before changing the SPL.\n\n**Personal HEC index — API error / auth denied (401/403):** Refresh tokens expire on most consumer APIs — re-authorize the scripted input, update `passwords.conf` / env vars, and verify the vendor developer app is not in sandbox mode.\n\n**Personal HEC index — endpoint timeout / HTTP 429 throttling:** Back off the poll interval in the connector; Personal HEC index APIs often cap requests per 15 minutes on free tiers.\n\n**Stale dashboard:** Confirm the saved search `dispatch.earliest_time` covers enough history for your `aggregation` buckets.",
  "grandmaExplanation": "It keeps a list of the characters the group has met, tracking who keeps coming back into the story.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
