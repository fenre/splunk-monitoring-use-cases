{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.4.15",
  "title": "Cloud Resource Tag Compliance and Drift",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Cloud Resource Tag Compliance and Drift' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_cloud_resource_tag_compliance_and_drift` (UC-4.4.15), SOC dashboard panel \"Operations — Cloud Resource Tag Compliance and Drift\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=aws` sourcetype='aws:config:resource' where `resourceType` satisfies `count` > 0 for UC-4.4.15 ('Cloud Resource Tag Compliance and Drift'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `aws` / `aws:config:resource` events where `resourceType` stays below `count` > 0, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-4.4.15."
  },
  "dataSources": "AWS Config (resource compliance), Azure Policy compliance, GCP labels API Collected via Splunk Add-on for Amazon Web Services (Splunkbase 1876) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_aws`, Azure Resource Graph, GCP Asset Inventory — Splunk Add-on for Amazon Web Services (Splunkbase 1876)",
  "spl": "index=aws sourcetype=\"aws:config:resource\" tag_compliance=\"non_compliant\"\n| stats count by resourceType, account_id, region\n| where count > 0\n| sort -count",
  "description": "Missing or inconsistent tags (Owner, CostCenter, Environment) block cost allocation, automation, and governance. Detecting untagged or non-compliant resources supports tag policy enforcement.",
  "value": "Early 'Cloud Resource Tag Compliance and Drift' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Use AWS Config rules (required-tags), Azure Policy (e.g. RequireTagAndValue), or GCP org policy for label requirements. Ingest compliance results. Alert when net new untagged resources appear or compliance score drops below threshold. Dashboard by OU/account and resource type.",
  "visualization": "Table (account, resource type, non-compliant count), Gauge (tag compliance %), Bar chart by tag key missing.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk_TA_aws",
      "url": "https://splunkbase.splunk.com/app/1876",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `aws`** — Planned work for 'Cloud Resource Tag Compliance and Drift' shifts `count` over `span=1h` while `resourceType` remains in-scope for UC-4.4.15. Cross-check CMDB CHG.\n\n2. **Batch `count` on `aws:config:resource`** — Scheduled automation elevates `account_id` without crossing `count` > 0. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `resourceType`** — Health checks in `aws` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `aws:config:resource`** — First 48h baseline for `resourceType` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `resourceType`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1578"
  ],
  "equipment": [
    "aws",
    "azure",
    "gcp"
  ],
  "equipmentModels": [
    "aws_generic",
    "azure_generic",
    "gcp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_aws`, Azure Resource Graph, GCP Asset Inventory.\n• Ensure the following data sources are available: AWS Config (resource compliance), Azure Policy compliance, GCP labels API.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nUse AWS Config rules (required-tags), Azure Policy (e.g. RequireTagAndValue), or GCP org policy for label requirements. Ingest compliance results. Alert when net new untagged resources appear or compliance score drops below threshold. Dashboard by OU/account and resource type.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=aws sourcetype=\"aws:config:resource\" tag_compliance=\"non_compliant\"\n| stats count by resourceType, account_id, region\n| where count > 0\n| sort -count\n```\n\nUnderstanding this SPL\n\n**Cloud Resource Tag Compliance and Drift** — Missing or inconsistent tags (Owner, CostCenter, Environment) block cost allocation, automation, and governance. Detecting untagged or non-compliant resources supports tag policy enforcement.\n\nDocumented **Data sources**: AWS Config (resource compliance), Azure Policy compliance, GCP labels API. **App/TA** (typical add-on context): `Splunk_TA_aws`, Azure Resource Graph, GCP Asset Inventory. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: aws; **sourcetype**: aws:config:resource. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=aws, sourcetype=\"aws:config:resource\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by resourceType, account_id, region** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where count > 0` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (account, resource type, non-compliant count), Gauge (tag compliance %), Bar chart by tag key missing.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_aws`, Azure Resource Graph, GCP Asset Inventory — Splunk Add-on for Amazon Web Services (Splunkbase 1876)). Enable the modular input or HEC token that writes sourcetype=aws:config:resource into aws. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`account_id, region, resourceType`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Missing or inconsistent tags (Owner, CostCenter, Environment) block cost allocation, automation, and governance.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
