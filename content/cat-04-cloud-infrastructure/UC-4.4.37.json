{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.4.37",
  "title": "Cross-Cloud MFA Disable Events",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Governance",
    "Security"
  ],
  "splunkPillar": "Platform",
  "compliance": [
    {
      "regulation": "NIST-800-53",
      "version": "Rev.5",
      "clause": "AU-2",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Saved search evidence for AU-2 when alert outputs are archived."
    }
  ],
  "controlTest": {
    "positiveScenario": "Replay or inject sample `aws:cloudtrail` events matching `*` into `index=cloud_multi`. Confirm UC-4.4.37 search returns >=1 row and alert fires.",
    "negativeScenario": "Replay benign `aws:cloudtrail` traffic without `*` markers. Confirm the search returns zero rows and alert does not fire."
  },
  "dataSources": "`index=cloud_multi` sourcetype=`aws:cloudtrail` collected via Splunk Add-on for AWS + Microsoft Cloud Services + GCP (multi-index correlation); validate ingest with `| stats count by sourcetype`.",
  "app": "Splunk Add-on for AWS + Microsoft Cloud Services + GCP (multi-index correlation)",
  "spl": "index=cloud_multi sourcetype=\"aws:cloudtrail\"\n| table _time userIdentity.arn eventName sourceIPAddress awsRegion\n| eval uc_marker=\"UC-4.4.37_cross_cloud_mfa_disable_37\"\n| sort -_time",
  "description": "Correlates MFA removal on privileged identities in AWS IAM, Entra ID, GCP IAM. This UC monitors `aws:cloudtrail` for cross-cloud mfa disable events and surfaces events for SOC triage, change review, and compliance evidence.",
  "value": "MFA disable across clouds is a strong account takeover signal. Detecting cross-cloud mfa disable events early reduces mean time to contain cloud incidents, supports audit logging controls, and prevents silent misconfiguration drift across accounts.",
  "implementation": "Configure required cloud inputs per provider guide; index=cloud_multi sourcetype=aws:cloudtrail.",
  "visualization": "Dashboard panel, alert, and weekly compliance report.",
  "cimModels": [],
  "references": [
    {
      "title": "Splunk Add-on for AWS",
      "url": "https://splunkbase.splunk.com/app/1876"
    },
    {
      "title": "Vendor documentation",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Overview"
    }
  ],
  "knownFalsePositives": "Approved change windows; documented automation service principals; sandbox accounts excluded via lookup.",
  "mitreAttack": [
    "T1078.004"
  ],
  "securityDomain": "cloud",
  "equipment": [
    "aws",
    "azure",
    "gcp",
    "m365"
  ],
  "equipmentModels": [
    "aws_cloudtrail"
  ],
  "status": "community",
  "cost": {
    "tier": "medium",
    "search_load": "moderate",
    "tstats_eligible": false
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=cloud_multi`.\n• Install and configure Splunk Add-on for AWS + Microsoft Cloud Services + GCP (multi-index correlation) with the modular input for `aws:cloudtrail`.\n• Prerequisite crawl UC(s): UC-4.4.33.\n• Confirm field extractions in `props.conf` for `userIdentity.arn` and related actor fields.\n\nStep 1 — Configure data collection\nConfigure required cloud inputs per provider guide; index=cloud_multi sourcetype=aws:cloudtrail.\nValidate ingest before alerting:\n```spl\nindex=cloud_multi sourcetype=\"aws:cloudtrail\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=cloud_multi sourcetype=\"aws:cloudtrail\"\n| table _time userIdentity.arn eventName sourceIPAddress awsRegion\n| eval uc_marker=\"UC-4.4.37_cross_cloud_mfa_disable_37\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `aws:cloudtrail` in `index=cloud_multi` to detect cross-cloud mfa disable events. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the AWS CloudTrail console Event history for the same 24-hour window. Confirm row counts and `userIdentity.arn` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_4_37_alert` with severity aligned to `high` criticality. Dashboard: Dashboard panel, alert, and weekly compliance report..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade Splunk Add-on for AWS + Microsoft Cloud Services + GCP (multi-index correlation) and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of cross-cloud mfa disable events so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.4.33"
  ],
  "splunkbaseApps": [
    {
      "id": 1876,
      "name": "Splunk Add-on for AWS",
      "role": "primary",
      "requiresSmeReview": true
    }
  ],
  "wave": "walk"
}
