{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.6.3",
  "title": "Cloud Security Finding Trending",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "controlTest": {
    "positiveScenario": "Inject sample events matching UC-4.6.3 SPL; confirm the search returns rows and alert fires.",
    "negativeScenario": "Replay benign traffic without matching markers; confirm UC-4.6.3 search returns zero rows."
  },
  "dataSources": "`index=cloud` sourcetype=`aws:cloudtrail` collected via AWS Security Hub, Azure Defender, GCP Security Command Center — forwarded via HEC or add-on; validate ingest with `| stats count by sourcetype`.",
  "app": "AWS Security Hub, Azure Defender, GCP Security Command Center — forwarded via HEC or add-on",
  "spl": "index=cloud sourcetype=\"aws:cloudtrail\" sourcetype IN (\"aws:securityhub:finding\", \"azure:defender:alert\", \"gcp:scc:finding\")\n| table _time userIdentity.arn eventName sourceIPAddress awsRegion\n| eval uc_marker=\"UC-4.6.3_cloud_security_finding_t_3\"\n| sort -_time",
  "description": "Tracking new versus resolved security findings over time shows whether your cloud security posture is improving and whether scanners or policies are flagging more issues than teams can remediate. Supports executive reporting and backlog triage.",
  "value": "Detecting cloud security finding trending early reduces mean time to contain cloud incidents, supports audit logging controls, and prevents silent misconfiguration drift across accounts.",
  "implementation": "Map your findings feed so each event represents a finding state change or daily snapshot with Severity and status. For snapshot models, compare consecutive days to derive new and resolved counts via summary search. Align severities (Critical/High/Medium) across clouds for a combined view or use separate panels per provider. Refresh suppression lookups so trends reflect true risk.",
  "visualization": "Stacked column chart (new vs resolved per day), line chart (open critical count trend), area chart (cumulative open findings).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/"
    },
    {
      "title": "Vendor cloud documentation",
      "url": "https://docs.aws.amazon.com/"
    }
  ],
  "knownFalsePositives": "A fresh scanner run or a vendor re-rate can resurface a finding we already know about. We deduplicate on resource and time and we only escalate when the exposure is new or the severity stepped up.",
  "mitreAttack": [
    "T1580",
    "T1526"
  ],
  "securityDomain": "cloud",
  "equipment": [
    "aws",
    "azure",
    "gcp"
  ],
  "equipmentModels": [
    "aws_cloudtrail"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=cloud`.\n• Install and configure AWS Security Hub, Azure Defender, GCP Security Command Center — forwarded via HEC or add-on with the modular input for `aws:cloudtrail`.\n• Prerequisite crawl UC(s): UC-4.6.7.\n• Confirm field extractions in `props.conf` for `userIdentity.arn` and related actor fields.\n\nStep 1 — Configure data collection\nMap your findings feed so each event represents a finding state change or daily snapshot with Severity and status. For snapshot models, compare consecutive days to derive new and resolved counts via summary search. Align severities (Critical/High/Medium) across clouds for a combined view or use separate panels per provider. Refresh suppression lookups so trends reflect true risk.\nValidate ingest before alerting:\n```spl\nindex=cloud sourcetype=\"aws:cloudtrail\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=cloud sourcetype=\"aws:cloudtrail\" sourcetype IN (\"aws:securityhub:finding\", \"azure:defender:alert\", \"gcp:scc:finding\")\n| table _time userIdentity.arn eventName sourceIPAddress awsRegion\n| eval uc_marker=\"UC-4.6.3_cloud_security_finding_t_3\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `aws:cloudtrail` in `index=cloud` to detect cloud security finding trending. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the AWS CloudTrail console Event history for the same 24-hour window. Confirm row counts and `userIdentity.arn` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_6_3_alert` with severity aligned to `high` criticality. Dashboard: Stacked column chart (new vs resolved per day), line chart (open critical count trend), area chart (cumulative open findings)..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade AWS Security Hub, Azure Defender, GCP Security Command Center — forwarded via HEC or add-on and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of cloud security finding trending so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.6.7"
  ],
  "wave": "walk"
}
