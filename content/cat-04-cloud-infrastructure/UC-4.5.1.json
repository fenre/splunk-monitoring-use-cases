{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.5.1",
  "title": "Lambda Invocation Errors and Failed Invocations",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "controlTest": {
    "positiveScenario": "Replay sample `aws:cloudwatch` events with `namespace=\"AWS/Lambda\" metric_name=\"Errors\"` into `index=aws`. Confirm UC-4.5.1 returns >=1 row and the alert fires.",
    "negativeScenario": "Replay benign `aws:cloudwatch` traffic without `namespace=\"AWS/Lambda\" metric_name=\"Errors\"`. Confirm UC-4.5.1 returns zero rows and the alert does not fire."
  },
  "dataSources": "`index=aws` sourcetype=`aws:cloudwatch` collected via `Splunk_TA_aws`; validate ingest with `| stats count by sourcetype`.",
  "app": "`Splunk_TA_aws`",
  "spl": "index=aws sourcetype=\"aws:cloudwatch\" namespace=\"AWS/Lambda\" metric_name=\"Errors\"\n| table _time metric_name namespace unit average maximum\n| eval uc_marker=\"UC-4.5.1_lambda_invocation_errors_1\"\n| sort -_time",
  "description": "UC-4.5.1 — Lambda Invocation Errors and Failed Invocations: queries AWS `aws:cloudwatch` for search criteria `namespace=\"AWS/Lambda\" metric_name=\"Errors\"` and returns actor, resource, and region context for availability review and change validation.",
  "value": "On AWS, detecting 'Lambda Invocation Errors and Failed Invocations' early shortens cloud incident response and produces defensible audit evidence for identity and change-management controls. UC-4.5.1 drives high-priority availability alerting before customer-impacting drift.",
  "implementation": "Enable CloudWatch metric collection for the Lambda namespace (Errors, Invocations). Ingest via Splunk_TA_aws. Optionally correlate with Lambda application logs from CloudWatch Logs subscription. Alert when error rate exceeds policy (for example 1–5% sustained over 15 minutes).",
  "visualization": "Line chart (errors and invocations over time by function), Single value (error rate %), Table (FunctionName, errors, invocations, error_rate).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk_TA_aws",
      "url": "https://splunkbase.splunk.com/app/1876"
    },
    {
      "title": "Vendor cloud documentation",
      "url": "https://docs.aws.amazon.com/"
    }
  ],
  "knownFalsePositives": "Cold starts, retries, and a bad client sending junk traffic can all raise error counts without a code bug. We look at the share of invocations and we compare the same time last week before we assume the function is broken.",
  "securityDomain": "cloud",
  "equipment": [
    "aws"
  ],
  "equipmentModels": [
    "aws_cloudwatch"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=aws`.\n• Install and configure `Splunk_TA_aws` with the modular input for `aws:cloudwatch`.\n• Prerequisite crawl UC(s): UC-4.5.16.\n• Confirm field extractions in `props.conf` for `metric_name` and related actor fields.\n\nStep 1 — Configure data collection\nEnable CloudWatch metric collection for the Lambda namespace (Errors, Invocations). Ingest via Splunk_TA_aws. Optionally correlate with Lambda application logs from CloudWatch Logs subscription. Alert when error rate exceeds policy (for example 1–5% sustained over 15 minutes).\nValidate ingest before alerting:\n```spl\nindex=aws sourcetype=\"aws:cloudwatch\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=aws sourcetype=\"aws:cloudwatch\" namespace=\"AWS/Lambda\" metric_name=\"Errors\"\n| table _time metric_name namespace unit average maximum\n| eval uc_marker=\"UC-4.5.1_lambda_invocation_errors_1\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `aws:cloudwatch` in `index=aws` to detect lambda invocation errors and failed invocations. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the Amazon CloudWatch console Metrics for the same 24-hour window. Confirm row counts and `metric_name` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_5_1_alert` with severity aligned to `high` criticality. Dashboard: Line chart (errors and invocations over time by function), Single value (error rate %), Table (FunctionName, errors, invocations, error_rate)..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade `Splunk_TA_aws` and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of lambda invocation errors and failed invocations so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.5.16"
  ],
  "wave": "walk"
}
