{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.4.6",
  "title": "Multi-Cloud Security Posture (CSPM) Findings",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "controlTest": {
    "positiveScenario": "Inject sample events matching UC-4.4.6 SPL; confirm the search returns rows and alert fires.",
    "negativeScenario": "Replay benign traffic without matching markers; confirm UC-4.4.6 search returns zero rows."
  },
  "dataSources": "`index=security` sourcetype=`aws:cloudtrail` collected via Splunk TAs for each cloud, or CSPM product integration (e.g. Prisma Cloud, Wiz); validate ingest with `| stats count by sourcetype`.",
  "app": "Splunk TAs for each cloud, or CSPM product integration (e.g. Prisma Cloud, Wiz)",
  "spl": "index=security sourcetype=\"aws:cloudtrail\" (sourcetype=aws:securityhub OR sourcetype=azure:defender OR sourcetype=gcp:scc)\n| table _time userIdentity.arn eventName sourceIPAddress awsRegion\n| eval uc_marker=\"UC-4.4.6_multi_cloud_security_pos_6\"\n| sort -_time",
  "description": "CSPM tools (or native Security Hub, Defender, SCC) produce findings across clouds. Centralizing in Splunk enables unified prioritization and remediation tracking.",
  "value": "Detecting multi-cloud security posture (cspm) findings early reduces mean time to contain cloud incidents, supports audit logging controls, and prevents silent misconfiguration drift across accounts.",
  "implementation": "Ingest Security Hub, Defender for Cloud, and SCC findings into a common index. Normalize severity and finding type. Alert on new critical/high. Dashboard open findings by cloud, severity, and category (e.g. encryption, networking).",
  "visualization": "Table (cloud, severity, type, count), Bar chart (findings by cloud), Trend line (findings over time).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/"
    },
    {
      "title": "Vendor cloud documentation",
      "url": "https://docs.aws.amazon.com/"
    }
  ],
  "knownFalsePositives": "A fresh scanner run or a vendor re-rate can resurface a finding we already know about. We deduplicate on resource and time and we only escalate when the exposure is new or the severity stepped up.",
  "mitreAttack": [
    "T1580",
    "T1526"
  ],
  "securityDomain": "cloud",
  "equipment": [
    "aws",
    "azure",
    "defender",
    "m365",
    "nutanix",
    "paloalto"
  ],
  "equipmentModels": [
    "aws_cloudtrail",
    "paloalto_prisma"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=security`.\n• Install and configure Splunk TAs for each cloud, or CSPM product integration (e.g. Prisma Cloud, Wiz) with the modular input for `aws:cloudtrail`.\n• Prerequisite crawl UC(s): UC-4.4.33.\n• Confirm field extractions in `props.conf` for `userIdentity.arn` and related actor fields.\n\nStep 1 — Configure data collection\nIngest Security Hub, Defender for Cloud, and SCC findings into a common index. Normalize severity and finding type. Alert on new critical/high. Dashboard open findings by cloud, severity, and category (e.g. encryption, networking).\nValidate ingest before alerting:\n```spl\nindex=security sourcetype=\"aws:cloudtrail\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=security sourcetype=\"aws:cloudtrail\" (sourcetype=aws:securityhub OR sourcetype=azure:defender OR sourcetype=gcp:scc)\n| table _time userIdentity.arn eventName sourceIPAddress awsRegion\n| eval uc_marker=\"UC-4.4.6_multi_cloud_security_pos_6\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `aws:cloudtrail` in `index=security` to detect multi-cloud security posture (cspm) findings. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the AWS CloudTrail console Event history for the same 24-hour window. Confirm row counts and `userIdentity.arn` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_4_6_alert` with severity aligned to `high` criticality. Dashboard: Table (cloud, severity, type, count), Bar chart (findings by cloud), Trend line (findings over time)..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade Splunk TAs for each cloud, or CSPM product integration (e.g. Prisma Cloud, Wiz) and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of multi-cloud security posture (cspm) findings so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.4.33"
  ],
  "wave": "walk"
}
