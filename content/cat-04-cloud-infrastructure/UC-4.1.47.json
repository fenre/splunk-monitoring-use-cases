{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.1.47",
  "title": "Glue Job Run Failures and Duration",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "controlTest": {
    "positiveScenario": "Replay sample `aws:cloudwatch` events with `namespace=\"AWS/Glue\" metric_name=\"JobRunFailureCount\"` into `index=aws`. Confirm UC-4.1.47 returns >=1 row and the alert fires.",
    "negativeScenario": "Replay benign `aws:cloudwatch` traffic without `namespace=\"AWS/Glue\" metric_name=\"JobRunFailureCount\"`. Confirm UC-4.1.47 returns zero rows and the alert does not fire."
  },
  "dataSources": "`index=aws` sourcetype=`aws:cloudwatch` collected via `Splunk_TA_aws`; validate ingest with `| stats count by sourcetype`.",
  "app": "`Splunk_TA_aws`",
  "spl": "index=aws sourcetype=\"aws:cloudwatch\" namespace=\"AWS/Glue\" metric_name=\"JobRunFailureCount\"\n| table _time metric_name namespace unit average maximum\n| eval uc_marker=\"UC-4.1.47_glue_job_run_failures_an_47\"\n| sort -_time",
  "description": "UC-4.1.47 — Glue Job Run Failures and Duration: queries AWS `aws:cloudwatch` for search criteria `namespace=\"AWS/Glue\" metric_name=\"JobRunFailureCount\"` and returns actor, resource, and region context for fault review and change validation.",
  "value": "On AWS, detecting 'Glue Job Run Failures and Duration' early shortens cloud incident response and produces defensible audit evidence for identity and change-management controls. UC-4.1.47 drives high-priority fault alerting before customer-impacting drift.",
  "implementation": "Collect Glue metrics. Alert when JobRunFailureCount > 0. Track JobRunDuration for SLA and DPU tuning. Ingest job run events from EventBridge for run-level detail.",
  "visualization": "Line chart (failures, duration by job), Table (job, failure count), Single value.",
  "cimModels": [
    "Change",
    "Performance"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Change.All_Changes\n  where match(All_Changes.app, \"(?i)glue|lambda|logs\")\n  by All_Changes.user All_Changes.status span=1h\n| sort -count",
  "dataModelAcceleration": "Change / Performance: only if the TA maps job or platform logs; raw CloudWatch/CloudTrail is usually primary. Enable Data Model Acceleration for the CIM data models in `cimModels` once `Splunk_TA_aws` (and any CIM tags/aliases) map your fields; otherwise the `cimSpl` summary search may be empty. Step 2 on raw or metric indexes stays primary for most sites.",
  "references": [
    {
      "title": "Splunk_TA_aws",
      "url": "https://splunkbase.splunk.com/app/1876"
    },
    {
      "title": "Vendor cloud documentation",
      "url": "https://docs.aws.amazon.com/"
    }
  ],
  "knownFalsePositives": "Known slow cold starts, scheduled batch jobs, and canary invocations in lower envs you already noise-reduced.",
  "securityDomain": "cloud",
  "equipment": [
    "aws"
  ],
  "equipmentModels": [
    "aws_cloudwatch"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=aws`.\n• Install and configure `Splunk_TA_aws` with the modular input for `aws:cloudwatch`.\n• Prerequisite crawl UC(s): UC-4.1.78.\n• Confirm field extractions in `props.conf` for `metric_name` and related actor fields.\n\nStep 1 — Configure data collection\nCollect Glue metrics. Alert when JobRunFailureCount > 0. Track JobRunDuration for SLA and DPU tuning. Ingest job run events from EventBridge for run-level detail.\nValidate ingest before alerting:\n```spl\nindex=aws sourcetype=\"aws:cloudwatch\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=aws sourcetype=\"aws:cloudwatch\" namespace=\"AWS/Glue\" metric_name=\"JobRunFailureCount\"\n| table _time metric_name namespace unit average maximum\n| eval uc_marker=\"UC-4.1.47_glue_job_run_failures_an_47\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `aws:cloudwatch` in `index=aws` to detect glue job run failures and duration. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the Amazon CloudWatch console Metrics for the same 24-hour window. Confirm row counts and `metric_name` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_1_47_alert` with severity aligned to `high` criticality. Dashboard: Line chart (failures, duration by job), Table (job, failure count), Single value..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade `Splunk_TA_aws` and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of glue job run failures and duration so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.1.78"
  ],
  "splunkbaseApps": [
    {
      "id": 1876,
      "name": "Splunk Add-on for AWS",
      "role": "primary",
      "requiresSmeReview": true
    }
  ],
  "wave": "walk"
}
