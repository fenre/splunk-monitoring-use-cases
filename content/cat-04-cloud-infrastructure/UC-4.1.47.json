{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.1.47",
  "title": "Glue Job Run Failures and Duration",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Glue Job Run Failures and Duration' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_glue_job_run_failures_and_duration` (UC-4.1.47), SOC dashboard panel \"Operations — Glue Job Run Failures and Duration\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-4.1.47 positive (Glue, Job, Run, Failures, and): ingest synthetic events into `index=aws` sourcetype='aws:cloudwatch' where `sum` grouped by `JobName` satisfies `Sum` > 0 for 'Glue Job Run Failures and Duration'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-4.1.47 negative (and Duration): ingest benign `aws` / `aws:cloudwatch` events where `JobName` stays below `Sum` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 8 confirms zero alert rows for 'Glue Job Run Failures and Duration'."
  },
  "dataSources": "CloudWatch Glue metrics (JobRunFailureCount, JobRunDuration), Glue job run history Collected via Splunk Add-on for Amazon Web Services (Splunkbase 1876) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_aws` — Splunk Add-on for Amazon Web Services (Splunkbase 1876)",
  "spl": "index=aws sourcetype=\"aws:cloudwatch\" namespace=\"AWS/Glue\" metric_name=\"JobRunFailureCount\"\n| where Sum > 0\n| timechart span=1h sum(Sum) by JobName",
  "description": "Alerts on UC-4.1.47 — Glue, Job, Run, Failures, and — using `index=aws` sourcetype `aws:cloudwatch`, grouped by `JobName`. Outputs `sum` each `1h`; alert when `Sum` > 0. Primary focus: and Duration. Purpose-built for 'Glue Job Run Failures and Duration' in Operations workflows.",
  "value": "UC-4.1.47 accelerates insight into and Duration ('Glue Job Run Failures and Duration') by tracking `activity` on `aws:cloudwatch` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Collect Glue metrics. Alert when JobRunFailureCount > 0. Track JobRunDuration for SLA and DPU tuning. Ingest job run events from EventBridge for run-level detail.",
  "visualization": "Line chart (failures, duration by job), Table (job, failure count), Single value.",
  "cimModels": [
    "Change",
    "Performance"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Change.All_Changes\n  where match(All_Changes.app, \"(?i)glue|lambda|logs\")\n  by All_Changes.user All_Changes.status span=1h\n| sort -count",
  "dataModelAcceleration": "Change / Performance: only if the TA maps job or platform logs; raw CloudWatch/CloudTrail is usually primary. Enable Data Model Acceleration for the CIM data models in `cimModels` once `Splunk_TA_aws` (and any CIM tags/aliases) map your fields; otherwise the `cimSpl` summary search may be empty. Step 2 on raw or metric indexes stays primary for most sites.",
  "references": [
    {
      "title": "Splunk_TA_aws",
      "url": "https://splunkbase.splunk.com/app/1876",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Change",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Change",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Performance",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Performance",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `aws` (UC-4.1.47)** — Planned work for 'Glue Job Run Failures and Duration' shifts `sum` over `span=1h` while `JobName` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `sum` on `aws:cloudwatch`** — Scheduled automation elevates `src` without crossing `Sum` > 0 for UC-4.1.47. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `JobName`** — Health checks in `aws` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `aws:cloudwatch`** — First 48h baseline for UC-4.1.47 on `JobName` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `JobName`, `exception_reason`, `valid_until`.",
  "equipment": [
    "aws"
  ],
  "equipmentModels": [
    "aws_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_aws`.\n• Ensure the following data sources are available: CloudWatch Glue metrics (JobRunFailureCount, JobRunDuration), Glue job run history.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nCollect Glue metrics. Alert when JobRunFailureCount > 0. Track JobRunDuration for SLA and DPU tuning. Ingest job run events from EventBridge for run-level detail.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=aws sourcetype=\"aws:cloudwatch\" namespace=\"AWS/Glue\" metric_name=\"JobRunFailureCount\"\n| where Sum > 0\n| timechart span=1h sum(Sum) by JobName\n```\n\nUnderstanding this SPL\n\n**Glue Job Run Failures and Duration** — Glue job failures break ETL pipelines. Duration trends support capacity and cost optimization.\n\nDocumented **Data sources**: CloudWatch Glue metrics (JobRunFailureCount, JobRunDuration), Glue job run history. **App/TA** (typical add-on context): `Splunk_TA_aws`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: aws; **sourcetype**: aws:cloudwatch. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=aws, sourcetype=\"aws:cloudwatch\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where Sum > 0` — typically the threshold or rule expression for this monitoring goal.\n• `timechart` plots the metric over time using **span=1h** buckets with a separate series **by JobName** — ideal for trending and alerting on this use case.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (failures, duration by job), Table (job, failure count), Single value.\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_aws` — Splunk Add-on for Amazon Web Services (Splunkbase 1876)). Enable the modular input or HEC token that writes sourcetype=aws:cloudwatch into aws. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`JobName, Sum`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We keep an eye on glue job run failures and duration. Glue job failures break ETL pipelines. Duration trends support capacity and cost optimization. We want the right people to notice in time, not only when a customer or an auditor stumbles on it first.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
