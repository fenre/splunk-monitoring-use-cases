{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.6.4",
  "title": "S3/Blob Storage Growth Trending",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Observability",
  "controlTest": {
    "positiveScenario": "Inject sample events matching UC-4.6.4 SPL; confirm the search returns rows and alert fires.",
    "negativeScenario": "Replay benign traffic without matching markers; confirm UC-4.6.4 search returns zero rows."
  },
  "dataSources": "`index=cloud` sourcetype=`aws:cloudwatch` collected via Splunk Add-on for AWS (S3 storage metrics), Azure Monitor metrics; validate ingest with `| stats count by sourcetype`.",
  "app": "Splunk Add-on for AWS (S3 storage metrics), Azure Monitor metrics",
  "spl": "index=cloud sourcetype=\"aws:cloudwatch\" Namespace=\"AWS/S3\" MetricName=\"BucketSizeBytes\"\n| table _time metric_name namespace unit average maximum\n| eval uc_marker=\"UC-4.6.4_s3_blob_storage_growth_t_4\"\n| sort -_time",
  "description": "Total object storage bytes month over month highlights data hoarding, log retention growth, or unexpected replication. Supports budgeting and lifecycle policy decisions before bills spike.",
  "value": "Detecting s3/blob storage growth trending early reduces mean time to contain cloud incidents, supports audit logging controls, and prevents silent misconfiguration drift across accounts.",
  "implementation": "Ingest daily CloudWatch BucketSizeBytes per bucket or storage account metrics for Azure/Blob. Use span=1mon aligned to calendar months for FinOps reporting. Convert bytes to TB for readability. Optionally exclude archive buckets matched to a lookup. Alert on month-over-month growth above a percentage threshold. Use predict to forecast 3 months ahead for capacity planning.",
  "visualization": "Line chart (total TB monthly with 3-month forecast), bar chart (top buckets by size), table (month-over-month growth %).",
  "cimModels": [
    "Performance"
  ],
  "cimSpl": "| tstats `summariesonly` avg(Performance.storage_free_percent) as free_pct\n  from datamodel=Performance where nodename=Performance.Storage\n  by Performance.host span=1h\n| sort 10 free_pct",
  "dataModelAcceleration": "Enable data model acceleration for Performance (Storage node).",
  "references": [
    {
      "title": "Splunk Add-on for AWS",
      "url": "https://splunkbase.splunk.com/app/1876"
    },
    {
      "title": "CIM: Performance",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Performance"
    }
  ],
  "knownFalsePositives": "Inventories can skip a run when an API is throttled or a connector is in maintenance, which looks like a big drop. We check the ingest time and the last good snapshot before we call it shrinkage.",
  "securityDomain": "cloud",
  "equipment": [
    "aws",
    "azure"
  ],
  "equipmentModels": [
    "aws_cloudwatch"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=cloud`.\n• Install and configure Splunk Add-on for AWS (S3 storage metrics), Azure Monitor metrics with the modular input for `aws:cloudwatch`.\n• Prerequisite crawl UC(s): UC-4.6.7.\n• Confirm field extractions in `props.conf` for `metric_name` and related actor fields.\n\nStep 1 — Configure data collection\nIngest daily CloudWatch BucketSizeBytes per bucket or storage account metrics for Azure/Blob. Use span=1mon aligned to calendar months for FinOps reporting. Convert bytes to TB for readability. Optionally exclude archive buckets matched to a lookup. Alert on month-over-month growth above a percentage threshold. Use predict to forecast 3 months ahead for capacity planning.\nValidate ingest before alerting:\n```spl\nindex=cloud sourcetype=\"aws:cloudwatch\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=cloud sourcetype=\"aws:cloudwatch\" Namespace=\"AWS/S3\" MetricName=\"BucketSizeBytes\"\n| table _time metric_name namespace unit average maximum\n| eval uc_marker=\"UC-4.6.4_s3_blob_storage_growth_t_4\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `aws:cloudwatch` in `index=cloud` to detect s3/blob storage growth trending. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the Amazon CloudWatch console Metrics for the same 24-hour window. Confirm row counts and `metric_name` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_6_4_alert` with severity aligned to `medium` criticality. Dashboard: Line chart (total TB monthly with 3-month forecast), bar chart (top buckets by size), table (month-over-month growth %)..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade Splunk Add-on for AWS (S3 storage metrics), Azure Monitor metrics and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of s3/blob storage growth trending so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.6.7"
  ],
  "wave": "walk"
}
