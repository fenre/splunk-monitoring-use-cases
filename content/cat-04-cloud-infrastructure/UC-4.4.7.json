{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.4.7",
  "title": "Cross-Cloud Log Ingestion Pipeline Health",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Fault"
  ],
  "splunkPillar": "Security",
  "controlTest": {
    "positiveScenario": "Replay sample `aws:cloudtrail` events with `source=*metrics* group=per_sourcetype_thruput` into `index=_internal`. Confirm UC-4.4.7 returns >=1 row and the alert fires.",
    "negativeScenario": "Replay benign `aws:cloudtrail` traffic without `source=*metrics* group=per_sourcetype_thruput`. Confirm UC-4.4.7 returns zero rows and the alert does not fire."
  },
  "dataSources": "`index=_internal` sourcetype=`aws:cloudtrail` collected via Splunk _internal, ingest metrics, or custom heartbeat; validate ingest with `| stats count by sourcetype`.",
  "app": "Splunk _internal, ingest metrics, or custom heartbeat",
  "spl": "index=_internal sourcetype=\"aws:cloudtrail\" source=*metrics* group=per_sourcetype_thruput\n| table _time userIdentity.arn eventName sourceIPAddress awsRegion\n| eval uc_marker=\"UC-4.4.7_cross_cloud_log_ingestio_7\"\n| sort -_time",
  "description": "UC-4.4.7 — Cross-Cloud Log Ingestion Pipeline Health: queries multi-cloud (AWS/Azure/GCP) `aws:cloudtrail` for search criteria `source=*metrics* group=per_sourcetype_thruput` and returns actor, resource, and region context for security review and change validation.",
  "value": "On multi-cloud (AWS/Azure/GCP), detecting 'Cross-Cloud Log Ingestion Pipeline Health' early shortens cloud incident response and produces defensible audit evidence for identity and change-management controls. UC-4.4.7 drives high-priority security alerting before customer-impacting drift.",
  "implementation": "Track last event time per cloud sourcetype (e.g. aws:cloudtrail, mscs:azure:audit, google:gcp:pubsub). Alert when no events received for >15–30 minutes. Monitor Event Hub consumer lag and Pub/Sub subscription backlog as pipeline indicators.",
  "visualization": "Table (sourcetype, last event, delay), Single value (stale pipelines), Timeline (ingest volume by source).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/"
    },
    {
      "title": "Vendor cloud documentation",
      "url": "https://docs.aws.amazon.com/"
    }
  ],
  "knownFalsePositives": "This pattern can surge during change windows, in sandboxes, and when a platform is in maintenance, so we line alerts up with the calendar and take a second look before we call it an incident.",
  "mitreAttack": [
    "T1562.008"
  ],
  "securityDomain": "cloud",
  "equipment": [
    "aws",
    "azure"
  ],
  "equipmentModels": [
    "aws_cloudtrail"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=_internal`.\n• Install and configure Splunk _internal, ingest metrics, or custom heartbeat with the modular input for `aws:cloudtrail`.\n• Prerequisite crawl UC(s): UC-4.4.33.\n• Confirm field extractions in `props.conf` for `userIdentity.arn` and related actor fields.\n\nStep 1 — Configure data collection\nTrack last event time per cloud sourcetype (e.g. aws:cloudtrail, mscs:azure:audit, google:gcp:pubsub). Alert when no events received for >15–30 minutes. Monitor Event Hub consumer lag and Pub/Sub subscription backlog as pipeline indicators.\nValidate ingest before alerting:\n```spl\nindex=_internal sourcetype=\"aws:cloudtrail\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=_internal sourcetype=\"aws:cloudtrail\" source=*metrics* group=per_sourcetype_thruput\n| table _time userIdentity.arn eventName sourceIPAddress awsRegion\n| eval uc_marker=\"UC-4.4.7_cross_cloud_log_ingestio_7\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `aws:cloudtrail` in `index=_internal` to detect cross-cloud log ingestion pipeline health. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the AWS CloudTrail console Event history for the same 24-hour window. Confirm row counts and `userIdentity.arn` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_4_7_alert` with severity aligned to `high` criticality. Dashboard: Table (sourcetype, last event, delay), Single value (stale pipelines), Timeline (ingest volume by source)..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade Splunk _internal, ingest metrics, or custom heartbeat and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of cross-cloud log ingestion pipeline health so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.4.33"
  ],
  "wave": "walk"
}
