{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.3.40",
  "title": "GCP Cloud Run Task Health",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "controlTest": {
    "positiveScenario": "Replay sample `gcp:monitoring:timeseries` events with `matching activity` into `index=cloud`. Confirm UC-4.3.40 returns >=1 row and the alert fires.",
    "negativeScenario": "Replay benign `gcp:monitoring:timeseries` traffic without `matching activity`. Confirm UC-4.3.40 returns zero rows and the alert does not fire."
  },
  "dataSources": "`index=cloud` sourcetype=`gcp:monitoring:timeseries` collected via `Splunk_TA_google-cloudplatform` (Pub/Sub logging/metrics) or OTel export from Cloud Ops; validate ingest with `| stats count by sourcetype`.",
  "app": "`Splunk_TA_google-cloudplatform` (Pub/Sub logging/metrics) or OTel export from Cloud Ops",
  "spl": "index=cloud sourcetype=\"gcp:monitoring:timeseries\"\n| table _time userIdentity.arn eventName sourceIPAddress\n| eval uc_marker=\"UC-4.3.40_gcp_cloud_run_task_healt_40\"\n| sort -_time",
  "description": "UC-4.3.40 — GCP Cloud Run Task Health: queries Google Cloud `gcp:monitoring:timeseries` for configured audit predicates and returns actor, resource, and region context for performance review and change validation.",
  "value": "On Google Cloud, detecting 'GCP Cloud Run Task Health' early shortens cloud incident response and produces defensible audit evidence for identity and change-management controls. UC-4.3.40 drives high-priority performance alerting before customer-impacting drift.",
  "implementation": "Export Cloud Run request, latency, and instance metrics via GCP monitoring sink to Pub/Sub and ingest with `Splunk_TA_google-cloudplatform`, or forward OpenTelemetry from a sidecar/collector if you run hybrid instrumentation. Ensure `service_name` and `revision_name` are extracted. Alert on elevated `server_request_latencies` and `5xx` ratio versus SLO.",
  "visualization": "Time chart (p95 latency, request rate), Table (service, revision, error rate), Single value (active instances).",
  "cimModels": [
    "Performance"
  ],
  "cimSpl": "| tstats `summariesonly` avg(Performance.cpu_load_percent) as agg_value\n  from datamodel=Performance where nodename=Performance.CPU\n  by Performance.host span=5m\n| sort - agg_value",
  "dataModelAcceleration": "Enable data model acceleration for Performance (CPU node).",
  "references": [
    {
      "title": "Splunk_TA_google-cloudplatform",
      "url": "https://splunkbase.splunk.com/app/3088"
    },
    {
      "title": "CIM: Performance",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Performance"
    }
  ],
  "knownFalsePositives": "This pattern can surge during change windows, in sandboxes, and when a platform is in maintenance, so we line alerts up with the calendar and take a second look before we call it an incident.",
  "securityDomain": "cloud",
  "equipment": [
    "gcp",
    "opentelemetry"
  ],
  "equipmentModels": [
    "gcp_monitoring"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=cloud`.\n• Install and configure `Splunk_TA_google-cloudplatform` (Pub/Sub logging/metrics) or OTel export from Cloud Ops with the modular input for `gcp:monitoring:timeseries`.\n• Prerequisite crawl UC(s): UC-4.3.41.\n• Confirm field extractions in `props.conf` for `userIdentity.arn` and related actor fields.\n\nStep 1 — Configure data collection\nExport Cloud Run request, latency, and instance metrics via GCP monitoring sink to Pub/Sub and ingest with `Splunk_TA_google-cloudplatform`, or forward OpenTelemetry from a sidecar/collector if you run hybrid instrumentation. Ensure `service_name` and `revision_name` are extracted. Alert on elevated `server_request_latencies` and `5xx` ratio versus SLO.\nValidate ingest before alerting:\n```spl\nindex=cloud sourcetype=\"gcp:monitoring:timeseries\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=cloud sourcetype=\"gcp:monitoring:timeseries\"\n| table _time userIdentity.arn eventName sourceIPAddress\n| eval uc_marker=\"UC-4.3.40_gcp_cloud_run_task_healt_40\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `gcp:monitoring:timeseries` in `index=cloud` to detect gcp cloud run task health. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the provider cloud console audit view for the same 24-hour window. Confirm row counts and `userIdentity.arn` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_3_40_alert` with severity aligned to `high` criticality. Dashboard: Time chart (p95 latency, request rate), Table (service, revision, error rate), Single value (active instances)..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade `Splunk_TA_google-cloudplatform` (Pub/Sub logging/metrics) or OTel export from Cloud Ops and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of gcp cloud run task health so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.3.41"
  ],
  "splunkbaseApps": [
    {
      "id": 3088,
      "name": "Splunk Add-on for Google Cloud Platform",
      "role": "primary",
      "requiresSmeReview": true
    }
  ],
  "wave": "walk"
}
