{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.3.11",
  "title": "Cloud Storage (GCS) Request Metrics and Cost",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Security",
    "Performance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Cloud Storage (GCS) Request Metrics and Cost' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_cloud_storage_gcs_request_metrics_and_cost` (UC-4.3.11), SOC dashboard panel \"Operations — Cloud Storage (GCS) Request Metrics and Cost\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-4.3.11 positive (Cloud, Storage, (GCS), Request, Metrics): ingest synthetic events into `index=gcp` sourcetype='google:gcp:pubsub:message' where `count` grouped by `method`, `resource.labels.bucket_name` satisfies detection threshold for 'Cloud Storage (GCS) Request Metrics and Cost'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-4.3.11 negative (and Cost): ingest benign `gcp` / `google:gcp:pubsub:message` events where `method`, `resource.labels.bucket_name` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 12 confirms zero alert rows for 'Cloud Storage (GCS) Request Metrics and Cost'."
  },
  "dataSources": "Cloud Monitoring (storage.googleapis.com/request_count), Billing export Collected via Splunk Add-on for Google Cloud Platform (Splunkbase 3084) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_google-cloudplatform` — Splunk Add-on for Google Cloud Platform (Splunkbase 3084)",
  "spl": "index=gcp sourcetype=\"google:gcp:pubsub:message\" protoPayload.serviceName=\"storage.googleapis.com\"\n| spath output=method path=protoPayload.methodName\n| stats count by method resource.labels.bucket_name\n| sort -count",
  "description": "Detects UC-4.3.11 — Cloud, Storage, (GCS), Request, Metrics — using `index=gcp` sourcetype `google:gcp:pubsub:message`, grouped by `method`, `resource.labels.bucket_name`. Outputs `count` each `the scheduled window`; alert when configured threshold. Primary focus: and Cost. Purpose-built for 'Cloud Storage (GCS) Request Metrics and Cost' in Operations workflows.",
  "value": "Tracking `activity` via UC-4.3.11 on `google:gcp:pubsub:message` feeds improves response to 'Cloud Storage (GCS) Request Metrics and Cost' and reduces operational risk and delayed incident response.",
  "implementation": "Enable GCS request logging to Cloud Logging; sink to Pub/Sub for Splunk. Collect storage metrics. Ingest billing export for cost by bucket. Alert on anomalous request volume or cost spike.",
  "visualization": "Line chart (requests, cost by bucket), Table (bucket, method, count), Bar chart (cost by bucket).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk_TA_google-cloudplatform",
      "url": "https://splunkbase.splunk.com/app/3088",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 3084",
      "url": "https://splunkbase.splunk.com/app/3084",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `gcp` (UC-4.3.11)** — Planned work for 'Cloud Storage (GCS) Request Metrics and Cost' shifts `count` over `span=1h` while `method`, `resource.labels.bucket_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `google:gcp:pubsub:message`** — Scheduled automation elevates `resource.labels.bucket_name` without crossing configured threshold for UC-4.3.11. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `method`, `resource.labels.bucket_name`** — Health checks in `gcp` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `google:gcp:pubsub:message`** — First 48h baseline for UC-4.3.11 on `method` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `method`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1619",
    "T1530"
  ],
  "equipment": [
    "gcp"
  ],
  "equipmentModels": [
    "gcp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_google-cloudplatform`.\n• Ensure the following data sources are available: Cloud Monitoring (storage.googleapis.com/request_count), Billing export.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnable GCS request logging to Cloud Logging; sink to Pub/Sub for Splunk. Collect storage metrics. Ingest billing export for cost by bucket. Alert on anomalous request volume or cost spike.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=gcp sourcetype=\"google:gcp:pubsub:message\" protoPayload.serviceName=\"storage.googleapis.com\"\n| spath output=method path=protoPayload.methodName\n| stats count by method resource.labels.bucket_name\n| sort -count\n```\n\nUnderstanding this SPL\n\n**Cloud Storage (GCS) Request Metrics and Cost** — GCS request count and latency support performance tuning. Cost tracking by bucket/class prevents bill shock.\n\nDocumented **Data sources**: Cloud Monitoring (storage.googleapis.com/request_count), Billing export. **App/TA** (typical add-on context): `Splunk_TA_google-cloudplatform`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: gcp; **sourcetype**: google:gcp:pubsub:message. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=gcp, sourcetype=\"google:gcp:pubsub:message\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Extracts structured paths (JSON/XML) with `spath`.\n• `stats` rolls up events into metrics; results are split **by method resource.labels.bucket_name** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (requests, cost by bucket), Table (bucket, method, count), Bar chart (cost by bucket).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_google-cloudplatform` — Splunk Add-on for Google Cloud Platform (Splunkbase 3084)). Enable the modular input or HEC token that writes sourcetype=google:gcp:pubsub:message into gcp. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`method, resource.labels.bucket_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "GCS request count and latency support performance tuning.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
