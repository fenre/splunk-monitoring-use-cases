{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.4.18",
  "title": "Cloud Endpoint and DNS Resolution Health",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Availability"
  ],
  "splunkPillar": "Security",
  "controlTest": {
    "positiveScenario": "Replay sample `endpoint:health` events with `matching activity` into `index=cloud`. Confirm UC-4.4.18 returns >=1 row and the alert fires.",
    "negativeScenario": "Replay benign `endpoint:health` traffic without `matching activity`. Confirm UC-4.4.18 returns zero rows and the alert does not fire."
  },
  "dataSources": "`index=cloud` sourcetype=`endpoint:health` collected via Custom scripted input (nslookup, curl to endpoint), CloudWatch Route53 health; validate ingest with `| stats count by sourcetype`.",
  "app": "Custom scripted input (nslookup, curl to endpoint), CloudWatch Route53 health",
  "spl": "index=cloud sourcetype=\"endpoint:health\"\n| table _time userIdentity.arn eventName sourceIPAddress\n| eval uc_marker=\"UC-4.4.18_cloud_endpoint_and_dns_r_18\"\n| sort -_time",
  "description": "UC-4.4.18 — Cloud Endpoint and DNS Resolution Health: queries multi-cloud (AWS/Azure/GCP) `endpoint:health` for configured audit predicates and returns actor, resource, and region context for security review and change validation.",
  "value": "On multi-cloud (AWS/Azure/GCP), detecting 'Cloud Endpoint and DNS Resolution Health' early shortens cloud incident response and produces defensible audit evidence for identity and change-management controls. UC-4.4.18 drives medium-priority security alerting before customer-impacting drift.",
  "implementation": "Run periodic probes (DNS lookup for private hosted zone, HTTPS to VPC endpoint) from a central host or Lambda. Ingest success/failure and latency. Alert when endpoint is unreachable or RTT exceeds threshold.",
  "visualization": "Status grid (endpoint, OK/fail), Table (endpoint, RTT), Line chart (RTT over time).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/"
    },
    {
      "title": "Vendor cloud documentation",
      "url": "https://docs.splunk.com/Documentation/CIM"
    }
  ],
  "knownFalsePositives": "This pattern can surge during change windows, in sandboxes, and when a platform is in maintenance, so we line alerts up with the calendar and take a second look before we call it an incident.",
  "mitreAttack": [
    "T1580"
  ],
  "securityDomain": "cloud",
  "equipment": [
    "aws",
    "azure",
    "m365"
  ],
  "equipmentModels": [
    "aws_health"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=cloud`.\n• Install and configure Custom scripted input (nslookup, curl to endpoint), CloudWatch Route53 health with the modular input for `endpoint:health`.\n• Prerequisite crawl UC(s): UC-4.4.33.\n• Confirm field extractions in `props.conf` for `userIdentity.arn` and related actor fields.\n\nStep 1 — Configure data collection\nRun periodic probes (DNS lookup for private hosted zone, HTTPS to VPC endpoint) from a central host or Lambda. Ingest success/failure and latency. Alert when endpoint is unreachable or RTT exceeds threshold.\nValidate ingest before alerting:\n```spl\nindex=cloud sourcetype=\"endpoint:health\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=cloud sourcetype=\"endpoint:health\"\n| table _time userIdentity.arn eventName sourceIPAddress\n| eval uc_marker=\"UC-4.4.18_cloud_endpoint_and_dns_r_18\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `endpoint:health` in `index=cloud` to detect cloud endpoint and dns resolution health. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the provider cloud console audit view for the same 24-hour window. Confirm row counts and `userIdentity.arn` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_4_18_alert` with severity aligned to `medium` criticality. Dashboard: Status grid (endpoint, OK/fail), Table (endpoint, RTT), Line chart (RTT over time)..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade Custom scripted input (nslookup, curl to endpoint), CloudWatch Route53 health and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of cloud endpoint and dns resolution health so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.4.33"
  ],
  "wave": "walk"
}
