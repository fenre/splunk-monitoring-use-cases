{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.3.39",
  "title": "Anthos Service Mesh Health",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Anthos Service Mesh Health' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_anthos_service_mesh_health` (UC-4.3.39), SOC dashboard panel \"Operations — Anthos Service Mesh Health\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-4.3.39 positive (Anthos, Service, Mesh, Health): ingest synthetic events into `index=gcp` sourcetype='google:gcp:monitoring' where `err_rate` grouped by `metric.labels.destination_service_name` satisfies `err_rate` > 0.01 for 'Anthos Service Mesh Health'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-4.3.39 negative (Mesh Health): ingest benign `gcp` / `google:gcp:monitoring` events where `metric.labels.destination_service_name` stays below `err_rate` > 0.01, or exclude the test entity via `ops_exception_register.csv`. Variant 3 confirms zero alert rows for 'Anthos Service Mesh Health'."
  },
  "dataSources": "`sourcetype=google:gcp:monitoring` (Istio canonical metrics) Collected via Splunk Add-on for Google Cloud Platform (Splunkbase 3084) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_google-cloudplatform`, Anthos Service Mesh telemetry — Splunk Add-on for Google Cloud Platform (Splunkbase 3084)",
  "spl": "index=gcp sourcetype=\"google:gcp:monitoring\"\n| where match(metric.type, \"(istio|kubernetes\\.io/istio)\")\n| stats avg(value) as err_rate by metric.labels.destination_service_name\n| where err_rate > 0.01\n| sort - err_rate",
  "description": "Monitors when Mesh Health criteria fire for 'Anthos Service Mesh Health' (UC-4.3.39) in `index=gcp` sourcetype `google:gcp:monitoring`. Pipeline aggregates `err_rate` by `metric.labels.destination_service_name` over `the scheduled window`. Alert logic enforces `err_rate` > 0.01. Title terms: Anthos, Service, Mesh, Health. Eliminates manual hunting for UC-4.3.39 conditions.",
  "value": "UC-4.3.39 helps teams contain Anthos, Service, Mesh, Health scenarios and reduce operational risk and delayed incident response before incidents compound.",
  "implementation": "Export Istio canonical metrics (4xx/5xx, request duration) to Cloud Monitoring and Splunk. Dashboard golden signals per service. Alert on error rate > 1% or p99 latency vs SLO. Include control plane (istiod) pod health.",
  "visualization": "Service mesh graph (external tool) plus Table (service, error rate), Line chart (p50/p99 latency).",
  "cimModels": [
    "Performance"
  ],
  "cimSpl": "| tstats `summariesonly` avg(Performance.cpu_load_percent) as agg_value\n  from datamodel=Performance where nodename=Performance.CPU\n  by Performance.host span=5m\n| sort - agg_value",
  "dataModelAcceleration": "Enable data model acceleration for Performance (CPU node).",
  "references": [
    {
      "title": "Splunk_TA_google-cloudplatform",
      "url": "https://splunkbase.splunk.com/app/3088",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Performance",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Performance",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `gcp` (UC-4.3.39)** — Planned work for 'Anthos Service Mesh Health' shifts `err_rate` over `span=1h` while `metric.labels.destination_service_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `avg` on `google:gcp:monitoring`** — Scheduled automation elevates `src` without crossing `err_rate` > 0.01 for UC-4.3.39. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `metric.labels.destination_service_name`** — Health checks in `gcp` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `google:gcp:monitoring`** — First 48h baseline for UC-4.3.39 on `metric.labels.destination_service_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `metric.labels.destination_service_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "gcp",
    "kubernetes"
  ],
  "equipmentModels": [
    "kubernetes_k8s"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_google-cloudplatform`, Anthos Service Mesh telemetry.\n• Ensure the following data sources are available: `sourcetype=google:gcp:monitoring` (Istio canonical metrics).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nExport Istio canonical metrics (4xx/5xx, request duration) to Cloud Monitoring and Splunk. Dashboard golden signals per service. Alert on error rate > 1% or p99 latency vs SLO. Include control plane (istiod) pod health.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=gcp sourcetype=\"google:gcp:monitoring\"\n| where match(metric.type, \"(istio|kubernetes\\.io/istio)\")\n| stats avg(value) as err_rate by metric.labels.destination_service_name\n| where err_rate > 0.01\n| sort - err_rate\n```\n\nUnderstanding this SPL\n\n**Anthos Service Mesh Health** — Istio-based meshes add control-plane and sidecar failure modes; monitoring error budgets and latency protects microservices SLOs.\n\nDocumented **Data sources**: `sourcetype=google:gcp:monitoring` (Istio canonical metrics). **App/TA** (typical add-on context): `Splunk_TA_google-cloudplatform`, Anthos Service Mesh telemetry. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: gcp; **sourcetype**: google:gcp:monitoring. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=gcp, sourcetype=\"google:gcp:monitoring\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where match(metric.type, \"(istio|kubernetes\\.io/istio)\")` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by metric.labels.destination_service_name** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where err_rate > 0.01` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` avg(Performance.cpu_load_percent) as agg_value\n  from datamodel=Performance where nodename=Performance.CPU\n  by Performance.host span=5m\n| sort - agg_value\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Anthos Service Mesh Health** — Istio-based meshes add control-plane and sidecar failure modes; monitoring error budgets and latency protects microservices SLOs.\n\nIf you map cloud vendor fields into the CIM, this variant uses normalized names and `tstats` on accelerated models. The raw vendor search in Step 2 is still the first stop for troubleshooting.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` on accelerated data model the CPU-related Performance model — enable that model in Data Models and CIM add-ons, or the search may return no rows.\n\n• Uses `sort` to rank results; add `head` to limit the table.\n\nEnable Data Model Acceleration (and the right field aliases) for the models or datasets above; otherwise `tstats` may not find summaries.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` avg(Performance.cpu_load_percent) as agg_value\n  from datamodel=Performance where nodename=Performance.CPU\n  by Performance.host span=5m\n| sort - agg_value\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Anthos Service Mesh Health** — Istio-based meshes add control-plane and sidecar failure modes; monitoring error budgets and latency protects microservices SLOs.\n\nIf you map cloud vendor fields into the CIM, this variant uses normalized names and `tstats` on accelerated models. The raw vendor search in Step 2 is still the first stop for troubleshooting.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` on the `Performance` data model (CPU child datasets)—enable that model in Data Models and the CIM add-on, or the search may return no rows.\n\n• Uses `sort` to rank results; add `head` to limit the table.\n\nEnable Data Model Acceleration (and the right field aliases) for the models or datasets above; otherwise `tstats` may not find summaries.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` avg(Performance.cpu_load_percent) as agg_value\n  from datamodel=Performance where nodename=Performance.CPU\n  by Performance.host span=5m\n| sort - agg_value\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Anthos Service Mesh Health** — Istio-based meshes add control-plane and sidecar failure modes; monitoring error budgets and latency protects microservices SLOs.\n\nIf you map cloud vendor fields into the CIM, this variant uses normalized names and `tstats` on accelerated models. The raw vendor search in Step 2 is still the first stop for troubleshooting.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` on the `Performance` data model (CPU child datasets)—enable that model in Data Models and the CIM add-on, or the search may return no rows.\n\n• Uses `sort` to rank results; add `head` to limit the table.\n\nEnable Data Model Acceleration (and the right field aliases) for the models or datasets above; otherwise `tstats` may not find summaries.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` avg(Performance.cpu_load_percent) as agg_value\n  from datamodel=Performance where nodename=Performance.CPU\n  by Performance.host span=5m\n| sort - agg_value\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Anthos Service Mesh Health** — Istio-based meshes add control-plane and sidecar failure modes; monitoring error budgets and latency protects microservices SLOs.\n\nIf you map cloud vendor fields into the CIM, this variant uses normalized names and `tstats` on accelerated models. The raw vendor search in Step 2 is still the first stop for troubleshooting.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` on the `Performance` data model (CPU child datasets)—enable that model in Data Models and the CIM add-on, or the search may return no rows.\n\n• Uses `sort` to rank results; add `head` to limit the table.\n\nEnable Data Model Acceleration (and the right field aliases) for the models or datasets above; otherwise `tstats` may not find summaries.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` avg(Performance.cpu_load_percent) as agg_value\n  from datamodel=Performance where nodename=Performance.CPU\n  by Performance.host span=5m\n| sort - agg_value\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Anthos Service Mesh Health** — Istio-based meshes add control-plane and sidecar failure modes; monitoring error budgets and latency protects microservices SLOs.\n\nIf you map cloud vendor fields into the CIM, this variant uses normalized names and `tstats` on accelerated models. The raw vendor search in Step 2 is still the first stop for troubleshooting.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` on the `Performance` data model (CPU child datasets)—enable that model in Data Models and the CIM add-on, or the search may return no rows.\n\n• Uses `sort` to rank results; add `head` to limit the table.\n\nEnable Data Model Acceleration (and the right field aliases) for the models or datasets above; otherwise `tstats` may not find summaries.\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Service mesh graph (external tool) plus Table (service, error rate), Line chart (p50/p99 latency).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_google-cloudplatform`, Anthos Service Mesh telemetry — Splunk Add-on for Google Cloud Platform (Splunkbase 3084)). Enable the modular input or HEC token that writes sourcetype=google:gcp:monitoring into gcp. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`err_rate, metric.labels.destination_service_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Istio-based meshes add control-plane and sidecar failure modes; monitoring error budgets and latency protects microservices service goals.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
