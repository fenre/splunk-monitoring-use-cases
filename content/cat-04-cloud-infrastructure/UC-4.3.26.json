{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.3.26",
  "title": "GKE Autopilot Pod Scaling",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'GKE Autopilot Pod Scaling' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_gke_autopilot_pod_scaling` (UC-4.3.26), SOC dashboard panel \"Operations — GKE Autopilot Pod Scaling\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=gcp` sourcetype='google:gcp:pubsub:message' where `resource.labels.cluster_name` satisfies detection threshold for UC-4.3.26 ('GKE Autopilot Pod Scaling'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `gcp` / `google:gcp:pubsub:message` events where `resource.labels.cluster_name` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-4.3.26."
  },
  "dataSources": "`sourcetype=google:gcp:pubsub:message` (GKE cluster logs), `sourcetype=google:gcp:monitoring` (scheduler, pending pods) Collected via Splunk Add-on for Google Cloud Platform (Splunkbase 3084) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_google-cloudplatform` — Splunk Add-on for Google Cloud Platform (Splunkbase 3084)",
  "spl": "index=gcp sourcetype=\"google:gcp:pubsub:message\" resource.type=\"k8s_cluster\" (\"FailedScheduling\" OR \"Insufficient cpu\" OR \"Insufficient memory\")\n| stats count by resource.labels.cluster_name, jsonPayload.reason\n| sort -count",
  "description": "Autopilot scales node pools automatically; failed scale-outs leave pods pending and degrade SLOs. Searches `index=gcp` with sourcetype `google:gcp:pubsub:message` for UC-4.3.26, aggregating `count` by `resource.labels.cluster_name` over `the scheduled window`. Alerts when grouped results cross configured threshold in the saved search. Gives Operations operators a repeatable signal for 'GKE Autopilot Pod Scaling' without manual log review.",
  "value": "Early 'GKE Autopilot Pod Scaling' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Enable GKE logging and filter for scheduling events. Correlate with pending pod metrics if exported. Alert on rising pending pod count or repeated scale failures.",
  "visualization": "Timeline (scheduling failures), Table (cluster, reason, count), Line chart (pending pods).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk_TA_google-cloudplatform",
      "url": "https://splunkbase.splunk.com/app/3088",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 3084",
      "url": "https://splunkbase.splunk.com/app/3084",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `gcp`** — Planned work for 'GKE Autopilot Pod Scaling' shifts `count` over `span=1h` while `resource.labels.cluster_name` remains in-scope for UC-4.3.26. Cross-check CMDB CHG.\n\n2. **Batch `count` on `google:gcp:pubsub:message`** — Scheduled automation elevates `jsonPayload.reason` without crossing configured threshold. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `resource.labels.cluster_name`** — Health checks in `gcp` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `google:gcp:pubsub:message`** — First 48h baseline for `resource.labels.cluster_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `resource.labels.cluster_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "gcp"
  ],
  "equipmentModels": [
    "gcp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_google-cloudplatform`.\n• Ensure the following data sources are available: `sourcetype=google:gcp:pubsub:message` (GKE cluster logs), `sourcetype=google:gcp:monitoring` (scheduler, pending pods).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnable GKE logging and filter for scheduling events. Correlate with pending pod metrics if exported. Alert on rising pending pod count or repeated scale failures.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=gcp sourcetype=\"google:gcp:pubsub:message\" resource.type=\"k8s_cluster\" (\"FailedScheduling\" OR \"Insufficient cpu\" OR \"Insufficient memory\")\n| stats count by resource.labels.cluster_name, jsonPayload.reason\n| sort -count\n```\n\nUnderstanding this SPL\n\n**GKE Autopilot Pod Scaling** — Autopilot scales node pools automatically; failed scale-outs leave pods pending and degrade SLOs.\n\nDocumented **Data sources**: `sourcetype=google:gcp:pubsub:message` (GKE cluster logs), `sourcetype=google:gcp:monitoring` (scheduler, pending pods). **App/TA** (typical add-on context): `Splunk_TA_google-cloudplatform`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: gcp; **sourcetype**: google:gcp:pubsub:message. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=gcp, sourcetype=\"google:gcp:pubsub:message\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by resource.labels.cluster_name, jsonPayload.reason** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Timeline (scheduling failures), Table (cluster, reason, count), Line chart (pending pods).",
  "grandmaExplanation": "Autopilot scales node pools automatically; failed scale-outs leave pods pending and degrade service goals.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
