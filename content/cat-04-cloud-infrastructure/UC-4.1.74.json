{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.1.74",
  "title": "IAM Access Analyzer Findings",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "controlTest": {
    "positiveScenario": "Inject sample events matching UC-4.1.74 SPL; confirm the search returns rows and alert fires.",
    "negativeScenario": "Replay benign traffic without matching markers; confirm UC-4.1.74 search returns zero rows."
  },
  "dataSources": "`index=aws` sourcetype=`aws:cloudwatch:events` collected via `Splunk_TA_aws`; validate ingest with `| stats count by sourcetype`.",
  "app": "`Splunk_TA_aws`",
  "spl": "index=aws sourcetype=\"aws:cloudwatch:events\" detail-type=\"Access Analyzer Finding\" detail.status=\"ACTIVE\"\n| table _time detail-type source detail.eventName\n| eval uc_marker=\"UC-4.1.74_iam_access_analyzer_find_74\"\n| sort -_time",
  "description": "Access Analyzer identifies unintended external access to S3, IAM roles, KMS keys, and other resources—reducing public exposure risk.",
  "value": "Detecting iam access analyzer findings early reduces mean time to contain cloud incidents, supports audit logging controls, and prevents silent misconfiguration drift across accounts.",
  "implementation": "Enable organization-wide analyzer. Send findings to EventBridge and Splunk. Auto-remediate public S3 where policy allows or ticket owners. Weekly review of new external access paths.",
  "visualization": "Table (resource type, account, public), Bar chart (findings by type), Single value (active findings).",
  "cimModels": [
    "Alerts"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Alerts.Alerts\n  where match(Alerts.app, \"(?i)access.analyzer|iam\") OR match(Alerts.signature, \"(?i)external|public|access\")\n  by Alerts.severity Alerts.src span=1h\n| sort -count",
  "dataModelAcceleration": "Alerts: map IAM Access Analyzer style findings into the Alerts data model. Enable Data Model Acceleration for the CIM data models in `cimModels` once `Splunk_TA_aws` (and any CIM tags/aliases) map your fields; otherwise the `cimSpl` summary search may be empty. Step 2 on raw or metric indexes stays primary for most sites.",
  "references": [
    {
      "title": "Splunk_TA_aws",
      "url": "https://splunkbase.splunk.com/app/1876"
    },
    {
      "title": "Vendor cloud documentation",
      "url": "https://docs.aws.amazon.com/"
    }
  ],
  "knownFalsePositives": "Intentional public buckets or roles for a static site, partner data exchange, or test account that the policy still documents.",
  "mitreAttack": [
    "T1087.004",
    "T1580"
  ],
  "securityDomain": "cloud",
  "equipment": [
    "aws"
  ],
  "equipmentModels": [
    "aws_cloudwatch"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=aws`.\n• Install and configure `Splunk_TA_aws` with the modular input for `aws:cloudwatch:events`.\n• Prerequisite crawl UC(s): UC-4.1.78.\n• Confirm field extractions in `props.conf` for `detail-type` and related actor fields.\n\nStep 1 — Configure data collection\nEnable organization-wide analyzer. Send findings to EventBridge and Splunk. Auto-remediate public S3 where policy allows or ticket owners. Weekly review of new external access paths.\nValidate ingest before alerting:\n```spl\nindex=aws sourcetype=\"aws:cloudwatch:events\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=aws sourcetype=\"aws:cloudwatch:events\" detail-type=\"Access Analyzer Finding\" detail.status=\"ACTIVE\"\n| table _time detail-type source detail.eventName\n| eval uc_marker=\"UC-4.1.74_iam_access_analyzer_find_74\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `aws:cloudwatch:events` in `index=aws` to detect iam access analyzer findings. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the provider cloud console audit view for the same 24-hour window. Confirm row counts and `detail-type` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_1_74_alert` with severity aligned to `medium` criticality. Dashboard: Table (resource type, account, public), Bar chart (findings by type), Single value (active findings)..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade `Splunk_TA_aws` and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of iam access analyzer findings so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.1.78"
  ],
  "splunkbaseApps": [
    {
      "id": 1876,
      "name": "Splunk Add-on for AWS",
      "role": "primary",
      "requiresSmeReview": true
    }
  ],
  "wave": "walk"
}
