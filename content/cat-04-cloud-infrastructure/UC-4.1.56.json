{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.1.56",
  "title": "AWS Lambda Cold Start Monitoring",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "controlTest": {
    "positiveScenario": "Inject sample events matching UC-4.1.56 SPL; confirm the search returns rows and alert fires.",
    "negativeScenario": "Replay benign traffic without matching markers; confirm UC-4.1.56 search returns zero rows."
  },
  "dataSources": "`index=aws` sourcetype=`aws:cloudwatchlogs` collected via `Splunk_TA_aws`; validate ingest with `| stats count by sourcetype`.",
  "app": "`Splunk_TA_aws`",
  "spl": "index=aws sourcetype=\"aws:cloudwatchlogs\" (\"REPORT RequestId\" OR \"INIT_START\")\n| table _time userIdentity.arn eventName sourceIPAddress\n| eval uc_marker=\"UC-4.1.56_aws_lambda_cold_start_mo_56\"\n| sort -_time",
  "description": "Cold start frequency and duration impact user experience. High cold start rates or long init times cause request latency spikes and timeouts.",
  "value": "Detecting aws lambda cold start monitoring early reduces mean time to contain cloud incidents, supports audit logging controls, and prevents silent misconfiguration drift across accounts.",
  "implementation": "Enable CloudWatch Logs for Lambda (platform logs include REPORT and INIT). Optionally ingest X-Ray traces for end-to-end cold start visibility. Parse REPORT/INIT_START lines to extract init duration and invocation type. Alert when cold start rate exceeds 10% or init duration > 1s for critical functions. Consider provisioned concurrency for latency-sensitive workloads.",
  "visualization": "Line chart (cold start % and init duration by function over time), Table (function, cold starts, avg init ms), Single value (cold start rate).",
  "cimModels": [
    "Change"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Change.All_Changes\n  where match(All_Changes.app, \"(?i)glue|lambda|logs\")\n  by All_Changes.user All_Changes.status span=1h\n| sort -count",
  "dataModelAcceleration": "Change / Performance: only if the TA maps job or platform logs; raw CloudWatch/CloudTrail is usually primary. Enable Data Model Acceleration for the CIM data models in `cimModels` once `Splunk_TA_aws` (and any CIM tags/aliases) map your fields; otherwise the `cimSpl` summary search may be empty. Step 2 on raw or metric indexes stays primary for most sites.",
  "references": [
    {
      "title": "Splunk_TA_aws",
      "url": "https://splunkbase.splunk.com/app/1876"
    },
    {
      "title": "Vendor cloud documentation",
      "url": "https://docs.aws.amazon.com/"
    }
  ],
  "knownFalsePositives": "Known slow cold starts, scheduled batch jobs, and canary invocations in lower envs you already noise-reduced.",
  "securityDomain": "cloud",
  "equipment": [
    "aws"
  ],
  "equipmentModels": [
    "aws_cloudwatch"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=aws`.\n• Install and configure `Splunk_TA_aws` with the modular input for `aws:cloudwatchlogs`.\n• Prerequisite crawl UC(s): UC-4.1.78.\n• Confirm field extractions in `props.conf` for `userIdentity.arn` and related actor fields.\n\nStep 1 — Configure data collection\nEnable CloudWatch Logs for Lambda (platform logs include REPORT and INIT). Optionally ingest X-Ray traces for end-to-end cold start visibility. Parse REPORT/INIT_START lines to extract init duration and invocation type. Alert when cold start rate exceeds 10% or init duration > 1s for critical functions. Consider provisioned concurrency for latency-sensitive workloads.\nValidate ingest before alerting:\n```spl\nindex=aws sourcetype=\"aws:cloudwatchlogs\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=aws sourcetype=\"aws:cloudwatchlogs\" (\"REPORT RequestId\" OR \"INIT_START\")\n| table _time userIdentity.arn eventName sourceIPAddress\n| eval uc_marker=\"UC-4.1.56_aws_lambda_cold_start_mo_56\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `aws:cloudwatchlogs` in `index=aws` to detect aws lambda cold start monitoring. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the provider cloud console audit view for the same 24-hour window. Confirm row counts and `userIdentity.arn` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_1_56_alert` with severity aligned to `medium` criticality. Dashboard: Line chart (cold start % and init duration by function over time), Table (function, cold starts, avg init ms), Single value (cold start rate)..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade `Splunk_TA_aws` and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of aws lambda cold start monitoring so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.1.78"
  ],
  "splunkbaseApps": [
    {
      "id": 1876,
      "name": "Splunk Add-on for AWS",
      "role": "primary",
      "requiresSmeReview": true
    }
  ],
  "wave": "walk"
}
