{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.1.71",
  "title": "Systems Manager Patch Compliance",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Systems Manager Patch Compliance' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_systems_manager_patch_compliance` (UC-4.1.71), SOC dashboard panel \"Operations — Systems Manager Patch Compliance\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-4.1.71 positive (Systems, Manager, Patch, Compliance): ingest synthetic events into `index=aws` sourcetype='aws:ssm:compliance' where `count` grouped by `resourceId`, `PatchSeverity` satisfies detection threshold for 'Systems Manager Patch Compliance'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-4.1.71 negative (Patch Compliance): ingest benign `aws` / `aws:ssm:compliance` events where `resourceId`, `PatchSeverity` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 6 confirms zero alert rows for 'Systems Manager Patch Compliance'."
  },
  "dataSources": "`sourcetype=aws:ssm:compliance`, SSM Inventory association Collected via Splunk Add-on for Amazon Web Services (Splunkbase 1876) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_aws` — Splunk Add-on for Amazon Web Services (Splunkbase 1876)",
  "spl": "index=aws sourcetype=\"aws:ssm:compliance\" ComplianceType=\"Patch\"\n| stats latest(status) as patch_status by resourceId, PatchSeverity\n| where patch_status!=\"Compliant\"\n| stats count by resourceId\n| sort -count",
  "description": "Alerts on Patch Compliance anomalies for UC-4.1.71 with window `the scheduled window` on `index=aws` sourcetype `aws:ssm:compliance`. Combines `count` by `resourceId`, `PatchSeverity` and compares against configured threshold. Mapped to keywords Systems, Manager, Patch, Compliance for dashboard routing.",
  "value": "Alerting on Systems, Manager, Patch, Compliance through UC-4.1.71 reduces operational risk and delayed incident response by surfacing `activity` early.",
  "implementation": "Schedule `AWS-RunPatchBaseline` and ingest compliance association results. Dashboard by OU and environment tag. Alert when CRITICAL severity patches are non-compliant past SLA window.",
  "visualization": "Table (instance, missing count), Pie chart (compliant %), Bar chart (severity).",
  "cimModels": [
    "Updates"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Updates.Updates\n  where match(Updates.status, \"(?i)non-?compliant|missing|failed|not.?installed\")\n  by Updates.dest Updates.app span=1d\n| sort -count",
  "dataModelAcceleration": "Updates: for `aws:ssm:compliance` and patch status mapped to CIM. Enable Data Model Acceleration for the CIM data models in `cimModels` once `Splunk_TA_aws` (and any CIM tags/aliases) map your fields; otherwise the `cimSpl` summary search may be empty. Step 2 on raw or metric indexes stays primary for most sites.",
  "references": [
    {
      "title": "Splunk_TA_aws",
      "url": "https://splunkbase.splunk.com/app/1876",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Updates",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Updates",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `aws` (UC-4.1.71)** — Planned work for 'Systems Manager Patch Compliance' shifts `count` over `span=1h` while `resourceId`, `PatchSeverity` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `aws:ssm:compliance`** — Scheduled automation elevates `PatchSeverity` without crossing configured threshold for UC-4.1.71. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `resourceId`, `PatchSeverity`** — Health checks in `aws` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `aws:ssm:compliance`** — First 48h baseline for UC-4.1.71 on `resourceId` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `resourceId`, `exception_reason`, `valid_until`.",
  "equipment": [
    "aws"
  ],
  "equipmentModels": [
    "aws_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_aws`.\n• Ensure the following data sources are available: `sourcetype=aws:ssm:compliance`, SSM Inventory association.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nSchedule `AWS-RunPatchBaseline` and ingest compliance association results. Dashboard by OU and environment tag. Alert when CRITICAL severity patches are non-compliant past SLA window.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=aws sourcetype=\"aws:ssm:compliance\" ComplianceType=\"Patch\"\n| stats latest(status) as patch_status by resourceId, PatchSeverity\n| where patch_status!=\"Compliant\"\n| stats count by resourceId\n| sort -count\n```\n\nUnderstanding this SPL\n\n**Systems Manager Patch Compliance** — Patch baselines reduce exploit exposure; instance-level compliance gaps show outdated AMIs or broken agents.\n\nDocumented **Data sources**: `sourcetype=aws:ssm:compliance`, SSM Inventory association. **App/TA** (typical add-on context): `Splunk_TA_aws`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: aws; **sourcetype**: aws:ssm:compliance. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=aws, sourcetype=\"aws:ssm:compliance\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by resourceId, PatchSeverity** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where patch_status!=\"Compliant\"` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by resourceId** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` count\n  from datamodel=Updates.Updates\n  where match(Updates.status, \"(?i)non-?compliant|missing|failed|not.?installed\")\n  by Updates.dest Updates.app span=1d\n| sort -count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Systems Manager Patch Compliance** — Patch baselines reduce exploit exposure; instance-level compliance gaps show outdated AMIs or broken agents.\n\nDocumented **Data sources**: `sourcetype=aws:ssm:compliance`, SSM Inventory association. **App/TA** (typical add-on context): `Splunk_TA_aws`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Updates.Updates` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (instance, missing count), Pie chart (compliant %), Bar chart (severity).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_aws` — Splunk Add-on for Amazon Web Services (Splunkbase 1876)). Enable the modular input or HEC token that writes sourcetype=aws:ssm:compliance into aws. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`PatchSeverity, resourceId`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We keep an eye on systems manager patch compliance. Which machines are not in the patch state the business asked for, before auditors or ransomware find them first. We want the right people to notice in time, not only when a customer or an auditor stumbles on it first.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
