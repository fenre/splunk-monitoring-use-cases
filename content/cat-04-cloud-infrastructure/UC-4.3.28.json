{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.3.28",
  "title": "VPC Service Controls Violations",
  "criticality": "critical",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'VPC Service Controls Violations' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_vpc_service_controls_violations` (UC-4.3.28), SOC dashboard panel \"Operations — VPC Service Controls Violations\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-4.3.28 positive (VPC, Service, Controls, Violations): ingest synthetic events into `index=gcp` sourcetype='google:gcp:pubsub:message' where `count` grouped by `protoPayload.authenticationInfo.principalEmail`, `resource.labels.project_id` satisfies detection threshold for 'VPC Service Controls Violations'. Validate filter `violation OR denied OR blocked`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-4.3.28 negative (Controls Violations): ingest benign `gcp` / `google:gcp:pubsub:message` events where `protoPayload.authenticationInfo.principalEmail`, `resource.labels.project_id` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 9 confirms zero alert rows for 'VPC Service Controls Violations'."
  },
  "dataSources": "Access Context Manager audit via `sourcetype=google:gcp:pubsub:message` Collected via Splunk Add-on for Google Cloud Platform (Splunkbase 3084) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_google-cloudplatform` — Splunk Add-on for Google Cloud Platform (Splunkbase 3084)",
  "spl": "index=gcp sourcetype=\"google:gcp:pubsub:message\" protoPayload.serviceName=\"accesscontextmanager.googleapis.com\"\n| search violation OR denied OR blocked\n| stats count by protoPayload.authenticationInfo.principalEmail, resource.labels.project_id\n| sort -count",
  "description": "Detects UC-4.3.28 — VPC, Service, Controls, Violations — using `index=gcp` sourcetype `google:gcp:pubsub:message`, grouped by `protoPayload.authenticationInfo.principalEmail`, `resource.labels.project_id`. Outputs `count` each `the scheduled window`; alert when configured threshold. Filter chain includes filter `violation OR denied OR blocked`. Purpose-built for 'VPC Service Controls Violations' in Operations workflows.",
  "value": "Operators running UC-4.3.28 on `google:gcp:pubsub:message` feeds shorten triage for Controls Violations tied to 'VPC Service Controls Violations', lowering operational risk and delayed incident response.",
  "implementation": "Ensure VPC SC dry-run and enforce modes both log. Route to SIEM with severity by service (BigQuery, GCS). Weekly review of top principals for false positives.",
  "visualization": "Table (principal, project, count), Timeline (violations), Pie chart (service).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk_TA_google-cloudplatform",
      "url": "https://splunkbase.splunk.com/app/3088",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 3084",
      "url": "https://splunkbase.splunk.com/app/3084",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `gcp` (UC-4.3.28)** — Planned work for 'VPC Service Controls Violations' shifts `count` over `span=1h` while `protoPayload.authenticationInfo.principalEmail`, `resource.labels.project_id` remains in-scope. Applies filter `violation OR denied OR blocked`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `google:gcp:pubsub:message`** — Scheduled automation elevates `resource.labels.project_id` without crossing configured threshold for UC-4.3.28. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `protoPayload.authenticationInfo.principalEmail`, `resource.labels.project_id`** — Health checks in `gcp` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `google:gcp:pubsub:message`** — First 48h baseline for UC-4.3.28 on `protoPayload.authenticationInfo.principalEmail` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `protoPayload.authenticationInfo.principalEmail`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1562.007"
  ],
  "equipment": [
    "gcp"
  ],
  "equipmentModels": [
    "gcp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_google-cloudplatform`.\n• Ensure the following data sources are available: Access Context Manager audit via `sourcetype=google:gcp:pubsub:message`.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnsure VPC SC dry-run and enforce modes both log. Route to SIEM with severity by service (BigQuery, GCS). Weekly review of top principals for false positives.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=gcp sourcetype=\"google:gcp:pubsub:message\" protoPayload.serviceName=\"accesscontextmanager.googleapis.com\"\n| search violation OR denied OR blocked\n| stats count by protoPayload.authenticationInfo.principalEmail, resource.labels.project_id\n| sort -count\n```\n\nUnderstanding this SPL\n\n**VPC Service Controls Violations** — Real-time violation tracking complements perimeter design reviews and catches data exfiltration paths early.\n\nDocumented **Data sources**: Access Context Manager audit via `sourcetype=google:gcp:pubsub:message`. **App/TA** (typical add-on context): `Splunk_TA_google-cloudplatform`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: gcp; **sourcetype**: google:gcp:pubsub:message. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=gcp, sourcetype=\"google:gcp:pubsub:message\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Applies an explicit `search` filter to narrow the current result set.\n• `stats` rolls up events into metrics; results are split **by protoPayload.authenticationInfo.principalEmail, resource.labels.project_id** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (principal, project, count), Timeline (violations), Pie chart (service).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_google-cloudplatform` — Splunk Add-on for Google Cloud Platform (Splunkbase 3084)). Enable the modular input or HEC token that writes sourcetype=google:gcp:pubsub:message into gcp. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`protoPayload.authenticationInfo.principalEmail, resource.labels.project_id`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Real-time violation tracking complements perimeter design reviews and catches data exfiltration paths early.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
