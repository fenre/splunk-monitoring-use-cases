{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.1.60",
  "title": "Security Hub Alert Aggregation",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'Security Hub Alert Aggregation' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_security_hub_alert_aggregation` (UC-4.1.60), SOC dashboard panel \"Operations — Security Hub Alert Aggregation\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-4.1.60 positive (Security, Hub, Alert, Aggregation): ingest synthetic events into `index=aws` sourcetype='aws:cloudwatch:events' where `count` grouped by `sev`, `title`, `account` satisfies detection threshold for 'Security Hub Alert Aggregation'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-4.1.60 negative (Alert Aggregation): ingest benign `aws` / `aws:cloudwatch:events` events where `sev`, `title`, `account` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 8 confirms zero alert rows for 'Security Hub Alert Aggregation'."
  },
  "dataSources": "`sourcetype=aws:firehose` or `sourcetype=aws:cloudwatch:events` (Security Hub findings), EventBridge to Splunk Collected via Splunk Add-on for Amazon Web Services (Splunkbase 1876) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_aws` — Splunk Add-on for Amazon Web Services (Splunkbase 1876)",
  "spl": "index=aws sourcetype=\"aws:cloudwatch:events\" detail-type=\"Security Hub Findings - Imported\"\n| spath path=detail.findings{}\n| mvexpand detail.findings{} limit=500\n| spath input=detail.findings{} output=sev path=Severity.Label\n| spath input=detail.findings{} output=title path=Title\n| stats count by sev, title, account\n| sort -count",
  "description": "Alerts on UC-4.1.60 — Security, Hub, Alert, Aggregation — using `index=aws` sourcetype `aws:cloudwatch:events`, grouped by `sev`, `title`, `account`. Outputs `count` each `the scheduled window`; alert when configured threshold. Primary focus: Alert Aggregation. Purpose-built for 'Security Hub Alert Aggregation' in Operations workflows.",
  "value": "UC-4.1.60 accelerates insight into Alert Aggregation ('Security Hub Alert Aggregation') by tracking `activity` on `aws:cloudwatch:events` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Send Security Hub custom actions or EventBridge rules to Firehose/HEC. Normalize `Severity` and `ComplianceStatus`. Auto-ticket CRITICAL/HIGH. Deduplicate by finding ID across updates. Feed executive dashboards with counts by standard (CIS, PCI).",
  "visualization": "Bar chart (findings by severity), Table (title, account, count), Single value (open critical).",
  "cimModels": [
    "Alerts",
    "Intrusion_Detection"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Alerts.Alerts\n  where match(Alerts.app, \"(?i)security|hub|firehose|findings\")\n  by Alerts.severity Alerts.signature span=1h\n| sort -count",
  "dataModelAcceleration": "Alerts and Intrusion_Detection: map Security Hub import events into CIM. Enable Data Model Acceleration for the CIM data models in `cimModels` once `Splunk_TA_aws` (and any CIM tags/aliases) map your fields; otherwise the `cimSpl` summary search may be empty. Step 2 on raw or metric indexes stays primary for most sites.",
  "references": [
    {
      "title": "Splunk_TA_aws",
      "url": "https://splunkbase.splunk.com/app/1876",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Intrusion_Detection",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Intrusion_Detection",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `aws` (UC-4.1.60)** — Planned work for 'Security Hub Alert Aggregation' shifts `count` over `span=1h` while `sev`, `title`, `account` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `aws:cloudwatch:events`** — Scheduled automation elevates `title` without crossing configured threshold for UC-4.1.60. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `sev`, `title`, `account`** — Health checks in `aws` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `aws:cloudwatch:events`** — First 48h baseline for UC-4.1.60 on `sev` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `sev`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1580",
    "T1526"
  ],
  "equipment": [
    "aws"
  ],
  "equipmentModels": [
    "aws_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_aws`.\n• Ensure the following data sources are available: `sourcetype=aws:firehose` or `sourcetype=aws:cloudwatch:events` (Security Hub findings), EventBridge to Splunk.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nSend Security Hub custom actions or EventBridge rules to Firehose/HEC. Normalize `Severity` and `ComplianceStatus`. Auto-ticket CRITICAL/HIGH. Deduplicate by finding ID across updates. Feed executive dashboards with counts by standard (CIS, PCI).\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=aws sourcetype=\"aws:cloudwatch:events\" detail-type=\"Security Hub Findings - Imported\"\n| spath path=detail.findings{}\n| mvexpand detail.findings{} limit=500\n| spath input=detail.findings{} output=sev path=Severity.Label\n| spath input=detail.findings{} output=title path=Title\n| stats count by sev, title, account\n| sort -count\n```\n\nUnderstanding this SPL\n\n**Security Hub Alert Aggregation** — Security Hub rolls up Config, GuardDuty, Inspector, and partner findings; aggregating by account and severity prioritizes remediation queues.\n\nDocumented **Data sources**: `sourcetype=aws:firehose` or `sourcetype=aws:cloudwatch:events` (Security Hub findings), EventBridge to Splunk. **App/TA** (typical add-on context): `Splunk_TA_aws`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: aws; **sourcetype**: aws:cloudwatch:events. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=aws, sourcetype=\"aws:cloudwatch:events\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Extracts structured paths (JSON/XML) with `spath`.\n• Expands multivalue fields with `mvexpand` — use `limit=` to cap row explosion.\n• Extracts structured paths (JSON/XML) with `spath`.\n• Extracts structured paths (JSON/XML) with `spath`.\n• `stats` rolls up events into metrics; results are split **by sev, title, account** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` count\n  from datamodel=Alerts.Alerts\n  where match(Alerts.app, \"(?i)security|hub|firehose|findings\")\n  by Alerts.severity Alerts.signature span=1h\n| sort -count\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Security Hub Alert Aggregation** — Security Hub rolls up Config, GuardDuty, Inspector, and partner findings; aggregating by account and severity prioritizes remediation queues.\n\nDocumented **Data sources**: `sourcetype=aws:firehose` or `sourcetype=aws:cloudwatch:events` (Security Hub findings), EventBridge to Splunk. **App/TA** (typical add-on context): `Splunk_TA_aws`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThis **CIM or accelerated** block uses normalized field names and/or `tstats` over data models. Enable **acceleration** on the referenced models (and correct CIM knowledge objects) or the search may return nothing.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` against accelerated summaries for data model `Intrusion_Detection.IDS_Attacks` — enable acceleration for that model.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\nEnable Data Model Acceleration (and metric indexes for `mstats`) for the models or datasets referenced above; otherwise `tstats`/`mstats` may return no results from summaries.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Bar chart (findings by severity), Table (title, account, count), Single value (open critical).",
  "grandmaExplanation": "We keep an eye on security hub alert aggregation. Security Hub results pulled into one stream so a single place can own triage, not a dozen product consoles. We want the right people to notice in time, not only when a customer or an auditor stumbles on it first.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
