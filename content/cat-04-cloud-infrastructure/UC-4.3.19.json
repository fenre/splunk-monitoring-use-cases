{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.3.19",
  "title": "Cloud Billing Budget Alerts and Anomaly",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Cloud Billing Budget Alerts and Anomaly' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_cloud_billing_budget_alerts_and_anomaly` (UC-4.3.19), SOC dashboard panel \"Operations — Cloud Billing Budget Alerts and Anomaly\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-4.3.19 positive (Cloud, Billing, Budget, Alerts, and): ingest synthetic events into `index=gcp` sourcetype='gcp:billing' where `daily_cost`, `avg_cost`, `stdev_cost` grouped by `_time`, `service` satisfies detection threshold for 'Cloud Billing Budget Alerts and Anomaly'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-4.3.19 negative (and Anomaly): ingest benign `gcp` / `gcp:billing` events where `_time`, `service` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 5 confirms zero alert rows for 'Cloud Billing Budget Alerts and Anomaly'."
  },
  "dataSources": "Billing export to BigQuery or Pub/Sub, Budget alert notifications Collected via Splunk Add-on for Google Cloud Platform (Splunkbase 3084) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_google-cloudplatform` — Splunk Add-on for Google Cloud Platform (Splunkbase 3084)",
  "spl": "index=gcp sourcetype=\"gcp:billing\"\n| bin _time span=1d\n| stats sum(cost) as daily_cost by _time, service\n| eventstats avg(daily_cost) as avg_cost, stdev(daily_cost) as stdev_cost by service\n| where daily_cost > avg_cost + 2*stdev_cost",
  "description": "Monitors 'Cloud Billing Budget Alerts and Anomaly' patterns (UC-4.3.19) sourced from `index=gcp` sourcetype `gcp:billing`. Threshold bundle: configured threshold; aggregation grain: `_time`, `service`; metrics: `daily_cost`, `avg_cost`, `stdev_cost`. Watch terms: Cloud, Billing, Budget, Alerts, and.",
  "value": "UC-4.3.19 gives Operations teams earlier visibility into and Anomaly through `gcp:billing` feeds, lowering operational risk and delayed incident response.",
  "implementation": "Enable billing export. Ingest daily/monthly cost data. Create budget alerts and forward to Splunk. Calculate baseline and alert on 2-sigma anomaly by service or project.",
  "visualization": "Line chart (cost with threshold), Table (service, cost, anomaly), Stacked area (cost by service).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk_TA_google-cloudplatform",
      "url": "https://splunkbase.splunk.com/app/3088",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 3084",
      "url": "https://splunkbase.splunk.com/app/3084",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `gcp` (UC-4.3.19)** — Planned work for 'Cloud Billing Budget Alerts and Anomaly' shifts `daily_cost`, `avg_cost`, `stdev_cost` over `span=1d` while `_time`, `service` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `sum` on `gcp:billing`** — Scheduled automation elevates `service` without crossing configured threshold for UC-4.3.19. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `_time`, `service`** — Health checks in `gcp` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `gcp:billing`** — First 48h baseline for UC-4.3.19 on `_time` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `_time`, `exception_reason`, `valid_until`.",
  "equipment": [
    "gcp"
  ],
  "equipmentModels": [
    "gcp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_google-cloudplatform`.\n• Ensure the following data sources are available: Billing export to BigQuery or Pub/Sub, Budget alert notifications.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnable billing export. Ingest daily/monthly cost data. Create budget alerts and forward to Splunk. Calculate baseline and alert on 2-sigma anomaly by service or project.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=gcp sourcetype=\"gcp:billing\"\n| bin _time span=1d\n| stats sum(cost) as daily_cost by _time, service\n| eventstats avg(daily_cost) as avg_cost, stdev(daily_cost) as stdev_cost by service\n| where daily_cost > avg_cost + 2*stdev_cost\n```\n\nUnderstanding this SPL\n\n**Cloud Billing Budget Alerts and Anomaly** — Budget alerts and spend anomalies prevent cost overruns. Early detection enables corrective action before invoice.\n\nDocumented **Data sources**: Billing export to BigQuery or Pub/Sub, Budget alert notifications. **App/TA** (typical add-on context): `Splunk_TA_google-cloudplatform`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: gcp; **sourcetype**: gcp:billing. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=gcp, sourcetype=\"gcp:billing\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by _time, service** so each row reflects one combination of those dimensions.\n• `eventstats` rolls up events into metrics; results are split **by service** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where daily_cost > avg_cost + 2*stdev_cost` — typically the threshold or rule expression for this monitoring goal.\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (cost with threshold), Table (service, cost, anomaly), Stacked area (cost by service).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_google-cloudplatform` — Splunk Add-on for Google Cloud Platform (Splunkbase 3084)). Enable the modular input or HEC token that writes sourcetype=gcp:billing into gcp. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`_time, service`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Budget alerts and spend anomalies prevent cost overruns.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
