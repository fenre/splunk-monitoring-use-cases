{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.1.58",
  "title": "AWS Transit Gateway Attachment Health",
  "criticality": "high",
  "difficulty": "advanced",
  "monitoringType": [
    "Availability",
    "Fault"
  ],
  "splunkPillar": "Observability",
  "controlTest": {
    "positiveScenario": "Inject sample events matching UC-4.1.58 SPL; confirm the search returns rows and alert fires.",
    "negativeScenario": "Replay benign traffic without matching markers; confirm UC-4.1.58 search returns zero rows."
  },
  "dataSources": "`index=aws` sourcetype=`aws:config:notification` collected via `Splunk_TA_aws`; validate ingest with `| stats count by sourcetype`.",
  "app": "`Splunk_TA_aws`",
  "spl": "index=aws sourcetype=\"aws:config:notification\" ( resourceType=\"AWS::EC2::TransitGatewayAttachment\" OR (sourcetype=\"aws:cloudwatch\" namespace=\"AWS/TransitGateway\" metric_name=\"BytesIn\"))\n| table _time userIdentity.arn eventName sourceIPAddress\n| eval uc_marker=\"UC-4.1.58_aws_transit_gateway_atta_58\"\n| sort -_time",
  "description": "TGW route propagation and attachment state affect cross-VPC and hybrid connectivity. Failed attachments or stale routes cause network outages.",
  "value": "Detecting aws transit gateway attachment health early reduces mean time to contain cloud incidents, supports audit logging controls, and prevents silent misconfiguration drift across accounts.",
  "implementation": "Enable AWS Config for TGW attachments to track state changes. Ingest TGW flow logs to S3 and forward to Splunk for traffic analysis. Collect CloudWatch TGW metrics (BytesIn, BytesOut) per attachment. Alert when attachment state is not available or traffic drops to zero unexpectedly. Correlate with route table propagation events. Use for hybrid connectivity and SD-WAN monitoring.",
  "visualization": "Table (attachment, state, traffic), Status grid (attachment health), Line chart (bytes in/out by attachment).",
  "cimModels": [
    "Performance"
  ],
  "cimSpl": "| tstats `summariesonly` max(Performance.cpu_load_percent) as peak\n  from datamodel=Performance.All_Performance\n  by Performance.object Performance.host span=1h\n| where isnotnull(peak)\n| sort - peak",
  "dataModelAcceleration": "Performance: enable if `aws:cloudwatch` (or your metric store) is mapped to CIM Performance, or use `mstats` on the same CloudWatch metrics. Enable Data Model Acceleration for the CIM data models in `cimModels` once `Splunk_TA_aws` (and any CIM tags/aliases) map your fields; otherwise the `cimSpl` summary search may be empty. Step 2 on raw or metric indexes stays primary for most sites.",
  "references": [
    {
      "title": "Splunk_TA_aws",
      "url": "https://splunkbase.splunk.com/app/1876"
    },
    {
      "title": "Vendor cloud documentation",
      "url": "https://docs.aws.amazon.com/"
    }
  ],
  "knownFalsePositives": "Planned deploys, game-day tests, and vendor maintenance that you expect to move CPU, error, or lag metrics; compare with the change window.",
  "securityDomain": "cloud",
  "equipment": [
    "aws"
  ],
  "equipmentModels": [
    "aws_config"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=aws`.\n• Install and configure `Splunk_TA_aws` with the modular input for `aws:config:notification`.\n• Prerequisite crawl UC(s): UC-4.1.78.\n• Confirm field extractions in `props.conf` for `userIdentity.arn` and related actor fields.\n\nStep 1 — Configure data collection\nEnable AWS Config for TGW attachments to track state changes. Ingest TGW flow logs to S3 and forward to Splunk for traffic analysis. Collect CloudWatch TGW metrics (BytesIn, BytesOut) per attachment. Alert when attachment state is not available or traffic drops to zero unexpectedly. Correlate with route table propagation events. Use for hybrid connectivity and SD-WAN monitoring.\nValidate ingest before alerting:\n```spl\nindex=aws sourcetype=\"aws:config:notification\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=aws sourcetype=\"aws:config:notification\" ( resourceType=\"AWS::EC2::TransitGatewayAttachment\" OR (sourcetype=\"aws:cloudwatch\" namespace=\"AWS/TransitGateway\" metric_name=\"BytesIn\"))\n| table _time userIdentity.arn eventName sourceIPAddress\n| eval uc_marker=\"UC-4.1.58_aws_transit_gateway_atta_58\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `aws:config:notification` in `index=aws` to detect aws transit gateway attachment health. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the provider cloud console audit view for the same 24-hour window. Confirm row counts and `userIdentity.arn` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_1_58_alert` with severity aligned to `high` criticality. Dashboard: Table (attachment, state, traffic), Status grid (attachment health), Line chart (bytes in/out by attachment)..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade `Splunk_TA_aws` and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of aws transit gateway attachment health so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.1.78"
  ],
  "splunkbaseApps": [
    {
      "id": 1876,
      "name": "Splunk Add-on for AWS",
      "role": "primary",
      "requiresSmeReview": true
    }
  ],
  "wave": "walk"
}
