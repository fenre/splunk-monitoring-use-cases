{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.5.2",
  "title": "Lambda Cold Start and Init Duration Latency",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "controlTest": {
    "positiveScenario": "Replay sample `aws:cloudwatch` events with `namespace=\"AWS/Lambda\" metric_name=\"InitDuration\"` into `index=aws`. Confirm UC-4.5.2 returns >=1 row and the alert fires.",
    "negativeScenario": "Replay benign `aws:cloudwatch` traffic without `namespace=\"AWS/Lambda\" metric_name=\"InitDuration\"`. Confirm UC-4.5.2 returns zero rows and the alert does not fire."
  },
  "dataSources": "`index=aws` sourcetype=`aws:cloudwatch` collected via `Splunk_TA_aws`; validate ingest with `| stats count by sourcetype`.",
  "app": "`Splunk_TA_aws`",
  "spl": "index=aws sourcetype=\"aws:cloudwatch\" namespace=\"AWS/Lambda\" metric_name=\"InitDuration\"\n| table _time metric_name namespace unit average maximum\n| eval uc_marker=\"UC-4.5.2_lambda_cold_start_and_in_2\"\n| sort -_time",
  "description": "UC-4.5.2 — Lambda Cold Start and Init Duration Latency: queries AWS `aws:cloudwatch` for search criteria `namespace=\"AWS/Lambda\" metric_name=\"InitDuration\"` and returns actor, resource, and region context for performance review and change validation.",
  "value": "On AWS, detecting 'Lambda Cold Start and Init Duration Latency' early shortens cloud incident response and produces defensible audit evidence for identity and change-management controls. UC-4.5.2 drives medium-priority performance alerting before customer-impacting drift.",
  "implementation": "Collect the `InitDuration` CloudWatch metric for each function. For log-based validation, subscribe Lambda log groups to Splunk and parse `REPORT` lines for `Init Duration`. Baseline p95/p99 init time per function and alert when cold-start latency breaches SLO after deploys or scaling events.",
  "visualization": "Line chart (avg/max Init Duration by function), Box plot or percentile overlay (if precomputed), Table (FunctionName, p95 init ms).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk_TA_aws",
      "url": "https://splunkbase.splunk.com/app/1876"
    },
    {
      "title": "Vendor cloud documentation",
      "url": "https://docs.aws.amazon.com/"
    }
  ],
  "knownFalsePositives": "Cold starts, retries, and a bad client sending junk traffic can all raise error counts without a code bug. We look at the share of invocations and we compare the same time last week before we assume the function is broken.",
  "securityDomain": "cloud",
  "equipment": [
    "aws"
  ],
  "equipmentModels": [
    "aws_cloudwatch"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=aws`.\n• Install and configure `Splunk_TA_aws` with the modular input for `aws:cloudwatch`.\n• Prerequisite crawl UC(s): UC-4.5.16.\n• Confirm field extractions in `props.conf` for `metric_name` and related actor fields.\n\nStep 1 — Configure data collection\nCollect the `InitDuration` CloudWatch metric for each function. For log-based validation, subscribe Lambda log groups to Splunk and parse `REPORT` lines for `Init Duration`. Baseline p95/p99 init time per function and alert when cold-start latency breaches SLO after deploys or scaling events.\nValidate ingest before alerting:\n```spl\nindex=aws sourcetype=\"aws:cloudwatch\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=aws sourcetype=\"aws:cloudwatch\" namespace=\"AWS/Lambda\" metric_name=\"InitDuration\"\n| table _time metric_name namespace unit average maximum\n| eval uc_marker=\"UC-4.5.2_lambda_cold_start_and_in_2\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `aws:cloudwatch` in `index=aws` to detect lambda cold start and init duration latency. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the Amazon CloudWatch console Metrics for the same 24-hour window. Confirm row counts and `metric_name` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_5_2_alert` with severity aligned to `medium` criticality. Dashboard: Line chart (avg/max Init Duration by function), Box plot or percentile overlay (if precomputed), Table (FunctionName, p95 init ms)..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade `Splunk_TA_aws` and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of lambda cold start and init duration latency so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.5.16"
  ],
  "wave": "walk"
}
