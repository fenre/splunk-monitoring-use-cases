{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.5.24",
  "title": "OCI Functions Invocation Errors",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "compliance": [
    {
      "regulation": "NIST-800-53",
      "version": "Rev.5",
      "clause": "AU-2",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Saved search evidence for AU-2 when alert outputs are archived."
    }
  ],
  "controlTest": {
    "positiveScenario": "Replay or inject sample `oci:functions:invocation` events matching `*` into `index=oci`. Confirm UC-4.5.24 search returns >=1 row and alert fires.",
    "negativeScenario": "Replay benign `oci:functions:invocation` traffic without `*` markers. Confirm the search returns zero rows and alert does not fire."
  },
  "dataSources": "`index=oci` sourcetype=`oci:functions:invocation` collected via Provider serverless monitoring via cloud TA; validate ingest with `| stats count by sourcetype`.",
  "app": "Provider serverless monitoring via cloud TA",
  "spl": "index=oci sourcetype=\"oci:functions:invocation\"\n| table _time functionId status duration\n| eval uc_marker=\"UC-4.5.24_oci_functions_invocation_24\"\n| sort -_time",
  "description": "Failed OCI function invocations. This UC monitors `oci:functions:invocation` for oci functions invocation errors and surfaces events for SOC triage, change review, and compliance evidence.",
  "value": "OCI Functions Invocation Errors impacts serverless SLOs and cost efficiency. Detecting oci functions invocation errors early reduces mean time to contain cloud incidents, supports audit logging controls, and prevents silent misconfiguration drift across accounts.",
  "implementation": "Configure required cloud inputs per provider guide; index=oci sourcetype=oci:functions:invocation.",
  "visualization": "Dashboard panel, alert, and weekly compliance report.",
  "cimModels": [],
  "references": [
    {
      "title": "Splunk Add-on for AWS",
      "url": "https://splunkbase.splunk.com/app/1876"
    },
    {
      "title": "Vendor documentation",
      "url": "https://docs.aws.amazon.com/lambda/"
    }
  ],
  "knownFalsePositives": "Approved change windows; documented automation service principals; sandbox accounts excluded via lookup.",
  "mitreAttack": [
    "T1078.004"
  ],
  "securityDomain": "cloud",
  "equipment": [
    "oci"
  ],
  "equipmentModels": [
    "oci_functions"
  ],
  "status": "community",
  "cost": {
    "tier": "medium",
    "search_load": "moderate",
    "tstats_eligible": false
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=oci`.\n• Install and configure Provider serverless monitoring via cloud TA with the modular input for `oci:functions:invocation`.\n• Prerequisite crawl UC(s): UC-4.5.16.\n• Confirm field extractions in `props.conf` for `functionId` and related actor fields.\n\nStep 1 — Configure data collection\nConfigure required cloud inputs per provider guide; index=oci sourcetype=oci:functions:invocation.\nValidate ingest before alerting:\n```spl\nindex=oci sourcetype=\"oci:functions:invocation\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=oci sourcetype=\"oci:functions:invocation\"\n| table _time functionId status duration\n| eval uc_marker=\"UC-4.5.24_oci_functions_invocation_24\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `oci:functions:invocation` in `index=oci` to detect oci functions invocation errors. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the provider cloud console audit view for the same 24-hour window. Confirm row counts and `functionId` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_5_24_alert` with severity aligned to `high` criticality. Dashboard: Dashboard panel, alert, and weekly compliance report..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade Provider serverless monitoring via cloud TA and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of oci functions invocation errors so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.5.16"
  ],
  "splunkbaseApps": [
    {
      "id": 1876,
      "name": "Splunk Add-on for AWS",
      "role": "primary",
      "requiresSmeReview": true
    }
  ],
  "wave": "walk"
}
