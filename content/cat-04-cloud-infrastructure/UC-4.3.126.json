{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.3.126",
  "title": "GCP Spanner instance create",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Security",
    "Audit"
  ],
  "splunkPillar": "Security",
  "compliance": [
    {
      "regulation": "NIST-800-53",
      "version": "Rev.5",
      "clause": "AU-2",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Saved search evidence for AU-2 when alert outputs are archived."
    }
  ],
  "controlTest": {
    "positiveScenario": "Replay sample `google:gcp:audit` events with `(protoPayload.methodName=\"*spanner.instances.create*\" OR protoPayload.methodName=\"spanner.instances.create\")` into `index=gcp`. Confirm UC-4.3.126 returns >=1 row and the alert fires.",
    "negativeScenario": "Replay benign `google:gcp:audit` traffic without `(protoPayload.methodName=\"*spanner.instances.create*\" OR protoPayload.methodName=\"spanner.instances.create\")`. Confirm UC-4.3.126 returns zero rows and the alert does not fire."
  },
  "dataSources": "`index=gcp` sourcetype=`google:gcp:audit` collected via Splunk Add-on for Google Cloud Platform (`Splunk_TA_google-cloudplatform`, Splunkbase 3088); validate ingest with `| stats count by sourcetype`.",
  "app": "Splunk Add-on for Google Cloud Platform (`Splunk_TA_google-cloudplatform`, Splunkbase 3088)",
  "spl": "index=gcp sourcetype=\"google:gcp:audit\" (protoPayload.methodName=\"*spanner.instances.create*\" OR protoPayload.methodName=\"spanner.instances.create\")\n| table _time protoPayload.methodName protoPayload.authenticationInfo.principalEmail resourceName\n| eval uc_marker=\"UC-4.3.126_gcp_spanner_instance_cre_126\"\n| sort -_time",
  "description": "UC-4.3.126 — GCP Spanner instance create: queries Google Cloud `google:gcp:audit` for `*spanner.instances.create*`, `spanner.instances.create` and returns actor, resource, and region context for security review and change validation.",
  "value": "On Google Cloud, unapproved creation tied to 'GCP Spanner instance create' expands credential and network attack surface and often precedes persistence or crypto-mining activity. UC-4.3.126 drives high-priority security alerting before customer-impacting drift.",
  "implementation": "Export Cloud Audit Logs to Pub/Sub; configure `Splunk_TA_google-cloudplatform` Pub/Sub input into `index=gcp`. Validate protoPayload.methodName extractions.",
  "visualization": "Events list, methodName timeline, principalEmail table.",
  "cimModels": [
    "Change"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Change.All_Changes\n  where All_Changes.action=*\n  by All_Changes.user All_Changes.action All_Changes.object span=1h\n| sort -count",
  "dataModelAcceleration": "Enable acceleration for Change after TA field mappings are verified.",
  "references": [
    {
      "title": "Splunk Add-on for Google Cloud Platform",
      "url": "https://splunkbase.splunk.com/app/3088"
    },
    {
      "title": "Cloud Audit Logs overview",
      "url": "https://cloud.google.com/logging/docs/audit"
    }
  ],
  "knownFalsePositives": "Approved change windows; documented automation service principals; sandbox accounts excluded via lookup.",
  "mitreAttack": [
    "T1078.004"
  ],
  "securityDomain": "cloud",
  "equipment": [
    "gcp"
  ],
  "equipmentModels": [
    "gcp_audit_log"
  ],
  "status": "community",
  "cost": {
    "tier": "medium",
    "search_load": "moderate",
    "tstats_eligible": true
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=gcp`.\n• Install and configure Splunk Add-on for Google Cloud Platform (`Splunk_TA_google-cloudplatform`, Splunkbase 3088) with the modular input for `google:gcp:audit`.\n• Prerequisite crawl UC(s): UC-4.3.41.\n• Confirm field extractions in `props.conf` for `protoPayload.methodName` and related actor fields.\n\nStep 1 — Configure data collection\nExport Cloud Audit Logs to Pub/Sub; configure `Splunk_TA_google-cloudplatform` Pub/Sub input into `index=gcp`. Validate protoPayload.methodName extractions.\nValidate ingest before alerting:\n```spl\nindex=gcp sourcetype=\"google:gcp:audit\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=gcp sourcetype=\"google:gcp:audit\" (protoPayload.methodName=\"*spanner.instances.create*\" OR protoPayload.methodName=\"spanner.instances.create\")\n| table _time protoPayload.methodName protoPayload.authenticationInfo.principalEmail resourceName\n| eval uc_marker=\"UC-4.3.126_gcp_spanner_instance_cre_126\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `google:gcp:audit` in `index=gcp` to detect gcp spanner instance create. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the GCP Logs Explorer console for the same 24-hour window. Confirm row counts and `protoPayload.methodName` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_3_126_alert` with severity aligned to `high` criticality. Dashboard: Events list, methodName timeline, principalEmail table..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade Splunk Add-on for Google Cloud Platform (`Splunk_TA_google-cloudplatform`, Splunkbase 3088) and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of gcp spanner instance create so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.3.41"
  ],
  "splunkbaseApps": [
    {
      "id": 3088,
      "name": "Splunk Add-on for Google Cloud Platform",
      "role": "primary",
      "requiresSmeReview": true
    }
  ],
  "wave": "walk"
}
