{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.1.66",
  "title": "AWS Config Rule Compliance Drift",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "exclusions": "Covers the detection logic documented for 'AWS Config Rule Compliance Drift' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_aws_config_rule_compliance_drift` (UC-4.1.66), SOC dashboard panel \"Operations — AWS Config Rule Compliance Drift\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-4.1.66 positive (AWS, Config, Rule, Compliance, Drift): ingest synthetic events into `index=aws` sourcetype='aws:config:notification' where `state_changes` grouped by `resourceId`, `configRuleName` satisfies `state_changes` > 1 for 'AWS Config Rule Compliance Drift'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-4.1.66 negative (Compliance Drift): ingest benign `aws` / `aws:config:notification` events where `resourceId`, `configRuleName` stays below `state_changes` > 1, or exclude the test entity via `ops_exception_register.csv`. Variant 8 confirms zero alert rows for 'AWS Config Rule Compliance Drift'."
  },
  "dataSources": "`sourcetype=aws:config:notification`, Config history snapshots Collected via Splunk Add-on for Amazon Web Services (Splunkbase 1876) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_aws` — Splunk Add-on for Amazon Web Services (Splunkbase 1876)",
  "spl": "index=aws sourcetype=\"aws:config:notification\" configRuleList{}.complianceType=*\n| mvexpand configRuleList{} limit=500\n| spath input=configRuleList{} path=complianceType\n| spath input=configRuleList{} path=configRuleName\n| stats dc(complianceType) as state_changes by resourceId, configRuleName\n| where state_changes > 1\n| sort -state_changes",
  "description": "Identifies UC-4.1.66 — AWS, Config, Rule, Compliance, Drift — using `index=aws` sourcetype `aws:config:notification`, grouped by `resourceId`, `configRuleName`. Outputs `state_changes` each `the scheduled window`; alert when `state_changes` > 1. Primary focus: Compliance Drift. Purpose-built for 'AWS Config Rule Compliance Drift' in Operations workflows.",
  "value": "UC-4.1.66 accelerates insight into Compliance Drift ('AWS Config Rule Compliance Drift') by tracking `state_changes` on `aws:config:notification` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Ingest configuration item change streams. Track flapping rules weekly. Alert when critical rules (encryption, public access) change state more than N times per day. Root-cause with CloudTrail correlation.",
  "visualization": "Table (resource, rule, changes), Line chart (compliant % over time), Single value (flapping resources).",
  "cimModels": [
    "Performance"
  ],
  "cimSpl": "| tstats `summariesonly` max(Performance.cpu_load_percent) as peak\n  from datamodel=Performance.All_Performance\n  by Performance.object Performance.host span=1h\n| where isnotnull(peak)\n| sort - peak",
  "dataModelAcceleration": "Performance: enable if `aws:cloudwatch` (or your metric store) is mapped to CIM Performance, or use `mstats` on the same CloudWatch metrics. Enable Data Model Acceleration for the CIM data models in `cimModels` once `Splunk_TA_aws` (and any CIM tags/aliases) map your fields; otherwise the `cimSpl` summary search may be empty. Step 2 on raw or metric indexes stays primary for most sites.",
  "references": [
    {
      "title": "Splunk_TA_aws",
      "url": "https://splunkbase.splunk.com/app/1876",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Performance",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Performance",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `aws` (UC-4.1.66)** — Planned work for 'AWS Config Rule Compliance Drift' shifts `state_changes` over `span=1h` while `resourceId`, `configRuleName` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `dc` on `aws:config:notification`** — Scheduled automation elevates `configRuleName` without crossing `state_changes` > 1 for UC-4.1.66. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `resourceId`, `configRuleName`** — Health checks in `aws` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `aws:config:notification`** — First 48h baseline for UC-4.1.66 on `resourceId` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `resourceId`, `exception_reason`, `valid_until`.",
  "mitreAttack": [
    "T1578"
  ],
  "equipment": [
    "aws"
  ],
  "equipmentModels": [
    "aws_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_aws`.\n• Ensure the following data sources are available: `sourcetype=aws:config:notification`, Config history snapshots.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest configuration item change streams. Track flapping rules weekly. Alert when critical rules (encryption, public access) change state more than N times per day. Root-cause with CloudTrail correlation.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=aws sourcetype=\"aws:config:notification\" configRuleList{}.complianceType=*\n| mvexpand configRuleList{} limit=500\n| spath input=configRuleList{} path=complianceType\n| spath input=configRuleList{} path=configRuleName\n| stats dc(complianceType) as state_changes by resourceId, configRuleName\n| where state_changes > 1\n| sort -state_changes\n```\n\nUnderstanding this SPL\n\n**AWS Config Rule Compliance Drift** — Resources oscillating between COMPLIANT and NON_COMPLIANT indicate automation fights or manual changes—drift trends surface systemic issues.\n\nDocumented **Data sources**: `sourcetype=aws:config:notification`, Config history snapshots. **App/TA** (typical add-on context): `Splunk_TA_aws`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: aws; **sourcetype**: aws:config:notification. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=aws, sourcetype=\"aws:config:notification\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Expands multivalue fields with `mvexpand` — use `limit=` to cap row explosion.\n• Extracts structured paths (JSON/XML) with `spath`.\n• Extracts structured paths (JSON/XML) with `spath`.\n• `stats` rolls up events into metrics; results are split **by resourceId, configRuleName** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where state_changes > 1` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (resource, rule, changes), Line chart (compliant % over time), Single value (flapping resources).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_aws` — Splunk Add-on for Amazon Web Services (Splunkbase 1876)). Enable the modular input or HEC token that writes sourcetype=aws:config:notification into aws. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`configRuleName, resourceId, state_changes`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We keep an eye on aws config rule compliance drift. The key performance numbers the cloud already exposes so the team can plan capacity and notice problems before user-visible outages. We want the right people to notice in time, not only when a customer or an auditor stumbles on it first.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
