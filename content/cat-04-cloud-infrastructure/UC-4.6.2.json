{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.6.2",
  "title": "Lambda/Function Invocation Volume Trending",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Lambda/Function Invocation Volume Trending' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_lambda_function_invocation_volume_trending` (UC-4.6.2), SOC dashboard panel \"Operations — Lambda/Function Invocation Volume Trending\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-4.6.2 positive (Lambda, Function, Invocation, Volume, Trending): ingest synthetic events into `index=cloud` sourcetype='aws:cloudwatch' where `invocations`, `total_invocations` grouped by `_time`, `FunctionName` satisfies detection threshold for 'Lambda/Function Invocation Volume Trending'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-4.6.2 negative (Volume Trending): ingest benign `cloud` / `aws:cloudwatch` events where `_time`, `FunctionName` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 6 confirms zero alert rows for 'Lambda/Function Invocation Volume Trending'."
  },
  "dataSources": "`index=cloud sourcetype=aws:cloudwatch` (Lambda Invocations metric); Azure Functions metrics Collected via Splunk Add-on for Amazon Web Services (Splunkbase 1876) modular input or scripted poll into the index named in the SPL.",
  "app": "Splunk Add-on for AWS (CloudWatch metrics), Azure Monitor — Splunk Add-on for Amazon Web Services (Splunkbase 1876)",
  "spl": "index=cloud sourcetype=\"aws:cloudwatch\" Namespace=\"AWS/Lambda\" MetricName=\"Invocations\"\n| bin _time span=1d\n| stats sum(Sum) as invocations by _time, FunctionName\n| timechart span=1d sum(invocations) as total_invocations\n| trendline sma7(total_invocations) as invocation_trend",
  "description": "Identifies Volume Trending anomalies for UC-4.6.2 with window `1d` on `index=cloud` sourcetype `aws:cloudwatch`. Combines `invocations`, `total_invocations` by `_time`, `FunctionName` and compares against configured threshold. Mapped to keywords Lambda, Function, Invocation, Volume, Trending for dashboard routing.",
  "value": "Alerting on Lambda, Function, Invocation, Volume, Trending through UC-4.6.2 reduces operational risk and delayed incident response by surfacing `invocations`, `total_invocations` early.",
  "implementation": "Enable CloudWatch metric ingestion for AWS/Lambda Invocations with FunctionName dimension. For Azure, use Microsoft.Web/sites/functions equivalent metrics. Normalize time to UTC for daily buckets. Use top-N functions by volume to keep the chart readable. Correlate step changes with deployments from CI/CD timestamps.",
  "visualization": "Line chart (daily invocations with 7-day SMA, 30 days), column chart (top functions by volume).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Add-on for AWS",
      "url": "https://splunkbase.splunk.com/app/1876",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `cloud` (UC-4.6.2)** — Planned work for 'Lambda/Function Invocation Volume Trending' shifts `invocations`, `total_invocations` over `span=1d` while `_time`, `FunctionName` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `sum` on `aws:cloudwatch`** — Scheduled automation elevates `FunctionName` without crossing configured threshold for UC-4.6.2. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `_time`, `FunctionName`** — Health checks in `cloud` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `aws:cloudwatch`** — First 48h baseline for UC-4.6.2 on `_time` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `_time`, `exception_reason`, `valid_until`.",
  "equipment": [
    "aws",
    "azure"
  ],
  "equipmentModels": [
    "aws_generic",
    "azure_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: Splunk Add-on for AWS (CloudWatch metrics), Azure Monitor.\n• Ensure the following data sources are available: `index=cloud sourcetype=aws:cloudwatch` (Lambda Invocations metric); Azure Functions metrics.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnable CloudWatch metric ingestion for AWS/Lambda Invocations with FunctionName dimension. For Azure, use Microsoft.Web/sites/functions equivalent metrics. Normalize time to UTC for daily buckets. Use top-N functions by volume to keep the chart readable. Correlate step changes with deployments from CI/CD timestamps.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=cloud sourcetype=\"aws:cloudwatch\" Namespace=\"AWS/Lambda\" MetricName=\"Invocations\"\n| bin _time span=1d\n| stats sum(Sum) as invocations by _time, FunctionName\n| timechart span=1d sum(invocations) as total_invocations\n| trendline sma7(total_invocations) as invocation_trend\n```\n\nUnderstanding this SPL\n\n**Lambda/Function Invocation Volume Trending** — Daily invocation counts show traffic growth, seasonal patterns, and the impact of new features or batch jobs. Sharp changes often precede cost spikes or throttling if concurrency limits are fixed.\n\nDocumented **Data sources**: `index=cloud sourcetype=aws:cloudwatch` (Lambda Invocations metric); Azure Functions metrics. **App/TA** (typical add-on context): Splunk Add-on for AWS (CloudWatch metrics), Azure Monitor. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: cloud; **sourcetype**: aws:cloudwatch. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=cloud, sourcetype=\"aws:cloudwatch\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Discretizes time or numeric ranges with `bin`/`bucket`.\n• `stats` rolls up events into metrics; results are split **by _time, FunctionName** so each row reflects one combination of those dimensions.\n• `timechart` plots the metric over time using **span=1d** buckets — ideal for trending and alerting on this use case.\n• Pipeline stage (see **Lambda/Function Invocation Volume Trending**): trendline sma7(total_invocations) as invocation_trend\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (daily invocations with 7-day SMA, 30 days), column chart (top functions by volume).",
  "grandmaExplanation": "Daily invocation counts show traffic growth, seasonal patterns, and the impact of new features or batch jobs.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
