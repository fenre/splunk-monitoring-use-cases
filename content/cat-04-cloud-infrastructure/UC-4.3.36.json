{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.3.36",
  "title": "Memorystore (Redis) Health",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Memorystore (Redis) Health' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_memorystore_redis_health` (UC-4.3.36), SOC dashboard panel \"Operations — Memorystore (Redis) Health\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=gcp` sourcetype='google:gcp:monitoring' where `resource.labels.instance_id` satisfies `mem_ratio` > 0.9 for UC-4.3.36 ('Memorystore (Redis) Health'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `gcp` / `google:gcp:monitoring` events where `resource.labels.instance_id` stays below `mem_ratio` > 0.9, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-4.3.36."
  },
  "dataSources": "`sourcetype=google:gcp:monitoring` (`redis.googleapis.com/stats/memory/usage_ratio`, `replication/role`, `cpu/utilization`) Collected via Splunk Add-on for Google Cloud Platform (Splunkbase 3084) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_google-cloudplatform` — Splunk Add-on for Google Cloud Platform (Splunkbase 3084)",
  "spl": "index=gcp sourcetype=\"google:gcp:monitoring\" metric.type=\"redis.googleapis.com/stats/memory/usage_ratio\"\n| bin _time span=5m\n| stats latest(value) as mem_ratio by resource.labels.instance_id, _time\n| where mem_ratio > 0.9\n| sort - mem_ratio",
  "description": "Redis backs sessions and caches; memory pressure and replication lag cause timeouts and stale reads. Searches `index=gcp` with sourcetype `google:gcp:monitoring` for UC-4.3.36, aggregating `count` by `resource.labels.instance_id` over `5m`. Alerts when grouped results cross `mem_ratio` > 0.9 in the saved search. Gives Operations operators a repeatable signal for 'Memorystore (Redis) Health' without manual log review.",
  "value": "Early 'Memorystore (Redis) Health' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Alert on memory usage above 90%, high CPU, or replica lag metrics. Plan tier upgrades or key eviction policies. Monitor persistence (RDB/AOF) failures if enabled.",
  "visualization": "Line chart (memory ratio, CPU), Table (instance, tier), Single value (evictions if exported).",
  "cimModels": [
    "Performance"
  ],
  "cimSpl": "| tstats `summariesonly` avg(Performance.mem_used_percent) as agg_value\n  from datamodel=Performance where nodename=Performance.Memory\n  by Performance.host span=5m\n| sort - agg_value",
  "dataModelAcceleration": "Enable data model acceleration for Performance (Memory node).",
  "references": [
    {
      "title": "Splunk_TA_google-cloudplatform",
      "url": "https://splunkbase.splunk.com/app/3088",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Performance",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Performance",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Enterprise Documentation",
      "url": "https://docs.splunk.com/Documentation/Splunk",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk Search Reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/SearchReference/What'sInThisManual",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `gcp`** — Planned work for 'Memorystore (Redis) Health' shifts `count` over `span=5m` while `resource.labels.instance_id` remains in-scope for UC-4.3.36. Cross-check CMDB CHG.\n\n2. **Batch `count` on `google:gcp:monitoring`** — Scheduled automation elevates `_time` without crossing `mem_ratio` > 0.9. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `resource.labels.instance_id`** — Health checks in `gcp` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `google:gcp:monitoring`** — First 48h baseline for `resource.labels.instance_id` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `resource.labels.instance_id`, `exception_reason`, `valid_until`.",
  "equipment": [
    "gcp",
    "redis"
  ],
  "equipmentModels": [
    "gcp_generic",
    "redis_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_google-cloudplatform`.\n• Ensure the following data sources are available: `sourcetype=google:gcp:monitoring` (`redis.googleapis.com/stats/memory/usage_ratio`, `replication/role`, `cpu/utilization`).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nAlert on memory usage above 90%, high CPU, or replica lag metrics. Plan tier upgrades or key eviction policies. Monitor persistence (RDB/AOF) failures if enabled.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=gcp sourcetype=\"google:gcp:monitoring\" metric.type=\"redis.googleapis.com/stats/memory/usage_ratio\"\n| stats latest(value) as mem_ratio by resource.labels.instance_id, bin(_time, 5m)\n| where mem_ratio > 0.9\n| sort - mem_ratio\n```\n\nUnderstanding this SPL\n\n**Memorystore (Redis) Health** — Redis backs sessions and caches; memory pressure and replication lag cause timeouts and stale reads.\n\nDocumented **Data sources**: `sourcetype=google:gcp:monitoring` (`redis.googleapis.com/stats/memory/usage_ratio`, `replication/role`, `cpu/utilization`). **App/TA** (typical add-on context): `Splunk_TA_google-cloudplatform`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: gcp; **sourcetype**: google:gcp:monitoring. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=gcp, sourcetype=\"google:gcp:monitoring\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `stats` rolls up events into metrics; results are split **by resource.labels.instance_id, bin(_time, 5m)** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where mem_ratio > 0.9` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` avg(Performance.mem_used_percent) as agg_value\n  from datamodel=Performance where nodename=Performance.Memory\n  by Performance.host span=5m\n| sort - agg_value\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Memorystore (Redis) Health** — Redis backs sessions and caches; memory pressure and replication lag cause timeouts and stale reads.\n\nIf you map cloud vendor fields into the CIM, this variant uses normalized names and `tstats` on accelerated models. The raw vendor search in Step 2 is still the first stop for troubleshooting.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` on accelerated data model `Performance.Memory` — enable that model in Data Models and CIM add-ons, or the search may return no rows.\n\n• Uses `sort` to rank results; add `head` to limit the table.\n\nEnable Data Model Acceleration (and the right field aliases) for the models or datasets above; otherwise `tstats` may not find summaries.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` avg(Performance.mem_used_percent) as agg_value\n  from datamodel=Performance where nodename=Performance.Memory\n  by Performance.host span=5m\n| sort - agg_value\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Memorystore (Redis) Health** — Redis backs sessions and caches; memory pressure and replication lag cause timeouts and stale reads.\n\nIf you map cloud vendor fields into the CIM, this variant uses normalized names and `tstats` on accelerated models. The raw vendor search in Step 2 is still the first stop for troubleshooting.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` on the `Performance` data model (Memory node)—enable that model in Data Models and the CIM add-on, or the search may return no rows.\n\n• Uses `sort` to rank results; add `head` to limit the table.\n\nEnable Data Model Acceleration (and the right field aliases) for the models or datasets above; otherwise `tstats` may not find summaries.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` avg(Performance.mem_used_percent) as agg_value\n  from datamodel=Performance where nodename=Performance.Memory\n  by Performance.host span=5m\n| sort - agg_value\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Memorystore (Redis) Health** — Redis backs sessions and caches; memory pressure and replication lag cause timeouts and stale reads.\n\nIf you map cloud vendor fields into the CIM, this variant uses normalized names and `tstats` on accelerated models. The raw vendor search in Step 2 is still the first stop for troubleshooting.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` on the `Performance` data model (Memory node)—enable that model in Data Models and the CIM add-on, or the search may return no rows.\n\n• Uses `sort` to rank results; add `head` to limit the table.\n\nEnable Data Model Acceleration (and the right field aliases) for the models or datasets above; otherwise `tstats` may not find summaries.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` avg(Performance.mem_used_percent) as agg_value\n  from datamodel=Performance where nodename=Performance.Memory\n  by Performance.host span=5m\n| sort - agg_value\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Memorystore (Redis) Health** — Redis backs sessions and caches; memory pressure and replication lag cause timeouts and stale reads.\n\nIf you map cloud vendor fields into the CIM, this variant uses normalized names and `tstats` on accelerated models. The raw vendor search in Step 2 is still the first stop for troubleshooting.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` on the `Performance` data model (Memory node)—enable that model in Data Models and the CIM add-on, or the search may return no rows.\n\n• Uses `sort` to rank results; add `head` to limit the table.\n\nEnable Data Model Acceleration (and the right field aliases) for the models or datasets above; otherwise `tstats` may not find summaries.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` avg(Performance.mem_used_percent) as agg_value\n  from datamodel=Performance where nodename=Performance.Memory\n  by Performance.host span=5m\n| sort - agg_value\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Memorystore (Redis) Health** — Redis backs sessions and caches; memory pressure and replication lag cause timeouts and stale reads.\n\nIf you map cloud vendor fields into the CIM, this variant uses normalized names and `tstats` on accelerated models. The raw vendor search in Step 2 is still the first stop for troubleshooting.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` on the `Performance` data model (Memory node)—enable that model in Data Models and the CIM add-on, or the search may return no rows.\n\n• Uses `sort` to rank results; add `head` to limit the table.\n\nEnable Data Model Acceleration (and the right field aliases) for the models or datasets above; otherwise `tstats` may not find summaries.\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (memory ratio, CPU), Table (instance, tier), Single value (evictions if exported).",
  "grandmaExplanation": "Redis backs sessions and caches; memory pressure and replication lag cause timeouts and stale reads.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
