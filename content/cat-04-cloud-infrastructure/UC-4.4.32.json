{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.4.32",
  "title": "Cloud Control Plane API Call Volume Anomaly (MLTK)",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Security",
    "Performance"
  ],
  "splunkPillar": "Security",
  "controlTest": {
    "positiveScenario": "Inject sample events matching UC-4.4.32 SPL; confirm the search returns rows and alert fires.",
    "negativeScenario": "Replay benign traffic without matching markers; confirm UC-4.4.32 search returns zero rows."
  },
  "dataSources": "`index=cloud` sourcetype=`aws:cloudtrail` collected via Splunk Machine Learning Toolkit (MLTK), Splunk Add-on for AWS / Azure / GCP; validate ingest with `| stats count by sourcetype`.",
  "app": "Splunk Machine Learning Toolkit (MLTK), Splunk Add-on for AWS / Azure / GCP",
  "spl": "index=cloud sourcetype=\"aws:cloudtrail\" sourcetype IN (\"aws:cloudtrail\",\"azure:monitor:activity\",\"google:gcp:pubsub:message\")\n| table _time userIdentity.arn eventName sourceIPAddress awsRegion\n| eval uc_marker=\"UC-4.4.32_cloud_control_plane_api_32\"\n| sort -_time",
  "description": "Cloud control plane API calls (EC2 RunInstances, IAM CreateUser, S3 PutBucketPolicy) follow predictable patterns tied to deployment schedules and automation cadence. Anomalous spikes in API call volume may indicate compromised credentials, runaway automation, or an attacker enumerating resources — all of which are invisible to static rate limits but detectable through ML-based baselining.",
  "value": "Detecting cloud control plane api call volume anomaly (mltk) early reduces mean time to contain cloud incidents, supports audit logging controls, and prevents silent misconfiguration drift across accounts.",
  "implementation": "Aggregate CloudTrail / Activity Log / Admin Activity events hourly by API action and principal. Train DensityFunction models per API action on 30 days of data to capture automation schedules and deployment patterns. Flag calls that exceed 3 standard deviations from the learned baseline. Prioritize high-risk APIs: IAM mutations, security group changes, KMS key operations, and resource creation. Enrich with source IP geolocation and threat intelligence. Correlate with CI/CD deployment events (cat-12) to suppress planned automation bursts. Generate risk events for Splunk ES with MITRE T1078/T1580 annotations. Retrain models weekly.",
  "visualization": "Line chart (API call volume vs baseline), Table (anomalous API calls with z-scores), Bar chart (top anomalous APIs by principal).",
  "cimModels": [
    "Change"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Change.All_Changes\n  by All_Changes.user All_Changes.object_category All_Changes.action span=1h\n| sort -count",
  "dataModelAcceleration": "Enable data model acceleration for Change (All_Changes).",
  "references": [
    {
      "title": "Splunk Add-on for AWS",
      "url": "https://splunkbase.splunk.com/app/1876"
    },
    {
      "title": "CIM: Change",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Change"
    }
  ],
  "knownFalsePositives": "This pattern can surge during change windows, in sandboxes, and when a platform is in maintenance, so we line alerts up with the calendar and take a second look before we call it an incident.",
  "mitreAttack": [
    "T1078.004",
    "T1526",
    "T1580",
    "T1098.001"
  ],
  "securityDomain": "cloud",
  "equipment": [
    "aws",
    "azure",
    "gcp"
  ],
  "equipmentModels": [
    "aws_cloudtrail"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=cloud`.\n• Install and configure Splunk Machine Learning Toolkit (MLTK), Splunk Add-on for AWS / Azure / GCP with the modular input for `aws:cloudtrail`.\n• Prerequisite crawl UC(s): UC-4.4.33.\n• Confirm field extractions in `props.conf` for `userIdentity.arn` and related actor fields.\n\nStep 1 — Configure data collection\nAggregate CloudTrail / Activity Log / Admin Activity events hourly by API action and principal. Train DensityFunction models per API action on 30 days of data to capture automation schedules and deployment patterns. Flag calls that exceed 3 standard deviations from the learned baseline. Prioritize high-risk APIs: IAM mutations, security group changes, KMS key operations, and resource creation. Enrich with source IP geolocation and threat intelligence. Correlate with CI/CD deployment events (cat-12) to suppress planned automation bursts. Generate risk events for Splunk ES with MITRE T1078/T1580 annotations. Retrain models weekly.\nValidate ingest before alerting:\n```spl\nindex=cloud sourcetype=\"aws:cloudtrail\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=cloud sourcetype=\"aws:cloudtrail\" sourcetype IN (\"aws:cloudtrail\",\"azure:monitor:activity\",\"google:gcp:pubsub:message\")\n| table _time userIdentity.arn eventName sourceIPAddress awsRegion\n| eval uc_marker=\"UC-4.4.32_cloud_control_plane_api_32\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `aws:cloudtrail` in `index=cloud` to detect cloud control plane api call volume anomaly (mltk). Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the AWS CloudTrail console Event history for the same 24-hour window. Confirm row counts and `userIdentity.arn` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_4_32_alert` with severity aligned to `critical` criticality. Dashboard: Line chart (API call volume vs baseline), Table (anomalous API calls with z-scores), Bar chart (top anomalous APIs by principal)..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade Splunk Machine Learning Toolkit (MLTK), Splunk Add-on for AWS / Azure / GCP and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of cloud control plane api call volume anomaly (mltk) so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.4.33"
  ],
  "subcategory": "4.5",
  "wave": "walk"
}
