{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.2.50",
  "title": "Azure Data Factory Pipeline Failures",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Fault"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Azure Data Factory Pipeline Failures' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_azure_data_factory_pipeline_failures` (UC-4.2.50), SOC dashboard panel \"Operations — Azure Data Factory Pipeline Failures\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-4.2.50 positive (Azure, Data, Factory, Pipeline, Failures): ingest synthetic events into `index=cloud` sourcetype='azure:diagnostics' where `count` grouped by `pipelineName`, `resource_name` satisfies detection threshold for 'Azure Data Factory Pipeline Failures'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-4.2.50 negative (Pipeline Failures): ingest benign `cloud` / `azure:diagnostics` events where `pipelineName`, `resource_name` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 2 confirms zero alert rows for 'Azure Data Factory Pipeline Failures'."
  },
  "dataSources": "`sourcetype=azure:diagnostics` (PipelineRuns, ActivityRuns, TriggerRuns) Collected via Splunk Add-on for Microsoft Azure (Splunkbase 3110) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_microsoft-cloudservices` (Azure Monitor diagnostics) — Splunk Add-on for Microsoft Azure (Splunkbase 3110)",
  "spl": "index=cloud sourcetype=\"azure:diagnostics\" Category=\"PipelineRuns\"\n| where status=\"Failed\"\n| stats count as failures, latest(start) as last_failure by pipelineName, resource_name\n| sort -failures",
  "description": "Surfaces UC-4.2.50 for Azure, Data, Factory, Pipeline, Failures over `index=cloud` sourcetype `azure:diagnostics` with thresholds configured threshold. Groups `count` by `pipelineName`, `resource_name` each `the scheduled window`. Centered on Pipeline Failures indicators. Supports repeatable operations triage for 'Azure Data Factory Pipeline Failures'.",
  "value": "For 'Azure Data Factory Pipeline Failures', UC-4.2.50 converts `activity` on `azure:diagnostics` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Enable diagnostics on Data Factory to route `PipelineRuns`, `ActivityRuns`, and `TriggerRuns` to Splunk via Event Hub. Alert on failed pipeline runs. Track activity-level errors for root cause (copy failures, data flow errors, linked service timeouts). Monitor pipeline duration trending to detect degradation before SLA breach.",
  "visualization": "Table (failed pipelines with error), Bar chart (failures by pipeline), Line chart (pipeline duration trend).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk_TA_microsoft-cloudservices",
      "url": "https://splunkbase.splunk.com/app/3110",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk savedsearches.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Savedsearchesconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `cloud` (UC-4.2.50)** — Planned work for 'Azure Data Factory Pipeline Failures' shifts `count` over `span=1h` while `pipelineName`, `resource_name` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `count` on `azure:diagnostics`** — Scheduled automation elevates `resource_name` without crossing configured threshold for UC-4.2.50. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `pipelineName`, `resource_name`** — Health checks in `cloud` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `azure:diagnostics`** — First 48h baseline for UC-4.2.50 on `pipelineName` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `pipelineName`, `exception_reason`, `valid_until`.",
  "equipment": [
    "azure",
    "m365"
  ],
  "equipmentModels": [
    "azure_generic",
    "m365_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_microsoft-cloudservices` (Azure Monitor diagnostics).\n• Ensure the following data sources are available: `sourcetype=azure:diagnostics` (PipelineRuns, ActivityRuns, TriggerRuns).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nEnable diagnostics on Data Factory to route `PipelineRuns`, `ActivityRuns`, and `TriggerRuns` to Splunk via Event Hub. Alert on failed pipeline runs. Track activity-level errors for root cause (copy failures, data flow errors, linked service timeouts). Monitor pipeline duration trending to detect degradation before SLA breach.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=cloud sourcetype=\"azure:diagnostics\" Category=\"PipelineRuns\"\n| where status=\"Failed\"\n| stats count as failures, latest(start) as last_failure by pipelineName, resource_name\n| sort -failures\n```\n\nUnderstanding this SPL\n\n**Azure Data Factory Pipeline Failures** — Data Factory orchestrates ETL/ELT pipelines that feed data warehouses, analytics, and operational systems. Pipeline failures cause stale data, broken dashboards, and missed SLAs.\n\nDocumented **Data sources**: `sourcetype=azure:diagnostics` (PipelineRuns, ActivityRuns, TriggerRuns). **App/TA** (typical add-on context): `Splunk_TA_microsoft-cloudservices` (Azure Monitor diagnostics). The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: cloud; **sourcetype**: azure:diagnostics. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=cloud, sourcetype=\"azure:diagnostics\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Filters the current rows with `where status=\"Failed\"` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by pipelineName, resource_name** so each row reflects one combination of those dimensions.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (failed pipelines with error), Bar chart (failures by pipeline), Line chart (pipeline duration trend).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_microsoft-cloudservices` (Azure Monitor diagnostics) — Splunk Add-on for Microsoft Azure (Splunkbase 3110)). Enable the modular input or HEC token that writes sourcetype=azure:diagnostics into cloud. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`pipelineName, resource_name`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Data Factory orchestrates ETL/ELT pipelines that feed data warehouses, analytics, and operational systems.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
