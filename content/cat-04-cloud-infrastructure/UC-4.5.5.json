{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.5.5",
  "title": "Azure Functions Execution Duration",
  "criticality": "medium",
  "difficulty": "beginner",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "controlTest": {
    "positiveScenario": "Replay sample `mscs:azure:metrics` events with `metricName=\"FunctionExecutionDuration\"` into `index=azure`. Confirm UC-4.5.5 returns >=1 row and the alert fires.",
    "negativeScenario": "Replay benign `mscs:azure:metrics` traffic without `metricName=\"FunctionExecutionDuration\"`. Confirm UC-4.5.5 returns zero rows and the alert does not fire."
  },
  "dataSources": "`index=azure` sourcetype=`mscs:azure:metrics` collected via `Splunk_TA_microsoft-cloudservices`; validate ingest with `| stats count by sourcetype`.",
  "app": "`Splunk_TA_microsoft-cloudservices`",
  "spl": "index=azure sourcetype=\"mscs:azure:metrics\" metricName=\"FunctionExecutionDuration\"\n| table _time userIdentity.arn eventName sourceIPAddress\n| eval uc_marker=\"UC-4.5.5_azure_functions_executio_5\"\n| sort -_time",
  "description": "UC-4.5.5 — Azure Functions Execution Duration: queries Microsoft Azure `mscs:azure:metrics` for search criteria `metricName=\"FunctionExecutionDuration\"` and returns actor, resource, and region context for performance review and change validation.",
  "value": "On Microsoft Azure, detecting 'Azure Functions Execution Duration' early shortens cloud incident response and produces defensible audit evidence for identity and change-management controls. UC-4.5.5 drives medium-priority performance alerting before customer-impacting drift.",
  "implementation": "Enable Azure Monitor metrics for Function Apps and ingest via the TA (dimensions: function name where available). Establish baselines per function. Alert when p95 duration approaches the function timeout or degrades after releases.",
  "visualization": "Line chart (avg/max duration by app or function), Heatmap (duration by hour), Table (resourceName, avg_ms, max_ms).",
  "cimModels": [
    "Performance"
  ],
  "cimSpl": "| tstats `summariesonly` avg(Performance.cpu_load_percent) as agg_value\n  from datamodel=Performance where nodename=Performance.CPU\n  by Performance.host span=5m\n| sort - agg_value",
  "dataModelAcceleration": "Enable data model acceleration for Performance (CPU node).",
  "references": [
    {
      "title": "Splunk_TA_microsoft-cloudservices",
      "url": "https://splunkbase.splunk.com/app/3110"
    },
    {
      "title": "CIM: Performance",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Performance"
    }
  ],
  "knownFalsePositives": "Short spikes at deploy time, autoscale thrash, or a noisy neighbor on shared hosts can look bad for a few minutes. We require the condition to last across several intervals or clear on its own before we wake someone.",
  "securityDomain": "cloud",
  "equipment": [
    "azure"
  ],
  "equipmentModels": [
    "azure_metrics"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=azure`.\n• Install and configure `Splunk_TA_microsoft-cloudservices` with the modular input for `mscs:azure:metrics`.\n• Prerequisite crawl UC(s): UC-4.5.16.\n• Confirm field extractions in `props.conf` for `userIdentity.arn` and related actor fields.\n\nStep 1 — Configure data collection\nEnable Azure Monitor metrics for Function Apps and ingest via the TA (dimensions: function name where available). Establish baselines per function. Alert when p95 duration approaches the function timeout or degrades after releases.\nValidate ingest before alerting:\n```spl\nindex=azure sourcetype=\"mscs:azure:metrics\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=azure sourcetype=\"mscs:azure:metrics\" metricName=\"FunctionExecutionDuration\"\n| table _time userIdentity.arn eventName sourceIPAddress\n| eval uc_marker=\"UC-4.5.5_azure_functions_executio_5\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `mscs:azure:metrics` in `index=azure` to detect azure functions execution duration. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the provider cloud console audit view for the same 24-hour window. Confirm row counts and `userIdentity.arn` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_5_5_alert` with severity aligned to `medium` criticality. Dashboard: Line chart (avg/max duration by app or function), Heatmap (duration by hour), Table (resourceName, avg_ms, max_ms)..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade `Splunk_TA_microsoft-cloudservices` and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of azure functions execution duration so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.5.16"
  ],
  "wave": "walk"
}
