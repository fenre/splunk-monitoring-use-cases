{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.6.1",
  "title": "Cloud Resource Count Trending",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Capacity"
  ],
  "splunkPillar": "Security",
  "controlTest": {
    "positiveScenario": "Replay sample `aws:config:notification` events with `resourceType=\"AWS::EC2::Instance\"` into `index=cloud`. Confirm UC-4.6.1 returns >=1 row and the alert fires.",
    "negativeScenario": "Replay benign `aws:config:notification` traffic without `resourceType=\"AWS::EC2::Instance\"`. Confirm UC-4.6.1 returns zero rows and the alert does not fire."
  },
  "dataSources": "`index=cloud` sourcetype=`aws:config:notification` collected via Splunk Add-on for AWS, Splunk Add-on for Microsoft Cloud Services, Google Cloud add-ons; validate ingest with `| stats count by sourcetype`.",
  "app": "Splunk Add-on for AWS, Splunk Add-on for Microsoft Cloud Services, Google Cloud add-ons",
  "spl": "index=cloud sourcetype=\"aws:config:notification\" resourceType=\"AWS::EC2::Instance\"\n| table _time userIdentity.arn eventName sourceIPAddress\n| eval uc_marker=\"UC-4.6.1_cloud_resource_count_tre_1\"\n| sort -_time",
  "description": "UC-4.6.1 — Cloud Resource Count Trending: queries multi-cloud (AWS/Azure/GCP) `aws:config:notification` for search criteria `resourceType=\"AWS::EC2::Instance\"` and returns actor, resource, and region context for capacity review and change validation.",
  "value": "On multi-cloud (AWS/Azure/GCP), detecting 'Cloud Resource Count Trending' early shortens cloud incident response and produces defensible audit evidence for identity and change-management controls. UC-4.6.1 drives medium-priority capacity alerting before customer-impacting drift.",
  "implementation": "Ingest periodic inventory snapshots (AWS Config, DescribeInstances exports, or Azure Resource Graph) into index=cloud with one event per instance per snapshot. If only change streams exist, maintain state with a nightly summary search. Chart instance_count over 90 days; optionally split by accountId or region. For multi-cloud, normalize resourceType across providers.",
  "visualization": "Line chart (instance count over 90 days with trend and 30-day forecast), area chart stacked by account.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Add-on for AWS",
      "url": "https://splunkbase.splunk.com/app/1876"
    },
    {
      "title": "Splunk Add-on for Microsoft Cloud Services",
      "url": "https://splunkbase.splunk.com/app/3110"
    }
  ],
  "knownFalsePositives": "Inventories can skip a run when an API is throttled or a connector is in maintenance, which looks like a big drop. We check the ingest time and the last good snapshot before we call it shrinkage.",
  "securityDomain": "cloud",
  "equipment": [
    "aws",
    "azure",
    "gcp"
  ],
  "equipmentModels": [
    "aws_config"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=cloud`.\n• Install and configure Splunk Add-on for AWS, Splunk Add-on for Microsoft Cloud Services, Google Cloud add-ons with the modular input for `aws:config:notification`.\n• Prerequisite crawl UC(s): UC-4.6.7.\n• Confirm field extractions in `props.conf` for `userIdentity.arn` and related actor fields.\n\nStep 1 — Configure data collection\nIngest periodic inventory snapshots (AWS Config, DescribeInstances exports, or Azure Resource Graph) into index=cloud with one event per instance per snapshot. If only change streams exist, maintain state with a nightly summary search. Chart instance_count over 90 days; optionally split by accountId or region. For multi-cloud, normalize resourceType across providers.\nValidate ingest before alerting:\n```spl\nindex=cloud sourcetype=\"aws:config:notification\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=cloud sourcetype=\"aws:config:notification\" resourceType=\"AWS::EC2::Instance\"\n| table _time userIdentity.arn eventName sourceIPAddress\n| eval uc_marker=\"UC-4.6.1_cloud_resource_count_tre_1\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `aws:config:notification` in `index=cloud` to detect cloud resource count trending. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the provider cloud console audit view for the same 24-hour window. Confirm row counts and `userIdentity.arn` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_6_1_alert` with severity aligned to `medium` criticality. Dashboard: Line chart (instance count over 90 days with trend and 30-day forecast), area chart stacked by account..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade Splunk Add-on for AWS, Splunk Add-on for Microsoft Cloud Services, Google Cloud add-ons and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of cloud resource count trending so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.6.7"
  ],
  "wave": "walk"
}
