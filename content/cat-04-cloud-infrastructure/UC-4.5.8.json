{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.5.8",
  "title": "GCP Cloud Functions Timeout Monitoring",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "controlTest": {
    "positiveScenario": "Inject sample events matching UC-4.5.8 SPL; confirm the search returns rows and alert fires.",
    "negativeScenario": "Replay benign traffic without matching markers; confirm UC-4.5.8 search returns zero rows."
  },
  "dataSources": "`index=gcp` sourcetype=`google:gcp:pubsub:message` collected via `Splunk_TA_google-cloudplatform`; validate ingest with `| stats count by sourcetype`.",
  "app": "`Splunk_TA_google-cloudplatform`",
  "spl": "index=gcp sourcetype=\"google:gcp:pubsub:message\" resource.type=\"cloud_function\"\n| table _time messageId publishTime attributes\n| eval uc_marker=\"UC-4.5.8_gcp_cloud_functions_time_8\"\n| sort -_time",
  "description": "Timeouts indicate hung dependencies or insufficient deadline; they drive retries, duplicate side effects, and user-visible failures in synchronous invocations.",
  "value": "Detecting gcp cloud functions timeout monitoring early reduces mean time to contain cloud incidents, supports audit logging controls, and prevents silent misconfiguration drift across accounts.",
  "implementation": "Forward Cloud Functions logs to Pub/Sub and ingest with `resource.type=\"cloud_function\"`. Optionally add monitoring metrics for execution times and error result codes. Alert on timeout string patterns or rising timeout counts after dependency or region incidents.",
  "visualization": "Column chart (timeouts by function), Line chart (timeouts over time), Table (function_name, region, count).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk_TA_google-cloudplatform",
      "url": "https://splunkbase.splunk.com/app/3088"
    },
    {
      "title": "Vendor cloud documentation",
      "url": "https://cloud.google.com/docs"
    }
  ],
  "knownFalsePositives": "This pattern can surge during change windows, in sandboxes, and when a platform is in maintenance, so we line alerts up with the calendar and take a second look before we call it an incident.",
  "securityDomain": "cloud",
  "equipment": [
    "gcp"
  ],
  "equipmentModels": [
    "gcp_pubsub"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=gcp`.\n• Install and configure `Splunk_TA_google-cloudplatform` with the modular input for `google:gcp:pubsub:message`.\n• Prerequisite crawl UC(s): UC-4.5.16.\n• Confirm field extractions in `props.conf` for `messageId` and related actor fields.\n\nStep 1 — Configure data collection\nForward Cloud Functions logs to Pub/Sub and ingest with `resource.type=\"cloud_function\"`. Optionally add monitoring metrics for execution times and error result codes. Alert on timeout string patterns or rising timeout counts after dependency or region incidents.\nValidate ingest before alerting:\n```spl\nindex=gcp sourcetype=\"google:gcp:pubsub:message\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=gcp sourcetype=\"google:gcp:pubsub:message\" resource.type=\"cloud_function\"\n| table _time messageId publishTime attributes\n| eval uc_marker=\"UC-4.5.8_gcp_cloud_functions_time_8\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `google:gcp:pubsub:message` in `index=gcp` to detect gcp cloud functions timeout monitoring. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the provider cloud console audit view for the same 24-hour window. Confirm row counts and `messageId` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_5_8_alert` with severity aligned to `high` criticality. Dashboard: Column chart (timeouts by function), Line chart (timeouts over time), Table (function_name, region, count)..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade `Splunk_TA_google-cloudplatform` and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of gcp cloud functions timeout monitoring so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.5.16"
  ],
  "wave": "walk"
}
