{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.3.37",
  "title": "Cloud CDN Cache Performance",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Cloud CDN Cache Performance' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_cloud_cdn_cache_performance` (UC-4.3.37), SOC dashboard panel \"Operations — Cloud CDN Cache Performance\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=gcp` sourcetype='google:gcp:pubsub:message' where `resource.labels.url_map_name` satisfies `hit_ratio` < 60 for UC-4.3.37 ('Cloud CDN Cache Performance'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `gcp` / `google:gcp:pubsub:message` events where `resource.labels.url_map_name` stays below `hit_ratio` < 60, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-4.3.37."
  },
  "dataSources": "HTTP(S) LB logs with cache fill/lookup fields, `sourcetype=google:gcp:monitoring` (cdn metrics) Collected via Splunk Add-on for Google Cloud Platform (Splunkbase 3084) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_google-cloudplatform` — Splunk Add-on for Google Cloud Platform (Splunkbase 3084)",
  "spl": "index=gcp sourcetype=\"google:gcp:pubsub:message\" httpRequest.latency!=\"\"\n| eval cache_hit=if(match(_raw,\"cacheHit|CACHE_HIT\"),1,0)\n| stats sum(cache_hit) as hits, count as total by resource.labels.url_map_name\n| eval hit_ratio=round(100*hits/total,2)\n| where hit_ratio < 60 AND total > 1000\n| sort hit_ratio",
  "description": "Low hit ratio raises origin load and latency; optimizing cache keys and TTL improves cost and user experience. Searches `index=gcp` with sourcetype `google:gcp:pubsub:message` for UC-4.3.37, aggregating `sum` by `resource.labels.url_map_name` over `the scheduled window`. Alerts when grouped results cross `hit_ratio` < 60 in the saved search. Gives Operations operators a repeatable signal for 'Cloud CDN Cache Performance' without manual log review.",
  "value": "Early 'Cloud CDN Cache Performance' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Parse cache hit/miss from load balancer logs. Segment by content type and geography. Alert when hit ratio drops vs 14-day baseline. Review cache mode and Vary headers.",
  "visualization": "Line chart (hit ratio by URL map), Bar chart (origin egress), Table (backend, hit %).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk_TA_google-cloudplatform",
      "url": "https://splunkbase.splunk.com/app/3088",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 3084",
      "url": "https://splunkbase.splunk.com/app/3084",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `gcp`** — Planned work for 'Cloud CDN Cache Performance' shifts `sum` over `span=1h` while `resource.labels.url_map_name` remains in-scope for UC-4.3.37. Cross-check CMDB CHG.\n\n2. **Batch `sum` on `google:gcp:pubsub:message`** — Scheduled automation elevates `cache_hit` without crossing `hit_ratio` < 60. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `resource.labels.url_map_name`** — Health checks in `gcp` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `google:gcp:pubsub:message`** — First 48h baseline for `resource.labels.url_map_name` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `resource.labels.url_map_name`, `exception_reason`, `valid_until`.",
  "equipment": [
    "gcp"
  ],
  "equipmentModels": [
    "gcp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_google-cloudplatform`.\n• Ensure the following data sources are available: HTTP(S) LB logs with cache fill/lookup fields, `sourcetype=google:gcp:monitoring` (cdn metrics).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nParse cache hit/miss from load balancer logs. Segment by content type and geography. Alert when hit ratio drops vs 14-day baseline. Review cache mode and Vary headers.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=gcp sourcetype=\"google:gcp:pubsub:message\" httpRequest.latency!=\"\"\n| eval cache_hit=if(match(_raw,\"cacheHit|CACHE_HIT\"),1,0)\n| stats sum(cache_hit) as hits, count as total by resource.labels.url_map_name\n| eval hit_ratio=round(100*hits/total,2)\n| where hit_ratio < 60 AND total > 1000\n| sort hit_ratio\n```\n\nUnderstanding this SPL\n\n**Cloud CDN Cache Performance** — Low hit ratio raises origin load and latency; optimizing cache keys and TTL improves cost and user experience.\n\nDocumented **Data sources**: HTTP(S) LB logs with cache fill/lookup fields, `sourcetype=google:gcp:monitoring` (cdn metrics). **App/TA** (typical add-on context): `Splunk_TA_google-cloudplatform`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: gcp; **sourcetype**: google:gcp:pubsub:message. If that sourcetype is not mentioned in Data sources, double-check parsing or update the documentation to match the feed you actually ingest.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=gcp, sourcetype=\"google:gcp:pubsub:message\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **cache_hit** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by resource.labels.url_map_name** so each row reflects one combination of those dimensions.\n• `eval` defines or adjusts **hit_ratio** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where hit_ratio < 60 AND total > 1000` — typically the threshold or rule expression for this monitoring goal.\n• Orders rows with `sort` — combine with `head`/`tail` for top-N patterns.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Line chart (hit ratio by URL map), Bar chart (origin egress), Table (backend, hit %).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_google-cloudplatform` — Splunk Add-on for Google Cloud Platform (Splunkbase 3084)). Enable the modular input or HEC token that writes sourcetype=google:gcp:pubsub:message into gcp. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`cache_hit, hit_ratio, resource.labels.url_map_name, total`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "Low hit ratio raises origin load and latency; optimizing cache keys and TTL improves cost and user experience.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
