{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.4.24",
  "title": "Hybrid Connectivity Status",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Hybrid Connectivity Status' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_hybrid_connectivity_status` (UC-4.4.24), SOC dashboard panel \"Operations — Hybrid Connectivity Status\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-4.4.24 positive (Hybrid, Connectivity, Status): ingest synthetic events into `index=aws` sourcetype='aws:cloudwatch' where `healthy` grouped by `resourceId`, `resource.labels.*`, `_time` satisfies `average` > 0, or `value` > 0 for 'Hybrid Connectivity Status'.  Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-4.4.24 negative (Connectivity Status): ingest benign `aws` / `aws:cloudwatch` events where `resourceId`, `resource.labels.*`, `_time` stays below `average` > 0, or `value` > 0, or exclude the test entity via `ops_exception_register.csv`. Variant 8 confirms zero alert rows for 'Hybrid Connectivity Status'."
  },
  "dataSources": "`sourcetype=aws:cloudwatch` (DX, VPN, TGW), Azure Monitor VPN/ExpressRoute metrics, `sourcetype=google:gcp:monitoring` (VPN/interconnect) Collected via Splunk Add-on for Amazon Web Services (Splunkbase 1876) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_aws`, `Splunk_TA_microsoft-cloudservices`, `Splunk_TA_google-cloudplatform` — Splunk Add-on for Amazon Web Services (Splunkbase 1876)",
  "spl": "(index=aws sourcetype=\"aws:cloudwatch\" (namespace=\"AWS/DX\" OR namespace=\"AWS/VPN\") (metric_name=\"ConnectionState\" OR metric_name=\"TunnelState\"))\n OR (index=azure sourcetype=\"mscs:azure:metrics\" resourceType=\"Microsoft.Network/expressRouteCircuits\" metric_name=\"BgpPeerStatus\")\n OR (index=gcp sourcetype=\"google:gcp:monitoring\" metric.type=\"vpn.googleapis.com/tunnel_established\")\n| eval link_up=case(metric_name=\"ConnectionState\" AND maximum=1,1, metric_name=\"TunnelState\" AND maximum=1,1, metric_name=\"BgpPeerStatus\" AND average>0,1, metric.type=\"vpn.googleapis.com/tunnel_established\" AND value>0,1,1=1,0)\n| bin _time span=5m\n| stats min(link_up) as healthy by resourceId, resource.labels.*, _time\n| where healthy=0",
  "description": "Monitors UC-4.4.24 — Hybrid, Connectivity, Status — using `index=aws` sourcetype `aws:cloudwatch`, grouped by `resourceId`, `resource.labels.*`, `_time`. Outputs `healthy` each `5m`; alert when `average` > 0, or `value` > 0. Primary focus: Connectivity Status. Purpose-built for 'Hybrid Connectivity Status' in Operations workflows.",
  "value": "UC-4.4.24 accelerates insight into Connectivity Status ('Hybrid Connectivity Status') by tracking `healthy` on `aws:cloudwatch` feeds, reducing operational risk and delayed incident response before spreadsheet checks hide regressions.",
  "implementation": "Align metric semantics per provider in lookups; alert on sustained unhealthy state. Correlate with provider status pages ingested as `sourcetype=cloud:status`. Dashboard RTO/RPO targets for hybrid links.",
  "visualization": "Timeline (link state), Table (circuit, tunnel, status), Map (peering location if geo fields exist).",
  "cimModels": [
    "Performance"
  ],
  "cimSpl": "| tstats `summariesonly` avg(Performance.cpu_load_percent) as agg_value\n  from datamodel=Performance where nodename=Performance.CPU\n  by Performance.host span=5m\n| sort - agg_value",
  "dataModelAcceleration": "Enable data model acceleration for Performance (CPU node).",
  "references": [
    {
      "title": "Splunk_TA_aws",
      "url": "https://splunkbase.splunk.com/app/1876",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk_TA_microsoft-cloudservices",
      "url": "https://splunkbase.splunk.com/app/3110",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk_TA_google-cloudplatform",
      "url": "https://splunkbase.splunk.com/app/3088",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Performance",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Performance",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `aws` (UC-4.4.24)** — Planned work for 'Hybrid Connectivity Status' shifts `healthy` over `span=5m` while `resourceId`, `resource.labels.*`, `_time` remains in-scope.  Cross-check CMDB CHG.\n\n2. **Batch `min` on `aws:cloudwatch`** — Scheduled automation elevates `resource.labels.*` without crossing `average` > 0 for UC-4.4.24. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `resourceId`, `resource.labels.*`, `_time`** — Health checks in `aws` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `aws:cloudwatch`** — First 48h baseline for UC-4.4.24 on `resourceId` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `resourceId`, `exception_reason`, `valid_until`.",
  "equipment": [
    "aws",
    "azure",
    "gcp",
    "m365"
  ],
  "equipmentModels": [
    "aws_generic",
    "azure_generic",
    "gcp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_aws`, `Splunk_TA_microsoft-cloudservices`, `Splunk_TA_google-cloudplatform`.\n• Ensure the following data sources are available: `sourcetype=aws:cloudwatch` (DX, VPN, TGW), Azure Monitor VPN/ExpressRoute metrics, `sourcetype=google:gcp:monitoring` (VPN/interconnect).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nAlign metric semantics per provider in lookups; alert on sustained unhealthy state. Correlate with provider status pages ingested as `sourcetype=cloud:status`. Dashboard RTO/RPO targets for hybrid links.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\n(index=aws sourcetype=\"aws:cloudwatch\" (namespace=\"AWS/DX\" OR namespace=\"AWS/VPN\") (metric_name=\"ConnectionState\" OR metric_name=\"TunnelState\"))\n OR (index=azure sourcetype=\"mscs:azure:metrics\" resourceType=\"Microsoft.Network/expressRouteCircuits\" metric_name=\"BgpPeerStatus\")\n OR (index=gcp sourcetype=\"google:gcp:monitoring\" metric.type=\"vpn.googleapis.com/tunnel_established\")\n| eval link_up=case(metric_name=\"ConnectionState\" AND maximum=1,1, metric_name=\"TunnelState\" AND maximum=1,1, metric_name=\"BgpPeerStatus\" AND average>0,1, metric.type=\"vpn.googleapis.com/tunnel_established\" AND value>0,1,1=1,0)\n| stats min(link_up) as healthy by resourceId, resource.labels.*, bin(_time, 5m)\n| where healthy=0\n```\n\nUnderstanding this SPL\n\n**Hybrid Connectivity Status** — ExpressRoute, Direct Connect, VPN, and Interconnect carry production traffic; tunnel or BGP drops partition workloads between on-prem and cloud.\n\nDocumented **Data sources**: `sourcetype=aws:cloudwatch` (DX, VPN, TGW), Azure Monitor VPN/ExpressRoute metrics, `sourcetype=google:gcp:monitoring` (VPN/interconnect). **App/TA** (typical add-on context): `Splunk_TA_aws`, `Splunk_TA_microsoft-cloudservices`, `Splunk_TA_google-cloudplatform`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: aws, azure, gcp; **sourcetype**: aws:cloudwatch, mscs:azure:metrics, Microsoft.Network/expressRouteCircuits, google:gcp:monitoring. Those sourcetypes align with what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=aws, index=azure, index=gcp, sourcetype=\"aws:cloudwatch\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **link_up** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `stats` rolls up events into metrics; results are split **by resourceId, resource.labels.*, bin(_time, 5m)** so each row reflects one combination of those dimensions.\n• Filters the current rows with `where healthy=0` — typically the threshold or rule expression for this monitoring goal.\n\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` avg(Performance.cpu_load_percent) as agg_value\n  from datamodel=Performance where nodename=Performance.CPU\n  by Performance.host span=5m\n| sort - agg_value\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Hybrid Connectivity Status** — ExpressRoute, Direct Connect, VPN, and Interconnect carry production traffic; tunnel or BGP drops partition workloads between on-prem and cloud.\n\nIf you map cloud vendor fields into the CIM, this variant uses normalized names and `tstats` on accelerated models. The raw vendor search in Step 2 is still the first stop for troubleshooting.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` on accelerated data model the CPU-related Performance model — enable that model in Data Models and CIM add-ons, or the search may return no rows.\n\n• Uses `sort` to rank results; add `head` to limit the table.\n\nEnable Data Model Acceleration (and the right field aliases) for the models or datasets above; otherwise `tstats` may not find summaries.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` avg(Performance.cpu_load_percent) as agg_value\n  from datamodel=Performance where nodename=Performance.CPU\n  by Performance.host span=5m\n| sort - agg_value\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Hybrid Connectivity Status** — ExpressRoute, Direct Connect, VPN, and Interconnect carry production traffic; tunnel or BGP drops partition workloads between on-prem and cloud.\n\nIf you map cloud vendor fields into the CIM, this variant uses normalized names and `tstats` on accelerated models. The raw vendor search in Step 2 is still the first stop for troubleshooting.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` on the `Performance` data model (CPU child datasets)—enable that model in Data Models and the CIM add-on, or the search may return no rows.\n\n• Uses `sort` to rank results; add `head` to limit the table.\n\nEnable Data Model Acceleration (and the right field aliases) for the models or datasets above; otherwise `tstats` may not find summaries.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` avg(Performance.cpu_load_percent) as agg_value\n  from datamodel=Performance where nodename=Performance.CPU\n  by Performance.host span=5m\n| sort - agg_value\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Hybrid Connectivity Status** — ExpressRoute, Direct Connect, VPN, and Interconnect carry production traffic; tunnel or BGP drops partition workloads between on-prem and cloud.\n\nIf you map cloud vendor fields into the CIM, this variant uses normalized names and `tstats` on accelerated models. The raw vendor search in Step 2 is still the first stop for troubleshooting.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` on the `Performance` data model (CPU child datasets)—enable that model in Data Models and the CIM add-on, or the search may return no rows.\n\n• Uses `sort` to rank results; add `head` to limit the table.\n\nEnable Data Model Acceleration (and the right field aliases) for the models or datasets above; otherwise `tstats` may not find summaries.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` avg(Performance.cpu_load_percent) as agg_value\n  from datamodel=Performance where nodename=Performance.CPU\n  by Performance.host span=5m\n| sort - agg_value\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Hybrid Connectivity Status** — ExpressRoute, Direct Connect, VPN, and Interconnect carry production traffic; tunnel or BGP drops partition workloads between on-prem and cloud.\n\nIf you map cloud vendor fields into the CIM, this variant uses normalized names and `tstats` on accelerated models. The raw vendor search in Step 2 is still the first stop for troubleshooting.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` on the `Performance` data model (CPU child datasets)—enable that model in Data Models and the CIM add-on, or the search may return no rows.\n\n• Uses `sort` to rank results; add `head` to limit the table.\n\nEnable Data Model Acceleration (and the right field aliases) for the models or datasets above; otherwise `tstats` may not find summaries.\n\nOptional CIM / accelerated variant (same use case, normalized fields via Common Information Model):\n\n```spl\n| tstats `summariesonly` avg(Performance.cpu_load_percent) as agg_value\n  from datamodel=Performance where nodename=Performance.CPU\n  by Performance.host span=5m\n| sort - agg_value\n```\n\nUnderstanding this CIM / accelerated SPL\n\n**Hybrid Connectivity Status** — ExpressRoute, Direct Connect, VPN, and Interconnect carry production traffic; tunnel or BGP drops partition workloads between on-prem and cloud.\n\nIf you map cloud vendor fields into the CIM, this variant uses normalized names and `tstats` on accelerated models. The raw vendor search in Step 2 is still the first stop for troubleshooting.\n\n**Pipeline walkthrough**\n\n• Uses `tstats` on the `Performance` data model (CPU child datasets)—enable that model in Data Models and the CIM add-on, or the search may return no rows.\n\n• Uses `sort` to rank results; add `head` to limit the table.\n\nEnable Data Model Acceleration (and the right field aliases) for the models or datasets above; otherwise `tstats` may not find summaries.\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Timeline (link state), Table (circuit, tunnel, status), Map (peering location if geo fields exist).",
  "grandmaExplanation": "ExpressRoute, Direct Connect, remote access, and Interconnect carry production traffic; tunnel or drops partition workloads between on-prem and cloud.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
