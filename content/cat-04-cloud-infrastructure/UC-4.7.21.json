{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.7.21",
  "title": "OCI Load Balancer Unhealthy Backend Monitoring",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Operations"
  ],
  "splunkPillar": "Security",
  "compliance": [
    {
      "regulation": "NIST-800-53",
      "version": "Rev.5",
      "clause": "AU-2",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Saved search evidence for AU-2 when alert outputs are archived."
    }
  ],
  "controlTest": {
    "positiveScenario": "Replay or inject sample `oci:log` events matching `*` into `index=oci`. Confirm UC-4.7.21 search returns >=1 row and alert fires.",
    "negativeScenario": "Replay benign `oci:log` traffic without `*` markers. Confirm the search returns zero rows and alert does not fire."
  },
  "dataSources": "`index=oci` `sourcetype=oci:log`",
  "app": "Splunk Add-on for Oracle Cloud Infrastructure",
  "spl": "index=oci sourcetype=\"oci:log\"\n| table _time action user resource\n| sort -_time",
  "description": "Monitors OCI load balancer unhealthy backend telemetry.",
  "value": "Load Balancer Unhealthy Backend anomalies affect OCI workload availability and security.",
  "implementation": "Configure required cloud inputs per provider guide; index=oci sourcetype=oci:log.",
  "visualization": "Dashboard panel, alert, and weekly compliance report.",
  "cimModels": [],
  "references": [
    {
      "title": "Splunk Add-on for Oracle Cloud Infrastructure",
      "url": "https://splunkbase.splunk.com/app/5680"
    },
    {
      "title": "Vendor documentation",
      "url": "https://docs.oracle.com/en-us/iaas/"
    }
  ],
  "knownFalsePositives": "Approved change windows; documented automation service principals; sandbox accounts excluded via lookup.",
  "mitreAttack": [
    "T1078.004"
  ],
  "securityDomain": "cloud",
  "equipment": [
    "oci",
    "oracle"
  ],
  "equipmentModels": [
    "oracle_oracle_db"
  ],
  "status": "community",
  "cost": {
    "tier": "medium",
    "search_load": "moderate",
    "tstats_eligible": false
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with permission to search `index=oci`.\n• Install and configure: Splunk Add-on for Oracle Cloud Infrastructure.\n• Ensure `oci:log` events are flowing (prerequisite UC: 4.7.1 when applicable).\n• Map `oci` and field extractions per `docs/guides/aws.md`, `docs/guides/azure.md`, or `docs/guides/gcp.md` as appropriate.\n\nStep 1 — Configure data collection\nConfigure required cloud inputs per provider guide; index=oci sourcetype=oci:log.\nExpected sourcetype: `oci:log`. Validate with:\n```spl\nindex=oci sourcetype=\"oci:log\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=oci sourcetype=\"oci:log\"\n| table _time action user resource\n| sort -_time\n```\n\nUnderstanding this SPL\nThe search scopes `oci:log` in `index=oci` and applies `*` to surface `OCI Load Balancer Unhealthy Backend Monitoring`. Tune time range and alert threshold to your change cadence; exclude break-glass principals via a lookup.\n\nStep 3 — Validate\nCompare result counts to the vendor console (CloudTrail Event history, Azure Activity Log, GCP Logs Explorer, or service-native UI). Spot-check `_time` and actor fields. Confirm CIM acceleration only after TA field mappings are verified.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_7_21_alert` with appropriate severity. Dashboard: Dashboard panel, alert, and weekly compliance report..\nFailure modes: (1) empty results — verify TA input and index routing; (2) duplicate events — check S3/SQS duplication; (3) missing fields — upgrade `Splunk Add-on for Oracle Cloud Infrastructure` and review `props.conf` aliases; (4) alert fatigue — add CMDB/allow-list exclusions documented in knownFalsePositives.\n",
  "grandmaExplanation": "We watch for oci load balancer unhealthy backend monitoring in your cloud audit logs so the team catches important changes early instead of finding out from auditors or customers.",
  "prerequisiteUseCases": [
    "UC-4.7.1"
  ],
  "splunkbaseApps": [
    {
      "id": 5680,
      "name": "Splunk Add-on for Oracle Cloud Infrastructure",
      "role": "primary",
      "requiresSmeReview": true
    }
  ],
  "wave": "walk"
}
