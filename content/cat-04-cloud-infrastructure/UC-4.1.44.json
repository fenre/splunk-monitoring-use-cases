{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.1.44",
  "title": "Inspector Vulnerability and Finding Trends",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security"
  ],
  "splunkPillar": "Security",
  "controlTest": {
    "positiveScenario": "Inject sample events matching UC-4.1.44 SPL; confirm the search returns rows and alert fires.",
    "negativeScenario": "Replay benign traffic without matching markers; confirm UC-4.1.44 search returns zero rows."
  },
  "dataSources": "`index=aws` sourcetype=`aws:inspector` collected via `Splunk_TA_aws`; validate ingest with `| stats count by sourcetype`.",
  "app": "`Splunk_TA_aws`",
  "spl": "index=aws sourcetype=\"aws:inspector\" severity=\"CRITICAL\" OR severity=\"HIGH\"\n| table _time userIdentity.arn eventName sourceIPAddress\n| eval uc_marker=\"UC-4.1.44_inspector_vulnerability_44\"\n| sort -_time",
  "description": "Inspector findings (EC2, ECR, Lambda) identify vulnerabilities. Tracking trends and new critical findings supports patch and image hygiene.",
  "value": "Detecting inspector vulnerability and finding trends early reduces mean time to contain cloud incidents, supports audit logging controls, and prevents silent misconfiguration drift across accounts.",
  "implementation": "Configure Inspector to send findings to EventBridge or SNS; ingest in Splunk. Alert on new CRITICAL findings. Dashboard open findings by severity and age. Correlate with patch compliance (SSM).",
  "visualization": "Table (severity, type, count), Bar chart by severity, Trend line (findings over time).",
  "cimModels": [
    "Intrusion_Detection"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Intrusion_Detection.IDS_Attacks\n  where match(lower(IDS_Attacks.severity), \"high|critical\")\n  by IDS_Attacks.signature IDS_Attacks.src span=1d\n| sort -count",
  "dataModelAcceleration": "Intrusion_Detection: map Inspector/ECR scan fields into CIM IDS-style objects when available. Enable Data Model Acceleration for the CIM data models in `cimModels` once `Splunk_TA_aws` (and any CIM tags/aliases) map your fields; otherwise the `cimSpl` summary search may be empty. Step 2 on raw or metric indexes stays primary for most sites.",
  "references": [
    {
      "title": "Splunk_TA_aws",
      "url": "https://splunkbase.splunk.com/app/1876"
    },
    {
      "title": "Vendor cloud documentation",
      "url": "https://docs.aws.amazon.com/"
    }
  ],
  "knownFalsePositives": "Accepted risks for legacy apps; images you already planned to replace; test registries in lower accounts.",
  "mitreAttack": [
    "T1526",
    "T1613"
  ],
  "securityDomain": "cloud",
  "equipment": [
    "aws"
  ],
  "equipmentModels": [
    "aws_inspector"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=aws`.\n• Install and configure `Splunk_TA_aws` with the modular input for `aws:inspector`.\n• Prerequisite crawl UC(s): UC-4.1.78.\n• Confirm field extractions in `props.conf` for `userIdentity.arn` and related actor fields.\n\nStep 1 — Configure data collection\nConfigure Inspector to send findings to EventBridge or SNS; ingest in Splunk. Alert on new CRITICAL findings. Dashboard open findings by severity and age. Correlate with patch compliance (SSM).\nValidate ingest before alerting:\n```spl\nindex=aws sourcetype=\"aws:inspector\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=aws sourcetype=\"aws:inspector\" severity=\"CRITICAL\" OR severity=\"HIGH\"\n| table _time userIdentity.arn eventName sourceIPAddress\n| eval uc_marker=\"UC-4.1.44_inspector_vulnerability_44\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `aws:inspector` in `index=aws` to detect inspector vulnerability and finding trends. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the provider cloud console audit view for the same 24-hour window. Confirm row counts and `userIdentity.arn` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_1_44_alert` with severity aligned to `high` criticality. Dashboard: Table (severity, type, count), Bar chart by severity, Trend line (findings over time)..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade `Splunk_TA_aws` and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of inspector vulnerability and finding trends so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.1.78"
  ],
  "splunkbaseApps": [
    {
      "id": 1876,
      "name": "Splunk Add-on for AWS",
      "role": "primary",
      "requiresSmeReview": true
    }
  ],
  "wave": "walk"
}
