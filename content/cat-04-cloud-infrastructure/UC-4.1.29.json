{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.1.29",
  "title": "EC2 Spot Instance Interruption Notices",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Availability"
  ],
  "splunkPillar": "Security",
  "controlTest": {
    "positiveScenario": "Replay sample `aws:cloudwatch:events` events with `detail-type=\"EC2 Spot Instance Interruption Warning\"` into `index=aws`. Confirm UC-4.1.29 returns >=1 row and the alert fires.",
    "negativeScenario": "Replay benign `aws:cloudwatch:events` traffic without `detail-type=\"EC2 Spot Instance Interruption Warning\"`. Confirm UC-4.1.29 returns zero rows and the alert does not fire."
  },
  "dataSources": "`index=aws` sourcetype=`aws:cloudwatch:events` collected via `Splunk_TA_aws`; validate ingest with `| stats count by sourcetype`.",
  "app": "`Splunk_TA_aws`",
  "spl": "index=aws sourcetype=\"aws:cloudwatch:events\" detail-type=\"EC2 Spot Instance Interruption Warning\"\n| table _time detail-type source detail.eventName\n| eval uc_marker=\"UC-4.1.29_ec2_spot_instance_interr_29\"\n| sort -_time",
  "description": "UC-4.1.29 — EC2 Spot Instance Interruption Notices: queries AWS `aws:cloudwatch:events` for search criteria `detail-type=\"EC2 Spot Instance Interruption Warning\"` and returns actor, resource, and region context for availability review and change validation.",
  "value": "On AWS, detecting 'EC2 Spot Instance Interruption Notices' early shortens cloud incident response and produces defensible audit evidence for identity and change-management controls. UC-4.1.29 drives high-priority availability alerting before customer-impacting drift.",
  "implementation": "Create EventBridge rule for EC2 Spot Instance Interruption Warning. Forward to SNS or Lambda for Splunk ingestion. Alert on every interruption; use for fleet metrics and hybrid/on-demand fallback decisions.",
  "visualization": "Table (instance, action, time), Timeline (interruptions by AZ), Bar chart (interruptions by instance type).",
  "cimModels": [
    "Change"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Change.All_Changes\n  where match(All_Changes.object, \"(?i)spot|instance\") OR match(All_Changes.app, \"(?i)ec2\")\n  by All_Changes.user All_Changes.action span=1h\n| sort -count",
  "dataModelAcceleration": "Change: for `aws:cloudwatch:events` / EventBridge control messages mapped to CIM. Enable Data Model Acceleration for the CIM data models in `cimModels` once `Splunk_TA_aws` (and any CIM tags/aliases) map your fields; otherwise the `cimSpl` summary search may be empty. Step 2 on raw or metric indexes stays primary for most sites.",
  "references": [
    {
      "title": "Splunk_TA_aws",
      "url": "https://splunkbase.splunk.com/app/1876"
    },
    {
      "title": "Vendor cloud documentation",
      "url": "https://docs.aws.amazon.com/"
    }
  ],
  "knownFalsePositives": "Intentional spot reclaim during game-day, batch jobs, and capacity tests where interruption is the design.",
  "securityDomain": "cloud",
  "equipment": [
    "aws"
  ],
  "equipmentModels": [
    "aws_cloudwatch"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=aws`.\n• Install and configure `Splunk_TA_aws` with the modular input for `aws:cloudwatch:events`.\n• Prerequisite crawl UC(s): UC-4.1.78.\n• Confirm field extractions in `props.conf` for `detail-type` and related actor fields.\n\nStep 1 — Configure data collection\nCreate EventBridge rule for EC2 Spot Instance Interruption Warning. Forward to SNS or Lambda for Splunk ingestion. Alert on every interruption; use for fleet metrics and hybrid/on-demand fallback decisions.\nValidate ingest before alerting:\n```spl\nindex=aws sourcetype=\"aws:cloudwatch:events\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=aws sourcetype=\"aws:cloudwatch:events\" detail-type=\"EC2 Spot Instance Interruption Warning\"\n| table _time detail-type source detail.eventName\n| eval uc_marker=\"UC-4.1.29_ec2_spot_instance_interr_29\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `aws:cloudwatch:events` in `index=aws` to detect ec2 spot instance interruption notices. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the provider cloud console audit view for the same 24-hour window. Confirm row counts and `detail-type` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_1_29_alert` with severity aligned to `high` criticality. Dashboard: Table (instance, action, time), Timeline (interruptions by AZ), Bar chart (interruptions by instance type)..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade `Splunk_TA_aws` and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of ec2 spot instance interruption notices so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.1.78"
  ],
  "splunkbaseApps": [
    {
      "id": 1876,
      "name": "Splunk Add-on for AWS",
      "role": "primary",
      "requiresSmeReview": true
    }
  ],
  "wave": "walk"
}
