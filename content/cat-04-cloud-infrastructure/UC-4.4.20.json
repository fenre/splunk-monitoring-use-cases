{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.4.20",
  "title": "Multi-Cloud DNS Resolution Latency",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "controlTest": {
    "positiveScenario": "Replay sample `dns:resolution` events with `matching activity` into `index=cloud`. Confirm UC-4.4.20 returns >=1 row and the alert fires.",
    "negativeScenario": "Replay benign `dns:resolution` traffic without `matching activity`. Confirm UC-4.4.20 returns zero rows and the alert does not fire."
  },
  "dataSources": "`index=cloud` sourcetype=`dns:resolution` collected via Custom scripted input (dig, nslookup); validate ingest with `| stats count by sourcetype`.",
  "app": "Custom scripted input (dig, nslookup)",
  "spl": "index=cloud sourcetype=\"dns:resolution\"\n| table _time userIdentity.arn eventName sourceIPAddress\n| eval uc_marker=\"UC-4.4.20_multi_cloud_dns_resoluti_20\"\n| sort -_time",
  "description": "UC-4.4.20 — Multi-Cloud DNS Resolution Latency: queries Microsoft Azure `dns:resolution` for configured audit predicates and returns actor, resource, and region context for performance review and change validation.",
  "value": "On Microsoft Azure, detecting 'Multi-Cloud DNS Resolution Latency' early shortens cloud incident response and produces defensible audit evidence for identity and change-management controls. UC-4.4.20 drives medium-priority performance alerting before customer-impacting drift.",
  "implementation": "Run periodic DNS probes (dig, nslookup, or custom script) from Lambda, Azure Functions, Cloud Functions, or on-prem agents. Measure resolution time per domain. Ingest results via HEC with fields: provider, vantage_point, domain, resolution_ms, success. Alert when avg latency exceeds 200ms or failure rate > 5%. Compare providers for DNS migration decisions.",
  "visualization": "Line chart (resolution latency by provider and domain over time), Table (provider, domain, avg ms), Heat map (provider vs domain).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/"
    },
    {
      "title": "Vendor cloud documentation",
      "url": "https://docs.splunk.com/Documentation/CIM"
    }
  ],
  "knownFalsePositives": "This pattern can surge during change windows, in sandboxes, and when a platform is in maintenance, so we line alerts up with the calendar and take a second look before we call it an incident.",
  "securityDomain": "cloud",
  "equipment": [
    "azure"
  ],
  "equipmentModels": [
    "azure_resolution"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=cloud`.\n• Install and configure Custom scripted input (dig, nslookup) with the modular input for `dns:resolution`.\n• Prerequisite crawl UC(s): UC-4.4.33.\n• Confirm field extractions in `props.conf` for `userIdentity.arn` and related actor fields.\n\nStep 1 — Configure data collection\nRun periodic DNS probes (dig, nslookup, or custom script) from Lambda, Azure Functions, Cloud Functions, or on-prem agents. Measure resolution time per domain. Ingest results via HEC with fields: provider, vantage_point, domain, resolution_ms, success. Alert when avg latency exceeds 200ms or failure rate > 5%. Compare providers for DNS migration decisions.\nValidate ingest before alerting:\n```spl\nindex=cloud sourcetype=\"dns:resolution\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=cloud sourcetype=\"dns:resolution\"\n| table _time userIdentity.arn eventName sourceIPAddress\n| eval uc_marker=\"UC-4.4.20_multi_cloud_dns_resoluti_20\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `dns:resolution` in `index=cloud` to detect multi-cloud dns resolution latency. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the provider cloud console audit view for the same 24-hour window. Confirm row counts and `userIdentity.arn` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_4_20_alert` with severity aligned to `medium` criticality. Dashboard: Line chart (resolution latency by provider and domain over time), Table (provider, domain, avg ms), Heat map (provider vs domain)..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade Custom scripted input (dig, nslookup) and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of multi-cloud dns resolution latency so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.4.33"
  ],
  "wave": "walk"
}
