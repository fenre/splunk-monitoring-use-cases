{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.3.157",
  "title": "GCP Vertex model upload",
  "criticality": "high",
  "difficulty": "beginner",
  "monitoringType": [
    "Security",
    "Audit"
  ],
  "splunkPillar": "Security",
  "compliance": [
    {
      "regulation": "NIST-800-53",
      "version": "Rev.5",
      "clause": "AU-2",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Saved search evidence for AU-2 when alert outputs are archived."
    }
  ],
  "controlTest": {
    "positiveScenario": "Replay or inject sample `google:gcp:audit` events matching `(protoPayload.methodName=\"*aiplatform.models.upload*\" OR protoPayload.methodName=\"aiplatform.models.upload\")` into `index=gcp`. Confirm UC-4.3.157 search returns >=1 row and alert fires.",
    "negativeScenario": "Replay benign `google:gcp:audit` traffic without `(protoPayload.methodName=\"*aiplatform.models.upload*\" OR protoPayload.methodName=\"aiplatform.models.upload\")` markers. Confirm the search returns zero rows and alert does not fire."
  },
  "dataSources": "`index=gcp` `sourcetype=google:gcp:audit`",
  "app": "Splunk Add-on for Google Cloud Platform (`Splunk_TA_google-cloudplatform`, Splunkbase 3088)",
  "spl": "index=gcp sourcetype=\"google:gcp:audit\" (protoPayload.methodName=\"*aiplatform.models.upload*\" OR protoPayload.methodName=\"aiplatform.models.upload\")\n| table _time protoPayload.methodName protoPayload.authenticationInfo.principalEmail resource.labels.project_id\n| sort -_time",
  "description": "Detects GCP audit method aiplatform.models.upload.",
  "value": "Model uploads affect ML supply chain.",
  "implementation": "Export Cloud Audit Logs to Pub/Sub; configure `Splunk_TA_google-cloudplatform` Pub/Sub input into `index=gcp`. Validate protoPayload.methodName extractions.",
  "visualization": "Events list, methodName timeline, principalEmail table.",
  "cimModels": [
    "Change"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Change.All_Changes\n  where All_Changes.action=*\n  by All_Changes.user All_Changes.action All_Changes.object span=1h\n| sort -count",
  "dataModelAcceleration": "Enable acceleration for Change after TA field mappings are verified.",
  "references": [
    {
      "title": "Splunk Add-on for Google Cloud Platform",
      "url": "https://splunkbase.splunk.com/app/3088"
    },
    {
      "title": "Cloud Audit Logs overview",
      "url": "https://cloud.google.com/logging/docs/audit"
    }
  ],
  "knownFalsePositives": "Approved change windows; documented automation service principals; sandbox accounts excluded via lookup.",
  "mitreAttack": [
    "T1078.004"
  ],
  "securityDomain": "cloud",
  "equipment": [
    "gcp"
  ],
  "equipmentModels": [
    "gcp_audit_log"
  ],
  "status": "community",
  "cost": {
    "tier": "medium",
    "search_load": "moderate",
    "tstats_eligible": true
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with permission to search `index=gcp`.\n• Install and configure: Splunk Add-on for Google Cloud Platform (`Splunk_TA_google-cloudplatform`, Splunkbase 3088).\n• Ensure `google:gcp:audit` events are flowing (prerequisite UC: 4.3.41 when applicable).\n• Map `gcp` and field extractions per `docs/guides/aws.md`, `docs/guides/azure.md`, or `docs/guides/gcp.md` as appropriate.\n\nStep 1 — Configure data collection\nExport Cloud Audit Logs to Pub/Sub; configure `Splunk_TA_google-cloudplatform` Pub/Sub input into `index=gcp`. Validate protoPayload.methodName extractions.\nExpected sourcetype: `google:gcp:audit`. Validate with:\n```spl\nindex=gcp sourcetype=\"google:gcp:audit\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=gcp sourcetype=\"google:gcp:audit\" (protoPayload.methodName=\"*aiplatform.models.upload*\" OR protoPayload.methodName=\"aiplatform.models.upload\")\n| table _time protoPayload.methodName protoPayload.authenticationInfo.principalEmail resource.labels.project_id\n| sort -_time\n```\n\nUnderstanding this SPL\nThe search scopes `google:gcp:audit` in `index=gcp` and applies `(protoPayload.methodName=\"*aiplatform.models.upload*\" OR protoPayload.methodName=\"aiplatform.models.upload\")` to surface `GCP Vertex model upload`. Tune time range and alert threshold to your change cadence; exclude break-glass principals via a lookup.\n\nStep 3 — Validate\nCompare result counts to the vendor console (CloudTrail Event history, Azure Activity Log, GCP Logs Explorer, or service-native UI). Spot-check `_time` and actor fields. Confirm CIM acceleration only after TA field mappings are verified.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_3_157_alert` with appropriate severity. Dashboard: Events list, methodName timeline, principalEmail table..\nFailure modes: (1) empty results — verify TA input and index routing; (2) duplicate events — check S3/SQS duplication; (3) missing fields — upgrade `Splunk Add-on for Google Cloud Platform (`Splunk_TA_google-cloudplatform`, Splunkbase 3088)` and review `props.conf` aliases; (4) alert fatigue — add CMDB/allow-list exclusions documented in knownFalsePositives.\n",
  "grandmaExplanation": "We watch for gcp vertex model upload in your cloud audit logs so the team catches important changes early instead of finding out from auditors or customers.",
  "prerequisiteUseCases": [
    "UC-4.3.41"
  ],
  "splunkbaseApps": [
    {
      "id": 3088,
      "name": "Splunk Add-on for Google Cloud Platform",
      "role": "primary",
      "requiresSmeReview": true
    }
  ],
  "wave": "walk"
}
