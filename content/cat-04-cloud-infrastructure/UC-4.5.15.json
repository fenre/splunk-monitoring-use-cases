{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.5.15",
  "title": "GCP Cloud Functions Retry and Error Rate Trending",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'GCP Cloud Functions Retry and Error Rate Trending' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_gcp_cloud_functions_retry_and_error_rate_trendin` (UC-4.5.15), SOC dashboard panel \"Operations — GCP Cloud Functions Retry and Error Rate Trending\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "Ingest synthetic events into `index=gcp` sourcetype='google:gcp:pubsub:message' where `fn` satisfies `invocations` > 0 for UC-4.5.15 ('GCP Cloud Functions Retry and Error Rate Trending'). Run the saved search and confirm at least one alertable row with expected fields populated.",
    "negativeScenario": "Ingest benign `gcp` / `google:gcp:pubsub:message` events where `fn` stays below `invocations` > 0, or exclude the test entity via `ops_exception_register.csv` with a valid window. Confirm zero alert rows for UC-4.5.15."
  },
  "dataSources": "`sourcetype=google:gcp:pubsub:message` (Cloud Logging), optional `sourcetype=google:gcp:monitoring` (execution metrics) Collected via Splunk Add-on for Google Cloud Platform (Splunkbase 3084) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_google-cloudplatform` — Splunk Add-on for Google Cloud Platform (Splunkbase 3084)",
  "spl": "index=gcp sourcetype=\"google:gcp:pubsub:message\" resource.type=\"cloud_function\"\n| eval fn=resource.labels.function_name\n| eval is_err=if(severity=\"ERROR\", 1, 0)\n| timechart span=1h sum(is_err) as errors, count as invocations by fn\n| eval err_rate=if(invocations>0, round(100*errors/invocations, 2), 0)\n| where err_rate > 5",
  "description": "Rising retries and error rates signal unstable dependencies or quota issues before quotas hard-stop traffic; trending supports SLO review and incident prevention.",
  "value": "Early 'GCP Cloud Functions Retry and Error Rate Trending' detection reduces operational risk and delayed incident response by giving Operations operators a Splunk-native signal before manual triage or audit channels surface the issue.",
  "implementation": "Ingest execution count metrics with result/status labels from Cloud Monitoring. Supplement with log-based counts from Cloud Logging for detailed error classes. Baseline hourly error and retry rates per function. Alert when error share exceeds threshold or retries spike versus invocations.",
  "visualization": "Stacked area chart (executions by outcome), Line chart (error rate % over time), Table (function_name, invocations, errors, retry estimate).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk_TA_google-cloudplatform",
      "url": "https://splunkbase.splunk.com/app/3088",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunkbase app 3084",
      "url": "https://splunkbase.splunk.com/app/3084",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `gcp`** — Planned work for 'GCP Cloud Functions Retry and Error Rate Trending' shifts `sum` over `span=1h` while `fn` remains in-scope for UC-4.5.15. Cross-check CMDB CHG.\n\n2. **Batch `sum` on `google:gcp:pubsub:message`** — Scheduled automation elevates `fn` without crossing `invocations` > 0. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `fn`** — Health checks in `gcp` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `google:gcp:pubsub:message`** — First 48h baseline for `fn` is unstable; window via `ops_exception_register.csv`.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `fn`, `exception_reason`, `valid_until`.",
  "equipment": [
    "gcp"
  ],
  "equipmentModels": [
    "gcp_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_google-cloudplatform`.\n• Ensure the following data sources are available: `sourcetype=google:gcp:pubsub:message` (Cloud Logging), optional `sourcetype=google:gcp:monitoring` (execution metrics).\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nIngest execution count metrics with result/status labels from Cloud Monitoring. Supplement with log-based counts from Cloud Logging for detailed error classes. Baseline hourly error and retry rates per function. Alert when error share exceeds threshold or retries spike versus invocations.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=gcp sourcetype=\"google:gcp:pubsub:message\" resource.type=\"cloud_function\"\n| eval fn=resource.labels.function_name\n| eval is_err=if(severity=\"ERROR\", 1, 0)\n| timechart span=1h sum(is_err) as errors, count as invocations by fn\n| eval err_rate=if(invocations>0, round(100*errors/invocations, 2), 0)\n| where err_rate > 5\n```\n\nUnderstanding this SPL\n\n**GCP Cloud Functions Retry and Error Rate Trending** — Rising retries and error rates signal unstable dependencies or quota issues before quotas hard-stop traffic; trending supports SLO review and incident prevention.\n\nDocumented **Data sources**: `sourcetype=google:gcp:pubsub:message` (Cloud Logging), optional `sourcetype=google:gcp:monitoring` (execution metrics). **App/TA** (typical add-on context): `Splunk_TA_google-cloudplatform`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: gcp; **sourcetype**: google:gcp:pubsub:message. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=gcp, sourcetype=\"google:gcp:pubsub:message\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• `eval` defines or adjusts **fn** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `eval` defines or adjusts **is_err** — often to normalize units, derive a ratio, or prepare for thresholds.\n• `timechart` plots the metric over time using **span=1h** buckets with a separate series **by fn** — ideal for trending and alerting on this use case.\n• `eval` defines or adjusts **err_rate** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Filters the current rows with `where err_rate > 5` — typically the threshold or rule expression for this monitoring goal.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Stacked area chart (executions by outcome), Line chart (error rate % over time), Table (function_name, invocations, errors, retry estimate).",
  "grandmaExplanation": "Rising retries and error rates signal unstable dependencies or quota issues before quotas hard-stop traffic; trending supports service goals review and incident prevention.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
