{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.5.15",
  "title": "GCP Cloud Functions Retry and Error Rate Trending",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Performance"
  ],
  "splunkPillar": "Observability",
  "controlTest": {
    "positiveScenario": "Replay sample `google:gcp:pubsub:message` events with `resource.type=\"cloud_function\"` into `index=gcp`. Confirm UC-4.5.15 returns >=1 row and the alert fires.",
    "negativeScenario": "Replay benign `google:gcp:pubsub:message` traffic without `resource.type=\"cloud_function\"`. Confirm UC-4.5.15 returns zero rows and the alert does not fire."
  },
  "dataSources": "`index=gcp` sourcetype=`google:gcp:pubsub:message` collected via `Splunk_TA_google-cloudplatform`; validate ingest with `| stats count by sourcetype`.",
  "app": "`Splunk_TA_google-cloudplatform`",
  "spl": "index=gcp sourcetype=\"google:gcp:pubsub:message\" resource.type=\"cloud_function\"\n| table _time messageId publishTime attributes\n| eval uc_marker=\"UC-4.5.15_gcp_cloud_functions_retr_15\"\n| sort -_time",
  "description": "UC-4.5.15 — GCP Cloud Functions Retry and Error Rate Trending: queries Google Cloud `google:gcp:pubsub:message` for search criteria `resource.type=\"cloud_function\"` and returns actor, resource, and region context for performance review and change validation.",
  "value": "On Google Cloud, detecting 'GCP Cloud Functions Retry and Error Rate Trending' early shortens cloud incident response and produces defensible audit evidence for identity and change-management controls. UC-4.5.15 drives high-priority performance alerting before customer-impacting drift.",
  "implementation": "Ingest execution count metrics with result/status labels from Cloud Monitoring. Supplement with log-based counts from Cloud Logging for detailed error classes. Baseline hourly error and retry rates per function. Alert when error share exceeds threshold or retries spike versus invocations.",
  "visualization": "Stacked area chart (executions by outcome), Line chart (error rate % over time), Table (function_name, invocations, errors, retry estimate).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk_TA_google-cloudplatform",
      "url": "https://splunkbase.splunk.com/app/3088"
    },
    {
      "title": "Vendor cloud documentation",
      "url": "https://cloud.google.com/docs"
    }
  ],
  "knownFalsePositives": "Inventories can skip a run when an API is throttled or a connector is in maintenance, which looks like a big drop. We check the ingest time and the last good snapshot before we call it shrinkage.",
  "securityDomain": "cloud",
  "equipment": [
    "gcp"
  ],
  "equipmentModels": [
    "gcp_pubsub"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=gcp`.\n• Install and configure `Splunk_TA_google-cloudplatform` with the modular input for `google:gcp:pubsub:message`.\n• Prerequisite crawl UC(s): UC-4.5.16.\n• Confirm field extractions in `props.conf` for `messageId` and related actor fields.\n\nStep 1 — Configure data collection\nIngest execution count metrics with result/status labels from Cloud Monitoring. Supplement with log-based counts from Cloud Logging for detailed error classes. Baseline hourly error and retry rates per function. Alert when error share exceeds threshold or retries spike versus invocations.\nValidate ingest before alerting:\n```spl\nindex=gcp sourcetype=\"google:gcp:pubsub:message\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=gcp sourcetype=\"google:gcp:pubsub:message\" resource.type=\"cloud_function\"\n| table _time messageId publishTime attributes\n| eval uc_marker=\"UC-4.5.15_gcp_cloud_functions_retr_15\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `google:gcp:pubsub:message` in `index=gcp` to detect gcp cloud functions retry and error rate trending. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the provider cloud console audit view for the same 24-hour window. Confirm row counts and `messageId` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_5_15_alert` with severity aligned to `high` criticality. Dashboard: Stacked area chart (executions by outcome), Line chart (error rate % over time), Table (function_name, invocations, errors, retry estimate)..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade `Splunk_TA_google-cloudplatform` and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of gcp cloud functions retry and error rate trending so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.5.16"
  ],
  "wave": "walk"
}
