{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.4.1",
  "title": "Terraform Drift Detection",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Configuration"
  ],
  "splunkPillar": "Security",
  "controlTest": {
    "positiveScenario": "Replay sample `terraform:plan` events with `matching activity` into `index=devops`. Confirm UC-4.4.1 returns >=1 row and the alert fires.",
    "negativeScenario": "Replay benign `terraform:plan` traffic without `matching activity`. Confirm UC-4.4.1 returns zero rows and the alert does not fire."
  },
  "dataSources": "`index=devops` sourcetype=`terraform:plan` collected via Custom input (Terraform CLI output, CI/CD integration); validate ingest with `| stats count by sourcetype`.",
  "app": "Custom input (Terraform CLI output, CI/CD integration)",
  "spl": "index=devops sourcetype=\"terraform:plan\"\n| table _time userIdentity.arn eventName sourceIPAddress\n| eval uc_marker=\"UC-4.4.1_terraform_drift_detectio_1\"\n| sort -_time",
  "description": "UC-4.4.1 — Terraform Drift Detection: queries HASHICORP `terraform:plan` for configured audit predicates and returns actor, resource, and region context for security review and change validation.",
  "value": "On HASHICORP, detecting 'Terraform Drift Detection' early shortens cloud incident response and produces defensible audit evidence for identity and change-management controls. UC-4.4.1 drives medium-priority security alerting before customer-impacting drift.",
  "implementation": "Run `terraform plan -detailed-exitcode` on schedule in CI/CD. Forward plan output to Splunk via HEC. Exit code 2 = changes detected (drift). Alert on any drift in production workspaces.",
  "visualization": "Table (workspace, resource, drift), Single value (drifted resources), Bar chart.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/"
    },
    {
      "title": "Vendor cloud documentation",
      "url": "https://docs.splunk.com/Documentation/CIM"
    }
  ],
  "knownFalsePositives": "A plan can show a diff right after a release while state catches up, or when we intentionally tuned something in the console during an emergency. We confirm the workspace and the window before we treat it as unplanned drift.",
  "mitreAttack": [
    "T1578"
  ],
  "securityDomain": "cloud",
  "equipment": [
    "hashicorp"
  ],
  "equipmentModels": [
    "hashicorp_plan"
  ],
  "cost": {
    "tier": "medium",
    "search_load": "moderate"
  },
  "detailedImplementation": "Prerequisites\n• Splunk Enterprise or Splunk Cloud with search access to `index=devops`.\n• Install and configure Custom input (Terraform CLI output, CI/CD integration) with the modular input for `terraform:plan`.\n• Prerequisite crawl UC(s): UC-4.4.33.\n• Confirm field extractions in `props.conf` for `userIdentity.arn` and related actor fields.\n\nStep 1 — Configure data collection\nRun `terraform plan -detailed-exitcode` on schedule in CI/CD. Forward plan output to Splunk via HEC. Exit code 2 = changes detected (drift). Alert on any drift in production workspaces.\nValidate ingest before alerting:\n```spl\nindex=devops sourcetype=\"terraform:plan\" | stats count by sourcetype\n```\n\nStep 2 — Create the search and alert\n```spl\nindex=devops sourcetype=\"terraform:plan\"\n| table _time userIdentity.arn eventName sourceIPAddress\n| eval uc_marker=\"UC-4.4.1_terraform_drift_detectio_1\"\n| sort -_time\n```\n\nUnderstanding this SPL\nScopes `terraform:plan` in `index=devops` to detect terraform drift detection. Tune thresholds to your change cadence; exclude break-glass principals and sandbox accounts via a lookup.\n\nStep 3 — Validate\nCompare to the provider cloud console audit view for the same 24-hour window. Confirm row counts and `userIdentity.arn` values match the vendor console. Run `| fieldsummary` on a 1-hour sample to verify extractions.\n\nStep 4 — Operationalize and troubleshoot\nSave as `4_4_1_alert` with severity aligned to `medium` criticality. Dashboard: Table (workspace, resource, drift), Single value (drifted resources), Bar chart..\nFailure modes: (1) no events — verify modular input/API credentials and index routing; (2) empty actor fields — upgrade Custom input (Terraform CLI output, CI/CD integration) and review alias mappings; (3) API throttling — reduce poll frequency or enable queue buffering; (4) alert fatigue — tune thresholds and document exclusions in knownFalsePositives.\n",
  "grandmaExplanation": "We watch cloud logs for signs of terraform drift detection so your team catches problems in your online services before customers or auditors do.",
  "prerequisiteUseCases": [
    "UC-4.4.33"
  ],
  "wave": "walk"
}
