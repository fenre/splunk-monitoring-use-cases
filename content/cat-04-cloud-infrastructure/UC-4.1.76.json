{
  "$schema": "../../schemas/uc.schema.json",
  "id": "4.1.76",
  "title": "Lambda Layer Version Compliance",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Observability",
  "exclusions": "Covers the detection logic documented for 'Lambda Layer Version Compliance' only. Does not supplant cross-domain SIEM playbooks for unrelated domains or regulatory attestation. Entities listed in `operational_exceptions.csv` during approved tests are excluded.",
  "evidence": "Saved search `operations_lambda_layer_version_compliance` (UC-4.1.76), SOC dashboard panel \"Operations — Lambda Layer Version Compliance\", and weekly export to `index=audit_evidence` sourcetype=csv:ops_controls.",
  "controlTest": {
    "positiveScenario": "UC-4.1.76 positive (Lambda, Layer, Version, Compliance): ingest synthetic events into `index=aws` sourcetype='aws:cloudtrail' where `count` grouped by `userIdentity.arn`, `requestParameters.functionName`, `layer_arn` satisfies detection threshold for 'Lambda Layer Version Compliance'. Validate lookup `approved_lambda_layers`. Run the saved search and confirm at least one alertable row.",
    "negativeScenario": "UC-4.1.76 negative (Version Compliance): ingest benign `aws` / `aws:cloudtrail` events where `userIdentity.arn`, `requestParameters.functionName`, `layer_arn` stays below detection threshold, or exclude the test entity via `ops_exception_register.csv`. Variant 2 confirms zero alert rows for 'Lambda Layer Version Compliance'."
  },
  "dataSources": "`sourcetype=aws:cloudtrail` (lambda:GetFunction, PublishLayerVersion), Config custom rule output Collected via Splunk Add-on for Amazon Web Services (Splunkbase 1876) modular input or scripted poll into the index named in the SPL.",
  "app": "`Splunk_TA_aws` — Splunk Add-on for Amazon Web Services (Splunkbase 1876)",
  "spl": "index=aws sourcetype=\"aws:cloudtrail\" eventSource=\"lambda.amazonaws.com\" eventName=\"UpdateFunctionConfiguration\"\n| spath path=requestParameters.layers{}\n| mvexpand requestParameters.layers{} limit=200\n| eval layer_arn=requestParameters.layers{}\n| lookup approved_lambda_layers layer_arn OUTPUT approved\n| where isnull(approved)\n| stats count by userIdentity.arn, requestParameters.functionName, layer_arn",
  "description": "Identifies UC-4.1.76 for Lambda, Layer, Version, Compliance over `index=aws` sourcetype `aws:cloudtrail` with thresholds configured threshold. Groups `count` by `userIdentity.arn`, `requestParameters.functionName`, `layer_arn` each `the scheduled window`. Differentiated by lookup `approved_lambda_layers`. Supports repeatable operations triage for 'Lambda Layer Version Compliance'.",
  "value": "For 'Lambda Layer Version Compliance', UC-4.1.76 converts `activity` on `aws:cloudtrail` feeds into actionable rows and reduces operational risk and delayed incident response.",
  "implementation": "Maintain CSV lookup of approved layer version ARNs. Alert on attach of unapproved layer or version drift weekly scan via `ListFunctions`. Integrate with CI/CD to block deploys pre-merge.",
  "visualization": "Table (function, layer, user), Bar chart (non-compliant functions), Timeline (changes).",
  "cimModels": [
    "Change"
  ],
  "cimSpl": "| tstats `summariesonly` count\n  from datamodel=Change.All_Changes\n  where match(All_Changes.action, \"(?i).+\")\n  by All_Changes.user All_Changes.object All_Changes.action span=1h\n| sort -count",
  "dataModelAcceleration": "Change (All_Changes): for CloudTrail management events mapped to CIM. Enable Data Model Acceleration for the CIM data models in `cimModels` once `Splunk_TA_aws` (and any CIM tags/aliases) map your fields; otherwise the `cimSpl` summary search may be empty. Step 2 on raw or metric indexes stays primary for most sites.",
  "references": [
    {
      "title": "Splunk_TA_aws",
      "url": "https://splunkbase.splunk.com/app/1876",
      "retrieved": "2026-04-25"
    },
    {
      "title": "CIM: Change",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Change",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk HTTP Event Collector",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Data/UsetheHTTPEventCollector",
      "retrieved": "2026-04-25"
    },
    {
      "title": "Splunk inputs.conf reference",
      "url": "https://docs.splunk.com/Documentation/Splunk/latest/Admin/Inputsconf",
      "retrieved": "2026-04-25"
    }
  ],
  "knownFalsePositives": "1. **Maintenance window on `aws` (UC-4.1.76)** — Planned work for 'Lambda Layer Version Compliance' shifts `count` over `span=1h` while `userIdentity.arn`, `requestParameters.functionName`, `layer_arn` remains in-scope. Applies lookup `approved_lambda_layers`. Cross-check CMDB CHG.\n\n2. **Batch `count` on `aws:cloudtrail`** — Scheduled automation elevates `requestParameters.functionName` without crossing configured threshold for UC-4.1.76. Compare `_time` minute against the job schedule.\n\n3. **Synthetic probe on `userIdentity.arn`, `requestParameters.functionName`, `layer_arn`** — Health checks in `aws` populate the same fields the SPL groups on. Exclude monitoring-tagged assets before paging.\n\n4. **Onboarding burst for `aws:cloudtrail`** — First 48h baseline for UC-4.1.76 on `userIdentity.arn` is unstable; join `approved_lambda_layers` prior to threshold.\n\n**Suppression mechanism:** `ops_exception_register.csv` keyed on `userIdentity.arn`, `exception_reason`, `valid_until`.",
  "equipment": [
    "aws"
  ],
  "equipmentModels": [
    "aws_generic"
  ],
  "splunkVersions": [
    "9.2+",
    "Cloud"
  ],
  "detailedImplementation": "Prerequisites\n• Install and configure the required add-on or app: `Splunk_TA_aws`.\n• Ensure the following data sources are available: `sourcetype=aws:cloudtrail` (lambda:GetFunction, PublishLayerVersion), Config custom rule output.\n• For app installation, inputs.conf, and Splunk directory layout, see the Implementation guide: docs/implementation-guide.md\n\nStep 1 — Configure data collection\nMaintain CSV lookup of approved layer version ARNs. Alert on attach of unapproved layer or version drift weekly scan via `ListFunctions`. Integrate with CI/CD to block deploys pre-merge.\n\nStep 2 — Create the search and alert\nRun the following SPL in Search (then save as report or alert; adjust time range and threshold as needed):\n\n```spl\nindex=aws sourcetype=\"aws:cloudtrail\" eventSource=\"lambda.amazonaws.com\" eventName=\"UpdateFunctionConfiguration\"\n| spath path=requestParameters.layers{}\n| mvexpand requestParameters.layers{} limit=200\n| eval layer_arn=requestParameters.layers{}\n| lookup approved_lambda_layers layer_arn OUTPUT approved\n| where isnull(approved)\n| stats count by userIdentity.arn, requestParameters.functionName, layer_arn\n```\n\nUnderstanding this SPL\n\n**Lambda Layer Version Compliance** — Outdated layers carry vulnerable dependencies; enforcing approved layer ARNs avoids shadow IT libraries in functions.\n\nDocumented **Data sources**: `sourcetype=aws:cloudtrail` (lambda:GetFunction, PublishLayerVersion), Config custom rule output. **App/TA** (typical add-on context): `Splunk_TA_aws`. The SPL below should target the same indexes and sourcetypes you configured for that feed—rename `index=` / `sourcetype=` if your deployment differs.\n\nThe first pipeline stage scopes events using **index**: aws; **sourcetype**: aws:cloudtrail. That sourcetype matches what this use case lists under Data sources.\n\n**Pipeline walkthrough**\n\n• Scopes the data: index=aws, sourcetype=\"aws:cloudtrail\". Cross-check against **Data sources** above so indexes and sourcetypes match your ingestion.\n• Extracts structured paths (JSON/XML) with `spath`.\n• Expands multivalue fields with `mvexpand` — use `limit=` to cap row explosion.\n• `eval` defines or adjusts **layer_arn** — often to normalize units, derive a ratio, or prepare for thresholds.\n• Enriches events using `lookup` (lookup definition + optional OUTPUT fields).\n• Filters the current rows with `where isnull(approved)` — typically the threshold or rule expression for this monitoring goal.\n• `stats` rolls up events into metrics; results are split **by userIdentity.arn, requestParameters.functionName, layer_arn** so each row reflects one combination of those dimensions.\n\n\nStep 3 — Validate\nConfirm that events are present in the index and that the search returns expected results. Compare with known good/bad scenarios if applicable. Verify field extractions and index permissions.\n\nStep 4 — Operationalize\nAdd the search to a dashboard or set up alert actions (email, webhook, PagerDuty, etc.) as required. Document the use case in your runbook and assign an owner. Consider visualizations: Table (function, layer, user), Bar chart (non-compliant functions), Timeline (changes).\n\nStep 1 — Configure data collection\n\nInstall and configure the add-on referenced in the app field (`Splunk_TA_aws` — Splunk Add-on for Amazon Web Services (Splunkbase 1876)). Enable the modular input or HEC token that writes sourcetype=aws:cloudtrail into aws. Default poll interval 300–900 seconds unless the vendor API rate-limits lower. Grant the ingestion role `list_inputs` and `search` on the target index.\n\nStep 2 — Create the search and alert\n\nSchedule the SPL with earliest=-24h@h and throttle per entity to avoid duplicate pages. Document tunable macros in the comment header. Understanding this SPL: filters in the first pipeline reduce license cost; stats/timechart aggregates define the alert grain; joins/lookups enrich owner or asset metadata for routing.\n\nStep 3 — Validate\n\nCompare `| tstats count where index=... sourcetype=...` against the vendor admin UI inventory count (+/-5%). Spot-check two known entities for field presence (`approved, layer_arn, requestParameters.functionName, userIdentity.arn`). Run `timechart count span=1h` over 7 days to find ingestion gaps longer than two poll intervals.\n\nStep 4 — Operationalize\n\nAdd a dashboard row with single-value KPI tiles, a 24-hour timechart, and a drilldown table mirroring the SPL output columns. Route critical severity to the platform on-call channel; route warning to the service owner from the lookup.\n\nStep 5 — Troubleshooting\n\nNo events: verify forwarder connectivity, HEC token ACLs, and that the modular input appears in `index=_internal source=*splunkd*`. NULL fields: check props/transforms order and TA version. Fewer results than expected: confirm index allow-lists on the search head role and that `_time` uses the correct timezone. API errors: inspect splunkd.log for HTTP 401/429 from the vendor endpoint configured in inputs.conf.\n",
  "grandmaExplanation": "We keep an eye on lambda layer version compliance. Outdated layers carry vulnerable dependencies; enforcing approved layer ARNs avoids shadow IT libraries in functions. We want the right people to notice in time, not only when a customer or an auditor stumbles on it first.",
  "prerequisiteUseCases": [],
  "wave": "crawl"
}
