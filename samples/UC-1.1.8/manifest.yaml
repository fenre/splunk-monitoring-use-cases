uc_id: "1.1.8"
sourcetype: "linux_secure"
index: "os"
source: "/var/log/secure"
host: "linux-web-01"
timerange: "-24h"
expected:
  min_count: 5
  fields:
    - name: src
      regex: "^(10|172|192)\\."
origin: "vendor-doc"
origin_ref: "https://www.openssh.com/manual.html"
last_reviewed: "2026-04-16"
reviewer: "@splunk-monitoring-use-cases"
notes: |
  Seven "Failed password" events from a single source IP (10.99.0.42) targeting
  several usernames — the classic brute-force pattern UC-1.1.8 alerts on. The
  rex extraction `from (?<src>\\d+\\.\\d+\\.\\d+\\.\\d+)` expects the standard
  `Failed password for <user> from <src> port <port> ssh2` sshd log format.
