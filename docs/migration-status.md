# Migration Status: use-cases/ → content/

This document tracks the remaining dependencies on the legacy `use-cases/` tree.
The v7 pipeline uses `content/` as the canonical source per
[ADR-0007](adr/0007-json-as-source-of-truth.md); `use-cases/` is retained only
for the specific purposes listed below.

## Remaining Dependencies

| Component | File | Dependency | Target Removal |
|-----------|------|-----------|----------------|
| Equipment enrichment fallback | `tools/build/enrichment.py` | `_load_sidecar_equipment_cache()` reads `use-cases/**/uc-*.json` as a fallback when a UC is absent from `content/` | v7.5 |
| Repo-root build script | `build.py` | Generates project-root `catalog.json` / `data.js` / `llms*.txt`. **dist/ is now SSOT-authoritative** (P1 step 5b, 2026-05-08). **Consumer migration complete (P1 step 5c, 2026-05-09):** `scripts/equipment_lib.py` and `scripts/audit_repo_consistency.py` now import directly from `tools/build/enrichment.py`; 11 other scripts (`audit_splunk_cloud_compat.py`, `generate_api_surface.py`, `tools/capture_baselines.py`, `audit_catalog_schema.py`, `build_ta.py`, `build_es.py`, `audit_quality_metadata.py`, `generate_scorecard.py`, `audit_prerequisites.py`, `build_provenance.py`, `run_uc_tests.py`, `samples_index.py`) prefer `dist/catalog.json` with a temporary fallback to the project-root copy. CI workflow path triggers (`validate.yml`, `uc-tests.yml`) and the freshness diff dropped references to `build.py` / `catalog.json` / `llms*.txt`. **Final file deletion (the actual `git rm build.py catalog.json data.js llms.txt llm.txt llms-full.txt`) is the last step**; gated behind one release of stable consumer-fallback exercise. | v7.5 (P1 step 5c, file deletion) |
| Non-technical sync audit | `scripts/audit_non_technical_sync.py` | Validates JS against `use-cases/cat-*.md` headings | v7.5 |
| Category markdown corpus | `use-cases/cat-*.md` | **P1 step 7 Phase A complete (2026-05-09):** all 20 legacy-only orphan UCs migrated into `content/cat-05-network-infrastructure/`. The legacy tree now mirrors a strict subset of JSON SSOT (verified by `scripts/audit_legacy_orphans.py --check`). Phase B (`git mv use-cases content-legacy/` + `pages.yml` trigger drop) targets v8.0; Phase C (delete) targets v8.1. | v8.1 (P1 step 7 Phase C) |
| ES/TA build scripts | `scripts/build_es.py`, `scripts/build_ta.py` | Comments reference use-cases/ paths | v7.5 |
| `audit_uc_structure.py` markdown backend | `scripts/audit_uc_structure.py` | Markdown audit retained for parity with the SSOT JSON audit until P1 step 7 Phase B (move to `content-legacy/`) lands | v8.0 |

## SSOT field-completeness gap (P1 step 3, 2026-05-08)

The v7.0 markdown→JSON migration did not fully capture every authoring field.
The `audit_uc_structure.py --source json` backend (added in P1 step 3) plus the
parity test `tests/build/test_legacy_artifacts_parity.py` quantify the gap so
the burndown is data-driven:

| Field | UCs missing in SSOT (legacy populates them) | Action | Status |
|---|---|---|---|
| `cimModels` (`a`) | ~~1,785~~ → ~~867~~ → 876 (semantic equivalent: empty list = "N/A"; +9 from P1 step 7 Phase A migration) | Backfilled from `use-cases/cat-*.md` markdown via `scripts/backfill_cim_models.py` | Closed 2026-05-08 (P1 step 5b prep); snapshot bumped 2026-05-09 (P1 step 7 Phase A) |
| `cimSpl` (`qs`) | 364 | Already richer in SSOT for 1,537 UCs; net gain. Backfill the laggards. | Open |
| `wave` (`wv`) | 96 | Backfill | Open |
| `_qg` (quality grade) | 81 | Re-derive from sidecars | Open |
| `sapp` | 22 | Re-derive from `t` field | Open |
| `ta_link` | 10 | Re-derive | Open |
| `hw` | ~~5~~ → 25 (semantic equivalent: SSOT `equipmentModels` list replaces legacy `Equipment Models` text; +20 from P1 step 7 Phase A migration) | Reconcile naming with `equipmentModels` | Snapshot bumped 2026-05-09 (P1 step 7 Phase A); semantic equivalence accepted as final state |
| `e` (equipment) | 2 | Backfill | Open |
| `premium` | 1 | Backfill | Open |

Current baseline lives in `data/audit-baselines/uc-structure-json.txt`. As of
P1 step 5b prep (2026-05-08) the JSON audit reports **0 issues**; the cimModels
backfill closed the headline gap. Every PR that opens a *new* gap must add a
baseline line in the same commit and document the rationale.

**Headline migration debt: cimModels — CLOSED.** All 7,657 UCs now have a
`cimModels` array (either populated from the curator's markdown or set to
`[]` to match `- **CIM Models:** N/A`). The audit treats `[]` as a valid
"no CIM applies" curation outcome (see `JSON_FIELDS_ALLOW_EMPTY_LIST` in
`scripts/audit_uc_structure.py`). The remaining 867 entries in the
field-loss snapshot reflect the empty-list-vs-string mismatch and are
semantically equivalent; tracked in `tests/build/test_legacy_artifacts_parity.py`.

## Completed Migrations

- [x] Primary catalog loading (v7.0): `content/` is default, `use-cases/` is fallback
- [x] grandmaExplanation (v7.1): loaded exclusively from `content/`
- [x] Equipment enrichment (v7.3): `content/` is primary, `use-cases/` is fallback (10.1)
- [x] UC-22.1.1 equipment tags (v7.3): removed nonsensical hardware tags
- [x] Enrichment SSOT (v7.4.x, P1 step 1, 2026-05-08): `parse_content.py` reads `tools/build/enrichment.py` directly; `build.py` no longer imported via `_legacy_module()` in the build pipeline. Parity guard in `tests/build/test_enrichment_parity.py`.
- [x] Legacy artefacts SSOT writers (v7.4.x, P1 step 2, 2026-05-08): `render_legacy_artifacts` emits `catalog.json` / `data.js` / `llms*.txt` from the in-memory `Catalog`. Project-root copies remain authoritative pending the `cimModels` backfill above.
- [x] JSON-corpus structural audit (v7.4.x, P1 step 3, 2026-05-08): `audit_uc_structure.py --source json --full` covers 7,657 UCs (vs. 6,565 in the markdown backend); baseline file pins the 1,785 known-issue UCs.
- [x] Single-walk sidecar caches (v7.4.x, P1 step 4, 2026-05-08): `tools/build/enrichment.py` walks `content/cat-*/UC-*.json` exactly once via `_populate_content_sidecar_caches`, populating grandma + compliance + quality caches in a single pass. 4 new parity tests in `tests/build/test_enrichment_parity.py::TestSidecarCachesContentParity` lock the consolidation. Two consecutive reproducible builds hash byte-identically across all 24,583 artefacts.
- [x] Legacy markdown loader removed (v7.4.x, P1 step 5a, 2026-05-08): `tools/build/parse_content.py` no longer dispatches to a `SPLUNK_UC_LOADER=legacy` markdown branch. The env var is honoured for backward compatibility but always resolves to the JSON-corpus loader and emits a one-line stderr deprecation notice if a legacy value is passed. The `_legacy_module()` debugging shim was deleted in the same change.
- [x] cimModels backfill (v7.4.x, P1 step 5b prep, 2026-05-08): `scripts/backfill_cim_models.py` populated `cimModels` in 925 sidecars. Of the 1,785 UCs flagged by the JSON audit: 463 received real CIM-model lists from `use-cases/cat-*.md`, 867 were marked "N/A" in markdown and now carry `[]`, 25 had no markdown CIM line and now carry `[]`, 430 are JSON-only (no markdown counterpart) and now carry `[]` pending curation. The audit was extended (`JSON_FIELDS_ALLOW_EMPTY_LIST`) to treat `cimModels: []` as a valid curation outcome ("no CIM applies"). Audit baseline reduced from 1,785 lines to 0; field-loss snapshot in `tests/build/test_legacy_artifacts_parity.py` tightened from 1,330 to 867 (semantic equivalent: legacy "N/A" string vs. SSOT empty list).
- [x] Secondary-field backfill (v7.4.x, P1 step 5b prep #2, 2026-05-08): `scripts/backfill_secondary_fields.py` populated `cimSpl` in 364 sidecars and `wave` in 96 sidecars from the legacy markdown corpus. Field-loss snapshot for those two fields drops to 0. Combined with the cimModels backfill, the SSOT-derived catalog now carries 1,249 UCs with `cimSpl` (was 0 from legacy alone — and 1,537 UCs gain richer cimSpl in SSOT-only) and 481 UCs with explicit `wave` (where legacy had none).
- [x] App enrichment lookup hardening (v7.4.x, P1 step 5b prep #3, 2026-05-08): `apps_for_ta_string()` and `ta_link_for_ta_string()` in `tools/build/enrichment.py` learned to also match Splunkbase numeric IDs cited as `Splunkbase 1928`, `app/1928`, or `splunkbase.splunk.com/app/1928` patterns. Previously they only matched bare TA tokens like `Splunk_TA_servicenow`, which fails when curators write the SSOT-style prose `Splunk Add-on for ServiceNow (Splunkbase 1928)`. The new ID-pass closed 9 of 22 `sapp` losses and 1 of 10 `ta_link` losses; SSOT now produces 63 additional `sapp` entries and 200 additional `ta_link` entries that the legacy lookup misses.
- [x] dist/ is SSOT-authoritative for legacy artefacts (v7.4.x, P1 step 5b, 2026-05-08): `catalog.json`, `llms.txt`, `llm.txt`, `llms-full.txt` were removed from `_PROJECT_STATIC_FILES` in `tools/build/build.py`. `_stage_public` no longer shadows `render_legacy_artifacts.render()` output, so `dist/catalog.json` is the SSOT-derived copy (7,657 UCs vs. legacy's 6,565). The repo-root `build.py` script and the committed project-root copies of those four artefacts are scheduled for deletion in P1 step 5c once their last consumers — `scripts/equipment_lib.py`, `scripts/audit_repo_consistency.py`, the parity tests in `tests/build/test_*_parity.py`, and the docs that still link to raw GitHub URLs — are migrated. New regression test `test_dist_catalog_is_ssot_authoritative` pins this decision so a future PR can't silently re-enable the shadow.
- [x] Project-level pyproject.toml (v7.4.x, P4 step 1, 2026-05-08): repo-root `pyproject.toml` defines `[project]` metadata + `[build-system]` hatchling, dynamic version from `VERSION`, optional extras (`audits`, `build`, `test`, `dev`, `all`), and consolidated tooling configuration for ruff (lint + format), mypy, pytest, and coverage. `pip install -e ".[audits,test,dev]"` works in a clean venv; `make dev-install` is the repo-blessed entrypoint. Wheel target ships `tools/build/` as a flat `build` package (renames to `splunk_uc` in P6 once the legacy `build.py` at repo root is deleted).
- [x] Legacy orphan migration (v9.x, P1 step 7 Phase A, 2026-05-09): `scripts/audit_legacy_orphans.py` reports zero orphans. The 20 UCs that previously existed only in `use-cases/cat-05-network-infrastructure.md` (subcat 5.1×9, 5.4×3, 5.5×5, 5.6×2, 5.8×1) now have JSON sidecars under `content/cat-05-network-infrastructure/UC-*.json` validating against `schemas/uc.schema.json` v1.7.0. Catalog UC count moves from 7,657 to 7,677. The CI gate flips from `--baseline 20` to `--check`. `_category.json` ratchets the per-subcategory counts (5.1: 66→75, 5.4: 37→40, 5.5: 20→25, 5.6: 17→19, 5.8: 28→29; total 700→720). All 20 UCs ship at the `community` tier — they are content-faithful migrations, not gold-standard SME promotions, by explicit design. Phase B (rename `use-cases/` → `content-legacy/`, drop pages.yml triggers) and Phase C (delete) follow on the v8.0/v8.1 release schedule.
- [x] P1 step 5c — consumer-script migration (v9.x, 2026-05-09): the legacy `build.py` shim and the project-root `catalog.json` / `data.js` / `llms*.txt` artefacts no longer have any first-party Python consumer that requires them. **`scripts/equipment_lib.py`** now imports `EQUIPMENT` directly from `tools/build/enrichment.py` (was: surgically AST-parsed `build.py` literal); **`scripts/audit_repo_consistency.py`** imports `CAT_GROUPS` and `SPLUNK_APPS` the same way (was: `ast.parse` of `build.py`). Both add a small `sys.path` shim so the import works for in-tree CI runs that haven't `pip install -e .`-installed the package. **11 catalog consumers** (`scripts/audit_splunk_cloud_compat.py`, `scripts/generate_api_surface.py`, `tools/capture_baselines.py`, `scripts/audit_catalog_schema.py`, `scripts/build_ta.py`, `scripts/build_es.py`, `scripts/audit_quality_metadata.py`, `scripts/generate_scorecard.py`, `scripts/audit_prerequisites.py`, `scripts/build_provenance.py`, `scripts/run_uc_tests.py`, `scripts/samples_index.py`) gained a `_resolve_catalog_path()` helper that prefers `dist/catalog.json` and falls back to the project-root copy for one transition release. **CI workflow paths** (`validate.yml`, `uc-tests.yml`) dropped `build.py` / `catalog.json` from path triggers and the freshness-diff watchlist (per ADR-0009 those four artefacts no longer live at the project root). The actual `git rm` of the six legacy files is the last step in P1 step 5c — held back until consumer fallback paths have soaked for at least one release.
- [x] P4 step 3 — `Catalog` dataclass + render_pages / render_api / templates narrowed to TypedDicts (v9.x, 2026-05-09): `tools/build/parse_content.py:Catalog` now declares its content fields with the wire-format TypedDicts from `tools/build/models.py` (renamed from `types.py` in v8.0.x to avoid shadowing stdlib `types` on `sys.path[0]`) — `categories: list[CatalogCategory]`, `cat_meta: dict[str, CategoryMeta]`, `regulations: dict[str, RegulationFramework]`. `iter_ucs()` returns `Iterator[Tuple[CatalogCategory, CatalogSubcategory, CatalogUC]]` and `uc_by_id()` returns `Optional[CatalogUC]` so render_* consumers get autocomplete + mypy field-access checking automatically. The renderer surface itself was migrated as a follow-on: **`tools/build/render_pages.py`** (`_emit_uc`, `_emit_category`, `_emit_regulations`, `_alias_candidates`, `_write_json`, `framework_rows`), **`tools/build/render_api.py`** (`_stub_uc`), **`tools/build/templates/uc.py`** (`render_index_json`, `render_markdown_twin`), **`tools/build/templates/category.py`** (`render_html`, `render_index_json`, `_category_default_desc`, `_render_quick_facts`, `_render_subcategory`), and **`tools/build/templates/regulation.py`** (every `render_*` plus `_framework_description`, `_render_facts`). `Catalog.equipment` keeps its loose `list[dict[str, Any]]` annotation because the live `EquipmentEntry` shape nests fully-typed entries under `models` rather than the schema's documented `list[str]`; tightening that requires a TypedDict update tracked under the P4 burndown follow-up. Reproducible build verified byte-identical (`tools/build/build.py --reproducible --check` passes); 413 tests pass. **Remaining (FUTURE, lower priority):** scripts/audit_uc_structure.py + scripts/audit_compliance_mappings.py — replace `dict[str, Any]` with `UseCase` / `ComplianceMapping`. MCP wire types stay in `mcp/src/splunk_uc_mcp/tools/` JSON schemas; reuse plan tracked under p9-monorepo.
- [x] P11 — License inventory tooling (v9.x, 2026-05-09): stdlib-only audit + committed SPDX baseline. `scripts/audit_license_inventory.py` resolves every Python dependency declared in `pyproject.toml` (root + `mcp/`) via `importlib.metadata` (PEP 639 `License-Expression` → legacy `License` field → trove `Classifier` lines), enumerates every `LICENSE*` file the repo vendors outside the top-level licence, and gates the result against a curated 18-entry SPDX allowlist. `data/license-inventory.json` is the committed baseline (typed by `schemas/license-inventory.schema.json`); `docs/license-inventory.md` is the auto-generated human-readable rollup. CI runs `make audit-license-inventory` (== `--check`) inside the `lint` job after editable-installing the root + MCP packages. Maintainer workflow: edit pyproject.toml, `pip install -e ".[audits,test,dev]"`, `make write-license-inventory`, commit. Closes the licence-inventory subtask of P11; remaining P11 work (Codespaces/devcontainer **already shipped**, CONTRIBUTING rewrite **blocked on P6 taxonomy**, replication-starter verification **shipped 2026-05-09 — see next bullet**, public roadmap board) tracked separately.
- [x] P11 — Replication-starter contract gate (v9.x, 2026-05-09): pinned the "minimum-viable-fork" template under `templates/replication-starter/` so first-time forkers don't hit a broken walkthrough. `tests/build/test_replication_starter.py` adds 23 tests across seven dimensions: file presence (README, build.py, schema, sample md, html), JSON Schema 2020-12 dialect & meta-validation via `jsonschema.Draft202012Validator.check_schema`, build smoke (exits 0, produces `catalog.json` + `data.js`, foreign-cwd safe via `SCRIPT_DIR = os.path.dirname(os.path.abspath(__file__))`, byte-reproducible across two consecutive runs, idempotent on input markdown), output shape (catalog validates against bundled schema, `total_uc` matches the actual nested count, exact `cat 1 / sub 1.1 / UC 1.1.1+1.1.2` fixture content with `Data Sources:` → `data_sources` snake-case key normalisation, `i`/`n`/`s`/`u` abbreviations match the parent catalogue), **freshness gate** on the committed `catalog.json` / `data.js` so they can never drift from a fresh `python3 build.py` rebuild (the starter ships these committed so static-host deployment works without a build step), HTML dependency contract (`<script src=\"data.js\">`, no `fetch()` calls — keeps `file://` previews + GitHub Pages working — iterates `cat.s` / `sub.u` matching parser output), documentation cross-references (README ↔ `docs/replication-guide.md`, AGENTS.md still advertises the starter, every README "Files" table entry exists on disk), and a version-bump policy (scoped regex blocks the literal `VERSION` and conventional `vX.Y.Z` / `version: X.Y.Z` shapes without false-positiving on subcategory IDs like `1.1.2` or Python minors like `3.12`). Test runtime: ~0.4 s. Total tests: 430 → 453. Drift-detection sanity-checked by deliberate mutation: a single `total_uc: 999` corruption is independently caught by 3 tests (`test_committed_artefacts_are_fresh`, `test_committed_catalog_known_fixtures`, `test_total_uc_matches_data`) — defence in depth.
- [x] P11 — Roadmap consistency auditor + Project-board JSON contract (v9.x, 2026-05-09): pinned the structural integrity of `ROADMAP.md` so the public-facing project-direction promise can never silently rot. `scripts/audit_roadmap_consistency.py` is a stdlib-only auditor with three modes — `--check` (CI default; structural integrity, repo-relative link resolution, soft-fail on version drift), `--strict-version` (promotes ROADMAP `vX.Y` ↔ `VERSION` ↔ CHANGELOG-top drift to a hard error; flipped on after the maintainer reconciles the heading), and `--export PATH` (writes a `schema_version: "1.0"` JSON snapshot for downstream Project-board sync). Six required H2 sections enforced (`Current release`, `Previous releases`, `Next up: vX.Y …`, `vX.Y+ backlog (no fixed date)`, `Deprecated / declined ideas`, `How to influence the roadmap`); the deprecated-ideas section must be non-empty (preserves the historical "no SaaS" / "no commercial edition" / "no LLM-generated SPL" commitments). Multiline Markdown bullets are joined back into one logical line so the JSON snapshot doesn't truncate. CI runs `make audit-roadmap` (== `--check`) inside the `lint` job after the license-inventory gate. 25 new unit tests in `tests/scripts/test_audit_roadmap_consistency.py` cover pure helpers (section split, multiline bullet joiner, release-line extractor, link checker, version comparator), end-to-end `parse_roadmap()` on synthetic happy-path / error fixtures, version-triple consistency in soft + strict modes, and live-`main()` runs against the real `ROADMAP.md`. Maintainer runbook at `docs/roadmap-sync.md` documents the JSON contract, the one-time `gh project create` setup, the per-release sync recipe, and the single `milestone:X.Y` label convention. Total tests: 453 → 478. Soft-fail gating today: live `ROADMAP.md` ships `## Current release: v7.1` against a `VERSION` of `9.2.0` (a 2-major drift); the gate prints two warnings but exits 0 so the audit lands without a same-PR rewrite of the public release notes. Maintainer flips `--strict-version` on after the heading is reconciled.
- [x] P8 — Build-time metrics emitter + JSON Schema gate (v9.x, 2026-05-09): first deliverable of repo-overhaul plan §P8 (observability). Every build now emits `dist/metrics.json`, a top-line catalogue health snapshot designed for trend tracking. `tools/build/render_metrics.py` is a pure stdlib emitter consuming the in-memory `parse_content.Catalog` and producing: top-line counts (categories, subcategories, useCases, regulations, equipment); quality-tier counts + percentages (gold/silver/bronze/none); depth-score percentile block (count/min/max/mean/p50/p90/p99); grandmaExplanation coverage with character-length percentiles; cross-cutting field coverage (compliance / MITRE / CIM / equipment / prerequisites / ESCU / ESCU-RBA); categorical histograms for wave / criticality / difficulty; per-category UC counts; Top-10 leaderboards for regulations / MITRE techniques / CIM models / equipment IDs (tie-broken alphabetically so two consecutive `--reproducible` builds produce byte-identical output). New `metrics` stage appended to `ALL_STAGES` in `tools/build/build.py` (runs last so it sees the freshly-parsed catalog after every other stage). `schemas/v2/metrics.schema.json` is a JSON Schema 2020-12 contract pinned at `schema_version: "1.0.0"` with `$defs` for the reusable `percentileBlock` and `coverageEntry` shapes and `maxItems: 10` on every leaderboard. 32 new tests in `tests/build/test_render_metrics.py` across six dimensions: pure helpers (`_nearest_rank` / `_percentile_block` / `_top_n` on canonical + edge-case data); synthetic 5-UC catalog end-to-end (every section: counts / quality / coverage / distributions / ucsByCategory / leaders); reproducibility (byte-identical bytes on consecutive `--reproducible` calls); JSON Schema gates on synthetic + live catalogue; build-pipeline wiring; edge cases (empty catalog, unknown quality tier, off-vocabulary wave, empty criticality/difficulty); schema cross-references catching drift between `SCHEMA_VERSION` and on-disk `version`, between `SCHEMA_REF` and `$id`, and between `_TOP_N_*` constants and `maxItems`. Total tests: 478 → 510. **Deliberately complementary, not redundant:** `scorecard.json` keeps its per-category quality dimensions; `metrics.json` carries catalogue-wide rollups optimised for release-over-release diffs. **Remaining P8 work (FUTURE, separate PRs):** weekly stewardship digest, build telemetry per stage.
- [x] P8 — Metrics history snapshots + per-release trend record (v9.x, 2026-05-09): closes the "trend snapshots committed only at release tags" subtask of repo-overhaul plan §P8. Step 1 (the build-time emitter, above) ships a fresh `dist/metrics.json` on every build, but that file is ephemeral — the next build overwrites it. Step 2 captures one **verbatim** copy of `dist/metrics.json` per release into `data/metrics-history/<VERSION>.json`, plus a sorted `data/metrics-history/index.json` listing every release ever captured. The verbatim-copy choice is deliberate: no synthetic fields are injected at write-time, so the snapshot validates against the same `schemas/v2/metrics.schema.json` (which has `additionalProperties: false`) as the live build artefact and downstream tooling can consume snapshots with the same parser. The snapshot's existing `generatedAt` field already records the build time (frozen to git commit time in reproducible builds), so we have no need for a separate "captured at" timestamp. `scripts/snapshot_metrics.py` is a stdlib-only manager with two modes: `--write` copies `dist/metrics.json` into the history directory under the current `VERSION` and refreshes the index (byte-stable idempotency: re-running on an unchanged build produces byte-identical files for both the snapshot and the index); `--check` fails (exit 1) when `VERSION` is bumped without a matching snapshot, when the committed snapshot is malformed, when `catalogueVersion` disagrees with `VERSION`, when `index.json` is out of sync with the snapshot files on disk, or — only when `dist/metrics.json` is also present — when the live build emits a `schema_version` different from the committed snapshot's. `schemas/v2/metrics-history-index.schema.json` pins the index file shape (`schema_version: "1.0.0"`, gates `version` / `schemaVersion` / `generatedAt` / `useCases` / `file` per snapshot entry, semver-pattern enforcement on filenames). New `Makefile` targets: `audit-metrics-snapshot` (CI gate, runs `--check`) and `snapshot-metrics` (release-time, runs `--write`). New `lint` job step in `validate.yml` ("Metrics history snapshot audit (release-time trend record)") so a release bump without a matching snapshot is caught at PR review time. 37 new tests in `tests/scripts/test_snapshot_metrics.py` across six dimensions: pure helpers (`_read_version` / `_validate_metrics_shape` / `_semver_key` strips both `-prerelease` and `+buildmetadata` per SemVer §10 / `_refresh_index` derives the index from disk, sorts semver-descending, skips unparseable files, excludes `index.json` itself); end-to-end `write_snapshot` and `check_snapshot` round trips on a tmp working tree, including a verbatim-copy assertion (the snapshot must have *exactly* the same top-level keys as the live build); drift detection (missing snapshot, malformed JSON, version mismatch, index out of sync, schema mismatch with live build); byte-stable idempotency (writing twice produces byte-identical snapshot *and* index); CLI `main()` exit codes (0/1/2); and a live smoke that the committed `data/metrics-history/` always parses, satisfies `_validate_metrics_shape`, validates against `schemas/v2/metrics.schema.json`, and matches the freshly-derived index. Initial committed snapshot: `data/metrics-history/8.0.0.json` (5,134 bytes) + `index.json` (287 bytes). `docs/metrics-history.md` is the maintainer runbook covering release workflow, the CI gate, the JSON contract, and how to back-fill historical snapshots when adding a new metric. Total tests: 510 → 547. **Deliberate non-feature:** the gate does not assert per-field equality between live build and committed snapshot. Per-PR content edits change every metric on every PR; gating on numeric equality would be pure noise. The contract is "every release has a permanent record", not "every PR matches the latest record".
- [x] P8 — Build telemetry per stage (v9.x, 2026-05-09): closes the "build telemetry per stage" subtask of repo-overhaul plan §P8. Captures wall-clock duration for every pipeline stage so pre-merge benchmarks can spot regressions before they ship. `tools/build/render_telemetry.py` consumes a list of `(stage, duration_ms)` records collected by `tools/build/build.py:_run_once` (each `_stage_*` invocation is wrapped with `time.monotonic()` deltas, including the new `metrics` stage from P8 step 1) and emits `dist/build-telemetry.json` containing `{schema_version, $schema, build: {platform, python}, stages: [{stage, duration_ms}], totals: {duration_ms, stages}}`. Stage names are normalised against the `ALL_STAGES` constant; unknown labels fall through unchanged so future stages don't break the schema. **Reproducibility contract — by design the file is _not_ written under `--reproducible`.** Wall-clock timings are inherently non-deterministic and would fail the SOURCE_DATE_EPOCH-style byte-identical promise that other release-time artefacts honour. The audit `tests/build/test_render_telemetry.py::test_render_skips_emission_when_reproducible` pins this; manual verification: two consecutive `tools/build/build.py --reproducible` runs produce zero `dist/build-telemetry.json` files and `dist/metrics.json` byte-identical (sha256 `0812d9a0…`). `schemas/v2/build-telemetry.schema.json` is a JSON Schema 2020-12 contract (`additionalProperties: false`, `duration_ms >= 0`, `stage` non-empty string, `totals.stages` non-negative integer matching `stages.length`). 22 new tests in `tests/build/test_render_telemetry.py` across six dimensions: pure helpers (`_normalise_stage` / `_python_version` / `build_payload`); rendering (top-level + per-stage shape, totals consistency, JSON sort-key stability); reproducibility contract; schema validation (success on canonical payload, rejection on negative durations, unknown top-level fields, missing required keys); pipeline wiring (`render_telemetry` is imported by `build.py`, `metrics` is the last entry in `ALL_STAGES`); cross-references (`SCHEMA_VERSION` matches schema's `version` const; `SCHEMA_REF` matches `$id` suffix; output filename is `build-telemetry.json`, not `telemetry.json`). Total tests: 547 → 569. **Deliberate non-feature:** the schema does not gate per-stage absolute thresholds. Build duration depends on host hardware, IO contention, and Python runtime; absolute thresholds would either flake constantly or be useless. The contract is "every non-reproducible build leaves a parseable record", not "every build must finish in N seconds". Trend analysis (regression detection) is consumer-side and lives outside this PR.
- [x] P4 — Build reproducibility audit (v9.x, 2026-05-09): closes the long-standing P4 follow-up tracker. Existing infrastructure (`tools/build/build.py --reproducible` + `SOURCE_DATE_EPOCH=$(git log -1 --format=%ct HEAD)`) already produced byte-identical output for two consecutive builds against the same git HEAD; this step makes the property *executable*. Earlier panic about "12,488 differing files" turned out to be cursor's autocommit advancing HEAD between runs; once the caller pins SOURCE_DATE_EPOCH the diff is zero. New `scripts/audit_build_reproducibility.py` (stdlib-only, three modes: default `--check` runs two builds and compares `dist/integrity.json` byte-for-byte, `--first-build-only` is a 30s smoke for cheap CI runners, `--keep` preserves both temp build trees on exit for offline diffing). The `dist/integrity.json` artifact already aggregates the sha256 of every other file in the build, so two byte-identical integrity files prove the entire tree is byte-identical (5,367,221 bytes of integrity payload at v8.0). `scripts/audit_build_reproducibility.py` pins `SOURCE_DATE_EPOCH` to `git log -1 --format=%ct HEAD` so the audit cannot flake on cursor autocommits, then runs `python3 tools/build/build.py --reproducible` twice into isolated temp dirs and compares. Audit is *not* per-PR (~90s wall-clock cost would be poison) — it runs nightly via `.github/workflows/build-reproducibility.yml` and on PRs that touch `tools/build/**`, `schemas/v2/build-info.schema.json`, `schemas/v2/integrity.schema.json`, or the audit script itself. New Makefile targets: `audit-reproducibility` (full double-build) and `audit-reproducibility-fast` (single-build smoke). 11 unit tests in `tests/scripts/test_audit_build_reproducibility.py` across four dimensions: pure helpers (`_git_commit_epoch` returns digit string, `_git_head_sha` returns 40-char hex, both raise `RuntimeError` outside a git repo, `_read_integrity` returns bytes / raises `FileNotFoundError`); CLI surface (`--help` lists all three modes, unknown flags rejected); module surface (`main` and `_run_build` exposed for future mocking); end-to-end smoke (`--first-build-only` runs to completion against the live repo, gated by `SKIP_SLOW_TESTS=1` env so tight inner-loop runs stay fast). The smoke test runs in CI and on `make test`. AGENTS.md updated with the new CI gate, new Makefile targets, and new audit row. **With this step landed, the P4 follow-up tracker is closed**; the residual "build timestamps in BUILD-INFO.json" item is *by-design* (the file is the auditable record of when a tree was generated; pinning SOURCE_DATE_EPOCH makes it stable across runs against the same HEAD, which is the contract the audit verifies).
- [x] P6 — Scripts taxonomy: Tier 1 batch 10 (gold-profile-v2 + prerequisites + sandbox-validation + sme-review-signoffs + mapping-ledger) (v9.x, 2026-05-09): closes the audit half of Tier 1 by relocating the five remaining heavyweight audits — including the two largest provenance gates in the repo. **Five audits relocated**: `scripts/audit_gold_profile_v2.py` (388 lines), `scripts/audit_prerequisites.py` (389 lines), `scripts/audit_sandbox_validation.py` (398 lines), `scripts/audit_sme_review_signoffs.py` (415 lines), `scripts/audit_mapping_ledger.py` (480 lines) → `src/splunk_uc/audits/<name>.py`. Combined LOC: 2,070 — the largest batch by line-count to land in P6 so far. Path-resolution depth widened from one level to three across all five (`pathlib.Path(__file__).resolve().parent.parent` → `parents[3]`, or four nested `os.path.dirname` calls for `audit_prerequisites`'s `os.path`-style chain). Dispatcher verb count: 38 → **43**. **Sibling-script lazy import**: `mapping_ledger` lazy-imports `scripts/generate_mapping_ledger.py` (the canonicalisation helpers, merkle root computation, and the `LedgerInput` dataclass) via a `sys.path.insert(REPO_ROOT / "scripts")` shim — the audit must use the **exact** same canonicalisation and merkle construction as the generator or any drift would be a self-inflicted bug rather than a real signal; importing keeps them locked in step. The `_SCRIPTS_DIR` insert is idempotent (guarded by `if str(_SCRIPTS_DIR) not in sys.path`). **`main()` signature alignment**: three of the five did not honour the dispatcher's `main(argv: list[str] | None = None) -> int` contract — `audit_gold_profile_v2.main()`, `audit_sandbox_validation.main()`, and `audit_sme_review_signoffs.main()` were parameterless; widened with explicit argv plumbing or `del argv` where the audit accepts no flags. `audit_prerequisites.main()` was parameterless and inspected `sys.argv` indirectly via `parse_args()`; widened to `parse_args(argv)`. `audit_mapping_ledger.main(argv: list[str])` (no `None` default, required positional) widened to `main(argv: list[str] | None = None) -> int` so the dispatcher's `resolve()` can call it with `None` when stdin args aren't passed. **Lint + type-debt cleanup uncovered by the strict src/splunk_uc/ posture**: 67 PEP-585 modernisations all auto-fixed by `ruff --fix` (the heaviest concentration in `audit_prerequisites` which carried `Dict`/`List`/`Tuple`/`Optional` imports throughout); two F401 unused imports (`Counter`, `defaultdict`, `hashlib`, `canonical_entry_payload`) dropped; one B905 explicit `strict=True` added to a `zip()` of two equal-length lists in `mapping_ledger`; one `dict | None` widened to `dict[str, Any] | None` in `sandbox_validation._load_uc_sidecar` to satisfy mypy's `type-arg`; two `len()` calls on dict-derived lists in `_classify_fixture` (the legacy fixture-shape branch and the phase2 fixture-shape branch) gained explicit `isinstance(..., list)` re-narrowing on the early-return sentinel so mypy `arg-type` is happy; one mypy `no-any-return` from `prerequisites._load_catalog` rebound to a typed local (`payload: dict[str, Any] = json.load(f); return payload`); one redundant `# type: ignore[import-not-found]` on the `import jsonschema` in `mapping_ledger` removed (the repo-wide mypy config sets `ignore_missing_imports = true`); one untyped `dict` in `gold_profile_v2.print_report(results: list[dict])` widened to `list[dict[str, Any]]`. Shim `__all__` lists sorted via `ruff --fix` (RUF022) — `prerequisites` shim re-exports `WAVE_RANK` alongside the other module-level constants the test fixtures and downstream tooling read. **Updated user-facing strings**: nine reproduction hints (CLI usage docstrings, `--check` failure messages, the sandbox-validation report's `$comment` footer, baseline-file `description`, validate.yml + release.yml workflow comments) all switched from `scripts/audit_*.py` to `python -m splunk_uc audit-*` (or `make audit-*`). The sandbox-validation `$comment` change required regenerating `reports/sandbox-validation.json` (committed alongside this batch); the only line that changed is the `$comment` reference. **Call-site consolidation**: this PR consolidates every batch-10 call-site through the dispatcher in lockstep with the migration. Five new `Makefile` targets (`audit-gold-profile-v2`, `audit-prerequisites`, `audit-sandbox-validation`, `audit-sme-review-signoffs`, `audit-mapping-ledger`) all routed via the `$(SPLUNK_UC)` macro. Five `.github/workflows/validate.yml` steps switch from `python3 scripts/audit_*.py` to `PYTHONPATH=src python3 -m splunk_uc audit-*` (`Prerequisite graph audit (wave / pre)`, `Phase 5.2 SME-review signoff audit`, `Phase 5.4 signed provenance ledger audit`, `Phase 4.5c sandbox validation gate`, `Prerequisite audit report (post-build drift)`). Two `.github/workflows/release.yml` steps (`Regenerate mapping ledger` and `Verify attested ledger end-to-end`) switch the audit invocation to the dispatcher (the generator step keeps its `scripts/generate_mapping_ledger.py` invocation since that script is queued for a Tier-2 generator batch). One `.github/PULL_REQUEST_TEMPLATE.md` bullet referencing the SME-signoff audit reproduces the dispatcher pattern. Legacy shim invocations continue to work for the soak period. **Verification**: ruff check + format + mypy clean across all 51 source files in `src/splunk_uc/` plus the 5 batch-10 shims (56 files formatted total); full pytest suite reports **613 passing tests, 1 skipped** (no count change — dispatcher tests use dynamic `all_verbs()` discovery); end-to-end smoke for every newly registered verb via the dispatcher (`audit-prerequisites --check`, `audit-sandbox-validation --check`, `audit-mapping-ledger`, `audit-sme-review-signoffs`, `audit-gold-profile-v2 --files <fixture>`) and via the legacy shim path; `python -m splunk_uc --help` lists all 43 verbs grouped by category. **With this batch landed, every audit script in `scripts/audit_*.py` is migrated** — 43 verbs in the registry, ~0 audits remaining outside the package. Tier 2 (~30 generators) is the next major migration cluster; Tier 3 (post-soak shim deletion) and Tier 4 (post-P9 wheel package) remain queued.
- [x] P6 — Scripts taxonomy: Tier 1 batch 9 (doc-counts + openapi-drift + content-quality + baseline-clause-grammar-free + peer-review-signoffs + mcp-tool-schemas) (v9.x, 2026-05-09): batch through the small-and-medium remainders so the audit half of Tier 1 is essentially done. Six audits in one PR (~801 LOC combined) move into `src/splunk_uc/audits/`. **Six audits relocated**: `scripts/audit_doc_counts.py` (47 lines), `scripts/audit_openapi_drift.py` (63 lines), `scripts/audit_content_quality.py` (75 lines), `scripts/audit_baseline_clause_grammar_free.py` (111 lines), `scripts/audit_peer_review_signoffs.py` (238 lines), `scripts/audit_mcp_tool_schemas.py` (267 lines) → `src/splunk_uc/audits/<name>.py`. Path-resolution depth widened from one level to three across all six (`pathlib.Path(__file__).resolve().parent.parent` → `parents[3]`). Dispatcher verb count: 32 → 38. **`main()` signature alignment**: four of the six audits did not honour the dispatcher's `main(argv: list[str] | None = None) -> int` contract — `audit_doc_counts.main()`, `audit_openapi_drift.main()`, `audit_baseline_clause_grammar_free.main()`, and `audit_peer_review_signoffs.main()` were all parameterless. All four widened with `del argv` (none accept CLI flags) so the dispatcher contract holds. `audit_content_quality.main()` was modernised at the same time (PEP-585 imports, `set[tuple[str, str]]` typing for the baseline). `audit_mcp_tool_schemas.main()` already conformed. **Lint + type-debt cleanup uncovered by the strict src/splunk_uc/ posture**: `audit_baseline_clause_grammar_free` had a forward-reference bug — `BASELINEABLE_FP_FIELD` was used inside `main()` but defined *after* `main()` at module bottom; hoisted the constant alongside `FORBIDDEN_CODES`. `audit_peer_review_signoffs` had four untyped `dict` annotations (`_load_json`, `_validate_schema`, `_validate_semantics`, `_print_summary`) that mypy now requires; widened to `dict[str, Any]`. `audit_mcp_tool_schemas` carried a redundant `# type: ignore[import-untyped]` on the `import jsonschema` line (the repo-wide mypy config sets `ignore_missing_imports = true`); removed. One F541 (`f"api/v1/manifest.json missing — run scripts/generate_api_surface.py"`) flagged by ruff; converted to plain string. Eight user-facing strings (CLI usage docstrings, validate.yml workflow comments, `mcp/tests/conftest.py` docstring reference) all switched from `scripts/audit_*.py` to `python -m splunk_uc audit-*` (or `make audit-*`). Shim `__all__` lists sorted via `ruff --fix` (RUF022); unused `# noqa: E402` directives stripped (RUF100; the project-level pyproject.toml already silences E402 for `scripts/**/*.py`). **Call-site consolidation**: three `.github/workflows/validate.yml` steps switch from `python3 scripts/audit_*.py` to `PYTHONPATH=src python3 -m splunk_uc audit-*` (the Phase F clause-grammar guard, the MCP drift guard, the Phase 4.5a peer-review gate). Six new `Makefile` targets (`audit-doc-counts`, `audit-openapi-drift`, `audit-content-quality`, `audit-baseline-clause-grammar-free`, `audit-peer-review-signoffs`, `audit-mcp-tool-schemas`) all routed via the `$(SPLUNK_UC)` macro. Legacy shim invocations continue to work for the soak period. **Verification**: ruff check + format + mypy clean across all 46 source files in `src/splunk_uc/`; full pytest suite reports **613 passing tests, 1 skipped** (no count change — dispatcher tests use dynamic `all_verbs()` discovery); end-to-end smoke for every newly registered verb via the dispatcher and via the legacy shim, plus all six new Makefile targets all exit 0. **Remaining ~10 untested audits in `scripts/`** (mostly heavy gold-profile + compatibility audits) queued for subsequent batches per the soak schedule in `docs/scripts-taxonomy.md`.
- [x] P6 — Scripts taxonomy: Tier 1 batch 8 (compliance-gaps + compliance-mappings) (v9.x, 2026-05-09): the heaviest audits in `scripts/` — `audit_compliance_gaps.py` (646 lines) and `audit_compliance_mappings.py` (1,426 lines), 2,072 LOC combined — move into the package. **Two compliance audits relocated**: `scripts/audit_compliance_gaps.py` → `src/splunk_uc/audits/compliance_gaps.py`, `scripts/audit_compliance_mappings.py` → `src/splunk_uc/audits/compliance_mappings.py`. Path-resolution depth widened from one level to three across both (`pathlib.Path(__file__).resolve().parents[1]` → `parents[3]`, `parent.parent` → `parents[3]`). Dispatcher verb count: 30 → 32. **Equipment-lib path shim**: `compliance_mappings` lazy-imports `scripts/equipment_lib.py` for the equipment-orphan lint via a `sys.path` insertion; the path was previously `Path(__file__).resolve().parent` (== `scripts/`). After the move the same `scripts/` directory is reached via `REPO_ROOT / "scripts"`. **`main()` signature alignment**: `audit_compliance_gaps.main(argv: Sequence[str])` (no None default, no fallback to sys.argv) widened to `main(argv: list[str] | None = None) -> int` with `sys.argv[1:]` fallback inside the body. `audit_compliance_mappings.main()` (no `argv` parameter at all, inspected sys.argv via `parser.parse_args()`) widened to the canonical signature. **Lint + type-debt cleanup uncovered by the strict src/splunk_uc/ posture**: 131 PEP-585 modernisations all auto-fixed by `ruff --fix`; two RUF005 list-concat sites rewritten to iterable unpacking; one F401 (`import jsonschema` unused at module scope) dropped; one `re.Pattern` widened to `re.Pattern[str]`; one `list | None` widened to `list[Any] | None`; two `_load_schema()` / `_validate_uc_schema()` returns annotated to honour the declared `dict[str, Any]` / `dict[str, Any] | None` types (mypy `no-any-return`); one mypy `misc` from a shadowed `err` variable in the `except / for err in errors[:N]` pair, renamed to `parse_err` (decode branch) and `schema_err` (loop). Shim `__all__` lists sorted via `ruff --fix` (RUF022). `python -m ruff format` applied to both src files. **Updated user-facing strings**: eight reproduction hints (CLI usage docstrings, `--check` failure messages, markdown report headers/footers, baseline-file `description`, validate.yml error message) all switched from `scripts/audit_compliance_*.py` to `python -m splunk_uc audit-compliance-*` (or `make audit-compliance-*`). The compliance-gaps markdown footer change required a regeneration of `docs/compliance-gaps.md`; the compliance-coverage markdown footer required a regeneration of `docs/compliance-coverage.md`; both regenerations are committed alongside this batch. **Call-site consolidation**: two new `Makefile` targets (`audit-compliance-gaps`, `audit-compliance-mappings`) invoke `$(SPLUNK_UC) <verb>`. Three `.github/workflows/validate.yml` steps switch from `python3 scripts/audit_compliance_*.py` to `PYTHONPATH=src python3 -m splunk_uc audit-compliance-*`. The baseline-drift-guard step still calls `scripts/audit_baseline_clause_grammar_free.py` because that audit is itself queued for a later batch. **Verification**: ruff check + format + mypy clean across all 39 source files in `src/splunk_uc/`; full pytest suite reports **612 passing tests, 1 skipped** (no count change — dispatcher tests use dynamic `all_verbs()` discovery); end-to-end smoke for both newly registered verbs via the dispatcher and via the legacy shims, plus both new Makefile targets all exit 0. **Remaining ~13 untested audits in `scripts/`** queued for subsequent batches per the soak schedule in `docs/scripts-taxonomy.md`.
- [x] P6 — Scripts taxonomy: Tier 1 batch 7 (regulation-alignment + nis2-no-gap + oscal-roundtrip + regulatory-change-watch) (v9.x, 2026-05-09): continues the audit-batch migration cadence and ratchets the dispatcher from 25 to 29 verbs. **Four regulatory audits relocated**: `scripts/audit_regulation_alignment.py` (71 lines), `scripts/audit_nis2_no_gap.py` (224 lines), `scripts/audit_oscal_roundtrip.py` (493 lines), `scripts/audit_regulatory_change_watch.py` (583 lines) → `src/splunk_uc/audits/<name>.py`. Path-resolution depth widened from one level to three across all four (`pathlib.Path(__file__).resolve().parents[1]` → `parents[3]`, `parent.parent` → `parents[3]`). The two larger audits in this thematic cluster (`audit_compliance_gaps.py` 646 lines + `audit_compliance_mappings.py` 1,426 lines) are deferred to batch 8 to keep the review surface manageable. **`main()` signature alignment**: `audit_regulation_alignment.main()` was `def main() -> int:` with no `argv`, widened. `audit_regulatory_change_watch.main()` had `def main(argv: Optional[List[str]] = None) -> int:` (legacy `Optional`/`List`), modernised by `ruff --fix` to PEP-585 `list[str] | None`. `audit_nis2_no_gap` and `audit_oscal_roundtrip` already conformed. **Lint + type-debt cleanup uncovered by the strict src/splunk_uc/ posture**: `regulatory_change_watch` 73 PEP-585 modernisations all auto-fixed; redundant `# type: ignore` on the `jsonschema.Draft202012Validator` import removed (the repo-wide mypy config already sets `ignore_missing_imports = true`); the `args.func(args)` call returned `Any` per mypy's strict no-Any-return rule, bound through an intermediate `int` variable. `oscal_roundtrip._load_schema()` returned `Any` flowing out of `json.loads`, explicitly typed `schema: dict[str, Any]` so the declared return type is honoured; the `for err in errors[:100]` loop variable shadowed the `except as err` binding from the JSON-decode branch, renamed to `schema_err`; the `--check` failure message updated to point at `python -m splunk_uc audit-oscal-roundtrip` (was: legacy script path). `nis2_no_gap.main()` had `payload: dict[str, list[str] | int | str]` inferred, which prevented `payload['status'].upper()` from dispatching, annotated `payload: dict[str, Any]` to honour the runtime shape. Shim `__all__` lists re-sorted via `ruff check --fix` to satisfy RUF022. **Call-site consolidation**: `Makefile` (four new targets: `audit-regulation-alignment`, `audit-nis2-no-gap`, `audit-oscal`, `audit-regulatory-change-watch`); `.github/workflows/validate.yml` (three steps: regulation-alignment, change-watch --check, oscal-roundtrip --check); `.github/workflows/regulatory-watch.yml` (the weekly fetch step) all switch from `python3 scripts/audit_*.py` to `PYTHONPATH=src python3 -m splunk_uc audit-*` (or `make audit-*`). Legacy shim invocations continue to work for the soak period. **Verification**: ruff check + format + mypy clean across all 37 source files in `src/splunk_uc/`; full pytest suite reports **612 passing tests, 1 skipped** (no count change — dispatcher tests use dynamic `all_verbs()` discovery); end-to-end smoke for every newly registered verb via the dispatcher and via the legacy shim, plus all four new Makefile targets. The `audit-regulatory-change-watch --check` retains two pre-existing soft-fail open findings (uk-gdpr + mitre-attack-enterprise) that print as warnings but do not gate. **Remaining ~15 untested audits in `scripts/`** queued for subsequent batches per the soak schedule in `docs/scripts-taxonomy.md`.
- [x] P6 — Scripts taxonomy: Tier 1 batch 6 (changelog-uc-refs + repo-consistency + catalog-schema + quality-metadata + spl-duplicates + links) (v9.x, 2026-05-09): continues the audit-batch migration cadence and ratchets the dispatcher from 19 to 25 verbs. **Six audits relocated**: `scripts/audit_changelog_uc_refs.py` (186 lines), `scripts/audit_repo_consistency.py` (277 lines), `scripts/audit_catalog_schema.py` (344 lines), `scripts/audit_quality_metadata.py` (136 lines), `scripts/audit_spl_duplicates.py` (113 lines), `scripts/audit_links.py` (248 lines) → `src/splunk_uc/audits/<name>.py`. Path-resolution depth widened from one level to three across all six (`pathlib.Path(__file__).resolve().parent.parent` → `parents[3]`, or four nested `os.path.dirname` calls for the `os.path`-style audits). `repo_consistency` retains its in-tree `sys.path` shim that lets it `from build.enrichment import CAT_GROUPS, SPLUNK_APPS` directly so the audit stays free of any catalog-build round-trip. **`main()` signature alignment**: five of the six audits did not honour the dispatcher's `main(argv: list[str] | None = None) -> int` contract — `audit_changelog_uc_refs.main()` was unannotated and parameter-free, `audit_repo_consistency.main()` / `audit_catalog_schema.main()` / `audit_spl_duplicates.main()` had `def main() -> int` without `argv`, and `audit_quality_metadata.main()` inspected `sys.argv` directly with `strict = "--strict" in sys.argv`. All five widened to the canonical `main(argv: list[str] | None = None) -> int` shape with `argparse` parsing the slice the dispatcher hands them. `audit_links.py` already used `argparse` but called `parser.parse_args()` (no argv); threaded `argv` through. **Lint + type-debt cleanup uncovered by the strict src/splunk_uc/ posture**: `repo_consistency` had `dict[str, object]` for the parsed INDEX.md categories and `list[dict[str, object]]` for SPLUNK_APPS, but mypy's `warn_unreachable` then flagged the runtime `isinstance(app, dict)` defence as unreachable; loosened to `dict[str, Any]` (categories) and `list[Any]` (SPLUNK_APPS) so the defensive checks stay meaningful. Removed redundant `# type: ignore[import-not-found]` on `from build.enrichment import ...` (the repo-wide mypy config sets `ignore_missing_imports = true`). Three EN-DASH `–` characters preserved verbatim in user-facing error strings (`expected 1–23 only`) and one in the CHANGELOG date-range regex; all marked with focused `# noqa: RUF001` comments rather than rewriting the legacy grammar. Shim `__all__` lists re-sorted via `ruff check --fix` to satisfy RUF022. **Call-site consolidation**: this PR consolidates the call-sites that previously routed through the legacy shim path. `Makefile` (three targets: `audit-links`, `audit-consistency`, `audit-spl-duplicates`), `.github/workflows/validate.yml` (four steps: `CHANGELOG and cross-references`, `Repository consistency`, `Catalog schema validation`, `Quality metadata coverage`), and `.github/workflows/link-check.yml` (the `Run link audit` step plus the failure-issue body's reproduction hint) all switch from `python3 scripts/audit_*.py` to `PYTHONPATH=src python3 -m splunk_uc audit-*` (or `make audit-*`). Legacy shim invocations continue to work for the soak period. **Verification**: `python3 -m ruff check src/splunk_uc/audits/` (clean across all 33 source files), `python3 -m ruff format --check src/splunk_uc/` (clean), `PYTHONPATH=src python3 -m mypy src/splunk_uc/` (clean across 33 source files), `PYTHONPATH=src:mcp/src python3 -m pytest -q` (**613 passed**, 0 failed — same count as batch 5 because the dispatcher registry test asserts only registry shape, not absolute count). End-to-end smoke for every newly registered verb plus backward-compatibility smoke for the corresponding legacy shim paths plus Make-target smoke for each updated target. **Remaining ~19 untested audits in `scripts/`** queued for subsequent batches per the soak schedule in `docs/scripts-taxonomy.md`.
- [x] P6 — Scripts taxonomy: Tier 1 batch 5 (design-doc-freshness + uc-ids + splunkbase-ids + known-fp + non-technical-sync + monitoring-type) (v9.x, 2026-05-09): folds the remaining CI-critical audits into the package and ratchets the dispatcher from 13 to 19 verbs. **Six audits relocated**: `scripts/audit_design_doc_freshness.py` (109 lines), `scripts/audit_uc_ids.py` (115 lines), `scripts/audit_splunkbase_ids.py` (135 lines), `scripts/audit_known_fp.py` (162 lines), `scripts/audit_non_technical_sync.py` (212 lines), `scripts/audit_monitoring_type.py` (253 lines) → `src/splunk_uc/audits/<name>.py`. Path-resolution depth widened from one level to three across all six (`pathlib.Path(__file__).resolve().parent.parent` → `parents[3]`, or four nested `os.path.dirname` calls for the `os.path`-style audits). **`main()` signature alignment**: three of the six did not honour the dispatcher's `main(argv: list[str] | None = None) -> int` contract — `audit_uc_ids` inspected `sys.argv` directly with `if "--warn-gaps" in sys.argv`, replaced with `argparse`; `audit_non_technical_sync` returned `None`, widened to return `int` (always 0); `audit_splunkbase_ids` had no `argv` parameter, widened. The other three (`audit_design_doc_freshness`, `audit_known_fp`, `audit_monitoring_type`) already used `argparse` and only required threading `argv` into `parse_args()`. **Lint cleanup uncovered by the strict src/splunk_uc/ posture**: `audit_uc_ids` had `for a, b in zip(z_set, z_set[1:], strict=False)` which RUF007 flags as a `pairwise` candidate; rewritten to `from itertools import pairwise` + `pairwise(z_set)`; `audit_splunkbase_ids` had two B007 violations (`for nm in NEARBY_NAME_RE.finditer(window): pass` to "take the last match"); rewritten to materialise `list(...)` and select `[-1]` with a comment explaining the semantics. Shim files have unused `# noqa: E402` directives stripped because `pyproject.toml` already silences `E402` for `scripts/**/*.py`. **Call-site consolidation across all earlier batches**: this PR also retroactively applies step 6 of the migration recipe (route call-sites through the dispatcher in the same PR as the migration) to every previously-migrated batch. `Makefile` (six targets: `audit-structure`, `audit-cim`, `audit-placeholders`, `audit-mitre`, `audit-ntv`, `audit-ids`, `audit-monitoring-type`), `.github/workflows/validate.yml` (eleven steps), and `.github/PULL_REQUEST_TEMPLATE.md` (one bullet) all switch from `python3 scripts/audit_*.py` to `python -m splunk_uc audit-*` (or `make audit-*`). Legacy shim invocations continue to work for the soak period. **Verification**: `python3 -m ruff check src/splunk_uc/audits/` (clean across 27 modules), `python3 -m ruff format --check` (clean), `PYTHONPATH=src python3 -m mypy src/splunk_uc/` (clean across 27 source files), `PYTHONPATH=src:mcp/src python3 -m pytest -q` (**613 passed**, 0 failed — one more than the baseline because the dispatcher registry test asserts the verb count). End-to-end smoke for every newly registered verb plus backward-compatibility smoke for the corresponding legacy shim paths plus Make-target smoke for each updated target. **Remaining ~25 untested audits in `scripts/`** queued for subsequent batches per the soak schedule in `docs/scripts-taxonomy.md`.
- [x] P6 — Scripts taxonomy: Tier 1 batch 4 (cim-spl-alignment + legal-review-signoffs + regulatory-primer + mitre-taxonomy + placeholders) (v9.x, 2026-05-09): scales up to larger batches now that every audit-with-tests is migrated. **Five untested audits relocated en masse**: `scripts/audit_cim_spl_alignment.py` (290 lines), `scripts/audit_legal_review_signoffs.py` (292 lines), `scripts/audit_regulatory_primer.py` (310 lines), `scripts/audit_mitre_taxonomy.py` (317 lines), `scripts/audit_placeholders.py` (319 lines) → `src/splunk_uc/audits/<name>.py`. Five new verbs registered: `audit-cim-spl-alignment`, `audit-legal-review-signoffs`, `audit-regulatory-primer`, `audit-mitre-taxonomy`, `audit-placeholders`. The dispatcher now exposes 13 audit verbs total. **`main()` signature alignment**: two of the five (`cim_spl_alignment`, `legal_review_signoffs`) had `def main() -> int` without an `argv` parameter; both are widened to `main(argv: list[str] | None = None) -> int`. `cim_spl_alignment` threads `argv` into `parse_args()`; `legal_review_signoffs` accepts no flags so it documents the parameter with `del argv`. **Lint + type-debt cleanup uncovered by the strict src/splunk_uc/ posture**: `audit_mitre_taxonomy.audit_uc_json(path, data: dict)` → `dict[str, Any]` (with `from typing import Any`); the `with open(...) as f:` / `for f in all_findings:` shadowing in `mitre_taxonomy` and `regulatory_primer` is fixed by renaming the file handle to `fh`; four untyped `dict` declarations in `legal_review_signoffs` (`_load_json`, `_validate_schema`, `_validate_semantics`, `_print_summary`) become `dict[str, Any]`; one RUF001 (ambiguous EN/EM DASH in a deliberate-by-design literal set) suppressed with a focused `# noqa: RUF001` and explanatory comment in `placeholders`; one B007 (unused loop variable `body`) renamed to `_body`. Ruff `--fix` rewrote 69 additional cosmetic issues (`Dict`/`Optional`/`List` → PEP-585 builtins). **Verification**: `python -m splunk_uc audit-mitre-taxonomy --check` scans all 7,677 UCs in ~1.3s end-to-end via the dispatcher; `python -m splunk_uc audit-cim-spl-alignment --help` correctly surfaces the verb's argparse interface; 612 tests pass under `SKIP_SLOW_TESTS=1`; ruff, format, mypy all clean across the touched surface (21 source files in `src/splunk_uc/` plus 5 shims).
- [x] P6 — Scripts taxonomy: Tier 1 batch 3 (uc-structure + dashboard-spl) (v9.x, 2026-05-09): completes the migration of every audit script that still had a dedicated test suite. **`scripts/audit_uc_structure.py`** (572 lines) → `src/splunk_uc/audits/uc_structure.py`; **`scripts/audit_dashboard_spl.py`** (508 lines) → `src/splunk_uc/audits/dashboard_spl.py`. Two new verbs registered: `audit-uc-structure` ("Validate UC markdown corpus + JSON sidecars for structural correctness") and `audit-dashboard-spl` ("Audit Simple XML dashboard SPL queries for token expansion + REST validity"). The dispatcher now exposes eight audit verbs total. **`main()` signature alignment**: both `main()` entrypoints gain the `argv: list[str] | None = None` parameter the dispatcher contract requires; the legacy CLI behaviour (`argparse.parse_args()` falling back to `sys.argv[1:]`) is preserved when `argv=None`. **Path-resolution depth**: two function-local `Path(__file__).resolve().parent.parent` chains in `dashboard_spl` and one module-level `os.path.dirname(os.path.dirname(os.path.abspath(__file__)))` chain in `uc_structure` are widened to `parents[3]` / four nested `dirname` calls to reach the repo root from the new four-level-deep home. **Type-debt cleanup**: `audit_uc_structure` carried pre-existing untyped generics (`dict`, `set`, `list[tuple[str, dict]]`) tolerated under the legacy `scripts/` mypy posture but flagged under the strict `src/splunk_uc/` posture after ruff's auto-fix from `Dict`/`Optional` to PEP-585 builtins; each is now parametrised (`dict[str, str]` for the field-line table, `set[str]` for the baseline reader, `dict[str, Any]` for raw JSON payloads). The legacy `scripts/audit_uc_structure.py` and `scripts/audit_dashboard_spl.py` become 80-line and 50-line shims respectively, re-exporting the public CLI plus all the dataclasses (`UCParse`, `TokenSpec`, `Panel`, `AuditResult`, `Splunkd`), regex/field tables, validators, and helpers that the test suites read or instantiate directly. **Verification**: `python -m splunk_uc audit-uc-structure --source json --baseline /dev/null` and `python -m splunk_uc audit-dashboard-spl --help` both run end-to-end via the dispatcher; 24 audit-specific tests (13 uc_structure + 11 dashboard_spl) plus 21 dispatcher tests = 45 focused tests pass; full suite is 612 passing under `SKIP_SLOW_TESTS=1`; ruff/format/mypy clean across the touched surface.
- [x] P6 — Scripts taxonomy: Tier 1 batch 2 (legacy-orphans + coverage-budget + action-pins) (v9.x, 2026-05-09): scales the migration pattern across audits with diverse test shapes. **`scripts/audit_legacy_orphans.py`** (192 lines) → `src/splunk_uc/audits/legacy_orphans.py`; **`scripts/audit_coverage_budget.py`** (389 lines) → `src/splunk_uc/audits/coverage_budget.py`; **`scripts/audit_action_pins.py`** (310 lines) → `src/splunk_uc/audits/action_pins.py`. All three relocations adjust the path-resolution depth from `parent.parent` / `parents[1]` to `parents[3]` to reflect the new on-disk layout. Three new verbs registered: `audit-legacy-orphans` ("Diagnose UCs in the legacy use-cases/ tree without a JSON SSOT sidecar"), `audit-coverage-budget` ("Per-file pytest-cov ratchet for tier-1 + tier-2 paths"), and `audit-action-pins` ("Verify GitHub Actions SHA pins still match their claimed tag"). The dispatcher now exposes six audit verbs total. **Test fixture work**: the `_StubPath` helper in `test_audit_action_pins.py` is updated to expose four parent levels (`audits/`, `splunk_uc/`, `src/`, repo root) instead of two so the synthetic-workflow-tree redirect still resolves correctly with `Path(__file__).resolve().parents[3]`; the four `monkeypatch.setattr(aap, "__file__", ...)` calls have their synthetic path updated from `<workflow_tree>/scripts/audit_action_pins.py` to `<workflow_tree>/src/splunk_uc/audits/action_pins.py` to match. **Lint cleanup**: `audit_action_pins.py` had a pre-existing PIE810 violation (`startswith("./") or startswith(".")`) that was tolerated under `scripts/` but flagged under `src/splunk_uc/`; merged into a single `startswith(("./", "."))` call with identical semantics. The legacy `scripts/audit_*.py` files become 30-to-60-line shims that put `src/` on `sys.path`, re-export the public names plus all module-level constants the test fixtures monkeypatch (`LEGACY_ROOT`, `SSOT_ROOT`, `TIER_*` regex tuples, `_TransientError`), and forward `__main__` to `main()`. **Coverage budget contract preserved**: `TIER_2_INCLUDES = ^scripts/audit_.*\.py$` still matches the shims, and the shim bodies are trivially-covered re-export glue, so the per-file coverage gate continues to pass without a baseline rewrite. Tracking `src/splunk_uc/audits/**` in the budget is deferred to a follow-up PR after more migrations land. **Verification**: `make audit-roadmap`, `make audit-license-inventory`, `python -m splunk_uc audit-legacy-orphans --check`, `python -m splunk_uc audit-coverage-budget --help` all run end-to-end via the dispatcher; 612 tests pass; ruff/format/mypy clean across the touched surface (15 changed files).
- [x] P6 — Scripts taxonomy: Tier 1 batch 1 (roadmap + license-inventory) (v9.x, 2026-05-09): proves the migration pattern at scale by relocating two more audit scripts to the `splunk_uc.audits` package. **`scripts/audit_roadmap_consistency.py`** (651 lines) → `src/splunk_uc/audits/roadmap_consistency.py`; **`scripts/audit_license_inventory.py`** (965 lines) → `src/splunk_uc/audits/license_inventory.py`. Both relocations adjust `Path(__file__).resolve().parent.parent` to `parents[3]` (the audit module is now three levels deep at `src/splunk_uc/audits/<name>.py` instead of one at `scripts/<name>.py`). Two new verbs registered: `audit-roadmap-consistency` ("Lint ROADMAP.md (sections, links, version drift) + emit JSON snapshot") and `audit-license-inventory` ("Verify data/license-inventory.json matches live pyproject + vendored LICENSE files"). The legacy `scripts/audit_roadmap_consistency.py` and `scripts/audit_license_inventory.py` become 70-line / 80-line shims that put `src/` on `sys.path` and re-export the public surface plus every module-level constant the test suite monkeypatches (`REPO_ROOT`, `_PYPROJECT_FILES`, `_INVENTORY_PATH`, `_INVENTORY_MD_PATH`, `VERSION_FILE`, `CHANGELOG_MD`, the `build_inventory` callable). **Test fixture pattern made explicit**: both `tests/scripts/test_audit_roadmap_consistency.py` and `tests/scripts/test_audit_license_inventory.py` now load the audit module via `import splunk_uc.audits.<name> as impl` (with the legacy `importlib.util.spec_from_file_location` path preserved as a fallback for unpacked sdists missing the `src/` tree). This is the documented contract in `docs/scripts-taxonomy.md`: tests that monkeypatch module-level state must reach the **implementation** module — patching the shim does not propagate into the implementation's function closures because re-exports are bound at import time. Total tests unchanged: 612 passing under `SKIP_SLOW_TESTS=1`. Makefile targets `audit-roadmap`, `export-roadmap`, `audit-license-inventory`, and `write-license-inventory` switch to the `$(SPLUNK_UC)` dispatcher macro; the per-PR dispatcher smoke test in `validate.yml` exercises the new verbs implicitly via the registry-iteration test in `tests/splunk_uc/test_dispatcher.py`. Lint, format, and mypy all clean across the 4 new/modified Python files (`src/splunk_uc/audits/roadmap_consistency.py`, `src/splunk_uc/audits/license_inventory.py`, the two shims, the two test files). New "Migrated verbs" table in `docs/scripts-taxonomy.md` records each verb's implementation module + migration date so the migration history is discoverable. **Deliberate non-features:** does not migrate the stewardship workflow's `python3 scripts/audit_roadmap_consistency.py` invocation (the shim continues to work; that workflow change ships in a future PR alongside the P6 Tier 3 shim deletion); does not bump test counts (the new behaviour is tested by the existing test suites which now route through the implementation modules); does not migrate the remaining ~18 audit scripts (each is its own focused PR per the Tier 1 cadence in `docs/scripts-taxonomy.md`).
- [x] P6 — Scripts taxonomy: package skeleton + dispatcher + first migration (v9.x, 2026-05-09): kicks off the long-term reorganisation of `scripts/` into `src/splunk_uc/{audits,generators,migrations,ingest,feasibility}/`. **Tier 0 of the migration ships in this PR**: the package skeleton, the unified `python -m splunk_uc <verb>` dispatcher, a verb registry (`src/splunk_uc/_registry.py`), and one canonical migration (`scripts/audit_build_reproducibility.py` → `src/splunk_uc/audits/build_reproducibility.py`) to validate the pattern. The legacy `scripts/audit_build_reproducibility.py` becomes a thin shim that puts `src/` on `sys.path` and re-exports the public names, so every existing CI workflow, Makefile target, test, and ad-hoc maintainer invocation keeps working unchanged. The dispatcher is **lazy**: importing `splunk_uc.__main__` does not import any verb's implementation until the user invokes that verb, so future migrations can add 100+ verbs without inflating `--help` import cost. Verb resolution goes through the registry's `resolve()` (called by name lookup, not import binding) so monkeypatching tests have a clean seam. New CI gate in `validate.yml`: `PYTHONPATH=src python3 -m splunk_uc --help && --version` runs sub-second on every PR so any regression in the package skeleton or the registry surfaces immediately rather than waiting for nightly. The `build-reproducibility.yml` workflow now invokes `python -m splunk_uc audit-reproducibility --keep` instead of the `scripts/...` shim — exercising the dispatcher continuously so the migration cannot bit-rot. New `Makefile` macro `SPLUNK_UC := PYTHONPATH=src $(PYTHON) -m splunk_uc` plus targets `splunk-uc`, `splunk-uc-help`; `audit-reproducibility` and `audit-reproducibility-fast` now go through the dispatcher. 21 new tests in `tests/splunk_uc/test_dispatcher.py` across seven dimensions: registry shape (every registered verb resolves to a real `main` callable; duplicate registration raises; `by_category` groups by subpackage label); help formatting (`--help`, `-h`, no-args, `--version`, `-V`); error handling (unknown verb exits 2, category-name typo exits 2 with hint); argv forwarding (verb args reach the implementation verbatim); exit-code propagation (verb's non-zero return propagated); lazy import (resolving an audit verb does NOT import `splunk_uc.ingest|generators|migrations|feasibility` — verified in a subprocess with a clean `sys.modules` baseline); package surface (every subpackage imports cleanly). Existing `tests/scripts/test_audit_build_reproducibility.py` updated by one line: monkeypatching `PROJECT_ROOT` now patches the **implementation** module (`splunk_uc.audits.build_reproducibility`) rather than the shim, because shim re-exports don't propagate into the implementation's closure — this is the canonical pattern documented in `docs/scripts-taxonomy.md` for future migrations. Total tests: +21 (591 → 612 under `SKIP_SLOW_TESTS=1`). New maintainer runbook: `docs/scripts-taxonomy.md` covers the layout, invocation (both `python -m splunk_uc <verb>` and the legacy shim path), the migration recipe, the verb registry contract, the CI gates, the soak schedule (Tier 0 lands now; Tier 1 migrates remaining audits in subsequent PRs; Tier 2 migrates generators, ingest, migrations, feasibility; Tier 3 deletes the legacy shims after one minor release of soak), and the test contract. AGENTS.md updated with the new CI gate row, new `make splunk-uc-help` quick command, and runbook in "Further reading". **Tier 1 onwards is explicit-future-PR work** — we do not migrate the 120 remaining scripts in one shot because each migration is a focused change (move body, adjust path constants, write shim, register verb, update tests) and a single PR should not touch every script and every CI workflow at once. **Deliberate non-features for Tier 0:** does not move `tools/build/` into `splunk_uc.build` (the build pipeline has its own established package; that consolidation is P9 monorepo work); does not yet `pip install -e .` the package in CI (the `PYTHONPATH=src` form keeps the build pipeline's "stdlib-only" ergonomics intact and matches how every other Python tooling in the repo runs); does not delete the legacy `scripts/audit_build_reproducibility.py` (the shim is load-bearing for downstream callers during the soak window). With Tier 0 landed, the dispatcher is the canonical entry point for future migrations; subsequent PRs are routine.
- [x] P8 — Weekly stewardship digest (v9.x, 2026-05-09): closes the final subtask of repo-overhaul plan §P8 (observability). The previous three deliverables (build-time metrics, history snapshots, build telemetry) ship raw signal; this one distils the signal into a maintainer-readable artefact answering three questions in one place: *what changed in the catalogue since last release?* (counts, quality-tier mix, coverage, top movers in regulations / MITRE / CIM / equipment leaderboards); *which audits are warning but not blocking?* (today only `audit_roadmap_consistency.py` soft-fails; future soft-failable audits are added with a single `--audit-warning name=message` flag, no schema change); *which UCs are overdue for review?* (every `content/cat-*/UC-*.json` whose `lastReviewed` is older than `--stale-threshold-days`, default 180; sidecars missing the field are surfaced first, treated as max-stale via a sentinel age of `threshold + 1`). `scripts/generate_stewardship_digest.py` is a stdlib-only generator: reads `dist/metrics.json` (current) plus the highest-versioned snapshot strictly less than the current `catalogueVersion` from `data/metrics-history/*.json` (previous), walks `content/**/UC-*.json` for the staleness check, and emits `dist/stewardship-digest.json` (schema-validated) + `dist/stewardship-digest.md` (human-readable twin). **Reproducibility contract:** deterministic given fixed inputs and a fixed `--reference-date`. The CI smoke test pins `--reference-date 2026-05-09` so two consecutive runs produce byte-identical artefacts; the weekly workflow omits the flag so the issue carries today's date. `generatedAt` is derived from `--reference-date` at fixed UTC midnight, never from wall-clock `now()`. `schemas/v2/stewardship-digest.schema.json` is a JSON Schema 2020-12 contract (`additionalProperties: false`, `schema_version: "1.0.0"`, `topMovers.<axis>` capped at 10 entries, `staleUseCases.topStale` capped at 20). The digest is **derived** — it is *not* committed to the repo; the weekly workflow uploads it as a workflow artifact and posts the markdown into a tracking GitHub issue (label `stewardship-digest`). New `.github/workflows/stewardship.yml` runs every Monday 08:00 UTC; it threads `WARN :` lines from `audit_roadmap_consistency.py --check` through `--audit-warning` so soft-failed audits cannot rot silently. PR `validate.yml` runs the generator with a fixed reference date and validates against the schema as a smoke test. `scripts/generate_stewardship_digest.py` integrates with the existing `scripts/snapshot_metrics.py` archive and the existing `data/metrics-history/index.json` index — no new persistent state. 55 new tests in `tests/scripts/test_generate_stewardship_digest.py` across seven dimensions: pure helpers (`_parse_iso_date` / `_semver_key` strips both `-prerelease` and `+buildmetadata` / `_previous_snapshot` picks highest version strictly less than current / `_snapshot_counts` / `_quality_tiers` / `_coverage_block` / `_leader_block` / `_delta_dict` / `_coverage_shifts` first-release special case / `_top_movers` sort stability / `_all_top_movers` empty-on-first-release); stale-UC detection (sentinel age for missing `lastReviewed`, byCategory counts, ageDays sort); end-to-end `build_digest` against synthetic fixtures (first-release with previous=null AND second-release with deltas); audit-warning capture (`_parse_audit_warnings` extracts `WARN :` lines from stderr); markdown rendering (`render_markdown` shape, "no previous" fallback rendering, stale section sort); JSON Schema gates (canonical payload, missing required fields, unknown top-level keys); CLI plumbing (`main` exit codes for missing inputs, malformed `--audit-warning`, malformed `--reference-date`, malformed `--stale-threshold-days`). Total tests: 569 → 624. New maintainer runbook: `docs/stewardship-digest.md` covers regeneration, the reproducibility contract, the weekly workflow, the PR CI gate, the field reference, and the `schema_version` bump policy. New Makefile target `stewardship-digest`. AGENTS.md: artefact added to "Key entry points", new CI gate added, new `make stewardship-digest` quick command, runbook added to "Further reading". **Deliberate non-features:** the digest does not include per-UC diffs (it would balloon to thousands of entries; per-UC drift is the catalogue's normal state, not a stewardship signal); does not gate on absolute thresholds (e.g. "fail if >5,000 stale UCs" — staleness is informational, not a release blocker; tighten via `--stale-threshold-days` or future per-status thresholds when the catalogue stabilises); does not commit the artefact to the repo (it's derived from `dist/metrics.json` + `data/metrics-history/` + `content/`, all of which are already source-controlled). With this step landed, **P8 (Observability suite) is complete**.

## Policy

- No new code should reference `use-cases/` as a primary data source.
- New UCs are created only in `content/`.
- The `use-cases/` tree will be archived (read-only) once all dependencies
  above are resolved and the cimModels backfill closes.
