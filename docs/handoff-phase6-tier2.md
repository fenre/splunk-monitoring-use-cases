# Phase 6 — Handoff (retired)

> **Phase 6 is closed.** This document is preserved as a historical
> record of the Tier 1 + Tier 2 migration. It is no longer maintained.

## Status: closed (2026-05-11, v8.2.0)

All four tiers of the scripts-taxonomy reorganisation are complete:

- **Tier 1 (audits)** — closed 2026-05-09 across batches 1–12. Every
  full-body `scripts/audit_*.py` migrated to `src/splunk_uc/audits/`.
- **Tier 2 (generators + ingest + feasibility + migrations + tools)**
  — closed 2026-05-10 across batches 1–11. Every committed, recurring
  script reachable through the dispatcher.
- **Tier 3 (shim deletion)** — closed 2026-05-11 in v8.2.0. All 85
  legacy `scripts/*.py` compatibility shims (84 verb shims + 1 ingest
  helper) deleted. 128 caller files rewired in a single sweep to
  invoke `python3 -m splunk_uc <verb>` directly.
- **Tier 4 (packaging infrastructure)** — closed 2026-05-11 in v8.2.0.
  `pyproject.toml` configured for Hatchling wheel + sdist; the
  `splunk-uc` console script is wired via `[project.scripts]`.
  `pip install -e .` exposes the dispatcher on `$PATH`. No PyPI
  publish today; that toggle is one-line work for a future release.

## Where the live documentation lives now

- **Layout, invocation, registry contract, migration eligibility rule**
  → [`docs/scripts-taxonomy.md`](scripts-taxonomy.md)
- **Phase 6 closure narrative** → [`CHANGELOG.md`](../CHANGELOG.md)
  under the `## [8.2.0]` heading
- **CI smoke / dispatcher contract** →
  [`tests/splunk_uc/test_dispatcher.py`](../tests/splunk_uc/test_dispatcher.py)
- **Migration history (per-batch deltas)** →
  [`docs/migration-status.md`](migration-status.md)

## Final state at closure

- **83 verbs** registered (`splunk_uc._registry._REGISTRY`):
  49 audits + 16 generators + 6 ingest + 4 feasibility + 3 migrations
  + 5 tools. Authoritative count emitted by `all_verbs()` at HEAD.
- **76 Python files remain in `scripts/`** by the migration eligibility
  rule: underscore one-shots, tier-uplift content fixers, burndown
  helpers, snapshot tools, gitignored Splunk-deployment generators,
  the `equipment_lib.py` library helper, and the markdown-corpus
  documentation tools (`generate_backlinks.py`,
  `generate_doc_references.py`, `audit_auto_gen_provenance.py`).
- **0 legacy verb shims remain.** The `python3 scripts/<verb>.py`
  invocation form is **dead**; every caller goes through the
  dispatcher (or the `splunk-uc` console script after editable
  install).

There is nothing left to hand off.

---

<!-- BEGIN-AUTOGENERATED-SOURCES -->

## References

*Auto-generated by `scripts/generate_doc_references.py` from `data/source-references.json` and `data/source-mappings.json`. Edit those files (or the document body) to change citations; this footer is rewritten on every run.*

_No external sources are cited inline in this document and no repository documents currently link to it._

<!-- END-AUTOGENERATED-SOURCES -->
