# Known issues for `scripts/audit_uc_structure.py --source json --full`.
#
# Each line is the EXACT issue string emitted by the audit (UC-id +
# repo-relative path + reason). Lines beginning with '#' or blank lines
# are ignored.
#
# Today's snapshot (2026-05-08) captures the migration gap surfaced in
# repo-overhaul plan §P1 step 3:
#
#   1,785 UCs missing or empty `cimModels` in their canonical sidecar
#   under `content/cat-*/UC-*.json`. The legacy markdown corpus under
#   `use-cases/cat-*.md` has the data; the v7.0 JSON migration script
#   did not capture it for these UCs. Closing this gap is the work
#   of P1 step 7 (use-cases burndown).
#
# When you fix one of these UCs (typically by adding/populating
# `cimModels` in the sidecar), DELETE the corresponding line from this
# file in the same PR. The audit will tell you which lines are now
# fixed (under "FIXED BASELINE LINES") so the burndown is mechanical.
#
# Refresh procedure:
#   python3 scripts/audit_uc_structure.py --source json --full \
#     --print-baseline > data/audit-baselines/uc-structure-json.txt
#
# Never expand this baseline without a documented reason in the PR.
# CI calls the audit without --print-baseline and fails on any line
# that is in the audit output but NOT in this file.

