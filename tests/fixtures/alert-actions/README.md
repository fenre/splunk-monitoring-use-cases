# Alert action template golden fixtures (Task H-4)
#
# Three representative UCs (high / medium / low criticality) × three
# template kinds (SOAR YAML, HTML email, plain-text email). CI runs
# ``generate-alert-actions --check --limit 20`` against these files.
