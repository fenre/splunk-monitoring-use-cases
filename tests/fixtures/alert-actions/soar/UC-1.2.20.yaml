# Splunk SOAR playbook stub — generated from catalogue metadata.
# Import manually into SOAR; customise escalation contacts before production use.
name: "FP Triage: Print Spooler Issues"
uc_id: "UC-1.2.20"
uc_url: "https://fenre.github.io/splunk-monitoring-use-cases/uc/UC-1.2.20/"
description: |
  Print spooler crashes affect print services and have historically been attack vectors (PrintNightmare). Monitoring catches both operational and security issues.

  Business value: Print outages hit real work; the same component has been abused in the past, so this is both an ops and a security view.
human_acknowledgement_required: true
escalation_contact_placeholder: "{customer-on-call}"
pre_conditions: |
  Human-in-the-loop required for OT/safety-critical actions. Confirm alert is not a known maintenance window or approved test activity.
do_not_section: |
  No OT/safety-specific prohibitions apply to this use case. Follow standard change-management for any containment action.
spl_reference: "index=wineventlog source=\"WinEventLog:Microsoft-Windows-PrintService/Operational\" (EventCode=372 OR EventCode=805 OR EventCode=842)\n| stats count by host, EventCode\n| sort -count"
mitre_tags:
  []
regulatory_tags:
  []
playbook_contract:
  start: filter_and_decide
  branches:
    - investigate
    - notify
    - end
  notes: |
    One playbook per UC alert. Mirror the atomic runbook for UC-1.2.20.
    Never execute OT/safety-impacting containment without human approval.
