{
  "_fixture": "upstream-wrong-schema",
  "kind": "totally-unrelated-document",
  "items": [
    {"foo": 1, "bar": "baz"},
    {"foo": 2, "bar": "qux"}
  ],
  "note": "Valid JSON, valid HTTP 200, but no apiVersion / catalogueVersion / sourcetypes / cim / apps / sb keys. The JS must detect the missing top-level keys and refuse to mutate STATE.indexes."
}
