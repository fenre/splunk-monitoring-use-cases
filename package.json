{
  "name": "splunk-monitoring-use-cases",
  "private": true,
  "description": "Tooling scripts for the Splunk Monitoring Use Cases catalog. Runtime is static HTML + JSON; this package.json exists only for Node-based tooling (headless screenshots, JSON-schema validation).",
  "dependencies": {
    "puppeteer": "^24.40.0"
  },
  "devDependencies": {
    "ajv": "^8.18.0",
    "ajv-formats": "^3.0.1",
    "axe-core": "4.11.3",
    "jsdom": "29.0.2"
  },
  "scripts": {
    "validate-oscal": "node scripts/feasibility/oscal_validate.mjs"
  }
}
