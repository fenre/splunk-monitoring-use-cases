{
  "$schema": "../../schemas/uc.schema.json",
  "id": "22.10.16",
  "title": "Encryption of ePHI at Rest — TDE / BitLocker Status for Clinical Datastores",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "compliance": [
    {
      "regulation": "HIPAA Security",
      "version": "2013-final",
      "clause": "§164.312(a)(2)(iv)",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Auto-migrated from cat-22 markdown on Phase 1.3 run. Requires SME review to confirm clause accuracy, mode (satisfies vs detects-violation-of), and assurance level (contributing/partial/full).",
      "provenance": "maintainer"
    }
  ],
  "dataSources": "`mssql:audit` or scripted input `sqlserver:tde_status`, `WinEventLog:Microsoft-Windows-BitLocker/Operational`",
  "app": "Splunk Add-on for Microsoft SQL Server (Splunkbase 2648), Splunk Add-on for Microsoft Windows (742)",
  "spl": "index=database sourcetype=\"sqlserver:tde_status\" earliest=-1d\n| eval encrypted=if(match(lower(encryption_state),\"encrypted|on\"),1,0)\n| lookup ephi_sql_instances.csv instance_name OUTPUT contains_phi, owner_team\n| where contains_phi=\"true\" AND encrypted=0\n| stats latest(_time) as last_check by instance_name, database_name, owner_team",
  "value": "Surfaces SQL Server instances hosting Clarity that lack Transparent Data Encryption or hosts missing full-disk encryption so at-rest ePHI protections meet the addressable implementation specification evidence bar.",
  "implementation": "(1) Collect TDE via DB Connect scheduled statement against `sys.databases`; (2) ingest BitLocker compliance from SCCM/Intune if available; (3) track exceptions with approved risk acceptance IDs.",
  "visualization": "Table (instance, DB, encryption state), Single value (non-compliant count).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Add-on for Microsoft SQL Server",
      "url": "https://splunkbase.splunk.com/app/2648"
    },
    {
      "title": "Splunk Add-on for Microsoft Windows",
      "url": "https://splunkbase.splunk.com/app/742"
    }
  ],
  "knownFalsePositives": "Administrative tasks, scheduled jobs or platform updates can match this pattern — correlate with change management, maintenance windows and user role before raising severity.",
  "equipment": [
    "db_connect",
    "mssql"
  ]
}
