{
  "$schema": "../../schemas/uc.schema.json",
  "id": "22.14.72",
  "title": "Session Authenticity — Token Replay Across Geographies",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "compliance": [
    {
      "regulation": "NIST 800-53",
      "version": "Rev. 5",
      "clause": "SC-23",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Auto-migrated from cat-22 markdown on Phase 1.3 run. Requires SME review to confirm clause accuracy, mode (satisfies vs detects-violation-of), and assurance level (contributing/partial/full).",
      "provenance": "maintainer"
    }
  ],
  "dataSources": "`ms:o365:management` token events if available, or proxy OAuth logs",
  "app": "Splunk Add-on for Microsoft Office 365 (Splunkbase 4055)",
  "spl": "index=o365 sourcetype=ms:o365:management earliest=-24d Operation=\"Token issued*\"\n| iplocation ClientIP\n| stats earliest(_time) as t1, latest(_time) as t2, values(Country) as countries, values(ClientIP) as ips by UserId, SessionId\n| eval dt=t2-t1\n| where mvcount(countries)>1 AND dt < 7200",
  "value": "Detects same refresh token used from distant geolocations within short windows for SC-23 session authenticity.",
  "implementation": "(1) Confirm operation strings; (2) reduce noise with corporate travel; (3) integrate with IR-4; (4) force re-auth playbooks.",
  "visualization": "Table (suspected replay), Map (IPs), Timeline",
  "cimModels": [
    "Authentication"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Authentication.Authentication by Authentication.action, Authentication.user, Authentication.src | sort - count",
  "references": [
    {
      "title": "Splunk Add-on for Microsoft Office 365",
      "url": "https://splunkbase.splunk.com/app/4055"
    },
    {
      "title": "CIM: Authentication",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Authentication"
    }
  ],
  "knownFalsePositives": "Administrative tasks, scheduled jobs or platform updates can match this pattern — correlate with change management, maintenance windows and user role before raising severity.",
  "equipment": [
    "m365"
  ]
}
