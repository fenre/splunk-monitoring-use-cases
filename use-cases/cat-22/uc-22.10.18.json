{
  "$schema": "../../schemas/uc.schema.json",
  "id": "22.10.18",
  "title": "Integrity — Unexpected UPDATE/DELETE on PHI Tables (Clarity/Caboodle)",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "compliance": [
    {
      "regulation": "HIPAA Security",
      "version": "2013-final",
      "clause": "§164.312(c)(1)",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Auto-migrated from cat-22 markdown on Phase 1.3 run. Requires SME review to confirm clause accuracy, mode (satisfies vs detects-violation-of), and assurance level (contributing/partial/full).",
      "provenance": "maintainer"
    }
  ],
  "dataSources": "`index=mssql` `sourcetype=mssql:audit` (`statement`, `schema_name`, `object_name`, `application_name`, `session_server_principal_name`)",
  "app": "Splunk DB Connect (2686), Splunk Add-on for Microsoft SQL Server (2648)",
  "spl": "index=mssql sourcetype=\"mssql:audit\" earliest=-24h action_class=\"DML\"\n| where match(lower(object_name),\"pat|patient|phi|epic\") AND match(lower(statement),\"update|delete\")\n| eval is_bulk=if(match(statement,\"(?i)where\\s+1=1|truncate|delete\\s+from\"),1,0)\n| where is_bulk=1 OR (match(lower(application_name),\"microsoft sql server management studio\") AND _time < relative_time(now(),\"@d+18h\"))\n| stats count by session_server_principal_name, object_name, application_name\n| sort - count",
  "value": "Alerts on bulk or off-hours SQL changes to tables holding patient demographics or clinical facts so improper alteration of ePHI is detected and investigated.",
  "implementation": "(1) Enable SQL Server Audit to SIEM; (2) map ETL service accounts to allow list; (3) pair with change tickets from ServiceNow; (4) retain immutable copy to WORM storage if required.",
  "visualization": "Table (principal, object, count), Timeline (bulk ops).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk DB Connect",
      "url": "https://splunkbase.splunk.com/app/2686"
    },
    {
      "title": "Splunk Add-on for Microsoft SQL Server",
      "url": "https://splunkbase.splunk.com/app/2648"
    }
  ],
  "knownFalsePositives": "Administrative tasks, scheduled jobs or platform updates can match this pattern — correlate with change management, maintenance windows and user role before raising severity.",
  "equipment": [
    "db_connect",
    "mssql",
    "servicenow"
  ]
}
