{
  "$schema": "../../schemas/uc.schema.json",
  "id": "22.14.70",
  "title": "Cryptographic Key Management Events from Cloud KMS",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "compliance": [
    {
      "regulation": "NIST 800-53",
      "version": "Rev. 5",
      "clause": "SC-12",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Auto-migrated from cat-22 markdown on Phase 1.3 run. Requires SME review to confirm clause accuracy, mode (satisfies vs detects-violation-of), and assurance level (contributing/partial/full).",
      "provenance": "maintainer"
    }
  ],
  "dataSources": "`aws:cloudtrail` KMS events",
  "app": "Splunk Add-on for AWS (Splunkbase 1876), Splunk Add-on for GCP (TA)",
  "spl": "index=cloud sourcetype=aws:cloudtrail earliest=-30d eventSource=\"kms.amazonaws.com\" eventName IN (\"DisableKey\",\"ScheduleKeyDeletion\",\"DeleteImportedKeyMaterial\")\n| stats values(eventName) as actions, latest(_time) as last by userName, requestParameters",
  "value": "Audits KMS key disable/delete operations for SC-12 cryptographic key establishment and management.",
  "implementation": "(1) Parse `requestParameters` for key ARNs into fields; (2) alert outside maintenance CAB; (3) dual-control evidence from SNOW comments optional join; (4) map to SC-13.",
  "visualization": "Table (KMS changes), Timeline, Single value (destructive ops)",
  "cimModels": [
    "Change"
  ],
  "cimSpl": "| tstats summariesonly=t latest(All_Changes.status) as agg_value from datamodel=Change.All_Changes by All_Changes.user | sort - agg_value",
  "references": [
    {
      "title": "Splunk Add-on for AWS",
      "url": "https://splunkbase.splunk.com/app/1876"
    },
    {
      "title": "CIM: Change",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Change"
    }
  ],
  "knownFalsePositives": "Administrative tasks, scheduled jobs or platform updates can match this pattern — correlate with change management, maintenance windows and user role before raising severity.",
  "equipment": [
    "aws",
    "gcp",
    "servicenow"
  ]
}
