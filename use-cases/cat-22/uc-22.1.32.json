{
  "$schema": "../../schemas/uc.schema.json",
  "id": "22.1.32",
  "title": "GDPR DPIA Completion Tracking Against High-Risk Processing",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Security",
  "compliance": [
    {
      "regulation": "GDPR",
      "version": "2016/679",
      "clause": "Art.35(7)",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Auto-migrated from cat-22 markdown on Phase 1.3 run. Requires SME review to confirm clause accuracy, mode (satisfies vs detects-violation-of), and assurance level (contributing/partial/full).",
      "provenance": "maintainer"
    }
  ],
  "dataSources": "`gdpr_ropa_register.csv`, `gdpr_dpia_register.csv`, change records `snow:change_request`",
  "app": "Splunk Add-on for ServiceNow (Splunkbase 1928)",
  "spl": "| inputlookup gdpr_ropa_register.csv WHERE high_risk=\"true\"\n| lookup gdpr_dpia_register.csv processing_activity OUTPUT dpia_status, dpia_completed_date\n| eval dpi_gap=case(\n    dpia_status=\"completed\", \"OK\",\n    dpia_status=\"in_progress\", \"IN_PROGRESS\",\n    1=1, \"MISSING\")\n| join processing_activity type=left [\n    search index=itsm sourcetype=\"snow:change_request\" earliest=-90d\n    | where match(lower(short_description),\"(?i)go.live|launch|migrate\")\n    | stats latest(start_date) as last_major_change by cmdb_ci\n    | rename cmdb_ci as system_name\n]\n| where dpi_gap!=\"OK\"\n| table processing_activity, system_name, dpi_gap, dpia_status, last_major_change",
  "value": "Where processing is likely to result in a high risk, the controller shall carry out a DPIA prior to processing. This use case reconciles the ROPA, system go-live dates, and DPIA register to flag processing that started or changed materially without a completed DPIA.",
  "implementation": "(1) Keep `high_risk` flags on ROPA entries per Art. 35(3); (2) join DPIA register by processing activity; (3) correlate major changes with DPIA refresh policy; (4) alert DPO on `MISSING` before production changes; (5) align with UC-22.1.14 for consolidated DPIA coverage views.",
  "visualization": "Table (missing DPIAs), Single value (high-risk gaps), Pie chart (status distribution), Timeline (go-live vs DPIA dates).",
  "cimModels": [
    "Change"
  ],
  "cimSpl": "| tstats summariesonly=t latest(All_Changes.status) as agg_value from datamodel=Change.All_Changes by All_Changes.action, All_Changes.object_category, All_Changes.user | sort - agg_value",
  "references": [
    {
      "title": "Splunk Add-on for ServiceNow",
      "url": "https://splunkbase.splunk.com/app/1928"
    },
    {
      "title": "CIM: Change",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Change"
    }
  ],
  "knownFalsePositives": "Administrative tasks, scheduled jobs or platform updates can match this pattern — correlate with change management, maintenance windows and user role before raising severity.",
  "equipment": [
    "servicenow"
  ]
}
