{
  "$schema": "../../schemas/uc.schema.json",
  "id": "22.4.11",
  "title": "CCPA/CPRA — Sensitive PI — Racial or Ethnic Origin Attributes in Model Training Feature Stores",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Compliance"
  ],
  "splunkPillar": "Security",
  "compliance": [
    {
      "regulation": "CCPA/CPRA",
      "version": "CPRA (as amended)",
      "clause": "§1798.121",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Auto-migrated from cat-22 markdown on Phase 1.3 run, then normalised in Phase A (clause 'unspecified' → '§1798.121'). Requires SME review to confirm clause accuracy, mode (satisfies vs detects-violation-of), and assurance level (contributing/partial/full).",
      "provenance": "maintainer"
    }
  ],
  "dataSources": "`index=ml` `sourcetype=\"feast:feature_view\"` (view_name, feature_name, source_table, created_by, _time)",
  "app": "HTTP Event Collector from MLOps metadata service",
  "spl": "index=ml sourcetype=\"feast:feature_view\" earliest=-30d\n| where match(feature_name,\"(?i)race|ethnic|religion|union|orientation|immigration|citizen\")\n| stats dc(view_name) as views, values(feature_name) as features by source_table, created_by\n| sort - views",
  "value": "Use of sensitive categories for automated decisioning is tightly constrained. Auditing feature-store schemas and ETL logs for prohibited attribute names or proxy variables supports demonstrable limits of processing for sensitive PI.",
  "implementation": "(1) Expand keyword list with privacy counsel; (2) join `source_table` to approved sensitive-processing register; (3) block CI deploys on match in pre-prod mirror; (4) quarterly model governance review; (5) document lawful basis where processing is permitted.",
  "visualization": "Table (feature views), Bar chart (by owner), Single value (distinct views).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/"
    }
  ],
  "knownFalsePositives": "Administrative tasks, scheduled jobs or platform updates can match this pattern — correlate with change management, maintenance windows and user role before raising severity."
}
