{
  "$schema": "../../schemas/uc.schema.json",
  "id": "22.6.5",
  "title": "ISO 27001 Cryptographic Key and Certificate Lifecycle Monitoring",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Compliance",
    "Security"
  ],
  "splunkPillar": "Security",
  "compliance": [
    {
      "regulation": "ISO 27001",
      "version": "2022",
      "clause": "A.10.1.2",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Auto-migrated from cat-22 markdown on Phase 1.3 run. Requires SME review to confirm clause accuracy, mode (satisfies vs detects-violation-of), and assurance level (contributing/partial/full).",
      "provenance": "maintainer"
    }
  ],
  "dataSources": "`index=security` `sourcetype=\"cert:inventory\"` (cn, san, not_after, issuer, host); `index=secrets` `sourcetype=\"_json\"` `source=\"http:vault_audit\"` (path, operation, _time) for asymmetric key rotations",
  "app": "Splunk Add-on for OpenTelemetry Collector (Splunkbase 7125) or certificate scan TA, Splunk HTTP Event Collector (core platform)",
  "spl": "index=security sourcetype=\"cert:inventory\" earliest=-1d\n| eval exp_epoch=coalesce(\n    strptime(not_after,\"%Y-%m-%dT%H:%M:%SZ\"),\n    strptime(not_after,\"%Y-%m-%d %H:%M:%S\"))\n| eval days_to_exp=round((exp_epoch-now())/86400,1)\n| where isnotnull(days_to_exp) AND days_to_exp<45\n| stats values(host) as hosts, min(days_to_exp) as min_days by cn, issuer\n| sort min_days\n```\n```spl\nindex=secrets sourcetype=\"_json\" source=\"http:vault_audit\" operation=\"rotate\" earliest=-30d\n| stats count as rotations by path\n| sort - rotations",
  "value": "Key management requires defined lifecycles and protection of secret keys. Tracking upcoming TLS certificate expirations and vault key-rotation events demonstrates operational control over cryptography supporting confidentiality and integrity commitments.",
  "implementation": "(1) Ingest nightly cert inventory from ACM or Venafi with `not_after` in consistent UTC format; (2) forward HashiCorp Vault audit or cloud KMS rotation webhooks to `index=secrets` for signing keys; (3) alert at 30/14/7 days on TLS `min_days`; (4) document owners in lookup `cert_owner.csv`; (5) tie renewals to change tickets for A.12.1.",
  "visualization": "Table (expiring certs), Timeline (rotation events), Single value (count expiring <14 days).",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Add-on for OpenTelemetry Collector",
      "url": "https://splunkbase.splunk.com/app/7125"
    }
  ],
  "knownFalsePositives": "Administrative tasks, scheduled jobs or platform updates can match this pattern — correlate with change management, maintenance windows and user role before raising severity.",
  "mitreAttack": [
    "T1098",
    "T1562"
  ],
  "equipment": [
    "hashicorp",
    "opentelemetry"
  ],
  "equipmentModels": [
    "hashicorp_vault"
  ]
}
