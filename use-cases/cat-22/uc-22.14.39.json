{
  "$schema": "../../schemas/uc.schema.json",
  "id": "22.14.39",
  "title": "Software and Firmware Integrity — Unexpected Driver Loads",
  "criticality": "critical",
  "difficulty": "advanced",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "compliance": [
    {
      "regulation": "NIST 800-53",
      "version": "Rev. 5",
      "clause": "SI-7",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Auto-migrated from cat-22 markdown on Phase 1.3 run. Requires SME review to confirm clause accuracy, mode (satisfies vs detects-violation-of), and assurance level (contributing/partial/full).",
      "provenance": "maintainer"
    }
  ],
  "dataSources": "`XmlWinEventLog:Microsoft-Windows-Sysmon/Operational` EventCode 6",
  "app": "Splunk Add-on for Microsoft Sysmon",
  "spl": "index=endpoint sourcetype=XmlWinEventLog:Microsoft-Windows-Sysmon/Operational EventCode=6 earliest=-7d\n| stats count by ImageLoaded, host\n| eventstats median(count) as med global=t\n| where count < med*0.01 OR match(ImageLoaded,\"(?i)temp|users\\\\public\")",
  "value": "Flags rare Sysmon driver loads and kernel module events for SI-7 software and firmware integrity monitoring.",
  "implementation": "(1) Enrich with driver signer if captured; (2) allowlist known AV drivers via lookup; (3) alert on temp-path loads; (4) integrate with binary reputation service.",
  "visualization": "Table (rare drivers), Bar chart (by host), Scatter (prevalence vs first_seen)",
  "cimModels": [
    "Endpoint"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Endpoint.Processes by Processes.dest | sort - count",
  "references": [
    {
      "title": "CIM: Endpoint",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Endpoint"
    }
  ],
  "knownFalsePositives": "Administrative tasks, scheduled jobs or platform updates can match this pattern — correlate with change management, maintenance windows and user role before raising severity."
}
