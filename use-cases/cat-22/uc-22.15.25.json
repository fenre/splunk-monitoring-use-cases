{
  "$schema": "../../schemas/uc.schema.json",
  "id": "22.15.25",
  "title": "Clock Skew on OT Hosts vs NTP Stratum",
  "criticality": "high",
  "difficulty": "intermediate",
  "monitoringType": [
    "Security",
    "Compliance"
  ],
  "splunkPillar": "Security",
  "compliance": [
    {
      "regulation": "IEC 62443",
      "version": "2013-ongoing",
      "clause": "SR 2.11",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Auto-migrated from cat-22 markdown on Phase 1.3 run. Requires SME review to confirm clause accuracy, mode (satisfies vs detects-violation-of), and assurance level (contributing/partial/full).",
      "provenance": "maintainer"
    }
  ],
  "dataSources": "`index=ot_infra` with `_time` and `orig_time` if extracted",
  "app": "Universal Forwarder time debug or vendor syslog",
  "spl": "index=ot_infra earliest=-24h\n| eval skew=abs(_indextime-_time)\n| where skew>120\n| stats max(skew) as max_skew, perc95(skew) as p95_skew by host, sourcetype\n| sort - p95_skew",
  "value": "Surfaces OT servers and RTUs with large `_indextime` vs `_time` deltas undermining trustworthy audit timestamps.",
  "implementation": "(1) Enforce PTP/NTP on all IEDs; (2) fix dual-homed syslog paths; (3) alert >120s; (4) document stratum hierarchy per site.",
  "visualization": "Table, Single value (worst skew), Bar by site.",
  "cimModels": [
    "N/A"
  ],
  "references": [
    {
      "title": "Splunk Lantern — use case library",
      "url": "https://lantern.splunk.com/"
    }
  ],
  "knownFalsePositives": "Administrative tasks, scheduled jobs or platform updates can match this pattern — correlate with change management, maintenance windows and user role before raising severity.",
  "equipment": [
    "syslog"
  ]
}
