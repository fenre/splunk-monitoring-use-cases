{
  "$schema": "../../schemas/uc.schema.json",
  "id": "22.11.55",
  "title": "Session Timeout Enforcement on Payment Web Admin Consoles",
  "criticality": "medium",
  "difficulty": "intermediate",
  "monitoringType": [
    "Compliance",
    "Security"
  ],
  "splunkPillar": "Security",
  "compliance": [
    {
      "regulation": "PCI DSS",
      "version": "v4.0",
      "clause": "8.2.8",
      "mode": "satisfies",
      "assurance": "contributing",
      "assurance_rationale": "Auto-migrated from cat-22 markdown on Phase 1.3 run, then normalised in Phase A (clause 'PCI DSS Req 8.2.8' → '8.2.8'). Requires SME review to confirm clause accuracy, mode (satisfies vs detects-violation-of), and assurance level (contributing/partial/full).",
      "provenance": "maintainer"
    }
  ],
  "dataSources": "`index=web` `sourcetype=\"access_combined\"` for `/admin` URIs with `session_id`",
  "app": "Splunk Add-on for Stream (1809), `index=web` `access_combined`",
  "spl": "index=web sourcetype=\"access_combined\" earliest=-24h\n| where match(uri,\"(?i)/admin|/portal/manage\")\n| transaction session_id maxpause=30m maxspan=8h\n| eval idle_mins=round(duration/60,2)\n| where idle_mins>15\n| stats max(idle_mins) as max_idle by session_id, clientip\n| sort - max_idle",
  "value": "Measures idle session duration before logout on administrative payment portals using HTTP session cookies or app logs so inactive sessions re-authenticate within policy limits.",
  "implementation": "(1) Align `maxpause` with configured server session timeout. (2) Mask `session_id` in dashboards. (3) Tune for long-running report downloads. (4) Pair with WAF session fixation checks.",
  "visualization": "Histogram (idle_mins), table (longest sessions), single value (sessions exceeding policy).",
  "cimModels": [
    "Web"
  ],
  "cimSpl": "| tstats summariesonly=t count from datamodel=Web.Web by Web.status, Web.http_method, Web.dest | sort - count",
  "references": [
    {
      "title": "Splunk Add-on for Stream",
      "url": "https://splunkbase.splunk.com/app/1809"
    },
    {
      "title": "CIM: Web",
      "url": "https://docs.splunk.com/Documentation/CIM/latest/User/Web"
    }
  ],
  "knownFalsePositives": "Administrative tasks, scheduled jobs or platform updates can match this pattern — correlate with change management, maintenance windows and user role before raising severity."
}
